---
phase: 01-engine-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/engine/deepseek.ts
  - src/lib/engine/apollo-core.ts
  - src/lib/engine/version.ts
  - src/lib/engine/__tests__/version.test.ts
  - src/lib/engine/__tests__/apollo-cite-resolution.test.ts
autonomous: false
requirements: [ENG-02]
must_haves:
  truths:
    - "Apollo emits only §-cites that resolve to a section present in the lean runtime KNOWLEDGE_CORE — no dangling §2.6/§7/§8/§9 cite reaches the board (D-05 in-engine grounding fix; resolves the D-07 audit-then-decide on the dangling §2.6 + dropped §7/§8)"
    - "Davide has seen the real runtime §-cites (apollo-core-smoke output) and chosen restore-vs-remap-vs-redesign from evidence (D-01 audit-inline / evidence-before-edits; D-02 adaptive testing rig)"
  artifacts:
    - path: "src/lib/engine/apollo-core.ts"
      provides: "Lean runtime KNOWLEDGE_CORE + APOLLO_INSTRUCTION — the §-grounding source the cited assessment must resolve against"
    - path: "src/lib/engine/deepseek.ts"
      provides: "Apollo reasoner call + Zod post-parse backstop where a §-cite resolution guard fits"
  key_links:
    - from: "src/lib/engine/deepseek.ts"
      to: "src/lib/engine/apollo-core.ts"
      via: "APOLLO_SYSTEM_PROMPT = KNOWLEDGE_CORE (lean) + APOLLO_INSTRUCTION; emitted §-cites must point into the lean core"
      pattern: "APOLLO_SYSTEM_PROMPT|KNOWLEDGE_CORE"
---

<objective>
ENG-02 — make the Apollo reasoner's §-citation grounding HONEST in-engine: every §-cite Apollo emits must resolve to a section actually present in the lean runtime KNOWLEDGE_CORE. This is the highest-value, highest-ambiguity thread of the phase and the engine moat.

This plan is AUDIT-FIRST and CO-REVIEW-GATED. It does NOT pre-pick the fix. It runs the live `apollo-core-smoke.ts` rig to surface which §-cites Apollo actually emits at runtime (answers D-07), presents the evidence + the three options (restore / remap / redesign — D-06 puts fundamental redesign on the table), STOPS for Davide's decision, and only then applies the agreed engine-side fix.

Per the research reframe: the board has NO §-cite bug (InsightHeroFrame renders cites as opaque strings); the CHAT fake legend is Phase 4 scope. This plan touches engine output honesty only — NOT board or chat rendering.

Purpose: Apollo is the moat; an unresolvable §-cite is a confident-looking lie. Resolve it at the source.
Output: a captured Davide decision + the agreed engine-side grounding fix (or audit-only confirmation), with version.ts bumped if output shape changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-engine-pipeline/01-CONTEXT.md
@.planning/phases/01-engine-pipeline/01-RESEARCH.md
@.planning/corpus/KNOWLEDGE-CORE.md

<interfaces>
<!-- The §-scheme facts the executor needs (from RESEARCH.md §ENG-02). Do NOT re-derive — these are confirmed. -->
- Scheme 1 (real corpus, KNOWLEDGE-CORE.md): §1 Persona · §2.0/§2.0a/§2.1 Hooks/§2.2 Retention/§2.3 Share/§2.4 CTA/§2.5 Substance/§2.6 Behavioral (EMPTY, reserved) · §3 Anti-Patterns · §4 Scoring Rubric (§4.1 Platform) · §5 Decode · §6 Rewrite · §7 Audience · §8 Sources.
- Scheme 2 (LEAN RUNTIME core, apollo-core.ts:8-19): T3.1 dropped §2.6, §7, §8 + header meta. Runtime prefix = §1–§6 only (craft layer byte-unchanged).
- Scheme 3 (what Apollo is instructed to emit, deepseek.ts:319-353 + APOLLO_INSTRUCTION apollo-core.ts:240-246): free-text §-cites into §2.x and §4 — present in lean core. The dangling risk is §2.6 (empty+dropped), §7/§8 (dropped), or a hallucinated §9.
- Scheme 4 (board, InsightHeroFrame.tsx): renders cites as OPAQUE strings; only §-aware code is line 354 `rw.lever_fixed.includes('§2.2')`. NO board fix needed.
- Scheme 5 (chat seed-context.ts:96-106): flat fake §1–§10 legend — PHASE 4, do NOT touch.
- Existing Zod post-parse backstop pattern lives at deepseek.ts:128-190 (where a remap/strip guard fits).
- Cache invariant (version.ts:63-65): any output-shape change MUST bump ENGINE_VERSION + version.test.ts:9-10 (hardcodes "3.13.0").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AUDIT — run apollo-core-smoke and map runtime §-cites vs lean core</name>
  <read_first>
    - scripts/apollo-core-smoke.ts (the rig command + what it prints)
    - src/lib/engine/apollo-core.ts (the lean runtime KNOWLEDGE_CORE + APOLLO_INSTRUCTION — the whitelist of sections actually shipped)
    - src/lib/engine/deepseek.ts:244-356 (buildDeepSeekUserMessage output contract — where §-cites are requested)
    - .planning/corpus/KNOWLEDGE-CORE.md (full taxonomy — what §2.6/§7/§8 contain and why they were dropped, T3.1)
    - .planning/phases/01-engine-pipeline/01-RESEARCH.md §ENG-02 (the 5-scheme map + 3 options)
  </read_first>
  <action>
    Run the live rig: `npx tsx scripts/apollo-core-smoke.ts "$HOME/Downloads/TikTok Video Downloader.mp4"` (copy the file into ./.playwright-mcp/ first only if the rig needs a web upload — for the script path it reads the file directly). Capture the §-cited assessment Apollo actually emits. Extract every literal §-token from the output (dimensions' lever/evidence/ceiling_capper + rewrites' lever_fixed). Build a resolution table: each emitted §-token → present in lean core (apollo-core.ts §1–§6) YES/NO. Flag any §2.6/§7/§8/§9+ dangling cite. Cross-reference the dropped-section rationale from version.ts T3.1 (§2.6 empty, §8 bookkeeping). Do NOT edit any file in this task — this is evidence-gathering only (D-01 audit-inline). Present the resolution table as the deliverable.
  </action>
  <verify>
    <automated>npx tsx scripts/apollo-core-smoke.ts "$HOME/Downloads/TikTok Video Downloader.mp4" 2>&1 | tee /tmp/apollo-cite-audit.txt; grep -oE '§[0-9]+(\.[0-9]+[a-z]?)?' /tmp/apollo-cite-audit.txt | sort -u</automated>
  </verify>
  <acceptance_criteria>
    - apollo-core-smoke ran on a real video and produced an assessment (non-empty output captured to /tmp/apollo-cite-audit.txt)
    - A resolution table exists mapping every emitted §-token → present-in-lean-core (YES/NO), with any dangling cite explicitly flagged
    - The table is presented to Davide as the evidence basis for the Task 2 decision (answers D-07: what cites does Apollo actually emit)
  </acceptance_criteria>
  <done>Runtime §-cite resolution table produced from real apollo-core-smoke output; danglers (if any) identified with the §2.6/§7/§8 drop rationale noted.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: CO-REVIEW CHECKPOINT — restore vs remap vs redesign (D-06/D-07/D-12)</name>
  <action>STOP and present Task 1's runtime §-cite resolution table to Davide alongside the three options below. Do NOT auto-resolve. Capture his restore/remap/redesign decision before any edit (D-06/D-07/D-12).</action>
  <read_first>
    - /tmp/apollo-cite-audit.txt (Task 1 output)
    - .planning/phases/01-engine-pipeline/01-RESEARCH.md §ENG-02 options table
  </read_first>
  <decision>How to make Apollo's §-grounding honest in-engine, given the runtime evidence from Task 1.</decision>
  <context>The board has no §-cite bug (opaque-string render). The engine must guarantee emitted §-cites resolve. D-05 requires an in-engine fix; D-06 explicitly puts a fundamental redesign on the table — do NOT default to the minimal patch. Present Task 1's resolution table alongside these three options.</context>
  <options>
    <option id="restore">
      <name>Restore</name>
      <pros>Every corpus cite resolves; simplest conceptually; no new validation code.</pros>
      <cons>Re-adds §2.6 (empty) + §7/§8 (~2k chars bookkeeping/IP) to the cached prefix → prefix-cache + latency cost for cites the rubric never needs. Requires version.ts bump (system-prefix bytes change).</cons>
    </option>
    <option id="remap">
      <name>Remap (runtime resolution guard)</name>
      <pros>Honest output guaranteed; small code add at the existing Zod post-parse backstop (deepseek.ts:128-190); validates each emitted §-cite against a whitelist of sections present (§1–§6, §2.0a, §4.1), strips/flags + logs danglers. Keeps the lean prefix. This is the honest-output floor satisfying D-05 cheaply.</pros>
      <cons>Doesn't restructure the taxonomy; a stripped cite leaves prose referencing a now-absent §. version.ts bump only if the strip changes output shape.</cons>
    </option>
    <option id="redesign">
      <name>Redesign (structured lever taxonomy — D-06)</name>
      <pros>Highest value: replaces free-text §-cites with an enumerated lever-ID taxonomy the Zod schema machine-validates; sets up Phase 4 chat reconciliation cleanly with one shared legend. The §2.x lever names already exist as free text in ApolloDimensionSchema.lever.</pros>
      <cons>Highest effort + content work; changes the Apollo output contract → version.ts bump + schema change + prompt rewrite (heavy co-review, ripples to ENG-06 plan 02 and Phase 4).</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Davide's chosen option (restore / remap / redesign) is captured in the plan's SUMMARY and the commit message
    - If redesign chosen: the agreed lever-ID enum scope is recorded (which §-levers become enum members), and the ripple to plan 02 (ENG-06) is noted
    - The agreed direction is recorded explicitly enough that Task 3 has no remaining ambiguity
  </acceptance_criteria>
  <resume-signal>Select: restore, remap, or redesign (with any scoping notes)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: FIX (conditional on Task 2) — apply the agreed grounding fix + bump version</name>
  <read_first>
    - src/lib/engine/deepseek.ts:128-190 (Zod post-parse backstop — where a remap guard inserts) and :244-356 (prompt contract — where a redesign rewrites)
    - src/lib/engine/apollo-core.ts:8-19, :240-246 (lean core + APOLLO_INSTRUCTION — where a restore re-adds sections or a redesign re-words the cite instruction)
    - src/lib/engine/version.ts:63-67 (cache invariant + current "3.13.0")
    - src/lib/engine/__tests__/version.test.ts:9-10 (hardcoded version assertion to update)
    - src/lib/engine/__tests__/deepseek.test.ts (existing Apollo contract tests — extend, don't break)
  </read_first>
  <action>
    Apply ONLY the option Davide selected in Task 2.
    - If RESTORE: re-add the §2.6/§7/§8 section bodies to the lean KNOWLEDGE_CORE in apollo-core.ts so emitted cites resolve; note the prefix-cache/latency cost for the ENG-03 cleanup pass (plan 04).
    - If REMAP: add a §-cite resolution guard at the deepseek.ts:128-190 post-parse backstop — validate each emitted §-token (lever/evidence/ceiling_capper/lever_fixed) against a whitelist constant of sections present in the lean core; strip or annotate danglers; log a `cite_drift` warning when one is stripped (fits the existing Zod-backstop + warnings[] pattern). Create the new test file src/lib/engine/__tests__/apollo-cite-resolution.test.ts asserting a fabricated §9 cite is stripped/flagged and a valid §2.2 passes (Wave 0 gap from VALIDATION.md).
    - If REDESIGN: introduce the enumerated lever-ID taxonomy agreed in Task 2 — add the enum to the Apollo schema, rewrite the APOLLO_INSTRUCTION + buildDeepSeekUserMessage cite contract to emit lever IDs, validate via Zod. Create apollo-cite-resolution.test.ts asserting only enum members validate. Record the contract change for plan 02 (ENG-06) co-review.
    In ALL fix branches that change output shape OR the cached system-prefix bytes (restore, remap-with-strip, redesign): bump ENGINE_VERSION in version.ts (3.13.0 → 3.14.0) with a change-history comment, and update the hardcoded assertion in version.test.ts:10 to "3.14.0". (Audit-only/no-shape-change branch: no bump — record why in the SUMMARY.) Do NOT add fenced code; read the files for current structure.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/deepseek.test.ts src/lib/engine/__tests__/version.test.ts src/lib/engine/__tests__/apollo-cite-resolution.test.ts 2>&1 | grep -v '^#' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The applied change matches EXACTLY the option captured in Task 2 (no second option silently implemented)
    - No §-cite that fails to resolve against the lean core can reach the dimension/rewrite output (asserted by apollo-cite-resolution.test.ts for remap/redesign; or by restore making all corpus sections present)
    - If output shape OR cached system-prefix bytes changed: ENGINE_VERSION bumped to 3.14.0 AND version.test.ts assertion updated to "3.14.0" (both, or neither)
    - `npx vitest run src/lib/engine/__tests__/deepseek.test.ts src/lib/engine/__tests__/version.test.ts` is green
    - The Apify-token / signed-URL flow and the Zod safeParse boundary in deepseek.ts are unchanged (no V5 regression)
  </acceptance_criteria>
  <done>Agreed grounding fix shipped; no dangling §-cite can reach the board; version + version test bumped iff output/prefix changed; engine suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apollo LLM → engine output | Untrusted qwen3.6-plus JSON (free-text §-cites) crosses into PredictionResult and onward to the board |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | deepseek.ts Zod safeParse + post-parse clamps (:128-190) | mitigate (PRESERVE) | Any remap/redesign guard inserts AT this backstop — must not bypass or weaken the existing safeParse; add the cite-resolution check after parse, before assembly |
| T-01-02 | Information Disclosure (integrity) | emitted §-cite resolving to nothing = confident-looking false grounding | mitigate | This plan's core fix: strip/flag/restore so no unresolvable §-cite reaches output |
| T-01-03 | Tampering | Apify token → DashScope (pipeline.ts:400-496 signed-URL flow) | accept (out of plan scope, do not touch) | Plan edits deepseek.ts/apollo-core.ts only; read-path untouched — flag if any edit nears the rehost flow |
</threat_model>

<verification>
- `npx vitest run src/lib/engine/__tests__/deepseek.test.ts version.test.ts apollo-cite-resolution.test.ts` green.
- One live `apollo-core-smoke.ts` run captured (Task 1) showing the §-cite set.
- Per D-02, after this fix batch with plan 02's, offer Davide the testing menu (he UATs / Claude Playwright /analyze on the test video / scripts) — do not pre-commit a method.
</verification>

<success_criteria>
- ROADMAP criterion 2 advanced: "Apollo reasoner stays grounded in the knowledge core (§ citations resolve to the real corpus, not flat fake legend labels)" — engine side now guarantees resolution.
- Davide's restore/remap/redesign decision captured (D-06/D-07/D-12).
- version.ts cache invariant respected.
</success_criteria>

<output>
Create `.planning/phases/01-engine-pipeline/01-01-SUMMARY.md` when done. Record: the runtime §-cite resolution table, Davide's chosen option + rationale, whether version was bumped, and any ripple flagged to plan 02 (if redesign).
</output>
