---
phase: 05-wire-surface
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/lib/engine/types.ts
  - src/lib/engine/aggregator.ts
  - src/lib/engine/__tests__/aggregator.test.ts
autonomous: true
requirements: [R11, R5, R6]
must_haves:
  truths:
    - "predicted_engagement is a grounded RANGE (lo–hi) anchored to creator follower_count × Apollo+fold quality read, never a false-precise point (R11, D-05, D-06)"
    - "The R11 range is computed on PredictionResult and rendered from the LIVE result (analyzeMutation.data) only; it is intentionally NOT persisted this phase — there is no analysis_results.predicted_engagement column and none is added (LIVE-RESULTS-PANEL-ONLY scope)"
    - "Permalink/DB persistence of the R11 range is OUT OF SCOPE this phase — DEFERRED to the per-creator-avg_views median follow-up (D-06). No phantom persist path, no buildInsertRow column, no threading test for predicted_engagement is added"
    - "Two creators of materially different follower tiers get materially different ranges for the same video (R11 verify)"
    - "The estimate is computed in the aggregator from creatorContext already present on PipelineResult — no new LLM call, no R6 regression (R11, R7)"
    - "When follower_count is absent (creator not found), predicted_engagement is null — no fabricated number (R9/R11 honesty)"
    - "No latency-reduction / <45s work is pursued — the engine is wire+surface only; R6 (74s baseline) is preserved by construction (D-04 guardrail honored, not built against)"
  artifacts:
    - path: "src/lib/engine/types.ts"
      provides: "EngagementRange type (lo/hi/confidence/basis) replacing the point-shape for the grounded estimate"
      contains: "EngagementRange"
    - path: "src/lib/engine/aggregator.ts"
      provides: "computeEngagementRange(creatorContext, overall_score) wired at the predicted_engagement assembly site (was hard-null at :977)"
      contains: "computeEngagementRange"
  key_links:
    - from: "src/lib/engine/aggregator.ts"
      to: "pipelineResult.creatorContext.follower_count"
      via: "computeEngagementRange anchor"
      pattern: "follower_count"
    - from: "src/lib/engine/aggregator.ts"
      to: "predicted_engagement"
      via: "assembly site replaces the hard null"
      pattern: "predicted_engagement: computeEngagementRange|predicted_engagement:"
---

<objective>
Build the R11 grounded engagement estimate: replace the hard-null'd `predicted_engagement` (aggregator.ts:977) with a deterministic range anchored to the creator's real `follower_count` (live on `PipelineResult.creatorContext`) multiplied by the Apollo+fold quality read (`overall_score`). The research gate resolved YES — the inputs reach the engine today — so this is a genuine new computation, not a wiring fix.

Purpose: R11 (grounded engagement estimate as a range + confidence, history-relative, never a point). Honest by construction — the range widens when quality/confidence is low (virality is fat-tailed) and is null when no creator baseline exists.
Output: an `EngagementRange` type + a pure `computeEngagementRange(creatorContext, overall_score)` derivation wired at the existing assembly site, with tier-sensitivity + range-not-point tests.

LOCKED (planning context): R11 anchor = `follower_count` × quality read as a history-relative RANGE. The exact per-creator "vs your ~15k median" framing (needs `avg_views` history, null today) is DEFERRED to a follow-up — do NOT build a per-creator avg_views query this plan. `follower_count` alone satisfies the R11 verify (different tiers → different ranges).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-wire-surface/05-CONTEXT.md
@.planning/phases/05-wire-surface/05-RESEARCH.md
@.planning/phases/05-wire-surface/05-PATTERNS.md

@.planning/phases/05-wire-surface/05-01-SUMMARY.md

<interfaces>
<!-- R11 inputs + assembly site. Extracted from codebase. -->

src/lib/engine/creator.ts:14-21 — CreatorContext (the R11 anchor source):
  follower_count: number | null;       // real when creator profile exists
  avg_views: number | null;            // per-creator NULL today (DEFERRED framing)
  engagement_rate: number | null;
  platform_averages: { avg_views: number; avg_engagement_rate: number; ... }  // real DB aggregate / 50000 fallback

src/lib/engine/pipeline.ts:828 — creatorContext is ON the PipelineResult returned to aggregateScores:
  return { payload, geminiResult, creatorContext, deepseekResult, ... }
  // → aggregator reads pipelineResult.creatorContext — NO new threading needed

src/lib/engine/aggregator.ts:726, 747 — quality read source (overall_score already computed):
  const apollo_score = deepseek?.composite_score ?? 0;   // now the D-01 sum (Plan 01)
  const overall_score = raw_overall_score;               // 0-100 quality read

src/lib/engine/aggregator.ts:968-977 — assembly site (predicted_engagement is hard null TODAY):
  predicted_engagement: null, // Plan 02 D1.1: sine-jitter deleted; null until Plan 05 regrounding

src/lib/engine/types.ts:239-265 — CURRENT shape is a POINT (must add a RANGE type):
  interface PredictedEngagement { likes; comments; shares; saves; views: number }  // point — R11 needs a range
  predicted_engagement: PredictedEngagement | null;   // on PredictionResult:265
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EngagementRange type + tier-sensitivity Wave 0 test</name>
  <read_first>
    - src/lib/engine/types.ts:239-265 (current point-shape PredictedEngagement + the predicted_engagement field on PredictionResult)
    - src/lib/engine/creator.ts:14-21 (CreatorContext anchor fields)
    - src/lib/engine/__tests__/aggregator.test.ts (extend — existing aggregator assembly tests)
    - .planning/phases/05-wire-surface/05-CONTEXT.md (D-05, D-06 — range form, history-relative, never a point)
  </read_first>
  <behavior>
    - Test: `computeEngagementRange` with a high follower_count (e.g. 500k) and a low follower_count (e.g. 5k) on the SAME overall_score returns materially different ranges (R11 verify — tier sensitivity).
    - Test: the output is a RANGE — `lo < hi` strictly, never a single point (D-06).
    - Test: a higher overall_score (quality read) shifts the range upward for the same follower tier (quality multiplies the anchor).
    - Test: a lower confidence / lower quality widens the range (fat-tailed honesty — band reflects residual uncertainty).
    - Test (null honesty): `creatorContext.follower_count == null` → `computeEngagementRange` returns null (no fabricated number, R9).
  </behavior>
  <action>
    Add an `EngagementRange` type to types.ts (e.g. `{ lo: number; hi: number; confidence: number; basis: string }` — `basis` is a short human label like "follower-tier × quality read" for the UI). Decide its relationship to the existing point-shape `PredictedEngagement`: introduce `EngagementRange` as the grounded R11 shape and make `predicted_engagement` reference it (the point-shape was fabricated and is being replaced — keep the field name `predicted_engagement` on PredictionResult:265 and change its type to the range; this is a LIVE in-memory result field only — do NOT add a DB column or persist path for it this phase). Add the five RED test cases above to the EXISTING `aggregator.test.ts` referencing the not-yet-written `computeEngagementRange`. Do NOT use watch mode.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/aggregator.test.ts 2>&1 | grep -iE "engagement|range|tier" | head; grep -n "EngagementRange" src/lib/engine/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "EngagementRange" src/lib/engine/types.ts` returns the new type.
    - The five test cases exist and FAIL against the unbuilt `computeEngagementRange` (RED).
    - `predicted_engagement` field type on PredictionResult references the range shape (nullable preserved).
  </acceptance_criteria>
  <done>EngagementRange type added; five RED R11 tests (tier sensitivity, range-not-point, quality shift, width, null) in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: computeEngagementRange derivation + wire at the aggregator assembly site</name>
  <read_first>
    - src/lib/engine/aggregator.ts:695-747 (behavioral + apollo blend → overall_score, the quality read)
    - src/lib/engine/aggregator.ts:968-977 (apollo_reasoning + predicted_engagement assembly site — the hard null to replace)
    - src/lib/engine/creator.ts:113-148, 216-240 (platform_averages real vs follower_count, avg_views null path)
    - .planning/phases/05-wire-surface/05-PATTERNS.md (§ "aggregator.ts — R11 estimate" + § "Post-parse clamp") — NOTE: the PATTERNS "rides the existing SSE upsert" claim for predicted_engagement is INCORRECT (buildInsertRow route.ts:570-661 has no predicted_engagement column); R11 is LIVE-RESULTS-ONLY this phase (no persist)
    - .planning/phases/05-wire-surface/05-RESEARCH.md (Q1 — follower_count real, avg_views null; A2 — follower_count suffices for the verify)
  </read_first>
  <action>
    Implement a pure `computeEngagementRange(creatorContext: CreatorContext, overall_score: number): EngagementRange | null`:
    - Anchor = `creatorContext.follower_count`. If null → return null (honesty; the strip task deletes the dead UI slot). Optionally fall back to `platform_averages.avg_views` ONLY if you can do so honestly as a tier proxy — but the locked default is follower_count-anchored; do NOT introduce per-creator avg_views.
    - Quality multiplier = function of `overall_score` (0-100) → a reach factor (higher quality → higher expected reach off the follower base). Keep it a transparent, deterministic formula (no jitter, no sine — that was the deleted fabrication, R9).
    - Range width = function of confidence / quality — wider when uncertain (fat-tailed). Return `{ lo, hi, confidence, basis }` with `lo < hi`, rounded, clamped to ≥ 0. Apply the post-parse clamp idiom (no negative reach).
    - Wire it at aggregator.ts:977: replace `predicted_engagement: null` with `predicted_engagement: computeEngagementRange(pipelineResult.creatorContext, overall_score) ?? null`. `creatorContext` is already on `pipelineResult` (pipeline.ts:828) — read it directly; do NOT add a new option or threading. The range lives on the in-memory `PredictionResult.predicted_engagement` and is consumed by the live results-panel (analyzeMutation.data). DO NOT add a DB column, a buildInsertRow entry, or any route.ts persist — `analysis_results` has no predicted_engagement column and none is added this phase (LIVE-RESULTS-ONLY; permalink persistence DEFERRED, D-06). On permalink reload the range is correctly absent (no fabricated number).
    Keep the function pure + under the 500-line file rule (extract to a small helper near the blend if aggregator.ts is near the cap).
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/aggregator.test.ts 2>&1 | tail -20; grep -n "computeEngagementRange(pipelineResult.creatorContext" src/lib/engine/aggregator.ts</automated>
  </verify>
  <acceptance_criteria>
    - All five Task 1 R11 tests pass GREEN (tier sensitivity, range-not-point lo<hi, quality shift, width, null→null).
    - `grep -n "predicted_engagement: computeEngagementRange" src/lib/engine/aggregator.ts` (or equivalent wiring) confirms the hard null is replaced.
    - The range is asserted to come from the returned `PredictionResult` object (live result), NOT from any DB read — the test reads `result.predicted_engagement` off the aggregateScores return value.
    - `grep -c "predicted_engagement" src/app/api/analyze/route.ts` is unchanged from baseline (no new persist column / no buildInsertRow entry added).
    - No new LLM call, no new option on AggregateScoresOptions, no route.ts persist change (R6/R7 preserved).
    - `npx tsc --noEmit` zero new errors.
  </acceptance_criteria>
  <done>predicted_engagement is a grounded, deterministic, tier-sensitive range computed from creator baseline × quality read; null when no baseline; lives on the live PredictionResult and is NOT persisted this phase (permalink persistence deferred, D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| creator_context → R11 compute | follower_count / platform data originates from scraped creator profiles (untrusted upstream) feeding the estimate |
| engine → live results-panel | predicted_engagement range lives on the in-memory PredictionResult; rendered from live analyzeMutation.data only (not persisted this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | `computeEngagementRange` numeric output | mitigate | clamp lo/hi ≥ 0 (post-parse clamp idiom); deterministic formula — no jitter; null when follower_count absent (no fabrication) |
| T-05-05 | Tampering | prompt injection via creator_context feeding R11 | mitigate | R11 reads only the NUMERIC `follower_count` from CreatorContext (not free-text pain_points/reference_creators); the existing `<<<USER_CONTENT>>>` delimiter + sentinel strip in creator.ts stays intact — R11 must NOT un-wrap or re-inject creator free-text |
| T-05-06 | Info disclosure | predicted_engagement (live-only) | accept | NOT persisted this phase — the range exists only on the live in-memory PredictionResult; no DB write path is added, so there is no cross-user persistence surface to guard |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero external installs in P5 (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx vitest run src/lib/engine/__tests__/aggregator.test.ts` — all R11 cases GREEN.
- `npx tsc --noEmit` — zero new errors.
- `npm test` (full suite) before wave merge.
- Manual/live (phase gate): a real run for a known-follower creator RENDERS a non-null range on the live results-panel; a no-creator run renders no range. On permalink reload the range is correctly absent (live-only, persistence deferred). R6 latency unchanged (no new call).

**R12 note (exclusion, not a gap):** R12 "one brain across modes" is an explicit P5 BUILD-EXCLUSION — Remix decode/adapt were re-grounded on the shared Apollo core in P3 (03-03). This phase does NOT touch Remix; R12 is verify-untouched only (no regression), with no P5 build task.
</verification>

<success_criteria>
- predicted_engagement is a grounded range (lo<hi) anchored to follower_count × quality read (R11, D-05, D-06).
- Different follower tiers → materially different ranges for the same video (R11 verify).
- Null when no creator baseline (honesty, R9). No new LLM call / no R6 regression (R7/R6).
</success_criteria>

<output>
Create `.planning/phases/05-wire-surface/05-02-SUMMARY.md` when done.
</output>
