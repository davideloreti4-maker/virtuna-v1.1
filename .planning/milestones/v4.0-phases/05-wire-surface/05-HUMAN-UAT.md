---
status: complete
phase: 05-wire-surface
source: [05-VERIFICATION.md]
started: 2026-06-06T18:33:56Z
updated: 2026-06-06T19:48:00Z
---

## Current Test

ALL 5 PASS (2026-06-06). #4 R11 positive path closed end-to-end (live render + persist + permalink reload, analysis H4EwvOGMV0iu) after surfacing the range on the board — the prior PARTIAL's true root cause was #R11-no-live-surface (range computed but never rendered), not just handle-threading. All session findings FIXED: #R11-handle-threading (L1), #R11-follower-scrape (L2 + 0-guard), #R11-no-live-surface (surface+persist), #IN-02 (UI + engine halves), omni-flash drift (emotion_arc via PR #15 + weakest_modality 2nd vector). See Gaps. Engine 3.8.0; clean run 62.4s.

## Tests

### 1. R6 — Full E2E latency ≤90s on a live run
expected: `npx tsx scripts/measure-pipeline.ts <video-url>` completes ≤90s total; omni first paint ~17s; fold ∥ Apollo parallel (no serialization regression from the surface work).
result: PASS (2026-06-06) — 2 runs same video: 79.3s and 70.7s, both ≤90s. fold ∥ apollo confirmed parallel (fold 52.4s ∥ deepseek/apollo 55.1s, both start @23.6s). NOTE: run 1's 79.3s included an omni-flash Zod retry (~10s — invalid `emotion_arc` label outside low/mid/high enum, attempt0→attempt1); clean run = 70.7s, matching the 74s baseline. Omni-flash schema-drift is a separate robustness item (see Gaps).

### 2. Live same-video-twice determinism gate (R8 / D-01)
expected: Run `/analyze` (or measure-pipeline) on the SAME video twice; both `overall_score` values are byte-identical OR land in the same D-02 band. Record both composites. (Arithmetic determinism is unit-proven; this confirms the live cure given thinking-mode residual + the untouched behavioral half.)
result: PASS via band clause (2026-06-06) — NOT byte-identical: overall 78 (behavioral 74) vs 76 (behavioral 71), Δ2. Both → bandLabel "High potential" (≥70); confidenceRange@0.75 = [72,84] vs [70,82] (overlapping); Δ2 < band half-width 6. Derived apollo composite ~82.7 vs ~81.8 (Δ~1 — rubric-sum stabilized as designed). Residual swing lives in the UNTOUCHED fold/behavioral half (Δ3, weight 0.533), confirming the b593c124 audit: the D-02 band is the cure, not byte-identity. Acceptable per the OR clause.

### 3. Real-run DB persist of `variants.apollo.dimensions[].score` (D-01 threading)
expected: Run a real `/analyze`, query the `analysis_results` row, confirm the 6 dimension `score` values persist non-null; permalink reload renders the insight-hero from `variants.apollo`.
result: PASS (2026-06-06, live UI run via Playwright + throwaway user; analysis id `1dtSt9diWtgS`, video = @areyoukiddingtv 120s clip). DB row: `engine_version=3.8.0`, `latency_ms=78023` (78s), `overall_score=74`, confidence 0.75 HIGH. **All 6 `variants.apollo.dimensions[].score` non-null at the fixed band anchors** — hook 85, retention 85, clarity 85, share_pull 50, substance 50, credibility 85 (strong→85/mid→50, zero weak). `apollo.composite_score=82`; rubric-sum check exact: 85·0.80 + (85+85+50+50+85)·0.04 = 68 + 14.2 = 82.2 → 82. `rewrites`=3. **This is the CR-01 fix proven live** — had the prompt blueprint still omitted `score`, Zod would have failed → null → no apollo; instead all 6 emitted band-anchored scores. Permalink reload (`/analyze/1dtSt9diWtgS`) re-rendered the full insight-hero from `variants.apollo` (dual-read confirmed, 0 console errors).

### 4. Live R11 range surface + null-gate behavior
expected: On a fresh analysis with a creator baseline (follower_count > 0), the EngagementRangeCard renders a lo–hi range + confidence; on permalink reload (live-only design) it is absent with the "estimate available on fresh run" affordance reading as intentional; no card when no baseline.
result: **PASS (2026-06-06, positive path + null-gate + persistence, live).** Verified end-to-end on a fresh analysis (`H4EwvOGMV0iu`, e2e_creator seeded to 250k followers): the board Verdict frame renders **"Projected views 15K–46K · High confidence"** — the grounded R11 range (follower_count × quality read), and it survives a full permalink reload (DB `variants.engagement_range = {lo:15448,hi:46343,conf:0.77}`). Null-gate still holds (no baseline → no block, R9). **Root cause of the prior PARTIAL went deeper than handle-threading:** the range had NO live surface — the board (the sole live `/analyze` surface) never read `predicted_engagement`, and the only renderer (`EngagementRangeCard`) sits on the unrouted `results-panel`/`test-creation-flow` that the board redesign superseded. Fixed by surfacing the range on the board (VerdictNode, dual-read live ?? `variants.engagement_range`) + persisting it (`22ccddff`). Layers 1+2 (handle-thread `dce80a43`, follower-backfill `0b99e0f2`/`6b55b08d`) were necessary precursors; this surface+persist is what made the positive path visible. See Gaps (#R11-no-live-surface).

### 5. D-08 insight-hero visual hierarchy (incl. IN-02 ordering)
expected: Insight-hero reads as the board HERO; rewrites are struck-through original + copyable variants; 6 scored dimensions cited; score band demoted. Confirm the ceiling_capper vs confidence_scope hierarchy reads correctly (IN-02 was flagged INFO — subjective).
result: PASS with one INFO note (2026-06-06, Insight region rendered on permalink). Renders: **3 hook rewrites** each = struck-through verbatim original (`<del>`) + distinct copyable variant + Copy button (verbatim-grounded, D-08 ✓); **6 dimension scores** numeric + band (Hook 85 Strong, Retention 85 Strong, Clarity 85 Strong, Share pull 50 Mid, Substance 50 Mid, Credibility 85 Strong — exact DB match); **demoted score band** "High potential · 68–80" (D-02 ✓). IN-02 **FIXED 2026-06-06** (was: hero led with the `confidence_scope` caveat, capper buried) — see Gaps (#IN-02). Re-verified live on `H4EwvOGMV0iu`: hero now LEADS with the `ceiling_capper` ("The premise relies on a well-worn 'girls vs guys' trope … capping originality §2.5"), `confidence_scope` demoted to the second line, `platform_note` third. Survives permalink reload.

## Summary

total: 5
passed: 5
issues: 0
pending: 0
partial: 0
skipped: 0
blocked: 0

## Gaps

- **Omni-flash schema-drift — FIXED 2026-06-06 (two vectors).** N-video QA sweep (2 live runs, same video) surfaced the real shape: (1) `emotion_arc[].label` drift — fixed by PR #15 (`fc79b5a7`, synonym-normalize + `.catch(undefined)`); (2) **NEW 2nd vector** `hook_decomposition.weakest_modality` — a REQUIRED enum the flash model drifted outside, causing a full ~17s Omni retry (observed live run 1: 79.5s w/ retry vs run 2: 62.4s clean — a ~17s tax). Fixed (`d097749e`): `weakest_modality` is a DERIVED field (lowest of the 4 modality scores), so accept a valid value else `.catch→undefined` and compute it from the scores in a transform — kills the retry AND fixes model self-inconsistency. Run 2 confirmed: omni completed attempt 0, no retry. Broader enum-drift risk on other omni fields remains a candidate for an ongoing N-video sweep, but the two observed vectors are closed.
- **#IN-02 — FIXED 2026-06-06 (two halves, `05f1ca68` UI + `d097749e` engine).** The insight-hero led with `confidence_scope` (a sensor-coverage caveat) instead of `ceiling_capper` (the highest-leverage insight). UI half: `InsightHeroFrame` now leads with ceiling_capper, demotes confidence_scope, falls back when capper absent (Tests 7+8). Engine half (the real root cause): the **aggregator was dropping ceiling_capper + platform_note** when building `apollo_reasoning` — DeepSeek returns them and the §4 schema requires ceiling_capper, but they never reached the frame. Threaded both through + widened the type. Re-verified live + on permalink reload (`H4EwvOGMV0iu`).
- **#R11-no-live-surface — FIXED 2026-06-06 (`22ccddff`).** The deepest layer under the prior PARTIAL: even with handle-thread (L1) + follower-backfill (L2) done and `predicted_engagement` computed live, the range had NO live surface. The board (sole live `/analyze` surface) never read `predicted_engagement`; the only renderer (`EngagementRangeCard`) lives on `results-panel`/`test-creation-flow`, which **no route imports** (orphaned when the board redesign replaced that surface). P5-02/05 wired the card into a surface the board redesign then superseded. Fix: surface the range on the board (`VerdictNode` "Projected views" block, null-gated, dual-read live ?? `variants.engagement_range`) + persist it into `variants.engagement_range` (read-merge-write, no migration; null never persisted so permalink mirrors the live null-gate). This is what finally made R11's positive path user-visible.
- ~~#3 / #4 / #5 pending~~ — RESOLVED 2026-06-06 via live Playwright UI run (analysis `1dtSt9diWtgS`). #3 PASS, #5 PASS, #4 PARTIAL (null-gate only).
- **#R11-handle-threading — FIXED 2026-06-06 (layer 1 of 2):** the analyze route never put the signed-in user's `creator_handle` into the pipeline input, so `runPredictionPipeline` always cold-started (`"No creator handle provided"`) → `follower_count` null → R11 dormant. Fix in `src/app/api/analyze/route.ts`: extended the existing `creator_profiles` select to include `tiktok_handle` and inject `validated.creator_handle ??= creatorProfile.tiktok_handle` after parse (covers both SSE + JSON pipeline calls). 45/45 route tests green, 0 new tsc. The engine now loads the creator profile (niche, engagement_rate, tiktok_followers when present).
- **#R11-follower-scrape — FIXED 2026-06-06 (layer 2 of 2, `0b99e0f2`):** even with the handle threaded, the R11 *range* needs `creator_profiles.tiktok_followers` populated, and that column was ONLY filled by the background scrape fired from `/api/profile` PATCH (route.ts:132). The ONBOARDING handle step writes `tiktok_handle` directly and did NOT trigger that scrape, so `tiktok_followers` stayed null for onboarding-only users. Fix: `backfillCreatorFollowers()` in `src/app/api/analyze/route.ts` fires the same best-effort scrape from the analyze path whenever a handle exists but `tiktok_followers == null`. Fire-and-forget (zero latency on the ≤90s budget): THIS run still cold-starts, the column is populated for the user's NEXT run (= "backfill on first analyze"). Self-healing for any handle-having user regardless of how the handle was set; failures swallowed → honest null, never fabricated. 73 route tests green, 0 new tsc. **Both R11 layers now closed.** Live verify of the positive range: run analyze twice on a handle-having account — 2nd run renders the lo–hi EngagementRangeCard (the card lives on the results-panel/simulation surface, not the board).
- **IN-02 hierarchy (INFO, from code review):** insight-hero leads with `confidence_scope` (caveat) instead of `ceiling_capper` (highest-leverage insight). Confirmed in live render. Subjective copy-hierarchy polish → defer to UI/UX milestone.
- **Omni-flash schema-drift (still open):** the live UI run was CLEAN (no retry, emotion_arc 7 pts), but the earlier 2-run measure showed a 50% `emotion_arc[].label` Zod-retry rate — keep the wider N-video QA sweep before main merge.
