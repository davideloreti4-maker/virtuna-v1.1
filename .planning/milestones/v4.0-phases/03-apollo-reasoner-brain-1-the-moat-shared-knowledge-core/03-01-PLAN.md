---
phase: 03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/corpus/KNOWLEDGE-CORE.md
  - src/lib/engine/apollo-core.ts
  - src/lib/engine/__tests__/apollo-core.test.ts
  - src/lib/engine/__tests__/deepseek.test.ts
  - src/lib/engine/__tests__/aggregator.test.ts
  - src/lib/engine/__tests__/remix-core-grounding.test.ts
autonomous: true
requirements: [R2, R12]
must_haves:
  truths:
    - "The knowledge core carries the outlier=≥5× follower-count number before creator-rules is dormanted (no silent loss)"
    - "apollo-core.ts exposes a byte-stable system-prompt constant with zero interpolation"
    - "Wave 0 test scaffolds exist for every R2/R5/R12 behavior so downstream tasks fail loudly when wrong"
  artifacts:
    - path: "src/lib/engine/apollo-core.ts"
      provides: "KNOWLEDGE_CORE + APOLLO_INSTRUCTION + APOLLO_SYSTEM_PROMPT byte-stable constants"
      exports: ["KNOWLEDGE_CORE", "APOLLO_INSTRUCTION", "APOLLO_SYSTEM_PROMPT"]
    - path: "src/lib/engine/__tests__/apollo-core.test.ts"
      provides: "Byte-stability + zero-interpolation + core-content assertions"
    - path: ".planning/corpus/KNOWLEDGE-CORE.md"
      provides: "§2.0a anchor including outlier=≥5× follower count"
      contains: "5× follower count"
  key_links:
    - from: "src/lib/engine/apollo-core.ts"
      to: ".planning/corpus/KNOWLEDGE-CORE.md"
      via: "build-embedded constant with source-of-truth header"
      pattern: "KNOWLEDGE-CORE\\.md"
---

<objective>
Lay the Phase-3 foundation: (1) port the one missing calibration number into KNOWLEDGE-CORE.md so dormanting creator-rules.ts loses nothing (D-02 BLOCKING prerequisite), (2) generate the byte-stable `apollo-core.ts` constant module that every Apollo/decode/adapt call will consume as its cached system prefix, and (3) author the Wave 0 test scaffolds for all R2/R5/R12 behaviors so downstream plans get loud red→green feedback.

Purpose: D-02 number-port GATES the creator-rules dormant (Plan 02). apollo-core.ts is the shared SSOT prefix consumed by Plans 02 and 03. Wave 0 tests make Nyquist sampling possible for the whole phase.
Output: Updated core §2.0a, new `apollo-core.ts`, new + extended test files (initially red where they assert not-yet-built behavior).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-CONTEXT.md
@.planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-RESEARCH.md
@.planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-PATTERNS.md
@.planning/corpus/KNOWLEDGE-CORE.md

<interfaces>
<!-- Byte-stability contract to mirror — extracted from creator-rules.ts:1-17 -->
creator-rules.ts header: "BYTE-STABILITY CONTRACT: every export here is a build-time constant string with NO interpolation of Date.now()/Math.random()/per-request data ... preserving Qwen automatic input-cache hits."

<!-- The validated production prompt-suffix seed lives in scripts/apollo-core-smoke.ts as APOLLO_INSTRUCTION (A/B-validated, scores 26-86). Lift it as APOLLO_INSTRUCTION. -->

<!-- deepseek.ts STABLE_SYSTEM_PROMPT shape (the prefix being replaced) — deepseek.ts:40 -->

<!-- KNOWLEDGE-CORE.md anchors: §2.0a Calibration anchors at line 38; §4 Scoring Rubric at line 144; §5 Decode Lens at line 176; §6 Rewrite lens at line 186. The outlier number lives in creator-rules.ts:65 CREATOR_RULES_NUMERIC row 1: "Outlier = ≥5× follower count in views (Ava)" — ABSENT from core §2.0a. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the missing outlier calibration number into KNOWLEDGE-CORE.md §2.0a (D-02 BLOCKING)</name>
  <files>.planning/corpus/KNOWLEDGE-CORE.md</files>
  <read_first>
    - .planning/corpus/KNOWLEDGE-CORE.md (read §2.0a in full, lines 38-52, and §5 luck lens lines 176-185)
    - src/lib/engine/creator-rules.ts:60-70 (CREATOR_RULES_NUMERIC source — row 1 is the missing number)
    - .planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-RESEARCH.md (Pitfall 2 + D-02 diff)
  </read_first>
  <action>
    Add the calibration anchor "Outlier = ≥5× follower count in views (Ava)" to KNOWLEDGE-CORE.md §2.0a (the "Calibration anchors (hard numbers — Hoyos / Hormozi / Ava)" section starting at line 38). Phrase it as a hard number consistent with the existing §2.0a bullet style (creator-cited, e.g. "Outlier benchmark — a video is an algorithmic outlier when views ≥ 5× the creator's follower count (Ava)."). This is a content port per D-02; it is the GUARD that gates the creator-rules.ts dormant in Plan 02. RESEARCH verified the other 8 of 9 video-scoreable numbers already carry; the remaining creator-rules rows are channel/business-strategy that §8 parks by design — do NOT port those. Do NOT touch §4/§5/§6 content. Do NOT alter any other §2.0a anchor (byte-stability of the generated constant must change only by this addition).
  </action>
  <verify>
    <automated>grep -q "5× follower count" .planning/corpus/KNOWLEDGE-CORE.md &amp;&amp; grep -c "5× follower count" .planning/corpus/KNOWLEDGE-CORE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "5× follower count" .planning/corpus/KNOWLEDGE-CORE.md` exits 0 (the number is present)
    - The new line sits inside §2.0a (between line 38 header and the §2.1 header at line 52) — verify with `grep -n "follower count" .planning/corpus/KNOWLEDGE-CORE.md` returns a line number > 38 and < the §2.1 line
    - No §4/§5/§6 lines changed: `git diff .planning/corpus/KNOWLEDGE-CORE.md` shows additions only within §2.0a
  </acceptance_criteria>
  <done>The outlier=≥5× follower count number lives in core §2.0a; creator-rules.ts dormant (Plan 02) is now unblocked with zero silent number loss.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate the byte-stable apollo-core.ts constant module</name>
  <files>src/lib/engine/apollo-core.ts</files>
  <read_first>
    - .planning/corpus/KNOWLEDGE-CORE.md (the full core — this is the SSOT being embedded; read after Task 1's edit so the embedded copy is current)
    - src/lib/engine/creator-rules.ts:1-17 (BYTE-STABILITY CONTRACT header to mirror)
    - src/lib/engine/deepseek.ts:40-109 (STABLE_SYSTEM_PROMPT shape — the prefix being replaced)
    - scripts/apollo-core-smoke.ts (APOLLO_INSTRUCTION — the validated production-suffix seed to lift)
    - .planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-PATTERNS.md (apollo-core.ts section, lines 29-55)
  </read_first>
  <behavior>
    - Test: `APOLLO_SYSTEM_PROMPT` is byte-identical across two reads (module-level const, no interpolation)
    - Test: `KNOWLEDGE_CORE` contains "5× follower count" (proves Task 1's port made it into the embedded copy)
    - Test: `KNOWLEDGE_CORE` contains no interpolation artifacts — assert it does NOT contain "${" and does NOT contain "Date.now" / "Math.random"
    - Test: `APOLLO_SYSTEM_PROMPT === KNOWLEDGE_CORE + "\n\n---\n\n" + APOLLO_INSTRUCTION` (composition contract)
  </behavior>
  <action>
    Create src/lib/engine/apollo-core.ts exporting three module-level const strings: `KNOWLEDGE_CORE` (the full post-Task-1 KNOWLEDGE-CORE.md content embedded verbatim as a single template literal — escape backticks/`${` in the source so the literal is inert; NO interpolation), `APOLLO_INSTRUCTION` (lifted from scripts/apollo-core-smoke.ts APOLLO_INSTRUCTION — the §4-OUTPUT-CONTRACT-eliciting suffix that the A/B harness validated), and `APOLLO_SYSTEM_PROMPT = `${KNOWLEDGE_CORE}\n\n---\n\n${APOLLO_INSTRUCTION}``. Copy the creator-rules.ts:1-17 BYTE-STABILITY CONTRACT header verbatim into apollo-core.ts, retitling the source-of-truth note to `.planning/corpus/KNOWLEDGE-CORE.md` and documenting that the constant is regenerated from that file (note the regen step in the header comment per RESEARCH Code Examples). ZERO interpolation in any export (Anti-pattern, RESEARCH:187): no verbatim, no creator context, no platform name, no Date.now — those all live in the user message built downstream. Keep file under 500 lines per CLAUDE.md by using one large template literal (the ~30KB core is a single string, not 500 lines of code).
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/apollo-core.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/engine/__tests__/apollo-core.test.ts` passes (built in Task 3)
    - `node -e "const m=require('./src/lib/engine/apollo-core.ts')"` not required; instead: `grep -c 'export const' src/lib/engine/apollo-core.ts` returns 3
    - apollo-core.ts contains the string "5× follower count" (embedded core is post-Task-1): `grep -q "5× follower count" src/lib/engine/apollo-core.ts`
    - apollo-core.ts header references "KNOWLEDGE-CORE.md": `grep -q "KNOWLEDGE-CORE.md" src/lib/engine/apollo-core.ts`
    - `npm run build` (tsc) succeeds — the template literal compiles
  </acceptance_criteria>
  <done>apollo-core.ts exports KNOWLEDGE_CORE, APOLLO_INSTRUCTION, APOLLO_SYSTEM_PROMPT as byte-stable zero-interpolation constants; Plans 02 and 03 can import them.</done>
</task>

<task type="auto">
  <name>Task 3: Author Wave 0 test scaffolds for all R2/R5/R12 phase behaviors</name>
  <files>src/lib/engine/__tests__/apollo-core.test.ts, src/lib/engine/__tests__/deepseek.test.ts, src/lib/engine/__tests__/aggregator.test.ts, src/lib/engine/__tests__/remix-core-grounding.test.ts</files>
  <read_first>
    - src/lib/engine/__tests__/deepseek.test.ts (existing, 17.1K — extend, mirror its mock-Qwen idioms)
    - src/lib/engine/__tests__/aggregator.test.ts (existing, 44.3K — extend, mirror blend-math test idioms)
    - .planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map, lines 407-431)
    - src/lib/engine/remix/decode-types.ts:27-33 (BEAT_IDS for the D-13 static assert)
  </read_first>
  <action>
    Create/extend the Wave 0 test scaffolds named in RESEARCH lines 427-431. (a) apollo-core.test.ts (new) — the Task-2 behavior tests (byte-stability, zero-interpolation, embedded-number, composition). (b) Extend deepseek.test.ts with cases named exactly: "apollo schema validates" (6 dims + composite 0–100 + confidence_scope + rewrites 2–3), "rewrite original matches verbatim hook" (R2 verify), "rewrites fix distinct §2 levers" (D-08). For behaviors that depend on Plan-02 code not yet written, write the test against the intended extended schema/shape and mark it with a clear failing assertion (red is expected until Plan 02 lands — this is the Nyquist scaffold). (c) Extend aggregator.test.ts with cases named "blend uses behavioral + apollo" and "gemini retired from raw_overall_score" (R5/D-04) — red until Plan 04. (d) Create remix-core-grounding.test.ts (new) — assert DECODE_SYSTEM_PROMPT and ADAPT_SYSTEM_PROMPT each reference KNOWLEDGE_CORE (R12 grep/string assert) AND a static assert that §5's 4 beats (hook_pattern/structure_pacing/the_turn/emotional_beat) equal decode-types BEAT_IDS (D-13). Use the existing mock-Qwen helper pattern from deepseek.test.ts — do NOT hit the real API. Each new test file/case maps to the Per-Task Verification Map in 03-VALIDATION.md.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/apollo-core.test.ts src/lib/engine/__tests__/remix-core-grounding.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apollo-core.test.ts GREEN (Task 2's constant exists)
    - remix-core-grounding.test.ts D-13 beat-mapping case GREEN (BEAT_IDS already exist); the R12 "references KNOWLEDGE_CORE" cases are RED until Plan 03 (documented expected-red)
    - deepseek.test.ts new Apollo cases present and RED-until-Plan-02 (documented); aggregator.test.ts new blend cases present and RED-until-Plan-04 (documented)
    - No test uses watch mode; all run under `vitest run`
    - `grep -c "KNOWLEDGE_CORE" src/lib/engine/__tests__/remix-core-grounding.test.ts` ≥ 2 (decode + adapt assertions)
  </acceptance_criteria>
  <done>Every R2/R5/R12 behavior has a named automated test; Plans 02/03/04 turn the documented-red cases green. 03-VALIDATION.md Per-Task Verification Map populated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KNOWLEDGE-CORE.md → apollo-core.ts | content SSOT crosses into a build-embedded constant; drift = stale brain in prod |
| (no new runtime input surface) | this plan is content + constants + tests; no LLM call wired yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | apollo-core.ts embedded constant drifts from KNOWLEDGE-CORE.md | mitigate | Header documents source-of-truth + regen note; Task 2 acceptance asserts the post-Task-1 number is embedded (catches stale copy) |
| T-03-02 | Tampering | accidental interpolation in the cached prefix | mitigate | apollo-core.test.ts asserts no "${" / Date.now / Math.random in KNOWLEDGE_CORE; preserves DashScope prefix-cache integrity |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new packages this phase (RESEARCH Package Legitimacy Audit: not applicable); no install task exists |
</threat_model>

<verification>
- D-02 GUARD satisfied: `grep "5× follower count" .planning/corpus/KNOWLEDGE-CORE.md` exits 0 BEFORE any dormant task runs.
- apollo-core.ts byte-stable + zero-interpolation (apollo-core.test.ts green).
- `npm run build` succeeds (template literal compiles).
- Wave 0 scaffolds exist for R2/R5/R12; documented-red cases identified for Plans 02/03/04.
</verification>

<success_criteria>
- KNOWLEDGE-CORE.md §2.0a carries the outlier number (gates Plan 02 dormant).
- apollo-core.ts exports the 3 byte-stable constants, importable by Plans 02/03.
- All Wave 0 test files exist and run under `vitest run`; D-13 static assert green now.
</success_criteria>

<output>
Create `.planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-01-SUMMARY.md` when done.
</output>
