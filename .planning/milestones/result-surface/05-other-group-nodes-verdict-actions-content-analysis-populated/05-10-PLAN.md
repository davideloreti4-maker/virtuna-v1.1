---
phase: 05
plan: 10
type: execute
wave: 5
depends_on: [09]
files_modified: []
autonomous: false
requirements: [R1.3, R1.4, R1.5, R1.6, R1.7, R1.8, R1.9]
tags: [phase-5, checkpoint, manual-verify, visual-smoke]

must_haves:
  truths:
    - "Verdict / Actions / Content Analysis frames render correctly against a streaming + completed analysis"
    - "Anti-virality state ripples coordinated across Verdict (orange header) + Audience (orange chips) + Actions (grown reshoot hero) in a single user-visible transition"
    - "Recharts emotion arc renders at all 4 breakpoints (320 / 768 / 1280 / 1920)"
    - "react-markdown reasoning narrative renders safely (no script execution)"
    - "Manual smoke confirms collapsibles open/close + persist state across simple navigation"
  artifacts: []
  key_links: []
---

<objective>
Human-verify checkpoint at the end of Phase 5. All automated tests are green; this gate confirms the visual + functional behavior in a real browser session before merging Phase 5.

Purpose: Phase 5 ships 3 group node bodies with complex visual contracts (Recharts charts, react-markdown, conditional anti-virality treatments). Automated DOM tests cover behavior; only a human can confirm: layout fits in frame bounds, gradients read correctly on dark Raycast background, transitions feel smooth, no flashing/cls/cumulative-shift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-other-group-nodes-verdict-actions-content-analysis-populated/05-CONTEXT.md
@.planning/phases/05-other-group-nodes-verdict-actions-content-analysis-populated/05-VALIDATION.md §Manual-Only Verifications
@.planning/phases/05-other-group-nodes-verdict-actions-content-analysis-populated/05-UI-SPEC.md §Checker Sign-Off
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Phase 5 visual + functional gate</name>

  <what-built>
    Three new group node bodies wired into `/analyze/[id]`:
    1. **Verdict node** (top-right group frame): big percentile chip + confidence pill + anti-virality header (conditional) + "Why this verdict?" collapsible (markdown + 4 buckets) + "vs my history" collapsible (2 Recharts BarCharts or empty state)
    2. **Actions node** (mid-right group frame): 2x2 grid with 3 dashed placeholders (Reshoot/OptimalPost/Share) + real SimilarVideosCard; in anti-virality state, frame grows and ReshootHeroSlot spans the top
    3. **Content Analysis frame** (bottom group frame, horizontal split): Hook decomposition node (4-bar GlassProgress stack + chips, left ~480px) + Emotion arc node (Recharts AreaChart with coral gradient + peak/valley dots, right ~872px)

    Plus a cross-group anti-virality ripple wired through `useAntiViralityAffectedFrames()` — when the engine flags `anti_virality_gated`, Verdict + Audience + Actions all transition together.
  </what-built>

  <how-to-verify>
    Start the dev server, log into a test account, and visit a completed analysis at `/analyze/[id]`. Test against TWO analyses:

    **Analysis A — non-anti-virality (overall_score >= 70)**

    1. Confirm the **Verdict frame** shows:
       - Big coral percentile number (since score >= 70)
       - Confidence pill on the right showing the actual confidence label
       - NO orange anti-virality band at the top of the frame
       - "Why this verdict?" collapsible closed by default
       - "vs my history" collapsible closed by default

    2. Tap "Why this verdict?" — confirm:
       - 4 sub-sections render (Why this works / Why this might not / What the engine flagged / Counterfactual considered)
       - Intro markdown text renders with bold/italic where present
       - Open browser DevTools console; verify NO script execution warning (rehype-sanitize is doing its job)

    3. Tap "vs my history" — confirm:
       - If the account has < 3 prior analyses: shows "Need 3+ prior analyses to show comparison. {N}/3 complete."
       - If 3+: shows the "vs your last 10" horizontal bar chart (coral current bar + white history bars)
       - Niche section shows "Niche comparison coming soon" (Phase 5 lock)

    4. Confirm the **Actions frame** shows:
       - 2x2 grid of cards in the default 360x200 layout
       - Top-left: dashed Reshoot script placeholder "Coming in Phase 6"
       - Top-right: dashed When-to-post placeholder "Coming in Phase 6"
       - Bottom-left: SimilarVideosCard with 5 mini-rows (handles + view counts + similarity %)
       - Bottom-right: dashed Share & export placeholder "Coming in Phase 7"
       - Tap any similar-video row: TikTok embed modal opens at body level

    5. Confirm the **Content Analysis frame** shows (1352x200 horizontal split):
       - Hook decomp (left ~480px): 4 horizontal bars labeled Visual stop power / Audio hook / Text overlay / First words; weakest modality has a coral-tinted background; chip row with "Coherence: X.X/10" and "Cognitive load: Low/Med/High" (NEVER a raw 0-10 number)
       - Emotion arc (right ~872px): Recharts area chart with coral gradient (top) fading to gray (bottom), peak dots in coral, valley dots in gray, headline shows "Peak at m:ss"

    6. Resize the browser to 4 widths (320, 768, 1280, 1920). Confirm:
       - Recharts emotion arc renders at all widths (no blank chart — Pitfall 1 mitigation)
       - All node bodies stay within their group frame bounds (no overflow)

    **Analysis B — anti-virality state**

    Use an analysis where `result.anti_virality_gated === true` (or temporarily set the boardState via DevTools to 'anti-virality' if needed).

    7. Confirm **all three frames update simultaneously** (cross-group ripple R1.9):
       - Verdict frame: orange header band appears at top "⚠ Don't post yet — fixable in N steps" with "Post anyway →" link on the right
       - Audience frame: orange chips appear (Phase 4 wiring inherited)
       - Actions frame: grows from 360x200 to 360x360; ReshootHeroSlot spans the top (still a dashed Phase 6 placeholder content); Optimal-post / SimilarVideos / Share still visible in the bottom row

    8. Tap "Post anyway →" — confirm:
       - Orange band dismisses immediately
       - On hard refresh of the same analysis URL, the band STAYS dismissed (localStorage persistence)
       - Open DevTools Application → Local Storage; verify a key like `virtuna:verdict-av-override:{analysisId}` set to `'1'`

    9. With "Why this verdict?" expanded in anti-virality state, confirm:
       - The 4 sub-sections render
       - Inside "Counterfactual considered", the TOP-3 fixes list renders with [seg · m:ss] timestamp pills + headline + detail
       - Tap a timestamp pill: confirm the board pans to the Audience frame (setActivePreset('audience') is wired)

    **Reduced motion check (system-level setting):**

    10. Enable `prefers-reduced-motion: reduce` (macOS: System Settings → Accessibility → Display → Reduce motion. Or DevTools → Rendering → Emulate CSS media → reduce).
    11. Reload `/analyze/[id]`. Confirm:
        - Verdict collapsibles open/close instantly (no height transition)
        - Recharts emotion arc renders WITHOUT entrance animation
        - Hook decomp bars jump to final values (no fill animation)
        - Actions frame anti-virality grow/shrink happens instantly

    **Console hygiene:**

    12. Open browser DevTools console. Confirm NO errors during the smoke test. Warnings are OK if they pre-date Phase 5.
  </how-to-verify>

  <resume-signal>Type "approved" if all checks pass, or describe any failures (with screenshots if possible).</resume-signal>
</task>

</tasks>

<verification>
Manual checks above must all pass. If any check fails, the executor should NOT mark Phase 5 done. Instead, surface a "Verify failed" outcome to the orchestrator with the specific check that failed; gap closure plans may be needed.
</verification>

<success_criteria>
- All 12 manual checks pass
- Visual confirmation of cross-group anti-virality ripple R1.9
- react-markdown XSS sanitization confirmed (no console security warnings, no script execution)
- Recharts emotion arc renders correctly at 4 breakpoints
- localStorage AV override persists across page reload
- Reduced motion fallback respected for all three node bodies
</success_criteria>

<output>
After completion (user approves), append to `.planning/phases/05-other-group-nodes-verdict-actions-content-analysis-populated/05-10-SUMMARY.md`:
- Date of manual verification
- Browser + viewport used
- Screenshots saved to `.planning/phases/05-other-group-nodes-verdict-actions-content-analysis-populated/screenshots/` (if user provides)
- Any deviations from expected behavior the user noted but accepted (e.g. "Recharts gradient slightly lighter than mockup, accepted")
</output>
