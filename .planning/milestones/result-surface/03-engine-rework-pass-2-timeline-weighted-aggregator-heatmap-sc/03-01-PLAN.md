---
phase: 03
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/engine/__tests__/pass2.test.ts
  - src/lib/engine/__tests__/weighted-aggregator.test.ts
  - src/lib/engine/__tests__/filmstrip.test.ts
  - src/lib/engine/__tests__/persona-weights.test.ts
  - src/lib/engine/__tests__/anti-virality.test.ts
autonomous: true
requirements: [R2, R1.9, NF4]
tags: [phase-3, wave-0, test-scaffolding, vitest]

must_haves:
  truths:
    - "Vitest stubs exist for every new Phase 3 module so Nyquist sampling has a target file before implementation"
    - "Running `pnpm vitest run src/lib/engine/__tests__/` exits 0 (stubbed tests skipped/passing, never red)"
  artifacts:
    - path: src/lib/engine/__tests__/pass2.test.ts
      provides: "Wave 0 stub for R2.2 Pass 2 orchestrator"
      contains: "describe('runWave3Pass2'"
    - path: src/lib/engine/__tests__/weighted-aggregator.test.ts
      provides: "Wave 0 stub for R2.3 weighted curve + HeatmapPayload"
      contains: "describe('buildWeightedCurve'"
    - path: src/lib/engine/__tests__/filmstrip.test.ts
      provides: "Wave 0 stub for R2.6 filmstrip extract/storage/queue"
      contains: "describe('filmstrip'"
    - path: src/lib/engine/__tests__/persona-weights.test.ts
      provides: "Wave 0 stub for D-20 weight precedence resolver"
      contains: "describe('resolveWeights'"
    - path: src/lib/engine/__tests__/anti-virality.test.ts
      provides: "Extended Wave 0 fixtures for R1.9 timeline-pattern trigger"
      contains: "isTimelinePatternTriggered"
  key_links:
    - from: src/lib/engine/__tests__/pass2.test.ts
      to: src/lib/engine/wave3/pass2.ts
      via: "import { runWave3Pass2 }"
      pattern: "wave3/pass2"
    - from: src/lib/engine/__tests__/weighted-aggregator.test.ts
      to: src/lib/engine/wave3/weighted-aggregator.ts
      via: "import { buildWeightedCurve, assembleHeatmapPayload }"
      pattern: "wave3/weighted-aggregator"
---

<objective>
Create Vitest test scaffolds for every new Phase 3 module BEFORE implementation begins. Nyquist rule mandates that every executor task have an `<automated>` verify command pointing at a real file; this Wave 0 plan creates those files as `describe.todo` / `it.skip` stubs so later plans can flip them to live assertions without breaking the sampling continuity.

Purpose: enforce TDD-adjacent discipline on a backend-only engine extension where regressions are silent (Pass 2 returns null → fallback silently degrades). Wave 0 stubs lock the test surface area before model output uncertainty creeps in.

Output: 5 test files (4 new, 1 extended) that import the not-yet-existent modules under a `// @ts-expect-error pending implementation` comment so type-checks stay green. Each file contains `describe.todo` or `it.skip` blocks for every test row in 03-VALIDATION.md Per-Task Verification Map and 03-RESEARCH.md Phase Requirements → Test Map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-CONTEXT.md
@.planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-RESEARCH.md
@.planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-PATTERNS.md
@.planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-VALIDATION.md

<interfaces>
Key analog already in codebase (executor reads first, does not re-discover):

From src/lib/engine/__tests__/wave3.test.ts (lines 27-62) — exact mock infrastructure pattern to copy:
```typescript
vi.mock("@/lib/logger", () => ({ createLogger: vi.fn(() => ({ debug: vi.fn(), info: vi.fn(), warn: vi.fn(), error: vi.fn() })) }));
vi.mock("@sentry/nextjs", () => ({ captureException: vi.fn(), addBreadcrumb: vi.fn() }));
const { mockCreate } = vi.hoisted(() => ({ mockCreate: vi.fn() }));
vi.mock("openai", () => {
  const MockOpenAI = vi.fn(function (this: Record<string, unknown>) {
    this.chat = { completions: { create: mockCreate } };
  });
  return { default: MockOpenAI };
});
process.env.DASHSCOPE_API_KEY = "test-key";
```

From src/lib/engine/__tests__/anti-virality.test.ts — pure unit test pattern (no mocks needed for math/logic modules).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 4 new Vitest stub files + extend anti-virality.test.ts</name>
  <files>src/lib/engine/__tests__/pass2.test.ts, src/lib/engine/__tests__/weighted-aggregator.test.ts, src/lib/engine/__tests__/filmstrip.test.ts, src/lib/engine/__tests__/persona-weights.test.ts, src/lib/engine/__tests__/anti-virality.test.ts</files>
  <read_first>
    - src/lib/engine/__tests__/wave3.test.ts (full file — copy mock infrastructure pattern from lines 27-62 verbatim into pass2.test.ts and filmstrip.test.ts)
    - src/lib/engine/__tests__/anti-virality.test.ts (full file — preserve existing tests when extending)
    - src/lib/engine/__tests__/wave3-aggregator.test.ts (full file — copy pure-math test pattern into weighted-aggregator.test.ts)
    - .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-PATTERNS.md lines 555-644 (test fixture factories + 12-test surface for pass2.test.ts; persona-weights.test.ts cases)
    - .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-VALIDATION.md lines 49-57 (Wave 0 requirements list)
    - .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-RESEARCH.md lines 749-779 (Requirement → Test Map)
  </read_first>
  <action>
    Create exactly these 5 files with `describe.todo` and `it.skip` blocks. Do NOT write live assertions — implementation lands in later plans. Goal: every test command in 03-VALIDATION.md / 03-RESEARCH.md resolves to a real file that exits 0 today.

    **File 1: `src/lib/engine/__tests__/pass2.test.ts`** (R2.2, NF4)
    - Copy mock infrastructure verbatim from wave3.test.ts lines 27-62 (logger, Sentry, openai mocks, DASHSCOPE_API_KEY env).
    - Add `// @ts-expect-error pending Plan 06 implementation` above the import line, then `import { runWave3Pass2 } from "../wave3/pass2";` (file does not exist yet — Plan 06 creates it).
    - Add helper stub:
      ```typescript
      function makeSegments(count = 5) {
        return Array.from({ length: count }, (_, i) => ({
          idx: i, t_start: i * 2, t_end: (i + 1) * 2,
          visual_event: `event_${i}`, audio_event: `audio_${i}`,
          is_hook_zone: i === 0,
        }));
      }
      ```
    - Add `describe("runWave3Pass2 — Phase 3 Pass 2 orchestrator (Wave 0 stub)", () => { ... })` containing exactly 12 `it.skip` cases matching the test surface in 03-PATTERNS.md lines 597-609:
      1. "fires exactly 10 parallel calls with enable_thinking: true"
      2. "all-succeed → 10 Pass2PersonaResult entries with full shape"
      3. "7/10 succeed → pass2_aggregate_built: true + 3 failure warnings"
      4. "5/10 succeed → pass2_aggregate_built: false, heatmap null"
      5. "Promise.allSettled isolation — 10 attempted even with rejections"
      6. "cost telemetry: wave_3_pass2 stage_end carries cost_cents >= 0"
      7. "events: 22 stage events when all 10 succeed (10 per-persona pairs + 1 wave pair)"
      8. "Zod validation failure → retry-once → 11 total calls"
      9. "segment count mismatch validation → persona dropped from aggregate (D-06)"
      10. "AbortError → no retry → slot fails after first attempt"
      11. "thinking_budget: 8000 present in every API call"
      12. "D-23 telemetry: pass2_latency_ms + pass2_cost_cents logged per persona"

    **File 2: `src/lib/engine/__tests__/weighted-aggregator.test.ts`** (R2.3, R2.5)
    - No mock infrastructure needed (pure math).
    - Add `// @ts-expect-error pending Plan 04 implementation` then `import { buildWeightedCurve, assembleHeatmapPayload, DEFAULT_WEIGHTS } from "../wave3/weighted-aggregator";`.
    - `describe("buildWeightedCurve + assembleHeatmapPayload (Wave 0 stub)", () => { ... })` with `it.skip`:
      1. "buildWeightedCurve: weighted_curve = correct weighted mean per segment"
      2. "buildWeightedCurve: weighted_hook_score = mean attention in is_hook_zone segments"
      3. "buildWeightedCurve: weighted_top_dropoff_t = t_start of biggest segment-to-segment drop"
      4. "buildWeightedCurve: weighted_completion_pct = mean of per-persona timeline means (weighted)"
      5. "normalizeOverSurvivors: missing cross_niche slot redistributes weight to sum=1.0 (Pitfall 6)"
      6. "normalizeOverSurvivors: all-types-surviving keeps default weights exactly"
      7. "assembleHeatmapPayload: returns full D-13 shape (segments, personas, weighted_curve, weights, weights_source)"
      8. "assembleHeatmapPayload: keyframe_uri starts null for every segment (Pitfall 3)"
      9. "assembleHeatmapPayload: segment_reasons sparse — only at inflection points"

    **File 3: `src/lib/engine/__tests__/filmstrip.test.ts`** (R2.6)
    - Copy logger + Sentry mocks from wave3.test.ts.
    - Mock `child_process` and `ffmpeg-static`:
      ```typescript
      vi.mock("ffmpeg-static", () => ({ default: "/mock/ffmpeg" }));
      vi.mock("child_process", () => ({ spawn: vi.fn() }));
      ```
    - Mock `@/lib/supabase/service`:
      ```typescript
      vi.mock("@/lib/supabase/service", () => ({ createServiceClient: vi.fn() }));
      ```
    - Add three `describe.todo`:
      - `describe("extractFrameAtTimestamp (filmstrip/extract.ts)")` — `it.skip` for: "spawns ffmpeg with -ss <t_start> -frames:v 1 -q:v 4 args", "returns Buffer on exit code 0", "returns null on non-zero exit", "returns null on spawn error (never throws)"
      - `describe("uploadFrameAndGetSignedUrl (filmstrip/storage.ts)")` — `it.skip` for: "uploads JPEG to filmstrips/<analysisId>/<segmentIdx>.jpg", "calls createSignedUrl with 30-day TTL (2592000 seconds)", "returns null on upload error (never throws)", "returns signed URL string on success"
      - `describe("triggerFilmstripGeneration (filmstrip/queue.ts)")` — `it.skip` for: "fire-and-forget: returns void immediately (does not await fetch)", "logs error on fetch rejection but does not throw", "POSTs to /api/filmstrip/extract with analysisId + segments + videoUrl"

    **File 4: `src/lib/engine/__tests__/persona-weights.test.ts`** (D-20)
    - Pure unit test (no mocks).
    - `import { resolveWeights, normalizeWeights, DEFAULT_PERSONA_WEIGHT_CONFIG } from "../persona-weights";` (with `@ts-expect-error pending Plan 04`).
    - `describe("persona-weights precedence resolver (Wave 0 stub)", () => { ... })` with `it.skip`:
      1. "returns default when no overrides"
      2. "analysis_override wins over creator_override"
      3. "creator_override wins over niche_override"
      4. "niche_override wins over default"
      5. "weights_source matches resolved tier ('default' | 'niche_override' | 'creator_override' | 'analysis_override')"
      6. "normalizeWeights: values sum to 1.0 ± 0.001"
      7. "normalizeWeights: missing cross_niche slot redistributes correctly (Pitfall 6)"
      8. "normalizeWeights: all-zero input returns default weights"

    **File 5: `src/lib/engine/__tests__/anti-virality.test.ts`** (R1.9 — EXTEND, do not replace)
    - Read current file content. PRESERVE all existing tests verbatim.
    - APPEND new `describe.todo("isTimelinePatternTriggered (D-17 dual-trigger, Wave 0 stub)", () => { ... })` with `it.skip`:
      1. "returns false for null heatmap (graceful degradation)"
      2. "returns true when attention loss >= 0.40 in first 5s AND >= 70% persona consensus"
      3. "returns false when attention loss >= 0.40 but persona consensus < 70%"
      4. "returns false when persona consensus >= 70% but attention loss < 0.40"
      5. "returns false when first-5s window has <2 segments (insufficient data)"
    - APPEND new `describe.todo("isAntiViralityGatedFull — dual-trigger OR logic")` with `it.skip`:
      1. "returns gated=true with reason='confidence' when confidence < 0.4 and heatmap null"
      2. "returns gated=true with reason='timeline_pattern' when timeline triggers and confidence >= 0.4"
      3. "returns gated=true with reason='both' when both fire"
      4. "returns gated=false with reason=null when neither fires"
      5. "backward compat: existing isAntiViralityGated(confidence) signature unchanged (single-arg)"

    All test files must compile (TypeScript strict). Use `@ts-expect-error pending` on each import that references not-yet-existing modules.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-result-surface && pnpm vitest run src/lib/engine/__tests__/pass2.test.ts src/lib/engine/__tests__/weighted-aggregator.test.ts src/lib/engine/__tests__/filmstrip.test.ts src/lib/engine/__tests__/persona-weights.test.ts src/lib/engine/__tests__/anti-virality.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/engine/__tests__/pass2.test.ts` exists and contains `describe("runWave3Pass2`
    - File `src/lib/engine/__tests__/pass2.test.ts` contains exactly 12 `it.skip(` occurrences (grep -c)
    - File `src/lib/engine/__tests__/weighted-aggregator.test.ts` exists and contains `describe("buildWeightedCurve`
    - File `src/lib/engine/__tests__/weighted-aggregator.test.ts` contains at least 9 `it.skip(` occurrences
    - File `src/lib/engine/__tests__/filmstrip.test.ts` exists and contains `describe("extractFrameAtTimestamp` AND `describe("uploadFrameAndGetSignedUrl` AND `describe("triggerFilmstripGeneration`
    - File `src/lib/engine/__tests__/persona-weights.test.ts` exists and contains `describe("persona-weights`
    - File `src/lib/engine/__tests__/persona-weights.test.ts` contains at least 8 `it.skip(` occurrences
    - File `src/lib/engine/__tests__/anti-virality.test.ts` contains BOTH `isTimelinePatternTriggered` AND `isAntiViralityGatedFull` describe blocks (and preserves all pre-existing `it(` cases — count must not decrease)
    - Command `pnpm vitest run src/lib/engine/__tests__/pass2.test.ts src/lib/engine/__tests__/weighted-aggregator.test.ts src/lib/engine/__tests__/filmstrip.test.ts src/lib/engine/__tests__/persona-weights.test.ts src/lib/engine/__tests__/anti-virality.test.ts` exits 0
    - Each import that references a not-yet-existing module has `@ts-expect-error pending` comment above it
  </acceptance_criteria>
  <done>5 test files exist; all `describe`/`it.skip` blocks present; vitest run exits 0; TypeScript compile is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-runner → mocks | Vitest mocks are trust-controlled; not a threat surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | Test files in repo | accept | No secrets in stubs; `DASHSCOPE_API_KEY="test-key"` placeholder only (existing wave3.test.ts pattern) |
| T-03-01-02 | Tampering | Test stubs locked before implementation | accept | `it.skip` cannot regress; Plan 06+ flips skip→live with explicit review |
</threat_model>

<verification>
- `pnpm vitest run src/lib/engine/__tests__/pass2.test.ts ...` exits 0
- `pnpm tsc --noEmit` succeeds (TypeScript compile green)
- Grep counts match acceptance criteria above
</verification>

<success_criteria>
All 4 new test stub files exist, anti-virality.test.ts extended without breaking existing tests, vitest exits 0, no TypeScript errors. Wave 0 sampling target ready for Plans 04–08.
</success_criteria>

<output>
After completion, create `.planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-01-SUMMARY.md` recording the 5 test files, count of `it.skip` blocks per file, and vitest exit code.
</output>
