---
phase: 03
plan: 07
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - src/lib/engine/filmstrip/extract.ts
  - src/lib/engine/filmstrip/storage.ts
  - src/lib/engine/filmstrip/queue.ts
  - src/app/api/filmstrip/extract/route.ts
  - src/lib/engine/__tests__/filmstrip.test.ts
autonomous: true
requirements: [R2]
tags: [phase-3, filmstrip, ffmpeg, supabase-storage]

must_haves:
  truths:
    - "extractFrameAtTimestamp spawns ffmpeg with -ss <t_start> -frames:v 1 -q:v 4 -f image2 and returns Buffer (D-09)"
    - "uploadFrameAndGetSignedUrl writes to filmstrips/<analysisId>/<segmentIdx>.jpg and returns 30-day signed URL (D-10)"
    - "triggerFilmstripGeneration is fire-and-forget: returns void immediately, never awaits the fetch (D-11)"
    - "/api/filmstrip/extract route handles POST with analysisId+segments+videoUrl and produces JPEGs for each segment"
    - "All filmstrip modules follow graceful-degradation: never throw, return null on failure"
    - "All Plan 01 filmstrip stubs flipped to live tests and passing"
  artifacts:
    - path: src/lib/engine/filmstrip/extract.ts
      provides: "ffmpeg keyframe extraction at t_start"
      contains: "export async function extractFrameAtTimestamp"
    - path: src/lib/engine/filmstrip/storage.ts
      provides: "Supabase Storage upload + 30-day signed URL minting"
      contains: "export async function uploadFrameAndGetSignedUrl"
    - path: src/lib/engine/filmstrip/queue.ts
      provides: "Fire-and-forget trigger from pipeline at wave_0_complete"
      contains: "export function triggerFilmstripGeneration"
    - path: src/app/api/filmstrip/extract/route.ts
      provides: "Dedicated Vercel route with isolated ffmpeg-static binary"
      contains: "export async function POST"
  key_links:
    - from: src/lib/engine/filmstrip/queue.ts
      to: src/app/api/filmstrip/extract/route.ts
      via: "void fetch POST /api/filmstrip/extract"
      pattern: "/api/filmstrip/extract"
    - from: src/app/api/filmstrip/extract/route.ts
      to: src/lib/engine/filmstrip/extract.ts + storage.ts
      via: "extractFrameAtTimestamp + uploadFrameAndGetSignedUrl per segment"
      pattern: "extractFrameAtTimestamp"
    - from: src/lib/engine/filmstrip/storage.ts
      to: storage.buckets filmstrips (Plan 03 migration)
      via: "supabase.storage.from('filmstrips').upload + createSignedUrl"
      pattern: "filmstrips"
---

<objective>
Build the filmstrip generation pipeline that produces keyframe JPEGs for the Audience node top axis (R2.6, D-09, D-10, D-11):

1. extract.ts — child_process.spawn wrapper around ffmpeg-static, pulls one frame at the segment t_start, returns JPEG Buffer or null. Graceful degradation contract — never throws.
2. storage.ts — Supabase Storage upload via service-role client, mints 30-day signed URL. Never throws.
3. queue.ts — fire-and-forget trigger called from pipeline.ts at wave_0_complete (Plan 08 wires the call). Voids the fetch promise; pipeline never blocks on filmstrip.
4. /api/filmstrip/extract route — dedicated Vercel API route that isolates ffmpeg-static (Pitfall 1 workaround: next.config.ts marks ffmpeg-static external — landed in Plan 02). POST handler loops over segments, extracts + uploads + (later, Plan 08) emits filmstrip_segment_ready SSE events.

Purpose: Phase 4 Audience node consumes signed JPEG URLs via HeatmapPayload.segments[].keyframe_uri. This plan keeps that field null on first emit and fills it via background non-blocking work (D-11). Filmstrip latency does NOT count against the 60s engine SLA.

Output: 3 new lib modules + 1 new API route + Plan 01 filmstrip.test.ts flipped from stubs to live tests, all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-CONTEXT.md
@.planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-RESEARCH.md
@.planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-PATTERNS.md
@src/lib/supabase/service.ts
@src/app/api/analyze/[id]/stream/route.ts

<interfaces>
Plan 02 added: next.config.ts marks ffmpeg-static as external (Pitfall 1 workaround). Plan 03 migrated the filmstrips bucket (private, 5MB JPEG only) and outcomes table. This plan implements the runtime code that puts objects into that bucket.

ffmpeg-static usage (RESEARCH §Code Examples + 03-PATTERNS.md lines 334-377):
- Import: import ffmpegPath from "ffmpeg-static" (default export is string binary path or null on unsupported platforms)
- Spawn: spawn(ffmpegPath!, [args...])
- Args: ['-ss', String(t_start), '-i', videoUrl, '-frames:v', '1', '-q:v', '4', '-f', 'image2', 'pipe:1']
- Stream stdout chunks into Buffer.concat
- On non-zero exit code or spawn error: log and return null (never throw)

Supabase Storage usage (03-PATTERNS.md lines 297-323):
- createServiceClient() per call (not module-level singleton)
- supabase.storage.from('filmstrips').upload(path, jpegBuffer, { contentType: 'image/jpeg', upsert: true })
- supabase.storage.from('filmstrips').createSignedUrl(path, 60 * 60 * 24 * 30) — 30-day TTL
- Errors → log + return null

Fire-and-forget trigger pattern (03-PATTERNS.md lines 392-415):
- void fetch(`${process.env.NEXT_PUBLIC_APP_URL}/api/filmstrip/extract`, ...)
- DO NOT await; pipeline must not block
- .catch swallows errors with log.error
- Returns void synchronously after dispatching

API route shape (03-PATTERNS.md lines 420-438):
- export const runtime = "nodejs"
- export const dynamic = "force-dynamic"
- export const maxDuration = 300 (filmstrip can take time for long videos)
- POST body: { analysisId, segments: SegmentGrid[], videoUrl: string }
- Looping body: for each segment → extractFrameAtTimestamp(videoUrl, t_start) → uploadFrameAndGetSignedUrl(analysisId, idx, buffer)
- Future Plan 08: emit filmstrip_segment_ready SSE event per success; this plan persists the signed URL to a state-store (Supabase realtime broadcast OR direct DB column update on the analyses row) that the SSE route can read.

For this plan, the API route writes signed URLs into a NEW JSONB column or per-segment messages via Supabase Realtime channel. To keep scope tight in Plan 07, we use a simple per-analysis DB write: update analyses.analysis_results.heatmap.segments[idx].keyframe_uri on each successful upload (existing analyses.analysis_results JSONB column per ARCHITECTURE). Plan 08 will wire SSE consumption.

Service-role auth on /api/filmstrip/extract:
- Verify caller via shared secret (FILMSTRIP_EXTRACT_SECRET env var passed in Authorization: Bearer header by queue.ts). Prevents random callers from triggering ffmpeg work.
- Reject if header missing or mismatched.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement extract.ts + storage.ts + queue.ts (lib modules)</name>
  <files>src/lib/engine/filmstrip/extract.ts, src/lib/engine/filmstrip/storage.ts, src/lib/engine/filmstrip/queue.ts, src/lib/engine/__tests__/filmstrip.test.ts</files>
  <read_first>
    - src/lib/supabase/service.ts (FULL file — createServiceClient pattern; never instantiate own client)
    - src/lib/engine/qwen/omni-analysis.ts (graceful-degradation pattern — log + return null, never throw)
    - src/lib/engine/__tests__/filmstrip.test.ts (Plan 01 stub with 3 describe blocks — flip every it.skip to live it())
    - src/lib/engine/__tests__/wave3.test.ts (lines 27-62 mock infrastructure)
    - .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-PATTERNS.md lines 284-415 (extract.ts + storage.ts + queue.ts structure)
    - .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-RESEARCH.md lines 178-205 (ffmpeg-static hosting decision + Pitfall 1)
    - .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-CONTEXT.md lines 70-74 (D-09, D-10, D-11 verbatim)
  </read_first>
  <behavior>
    extract.ts:
    - extractFrameAtTimestamp(videoUrl, tStartSeconds) returns Promise<Buffer | null>
    - Spawns ffmpeg with -ss / -i / -frames:v 1 / -q:v 4 / -f image2 / pipe:1
    - Accumulates stdout chunks into Buffer; resolves on close code 0
    - On non-zero code: log error, resolve(null) — never throws
    - On spawn error: log error, resolve(null)

    storage.ts:
    - uploadFrameAndGetSignedUrl(analysisId, segmentIdx, jpegBuffer) returns Promise<string | null>
    - Path: `${analysisId}/${segmentIdx}.jpg`
    - upload with contentType: 'image/jpeg', upsert: true
    - createSignedUrl with 2592000 seconds (30 days)
    - Both error paths: log + return null

    queue.ts:
    - triggerFilmstripGeneration(analysisId, segments, videoUrl) returns void synchronously
    - Dispatches POST to /api/filmstrip/extract with body { analysisId, segments, videoUrl }
    - Includes Authorization: Bearer ${FILMSTRIP_EXTRACT_SECRET} header
    - DOES NOT await — uses `void fetch(...)`
    - Catches rejection via .catch(err => log.error("filmstrip trigger failed", { analysisId, error: String(err) }))
    - Returns immediately

    Tests:
    - extractFrameAtTimestamp: ffmpeg args correct; returns Buffer on code=0; returns null on non-zero/spawn error
    - uploadFrameAndGetSignedUrl: calls upload with correct path + contentType; calls createSignedUrl with 30-day TTL; returns signed URL on success; returns null on upload error; returns null on createSignedUrl error
    - triggerFilmstripGeneration: returns void; POSTs with correct body shape; includes Authorization Bearer header; logs error on fetch rejection but does not throw; called with correct URL pattern
  </behavior>
  <action>
    Create three files:

    **src/lib/engine/filmstrip/extract.ts:**
    Import: spawn from "child_process"; ffmpegPath from "ffmpeg-static" (default export is string|null); createLogger from "@/lib/logger".
    Const log = createLogger({ module: "engine.filmstrip.extract" }).
    Export async function extractFrameAtTimestamp(videoUrl: string, tStartSeconds: number): Promise<Buffer | null>:
    - If ffmpegPath is null/undefined: log.error and return null.
    - Return new Promise<Buffer | null>((resolve) => { const chunks: Buffer[] = []; const proc = spawn(ffmpegPath!, ["-ss", String(tStartSeconds), "-i", videoUrl, "-frames:v", "1", "-q:v", "4", "-f", "image2", "pipe:1"]); proc.stdout.on("data", chunk => chunks.push(chunk)); proc.on("close", code => { if (code !== 0) { log.error("ffmpeg non-zero exit", { code, tStartSeconds, videoUrl }); resolve(null); } else resolve(Buffer.concat(chunks)); }); proc.on("error", err => { log.error("ffmpeg spawn error", { error: err.message }); resolve(null); }); });

    **src/lib/engine/filmstrip/storage.ts:**
    Import: createServiceClient from "@/lib/supabase/service"; createLogger from "@/lib/logger".
    Const log.
    Export async function uploadFrameAndGetSignedUrl(analysisId: string, segmentIdx: number, jpegBuffer: Buffer): Promise<string | null>:
    - const supabase = createServiceClient();
    - const path = `${analysisId}/${segmentIdx}.jpg`;
    - const uploadResult = await supabase.storage.from('filmstrips').upload(path, jpegBuffer, { contentType: 'image/jpeg', upsert: true });
    - If uploadResult.error: log.error; return null.
    - const urlResult = await supabase.storage.from('filmstrips').createSignedUrl(path, 60 * 60 * 24 * 30);
    - If urlResult.error or !urlResult.data: log.error; return null.
    - Return urlResult.data.signedUrl.

    **src/lib/engine/filmstrip/queue.ts:**
    Import: createLogger; type SegmentGrid from "../types".
    Const log.
    Export function triggerFilmstripGeneration(analysisId: string, segments: SegmentGrid[], videoUrl: string): void:
    - log.info("filmstrip generation triggered", { analysisId, segmentCount: segments.length }).
    - void fetch(`${process.env.NEXT_PUBLIC_APP_URL}/api/filmstrip/extract`, { method: "POST", headers: { "Content-Type": "application/json", "Authorization": `Bearer ${process.env.FILMSTRIP_EXTRACT_SECRET ?? ''}` }, body: JSON.stringify({ analysisId, segments, videoUrl }) }).catch((err: unknown) => log.error("filmstrip trigger failed", { analysisId, error: String(err) }));
    - Return (implicit void).

    **Update src/lib/engine/__tests__/filmstrip.test.ts:**

    Copy mock infrastructure: createLogger mock, Sentry mock. Add:
    - vi.mock("ffmpeg-static", () => ({ default: "/mock/ffmpeg" })).
    - vi.mock("child_process", () => ({ spawn: vi.fn() })).
    - vi.mock("@/lib/supabase/service", () => ({ createServiceClient: vi.fn() })).
    - vi.mock("@/lib/logger", ...) per wave3.test.ts pattern.

    For queue.test: mock global.fetch with vi.fn().

    Implement all stubs from Plan 01 as live tests. For spawn mock: create a fake EventEmitter that emits 'data' chunk + 'close' code=0 (success path) or code=1 (failure) or 'error' event (spawn error).
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-result-surface &amp;&amp; pnpm vitest run src/lib/engine/__tests__/filmstrip.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/engine/filmstrip/extract.ts exists
    - grep -c "export async function extractFrameAtTimestamp" src/lib/engine/filmstrip/extract.ts returns 1
    - grep -c '"-ss"' src/lib/engine/filmstrip/extract.ts returns 1
    - grep -c '"-frames:v"' src/lib/engine/filmstrip/extract.ts returns 1
    - grep -c '"-q:v"' src/lib/engine/filmstrip/extract.ts returns 1
    - grep -c 'pipe:1' src/lib/engine/filmstrip/extract.ts returns 1
    - File src/lib/engine/filmstrip/storage.ts exists
    - grep -c "export async function uploadFrameAndGetSignedUrl" src/lib/engine/filmstrip/storage.ts returns 1
    - grep -c "filmstrips" src/lib/engine/filmstrip/storage.ts returns at least 2 (from + paths)
    - grep -c "createSignedUrl" src/lib/engine/filmstrip/storage.ts returns 1
    - grep -c "60 \* 60 \* 24 \* 30" src/lib/engine/filmstrip/storage.ts returns 1 (30-day TTL in seconds)
    - File src/lib/engine/filmstrip/queue.ts exists
    - grep -c "export function triggerFilmstripGeneration" src/lib/engine/filmstrip/queue.ts returns 1
    - grep -c "void fetch" src/lib/engine/filmstrip/queue.ts returns 1
    - grep -c "/api/filmstrip/extract" src/lib/engine/filmstrip/queue.ts returns 1
    - grep -c "FILMSTRIP_EXTRACT_SECRET" src/lib/engine/filmstrip/queue.ts returns at least 1
    - Test file has 0 `it.skip(` cases
    - pnpm vitest run src/lib/engine/__tests__/filmstrip.test.ts exits 0 with all tests passing
    - pnpm tsc --noEmit produces no new errors in any of the 3 modules
  </acceptance_criteria>
  <done>extract.ts + storage.ts + queue.ts implemented; all filmstrip.test.ts cases live and passing; graceful-degradation contract held (no throws).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement /api/filmstrip/extract route</name>
  <files>src/app/api/filmstrip/extract/route.ts</files>
  <read_first>
    - src/app/api/analyze/[id]/stream/route.ts (FULL file — Next.js App Router POST route shape, runtime/dynamic/maxDuration exports; service-role usage)
    - src/lib/engine/filmstrip/extract.ts (Task 1 — extractFrameAtTimestamp)
    - src/lib/engine/filmstrip/storage.ts (Task 1 — uploadFrameAndGetSignedUrl)
    - src/lib/supabase/service.ts (createServiceClient for analyses table update)
    - .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-PATTERNS.md lines 420-438 (route structure)
    - .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-RESEARCH.md lines 489-495 (Pitfall 1 — why this route is isolated)
  </read_first>
  <action>
    Create src/app/api/filmstrip/extract/route.ts.

    Imports:
    - NextRequest, NextResponse from "next/server"
    - extractFrameAtTimestamp from "@/lib/engine/filmstrip/extract"
    - uploadFrameAndGetSignedUrl from "@/lib/engine/filmstrip/storage"
    - createServiceClient from "@/lib/supabase/service"
    - createLogger from "@/lib/logger"
    - z from "zod"

    Route config exports:
    - export const runtime = "nodejs"
    - export const dynamic = "force-dynamic"
    - export const maxDuration = 300 (5 min — long videos)

    Const log = createLogger({ module: "api.filmstrip.extract" }).

    Const BodySchema = z.object({
      analysisId: z.string().uuid(),
      videoUrl: z.string().url(),
      segments: z.array(z.object({
        t_start: z.number().min(0),
        t_end: z.number().min(0),
        visual_event: z.string().max(200).optional(),
        audio_event: z.string().max(200).optional(),
      })).min(1).max(50),  // sanity cap
    });

    Export async function POST(request: NextRequest): Promise<NextResponse>:

    1. AUTH GATE: const authHeader = request.headers.get("authorization"); const expected = `Bearer ${process.env.FILMSTRIP_EXTRACT_SECRET ?? ''}`; if (!process.env.FILMSTRIP_EXTRACT_SECRET || authHeader !== expected) { log.warn("filmstrip extract auth failed", { hasHeader: !!authHeader }); return NextResponse.json({ error: "unauthorized" }, { status: 401 }); }

    2. VALIDATE BODY: const parsedBody = BodySchema.safeParse(await request.json()); if (!parsedBody.success) return NextResponse.json({ error: "bad request", details: parsedBody.error.flatten() }, { status: 400 });

    3. SSRF GUARD on videoUrl: parse URL, reject if hostname is localhost, 127.0.0.1, 10.x, 169.254.x, 172.16-31.x, 192.168.x (private/internal ranges). RESEARCH security domain identifies SSRF risk on signed video URLs.

    4. Fire response back immediately with 202 Accepted (background work), then run extraction loop in event-loop-detached way:
       NOTE: Next.js App Router does not have a true fire-and-forget; instead, we return a streaming response or just process inline and return success after all segments complete. Since maxDuration=300, prefer inline processing here. The pipeline call to triggerFilmstripGeneration is already fire-and-forget (queue.ts uses void fetch — pipeline doesn't await). So this route can do the work inline.

    5. EXTRACTION LOOP: const { analysisId, videoUrl, segments } = parsedBody.data; const supabase = createServiceClient(); const results: Array<{ idx: number; keyframe_uri: string | null }> = [];
       For each segment (i in 0..segments.length-1):
       - const buffer = await extractFrameAtTimestamp(videoUrl, segments[i].t_start);
       - if (!buffer) { results.push({ idx: i, keyframe_uri: null }); continue; }
       - const uri = await uploadFrameAndGetSignedUrl(analysisId, i, buffer);
       - results.push({ idx: i, keyframe_uri: uri });

    6. PERSIST: After loop, update analyses.analysis_results to merge keyframe_uri into the heatmap.segments[idx]:
       - Read current analysis_results JSONB.
       - For each result with non-null keyframe_uri: set analysis_results.heatmap.segments[idx].keyframe_uri = uri.
       - Update via supabase.from("analyses").update({ analysis_results: ... }).eq("id", analysisId).
       - This is the persistent state that the SSE route will later (Plan 08) read to emit filmstrip_segment_ready events.

    7. Return NextResponse.json({ ok: true, segments_processed: results.length, segments_succeeded: results.filter(r => r.keyframe_uri).length }).

    Catch top-level exceptions: log.error and return 500 with { error: "internal" } — never expose stack traces.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-result-surface &amp;&amp; pnpm tsc --noEmit 2&gt;&amp;1 | grep "api/filmstrip" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/app/api/filmstrip/extract/route.ts exists
    - grep -c 'export const runtime = "nodejs"' src/app/api/filmstrip/extract/route.ts returns 1
    - grep -c 'export const dynamic = "force-dynamic"' src/app/api/filmstrip/extract/route.ts returns 1
    - grep -c 'export const maxDuration' src/app/api/filmstrip/extract/route.ts returns 1
    - grep -c "export async function POST" src/app/api/filmstrip/extract/route.ts returns 1
    - grep -c "FILMSTRIP_EXTRACT_SECRET" src/app/api/filmstrip/extract/route.ts returns at least 2 (env check + expected header)
    - grep -c "status: 401" src/app/api/filmstrip/extract/route.ts returns at least 1 (auth gate)
    - grep -cE "localhost|127\.0\.0\.1|169\.254|192\.168" src/app/api/filmstrip/extract/route.ts returns at least 3 (SSRF deny list)
    - grep -c "BodySchema" src/app/api/filmstrip/extract/route.ts returns at least 2 (definition + use)
    - grep -c "extractFrameAtTimestamp" src/app/api/filmstrip/extract/route.ts returns at least 1
    - grep -c "uploadFrameAndGetSignedUrl" src/app/api/filmstrip/extract/route.ts returns at least 1
    - grep -c "analysis_results" src/app/api/filmstrip/extract/route.ts returns at least 1
    - pnpm tsc --noEmit produces no new errors in the route
  </acceptance_criteria>
  <done>/api/filmstrip/extract route compiled, auth-gated, SSRF-guarded, validates body via Zod, loops extract+upload, persists keyframe_uri into analyses.analysis_results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline.ts → /api/filmstrip/extract | Internal service-to-service; gated by FILMSTRIP_EXTRACT_SECRET shared bearer token |
| /api/filmstrip/extract → ffmpeg subprocess | Untrusted videoUrl passed as ffmpeg -i argument; SSRF risk |
| ffmpeg output → Supabase Storage | Service-role write; bucket public=false; signed URLs only |
| Signed URL → DashScope (Plan 06) | URLs are 30-day Supabase-signed; recipient is qwen3.6-plus vision input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | Spoofing (unauthorized caller triggers ffmpeg work) | /api/filmstrip/extract POST | mitigate | Bearer token gate (FILMSTRIP_EXTRACT_SECRET); 401 on mismatch; env var required (empty secret rejects all) |
| T-03-07-02 | Tampering (ffmpeg argument injection via t_start) | extractFrameAtTimestamp | mitigate | t_start passed via String(number) — TypeScript number type + Zod validation upstream (BodySchema and SegmentSchema). Args array form of spawn prevents shell interpolation |
| T-03-07-03 | SSRF (malicious videoUrl points to internal services) | /api/filmstrip/extract POST | mitigate | URL hostname deny-list (localhost, 127/8, 10/8, 169.254/16, 172.16/12, 192.168/16); 400 on match. Supabase-signed video URLs are external by design |
| T-03-07-04 | Information Disclosure (signed URL leakage) | createSignedUrl 30-day TTL | accept | 30-day TTL acceptable per R2.6; bucket public=false; URL is rendered only inside DashScope call (Plan 06) and Phase 4 client; lifetime aligned with video storage retention |
| T-03-07-05 | Denial of Service (oversized segments array) | BodySchema | mitigate | z.array().max(50) caps segment count; videos longer than 100s rare; bound prevents pathological inputs |
| T-03-07-06 | Denial of Service (ffmpeg cold-start / memory pressure) | extract route runtime | accept | maxDuration=300 caps; Vercel function memory is upstream concern; degrade gracefully when extract returns null |
| T-03-07-07 | Tampering (cross-analysis filmstrip overwrite) | uploadFrameAndGetSignedUrl path | mitigate | path includes analysisId UUID; upsert true allows same-segment replacement only; foreign analysis cannot collide |
</threat_model>

<verification>
- pnpm vitest run src/lib/engine/__tests__/filmstrip.test.ts exits 0
- pnpm tsc --noEmit green
- All grep counts above pass
- FILMSTRIP_EXTRACT_SECRET will need to be set as an env var (note in SUMMARY for user_setup); Plan 08 wiring depends on it
</verification>

<success_criteria>
Filmstrip lib triple (extract / storage / queue) plus dedicated Vercel route ready. Plan 08 will call triggerFilmstripGeneration from pipeline.ts at wave_0_complete; the route handles the heavy lifting and persists keyframe_uri into analysis_results so Phase 4 can render.
</success_criteria>

<output>
After completion, create .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-07-SUMMARY.md recording: 3 lib module signatures, route endpoint contract (auth + body + response), env var requirement (FILMSTRIP_EXTRACT_SECRET), test pass count, and confirmation that Pitfall 1 workaround (next.config.ts external) from Plan 02 is in place.
</output>
