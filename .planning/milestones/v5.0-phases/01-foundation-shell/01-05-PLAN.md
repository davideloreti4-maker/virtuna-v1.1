---
phase: 01-foundation-shell
plan: 05
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03, 01-04]
files_modified:
  - .planning/phases/01-foundation-shell/uat/.gitkeep
autonomous: false
requirements: [THEME-01, THEME-03, THEME-06, SHELL-07]
must_haves:
  truths:
    - "The full Vitest suite is green and a clean npm run build succeeds (the flat-warm @theme compiles)"
    - "The running shell shows the empty home (serif greeting + centered composer + NumenMark) and the active layout (composer bottom-pinned + Simulations sidebar with score chips)"
    - "Both states render correctly at a desktop width (persistent sidebar, ~760px column) and a phone width (drawer sidebar, full-width composer)"
    - "The Raycast glass is fully stripped everywhere including the sidebar (no 137deg gradient, no blur, no inset shine, no glow)"
    - "A human reviews the built shell and signs off the [UAT] values (charcoal ramp, coral hex, serif typeface, score zones, greeting copy) as locked for rollout"
  artifacts:
    - path: ".planning/phases/01-foundation-shell/uat/"
      provides: "The 4 Playwright UAT screenshots (desktop-empty, desktop-active, mobile-empty, mobile-active) attached for the human gate"
  key_links:
    - from: "the running shell (all of 01-01..01-04)"
      to: "the human reviewer"
      via: "4 Playwright screenshots + a live dev server after a clean rebuild"
      pattern: "uat"
---

<objective>
The **phase exit gate** (THEME-06 / D-07): the flat-warm visual system is locked for rollout ONLY after a human reviews it **on the built, running shell** — not on the swatch. Downstream phases (2-5) reskin onto this system, so it must be approved against something real.

This plan does NOT build features — everything is built (plans 01-01..01-04). It (1) proves the structural preconditions automatically (full suite green + clean build compiles the tokens), (2) captures the reviewable surface (4 Playwright shots: desktop/mobile × empty/active), and (3) runs the blocking human gate where the `[UAT]` values sign.

What signs at the gate (per UI-SPEC § Color / § THEME-06): exact charcoal hex ramp · exact coral hex · exact serif typeface (Newsreader vs Source Serif 4) · exact score-zone green/amber/red · greeting micro-copy. Until signed, downstream phases treat these as Frame-A provisional.

Depends on ALL implementation plans — the gate is on the assembled, running shell.
Purpose: lock the visual foundation so Phase 2+ can reskin onto a signed-off system.
Output: 4 UAT screenshots + a human sign-off (or a punch-list of changes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md
@.planning/phases/01-foundation-shell/01-VALIDATION.md

<interfaces>
<!-- The UAT gate surface + preconditions. RESEARCH § "THEME-06 Human-UAT Gate" + UI-SPEC § THEME-06 are binding. -->

Reviewable surface to produce (UI-SPEC § THEME-06):
- Empty state: /home — serif greeting ("Ready to simulate your audience, [Name]?") + centered composer + NumenMark stele glyph, NO chips.
- Active layout: /analyze/[id] for a real or seeded completed Simulation — composer bottom-pinned + the Simulations sidebar with score chips.
- BOTH at a desktop width (persistent sidebar, ~760px column) AND a phone width (drawer sidebar, full-width composer with gutters).
- Glass fully stripped (no 137deg gradient, no blur, no inset shine, no glow) — corroborated by the THEME-02 source tests from 01-02 + eyes.

Precondition (RESEARCH Pitfall 3): a CLEAN rebuild (rm -rf .next node_modules/.cache -> restart) so the reviewer sees the true compiled tokens (dev-server cache otherwise masks token changes).

Tooling available (RESEARCH Environment Availability): @playwright/test ^1.58.0. A real or seeded completed Simulation is needed for the active shot — reuse an existing history row if the account has one (useAnalysisHistory), else run one Simulation via the composer to produce an /analyze/[id] to screenshot.

[UAT] fields that sign here (do not finalize before this gate): charcoal hex ramp, coral hex, serif typeface, score-zone green/amber/red, greeting micro-copy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structural precondition — full suite green + clean build compiles the flat-warm tokens</name>
  <files>(no source edits — full-suite + clean-build gate)</files>
  <read_first>
    - .planning/phases/01-foundation-shell/01-VALIDATION.md § Sampling Rate ("Before /gsd:verify-work: full suite green AND npm run build succeeds … THEN the THEME-06 human gate")
    - .planning/phases/01-foundation-shell/01-RESEARCH.md Pitfall 3 (clean rebuild before the gate)
  </read_first>
  <action>
    Run the full Vitest suite: `npm test` (= vitest run; excludes _dormant). It MUST be green — this confirms the sidebar collapse/glass/recent tests (01-02) + the home/composer/layout tests (01-03) + the landing test (01-04) all pass on the assembled shell.
    Then a clean rebuild (Pitfall 3): `rm -rf .next node_modules/.cache && npm run build` — proves the flat-warm `@theme` compiles after all token edits (THEME-01/03 structural proof; the perceptual check is the human gate).
    If anything is red, STOP and report — do not proceed to the screenshots/gate on a broken build (the reviewer must see the true shell).
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-rework && npm test 2>&1 | tail -6 && rm -rf .next node_modules/.cache && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (full suite green: sidebar + home + composer + landing tests)
    - `npm run build` exits 0 after a clean rebuild (the flat-warm @theme compiles)
  </acceptance_criteria>
  <done>The structural preconditions hold — full suite green, clean build compiles — so the human reviews a true, working shell.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the 4 UAT screenshots (desktop/mobile × empty/active)</name>
  <files>.planning/phases/01-foundation-shell/uat/.gitkeep</files>
  <read_first>
    - .planning/phases/01-foundation-shell/01-UI-SPEC.md § THEME-06 (the exact 4 shots + states) + § Layout & Interaction (desktop ~760px column / mobile gutters / sidebar persistent vs drawer)
    - .planning/phases/01-foundation-shell/01-RESEARCH.md § THEME-06 Human-UAT Gate (4 shots: desktop-empty, desktop-active, mobile-empty, mobile-active) + Environment Availability (Playwright)
    - MEMORY note "UAT test video" — use ~/Downloads/TikTok Video Downloader.mp4 (short) if a Simulation must be run; copy into ./.playwright-mcp/ for upload if needed
  </read_first>
  <action>
    Start the dev server from the clean rebuild (Task 1). Using Playwright (@playwright/test), capture 4 screenshots into `.planning/phases/01-foundation-shell/uat/` (create the dir; add a .gitkeep):
    1. desktop-empty.png — /home at a desktop viewport (e.g. 1440×900): serif greeting + centered composer + NumenMark, persistent sidebar (expanded), ~760px column.
    2. desktop-active.png — /analyze/[id] at desktop for a completed Simulation: composer bottom-pinned + Simulations sidebar with score chips.
    3. mobile-empty.png — /home at a phone viewport (e.g. 390×844): greeting + full-width composer with gutters, sidebar as a drawer (closed, hamburger visible).
    4. mobile-active.png — /analyze/[id] at phone width: bottom-pinned composer, drawer sidebar.
    For the active shots: reuse an existing completed history row if the signed-in account has one; otherwise run ONE Simulation through the composer (use the short MEMORY UAT video) to produce an /analyze/[id] to screenshot. (Authenticated capture: follow the MEMORY "Numen fixture capture" pattern — Playwright login first.)
    These shots are the artifact the human reviews at the gate (Task 3). Do not edit any source in this task.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-rework && ls .planning/phases/01-foundation-shell/uat/ 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-foundation-shell/uat/` contains 4 screenshots: desktop-empty, desktop-active, mobile-empty, mobile-active
    - each shot shows the flat-warm shell (no visible Raycast glass) in the stated state + viewport
  </acceptance_criteria>
  <done>The 4 UAT screenshots are captured and saved for the human gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: THEME-06 human-UAT gate — sign off the flat-warm system on the built shell</name>
  <files>(no source edits — human review of the running shell + the 4 UAT screenshots)</files>
  <action>
    AUTOMATION IS COMPLETE (plans 01-01..01-04 + Tasks 1-2 of this plan). This task is a BLOCKING human gate — it pauses execution for the human reviewer. Do NOT auto-approve (auto_advance does not apply to the phase exit gate). Present the 4 UAT screenshots + the running dev server, walk the reviewer through the <how-to-verify> checklist, and capture the sign-off decision (approved + the locked [UAT] values) OR the punch-list of [UAT] adjustments into 01-05-SUMMARY.md.
  </action>
  <what-built>
    The complete Phase-1 shell, running after a clean rebuild: the flat-warm @theme token system (01-01), the lean reskinned collapsible sidebar with the glass stripped (01-02), the clean home + slim composer with the serif greeting + NumenMark (01-03), and the /home authed landing (01-04). The 4 UAT screenshots (desktop/mobile × empty/active) are in `.planning/phases/01-foundation-shell/uat/`. The full Vitest suite is green and the build compiles.
  </what-built>
  <how-to-verify>
    1. Confirm the dev server is running from the clean rebuild (Task 1: `rm -rf .next node_modules/.cache && npm run build`, then `npm run dev`). If colors look stale, the cache was not cleared — rebuild (Pitfall 3).
    2. Review the 4 screenshots in `.planning/phases/01-foundation-shell/uat/` AND open the live shell:
       - Empty home (`/home`): serif greeting "Ready to simulate your audience, [Name]?" + centered composer + NumenMark stele glyph; NO starter chips; NO Simulation list under the composer.
       - Active (`/analyze/[id]`): composer bottom-pinned; sidebar lists "Simulations" with score chips; no Pinned/Projects/Boards.
       - Desktop width: persistent sidebar, ~760px centered column, Cmd-backslash collapses to the icon rail (content shifts, no overlap).
       - Phone width: sidebar is a left drawer with backdrop; composer is full-width with gutters.
    3. Confirm the flat-warm matte look: neutral charcoal surfaces (NOT warm-brown, NOT cold #07080a), cream text (never pure white), terracotta coral as the LONE accent, hairline borders, the Raycast glass GONE everywhere including the sidebar (no 137deg gradient / blur / inset shine / glow).
    4. Sign off — or adjust — the [UAT] values: the exact charcoal hex ramp, the exact coral hex (~#d97757 target), the serif typeface (Newsreader as shipped, or swap to Source Serif 4), the score-zone green/amber/red, and the greeting micro-copy. These lock for rollout here; downstream phases reskin onto them.
  </how-to-verify>
  <resume-signal>Type "approved" to lock the flat-warm system for rollout, OR list the specific [UAT] adjustments (exact hex / serif swap / copy wording) to apply before re-review.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan runs tests, captures screenshots, and gates on human review. The only new-ish surface is the authenticated Playwright capture, which uses the same auth flow as a real user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | UAT screenshots of a real Simulation | mitigate | Prefer a seeded/known-viral Simulation for the active shot; if a real account row is used, it is the signed-in developer's own data (no third-party PII). The screenshots live under `.planning/` (committed docs) — avoid capturing any secret/token UI; the shell shows no secrets. |
| T-05-02 | Spoofing | authenticated Playwright capture | accept (mitigated by reusing existing validated code) | The capture logs in through the normal Supabase auth flow (MEMORY fixture pattern) — it does not bypass the gate or fabricate a session. No new auth surface. |
</threat_model>

<verification>
- `npm test` green + clean `npm run build` succeeds (THEME-01/03 structural proof; SHELL-07 + all prior tests).
- 4 UAT screenshots present in `.planning/phases/01-foundation-shell/uat/`.
- Human gate signs the [UAT] values (charcoal ramp, coral hex, serif typeface, score zones, greeting copy) — THEME-06 lock, the phase exit gate.
- This is a blocking `checkpoint:human-verify` (autonomous: false) — execution pauses for the human.
</verification>

<success_criteria>
- Full suite green + clean build compiles the flat-warm tokens.
- The 4-shot reviewable surface (desktop/mobile × empty/active) is captured.
- A human reviews the running shell and signs off (or returns a punch-list); on "approved" the flat-warm system is locked for rollout.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-05-SUMMARY.md` when done — record the gate outcome (approved + the locked [UAT] values, OR the punch-list applied).
</output>
