---
phase: 01-foundation-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true
requirements: [THEME-01, THEME-02, THEME-03, THEME-04, THEME-05]
must_haves:
  truths:
    - "The app background renders neutral charcoal (Frame A ~#262624), not cold #07080a"
    - "A serif font family is available as --font-serif and applied via the font-serif utility"
    - "No Raycast glass tokens (137deg gradient, glow, inset-shine) remain in globals.css"
    - "npm run build succeeds — globals.css compiles after the token migration"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Flat-warm @theme token SSOT — charcoal surfaces (hex), terracotta coral, --font-serif, no glass/glow tokens"
      contains: "--font-serif"
    - path: "src/app/layout.tsx"
      provides: "Newsreader serif wired via next/font/google as --font-serif alongside Inter"
      contains: "Newsreader"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "html className exposes --font-serif var consumed by @theme --font-serif"
      pattern: "font-serif"
    - from: "globals.css --color-background"
      to: "charcoal hex primitive"
      via: "semantic token repoint off the cold gray ramp"
      pattern: "--color-background"
---

<objective>
Migrate the Tailwind v4 `@theme` token SSOT in `globals.css` from the cold Raycast glass system to the flat-warm matte system (THEME-01/02/03/05), and wire an editorial serif as `--font-serif` (THEME-04). This is the visual foundation every other Phase-1 surface renders on — it MUST land first.

Two layers of the THEME-02 glass strip exist; this plan owns **Layer A (token level)**. Layer B (inline styles in `Sidebar.tsx`) is owned by plan 01-02.

All pixel-exact values (charcoal hex ramp, coral hex, serif typeface, score-zone colors) are **`[UAT]` per D-08** — author the Frame A starting values here; they lock at the THEME-06 human gate (plan 01-05), NOT in this plan.

Purpose: establish the single source of truth for the flat-warm look so sidebar, home, and composer all consume the same tokens.
Output: reworked `globals.css` (flat-warm), `layout.tsx` (serif wired), build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-foundation-shell/01-CONTEXT.md
@.planning/phases/01-foundation-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md

<interfaces>
<!-- Verified line numbers in the live files (RESEARCH C7-C11, re-verified 2026-06-13). -->
<!-- Executor: read globals.css + layout.tsx in full before editing; these are the load-bearing spans. -->

globals.css @theme — TARGETS (verified):
- L19  --color-coral-500: oklch(0.72 0.16 40);  /* Base #FF7F50 — mature toward terracotta ~#d97757, hue ~33-37 */
- L38  --color-gray-950: #07080a;                /* cold base — DO NOT overload the gray ramp; add charcoal primitives */
- L69  --color-background: var(--color-gray-950);   /* repoint -> charcoal */
- L71  --color-surface: #18191a;                     /* repoint -> charcoal composer tone */
- L72  --color-surface-elevated: #222326;
- L105 --font-sans: var(--font-inter), ui-sans-serif, system-ui, sans-serif;  /* add --font-serif alongside */
- L145 --shadow-glass: 0 8px 32px oklch(0 0 0 / 0.2), inset 0 1px 0 oklch(1 0 0 / 0.1);  /* DELETE */
- L146 --shadow-glow-accent: 0 0 20px oklch(0.72 0.16 40 / 0.3);  /* DELETE (vetoed glow) */
- L147 --shadow-button: …two inset white-shine layers…;  /* STRIP the two inset white layers, keep flat matte */
- L191 --gradient-coral: linear-gradient(135deg, …);  /* keep (it is coral, not glass) but reference the matured coral */
- L192 --gradient-card-bg: linear-gradient(137deg, #111214 …);  /* FLATTEN to solid charcoal */
- L194 --gradient-navbar: linear-gradient(137deg, …);  /* FLATTEN/REMOVE */
- L195 --gradient-glass:  linear-gradient(137deg, …);  /* REMOVE */
- L196 --gradient-feature: radial-gradient(… halo …);  /* REMOVE (vetoed ambient lighting) */

layout.tsx — existing Inter wiring pattern to mirror (50 lines, read in full):
  import { Inter } from "next/font/google";
  const inter = Inter({ subsets:["latin"], display:"swap", variable:"--font-inter" });
  <html className={inter.variable}>

Frame A starting values ([UAT] — from UI-SPEC § Color, lock at THEME-06):
- app bg #262624 | sidebar #1a1a18 | composer #1e1d1b | chip #2f2e2b
- cream text #ece7de (never pure white) | secondary #c2bdb4 | muted #8a857c
- coral terracotta ~#d97757 (oklch ~0.68 0.13 33) | accent-fg retune from #1a0f0a (verify AA)
- score zones: success oklch(0.68 0.17 145) | warning oklch(0.75 0.15 85) | error oklch(0.60 0.20 25)
- floating shadow (composer/popover ONLY): 0 10px 30px rgba(0,0,0,0.35)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-base @theme surfaces flat-warm + strip glass tokens (Layer A)</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (read ENTIRE file — the @theme block is the SSOT; know every token before editing)
    - .planning/phases/01-foundation-shell/01-UI-SPEC.md § Color + § Glass Strip Map (Layer A table) — the binding direction + Frame A starting values
    - .planning/phases/01-foundation-shell/01-RESEARCH.md § "Flat-warm token re-base (THEME-01/02 / R2)" + Pitfall 1 (oklch L<0.15 miscompile) + Pitfall 4 (inline glass survives token reskin)
  </read_first>
  <behavior>
    - After build, the compiled CSS contains no `linear-gradient(137deg` (gradient-glass/navbar/card-bg removed/flattened)
    - After build, the compiled CSS contains no `--shadow-glow-accent` and no `--shadow-glass`
    - `--color-background` resolves to a charcoal hex (Frame A #262624), authored as HEX not oklch (Pitfall 1)
    - Score-zone tokens (--color-success/warning/error) remain defined (they are data colors, kept per THEME-03)
  </behavior>
  <action>
    Add NEW charcoal surface primitives as exact HEX (oklch L<0.15 compiles wrong — Pitfall 1): `--color-charcoal-app: #262624`, `--color-charcoal-sidebar: #1a1a18`, `--color-charcoal-composer: #1e1d1b`, `--color-charcoal-chip: #2f2e2b`. Do NOT overload the existing gray ramp (gray-950 etc. still feed data/score components) — add new primitives and repoint semantics.
    Repoint semantics: `--color-background` (L69) -> `var(--color-charcoal-app)`; `--color-surface` (L71) -> `var(--color-charcoal-composer)`. Retune cream text tokens toward `#ece7de`/`#c2bdb4`/`#8a857c` (never pure white, D-02) — apply to the foreground/foreground-muted semantic tokens.
    Mature coral (L19): `--color-coral-500` -> terracotta, oklch is FINE here (L~0.68, above the miscompile zone) — `oklch(0.68 0.13 33)` ≈ #d97757. Keep coral distinct from alert-red (error hue ~25). Adjust the coral 100..900 ramp around the new base proportionally.
    STRIP glass tokens (THEME-02 Layer A): DELETE `--gradient-glass` (L195), `--gradient-navbar` (L194), `--gradient-feature` (L196); FLATTEN `--gradient-card-bg` (L192) to a solid charcoal value; DELETE `--shadow-glass` (L145) and `--shadow-glow-accent` (L146); strip the two `inset … white` shine layers from `--shadow-button` (L147), keeping a flat matte border/shadow treatment.
    Add a single whisper-soft floating shadow token for composer/popovers ONLY (D-05): e.g. `--shadow-float: 0 10px 30px rgba(0,0,0,0.35)`. No glow, no halo, no inset shine anywhere.
    Keep the existing `--color-border` hairline scale (0.06 / 0.10 hover) and the spacing/duration/ease tokens untouched (THEME-05 reuses them).
    Mark every pixel value with a `/* [UAT] Frame A — locks at THEME-06 */` comment so the gate reviewer knows what is provisional.
    All values here are the Frame A STARTING point (D-08) — do not present them as final.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-rework && grep -v '^[[:space:]]*/\*\|^[[:space:]]*\*' src/app/globals.css | grep -c 'linear-gradient(137deg' | grep -qx 0 && grep -v '^[[:space:]]*/\*\|^[[:space:]]*\*' src/app/globals.css | grep -c -- '--shadow-glow-accent' | grep -qx 0 && echo GLASS_TOKENS_STRIPPED</automated>
  </verify>
  <acceptance_criteria>
    - comment-filtered grep of src/app/globals.css contains zero `linear-gradient(137deg` occurrences
    - comment-filtered grep of src/app/globals.css contains zero `--shadow-glow-accent` and zero `--shadow-glass`
    - src/app/globals.css contains `#262624` (charcoal app bg) and the string `[UAT]` (provisional markers present)
    - --color-success, --color-warning, --color-error are still present (score-zone data colors retained)
  </acceptance_criteria>
  <done>Charcoal surfaces (hex), terracotta coral, flat shadows, and stripped glass tokens are in globals.css; every pixel value carries an [UAT] comment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Newsreader serif as --font-serif (THEME-04)</name>
  <files>src/app/layout.tsx, src/app/globals.css</files>
  <read_first>
    - src/app/layout.tsx (read in full — mirror the existing Inter next/font/google -> --font-inter wiring exactly)
    - src/app/globals.css L105 (--font-sans declaration — add --font-serif directly alongside it)
    - .planning/phases/01-foundation-shell/01-UI-SPEC.md § Typography + § Design System (font rows) — serif is voice-moments ONLY (greeting/hero); Inter for ALL data
    - .planning/phases/01-foundation-shell/01-RESEARCH.md § "Wiring the serif font" code example
  </read_first>
  <behavior>
    - After build, `--font-serif` is defined in the compiled CSS @theme layer
    - The `<html>` element exposes both the Inter and Newsreader CSS variables
    - A `font-serif` Tailwind utility resolves to the Newsreader stack (Tailwind v4 generates it from the --font-serif theme var)
  </behavior>
  <action>
    In `src/app/layout.tsx`: import `Newsreader` from `next/font/google` alongside `Inter`. Configure `Newsreader({ subsets:["latin"], display:"swap", variable:"--font-serif", style:["normal","italic"], weight:["400"] })` — italic is needed for the name accent in the greeting (UI-SPEC display row); 400 only (UI-SPEC restraint). Add `newsreader.variable` to the `<html>` className alongside `inter.variable`.
    In `src/app/globals.css` (next to L105 `--font-sans`): add `--font-serif: var(--font-serif), ui-serif, Georgia, serif;` inside `@theme`.
    Newsreader is the Frame-A-matching STARTING typeface (D-08 A3) — the swap to Source Serif 4 is a one-line change at the THEME-06 gate if the human prefers it. Do NOT hard-lock the choice; leave a `/* [UAT] serif typeface — Newsreader (Frame A); may swap to Source Serif 4 at THEME-06 */` comment.
    Do NOT apply the serif to any element here (greeting usage is plan 01-03). This task only makes `font-serif` AVAILABLE.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-rework && grep -q Newsreader src/app/layout.tsx && grep -q -- '--font-serif' src/app/globals.css && echo SERIF_WIRED</automated>
  </verify>
  <acceptance_criteria>
    - src/app/layout.tsx imports `Newsreader` from `next/font/google` and adds `newsreader.variable` to the html className
    - src/app/globals.css contains `--font-serif:` inside the `@theme` block
    - no element consumes `font-serif` yet (greeting is plan 01-03)
  </acceptance_criteria>
  <done>--font-serif (Newsreader) is wired and available as a font-serif utility; no element consumes it yet.</done>
</task>

<task type="auto">
  <name>Task 3: Clean-rebuild + prove globals.css compiles</name>
  <files>(no source edits — clean rebuild + build gate)</files>
  <read_first>
    - .planning/phases/01-foundation-shell/01-RESEARCH.md Pitfall 3 (dev-server CSS cache masks token changes — must clean-rebuild)
    - CLAUDE.md "Known Technical Issues" (Tailwind v4 oklch + Lightning CSS + dev-server cache gotchas)
  </read_first>
  <action>
    Clear stale compiled CSS (Pitfall 3): `rm -rf .next node_modules/.cache`. Then run `npm run build` and confirm it succeeds — this is the structural proof that the flat-warm `@theme` migration compiles (THEME-01/02/03 are otherwise manual-only/visual). If the build fails on a token, fix the token (most likely an oklch dark surface that should be hex — Pitfall 1).
    Do NOT start a long-lived dev server in this task (the running-shell review happens at the THEME-06 gate, plan 01-05, after a clean rebuild).
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-rework && rm -rf .next node_modules/.cache && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (Next build completes; globals.css compiles with the new tokens)
    - No build error referencing globals.css or an unparseable color token
  </acceptance_criteria>
  <done>A clean `npm run build` succeeds with the flat-warm tokens compiled — the foundation is provably valid for downstream plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits CSS tokens + a font import only — it crosses no trust boundary. No input, no auth, no data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | next/font/google (Newsreader) build-time fetch | accept | Build-time Google Font fetch is a first-party Next.js mechanism (no runtime npm install, no third-party registry — RESEARCH Package Legitimacy Audit: zero external installs). No supply-chain surface introduced. |
| T-01-02 | Denial of Service | globals.css malformed token breaks the build | mitigate | Task 3 clean-rebuild + `npm run build` gate catches a broken token before it ships; oklch-dark-surface authored as HEX per Pitfall 1 to avoid a silent miscompile. |
</threat_model>

<verification>
- Comment-filtered grep proves no `linear-gradient(137deg` / `--shadow-glow-accent` / `--shadow-glass` remain in `globals.css` (THEME-02 Layer A).
- `--font-serif` present in `globals.css`; `Newsreader` imported in `layout.tsx` (THEME-04).
- `npm run build` exits 0 after a clean rebuild (THEME-01/03 structural proof).
- Exact color/serif perception is `[UAT]` — verified by eyes at the THEME-06 gate (plan 01-05), not here.
</verification>

<success_criteria>
- Flat-warm charcoal surfaces (hex), terracotta coral, and stripped glass tokens are in `globals.css`, every pixel value marked `[UAT]`.
- `--font-serif` (Newsreader) is wired and available; no element consumes it yet.
- A clean `npm run build` succeeds.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-01-SUMMARY.md` when done.
</output>
