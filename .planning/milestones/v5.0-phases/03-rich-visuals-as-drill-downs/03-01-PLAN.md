---
phase: 03-rich-visuals-as-drill-downs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/reading/__tests__/reading.panels.test.tsx
  - src/components/reading/__tests__/fixtures/reading-fixture.ts
  - src/components/reading/__tests__/reskin-matte.test.ts
autonomous: true
requirements: [READ-09]

must_haves:
  truths:
    - "A render test exists for each of the 5 drill-down panels asserting no throw on real data"
    - "A degradation test exists asserting each panel falls to PanelEmpty (not throw, not grey-cell) on null/thin data"
    - "A matte-lint test asserts the transplanted chart files contain no glow/blur/old-coral/137deg-gradient"
  artifacts:
    - path: "src/components/reading/__tests__/reading.panels.test.tsx"
      provides: "Per-panel render + degradation coverage for the 5 rich panels (the Wave-0 gap)"
      min_lines: 60
    - path: "src/components/reading/__tests__/fixtures/reading-fixture.ts"
      provides: "Empty-data fixture helpers (heatmap:null / empty-personas / empty-segments)"
      contains: "makeReadingResult"
    - path: "src/components/reading/__tests__/reskin-matte.test.ts"
      provides: "Static grep gate for matte regressions across the transplanted charts"
      min_lines: 20
  key_links:
    - from: "src/components/reading/__tests__/reading.panels.test.tsx"
      to: "src/components/reading/reading.tsx"
      via: "renders <Reading/> + opens each panel, asserts the rich visual mounts"
      pattern: "reading\\.tsx|PanelContent|setPanel"
---

<objective>
Author the Wave-0 test scaffold the rest of Phase 3 verifies against. Today the SVG leaf charts (`RetentionChart`, `SegmentTable`, `PersonaGraph`, `CraftFilmstrip`, `ScoreDistribution`, `StatTile`) have NO direct render tests — this is the coverage gap `03-VALIDATION.md` flags. This plan creates the failing/guard tests FIRST so the wiring plans (03-04/03-05) and reskin plans (03-02/03-03) have a real gate.

Purpose: SC-2 is the phase spine — every transplanted visual must render real engine output with no throw and no grey-cell fallback. That contract is only enforceable if a test asserts it. The matte-lint test cheaply catches reskin regressions (SC-3) before the expensive human-UAT gate.
Output: `reading.panels.test.tsx` (per-panel render + degradation), extended `reading-fixture.ts` (empty-data helpers), `reskin-matte.test.ts` (static grep gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rich-visuals-as-drill-downs/03-VALIDATION.md

<interfaces>
<!-- Existing fixture factory + the panels the tests must drive. Use these directly. -->

From src/components/reading/__tests__/fixtures/reading-fixture.ts (existing exports — EXTEND, don't rewrite):
```typescript
export function makeReadingResult(over: Partial<PredictionResult> = {}): PredictionResult;   // L123 — full happy-path result
export function makeUnavailableResult(over: Partial<PredictionResult> = {}): PredictionResult; // L158
export function makePartialResult(over: Partial<PredictionResult> = {}): PredictionResult;     // L169
export function makeApolloNullResult(over: Partial<PredictionResult> = {}): PredictionResult;  // L178 — dims null
```

From src/components/reading/reading.tsx — the container under test:
```typescript
export function Reading();  // subscribes usePermalinkAnalysis(); panel state is internal useState<PanelId|null>
// PanelContent switch cases (current, pre-P3): 'hook' | 'retention' | 'shareability' | 'personas'
// 03-04 will add 'score'; tests in THIS plan target the 4 existing + leave a ready slot for 'score'
```

Existing reading container test (the pattern to mirror for mounting + opening panels):
```
src/components/reading/__tests__/reading.test.tsx        — container render + panel-open via fireEvent
src/components/reading/__tests__/reading.degraded.test.tsx — D-13 degradation patterns (mirror these)
```
Test env pragma required at top of component test files: `/** @vitest-environment happy-dom */`
</interfaces>

<reskin_targets>
<!-- The files the matte-lint test must scan. These are the transplant set. -->
src/components/board/verdict/ScoreDistribution.tsx
src/components/board/audience/RetentionChart.tsx
src/components/board/content-analysis/CraftFilmstrip.tsx
src/components/board/audience/SegmentTable.tsx
src/components/board/_kit/PersonaGraph.tsx
src/components/board/_kit/StatTile.tsx
src/components/board/_kit/DataTable.tsx
src/components/board/_kit/KeyframeImage.tsx
src/components/board/audience/RetentionPlayer.tsx
</reskin_targets>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend reading-fixture.ts with empty-data helpers</name>
  <files>src/components/reading/__tests__/fixtures/reading-fixture.ts</files>
  <read_first>
    - src/components/reading/__tests__/fixtures/reading-fixture.ts (the file being modified — read the full existing factory shape, esp. how `heatmap`, `personas`, `segments`, `behavioral_predictions`, `apollo_reasoning` are constructed in `makeReadingResult` L123+)
    - src/lib/engine/types.ts (HeatmapPayload L41–79, PredictionResult L300+ — to keep override types valid)
  </read_first>
  <behavior>
    - makeEmptyHeatmapResult(): a PredictionResult whose `heatmap` is null → retention/personas panels must degrade
    - makeEmptyPersonasResult(): heatmap present but `heatmap.personas: []` and no sim results → personas panel degrades
    - makeEmptySegmentsResult(): heatmap present but `heatmap.segments: []` → retention curve/table degrade
    - makeNoBehavioralResult(): `behavioral_predictions` absent → shareability tiles empty
    - Each helper composes on TOP of makeReadingResult(over) so unrelated fields stay valid (no hand-built partial result)
  </behavior>
  <action>
    Add four exported factory helpers to reading-fixture.ts, each delegating to the existing `makeReadingResult(over)` and overriding only the field under test. `makeEmptyHeatmapResult` overrides `heatmap: null`. `makeEmptyPersonasResult` overrides `heatmap` to the existing shape with `personas: []` and clears `persona_simulation_results`. `makeEmptySegmentsResult` overrides `heatmap.segments: []` (keep personas so only the segment-derived paths go empty). `makeNoBehavioralResult` overrides `behavioral_predictions: undefined`. Reuse `makeApolloNullResult` for the apollo-null degradation (already exists — do NOT duplicate). Keep every helper typed `Partial<PredictionResult>`-driven so a schema drift fails at compile. Do NOT add prose/UI — fixtures only.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `reading-fixture.ts` exports `makeEmptyHeatmapResult`, `makeEmptyPersonasResult`, `makeEmptySegmentsResult`, `makeNoBehavioralResult`
    - `npx tsc --noEmit` has no new errors from the fixture file
    - Existing reading tests still pass (`npx vitest run src/components/reading` exits 0)
  </acceptance_criteria>
  <done>Four empty-data fixture helpers exist, typed, delegating to makeReadingResult; existing suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author reading.panels.test.tsx (per-panel render + degradation)</name>
  <files>src/components/reading/__tests__/reading.panels.test.tsx</files>
  <read_first>
    - src/components/reading/reading.tsx (the container — `PanelContent` switch L208–223, `PanelEmpty` L361–363, the gauge/cloud/driver-row tap sources that open panels)
    - src/components/reading/__tests__/reading.test.tsx (the established mount + panel-open pattern — mirror its fireEvent approach for opening a panel)
    - src/components/reading/__tests__/reading.degraded.test.tsx (the established degradation assertion pattern)
    - src/components/reading/__tests__/fixtures/reading-fixture.ts (the helpers from Task 1)
    - src/hooks/queries/use-permalink-analysis.ts (so the test mocks the single subscription the way reading.test.tsx already does)
  </read_first>
  <behavior>
    - For panels that exist TODAY (hook, retention, shareability, personas): open each → assert it renders without throwing on makeReadingResult()
    - Each of those panels on its empty fixture → asserts the "Not available for this read." PanelEmpty copy is present, and the panel does NOT throw and does NOT render a grey-cell box
    - Leave clearly-marked placeholder test blocks (it.todo or skipped) for the rich-visual assertions that 03-02/03-03/03-04/03-05 will activate — so the file is the agreed contract surface, not a moving target
    - The 'score' panel test is authored as `it.todo`/skipped here (the panel doesn't exist until 03-04) with a comment pointing to 03-04
  </behavior>
  <action>
    Create `reading.panels.test.tsx` with the `/** @vitest-environment happy-dom */` pragma. Mock `usePermalinkAnalysis` to return `{ id: 'test-id', data, isLoading: false }` (mirror reading.test.tsx's mock setup exactly — same vi.mock target). For each existing panel (hook, retention, shareability, personas): render `<Reading/>`, open the panel via its tap source (driver row / persona cloud — use the same `getByTestId`/role queries reading.test.tsx uses), assert the panel testid mounts with `makeReadingResult()`. Then re-render with the matching empty fixture (Task 1 helpers + `makeApolloNullResult`) and assert `getByText('Not available for this read.')` is present (no throw). Add `it.todo('score panel opens from gauge onOpen → ScoreDistribution mounts (wired in 03-04)')` and `it.todo` placeholders for each rich-visual mount the later plans activate. Do NOT assert against chart-internal SVG structure yet (the rich charts aren't wired) — those todos belong to 03-04/03-05. Keep assertions behavior-level (testid present, copy present, no throw).
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/reading.panels.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `reading.panels.test.tsx` exists and runs (exits 0 — active tests pass, todos reported as pending)
    - The four existing panels each have a render assertion + a PanelEmpty degradation assertion
    - `score`-panel and rich-visual-mount assertions exist as `it.todo`/skipped with comments naming the activating plan
    - File contains the `happy-dom` pragma
  </acceptance_criteria>
  <done>Per-panel render + degradation test exists and is green; rich-visual + score assertions staged as todos for downstream plans.</done>
</task>

<task type="auto">
  <name>Task 3: Author reskin-matte.test.ts (static grep gate)</name>
  <files>src/components/reading/__tests__/reskin-matte.test.ts</files>
  <read_first>
    - .planning/phases/03-rich-visuals-as-drill-downs/03-RESEARCH.md (§ Reskin Surface Area Tier-2 table — the exact violation tokens: `#FF7F50`, `box-shadow: 0 0`, `backdrop-filter`, `linear-gradient(137deg`)
    - src/components/board/verdict/ScoreDistribution.tsx + src/components/board/audience/RetentionChart.tsx (read the CURRENT violation lines so the test starts RED for the files 03-02/03-03 will clean — confirm the strings are present today)
    - vitest.config.ts (default env is `node` — a pure-fs test needs no happy-dom pragma)
  </read_first>
  <behavior>
    - For each file in the transplant set, read its source text and assert it contains NONE of: `#FF7F50`, `backdrop-filter`, `linear-gradient(137deg`, and no `box-shadow` value matching the `0 0 <n>px` glow shape
    - The matte-lint runs in the default `node` env (fs read), NOT happy-dom
    - The test starts RED for files that still carry violations (ScoreDistribution, RetentionChart, CraftFilmstrip, PersonaGraph, RetentionPlayer, KeyframeImage) — it goes GREEN as 03-02/03-03 reskin them; this is the intended Wave-0 gate
  </behavior>
  <action>
    Create `reskin-matte.test.ts` (node env — no pragma). Use `node:fs` `readFileSync` to load each transplant file by absolute-from-`src` path (resolve via `path.join(__dirname, ...)` or the `@/` source root). Define the forbidden patterns: literal `#FF7F50` (old coral), `backdrop-filter`, `linear-gradient(137deg` (Raycast glass), and a regex for glow `box-shadow` of the form `0 0 \d` (the `0 0 Npx` halo — distinguish from legitimate `inset 0 1px` hairlines by anchoring on the `0 0 ` offset pair followed by a non-zero blur). Iterate the file list with `it.each`, asserting each file matches none. IMPORTANT grep-gate hygiene: scan ONLY the transplant files listed in `<reskin_targets>`; do NOT count occurrences in comments as a separate pass — the assertion is presence/absence of the forbidden render strings. Mark the currently-violating files with a leading comment "RED until 03-02/03-03" so the executor of those plans knows this gate must flip GREEN. Do NOT include `reading.tsx` HookPanel (already on tokens) in the forbidden-glow scan beyond confirming it's clean.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/reskin-matte.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `reskin-matte.test.ts` exists and executes
    - It asserts absence of `#FF7F50`, `backdrop-filter`, `linear-gradient(137deg`, and `box-shadow: 0 0 Npx` glows across the 9 transplant files
    - It is RED today for the known-violating files (ScoreDistribution / RetentionChart / CraftFilmstrip / PersonaGraph / RetentionPlayer / KeyframeImage) — confirming the gate has teeth; 03-02/03-03 turn it green
    - `git grep -nE "0 0 [1-9]" -- src/components/reading/__tests__/reskin-matte.test.ts` shows the glow regex is present
  </acceptance_criteria>
  <done>Static matte-lint exists, scans the transplant set for the four forbidden render strings, and is RED for the not-yet-reskinned files (the intended Wave-0 gate that 03-02/03-03 flip GREEN).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → source files | Tests read source as data (fs) + render components; no untrusted external input crosses here. Presentation-layer, engine frozen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering/Info-disclosure | `reading.tsx` `PanelId` allow-list | accept (this plan) | This plan authors the TEST that 03-04 satisfies (closed-union safety). No code path here reflects a key; the `score`-panel union extension + its test land in 03-04. The `Record<PanelId,string>` compile-enforcement is asserted there. |
| READ-10 | Info-disclosure | drill-down field reads | accept | The existing `reading.no-cut-data.test.tsx` already guards no-raw-spread; this plan does not add new field reads. 03-04/03-05 extend that guard to the new panels. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH § Package Legitimacy Audit: not triggered — zero external deps). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run src/components/reading` runs green for the active tests (the matte-lint is intentionally RED for the not-yet-reskinned files — that is the Wave-0 gate state, documented in Task 3 `<done>`).
- `npx tsc --noEmit` introduces no new errors from the fixture/test additions.
- The three artifacts exist with the contract surfaces (per-panel render+degradation, empty-data helpers, matte grep gate).
</verification>

<success_criteria>
- `reading.panels.test.tsx` covers render + PanelEmpty degradation for the existing panels and stages todos for the rich-visual + `score` assertions.
- `reading-fixture.ts` exports the four empty-data helpers, typed and delegating to `makeReadingResult`.
- `reskin-matte.test.ts` enforces the four forbidden matte strings across the 9 transplant files and is RED for the files 03-02/03-03 will clean.
- No new tsc errors; existing suite unaffected.
</success_criteria>

<output>
Create `.planning/phases/03-rich-visuals-as-drill-downs/03-01-SUMMARY.md` when done.
Note in the summary: the matte-lint RED file list (so 03-02/03-03 know exactly which files must flip the gate GREEN), and which rich-visual `it.todo`s remain for 03-04/03-05 to activate.
</output>
