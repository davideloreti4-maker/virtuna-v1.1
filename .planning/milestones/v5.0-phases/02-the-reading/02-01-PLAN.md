---
phase: 02-the-reading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/reading/__tests__/fixtures/reading-fixture.ts
  - src/components/reading/__tests__/score-gauge.test.tsx
  - src/components/reading/__tests__/drill-sheet.test.tsx
  - src/components/reading/score-gauge.tsx
  - src/components/reading/drill-sheet.tsx
autonomous: true
requirements: [READ-02, READ-07]
must_haves:
  truths:
    - "The hero score renders as a flat stroked arc gauge, fill zone-colored by bandTone(score) (>=70 green / 40-69 amber / <40 red), number + band word centered"
    - "The gauge animates fill via CSS transition only when usePrefersReducedMotion() is false; snaps to final when reduced"
    - "A generic DrillSheet opens as a bottom sheet on mobile and a right drawer on desktop, receiving ANY panel as children (the Phase-3/5 mount point)"
  artifacts:
    - path: "src/components/reading/score-gauge.tsx"
      provides: "Hand-rolled inline-SVG arc gauge; fill is a prop (Phase-4-ready); zone color from bandTone SSOT"
      contains: "bandTone"
    - path: "src/components/reading/drill-sheet.tsx"
      provides: "Generic children-based Sheet wrapper, side switches via useIsMobile (bottom mobile / right desktop)"
      contains: "useIsMobile"
    - path: "src/components/reading/__tests__/fixtures/reading-fixture.ts"
      provides: "Shared PredictionResult fixture builder for all reading component tests (re-exports/extends board fixtures.antiVirality)"
      exports: ["makeReadingResult"]
  key_links:
    - from: "src/components/reading/score-gauge.tsx"
      to: "src/components/board/verdict/verdict-derive.ts"
      via: "import bandTone (SSOT zone color)"
      pattern: "bandTone"
    - from: "src/components/reading/drill-sheet.tsx"
      to: "src/components/ui/sheet.tsx"
      via: "import Sheet/SheetContent (Radix Dialog, side prop)"
      pattern: "from '@/components/ui/sheet'"
---

<objective>
Lay the two foundational presentation primitives every content block depends on — the hero **ScoreGauge** (READ-02) and the generic **DrillSheet** disclosure container (READ-07, the Phase-3/5 mount point) — plus the Nyquist Wave-0 test scaffold (`src/components/reading/__tests__/` + a shared `PredictionResult` fixture) that all subsequent reading component tests reuse.

Purpose: These three unblock the parallel content-block plans (02-02/03/04). The gauge is the hero's centerpiece; the DrillSheet is the single most-reused decision in the milestone (Phase 3 rich visuals AND Phase 5 chat both mount into it); the fixture is the test foundation. Build them deterministic, token-correct, and Phase-4-ready (gauge fill is a prop).
Output: `score-gauge.tsx`, `drill-sheet.tsx`, the `__tests__/` directory with a shared fixture + two passing test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-the-reading/02-CONTEXT.md
@.planning/phases/02-the-reading/02-RESEARCH.md
@.planning/phases/02-the-reading/02-PATTERNS.md
@.planning/phases/02-the-reading/02-UI-SPEC.md
@.planning/phases/02-the-reading/02-VALIDATION.md

<interfaces>
<!-- Verbatim contracts the executor needs. Use directly — no codebase exploration. -->

Pure helpers (REUSE VERBATIM — import, do NOT copy):
From src/components/board/verdict/verdict-derive.ts (L58-67):
```typescript
export type ScoreTone = 'good' | 'warn' | 'crit' | 'neutral';
// ≥70 'good' (green) · 40–69 'warn' (amber) · <40 'crit' (red) — THE SSOT for the gauge fill
export function bandTone(score: number): ScoreTone;
```
From src/components/board/verdict/verdict-constants.ts (L61-65):
```typescript
// Strong/Mid/Low — the locked-mock vocabulary. NOT bandLabel() (verdict-derive L25 = wrong words).
export function bandFromScore(score: number): 'Strong' | 'Mid' | 'Low';
```

Vendored Radix primitive (the DrillSheet mechanism — no new dep, no vaul):
From src/components/ui/sheet.tsx — `SheetContent` accepts `side: "top"|"right"|"bottom"|"left"` with correct slide anims + responsive width; Radix Dialog ships focus-trap/Esc/scroll-lock/aria-modal. Exports: `Sheet, SheetContent, SheetHeader, SheetTitle, SheetClose, SheetTrigger`.

Hooks (in-repo):
From src/hooks/useIsMobile.ts: `export function useIsMobile(): boolean` (MOBILE_BREAKPOINT=768; returns false SSR→first-paint, flips after mount — harmless for a portal that only opens post-hydration).
From src/hooks/usePrefersReducedMotion.ts: `export function usePrefersReducedMotion(): boolean` (defaults true SSR).

Tokens (globals.css @theme, THEME-06 LOCKED — reference, do NOT add tokens):
`--color-success` oklch(0.68 0.17 145) · `--color-warning` oklch(0.75 0.15 85) · `--color-error` oklch(0.60 0.20 25) · `--color-border` (6% hairline) · `--color-surface` #1e1d1b · `--color-foreground` #ece7de (cream) · `--ease-out-cubic` cubic-bezier(0.215,0.61,0.355,1) · `--shadow-float`.

Shared test fixture source to extend:
src/components/board/verdict/__tests__/fixtures/prediction-result.ts exports `fixtures` (incl. `fixtures.antiVirality` — a full PredictionResult). The proven minimal-fixture pattern (verdict-derive.test.ts L23): `(over: Partial<PredictionResult> = {}) => ({ ...base }) as unknown as PredictionResult`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 test scaffold + shared reading fixture</name>
  <files>src/components/reading/__tests__/fixtures/reading-fixture.ts</files>
  <read_first>
    - .planning/phases/02-the-reading/02-VALIDATION.md (the req→test map + Wave 0 requirements)
    - src/components/board/verdict/__tests__/fixtures/prediction-result.ts (the existing `fixtures` object incl. `fixtures.antiVirality` full PredictionResult — the base to extend)
    - src/components/board/verdict/__tests__/verdict-derive.test.ts (L22-30 — the `(over) => ({...}) as unknown as PredictionResult` minimal-fixture pattern to mirror)
    - src/components/board/verdict/__tests__/AntiViralityHeader.test.tsx (L39-100 — the degraded/override fixture-spread pattern the degraded tests will model on)
    - src/lib/engine/types.ts (L300-393 PredictionResult honesty flags; L840-861 ApolloDimension/ApolloRewrite; L41-79 HeatmapPayload personas; L573-591 CounterfactualSuggestionItem)
  </read_first>
  <action>
    Create `src/components/reading/__tests__/fixtures/reading-fixture.ts` exporting `makeReadingResult(over: Partial<PredictionResult> = {}): PredictionResult`. Base it on the existing board `fixtures.antiVirality` (import from `@/components/board/verdict/__tests__/fixtures/prediction-result`) spread with a healthy default: `analysis_unavailable: false`, `partial_analysis: false`, `anti_virality_gated: false`, `overall_score: 71`, an `apollo_reasoning` with a length-6 `dimensions[]` (order `[hook, retention, clarity, share_pull, substance, credibility]`, scores e.g. 87/55/72/64/70/80) and 2 `rewrites[]` (`{original, variant, lever_fixed}`), a `heatmap` with `personas[]` (≥3, mixed `slot_type`), `weighted_top_dropoff_t: 8` (SECONDS), `weighted_completion_pct: 0.57`, and `counterfactuals.suggestions[]` with ≥3 `type:'fix'` items (`{type, headline, detail, timestamp_ms, signal_anchor}`). Apply `over` last so each test overrides only the fields it asserts on. Also export named scenario helpers: `makeUnavailableResult()` (analysis_unavailable:true, overall_score:0), `makePartialResult()` (partial_analysis:true), `makeApolloNullResult()` (apollo_reasoning:null). Cast via `as unknown as PredictionResult` to avoid wiring the ~40 unrelated fields — mirror the verdict-derive.test.ts pattern exactly.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading 2>&1 | tail -5 ; test -f src/components/reading/__tests__/fixtures/reading-fixture.ts && echo "FIXTURE OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/reading/__tests__/fixtures/reading-fixture.ts` exists and exports `makeReadingResult`, `makeUnavailableResult`, `makePartialResult`, `makeApolloNullResult`.
    - `makeReadingResult().apollo_reasoning.dimensions` has length 6 in order [hook, retention, clarity, share_pull, substance, credibility].
    - `makeReadingResult().heatmap.weighted_top_dropoff_t === 8` and `makeReadingResult().counterfactuals.suggestions.filter(s => s.type==='fix').length >= 3`.
    - `makeUnavailableResult().analysis_unavailable === true && makeUnavailableResult().overall_score === 0`; `makeApolloNullResult().apollo_reasoning === null`.
    - `npx vitest run src/components/reading` exits 0 (no test files yet besides those added in Tasks 2-3 within this plan, all green).
  </acceptance_criteria>
  <done>The reading test directory + shared fixture exist; all subsequent reading component tests import from `reading-fixture.ts`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScoreGauge — hand-rolled SVG arc, zone fill from bandTone (READ-02)</name>
  <files>src/components/reading/score-gauge.tsx, src/components/reading/__tests__/score-gauge.test.tsx</files>
  <read_first>
    - .planning/phases/02-the-reading/02-PATTERNS.md (§score-gauge.tsx assignment — the hand-rolled build + geometry; CORRECTION #2 amber spans 40-69)
    - .planning/phases/02-the-reading/02-RESEARCH.md (Focus 2 — the full SVG `stroke-dasharray` recipe + Phase-4 animation contract)
    - .planning/phases/02-the-reading/02-UI-SPEC.md (§Visual Primitives > Arc gauge — geometry 120/96px, 8px stroke, 270° sweep, Inter 36px/600/tabular-nums; §Typography band word = sans)
    - src/components/board/_kit/PersonaGraph.tsx (L179-196 — the inline-SVG `<circle>` + token `stroke` idiom to mirror)
    - src/components/board/verdict/verdict-derive.ts (L58-67 — bandTone, the zone-color SSOT)
    - src/components/board/verdict/verdict-constants.ts (L61-65 — bandFromScore, the band word)
    - src/hooks/usePrefersReducedMotion.ts (the reduced-motion gate)
  </read_first>
  <behavior>
    - Test: score=85 → fill stroke = var(--color-success); aria-label contains "Score 85 of 100, Strong".
    - Test: score=55 → fill stroke = var(--color-warning) (40-69 amber band, CORRECTION #2 — NOT red); band word "Mid".
    - Test: score=30 → fill stroke = var(--color-error); band word "Low".
    - Test: the rendered number text is "85"/"55"/"30" (Inter, tabular-nums) inside the gauge.
    - Test (reduced-motion): when usePrefersReducedMotion mocked true, the fill circle has NO `transition` inline style; when false, it has `transition: stroke-dasharray ...`.
    - Test (axe): the gauge container passes vitest-axe (role="img" + aria-label).
  </behavior>
  <action>
    Create `'use client'` `src/components/reading/score-gauge.tsx` exporting `ScoreGauge({ score, size = 120, stroke = 8 }: { score: number; size?: number; stroke?: number })`. Hand-roll inline SVG per RESEARCH Focus 2: compute `r=(size-stroke)/2`, `C=2*Math.PI*r`, `arcLen=C*0.75` (270° dial), `dash=arcLen*(clamp(score,0,100)/100)`. Track `<circle>` stroke=`var(--color-border)` with `strokeDasharray={`${arcLen} ${C}`}`; fill `<circle>` stroke = the zone color from `bandTone(score)` mapped `{ good:'var(--color-success)', warn:'var(--color-warning)', crit:'var(--color-error)', neutral:'var(--color-warning)' }`, `strokeLinecap="round"`, `strokeDasharray={`${dash} ${C}`}`. Rotate the svg `-rotate-[225deg]` (opening at dial bottom). Number Inter 36px/600/`tabular-nums` cream (`text-foreground`) + band word from `bandFromScore(score)` below in sans medium 16px same zone color, centered via absolute overlay. Set `role="img"` + `aria-label={`Score ${score} of 100, ${bandFromScore(score)}`}`. Apply `style={ reduced ? undefined : { transition: 'stroke-dasharray 700ms var(--ease-out-cubic)' } }` on the fill circle (`reduced = usePrefersReducedMotion()`). NO glow/halo/filter (matte). The `score` stays a prop — Phase 4 drives it from `useAnalysisStream`; do NOT internalize a stream. Write the test file `score-gauge.test.tsx` covering the 6 behaviors above, importing `makeReadingResult` only if a result is needed (the gauge takes a raw `score`, so tests pass literal scores). Use `vi.mock` for `usePrefersReducedMotion` to assert both branches.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/score-gauge.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `score-gauge.test.tsx` exits 0.
    - For score=55 the fill `<circle>` uses `var(--color-warning)` (assert via the rendered stroke attribute) — proves the 40-69 amber band, not red.
    - aria-label for score=85 is exactly `Score 85 of 100, Strong`.
    - With usePrefersReducedMotion→true the fill circle has no `transition` in its style; with →false it does.
    - `grep -n "filter\|drop-shadow\|boxShadow\|0 0 10px" src/components/reading/score-gauge.tsx` returns nothing (matte — no glow).
    - `grep -c "useAnalysisStream\|usePermalinkAnalysis" src/components/reading/score-gauge.tsx` returns 0 (fill is a prop; no internal data subscription).
  </acceptance_criteria>
  <done>ScoreGauge renders a flat zone-colored arc with centered number + band word, animates only when motion is allowed, and exposes `score` as a Phase-4-drivable prop.</done>
</task>

<task type="auto">
  <name>Task 3: DrillSheet — generic children-based Sheet, side switches via useIsMobile (READ-07)</name>
  <files>src/components/reading/drill-sheet.tsx, src/components/reading/__tests__/drill-sheet.test.tsx</files>
  <read_first>
    - .planning/phases/02-the-reading/02-PATTERNS.md (§drill-sheet.tsx assignment — the generic container API + flat-warm reskin checklist; the `?panel=` discretion note)
    - .planning/phases/02-the-reading/02-RESEARCH.md (Focus 1 — the DrillSheetProps API, the library comparison, the useIsMobile hydration note, the `?panel=` shallow-sync recommendation + downgradeable fallback)
    - .planning/phases/02-the-reading/02-UI-SPEC.md (§Two-tier disclosure — heavy → Sheet bottom/right; flat-warm reskin: drop shadow-lg, hairline border, charcoal surface, no inset shine, no blur)
    - src/components/ui/sheet.tsx (the vendored Radix Dialog Sheet — `side` prop, SheetContent/SheetHeader/SheetTitle exports, slide anims)
    - src/hooks/useIsMobile.ts (the mobile/desktop switch)
  </read_first>
  <action>
    Create `'use client'` `src/components/reading/drill-sheet.tsx` exporting `DrillSheet({ open, onOpenChange, title, children }: { open: boolean; onOpenChange: (o: boolean) => void; title: string; children: React.ReactNode })`. Render `<Sheet open onOpenChange><SheetContent side={useIsMobile() ? 'bottom' : 'right'} ...>` with `<SheetHeader><SheetTitle>{title}</SheetTitle></SheetHeader>` (title REQUIRED — Radix warns if absent) and a scrollable `<div className="overflow-y-auto ...">{children}</div>`. Flat-warm reskin (UI-SPEC): className uses `bg-surface` (or `bg-background` → charcoal) + `border-[--color-border]` hairline + `sm:max-w-[560px]` + `data-[side=bottom]:max-h-[85vh]` + `rounded-t-[16px] sm:rounded-none`; DROP `shadow-lg` (overlay `bg-black/50` already dims) — add `--shadow-float` ONLY if it must read as floating; NO inset-shine; NO `backdrop-filter` (matte; if ever needed it must be inline `style` per CLAUDE.md Lightning-CSS gotcha — but prefer none). Keep the component panel-agnostic: it owns ONLY the surface + the mobile/desktop side switch + a11y; the caller chooses the child by panelId. Do NOT build a panel registry. For Phase 2, ship pure-ephemeral open/close (driven by a `useState` at the container in 02-05) — the `?panel=` shallow URL sync is a downgradeable enhancement (UI-SPEC sanctions ephemeral for v1); if added later, validate the param against the closed allow-list `hook`/`retention`/`shareability`/`personas` before opening (Security V5) and use `window.history.pushState` (App Router has no `router.push(url,{shallow})`). Write `drill-sheet.test.tsx`: render `<DrillSheet open title="Hook detail"><p>panel body</p></DrillSheet>`, assert the title + child render; render with `open={false}` and assert the child is not in the document; use RTL `userEvent` to trigger `onOpenChange(false)` via Esc/close and assert the callback fires; assert axe passes.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/drill-sheet.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `drill-sheet.test.tsx` exits 0.
    - `DrillSheet` accepts arbitrary `children` and renders them inside the sheet when `open` (proves the generic Phase-3/5 mount contract).
    - The provided `title` renders as the SheetTitle (a11y — no Radix missing-title warning in test output).
    - `grep -n "side={" src/components/reading/drill-sheet.tsx` shows the side bound to `useIsMobile()` (bottom mobile / right desktop), not hardcoded.
    - `grep -n "shadow-lg\|backdrop-filter\|boxShadow\|inset" src/components/reading/drill-sheet.tsx` returns no `shadow-lg`/inset-shine/blur (flat-warm matte).
    - `grep -c "RetentionChart\|PersonaGraph\|panelRegistry\|switch (panel" src/components/reading/drill-sheet.tsx` returns 0 (panel-agnostic — no panel knowledge baked in).
  </acceptance_criteria>
  <done>DrillSheet is a generic, flat-warm, a11y-correct disclosure container that opens bottom-on-mobile / right-on-desktop and receives any panel as children — the seam Phase 3 and Phase 5 mount into.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route param `id` → API | already IDOR-defended server-side by `GET /api/analysis/[id]`; this plan only consumes downstream data shapes, no new boundary |
| future `?panel=` query → DrillSheet open | reflected URL value could be used as a key; mitigated by allow-list before it reaches this code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | drill-sheet.tsx `?panel=` (if URL sync added) | mitigate | DrillSheet stays panel-agnostic; any caller mapping `?panel=` MUST validate against the closed allow-list (`hook`/`retention`/`shareability`/`personas`) before opening — documented in the action; never reflect the raw value or use it as untrusted markup key |
| T-02-02 | Information disclosure | score-gauge.tsx | accept | renders only the numeric `score` prop + derived band word; no PredictionResult fields, no telemetry — no leak surface |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | zero new dependencies (RESEARCH Package Legitimacy Audit: gate not triggered; `vaul`/`react-circular-progressbar` explicitly NOT added) — nothing to vet |
</threat_model>

<verification>
- `npx vitest run src/components/reading` green (3 new test files: fixture has none, score-gauge, drill-sheet).
- Full suite stays green at wave merge: `npm test` (~1967 tests must remain green).
- No new dependency added: `git diff package.json` shows no change.
</verification>

<success_criteria>
- ScoreGauge renders a flat stroked arc, fill zone-colored by `bandTone()` SSOT (amber owns 40-69), number + band word centered, motion gated on `usePrefersReducedMotion`, `score` a Phase-4-drivable prop, no glow.
- DrillSheet is a generic `children`-based container, bottom-on-mobile/right-on-desktop via `useIsMobile`, flat-warm (no shadow-lg/inset/blur), a11y-correct, panel-agnostic.
- `src/components/reading/__tests__/` exists with a shared `makeReadingResult` fixture all later plans reuse.
</success_criteria>

<output>
Create `.planning/phases/02-the-reading/02-01-SUMMARY.md` when done.
</output>
