---
phase: 02-the-reading
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/reading/driver-rows.tsx
  - src/components/reading/__tests__/driver-rows.test.tsx
  - src/components/sidebar/Sidebar.tsx
autonomous: true
requirements: [READ-05, READ-06]
must_haves:
  truths:
    - "Three always-visible driver rows render in fixed funnel order Hook → Retention → Shareability, each label + horizontal bar (0-100 fill) + value"
    - "Each row bar fills by its 0-100 Apollo dimension score; bars are neutral cream; only the single weakest of the three takes its zone color + ⚠"
    - "The Retention row value column shows the drop timestamp ⚠ formatTime(weighted_top_dropoff_t) in SECONDS, while its bar still fills by the retention score"
    - "Each row is a ≥44px tap target that calls onRowTap(panelId) to open the DrillSheet"
    - "When apollo_reasoning is null (no dimensions) the rows degrade gracefully — never render 0 as a score"
    - "Sidebar score chips use the score-zone tokens (--color-success/warning/error) via bandTone SSOT, not emerald/amber Tailwind"
  artifacts:
    - path: "src/components/reading/driver-rows.tsx"
      provides: "3 driver rows from ApolloDimension[] (0-100), fixed funnel, neutral bars only-weakest-colored, Retention drop-time value, row tap → onRowTap"
      contains: "weighted_top_dropoff_t"
    - path: "src/components/sidebar/Sidebar.tsx"
      provides: "Score chips unified onto score-zone tokens (P1 follow-up)"
      contains: "color-success"
  key_links:
    - from: "src/components/reading/driver-rows.tsx"
      to: "src/components/board/audience/audience-derive.ts"
      via: "formatTime (SECONDS variant) for the Retention drop value"
      pattern: "formatTime"
    - from: "src/components/reading/driver-rows.tsx"
      to: "src/components/board/verdict/verdict-derive.ts"
      via: "bandTone for the weakest-lever zone color"
      pattern: "bandTone"
---

<objective>
Build the **DriverRows** — the three always-visible levers (READ-05/06): Hook, Retention, Shareability, each a `label | bar | value` row. This is a NEW component (NOT a FactorBars reuse — RESEARCH correction #1: FactorBars is 0-10 with 5 axis-mismatches). It borrows only FactorBars' grid markup and rebuilds for 0-100 Apollo dimensions, neutral cream bars, only-weakest-colored, and the Retention special readout (drop timestamp). Also closes the carried-forward P1 follow-up: **unify the sidebar score chips** onto the score-zone tokens.

Purpose: The rows are "the levers you change next time" (brief §2.4) — the calm, comparable middle of the Reading, with the eye landing on the one weakest lever. The sidebar unify makes the whole app's score color language consistent on the locked THEME-06 zone tokens.
Output: `driver-rows.tsx` + test; `Sidebar.tsx` score-chip color edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-the-reading/02-CONTEXT.md
@.planning/phases/02-the-reading/02-RESEARCH.md
@.planning/phases/02-the-reading/02-PATTERNS.md
@.planning/phases/02-the-reading/02-UI-SPEC.md
@.planning/phases/02-the-reading/02-VALIDATION.md

<interfaces>
<!-- Verbatim contracts. Use directly. -->

Data contract (engine types.ts L840-848, VERIFIED 0-100):
```typescript
// ApolloDimension: { name: 'hook'|'retention'|'clarity'|'share_pull'|'substance'|'credibility'; band: 'strong'|'mid'|'weak'; score: 0–100; lever: string; evidence: string }
// apollo_reasoning.dimensions[] is length-6, order [hook, retention, clarity, share_pull, substance, credibility]
// Driver rows = find hook, retention, share_pull   |   Deeper read (02-04) = clarity, substance, credibility
// Retention drop value: heatmap.weighted_top_dropoff_t (SECONDS, types.ts L483)
```

Pure helpers (REUSE VERBATIM):
From src/components/board/audience/audience-derive.ts (L49-54): `export function formatTime(seconds: number): string` — the SECONDS variant. `weighted_top_dropoff_t` is in SECONDS → use THIS. (Do NOT use TopFixesList.formatTime(ms) — CORRECTION #4 / Landmine 6; mixing renders 0:08 as 0:00.)
From src/components/board/verdict/verdict-derive.ts (L58-67): `bandTone(score)` → 'good'|'warn'|'crit' → the weakest-lever zone color (NOT coral).

FactorBars grid markup to BORROW ONLY (FactorBars.tsx L25-52) — the 3-col grid + 5px rounded-full bar:
```
grid items-center gap-[15px] grid-cols-[130px_1fr_42px] sm:grid-cols-[152px_1fr_42px]   // widen value col for "⚠ 0:08"
h-[5px] rounded-full bg-white/[0.06] (track) > inner div width:`${pct}%`
```
STRIP from FactorBars (L44-49): the `linear-gradient(90deg,#E8703F,#FF7F50)` coral gradient + `boxShadow:'0 0 10px rgba(255,127,80,0.5)'` glow → neutral cream fill, NO glow. Do NOT sort (FactorBars sorts strongest-first; rows are FIXED funnel order). Do NOT add a `/{max_score}` suffix (that's the 0-10 contract).

Sidebar current score tone (Sidebar.tsx L77-80, the thing to unify):
```typescript
function scoreTone(score) { if (score==null) return 'text-foreground-muted'; if (score>=80) return 'text-emerald-400/70'; if (score<50) return 'text-amber-400/70'; return 'text-foreground-muted'; }
// chip renders at L397-401 with data-testid="sidebar-score-chip"
```

Test fixture: `import { makeReadingResult, makeApolloNullResult } from './fixtures/reading-fixture'` (02-01).

Tokens: `--color-success`/`-warning`/`-error` (zone) · `--color-foreground`/`-secondary`/`-muted` (cream) · `--color-border` (6% track).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DriverRows — 3 levers, 0-100 bars, Retention drop-time, only-weakest-colored (READ-05/06)</name>
  <files>src/components/reading/driver-rows.tsx, src/components/reading/__tests__/driver-rows.test.tsx</files>
  <read_first>
    - .planning/phases/02-the-reading/02-PATTERNS.md (§driver-rows.tsx assignment — the 5 mismatches, borrow-grid-only, the data contract, the D-06 Retention readout, the D-13 degrade)
    - .planning/phases/02-the-reading/02-RESEARCH.md (correction #1 FactorBars 0-10 vs 0-100; correction #4 formatTime seconds-vs-ms; Landmine 2; Landmine 6)
    - .planning/phases/02-the-reading/02-UI-SPEC.md (§Visual Primitives > Driver rows — grid, 5-6px bar, fill = dimension score, only-weakest zone color, Retention value = drop point, fixed funnel, ≥44px tap target; §Color driver-row bar = neutral cream)
    - src/components/board/verdict/FactorBars.tsx (L25-52 grid markup to borrow; L44-49 the coral gradient + glow to STRIP)
    - src/components/board/audience/audience-derive.ts (L49-54 formatTime SECONDS)
    - src/components/board/verdict/verdict-derive.ts (L58-67 bandTone)
    - src/lib/engine/types.ts (L840-848 ApolloDimension; L483 weighted_top_dropoff_t SECONDS)
  </read_first>
  <behavior>
    - Test: 3 rows render in fixed order Hook, Retention, Shareability (assert label order in the DOM), regardless of which scores highest (do NOT sort).
    - Test: Hook bar fill width = hook dimension score% (e.g. score 87 → width:"87%"); Shareability bar = share_pull score%.
    - Test: Retention bar fills by the RETENTION dimension score (e.g. 55 → width:"55%"), while its VALUE column shows "⚠ 0:08" for weighted_top_dropoff_t=8 (SECONDS via audience-derive.formatTime — NOT 0:00).
    - Test: only the single weakest of the 3 (min score) gets its zone color (bandTone) + ⚠; the other two bars are neutral cream (assert the two non-weakest fills are NOT a zone token, and contain no #FF7F50 / boxShadow glow).
    - Test (degrade): given makeApolloNullResult() (apollo_reasoning null → no dimensions), the rows degrade gracefully — labels may show but NO "0" score/value is rendered for the bars (assert the literal "0" is absent from the row values region).
    - Test: each row is a button/tap target; clicking a row calls the onRowTap prop with that row's panelId ('hook'|'retention'|'shareability').
  </behavior>
  <action>
    Create `'use client'` `src/components/reading/driver-rows.tsx` exporting `DriverRows({ dimensions, dropT, onRowTap }: { dimensions: ApolloDimension[] | null | undefined; dropT: number | null | undefined; onRowTap: (panelId: 'hook'|'retention'|'shareability') => void })`. Find the three by name: `hook = dimensions?.find(d => d.name==='hook')`, `retention = ...'retention'`, `share = ...'share_pull'`. If dimensions is null/missing (D-13 degrade), render the row labels with NO bar/value (or omit the rows section) — NEVER render `0` as a score. Compute the weakest among the three present scores (`min`); only that row's bar + value takes its zone color via `bandTone(weakestScore)` (mapped to `var(--color-success|warning|error)`) + a `⚠` glyph in its value. All other bars use neutral cream fill (a cream/white-alpha `linear-gradient` or solid `--color-foreground` at low alpha — NOT #FF7F50, NO `boxShadow` glow). Borrow ONLY the FactorBars grid: `grid-cols-[130px_1fr_42px] sm:grid-cols-[152px_1fr_42px]`, `h-[5px] rounded-full` bar with track `--color-border`; widen the value col for `⚠ 0:08`. Bar `pct = dimension.score` (0-100, no `/10`). Row VALUE column: Hook = `{hook.score}`, Shareability = `{share.score}`, Retention = `⚠ ${formatTime(dropT)}` (audience-derive SECONDS variant) while its bar fills by `retention.score` (D-06 bar=how-good, value=where-it-breaks). Fixed funnel order Hook→Retention→Shareability — do NOT sort. Wrap each row in a `<button>` (or role=button) ≥44px tall (pad transparent area, don't inflate the visible bar) calling `onRowTap('hook'|'retention'|'shareability')`. Labels: Inter 14px/500 `text-foreground-secondary`; values 14px/600 tabular-nums. Write `driver-rows.test.tsx` covering the 6 behaviors with `makeReadingResult` (drop the dimensions in/out) and `makeApolloNullResult`.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/driver-rows.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `driver-rows.test.tsx` exits 0.
    - DOM label order is exactly Hook, Retention, Shareability (fixed funnel — no sort).
    - For weighted_top_dropoff_t=8, the Retention value renders the string `⚠ 0:08` (NOT `⚠ 0:00`) — proves the SECONDS formatTime.
    - The two non-weakest bars render neither a zone token color NOR `#FF7F50`/`boxShadow` glow; `grep -c "FF7F50\|E8703F\|0 0 10px\|boxShadow" src/components/reading/driver-rows.tsx` returns 0.
    - With apollo_reasoning null, the rows-values region contains no literal `0` (degrade — never the fake score).
    - Clicking the Hook row calls `onRowTap('hook')` (RTL userEvent assertion).
    - `grep -c "\.sort(" src/components/reading/driver-rows.tsx` returns 0 (no sort; fixed order).
  </acceptance_criteria>
  <done>DriverRows shows three fixed-order levers with 0-100 neutral bars, the single weakest zone-colored, the Retention drop timestamp from the SECONDS formatTime, each a tap target to the DrillSheet, degrading cleanly when dimensions are absent.</done>
</task>

<task type="auto">
  <name>Task 2: Unify sidebar score chips onto score-zone tokens (P1 follow-up)</name>
  <files>src/components/sidebar/Sidebar.tsx</files>
  <read_first>
    - .planning/STATE.md (Blockers/Concerns — the "Sidebar score chips use Tailwind emerald-400/amber-400, not the --color-success/warning/error score-zone tokens — unify in Phase 2" follow-up)
    - .planning/phases/02-the-reading/02-CONTEXT.md (carried-forward locks — "Sidebar score chips still use emerald/amber Tailwind — unify to the score-zone tokens in this phase")
    - .planning/phases/02-the-reading/02-UI-SPEC.md (§Color palette migration — sidebar score chips → score-zone tokens)
    - src/components/sidebar/Sidebar.tsx (L71-80 `scoreTone()` + L397-401 the chip render with data-testid="sidebar-score-chip")
    - src/components/board/verdict/verdict-derive.ts (L58-67 bandTone — the SSOT thresholds to align to)
  </read_first>
  <action>
    Edit `src/components/sidebar/Sidebar.tsx` `scoreTone()` (L77-80) to return the score-zone token classes instead of the raw Tailwind `emerald-400`/`amber-400`. Replace `text-emerald-400/70` → `text-success`, `text-amber-400/70` → `text-warning`, and add the red zone for the weak band → `text-error` (the current code only has green/amber; align to the 3-zone `bandTone` SSOT). Recommended: derive the tone from `bandTone(score)` (`good`→`text-success`, `warn`→`text-warning`, `crit`→`text-error`) so the sidebar chip color matches the hero gauge exactly — import `bandTone` from `@/components/board/verdict/verdict-derive`. Keep the `score==null → 'text-foreground-muted'` branch (the em-dash/remix case). The chip retains `data-testid="sidebar-score-chip"`. This is a color-token swap ONLY — do NOT change the chip layout, the rounding, the threshold semantics beyond aligning to bandTone, or any other sidebar behavior. Verify no other sidebar sub-file still hardcodes `emerald-400`/`amber-400` for scores (grep).
  </action>
  <verify>
    <automated>grep -n "emerald-400\|amber-400" src/components/sidebar/Sidebar.tsx ; grep -c "text-success\|text-warning\|text-error\|bandTone" src/components/sidebar/Sidebar.tsx ; npx vitest run src/components/sidebar 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "emerald-400\|amber-400" src/components/sidebar/Sidebar.tsx` returns 0 (no raw Tailwind score colors remain).
    - `scoreTone()` returns `text-success` / `text-warning` / `text-error` (the zone tokens), derived from `bandTone(score)`.
    - The `data-testid="sidebar-score-chip"` element still renders the rounded score.
    - Any existing sidebar tests still pass (`npx vitest run src/components/sidebar` exits 0); no layout/behavior change beyond the color tokens.
  </acceptance_criteria>
  <done>Sidebar score chips render on the locked THEME-06 score-zone tokens via the bandTone SSOT, matching the hero gauge color language; the P1 follow-up is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PredictionResult dimensions → DOM | only whitelisted dimension fields (name/score) rendered; no raw spread (READ-10) |
| row `panelId` → DrillSheet open | panelId is a closed union ('hook'|'retention'|'shareability'), not a reflected URL value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information disclosure | driver-rows.tsx | mitigate | reads only `dimensions[].name`/`.score` + `weighted_top_dropoff_t`; never spreads raw PredictionResult; degrade hides values (no fabricated 0) when dimensions absent |
| T-02-07 | Tampering | driver-rows onRowTap panelId | accept | panelId is a hardcoded closed union passed to the container; not derived from any reflected/untrusted input |
| T-02-SC | Tampering | npm installs | accept | zero new dependencies — nothing to vet |
</threat_model>

<verification>
- `npx vitest run src/components/reading` green (adds driver-rows test).
- `npx vitest run src/components/sidebar` green (sidebar chip unify — no regression).
- Full suite green at wave merge: `npm test`.
</verification>

<success_criteria>
- DriverRows: 3 fixed-funnel levers, 0-100 neutral cream bars, only-weakest zone-colored + ⚠, Retention value = ⚠ drop-time (SECONDS), each a ≥44px tap target → onRowTap, graceful degrade on null dimensions.
- Sidebar score chips unified onto `--color-success/warning/error` via `bandTone` SSOT (P1 follow-up closed).
</success_criteria>

<output>
Create `.planning/phases/02-the-reading/02-03-SUMMARY.md` when done.
</output>
