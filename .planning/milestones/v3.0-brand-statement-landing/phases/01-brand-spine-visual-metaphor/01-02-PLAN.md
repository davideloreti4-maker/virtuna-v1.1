---
phase: 01-brand-spine-visual-metaphor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - BRAND-BIBLE.md
autonomous: true
requirements:
  - VIZ-01
  - VIZ-02
  - VIZ-03
  - VIZ-04
  - VIZ-05
user_setup: []

must_haves:
  truths:
    - "BRAND-BIBLE.md retains all 351 existing lines (addendum APPENDS, never replaces)"
    - "New `## Visual Metaphor Lock` section exists after the existing footer"
    - "Hero behavioral-simulation visual concept is documented with reference URLs and reduced-motion fallback"
    - "Engine pipeline 4-stage visual concept is documented with motion library choice (motion/react LazyMotion + m + domAnimation, ~15KB) and reduced-motion fallback"
    - "Tech rationale documents Canvas 2D ~30KB + motion/react ~15KB = ~45KB total under the 50KB ceiling"
    - "Rejected alternatives table documents WebGL (overkill) and GSAP (license) rejection rationale"
    - "Scale affordances document hero/mobile/in-app embed sizes for both visuals"
    - "Hive viz file:line landmarks cited so Phase 2 plan researcher can read them directly"
    - "CONTEXT.md decisions covered: D-05 (lock fidelity = written spec + reference links, no Figma), D-06 (addendum describes both visuals in prose with web links), D-07 (Phase 2 finalizes execution; Phase 1 locks the concept), D-08 (split tech: Canvas 2D for hero + SVG/motion for pipeline), D-09 (~45KB total under 50KB ceiling), D-10 (prefers-reduced-motion honored for both visuals), D-11 (WebGL + GSAP rejected with documented rationale)"
  artifacts:
    - path: "BRAND-BIBLE.md"
      provides: "Visual language source-of-truth for Phase 2-6 hero motion + pipeline build decisions"
      min_lines: 450
      contains:
        - "## Visual Metaphor Lock"
        - "Behavioral Simulation"
        - "Engine"
        - "Canvas 2D"
        - "motion/react"
        - "LazyMotion"
        - "Reduced-motion fallback"
        - "Rejected Alternatives"
        - "Performance Budget"
        - "use-hive-animation"
        - "Phase 2 implementation notes (per CLAUDE.md)"
        - "Tailwind v4 oklch inaccuracy"
  key_links:
    - from: "BRAND-BIBLE.md §Visual Metaphor Lock §1 Hero"
      to: "src/components/hive/HiveCanvas.tsx + use-hive-animation.ts + use-canvas-resize.ts"
      via: "explicit file:line citations Phase 2 plan researcher reads"
      pattern: "use-hive-animation\\.ts:|use-canvas-resize\\.ts:|HiveCanvas\\.tsx:"
    - from: "BRAND-BIBLE.md §Visual Metaphor Lock §2 Pipeline"
      to: "Phase 2 hero motion implementation"
      via: "motion/react LazyMotion + m + domAnimation pattern"
      pattern: "LazyMotion|domAnimation|motion/react"
    - from: "BRAND-BIBLE.md §Visual Metaphor Lock §3 Rejected Alternatives"
      to: "Future-Davide / future-Claude reasoning audit"
      via: "WebGL + GSAP rejection rationale documented so this isn't re-litigated"
      pattern: "WebGL|GSAP|three\\.js|Three\\.js"
---

<objective>
Append a `## Visual Metaphor Lock` section to the existing 351-line `BRAND-BIBLE.md` documenting both Phase 1-locked visuals (hero behavioral-simulation + 4-stage engine pipeline), the split-tech implementation rationale (Canvas 2D ~30KB + motion/react ~15KB ≤ 45KB total), reference URLs, scale affordances, reduced-motion fallbacks, and rejected alternatives.

Purpose: Lock the visual language of Virtuna (per VIZ-01..05, D-05..D-11) as a written spec without sketches/Figma. Phase 2 plan researcher reads this addendum to choose particle behaviors, pipeline icons, motion easing, and bundle-budget compliance during build prep. Without this addendum, Phase 2 hero motion has no source of truth and risks library-import drift (Pitfall 5: framer-motion vs motion/react).

Output: Modified `BRAND-BIBLE.md` — existing 351 lines preserved verbatim, new `## Visual Metaphor Lock` section appended after line 351, footer date updated to 2026-05-10. File grows to ~450-500 lines total.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-brand-spine-visual-metaphor/01-CONTEXT.md
@.planning/phases/01-brand-spine-visual-metaphor/01-RESEARCH.md
@.planning/phases/01-brand-spine-visual-metaphor/01-PATTERNS.md
@.planning/phases/01-brand-spine-visual-metaphor/01-VALIDATION.md
@BRAND-BIBLE.md
@CLAUDE.md

<append_protocol>
<!-- CRITICAL — per RESEARCH.md Pitfall 4 + PATTERNS.md "Append protocol". -->

Existing BRAND-BIBLE.md tail (lines 336-351) — MUST NOT be modified before line 351:

```
---

## Resources

- [Token Reference](docs/tokens.md) -- All design tokens with values and usage guidance
- [Component API](docs/components.md) -- Complete props, variants, and code examples
- [Component Index](docs/component-index.md) -- Source files, showcase pages, exports per component
- [Accessibility](docs/accessibility.md) -- WCAG AA contrast requirements per component
- [Contributing](docs/contributing.md) -- How to add components, modify tokens, extend the system
- [Design Specs](docs/design-specs.json) -- Structured token export (W3C Design Tokens-adjacent)
- [Live Showcase](/showcase) -- Interactive component demos

---

*Virtuna Design System v2.3.5 -- Brand Bible*
*Last updated: 2026-02-08*
```

**Append target:** AFTER line 351 (after the date footer). Insert new `---` separator + `## Visual Metaphor Lock` section + updated footer.

**Footer update:** Replace the EXISTING two-line footer at lines 350-351 with:
- `*Virtuna Design System v2.3.6 -- Brand Bible*`
- `*Last updated: 2026-05-10*`

**DO NOT** insert mid-file. **DO NOT** delete or reorder any of the existing 348 lines (1-348). Only the date footer at lines 350-351 changes; everything else is append-only.
</append_protocol>

<style_invariants>
<!-- Match BRAND-BIBLE.md style verbatim (per PATTERNS.md "Style invariants"). -->
- Double-hyphen ` -- `, NEVER em-dash `—`
- H3 uses bold-emphasis labels followed by content
- 3-column tables: `| X | Y | Z |` no alignment markers
- Blockquote `> Internal note: …` for asides
- Backticked file paths with line ranges: `path/to/file.ts:42-49` or `path/to/file.ts:42-49, 121-193`
</style_invariants>

<hive_landmarks>
<!-- file:line citations Phase 2 plan researcher MUST be able to follow.
     Each verified by direct read in PATTERNS.md (Confidence: HIGH). -->

| Pattern | File:line |
|---------|-----------|
| DPR-aware ResizeObserver | `src/components/hive/use-canvas-resize.ts:43-100` |
| RAF + module-level "complete" flag | `src/components/hive/use-hive-animation.ts:42-49` |
| Animation lifecycle / completion | `src/components/hive/use-hive-animation.ts:121-193` |
| Ref-based render state | `src/components/hive/HiveCanvas.tsx:54-57, 147-186` |
| Reduced-motion early return | `src/components/hive/use-hive-animation.ts:65-69` |
| Color batching | `src/components/hive/hive-renderer.ts:240-298` |
| Skeleton render path | `src/components/hive/hive-renderer.ts:464-511` |
| Reduced-motion hook (reuse verbatim) | `src/hooks/usePrefersReducedMotion.ts:1-29` |
| Easing token | `docs/tokens.md` § Easings — `--ease-out-cubic: cubic-bezier(0.215, 0.61, 0.355, 1)` matches `easeOutCubic(t)` at `src/components/hive/use-hive-animation.ts:57` |
</hive_landmarks>

<reference_urls>
<!-- D-19 picks (per RESEARCH.md skeleton lines 339-343, 376-379). -->

**Hero (behavioral simulation) reference picks:**
- Stripe homepage: https://stripe.com (restrained motion, premium feel)
- Anthropic Claude: https://www.anthropic.com/claude (calm, lab-credible motion)
- Linear homepage: https://linear.app (minimal motion, structural feel)
- Raycast homepage: https://raycast.com (ambient gradient + screenshot composition)

**Pipeline (engine diagram) reference picks:**
- Linear Insights: https://linear.app/insights (pipeline-style structural stages)
- Vercel Observability: https://vercel.com/products/observability (section transitions, restrained motion)
- Stripe Atlas: https://stripe.com/atlas (clear step-by-step structural feel)
</reference_urls>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 1 Plan 02 introduces no executable code. Output is an addendum appended to a markdown document at the repo root, read by humans and grep at planning time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | T (Tampering) | `BRAND-BIBLE.md` (existing 351 lines) | mitigate | Acceptance criteria forbid modification of any line 1-348; only line 350-351 (existing footer) is replaced with updated date. Diff review at commit time confirms preservation. |
| T-01-02-02 | I (Information Disclosure) | `BRAND-BIBLE.md` addendum | accept | Document is intended to be public-readable inside the repo; no secrets, no PII, no credentials. Reference URLs are public marketing pages. |

**Per phase security_threat_model:** No code introduced -- markdown document only. Threat surface = zero.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Append Visual Metaphor Lock addendum to BRAND-BIBLE.md</name>
  <files>BRAND-BIBLE.md</files>
  <read_first>
    - BRAND-BIBLE.md (FULL — confirm 351 lines, footer at lines 350-351, last existing H2 is `## Resources` at line 338, last existing `---` separator at line 348)
    - .planning/REQUIREMENTS.md lines 116-120 (VIZ-01..05 full text)
    - .planning/phases/01-brand-spine-visual-metaphor/01-CONTEXT.md (D-05..D-11, D-19 — concept-only, written-only, motion library choice, rejection rationale)
    - .planning/phases/01-brand-spine-visual-metaphor/01-RESEARCH.md §"Pattern 2: BRAND-BIBLE.md Addendum Structure" lines 312-412 (concrete skeleton + tech rationale)
    - .planning/phases/01-brand-spine-visual-metaphor/01-RESEARCH.md §"Code Examples" lines 637-756 (Example 1 pipeline pattern + Example 2 hero canvas skeleton — included verbatim in addendum as illustrative-only Phase 2 reference)
    - .planning/phases/01-brand-spine-visual-metaphor/01-PATTERNS.md §"BRAND-BIBLE.md (append-only addendum)" (style invariants + concrete append target)
    - src/components/hive/use-hive-animation.ts (lines 40-115, 121-193 — confirm `globalAnimationComplete` at line 43, `easeOutCubic` at 57, reduced-motion early return at 65-69)
    - src/components/hive/HiveCanvas.tsx (lines 54-57, 147-186 — ref-based render state pattern)
    - src/components/hive/use-canvas-resize.ts (lines 43-100 — DPR fallback chain)
    - src/hooks/usePrefersReducedMotion.ts (full 29 lines — reduced-motion hook reuse target)
    - docs/motion-guidelines.md (existing motion conventions for cross-reference)
  </read_first>
  <action>
**Step 1 — Verify file state.** Read `BRAND-BIBLE.md` and confirm: file is 351 lines, line 338 is `## Resources`, line 348 is `---`, line 350 is `*Virtuna Design System v2.3.5 -- Brand Bible*`, line 351 is `*Last updated: 2026-02-08*`. If any of these is different, STOP and report the discrepancy — do not proceed (Pitfall 4).

**Step 2 — Append the addendum.** Use the Edit tool to replace the existing footer block (lines 350-351) with the updated footer + addendum content. Do this in ONE Edit call:

OLD STRING (must match exactly, including blank line above):
```
*Virtuna Design System v2.3.5 -- Brand Bible*
*Last updated: 2026-02-08*
```

NEW STRING (the entire content below — preserves the date footer at the end):
```


---

## Visual Metaphor Lock

> Phase 1 of the Brand Statement Landing milestone locked the paired visual language of Virtuna. This section is the source of truth for both visuals; Phase 2 implementation honors these specs verbatim.

### 1. Hero -- Behavioral Simulation Visual

**Concept (VIZ-01):** Animated audience particles reacting to a video stimulus, aggregating into a confidence score. The particles represent simulated audience reactions; their convergence into a single shape (a number, a coral pill, or a video frame -- Phase 2 picks) communicates the prediction. One-shot animation on viewport entry, NOT a loop.

**Technical implementation (VIZ-04, D-08):** Canvas 2D, ~30 KB. No third-party motion library for the particle physics.

**Why Canvas (not WebGL or SVG):**

- Direct evidence base -- `src/components/hive/HiveCanvas.tsx` proves Canvas 2D handles 1300+ nodes at 60fps in this codebase.
- WebGL is overkill for 150-300 particles + adds bundle bloat (Three.js is 600 KB+ even tree-shaken).
- SVG degrades sharply past ~100 simultaneous animated DOM nodes.

**Patterns Phase 2 reuses verbatim:**

| Pattern | File:line | What it solves |
|---------|-----------|----------------|
| DPR-aware resize | `src/components/hive/use-canvas-resize.ts:43-100` | Crisp rendering on retina + Safari fallback chain |
| RAF-driven animation with module-level "complete" flag | `src/components/hive/use-hive-animation.ts:42-49, 121-193` | Animation plays once per session, doesn't replay on remount |
| Ref-based render state (no React re-renders per frame) | `src/components/hive/HiveCanvas.tsx:54-57, 147-186` | Camera, layout, interaction in refs; `render()` reads synchronously |
| Reduced-motion early return + full-visibility constant | `src/components/hive/use-hive-animation.ts:65-69, 122-130` | Static keyframe fallback in 4 lines |
| Color batching via `Map<colorKey, circles>` | `src/components/hive/hive-renderer.ts:240-298` | Fewer fillStyle changes per frame at high node counts |
| Reduced-motion hook (reuse verbatim) | `src/hooks/usePrefersReducedMotion.ts:1-29` | Returns boolean, defaults to `true` for SSR safety |
| Easing token | `docs/tokens.md` § Easings -- `--ease-out-cubic: cubic-bezier(0.215, 0.61, 0.355, 1)` matches `easeOutCubic(t)` at `src/components/hive/use-hive-animation.ts:57` | Consistent motion language with rest of design system |

**Reference visuals (D-19):**

- Stripe homepage hero gradient -- restrained motion, premium feel: https://stripe.com
- Anthropic Claude product page -- calm, lab-credible motion: https://www.anthropic.com/claude
- Linear homepage hero -- minimal motion, structural feel: https://linear.app
- Raycast homepage hero -- ambient gradient + screenshot composition: https://raycast.com

**What Phase 1 locks (concept, per D-05, D-06, D-07):**

- Particles aggregate into a confidence-score shape on viewport entry.
- Coral accent (#FF7F50) for the converged "majority" particles; Raycast neutrals for the minority.
- One-shot animation, never a loop (matches WORKS-02 pulse-once intent).
- Reduced-motion fallback: render the static converged keyframe with the confidence number visible. No animation. (D-10)

**What Phase 2 finalizes (execution, per D-07):**

- Exact particle count (suggest target 200-400 -- matches density of `hive` viz at smaller scale).
- Easing curves (suggest `easeOutCubic` matching `src/components/hive/use-hive-animation.ts:57`).
- Convergence vector field (geometry of the aggregation).
- Initial particle distribution (uniform / normal / perlin).
- Aggregation icon / center treatment (a number / a coral pill / a video frame).

**Reference Phase 2 skeleton (illustrative, NOT Phase 1 deliverable):**

```tsx
// Source pattern: src/components/hive/HiveCanvas.tsx (proven 1300+ nodes / 60fps)
'use client';
import { useCallback, useEffect, useRef } from "react";
import { useCanvasResize } from "@/components/hive/use-canvas-resize"; // REUSE VERBATIM
import { usePrefersReducedMotion } from "@/hooks/usePrefersReducedMotion"; // REUSE VERBATIM

let globalAnimationPlayed = false; // module-level — same pattern as use-hive-animation.ts:42-49

export function BehavioralSimulationHero() {
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const reducedMotion = usePrefersReducedMotion();
  // … RAF loop reading from sizeRef = useCanvasResize(canvasRef, render);
  // If reducedMotion || globalAnimationPlayed -> render static keyframe and return.
}
```

### 2. Pipeline -- Engine Diagram

**Concept (VIZ-02):** 4-stage horizontal diagram -- `video -> analyze -> simulate audience -> predict`. Subtle pulse motion fires once on viewport entry. Each stage has a 1-line label and an iconographic representation (icon source TBD in Phase 2 -- codebase has `@phosphor-icons/react` + `lucide-react` available).

**Technical implementation (VIZ-04, D-08, D-17):** SVG + `motion/react` (LazyMotion + `m` + `domAnimation` features), ~15 KB gzipped.

**Why SVG (not Canvas) for this visual:**

- Crisp at any DPI -- pipeline is small, label-led, structural.
- Stage labels are screen-reader accessible (Canvas 2D has no native a11y; pipeline labels are essential).
- One-shot motion fits SVG's strength -- no re-render every frame.

**Why `motion/react` (not framer-motion or plain CSS):**

- Already a project dep (`motion@^12.29.2`).
- Same API as framer-motion (which is also installed but used in legacy files); standardizing on `motion/react` reduces import inconsistency (per Pitfall 5).
- LazyMotion + `m` + `domAnimation` keeps bundle at ~15 KB instead of full 42.5 KB.
- Plain CSS keyframes considered and rejected: codebase standardized on `motion/react` for all scroll-reveal components (FadeIn, FadeInUp, SlideUp, StaggerReveal); mixing two animation paradigms increases maintenance cost.
- GSAP rejected: commercial license risk for paid plans + larger bundle than tree-shaken motion.

**Reference Phase 2 skeleton (illustrative, per RESEARCH.md Code Example 1):**

```tsx
'use client';
import { LazyMotion, domAnimation, m, useReducedMotion } from "motion/react";
import { useId } from "react";

const STAGES = [
  { label: "Video", icon: "video" },
  { label: "Analyze", icon: "analyze" },
  { label: "Simulate audience", icon: "audience" },
  { label: "Predict", icon: "predict" },
] as const;

export function EnginePipeline() {
  const reducedMotion = useReducedMotion();
  const titleId = useId();
  return (
    <LazyMotion strict features={domAnimation}>
      <svg role="img" aria-labelledby={titleId} viewBox="0 0 800 120" className="w-full h-auto">
        <title id={titleId}>4-stage prediction engine pipeline</title>
        {STAGES.map((stage, i) => (
          <m.g
            key={stage.label}
            initial={reducedMotion ? false : { opacity: 0.3, scale: 0.95 }}
            whileInView={{ opacity: 1, scale: 1 }}
            viewport={{ once: true, margin: "-80px" }}
            transition={{ duration: 0.6, delay: reducedMotion ? 0 : i * 0.15, ease: [0.215, 0.61, 0.355, 1] }}
          >
            {/* stage rendering */}
          </m.g>
        ))}
      </svg>
    </LazyMotion>
  );
}
```

**Reference visuals (D-19):**

- Linear Insights cycle graph -- pipeline-style with structural stages: https://linear.app/insights
- Vercel Observability product page -- section transitions with restrained motion: https://vercel.com/products/observability
- Stripe Atlas process diagrams -- clear step-by-step structural feel: https://stripe.com/atlas

**What Phase 1 locks (concept):**

- Exactly 4 stages: `video -> analyze -> simulate audience -> predict`.
- One-shot pulse on intersection-observer entry, NOT a continuous loop (matches WORKS-02).
- Reduced-motion fallback: static stages, no pulse, full visibility. (D-10)
- Stage labels are real text inside `<text>` SVG nodes for screen-reader access.

**What Phase 2 finalizes:**

- Specific icons per stage (Phosphor / Lucide picks).
- Pulse easing + duration (suggest `--ease-out-cubic` 600ms with 100-150ms stagger between stages, matching existing `StaggerReveal`).
- Connector style (line / arrow / dashed).
- Hover state on each stage (if any -- WORKS spec doesn't require).
- Mobile vertical-stack layout (per WORKS-05).

### 3. Scale Affordances (VIZ-03)

Both visuals must work at three scales: hero (desktop, full-bleed), mobile (≤640px), and future in-app embed (compact, ~400-600px wide).

| Visual | Hero (desktop) | Mobile (≤640px) | In-app embed |
|--------|----------------|-----------------|--------------|
| Hero behavioral simulation | Full-bleed canvas, 200-400 particles, ambient gradient backdrop | Reduce particle count to ~100; consider simplified static glyph if perf budget tight (per HERO-08) | 100-150 particles in card-bound area; aggregation target shrinks proportionally |
| Engine pipeline | Horizontal 4-stage diagram with icons + labels + pulse | Vertical 4-stage stack, same labels and icons, motion preserved or simplified (per WORKS-05) | Horizontal compact form, smaller icons, no labels (icon-only with tooltip) |

Reduced-motion fallback applies at all three scales: render static keyframe, no animation.

### 4. Rejected Alternatives (don't re-litigate)

| Considered | Rejected because | Reference |
|------------|------------------|-----------|
| WebGL hero | Bundle bloat (Three.js 600 KB+); no perceptual benefit at 150-400 particles | D-11 |
| GSAP for either visual | Commercial license cost on paid Business tier ($199/yr/dev) + larger than `motion/react` LazyMotion | D-11 |
| SVG for hero particles | DOM node count exceeds performant range past ~100 simultaneous particles | RESEARCH.md Pattern 2 |
| Canvas for pipeline | Stage labels need screen-reader access; Canvas has no native a11y | RESEARCH.md Pattern 2 |
| Plain CSS keyframes for pipeline | Codebase standardized on `motion/react`; mixing paradigms increases maintenance cost | RESEARCH.md Pattern 2 |
| Lottie animations | Asset-driven; doesn't fit hand-tuned canvas particle behavior; 50 KB+ even for simple animations | RESEARCH.md Pattern 2 |
| Figma frames or animated prototypes | D-05 chose written-only -- written spec + reference URLs is enough | D-05 |

### 5. Performance Budget (VIZ-04)

| Visual | Implementation | Gzipped Bundle Cost |
|--------|---------------|---------------------|
| Hero particles | Canvas 2D (no library, direct API) | ~30 KB (component code only) |
| Pipeline | SVG + `motion/react` LazyMotion + m + domAnimation | ~15 KB (motion subset, see motion.dev/docs/react-lazy-motion + motion.dev/docs/react-reduce-bundle-size) |
| **Total hero motion JS** | | **~45 KB** -- under VIZ-04 ceiling of 50 KB |

**Bundle pinning rule (per Pitfall 5):** All NEW motion code uses `motion/react` import path. The two `framer-motion` imports in `src/components/app/simulation/*.tsx` are legacy and slated for migration in a future cleanup. The vocab-lint script (Plan 04) flags new `from 'framer-motion'` imports at commit time.

### 6. How Phase 2-6 use this section

- Phase 2 plan researcher: read §1 + §2 to choose particle behaviors / pipeline icons / motion easing during build prep. Read all referenced file:line landmarks before writing the hero canvas component.
- Phase 2 executor: import `motion/react` (NOT `framer-motion`), use `LazyMotion` + `m` + `domAnimation` for SVG pipeline, reuse `useCanvasResize` and `usePrefersReducedMotion` verbatim from existing hooks.
- Phase 5 quality gates: bundle audit MUST verify hero JS ≤ 45 KB gzipped (under 50 KB ceiling). Lighthouse Performance MUST stay ≥ 95.
- Phase 6 reference audit: visuals reviewed against the reference URLs in §1 and §2.

### Phase 2 implementation notes (per CLAUDE.md)

Phase 2 build of these visuals must respect:

- **Tailwind v4 oklch inaccuracy** -- coral `#FF7F50` is referenced as exact hex above; do NOT round-trip through `oklch()` for colors with L < 0.15.
- **Lightning CSS strips backdrop-filter** -- N/A for canvas particles, but if any glass surface frames either visual, apply `backdropFilter` via React inline `style={{ ... }}`, not a CSS class.
- **Dev cache hygiene** -- when Phase 2 motion changes don't render, kill dev + clear `.next/` + clear `node_modules/.cache/` + clear browser cache.

Source: `CLAUDE.md` "Known Technical Issues" section.

---

*Virtuna Design System v2.3.6 -- Brand Bible*
*Last updated: 2026-05-10*
```

**Concrete value invariants (used by automated verification):**
- Section header literal: `## Visual Metaphor Lock`
- Phrases that MUST appear: `Behavioral Simulation`, `Engine Diagram` OR `4-stage`, `Canvas 2D`, `motion/react`, `LazyMotion`, `domAnimation`, `Reduced-motion fallback` (or `reduced-motion` × ≥3), `Rejected Alternatives`, `Performance Budget`, `WebGL`, `GSAP`, `Three.js`, `Scale Affordances`, `use-hive-animation`, `use-canvas-resize`, `HiveCanvas`, `usePrefersReducedMotion`, `easeOutCubic`, `~30 KB`, `~15 KB`, `~45 KB`
- Updated footer date: `2026-05-10` and version `v2.3.6`

**DO NOT:**
- Modify any of the existing 348 lines (1-348). Diff verification will catch this.
- Insert mid-file (Pitfall 4 explicitly forbids).
- Use em-dash `—` anywhere in the addendum (use double-hyphen ` -- `).
- Reference Figma frames, sketches, or Excalidraw diagrams (D-05).
- Recommend `framer-motion` import path for any NEW code (Pitfall 5).
- Document sound design or light-mode variants (deferred per CONTEXT.md).
  </action>
  <acceptance_criteria>
    - [ ] File exists and grew: `wc -l < BRAND-BIBLE.md` returns ≥ 450 (was 351, addendum adds ~150-200)
    - [ ] Existing structure preserved: `head -348 BRAND-BIBLE.md | grep -c "^## Resources$"` returns 1 (the existing Resources section is still there)
    - [ ] Existing footer line 350-351 has been replaced with new date: `grep -c "^\*Last updated: 2026-02-08\*$" BRAND-BIBLE.md` returns 0 AND `grep -c "^\*Last updated: 2026-05-10\*$" BRAND-BIBLE.md` returns 1
    - [ ] New version stamp: `grep -c "v2\.3\.6" BRAND-BIBLE.md` returns ≥ 1
    - [ ] VIZ-01 (Hero behavioral-simulation locked): `grep -cE "Behavioral Simulation|Behavioral-Simulation" BRAND-BIBLE.md` returns ≥ 1
    - [ ] VIZ-02 (4-stage engine pipeline locked): `grep -cE "4-stage|Engine.*Pipeline|Engine Diagram" BRAND-BIBLE.md` returns ≥ 1
    - [ ] VIZ-03 (Scale affordances): `grep -c "Scale Affordances" BRAND-BIBLE.md` returns 1
    - [ ] VIZ-04 (Tech rationale + bundle numbers): `grep -cE "Canvas 2D" BRAND-BIBLE.md` returns ≥ 1 AND `grep -cE "motion/react" BRAND-BIBLE.md` returns ≥ 2 AND `grep -c "LazyMotion" BRAND-BIBLE.md` returns ≥ 2 AND `grep -c "domAnimation" BRAND-BIBLE.md` returns ≥ 2 AND `grep -cE "30 ?KB" BRAND-BIBLE.md` returns ≥ 1 AND `grep -cE "15 ?KB" BRAND-BIBLE.md` returns ≥ 1 AND `grep -cE "45 ?KB" BRAND-BIBLE.md` returns ≥ 1
    - [ ] VIZ-05 (composite — both visuals + addendum located): `grep -c "^## Visual Metaphor Lock$" BRAND-BIBLE.md` returns 1
    - [ ] Section header on its own line: `grep -c "^## Visual Metaphor Lock$" BRAND-BIBLE.md` returns 1
    - [ ] Reduced-motion fallback documented for both visuals: `grep -ciE "reduced.?motion" BRAND-BIBLE.md` returns ≥ 4
    - [ ] Rejected alternatives present: `grep -c "Rejected Alternatives" BRAND-BIBLE.md` returns 1 AND `grep -c "WebGL" BRAND-BIBLE.md` returns ≥ 1 AND `grep -c "GSAP" BRAND-BIBLE.md` returns ≥ 1
    - [ ] Hive viz file:line landmarks cited: `grep -c "use-hive-animation" BRAND-BIBLE.md` returns ≥ 2 AND `grep -c "use-canvas-resize" BRAND-BIBLE.md` returns ≥ 2 AND `grep -c "HiveCanvas" BRAND-BIBLE.md` returns ≥ 1 AND `grep -c "usePrefersReducedMotion" BRAND-BIBLE.md` returns ≥ 2
    - [ ] Reference URLs present (Stripe, Linear, Anthropic, Vercel, Raycast): for each in "stripe.com" "linear.app" "anthropic.com" "vercel.com" "raycast.com", `grep -c "$url" BRAND-BIBLE.md` returns ≥ 1
    - [ ] Style: tail of file (after line 351) uses zero em-dash `—`: `tail -n +352 BRAND-BIBLE.md | grep -c "—"` returns 0
    - [ ] Pitfall 5 enforcement (NEW code uses motion/react not framer-motion in addendum examples): `tail -n +352 BRAND-BIBLE.md | grep -c "from 'framer-motion'"` returns 0 AND `tail -n +352 BRAND-BIBLE.md | grep -cE "from \"motion/react\"" ` returns ≥ 1
    - [ ] CLAUDE.md known-issues callout present in addendum: `grep -c "Phase 2 implementation notes (per CLAUDE.md)" BRAND-BIBLE.md` returns 1 AND `grep -c "Tailwind v4 oklch inaccuracy" BRAND-BIBLE.md` returns ≥ 1 AND `grep -c "Lightning CSS strips backdrop-filter" BRAND-BIBLE.md` returns ≥ 1 AND `grep -c "Dev cache hygiene" BRAND-BIBLE.md` returns ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>
      [ $(wc -l < BRAND-BIBLE.md) -ge 450 ] && \
      grep -q "^## Visual Metaphor Lock$" BRAND-BIBLE.md && \
      grep -q "Canvas 2D" BRAND-BIBLE.md && \
      grep -q "motion/react" BRAND-BIBLE.md && \
      grep -q "LazyMotion" BRAND-BIBLE.md && \
      grep -q "domAnimation" BRAND-BIBLE.md && \
      grep -qE "30 ?KB" BRAND-BIBLE.md && \
      grep -qE "15 ?KB" BRAND-BIBLE.md && \
      grep -qE "45 ?KB" BRAND-BIBLE.md && \
      grep -q "Scale Affordances" BRAND-BIBLE.md && \
      grep -q "Rejected Alternatives" BRAND-BIBLE.md && \
      grep -q "WebGL" BRAND-BIBLE.md && \
      grep -q "GSAP" BRAND-BIBLE.md && \
      grep -q "use-hive-animation" BRAND-BIBLE.md && \
      grep -q "use-canvas-resize" BRAND-BIBLE.md && \
      grep -q "HiveCanvas" BRAND-BIBLE.md && \
      grep -q "usePrefersReducedMotion" BRAND-BIBLE.md && \
      grep -q "^\*Last updated: 2026-05-10\*$" BRAND-BIBLE.md && \
      [ $(tail -n +352 BRAND-BIBLE.md | grep -c "—") -eq 0 ] && \
      [ $(tail -n +352 BRAND-BIBLE.md | grep -c "from 'framer-motion'") -eq 0 ] && \
      head -348 BRAND-BIBLE.md | grep -q "^## Resources$" && \
      grep -q "Phase 2 implementation notes (per CLAUDE.md)" BRAND-BIBLE.md && \
      grep -q "Tailwind v4 oklch inaccuracy" BRAND-BIBLE.md && \
      grep -q "Lightning CSS strips backdrop-filter" BRAND-BIBLE.md && \
      grep -q "Dev cache hygiene" BRAND-BIBLE.md
    </automated>
  </verify>
  <done>
    `BRAND-BIBLE.md` retains all 348 of its original first-348 lines verbatim. The original date footer (2026-02-08) is replaced with 2026-05-10 + v2.3.6. A new `## Visual Metaphor Lock` section is appended documenting: hero behavioral-simulation visual concept (Canvas 2D, ~30KB, references hive viz file:line landmarks, Stripe/Anthropic/Linear/Raycast reference URLs, reduced-motion fallback); engine pipeline visual concept (SVG + motion/react LazyMotion + m + domAnimation, ~15KB, Linear Insights/Vercel/Stripe Atlas reference URLs, reduced-motion fallback); scale affordances for hero/mobile/in-app embed; rejected alternatives table (WebGL, GSAP, SVG-for-hero, Canvas-for-pipeline, plain CSS, Lottie, Figma); performance budget table (~45KB total under 50KB ceiling); Phase 2-6 usage instructions. Tail of file uses zero em-dashes and zero `framer-motion` import strings.
  </done>
</task>

</tasks>

<verification>
Phase-level checks (run after task completes):

1. **Existing content preserved (anti-Pitfall-4):**
   ```bash
   head -348 BRAND-BIBLE.md | grep -q "^## Resources$" && \
   head -348 BRAND-BIBLE.md | grep -q "^# Virtuna Design System$"
   ```

2. **Footer correctly updated (replaces 2026-02-08, not duplicated):**
   ```bash
   [ $(grep -c "^\*Last updated: 2026-02-08\*$" BRAND-BIBLE.md) -eq 0 ] && \
   [ $(grep -c "^\*Last updated: 2026-05-10\*$" BRAND-BIBLE.md) -eq 1 ]
   ```

3. **Both visuals locked (VIZ-01, VIZ-02):**
   ```bash
   grep -qE "Behavioral Simulation|Behavioral-Simulation" BRAND-BIBLE.md && \
   grep -qE "Engine.*Pipeline|Engine Diagram|4-stage" BRAND-BIBLE.md
   ```

4. **Bundle numbers documented (VIZ-04):**
   ```bash
   grep -qE "30 ?KB" BRAND-BIBLE.md && \
   grep -qE "15 ?KB" BRAND-BIBLE.md && \
   grep -qE "45 ?KB" BRAND-BIBLE.md && \
   grep -q "LazyMotion" BRAND-BIBLE.md
   ```

5. **Library import discipline (Pitfall 5):**
   ```bash
   tail -n +352 BRAND-BIBLE.md | grep -qE 'from "motion/react"' && \
   [ $(tail -n +352 BRAND-BIBLE.md | grep -c "from 'framer-motion'") -eq 0 ]
   ```

6. **Hive viz landmarks cited:**
   ```bash
   for f in "use-hive-animation" "use-canvas-resize" "HiveCanvas" "usePrefersReducedMotion"; do
     grep -q "$f" BRAND-BIBLE.md || { echo "MISSING landmark: $f"; exit 1; }
   done
   ```
</verification>

<success_criteria>
- [ ] BRAND-BIBLE.md grew from 351 lines to ≥ 450 lines
- [ ] All 348 original lines (1-348) preserved verbatim
- [ ] Footer updated from 2026-02-08 to 2026-05-10 + v2.3.6
- [ ] VIZ-01 verified: hero behavioral-simulation visual concept in addendum
- [ ] VIZ-02 verified: 4-stage engine pipeline visual concept in addendum
- [ ] VIZ-03 verified: scale affordances table for hero/mobile/in-app
- [ ] VIZ-04 verified: tech rationale (Canvas 2D ~30KB + motion/react ~15KB ≤ 45KB ≤ 50KB ceiling)
- [ ] VIZ-05 verified: both visuals + addendum located
- [ ] D-11 verified: WebGL + GSAP rejection rationale documented
- [ ] D-10 verified: reduced-motion fallback for both visuals
- [ ] Pitfall 5 enforced: zero `from 'framer-motion'` strings in addendum, ≥1 `from "motion/react"`
- [ ] Hive viz file:line landmarks cited so Phase 2 plan researcher can read directly
</success_criteria>

<output>
After completion, create `.planning/phases/01-brand-spine-visual-metaphor/01-02-SUMMARY.md` with:
- File diff summary (lines before/after, sections added)
- VIZ-01..05 verification grep counts
- Reference URLs cited (D-19 picks per visual)
- Hive viz file:line landmarks cited (counted)
- Bundle numbers asserted (30/15/45 KB)
- Library import discipline check (zero framer-motion in addendum)
- Footer date diff (2026-02-08 → 2026-05-10, v2.3.5 → v2.3.6)
</output>
