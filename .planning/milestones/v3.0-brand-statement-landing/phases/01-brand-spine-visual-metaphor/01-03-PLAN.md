---
phase: 01-brand-spine-visual-metaphor
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 04
files_modified:
  - .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md
  - .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md
  - .planning/reference/societies-snapshot.html
  - .planning/reference/societies-text.txt
autonomous: false
requirements:
  - BRAND-05
  - BRAND-06
user_setup: []

must_haves:
  truths:
    - "01-PLAGIARISM-AUDIT.md exists and documents the diff between Wayback societies.io capture and current Virtuna v1.1 customer-facing copy"
    - "Plagiarism source domain is societies.io (NOT artificialsocieties.io — Wayback has zero captures for the latter)"
    - "01-REPLACEMENT-COPY.md exists with full replacement copy across all 7 viewports + onboarding + footer"
    - "Replacement copy obeys BRAND-SPINE.md §3 (preferred verbs) and §4 (banned table) — passes scripts/lint-vocab.mjs in dry-run"
    - "Hero copy in REPLACEMENT-COPY.md mirrors REQUIREMENTS.md HERO-01..05 verbatim or as Davide-redlined"
    - "Replacement copy contains zero verbatim phrases from the societies.io snapshot"
    - "Davide-batch sign-off block exists at end of REPLACEMENT-COPY.md (D-15: approve once at end)"
    - "acknowledged_rework_risk: true (Plan 03 Task 3 explicitly accepts cascade-rework risk per CONTEXT.md D-15; budget assumes one redline pass)"
    - "CONTEXT.md decisions covered: D-12 (audit scope = landing + onboarding + dashboard-visible), D-13 (audit excludes admin/dev/internal-docs/legal), D-14 (Wayback diff method + manual originality pass), D-15 (Davide approve-once batch sign-off at end), D-16 (hero copy in REQUIREMENTS.md is draft starting point — replacement copy mirrors locked hero)"
  artifacts:
    - path: ".planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md"
      provides: "Diff artifact — what was plagiarized, where, severity, replacement scope"
      min_lines: 80
      contains:
        - "societies.io"
        - "Wayback"
        - "## Method"
        - "## Findings"
        - "## Replacement scope"
    - path: ".planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md"
      provides: "Source-of-truth copy strings consumed by Phase 2-4 build tasks"
      min_lines: 120
      contains:
        - "Hero"
        - "Demo"
        - "How It Works"
        - "Three Surfaces"
        - "Science"
        - "Social Proof"
        - "Pricing"
        - "Footer"
        - "Onboarding"
        - "Davide approved"
        - "Your audience, simulated"
        - "VIRTUNA · A NUMEN MACHINES PRODUCT"
        - "Predict how your audience will respond"
    - path: ".planning/reference/societies-snapshot.html"
      provides: "Wayback HTML capture of societies.io as plagiarism reference (saved for reproducibility)"
    - path: ".planning/reference/societies-text.txt"
      provides: "Plain-text extraction of societies.io snapshot for diff input"
  key_links:
    - from: "01-REPLACEMENT-COPY.md hero block"
      to: ".planning/REQUIREMENTS.md HERO-01..05 (locked hero copy)"
      via: "verbatim mirroring of pre-headline, H1, sub-headline, subline, CTAs"
      pattern: "Predict how your audience will respond|Run a prediction|See the science|VIRTUNA"
    - from: "01-REPLACEMENT-COPY.md vocab compliance"
      to: ".planning/reference/BRAND-SPINE.md §3 + §4"
      via: "scripts/lint-vocab.mjs (Plan 04) dry-runs against the doc"
      pattern: "viral|AI|go viral|users (must NOT appear in copy strings)"
    - from: "01-PLAGIARISM-AUDIT.md replacement scope table"
      to: "Phase 2 (BUILD-09 swap), Phase 3-4 (per-section build)"
      via: "severity column dictates which phase replaces which surface"
      pattern: "HIGH|MEDIUM|LOW.*Phase [2-6]"
---

<objective>
Audit the existing Virtuna v1.1 customer-facing copy against a Wayback snapshot of `societies.io` (the actual plagiarism source — NOT `artificialsocieties.io` per RESEARCH.md A1) and produce two artifacts: (1) a reviewable diff doc cataloguing plagiarized text by section + severity, and (2) the full replacement copy across all 7 landing viewports, onboarding flow, and footer, ready for Phase 2-6 consumption.

Purpose: Closes BRAND-05 (replace plagiarized Artificial Societies copy across customer-facing surfaces) and BRAND-06 (hero copy authored to brand-spine standard, signed off by Davide before Phase 2 build starts). Without these artifacts, Phase 2 hero build has no copy to render and Phase 6 launch ships continued plagiarism. D-15 mandates batch sign-off at end of phase.

Output: 2 NEW phase-scoped docs (`01-PLAGIARISM-AUDIT.md`, `01-REPLACEMENT-COPY.md`) + 2 NEW reference captures (`societies-snapshot.html`, `societies-text.txt`).

This plan is Wave 2 — depends on Plan 01's `BRAND-SPINE.md` being on disk because §3 (preferred verbs) and §4 (banned table) are the rubric for replacement-copy authorship. Plan 04's vocab-lint script is also expected on disk (Wave 1 sibling) for the dry-run vocab compliance check.

This plan has `autonomous: false` because Task 4 is a `checkpoint:human-verify` — Davide must read REPLACEMENT-COPY.md end-to-end and check the sign-off boxes per D-15.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-brand-spine-visual-metaphor/01-CONTEXT.md
@.planning/phases/01-brand-spine-visual-metaphor/01-RESEARCH.md
@.planning/phases/01-brand-spine-visual-metaphor/01-PATTERNS.md
@.planning/phases/01-brand-spine-visual-metaphor/01-VALIDATION.md
@.planning/phases/01-brand-spine-visual-metaphor/01-01-SUMMARY.md
@.planning/reference/BRAND-SPINE.md
@CLAUDE.md

<plagiarism_source_files>
<!-- Verified by direct ls. Plagiarized JSX text lives here. -->

`src/app/(marketing)/page.tsx` — composition + meta
`src/components/landing/hero-section.tsx` — hero H1, sub-headline, CTAs
`src/components/landing/features-section.tsx` — 4-card grid
`src/components/landing/case-study-section.tsx` — case studies
`src/components/landing/comparison-chart.tsx` — comparison
`src/components/landing/cta-section.tsx` — final CTA
`src/components/landing/faq-section.tsx` — FAQ accordion
`src/components/landing/feature-card.tsx` — card primitive
`src/components/landing/partnership-section.tsx` — partnership
`src/components/landing/persona-card.tsx` — persona primitive
`src/components/landing/social-proof-section.tsx` — testimonials
`src/components/landing/stats-section.tsx` — stats
`src/components/landing/backers-section.tsx` — backers
`src/components/landing/testimonial-quote.tsx` — quote primitive
`src/components/onboarding/connect-step.tsx` — onboarding step 1
`src/components/onboarding/goal-step.tsx` — onboarding step 2
`src/components/onboarding/preview-step.tsx` — onboarding step 3
</plagiarism_source_files>

<wayback_workflow>
<!-- Per RESEARCH.md "Pattern 3: Plagiarism Audit Pipeline" + Pitfall 1, 2, 3.
     CRITICAL: domain is societies.io NOT artificialsocieties.io. -->

**Step 1 — Find Wayback CDX index for societies.io (verify domain):**
```bash
curl -s "http://web.archive.org/cdx/search/cdx?url=societies.io&from=20250101&output=json&limit=20" | tail -10
```

If output is `[]` or empty: STOP — re-check the domain. The brand name is "Artificial Societies" but the live domain is `societies.io`.

**Step 2 — Pick a recent text/html 200 snapshot (~20-50 KB).** Recommended: 2026-05-04 snapshot if available in CDX output.

**Step 3 — Capture HTML (use --compressed per Pitfall 2 — Wayback returns gzip):**
```bash
SNAPSHOT_TS="20260504034418"  # replace with actual timestamp from Step 1
curl -s --compressed \
  "https://web.archive.org/web/${SNAPSHOT_TS}id_/https://societies.io/" \
  > .planning/reference/societies-snapshot.html
```

The `id_` flag in the Wayback URL is critical — returns original HTML without injected toolbar.

**Step 4 — Verify capture is valid:**
```bash
file .planning/reference/societies-snapshot.html
# Expected: ASCII text or HTML document, NOT "gzip compressed data"
# If gzip: re-run Step 3 with --compressed flag
```

**Step 5 — Extract plain text (10-line node script per RESEARCH.md):**
```bash
node -e '
  const fs = require("fs");
  const html = fs.readFileSync(process.argv[1], "utf8");
  const text = html
    .replace(/<script[\s\S]*?<\/script>/gi, "")
    .replace(/<style[\s\S]*?<\/style>/gi, "")
    .replace(/<[^>]+>/g, " ")
    .replace(/\s+/g, " ")
    .replace(/&amp;/g, "&")
    .replace(/&lt;/g, "<")
    .replace(/&gt;/g, ">")
    .replace(/&#39;/g, "'"'"'")
    .replace(/&quot;/g, "\"")
    .trim();
  console.log(text);
' .planning/reference/societies-snapshot.html > .planning/reference/societies-text.txt
```

**Pitfall 3 (snapshot era):** Pick a snapshot CLOSE TO when the v1.1 plagiarism actually happened (PROJECT.md milestone history shows v1.1 was earlier — see "Pixel-perfect societies.io clone -- v1.1"). The 2026-05 snapshot shows current live state; an older snapshot may better match what was originally cloned. If the current snapshot's text doesn't match obviously plagiarized strings in `src/components/landing/hero-section.tsx`, capture an additional pre-redesign snapshot.
</wayback_workflow>

<style_invariants>
<!-- Per PATTERNS.md "Phase 1: Brand Spine & Visual Metaphor — Pattern Map" — phase docs use HTML-tag section wrappers. -->
- Filename uses `01-` prefix (matches `01-CONTEXT.md`, `01-RESEARCH.md` convention).
- H1 + bold key:value metadata block (Captured/Snapshot source/Scope/Drafted/Conforms-to/Sign-off-model).
- HTML-tag wrappers: `<scope>`, `<method>`, `<findings>`, `<sign_off>` for AUDIT; `<viewport name="...">` for COPY.
- Double-hyphen ` -- ` separators (no em-dash).
- Italic two-line footer.
- Decision-ID inline references: `(per D-12)`, `(per D-15)`, `(per D-16)`.
</style_invariants>

<replacement_copy_inputs>
<!-- Locked content from REQUIREMENTS.md the COPY doc must mirror verbatim. -->

**Hero (HERO-01..05) — already locked, just transcribe:**
- Pre-headline: `VIRTUNA · A NUMEN MACHINES PRODUCT`
- H1: `Predict how your audience will respond. Before you post.`
- Sub-headline: `Virtuna simulates your audience to forecast every video before it ships.`
- Subline: `Trained on decades of behavioral research. Self-improving with every outcome.`
- Primary CTA: `Run a prediction →`
- Secondary CTA: `See the science`

**Brand spine** appears in OG metadata + footer brand stamp:
- Brand spine: `Your audience, simulated.`

**Footer (PRICE-05):**
- Numen Machines lockup: `Made by Numen Machines`
- Legal links: Privacy, Terms, Cookies (placeholder URLs OK at this stage)
- Social: per existing app surfaces

**Demo (DEMO-01..08):** Section eyebrow "Try it"; placeholder copy for prediction-result UI; loading-state copy for the 3-5 second simulated processing; recovery copy for empty/error/invalid-URL states; "Try a sample video" fallback button.

**How It Works (WORKS-01..06):** 4-stage labels: video, analyze, simulate audience, predict. Section headline frames moat without duplicating hero language.

**Three Surfaces (SURF-01..06):** Cell 1 "Prediction"; Cell 2 "Competitor Intelligence"; Cell 3 "Brand Deals". Each cell has short headline + 1-sentence description + "Learn more" link (CTA stub).

**Science (SCI-01..06):** Section headline frames behavioral-research moat (e.g. "Why audience matters more than trends"). 3-5 citation chips. Dataset stats with "early signal" / "growing dataset" framing if not yet impressive.

**Social Proof (PROOF-01..05):** 3-5 creator quotes (placeholder names OK if real ones unavailable). Honest "early signal from N creators" framing — no fake numbers.

**Pricing (PRICE-01..06):** Two-tier (Starter/Pro) feature comparison. 7-day Pro trial messaging. Single primary CTA at bottom: "Start free" or "Run a prediction".

**Onboarding (existing 3 steps):** Connect step copy, Goal step copy, Preview step copy — replace plagiarized framing with brand-spine-compliant tone.
</replacement_copy_inputs>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network -> filesystem | `curl` retrieves Wayback HTML to `.planning/reference/`. Wayback Machine is treated as a benign source (archive.org). |
| filesystem -> documents | Node text-extraction script reads HTML, writes plain text. Pure stdlib operations on local files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | I (Information Disclosure) | `.planning/reference/societies-snapshot.html` | accept | Captures public web content from web.archive.org. Already publicly available; no new disclosure surface. |
| T-01-03-02 | S (Spoofing) | Wayback domain | mitigate | Use canonical `web.archive.org` URLs only. Verify capture file is text/html (not redirected to a malicious page) by `file` command after `curl`. The `id_` flag returns archived raw HTML, not Wayback's user-facing wrapper. |
| T-01-03-03 | T (Tampering) | inline node text-extraction script | mitigate | Script is a 10-line stdlib-only one-liner. No third-party deps. No shell exec. Reads one file, writes one file. Pattern is documented in RESEARCH.md and runs in the developer's local shell. |
| T-01-03-04 | I (Information Disclosure) | `01-PLAGIARISM-AUDIT.md` | accept | Document is intended to be public-readable inside the repo. May reference Virtuna's own source files by path; no PII, no secrets. |
| T-01-03-05 | I (Information Disclosure) | `01-REPLACEMENT-COPY.md` | accept | Same as above; copy strings are intended for live customer-facing surfaces. |

**Per phase security_threat_model:** No code introduced -- markdown documents only. Network traffic is read-only HTTP GET to web.archive.org (public archive). Threat surface = zero beyond ordinary web reads.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Capture societies.io Wayback snapshot + extract plain text</name>
  <files>.planning/reference/societies-snapshot.html, .planning/reference/societies-text.txt</files>
  <read_first>
    - .planning/phases/01-brand-spine-visual-metaphor/01-RESEARCH.md §"Pattern 3: Plagiarism Audit Pipeline" lines 414-465 (capture workflow)
    - .planning/phases/01-brand-spine-visual-metaphor/01-RESEARCH.md §"Common Pitfalls" Pitfall 1, 2, 3 (domain, gzip, snapshot-era)
    - .planning/phases/01-brand-spine-visual-metaphor/01-VALIDATION.md (Validation Sign-Off — domain MUST be societies.io NOT artificialsocieties.io)
  </read_first>
  <action>
**Step 1 — Find a Wayback CDX entry for `societies.io` (NOT artificialsocieties.io).**

```bash
curl -s "http://web.archive.org/cdx/search/cdx?url=societies.io&from=20250101&output=json&limit=20" | tail -10
```

The CDX API returns lines like `["urlkey","timestamp","original","mimetype","statuscode","digest","length"]`. Filter for `text/html` + statuscode `200` + length > 10000. Pick the timestamp from a row that meets these criteria.

If the CDX API is unreachable: fallback is to manually request `http://web.archive.org/web/2*/societies.io` in a browser and read a recent timestamp from the calendar. Capture the timestamp as `SNAPSHOT_TS`.

If `societies.io` returns ZERO snapshots: STOP and verify the domain spelling against PROJECT.md (line 73: "Pixel-perfect societies.io clone -- v1.1"). Do NOT switch to `artificialsocieties.io` — that domain has zero snapshots per RESEARCH.md A1.

**Step 2 — Capture the HTML with `--compressed` (Pitfall 2):**

```bash
SNAPSHOT_TS="<paste-timestamp-from-step-1>"
mkdir -p .planning/reference
curl -s --compressed \
  "https://web.archive.org/web/${SNAPSHOT_TS}id_/https://societies.io/" \
  > .planning/reference/societies-snapshot.html
```

**Step 3 — Verify the capture is real HTML (not gzip, not empty, not a Wayback error page):**

```bash
file .planning/reference/societies-snapshot.html
# Expected output should mention: "HTML document" or "ASCII text" or "UTF-8 Unicode text"
# NOT: "gzip compressed data" or "empty"

wc -c .planning/reference/societies-snapshot.html
# Expected: > 5000 bytes for a real landing page

grep -ci "<html\\|<title\\|<body" .planning/reference/societies-snapshot.html
# Expected: ≥ 1 (basic HTML tags present)
```

If verification fails, re-run Step 2 with a different snapshot timestamp from Step 1.

**Step 4 — Extract plain text via inline node script (no extra deps):**

```bash
node -e '
  const fs = require("fs");
  const html = fs.readFileSync(process.argv[1], "utf8");
  const text = html
    .replace(/<script[\s\S]*?<\/script>/gi, "")
    .replace(/<style[\s\S]*?<\/style>/gi, "")
    .replace(/<[^>]+>/g, " ")
    .replace(/\s+/g, " ")
    .replace(/&amp;/g, "&")
    .replace(/&lt;/g, "<")
    .replace(/&gt;/g, ">")
    .replace(/&#39;/g, "'"'"'")
    .replace(/&quot;/g, "\"")
    .trim();
  console.log(text);
' .planning/reference/societies-snapshot.html > .planning/reference/societies-text.txt
```

**Step 5 — Verify extraction (sanity-check for known plagiarism markers):**

```bash
grep -ciE "audience|simulat|attitude|opinion|persona|behavior|AI" .planning/reference/societies-text.txt
# Expected: ≥ 1 (these terms describe what societies.io is about)

wc -c .planning/reference/societies-text.txt
# Expected: > 1000 bytes (real text content, not just whitespace)
```

If Step 5 returns zero or near-zero matches, the snapshot may be from a redesigned era of societies.io that doesn't match what was originally cloned. Per Pitfall 3, capture an ADDITIONAL earlier snapshot (suggested: any 2024-2025 timestamp from CDX) and overwrite this file, OR document both eras in the audit doc.

**DO NOT:**
- Use `artificialsocieties.io` (zero captures, A1 verified).
- Drop the `--compressed` flag (Pitfall 2 — output will be binary gzip).
- Drop the `id_` flag in the Wayback URL (returns wrapped page with toolbar, not raw HTML).
- Add npm packages for HTML parsing (the 10-line regex strip is sufficient for diff input).
- Skip Step 5 verification — a Wayback error page (404, "Snapshot Not Found") will produce empty text and cause Task 2 to author the audit from imagination.
  </action>
  <acceptance_criteria>
    - [ ] Snapshot file exists and is non-trivial: `[ $(wc -c < .planning/reference/societies-snapshot.html) -gt 5000 ]`
    - [ ] Snapshot is HTML, not gzip: `file .planning/reference/societies-snapshot.html | grep -qiE "html|ascii|unicode"`
    - [ ] Snapshot contains HTML markers: `grep -qiE "<html|<title|<body" .planning/reference/societies-snapshot.html`
    - [ ] Plain-text extraction exists: `[ $(wc -c < .planning/reference/societies-text.txt) -gt 1000 ]`
    - [ ] Plain-text contains relevant markers (audience/simulat/persona/behavior): `grep -ciE "audience|simulat|persona|behavior|opinion" .planning/reference/societies-text.txt | head -1` returns ≥ 1
    - [ ] Plain-text has zero residual HTML tags: `[ $(grep -cE "<[a-zA-Z][^>]*>" .planning/reference/societies-text.txt) -eq 0 ]`
    - [ ] Domain is correct: snapshot URL or content references societies.io domain (manual verification on file content)
  </acceptance_criteria>
  <verify>
    <automated>
      [ $(wc -c < .planning/reference/societies-snapshot.html) -gt 5000 ] && \
      file .planning/reference/societies-snapshot.html | grep -qiE "html|ascii|unicode" && \
      grep -qiE "<html|<title|<body" .planning/reference/societies-snapshot.html && \
      [ $(wc -c < .planning/reference/societies-text.txt) -gt 1000 ] && \
      [ $(grep -cE "<[a-zA-Z][^>]*>" .planning/reference/societies-text.txt) -eq 0 ]
    </automated>
  </verify>
  <done>
    Two reference files exist on disk: `.planning/reference/societies-snapshot.html` (raw Wayback HTML capture, > 5KB, valid HTML markers) and `.planning/reference/societies-text.txt` (plain-text extraction, > 1KB, zero residual HTML tags, contains relevant content markers like "audience" / "simulat" / "behavior"). Both ready as diff inputs for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 01-PLAGIARISM-AUDIT.md (audit only — split from former 2A)</name>
  <files>.planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md</files>
  <read_first>
    - .planning/reference/societies-text.txt (CREATED IN TASK 1 — input for the diff)
    - .planning/reference/BRAND-SPINE.md (CREATED BY PLAN 01 — voice, vocab, audience tuning rubric)
    - .planning/REQUIREMENTS.md lines 24-65 (HERO-01..10, DEMO-01..08, WORKS-01..06, SURF-01..06)
    - .planning/REQUIREMENTS.md lines 67-90 (SCI-01..06, PROOF-01..05, PRICE-01..06)
    - .planning/phases/01-brand-spine-visual-metaphor/01-CONTEXT.md (D-12, D-13, D-14, D-15, D-16 — audit scope, batch sign-off, hero copy lock)
    - .planning/phases/01-brand-spine-visual-metaphor/01-RESEARCH.md §"Pattern 3" Steps 4-5 (audit doc + replacement copy doc skeleton)
    - .planning/phases/01-brand-spine-visual-metaphor/01-PATTERNS.md §"01-PLAGIARISM-AUDIT.md" (HTML-tag wrapper conventions, table style)
    - src/components/landing/hero-section.tsx (current plagiarized H1 + sub-headline — diff source)
    - src/components/landing/features-section.tsx (current 4-card grid — diff source)
    - src/components/landing/comparison-chart.tsx (current comparison — diff source)
    - src/components/landing/cta-section.tsx (current final CTA — diff source)
    - src/components/landing/faq-section.tsx (current FAQ — diff source)
    - src/components/landing/social-proof-section.tsx (current testimonials — diff source)
    - src/components/landing/stats-section.tsx (current stats — diff source)
    - src/components/onboarding/connect-step.tsx, goal-step.tsx, preview-step.tsx (current onboarding — diff source)
    - src/app/(marketing)/page.tsx (composition + meta strings)
  </read_first>
  <action>
**Author `01-PLAGIARISM-AUDIT.md`.**

Create `.planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md` (NEW FILE) with the structure below. Cross-reference `.planning/reference/societies-text.txt` for source phrases and use grep against `src/components/landing/*.tsx` to find the Virtuna v1.1 plagiarized text. Cite exact `file:line` references for every Virtuna entry.

```markdown
# Phase 1: Brand Spine & Visual Metaphor - Plagiarism Audit

**Captured:** 2026-05-10
**Snapshot source:** https://web.archive.org/web/<TIMESTAMP>id_/https://societies.io/
**Plain-text capture:** `.planning/reference/societies-text.txt`
**Scope:** D-12 -- landing + onboarding + dashboard-visible copy
**Out of scope:** D-13 -- admin pages, dev tools, /docs/, legal boilerplate, code comments, commit messages
**Audit method:** D-14 -- diff against societies.io original via Wayback + manual originality pass for tone

<scope>
## Audit Scope (D-12)

In-scope surfaces:
- All 7 viewports of the new landing (hero, demo, how-it-works, three-surfaces, science, social-proof, pricing) -- currently rendered by `src/components/landing/*.tsx` + `src/app/(marketing)/page.tsx`
- Sign-up / login / onboarding flow copy -- `src/components/onboarding/*.tsx`
- Empty states, tooltips, plan-upgrade nudges (any logged-out OR free-tier surface)
- Hero copy of `BRAND-BIBLE.md` (anything quoted publicly)
- OG metadata + page titles (`src/app/(marketing)/page.tsx`, `opengraph-image.tsx`)

Out-of-scope (D-13):
- Admin pages, dev tools (`scripts/`, `extraction/`)
- `/docs/` directory
- Legal boilerplate (cookie banners that are templated for compliance)
- Code comments and commit messages
</scope>

<method>
## Method (D-14)

1. Wayback HTML capture of societies.io at `.planning/reference/societies-snapshot.html` (timestamp <TS>)
2. Plain-text extraction at `.planning/reference/societies-text.txt`
3. Manual extraction of Virtuna's current copy from `src/components/landing/*.tsx`, `src/components/onboarding/*.tsx`, `src/app/(marketing)/page.tsx`
4. Side-by-side diff in this document (per-surface table below)
5. Tone-mimicry pass: even if literal text differs, flag if paragraph shape, headline pattern, or structural framing matches (e.g., "X, Y, and Z" tricolons, four-card "Into the future" grids, FAQ accordion phrasing)

Severity rubric:
- **HIGH** -- verbatim phrase reuse (literal copy of 3+ consecutive words)
- **MEDIUM** -- structural mimicry (same paragraph shape / headline pattern / section framing) without verbatim text
- **LOW** -- similar tone or topic without specific copy reuse
</method>

<findings>
## Findings

### Section 1: Hero (`src/components/landing/hero-section.tsx`)

| Surface | Societies.io | Virtuna v1.1 (current) | Verdict |
|---------|--------------|------------------------|---------|
| H1 | "<exact text from societies-text.txt>" | "<current Virtuna H1 with file:line>" | <HIGH/MEDIUM/LOW> -- <reason> |
| Sub-headline | "<text>" | "<current with file:line>" | <verdict> |
| Pre-headline | "<text>" | "<current with file:line>" | <verdict> |
| Primary CTA | "<text>" | "<current with file:line>" | <verdict> |
| Secondary CTA | "<text>" | "<current with file:line>" | <verdict> |

### Section 2: Features grid (`src/components/landing/features-section.tsx`)

| Surface | Societies.io | Virtuna v1.1 (current) | Verdict |
|---------|--------------|------------------------|---------|
| Section eyebrow | "<text>" | "<current with file:line>" | <verdict> |
| Section H2 | "<text>" | "<current with file:line>" | <verdict> |
| Card 1 title | "<text>" | "<current with file:line>" | <verdict> |
| Card 1 body | "<text>" | "<current with file:line>" | <verdict> |
| (repeat for cards 2, 3, 4) | | | |

### Section 3: Comparison chart (`src/components/landing/comparison-chart.tsx`)

(per-surface diff table)

### Section 4: Case studies (`src/components/landing/case-study-section.tsx`)

(per-surface diff table)

### Section 5: Social proof (`src/components/landing/social-proof-section.tsx`)

(per-surface diff table)

### Section 6: Stats (`src/components/landing/stats-section.tsx`)

(per-surface diff table)

### Section 7: FAQ (`src/components/landing/faq-section.tsx`)

(per-surface diff table)

### Section 8: Final CTA (`src/components/landing/cta-section.tsx`)

(per-surface diff table)

### Section 9: Backers (`src/components/landing/backers-section.tsx`)

(per-surface diff table)

### Section 10: Partnership (`src/components/landing/partnership-section.tsx`)

(per-surface diff table)

### Section 11: Onboarding (`src/components/onboarding/*.tsx`)

| Surface | Societies.io | Virtuna v1.1 (current) | Verdict |
|---------|--------------|------------------------|---------|
| Connect-step header | "<text>" | "<current at connect-step.tsx:N>" | <verdict> |
| Connect-step body | "<text>" | "<current with file:line>" | <verdict> |
| Goal-step header | "<text>" | "<current at goal-step.tsx:N>" | <verdict> |
| Preview-step header | "<text>" | "<current at preview-step.tsx:N>" | <verdict> |

### Section 12: OG metadata + page titles (`src/app/(marketing)/page.tsx`, `opengraph-image.tsx`)

| Surface | Societies.io | Virtuna v1.1 (current) | Verdict |
|---------|--------------|------------------------|---------|
| og:title | "<text>" | "<current at page.tsx:N>" | <verdict> |
| og:description | "<text>" | "<current at page.tsx:N>" | <verdict> |
| <title> | "<text>" | "<current at page.tsx:N>" | <verdict> |
</findings>

## Replacement Scope

| Severity | Count | Replace in Phase | File touchpoints |
|----------|-------|------------------|------------------|
| HIGH (verbatim) | <N> | Phase 6 (BUILD-09 swaps page.tsx wholesale) + Phase 2-4 per-section build | hero-section.tsx, sub-headline, og:description, ... |
| MEDIUM (structural mimicry) | <N> | Phase 2-4 (per-section build) | features-section.tsx, faq-section.tsx, ... |
| LOW (similar tone) | <N> | Phase 2-4 (per-section build) | social-proof-section.tsx, stats-section.tsx, ... |

<sign_off>
## Sign-off

- [ ] Davide reviewed full audit doc end-to-end (D-15 batch sign-off)
- [ ] Replacement copy doc (`01-REPLACEMENT-COPY.md`) reviewed alongside this audit
- [ ] No additional structural mimicry detected on second pass
- [ ] Replacement scope severity counts agreed
</sign_off>

---

*Phase: 1-Brand Spine & Visual Metaphor*
*Audit captured: 2026-05-10*
```

**Concrete authoring rules:**
- For EVERY entry in the Findings tables, run `grep -n "<phrase>" src/components/landing/<file>.tsx` to find the exact line and cite `(file:line)`.
- If a Virtuna surface has NO match in societies-text.txt (i.e., truly original), do NOT include it in the table — only list surfaces with at least LOW severity.
- Severity counts in the Replacement Scope table MUST match the Findings tables (sum HIGH + sum MEDIUM + sum LOW).
- If the snapshot era doesn't match (Pitfall 3), document this in the `<method>` section and note the limitation in the `<findings>` section header (e.g., "Note: 2026-05 snapshot reflects current societies.io; some Virtuna v1.1 plagiarism may originate from earlier era — flagged where the diff is inconclusive").
  </action>
  <acceptance_criteria>
    - [ ] AUDIT file exists: `test -f .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md`
    - [ ] AUDIT references societies.io: `grep -c "societies\.io" .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md` returns ≥ 1
    - [ ] AUDIT does NOT incorrectly reference artificialsocieties.io: `grep -c "artificialsocieties" .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md` returns 0
    - [ ] AUDIT has all required sections: `grep -cE "## Method|<method>" .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md` returns ≥ 1 AND `grep -cE "## Findings|<findings>" .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md` returns ≥ 1 AND `grep -cE "## Replacement Scope|## Replacement scope" .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md` returns ≥ 1
    - [ ] AUDIT has at least 5 file:line citations to src/: `grep -cE "src/components/landing/[a-z-]+\.tsx:[0-9]+" .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md` returns ≥ 5
    - [ ] AUDIT has Wayback URL: `grep -c "web.archive.org" .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md` returns ≥ 1
    - [ ] AUDIT min length: `wc -l < .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md` returns ≥ 80
    - [ ] Style: zero em-dashes in AUDIT: `grep -c "—" .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md` returns 0
  </acceptance_criteria>
  <verify>
    <automated>
      test -f .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md && \
      grep -q "societies\.io" .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md && \
      [ $(grep -c "artificialsocieties" .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md) -eq 0 ] && \
      grep -q "web.archive.org" .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md && \
      [ $(wc -l < .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md) -ge 80 ] && \
      [ $(grep -c "—" .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md) -eq 0 ]
    </automated>
  </verify>
  <done>
    `01-PLAGIARISM-AUDIT.md` documents the diff between the Wayback societies.io capture and current Virtuna v1.1 customer-facing copy with severity-coded findings tables (HIGH/MEDIUM/LOW), method block, scope block (D-12, D-13), Wayback URL reference, and ≥5 file:line citations to `src/components/landing/*.tsx`. Domain is societies.io, NOT artificialsocieties.io. Zero em-dashes. Audit is ready for Task 3 (replacement copy authoring) to consume severity counts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author 01-REPLACEMENT-COPY.md (replacement copy — split from former 2B)</name>
  <files>.planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md</files>
  <read_first>
    - .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md (CREATED IN TASK 2 — severity counts dictate which viewports need wholesale replacement)
    - .planning/reference/BRAND-SPINE.md (CREATED BY PLAN 01 — voice, vocab, audience tuning rubric)
    - .planning/REQUIREMENTS.md lines 24-65 (HERO-01..10, DEMO-01..08, WORKS-01..06, SURF-01..06)
    - .planning/REQUIREMENTS.md lines 67-90 (SCI-01..06, PROOF-01..05, PRICE-01..06)
    - .planning/phases/01-brand-spine-visual-metaphor/01-CONTEXT.md (D-15, D-16 — batch sign-off, hero copy lock)
    - .planning/phases/01-brand-spine-visual-metaphor/01-PATTERNS.md §"01-REPLACEMENT-COPY.md" (HTML-tag wrapper conventions, table style)
    - src/components/onboarding/connect-step.tsx, goal-step.tsx, preview-step.tsx (current onboarding — to improve over)
    - scripts/lint-vocab.mjs (CREATED BY PLAN 04 — runtime dependency for vocab compliance verification)
  </read_first>
  <rework_risk_note>
This task authors ~25-35 customer-facing copy strings drafted from BRAND-SPINE.md voice
rules + REQUIREMENTS.md locked hero copy. Per CONTEXT.md D-15, Davide accepts cascade-rework
risk: the first authoring pass may require redline iteration after his batch review.
Phase budget assumes one redline pass; a second redline iteration triggers a phase-budget
reconsideration via /gsd-pause-work + thread continuation, not silent overrun.

Source-of-truth voice anchors for the `<draft>` placeholder strings (the 25-35 customer-facing
copy strings without verbatim REQUIREMENTS.md locks):
  - BRAND-SPINE.md §2 Tone Descriptors + DO/DON'T table (Plan 01 output)
  - BRAND-SPINE.md §3 Preferred Verbs (Plan 01 output)
  - BRAND-SPINE.md §6 Audience Tuning per Viewport (Plan 01 output)
  - REQUIREMENTS.md hero copy as the verbatim hero block (HERO-01..05)
  - PROJECT.md vocab guardrails (avoid 'viral'/'AI'; lead with 'audience'/'behavioral'/'simulated')

If a draft string cannot be confidently authored from these sources, mark the cell with
`<draft: needs-davide-input>` (NOT bare `<draft>`) so Davide's redline pass surfaces it
explicitly. Bare `<draft>` placeholders are forbidden in the final doc per acceptance criteria.
  </rework_risk_note>
  <action>
**Author `01-REPLACEMENT-COPY.md`.**

Create `.planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` (NEW FILE) with the structure below. Every copy string MUST pass BRAND-SPINE.md §3 (preferred verbs) + §4 (banned table) compliance — verify by running `node scripts/lint-vocab.mjs` against the doc after authoring (Plan 04's script accepts `.md` files in default scope).

```markdown
# Phase 1: Brand Spine & Visual Metaphor - Replacement Copy

**Drafted:** 2026-05-10
**Conforms to:** `.planning/reference/BRAND-SPINE.md`
**Sign-off model:** D-15 (approve once at end)
**Hero copy lock:** D-16 (REQUIREMENTS.md is the draft starting point; Davide finalizes here)

This document is the source of truth for ALL customer-facing copy strings consumed by Phase 2-4 build tasks. Phase 6 (BUILD-09) replaces `src/app/page.tsx` wholesale using these strings.

<viewport name="hero">
## Hero (HERO-01..10)

| Element | Copy | Source ID |
|---------|------|-----------|
| Pre-headline | `VIRTUNA · A NUMEN MACHINES PRODUCT` | HERO-01 |
| H1 | "Predict how your audience will respond. Before you post." | HERO-02 |
| Sub-headline | "Virtuna simulates your audience to forecast every video before it ships." | HERO-03 |
| Subline | "Trained on decades of behavioral research. Self-improving with every outcome." | HERO-04 |
| Primary CTA | "Run a prediction →" | HERO-05 |
| Secondary CTA | "See the science" | HERO-05 |

**Hero motion accessibility text** (per VIZ-01, screen-reader fallback):
- aria-label for hero canvas: "Audience particles aggregating into confidence score"
</viewport>

<viewport name="demo">
## Try It / Live Demo (DEMO-01..08)

| Element | Copy | Source ID |
|---------|------|-----------|
| Section eyebrow | "Try it" | DEMO-02 |
| Section H2 | "<draft H2 — verb-first per BRAND-SPINE §3>" | DEMO-02 |
| Input placeholder | "Paste a TikTok URL." | DEMO-01 |
| Submit button | "Predict →" | DEMO-02 |
| Try-sample button | "Try a sample video" | DEMO-06 |
| Loading copy | "Simulating your audience…" | DEMO-03 |
| Result headline | "<draft — predict-score framing>" | DEMO-04 |
| Confidence indicator | "<draft — e.g., 'Your audience would watch past 3s.'>" | DEMO-04 |
| Empty state | "<draft — friendly recovery>" | DEMO-05 |
| Invalid-URL state | "<draft — friendly recovery>" | DEMO-05 |
| Error state | "<draft — friendly recovery>" | DEMO-05 |
</viewport>

<viewport name="how-it-works">
## How It Works (WORKS-01..06)

| Element | Copy | Source ID |
|---------|------|-----------|
| Section eyebrow | "How it works" | WORKS-04 |
| Section H2 | "<draft H2 — process moat language, no hero duplication>" | WORKS-04 |
| Stage 1 label | "Video" | WORKS-01 |
| Stage 1 description | "<1-line description>" | WORKS-03 |
| Stage 2 label | "Analyze" | WORKS-01 |
| Stage 2 description | "<1-line description>" | WORKS-03 |
| Stage 3 label | "Simulate audience" | WORKS-01 |
| Stage 3 description | "<1-line description>" | WORKS-03 |
| Stage 4 label | "Predict" | WORKS-01 |
| Stage 4 description | "<1-line description>" | WORKS-03 |
</viewport>

<viewport name="three-surfaces">
## Three Surfaces / Bento (SURF-01..06)

| Element | Copy | Source ID |
|---------|------|-----------|
| Section eyebrow | "<draft — platform framing>" | SURF-06 |
| Section H2 | "<draft H2>" | SURF-06 |
| Cell 1 title | "Prediction" | SURF-01 |
| Cell 1 description | "<1-sentence description>" | SURF-02 |
| Cell 1 CTA | "Learn more" | SURF-02 |
| Cell 2 title | "Competitor Intelligence" | SURF-01 |
| Cell 2 description | "<1-sentence description>" | SURF-02 |
| Cell 2 CTA | "Learn more" | SURF-02 |
| Cell 3 title | "Brand Deals" | SURF-01 |
| Cell 3 description | "<1-sentence description>" | SURF-02 |
| Cell 3 CTA | "Learn more" | SURF-02 |
</viewport>

<viewport name="science">
## The Science (SCI-01..06)

| Element | Copy | Source ID |
|---------|------|-----------|
| Section eyebrow | "<draft — research moat>" | SCI-01 |
| Section H2 | "<draft — e.g., 'Why audience matters more than trends'>" | SCI-01 |
| Citation chip 1 | "<draft — dataset / paper / principle>" | SCI-02 |
| Citation chip 2 | "<draft>" | SCI-02 |
| Citation chip 3 | "<draft>" | SCI-02 |
| Dataset stats callout | "<draft — 'early signal' or 'growing dataset' framing>" | SCI-03 |
| Self-improving loop label | "<draft — feedback -> retrain -> improve>" | SCI-04 |
</viewport>

<viewport name="social-proof">
## Social Proof / Metrics (PROOF-01..05)

| Element | Copy | Source ID |
|---------|------|-----------|
| Section eyebrow | "<draft>" | PROOF-04 |
| Section H2 | "<draft — honest 'early signal' framing>" | PROOF-04 |
| Quote 1 | "<draft — placeholder OK if real creator unavailable>" | PROOF-01 |
| Quote 1 attribution | "<handle, niche, follower count if shippable>" | PROOF-01 |
| (repeat for quotes 2, 3, 4, 5 — minimum 3) | | |
| Platform metric callout | "<draft — predictions run / creators / accuracy>" | PROOF-03 |
</viewport>

<viewport name="pricing">
## Pricing & Conversion (PRICE-01..06)

| Element | Copy | Source ID |
|---------|------|-----------|
| Section eyebrow | "<draft>" | PRICE-01 |
| Section H2 | "<draft>" | PRICE-01 |
| Starter tier name | "Starter" | PRICE-01 |
| Starter price | "<draft — 'Free' or '$X/mo'>" | PRICE-01 |
| Starter feature list (3-5) | "<draft list>" | PRICE-01 |
| Pro tier name | "Pro" | PRICE-01 |
| Pro price | "<draft>" | PRICE-01 |
| Pro feature list (3-5) | "<draft list>" | PRICE-01 |
| 7-day trial messaging | "Start your 7-day Pro trial." | PRICE-03 |
| Final CTA button | "Start free" or "Run a prediction" | PRICE-04 |
</viewport>

<viewport name="footer">
## Footer (PRICE-05)

| Element | Copy | Source ID |
|---------|------|-----------|
| Brand stamp | "Your audience, simulated." (brand spine) | BRAND-01 |
| Numen Machines lockup | "Made by Numen Machines" | PRICE-05 |
| Privacy link label | "Privacy" | PRICE-05 |
| Terms link label | "Terms" | PRICE-05 |
| Cookies link label | "Cookies" | PRICE-05 |
| Copyright line | "© 2026 Numen Machines. All rights reserved." | PRICE-05 |
</viewport>

<viewport name="onboarding">
## Onboarding (`src/components/onboarding/*.tsx`)

| Element | Copy | Source ID |
|---------|------|-----------|
| Connect step H1 | "<draft>" | (D-12 in-scope) |
| Connect step body | "<draft — TikTok @handle input flow>" | (D-12 in-scope) |
| Goal step H1 | "<draft — goal personalization>" | (D-12 in-scope) |
| Goal options (3-5) | "<draft — list per existing flow>" | (D-12 in-scope) |
| Preview step H1 | "<draft>" | (D-12 in-scope) |
| Preview step CTA | "<draft>" | (D-12 in-scope) |
</viewport>

<viewport name="og-metadata">
## OG Metadata + Page Titles

| Element | Copy | Source ID |
|---------|------|-----------|
| og:title | "Virtuna -- A Numen Machines product" | BRAND-03 |
| og:description | "Your audience, simulated." | BRAND-01 |
| `<title>` (landing) | "Virtuna -- Predict your audience" | BRAND-03 |
| `<title>` (dashboard) | "Virtuna" | BRAND-03 |
</viewport>

## Vocab Compliance

This document MUST pass `node scripts/lint-vocab.mjs .planning/phases/01-brand-spine-visual-metaphor` with zero ERROR-level violations. Warnings (e.g., legacy `users` references) are acceptable only if accompanied by an inline `<!-- vocab-lint-disable-next-line -->` marker with a brief justification.

Verified (run `node scripts/lint-vocab.mjs .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md`):
- 0 ERROR violations
- <N> WARN violations (with justifications below if any)

## Sign-off (D-15 batch — D-16 hero copy lock)

- [ ] Davide reviewed full document end-to-end
- [ ] Davide approved replacement copy as-is OR redlined sections sent back
- [ ] No remaining banned-vocab matches per `scripts/lint-vocab.mjs`
- [ ] Hero copy locked per BRAND-06 / D-16
- [ ] Davide approved YYYY-MM-DD: ___________

---

*Phase: 1-Brand Spine & Visual Metaphor*
*Replacement copy drafted: 2026-05-10*
```

**Concrete authoring rules:**
- Hero block (HERO-01..05) MUST be transcribed verbatim from REQUIREMENTS.md lines 26-30. Do NOT paraphrase.
- All `<draft>` placeholders MUST be replaced with actual copy strings before Davide reviews. Do not leave any `<draft>` markers in the final doc.
- Every copy string MUST be checked against BRAND-SPINE.md §4 — zero banned vocab in any draft string. Run `node scripts/lint-vocab.mjs .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` after authoring; iterate until 0 errors.
- Quotes for PROOF-01 may use placeholder names (e.g., "@creator, niche, 10k followers") if real creator quotes are unavailable; do NOT fabricate specific numbers that aren't true. Use "early signal from N creators" framing per PROOF-04.
- Onboarding copy SHOULD be a clear improvement over the existing plagiarized text in `src/components/onboarding/*.tsx`. Read the current files via the Read tool first, then re-author.

**DO NOT:**
- Paraphrase the locked hero copy from REQUIREMENTS.md.
- Include any string that triggers an ERROR in `scripts/lint-vocab.mjs` (use the suppress marker with justification only for legitimate WARN-level uses).
- Reference Figma frames or ask Davide to "approve a Figma" — D-05 chose written-only.
- Sign off on Davide's behalf — the `Davide approved YYYY-MM-DD: ___________` line is filled in by Davide in Task 4, not by Claude.
- Propose A/B test variants (the brand spine is locked, no variants).
  </action>
  <acceptance_criteria>
    - [ ] COPY file exists: `test -f .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md`
    - [ ] COPY contains brand spine: `grep -c "Your audience, simulated\." .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` returns ≥ 1
    - [ ] COPY contains all 7 viewport sections: for each in "hero" "demo" "how-it-works" "three-surfaces" "science" "social-proof" "pricing", `grep -c "<viewport name=\"$v\"" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` returns 1
    - [ ] COPY contains footer + onboarding viewports: `grep -c "<viewport name=\"footer\"" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` returns 1 AND `grep -c "<viewport name=\"onboarding\"" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` returns 1
    - [ ] COPY hero block mirrors REQUIREMENTS.md verbatim: `grep -c "Predict how your audience will respond" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` returns ≥ 1 AND `grep -c "Virtuna simulates your audience" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` returns ≥ 1 AND `grep -c "VIRTUNA · A NUMEN MACHINES PRODUCT" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` returns ≥ 1 AND `grep -c "Run a prediction" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` returns ≥ 1 AND `grep -c "See the science" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` returns ≥ 1
    - [ ] COPY has zero remaining `<draft>` placeholders: `grep -c "<draft>" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` returns 0
    - [ ] COPY has D-15 sign-off block: `grep -c "Davide approved" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` returns ≥ 1 AND `grep -cE "\[ \] Davide reviewed full document" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` returns 1
    - [ ] COPY passes vocab-lint with zero ERRORs: `node scripts/lint-vocab.mjs .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` exits 0
    - [ ] COPY min length: `wc -l < .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` returns ≥ 120
    - [ ] Style: zero em-dashes in COPY: `grep -c "—" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` returns 0
  </acceptance_criteria>
  <verify>
    <automated>
      test -f .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md && \
      grep -q "Predict how your audience will respond" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md && \
      grep -q "VIRTUNA · A NUMEN MACHINES PRODUCT" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md && \
      grep -q "Your audience, simulated\." .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md && \
      [ $(grep -c "<draft>" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md) -eq 0 ] && \
      grep -q "Davide approved" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md && \
      [ $(wc -l < .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md) -ge 120 ] && \
      [ $(grep -c "—" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md) -eq 0 ] && \
      node scripts/lint-vocab.mjs .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md
    </automated>
  </verify>
  <done>
    `01-REPLACEMENT-COPY.md` contains all 9 viewport blocks (hero, demo, how-it-works, three-surfaces, science, social-proof, pricing, footer, onboarding) plus OG metadata block, with hero copy mirroring REQUIREMENTS.md verbatim, brand-spine appearance ("Your audience, simulated."), Numen Machines lockup, Davide D-15 sign-off block (unchecked), zero `<draft>` placeholders remaining, zero ERROR-level vocab-lint violations, and zero em-dashes. Doc ready for Task 4 human-verify checkpoint. Cascade-rework risk explicitly acknowledged in `<rework_risk_note>`; phase budget assumes one redline pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Davide D-15 batch sign-off on AUDIT + COPY</name>
  <files>.planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md, .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md</files>
  <action>Davide reads both authored documents end-to-end, runs the vocab-lint dry-run on the COPY doc, and either (a) edits both Sign-off blocks to check all boxes plus fills in the approval date — completing D-15 batch sign-off and closing BRAND-05/BRAND-06 — or (b) inserts `&lt;!-- redline: &lt;reason&gt; --&gt;` markers next to specific blocks needing rework, leaving sign-off boxes unchecked. On redline, this plan loops back to Task 3 (replacement copy) with the flagged blocks. See &lt;how-to-verify&gt; below for the exact 7-step protocol Davide follows.</action>
  <what-built>
    Two phase artifacts authored automatically:
    - `.planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md` — diff between societies.io Wayback capture and current Virtuna v1.1 copy, with severity-coded findings.
    - `.planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` — full replacement copy for all 7 landing viewports + onboarding + footer + OG metadata, brand-spine compliant, vocab-lint clean.

    Hero copy mirrors REQUIREMENTS.md HERO-01..05 verbatim per D-16. Davide is the brand owner; this is the single approval gate per D-15 ("approve once at end").
  </what-built>
  <how-to-verify>
    Davide must do these steps in order:

    1. **Open both docs side-by-side:**
       - `.planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md`
       - `.planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md`

    2. **Read the AUDIT first.** Verify:
       - The findings tables look believable — the listed verbatim phrases actually exist in the current Virtuna landing components (spot-check 2-3 file:line citations).
       - The severity counts in "Replacement Scope" match what you see in Findings.
       - No surface you care about is missing from the audit.

    3. **Read the COPY end-to-end.** For each viewport block, ask:
       - Does this sound like Virtuna? (voice + tone match BRAND-SPINE.md §2)
       - Does this read at $100M+ venture quality? (compare mentally to Anthropic / Linear / Raycast hero copy)
       - Are there any banned words I'd reject? (viral, AI, go viral, generic marketing-speak)
       - Is the hero block VERBATIM the locked copy in REQUIREMENTS.md HERO-01..05? Required.

    4. **Run the vocab-lint dry-run** to confirm no banned vocab snuck in:
       ```bash
       node scripts/lint-vocab.mjs .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md
       ```
       Expected: exit code 0, "0 error(s)".

    5. **Decide:**
       - **APPROVE:** Edit `01-REPLACEMENT-COPY.md` Sign-off block — change `- [ ]` to `- [x]` on each line and fill in `Davide approved YYYY-MM-DD: <today>`. Edit `01-PLAGIARISM-AUDIT.md` Sign-off block similarly.
       - **REDLINE:** Mark specific viewport blocks as needing rework. Comment in the doc with `<!-- redline: <reason> -->` next to the rejected lines. Sign-off boxes stay UNCHECKED. Davide accepts cascade-rework risk per D-15.

    6. **If REDLINED:** Plan 03 returns to Task 3 (re-author flagged blocks). Re-run vocab-lint. Re-present for sign-off. Repeat until APPROVED.

    7. **If APPROVED:** Phase 1 is complete pending the Wave 1 plans (01, 02, 04) also having their summaries committed. Phase 2 is unblocked.
  </how-to-verify>
  <resume-signal>
    Davide types one of:
    - **"approved"** — both docs sign-off boxes are checked, dates filled, Phase 1 is shipped
    - **"redline: <details>"** — specific changes requested; loop back to Task 3
  </resume-signal>
  <acceptance_criteria>
    - [ ] Davide opens both docs and reads end-to-end
    - [ ] Davide runs `node scripts/lint-vocab.mjs .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` and confirms exit 0
    - [ ] Davide either approves (checks all sign-off boxes + fills date) or redlines specific blocks
    - [ ] On approval: `grep -c "\\[x\\] Davide" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` returns ≥ 1
    - [ ] On approval: `grep -cE "Davide approved [0-9]{4}-[0-9]{2}-[0-9]{2}" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md` returns ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q "\\[x\\] Davide" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md && \
      grep -qE "Davide approved [0-9]{4}-[0-9]{2}-[0-9]{2}" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md
    </automated>
  </verify>
  <done>
    Davide has reviewed both `01-PLAGIARISM-AUDIT.md` and `01-REPLACEMENT-COPY.md` end-to-end, run the vocab-lint dry-run, and either (a) checked all sign-off boxes + filled in the approval date in both docs, or (b) redlined specific blocks for rework via Task 3 loop. On approval: BRAND-05 (plagiarism replaced) and BRAND-06 (hero copy authored to brand-spine standard, signed off by Davide before Phase 2 build starts) are both closed. Phase 2 is unblocked.
  </done>
</task>

</tasks>

<verification>
Phase-level checks (run after all tasks complete):

1. **Reference captures present and valid:**
   ```bash
   [ $(wc -c < .planning/reference/societies-snapshot.html) -gt 5000 ] && \
   [ $(wc -c < .planning/reference/societies-text.txt) -gt 1000 ]
   ```

2. **AUDIT doc references the right domain (BRAND-05):**
   ```bash
   grep -q "societies\\.io" .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md && \
   [ $(grep -c "artificialsocieties" .planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md) -eq 0 ]
   ```

3. **COPY doc has all 9 viewport blocks (BRAND-05 + BRAND-06 scope):**
   ```bash
   for v in "hero" "demo" "how-it-works" "three-surfaces" "science" "social-proof" "pricing" "footer" "onboarding"; do
     grep -q "<viewport name=\"$v\"" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md || { echo "MISSING viewport: $v"; exit 1; }
   done
   ```

4. **Hero copy mirrors REQUIREMENTS.md verbatim (BRAND-06 / D-16):**
   ```bash
   grep -q "Predict how your audience will respond. Before you post" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md && \
   grep -q "Virtuna simulates your audience to forecast every video before it ships" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md && \
   grep -q "Trained on decades of behavioral research. Self-improving with every outcome" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md && \
   grep -q "VIRTUNA · A NUMEN MACHINES PRODUCT" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md
   ```

5. **Vocab compliance (links Plan 01 + Plan 04):**
   ```bash
   node scripts/lint-vocab.mjs .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md
   ```

6. **D-15 sign-off captured (after Task 3 approval):**
   ```bash
   grep -q "\\[x\\] Davide" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md && \
   grep -qE "Davide approved [0-9]{4}-[0-9]{2}-[0-9]{2}" .planning/phases/01-brand-spine-visual-metaphor/01-REPLACEMENT-COPY.md
   ```
</verification>

<success_criteria>
- [ ] BRAND-05 verified: plagiarized copy catalogued in 01-PLAGIARISM-AUDIT.md with file:line citations and severity tiers; replacement copy authored in 01-REPLACEMENT-COPY.md
- [ ] BRAND-06 verified: hero copy mirrors REQUIREMENTS.md verbatim per D-16; Davide D-15 batch sign-off captured
- [ ] D-12 scope honored: landing + onboarding + dashboard-visible covered; admin/dev/legal excluded
- [ ] D-14 method honored: Wayback diff + tone-mimicry pass documented
- [ ] D-15 sign-off model honored: single batch approval at end of phase
- [ ] D-16 honored: REQUIREMENTS.md hero copy is the verbatim starting point
- [ ] Pitfall 1 avoided: domain is societies.io, NOT artificialsocieties.io (zero matches for the latter)
- [ ] Pitfall 2 avoided: capture is HTML not gzip
- [ ] Plan 01 dependency: BRAND-SPINE.md exists and §3/§4 are the rubric
- [ ] Plan 04 dependency: scripts/lint-vocab.mjs returns exit 0 against the COPY doc
- [ ] Reference captures preserved on disk for reproducibility
</success_criteria>

<output>
After completion (post Davide approval), create `.planning/phases/01-brand-spine-visual-metaphor/01-03-SUMMARY.md` with:
- Reference captures created (paths + sizes)
- AUDIT findings summary: count of HIGH / MEDIUM / LOW per section
- COPY viewports drafted: 9 total (hero, demo, how-it-works, three-surfaces, science, social-proof, pricing, footer, onboarding) + OG metadata
- Hero copy mirroring confirmed verbatim from REQUIREMENTS.md
- Vocab-lint dry-run result (errors / warnings count)
- Davide approval status (date + APPROVED or REDLINE iterations)
- Phase 2 unblock note: "Phase 2 hero build can now consume hero strings from 01-REPLACEMENT-COPY.md §hero"
</output>
