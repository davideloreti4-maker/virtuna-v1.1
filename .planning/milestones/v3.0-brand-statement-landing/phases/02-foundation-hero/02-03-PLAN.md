---
phase: 02-foundation-hero
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md
  - BRAND-BIBLE.md
autonomous: true
requirements: [BUILD-02]
must_haves:
  truths:
    - "02-EXTERNAL-COMPONENT-POLICY.md exists in the phase directory"
    - "Policy doc declares ZERO external imports (Magic UI / Aceternity / Origin UI / Cult UI) for Phase 2 hero"
    - "Policy doc contains a REJECT criteria matrix with 6 numbered criteria"
    - "Policy doc contains an ACCEPT criteria matrix with 7 numbered criteria for future phases"
    - "Policy doc inventories the four surveyed libraries with their distinctive aesthetic + Raycast-fit assessment"
    - "BRAND-BIBLE.md §Visual Metaphor Lock references the policy doc as the Phase 2 lock with a 'See external component policy' line"
  artifacts:
    - path: ".planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md"
      provides: "Phase-2-scoped REJECT/ACCEPT matrix for external UI library imports"
      contains: "REJECT"
    - path: "BRAND-BIBLE.md"
      provides: "Updated cross-reference line under §Visual Metaphor Lock pointing to policy doc"
      contains: "external component policy"
  key_links:
    - from: "BRAND-BIBLE.md §Visual Metaphor Lock"
      to: ".planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md"
      via: "markdown link"
      pattern: "02-EXTERNAL-COMPONENT-POLICY\\.md"
---

<objective>
Author the external component vetting policy doc that BUILD-02 requires, and add a single cross-reference line to BRAND-BIBLE.md §Visual Metaphor Lock so the policy is discoverable from the canonical design language doc (orchestrator decision #5).

Purpose: BUILD-02 calls for "external component imports vetted; rejection criteria documented." RESEARCH.md §3 produced the inventory + criteria matrix; this plan converts that research into the canonical phase artifact and wires the BRAND-BIBLE cross-reference. Phases 3-6 will consult this doc before considering any Magic UI / Aceternity / Origin UI / Cult UI imports.

Output: 1 new policy doc (~150 LOC) under `.planning/phases/02-foundation-hero/`, 1 line added to BRAND-BIBLE.md under §Visual Metaphor Lock. The orchestrator decision #5 explicitly says do NOT add to PROJECT.md; per-phase CONTEXT.md cross-refs happen organically as future phases discover the doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-foundation-hero/02-CONTEXT.md
@.planning/phases/02-foundation-hero/02-RESEARCH.md
@BRAND-BIBLE.md
@.planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 02-EXTERNAL-COMPONENT-POLICY.md</name>
  <files>.planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md</files>
  <read_first>
    - `.planning/phases/02-foundation-hero/02-RESEARCH.md` §3 lines 439-494 (full REJECT/ACCEPT matrix, surveyed libraries inventory, Phase 2 explicit policy, future phase considerations)
    - `.planning/phases/02-foundation-hero/02-CONTEXT.md` D-18, D-19, D-20 (locked policy decisions — ZERO imports for Phase 2; rejection criteria + future ACCEPT criteria scope)
    - `.planning/phases/01-brand-spine-visual-metaphor/01-PLAGIARISM-AUDIT.md` if it exists — closest analog for a phase-scoped audit/policy doc (same dir, same `[NN]-` prefix pattern, same role)
    - `BRAND-BIBLE.md` lines 1-555 (need to understand "Raycast restraint" so policy criteria reference the right aesthetic anchor)
  </read_first>
  <action>
Create `.planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md` following RESEARCH.md §5 skeleton (lines 1085-1123). The doc must include the following sections in order:

**Header block:**

```markdown
# Phase 2: Foundation & Hero - External Component Policy

**Documented:** 2026-05-10
**Phase:** 02-foundation-hero
**Requirement:** BUILD-02
**Decision lineage:** CONTEXT.md D-18, D-19, D-20; RESEARCH.md §3
```

**`<scope>` section:** Explains the doc is the BUILD-02 artifact that codifies REJECT criteria for Magic UI / Aceternity / Origin UI / Cult UI imports during the Brand Statement Landing milestone (Phases 2-6). State that Phase 2 explicitly rejects ALL imports from these libraries; future phases MAY revisit per the ACCEPT criteria.

**`<surveyed_libraries>` section:** Inventory table (4 rows) from RESEARCH.md §3 lines 445-450 verbatim. Columns: Library | Site | What they ship | Distinctive aesthetic | Fit for Raycast restraint? — Each row covers Magic UI (magicui.design), Aceternity UI (ui.aceternity.com), Origin UI (coss.com/origin), Cult UI (cult-ui.com).

**`<criteria>` section with two sub-tables:**

REJECT Criteria table (6 numbered rows R1-R6) verbatim from RESEARCH.md §3 lines 454-461. Each row: # | Criterion | Concrete examples (real components). The 6 criteria cover: R1 maximalist motion components, R2 ships own design tokens, R3 peer-dep conflicts, R4 bundle delta >10 KB gzipped, R5 a11y gaps, R6 forces ad-hoc styling overrides.

ACCEPT Criteria table (7 numbered rows A1-A7) verbatim from RESEARCH.md §3 lines 466-475. Each row: # | Criterion. The 7 criteria cover: A1 single-component copy-paste, A2 restyled to coral + Raycast tokens, A3 vocab-lint passes, A4 bundle delta verified, A5 a11y verified, A6 reference-fidelity check, A7 no peer-dep conflict.

**`<phase2_policy>` section:** Hard zero-imports rule with the 7 allowed sources verbatim from RESEARCH.md §3 lines 479-487:
1. Raw Tailwind v4 utility classes
2. Existing `src/components/ui/button.tsx` (CVA Button)
3. Existing `src/components/hive/use-canvas-resize.ts` (hook reuse)
4. Existing `src/hooks/usePrefersReducedMotion.ts` (hook reuse)
5. Inline Canvas 2D API (no library)
6. CSS `radial-gradient` (no library)
7. `motion/react` for any future scroll-reveal motion (NOT needed in Phase 2 hero)

**`<future_phases>` section:** Notes for Phase 3 (Demo + How It Works + Bento — pipeline diagram uses SVG + `motion/react`; bento reuses existing `extension-card.tsx`) and Phase 4 (Marquee for testimonials — reuses existing `marquee.tsx`). State: no external library needs identified through Phase 4; reassessment trigger is "a planner identifies a component that genuinely matches all 7 ACCEPT criteria".

**`## Sign-off` section:** Two checkboxes:
- `[ ] Davide reviewed policy`
- `[ ] Phase 2 hero ships zero external imports — verified by grep against src/`

The doc is plain markdown with HTML-tag-style section wrappers (matches `01-PATTERNS.md` style per RESEARCH.md §5). Total length ~150 LOC.

Banned terms must NOT appear: "viral", "AI" (whole-word), "go viral". The doc may quote those terms inside `<criteria>` examples only if specifically required — RESEARCH.md §3 does not require them, so omit. NOTE: Filenames under `.planning/` are NOT in the lint-vocab scan dirs (`src/app src/components/landing src/components/onboarding`), so vocab-lint will not flag this file — but maintain hygiene anyway.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md` returns 0
    - `grep -q "REJECT" .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md` → match
    - `grep -q "ACCEPT" .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md` → match
    - `grep -q "BUILD-02" .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md` → traceability to requirement
    - `grep -q "Magic UI" .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md` → library inventory present
    - `grep -q "Aceternity" .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md` → library inventory present
    - `grep -q "Origin UI" .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md` → library inventory present
    - `grep -q "Cult UI" .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md` → library inventory present
    - `grep -c "^| R[0-9]" .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md` returns 6 (six REJECT rows)
    - `grep -c "^| A[0-9]" .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md` returns 7 (seven ACCEPT rows)
    - `grep -q "ZERO" .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md` → zero-imports policy stated explicitly
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md && grep -q "REJECT" .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md && grep -q "ACCEPT" .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md && [ "$(grep -c '^| R[0-9]' .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md)" -eq 6 ] && [ "$(grep -c '^| A[0-9]' .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md)" -eq 7 ]</automated>
  </verify>
  <done>Policy doc exists with 6 REJECT rows, 7 ACCEPT rows, all four libraries inventoried, ZERO-imports policy stated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add cross-reference line to BRAND-BIBLE.md §Visual Metaphor Lock</name>
  <files>BRAND-BIBLE.md</files>
  <read_first>
    - `BRAND-BIBLE.md` lines 352-555 (the §Visual Metaphor Lock addendum from Phase 1 — find the natural insertion point near the top of the addendum where "Phase 2 lock" notes belong)
    - `.planning/phases/02-foundation-hero/02-CONTEXT.md` canonical_refs section (lines 159-217 — confirms BRAND-BIBLE addendum is the discoverable cross-ref spot per orchestrator decision #5)
  </read_first>
  <action>
Locate the §Visual Metaphor Lock addendum in BRAND-BIBLE.md (starts around line 352). Find an appropriate insertion point in the addendum header area (after the section opening but before any code/spec blocks).

Add a single one-line cross-reference statement, using the same tone and format as the addendum's existing reference lines. Use a markdown link to the policy doc:

```
> Phase 2 lock — see [external component policy](.planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md) for the BUILD-02 REJECT/ACCEPT criteria covering Magic UI / Aceternity / Origin UI / Cult UI imports.
```

Insert this as a blockquote (per orchestrator decision #5: "Reference from BRAND-BIBLE.md under §Visual Metaphor Lock as a 'Phase 2 lock — see external component policy' line"). The blockquote format makes it visually distinct from the surrounding spec content and signals "this is a phase-scoped lock that lives outside this doc."

Do NOT add the policy doc reference to PROJECT.md (orchestrator decision #5 explicitly forbids — keeps PROJECT.md stable). Do NOT modify the addendum's other content; this is a single-line addition.

If `.planning/codebase/STRUCTURE.md` exists and a "See also" pattern is already present, consider adding a one-line `See also: .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md` entry (orchestrator decision #5 says "Add a small 'See also' line to .planning/codebase/STRUCTURE.md if it makes sense" — discretion). If STRUCTURE.md has no such pattern or doesn't exist, skip — this is optional polish, not required.
  </action>
  <acceptance_criteria>
    - `grep -q "external component policy" BRAND-BIBLE.md` → match present (case-insensitive — `-i` not needed since the line uses lowercase)
    - `grep -q "02-EXTERNAL-COMPONENT-POLICY" BRAND-BIBLE.md` → path reference present
    - `grep -q "Visual Metaphor Lock" BRAND-BIBLE.md` → existing section still present (not accidentally deleted)
    - File line count grew by 1-3 lines from the previous state (single-line addition, possibly with surrounding blank lines)
    - `! grep -q "external component policy" PROJECT.md` → policy NOT added to PROJECT.md (orchestrator decision #5)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "external component policy" BRAND-BIBLE.md && grep -q "02-EXTERNAL-COMPONENT-POLICY" BRAND-BIBLE.md && grep -q "Visual Metaphor Lock" BRAND-BIBLE.md && ! grep -q "external component policy" PROJECT.md</automated>
  </verify>
  <done>BRAND-BIBLE.md has one new line under §Visual Metaphor Lock pointing to the policy doc; PROJECT.md untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning doc -> future planners | This policy doc is consumed by future-phase planners/researchers when evaluating imports — its accuracy gates supply-chain decisions |
| BRAND-BIBLE.md edit -> repo state | Cross-ref line is markdown link only; no executable content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-09 | Information Disclosure (I) | External library inventory | accept | Library names + sites are public information. No internal architecture or vulnerability data exposed. |
| T-2-10 | Tampering (T) | Future plans bypass the policy | mitigate | Plan 04's `BehavioralHero.tsx` imports MUST match the 7 allowed sources from §phase2_policy. Plan 04 acceptance criteria includes a `grep` check that rejects any import from `magic-ui`, `aceternity-ui`, `origin-ui`, `cult-ui` packages. Phase-checker enforces. |
| T-2-11 | Repudiation (R) | Davide review of policy | mitigate | Policy doc ends with explicit sign-off checkboxes for Davide review. Manual gate before phase verification per VALIDATION.md "Manual-Only Verifications" table row "External component policy applied". |
</threat_model>

<verification>
```bash
# Doc exists with required structure
test -f .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md && \
grep -q "REJECT" .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md && \
grep -q "ACCEPT" .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md && \
grep -q "ZERO" .planning/phases/02-foundation-hero/02-EXTERNAL-COMPONENT-POLICY.md && \

# Cross-ref wired correctly
grep -q "02-EXTERNAL-COMPONENT-POLICY" BRAND-BIBLE.md && \
! grep -q "external component policy" PROJECT.md && \

echo "Plan 03 OK"
```
</verification>

<success_criteria>
- Policy doc exists in `.planning/phases/02-foundation-hero/`
- Doc contains 6-row REJECT matrix + 7-row ACCEPT matrix
- Doc inventories all four surveyed libraries (Magic UI / Aceternity / Origin UI / Cult UI)
- Doc states ZERO-imports policy for Phase 2 hero explicitly
- BRAND-BIBLE.md §Visual Metaphor Lock has one new line linking to the policy doc
- PROJECT.md NOT modified (orchestrator decision #5)
</success_criteria>

<output>
After completion, create `.planning/phases/02-foundation-hero/02-03-SUMMARY.md` documenting:
- Policy doc LOC and section structure
- One-line BRAND-BIBLE.md addition + verification PROJECT.md untouched
- Manual gate: Davide review pending (per VALIDATION.md "Manual-Only Verifications")
- VALIDATION.md Per-Task Verification Map row entries for 02-03-01, 02-03-02
</output>
