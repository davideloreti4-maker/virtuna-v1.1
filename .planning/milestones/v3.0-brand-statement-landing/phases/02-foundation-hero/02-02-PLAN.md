---
phase: 02-foundation-hero
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/behavioral-hero-constants.ts
  - src/components/landing/BehavioralCanvas.tsx
  - src/components/landing/__tests__/behavioral-hero-constants.test.ts
autonomous: true
requirements: [HERO-06, HERO-07, HERO-08, HERO-10]
must_haves:
  truths:
    - "behavioral-hero-constants.ts exports particle/motion/chip/gradient values as named constants"
    - "BehavioralCanvas renders a canvas element with role=img and aria-label declaring 87 percent"
    - "BehavioralCanvas uses module-level flag named behavioralHeroAnimationComplete (NOT globalAnimationComplete)"
    - "BehavioralCanvas reuses useCanvasResize and usePrefersReducedMotion verbatim"
    - "Canvas paints particles ONLY; the 87 percent chip is rendered by Plan 04 as a DOM overlay"
    - "Animation is one-shot, ~2.2s, easeOutCubic, drift+attract per D-36"
    - "Reduced-motion path skips RAF and renders converged particle positions immediately"
    - "Mobile branch (width < 640px) uses 120 particles with reduced Brownian sigma; desktop uses 250"
    - "Vitest test asserts mobile<desktop, percentage 80-95, animation 2000-2400ms, easeOutCubic boundaries"
  artifacts:
    - path: "src/components/landing/behavioral-hero-constants.ts"
      provides: "All numeric constants + HERO_GRADIENT string + easeOutCubic function"
      contains: "easeOutCubic"
    - path: "src/components/landing/BehavioralCanvas.tsx"
      provides: "Client-component canvas with one-shot drift+attract animation"
      contains: "behavioralHeroAnimationComplete"
    - path: "src/components/landing/__tests__/behavioral-hero-constants.test.ts"
      provides: "Vitest invariant suite for constants"
      contains: "describe"
  key_links:
    - from: "src/components/landing/BehavioralCanvas.tsx"
      to: "src/components/hive/use-canvas-resize"
      via: "import"
      pattern: "from '@/components/hive/use-canvas-resize'"
    - from: "src/components/landing/BehavioralCanvas.tsx"
      to: "src/hooks/usePrefersReducedMotion"
      via: "import"
      pattern: "from '@/hooks/usePrefersReducedMotion'"
    - from: "src/components/landing/BehavioralCanvas.tsx"
      to: "src/components/landing/behavioral-hero-constants"
      via: "import"
      pattern: "from './behavioral-hero-constants'"
---

<objective>
Build the behavioral-simulation Canvas 2D viz and its colocated constants/gradient file, plus a Vitest invariant suite. The canvas paints 250 (desktop) / 120 (mobile, width < 640px) particles using drift+attract motion converging toward the upper-center where Plan 04 will DOM-overlay a confidence chip. The constants file also exports `HERO_GRADIENT` — the radial-gradient string consumed by Plan 04 for the ambient backdrop.

Purpose: Satisfies HERO-06 (behavioral-simulation visual), HERO-08 (mobile graceful scaling), HERO-10 (no banned terms) and provides the gradient string + chip constants for HERO-07. The DOM-accessible chip (orchestrator decision #6) is rendered by Plan 04 — canvas only handles particles + reduced-motion converged state.

Output: 1 constants file (~85 LOC), 1 canvas client component (~150 LOC), 1 Vitest test (~30 LOC). The canvas itself is not unit-tested (orchestrator decision #4) — manual viewport observation + reduced-motion toggle covers it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-foundation-hero/02-CONTEXT.md
@.planning/phases/02-foundation-hero/02-RESEARCH.md
@.planning/phases/02-foundation-hero/02-VALIDATION.md
@./CLAUDE.md
@src/components/hive/HiveCanvas.tsx
@src/components/hive/use-canvas-resize.ts
@src/components/hive/use-hive-animation.ts
@src/components/hive/hive-renderer.ts
@src/components/hive/hive-constants.ts
@src/hooks/usePrefersReducedMotion.ts
@docs/tokens.md
@BRAND-BIBLE.md

<interfaces>
Key contracts from the existing codebase:

From `src/components/hive/use-canvas-resize.ts`:
```typescript
export function useCanvasResize(
  canvasRef: React.RefObject<HTMLCanvasElement | null>,
  onResize: () => void
): React.RefObject<{ width: number; height: number; dpr: number }>;
```

From `src/hooks/usePrefersReducedMotion.ts`:
```typescript
export function usePrefersReducedMotion(): boolean;
// Returns true if user prefers reduced motion. Defaults to true (SSR safety).
// Flips to false once matchMedia('(prefers-reduced-motion: no-preference)') matches.
```

From `src/components/hive/use-hive-animation.ts` (line 43 — DO NOT collide with this name):
```typescript
let globalAnimationComplete = false;
// New canvas declares its own file-scoped `behavioralHeroAnimationComplete`.
```

Color batching pattern from `src/components/hive/hive-renderer.ts:240-298`:
```typescript
const colorGroups = new Map<string, Particle[]>();
for (const p of particles) {
  let group = colorGroups.get(p.color);
  if (!group) { group = []; colorGroups.set(p.color, group); }
  group.push(p);
}
for (const [color, particles] of colorGroups) {
  ctx.fillStyle = color;
  ctx.beginPath();
  for (const p of particles) {
    ctx.moveTo(p.x + p.size, p.y);
    ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
  }
  ctx.fill();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create behavioral-hero-constants.ts</name>
  <files>src/components/landing/behavioral-hero-constants.ts</files>
  <behavior>
    - `PARTICLE_COUNTS.desktop = 250`, `PARTICLE_COUNTS.mobile = 120` (mobile less than desktop)
    - `PARTICLE_SIZES = { min: 2, max: 3, mobileScale: 0.85 }`
    - `PARTICLE_COLORS = { coral: '#FF7F50', neutral: '#9c9c9d', coralRatio: 0.7 }`
    - `PARTICLE_MOTION` carries Brownian sigma (8 desktop, 6 mobile), attractor 1.4, damping 0.92, duration 2200ms, target offset Y = -0.05
    - `CONFIDENCE_CHIP` has percentage 87, label "87%", coral rgba bg/border, text #FF7F50, 12px radius, weight 500, size 16, padding 16/8
    - `HERO_GRADIENT` is the exact radial-gradient string with ellipse 90% 70% at 50% 35%, coral at 0.18/0.10/0.04 stops, #07080a at 70%
    - `easeOutCubic(t) = 1 - Math.pow(1 - t, 3)` (verbatim match to use-hive-animation.ts:57)
    - All object exports use `as const`; no `any` types; no banned terms
  </behavior>
  <read_first>
    - `src/components/hive/hive-constants.ts` (closest analog — kebab-case filename, `as const` object exports, JSDoc on tunable values, grouped by domain)
    - `src/components/hive/use-hive-animation.ts` lines 55-59 (verbatim source for `easeOutCubic`)
    - `.planning/phases/02-foundation-hero/02-RESEARCH.md` §2 D-34 (87%), D-36 (motion equations), D-37 (gradient string), D-38 (full constants file contents at lines 350-401)
    - `./CLAUDE.md` "Tailwind v4 oklch inaccuracy" rule — coral MUST be hex `#FF7F50` and rgba(255, 127, 80, alpha), NOT round-tripped through oklch()
  </read_first>
  <action>
Create the file with the literal content from RESEARCH.md §2 D-38 (lines 350-401). The file must export, in this order:

1. `PARTICLE_COUNTS` — object with `desktop: 250`, `mobile: 120`, `as const`
2. `PARTICLE_SIZES` — object with `min: 2`, `max: 3`, `mobileScale: 0.85`, `as const`
3. `PARTICLE_COLORS` — object with `coral: '#FF7F50'`, `neutral: '#9c9c9d'`, `coralRatio: 0.7`, `as const`
4. `PARTICLE_MOTION` — object with `brownianSigmaPxPerSec: 8`, `brownianSigmaMobile: 6`, `attractorPeakStrength: 1.4`, `damping: 0.92`, `animationDurationMs: 2200`, `targetOffsetY: -0.05`, `as const`
5. `CONFIDENCE_CHIP` — object with `percentage: 87`, `label: '87%'`, `bgColor: 'rgba(255, 127, 80, 0.15)'`, `borderColor: 'rgba(255, 127, 80, 0.4)'`, `textColor: '#FF7F50'`, `borderRadius: 12`, `fontWeight: 500`, `fontSizePx: 16`, `paddingX: 16`, `paddingY: 8`, `as const`
6. `HERO_GRADIENT` — exact string literal: `radial-gradient(ellipse 90% 70% at 50% 35%, rgba(255, 127, 80, 0.18) 0%, rgba(255, 127, 80, 0.10) 18%, rgba(255, 127, 80, 0.04) 38%, #07080a 70%)` with `as const`
7. `easeOutCubic(t: number): number` — function returning `1 - Math.pow(1 - t, 3)`

Add file header comments:
- Line 1: file purpose
- Line 2: reference to CONTEXT.md D-30/D-38
- Note: coral uses exact hex `#FF7F50` (NOT oklch) per CLAUDE.md known issue; alpha values use rgba(255, 127, 80, a) form

Group exports with section comment dividers matching `hive-constants.ts` style (lines of dashes). Add JSDoc-style line comments above each export explaining the source decision ID (D-05, D-09, D-10, D-34, D-36, D-37).

Do NOT add a default export. Do NOT import from `framer-motion`. Do NOT use `any`. Strings "viral", "AI" (whole-word), or "go viral" must NOT appear anywhere in the file.
  </action>
  <acceptance_criteria>
    - `test -f src/components/landing/behavioral-hero-constants.ts` returns 0
    - `grep -q "export const PARTICLE_COUNTS" src/components/landing/behavioral-hero-constants.ts`
    - `grep -q "desktop: 250" src/components/landing/behavioral-hero-constants.ts`
    - `grep -q "mobile: 120" src/components/landing/behavioral-hero-constants.ts`
    - `grep -q "export const HERO_GRADIENT" src/components/landing/behavioral-hero-constants.ts`
    - `grep -q "radial-gradient(ellipse 90% 70% at 50% 35%" src/components/landing/behavioral-hero-constants.ts`
    - `grep -q "export function easeOutCubic" src/components/landing/behavioral-hero-constants.ts`
    - `grep -q "1 - Math.pow(1 - t, 3)" src/components/landing/behavioral-hero-constants.ts`
    - `grep -q "#FF7F50" src/components/landing/behavioral-hero-constants.ts`
    - `grep -q "percentage: 87" src/components/landing/behavioral-hero-constants.ts`
    - `grep -q "animationDurationMs: 2200" src/components/landing/behavioral-hero-constants.ts`
    - `grep -vE '^\s*(//|\*|/\*)' src/components/landing/behavioral-hero-constants.ts | grep -cE '\bAI\b|viral' | grep -q '^0$'` (zero banned terms in non-comment lines)
    - `pnpm lint:vocab` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f src/components/landing/behavioral-hero-constants.ts && grep -q "export const PARTICLE_COUNTS" src/components/landing/behavioral-hero-constants.ts && grep -q "export const HERO_GRADIENT" src/components/landing/behavioral-hero-constants.ts && grep -q "1 - Math.pow(1 - t, 3)" src/components/landing/behavioral-hero-constants.ts && grep -q "radial-gradient(ellipse 90% 70% at 50% 35%" src/components/landing/behavioral-hero-constants.ts && pnpm lint:vocab</automated>
  </verify>
  <done>Constants file exists with all required exports; lint-vocab clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create BehavioralCanvas.tsx client component</name>
  <files>src/components/landing/BehavioralCanvas.tsx</files>
  <behavior>
    - Renders a single `<canvas>` element with `role="img"` and the aria-label "Audience particles aggregating into a confidence score of 87 percent"
    - On mount: initializes count particles (250 desktop / 120 mobile) at uniform random positions; 70% coral / 30% neutral; size random in 2-3px range times mobileScale on mobile
    - If `usePrefersReducedMotion()` returns true OR module-level `behavioralHeroAnimationComplete` already true: skip RAF, cluster particles around target with small radius jitter, set flag true, render once
    - Otherwise: start RAF for ~2.2s. Each frame: Brownian Gaussian velocity perturbation + attractor force toward target scaled by easedProgress + damping; advance position by velocity * dt
    - On animation complete: set `behavioralHeroAnimationComplete = true`, render final frame, RAF stops
    - Canvas paints particles ONLY — no fillText, no chip drawn in canvas (chip is DOM overlay in Plan 04)
    - On unmount: cancel RAF
    - Module-level flag is named `behavioralHeroAnimationComplete` (file-scoped, NOT exported)
    - No `background-color` on canvas element (transparent so parent gradient shows through — Pitfall 10)
    - Imports `useCanvasResize` from `@/components/hive/use-canvas-resize` (REUSE, no duplication)
    - Imports `usePrefersReducedMotion` from `@/hooks/usePrefersReducedMotion` (REUSE, no duplication)
    - Imports constants from `./behavioral-hero-constants`
  </behavior>
  <read_first>
    - `src/components/hive/HiveCanvas.tsx` lines 53-186 (refs declaration, `render()` callback, `useCanvasResize` call signature, ctx.clearRect/scale/DPR usage)
    - `src/components/hive/use-hive-animation.ts` lines 42-49 + 121-193 (module-level flag pattern + RAF lifecycle with reduced-motion early return)
    - `src/components/hive/hive-renderer.ts` lines 240-298 (color batching via Map)
    - `src/hooks/usePrefersReducedMotion.ts` (29 lines — verbatim reuse, returns boolean)
    - `.planning/phases/02-foundation-hero/02-RESEARCH.md` §5 BehavioralCanvas analog block (lines 822-1004 — full reference implementation, BUT do NOT include the `drawConfidenceChip` and `roundRect` helpers — those are removed because chip is DOM in Plan 04)
    - `.planning/phases/02-foundation-hero/02-RESEARCH.md` §6 Pitfall 5 (flag name MUST be `behavioralHeroAnimationComplete`)
    - `.planning/phases/02-foundation-hero/02-RESEARCH.md` §6 Pitfall 6 (RSC boundary — `'use client'` at top of THIS file)
    - `.planning/phases/02-foundation-hero/02-RESEARCH.md` §6 Pitfall 10 (canvas seam — no background-color, ctx.clearRect at frame start)
  </read_first>
  <action>
Create `src/components/landing/BehavioralCanvas.tsx` following RESEARCH.md §5 reference implementation with these modifications based on orchestrator decisions:

**Header:** `'use client';` directive at top (line 1). File header comments describing purpose, reuse list, and note about the chip being rendered by Plan 04 as DOM overlay.

**Imports:**
- `useCallback, useEffect, useRef` from `'react'`
- `useCanvasResize` from `'@/components/hive/use-canvas-resize'`
- `usePrefersReducedMotion` from `'@/hooks/usePrefersReducedMotion'`
- Named imports `PARTICLE_COUNTS, PARTICLE_SIZES, PARTICLE_COLORS, PARTICLE_MOTION, easeOutCubic` from `'./behavioral-hero-constants'` — note: do NOT import `CONFIDENCE_CHIP` (chip is DOM in Plan 04)

**Module-level flag:** `let behavioralHeroAnimationComplete = false;` immediately after imports. Do NOT export.

**Particle type:** `interface Particle { x, y, vx, vy: number; color: string; size: number; }`

**Helper:** `function gaussian(): number` — Box-Muller transform: `Math.sqrt(-2 * Math.log(Math.random())) * Math.cos(2 * Math.PI * Math.random())`. Used for Brownian velocity perturbation.

**Props:** `interface BehavioralCanvasProps { className?: string; }`

**Component:** `export function BehavioralCanvas({ className }: BehavioralCanvasProps): React.JSX.Element`

**Refs:** `canvasRef`, `particlesRef` (Particle[]), `startTimeRef` (number), `lastFrameRef` (number)

**`reducedMotion`:** call `usePrefersReducedMotion()` and store in const.

**`render` callback (useCallback with empty deps array):**
- Get canvas + ctx; early return if missing
- Read `sizeRef.current` for `{ width, height, dpr }`; early return if width or height <= 0
- `ctx.clearRect(0, 0, canvas.width, canvas.height)` — clear full buffer (pixel space, not CSS pixels)
- `ctx.save(); ctx.scale(dpr, dpr);`
- Color batching: build `Map<string, Particle[]>` keyed by particle color, then per group: `ctx.fillStyle = color; ctx.beginPath(); for each particle ctx.moveTo + ctx.arc; ctx.fill();`
- `ctx.restore();`
- NO chip drawing — particles only. NO ctx.fillText, NO ctx.strokeText.

**`sizeRef`:** `const sizeRef = useCanvasResize(canvasRef, render);` — same call signature as HiveCanvas.tsx:189.

**`useEffect` (deps `[reducedMotion, render]`):**
- Read `{ width, height }` from `sizeRef.current`; early return if 0
- Compute `isMobile = width < 640`, `count`, `sigmaScale`, `sizeMultiplier` from constants
- Initialize `particlesRef.current` via `Array.from({ length: count }, () => { /* uniform random + 70% coral / 30% neutral + size with multiplier */ })`
- Compute `target = { x: width / 2, y: height * (0.5 + PARTICLE_MOTION.targetOffsetY) }`
- Branch A (`reducedMotion || behavioralHeroAnimationComplete`):
  - For each particle: place at `target + Math.cos(angle) * r, Math.sin(angle) * r` where `angle = random * 2π`, `r = random * (min(width, height) * 0.12)`
  - Set `behavioralHeroAnimationComplete = true`
  - Call `render()` once
  - Return (no RAF)
- Branch B (animation path):
  - `startTimeRef.current = performance.now(); lastFrameRef.current = startTimeRef.current;`
  - Declare local `let rafId = 0;`
  - Define `tick(now: number): void`:
    - `elapsed = now - startTimeRef.current`
    - `dt = (now - lastFrameRef.current) / 1000`
    - `lastFrameRef.current = now`
    - `t = Math.min(1, elapsed / PARTICLE_MOTION.animationDurationMs)`
    - `easedProgress = easeOutCubic(t)`
    - For each particle: Brownian (add `gaussian() * sigmaScale * dt` to vx and vy independently); attractor (`p.vx += (target.x - p.x) * k * dt` where `k = PARTICLE_MOTION.attractorPeakStrength * easedProgress`; same for vy); damping (`p.vx *= PARTICLE_MOTION.damping`); apply position (`p.x += p.vx * dt`)
    - `render()`
    - If `t < 1`: `rafId = requestAnimationFrame(tick);` else: `behavioralHeroAnimationComplete = true; render();` (final draw)
  - `rafId = requestAnimationFrame(tick);`
  - Return cleanup `() => cancelAnimationFrame(rafId);`

**JSX:** Single `<canvas>` element with:
- `ref={canvasRef}`
- `className={className}` (allows parent layout to pass sizing classes)
- `style={{ width: '100%', height: '100%', touchAction: 'none' }}` — NO `background-color` (Pitfall 10)
- `role="img"`
- `aria-label="Audience particles aggregating into a confidence score of 87 percent"`

Do NOT render any chip element inside this component. Do NOT export the flag. Do NOT import `framer-motion`. Do NOT add `motion/react` import (canvas drives its own RAF; no motion library needed in Phase 2). No banned terms ("viral", "AI" whole-word) anywhere in the file.
  </action>
  <acceptance_criteria>
    - `test -f src/components/landing/BehavioralCanvas.tsx` returns 0
    - `grep -q "'use client'" src/components/landing/BehavioralCanvas.tsx`
    - `grep -q "let behavioralHeroAnimationComplete" src/components/landing/BehavioralCanvas.tsx`
    - `! grep -q "globalAnimationPlayed" src/components/landing/BehavioralCanvas.tsx`
    - `! grep -q "let globalAnimationComplete" src/components/landing/BehavioralCanvas.tsx` (must not redeclare hive's flag)
    - `grep -q "from '@/components/hive/use-canvas-resize'" src/components/landing/BehavioralCanvas.tsx`
    - `grep -q "from '@/hooks/usePrefersReducedMotion'" src/components/landing/BehavioralCanvas.tsx`
    - `grep -q "from './behavioral-hero-constants'" src/components/landing/BehavioralCanvas.tsx`
    - `grep -q "Audience particles aggregating into a confidence score of 87 percent" src/components/landing/BehavioralCanvas.tsx`
    - `grep -q 'role="img"' src/components/landing/BehavioralCanvas.tsx`
    - `! grep -E "fillText|strokeText" src/components/landing/BehavioralCanvas.tsx` (no canvas-drawn text — chip is DOM in Plan 04)
    - `grep -vE '^\s*(//|\*|/\*)' src/components/landing/BehavioralCanvas.tsx | grep -cE '\bAI\b|viral|framer-motion' | grep -q '^0$'`
    - `pnpm lint:vocab` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f src/components/landing/BehavioralCanvas.tsx && grep -q "'use client'" src/components/landing/BehavioralCanvas.tsx && grep -q "let behavioralHeroAnimationComplete" src/components/landing/BehavioralCanvas.tsx && ! grep -q "globalAnimationPlayed" src/components/landing/BehavioralCanvas.tsx && grep -q "Audience particles aggregating into a confidence score of 87 percent" src/components/landing/BehavioralCanvas.tsx && ! grep -E "fillText|strokeText" src/components/landing/BehavioralCanvas.tsx && pnpm lint:vocab</automated>
  </verify>
  <done>Canvas component exists with correct flag name, hook reuses, no canvas-drawn chip, no banned terms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Vitest invariant suite for behavioral-hero-constants</name>
  <files>src/components/landing/__tests__/behavioral-hero-constants.test.ts</files>
  <behavior>
    - `it('mobile has fewer particles than desktop')` — asserts `PARTICLE_COUNTS.mobile < PARTICLE_COUNTS.desktop`
    - `it('confidence percentage is in plausible range')` — asserts `CONFIDENCE_CHIP.percentage >= 80 && <= 95`
    - `it('animation duration is in 2.0-2.4s window per CONTEXT.md D-07')` — asserts `PARTICLE_MOTION.animationDurationMs >= 2000 && <= 2400`
    - `it('easeOutCubic returns correct boundary values')` — asserts `easeOutCubic(0) === 0`, `easeOutCubic(1) === 1`, `expect(easeOutCubic(0.5)).toBeCloseTo(0.875, 3)`
    - All 4 tests pass when run via `pnpm test -- behavioral-hero-constants`
  </behavior>
  <read_first>
    - `vitest.config.ts` (verify path alias + globals.test config — Vitest 4 is configured)
    - `.planning/phases/02-foundation-hero/02-RESEARCH.md` §4 SC2 lines 567-594 (full test suite contents to copy)
    - `src/components/landing/behavioral-hero-constants.ts` (Task 1 output — the file under test)
  </read_first>
  <action>
Create `src/components/landing/__tests__/behavioral-hero-constants.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import {
  PARTICLE_COUNTS,
  PARTICLE_MOTION,
  CONFIDENCE_CHIP,
  easeOutCubic,
} from '../behavioral-hero-constants';

describe('behavioral-hero-constants', () => {
  it('mobile has fewer particles than desktop', () => {
    expect(PARTICLE_COUNTS.mobile).toBeLessThan(PARTICLE_COUNTS.desktop);
  });

  it('confidence percentage is in plausible range', () => {
    expect(CONFIDENCE_CHIP.percentage).toBeGreaterThanOrEqual(80);
    expect(CONFIDENCE_CHIP.percentage).toBeLessThanOrEqual(95);
  });

  it('animation duration is in 2.0-2.4s window per CONTEXT.md D-07', () => {
    expect(PARTICLE_MOTION.animationDurationMs).toBeGreaterThanOrEqual(2000);
    expect(PARTICLE_MOTION.animationDurationMs).toBeLessThanOrEqual(2400);
  });

  it('easeOutCubic returns correct boundary values', () => {
    expect(easeOutCubic(0)).toBe(0);
    expect(easeOutCubic(1)).toBe(1);
    expect(easeOutCubic(0.5)).toBeCloseTo(0.875, 3);
  });
});
```

The file is colocated under `__tests__/` directory (matches Vitest's default discovery pattern). Imports use relative path `../behavioral-hero-constants` (Vitest resolves this via tsconfig paths). No banned terms ("viral", "AI" whole-word, "framer-motion").
  </action>
  <acceptance_criteria>
    - `test -f src/components/landing/__tests__/behavioral-hero-constants.test.ts` returns 0
    - `grep -q "describe('behavioral-hero-constants'" src/components/landing/__tests__/behavioral-hero-constants.test.ts`
    - `grep -c "it(" src/components/landing/__tests__/behavioral-hero-constants.test.ts` returns 4 (four test cases)
    - `grep -q "PARTICLE_COUNTS.mobile" src/components/landing/__tests__/behavioral-hero-constants.test.ts`
    - `grep -q "easeOutCubic(0.5)" src/components/landing/__tests__/behavioral-hero-constants.test.ts`
    - `pnpm test -- behavioral-hero-constants` exits 0 with all 4 tests passing
    - `pnpm lint:vocab` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f src/components/landing/__tests__/behavioral-hero-constants.test.ts && [ "$(grep -c 'it(' src/components/landing/__tests__/behavioral-hero-constants.test.ts)" -eq 4 ] && pnpm test -- behavioral-hero-constants</automated>
  </verify>
  <done>4 Vitest tests exist and pass; constants invariants enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Module init -> RAF loop | Canvas runs untrusted user-controlled clock (performance.now()); no user input shapes particle data |
| Constants -> components | All constants are compile-time literals; no dynamic config, no env vars, no remote data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-05 | Tampering (T) | `behavioral-hero-constants.ts` numeric tunables | accept | All values are compile-time literals with no runtime configurability. A malicious local edit would surface in code review / pre-commit lint. The Vitest test in Task 3 catches accidental drift in `mobile<desktop`, percentage range, animation duration range, and easeOutCubic boundaries. |
| T-2-06 | Information Disclosure (I) | Canvas aria-label "87 percent" | accept | The percentage is a marketing illustration value, not real prediction data. No PII, no user-specific information painted or announced. Acceptable per orchestrator decision #6 (DOM overlay also uses the same hardcoded value). |
| T-2-07 | Denial of Service (D) | RAF loop in BehavioralCanvas | mitigate | Loop runs for exactly `PARTICLE_MOTION.animationDurationMs` (2200ms) once per session via module-level `behavioralHeroAnimationComplete` flag. Reduced-motion path skips RAF entirely. RAF is cancelled on unmount. Maximum particle count is 250 (compile-time constant); cannot be inflated by user input. |
| T-2-08 | Tampering (T) | Module-level flag persistence | accept | Flag is file-scoped `let` (not exported, not on window). Resets only on full page reload — survives client-side navigation in Next.js App Router. No attack vector. |
</threat_model>

<verification>
After all three tasks complete, run from repo root:

```bash
# 1. Files exist
test -f src/components/landing/behavioral-hero-constants.ts && \
test -f src/components/landing/BehavioralCanvas.tsx && \
test -f src/components/landing/__tests__/behavioral-hero-constants.test.ts && \

# 2. Critical greps pass
grep -q "export const HERO_GRADIENT" src/components/landing/behavioral-hero-constants.ts && \
grep -q "let behavioralHeroAnimationComplete" src/components/landing/BehavioralCanvas.tsx && \
grep -q "Audience particles aggregating into a confidence score of 87 percent" src/components/landing/BehavioralCanvas.tsx && \
! grep -E "fillText|strokeText" src/components/landing/BehavioralCanvas.tsx && \

# 3. Vocab + test green
pnpm lint:vocab && pnpm test -- behavioral-hero-constants && \

echo "Plan 02 OK"
```

Note: `pnpm build` is expected to FAIL after this plan (BehavioralHero from Plan 04 not yet present; Plan 01 swapped page.tsx to import BehavioralHero). Wave 1 is "complete and shippable" only after Plan 04 lands.
</verification>

<success_criteria>
- Constants file exports all 7 named exports (PARTICLE_COUNTS, PARTICLE_SIZES, PARTICLE_COLORS, PARTICLE_MOTION, CONFIDENCE_CHIP, HERO_GRADIENT, easeOutCubic)
- HERO_GRADIENT contains exact radial-gradient signature from RESEARCH.md §2 D-37
- BehavioralCanvas uses module-level `behavioralHeroAnimationComplete` (NOT `globalAnimationComplete`)
- BehavioralCanvas reuses `useCanvasResize` + `usePrefersReducedMotion` verbatim (no duplication)
- Canvas paints particles ONLY (no fillText/strokeText for chip — chip is DOM in Plan 04)
- Vitest suite has 4 passing tests covering mobile<desktop, percentage range, animation window, easing boundaries
- Zero banned terms ("viral", "AI" whole-word, "framer-motion") in any new file
- `pnpm lint:vocab` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-foundation-hero/02-02-SUMMARY.md` documenting:
- Three files created and their LOC counts
- Confirmation: module-level flag is `behavioralHeroAnimationComplete` (not the hive module's `globalAnimationComplete`)
- Confirmation: canvas paints particles only — no fillText/strokeText calls
- Vitest run output showing 4 passing tests
- VALIDATION.md Per-Task Verification Map row entries for 02-02-01, 02-02-02, 02-02-03
</output>
