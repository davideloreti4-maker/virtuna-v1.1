# Plan 03-04: Backers & Features Sections

## Frontmatter
```yaml
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/components/landing/backers-section.tsx
  - src/components/landing/features-section.tsx
  - src/components/landing/feature-card.tsx
  - src/components/landing/index.ts
autonomous: true
```

## Context
Build the "Backed by" and "Features" sections that appear below the hero. These sections showcase investor logos and the four main feature cards in a 2x2 grid. Depends on 03-02 for logo assets.

**v0 MCP Requirement:** Use v0 MCP tool to generate pixel-perfect UI components. Before implementing BackersSection and FeaturesSection, query v0 with `.reference/landing/_assets/features-viewport.png` to get accurate component code that matches societies.io exactly.

## Tasks

<task id="1">
<title>Create BackersSection component</title>
<description>
Create the investors/backers section with logo rows:

**File:** `src/components/landing/backers-section.tsx`

**Layout:**
```
┌─────────────────────────────────────────────────────────┐
│  Backed by                                              │
│  [Point72 Ventures]  [Kindred Capital]  [Y Combinator]  │
│                                                         │
│  With Investors from                                    │
│  [Sequoia] [Google] [DeepMind] [Prolific] [Strava]      │
└─────────────────────────────────────────────────────────┘
```

**Structure:**
```tsx
<section className="py-16 border-t border-white/10">
  <div className="max-w-6xl mx-auto px-6">
    {/* Backed by */}
    <div className="mb-12">
      <span className="text-sm text-foreground-secondary px-4">Backed by</span>
      <div className="flex items-center gap-8 mt-4">
        {/* Logo items with icon + name */}
      </div>
    </div>

    {/* With Investors from */}
    <div>
      <span className="text-sm text-foreground-secondary px-4">With Investors from</span>
      <div className="flex items-center gap-6 mt-4">
        {/* Smaller logo items */}
      </div>
    </div>
  </div>
</section>
```

**Typography:**
- Label text: font-sans text-sm (14px) text-foreground-secondary (#F5F5F5)
- Company names: font-sans text-sm/base text-white

**Logo Display:**
- Use Image component with logos from public/logos/
- Primary backers: larger icons with text beside
- Investor logos: smaller, just icon + name
- All logos white/grayscale for dark mode

**Animation:**
- Scroll-triggered fade in
- Start: opacity-0, translateY(15px)
- End: opacity-1, translateY(0)
- Stagger per logo item

**Responsive:**
- Mobile: Wrap logos, possibly 2 columns
- Desktop: Single row

Reference: `.reference/landing/backers.md`
</description>
</task>

<task id="2">
<title>Create FeatureCard component</title>
<description>
Create a reusable feature card component:

**File:** `src/components/landing/feature-card.tsx`

**Props Interface:**
```ts
interface FeatureCardProps {
  icon: React.ReactNode;
  title: string;
  description: string;
  className?: string;
}
```

**Structure:**
```tsx
<div className="p-8 border border-white/10 rounded-lg">
  <div className="w-8 h-8 mb-6 text-white">
    {icon}
  </div>
  <h3 className="font-display text-lg font-medium text-white mb-3">
    {title}
  </h3>
  <p className="text-foreground-muted text-base leading-relaxed">
    {description}
  </p>
</div>
```

**Styling:**
- Container: p-8 (actually p-[48px_26px] per reference), border border-white/10, rounded-lg
- Icon: 28-32px, white color
- Title: font-display text-lg (18px) font-medium text-white mb-3
- Description: font-sans text-base (16px) leading-[24px] text-foreground-muted (#CCCCCC)

**Animation:**
- Fade in on scroll
- Optional subtle hover effect (border brightness)
</description>
</task>

<task id="3">
<title>Create FeaturesSection component</title>
<description>
Create the features section with header and 2x2 grid:

**File:** `src/components/landing/features-section.tsx`

**Layout:**
```
┌─────────────────────────────────────────────────────────┐
│  Into the future                                        │
│                                                         │
│  Research that was impossible                           │
│  is now instant                                         │
│                                                         │
│  Access high-value audiences. Understand decision-      │
│  makers. Discover critical insights.                    │
│                                                         │
│  ┌──────────────────┐  ┌──────────────────┐            │
│  │ Unreachable      │  │ Instant insights │            │
│  │ audiences        │  │                  │            │
│  └──────────────────┘  └──────────────────┘            │
│  ┌──────────────────┐  ┌──────────────────┐            │
│  │ Millions of      │  │ True             │            │
│  │ personas         │  │ understanding    │            │
│  └──────────────────┘  └──────────────────┘            │
└─────────────────────────────────────────────────────────┘
```

**Section Header:**
```tsx
<div className="mb-16">
  <span className="text-sm text-foreground-secondary">Into the future</span>
  <h2 className="font-display text-[40px] leading-[44px] font-[350] text-white mt-4">
    Research that was impossible<br />
    is now instant
  </h2>
  <p className="text-white/80 text-lg mt-6 max-w-2xl">
    Access high-value audiences. Understand decision-makers. Discover critical insights.
  </p>
</div>
```

**Feature Cards Data:**
```ts
const features = [
  {
    icon: <Crosshair size={28} weight="light" />,
    title: "Unreachable audiences",
    description: "Survey Fortune 500 executives, rare specialists, or hyper-specific demographics that traditional panels cannot access."
  },
  {
    icon: <Lightning size={28} weight="light" />,
    title: "Instant insights",
    description: "Replace weeks of recruitment and fieldwork with instant responses. Run thousands of interviews before your competitor sends one survey."
  },
  {
    icon: <UsersThree size={28} weight="light" />,
    title: "Millions of personas",
    description: "Every persona is demographically and psychographically calibrated, creating responses as nuanced and diverse as real humans."
  },
  {
    icon: <Brain size={28} weight="light" />,
    title: "True understanding",
    description: "Go beyond surface-level answers. Our personas reason, reflect, and respond with the depth of genuine human cognition."
  }
];
```

**Grid:**
```tsx
<div className="grid grid-cols-1 md:grid-cols-2 gap-6">
  {features.map((feature) => (
    <FeatureCard key={feature.title} {...feature} />
  ))}
</div>
```

**Animation:**
- Section header: fade in on scroll
- Cards: staggered fade in

Reference: `.reference/landing/features.md` and `_assets/features-viewport.png`
</description>
</task>

<task id="4">
<title>Update landing components barrel export</title>
<description>
Add new components to the index file:

**File:** `src/components/landing/index.ts`

```ts
export { HeroSection } from "./hero-section";
export { PersonaCard } from "./persona-card";
export { BackersSection } from "./backers-section";
export { FeaturesSection } from "./features-section";
export { FeatureCard } from "./feature-card";
```
</description>
</task>

## Verification

```bash
# Build check
npm run build

# Dev server visual check
npm run dev
```

**Manual checks:**
- [ ] "Backed by" section displays 3 primary investor logos
- [ ] "With Investors from" displays 5 investor logos
- [ ] Features section header typography matches (40px heading)
- [ ] 2x2 grid of feature cards on desktop
- [ ] Feature cards have correct icons from Phosphor
- [ ] Card borders match (border-white/10)
- [ ] Scroll animations trigger correctly
- [ ] Mobile: cards stack in single column

## must_haves
- Backers section shows both rows of investor logos
- Features section has correct heading hierarchy and typography
- 4 feature cards in 2x2 grid with correct content
- Icons from Phosphor (Crosshair, Lightning, UsersThree, Brain)
- Responsive grid (1 col mobile, 2 col desktop)
