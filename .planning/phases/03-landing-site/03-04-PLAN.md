---
phase: 03-landing-site
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/app/pricing/page.tsx
  - src/components/landing/pricing-table.tsx
  - src/components/landing/pricing-toggle.tsx
  - src/components/landing/pricing-faq.tsx
autonomous: true

must_haves:
  truths:
    - "Pricing page layout matches societies.io exactly"
    - "Pricing tiers display correct prices, features, and CTAs"
    - "Monthly/yearly toggle works if societies.io has one"
    - "FAQ section matches societies.io if present"
    - "Responsive behavior matches societies.io"
  artifacts:
    - path: "src/app/pricing/page.tsx"
      provides: "Pricing page route"
      min_lines: 15
    - path: "src/components/landing/pricing-table.tsx"
      provides: "Pricing tiers component"
      exports: ["PricingTable"]
    - path: "src/components/landing/pricing-toggle.tsx"
      provides: "Monthly/yearly toggle component"
      exports: ["PricingToggle"]
  key_links:
    - from: "src/app/pricing/page.tsx"
      to: "src/components/landing/pricing-*.tsx"
      via: "import"
      pattern: "import.*from.*pricing"
---

<objective>
Build the complete Pricing page matching societies.io pixel-perfect, including pricing tiers, toggle, and FAQ.

Purpose: Create the pricing page that showcases subscription options, exactly replicating societies.io design and interactions.

Output: Pricing page with tiers, monthly/yearly toggle (if applicable), feature comparison, and FAQ section.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-landing-site/03-CONTEXT.md
@.planning/phases/03-landing-site/03-RESEARCH.md
@.planning/phases/03-landing-site/03-01-SUMMARY.md
@.planning/phases/03-landing-site/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Pricing components</name>
  <files>
    src/components/landing/pricing-table.tsx
    src/components/landing/pricing-toggle.tsx
    src/components/landing/pricing-faq.tsx
    src/lib/constants/pricing.ts
  </files>
  <action>
    Create pricing components matching societies.io exactly.

    First, visit https://societies.io/pricing and extract:
    - Number of pricing tiers
    - Tier names, prices (monthly and yearly if toggle exists)
    - Features list for each tier
    - CTA button text for each tier
    - Highlighted/recommended tier styling
    - Toggle behavior (monthly/yearly) if present
    - FAQ section content if present
    - Any feature comparison table

    **Pricing Constants** (src/lib/constants/pricing.ts):
    Store all pricing data in a constants file:
    ```typescript
    export interface PricingTier {
      name: string
      monthlyPrice: number | null
      yearlyPrice: number | null
      description: string
      features: string[]
      highlighted?: boolean
      ctaText: string
      ctaHref: string
    }

    export const pricingTiers: PricingTier[] = [
      // Extract from societies.io
    ]

    export const pricingFAQs: { question: string; answer: string }[] = [
      // Extract from societies.io if FAQ exists
    ]
    ```

    **PricingToggle** (src/components/landing/pricing-toggle.tsx):
    - Only create if societies.io has monthly/yearly toggle
    - Use spring animation for toggle pill (per RESEARCH.md Pattern 5)
    - Callback to parent with billing period selection
    - Match exact styling from societies.io

    ```typescript
    interface PricingToggleProps {
      isAnnual: boolean
      onToggle: (isAnnual: boolean) => void
    }
    ```

    **PricingTable** (src/components/landing/pricing-table.tsx):
    - Display all tiers in correct layout (cards, columns)
    - Accept billing period prop to show correct prices
    - Highlight recommended tier if societies.io does
    - Match exact card styling (borders, shadows, backgrounds)
    - CTA buttons link to /signup?plan=tierName or similar
    - Use ScrollReveal or StaggeredGrid for scroll animations

    **PricingFAQ** (src/components/landing/pricing-faq.tsx):
    - Only create if societies.io has FAQ section
    - Accordion or list format matching societies.io
    - Match exact styling and animations
  </action>
  <verify>
    - npm run build succeeds
    - PricingTable renders all tiers
    - PricingToggle animates correctly (if applicable)
    - Pricing data matches societies.io exactly
  </verify>
  <done>
    Pricing components created matching societies.io, with correct data and styling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Assemble Pricing page</name>
  <files>
    src/app/pricing/page.tsx
  </files>
  <action>
    Create the Pricing page route assembling all pricing components.

    **src/app/pricing/page.tsx**:
    - Page heading matching societies.io (exact text)
    - Subheading if present
    - PricingToggle (if applicable)
    - PricingTable with current billing period
    - Feature comparison section if societies.io has one
    - PricingFAQ section if societies.io has one
    - Any CTA section at bottom

    If societies.io has monthly/yearly toggle:
    ```typescript
    "use client"

    import { useState } from "react"
    import { PricingToggle } from "@/components/landing/pricing-toggle"
    import { PricingTable } from "@/components/landing/pricing-table"
    import { PricingFAQ } from "@/components/landing/pricing-faq"

    export default function PricingPage() {
      const [isAnnual, setIsAnnual] = useState(false)

      return (
        <main className="pt-[header-height]">
          {/* Page heading */}
          <section>
            <h1>Exact headline from societies.io</h1>
            <p>Exact subheadline from societies.io</p>
          </section>

          {/* Toggle */}
          <PricingToggle isAnnual={isAnnual} onToggle={setIsAnnual} />

          {/* Tiers */}
          <PricingTable isAnnual={isAnnual} />

          {/* FAQ if present */}
          <PricingFAQ />
        </main>
      )
    }
    ```

    **Responsive behavior**:
    - Match societies.io pricing page at all breakpoints
    - Cards may stack on mobile
    - Toggle should be touch-friendly
    - FAQ accordion should work on touch

    **Scroll animations**:
    - Add scroll reveals matching societies.io behavior
    - Use appropriate animation components from 03-02
  </action>
  <verify>
    - npm run build succeeds
    - /pricing route loads correctly
    - Toggle switches prices (if applicable)
    - CTA buttons link to /signup
    - Responsive at all breakpoints
  </verify>
  <done>
    Pricing page complete, pixel-perfect match with societies.io including toggle behavior and responsive layout.
  </done>
</task>

</tasks>

<verification>
1. /pricing route loads without errors
2. All pricing tiers display with correct prices and features
3. Monthly/yearly toggle works correctly (if applicable)
4. CTA buttons link to appropriate signup routes
5. FAQ section expands/collapses correctly (if present)
6. Responsive layout matches societies.io
7. Scroll animations match societies.io
</verification>

<success_criteria>
- Pricing page pixel-perfect match with societies.io
- All pricing data exactly matches societies.io
- Interactive elements (toggle, FAQ) work correctly
- Responsive behavior matches societies.io
</success_criteria>

<output>
After completion, create `.planning/phases/03-landing-site/03-04-SUMMARY.md`
</output>
