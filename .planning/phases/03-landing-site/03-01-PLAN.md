---
phase: 03-landing-site
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/images/landing/
  - src/components/layout/header.tsx
  - src/components/layout/footer.tsx
  - src/components/layout/mobile-menu.tsx
  - src/lib/constants/navigation.ts
  - src/app/globals.css
autonomous: true

must_haves:
  truths:
    - "Navigation matches societies.io exactly (items, structure, dropdowns)"
    - "Header is sticky with hide-on-scroll behavior matching societies.io"
    - "Mobile menu slides in from right with proper animations"
    - "Footer structure and links match societies.io exactly"
    - "All landing page assets are downloaded and available in /public"
  artifacts:
    - path: "public/images/landing/"
      provides: "Downloaded assets from societies.io"
      min_files: 5
    - path: "src/components/layout/header.tsx"
      provides: "Pixel-perfect header matching societies.io"
      exports: ["Header"]
    - path: "src/components/layout/footer.tsx"
      provides: "Pixel-perfect footer matching societies.io"
      exports: ["Footer"]
    - path: "src/components/layout/mobile-menu.tsx"
      provides: "Animated mobile navigation drawer"
      exports: ["MobileMenu"]
    - path: "src/lib/constants/navigation.ts"
      provides: "Navigation items matching societies.io structure"
      exports: ["headerNavItems", "footerLinks"]
  key_links:
    - from: "src/components/layout/header.tsx"
      to: "src/lib/constants/navigation.ts"
      via: "import"
      pattern: "import.*from.*navigation"
    - from: "src/components/layout/header.tsx"
      to: "src/components/layout/mobile-menu.tsx"
      via: "component composition"
      pattern: "MobileMenu"
---

<objective>
Set up landing page foundation by downloading societies.io assets and creating pixel-perfect navigation components.

Purpose: Establish the shared navigation structure used across all landing pages, ensuring exact visual match with societies.io.

Output: Downloaded assets, updated Header with sticky behavior and mobile menu, updated Footer matching societies.io structure.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-landing-site/03-CONTEXT.md
@.planning/phases/03-landing-site/03-RESEARCH.md
@src/components/layout/header.tsx
@src/components/layout/footer.tsx
@src/components/layout/container.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download assets and reverse-engineer CSS from societies.io</name>
  <files>
    public/images/landing/
    src/app/globals.css
    src/lib/constants/design-tokens.ts
  </files>
  <action>
    Visit https://societies.io and extract:

    1. **Images**: Download all landing page images (hero backgrounds, logos, team photos, feature illustrations, icons). Save to /public/images/landing/ with descriptive names.

    2. **CSS Reverse Engineering**: Using DevTools, document:
       - Exact breakpoints (inspect media queries)
       - Exact fonts (font-family, sizes, weights, line-heights)
       - Exact colors (backgrounds, text, borders, accents)
       - Exact spacing values (padding, margins, gaps)
       - Header height and behavior
       - Footer structure

    3. **Design Tokens**: Create src/lib/constants/design-tokens.ts documenting the extracted values. Update globals.css with any CSS variables needed to match societies.io exactly.

    4. **Navigation Structure**: Document exact nav items, their labels, URLs, and any dropdowns.

    Use browser DevTools Network tab to find image URLs. Use Computed Styles tab to extract exact CSS values.

    IMPORTANT: This is a pixel-perfect clone. Every value must match exactly.
  </action>
  <verify>
    - /public/images/landing/ contains downloaded images (at least 5 files)
    - src/lib/constants/design-tokens.ts exists with documented values
    - src/app/globals.css updated with societies.io-matching variables
  </verify>
  <done>
    All landing page assets downloaded, CSS values documented, design tokens extracted and stored in constants file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create navigation constants and update Header/Footer</name>
  <files>
    src/lib/constants/navigation.ts
    src/components/layout/header.tsx
    src/components/layout/footer.tsx
    src/components/layout/mobile-menu.tsx
  </files>
  <action>
    1. **Navigation Constants** (src/lib/constants/navigation.ts):
       - Create headerNavItems array matching societies.io nav structure exactly
       - Create footerLinks object matching societies.io footer structure exactly
       - Include any dropdown menus if present
       - Links to unbuilt pages should use "/coming-soon"
       - Auth links (/login, /signup) should remain as-is

    2. **Header Update** (src/components/layout/header.tsx):
       - Convert to client component ("use client") for scroll behavior
       - Match societies.io header EXACTLY: logo, nav items, spacing, colors
       - Implement sticky header with hide-on-scroll-down, show-on-scroll-up behavior
       - Use useScroll and useMotionValueEvent from motion/react (per RESEARCH.md Pattern 3)
       - Add hamburger button for mobile (hidden on desktop)
       - Integrate MobileMenu component
       - Match exact pixel values from Task 1 extraction

    3. **Footer Update** (src/components/layout/footer.tsx):
       - Match societies.io footer EXACTLY: layout, columns, links, social icons
       - Use footerLinks from navigation.ts
       - Match exact colors, typography, spacing
       - Ensure responsive behavior matches societies.io

    4. **Mobile Menu** (src/components/layout/mobile-menu.tsx):
       - Create as separate client component
       - Slide-in from right with AnimatePresence (per RESEARCH.md Pattern 4)
       - Include backdrop overlay
       - Lock body scroll when open (add overflow:hidden to body)
       - Match societies.io mobile menu if they have one, or create clean implementation

    Code pattern for sticky header:
    ```typescript
    const [hidden, setHidden] = useState(false)
    const { scrollY } = useScroll()

    useMotionValueEvent(scrollY, "change", (latest) => {
      const previous = scrollY.getPrevious() ?? 0
      if (latest > previous && latest > 150) {
        setHidden(true)
      } else {
        setHidden(false)
      }
    })
    ```
  </action>
  <verify>
    - npm run build succeeds
    - Header renders with all nav items from navigation.ts
    - Header hides on scroll down, shows on scroll up
    - Mobile menu opens/closes with animation
    - Footer renders with all links from navigation.ts
  </verify>
  <done>
    Header and Footer match societies.io pixel-perfect, mobile menu functional with animations, all navigation uses centralized constants.
  </done>
</task>

</tasks>

<verification>
1. Visual comparison: Header matches societies.io at desktop and mobile sizes
2. Visual comparison: Footer matches societies.io at desktop and mobile sizes
3. Scroll behavior: Header hides/shows correctly on scroll
4. Mobile: Hamburger menu opens slide-out navigation with animation
5. Assets: All downloaded images load correctly from /public/images/landing/
6. Links: Navigation items link to correct routes (or /coming-soon for unbuilt)
</verification>

<success_criteria>
- Header pixel-perfect match with societies.io (fonts, colors, spacing, behavior)
- Footer pixel-perfect match with societies.io
- Mobile menu functional with proper animations
- All assets downloaded and serving correctly
- No console errors, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-landing-site/03-01-SUMMARY.md`
</output>
