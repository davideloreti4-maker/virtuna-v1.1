# Plan 03-03: Hero Section

## Frontmatter
```yaml
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/landing/hero-section.tsx
  - src/components/landing/persona-card.tsx
  - src/components/landing/index.ts
  - src/app/page.tsx
  - public/images/network-visualization.png
autonomous: true
```

## Context
Build the hero section matching societies.io exactly. The 3D network visualization will use a static placeholder image (complex Three.js animation is out of scope). Focus on pixel-perfect text content, persona card, and entrance animations.

**v0 MCP Requirement:** Use v0 MCP tool to generate pixel-perfect UI components. Before implementing HeroSection and PersonaCard, query v0 with `.reference/landing/_assets/hero-viewport.png` to get accurate component code that matches societies.io exactly.

## Tasks

<task id="1">
<title>Create PersonaCard component</title>
<description>
Create a floating persona card component that appears in the hero:

**File:** `src/components/landing/persona-card.tsx`

**Structure:**
```html
<div class="bg-background-elevated rounded-lg p-4 w-72">
  <!-- Header: Avatar + Name/Role -->
  <div class="flex items-center gap-3 mb-3">
    <div class="w-10 h-10 rounded-full bg-[#1a1a1a] flex items-center justify-center text-white text-sm font-medium">
      AP
    </div>
    <div>
      <div class="text-white font-medium">Aisha Patel</div>
      <div class="text-gray-400 text-sm">Data Scientist</div>
    </div>
  </div>

  <!-- Company & Bio -->
  <div class="mb-3">
    <div class="text-gray-400 text-sm">AI Dynamics</div>
    <div class="text-gray-300 text-sm mt-1">Building machine learning models that transform how businesses understand their customers.</div>
  </div>

  <!-- Tags -->
  <div class="flex flex-wrap gap-2">
    <span class="text-xs text-gray-400 flex items-center gap-1">
      <MapPin size={12} /> Toronto, Canada
    </span>
    <span class="text-xs text-gray-400 flex items-center gap-1">
      <GenderFemale size={12} /> Female
    </span>
    <span class="text-xs text-gray-400 flex items-center gap-1">
      <User size={12} /> Millennial
    </span>
  </div>
</div>
```

**Props Interface:**
```ts
interface PersonaCardProps {
  initials: string;
  name: string;
  role: string;
  company: string;
  bio: string;
  location: string;
  gender: "Male" | "Female" | "Non-binary";
  generation: string;
  className?: string;
}
```

**Icons:** Use Phosphor icons (MapPin, GenderFemale/GenderMale, User)

Reference: `.reference/landing/hero.md` persona card section
</description>
</task>

<task id="2">
<title>Create static network visualization placeholder</title>
<description>
Create or source a static image for the 3D network visualization:

**File:** `public/images/network-visualization.png`

**Requirements:**
- Dark background (transparent or #0D0D0D)
- Network of interconnected nodes (white/gray dots with lines)
- One highlighted node in orange (accent color)
- Approximately 626x550px dimensions
- Should look like a 3D particle/node network

**Options:**
1. Export a screenshot from the societies.io site
2. Create a simple static representation
3. Use a placeholder with similar aesthetic

**Implementation note:** The actual societies.io site uses Three.js for an interactive 3D visualization. For this clone, a static image placeholder is acceptable per project decisions.
</description>
</task>

<task id="3">
<title>Create HeroSection component</title>
<description>
Create the main hero section component:

**File:** `src/components/landing/hero-section.tsx`

**Layout:**
```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│   Human Behavior,              ┌─────────────────┐      │
│   Simulated.  (orange)         │  Network Image  │      │
│                                │  + Persona Card │      │
│   AI personas that replicate   │  (overlaid)     │      │
│   real-world attitudes...      └─────────────────┘      │
│                                                         │
│   [Get in touch]                                        │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**Typography (exact):**
- H1: font-display text-[52px] leading-[62px] font-[350] text-white
- "Simulated." span: text-accent (orange)
- Paragraph: font-sans text-xl leading-[30px] font-[450] text-white/90
- Button: bg-accent text-white px-4 py-2 rounded text-sm font-medium

**Layout:**
- max-w-6xl mx-auto px-6 py-24
- flex items-center justify-between
- Left content: max-w-xl
- Right: relative container for network + persona card

**Animations (entrance):**
- Text content: FadeIn + SlideUp staggered
- Opacity: 0 -> 1
- TranslateY: 20px -> 0
- Easing: ease-out-cubic
- Stagger: 0.1s per element

**Responsive:**
- Mobile: Stack vertically, network image above text
- Tablet+: Side by side layout

Reference: `.reference/landing/hero.md` and `_assets/hero-viewport.png`
</description>
</task>

<task id="4">
<title>Create landing components barrel export and update homepage</title>
<description>
Create index file for landing components:

**File:** `src/components/landing/index.ts`

```ts
export { HeroSection } from "./hero-section";
export { PersonaCard } from "./persona-card";
```

Update the main page.tsx to use the new hero:

**File:** `src/app/page.tsx`

**Structure:**
```tsx
import { HeroSection } from "@/components/landing";

export default function HomePage() {
  return (
    <main>
      <HeroSection />
      {/* More sections will be added in subsequent plans */}
    </main>
  );
}
```

**Notes:**
- Remove any existing placeholder content
- Keep it simple - sections will be added incrementally
- Header is already in layout.tsx (or add if needed)
</description>
</task>

## Verification

```bash
# Build check
npm run build

# Dev server
npm run dev
# Visit http://localhost:3000
```

**Manual checks:**
- [ ] Hero heading matches "Human Behavior, Simulated." exactly
- [ ] Orange accent color on "Simulated."
- [ ] Typography sizes match reference (52px heading, 20px paragraph)
- [ ] Persona card displays with all fields
- [ ] Network visualization placeholder visible
- [ ] "Get in touch" button styled correctly
- [ ] Entrance animations work (fade + slide)
- [ ] Mobile responsive layout works

## must_haves
- Hero heading typography exactly matches (52px, Funnel Display, 350 weight)
- "Simulated." text is orange accent color
- Persona card component renders with demo data
- Entrance animations implemented (opacity + translateY)
- Responsive layout (stacked on mobile, side-by-side on desktop)
