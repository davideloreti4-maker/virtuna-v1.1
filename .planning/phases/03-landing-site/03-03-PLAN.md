---
phase: 03-landing-site
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/app/page.tsx
  - src/components/landing/hero.tsx
  - src/components/landing/features.tsx
  - src/components/landing/testimonials.tsx
  - src/components/landing/cta-section.tsx
  - src/components/landing/logos-section.tsx
autonomous: true

must_haves:
  truths:
    - "Homepage hero section matches societies.io exactly (layout, copy, images)"
    - "Features section displays all features from societies.io with matching layout"
    - "Testimonials/social proof section matches societies.io"
    - "CTA sections match societies.io styling and copy"
    - "Scroll animations trigger at same points as societies.io"
    - "Mobile layout matches societies.io responsive behavior"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage assembling all sections"
      min_lines: 20
    - path: "src/components/landing/hero.tsx"
      provides: "Hero section component"
      exports: ["Hero"]
    - path: "src/components/landing/features.tsx"
      provides: "Features section component"
      exports: ["Features"]
    - path: "src/components/landing/testimonials.tsx"
      provides: "Testimonials/social proof section"
      exports: ["Testimonials"]
    - path: "src/components/landing/cta-section.tsx"
      provides: "CTA section component"
      exports: ["CTASection"]
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/landing/*.tsx"
      via: "import"
      pattern: "import.*from.*components/landing"
    - from: "src/components/landing/*.tsx"
      to: "src/components/animations"
      via: "import"
      pattern: "import.*from.*animations"
---

<objective>
Build the complete homepage matching societies.io pixel-perfect, including all sections with scroll animations.

Purpose: Create the main landing page that serves as the primary user entry point, exactly replicating societies.io design and interactions.

Output: Homepage with Hero, Features, Testimonials, CTAs, and all scroll animations matching societies.io.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-landing-site/03-CONTEXT.md
@.planning/phases/03-landing-site/03-RESEARCH.md
@.planning/phases/03-landing-site/03-01-SUMMARY.md
@.planning/phases/03-landing-site/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Hero section</name>
  <files>
    src/components/landing/hero.tsx
  </files>
  <action>
    Create pixel-perfect Hero section matching societies.io homepage.

    Visit https://societies.io and extract:
    - Exact headline text, subheadline, CTA button text
    - Hero image/illustration
    - Layout (text positioning, image positioning)
    - Background treatment (gradients, colors)
    - Animation behavior on load

    Implementation:
    1. Create src/components/landing/hero.tsx as client component
    2. Use Container from layout components
    3. Use images from /public/images/landing/ (downloaded in 03-01)
    4. Match exact typography (font-size, weight, line-height, colors)
    5. Match exact spacing (padding, margins)
    6. Use FadeIn or SlideUp for initial load animations
    7. If societies.io has parallax on hero image, use Parallax component
    8. CTAs link to /login or /signup
    9. Ensure responsive: match societies.io mobile hero layout exactly

    Use Next.js Image component for hero image with priority={true} for LCP.

    IMPORTANT: Copy EXACT text from societies.io. This is a pixel-perfect clone.
  </action>
  <verify>
    - npm run build succeeds
    - Hero section renders without errors
    - Images load from /public/images/landing/
    - CTA buttons are clickable (link to /login or /signup)
  </verify>
  <done>
    Hero section pixel-perfect match with societies.io including typography, spacing, images, and load animations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Features, Testimonials, CTAs sections</name>
  <files>
    src/components/landing/features.tsx
    src/components/landing/testimonials.tsx
    src/components/landing/cta-section.tsx
    src/components/landing/logos-section.tsx
  </files>
  <action>
    Create all remaining homepage sections matching societies.io exactly.

    For EACH section, visit societies.io and extract:
    - Exact content (headlines, descriptions, quotes)
    - Exact layout (grid columns, spacing, alignment)
    - Exact styling (colors, backgrounds, borders, shadows)
    - Any icons or images used
    - Scroll animation behavior

    **Features Section** (src/components/landing/features.tsx):
    - Match societies.io features grid exactly
    - Use StaggeredGrid for scroll-reveal animation
    - Include all feature cards with exact icons/illustrations
    - Match hover states if any

    **Testimonials Section** (src/components/landing/testimonials.tsx):
    - Match societies.io social proof section exactly
    - Include exact quotes, names, titles, company logos
    - Match layout (carousel, grid, or list)
    - Use ScrollReveal for animations

    **CTA Section** (src/components/landing/cta-section.tsx):
    - Match societies.io CTA sections (may be multiple on page)
    - Exact copy, button styling, background
    - Links to /login or /signup

    **Logos Section** (src/components/landing/logos-section.tsx) — if societies.io has one:
    - "Trusted by" or partner logos
    - Match exact logos and layout

    For scroll animations:
    - Observe when animations trigger on societies.io
    - Use appropriate threshold values to match timing
    - Use ScrollReveal, FadeIn scroll=true, or StaggeredGrid as appropriate
  </action>
  <verify>
    - npm run build succeeds
    - All sections render without errors
    - Scroll animations trigger when sections enter viewport
    - No hardcoded placeholder content
  </verify>
  <done>
    All homepage sections match societies.io pixel-perfect with correct scroll animations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Assemble Homepage and verify responsive behavior</name>
  <files>
    src/app/page.tsx
  </files>
  <action>
    Assemble all sections into the homepage and ensure responsive behavior matches societies.io.

    **src/app/page.tsx**:
    1. Remove any showcase content from Phase 2
    2. Import all landing sections in correct order
    3. Include Header and Footer from layout
    4. Order sections exactly as they appear on societies.io
    5. Add proper spacing between sections (match societies.io)

    Structure:
    ```typescript
    import { Hero } from "@/components/landing/hero"
    import { LogosSection } from "@/components/landing/logos-section"
    import { Features } from "@/components/landing/features"
    import { Testimonials } from "@/components/landing/testimonials"
    import { CTASection } from "@/components/landing/cta-section"

    export default function HomePage() {
      return (
        <main>
          <Hero />
          <LogosSection />
          <Features />
          <Testimonials />
          <CTASection />
          {/* Add more sections if societies.io has them */}
        </main>
      )
    }
    ```

    **Responsive verification**:
    1. Test at desktop (1280px+), tablet (768-1279px), mobile (<768px)
    2. Match societies.io responsive behavior at each breakpoint
    3. Ensure all images are responsive
    4. Ensure text sizes adjust correctly
    5. Ensure spacing adjusts correctly

    Note: Header/Footer may be in layout.tsx already from Phase 2 setup.
  </action>
  <verify>
    - npm run build succeeds
    - npm run dev shows homepage at http://localhost:3000
    - All sections visible in correct order
    - Responsive at all breakpoints
    - No console errors
  </verify>
  <done>
    Homepage complete with all sections in correct order, responsive behavior matching societies.io at all breakpoints.
  </done>
</task>

</tasks>

<verification>
1. Homepage loads at / route
2. All sections render in correct order matching societies.io
3. Scroll animations trigger at appropriate scroll positions
4. Images load from /public/images/landing/
5. CTAs link to /login and /signup
6. Responsive layout matches societies.io at 320px, 768px, 1024px, 1440px widths
7. No console errors or build warnings
</verification>

<success_criteria>
- Homepage is pixel-perfect match with societies.io at all breakpoints
- Scroll animations match societies.io timing and behavior
- All text content exactly matches societies.io
- All images load correctly
- Page performs well (no jank on scroll)
</success_criteria>

<output>
After completion, create `.planning/phases/03-landing-site/03-03-SUMMARY.md`
</output>
