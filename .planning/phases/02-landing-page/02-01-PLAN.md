---
phase: 02-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/hero-section.tsx
  - src/components/landing/faq-section.tsx
  - src/components/layout/footer.tsx
  - src/app/(marketing)/pricing/pricing-section.tsx
autonomous: true

must_haves:
  truths:
    - "Hero CTA 'Start free trial' navigates to /signup (not /auth/signup)"
    - "Footer CTA 'Start free trial' navigates to /signup (not /auth/signup)"
    - "Pricing page unauthenticated CTAs navigate to /signup?plan=<tier> (not /auth/signup?plan=<tier>)"
    - "FAQ accordion borders use 6% opacity (border-white/[0.06]) matching Raycast design language"
    - "Footer bar border uses 6% opacity (border-white/[0.06]) matching Raycast design language"
    - "Footer text colors use design tokens (text-foreground-muted) not raw Tailwind (text-gray-400)"
    - "All landing page sections render without errors at 320px, 375px, 768px, 1024px, and 1440px widths"
  artifacts:
    - path: "src/components/landing/hero-section.tsx"
      provides: "Hero section with correct CTA links"
      contains: 'href="/signup"'
    - path: "src/components/landing/faq-section.tsx"
      provides: "FAQ section with correct border opacity"
      contains: "border-white/[0.06]"
    - path: "src/components/layout/footer.tsx"
      provides: "Footer with correct CTA links and design tokens"
      contains: 'href="/signup"'
    - path: "src/app/(marketing)/pricing/pricing-section.tsx"
      provides: "Pricing section with correct auth redirect links"
      contains: 'href="/signup'
  key_links:
    - from: "src/components/landing/hero-section.tsx"
      to: "/signup"
      via: "Link href"
      pattern: 'href="/signup"'
    - from: "src/app/(marketing)/pricing/pricing-section.tsx"
      to: "/signup"
      via: "Link href with plan param"
      pattern: 'href.*"/signup\\?plan='
---

<objective>
Fix CTA link targets and Raycast design language inconsistencies across all landing page sections and pricing page.

Purpose: Phase 1 moved auth routes from /auth/signup to /signup. All CTAs on the landing page and pricing page still point to the old route. Additionally, several components use incorrect border opacities and raw Tailwind color classes instead of design tokens.

Output: All landing page CTAs link to correct auth routes, all borders match Raycast 6% opacity standard, all text colors use design tokens.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-page/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CTA link targets across landing page, footer, and pricing</name>
  <files>
    src/components/landing/hero-section.tsx
    src/components/layout/footer.tsx
    src/app/(marketing)/pricing/pricing-section.tsx
  </files>
  <action>
Phase 1 moved auth routes from /auth/signup to /signup (decision [01-01]). Update all CTA links:

1. **hero-section.tsx** (line 50): Change `href="/auth/signup"` to `href="/signup"`

2. **footer.tsx** (line 26): Change `href="/auth/signup"` to `href="/signup"`

3. **pricing-section.tsx** (line 64): Change `href={`/auth/signup?plan=${planTier}`}` to `href={`/signup?plan=${planTier}`}`

Do NOT change:
- The "See pricing" links (they correctly point to /pricing)
- Any other navigation links
- The pricing page's authenticated CTA behavior (checkout modal)
  </action>
  <verify>
Run `grep -rn '/auth/signup' src/components/landing/ src/components/layout/footer.tsx src/app/\(marketing\)/pricing/` — should return zero results.
Run `grep -rn 'href="/signup"' src/components/landing/hero-section.tsx src/components/layout/footer.tsx` — should return matches in both files.
Run `grep -rn '/signup?plan=' src/app/\(marketing\)/pricing/pricing-section.tsx` — should return a match.
  </verify>
  <done>All CTA links point to /signup (or /signup?plan=<tier>), zero references to /auth/signup remain in landing/pricing components.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Raycast design language inconsistencies (borders, text colors)</name>
  <files>
    src/components/landing/faq-section.tsx
    src/components/layout/footer.tsx
  </files>
  <action>
Fix styling inconsistencies to match Raycast design language:

1. **faq-section.tsx** (line 67): Change `border-white/10` to `border-white/[0.06]` on AccordionItem className. The Raycast standard is 6% border opacity, not 10%.

2. **footer.tsx** (line 41): Change `border-white/10` to `border-white/[0.06]` on the footer bar's border-t.

3. **footer.tsx** (lines 44, 52, 57, 69, 78): Replace all `text-gray-400` with `text-foreground-muted`. This uses the design token system instead of raw Tailwind gray classes. There are 5 occurrences:
   - Line 44: copyright text
   - Line 52: Privacy Policy link
   - Line 57: Terms of Service link
   - Line 69: X/Twitter icon link
   - Line 78: Email icon link

4. **footer.tsx** (line 33): Change `border-white/20` to `border-white/[0.06]` on the secondary CTA button's border. Raycast secondary button borders use 6% opacity.

Do NOT change:
- Hover states (hover:text-white, hover:bg-white/5 are correct patterns)
- The footer CTA section heading/paragraph styles
- Any inset shadow values
  </action>
  <verify>
Run `grep -n 'border-white/10' src/components/landing/faq-section.tsx src/components/layout/footer.tsx` — should return zero results.
Run `grep -n 'text-gray-400' src/components/layout/footer.tsx` — should return zero results.
Run `grep -n 'border-white/20' src/components/layout/footer.tsx` — should return zero results.
Run `grep -c 'foreground-muted' src/components/layout/footer.tsx` — should return 5.
Run `pnpm build` — should compile without errors.
  </verify>
  <done>All border opacities match Raycast 6% standard, all text colors use design tokens, build passes.</done>
</task>

</tasks>

<verification>
1. `grep -rn '/auth/signup' src/` — no references to old auth route in any landing/pricing component
2. `grep -rn 'border-white/10' src/components/landing/ src/components/layout/footer.tsx` — zero results
3. `grep -rn 'text-gray-400' src/components/layout/footer.tsx` — zero results
4. `pnpm build` passes without errors
5. Landing page renders: hero with CTAs, hive demo, features, stats, social proof, FAQ, footer
</verification>

<success_criteria>
- All hero and footer CTAs link to /signup
- Pricing page unauthenticated CTAs link to /signup?plan=<tier>
- FAQ accordion borders use border-white/[0.06]
- Footer bar border uses border-white/[0.06]
- All footer text colors use text-foreground-muted design token
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page/02-01-SUMMARY.md`
</output>
