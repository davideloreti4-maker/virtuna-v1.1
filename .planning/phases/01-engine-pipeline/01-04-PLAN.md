---
phase: 01-engine-pipeline
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - src/lib/engine/pipeline.ts
  - src/lib/engine/aggregator.ts
  - src/lib/engine/types.ts
  - src/lib/engine/version.ts
  - src/lib/engine/__tests__/version.test.ts
  - src/lib/engine/__tests__/aggregator.test.ts
  - src/lib/engine/__tests__/pipeline.test.ts
autonomous: false
requirements: [ENG-01, ENG-03, ENG-04]
must_haves:
  truths:
    - "A full E2E analyze run completes on a real video with every Qwen call returning usable output — no thrown frames; the silent single-signal-failure path is either accepted-as-graceful or annotated honestly per Davide's decision"
    - "The engagement range is a deterministic follower_count x quality formula (null when no baseline) and analysis_unavailable fires on dual failure — confirmed and LOCKED by a test (ENG-04/D-15)"
    - "Per-stage latency is measured on a real video and stays under the Vercel 300s cap; any temporary regression from plans 01-03 is reclaimed or explicitly accepted (ENG-03/D-08/D-09)"
  artifacts:
    - path: "src/lib/engine/pipeline.ts"
      provides: "3-call orchestration + the silent-fallback paths (Omni/fold/Apollo warnings[]) ENG-01 audits"
    - path: "src/lib/engine/aggregator.ts"
      provides: "Ensemble blend + computeEngagementRange + analysis_unavailable flag (ENG-04 honesty)"
  key_links:
    - from: "src/lib/engine/aggregator.ts"
      to: "src/lib/engine/types.ts"
      via: "analysis_unavailable (+ optional partial_analysis) flag on PredictionResult, derivable from signal_availability JSONB"
      pattern: "analysis_unavailable|computeEngagementRange|partial_analysis"
---

<objective>
Cross-cutting verification + honesty closeout. Three threads:

- ENG-01 — AUDIT every call's silent-fallback path (Omni all-null substitute, fold zero-retry drop, Apollo null-then-zero) on real videos; confirm no thrown frames; surface the ONE honesty gap (a single dead core signal silently halves the score basis with only a warnings[] entry the board filters out) for Davide's decision.
- ENG-04 — CONFIRM the already-shipped honesty (Math.sin jitter gone, engagement range = deterministic follower_count x quality / null, analysis_unavailable on dual failure) is STILL true and LOCK it with a test. Per D-04 do NOT add a score-determinism gate.
- ENG-03 — MEASURE-FIRST per-stage latency on a real video (measure-pipeline); confirm under the 300s cap; reclaim any temporary regression introduced by plans 01-03 (D-09) or explicitly accept it (D-08 quality wins).

Runs LAST because the single-failure honesty annotation (if chosen) is an output-shape change that must account for plans 01-03's version bumps, and the latency reclaim measures the post-refinement engine.

Purpose: prove the refined engine is correct, honest, and within cap — the phase's E2E gate.
Output: confirmed/locked honesty, a Davide decision on the single-failure annotation, a measured latency map, and any reclaim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-engine-pipeline/01-CONTEXT.md
@.planning/phases/01-engine-pipeline/01-RESEARCH.md
@.planning/phases/01-engine-pipeline/01-01-SUMMARY.md
@.planning/phases/01-engine-pipeline/01-02-SUMMARY.md
@.planning/phases/01-engine-pipeline/01-03-SUMMARY.md

<interfaces>
<!-- From RESEARCH.md §ENG-01/03/04 — confirmed as-built. -->
- Silent-fallback paths (ENG-01):
  - Read: 2 attempts (MAX_RETRIES=1, 60s) -> all-null output; pipeline.ts:538 pushes "Omni analysis failed" to warnings[], substitutes DEFAULT_GEMINI_RESULT (zeros). No thrown frame.
  - Fold: ONE attempt, no retry; fail -> fold_success=false; pipeline.ts:741-745 pushes "Fold unavailable", foldOutcome null; aggregator falls back.
  - Apollo: 3 attempts (MAX_RETRIES=2) + circuit breaker; persistent fail -> null; pipeline.ts:683-692 pushes warning, aggregator zeroes apollo_score.
  - Dual failure: analysis_unavailable = !availability.gemini && !availability.behavioral (aggregator.ts:856) -> board "couldn't analyze". The ONE honest path.
  - THE GAP: a SINGLE dead signal silently halves the score basis; InsightHeroFrame.tsx:222-232 filters out "Weights redistributed"/"Low confidence" warnings -> user sees nothing. (D-15 single-failure honesty annotation = OPEN decision.)
- ENG-04 confirmed-shipped (LOCK, don't rebuild): Math.sin jitter deleted (aggregator.ts:430-432,981-983); computeEngagementRange (aggregator.ts:182-231) = follower_count x quality formula, null when follower_count<=0 (:189-195), LIVE-RESULTS-ONLY not persisted (:1096-1097 — known intentional gap); analysis_unavailable required since v3.13.0 (:1138); ensemble blend (:827-841). rule_score:50 / trend_score:0 = dead constants (:1101-1102).
- ENG-03 latency: Read BLOCKS first (~17s), then Fold (~54s) || Apollo (~49s) in parallel; critical path ~70s under 300s cap. measure-pipeline.ts prints TOTAL_LATENCY_MS + per-stage + PARALLEL/SERIALIZED verdict + !!FAIL flags. PITFALL 2: lower a budget, never raise the fold's 90s HARD ceiling.
- Cache invariant: a partial_analysis flag (if chosen) is an output-shape change -> bump version.ts (read CURRENT value, account for 01-03 bumps) + version.test.ts.
- D-04: do NOT add a determinism-band gate. Measure-first only if revisited.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AUDIT — E2E fallback-path + honesty confirm + latency map on a real video</name>
  <read_first>
    - src/lib/engine/pipeline.ts:400-496 (Apify signed-URL flow — PRESERVE), :508-541 (Read block + warnings), :639-693 (Apollo), :710-746 (fold)
    - src/lib/engine/aggregator.ts:182-231 (computeEngagementRange), :827-856 (ensemble + analysis_unavailable), :430-432/:981-983 (Math.sin deletion notes), :1096-1102 (persistence + dead constants)
    - src/components/board/InsightHeroFrame.tsx:222-232 (the warning filter that hides single-failure)
    - scripts/measure-pipeline.ts and scripts/smoke-tiktok-pipeline.ts (the rigs + their FAIL/signal gates)
    - .planning/phases/01-engine-pipeline/01-RESEARCH.md §ENG-01, §ENG-03, §ENG-04
    - 01-01/02/03 SUMMARYs (what changed — did any plan already shift output shape / latency?)
  </read_first>
  <action>
    No edits (D-01). Run `npx tsx scripts/smoke-tiktok-pipeline.ts "$HOME/Downloads/TikTok Video Downloader.mp4"` (E2E signal-completeness gate) and `npx tsx scripts/measure-pipeline.ts "$HOME/Downloads/TikTok Video Downloader.mp4"` (per-stage latency + parallel verdict). Produce three audit artifacts. (1) Fallback table: for each call, which fallback fired on the real run (if any), whether it surfaced or stayed silent, and which silent paths are acceptable-graceful vs mask-a-broken-run — specifically pin down the single-dead-signal gap (fold-dead-Apollo-ok or vice-versa halves the basis silently). (2) Honesty confirmation: assert from the code + run that Math.sin is gone, engagement range is the follower x quality formula (or null), and analysis_unavailable fires only on dual failure — flag any confident-looking fake number still reaching output (candidate: rule_score:50/trend_score:0). (3) Latency map: per-stage durations + TOTAL vs 300s cap + whether plans 01-03 regressed it (compare to the ~70s baseline). Present all three.
  </action>
  <verify>
    <automated>npx tsx scripts/measure-pipeline.ts "$HOME/Downloads/TikTok Video Downloader.mp4" 2>&1 | tee /tmp/eng-latency.txt | grep -iE 'TOTAL_LATENCY_MS|FAIL|PARALLEL|SERIALIZED' | head; npx tsx scripts/smoke-tiktok-pipeline.ts "$HOME/Downloads/TikTok Video Downloader.mp4" 2>&1 | tee /tmp/eng-smoke.txt | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - smoke + measure rigs ran on a real video; outputs captured (/tmp/eng-smoke.txt, /tmp/eng-latency.txt)
    - Fallback table exists classifying each silent path acceptable-vs-masking, with the single-dead-signal gap explicitly pinned
    - Honesty confirmed: Math.sin gone, engagement range grounded-or-null, analysis_unavailable dual-failure-only — any leaking fake number flagged
    - Latency map exists: per-stage + TOTAL vs 300s cap + regression delta vs the ~70s baseline (accounting for plans 01-03)
  </acceptance_criteria>
  <done>E2E fallback table + honesty confirmation + measured latency map produced; no edits; no thrown frame observed on the real run.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: CO-REVIEW CHECKPOINT — single-failure honesty annotation + latency reclaim posture (ENG-01/D-15/ENG-03)</name>
  <action>STOP and present Task 1's fallback table + latency map to Davide. Do NOT auto-decide. Capture his single-failure decision (A accept / B partial flag / C fold retry) and latency posture (reclaim-now / accept-under-cap) before any edit (ENG-01/D-15/ENG-03/D-08).</action>
  <read_first>
    - /tmp/eng-smoke.txt, /tmp/eng-latency.txt + the Task 1 fallback table + latency map
    - .planning/phases/01-engine-pipeline/01-RESEARCH.md §ENG-01 options A/B/C
  </read_first>
  <decision>(a) Does a SINGLE dead core signal (not just dual) get a user-visible honesty annotation, or stay silent graceful-degradation? (b) Does the measured latency need a reclaim pass now, or is the current number accepted under the 300s cap (D-08)?</decision>
  <context>Today only DUAL failure surfaces (analysis_unavailable); a single dead signal silently halves the score basis and the board filters the warning. D-15 + ENG-01 ask whether that bar is honest enough. Present Task 1's fallback table. For latency: present the measured map vs the ~70s baseline and any regression from plans 01-03 (e.g. a read flip to plus, a restored core section).</context>
  <options>
    <option id="audit-only">
      <name>A — Accept as graceful (audit-only)</name>
      <pros>No output-shape change, no version bump; single-signal degradation is documented graceful behavior.</pros>
      <cons>User still sees nothing when half the basis dies.</cons>
    </option>
    <option id="partial-flag">
      <name>B — Add a partial_analysis honesty annotation</name>
      <pros>Honest: a single dead core signal annotates the result (a partial_analysis flag or a surfaced warning), pairing with ENG-04 honesty.</pros>
      <cons>Output-shape change -> version.ts bump (account for plans 01-03 bumps) + types.ts + aggregator + a test.</cons>
    </option>
    <option id="harden-retry">
      <name>C — Harden the fold retry</name>
      <pros>Adds a retry to fold (currently 0) so a single transient parse fail doesn't drop the audience half.</pros>
      <cons>Costs latency (D-09 tolerates temporary regression); doesn't make the remaining silent path honest on its own.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Davide's single-failure decision (A / B / C, or a combination) captured in SUMMARY + commit
    - The latency posture captured: reclaim-now (with which budget lever — never the fold timeout, PITFALL 2) or accept-under-cap
    - If B chosen: the flag name + where it surfaces recorded; if C: the fold retry count + the latency cost acknowledged
  </acceptance_criteria>
  <resume-signal>Select single-failure: A (accept), B (partial flag), or C (fold retry); and latency: reclaim-now or accept-under-cap</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LOCK + APPLY (conditional on Task 2) — honesty test, single-failure annotation, latency reclaim, version</name>
  <behavior>
    - Test 1 (ENG-04 LOCK, always): computeEngagementRange returns null when follower_count<=0 and a bounded range otherwise; no Math.sin path exists.
    - Test 2 (ENG-04 LOCK, always): analysis_unavailable is true iff BOTH core signals dead, false when at least one is live.
    - Test 3 (only if Task 2 = B): a single dead core signal sets partial_analysis true (and dual failure still sets analysis_unavailable).
    - Test 4 (only if Task 2 = C): a single transient fold parse fail triggers the new bounded fold retry before falling back.
  </behavior>
  <read_first>
    - src/lib/engine/aggregator.ts:182-231, :827-856, :1096-1102 (honesty surfaces to lock / extend)
    - src/lib/engine/pipeline.ts:710-746 (fold block — where a fold retry inserts if Task 2=C) and :683-692
    - src/lib/engine/types.ts (PredictionResult — where partial_analysis adds if Task 2=B)
    - src/lib/engine/version.ts (CURRENT value — read before bumping) + __tests__/version.test.ts:9-10
    - src/lib/engine/__tests__/aggregator.test.ts (honesty coverage to extend) + pipeline.test.ts
    - scripts/measure-pipeline.ts (re-run to confirm any reclaim)
  </read_first>
  <action>
    ALWAYS: add/extend aggregator.test.ts to LOCK the ENG-04 honesty (Tests 1 + 2 above) so a future regression that re-introduces fabrication or breaks the dual-failure flag fails CI — write these tests first (RED) then confirm GREEN against the unchanged honesty code. Then apply ONLY what Task 2 selected. If B (partial flag): add partial_analysis to PredictionResult (types.ts) + set it in aggregator.ts when exactly one core signal is dead, surface per the recorded direction; add Test 3. If C (fold retry): add a bounded retry to the fold block (pipeline.ts:710-746) within the latency budget — lower a budget if headroom is needed, NEVER raise the fold 90s HARD ceiling (PITFALL 2); add Test 4. If A: no code change beyond the LOCK tests. For the latency reclaim (if Task 2 = reclaim-now): apply the agreed budget lever (e.g. a thinking_budget/max_tokens trim) and re-run measure-pipeline.ts to confirm the reclaim; if accept-under-cap, record the accepted number. If B (or any output-shape change) was applied: read the CURRENT ENGINE_VERSION, bump one minor (account for plans 01-03 bumps), add a change-history comment, update version.test.ts:10. PRESERVE the Apify signed-URL flow (pipeline.ts:400-496) and all Zod boundaries. No fenced code — read files for current structure.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/aggregator.test.ts src/lib/engine/__tests__/pipeline.test.ts src/lib/engine/__tests__/version.test.ts 2>&1 | grep -v '^#' | tail -20; npm test 2>&1 | grep -v '^#' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - ENG-04 honesty LOCK tests (Tests 1 + 2) exist and pass — engagement-range null-when-no-baseline + analysis_unavailable dual-failure-only are now regression-guarded
    - The applied change matches EXACTLY Task 2's selection (A = lock only; B = partial_analysis + Test 3; C = fold retry + Test 4)
    - If a budget lever was trimmed for reclaim: measure-pipeline re-run shows TOTAL under the 300s cap and the reclaim recorded; the fold 90s ceiling was NOT raised (PITFALL 2)
    - If output shape changed (B): ENGINE_VERSION bumped from CURRENT value AND version.test.ts updated (both, or neither)
    - `npx vitest run aggregator.test.ts pipeline.test.ts version.test.ts` green AND full `npm test` engine suite green (phase gate)
    - Apify signed-URL flow + all Zod safeParse boundaries unchanged; no thrown frame on the E2E run
  </acceptance_criteria>
  <done>ENG-04 honesty locked by test; Task 2's single-failure + latency decisions applied; full engine suite green; E2E run clean under cap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/Apify -> engine read path | tiktok_url download uses the Apify token; must never leak into the URL fed to DashScope |
| 3 LLMs -> aggregator | Untrusted call outputs blend into the score + honesty flags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Apify token -> DashScope (pipeline.ts:400-496 server-side download -> short-TTL signed URL -> derive-and-drop cleanup) | mitigate (PRESERVE) | Any fold-retry / pipeline edit must NOT touch or bypass the signed-URL rehost + unconditional cleanup; flag if an edit nears it |
| T-04-02 | Tampering / DoS | malformed LLM JSON -> bounded retry + circuit breaker + graceful null fallback (no thrown frames) | mitigate (PRESERVE) | The ENG-01 audit confirms this holds; a new fold retry stays within the bounded envelope (no unbounded loop, no timeout raise) |
| T-04-03 | Integrity (honesty) | fabricated metrics shown as real (Math.sin deleted; engagement null-when-no-baseline; analysis_unavailable) | mitigate (LOCK) | This plan LOCKS these with tests so a future regression fails CI |
</threat_model>

<verification>
- `npx vitest run aggregator.test.ts pipeline.test.ts version.test.ts` green; full `npm test` engine suite green (phase gate).
- At least one live E2E rig run on a real video captured (Task 1) — the phase-gate live requirement from VALIDATION.md.
- Per D-02, this is the natural batched-verification point for the whole phase — offer Davide the full testing menu (he UATs the live /analyze on the test video via Playwright / Claude UATs / scripts).
</verification>

<success_criteria>
- ROADMAP criteria 1, 3, 4 advanced: E2E correctness (no thrown frames / honest fallbacks), honest grounded engagement range + deterministic-banded score, latency under cap toward <90s where free.
- Single-failure honesty decision captured (ENG-01/D-15); ENG-04 honesty locked by test (D-15/D-04 — no determinism gate added); latency measured + reclaimed-or-accepted (ENG-03/D-08/D-09).
</success_criteria>

<output>
Create `.planning/phases/01-engine-pipeline/01-04-SUMMARY.md` when done. Record: the fallback table + honesty confirmation, Davide's single-failure + latency decisions, the locked-honesty tests, any reclaim applied, and the final version value.
</output>
