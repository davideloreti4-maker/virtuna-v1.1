---
phase: 01-engine-pipeline
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - src/lib/engine/aggregator.ts
  - src/lib/engine/types.ts
  - src/lib/engine/stage10-critique.ts
  - src/lib/engine/version.ts
  - src/app/api/analyze/route.ts
  - src/lib/engine/__tests__/aggregator.test.ts
  - src/lib/engine/__tests__/stage10-critique.test.ts
  - src/lib/engine/__tests__/version.test.ts
autonomous: false
requirements: [ENG-04, ENG-06]
user_setup: []
must_haves:
  truths:
    - "Confidence 'model agreement' compares Apollo composite vs the FOLD audience score (two independent signals) on video — not Apollo vs itself; self-agreement is dead (F22/F44)"
    - "component_scores is dropped from the video output contract (kept for text mode) and no longer feeds confidence, since confidence now reads apollo-vs-fold (F24)"
    - "Stage-10's confidence-gap basis is re-based on apollo-vs-fold (or Stage-10 is retired per Davide's decision) so it no longer silently SKIPS on null gemini_score (F34)"
    - "The engine emits a first-class hero block { verdict_line, ceiling, the_one_fix, go_no_go, post_window } assembled from already-emitted Apollo fields — the moat insight is no longer buried only in ceiling_capper prose (F37/F41)"
    - "The hero block + engagement range survive permalink reload (persisted into the variants JSONB), so a shared/saved board shows what the live run showed (F42)"
  artifacts:
    - path: "src/lib/engine/aggregator.ts"
      provides: "calculateConfidence (apollo-vs-fold rebase), F24 component_scores drop on video, hero block assembly in aggregateScores"
    - path: "src/lib/engine/stage10-critique.ts"
      provides: "Re-based or retired Stage-10 confidence-gap critique (no null-skip)"
    - path: "src/app/api/analyze/route.ts"
      provides: "persistApolloToVariants extended to persist the hero block (F42)"
    - path: "src/lib/engine/types.ts"
      provides: "PredictionResult hero block field + component_scores video-mode contract"
  key_links:
    - from: "src/lib/engine/aggregator.ts"
      to: "calculateConfidence"
      via: "fold_audience_score passed as the agreement counterpart to apollo_score (replaces behavioral_score)"
      pattern: "calculateConfidence\\([^)]*fold"
    - from: "src/app/api/analyze/route.ts"
      to: "variants JSONB"
      via: "persistApolloToVariants merges hero block alongside apollo + engagement_range"
      pattern: "merged\\.(hero|apollo|engagement_range)"
---

<objective>
The COUPLED aggregator-assembly closeout. Four flags that ALL touch `calculateConfidence` / the final `aggregateScores` assembly and therefore MUST land in one wave, executed together (per synthesis + STATE.md coupling note):

- **F24** — drop `component_scores` from the video output contract (keep text mode). Verified safe on the overall blend (video = 0.5·apollo + 0.5·fold; `behavioral_score` derived from component_scores is UNUSED on video) EXCEPT it still feeds `calculateConfidence` — so F24 only lands cleanly WITH the F22 rebase.
- **F22/F44** — re-base confidence "model agreement" on **Apollo-vs-Fold** (currently `calculateConfidence` compares `apollo_score` vs `behavioral_score`, both from the SAME Apollo call → self-agreement, a fake trust anchor). The displayed "· likely lo–hi" band is board-derived from this same soft number (F44), so fixing F22 fixes the trust anchor at the source.
- **F34** — Stage-10's confidence-gap basis was `|gemini_score − behavioral_score|`; `gemini_score` died with D-R1 (3.17.0, now `number|null`, null on video) so Stage-10 currently SKIPS the gap check on null. Re-base on apollo-vs-fold (same basis as F22) OR retire Stage-10 — Davide decides at the checkpoint.
- **Hero block (F37/F41)** — emit a first-class `{ verdict_line, ceiling, the_one_fix, go_no_go, post_window }` so the moat insight isn't buried only in `ceiling_capper` prose. Apollo already emits the raw materials; this is AGGREGATOR assembly + persistence.

Plus **F42 persist** (the hero block + engagement range must survive permalink reload) because the hero block is a new output-shape field and persistence is the natural pairing.

Runs as Wave 4 (after 01-03's D-R1/F26 shipped). This is an output-shape change → version bump. Fold robustness, dead-tail prune, F7, and the E2E/honesty/latency closeout are SPLIT into plan 01-05 (which depends on this).

Purpose: make confidence REAL (apollo-vs-fold), lead with the moat (hero block), persist what's shown.
Output: rebased confidence, dropped component_scores on video, re-based/retired Stage-10, a first-class hero block persisted to permalink, a version bump.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-engine-pipeline/01-CONTEXT.md
@.planning/phases/01-engine-pipeline/01-RESEARCH.md
@.planning/phases/01-engine-pipeline/01-SYNTHESIS.md
@.planning/phases/01-engine-pipeline/01-WALKTHROUGH-LOG.md
@.planning/phases/01-engine-pipeline/01-03-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase (current as-built). Use these directly — no re-exploration needed. -->

aggregator.ts — the coupled assembly surfaces:
- `calculateConfidence(apolloScore, behavioralScore, ruleResult, trendEnrichment, hasVideo, deepseekConfidence, availability)` (~291-339). Agreement term (0-0.4) currently compares `apolloScore-50` vs `behavioralScore-50` → BOTH come from the same Apollo call (F22 self-agreement). Called at ~866 with `(apollo_score, behavioral_score, ...)`.
- `fold_audience_score` (~812-819): `foldOn ? round(0.50·completion + 0.25·share + 0.15·save + 0.10·comment) : 0`. `foldOn` (~811) = `foldOutcome.fold_success && foldAgg !== null`. THIS is the independent audience signal F22 should agree-check against.
- `apollo_score` (~796) = `deepseek?.composite_score ?? 0`. `apolloOn` (~830) = `deepseek != null`.
- `behavioral_score` (~776) = `round(avg(7 component_scores)*10)`. On video it does NOT enter `overall_score` (the `apolloOn && foldOn` branch uses `0.5·apollo + 0.5·fold`) — only enters confidence (F22) + the text-mode fallback blend (~839).
- `assembleFeatureVector` (~355-430) reads `deepseek?.component_scores.*` into FeatureVector — F24 must NOT break text mode here.
- `apollo_reasoning` block (~1083-1099): already surfaces `ceiling_capper`, `confidence_scope`, `platform_note`, `rewrites`, `dimensions`, `composite_score`. Hero-block source materials.
- Hero source materials (all already on `result` or `deepseek`): ceiling←`deepseek.ceiling_capper`; the_one_fix←`deepseek.rewrites[0].variant`; go_no_go←`result.anti_virality_gated` (+ `anti_virality_reason`); post_window←`result.optimal_post_window`; verdict_line←tier-from-`overall_score` + anti-virality-gated state.
- `analysis_unavailable` (~860), `anti_virality_gated`/`_reason` (~1141-1148, re-eval'd post-critique ~1223-1226).

stage10-critique.ts:
- `SIGNAL_GAP_THRESHOLD = 30` (:29). `deriveCritique` (~58-67): `if (result.gemini_score != null) { gap = |gemini_score - behavioral_score|; if gap>30 ... }` — SKIPS on null (D-R1). F34: re-base on `|apollo_score - fold_audience_score|` (needs those threaded onto PredictionResult or the critique input) OR retire.
- `applyCritiqueAdjustment(confidence, critique)` clamps adj to [-0.2, 0].

types.ts:
- `PredictionResult` (273-385). `apollo_reasoning?` (318-327) is the hero-material carrier. `gemini_score: number|null` (297). `behavioral_score: number` (298). Add the hero block field here (optional for back-compat).
- `ComponentScoresSchema` feeds `component_scores` (827). For F24, component_scores stays in the schema (text mode) but the video assembly path stops populating/persisting it.

route.ts persistence (F42):
- `persistApolloToVariants` (220-264): read-merge-write into `variants` JSONB. Already persists `merged.apollo = apollo` (248) + `merged.engagement_range = engagement_range` (249). engagement_range IS ALREADY persisted here — verify before claiming F42 gap on it. Extend with `merged.hero = <hero block>`. NO migration — `variants` is the JSONB extensibility bag.
- `buildInsertRow` (652-743) does NOT carry predicted_engagement or apollo_reasoning (they ride variants). Hero block rides variants too (no top-level column → no migration).

version.ts: CURRENT `ENGINE_VERSION = "3.17.0"` (:105, set by 01-03 D-R1). version.test.ts:9-10 asserts `"3.17.0"`. Output-shape change here → bump 3.17.0 → 3.18.0 + change-history comment + update version.test.ts:9-10.

D-04 (CONTEXT): do NOT add a score-determinism gate. D-00: human-in-the-loop — the Stage-10 fate + the hero verdict_line wording are co-review decisions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AUDIT — capture the live aggregator shapes for the coupled set (no edits)</name>
  <read_first>
    - src/lib/engine/aggregator.ts:760-870 (apollo_score / behavioral_score / fold_audience_score / calculateConfidence callsite), :291-339 (calculateConfidence), :1074-1185 (result assembly + apollo_reasoning), :1206-1235 (Stage-10 wiring)
    - src/lib/engine/stage10-critique.ts:29, :58-67 (the gemini_score gap basis that skips on null)
    - src/app/api/analyze/route.ts:209-264 (persistApolloToVariants — what is ALREADY persisted: apollo + engagement_range)
    - src/lib/engine/types.ts:273-385 (PredictionResult), :318-327 (apollo_reasoning hero materials)
    - scripts/smoke-tiktok-pipeline.ts, scripts/measure-pipeline.ts (the rigs)
    - .planning/phases/01-engine-pipeline/01-WALKTHROUGH-LOG.md (F22/F24/F34/F37/F41/F42/F44 definitions)
  </read_first>
  <action>
    No edits (D-01 audit-first / D-00). Run `npx tsx scripts/smoke-tiktok-pipeline.ts "$HOME/Downloads/TikTok Video Downloader.mp4"` 2>&1 | tee /tmp/eng-04-smoke.txt and capture the real run's `confidence`, `apollo_score`/composite, `fold_audience_score` (or fold_success), `behavioral_score`, `ceiling_capper`, `rewrites[0].variant`, `anti_virality_gated`/reason, and `optimal_post_window`. Produce three audit artifacts for the checkpoint: (1) CONFIDENCE EVIDENCE — show that today's agreement term is apollo-vs-behavioral (both Apollo-sourced = self-agreement, F22) and what the agreement term WOULD be under apollo-vs-fold on this run (compute both by hand from the captured numbers); confirm whether fold_success was true (if fold died, the rebase degrades — note the fallback). (2) HERO MATERIALS MAP — confirm each of {verdict_line, ceiling, the_one_fix, go_no_go, post_window} has a live non-null source on the real run (or note which are null and why), and propose the verdict_line wording rule (tier-from-overall_score gated by anti_virality). (3) PERSIST GAP — confirm via route.ts:209-264 that engagement_range is ALREADY persisted to variants.engagement_range (so the only NEW persist is the hero block), and confirm the hero block can ride variants.hero with NO migration. Present all three.
  </action>
  <verify>
    <automated>npx tsx scripts/smoke-tiktok-pipeline.ts "$HOME/Downloads/TikTok Video Downloader.mp4" 2>&1 | tee /tmp/eng-04-smoke.txt | grep -iE 'confidence|composite|fold|score|ceiling|anti_virality|post_window' | head -30</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/eng-04-smoke.txt captured from a real video run
    - CONFIDENCE EVIDENCE shows today's apollo-vs-behavioral self-agreement value AND the computed apollo-vs-fold value for the same run, with fold_success state noted
    - HERO MATERIALS MAP lists a live source (or justified null) for all 5 hero fields + a proposed verdict_line rule
    - PERSIST GAP confirms engagement_range already persisted (route.ts:249) and that the hero block needs NO migration (rides variants JSONB)
  </acceptance_criteria>
  <done>Three audit artifacts produced from a real run; no edits; the checkpoint has the evidence it needs.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: CO-REVIEW CHECKPOINT — Stage-10 fate + verdict_line wording (D-00/F34/F37)</name>
  <action>STOP and present Task 1's CONFIDENCE EVIDENCE + HERO MATERIALS MAP + PERSIST GAP to Davide. Do NOT auto-decide (D-00). Capture his Stage-10 fate (rebase-vs-retire) and the verdict_line wording rule before any edit.</action>
  <read_first>
    - /tmp/eng-04-smoke.txt + Task 1's three audit artifacts
    - src/lib/engine/stage10-critique.ts:29,:58-67 (Stage-10 gap basis)
    - .planning/phases/01-engine-pipeline/01-WALKTHROUGH-LOG.md F34, F37
  </read_first>
  <decision>(a) Stage-10: RE-BASE its confidence-gap on apollo-vs-fold (same basis as F22), or RETIRE Stage-10 entirely (a heuristic named "critique" whose only live check died with gemini_score)? (b) verdict_line wording: confirm the tier-from-overall_score + anti-virality-gated rule from Task 1 (e.g. gated → "Don't post yet"; else tier label), or adjust.</decision>
  <context>F22 rebases confidence on apollo-vs-fold. F34's Stage-10 gap currently skips on null gemini_score (dead on video). It either inherits the same apollo-vs-fold basis or is retired. Present Task 1's CONFIDENCE EVIDENCE (does the apollo-vs-fold gap even move confidence on the real run?) so the keep-vs-retire call is evidence-based. The hero verdict_line is the user-facing lead sentence — Davide must approve its wording rule (D-00 accuracy over speed).</context>
  <options>
    <option id="rebase-stage10">
      <name>A — Re-base Stage-10 on apollo-vs-fold</name>
      <pros>Keeps the consistency-check alive on video; one shared basis with F22; minimal deletion.</pros>
      <cons>Keeps a heuristic whose value is unproven; needs apollo_score + fold_audience_score threaded to the critique input.</cons>
    </option>
    <option id="retire-stage10">
      <name>B — Retire Stage-10</name>
      <pros>Removes a dead-on-video deterministic "critique" + its confidence nudge; simpler assembly tail; honest (no fake critique).</pros>
      <cons>Loses the (rarely-firing) consistency dampener; touches the post-critique anti-virality re-eval ordering (Pitfall 7).</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Davide's Stage-10 decision (A rebase / B retire) captured in SUMMARY + commit message
    - The approved verdict_line wording rule recorded (tier + anti-virality-gated mapping)
  </acceptance_criteria>
  <resume-signal>Select Stage-10: A (rebase apollo-vs-fold) or B (retire); and confirm/adjust the verdict_line wording rule</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: APPLY the coupled set — F22/F44 confidence rebase + F24 drop + F34 (per Task 2) + hero block + persist + version bump</name>
  <behavior>
    - Test 1 (F22): calculateConfidence agreement term is computed from apollo_score vs fold_audience_score on video — given apollo &amp; fold on the SAME side of 50, agreement=0.4; opposite sides &amp; far apart, agreement=0.0. Two equal Apollo-sourced inputs no longer auto-trigger the agree branch.
    - Test 2 (F24): on video mode the assembled PredictionResult does NOT carry component_scores (or it is null/omitted per the chosen field shape); text mode STILL carries component_scores (regression guard).
    - Test 3 (hero F37): aggregateScores emits a hero block with all 5 fields populated from a fixture where ceiling_capper/rewrites/anti_virality/optimal_post_window are present; verdict_line follows the approved rule; fields degrade to null individually when their source is absent (never throws).
    - Test 4 (F34, conditional on Task 2): if A — stage10 gap fires off |apollo_score - fold_audience_score| &gt; threshold and does NOT skip on null gemini_score; if B — runStage10Critique is removed/no-op and confidence is unchanged by it (assert the assembly tail still produces a valid result + anti-virality re-eval preserved).
  </behavior>
  <read_first>
    - src/lib/engine/aggregator.ts:291-339 (calculateConfidence), :760-885 (scores + confidence callsite), :1074-1185 (assembly + apollo_reasoning), :1206-1235 (Stage-10 tail)
    - src/lib/engine/stage10-critique.ts:29,:43-67 (gap basis + adjustment)
    - src/lib/engine/types.ts:273-385 (PredictionResult), :318-327 (apollo_reasoning)
    - src/app/api/analyze/route.ts:220-264 (persistApolloToVariants — extend with hero)
    - src/lib/engine/version.ts:91-105 (CURRENT 3.17.0 + change-history format), src/lib/engine/__tests__/version.test.ts:9-10
    - src/lib/engine/__tests__/aggregator.test.ts (confidence + assembly coverage to extend), stage10-critique.test.ts
  </read_first>
  <action>
    Write tests FIRST (RED), then implement. (1) F22/F44 — change `calculateConfidence` so the agreement term reads `apollo_score` vs `fold_audience_score` (the independent audience signal) instead of `behavioralScore`; update the callsite (~866) to pass `fold_audience_score` (and `foldOn`) so a dead fold degrades the agreement term honestly rather than self-agreeing. Keep the HARD-03 LOW floor on dual failure. (2) F24 — stop populating `component_scores` on the VIDEO assembly path (gate on `payload.input_mode !== "text"` / `hasVideo`); preserve text mode. Since confidence no longer reads behavioral_score's component-derived agreement, F24 is now safe. Keep `assembleFeatureVector` text-mode behavior intact. (3) F34 — apply Task 2's decision: if A, thread `apollo_score` + `fold_audience_score` onto the critique input and re-base the gap on `|apollo - fold|` (drop the null-skip); if B, remove `runStage10Critique`/`applyCritiqueAdjustment` from the tail and preserve the post-critique anti-virality re-eval ordering (Pitfall 7) by computing the gate from the final confidence directly. (4) HERO BLOCK — in `aggregateScores`, assemble `{ verdict_line, ceiling, the_one_fix, go_no_go, post_window }` from the already-emitted materials (ceiling←deepseek.ceiling_capper; the_one_fix←deepseek.rewrites?.[0]?.variant; go_no_go←anti_virality_gated+reason; post_window←optimal_post_window; verdict_line←approved tier+gated rule). Each field individually nullable, non-throwing. Add the field to PredictionResult in types.ts (optional). (5) F42 PERSIST — extend `persistApolloToVariants` to `merged.hero = result.hero` (read-merge-write preserved; NO migration — variants JSONB). (6) VERSION — read CURRENT ENGINE_VERSION (3.17.0), bump to 3.18.0 with a change-history comment (hero block + component_scores video-drop + confidence rebase = output-shape change → cache invalidation), update version.test.ts:9-10. NO determinism gate (D-04). No fenced code — read files for current structure.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/aggregator.test.ts src/lib/engine/__tests__/stage10-critique.test.ts src/lib/engine/__tests__/version.test.ts 2>&1 | grep -v '^#' | tail -25; npm test 2>&1 | grep -v '^#' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - F22: a test proves the agreement term reads apollo-vs-fold; two equal Apollo-sourced numbers no longer force the 0.4 agree branch (self-agreement dead)
    - F24: video-mode PredictionResult drops component_scores; text-mode regression test still green
    - F34: matches Task 2 exactly — A) gap re-based on apollo-vs-fold + no null-skip test; or B) Stage-10 removed + assembly-tail + anti-virality ordering test green
    - Hero block: all 5 fields assembled from live materials, each individually null-degrading, verdict_line follows the approved rule (Test 3 green)
    - F42: persistApolloToVariants writes merged.hero; NO Supabase migration added (rides variants JSONB)
    - ENGINE_VERSION bumped 3.17.0 → 3.18.0 AND version.test.ts updated (both, or neither)
    - `npx vitest run aggregator.test.ts stage10-critique.test.ts version.test.ts` green AND full `npm test` engine suite green
  </acceptance_criteria>
  <done>Confidence is apollo-vs-fold; component_scores dropped on video; Stage-10 per Davide; hero block emitted + persisted; version bumped; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 3 LLMs → aggregator | Untrusted Apollo/fold outputs blend into score + confidence + the new hero block |
| aggregator → variants JSONB persist | Hero block written into a user-scoped row (must keep V4 access control) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Apify token → DashScope signed-URL rehost (pipeline.ts:400-496) | mitigate (PRESERVE) | This plan does NOT touch pipeline.ts; the F7 + fold-retry edits that DO touch the signed-URL flow live in 01-05. Flag if any aggregator change reaches into pipeline. |
| T-04-02 | Tampering / Integrity (honesty) | confidence self-agreement (F22) presents a fake trust anchor as HIGH | mitigate (FIX) | Re-base on apollo-vs-fold so the displayed confidence + band reflect two independent signals, not one call graded twice. |
| T-04-03 | Tampering | hero block assembled from untrusted LLM strings (ceiling_capper, rewrite variant) | mitigate | Each hero field individually nullable + non-throwing; no eval/interpolation into prose; § prose-strip from 01-01 already applied upstream to ceiling_capper. |
| T-04-04 | Spoofing / access control | persistApolloToVariants hero write | mitigate (PRESERVE) | Keep the `.eq("user_id")` V4 guard on BOTH the read and write of the variants merge (route.ts:240,254). |
</threat_model>

<verification>
- `npx vitest run aggregator.test.ts stage10-critique.test.ts version.test.ts` green; full `npm test` engine suite green (wave gate per VALIDATION.md).
- At least one live smoke run captured (Task 1) on a real video — phase-gate live requirement.
- Per D-02, batch the deeper live verification (Playwright /analyze permalink reload to confirm the hero block + engagement_range survive) into 01-05's closeout, OR offer it here if Davide wants to UAT the hero block immediately.
</verification>

<success_criteria>
- ROADMAP criterion 3 advanced: confidence is honestly grounded (apollo-vs-fold, not self-agreement); the trust anchor (F44) is fixed at the source.
- The cheatcode hero block (F37/F41) is emitted first-class and persists across reload (F42) — the moat insight leads instead of being buried in prose.
- component_scores dropped on video (F24); Stage-10 re-based or retired (F34) per Davide; version bumped; no determinism gate added (D-04).
</success_criteria>

<output>
Create `.planning/phases/01-engine-pipeline/01-04-SUMMARY.md` when done. Record: the confidence evidence (before/after agreement value), Davide's Stage-10 decision + verdict_line rule, the hero block shape, the persist extension (variants.hero, no migration), and the new ENGINE_VERSION. Flag 01-05 as the dependent next plan (fold robustness + prune + F7 + E2E/honesty/latency).
</output>
