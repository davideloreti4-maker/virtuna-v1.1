---
phase: 01-engine-pipeline
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - src/lib/engine/pipeline.ts
  - src/lib/engine/wave3/fold.ts
  - src/lib/engine/aggregator.ts
  - src/lib/engine/types.ts
  - src/lib/engine/omni-analysis.ts
  - src/lib/engine/version.ts
  - src/app/api/analyze/route.ts
  - src/lib/engine/__tests__/aggregator.test.ts
  - src/lib/engine/__tests__/pipeline.test.ts
  - src/lib/engine/wave3/__tests__/fold.test.ts
  - src/lib/engine/__tests__/version.test.ts
autonomous: false
requirements: [ENG-01, ENG-03, ENG-04]
user_setup: []
must_haves:
  truths:
    - "Fold has ONE bounded retry (8s→16s, well under the 90s HARD ceiling which is NEVER raised) so a single transient parse/timeout hiccup no longer silently halves the score by dropping the audience half (F18)"
    - "A single bad persona no longer kills the whole fold — valid personas are salvaged instead of all-or-nothing dropped (F20); and below DIVERSITY_FLOOR the fold nudges a retry rather than only warning (F19)"
    - "A single dead core signal is surfaced honestly (partial_analysis), not just the dual-failure analysis_unavailable — the board no longer silently shows half a basis (F18 honesty / ENG-01)"
    - "Dead-tail constants confirmed unconsumed are pruned from the output contract (confidence:1.0 hardcode, rule_score:50/trend_score:0/ml_score:0, reasoning:'', dead score_weights keys, platform_fit/audio_fingerprint null) — stops dead numbers leaking to the UI as meaningful (F12/F35/F43)"
    - "The tiktok-rehost derive-and-drop race is verified on a real run and, if it races, moved to a true post-pipeline finally (after all 3 sighted calls resolve) — PRESERVING the Apify signed-URL security flow + unconditional cleanup (F7/T-04-01)"
    - "A full E2E run on a real video completes with no thrown frames + honest fallbacks; ENG-04 honesty is LOCKED by tests (Math.sin gone, engagement-range null-when-no-baseline, analysis_unavailable dual-failure-only); per-stage latency measured under the 300s cap and reclaimed-or-accepted (ENG-03/D-08/D-09)"
  artifacts:
    - path: "src/lib/engine/wave3/fold.ts"
      provides: "Bounded fold retry (F18) + salvage-valid-personas (F20) + diversity retry-nudge (F19)"
    - path: "src/lib/engine/pipeline.ts"
      provides: "F7 rehost-delete moved to a true post-pipeline finally (if it races) + fold single-signal surfacing"
    - path: "src/lib/engine/aggregator.ts"
      provides: "partial_analysis honesty flag + dead-tail prune (F35/F43)"
    - path: "src/lib/engine/types.ts"
      provides: "partial_analysis field + pruned PredictionResult contract"
  key_links:
    - from: "src/lib/engine/aggregator.ts"
      to: "src/lib/engine/types.ts"
      via: "partial_analysis flag set when exactly one core signal is dead (derivable from signal_availability)"
      pattern: "partial_analysis"
    - from: "src/lib/engine/pipeline.ts"
      to: "supabase.storage.remove(rehostPath)"
      via: "delete moved into a finally that runs after Omni + fold + Apollo resolve"
      pattern: "rehostPath|finally"
---

<objective>
The robustness + honesty + closeout plan (the second half of the 01-04 bundle, split out for context budget). Depends on 01-04 (the coupled aggregator set ships first). Five threads:

- **Fold robustness (F18/F20/F19)** — fold has ZERO retry today (one attempt; any hiccup → `fold_success=false` → score silently drops the audience half). Add ONE bounded retry (8s→16s, ≪ the 90s HARD ceiling — NEVER raised). F20: the segment-count guard is all-or-nothing (one bad persona kills all 10) → salvage valid personas. F19: the diversity guard is warn-only → add a retry-nudge below DIVERSITY_FLOOR (cheap now that fold is fast/flash). Surface single-signal death (partial_analysis), not just dual.
- **Dead-tail prune (F12/F35/F43)** — stop emitting/persisting dead constants once each is CONFIRMED unconsumed: confidence:1.0 hardcode (omni-analysis wave0), rule_score:50 / trend_score:0 / ml_score:0, reasoning:"" (always empty), dead score_weights keys, platform_fit/audio_fingerprint null. Conservative — verify each against consumers + buildInsertRow persistence before dropping.
- **F7** — verify the tiktok-rehost derive-and-drop race on a real run; if the fire-and-forget delete (pipeline.ts:478-496) can land before Omni/fold/Apollo fetch the signed URL → move it to a TRUE post-pipeline finally. PRESERVE the Apify signed-URL security flow + unconditional cleanup (T-04-01).
- **E2E verify + honesty LOCK + latency** — run smoke + measure-pipeline on a real video; confirm no thrown frames + honest fallbacks; LOCK ENG-04 honesty with tests (Math.sin gone, engagement-range null-when-no-baseline, analysis_unavailable dual-failure-only); measure per-stage latency under the 300s cap; reclaim-or-accept (D-08/D-09). NO determinism gate (D-04).

Runs LAST (Wave 5) because the honesty LOCK + latency measure the post-refinement engine (post-01-04 hero block + confidence rebase). partial_analysis + the prune are output-shape changes → version bump (account for 01-04's 3.18.0).

Purpose: make the engine robust (never silently halve the score) + honest (surface single-signal death, prune dead numbers) + proven (E2E green, latency under cap).
Output: fold retry/salvage, partial_analysis honesty, pruned dead tail, F7 fixed-or-cleared, locked honesty tests, a measured latency map, a version bump.
</objective>

<scope_note>
ENG-06 is deliberately OUT of this plan. ENG-06's LOCKED deliverable is D-12: the step-by-step 3-call prompt I/O co-review walk WITH Davide ("chunk B"), tracked in 01-03-SUMMARY + STATE.md as a SEPARATE still-open co-review session. The dead-tail prune (D-14 field map) + any latency token-trim here are output-contract / budget edits, NOT the prompt-quality review ENG-06 requires. ENG-06 remains its own owning open plan, to be run as a fresh co-review session before phase close. No ENG-06 task is invented here.
</scope_note>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-engine-pipeline/01-CONTEXT.md
@.planning/phases/01-engine-pipeline/01-RESEARCH.md
@.planning/phases/01-engine-pipeline/01-QUALITY-OPTIMIZATION.md
@.planning/phases/01-engine-pipeline/01-WALKTHROUGH-LOG.md
@.planning/phases/01-engine-pipeline/01-04-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase (current as-built). -->

fold.ts (the robustness surfaces):
- `runFold(slots, segments, verbatim, emotionArc, videoUrl, onStageEvent)` (291-449). SINGLE attempt: `PER_CALL_TIMEOUT_MS` abort timer (308), one `ai.chat.completions.create` (337), `FoldResponseSchema.safeParse` (372). On any failure → `fold_success=false` → empty arrays (F18 silent halve).
- `PER_CALL_TIMEOUT_MS` = the 90s HARD ceiling — NEVER raise (PITFALL 2). A bounded retry (e.g. one re-attempt) must fit inside the existing call envelope or use a short inner budget (8s→16s style), NOT extend the abort.
- Segment-count guard (383-393): `mismatchedPersonas = personas.filter(p => p.segment_reactions.length !== segments.length)`; if ANY mismatch → push warning + DON'T set fold_success (all-or-nothing, F20). Salvage: keep the valid personas, drop only the mismatched ones, set fold_success if enough remain.
- `DIVERSITY_FLOOR = 0.10` (102); `checkDiversityGuard(avgRange)` (126-138) warn-only (394-399). F19: below floor → one retry-nudge (re-call) before accepting.
- `Wave3FoldOutcome` (142-152): `{ pass2Results, personaSimResults, warnings, cost_cents, fold_success }`.

pipeline.ts:
- F7 race: rehost delete (478-496) is `void supabase.storage.from("videos").remove([pathToDelete]).catch(...)` — fire-and-forget, scheduled BEFORE Omni (510), fold (732), Apollo. Comment calls it "finally-equivalent" but it is NOT in a finally. Move to a true post-pipeline finally AFTER all 3 sighted calls resolve, IF the audit proves it races.
- Apify signed-URL flow (400-477): server-side download → short-TTL signed URL → `signedVideoUrl`. T-04-01 — PRESERVE. `rehostPath` is the temp object to delete unconditionally.
- Fold callsite (710-746): `foldOutcome = await runFold(...)`; on throw → `warnings.push("Fold unavailable: ...")`, foldOutcome stays null.

aggregator.ts (honesty + prune surfaces):
- `analysis_unavailable` (860) = `!availability.gemini && !availability.behavioral` (DUAL only). partial_analysis = exactly ONE of {gemini, behavioral/fold} dead — derivable from `availability` (724-749). Add to PredictionResult + set it.
- DEAD-TAIL candidates to verify-then-prune: `rule_score: 50` (1107), `trend_score: 0` (1108), `ml_score: 0` (1111), `reasoning: ""` (1080), `platform_fit: null` (1184), `audio_fingerprint` null, `score_weights: { ...weights, ml:0, rules:0, trends:0, gemini:0 }` (1161 — dead keys). `applyCtaPenalty` (137-147) is DEAD (defined, never called per F35/D-R1 note) — safe to delete. `computeEngagementRange` (184-233): Math.sin already deleted (432 comment) — LOCK it.
- buildInsertRow (route.ts:652-743) persists: rule_score (664), trend_score (665), ml_score (689), reasoning (680), score_weights (666). Each prune MUST be checked against this persist + against board consumers before dropping a column-backed field (keep the DB column, stop emitting a fake value, OR confirm the column is unconsumed).

omni-analysis.ts:
- F12: wave0 `confidence: 1.0` hardcode (~260-265) — the model never produces it; fake certainty. Confirm no consumer trusts it, then drop or mark dead.

stage10-critique.ts: if 01-04 RETIRED Stage-10 (Task 2=B), it is already gone — skip; if re-based, leave it.

version.ts: CURRENT value after 01-04 = `3.18.0` (read it — do not assume). partial_analysis + prune = output-shape change → bump 3.18.0 → 3.19.0 + change-history comment + update version.test.ts. D-23 cache invariant.

scripts: `measure-pipeline.ts` prints TOTAL_LATENCY_MS + per-stage + PARALLEL/SERIALIZED + !!FAIL. `smoke-tiktok-pipeline.ts` = E2E signal-completeness gate. Test video: `$HOME/Downloads/TikTok Video Downloader.mp4`. PITFALL 2: lower a budget, never raise the fold 90s ceiling. D-04: no determinism gate. D-08: quality wins over latency; D-09: temporary regression tolerated, reclaim before close.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AUDIT — F7 race + fallback-path + honesty confirm + latency map on a real run (no edits)</name>
  <read_first>
    - src/lib/engine/pipeline.ts:400-496 (Apify signed-URL flow + the F7 fire-and-forget delete — PRESERVE), :508-541 (Omni), :702-746 (fold)
    - src/lib/engine/wave3/fold.ts:291-449 (single-attempt structure, segment guard, diversity guard)
    - src/lib/engine/aggregator.ts:182-233 (computeEngagementRange + Math.sin-gone notes), :860 (analysis_unavailable dual-only), :1074-1185 (dead-tail constants in the result), :137-147 (dead applyCtaPenalty)
    - src/app/api/analyze/route.ts:652-743 (buildInsertRow — what each dead constant persists to)
    - src/components/board/InsightHeroFrame.tsx:222-232 (the warning filter that hides single-failure)
    - scripts/measure-pipeline.ts, scripts/smoke-tiktok-pipeline.ts
    - .planning/phases/01-engine-pipeline/01-WALKTHROUGH-LOG.md (F7/F12/F18/F19/F20/F35/F43)
  </read_first>
  <action>
    No edits (D-01/D-00). Run `npx tsx scripts/smoke-tiktok-pipeline.ts "$HOME/Downloads/TikTok Video Downloader.mp4"` and `npx tsx scripts/measure-pipeline.ts "$HOME/Downloads/TikTok Video Downloader.mp4"` and produce FOUR artifacts. (1) F7 RACE VERDICT — trace whether the fire-and-forget rehost delete (pipeline.ts:478-496) can resolve before Omni/fold/Apollo fetch the signed URL on a real tiktok_url run (or reason from the await ordering if a live tiktok_url isn't runnable): RACES (move to finally) or SAFE (clear F7 with evidence). (2) FALLBACK TABLE — for each call note which fallback fired + whether it surfaced or stayed silent; pin the single-dead-signal gap (fold-dead-Apollo-ok or vice-versa halves the basis with only a board-filtered warning). (3) HONESTY CONFIRMATION — assert from code+run: Math.sin gone (aggregator:432), engagement range = follower×quality-or-null, analysis_unavailable dual-failure-only; flag any leaking fake number (rule_score:50/trend_score:0/ml_score:0/confidence:1.0). (4) PRUNE SAFETY LIST — for EACH dead-tail candidate (rule_score, trend_score, ml_score, reasoning, score_weights dead keys, platform_fit, audio_fingerprint, confidence:1.0 hardcode, applyCtaPenalty), confirm via grep against consumers + buildInsertRow whether it is truly unconsumed (safe to drop), column-backed (keep column, stop fake value), or actually read (keep). (5) LATENCY MAP — per-stage + TOTAL vs 300s cap + delta vs the ~70s/Apollo-~92s baseline. Present all.
  </action>
  <verify>
    <automated>npx tsx scripts/measure-pipeline.ts "$HOME/Downloads/TikTok Video Downloader.mp4" 2>&1 | tee /tmp/eng-05-latency.txt | grep -iE 'TOTAL_LATENCY_MS|FAIL|PARALLEL|SERIALIZED' | head; npx tsx scripts/smoke-tiktok-pipeline.ts "$HOME/Downloads/TikTok Video Downloader.mp4" 2>&1 | tee /tmp/eng-05-smoke.txt | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - smoke + measure ran on a real video; outputs captured (/tmp/eng-05-smoke.txt, /tmp/eng-05-latency.txt)
    - F7 RACE VERDICT exists (RACES → move to finally / SAFE → cleared with evidence)
    - FALLBACK TABLE classifies each silent path, single-dead-signal gap pinned
    - HONESTY CONFIRMATION: Math.sin gone, engagement grounded-or-null, analysis_unavailable dual-only — leaking fake numbers flagged
    - PRUNE SAFETY LIST classifies every dead-tail candidate as drop/keep-column/keep with grep evidence against consumers + buildInsertRow
    - LATENCY MAP: per-stage + TOTAL vs 300s cap + baseline delta
  </acceptance_criteria>
  <done>Five audit artifacts produced from a real run; no edits; the checkpoint has the evidence.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: CO-REVIEW CHECKPOINT — single-signal honesty + fold robustness + prune list + latency posture (D-00/ENG-01/D-15/ENG-03)</name>
  <action>STOP and present Task 1's F7 verdict + fallback table + honesty confirmation + PRUNE SAFETY LIST + latency map to Davide. Do NOT auto-decide (D-00). Capture his single-signal honesty choice, fold robustness scope, approved prune list, and latency posture before any edit.</action>
  <read_first>
    - /tmp/eng-05-smoke.txt, /tmp/eng-05-latency.txt + Task 1's five artifacts
    - .planning/phases/01-engine-pipeline/01-RESEARCH.md §ENG-01 (options A/B/C), §ENG-03
  </read_first>
  <decision>(a) SINGLE-SIGNAL HONESTY: does a single dead core signal get a user-visible partial_analysis annotation (B), stay silent graceful (A), or get hardened with the fold retry only (C — or a combination, e.g. retry AND flag)? (b) FOLD ROBUSTNESS: confirm the fold retry bound (one re-attempt, 8s→16s style inner budget, never raise the 90s ceiling) + salvage-valid-personas (F20) + diversity retry-nudge (F19) — all in or trim any. (c) PRUNE LIST: approve which dead-tail fields are dropped vs kept-column-but-stop-fake-value, from Task 1's PRUNE SAFETY LIST (conservative — confirm each). (d) LATENCY: reclaim-now (which budget lever, never the fold timeout) or accept-under-cap (D-08).</decision>
  <context>Today only DUAL failure surfaces (analysis_unavailable); a single dead signal silently halves the score basis and the board filters the warning (InsightHeroFrame:222-232). D-15 + ENG-01 ask whether that bar is honest. Fold robustness (F18/F20/F19) is cheap now that fold is fast (flash). The prune (F43) removes dead numbers leaking to the UI — but is conservative: each field needs Task 1's consumer-confirmation before dropping. Present the PRUNE SAFETY LIST + the latency map + the F7 verdict.</context>
  <options>
    <option id="accept">
      <name>A — Accept single-signal as graceful (audit-only on that thread)</name>
      <pros>No partial_analysis output-shape change for the honesty thread.</pros>
      <cons>User still sees nothing when half the basis dies.</cons>
    </option>
    <option id="partial-flag">
      <name>B — Add partial_analysis honesty annotation</name>
      <pros>Honest: a single dead core signal annotates the result; pairs with ENG-04 honesty + the new hero block.</pros>
      <cons>Output-shape change → version bump + types.ts + aggregator + test.</cons>
    </option>
    <option id="harden-retry">
      <name>C — Harden fold retry (F18) ± salvage/nudge (F20/F19)</name>
      <pros>A single transient fold fail no longer drops the audience half — fixes the most common single-signal-death cause at the source.</pros>
      <cons>Small latency cost (D-09 tolerates); on its own doesn't make a hard fold death honest (pairs with B).</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Davide's single-signal decision (A/B/C or combination) captured in SUMMARY + commit
    - Fold robustness scope confirmed (retry bound + salvage + diversity nudge — all or trimmed), with the 90s ceiling explicitly NOT raised
    - The approved PRUNE LIST recorded field-by-field (drop / keep-column / keep)
    - Latency posture captured (reclaim-now + lever, or accept-under-cap)
  </acceptance_criteria>
  <resume-signal>Select single-signal A/B/C(+combo); confirm fold robustness scope; approve the prune list; latency reclaim-now-or-accept</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: APPLY robustness — fold retry/salvage/diversity-nudge (F18/F20/F19) + partial_analysis (per Task 2) + F7 fix (if races)</name>
  <behavior>
    - Test 1 (F18): a single transient fold parse/timeout failure on the first attempt triggers the bounded retry before falling back; a second failure → fold_success=false (graceful). The retry inner budget is well under PER_CALL_TIMEOUT_MS (90s ceiling unchanged).
    - Test 2 (F20): a fold response where ONE persona has a wrong segment_reactions count salvages the valid personas (fold_success=true with the good ones) instead of dropping all 10.
    - Test 3 (F19, if in scope): an avgCurveRange below DIVERSITY_FLOOR triggers one retry-nudge re-call before accepting the homogenized curves.
    - Test 4 (partial_analysis, if Task 2=B): exactly one dead core signal sets partial_analysis=true; dual failure still sets analysis_unavailable=true; both-live sets both false.
    - Test 5 (F7, if races): the rehost delete runs AFTER Omni+fold+Apollo resolve (in a finally), never before — assert ordering / that signedVideoUrl is still valid through all 3 calls.
  </behavior>
  <read_first>
    - src/lib/engine/wave3/fold.ts:291-449 (retry/salvage/diversity insertion points), :102,:126-138 (DIVERSITY_FLOOR + guard), :308 (PER_CALL_TIMEOUT_MS — DO NOT raise)
    - src/lib/engine/pipeline.ts:400-496 (F7 delete + Apify flow — PRESERVE), :702-746 (fold callsite)
    - src/lib/engine/aggregator.ts:724-749 (availability struct), :860 (analysis_unavailable), :1144 (where partial_analysis sets near it)
    - src/lib/engine/types.ts:335-343 (analysis_unavailable doc — add partial_analysis sibling)
    - src/lib/engine/wave3/__tests__/fold.test.ts, src/lib/engine/__tests__/aggregator.test.ts, pipeline.test.ts
  </read_first>
  <action>
    Write tests FIRST (RED), then implement Task 2's selections. (1) FOLD RETRY (F18) — wrap the single fold attempt in a bounded retry (ONE re-attempt on parse/timeout/transient failure) using a short inner budget (8s→16s style) that stays well under PER_CALL_TIMEOUT_MS; NEVER raise the 90s abort (PITFALL 2 — lower a budget if headroom needed). (2) SALVAGE (F20) — change the segment-count guard so mismatched personas are DROPPED individually and the valid ones are kept; set fold_success when enough valid personas remain (define the floor in the prompt-discipline spirit — keep diversity meaningful); warn listing the dropped archetypes. (3) DIVERSITY NUDGE (F19, if in scope) — below DIVERSITY_FLOOR, do one retry-nudge re-call before accepting; keep warn-only as the final fallback. (4) partial_analysis (if Task 2=B) — add the field to PredictionResult (types.ts) + set it in aggregator when exactly one core signal is dead (derive from `availability`); surface per the recorded direction (e.g. a board-visible warning that is NOT filtered, or the flag the board reads). (5) F7 (if Task 1 = RACES) — move the rehost delete out of the inline fire-and-forget into a TRUE post-pipeline finally that runs AFTER Omni+fold+Apollo resolve; PRESERVE the Apify signed-URL flow + the unconditional cleanup semantics (T-04-01); keep the `.catch` non-fatal behavior. If Task 1 = SAFE, update the misleading "finally-equivalent" comment to record the verification and skip the move. No fenced code — read files for current structure.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/wave3/__tests__/fold.test.ts src/lib/engine/__tests__/pipeline.test.ts src/lib/engine/__tests__/aggregator.test.ts 2>&1 | grep -v '^#' | tail -25; npm test 2>&1 | grep -v '^#' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - F18: a test proves one bounded retry fires before fold falls back; PER_CALL_TIMEOUT_MS (90s) unchanged (grep confirms no raise)
    - F20: a test proves valid personas are salvaged when one persona's segment count mismatches (not all-or-nothing)
    - F19 (if in scope): a test proves a sub-floor diversity triggers one retry-nudge
    - partial_analysis (if Task 2=B): a test proves single-dead-signal sets it true + dual still sets analysis_unavailable
    - F7: if RACES, a test/assertion proves the delete runs after all 3 sighted calls; the Apify signed-URL flow + unconditional cleanup are unchanged (T-04-01 intact). If SAFE, the comment is corrected with the verification note
    - `npx vitest run fold.test.ts pipeline.test.ts aggregator.test.ts` green AND full `npm test` engine suite green
  </acceptance_criteria>
  <done>Fold retries/salvages/nudges; partial_analysis per Davide; F7 fixed-or-cleared with T-04-01 preserved; suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: APPLY prune + LOCK honesty + latency + version (per Task 2)</name>
  <behavior>
    - Test 1 (ENG-04 LOCK, always): computeEngagementRange returns null when follower_count<=0 and a bounded lo<hi range otherwise; no Math.sin path exists.
    - Test 2 (ENG-04 LOCK, always): analysis_unavailable is true iff BOTH core signals dead, false when ≥1 live.
    - Test 3 (prune, per approved list): the dropped dead-tail fields no longer appear on the video-mode PredictionResult (or no longer carry the fake constant); back-compat/text-mode + persisted-column reads still pass.
    - Test 4 (version): ENGINE_VERSION reads the bumped value; version.test.ts updated.
  </behavior>
  <read_first>
    - src/lib/engine/aggregator.ts:137-147 (dead applyCtaPenalty), :182-233 (engagement range — LOCK), :860 (analysis_unavailable), :1074-1185 (the result assembly with rule_score/trend_score/ml_score/reasoning/score_weights/platform_fit)
    - src/lib/engine/omni-analysis.ts (F12 confidence:1.0 hardcode ~260-265)
    - src/app/api/analyze/route.ts:652-743 (buildInsertRow — what each pruned field persists to; keep DB columns, stop fake values where column-backed)
    - src/lib/engine/types.ts:273-385 (PredictionResult — remove/keep fields per approved list)
    - src/lib/engine/version.ts (CURRENT value after 01-04 = read it; bump) + __tests__/version.test.ts:9-10
    - src/lib/engine/__tests__/aggregator.test.ts (honesty + assembly coverage), scripts/measure-pipeline.ts (re-run for any reclaim)
    - Task 1 PRUNE SAFETY LIST + Task 2 approved prune list
  </read_first>
  <action>
    ALWAYS write the ENG-04 honesty LOCK tests FIRST (Tests 1+2) so a future regression that re-introduces fabrication or breaks the dual-failure flag fails CI (confirm GREEN against the unchanged honesty code). Then apply ONLY the Task-2-approved prune, field by field: for each field marked DROP, remove it from PredictionResult (types.ts) + stop emitting it in aggregateScores; for KEEP-COLUMN fields (rule_score/trend_score/ml_score/score_weights backed by buildInsertRow), stop emitting the fake constant where safe but preserve the DB column + back-compat read so old permalinks still deserialize; delete the dead `applyCtaPenalty` (confirmed never called); fix the F12 confidence:1.0 hardcode in omni-analysis (drop or mark dead per the list). Cross-check EACH removal against buildInsertRow (route.ts:652-743) so a pruned field doesn't break the insert. For LATENCY (if Task 2 = reclaim-now): apply the agreed budget lever (e.g. a thinking_budget/max_tokens trim — NEVER raise the fold 90s ceiling) and re-run measure-pipeline.ts to confirm under the 300s cap; if accept-under-cap, record the number. Finally read the CURRENT ENGINE_VERSION (post-01-04, ~3.18.0) AND assert version.test.ts already reflects that 01-04 value BEFORE bumping (a still-3.17.0 value means 01-04 was skipped/failed → STOP, don't bump on a broken chain); then bump one minor → ~3.19.0 with a change-history comment (partial_analysis + prune = output-shape change), update version.test.ts:9-10. NO determinism gate (D-04). PRESERVE the Apify signed-URL flow + all Zod boundaries. No fenced code.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/aggregator.test.ts src/lib/engine/__tests__/version.test.ts 2>&1 | grep -v '^#' | tail -20; npm test 2>&1 | grep -v '^#' | tail -8; npx tsx scripts/measure-pipeline.ts "$HOME/Downloads/TikTok Video Downloader.mp4" 2>&1 | grep -iE 'TOTAL_LATENCY_MS|FAIL' | head</automated>
  </verify>
  <acceptance_criteria>
    - ENG-04 honesty LOCK tests (Tests 1+2) exist and pass — engagement-range null-when-no-baseline + analysis_unavailable dual-only are regression-guarded
    - The prune matches Task 2's approved list EXACTLY (drop / keep-column / keep), each removal cross-checked against buildInsertRow — full `npm test` green confirms no insert/consumer break
    - dead applyCtaPenalty deleted; F12 confidence:1.0 hardcode dropped/marked
    - If reclaim-now: measure-pipeline re-run shows TOTAL under the 300s cap; fold 90s ceiling NOT raised (grep confirms)
    - ENGINE_VERSION: version.test.ts confirmed at the 01-04 value (~3.18.0) BEFORE the bump (broken-chain guard), THEN bumped to ~3.19.0 AND version.test.ts updated (both, or neither)
    - `npx vitest run aggregator.test.ts version.test.ts` green AND full `npm test` engine suite green (phase gate)
    - [ ] HARD (required, not optional): the live phase-gate rig ran on a real video — `smoke-tiktok-pipeline.ts` + `measure-pipeline.ts` on `$HOME/Downloads/TikTok Video Downloader.mp4` — captured with no thrown frames + TOTAL under the 300s cap. This is the VALIDATION.md "≥1 live rig run before phase close" gate; it does NOT pass on unit-green alone.
    - [ ] HARD (required, Davide-authenticated): the F42 permalink-reload UAT is done — /analyze is login-gated, so Davide drives a live run, then reloads the shared/saved permalink and CONFIRMS the 01-04 hero block + engagement range survive reload (shared board shows what the live run showed). This satisfies 01-04 must_haves' F42 truth ("hero block + engagement range survive permalink reload") — it is owned HERE so it is not lost between the two plans. Must be completed before phase close; cannot be auto-run (auth gate).
  </acceptance_criteria>
  <done>Dead tail pruned per Davide; ENG-04 honesty locked by test; latency reclaimed-or-accepted; version bumped; full suite green; E2E clean under cap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/Apify → engine read path | tiktok_url download uses the Apify token; must never leak into the URL fed to DashScope; the rehost temp object must be cleaned up |
| 3 LLMs → aggregator | Untrusted call outputs blend into score + honesty flags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Apify token → DashScope (pipeline.ts:400-496 server-side download → short-TTL signed URL → derive-and-drop cleanup) | mitigate (PRESERVE) | The F7 fix MUST preserve the signed-URL rehost + UNCONDITIONAL cleanup — moving the delete to a finally must keep it running on every path (success AND failure), never skip it. The fold-retry must NOT touch or bypass the signed-URL flow. Flag if any edit nears it. |
| T-04-02 | Tampering / DoS | malformed LLM JSON → bounded retry + circuit breaker + graceful null fallback (no thrown frames) | mitigate (PRESERVE+EXTEND) | The new fold retry stays within a bounded envelope (ONE re-attempt, short inner budget, no unbounded loop, no 90s ceiling raise — PITFALL 2). Salvage drops bad personas without throwing. |
| T-04-03 | Integrity (honesty) | fabricated metrics shown as real (Math.sin deleted; engagement null-when-no-baseline; analysis_unavailable; pruned dead constants) | mitigate (LOCK) | LOCK engagement-null + dual-failure flag with tests so a future regression fails CI; prune removes dead numbers that leak to the UI as meaningful; partial_analysis surfaces single-signal death. |
| T-04-04 | Availability (silent degrade) | single fold death silently halves the score basis (board filters the warning) | mitigate (FIX) | Fold retry/salvage reduce the death rate at the source; partial_analysis (if chosen) surfaces it instead of staying silent. |
</threat_model>

<verification>
- `npx vitest run fold.test.ts pipeline.test.ts aggregator.test.ts version.test.ts` green; full `npm test` engine suite green (phase gate per VALIDATION.md).
- REQUIRED (Task 4 hard checkbox): at least one live E2E rig run on a real video (smoke + measure-pipeline) — the VALIDATION.md phase-gate live requirement. Unit-green alone does NOT satisfy it.
- REQUIRED (Task 4 hard checkbox, Davide-authenticated): the F42 permalink-reload UAT — /analyze is login-gated, so Davide drives the live run + reloads the permalink and confirms the 01-04 hero block + engagement range survive. Owned here (cross-refs 01-04's F42 must_have) so it is not deferred indefinitely between the two plans.
- Per D-02 this is the batched-verification point for the WHOLE phase — beyond the two required checkboxes, offer Davide the full menu (Claude UATs via Playwright, scripts). Closes ENG-01/03/04 for phase 01; ENG-06 (D-12 chunk B) stays its own open plan (see <scope_note>).
</verification>

<success_criteria>
- ROADMAP criteria 1, 3, 4 advanced: E2E correctness (no thrown frames / honest fallbacks, no silent single-signal halve), honest grounded engagement range + dual-failure flag LOCKED, latency under the 300s cap toward <90s where free.
- Fold is robust (retry/salvage/nudge — never silently drops the audience half); single-signal death surfaced (partial_analysis) per Davide; dead tail pruned (F43); F7 race fixed-or-cleared with T-04-01 preserved; ENG-04 honesty locked by test (D-15/D-04 — no determinism gate); latency reclaimed-or-accepted (D-08/D-09).
</success_criteria>

<output>
Create `.planning/phases/01-engine-pipeline/01-05-SUMMARY.md` when done. Record: the F7 verdict + fix (or clear), the fold retry/salvage/nudge shape, Davide's single-signal + prune + latency decisions, the pruned field list, the locked-honesty tests, any reclaim, and the final ENGINE_VERSION. This SUMMARY closes phase 01's ENG-01/03/04/06 engine threads.
</output>
