---
phase: 01-engine-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/lib/engine/deepseek.ts
  - src/lib/engine/apollo-core.ts
  - src/lib/engine/wave3/fold-prompts.ts
  - src/lib/engine/qwen/omni-analysis.ts
  - src/lib/engine/types.ts
  - src/lib/engine/version.ts
  - src/lib/engine/__tests__/version.test.ts
  - src/lib/engine/__tests__/deepseek.test.ts
autonomous: false
requirements: [ENG-06]
must_haves:
  truths:
    - "All 3 calls' prompt I/O (Read, Fold, Apollo) have been walked step-by-step WITH Davide and his direction captured per call (D-12) — not auto-rewritten"
    - "Each T3.x trim has been audited against its version.ts rationale and a restore-vs-keep decision recorded per item (D-13)"
    - "A consumed-vs-dead output-field map exists and any Zod tightening / dead-field prune follows it (D-14) — no blind tightening"
  artifacts:
    - path: "src/lib/engine/deepseek.ts"
      provides: "Apollo prompt builder (buildDeepSeekUserMessage) + output contract — the moat prompt"
    - path: "src/lib/engine/wave3/fold-prompts.ts"
      provides: "Fold STABLE_FOLD_SYSTEM_PROMPT + buildFoldUserContent — the audience-sim prompt"
    - path: "src/lib/engine/qwen/omni-analysis.ts"
      provides: "Read buildSystemPrompt + buildUserHints — the sensor prompt"
  key_links:
    - from: "src/lib/engine/deepseek.ts"
      to: "src/lib/engine/types.ts"
      via: "Apollo output schema fields → consumed-vs-dead map → Zod tighten only what the map marks dead/required"
      pattern: "ApolloDimensionSchema|behavioral_predictions|component_scores"
---

<objective>
ENG-06 — review all 3 Qwen calls' prompt inputs/outputs STEP-BY-STEP WITH Davide (D-12 co-review, NOT delegated auto-rewrite), audit each T3.x prompt trim against its original rationale and decide restore-vs-keep per item (D-13), and MAP consumed-vs-dead output fields before any Zod tighten / dead-field prune (D-14).

This is the deepest co-review plan and the moat (Apollo gets the most attention). It is structured as: present each call's prompt + I/O, capture Davide's direction, only then apply agreed edits. EVERY edit task here is gated behind a co-review checkpoint. NO prompt is rewritten without Davide's per-call direction.

Depends on plans 01 (Apollo grounding may have changed the §-cite contract / KNOWLEDGE_CORE) and 02 (read model may have flipped, read prompt envelope may have a new retry) — those decisions ripple into this prompt walkthrough.

Purpose: ground-up understanding of Qwen I/O (D-03) is the phase goal; this is where it happens with Davide in the loop.
Output: captured per-call direction, per-trim restore/keep decisions, a consumed-vs-dead field map, and the agreed prompt/schema edits with version bumped iff output shape changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-engine-pipeline/01-CONTEXT.md
@.planning/phases/01-engine-pipeline/01-RESEARCH.md
@.planning/phases/01-engine-pipeline/01-01-SUMMARY.md
@.planning/phases/01-engine-pipeline/01-02-SUMMARY.md

<interfaces>
<!-- From RESEARCH.md §ENG-06 — the 3 prompt builders + the T3.x trim table + the consumed-vs-dead map. Confirmed as-built. -->
- 3 prompt builders:
  - Read: buildSystemPrompt() + buildUserHints() (omni-analysis.ts:63-178) — full JSON schema spec; T3.4 moved niche hints SYSTEM->USER (byte-stable prefix).
  - Fold: STABLE_FOLD_SYSTEM_PROMPT (10 archetype defs + Critical Divergence Requirement) + buildFoldUserContent() (fold-prompts.ts:34-206).
  - Apollo: APOLLO_SYSTEM_PROMPT = KNOWLEDGE_CORE(lean) + APOLLO_INSTRUCTION + buildDeepSeekUserMessage() (deepseek.ts:244-356, apollo-core.ts).
- T3.x trims (audit restore-vs-keep, D-13) — each with version.ts rationale:
  - T3.1: §2.6/§7/§8 + header meta dropped from KNOWLEDGE_CORE lean (ties to plan 01 ENG-02 decision — reconcile).
  - T3.2: "## Rule Matches" + "## Trend Context" dropped from Apollo user msg (deepseek.ts:285-290) — clean dead-text, keep defensible.
  - T3.3: behavioral_predictions gated to text mode (deepseek.ts:303-316) — on video fold owns it; VERIFY fold always supplies them (ENG-01 fold-failure path drops them); schema made .optional() (types.ts:822).
  - T3.4: niche/content-type hints SYSTEM->USER — keep (cache win).
  - fold reason drop (fold-prompts.ts:208-217) — dead output, keep.
  - budget trims: Apollo 3000->1500, fold 8000->4000 max_tokens — keep, re-validate if input size shifted (plan 02 flip).
- Consumed-vs-dead Apollo output map (D-14, map BEFORE tightening): dimensions/rewrites/ceiling_capper/confidence_scope/platform_note/suggestions/warnings/confidence = LIVE; composite_score LIVE but LLM's own value discarded (rubric-sum overwrites); component_scores partially live (blend term + feature_vector); behavioral_predictions DEAD on video / live on text. Fold output: all consumed. Read output: rich, many feed persisted FeatureVector; rule_score:50 / trend_score:0 are hardcoded dead constants (aggregator.ts:1101-1102).
- PITFALL 1: emotion_arc/hook_verbatim ride casts (omni-analysis.ts:289-296) — preserve on any schema tighten.
- Cache invariant: any output-shape change OR cached-system-prefix byte change -> bump version.ts + version.test.ts. NOTE plans 01/02 may have already bumped — read version.ts for current value before bumping again.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AUDIT — assemble the 3-call prompt I/O dossier + T3.x trim table + consumed-vs-dead map</name>
  <read_first>
    - src/lib/engine/qwen/omni-analysis.ts:63-178 (Read prompt builders)
    - src/lib/engine/wave3/fold-prompts.ts:34-206 (Fold prompt builders) and :208-217 (the dropped reason field)
    - src/lib/engine/deepseek.ts:244-356 (Apollo prompt + output contract) and :285-316 (T3.2/T3.3 trims)
    - src/lib/engine/apollo-core.ts (KNOWLEDGE_CORE lean + APOLLO_INSTRUCTION)
    - src/lib/engine/types.ts (Apollo/fold/read output schemas — for the consumed-vs-dead map; :815-822 behavioral_predictions optionality)
    - src/lib/engine/aggregator.ts:763-774, :930, :1098-1102 (where each Apollo field is consumed or dead-constant'd)
    - src/lib/engine/version.ts (the T3.x change-history rationale verbatim) + CURRENT ENGINE_VERSION value
    - .planning/phases/01-engine-pipeline/01-RESEARCH.md §ENG-06 (the full trim table + field map — the starting point)
    - .planning/phases/01-engine-pipeline/01-01-SUMMARY.md and 01-02-SUMMARY.md (what plans 01/02 already changed — reconcile T3.1 and the read envelope)
  </read_first>
  <action>
    Produce a single review dossier (no edits, D-01) with three artifacts. (1) For each of the 3 calls: the cached system prefix summary + the volatile user-message shape + a real sample I/O captured from a rig run (reuse the /tmp outputs from plans 01/02 where available, or run apollo-core-smoke once). Present per call. (2) The T3.x trim table: each trim, what was dropped, its version.ts rationale verbatim, and a restore-vs-keep recommendation WITH evidence — reconciling T3.1 against plan 01's ENG-02 grounding decision (read 01-01-SUMMARY). (3) The consumed-vs-dead output-field map for all 3 calls (extend the RESEARCH starting map; confirm rule_score:50 / trend_score:0 dead constants; confirm behavioral_predictions dead-on-video by tracing the fold-supplies-them path including the fold-failure case). This dossier is the input to the co-review checkpoints.
  </action>
  <verify>
    <automated>npx tsx scripts/apollo-core-smoke.ts "$HOME/Downloads/TikTok Video Downloader.mp4" 2>&1 | tee /tmp/eng06-sample-io.txt | head -40; grep -n "rule_score: 50\|trend_score: 0\|behavioral_predictions" src/lib/engine/aggregator.ts src/lib/engine/types.ts | grep -v '^#' | head</automated>
  </verify>
  <acceptance_criteria>
    - A dossier exists with: per-call (Read/Fold/Apollo) prompt summary + a real sample I/O; the full T3.x trim table with verbatim version.ts rationale + per-item restore/keep recommendation; the consumed-vs-dead field map for all 3 calls
    - rule_score:50 and trend_score:0 confirmed as dead hardcoded constants (grep located them)
    - behavioral_predictions confirmed dead-on-video, including what happens on the fold-failure path
    - T3.1's restore/keep recommendation is reconciled with plan 01's ENG-02 decision (cited from 01-01-SUMMARY)
  </acceptance_criteria>
  <done>Three-call prompt I/O dossier + T3.x trim table + consumed-vs-dead field map produced; no edits made.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CO-REVIEW CHECKPOINT — walk all 3 prompts step-by-step + decide trims + confirm dead fields (D-12/D-13/D-14)</name>
  <action>STOP and walk each of the 3 calls' prompt I/O with Davide step-by-step in pipeline order (Read, Fold, Apollo). Do NOT auto-rewrite. Capture per-call direction, per-trim restore/keep decisions, and the confirmed dead-field prune + tighten list before any edit (D-12/D-13/D-14).</action>
  <what-built>The Task 1 dossier: per-call prompt I/O, the T3.x trim table, and the consumed-vs-dead field map. Nothing edited yet.</what-built>
  <how-to-verify>
    Walk each call's prompt I/O WITH Davide, in pipeline order (Read, Fold, Apollo — deepest attention on Apollo, the moat), step by step (D-12). For each call capture his direction: keep / reword / restructure, and any token-efficiency note. Then for EACH T3.x trim, capture his restore-vs-keep decision (D-13). Then confirm the dead-field prune list and any Zod tightening from the consumed-vs-dead map (D-14) — do NOT tighten blind; only fields the map marks dead get pruned, only fields the map marks always-present get required. Record every per-call direction + per-trim decision + the confirmed prune/tighten list explicitly.
  </how-to-verify>
  <read_first>
    - /tmp/eng06-sample-io.txt and the Task 1 dossier
    - .planning/phases/01-engine-pipeline/01-RESEARCH.md §ENG-06
  </read_first>
  <acceptance_criteria>
    - Per-call direction captured for all 3 calls (Read, Fold, Apollo) in the SUMMARY (D-12)
    - A restore-vs-keep decision recorded for EACH T3.x trim item (T3.1, T3.2, T3.3, T3.4, fold-reason, budget trims) (D-13)
    - The confirmed dead-field prune list + the required/tighten list captured, each traced to the consumed-vs-dead map (D-14) — no field tightened without a map entry
    - The captured directions are unambiguous enough that Task 3 applies them without further interpretation
  </acceptance_criteria>
  <resume-signal>Type "approved" with the per-call directions, per-trim decisions, and the confirmed prune/tighten list (or describe adjustments)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: APPLY (conditional on Task 2) — agreed prompt edits + dead-field prune + Zod tighten + version bump</name>
  <read_first>
    - The Task 2 captured directions (in this plan's working notes / SUMMARY draft)
    - src/lib/engine/deepseek.ts:244-356 (Apollo prompt + contract), src/lib/engine/apollo-core.ts (KNOWLEDGE_CORE + instruction)
    - src/lib/engine/wave3/fold-prompts.ts:34-217 (Fold prompt + dropped reason)
    - src/lib/engine/qwen/omni-analysis.ts:63-178, :289-296 (Read prompt + PITFALL 1 cast-threaded fields)
    - src/lib/engine/types.ts:815-822 (schemas to tighten/prune per the D-14 map)
    - src/lib/engine/version.ts (CURRENT value — read before bumping) + __tests__/version.test.ts:9-10
    - src/lib/engine/__tests__/deepseek.test.ts (Apollo contract tests — extend to cover any tightened/required field)
  </read_first>
  <action>
    Apply ONLY the per-call directions, per-trim decisions, and prune/tighten list captured in Task 2 — nothing beyond it. For each agreed prompt reword/restructure: edit the relevant builder (omni-analysis buildSystemPrompt/buildUserHints, fold-prompts, deepseek buildDeepSeekUserMessage / apollo-core APOLLO_INSTRUCTION). For each trim decided RESTORE: re-add it (reconcile T3.1 with plan 01's outcome — do NOT double-apply if plan 01 already restored). For each dead field decided PRUNE: remove it from the schema + builder; for each field decided REQUIRE: tighten the Zod from .optional() to required ONLY where the map marks it always-present, preserving the PITFALL 1 cast-threaded emotion_arc/hook_verbatim. If any edit changes output shape OR the cached system-prefix bytes: read the current ENGINE_VERSION in version.ts, bump it one minor (e.g. 3.14.0->3.15.0 — account for plans 01/02 bumps), add a change-history comment, and update version.test.ts:10 to match. Extend deepseek.test.ts to assert any newly-required/pruned field. PRESERVE every Zod safeParse + post-parse clamp (V5 boundary) and the Apify signed-URL flow. No fenced code in this action — read the files for current structure.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/deepseek.test.ts src/lib/engine/__tests__/version.test.ts 2>&1 | grep -v '^#' | tail -20; npm test 2>&1 | grep -v '^#' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Every applied edit maps to a Task 2 captured decision; no prompt reword or schema change without a recorded direction
    - Pruned fields are gone from BOTH schema and builder; required fields are tightened ONLY where the map marks always-present; PITFALL 1 cast-threaded fields intact
    - T3.1 restore (if chosen) is reconciled with plan 01 — not double-applied
    - If output shape OR cached-prefix bytes changed: ENGINE_VERSION bumped from its CURRENT value AND version.test.ts updated to match (both, or neither)
    - `npx vitest run deepseek.test.ts version.test.ts` green AND full `npm test` engine suite green (per-wave-merge sampling)
    - All Zod safeParse boundaries + Apify signed-URL flow unchanged (no V5 regression)
  </acceptance_criteria>
  <done>Agreed prompt/schema edits shipped per the captured co-review directions; dead fields pruned + tightening applied per the D-14 map; version handled; full engine suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| All 3 LLMs -> engine | Untrusted Read/Fold/Apollo JSON crosses Zod boundaries; this plan edits prompts + tightens schemas at those boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Zod safeParse + post-parse clamps at all 3 boundaries (schemas.ts, fold-prompts.ts:245, deepseek.ts:128-190) | mitigate (PRESERVE) | Schema tightening (D-14) only narrows accepted shape; safeParse + clamp + graceful fallback must remain — tighten cannot turn a malformed response into a thrown frame |
| T-03-02 | Tampering | PITFALL 1 cast-threaded emotion_arc/hook_verbatim (omni-analysis.ts:289-296) | mitigate | Do NOT drop these fields during a schema cleanup — re-introduces the historical drop bug |
| T-03-03 | Information Disclosure (integrity) | dead-field prune (D-14) | accept | Pruning dead output reduces surface; ensure no LIVE consumer (aggregator/board) reads a pruned field before removing it |
</threat_model>

<verification>
- `npx vitest run deepseek.test.ts version.test.ts` green; full `npm test` engine suite green at wave merge.
- One live sample I/O captured (Task 1).
- Per D-02, batch live verification — offer Davide the testing menu (he UATs / Claude Playwright /analyze / scripts) after this plan, do not pre-commit a method.
</verification>

<success_criteria>
- ROADMAP criterion 5 advanced: "Qwen prompt I/O is reviewed for quality and token efficiency".
- All 3 prompts walked step-by-step with Davide (D-12); every T3.x trim decided (D-13); dead-field map drove every prune/tighten (D-14).
- Cache invariant respected on any output-shape change.
</success_criteria>

<output>
Create `.planning/phases/01-engine-pipeline/01-03-SUMMARY.md` when done. Record: per-call directions, per-trim restore/keep decisions, the consumed-vs-dead map + the prune/tighten list applied, and the version handling.
</output>
