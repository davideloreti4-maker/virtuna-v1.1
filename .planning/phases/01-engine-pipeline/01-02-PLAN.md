---
phase: 01-engine-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/engine/qwen/omni-analysis.ts
  - src/lib/engine/qwen/schemas.ts
  - src/lib/engine/qwen/client.ts
  - src/lib/engine/version.ts
  - src/lib/engine/__tests__/version.test.ts
  - src/lib/engine/__tests__/omni-analysis-critical-field-retry.test.ts
  - src/lib/engine/__tests__/omni-analysis-nospeech.test.ts
autonomous: false
requirements: [ENG-05]
must_haves:
  truths:
    - "A NO-SPEECH video (b-roll/music-only/ASMR) reads successfully — speech-derived fields (first_words_speech_score, audio_hook_quality) are legitimately null and do NOT fail the whole Zod parse (F46)"
    - "A long/dense video's read output is bounded (OMNI_MAX_TOKENS set) so it does not truncate mid-JSON → SyntaxError → silent read failure (F47)"
    - "A drifted/empty critical read field (emotion_arc empty, hook_verbatim null on a speech video, weakest_modality underivable) triggers a bounded retry and a read_drift log entry — not a silent drop-to-undefined (F9/D-11)"
    - "A terse legitimate audio_description ('music') no longer fails the parse on the min-length edge (F16)"
  artifacts:
    - path: "src/lib/engine/qwen/schemas.ts"
      provides: "OmniAnalysisZodSchema + HookDecompositionZodSchema — the boundary F46 (nullable speech fields) + F16 (loosen audio_description min) edit; existing weakest_modality/emotion_arc.label drift guards extended"
    - path: "src/lib/engine/qwen/omni-analysis.ts"
      provides: "Read call + MAX_RETRIES=1 + OMNI_MAX_TOKENS gate — where F47 (set the cap) + F9 (critical-field bounded retry + drift log) insert"
  key_links:
    - from: "src/lib/engine/qwen/omni-analysis.ts"
      to: "src/lib/engine/qwen/schemas.ts"
      via: "OmniAnalysisZodSchema safeParse → critical-field emptiness check → bounded retry preserving cached system prefix"
      pattern: "MAX_RETRIES|safeParse|emotion_arc|hook_verbatim|OMNI_MAX_TOKENS"
---

<objective>
ENG-05 — harden the Read (Omni) substrate, the perception layer the whole engine reasons over. Three classes of REAL read failure observed on the Ashton Hall 79s no-speech clip + downstream drift:
  - F46 (HIGH): speech-derived fields null on NO-SPEECH videos → required-NUMBER Zod field rejects the WHOLE read (silent total failure on b-roll/music-only/ASMR).
  - F47 (MEDIUM/HIGH): OMNI_MAX_TOKENS unset (=0) → DashScope's low default output limit truncates a long video's larger output mid-JSON → SyntaxError → silent read failure.
  - F9/D-11 (MED): retry fires only on whole-response JSON/Zod fail, NOT on a parsed-but-EMPTY critical field; no drift telemetry.
  - F16 (LOW): audio_description min(10) rejects a terse legit audio ("music") → whole parse fails.

D-10 (flash-vs-plus read model A/B) is CLOSED — keep flash (richer + faster substrate, A/B-validated; QWEN_OMNI_MODEL=qwen3.5-omni-flash). Models are LOCKED this milestone. No model A/B in this plan.

D-R1 (Read → pure sensor, drop factors/overall_impression/content_summary/gemini_score) is OUT OF THIS PLAN's safe-fix set — it is MUST-COORDINATE (dropping the fields breaks the Read's own Zod parse, aggregator gemini_score, Apollo formatGeminiSignals, creator rules, decode, stage10, AND won't compile). It is handled as its own coordinated atomic change (Task 5, checkpoint-gated) spanning read+schema+aggregator+Apollo+decode — NOT smeared across 01-02/03/04 leaving the build red.

CO-REVIEW-GATED (D-00, milestone human-in-loop): the F46/F47/F9/F16 robustness strategy AND the D-R1 sequencing decision are surfaced to Davide BEFORE applying (Task 1 checkpoint).
Output: a hardened, no-speech-safe, truncation-safe, drift-telemetered Read on the locked flash model; D-R1 sequenced per Davide's call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-engine-pipeline/01-CONTEXT.md
@.planning/phases/01-engine-pipeline/01-QUALITY-OPTIMIZATION.md
@.planning/phases/01-engine-pipeline/01-SYNTHESIS.md
@.planning/phases/01-engine-pipeline/01-WALKTHROUGH-LOG.md

<interfaces>
<!-- Confirmed as-built this session. Do NOT re-derive. -->
- Read model default: qwen3.5-omni-flash (qwen/client.ts:36, env QWEN_OMNI_MODEL). LOCKED — no A/B this plan. Rollback lever: QWEN_OMNI_MODEL=qwen3.5-omni-plus.
- OMNI_MAX_TOKENS (omni-analysis.ts:30): `Number(process.env.OMNI_MAX_TOKENS) || 0`; applied at :228 only when truthy. Currently UNSET → uncapped → DashScope low default truncates long output (F47).
- F46 failing field: hook_decomposition.first_words_speech_score is a REQUIRED ScoreSchema (schemas.ts:35) — null on a no-speech video fails the whole parse. audio_hook_quality (schemas.ts:34) same risk on no-audio. Pattern to mirror: GeminiAudioSignalsSchema voice_clarity_0_10 / audio_hook_first_2s_0_10 are already `.nullable()` per D-A2 (schemas.ts:145-146).
- weakest_modality derive (schemas.ts:45-51): reduces over the 4 hook scores via `h[k] < h[min]`. If any of the 4 becomes nullable, the reduce MUST skip nulls (else NaN/crash).
- F16: audio_description `z.string().min(10).max(280)` (schemas.ts:150) — loosen the min.
- Existing drift guards (extend, do not replace): weakest_modality derive (schemas.ts:31-51), emotion_arc.label synonym-normalize (schemas.ts:81-95), hook_verbatim nullable (schemas.ts:202-205).
- F9 GAP: Read has MAX_RETRIES=1 (omni-analysis.ts:25) firing ONLY on whole-response Zod/JSON fail (:240-244), NOT on parsed-OK-but-empty emotion_arc / null hook_verbatim on a speech video. No drift telemetry.
- Retry-nudge pattern to mirror: deepseek.ts:409-415 (re-send user message, preserve cached system prefix).
- PITFALL 1: emotion_arc + hook_verbatim ride `as GeminiVideoAnalysis` casts (omni-analysis.ts:289-296) — do NOT drop on any cleanup.
- PITFALL 2: do NOT raise TIMEOUT_MS to fix headroom; bound output instead (F47 is exactly this).
- Cache invariant: F46/F16/F9 do NOT change output SHAPE (only widen acceptance / add resilience) → NO version bump. F47 changes the substrate on long videos (formerly-truncated reads now complete) → arguably a substrate shift; DECIDE bump at Task 1. D-R1 (Task 5) DOES change shape → bump.
- D-R1 downstream consumers (MUST-COORDINATE, mapped 2026-06-11): factors → aggregator.ts:372/757/784-785/920, deepseek.ts:286-297, creator-rulebook.ts:300/310, wave3/persona-prompts.ts:132-135, stage10-critique.ts:69, remix/decode.ts:195/199 (+ decode-types.ts:77-82 REQUIRED → compile error). overall_impression/content_summary → deepseek.ts:296-297, decode.ts/decode-prompts.ts, aggregator variants.craft. gemini_score computed aggregator.ts:783-786, consumed aggregator:1105, stage10:63, route.ts:688, eval-runner.ts:230. Zod required at OmniAnalysisZodSchema (schemas.ts:169-171) + GeminiResponseSchema (types.ts:610-612).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: CO-REVIEW CHECKPOINT — approve Read-robustness strategy (F47/F46/F9/F16) + D-R1 sequencing (D-00)</name>
  <action>STOP. Present the concrete robustness strategy + the D-R1 must-coordinate finding to Davide. Do NOT auto-decide. Capture answers BEFORE any edit.</action>
  <decision>
    (a) Approve the F47 token cap value (proposed OMNI_MAX_TOKENS default = 8000) + whether F47 warrants a version bump (formerly-truncated long reads now complete = substrate shift on long videos).
    (b) Approve the F46 nullable strategy (make first_words_speech_score + audio_hook_quality nullable; teach weakest_modality derive to skip nulls) — mirrors D-A2 audio nulling.
    (c) Approve the F9 critical-field retry + read_drift log strategy (which fields trigger, retry count within MAX_RETRIES, log shape).
    (d) Approve F16 loosen audio_description min(10)→min(1).
    (e) D-R1 SEQUENCING — the fork (see options).
  </decision>
  <options>
    <option id="dr1-defer-coordinated">
      <name>A (RECOMMENDED) — ship F47/F46/F9/F16 in this plan (green); do D-R1 as its own coordinated atomic commit</name>
      <pros>Every commit compiles + tests green. The 4 fixes are the REAL read-failure fixes (no-speech crash, long-video truncation) — high leverage, zero downstream risk. D-R1 lands as one honest atomic change across read+schema+aggregator+Apollo+decode (its true dependency shape), not smeared red across 3 plans.</pros>
      <cons>D-R1 read-shape isn't "locked" before 01-03 starts — 01-03 Apollo rebuild must coordinate with the D-R1 commit (acceptable; they're the same atomic change).</cons>
    </option>
    <option id="dr1-readside-now">
      <name>B — drop D-R1 Read-side now in 01-02 as the original split intended</name>
      <pros>Matches the literal 01-02/03/04 split in the synthesis doc; Read shape locked first.</pros>
      <cons>FATAL: dropping factors/overall_impression/content_summary fails the Read's OWN Zod parse + crashes aggregator + won't compile (decode-types) until 01-03+01-04 land. Build + tests RED across plans. Violates accuracy-over-speed.</cons>
    </option>
    <option id="dr1-full-now">
      <name>C — do the full D-R1 (read+aggregator+Apollo+decode) inside this 01-02 execution now</name>
      <pros>D-R1 fully done in one go; build stays green.</pros>
      <cons>Pulls 01-03 (Apollo) + 01-04 (aggregator/gemini_score) work forward into 01-02 — large, contradicts wave order, couples the moat-rebuild with substrate fixes. Higher blast radius in one plan.</cons>
    </option>
  </options>
  <context>
    D-R1 open sub-decisions (decide here if A/C): (1) hook_decomposition 0-10 modality scores — keep (leaning keep: perception, not judgment) vs downgrade to presence; (2) audio_perceptual_score — keep (TS-derived perception); (3) perception skeleton Apollo's user message receives once factor rationales die (→ 01-03 detail). KEEP per D-R1: segments, hook_verbatim, emotion_arc, audio_signals, content_type, niche, video_signals. DROP: factors[].score/rationale, improvement_tip, overall_impression, content_summary → gemini_score.
  </context>
  <acceptance_criteria>
    - F47 token value + bump decision captured
    - F46 / F9 / F16 strategies approved (or adjusted) and recorded BEFORE Task 2-4 implement them
    - D-R1 sequencing choice (A/B/C) captured; if A, D-R1 (Task 5) is split out to its own coordinated change and this plan ships Tasks 2-4 only
  </acceptance_criteria>
  <resume-signal>Approve/adjust the robustness strategy + select D-R1 sequencing A/B/C</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FIX — F46 nullable speech fields + F16 loosen audio_description (no-speech-safe read)</name>
  <behavior>
    - Test 1: a NO-SPEECH read (first_words_speech_score=null, audio_hook_quality=null, audio fields null per D-A2) parses successfully — no Zod rejection.
    - Test 2: weakest_modality derive skips null scores and picks the lowest NON-null modality (no NaN, no crash) when one of the 4 is null.
    - Test 3: a terse audio_description ("music", 5 chars) now parses (F16).
    - Test 4: a normal speech read (all 4 modality scores present) still derives weakest_modality unchanged (no regression).
  </behavior>
  <read_first>
    - src/lib/engine/qwen/schemas.ts:31-51 (HookDecompositionZodSchema + weakest_modality derive), :143-151 (GeminiAudioSignalsSchema — the D-A2 nullable pattern to mirror), :150 (audio_description min)
    - src/lib/engine/__tests__/ (existing schema test for the nearest analog)
  </read_first>
  <action>
    Make hook_decomposition.first_words_speech_score and audio_hook_quality `ScoreSchema.nullable()` (mirror voice_clarity_0_10's D-A2 nulling). Update the weakest_modality derive transform (schemas.ts:45-51) to reduce only over NON-null modality scores (skip nulls; if all relevant null, leave undefined → downstream null-safe). Loosen audio_description `min(10)` → `min(1)` (F16). Add the test file omni-analysis-nospeech.test.ts covering the 4 behaviors (RED→GREEN). Preserve all existing guards. No version bump (acceptance widened, shape unchanged).
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/omni-analysis-nospeech.test.ts src/lib/engine/__tests__/omni-analysis-emotion-arc.test.ts 2>&1 | grep -v '^#' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - No-speech read (null speech fields) parses; weakest_modality derives over non-null scores; terse audio_description accepted; speech read unchanged
    - Existing schema guard tests green
  </acceptance_criteria>
  <done>F46 + F16 shipped: no-speech videos read successfully; weakest_modality null-safe; terse audio accepted.</done>
</task>

<task type="auto">
  <name>Task 3: FIX — F47 set OMNI_MAX_TOKENS (long-video truncation guard)</name>
  <read_first>
    - src/lib/engine/qwen/omni-analysis.ts:26-30 (OMNI_MAX_TOKENS gate), :203-229 (the create call where max_tokens applies)
    - src/lib/engine/version.ts (if Task 1 = bump)
  </read_first>
  <action>
    Set the OMNI_MAX_TOKENS default to the value approved in Task 1 (proposed 8000): `Number(process.env.OMNI_MAX_TOKENS) || 8000`. Update the comment to reflect it now defaults to a sane cap (no longer "UNSET = uncapped baseline") — bounds long-video output so it can't truncate mid-JSON (F47). Do NOT raise TIMEOUT_MS (PITFALL 2). If Task 1 approved a version bump, bump version.ts + version.test.ts with a change-history note.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/version.test.ts 2>&1 | grep -v '^#' | tail -10; grep -n 'OMNI_MAX_TOKENS' src/lib/engine/qwen/omni-analysis.ts</automated>
  </verify>
  <acceptance_criteria>
    - OMNI_MAX_TOKENS default = approved value; env override still honored
    - TIMEOUT_MS unchanged (PITFALL 2)
    - Version handled per Task 1 decision
  </acceptance_criteria>
  <done>F47 shipped: read output bounded; long-video truncation guarded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: FIX — F9/D-11 critical-field bounded retry + read_drift logging</name>
  <behavior>
    - Test 1: read parses OK but emotion_arc empty on a video → ONE bounded retry (preserving cached system prefix) before accepting.
    - Test 2: after retry still empty → graceful drop-to-undefined (no thrown frame) AND a read_drift log entry recorded.
    - Test 3: a valid full response → no retry, no drift log.
    - Test 4: hook_verbatim null on a speech-bearing video → same retry-then-log path. (No-speech video → null is LEGIT, no retry — coordinate with F46.)
  </behavior>
  <read_first>
    - src/lib/engine/qwen/omni-analysis.ts:25-30, :192-330 (retry loop where the critical-field check inserts), :289-296 (cast-threaded fields — PITFALL 1)
    - src/lib/engine/deepseek.ts:409-415 (retry-nudge: re-send user message, keep cached system prefix)
    - src/lib/engine/__tests__/omni-analysis-emotion-arc.test.ts, omni-analysis-verbatim.test.ts (extend)
  </read_first>
  <action>
    Implement EXACTLY the F9 strategy approved in Task 1. Insert a post-parse critical-field emptiness check in the retry loop: when emotion_arc / hook_verbatim / weakest_modality are empty-or-underivable on content that SHOULD have them (gate hook_verbatim on speech-presence so F46 no-speech videos don't false-trigger), fire a bounded retry within the existing MAX_RETRIES envelope (re-send the volatile user message, keep the byte-stable cached system prefix — mirror deepseek.ts:409-415). On persistent emptiness after retry, keep the graceful drop-to-undefined AND emit a `read_drift` log entry naming the drifted field. Preserve cast-threaded fields at :289-296 (PITFALL 1). Do NOT raise TIMEOUT_MS (PITFALL 2). Create omni-analysis-critical-field-retry.test.ts (RED→GREEN). No version bump (resilience, not shape).
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/omni-analysis-critical-field-retry.test.ts src/lib/engine/__tests__/omni-analysis-emotion-arc.test.ts src/lib/engine/__tests__/omni-analysis-verbatim.test.ts 2>&1 | grep -v '^#' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 4 behaviors pass (retry-on-empty, retry-then-log, no-retry-on-success, hook_verbatim speech-gated path)
    - read_drift log entry asserted on a drifted field — no silent drop
    - bounded retry preserves cached system prefix; existing emotion_arc + verbatim guard tests green
    - PITFALL 1 fields intact; TIMEOUT_MS unchanged
  </acceptance_criteria>
  <done>F9/D-11 critical-field retry + drift telemetry shipped on flash; read tests green.</done>
</task>

<task type="manual" gate="conditional">
  <name>Task 5: D-R1 — Read pure-sensor (ONLY if Task 1 = option B or C; if A, this is split to its own coordinated change)</name>
  <action>
    CONDITIONAL on Task 1. If A (recommended): SKIP here — D-R1 is tracked as its own coordinated atomic commit (read prompt drop + OmniAnalysisZodSchema/GeminiResponseSchema optional + aggregator factors/gemini_score fallbacks + deepseek formatGeminiSignals + decode-types + version bump), scheduled at the 01-03 boundary. Record the hand-off in SUMMARY.
    If B or C: implement per the approved sub-decisions (keep hook modality scores + audio_perceptual_score; drop factors/overall_impression/content_summary/gemini_score), coordinating EVERY consumer in the interfaces map in one atomic commit, bump version, and re-run the full engine test suite.
  </action>
  <acceptance_criteria>
    - If A: D-R1 hand-off recorded in SUMMARY with the consumer map; this plan ships Tasks 2-4 only
    - If B/C: all D-R1 consumers updated atomically, build compiles, full engine suite green, version bumped
  </acceptance_criteria>
  <done>D-R1 sequenced per Task 1.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-01 | Tampering | schemas.ts Zod guards | mitigate (PRESERVE) | F46 nullable + F16 loosen WIDEN acceptance; existing .optional().catch(undefined) safety nets stay. Critical-field retry inserts AFTER safeParse; never removes the graceful fallback. |
| T-02-02 | Denial of Service | bounded retry + OMNI_MAX_TOKENS | mitigate | Retry stays within MAX_RETRIES; NO timeout raise (PITFALL 2). OMNI_MAX_TOKENS bounds output (cannot unbound the read). |
| T-02-03 | Information Disclosure | signed-URL rehost flow (pipeline.ts) | accept (untouched) | This plan edits omni-analysis/schemas/client/version only. |
</threat_model>

<verification>
- `npx vitest run` on the read test files + version test green.
- Per D-02, batch live verification with plan 01's fixes — offer Davide the testing menu (UAT on the no-speech test video to confirm F46/F47; Playwright /analyze; scripts), do not pre-commit. The Ashton Hall 79s no-speech clip is the F46/F47 repro.
</verification>

<success_criteria>
- ROADMAP criterion 5 advanced: omni-flash read is hardened — no-speech videos read (F46), long videos don't truncate (F47), drift telemetered with bounded retry (F9), terse audio accepted (F16).
- D-R1 sequencing decided + recorded (D-00); strategy surfaced before applying.
- Cache invariant respected on any bump.
</success_criteria>

<output>
Create `.planning/phases/01-engine-pipeline/01-02-SUMMARY.md` when done. Record: the approved robustness strategy, F47 token value + bump decision, the D-R1 sequencing choice + (if A) the D-R1 hand-off + consumer map, and any latency note for plan 04.
</output>
