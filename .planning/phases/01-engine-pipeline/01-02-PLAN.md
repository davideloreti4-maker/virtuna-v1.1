---
phase: 01-engine-pipeline
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - src/lib/engine/qwen/omni-analysis.ts
  - src/lib/engine/qwen/schemas.ts
  - src/lib/engine/qwen/client.ts
  - src/lib/engine/version.ts
  - src/lib/engine/__tests__/version.test.ts
  - src/lib/engine/__tests__/omni-analysis-critical-field-retry.test.ts
autonomous: false
requirements: [ENG-05]
must_haves:
  truths:
    - "Davide has seen a flash-vs-plus read-quality A/B on a real video (verbatim, emotion_arc, segments) and decided whether to keep flash or flip to plus (D-10)"
    - "A drifted/empty critical read field (emotion_arc.label, weakest_modality, hook_verbatim) triggers a bounded retry and a drift log entry — not a silent drop-to-undefined (D-11)"
  artifacts:
    - path: "src/lib/engine/qwen/schemas.ts"
      provides: "Existing drift guards (weakest_modality derive, emotion_arc.label synonym-normalize, hook_verbatim) — the boundary D-11 extends"
    - path: "src/lib/engine/qwen/omni-analysis.ts"
      provides: "Read call + MAX_RETRIES=1 whole-response retry — where the critical-field bounded retry + drift logging insert"
  key_links:
    - from: "src/lib/engine/qwen/omni-analysis.ts"
      to: "src/lib/engine/qwen/schemas.ts"
      via: "OmniAnalysisZodSchema safeParse → critical-field emptiness check → bounded retry preserving cached system prefix"
      pattern: "MAX_RETRIES|safeParse|emotion_arc|hook_verbatim"
---

<objective>
ENG-05 — re-audit the read model (D-10: flash was picked on a thin 2-video speed A/B; the latency budget now allows plus) and harden read drift at the model boundary (D-11: Zod guard — already partly present — PLUS bounded retry on empty critical fields PLUS drift logging).

AUDIT-FIRST and CO-REVIEW-GATED: run a flash-vs-plus A/B on a real video, present the read-quality delta (verbatim richness, emotion_arc point count + label validity, segment boundaries), STOP for Davide's keep-flash-or-flip decision, then add the D-11 critical-field retry + drift logging on whichever model wins. Per-field drift-guard strategy is Claude's Discretion within D-11 but MUST be surfaced to Davide before applying (D-00).

Purpose: the read is the substrate the whole engine reasons over (sense-complete v3.9.0) — its richness and stability cap everything downstream.
Output: a captured model decision + the D-11 critical-field retry/logging guards fitting the existing temp0+seed+cached-prefix envelope, version bumped iff the read-model default flips.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-engine-pipeline/01-CONTEXT.md
@.planning/phases/01-engine-pipeline/01-RESEARCH.md

<interfaces>
<!-- From RESEARCH.md §ENG-05 — confirmed as-built. Do NOT re-derive. -->
- Read model default: qwen3.5-omni-flash (qwen/client.ts:36, env QWEN_OMNI_MODEL); rollback lever to plus is QWEN_OMNI_MODEL=qwen3.5-omni-plus.
- Existing drift guards (the D-11 pattern to extend, schemas.ts):
  - weakest_modality (:31-51): .optional().catch(undefined) + transform DERIVING it from the 4 hook scores. Never nukes the call.
  - emotion_arc.label (:81-95): synonym-normalization preprocessor (medium→mid, Low→low) + .optional().catch(undefined). Was failing ~50% before the guard.
  - hook_verbatim (:202-205): .optional(), nullable per-field, max-280. Threaded via casts at omni-analysis.ts:289-296 ("do NOT remove" warning — re-introduces the drop).
  - segments (:213-222): inline parse-time shape MUST mirror exported SegmentSchema (PITFALL 4).
- The GAP (D-11): guards drop-to-undefined gracefully but there is NO bounded retry on a valid-but-EMPTY critical field, and NO drift telemetry. Read has MAX_RETRIES=1 (omni-analysis.ts:25) but it only fires on whole-response Zod/JSON failure, NOT on "parsed OK but emotion_arc empty / hook_verbatim null on a speech video".
- Retry-nudge pattern to mirror: deepseek.ts:409-415 (retry the user message, preserve the cached system prefix).
- PITFALL 1: emotion_arc/hook_verbatim ride `as GeminiVideoAnalysis` casts (omni-analysis.ts:289-296) — do NOT drop on cleanup.
- PITFALL 2: do NOT raise a timeout to fix headroom; lower a budget instead.
- Cache invariant: a read-model DEFAULT flip shifts the substrate → scores shift → bump version.ts + version.test.ts:9-10.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AUDIT — flash-vs-plus read-quality A/B on a real video</name>
  <read_first>
    - scripts/apollo-core-smoke.ts and scripts/measure-pipeline.ts (which one prints the read's sensor signals + emotion_arc point count)
    - src/lib/engine/qwen/client.ts:31-38 (the 2-video A/B note that picked flash; the env defaults)
    - src/lib/engine/qwen/omni-analysis.ts:63-178 (buildSystemPrompt / read output contract — what "rich" means: verbatim, emotion_arc, segments)
    - src/lib/engine/qwen/schemas.ts (the fields to compare across models)
    - .planning/phases/01-engine-pipeline/01-RESEARCH.md §ENG-05
  </read_first>
  <action>
    Run the read on the SAME real video twice: once with default flash, once with QWEN_OMNI_MODEL=qwen3.5-omni-plus (env-flip A/B). Use `QWEN_OMNI_MODEL=qwen3.5-omni-plus npx tsx scripts/apollo-core-smoke.ts "$HOME/Downloads/TikTok Video Downloader.mp4"` vs the unset default. Capture for each: hook_verbatim spoken_words/on_screen_text presence + length, per-segment spoken_text fill rate, emotion_arc point count + label validity, segment boundary count. Build a side-by-side richness table. Also note each run's read-stage latency (relevant to D-08/D-09). Do NOT edit files — evidence only (D-01). Present the table.
  </action>
  <verify>
    <automated>npx tsx scripts/apollo-core-smoke.ts "$HOME/Downloads/TikTok Video Downloader.mp4" 2>&1 | tee /tmp/read-flash.txt | grep -iE 'emotion_arc|verbatim|segment' | head; QWEN_OMNI_MODEL=qwen3.5-omni-plus npx tsx scripts/apollo-core-smoke.ts "$HOME/Downloads/TikTok Video Downloader.mp4" 2>&1 | tee /tmp/read-plus.txt | grep -iE 'emotion_arc|verbatim|segment' | head</automated>
  </verify>
  <acceptance_criteria>
    - Both runs (flash + plus) completed on the same real video; outputs captured to /tmp/read-flash.txt and /tmp/read-plus.txt
    - A side-by-side richness table exists (verbatim fill, emotion_arc points + label validity, segment count) plus per-run read latency
    - The table is presented to Davide as the basis for the Task 2 decision (D-10)
  </acceptance_criteria>
  <done>Flash-vs-plus read-quality + latency A/B table produced from real-video runs.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: CO-REVIEW CHECKPOINT — keep flash or flip to plus + drift-guard strategy (D-10/D-11)</name>
  <action>STOP and present Task 1's flash-vs-plus richness+latency table + the proposed D-11 critical-field retry/drift-log strategy to Davide. Do NOT auto-decide. Capture his keep-flash/flip-plus choice and approved drift-guard strategy before any edit (D-10/D-11/D-00).</action>
  <read_first>
    - /tmp/read-flash.txt, /tmp/read-plus.txt (Task 1)
    - .planning/phases/01-engine-pipeline/01-RESEARCH.md §ENG-05 options A/B
  </read_first>
  <decision>(a) Keep flash or flip the read default to qwen3.5-omni-plus (D-10). (b) Approve the per-field critical-field retry + drift-logging strategy for D-11 (Claude's Discretion within D-11, surfaced per D-00).</decision>
  <context>flash was picked on 2 videos for speed (~17s vs ~36s); plus may be meaningfully richer and the budget now allows it (D-08/D-09 — quality wins, temporary latency regression OK, reclaimed in plan 04 cleanup). Present Task 1's richness+latency table. Also present the proposed D-11 strategy: which critical fields trigger a bounded retry (candidate: emotion_arc empty / hook_verbatim null on a speech video / weakest_modality underivable), how many retries (fit existing MAX_RETRIES envelope), and the drift-log shape.</context>
  <options>
    <option id="keep-flash">
      <name>Keep flash + add D-11 guards</name>
      <pros>No latency cost; substrate held in the original A/B. Lowest risk.</pros>
      <cons>If Task 1 shows plus is meaningfully richer, leaves substrate quality on the table.</cons>
    </option>
    <option id="flip-plus">
      <name>Flip default to plus + add D-11 guards</name>
      <pros>Richer substrate feeds fold + Apollo; D-08/D-09 budget allows it.</pros>
      <cons>Read latency regresses (~17s→~36s) — must be flagged for the plan 04 cleanup reclaim; requires version.ts bump (substrate shift) + Vercel env default note.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Davide's model choice (keep-flash / flip-plus) captured in SUMMARY + commit
    - The approved per-field retry + drift-log strategy is recorded (which fields, retry count, log shape) BEFORE Task 3 implements it (D-00)
    - If flip-plus: the latency-reclaim hand-off to plan 04 is noted
  </acceptance_criteria>
  <resume-signal>Select: keep-flash or flip-plus, and approve/adjust the D-11 drift-guard strategy</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FIX — D-11 critical-field bounded retry + drift logging (+ model default if flipped)</name>
  <behavior>
    - Test 1: read response parses OK but emotion_arc is empty on a speech video → triggers ONE bounded retry (preserving cached system prefix) before accepting/deriving.
    - Test 2: after the bounded retry still empty → graceful drop-to-undefined (no thrown frame) AND a drift-log entry is recorded.
    - Test 3: a valid full response → no retry, no drift log (retry only fires on empty critical field, not on whole-response success).
    - Test 4: hook_verbatim null on a speech-bearing video → same retry-then-log path.
  </behavior>
  <read_first>
    - src/lib/engine/qwen/omni-analysis.ts:25-30 (MAX_RETRIES/TIMEOUT), :192-330 (the retry loop where the critical-field check inserts), :289-296 (the cast-threaded fields — PITFALL 1)
    - src/lib/engine/qwen/schemas.ts:31-51, :81-95, :202-205 (existing guards — extend, do not replace)
    - src/lib/engine/deepseek.ts:409-415 (retry-nudge pattern: re-send user message, keep cached system prefix)
    - src/lib/engine/qwen/client.ts:36 (read-model default — change ONLY if Task 2 = flip-plus)
    - src/lib/engine/version.ts:63-67 and __tests__/version.test.ts:9-10
    - src/lib/engine/__tests__/omni-analysis-emotion-arc.test.ts, omni-analysis-verbatim.test.ts (existing guard coverage — extend)
  </read_first>
  <action>
    Implement EXACTLY the strategy approved in Task 2. Add a post-parse critical-field emptiness check in the omni-analysis.ts retry loop: when emotion_arc / hook_verbatim / weakest_modality are empty-or-underivable on a video that should have them, fire a bounded retry within the existing MAX_RETRIES envelope (re-send the volatile user message, keep the byte-stable cached system prefix — mirror deepseek.ts:409-415). On persistent emptiness after retry, keep the existing graceful drop-to-undefined AND emit a `read_drift` validation log entry naming the drifted field (the missing telemetry D-11 calls out). Preserve the cast-threaded fields at :289-296 (PITFALL 1 — do NOT remove). Do NOT raise TIMEOUT_MS to buy headroom (PITFALL 2). Create the new test file src/lib/engine/__tests__/omni-analysis-critical-field-retry.test.ts covering the 4 behaviors above (Wave 0 gap from VALIDATION.md) BEFORE the implementation (RED→GREEN). If Task 2 = flip-plus: change the qwen/client.ts:36 default to qwen3.5-omni-plus, bump ENGINE_VERSION 3.13.0→3.14.0 (or →3.15.0 if plan 01 already took 3.14.0 — read version.ts first) with a change-history comment, update version.test.ts:10 to match, and note the Vercel env override. If keep-flash: NO version bump (guards don't change output shape). No fenced code in this action.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/omni-analysis-critical-field-retry.test.ts src/lib/engine/__tests__/omni-analysis-emotion-arc.test.ts src/lib/engine/__tests__/omni-analysis-verbatim.test.ts src/lib/engine/__tests__/version.test.ts 2>&1 | grep -v '^#' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - omni-analysis-critical-field-retry.test.ts exists and its 4 behaviors pass (retry-on-empty, retry-then-log, no-retry-on-success, hook_verbatim path)
    - A drifted critical field produces a `read_drift` log entry (asserted in test) — no longer a silent drop
    - The bounded retry re-sends the user message and preserves the cached system prefix (no prefix-cache bust on retry)
    - Existing emotion_arc + verbatim guard tests still green (no regression to the drop-to-undefined safety net)
    - If flip-plus: client.ts:36 default = plus AND ENGINE_VERSION bumped AND version.test.ts updated (all three) ; if keep-flash: none bumped
    - PITFALL 1 cast-threaded fields intact; TIMEOUT_MS unchanged (PITFALL 2)
  </acceptance_criteria>
  <done>D-11 critical-field bounded retry + drift logging shipped on the chosen model; new retry test green; existing read tests green; version handled per the model decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Read LLM (omni) → engine | Untrusted omni JSON (verbatim, emotion_arc, segments) crosses into GeminiVideoAnalysis and feeds fold + Apollo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | schemas.ts Zod guards + omni-analysis.ts safeParse | mitigate (PRESERVE) | The new critical-field check inserts AFTER safeParse; existing .optional().catch(undefined) safety nets stay — retry adds resilience, never removes the graceful fallback |
| T-02-02 | Denial of Service | bounded retry on empty critical field | mitigate | Retry stays within existing MAX_RETRIES envelope; NO timeout raise (PITFALL 2) — cannot unbound the read stage |
| T-02-03 | Information Disclosure | Apify token → DashScope read path (pipeline.ts:400-496) | accept (preserve, do not touch) | Plan edits omni-analysis/schemas/client only; the signed-URL rehost flow is untouched — flag if any edit nears it |
</threat_model>

<verification>
- `npx vitest run` on the read test files + version test green.
- Two live read runs captured (Task 1 A/B).
- Per D-02, batch live verification with plan 01's fixes — offer Davide the testing menu (he UATs / Claude Playwright /analyze on the test video / scripts), do not pre-commit.
</verification>

<success_criteria>
- ROADMAP criterion 5 advanced: "omni-flash drift is hardened (emotion_arc.label, weakest_modality, verbatim hook/segments hold across runs)".
- D-10 model decision captured; D-11 retry+logging shipped; per-field strategy was surfaced before applying (D-00).
- Cache invariant respected on any default flip.
</success_criteria>

<output>
Create `.planning/phases/01-engine-pipeline/01-02-SUMMARY.md` when done. Record: the flash-vs-plus richness+latency table, Davide's model decision, the approved D-11 strategy, whether version bumped, and any latency-reclaim hand-off to plan 04.
</output>
