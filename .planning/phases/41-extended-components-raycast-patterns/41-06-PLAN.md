---
phase: 41-extended-components-raycast-patterns
plan: 06
type: execute
wave: 3
depends_on: ["41-01", "41-02", "41-03", "41-04", "41-05"]
files_modified:
  - src/components/ui/index.ts
  - src/app/(marketing)/ui-showcase/page.tsx
autonomous: false

must_haves:
  truths:
    - "All Phase 41 components are importable from @/components/ui"
    - "UI showcase page renders every new component in a visual grid"
    - "Dialog opens and closes correctly in the browser"
    - "Select dropdown is navigable with keyboard"
    - "Kbd renders with visible 3D depth"
    - "Toast notifications appear and auto-dismiss"
  artifacts:
    - path: "src/components/ui/index.ts"
      provides: "Barrel exports for all Phase 41 components"
      exports: ["Dialog", "DialogTrigger", "DialogContent", "DialogHeader", "DialogFooter", "DialogTitle", "DialogDescription", "DialogClose", "Toggle", "Tabs", "TabsList", "TabsTrigger", "TabsContent", "Avatar", "AvatarGroup", "Divider", "Select", "SearchableSelect", "ToastProvider", "useToast", "Kbd", "ShortcutBadge", "ExtensionCard", "TestimonialCard", "CategoryTabs"]
    - path: "src/app/(marketing)/ui-showcase/page.tsx"
      provides: "Visual showcase of all Phase 41 components"
  key_links:
    - from: "src/components/ui/index.ts"
      to: "src/components/ui/*.tsx"
      via: "barrel re-exports"
      pattern: "export.*from"
    - from: "src/app/(marketing)/ui-showcase/page.tsx"
      to: "src/components/ui/index.ts"
      via: "imports all components"
      pattern: "import.*from.*@/components/ui"
---

<objective>
Update the barrel export index and extend the UI showcase page to demonstrate all Phase 41 components.

Purpose: Ensure all new components are accessible via the canonical `@/components/ui` import path and visually verifiable at `/ui-showcase`.

Output: Updated `index.ts` with all Phase 41 exports, extended showcase page with interactive examples.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-extended-components-raycast-patterns/41-01-PLAN.md
@.planning/phases/41-extended-components-raycast-patterns/41-02-PLAN.md
@.planning/phases/41-extended-components-raycast-patterns/41-03-PLAN.md
@.planning/phases/41-extended-components-raycast-patterns/41-04-PLAN.md
@.planning/phases/41-extended-components-raycast-patterns/41-05-PLAN.md

# Existing files to update
@src/components/ui/index.ts
@src/app/(marketing)/ui-showcase/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update barrel exports and build showcase sections</name>
  <files>src/components/ui/index.ts, src/app/(marketing)/ui-showcase/page.tsx</files>
  <action>
**Update `src/components/ui/index.ts`:**

Add exports for all Phase 41 components after the existing exports. Group by plan:

```typescript
// Dialog (41-01)
export { Dialog, DialogTrigger, DialogPortal, DialogClose, DialogOverlay, DialogContent, DialogHeader, DialogFooter, DialogTitle, DialogDescription } from "./dialog";

// Toggle (41-01)
export { Toggle } from "./toggle";
export type { ToggleProps } from "./toggle";

// Tabs (41-02)
export { Tabs, TabsList, TabsTrigger, TabsContent } from "./tabs";

// Avatar (41-02)
export { Avatar, AvatarGroup } from "./avatar";
export type { AvatarProps } from "./avatar";

// Divider (41-02)
export { Divider } from "./divider";
export type { DividerProps } from "./divider";

// Select (41-03)
export { Select, SearchableSelect } from "./select";
export type { SelectProps, SearchableSelectProps } from "./select";

// Toast (41-04)
export { ToastProvider, useToast } from "./toast";

// Kbd (41-04)
export { Kbd } from "./kbd";
export type { KbdProps } from "./kbd";

// ShortcutBadge (41-04)
export { ShortcutBadge } from "./shortcut-badge";
export type { ShortcutBadgeProps } from "./shortcut-badge";

// ExtensionCard (41-05)
export { ExtensionCard } from "./extension-card";
export type { ExtensionCardProps } from "./extension-card";

// TestimonialCard (41-05)
export { TestimonialCard } from "./testimonial-card";
export type { TestimonialCardProps } from "./testimonial-card";

// CategoryTabs (41-05)
export { CategoryTabs } from "./category-tabs";
export type { CategoryTabsProps } from "./category-tabs";
```

**Update `src/app/(marketing)/ui-showcase/page.tsx`:**

Add new sections to the existing showcase page for Phase 41 components. Each section follows the established pattern from Phase 40's showcase. The page is a server component but interactive demos need client wrappers — create a separate `_components/phase-41-demos.tsx` client component if needed, or add "use client" sections.

Sections to add (after existing Phase 40 sections):

**1. Dialog section:**
- Button that opens a dialog with title, description, and close button
- Show different size variants (sm, md, lg)

**2. Toggle section:**
- Three toggles showing sm/md/lg sizes
- One toggle with label
- One toggle in checked state showing coral accent

**3. Tabs section:**
- Tabs with 3 tabs showing content switching
- Show the glass pill styling on active tab

**4. Avatar section:**
- Row of avatars in different sizes (xs through xl)
- Avatar with fallback initials (no image)
- AvatarGroup with 5 avatars, max 3 (shows +2)

**5. Divider section:**
- Horizontal divider
- Divider with label "OR"
- Vertical divider between two elements

**6. Select section:**
- Basic select with 5 options
- Select with option groups
- SearchableSelect with many options

**7. Toast section:**
- Buttons to trigger each toast variant (success, error, warning, info)
- Wrap in ToastProvider

**8. Kbd section:**
- Individual keys: Esc, ⌘, K, Enter
- Three sizes side by side
- Highlighted key with coral glow

**9. ShortcutBadge section:**
- Common shortcuts: Cmd+K, Cmd+Shift+P, Ctrl+C
- With "plus" separator variant

**10. ExtensionCard section:**
- Grid of 3 extension cards with different gradient themes (coral, purple, blue)

**11. TestimonialCard section:**
- Two testimonial cards, one featured

**12. CategoryTabs section:**
- Category navigation with 4-5 tabs, icons, and counts

Each section: `<section className="space-y-4">` with `<Heading as="h2" size="sm">Component Name</Heading>` header matching Phase 40's showcase pattern.
  </action>
  <verify>
`npx tsc --noEmit` — no type errors in index.ts or showcase page. Run `npm run build` to verify the full build passes. Visit `/ui-showcase` to see all components rendered.
  </verify>
  <done>
All Phase 41 components importable from @/components/ui. Showcase page renders all 12 new components with interactive examples.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All Phase 41 components: Dialog, Toggle, Tabs, Avatar, Divider, Select, Toast, Kbd, ShortcutBadge, ExtensionCard, TestimonialCard, CategoryTabs — rendered on the /ui-showcase page.</what-built>
  <how-to-verify>
1. Navigate to http://localhost:3000/ui-showcase (or Vercel preview URL)
2. Scroll to Phase 41 sections and verify:
   - **Dialog:** Click button to open — should show glass overlay + centered content. Press ESC to close. Click outside to close.
   - **Toggle:** Click toggles — coral accent appears when on. Space key toggles.
   - **Tabs:** Click tabs — content switches. Arrow keys move between tabs. Active tab has glass pill.
   - **Avatar:** Shows different sizes. Broken image shows initials. Group shows overlapping +N.
   - **Divider:** Horizontal line, vertical line, labeled "OR" divider all visible.
   - **Select:** Click to open dropdown. Arrow keys navigate. Enter selects. Escape closes. SearchableSelect filters.
   - **Toast:** Click buttons — toasts appear top-right with slide animation. Auto-dismiss after 5s.
   - **Kbd:** Keys look 3D with depth (4-layer shadow visible). Highlighted key has coral glow.
   - **ShortcutBadge:** Cmd+K renders with ⌘ symbol. Multiple key combos display correctly.
   - **ExtensionCard:** Gradient glow visible at top of cards. Different colors for each.
   - **TestimonialCard:** Quote text with attribution. Featured card has subtle glow.
   - **CategoryTabs:** Horizontal tabs with icons and counts. Scrollable if overflow.
3. Verify no console errors
4. Verify all components are visually consistent with Phase 40 components (same token system)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- All Phase 41 exports resolve from `@/components/ui`
- Showcase page loads at /ui-showcase without errors
- All 12 component sections visible and interactive
</verification>

<success_criteria>
- All Phase 41 components accessible via barrel exports
- Showcase page demonstrates every component with interactive examples
- Human verification confirms visual quality matches Raycast aesthetic
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/41-extended-components-raycast-patterns/41-06-SUMMARY.md`
</output>
