---
phase: 02-trustworthy-sim-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/audience/signature-equality.ts
  - src/lib/audience/__tests__/signature-determinism.test.ts
  - src/lib/audience/__tests__/fixtures/bake-input.fixture.json
  - src/lib/audience/__tests__/fixtures/bake-llm-outputs.fixture.json
autonomous: true
requirements: [TRUST-03]
must_haves:
  truths:
    - "normalizeSignature zeroes provenance.scraped_at and leaves every load-bearing field untouched (per D-01)"
    - "Two enrichSignature assemblies from identical inputs + replay deps are byte-identical post-normalization (TRUST-03 determinism leg)"
    - "scraped_at is the ONLY field that differs pre-normalization (Assumption A1 proven, not assumed)"
    - "A no-calibration SIM resolves Directional by rule; a calibration-bearing pack resolves Validated (per D-04 tiering leg)"
    - "The kept replay test runs zero-network and green via node ./node_modules/vitest/vitest.mjs run"
  artifacts:
    - path: "src/lib/audience/signature-equality.ts"
      provides: "normalizeSignature + signatureEqual + stableStringify (KEEP — P3 regression foundation)"
      exports: ["normalizeSignature", "signatureEqual"]
    - path: "src/lib/audience/__tests__/signature-determinism.test.ts"
      provides: "Replay determinism gate + scraped_at-only-delta proof + Directional-by-rule tiering assertion (KEEP)"
      contains: "signature determinism"
    - path: "src/lib/audience/__tests__/fixtures/bake-input.fixture.json"
      provides: "Frozen EnrichInput for the replay gate (KEEP)"
    - path: "src/lib/audience/__tests__/fixtures/bake-llm-outputs.fixture.json"
      provides: "Recorded WatchNote[] + SynthSchema for zero-network replay (KEEP)"
  key_links:
    - from: "src/lib/audience/__tests__/signature-determinism.test.ts"
      to: "src/lib/audience/signature-equality.ts"
      via: "import { normalizeSignature, signatureEqual }"
      pattern: "from \"\\.\\./signature-equality\""
    - from: "src/lib/audience/__tests__/signature-determinism.test.ts"
      to: "src/lib/audience/enrich-signature.ts"
      via: "enrichSignature(input, replayDeps)"
      pattern: "enrichSignature\\("
---

<objective>
Build the SPIKE's one surviving artifact: the determinism regression gate. This is the KEEP half (D-05) — `signature-equality.ts` (`normalizeSignature` + `signatureEqual`) and a zero-network Vitest replay test that proves the signature-assembly is byte-deterministic post-normalization, plus the Directional-by-rule tiering predicate. This becomes Phase 3's free-by-construction regression foundation (TRUST-01).

Purpose: prove the determinism + tiering legs of TRUST-03 as automated, CI-safe assertions independent of any paid live run. The make-or-break LLM-determinism question (does thinking-mode honor the seed) is answered by the live probe in 02-02; THIS plan locks the assembly + normalization + tiering rule so P3 inherits a green gate.

Output: `signature-equality.ts` (KEEP), `signature-determinism.test.ts` (KEEP), and two hand-authored replay fixtures (KEEP).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trustworthy-sim-spike/02-CONTEXT.md
@.planning/phases/02-trustworthy-sim-spike/02-RESEARCH.md
@.planning/phases/02-trustworthy-sim-spike/02-PATTERNS.md
@.planning/phases/02-trustworthy-sim-spike/02-VALIDATION.md

# Substrate this gate hangs off (read, do NOT modify — build on `main`, do NOT git merge rework/engine-core):
@src/lib/audience/enrich-signature.ts
@src/lib/audience/audience-types.ts
@src/lib/audience/__tests__/enrich-signature.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Determinism gate — signature-equality.ts + replay test + fixtures</name>
  <files>src/lib/audience/signature-equality.ts, src/lib/audience/__tests__/signature-determinism.test.ts, src/lib/audience/__tests__/fixtures/bake-input.fixture.json, src/lib/audience/__tests__/fixtures/bake-llm-outputs.fixture.json</files>
  <read_first>
    - src/lib/audience/enrich-signature.ts:204-219 (EnrichDeps + EnrichInput shapes) and :469-489 (signature assembly — confirm provenance.scraped_at at :484 is the sole `new Date()` field).
    - src/lib/audience/audience-types.ts (AudienceSignature / SignaturePersona / SignatureProvenance — the exact shapes to normalize over).
    - src/lib/audience/__tests__/enrich-signature.test.ts:27-106 (makeProfile / makeVideo / makeSynth / makeInput / WATCH_NOTE builders) and :190-197 (makeDeps dep-injection pattern). The fixtures serialize these SAME shapes; the test mirrors this file's structure exactly.
    - 02-RESEARCH.md Pattern 2 (normalization rule + volatile-vs-load-bearing table) and the "Kept regression test" code example.
    - 02-PATTERNS.md sections for signature-equality.ts and signature-determinism.test.ts.
  </read_first>
  <action>
    Create `src/lib/audience/signature-equality.ts` (KEEP, pure, dependency-free except `import type { AudienceSignature } from "./audience-types"`):
    - `stableStringify(value)`: JSON.stringify with recursively sorted object keys (guards key-order drift on nested objects/arrays).
    - `normalizeSignature(sig): AudienceSignature`: return a shallow clone with `provenance.scraped_at` set to the constant `"1970-01-01T00:00:00.000Z"` — this is the ONLY volatile field (enrich-signature.ts:484, per D-01 / Assumption A1). Do not touch any other field.
    - `signatureEqual(a, b): boolean`: `stableStringify(normalizeSignature(a)) === stableStringify(normalizeSignature(b))`.
    Hand-author the two replay fixtures under `src/lib/audience/__tests__/fixtures/` by mirroring the inline builders in enrich-signature.test.ts (no live data — these are synthetic, reviewable, secret-free):
    - `bake-input.fixture.json`: a valid `EnrichInput` ({handle, profile, videos[≥1], subCoverage, goalIntent:"grow"}) matching makeInput() shape.
    - `bake-llm-outputs.fixture.json`: `{ watchNotes: WatchNote[], synth: <SynthSchema-shaped object>, subtitle: string }` matching makeSynth()/WATCH_NOTE shapes (synth.audience.personas must list all archetypes the engine-fill expects so TEMPERATURE_DISPOSITION lookups resolve).
    Create `src/lib/audience/__tests__/signature-determinism.test.ts` (KEEP) mirroring enrich-signature.test.ts structure. Build `replayDeps: EnrichDeps` from the recorded fixture ({ watchVideo: async () => recorded.watchNotes[0], fetchSubtitle: async () => recorded.subtitle ?? null, synthesize: async () => recorded.synth }) so the test is zero-network. Assert: (a) `signatureEqual(a, b) === true` for two `enrichSignature(input, replayDeps)` assemblies; (b) `a.provenance.scraped_at !== b.provenance.scraped_at` (proves it IS volatile) yet `normalizeSignature(a)` deep-equals `normalizeSignature(b)` (proves normalization is the entire rule — Assumption A1). Do NOT hit live Qwen here; that is 02-02's job.
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/audience/__tests__/signature-determinism.test.ts</automated>
  </verify>
  <acceptance_criteria>
    The replay test runs zero-network and green via `node ./node_modules/vitest/vitest.mjs run` (NEVER `npm test`/`npx vitest` — they emit fake PASS(0)). `signatureEqual` returns true for two replay assemblies; the test proves `scraped_at` is the only pre-normalization delta. `signature-equality.ts` exports `normalizeSignature` and `signatureEqual` and imports nothing beyond the `AudienceSignature` type.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tiering leg — Directional-by-rule predicate assertion + audience suite green</name>
  <files>src/lib/audience/__tests__/signature-determinism.test.ts</files>
  <read_first>
    - 02-RESEARCH.md "Tiering rule assertion (pure predicate)" code example and the Summary's tiering paragraph (no resolver exists; the spike ASSERTS the rule — P3 builds the resolver per TRUST-01).
    - src/lib/engine/packs/socials.ts (confirm SOCIALS_PACK.calibration carries a non-empty baselineRef → the Validated anchor) and src/lib/engine/domain-pack.ts (CalibrationSpec shape).
    - 02-CONTEXT.md D-04 (tiering gate: a no-calibration SIM resolves Directional by rule, never Validated).
    - Anti-pattern (02-RESEARCH): do NOT conflate Audience.calibration (scrape provenance) with DomainPack.calibration (trust-tier basis). Key off the pack's CalibrationSpec only.
  </read_first>
  <action>
    Append a `describe("trust tiering rule")` block to `signature-determinism.test.ts` with a local pure predicate `resolveTier(calibration?: { baselineRef?: string }): "Validated" | "Directional"` returning `calibration?.baselineRef ? "Validated" : "Directional"`. Assert: `resolveTier(SOCIALS_PACK.calibration) === "Validated"` (calibration-baseline.json present) and `resolveTier(undefined) === "Directional"` (the no-calibration General case — Directional BY RULE, per D-04). Keep the predicate local to the test file — this plan asserts the rule only; the badge resolver is Phase 3 (TRUST-01). Import SOCIALS_PACK from the packs module; if importing the full pack pulls heavy deps into the test, assert against `{ baselineRef: SOCIALS_PACK.calibration?.baselineRef }` shape only. Do not build any resolver in src/.
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/audience</automated>
  </verify>
  <acceptance_criteria>
    The full `src/lib/audience` suite is green via `node ./node_modules/vitest/vitest.mjs run`. The tiering block proves Socials (calibration present) → "Validated" and no-calibration → "Directional" by rule. No Validated/Directional resolver is added to `src/lib` (over-building breaks D-05 scope) — the predicate lives only in the test.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
This plan creates (all KEEP — survive spike close per D-05):
- `src/lib/audience/signature-equality.ts` → `normalizeSignature(sig)`, `signatureEqual(a,b)`, `stableStringify(value)`
- `src/lib/audience/__tests__/signature-determinism.test.ts` → replay determinism gate + scraped_at-only-delta proof + `resolveTier` tiering assertion
- `src/lib/audience/__tests__/fixtures/bake-input.fixture.json` → frozen synthetic `EnrichInput`
- `src/lib/audience/__tests__/fixtures/bake-llm-outputs.fixture.json` → recorded `{ watchNotes, synth, subtitle }` replay payload
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → committed repo | Hand-authored synthetic JSON enters version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | `__tests__/fixtures/*.json` | mitigate | Fixtures are hand-authored synthetic data — no real scrape, no tokens, no `mediaUrl` query strings. Nothing to leak. |
| T-02-02 | Tampering | normalization rule | accept | `normalizeSignature` zeroes one known field; over-stripping a load-bearing field would weaken the gate but is caught by the deep-equal test (would falsely pass) — the live probe's full-diff self-check (02-02) is the backstop. Low risk. |

No package installs in this phase → no `T-02-SC` supply-chain checkpoint required (zero external packages per 02-RESEARCH Package Legitimacy Audit).
</threat_model>

<verification>
## TRUST-03 → Validation Map (Dimension 8)

| TRUST-03 behavior | VALIDATION.md row | Automated command |
|-------------------|-------------------|-------------------|
| determinism (assembly) | "determinism (assembly)" | `node ./node_modules/vitest/vitest.mjs run src/lib/audience/__tests__/signature-determinism.test.ts` |
| normalization (`scraped_at` only delta) | "normalization" | same file |
| tiering (Directional by rule) | "tiering" | same file |

Per-task commit gate: quick-run the determinism test. Plan-complete gate: `node ./node_modules/vitest/vitest.mjs run src/lib/audience` green.
</verification>

<success_criteria>
- `signature-equality.ts` exists, pure, exports `normalizeSignature` + `signatureEqual`.
- The replay test is green, zero-network, run via `node ./node_modules/vitest/vitest.mjs run`.
- The test proves: two replay assemblies are byte-identical post-normalization, `scraped_at` is the only pre-normalization delta, and no-calibration → Directional by rule.
- The full `src/lib/audience` suite stays green (no regression).
</success_criteria>

<output>
Create `.planning/phases/02-trustworthy-sim-spike/02-01-SUMMARY.md` when done.
</output>
