---
phase: 02-trustworthy-sim-spike
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - .planning/phases/02-trustworthy-sim-spike/SPIKE-VERDICT.md
autonomous: true
requirements: [TRUST-03]
must_haves:
  truths:
    - "SPIKE-VERDICT.md records a per-leg PASS/FAIL for determinism, provenance, and tiering against the hard 3-gate (D-04)"
    - "SPIKE-VERDICT.md carries an explicit GO or NO-GO — the phase gate, not an afterthought"
    - "If NO-GO, a written fallback plan states what to change before Phase 3"
    - "The verdict notes the source=user structural-shape recommendation for P3 (Open Question 3) and the videos_watched=INCONCLUSIVE caveat (Pitfall 2)"
    - "The throwaway scripts/spike scaffolding is deleted; the KEEP equality module + replay test survive and stay green (D-05)"
  artifacts:
    - path: ".planning/phases/02-trustworthy-sim-spike/SPIKE-VERDICT.md"
      provides: "The primary deliverable — per-leg result + GO/NO-GO + fallback (D-05)"
      contains: "GO"
  key_links:
    - from: ".planning/phases/02-trustworthy-sim-spike/SPIKE-VERDICT.md"
      to: "scripts/spike/trustworthy-sim-probe.ts (evidence)"
      via: "quotes the 02-02 probe evidence (determinism / provenance / tiering)"
      pattern: "[Dd]eterminism"
    - from: ".planning/phases/02-trustworthy-sim-spike/SPIKE-VERDICT.md"
      to: "src/lib/audience/__tests__/signature-determinism.test.ts"
      via: "cites the kept replay gate as green (determinism leg, CI-safe)"
      pattern: "signature-determinism"
---

<objective>
Close the spike. Write `SPIKE-VERDICT.md` — the primary deliverable (D-05) — carrying an explicit GO/NO-GO against the hard 3-gate (D-04: Determinism 2/2 identical post-normalization · Provenance 100% reactors ≥1 evidence + ungrounded distinguishable across scraped + source=user · Tiering Directional-by-rule). Then tear down the throwaway proto (D-05), leaving only the KEEP determinism gate as Phase 3's foundation.

Purpose: convert the 02-02 live evidence + the 02-01 green gate into a crisp, defensible go/no-go on whether the no-calibration trust model holds for the General surface. A fuzzy qualitative GO is rejected by D-04 — every leg gets a PASS/FAIL.

Output: `SPIKE-VERDICT.md` (PRIMARY, KEEP); `scripts/spike/` deleted (THROWAWAY).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trustworthy-sim-spike/02-CONTEXT.md
@.planning/phases/02-trustworthy-sim-spike/02-RESEARCH.md
@.planning/phases/02-trustworthy-sim-spike/02-02-SUMMARY.md

# The KEEP gate the verdict cites:
@src/lib/audience/__tests__/signature-determinism.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SPIKE-VERDICT.md — per-leg result + GO/NO-GO + fallback</name>
  <files>.planning/phases/02-trustworthy-sim-spike/SPIKE-VERDICT.md</files>
  <read_first>
    - 02-02-SUMMARY.md (the captured probe evidence — determinism A==B, differing-path list, per-reactor evidence counts, videos_watched per bake, tiering result, call count/cost).
    - 02-CONTEXT.md D-04 (the hard 3-gate bars) and D-05 (deliverable + throwaway line).
    - ROADMAP.md Phase 2 success criteria 1-3 (determinism / provenance / tiering + written go/no-go).
    - 02-RESEARCH.md Open Question 3 (source=user structural shape recommendation for P3) and Pitfall 2 (videos_watched mismatch = INCONCLUSIVE, not NO-GO).
  </read_first>
  <action>
    Create `.planning/phases/02-trustworthy-sim-spike/SPIKE-VERDICT.md` with these sections:
    - **Header**: phase, date, requirement (TRUST-03), the question being answered ("Can a user build a trustworthy General SIM with no calibration data?").
    - **Gate 1 — Determinism (D-04)**: PASS iff the 2 socials bakes are signatureEqual post-normalization AND the full-diff self-check found no differing path beyond `scraped_at`. Quote the probe evidence. If `videos_watched` differed, mark INCONCLUSIVE (transport caveat, Pitfall 2) and state the re-run outcome — do NOT score it NO-GO on transport alone. Cite the KEEP replay test as the CI-safe assembly-determinism backstop (green).
    - **Gate 2 — Provenance (D-04)**: PASS iff 100% of the 10 reactors carry ≥1 non-empty evidence quote, ungrounded personas are distinguishable, AND the General proto's source=user note surfaced as first-class evidence (D-03). Quote per-reactor counts.
    - **Gate 3 — Tiering (D-04)**: PASS iff the no-calibration General SIM resolved Directional by rule (never Validated). Cite the tiering assertion.
    - **OVERALL VERDICT**: a single explicit `GO` or `NO-GO` line (all three gates must PASS for GO). Make this line unambiguous and greppable.
    - **Fallback (only if NO-GO)**: concrete changes before Phase 3 (e.g. disable thinking-mode for the bake, pin the watched set, add an evidence-required schema guard).
    - **P3 carry-forward**: the source=user structural-shape recommendation (probe-local `provenance.custom_context` today → real field in P3 per D-defer-01); the KEEP gate (`signature-equality.ts` + replay test) is P3's regression foundation; the badge resolver is P3 (TRUST-01).
    - **Budget actuals**: Qwen + Apify call count + est cost vs the ~10 Qwen + 1 Apify, <$0.50 target.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-trustworthy-sim-spike/SPIKE-VERDICT.md && grep -v '^#' .planning/phases/02-trustworthy-sim-spike/SPIKE-VERDICT.md | grep -Eiq 'verdict|GO|NO-GO' && grep -v '^#' .planning/phases/02-trustworthy-sim-spike/SPIKE-VERDICT.md | grep -Eic 'determinism|provenance|tiering' | (read n; test "$n" -ge 3) && echo VERDICT-OK</automated>
  </verify>
  <acceptance_criteria>
    `SPIKE-VERDICT.md` exists with a per-leg PASS/FAIL for all three gates, an explicit GO/NO-GO line, a fallback plan if NO-GO, and the P3 carry-forward notes. Determinism/provenance/tiering each appear as a scored gate. A `videos_watched` transport mismatch (if any) is recorded as INCONCLUSIVE, not a NO-GO driver.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tear down throwaway proto; confirm KEEP gate survives green</name>
  <files>scripts/spike/trustworthy-sim-probe.ts, scripts/spike/chat-bundle-adapter.ts, scripts/spike/fixtures/socials-bundle.fixture.json</files>
  <read_first>
    - 02-CONTEXT.md D-05 (KEEP the determinism harness as a committed test; THROW AWAY the proto General-bundle scaffolding).
    - 02-RESEARCH.md Runtime State Inventory ("Delete `scripts/spike/` on close").
  </read_first>
  <action>
    Delete the entire `scripts/spike/` directory (probe + adapter + fixtures) — the evidence now lives in `SPIKE-VERDICT.md`, and the scaffolding is recoverable from git history if a NO-GO fallback needs to iterate (per D-05). Do NOT touch `src/lib/audience/signature-equality.ts` or `src/lib/audience/__tests__/signature-determinism.test.ts` (KEEP — P3 foundation). Confirm the KEEP gate still compiles and is green after removal (no dangling import from src into scripts/spike).
  </action>
  <verify>
    <automated>test ! -d scripts/spike && node ./node_modules/vitest/vitest.mjs run src/lib/audience</automated>
  </verify>
  <acceptance_criteria>
    `scripts/spike/` no longer exists. The KEEP equality module + replay test remain and the full `src/lib/audience` suite is green via `node ./node_modules/vitest/vitest.mjs run`. No src file imports anything from `scripts/spike`.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
This plan creates:
- `.planning/phases/02-trustworthy-sim-spike/SPIKE-VERDICT.md` (PRIMARY deliverable, KEEP) → per-leg gate results + explicit GO/NO-GO + fallback + P3 carry-forward
This plan deletes (THROWAWAY, D-05):
- `scripts/spike/trustworthy-sim-probe.ts`, `scripts/spike/chat-bundle-adapter.ts`, `scripts/spike/fixtures/socials-bundle.fixture.json`
Surviving phase artifacts after close: `signature-equality.ts`, `signature-determinism.test.ts` (+ fixtures), `SPIKE-VERDICT.md`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verdict doc → committed repo | The verdict quotes probe evidence into version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information disclosure | SPIKE-VERDICT.md | mitigate | Quote only signature fields + call counts in the verdict; never paste API keys or token-bearing URLs. Deleting `scripts/spike/` removes the (already-scrubbed) frozen fixture from the working tree. |

No package installs → no `T-02-SC` supply-chain checkpoint.
</threat_model>

<verification>
## TRUST-03 → Validation Map (Dimension 8)

| TRUST-03 behavior | VALIDATION.md gate | Verification |
|-------------------|--------------------|--------------|
| written go/no-go verdict | "Before /gsd-verify-work: Full suite green + SPIKE-VERDICT.md written with explicit GO/NO-GO" | verdict doc contains a scored 3-gate + GO/NO-GO line |
| determinism gate survives (P3 foundation) | kept replay test | `node ./node_modules/vitest/vitest.mjs run src/lib/audience` green after throwaway teardown |

Phase gate: full `src/lib/audience` suite green AND `SPIKE-VERDICT.md` carries an explicit GO/NO-GO.
</verification>

<success_criteria>
- `SPIKE-VERDICT.md` exists with a scored per-leg result (determinism / provenance / tiering) and an explicit GO or NO-GO against D-04.
- A fallback plan is written if the verdict is NO-GO.
- `scripts/spike/` is deleted; the KEEP equality module + replay test survive and stay green.
- The phase closes with a defensible go/no-go decision for the Phase 3 General surface.
</success_criteria>

<output>
Create `.planning/phases/02-trustworthy-sim-spike/02-03-SUMMARY.md` when done.
</output>
