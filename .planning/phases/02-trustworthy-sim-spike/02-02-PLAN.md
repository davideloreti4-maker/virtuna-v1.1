---
phase: 02-trustworthy-sim-spike
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/spike/chat-bundle-adapter.ts
  - scripts/spike/trustworthy-sim-probe.ts
  - scripts/spike/fixtures/socials-bundle.fixture.json
autonomous: false
requirements: [TRUST-03]
user_setup:
  - service: dashscope
    why: "Real Qwen (omni-flash watch + qwen-3.7-plus synthesis) — the make-or-break LLM-determinism double-bake"
    env_vars:
      - name: DASHSCOPE_API_KEY
        source: "Existing .env.local (already used by the substrate)"
  - service: apify
    why: "The single profile scrape (D-01a) + mp4 token append in prepareWatchUrl"
    env_vars:
      - name: APIFY_TOKEN
        source: "Existing .env.local (already used by the substrate)"
must_haves:
  truths:
    - "The probe scrapes Apify exactly once and freezes the bundle as EnrichInput — no per-bake re-scrape (D-01a)"
    - "The frozen bundle fixture carries NO APIFY_TOKEN and NO query strings on any mediaUrl (secret-scrub before write)"
    - "The probe bakes the socials control TWICE with real Qwen (temp 0 + seed) and reports load-bearing-field equality via signatureEqual (D-01)"
    - "The probe bakes a General text-bundle proto with ZERO omni calls (watchVideo stubbed null) through the real synthesis path (D-02)"
    - "Every reactor persona carries >=1 non-empty evidence quote; ungrounded personas are distinguishable; a source=user custom-context note is folded in and tagged (D-03)"
    - "Without DASHSCOPE_API_KEY the probe fails loudly with zero cost (env guard)"
    - "A watch-count (videos_watched) mismatch is reported as INCONCLUSIVE/transport, not a determinism NO-GO (Pitfall 2)"
  artifacts:
    - path: "scripts/spike/chat-bundle-adapter.ts"
      provides: "chatBundleToEnrichInput(text, customNote?) — text → EnrichInput (THROWAWAY, D-05)"
      exports: ["chatBundleToEnrichInput"]
    - path: "scripts/spike/trustworthy-sim-probe.ts"
      provides: "Live ×2 double-bake + General proto + provenance/tiering inspection + evidence emit (THROWAWAY, D-05)"
    - path: "scripts/spike/fixtures/socials-bundle.fixture.json"
      provides: "The 1 frozen Apify scrape as EnrichInput, secret-scrubbed (THROWAWAY, D-05)"
  key_links:
    - from: "scripts/spike/trustworthy-sim-probe.ts"
      to: "src/lib/audience/enrich-signature.ts"
      via: "enrichSignature(input) double-bake + enrichSignature(input,{watchVideo:()=>null}) general proto"
      pattern: "enrichSignature\\("
    - from: "scripts/spike/trustworthy-sim-probe.ts"
      to: "src/lib/audience/signature-equality.ts"
      via: "signatureEqual / normalizeSignature for the full-diff self-check"
      pattern: "signature-equality"
    - from: "scripts/spike/trustworthy-sim-probe.ts"
      to: "src/lib/scraping/apify-provider.ts"
      via: "scrapeProfileBundle(handle, 12) — ONCE"
      pattern: "scrapeProfileBundle"
---

<objective>
Run the make-or-break experiment. Author the THROWAWAY live probe (D-05) that does the real ×2 double-bake against a frozen Apify scrape (live Qwen, temp 0 + seed), the General text-bundle proto bake (zero omni), and the provenance + tiering inspection — then RUN it with real keys and capture the evidence that feeds the verdict.

Purpose: answer the one genuine unknown — does thinking-mode synthesis stay deterministic across bakes (Pitfall 3)? — and confirm provenance (≥1 evidence/reactor, ungrounded distinguishable, source=user tagged) holds for the General no-calibration case. All under the ~10 Qwen + 1 Apify, <$0.50 budget (D-01b).

Output: `scripts/spike/*` (all THROWAWAY) + captured probe evidence (stdout/JSON) consumed by 02-03's verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trustworthy-sim-spike/02-CONTEXT.md
@.planning/phases/02-trustworthy-sim-spike/02-RESEARCH.md
@.planning/phases/02-trustworthy-sim-spike/02-PATTERNS.md

# Substrate the probe drives (read, do NOT modify):
@src/lib/audience/enrich-signature.ts
@src/lib/scraping/apify-provider.ts
@src/lib/scraping/types.ts
@scripts/fold-validate-r1.ts

# KEEP module from 02-01 the probe imports:
@src/lib/audience/signature-equality.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: chat-bundle-adapter.ts — text → EnrichInput (the only net-new logic)</name>
  <files>scripts/spike/chat-bundle-adapter.ts</files>
  <read_first>
    - src/lib/audience/enrich-signature.ts:213-219 (EnrichInput) and :433-454 (how the synth payload reads caption/subtitleText — the seam at :454).
    - src/lib/scraping/types.ts (VideoData / ProfileData required fields).
    - 02-RESEARCH.md Pattern 3 (General-bundle proto bake) and 02-PATTERNS.md chat-bundle-adapter section — copy the field set verbatim.
  </read_first>
  <action>
    Create `scripts/spike/chat-bundle-adapter.ts` (THROWAWAY, D-05) exporting `chatBundleToEnrichInput(text: string, customNote?: string): EnrichInput`. Pack the chat/doc text into a single `VideoData` (caption = text, sliced to ~4000 chars) with `mediaUrl` ABSENT (so prepareWatchUrl never runs) and zero engagement metrics; build a minimal valid `ProfileData` (handle "general-proto", videoCount 1). When `customNote` is present, fold it into the caption as `\n\n[custom-context · source=user] ${customNote}` so the synthesis path sees it as first-class evidence (D-03). Return `{ handle:"general-proto", profile, videos:[v], subCoverage:"0/0", goalIntent:"grow" }`. No omni, no network — the probe injects `watchVideo: () => null` so zero omni calls fire. Import types only from `@/lib/audience/enrich-signature` and `@/lib/scraping/types`.
  </action>
  <verify>
    <automated>pnpm tsx -e "import { chatBundleToEnrichInput } from './scripts/spike/chat-bundle-adapter'; const i = chatBundleToEnrichInput('hello world chat', 'be terse'); if (i.videos.length!==1 || !i.videos[0].caption.includes('source=user')) { console.error('FAIL'); process.exit(1) } console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    `chatBundleToEnrichInput` returns a valid `EnrichInput` with exactly one video whose caption contains the chat text and, when a note is supplied, the `[custom-context · source=user]` tag. No `mediaUrl` is set. The module type-checks and imports resolve via tsx.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: trustworthy-sim-probe.ts — author the live probe orchestration</name>
  <files>scripts/spike/trustworthy-sim-probe.ts, scripts/spike/fixtures/socials-bundle.fixture.json</files>
  <read_first>
    - scripts/fold-validate-r1.ts:18-41 (header run command + dotenv + tsconfig-paths bootstrap — adjust depth to `../../` since spike/ is nested one level deeper).
    - src/lib/audience/enrich-signature.ts: enrichSignature signature, EnrichDeps, prepareWatchUrl:258-274, the graceful watch-null path :311-316, scraped_at :484.
    - src/lib/scraping/apify-provider.ts (scrapeProfileBundle(handle, limit) → ProfileBundle).
    - src/lib/audience/signature-equality.ts (signatureEqual, normalizeSignature, stableStringify).
    - 02-RESEARCH.md Patterns 1-3, Pitfalls 1-5, the Security Domain note (scrub APIFY_TOKEN/query strings), Assumption A1 (full-diff self-check), and Open Question 3 (model source=user as probe-local `provenance.custom_context = {source,note}`).
  </read_first>
  <action>
    Create `scripts/spike/trustworthy-sim-probe.ts` (THROWAWAY, D-05) with the fold-validate-r1 bootstrap (dotenv from `../../.env.local`, tsconfig-paths register with baseUrl `../..`). Header comment: run via `pnpm tsx scripts/spike/trustworthy-sim-probe.ts`.
    ENV GUARD (first thing): if `DASHSCOPE_API_KEY` is missing, print a loud error and `process.exit(1)` BEFORE any network/cost. `APIFY_TOKEN` is only needed for a fresh scrape — if `scripts/spike/fixtures/socials-bundle.fixture.json` already exists, re-bake from it and skip the scrape.
    (1) SOCIALS CONTROL — scrape ONCE (D-01a): `scrapeProfileBundle(<handle>, 12)`, assemble `EnrichInput`. SECRET SCRUB: before writing the fixture, strip `?token=`/all query strings from every `video.mediaUrl` (and any URL field) so no APIFY_TOKEN lands in the committed JSON (T-02-03). Write `socials-bundle.fixture.json`. Then `enrichSignature(input)` TWICE (3 omni + 1 synth each = 8 Qwen). FULL-DIFF SELF-CHECK (Assumption A1): walk the un-normalized pair and log EVERY differing path — do not assume only scraped_at differs; then assert `signatureEqual(a,b)`.
    (2) GENERAL PROTO (D-02/D-03): build a representative chat `.txt` bundle (~20 messages) + a custom-context note via `chatBundleToEnrichInput(text, note)`; bake `enrichSignature(input, { watchVideo: async () => null })` (0 omni, 1 synth) ×2 = 2 Qwen. Post-process: set probe-local `signature.provenance.custom_context = { source:"user", note }` and assert ≥1 persona's `evidence` references the note text.
    (3) PROVENANCE LEG (D-03 / Pitfall 5): for BOTH bakes, assert 100% of the 10 reactor personas have `evidence.trim().length > 0`; flag any empty/generic as UNGROUNDED and prove grounded vs ungrounded are distinguishable. Report counts.
    (4) TIERING LEG (D-04): assert `resolveTier(undefined) === "Directional"` for the no-calibration General SIM.
    (5) EMIT EVIDENCE: print a structured block (determinism A==B per leg, list of differing paths, per-reactor evidence counts, videos_watched per bake, tiering result, total Qwen+Apify call count + est cost). PITFALL 2: if `provenance.videos_watched` differs between A and B, label the determinism result INCONCLUSIVE (transport/graceful-omni-null), NOT a NO-GO — print the caveat explicitly.
    Keep total budget ~10 Qwen + 1 Apify, <$0.50 (D-01b); cap omni at MIN_WATCH=3.
  </action>
  <verify>
    <automated>DASHSCOPE_API_KEY= pnpm tsx scripts/spike/trustworthy-sim-probe.ts; test $? -ne 0 && echo "ENV-GUARD-OK"</automated>
  </verify>
  <acceptance_criteria>
    With `DASHSCOPE_API_KEY` empty the probe exits non-zero with a loud message and zero network/cost (proves the guard). The probe type-checks and imports resolve. The fixture writer strips tokens/query strings from every `mediaUrl` before write. The probe imports `signatureEqual`/`normalizeSignature` from the KEEP module (no duplicated diff logic) and `chatBundleToEnrichInput` from Task 1.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: RUN the live probe (real keys + cost) and capture evidence</name>
  <action>Run the throwaway live probe with real keys to gather the make-or-break evidence. The executor attempts `pnpm tsx scripts/spike/trustworthy-sim-probe.ts` if `DASHSCOPE_API_KEY` + `APIFY_TOKEN` are present in the environment; otherwise it pauses for the human to supply keys / approve the ~$0.50 spend (Claude cannot self-provision keys or authorize cost). Capture the emitted determinism / provenance / tiering evidence for 02-03's verdict.</action>
  <what-built>The throwaway live probe (`scripts/spike/trustworthy-sim-probe.ts` + adapter) that double-bakes the socials control with real Qwen, bakes the General proto, and inspects provenance + tiering. This is the autonomous:false step — it needs real keys (`DASHSCOPE_API_KEY` + `APIFY_TOKEN`) and spends ~$0.50 (~10 Qwen + 1 Apify), which Claude cannot self-provision.</what-built>
  <how-to-verify>
    1. Confirm `DASHSCOPE_API_KEY` and `APIFY_TOKEN` are present in `.env.local`.
    2. Run: `pnpm tsx scripts/spike/trustworthy-sim-probe.ts`
    3. Read the emitted evidence block and record (for the verdict):
       - DETERMINISM: are the two socials bakes `signatureEqual` (load-bearing fields identical post-normalization)? Did the full-diff self-check find ANY differing path beyond `scraped_at`? If `videos_watched` differs, this is INCONCLUSIVE (transport), not NO-GO — re-run once.
       - PROVENANCE: do 100% of the 10 reactors carry ≥1 non-empty evidence quote? Are ungrounded distinguishable? Does the General proto's source=user note appear in a persona's evidence?
       - TIERING: did the no-calibration General SIM resolve "Directional"?
       - BUDGET: total Qwen + Apify calls ≤ ~10 + 1, cost < $0.50?
    4. Save the raw probe stdout/JSON evidence somewhere reachable by 02-03 (paste into the SUMMARY or a scratch note) — the verdict doc quotes it; the throwaway scripts get deleted in 02-03.
  </how-to-verify>
  <resume-signal>Paste the probe evidence (determinism / provenance / tiering results + call count), or report "key/cost blocker" if it cannot run.</resume-signal>
</task>

</tasks>

<artifacts_produced>
This plan creates (all THROWAWAY — deleted at spike close in 02-03 per D-05; recoverable from git history if a NO-GO fallback needs iteration):
- `scripts/spike/chat-bundle-adapter.ts` → `chatBundleToEnrichInput(text, customNote?)`
- `scripts/spike/trustworthy-sim-probe.ts` → live ×2 double-bake + General proto bake + provenance/tiering inspection + evidence emit
- `scripts/spike/fixtures/socials-bundle.fixture.json` → the 1 frozen, secret-scrubbed Apify scrape as `EnrichInput`
Plus: captured probe evidence (stdout/JSON) — the input to 02-03's verdict.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe → DashScope / Apify | Outbound live calls with real API keys (untrusted external responses) |
| frozen bundle fixture → committed repo | Scraped data (incl. mp4 URLs that carry the Apify token) serialized to disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information disclosure | `socials-bundle.fixture.json` | mitigate | Strip `?token=` / all query strings from every `mediaUrl` (and any URL field) BEFORE writing the fixture — `prepareWatchUrl` re-appends `APIFY_TOKEN` at runtime, so the persisted fixture must carry none (per ./CLAUDE.md: never commit secrets). |
| T-02-04 | Tampering / SSRF | scraped mp4 watch URL | accept (already mitigated) | Reuse `prepareWatchUrl` unchanged — HTTPS + host allowlist enforce the SSRF guard; the probe adds no new fetch path. |
| T-02-05 | Information disclosure | probe stdout / SUMMARY | mitigate | Emit only signature fields + call counts; never echo `DASHSCOPE_API_KEY` / `APIFY_TOKEN` to stdout. |

No package installs → no `T-02-SC` supply-chain checkpoint (zero external packages).
</threat_model>

<verification>
## TRUST-03 → Validation Map (Dimension 8)

| TRUST-03 behavior | VALIDATION.md row | Verification |
|-------------------|-------------------|--------------|
| LLM determinism (live ×2 double-bake) | "LLM determinism (live ×2 double-bake)" | manual (throwaway probe) — `pnpm tsx scripts/spike/trustworthy-sim-probe.ts`; load-bearing fields identical across 2 bakes |
| provenance (scraped + source=user) | "provenance (≥1 evidence/reactor; ungrounded distinguishable; scraped + source=user)" | probe assertions + recorded in verdict |
| env guard (zero-cost failure) | n/a (guard) | `DASHSCOPE_API_KEY= pnpm tsx ...` exits non-zero |

This plan is the manual-only leg of the validation strategy (live network + cost — cannot run in CI). The CI-safe legs live in 02-01.
</verification>

<success_criteria>
- The adapter + probe type-check; the env guard fails loudly and cost-free without `DASHSCOPE_API_KEY`.
- The frozen bundle fixture contains no tokens / query strings on any URL.
- The live probe runs within budget (~10 Qwen + 1 Apify, <$0.50) and emits a structured evidence block covering determinism, provenance, and tiering.
- The evidence is captured for 02-03 (a `videos_watched` mismatch is recorded as INCONCLUSIVE, not NO-GO).
</success_criteria>

<output>
Create `.planning/phases/02-trustworthy-sim-spike/02-02-SUMMARY.md` when done (paste the captured probe evidence into it for 02-03).
</output>
