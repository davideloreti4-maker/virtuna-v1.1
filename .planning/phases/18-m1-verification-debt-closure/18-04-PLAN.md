---
phase: 18-m1-verification-debt-closure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/verif-phase2-uat.md
  - .planning/research/verif-phase3-smoke.md
  - .planning/research/verif-phase4-uat.md
autonomous: false
requirements:
  - VERIF-01
  - VERIF-02
  - VERIF-03
tags:
  - verification
  - uat
  - live-deploy
  - checklist

must_haves:
  truths:
    - ".planning/research/verif-phase2-uat.md exists with a step-by-step UAT checklist for the 9-card interview modal + creator_profiles persistence + reference-creator side effect"
    - ".planning/research/verif-phase3-smoke.md exists with a step-by-step smoke checklist for /api/analyze SSE + cache-hit against the live Vercel deploy"
    - ".planning/research/verif-phase4-uat.md exists with a step-by-step UAT checklist for Wave 0 content-type via /api/analyze and niche-detector cost_cents > 0"
    - "Each checklist includes a Pass/Fail recording block at the bottom and an explicit 'defer permanently' escape hatch with rationale template per D-19"
    - "Human checkpoint logs the outcome of each VERIF item (MET / FAILED / DEFERRED-PERMANENTLY) back into the SUMMARY"
  artifacts:
    - path: ".planning/research/verif-phase2-uat.md"
      provides: "VERIF-01 manual UAT script + result log"
      contains: "## Steps"
    - path: ".planning/research/verif-phase3-smoke.md"
      provides: "VERIF-02 manual smoke script + result log"
      contains: "## Steps"
    - path: ".planning/research/verif-phase4-uat.md"
      provides: "VERIF-03 manual UAT script + result log"
      contains: "## Steps"
    - path: ".planning/phases/18-m1-verification-debt-closure/18-04-SUMMARY.md"
      provides: "Closure record for VERIF-01/02/03 with PASS/FAIL/DEFERRED-PERMANENTLY outcomes"
  key_links:
    - from: "VERIF-01 checklist"
      to: "live Vercel deploy"
      via: "manual click-through of the deployed creator-profile onboarding flow"
      pattern: "creator_profiles"
    - from: "VERIF-02 checklist"
      to: "/api/analyze on live deploy"
      via: "curl / browser network panel against the Vercel-deployed endpoint"
      pattern: "/api/analyze"
    - from: "VERIF-03 checklist"
      to: "Wave 0 content-type + niche-detector"
      via: "/api/analyze hit + cost_cents inspection"
      pattern: "cost_cents"
---

<objective>
Generate three manual UAT / smoke checklists (markdown) that the user executes against the live Vercel deploy, then record PASS / FAIL / DEFERRED-PERMANENTLY outcomes. Closes VERIF-01, VERIF-02, VERIF-03.

Per D-16 / D-17 / D-18: no Playwright automation — these tests hit live Supabase + Qwen/DashScope APIs that can't be mocked. Manual execution is the correct shape. Plan generates the checklists; user follows them; checkpoint records outcomes.

Per D-19: each checklist includes a "defer permanently" escape hatch with rationale template, in case a live test cannot pass (live-deploy blocker, feature gated, or premise invalidated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/MILESTONE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-m1-verification-debt-closure/18-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write VERIF-01 Phase 2 UAT checklist</name>
  <files>.planning/research/verif-phase2-uat.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (VERIF-01 wording)
    - .planning/phases/18-m1-verification-debt-closure/18-CONTEXT.md — decisions D-16, D-17, D-19
    - .planning/PROJECT.md — for the live Vercel deploy URL (look for `Deployed:` line)
  </read_first>
  <action>
    Create `.planning/research/verif-phase2-uat.md` as a step-by-step manual UAT script for the M1 Phase 2 (Creator Profile) verification, scoped to what VERIF-01 explicitly requires.

    File structure (use this exact heading layout — content of each section follows):

    ```markdown
    # VERIF-01 — Phase 2 Creator Profile UAT

    **REQ:** VERIF-01 (Phase 18, milestone Engine Hardening)
    **Target:** live Vercel deploy
    **Date executed:** _to fill in_
    **Executed by:** Davide Loreti

    ## Goal
    Confirm the M1 Phase 2 creator-profile flow works end-to-end against the deployed app:
    1. 9-card interview modal completes on first upload
    2. All 9 fields persist to `creator_profiles` table
    3. Reference-creator side effect fires after submission

    ## Preconditions
    - [ ] Live deploy URL reachable (from PROJECT.md `Deployed:` line)
    - [ ] Test account credentials available (or fresh signup ready)
    - [ ] Supabase dashboard access for `creator_profiles` row inspection
    - [ ] A short test video (any tiktok-shape mp4) ready for upload

    ## Steps

    ### A. First-upload interview modal
    1. Sign in / sign up with a fresh account (one with no existing `creator_profiles` row).
    2. Upload the test video via the main upload flow.
    3. Expected: 9-card interview modal opens automatically. Record observed: ___
    4. Step through all 9 cards, providing realistic answers (don't skip).
    5. Submit the modal. Expected: modal closes, upload analysis continues.

    ### B. creator_profiles persistence
    1. Open Supabase dashboard → `creator_profiles` table.
    2. Filter by the test account's `user_id`.
    3. Expected: exactly one row exists for this user, with all 9 fields populated (no nulls in the interview-collected columns). Record column-by-column: ___
    4. Snapshot the row contents (paste below).

    ### C. Reference-creator side effect
    1. After modal submission, observe (logs / dashboard / app UI — whichever exposes it) whether the reference-creator side effect fired.
    2. Expected: side effect runs once, idempotently.
    3. Record observed: ___

    ## Result

    Choose ONE:
    - [ ] **PASS** — all three sections (A, B, C) green. VERIF-01 closed as MET.
    - [ ] **FAIL** — at least one section red. Open follow-up ticket; do NOT close VERIF-01.
    - [ ] **DEFERRED PERMANENTLY** — live-deploy blocker / feature gated / premise invalidated.

    ### Defer-permanently rationale (only if chosen above)
    - **What was attempted:** ___
    - **Blocker:** ___
    - **Why it's acceptable to leave deferred:** ___
    ```

    Do NOT invent live-deploy URLs or test-account credentials — leave those as fill-in placeholders. Do NOT include screenshots or expected-output JSON in the template (the user fills them in at execution time).

    Word count target: ~250-400 words. Keep terse and executable.
  </action>
  <acceptance_criteria>
    - File `.planning/research/verif-phase2-uat.md` exists
    - File contains the headings `## Goal`, `## Preconditions`, `## Steps`, `## Result`, `### Defer-permanently rationale`
    - File contains a checklist item for the 9-card interview modal (grep `9-card`)
    - File contains a checklist item for `creator_profiles` persistence (grep `creator_profiles`)
    - File contains a checklist item for the reference-creator side effect (grep `reference-creator`)
    - File contains the three Pass/Fail/Deferred radio options
    - `wc -w .planning/research/verif-phase2-uat.md` reports between 150 and 600 words
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/research/verif-phase2-uat.md && grep -q '9-card' .planning/research/verif-phase2-uat.md && grep -q 'creator_profiles' .planning/research/verif-phase2-uat.md && grep -q 'reference-creator' .planning/research/verif-phase2-uat.md && grep -q 'DEFERRED PERMANENTLY' .planning/research/verif-phase2-uat.md</automated>
  </verify>
  <done>Checklist file exists with three test sections (9-card modal, persistence, side effect), Pass/Fail/Deferred outcome block, and defer-permanently rationale template.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write VERIF-02 Phase 3 smoke checklist + VERIF-03 Phase 4 UAT checklist</name>
  <files>
    .planning/research/verif-phase3-smoke.md
    .planning/research/verif-phase4-uat.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (VERIF-02 and VERIF-03 wording)
    - .planning/phases/18-m1-verification-debt-closure/18-CONTEXT.md — decisions D-16, D-17, D-19
  </read_first>
  <action>
    Create both files. Same structural template as Task 1, but with the content adapted to each REQ.

    **File A — `.planning/research/verif-phase3-smoke.md` (VERIF-02):**

    Same heading layout as Task 1's template. Sections:
    - **Goal:** confirm `/api/analyze` SSE works end-to-end against the live Vercel deploy AND a second call with identical input returns a cache hit (Phase 3 SC#4 + SC#5).
    - **Preconditions:** live deploy URL, valid auth token, a short test video.
    - **Steps:**
      - A. **/api/analyze SSE smoke:** issue POST against `/api/analyze` on the live deploy with the test video; record whether SSE events stream (`event: progress`, `event: result`); record final response shape.
      - B. **Cache-hit smoke:** issue a second POST with the same input; expected: cache-hit response with markedly lower cost; record cost_cents_estimated before vs after (cache-hit should be near $0).
    - **Result:** Pass / Fail / Deferred Permanently with rationale template (same shape as Task 1).

    Notes for VERIF-02 specifically:
    - Per D-19, if SSE cannot be exercised live (e.g. Vercel edge stripping `text/event-stream`), record as DEFERRED PERMANENTLY with the rationale `"Vercel edge handling of SSE blocks live verification; covered by local integration tests instead"` (only if that's actually the case at execution time — otherwise pass through).

    **File B — `.planning/research/verif-phase4-uat.md` (VERIF-03):**

    Same heading layout. Sections:
    - **Goal:** confirm the two pending live-API tests from Phase 4:
      1. End-to-end Wave 0 content-type via `/api/analyze` (the type of content classified at Wave 0 matches expectation)
      2. Niche-detector `cost_cents > 0` with cache breakdown absent (i.e. the niche-detector contributed cost on a fresh, non-cached request)
    - **Preconditions:** live deploy, valid auth, two test videos representing distinct content types (e.g. one talking-head, one product-demo).
    - **Steps:**
      - A. **Wave 0 content-type:** hit `/api/analyze` with each test video; record the Wave 0 `content_type` label returned; confirm it matches the visual content.
      - B. **Niche-detector cost:** hit `/api/analyze` once with a fresh, never-seen video; in the response (or admin logs), inspect the cost breakdown; confirm `cost_cents > 0` AND the cache-breakdown stage is absent / `null` / `false`.
    - **Result:** Pass / Fail / Deferred Permanently with rationale template (same shape as Task 1).

    Both files: word count target ~250-400 words each. Do NOT invent specific URLs or credentials — leave fill-in placeholders.
  </action>
  <acceptance_criteria>
    - Both files exist: `.planning/research/verif-phase3-smoke.md` AND `.planning/research/verif-phase4-uat.md`
    - phase3-smoke.md contains keywords `SSE`, `/api/analyze`, `cache-hit` (or `cache hit`)
    - phase4-uat.md contains keywords `Wave 0`, `content-type` (or `content_type`), `niche-detector` (or `niche detector`), `cost_cents`
    - Both files contain the three Pass / Fail / Deferred Permanently radio options
    - Both files contain a defer-permanently rationale template
    - `wc -w .planning/research/verif-phase3-smoke.md` reports between 150 and 600 words
    - `wc -w .planning/research/verif-phase4-uat.md` reports between 150 and 600 words
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/research/verif-phase3-smoke.md && test -f .planning/research/verif-phase4-uat.md && grep -q 'SSE' .planning/research/verif-phase3-smoke.md && grep -q 'cache' .planning/research/verif-phase3-smoke.md && grep -q 'Wave 0' .planning/research/verif-phase4-uat.md && grep -q 'cost_cents' .planning/research/verif-phase4-uat.md && grep -q 'DEFERRED PERMANENTLY' .planning/research/verif-phase3-smoke.md && grep -q 'DEFERRED PERMANENTLY' .planning/research/verif-phase4-uat.md</automated>
  </verify>
  <done>Both checklist files exist with correct test sections, Pass/Fail/Deferred outcome block, and defer-permanently rationale template.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human executes the three checklists against live deploy and records outcomes</name>
  <what-built>
    Three manual verification checklists at:
    - .planning/research/verif-phase2-uat.md (VERIF-01)
    - .planning/research/verif-phase3-smoke.md (VERIF-02)
    - .planning/research/verif-phase4-uat.md (VERIF-03)
  </what-built>
  <how-to-verify>
    1. Open the live Vercel deploy (URL from PROJECT.md).
    2. Execute each of the three checklists end-to-end, filling in the Steps placeholders as you go.
    3. For each checklist, check exactly ONE of: PASS / FAIL / DEFERRED PERMANENTLY.
    4. If DEFERRED PERMANENTLY, fill in the rationale block (what was attempted / blocker / why acceptable).
    5. Commit the three filled-in files.
    6. When done, type a one-liner per VERIF item to the agent in this form:
       - `VERIF-01: PASS` (or `FAIL` or `DEFERRED PERMANENTLY: <one-line rationale>`)
       - `VERIF-02: ...`
       - `VERIF-03: ...`

    Expected: at minimum, all three checklists have a chosen outcome and (if deferred) a rationale. The agent will then record the outcomes in 18-04-SUMMARY.md and close VERIF-01/02/03 in the milestone roadmap accordingly.
  </how-to-verify>
  <resume-signal>Provide outcome lines for VERIF-01, VERIF-02, VERIF-03 (e.g. "VERIF-01: PASS / VERIF-02: PASS / VERIF-03: DEFERRED PERMANENTLY: niche-detector cost_cents always 0 while omni-plus is free")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation-only | This plan produces markdown checklists and records outcomes. No production code is modified. No external input crosses any trust boundary in this plan's deliverables. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-11 | Repudiation | UAT outcome unrecorded | mitigate | Each checklist forces a single explicit outcome (PASS / FAIL / DEFERRED PERMANENTLY). Defer requires a rationale block. The checkpoint task explicitly demands per-VERIF outcome lines from the human. Outcomes are committed to git via the checklist files + the SUMMARY. |
| T-18-12 | Information Disclosure | Test artifacts may include real user data | accept | Checklists are template-only — no production data is captured in this plan. If the user pastes real `creator_profiles` row data into the filled-in checklist, that lives in `.planning/research/` (a tracked, internal-only directory). The repo is private; no additional exposure beyond standard repo permissions. |
</threat_model>

<verification>
After completion:
- All three checklist files exist at the expected paths
- Each file has a clearly chosen outcome (PASS / FAIL / DEFERRED PERMANENTLY)
- The 18-04-SUMMARY.md records the outcome for VERIF-01, VERIF-02, VERIF-03 with one-line rationale per item
</verification>

<success_criteria>
- VERIF-01 has an explicit outcome recorded
- VERIF-02 has an explicit outcome recorded
- VERIF-03 has an explicit outcome recorded
- Per D-19, any FAIL or DEFERRED PERMANENTLY outcome includes the rationale block filled in
</success_criteria>

<output>
After completion, create `.planning/phases/18-m1-verification-debt-closure/18-04-SUMMARY.md` documenting:
- VERIF-01 outcome + path to the filled checklist
- VERIF-02 outcome + path to the filled checklist
- VERIF-03 outcome + path to the filled checklist
- For any DEFERRED PERMANENTLY: a one-paragraph rationale extract
</output>
