---
phase: 09-living-audience-interactive-simulation-ux
plan: 05
type: execute
wave: 4
depends_on: [09-04, 09-01]
files_modified:
  - src/components/audience-lens/PopulationSwarm.tsx
  - src/components/audience-lens/__tests__/population-swarm.test.tsx
  - src/components/audience-lens/AudienceLens.tsx
autonomous: true
requirements: [LIVE-05]
must_haves:
  truths:
    - "Population·1,000 renders a deterministic mulberry32 swarm instantiated from the 10 (zero model calls, byte-identical across renders) — D-02, determinism-gate-safe"
    - "Live counters are the weighted rollup of the 10 — Population and Panel·10 show the SAME aggregate numbers"
    - "Cascade-on-play runs as ONE batched motion timeline (NOT 1,000 SMIL elements), gated on reducedMotion; sr-only aggregate + archetype-breakdown mirror always present"
    - "An archetype breakdown with representative verbatims is the v1 drill path (per-dot drill DEFERRED, D-01); the scale toggle remembers last-used per-Read (D-07)"
  artifacts:
    - path: "src/components/audience-lens/PopulationSwarm.tsx"
      provides: "1,000-dot deterministic swarm, weighted-rollup counters, batched cascade, sr-only mirror, archetype breakdown"
      min_lines: 60
  key_links:
    - from: "src/components/audience-lens/PopulationSwarm.tsx"
      to: "src/components/audience-lens/lens-derive.ts"
      via: "instantiatePopulation + weightedRollup (the pure math from 09-01)"
      pattern: "instantiatePopulation"
    - from: "src/components/audience-lens/AudienceLens.tsx"
      to: "src/components/audience-lens/PopulationSwarm.tsx"
      via: "the Population·1,000 toggle side renders the swarm"
      pattern: "PopulationSwarm"
---

<objective>
Build Population·1,000 as a lean honest v1: a deterministic `mulberry32` 1,000-dot swarm instantiated from the 10 (zero model calls), live counters that are the weighted rollup of the 10, a batched cascade-on-play, and an archetype breakdown drill — wired into the scale toggle.

Purpose: The "missing touch" the locked sketch 005 was chosen for (D-01) — but the riskiest part (per-dot drill) is held back; archetype breakdown is the v1 drill. Two hard risks: 1,000-node perf (Pitfall 2 — single batched timeline, profile mobile, canvas-eligible) and determinism at scale (Pitfall 3 — mulberry32 only). Population is PRESENTATION over the 10's real verdicts — it must NOT touch the scoring path (STATE same-video score-identity protected).
Output: `PopulationSwarm.tsx` + perf/determinism tests, wired into the AudienceLens Population toggle side.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-RESEARCH.md
@.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-CONTEXT.md
@.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-UI-SPEC.md
@src/components/board/_kit/PersonaGraph.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PopulationSwarm — deterministic 1,000-dot render + weighted counters + tests</name>
  <files>src/components/audience-lens/PopulationSwarm.tsx, src/components/audience-lens/__tests__/population-swarm.test.tsx</files>
  <read_first>
    - src/components/audience-lens/lens-derive.ts (from 09-01 — `instantiatePopulation(nodes, {total,seed})` returns the deterministic dots; `weightedRollup(nodes)` is the counter SSOT; consume these, do NOT re-derive geometry math in the component beyond x/y placement)
    - src/components/board/_kit/PersonaGraph.tsx (the SVG `<circle>` render pattern + the `mulberry32` Box-Muller scatter for x/y placement — scale to 1,000; the existing 200-dot layout is the proven pattern; sr-only `<ul>` mirror to mirror)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-CONTEXT.md (D-02 honesty model — proportional distribution, bounded seeded variance, 30% affinity-neighbour blend, "1,000 viewers instantiated from your 10 calibrated archetypes" label; D-01 per-dot drill DEFERRED)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-UI-SPEC.md (Typography: Display 30px for the big live counters; Color: cream-alpha dots, coral worst-cluster only; honesty label --color-cream-muted; Spacing exemption for swarm geometry)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-RESEARCH.md (Pitfall 2 perf; Pitfall 3 hydration determinism; Architectural Responsibility Map — Population is Browser/Client, ZERO model calls / no API tier)
  </read_first>
  <behavior>
    - Test 1 (deterministic render): rendering PopulationSwarm twice with the same nodes + seed produces identical dot positions (snapshot/structural deep-equal) — guards hydration + the determinism gate.
    - Test 2 (counter identity): the rendered live counters equal `weightedRollup(nodes)` — i.e. Population and Panel·10 show the SAME aggregate numbers (D-02).
    - Test 3 (a11y mirror): an sr-only aggregate + archetype-breakdown mirror is always present in the DOM, independent of motion state (UI-SPEC reduced-motion mirror copy).
    - Test 4 (no scoring-path touch): the component imports NOTHING from the engine scoring/prediction modules (no runPredictionPipeline/aggregateScores/ENGINE_VERSION import) — Population is presentation only.
  </behavior>
  <action>
    Create `src/components/audience-lens/PopulationSwarm.tsx` (`'use client'`): render ~1,000 dots from `instantiatePopulation(nodes, {total:1000, seed})`, placing x/y via the `mulberry32` Box-Muller pattern (NO Math.random/Date.now — Pitfall 3). Render dots as a SINGLE batched SVG layer (NOT 1,000 per-circle `<animate>` SMIL elements — Pitfall 2); the worst cluster reads coral, all else cream-alpha. Show live counters (Display 30px) = `weightedRollup(nodes)` (the SAME numbers Panel shows). Always render the sr-only aggregate + archetype-breakdown mirror. Add the honesty label "1,000 viewers instantiated from your 10 calibrated archetypes." (--color-cream-muted). Build the archetype breakdown (rows per archetype with representative verbatim) as the v1 drill path — NO per-dot tap-to-drill (D-01 deferred). Import NOTHING from the engine scoring path. Add the four behavior tests. Keep under 500 lines.
  </action>
  <verify>
    <automated>npx vitest run src/components/audience-lens/__tests__/population-swarm.test.tsx && grep -L 'Math.random\|Date.now\|runPredictionPipeline\|aggregateScores\|ENGINE_VERSION' src/components/audience-lens/PopulationSwarm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Swarm is deterministic (Test 1) and makes zero model calls / no scoring-path import (Test 4).
    - Counters === weightedRollup(nodes) — Population and Panel agree (Test 2).
    - sr-only aggregate + archetype-breakdown mirror always present (Test 3).
    - Honesty label present; cream-alpha dots; coral worst-cluster only.
    - No `Math.random`/`Date.now`/scoring-path imports.
    - `npm run build` + `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Population·1,000 renders an honest, deterministic swarm whose counters are literally the weighted rollup of the 10 — same signal, denser resolution, zero new prediction.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PopulationSwarm into the scale toggle + batched cascade-on-play</name>
  <files>src/components/audience-lens/AudienceLens.tsx</files>
  <read_first>
    - src/components/audience-lens/AudienceLens.tsx (the Panel·10 ⇄ Population·1,000 scale-toggle scaffold from 09-02 with the Population placeholder slot + the per-Read last-used default — D-07; fill the slot with PopulationSwarm)
    - src/components/audience-lens/PopulationSwarm.tsx (from Task 1)
    - src/components/audience-lens/ReplayController.tsx (from 09-02 — the cascade driver; the Population cascade-on-play reuses one batched motion timeline, gated on reducedMotion)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-UI-SPEC.md (Interaction Contract: scale toggle per-Read last-used; "Population · 1,000" side = swarm + counters + cascade + breakdown; Motion/a11y gate)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-CONTEXT.md (D-07 toggle per-Read remembers last-used; D-01 lean v1)
  </read_first>
  <action>
    Replace the Population placeholder slot in AudienceLens (from 09-02) with `<PopulationSwarm>`, fed the same `nodes` the Panel uses (one source — D-02 honesty). Confirm the scale toggle persists last-used per-Read (D-07) and that switching Panel⇄Population does not refetch or re-score (pure presentation). Drive cascade-on-play through ONE batched motion timeline (staggered opacity over the dot layer — NOT 1,000 SMIL elements, Pitfall 2), gated on `reducedMotion` (static + the sr-only mirror when reduced). Keep AudienceLens under 500 lines.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -qi 'error' && exit 1 || echo OK; grep -q 'PopulationSwarm' src/components/audience-lens/AudienceLens.tsx</automated>
    <human-check>On a real mobile viewport, open the Lens on a Reading, toggle to Population·1,000, press Play: the 1,000-dot cascade animates smoothly without dropped frames / main-thread jank; the live counters match the Panel·10 numbers; with OS reduce-motion on, the swarm renders static with the sr-only summary. If SVG janks, fall the swarm-only render to canvas (per-dot drill is deferred, so canvas is viable — A3).</human-check>
  </verify>
  <acceptance_criteria>
    - Population·1,000 toggle side renders PopulationSwarm fed the same nodes as Panel; switching does not refetch/re-score.
    - Scale toggle remembers last-used per-Read (D-07).
    - Cascade-on-play is one batched timeline, reducedMotion-gated.
    - Human-check: smooth on mobile (or canvas fallback applied), counters match Panel, reduced-motion static path correct.
    - `npm run build` + `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Population·1,000 is live in the scale toggle: deterministic swarm + weighted counters + batched cascade, per-Read last-used, mobile-profiled (canvas fallback eligible).</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/audience-lens/__tests__/population-swarm.test.tsx` → green (determinism + counter identity + a11y mirror + no scoring-path touch).
- No `Math.random`/`Date.now`/scoring-path imports in PopulationSwarm (landmines 2, 3).
- Cascade = one batched timeline, reducedMotion-gated; sr-only mirror always present (Pitfall 2, landmine 5).
- Counters === weightedRollup === Panel·10 numbers (D-02 honesty).
- Human-check: mobile-smooth (or canvas fallback), reduced-motion static path correct.
- `npm run build` + `npm run lint` exit 0; engine suite still green (Population never touched the scoring path).
</verification>

<success_criteria>
- The audience scales from 10 to 1,000 as one honest signal at two resolutions — deterministic, gate-safe, never a fabricated crowd. The "missing touch" of sketch 005 ships as a lean v1.
</success_criteria>

<output>
Create `.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-05-SUMMARY.md` when done.
</output>
