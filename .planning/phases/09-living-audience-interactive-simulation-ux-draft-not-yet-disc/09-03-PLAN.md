---
phase: 09-living-audience-interactive-simulation-ux
plan: 03
type: execute
wave: 2
depends_on: [09-02]
files_modified:
  - src/lib/tools/runners/chat-runner.ts
  - src/lib/tools/runners/__tests__/chat-runner.test.ts
  - src/components/audience-lens/PersonaChatDrawer.tsx
  - src/components/audience-lens/AudienceLens.tsx
  - src/lib/tools/blocks.ts
  - src/app/api/tools/chat/route.ts
autonomous: true
requirements: [LIVE-03]
must_haves:
  truths:
    - "Tapping 'Ask them why →' on a persona node opens an in-context chat drawer scoped to the current Read (not a new top-level thread; chat stays OUT of the SkillId union)"
    - "The persona answers in-voice about THIS concept, grounded on its persona-registry prompt + its reaction (verdict/quote) + the concept text — routed through the existing <<<USER_CONTENT>>> fence"
    - "ARCHETYPE_DEFINITIONS is read, never mutated (cache byte-stability preserved)"
    - "The Q&A persists as a sub-thread within the Read and re-appears on reopen — verified to need NO migration, or a [BLOCKING] migration is applied"
  artifacts:
    - path: "src/components/audience-lens/PersonaChatDrawer.tsx"
      provides: "In-context chat drawer over the cloud, one persona at a time, scoped to the Read"
    - path: "src/lib/tools/runners/chat-runner.ts"
      provides: "Additive optional personaGrounding param on ChatPipelineInput — byte-identical for non-persona chat"
      contains: "personaGrounding"
  key_links:
    - from: "src/components/audience-lens/PersonaChatDrawer.tsx"
      to: "src/app/api/tools/chat/route.ts"
      via: "POSTs ask + personaGrounding, streams runChatPipeline tokens"
      pattern: "personaGrounding"
    - from: "src/lib/tools/runners/chat-runner.ts"
      to: "src/lib/engine/wave3/persona-registry.ts"
      via: "READS ARCHETYPE_DEFINITIONS[archetype] as grounding input (never mutates)"
      pattern: "ARCHETYPE_DEFINITIONS"
---

<objective>
Extend `chat-runner.ts` with an additive optional `personaGrounding` param so a tapped persona answers IN-VOICE about this concept, and build the in-context `PersonaChatDrawer` over the cloud, persisting the Q&A as a sub-thread within the Read.

Purpose: The flagship cheatcode (D-03). Isolated, test-anchored, HIGH-risk slice — the two failure modes are (a) cache invalidation from mutating ARCHETYPE_DEFINITIONS (Pitfall 5) and (b) sub-thread persistence at the schema boundary (Assumption A1). Both are de-risked here with an explicit verify-first task.
Output: `personaGrounding` param + tests, `PersonaChatDrawer.tsx`, drawer mounted in AudienceLens, sub-thread persistence (with a conditional [BLOCKING] migration only if verification proves one is needed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-RESEARCH.md
@.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-CONTEXT.md
@.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-UI-SPEC.md
@src/lib/tools/runners/chat-runner.ts
@src/lib/threads/messages.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend chat-runner with additive personaGrounding param + tests</name>
  <files>src/lib/tools/runners/chat-runner.ts, src/lib/tools/runners/__tests__/chat-runner.test.ts</files>
  <read_first>
    - src/lib/tools/runners/chat-runner.ts (`ChatPipelineInput` L46-64 — ask/platform/profileRow/priorTurns/audience; `runChatPipeline(input, onToken)`; the assembleBundle `<<<USER_CONTENT>>>` fence (D-07, injection-safe); the stream/timeout/abort machinery L168-205 which must stay UNTOUCHED — reuse-contract row 4)
    - src/lib/engine/wave3/persona-registry.ts (`ARCHETYPE_DEFINITIONS` + `ARCHETYPE_TRIGGERS` keyed by Archetype enum — byte-stable; the registry header warns "changing a single character invalidates the cache prefix"; READ as inputs, NEVER mutate — Pitfall 5 / landmine 4)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-RESEARCH.md (Code Examples §chat-runner extension surface — the exact personaGrounding shape; Security Domain — route concept/quote through the same fenced anchor, not raw into the system prompt)
  </read_first>
  <behavior>
    - Test 1 (additive no-op): runChatPipeline called WITHOUT personaGrounding produces a byte-identical assembled bundle vs the pre-change path (existing chat unaffected).
    - Test 2 (in-voice grounding): when personaGrounding = {archetype, reactionToConcept:{verdict,quote}, conceptText} is present, the assembled prompt PREPENDS the persona system prompt (from ARCHETYPE_DEFINITIONS[archetype]) + the reaction context; the concept text + quote flow through the <<<USER_CONTENT>>> fence (not raw into the system prompt).
    - Test 3 (registry immutability): after a grounded run, ARCHETYPE_DEFINITIONS is byte-identical to before (the runner reads, never writes) — assert deep-equal on a snapshot.
  </behavior>
  <action>
    Add `personaGrounding?: { archetype: Archetype; reactionToConcept: { verdict: "stop" | "scroll"; quote: string }; conceptText: string }` to `ChatPipelineInput` (optional → byte-identical for non-persona chat). When present, assemble a NEW grounding string from `ARCHETYPE_DEFINITIONS[archetype]` (+ `ARCHETYPE_TRIGGERS` if useful) prepended to the bundle so Qwen answers in-voice; route `conceptText` and `quote` through the existing `<<<USER_CONTENT>>>` fenced anchor (Security Domain) — NEVER concatenate user content raw into the system prompt, NEVER mutate the registry module. Leave the stream/timeout/abort path (L168-205) and `isColdStart` untouched. Add the three behavior tests.
  </action>
  <verify>
    <automated>npx vitest run src/lib/tools/runners/__tests__/chat-runner.test.ts && grep -q 'personaGrounding' src/lib/tools/runners/chat-runner.ts</automated>
  </verify>
  <acceptance_criteria>
    - `personaGrounding` is an OPTIONAL field; non-persona chat path is byte-identical (Test 1 green).
    - Grounding reads ARCHETYPE_DEFINITIONS, never mutates it (Test 3 green).
    - Concept/quote routed through `<<<USER_CONTENT>>>` fence (Test 2 green).
    - `npm run build` + `npm run lint` exit 0.
  </acceptance_criteria>
  <done>chat-runner answers in-voice when persona-grounded, with zero impact on existing chat and zero registry mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Verify sub-thread persistence model — confirm NO migration (or add a [BLOCKING] one)</name>
  <files>src/lib/tools/blocks.ts</files>
  <read_first>
    - src/lib/threads/messages.ts (`insertMessage(threadId, role, blocks)` writes a row with body = typed-blocks JSONB array; `loadMessages` re-validates each block via the registry; messages belong to a thread via `thread_id` — there is NO parent_message_id column)
    - supabase/migrations/20260617000000_threads_messages.sql (messages table DDL: id/thread_id/role/body/created_at — confirm the absence of any sub-thread parent ref; the Read is ALREADY a grounded thread via threads.reading_id)
    - src/lib/tools/blocks.ts (the existing typed-block schemas registered with the renderer — the pattern to add a new `persona-chat-turn` block, mirroring how chat/card blocks persist)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-RESEARCH.md (Runtime State Inventory + Assumption A1 — the planner-flagged verification)
  </read_first>
  <action>
    Resolve A1 decisively. The grounded Read is ALREADY a thread (`threads.reading_id`), and messages persist as typed-block JSONB rows in that thread — so a persona-chat sub-thread can be modeled WITHOUT a migration: add a new `persona-chat-turn` typed block to `blocks.ts` (props: `{ archetype: string; role: "user" | "assistant"; text: string }`) and persist these as `messages` rows in the SAME grounded thread via the existing `insertMessage`; "sub-thread" = the subset of messages whose blocks are `persona-chat-turn` for a given archetype. Register the block in the renderer registry so it round-trips through `loadMessages` (D-14 validation). CONFIRM in the SUMMARY that NO migration is required (state this explicitly — the researcher flagged it). ONLY IF the existing thread/message model genuinely cannot carry these rows (it can — verified above): create the migration file under supabase/migrations/ AND add a [BLOCKING] `supabase db push` task BEFORE this plan's verification (env workaround: `SUPABASE_ACCESS_TOKEN` set). Do not invent a parent_message_id column unless a real blocker forces it.
  </action>
  <verify>
    <automated>npx vitest run src/lib/tools/runners/__tests__ 2>/dev/null; grep -q 'persona-chat-turn\|personaChatTurn\|PersonaChatTurn' src/lib/tools/blocks.ts && npm run build 2>&1 | grep -qi 'error' && exit 1 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - A `persona-chat-turn` typed block exists in blocks.ts and is registered in the renderer registry (round-trips loadMessages validation).
    - The SUMMARY states explicitly whether a migration was needed; given the existing model it should state "NO migration required — persona-chat-turn rows persist in the existing grounded thread."
    - If (and only if) a migration was genuinely required, it exists + was applied via a [BLOCKING] `supabase db push` task before verification.
    - `npm run build` + `npm run lint` exit 0.
  </acceptance_criteria>
  <done>The sub-thread persistence model is resolved: persona-chat turns persist as typed blocks in the existing grounded thread, no migration required (or a [BLOCKING] migration applied if proven necessary).</done>
</task>

<task type="auto">
  <name>Task 3: PersonaChatDrawer + chat route wiring + mount in AudienceLens</name>
  <files>src/components/audience-lens/PersonaChatDrawer.tsx, src/app/api/tools/chat/route.ts, src/components/audience-lens/AudienceLens.tsx</files>
  <read_first>
    - src/app/api/tools/chat/route.ts (the existing SSE chat route that calls `runChatPipeline(input, onToken)` — extend its accepted body to pass `personaGrounding` through; reuse the streaming/error envelope, no new SSE machinery)
    - src/components/audience-lens/AudienceLens.tsx (built in 09-02 — the drawer mounts over the cloud; "Ask them why →" is the per-node secondary CTA)
    - src/components/audience-lens/PersonaChatDrawer.tsx (does not exist — create)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-UI-SPEC.md (Copywriting: "Ask them why →", chat-drawer empty/prompt "Ask {archetype} why they reacted this way.", error copy verbatim reuses SkillRunError; Interaction Contract: in-context drawer, one persona at a time; Color/Spacing for the drawer surface)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-CONTEXT.md (D-03 — one persona at a time; switching persona = new sub-conversation; chat NOT in SkillId union)
  </read_first>
  <action>
    Create `src/components/audience-lens/PersonaChatDrawer.tsx` (`'use client'`): an in-context drawer/sheet opened by "Ask them why →" on a persona node, scoped to the current Read, one persona at a time. It POSTs `{ ask, personaGrounding: { archetype, reactionToConcept, conceptText } }` to the chat route and streams the in-voice answer; it loads prior `persona-chat-turn` blocks for this archetype from the Read's thread on open (sub-thread rehydration) and appends new turns via the persistence path from Task 2. Reuse the shipped `SkillRunError` surface for errors (no new error UI) and the exact UI-SPEC copy. Extend `src/app/api/tools/chat/route.ts` to accept + forward `personaGrounding` into `runChatPipeline` (length-capped server-side like the other anchors — WARNING-5; no new boundary). Mount the drawer in `AudienceLens` wired to the node "Ask them why →" CTA. Keep `chat` OUT of the SkillId union (landmine 8). Files under 500 lines.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -qi 'error' && exit 1 || echo OK; grep -q 'personaGrounding' src/app/api/tools/chat/route.ts && grep -q 'Ask' src/components/audience-lens/PersonaChatDrawer.tsx && grep -q 'PersonaChatDrawer' src/components/audience-lens/AudienceLens.tsx</automated>
  </verify>
  <acceptance_criteria>
    - "Ask them why →" opens the drawer scoped to one persona; the answer streams in-voice from the chat route with personaGrounding.
    - Prior persona-chat turns rehydrate on reopen (sub-thread persistence round-trips).
    - Errors reuse SkillRunError; copy matches UI-SPEC verbatim.
    - `chat` is NOT added to the SkillId union.
    - `npm run build` + `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Chat-with-persona is live: tap a node → ask why → in-voice grounded answer, persisted as a sub-thread within the Read and rehydrated on reopen.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/tools/runners/__tests__/chat-runner.test.ts` → green (additive no-op + grounding + registry immutability).
- ARCHETYPE_DEFINITIONS byte-identical after grounded runs (no cache invalidation — landmine 4).
- Persona-chat turns persist + rehydrate; SUMMARY states migration disposition explicitly.
- `chat` absent from SkillId union (landmine 8); concept/quote fenced (Security Domain).
- `npm run build` + `npm run lint` exit 0.
</verification>

<success_criteria>
- A creator can tap any persona and ask it why — and get a grounded, in-voice answer about THIS concept, persisted within the Read. The flagship cheatcode works without forking the chat runner or mutating the persona registry.
</success_criteria>

<output>
Create `.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-03-SUMMARY.md` when done.
</output>
