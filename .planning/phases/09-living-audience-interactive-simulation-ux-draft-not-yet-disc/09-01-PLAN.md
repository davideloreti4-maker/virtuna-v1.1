---
phase: 09-living-audience-interactive-simulation-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - src/components/audience-lens/lens-derive.ts
  - src/components/audience-lens/__tests__/lens-derive.test.ts
autonomous: true
requirements: [LIVE-01, LIVE-02, LIVE-03, LIVE-04, LIVE-05, LIVE-06, LIVE-07]
must_haves:
  truths:
    - "LIVE-01..LIVE-07 exist as formal requirement entries in REQUIREMENTS.md with a Phase 9 traceability row each"
    - "lens-derive.ts instantiates ~1,000 dots from the 10 personas deterministically — byte-identical across two calls with the same seed"
    - "Population aggregate counters equal the weighted rollup of the 10 (same numbers Panel·10 shows)"
    - "Cascade order is a pure, deterministic function of verdict/weight (no Math.random / Date.now)"
  artifacts:
    - path: "src/components/audience-lens/lens-derive.ts"
      provides: "Pure 1,000-from-10 instantiation, weighted rollup, cascade ordering — the Population/cascade math core"
      exports: ["instantiatePopulation", "weightedRollup", "cascadeOrder"]
    - path: "src/components/audience-lens/__tests__/lens-derive.test.ts"
      provides: "Unit + determinism tests for lens-derive"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Formal LIVE-01..LIVE-07 entries + traceability rows"
      contains: "LIVE-01"
  key_links:
    - from: "src/components/audience-lens/lens-derive.ts"
      to: "src/components/board/_kit/PersonaGraph.tsx"
      via: "mulberry32 seeded-RNG pattern (copied, not imported if not exported)"
      pattern: "mulberry32"
    - from: "src/components/audience-lens/lens-derive.ts"
      to: "src/components/board/audience/audience-derive.ts"
      via: "consumes PersonaNode[] from buildPersonaNodes (weight drives dot count)"
      pattern: "PersonaNode"
---

<objective>
Formalize the provisional LIVE-01..LIVE-07 requirements into REQUIREMENTS.md (planning-hygiene prerequisite — zero LIVE-* entries exist today), and build the pure deterministic math core (`lens-derive.ts`) that Population·1,000 (W4) and the staggered cascade (W3) both consume.

Purpose: Unblock coverage tracking and isolate the two highest-risk numeric behaviors (1,000-from-10 instantiation, weighted rollup) as pure, test-anchored functions BEFORE any React/perf work touches them. Determinism is the load-bearing property (SSR hydration + engine-determinism gate, D-02).
Output: REQUIREMENTS.md LIVE-* entries; `lens-derive.ts` (pure, no React); a test file proving determinism + weighted-rollup identity.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-RESEARCH.md
@.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Formalize LIVE-01..LIVE-07 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (match the exact `### SECTION` + `- [ ] **ID**: desc` bullet format used by HOOKS/STUDIO/SCRIPT sections, lines 53-78; and the `| Requirement | Phase | Status |` traceability table starting line 116)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-RESEARCH.md (§Phase Requirements table — provisional ID descriptions)
  </read_first>
  <action>
    Add a new `### LIVE — Living Audience (AudienceLens, P9)` section to REQUIREMENTS.md immediately after the SCRIPT/REMIX section (before the `---` preceding Cross-cutting constraints). Add seven `- [ ] **LIVE-NN**: ...` bullets using these descriptions verbatim from 09-RESEARCH §Phase Requirements:
    LIVE-01 reaction replay (segment-by-segment on the video Test timeline via HeatmapPayload.personas[].attentions[]; staggered cascade elsewhere — D-06);
    LIVE-02 node drill-down (tap dot → archetype + verbatim reaction to this concept);
    LIVE-03 chat-with-persona (in-voice, grounded via chat-runner extension; sub-thread persisted within the Read);
    LIVE-04 segment clustering (Temp × Disposition lens via buildSegmentGroups);
    LIVE-05 Population·1,000 (deterministic mulberry32 swarm + live counters + cascade + archetype breakdown — D-02, zero model calls);
    LIVE-06 AudienceLens retrofit (one reusable Lens across all 6 skills, degrade by feature — D-04);
    LIVE-07 Rewrite-for-audience loop (lever-as-steering via CHAIN_HANDOFFS → new card + Read showing delta — D-05).
    Then append seven rows to the Traceability table: `| LIVE-NN | Phase 9 | Pending |` for each. Do NOT touch existing requirement rows.
  </action>
  <verify>
    <automated>grep -c -E '\*\*LIVE-0[1-7]\*\*' .planning/REQUIREMENTS.md | grep -qx 7 && grep -v '^#' .planning/REQUIREMENTS.md | grep -c -E '\| LIVE-0[1-7] \| Phase 9 \|' | grep -qx 7 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All seven LIVE-01..LIVE-07 bullets present under a `### LIVE` section.
    - All seven traceability rows present, each mapping to Phase 9, status Pending.
    - No existing requirement bullet or row modified.
  </acceptance_criteria>
  <done>REQUIREMENTS.md carries formal LIVE-01..LIVE-07 entries + traceability rows; coverage tracking can resolve every Phase 9 requirement ID.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build lens-derive.ts — pure deterministic Population/cascade math + tests</name>
  <files>src/components/audience-lens/lens-derive.ts, src/components/audience-lens/__tests__/lens-derive.test.ts</files>
  <read_first>
    - src/components/board/_kit/PersonaGraph.tsx (the `mulberry32(seed)` impl at L43-51 and the per-persona seeding pattern `mulberry32(9173 + i*31337)` + Box-Muller scatter — the determinism pattern to reuse; copy the function, do NOT add Math.random)
    - src/components/board/audience/audience-derive.ts (the `PersonaNode = {id,label,weight,watchThrough,segment?,dropAt?,tone?}` shape and `buildPersonaNodes` — weight is the proportional-distribution driver; this module CONSUMES PersonaNode[], it does not re-derive nodes)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-RESEARCH.md (Pattern 1 deterministic seeded layout; Pattern 2 weighted rollup = same signal two resolutions; D-02 honesty model)
  </read_first>
  <behavior>
    - Test 1 (instantiation count): instantiatePopulation(nodes, {total:1000, seed}) returns ~1000 dots; sum of per-archetype dot counts === total; counts are proportional to node weight (highest-weight archetype gets the most dots).
    - Test 2 (determinism): two calls to instantiatePopulation with the SAME nodes + seed produce byte-identical output (deep-equal) — guards SSR hydration + the engine-determinism gate.
    - Test 3 (weighted rollup identity): weightedRollup(nodes) returns aggregate stop/scroll counters that, normalized, equal the Panel·10 aggregate (the same weighted verdict mix) — Population and Panel render the same numbers at different resolutions.
    - Test 4 (variance bounded + neighbour blend): each dot carries its archetype's real verdict/sentiment plus bounded seeded jitter; ~30% of dots may blend toward an adjacent archetype (affinity-neighbour), but NO dot invents a verdict the 10 never emitted.
    - Test 5 (cascade order purity): cascadeOrder(nodes) returns a deterministic ordering keyed on verdict/weight (no Math.random/Date.now); two calls return the identical sequence.
  </behavior>
  <action>
    Create `src/components/audience-lens/lens-derive.ts` as a PURE module (no React, no DOM, tree-shakeable). Export:
    `instantiatePopulation(nodes: PersonaNode[], opts: { total?: number; seed: number }): PopulationDot[]` — distribute `total` (default 1000) dots proportional to `node.weight`, each dot stamped with its source archetype + that archetype's real verdict/watchThrough + bounded seeded variance via a local copy of `mulberry32` seeded from a stable index (per D-02; ~70/30 archetype/neighbour-variance split is discretion within the deterministic shape). NEVER call Math.random or Date.now.
    `weightedRollup(nodes: PersonaNode[]): { stop: number; scroll: number; total: number; byArchetype: Array<{archetype: string; stop: number; weight: number}> }` — the weighted rollup of the 10's real verdicts; this is the SSOT for BOTH the Population live counters AND the Panel·10 aggregate (do NOT re-roll band math — reuse the node verdicts).
    `cascadeOrder(nodes: PersonaNode[]): string[]` — deterministic reveal order (by verdict then weight) for the staggered-cascade path (D-06). Export a `PopulationDot` type ({id, archetype, x?, y?, verdict, sentiment} — geometry x/y optional, computed by the swarm component in W4, but the seeded variance + archetype assignment belong here so the math is testable without a DOM).
    Then create the test file exercising the five behaviors above with Vitest.
  </action>
  <verify>
    <automated>npx vitest run src/components/audience-lens/__tests__/lens-derive.test.ts && grep -L 'Math.random\|Date.now' src/components/audience-lens/lens-derive.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/audience-lens/__tests__/lens-derive.test.ts` exits 0 with all five behaviors green.
    - `grep 'Math.random\|Date.now' src/components/audience-lens/lens-derive.ts` returns NO matches (determinism gate).
    - `weightedRollup` output is the single source consumed by both Panel and Population (asserted by Test 3).
    - `npm run lint` and `npm run build` exit 0.
  </acceptance_criteria>
  <done>lens-derive.ts exports instantiatePopulation/weightedRollup/cascadeOrder as pure, deterministic, byte-stable functions; tests prove 1,000-from-10 instantiation, weighted-rollup identity, and cascade-order purity.</done>
</task>

</tasks>

<verification>
- `grep -E '\*\*LIVE-0[1-7]\*\*' .planning/REQUIREMENTS.md` → 7 entries; traceability rows present.
- `npx vitest run src/components/audience-lens/__tests__/lens-derive.test.ts` → green.
- No `Math.random`/`Date.now` in lens-derive.ts.
- `npm run lint` + `npm run build` exit 0.
</verification>

<success_criteria>
- LIVE-01..LIVE-07 formalized; every Phase 9 requirement ID resolvable.
- The Population/cascade math core exists as pure, deterministic, test-anchored functions, decoupled from React/perf concerns — W4 and W3 build on top without re-deriving the math.
</success_criteria>

<output>
Create `.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-01-SUMMARY.md` when done.
</output>
