---
phase: 09-living-audience-interactive-simulation-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/audience-lens/AudienceLens.tsx
  - src/components/audience-lens/ReplayController.tsx
  - src/components/reading/persona-cloud.tsx
  - src/components/reading/reading-panels.tsx
  - src/components/board/_kit/PersonaGraph.tsx
  - src/components/reading/__tests__/persona-cloud.test.tsx
autonomous: true
requirements: [LIVE-01, LIVE-02, LIVE-06]
must_haves:
  truths:
    - "Tapping the persona cloud on the video Test/Reading surface opens the AudienceLens sheet (the onOpen seam is wired — it was a stub wired nowhere)"
    - "The Lens header reuses the P8 multi-audience-read-block verbatim (interpret + lever), not a redesign"
    - "Tapping a persona node shows that archetype + its verbatim reaction to this concept (drill-down)"
    - "On the video Test surface, Replay reactions plays segment-by-segment from HeatmapPayload.attentions[]; gated on reducedMotion with an sr-only mirror"
  artifacts:
    - path: "src/components/audience-lens/AudienceLens.tsx"
      provides: "Reusable Lens sheet shell — header (P8 Read), Panel·10 cloud, degrade switch on signal shape"
      min_lines: 60
    - path: "src/components/audience-lens/ReplayController.tsx"
      provides: "Replay (timeline) vs cascade (flat) controller, reducedMotion-gated"
    - path: "src/components/reading/reading-panels.tsx"
      provides: "onOpen wired → opens AudienceLens with the Reading's heatmap/simResults"
      contains: "AudienceLens"
  key_links:
    - from: "src/components/reading/reading-panels.tsx"
      to: "src/components/audience-lens/AudienceLens.tsx"
      via: "onOpen handler opens the Lens with heatmap + simResults"
      pattern: "AudienceLens"
    - from: "src/components/audience-lens/AudienceLens.tsx"
      to: "src/components/thread/multi-audience-read-block.tsx"
      via: "renders MultiAudienceReadBlockRenderer as the sheet header"
      pattern: "MultiAudienceReadBlock"
    - from: "src/components/audience-lens/ReplayController.tsx"
      to: "src/lib/engine/types.ts"
      via: "reads HeatmapPayload.personas[].attentions[] timeline"
      pattern: "attentions"
---

<objective>
Build the reusable AudienceLens sheet shell and wire the `onOpen` seam on the rich-signal surface — the video Test/Reading view, which already renders `<PersonaGraph height={120}>` inline and already carries the `HeatmapPayload` segment timeline. Ship node drill-down and reaction replay (the timeline path) here.

Purpose: Rich-signal-first (D-04). This is the ONLY surface with a real `segment_reactions` timeline, so it is where replay must mount; it establishes the reusable shell + the open mechanism that W3 will reuse for the thin-signal skills. The `onOpen` seam is net-new wiring (Pitfall 1 — the prop exists on PersonaCloud but NO consumer calls it).
Output: `AudienceLens.tsx` shell, `ReplayController.tsx`, the wired open seam on `reading-panels.tsx`, PersonaGraph extended with replay-driven attention animation, extended cloud test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-RESEARCH.md
@.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-CONTEXT.md
@.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-UI-SPEC.md
@src/components/reading/persona-cloud.tsx
@src/components/board/_kit/PersonaGraph.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: AudienceLens sheet shell + P8 Read header + scale-toggle scaffold</name>
  <files>src/components/audience-lens/AudienceLens.tsx</files>
  <read_first>
    - src/components/thread/multi-audience-read-block.tsx (`MultiAudienceReadBlockRenderer({ block })` + `MultiAudienceReadBlockProps` — the P8 Read: interpret + lever + per-persona drill; REUSE verbatim as the header, do NOT recolor/redesign — landmine 9)
    - src/components/reading/persona-cloud.tsx (`PersonaCloud({ heatmap, simResults, onOpen })` + the degrade rule `if (laidOut.length === 0) return null` — match this empty-state behavior)
    - src/components/board/_kit/PersonaGraph.tsx (`PersonaGraph({ personas, height, reducedMotion, className })` + `PersonaNode` shape — the Panel·10 render base)
    - src/components/board/audience/audience-derive.ts (`buildPersonaNodes`, `buildSegmentGroups`, `worstBadGroupKey` — the same calls PersonaCloud makes to produce nodes)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-UI-SPEC.md (Color: flat-matte charcoal surfaces, coral reserved for worst-cluster + Rewrite CTA only; Spacing; Interaction Contract; Copywriting — empty/cold-start/error copy verbatim)
  </read_first>
  <action>
    Create `src/components/audience-lens/AudienceLens.tsx` as a `'use client'` Radix dialog/sheet. Props accept the opened Read's data: `{ heatmap, simResults, readBlock?, reducedMotion?, open, onOpenChange }` (readBlock = the P8 multi-audience-read block when present). Layout top-to-bottom per UI-SPEC: (1) HEADER = render `MultiAudienceReadBlockRenderer` when readBlock present (interpret + lever — verbatim, no recolor); (2) a scale-toggle scaffold (Radix tabs/switch) labeled "Panel · 10" ⇄ "Population · 1,000" — Population side renders a placeholder slot this wave (W4 fills PopulationSwarm); the toggle state is per-Lens-instance and seeds from a last-used value (a simple module/localStorage-backed default is fine — D-07); (3) Panel·10 region = `PersonaGraph` over `buildPersonaNodes(heatmap, simResults, worstBadGroupKey(buildSegmentGroups(...)))`. Flat-matte surfaces only (`--color-charcoal-app`/`--color-surface`); NO glass; if any blur is ever added use inline `style={{ backdropFilter }}` not a class (Pitfall 4). Empty-signal path: when buildPersonaNodes returns [], omit the cloud (match PersonaCloud's `return null`) and show the "No audience reaction yet." empty copy only where a reaction was expected. Keep the file under 500 lines; split helper render fns if needed.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -qi 'error' && exit 1 || echo OK; grep -q 'MultiAudienceReadBlock' src/components/audience-lens/AudienceLens.tsx && grep -q 'Panel · 10' src/components/audience-lens/AudienceLens.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AudienceLens renders the P8 Read header via MultiAudienceReadBlockRenderer (import present; no recolor).
    - Scale toggle present with "Panel · 10" / "Population · 1,000" labels; Population side is a placeholder slot (W4 fills it).
    - Panel·10 region renders PersonaGraph over buildPersonaNodes output; empty-nodes path omits the cloud.
    - No glass / no backdrop-filter class; flat-matte THEME-06 surfaces only.
    - `npm run build` + `npm run lint` exit 0.
  </acceptance_criteria>
  <done>The reusable AudienceLens sheet exists: P8 Read header reused verbatim, Panel·10 cloud renders, scale-toggle scaffold present with the Population slot reserved for W4.</done>
</task>

<task type="auto">
  <name>Task 2: ReplayController + PersonaGraph replay-driven attention animation</name>
  <files>src/components/audience-lens/ReplayController.tsx, src/components/board/_kit/PersonaGraph.tsx</files>
  <read_first>
    - src/lib/engine/types.ts (`HeatmapPayload.personas[]` shape: `{ id, slot_type, archetype?, attentions: number[], swipe_predicted_at, segment_reasons }` — `attentions` length === segments.length is THE only real timeline; Shape A from 09-RESEARCH Code Examples)
    - src/components/board/_kit/PersonaGraph.tsx (the existing `<animate>` pulse gated on `reducedMotion` at ~L207, the sr-only `<ul>` mirror, and the `mulberry32` seed plumbing — EXTEND with an optional replay/attention input; do NOT change layout math, hover/tap card, or link drawing — reuse-contract row 1)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-UI-SPEC.md (Replay control copy: "Replay reactions" for Test/Read segment-by-segment; reduced-motion sr-only mirror copy verbatim; Motion/a11y contract)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-CONTEXT.md (D-06 — segment-by-segment ONLY where a timeline exists; NO synthesized pseudo-timeline)
  </read_first>
  <action>
    Create `src/components/audience-lens/ReplayController.tsx` (`'use client'`): a controller that, given a signal shape, drives the cloud. When `attentions` timelines are present (video Test path), it exposes a "Replay reactions" control that advances a segment index, feeding per-segment attention into the cloud so nodes light/dim segment-by-segment as "the room watches." When no timeline is present (flat skills), it exposes a "Play" control that drives a staggered cascade — but this wave only needs the timeline path live; leave the cascade entry point wired to a `cascadeOrder`-driven stub that W3 completes (import the type seam, do not block on W3). ALL motion gated on `reducedMotion`: when reduced, render static + the sr-only aggregate mirror (copy verbatim from UI-SPEC). Then extend `PersonaGraph` with an optional prop to accept a current-segment attention vector (or a per-node attention override) so the existing pulse/opacity reflects the replayed segment — additive, default-undefined so existing call sites are byte-identical. Do NOT introduce Math.random; reuse existing seed plumbing. NO pseudo-timeline synthesis for flat data (D-06).
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -qi 'error' && exit 1 || echo OK; grep -q 'attentions' src/components/audience-lens/ReplayController.tsx && grep -q 'reducedMotion' src/components/audience-lens/ReplayController.tsx && grep -L 'Math.random' src/components/audience-lens/ReplayController.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ReplayController drives segment-by-segment replay from `HeatmapPayload.attentions[]` on the timeline path.
    - All replay/cascade motion gated on `reducedMotion`; sr-only mirror present when reduced.
    - PersonaGraph accepts the new attention input additively — existing call sites (reading-panels.tsx) compile unchanged.
    - No `Math.random` / no synthesized timeline for flat data.
    - `npm run build` + `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Replay (timeline) drives the cloud segment-by-segment on the Test surface, reducedMotion-gated; PersonaGraph is extended additively; the cascade seam is stubbed for W3.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the onOpen seam on the video Test/Reading surface + extend cloud test</name>
  <files>src/components/reading/reading-panels.tsx, src/components/reading/persona-cloud.tsx, src/components/reading/__tests__/persona-cloud.test.tsx</files>
  <read_first>
    - src/components/reading/reading-panels.tsx (L172 builds the persona nodes; L205 mounts `<PersonaGraph personas={nodes} height={120} reducedMotion={reducedMotion} />` inline — this is the rich-signal surface that already has heatmap + the timeline; this is where onOpen must be wired to open AudienceLens)
    - src/components/reading/persona-cloud.tsx (the `onOpen?` seam at L42 + L76-95 — `interactive = typeof onOpen === 'function'`, role=button, ≥44px, Enter/Space; currently wired NOWHERE — Pitfall 1)
    - src/components/reading/__tests__/persona-cloud.test.tsx (existing cloud render anchors — extend, keep green)
    - src/components/audience-lens/AudienceLens.tsx (the sheet to open — built in Task 1)
  </read_first>
  <action>
    In `reading-panels.tsx`, add AudienceLens open state and pass the Reading's `heatmap` + `simResults` + the Read block (if available on this surface) into `<AudienceLens>`. Decide the open trigger: the surface mounts `<PersonaGraph>` inline (not `PersonaCloud`), so wrap the inline cloud in a tap/keyboard-activatable affordance (role=button, ≥44px, Enter/Space — mirror PersonaCloud's existing interactive treatment) whose handler opens the Lens; OR swap to `PersonaCloud` with `onOpen` if cleaner — executor discretion, but the seam MUST open the Lens (Pitfall 1: there is no pre-existing live Lens to "reuse"). Node drill-down (archetype + verbatim) is delivered by PersonaGraph's existing hover/tap pinned detail card inside the Lens — confirm the Lens's PersonaGraph receives the verbatim `quote`/`scrollQuote` so drill shows real reaction text (LIVE-02). Extend `persona-cloud.test.tsx` with an assertion that when `onOpen` is provided the cloud is interactive (role=button + fires onOpen on click/Enter) and when absent it is not — locking the seam.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/persona-cloud.test.tsx && grep -q 'AudienceLens' src/components/reading/reading-panels.tsx</automated>
  </verify>
  <acceptance_criteria>
    - reading-panels.tsx opens `<AudienceLens>` from a tap/keyboard affordance on the persona cloud (the onOpen seam is now wired — no longer a dead stub).
    - The Lens's PersonaGraph receives per-persona verbatim so node drill-down shows real reaction text (LIVE-02).
    - persona-cloud.test.tsx asserts the onOpen interactive contract (role=button + fires on click/Enter when provided; inert when absent) and passes.
    - `npm run build` + `npm run lint` exit 0.
  </acceptance_criteria>
  <done>The video Test/Reading surface opens the live AudienceLens with replay + node drill-down; the previously-dead onOpen seam is wired and test-locked.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/reading/__tests__/persona-cloud.test.tsx` → green (seam locked).
- `grep AudienceLens src/components/reading/reading-panels.tsx` → present (seam wired — Pitfall 1 closed).
- Replay drives segment-by-segment from `attentions[]`; reducedMotion-gated; sr-only mirror present.
- P8 Read header reused verbatim (no recolor — landmine 9).
- No `Math.random` in any new geometry/motion; no pseudo-timeline for flat data.
- `npm run build` + `npm run lint` exit 0.
</verification>

<success_criteria>
- The AudienceLens is a real, reusable sheet that opens from the rich-signal surface; the room visibly reacts segment-by-segment (replay), nodes drill to verbatim, and the P8 Read leads as the header.
- The reusable shell + wired seam are ready for W2 (chat) and W3 (thin-signal mounts) to build on.
</success_criteria>

<output>
Create `.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-02-SUMMARY.md` when done.
</output>
