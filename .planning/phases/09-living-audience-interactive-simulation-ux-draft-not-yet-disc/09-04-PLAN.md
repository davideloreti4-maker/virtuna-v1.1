---
phase: 09-living-audience-interactive-simulation-ux
plan: 04
type: execute
wave: 3
depends_on: [09-02, 09-01]
files_modified:
  - src/components/audience-lens/AudienceLens.tsx
  - src/lib/tools/chain-handoff.ts
  - src/lib/tools/__tests__/chain-handoff.test.ts
  - src/components/thread/idea-card-block.tsx
  - src/components/thread/hook-card-block.tsx
  - src/components/thread/script-card-block.tsx
  - src/components/thread/remix-card-block.tsx
  - src/components/thread/personas-block.tsx
autonomous: true
requirements: [LIVE-04, LIVE-06, LIVE-07]
must_haves:
  truths:
    - "The Lens groups personas by the Temp × Disposition lens (buildSegmentGroups); the single worst cluster reads coral (worstBadGroupKey, ≤2 coral marks)"
    - "A sticky 'Rewrite for this audience →' CTA re-POSTs to the originating skill's runner carrying the Read's lever as steering, producing a new card + Read in-thread that shows the delta vs prior (LIVE-07)"
    - "The AudienceLens mounts inline on the 4 text skills (ideas/hooks/script/remix) + the text Read with a staggered cascade (no timeline → no fake segments, D-06)"
    - "Rewrite is hidden on plain chat turns (no regenerable concept object, D-05)"
  artifacts:
    - path: "src/lib/tools/chain-handoff.ts"
      provides: "New same-skill 'Rewrite for this audience →' CHAIN_HANDOFFS entries (from===to)"
      contains: "Rewrite for this audience"
    - path: "src/components/audience-lens/AudienceLens.tsx"
      provides: "Cluster-by-segment view + sticky Rewrite CTA + cascade for flat signal"
  key_links:
    - from: "src/components/audience-lens/AudienceLens.tsx"
      to: "src/components/board/audience/audience-derive.ts"
      via: "buildSegmentGroups + worstBadGroupKey drive the cluster lens"
      pattern: "buildSegmentGroups"
    - from: "src/lib/tools/chain-handoff.ts"
      to: "src/components/audience-lens/AudienceLens.tsx"
      via: "handoffsFor(skill) supplies the Rewrite CTA endpoint + lever-as-steering anchor"
      pattern: "handoffsFor"
    - from: "src/components/thread/hook-card-block.tsx"
      to: "src/components/audience-lens/AudienceLens.tsx"
      via: "card mounts the onOpen-wired cloud inline → opens the Lens with this card's persona data"
      pattern: "AudienceLens"
---

<objective>
Add the cluster-by-segment view and the Rewrite-for-audience loop to the Lens, and mount the Lens (staggered cascade — no timeline) inline across the four text skills + the text Read.

Purpose: Thin-signal skills follow rich (D-04). The Rewrite loop IS the product (the flywheel — D-05): lever-as-steering regenerate that honestly shows whether the lever worked. Cluster-by-segment is presentation over already-shipped pure functions. Main risk: confirming originating runners accept a self-handoff re-POST (Assumption A2).
Output: cluster view + cascade in AudienceLens, new same-skill CHAIN_HANDOFFS Rewrite entries + test, Lens mounted on 4 card-blocks + personas-block / text Read.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-RESEARCH.md
@.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-CONTEXT.md
@.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-UI-SPEC.md
@src/lib/tools/chain-handoff.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cluster-by-segment view + staggered cascade in AudienceLens</name>
  <files>src/components/audience-lens/AudienceLens.tsx</files>
  <read_first>
    - src/components/board/audience/audience-derive.ts (`buildSegmentGroups(heatmap, simResults): SegmentGroup[]` ({key,label,pct,desc,count}); `worstBadGroupKey(groups): SlotKey|null` (<40% rule, ≤2 coral); these are PURE + shipped — presentation only on top, do NOT reimplement the grouping — reuse-contract row 3 / Don't-Hand-Roll)
    - src/components/audience-lens/lens-derive.ts (from 09-01 — `cascadeOrder(nodes)` deterministic reveal order for the flat/cascade path)
    - src/components/audience-lens/ReplayController.tsx (from 09-02 — the cascade entry point stubbed there; complete the cascade path here using cascadeOrder)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-UI-SPEC.md (Color: coral reserved for worst cluster ONLY; cluster label typography/spacing; "Play" cascade copy; reducedMotion gate)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-CONTEXT.md (D-04 cluster discretion via Temp×Disposition; D-06 staggered cascade — NO synthesized pseudo-timeline)
  </read_first>
  <action>
    In AudienceLens add a cluster-by-segment view driven by `buildSegmentGroups(heatmap, simResults)` with the single worst group (from `worstBadGroupKey`) painted coral (≤2 coral marks per UI-SPEC — coral is reserved for worst cluster only). Complete the staggered-cascade path the ReplayController stubbed in 09-02: on flat-signal surfaces, "Play" reveals personas in `cascadeOrder(nodes)` sequence (ordered by verdict/weight), gated on `reducedMotion` (static + sr-only mirror when reduced). NO pseudo-timeline synthesis — the cascade is a reveal animation, not fabricated segments (D-06). Grouping/label presentation is discretion within the Temp×Disposition lens. Keep the file under 500 lines (extract a ClusterView helper if needed).
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -qi 'error' && exit 1 || echo OK; grep -q 'buildSegmentGroups' src/components/audience-lens/AudienceLens.tsx && grep -q 'cascadeOrder' src/components/audience-lens/AudienceLens.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Cluster view groups personas via buildSegmentGroups; worst cluster coral via worstBadGroupKey (≤2 coral marks).
    - Flat-signal cascade reveals in cascadeOrder, reducedMotion-gated, no fabricated segments.
    - `npm run build` + `npm run lint` exit 0.
  </acceptance_criteria>
  <done>The Lens shows which segment loved/hated the concept (Temp×Disposition cluster) and reveals flat reactions as a staggered cascade — honest, no fake timeline.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite-for-audience CHAIN_HANDOFFS entries + delta + test</name>
  <files>src/lib/tools/chain-handoff.ts, src/lib/tools/__tests__/chain-handoff.test.ts, src/components/audience-lens/AudienceLens.tsx</files>
  <read_first>
    - src/lib/tools/chain-handoff.ts (`SkillId` union (no `chat`); `ChainHandoff` {from,to,ctaLabel,endpoint,anchorFrom}; `CHAIN_HANDOFFS` array; `handoffsFor(skill)`; existing endpoints: idea→hooks `/api/tools/ideas/develop`, hooks→script `/api/tools/script`, remix→hooks `/api/tools/ideas/develop`, hooks→test null — reuse-contract row 6. Rewrite = same-skill from===to re-POST to the originating route)
    - src/lib/tools/__tests__/chain-handoff.test.ts (asserts exact endpoint strings + payload contracts — extend with the Rewrite entries; drift fails the test)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-RESEARCH.md (Code Examples §Rewrite loop + Assumption A2 — confirm originating runners accept a lever-steered self-handoff re-POST; if a runner rejects from===to, document the minimal adjustment)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-UI-SPEC.md (Copywriting: "Rewrite for this audience →" sticky primary CTA, coral + --shadow-button; hidden on plain chat turns)
  </read_first>
  <action>
    Append same-skill `ChainHandoff` entries (from===to) for the regenerable skills — `{ from: "idea"|"hooks"|"script"|"remix", to: same, ctaLabel: "Rewrite for this audience →", endpoint: <originating route>, anchorFrom: "card" }` — re-POSTing to each skill's own runner with the Read's LEVER injected as the steering anchor. Verify A2: confirm each originating runner accepts a self-handoff re-POST with a lever-steered anchor (the routes already accept `{ ask?, anchor, platform }`); if any rejects from===to, document + apply the minimal route adjustment in the SUMMARY. In AudienceLens, render the sticky "Rewrite for this audience →" CTA (coral + `--shadow-button`) sourced from `handoffsFor(skill)`, hidden where there is no regenerable concept object (plain chat turns — D-05). On a successful Rewrite the new card + Read stream in-thread; the Lens surfaces the DELTA vs the prior Read (e.g. stop-count / band shift). Extend `chain-handoff.test.ts` to assert the exact Rewrite entries (from===to, ctaLabel, endpoint) — drift fails.
  </action>
  <verify>
    <automated>npx vitest run src/lib/tools/__tests__/chain-handoff.test.ts && grep -q 'Rewrite for this audience' src/lib/tools/chain-handoff.ts</automated>
  </verify>
  <acceptance_criteria>
    - Same-skill Rewrite entries exist for idea/hooks/script/remix with the originating endpoints; test asserts them.
    - The sticky Rewrite CTA injects the lever as steering, produces a new card + Read in-thread, and shows the delta vs prior.
    - Rewrite hidden on plain chat turns.
    - A2 disposition documented in the SUMMARY (runners accept self-handoff, or the minimal adjustment applied).
    - `npm run build` + `npm run lint` exit 0.
  </acceptance_criteria>
  <done>The Rewrite-for-audience loop closes: lever-as-steering → new card + Read in-thread → delta shown — the flywheel works through CHAIN_HANDOFFS.</done>
</task>

<task type="auto">
  <name>Task 3: Mount the AudienceLens inline across the 4 text skills + text Read</name>
  <files>src/components/thread/idea-card-block.tsx, src/components/thread/hook-card-block.tsx, src/components/thread/script-card-block.tsx, src/components/thread/remix-card-block.tsx, src/components/thread/personas-block.tsx</files>
  <read_first>
    - src/components/thread/hook-card-block.tsx (and the idea/script/remix card-block siblings — each renders `{scrollQuote, personas[]}`; this is the per-card persona data the Lens consumes; mount the onOpen-wired cloud inline here — reuse-contract row 7)
    - src/components/thread/personas-block.tsx (the shared personas viz block; the text Read mounts the Lens through this; flat Shape B data: `{archetype, verdict, quote}` + `scrollQuote`, NO timeline)
    - src/components/audience-lens/AudienceLens.tsx (the sheet to open; built in 09-02, extended in this plan)
    - src/components/reading/persona-cloud.tsx (`PersonaCloud({ heatmap, simResults, onOpen })` — the onOpen seam to wire per card; flat skills have no heatmap timeline → cascade-only per the degrade matrix)
    - .planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-RESEARCH.md (Degrade-by-Feature Matrix — replay=cascade-only on these surfaces; cloud/drill/chat/cluster/Population/Rewrite all available)
  </read_first>
  <action>
    Mount the `onOpen`-wired cloud inline on each of the four text card-blocks (idea/hook/script/remix) and the shared `personas-block` (the text Read surface), passing each card's per-persona Shape-B data (`{archetype, verdict, quote}` + `scrollQuote`) into `<AudienceLens>`. These surfaces have NO `segment_reactions` timeline, so the Lens opens in cascade mode (replay control = "Play" staggered, not segment-by-segment — D-06 degrade). All other Lens features (drill, chat, cluster, Population, Rewrite) are available per the degrade matrix. Do NOT alter card layouts or stream-hook contracts (reuse-contract row 7 LEAVES ALONE) — only add the cloud affordance + Lens mount. Reuse a single shared open helper so all 6 surfaces (these 5 + the video Test surface from 09-02) use the identical Lens entry (D-04 — one reusable Lens, not six bespoke surfaces).
  </action>
  <verify>
    <automated>for f in idea hook script remix; do grep -q 'AudienceLens' src/components/thread/$f-card-block.tsx || exit 1; done; grep -q 'AudienceLens' src/components/thread/personas-block.tsx && npm run build 2>&1 | grep -qi 'error' && exit 1 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All four text card-blocks (idea/hook/script/remix) + personas-block mount the onOpen-wired cloud and open the same AudienceLens.
    - These surfaces open in cascade mode (no segment-by-segment replay — degrade matrix honored).
    - Card layouts + stream-hook contracts unchanged.
    - One shared Lens entry across all 6 skills (D-04).
    - `npm run build` + `npm run lint` exit 0.
  </acceptance_criteria>
  <done>The single reusable AudienceLens is mounted across all 6 skills, degrading by feature — cascade (not replay) on the flat text surfaces.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/tools/__tests__/chain-handoff.test.ts` → green (Rewrite entries locked).
- Cluster via buildSegmentGroups; worst cluster coral (≤2 marks); cascade via cascadeOrder, reducedMotion-gated.
- Lens mounted on all 4 text skills + text Read; cascade-only there (D-06 degrade).
- Rewrite shows delta vs prior; hidden on plain chat turns; A2 disposition documented.
- `npm run build` + `npm run lint` exit 0.
</verification>

<success_criteria>
- One reusable Lens lives across all 6 skills, degrading gracefully; the room clusters by segment; the Rewrite flywheel closes with an honest delta.
</success_criteria>

<output>
Create `.planning/phases/09-living-audience-interactive-simulation-ux-draft-not-yet-disc/09-04-SUMMARY.md` when done.
</output>
