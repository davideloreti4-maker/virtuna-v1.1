---
phase: 01-engine-thread-foundation
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/app/home/tool-chips.tsx
  - src/components/app/home/composer.tsx
  - src/components/app/home/__tests__/tool-chips.test.tsx
  - src/components/app/home/__tests__/composer-navigate-guard.test.tsx
autonomous: true
requirements: [THREAD-02]
must_haves:
  truths:
    - "The composer shows a tool-chip row (Test · Idea · Hooks · Chat); the active chip is always visible and drives the placeholder + send action (D-07)."
    - "Only Test is wired live (existing URL/upload → Reading); Idea/Hooks/Chat render disabled 'coming soon' with a reserved cost slot (D-08); no half-built tool ships in P1."
    - "An active-model field shows the model the active chip implies: Test → 'SIM-1 Max', Ideas/Hooks/Chat → 'SIM-1 Flash' — the creator sees which engine judges BEFORE firing (D-09)."
    - "The existing Test loop (URL/upload → /api/analyze, pendingNavRef arming) is preserved byte-for-behavior — chip selection NEVER arms navigation (Pitfall #5)."
  artifacts:
    - path: "src/components/app/home/tool-chips.tsx"
      provides: "ToolChips client component: chip row + active-model label + disabled 'coming soon' + cost slot"
      contains: "SIM-1"
    - path: "src/components/app/home/composer.tsx"
      provides: "composer extended with chip state; Test loop unchanged; placeholder/model driven by active chip"
      contains: "tool"
  key_links:
    - from: "src/components/app/home/composer.tsx"
      to: "src/components/app/home/tool-chips.tsx"
      via: "active tool id state drives placeholder + active-model field"
      pattern: "ToolChips|activeTool"
    - from: "src/components/app/home/tool-chips.tsx"
      to: "src/lib/tools/tool-runner.ts"
      via: "chip ids + model labels reuse the ToolRunner id/model union (single vocabulary)"
      pattern: "test|idea|hooks|chat|sim1"
---

<objective>
Make the **composer the universal entry door** (THREAD-02) — add a tool-chip row (Test · Idea · Hooks · Chat) and an active-model field, with ONLY Test wired live. The chip drives explicit tool selection (NOT auto-detect — D-06, because tools spend credits) and the active-model honesty field (chip → model: Test→SIM-1 Max, Ideas/Hooks/Chat→SIM-1 Flash — D-09). Idea/Hooks/Chat chips render disabled "coming soon" with a reserved cost slot (D-07/D-08); each later phase flips its own chip live. The existing URL/upload → Reading Test loop is preserved exactly (Pitfall #5 — chip state must not re-introduce the hydration-id navigation bug).

Purpose: this is the seam every later tool plugs into; P1 ships the chip + routing contract with Test live, so P3/P4/P5 only flip a chip. The active-model field is the owner's explicit mental model for the honesty spine (the creator sees which engine judges before firing).
Output: a new `ToolChips` component + the extended `composer.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-thread-foundation/01-CONTEXT.md
@.planning/phases/01-engine-thread-foundation/01-RESEARCH.md
@.planning/phases/01-engine-thread-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ToolChips component (chip row + active-model field + disabled cost slots)</name>
  <files>src/components/app/home/tool-chips.tsx, src/components/app/home/__tests__/tool-chips.test.tsx</files>
  <read_first>
    - src/components/app/home/composer.tsx (the host's styling vocabulary — Raycast tokens: border white/[0.06], hover white/[0.1], rounded-lg; the form layout the chip row sits above)
    - src/lib/tools/tool-runner.ts (Plan 01 — the `id: "test"|"idea"|"hooks"|"chat"` + `model: "sim1-max"|"sim1-flash"` unions to reuse as the chip vocabulary, single SSOT)
    - .planning/phases/01-engine-thread-foundation/01-RESEARCH.md §"Pattern 5: Composer tool chips, Test-only live"
    - CLAUDE.md §"Raycast Design Language Rules" (6% borders / 10% hover / 8px chip radius; coral only on accents; no glow)
  </read_first>
  <action>
    Create `tool-chips.tsx` (`'use client'`): a chip row of four chips (`test | idea | hooks | chat`, reusing the Plan-01 ToolRunner `id` union so the vocabulary never drifts). Props: `activeTool`, `onSelect`. Only `test` is enabled; `idea`/`hooks`/`chat` render `disabled` with a "coming soon" affordance and a reserved **cost slot** placeholder (credits are deferred — D-07 reserves the affordance only; no live ledger). Render an **active-model field** that maps the active chip to its model LABEL: `test → "SIM-1 Max"`, `idea|hooks|chat → "SIM-1 Flash"` (D-09 — same vocabulary as the block model tag D-10). The active chip is always visually distinct (selected state). Raycast styling: 6% borders, 10% hover, 8px radius, coral only on the active accent; no glow (CLAUDE.md). Disabled chips are non-interactive (aria-disabled, no onSelect fire).
  </action>
  <verify>
    <automated>npm test -- src/components/app/home/__tests__/tool-chips.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: the chip vocabulary reuses the ToolRunner union — `grep -nE "test|idea|hooks|chat" src/components/app/home/tool-chips.tsx` matches all four (D-07).
    - SOURCE: the active-model field maps chips to model labels — `grep -nE "SIM-1 Max|SIM-1 Flash" src/components/app/home/tool-chips.tsx` matches both (D-09).
    - BEHAVIOR: the test proves clicking `test` fires `onSelect("test")`; clicking a disabled chip (idea/hooks/chat) does NOT fire onSelect; the active-model label reads "SIM-1 Max" when test is active (D-08/D-09).
    - SOURCE: a reserved cost slot affordance exists on chips — `grep -niE "cost|credit" src/components/app/home/tool-chips.tsx` matches (D-07 reserves the slot; no live metering).
    - CLI: `npm test -- src/components/app/home/__tests__/tool-chips.test.tsx` exits 0.
  </acceptance_criteria>
  <done>ToolChips renders 4 chips with only Test enabled, Idea/Hooks/Chat disabled "coming soon" + cost slot, and an active-model field (Test→SIM-1 Max, others→SIM-1 Flash); disabled chips don't select; test green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire chips into composer, preserve the Test navigation guard</name>
  <files>src/components/app/home/composer.tsx, src/components/app/home/__tests__/composer-navigate-guard.test.tsx</files>
  <read_first>
    - src/components/app/home/composer.tsx (THE file to modify — the existing pendingNavRef/prevAnalysisIdRef arming pattern at L93-104 + the two submit paths that set pendingNavRef.current=true; these MUST stay intact — Pitfall #5 / WR-05)
    - src/components/app/home/tool-chips.tsx (Task 1 — the component to mount + its activeTool/onSelect contract)
    - src/components/app/home/__tests__/composer-navigate-guard.test.tsx (the EXISTING guard test — extend it, do not weaken it)
    - src/hooks/queries/use-analysis-stream.ts (where analysisId is set from the URL on hydration — the bug the guard prevents)
  </read_first>
  <action>
    Extend `composer.tsx`: add `activeTool` state (default `"test"`), mount `<ToolChips activeTool onSelect>` above the input row. The placeholder + active-model field follow the active chip (Test keeps the existing URL/upload placeholders). **PRESERVE the Test loop EXACTLY**: the existing URL/upload → `useAnalysisStream.start` → `/analyze/[id]` navigation, and crucially the `pendingNavRef.current = true` arming inside the submit handlers + the `prevAnalysisIdRef` effect (Pitfall #5 / WR-05) — chip selection must NEVER set `pendingNavRef.current = true` (a chip click is not a submit). In P1 the non-Test chips are disabled, so `handleSubmit` still only runs the Test path; do NOT add Idea/Hooks/Chat dispatch (those land in P3/P4/P5 — D-08). URL/upload may keep hard-routing to Test while the active chip stays shown (D-07). Extend the navigate-guard test to assert: selecting a chip does not navigate, and a hydration-sourced analysisId still does not navigate after a chip interaction.
  </action>
  <verify>
    <automated>npm test -- "src/components/app/home/__tests__/"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: the navigation arming pattern is preserved — `grep -nE "pendingNavRef|prevAnalysisIdRef" src/components/app/home/composer.tsx` still matches (Pitfall #5 not regressed).
    - SOURCE: chip selection does not arm navigation — there is no `pendingNavRef.current = true` inside any chip `onSelect`/select handler (assert by reading the onSelect handler; the only `pendingNavRef.current = true` assignments remain inside the two submit paths).
    - SOURCE: no Idea/Hooks/Chat dispatch added in P1 — `grep -niE "idea|hooks|chat" src/components/app/home/composer.tsx` shows only chip-state plumbing, no `stream.start` / fetch branch for them (D-08).
    - BEHAVIOR: the full composer suite (layout + navigate-guard + composer + home tests) stays green; the extended guard test proves a chip click does not navigate and a hydration id still does not navigate.
    - CLI: `npm test -- "src/components/app/home/__tests__/"` exits 0.
  </acceptance_criteria>
  <done>composer mounts ToolChips with activeTool state; placeholder + model field follow the active chip; the Test loop + pendingNavRef guard are intact; no non-Test dispatch added; the composer suite (incl. extended navigate-guard) is green.</done>
</task>

</tasks>

<verification>
- `npm test -- "src/components/app/home/"` GREEN (tool-chips + composer + layout + navigate-guard + home).
- `grep -nE "pendingNavRef|prevAnalysisIdRef" src/components/app/home/composer.tsx` confirms the Test navigation guard survived (Pitfall #5).
- `grep -nE "SIM-1 Max|SIM-1 Flash" src/components/app/home/tool-chips.tsx` confirms the active-model honesty field (D-09).
- `grep -niE "coming soon|disabled" src/components/app/home/tool-chips.tsx` confirms Idea/Hooks/Chat are disabled (D-08).
- No new `stream.start` / fetch branch for idea/hooks/chat in composer.tsx (no half-built tool in P1).
</verification>

<success_criteria>
- A creator selects a tool explicitly via composer chips; the active tool + the model it implies are always visible before firing (THREAD-02 / D-06 / D-07 / D-09 — ROADMAP success criterion 2, composer side).
- Only Test is wired live (URL/upload → existing Reading, unchanged); Idea/Hooks/Chat render disabled with a reserved cost slot (D-08 — no half-built tool ships).
- The active-model field carries the Flash-vs-Max honesty distinction at the composer (D-09) — the creator sees which engine judges before spending.
- The existing Test loop + hydration-id navigation guard are preserved (Pitfall #5 — no regression of the shipped composer).
</success_criteria>

<output>
Create `.planning/phases/01-engine-thread-foundation/01-04-SUMMARY.md` when done.
</output>
