# Plan 58-01: SectionWrapper Component

**Requirement:** XCUT-05 (infrastructure pattern for consistent section spacing)

## What

Create a `SectionWrapper` component that enforces consistent max-width, padding, and vertical spacing across all landing page sections.

## Files

- CREATE `src/components/landing/section-wrapper.tsx`
- EDIT `src/components/landing/index.ts` (add export)

## Implementation

```tsx
interface SectionWrapperProps {
  children: React.ReactNode;
  className?: string;
  id?: string;
  as?: "section" | "div";
}

// Max-width 1152px (72rem), px-6 on mobile, px-8 on desktop
// Vertical spacing: py-20 md:py-24 lg:py-32
```

## Verification

- Component renders with correct spacing at all breakpoints
- Build passes
