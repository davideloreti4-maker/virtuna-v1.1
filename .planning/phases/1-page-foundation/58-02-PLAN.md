# Plan 58-02: Raycast-style Navbar

**Requirements:** NAV-01, NAV-02, NAV-03, NAV-04

## What

Rewrite the Header component from societies.io style to Raycast-style Virtuna navbar:
- Sticky with glass blur background
- Virtuna logo (text-based for now)
- Nav links: Features, Trending, Dashboard
- Primary CTA: "Get started" button
- Mobile: hamburger menu with slide-out panel

## Files

- EDIT `src/components/layout/header.tsx`

## Design Tokens

- Glass navbar: `background: var(--gradient-navbar)` + `backdrop-filter: blur(5px)` (inline style) + `border-bottom: 1px solid rgba(255,255,255,0.06)` + `inset shadow: rgba(255,255,255,0.15) 0 1px 1px 0 inset`
- Nav links: `text-sm text-foreground-secondary hover:text-foreground` transition
- CTA button: Use existing `Button` component with `variant="primary" size="sm"`
- Border radius for navbar container: 16px (Raycast header pattern)
- Max-width container: match SectionWrapper inner width
- Height: ~64px

## Verification

- Desktop: logo + 3 nav links + CTA visible
- Mobile: hamburger button, slide-out with all links
- Glass blur visible on scroll
- Build passes
