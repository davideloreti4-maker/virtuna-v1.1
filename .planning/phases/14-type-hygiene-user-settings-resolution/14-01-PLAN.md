---
phase: 14-type-hygiene-user-settings-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/user-settings-audit.md
autonomous: true
requirements:
  - TYPES-01
  - TYPES-02
  - TYPES-05
requirements_addressed:
  - TYPES-01
  - TYPES-02
  - TYPES-05
must_haves:
  truths:
    - "[D-01] tsc --noEmit baseline on milestone/engine-hardening HEAD is 0 errors — the 966-error figure in MILESTONE.md / REQUIREMENTS.md is stale (pre-bbb4e81) and must be footnoted in the audit, not relied on"
    - "[D-04] Phase 14 writes zero new SQL migrations; teams + user_settings + team_members are already applied live on qyxvxleheckijapurisj. If planner concludes otherwise, surface as deviation"
    - "[D-05][D-06] Audit doc is THE primary TYPES-01 deliverable — inline notes in CONTEXT.md or commit messages do not satisfy the requirement"
    - "[D-08] Hand-patched blocks at lines 1166-1226 (team_members, teams) and 1479-1521 (user_settings) match live information_schema columns; drift-check also covers creator_profiles, analysis_results, platt_parameters, trending_sounds (added at 1c590ac, 37a7fa4, 8eec2a9, 5ee7693)"
    - "All 9 user_settings grep hits are reachable from deployed UI via React Query hooks (use-profile, use-team, use-creator-profile)"
    - "[D-03] Path (a) migrate decision is justified by evidence — not assumption — and explicitly recorded in §6"
    - "[D-12] UAT against the deployed app is out-of-scope here; runtime exercise is VERIF-01's job in Phase 18"
  artifacts:
    - path: ".planning/research/user-settings-audit.md"
      provides: "[D-05][D-06] TYPES-01 primary deliverable: full user_settings audit"
      contains: "## 1. TSC Baseline, ## 2. Live Schema Evidence, ## 3. Consumer Call Sites, ## 4. Hook → Route → Column Trace, ## 5. Hand-Patch vs Live Schema Diff, ## 6. Path Decision"
      min_lines: 120
  key_links:
    - from: ".planning/research/user-settings-audit.md §6"
      to: "[D-03] path (a) migrate decision"
      via: "evidence chain: live migrations + reachable consumers + zero column drift"
      pattern: "Path \\(a\\)|migrate.*confirmed|D-03"
---

<objective>
Produce `.planning/research/user-settings-audit.md` — the primary TYPES-01 deliverable. Verify and document the live-state finding from CONTEXT.md D-01 (tsc baseline = 0, not 966) and D-03 (path (a) migrate locked, no new SQL work). The audit doc is the evidence chain that lets Plan 02 regenerate `database.types.ts` confidently.

Purpose: TYPES-01 requires the audit doc as primary deliverable per D-05/D-06; TYPES-02 requires the path decision logged with evidence; TYPES-05 requires the baseline `tsc --noEmit` captured before any type churn.

Output: One audit doc at `.planning/research/user-settings-audit.md` containing all 6 D-05 content items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/MILESTONE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-type-hygiene-user-settings-resolution/14-CONTEXT.md
@.planning/phases/14-type-hygiene-user-settings-resolution/14-PATTERNS.md

<interfaces>
<!-- Consumer surface shapes the audit must trace. Extracted from PATTERNS.md §"Consumer Surface Patterns". -->

From `src/app/api/settings/notifications/route.ts` (lines 1–5):
```typescript
import type { Database } from "@/types/database.types";
type UserSettingsInsert = Database["public"]["Tables"]["user_settings"]["Insert"];
```

From `src/app/api/profile/route.ts` (lines 30–34 read; 99–107 upsert):
```typescript
const { data: settings } = await supabase
  .from("user_settings")
  .select("*")
  .eq("user_id", user.id)
  .maybeSingle();

const { error } = await supabase
  .from("user_settings")
  .upsert({ user_id: user.id, ...settingsData }, { onConflict: "user_id" });
```

Fields consumed by `profile/route.ts`: `display_name`, `company`, `role`, `avatar_url`, `notification_email_updates`, `notification_test_results`, `notification_weekly_digest`, `notification_marketing`.

From `src/app/api/profile/avatar/route.ts` (lines 61–65): writes `avatar_url` only.

Hand-patched type block (current `database.types.ts` lines 1479–1517) — `user_settings.Row`:
```typescript
{
  avatar_url: string | null
  company: string | null
  display_name: string | null
  notification_email_updates: boolean
  notification_marketing: boolean
  notification_test_results: boolean
  notification_weekly_digest: boolean
  role: string | null
  updated_at: string
  user_id: string
}
```

React Query consumer hooks (no direct `Database` import — they use shape types from `@/types/settings`):
- `src/hooks/queries/use-profile.ts` → `/api/profile`, `/api/profile/avatar`, `/api/settings/notifications`, `/api/settings/account/password`
- `src/hooks/queries/use-team.ts` → `/api/team`, `/api/team/invite`, `/api/team/members/[id]`
- `src/hooks/queries/use-creator-profile.ts` → `/api/profile/creator-profile`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture tsc baseline + live migration evidence</name>
  <files>.planning/research/user-settings-audit.md</files>
  <read_first>
    - /Users/davideloreti/virtuna-engine-hardening/.planning/phases/14-type-hygiene-user-settings-resolution/14-CONTEXT.md (D-01, D-05, D-06)
    - /Users/davideloreti/virtuna-engine-hardening/.planning/phases/14-type-hygiene-user-settings-resolution/14-PATTERNS.md (§"`.planning/research/user-settings-audit.md` (new audit deliverable)" — recommended section order)
    - /Users/davideloreti/virtuna-engine-hardening/.planning/milestones/v2.3-MILESTONE-AUDIT.md (only for structural tone — different artifact type, do not copy content)
  </read_first>
  <action>
Create `.planning/research/user-settings-audit.md` with frontmatter and the first two required sections. Use this exact section ordering (locked by PATTERNS.md §"Recommended section order"):

```markdown
---
phase: 14
audited: 2026-05-24
status: PASS
findings:
  tsc_baseline_errors: 0
  live_tables_present: [user_settings, teams, team_members]
  hand_patched_blocks: [team_members:1166-1206, teams:1207-1227, user_settings:1479-1517]
  path_decision: a-migrate
---

# user_settings Audit — Phase 14

**Audited:** 2026-05-24
**Status:** PASS — live schema already satisfies all consumer code; phase is verification + type regen, not new SQL work.

> **Stale baseline note:** MILESTONE.md and REQUIREMENTS.md cite "966 TS errors" — that figure pre-dates commit `bbb4e81`. Actual baseline on `milestone/engine-hardening` HEAD as of 2026-05-24 is **0 errors**. This audit re-establishes truth; subsequent commits in Phase 14 must keep it at 0.

## 1. TSC Baseline

Run before any type churn:

```bash
pnpm exec tsc --noEmit
```

Output (recorded 2026-05-24, milestone/engine-hardening HEAD):
```
TypeScript: No errors found
```

Baseline: **0 errors**. TYPES-05 acceptance gate (D-02) is already met on entry; phase exit must keep it green after `database.types.ts` regen.

## 2. Live Schema Evidence

Project: `qyxvxleheckijapurisj` (Supabase production for Virtuna).

### 2.1 Migration list (via Supabase MCP `list_migrations`)

| Local filename | Live version | Object |
|---|---|---|
| `supabase/migrations/20260217100000_user_settings.sql` | `20260519113322` | `user_settings` table + 3 RLS policies + `avatars` storage bucket + 3 storage policies |
| `supabase/migrations/20260217200000_teams.sql` | `20260519113337` | `teams` + `team_members` tables |

Local stamp / live re-stamp mismatch is normal (see CONTEXT.md `code_context` §"Migration naming"). Not a sync bug.

### 2.2 Table existence (via Supabase MCP `list_tables(schemas=["public"])`)

The following are present on live `qyxvxleheckijapurisj`:
- `user_settings`
- `teams`
- `team_members`

(Audit must include the actual MCP output of `list_tables` filtered to these three — paste the full row including columns + RLS status.)

### 2.3 Column-level cross-check (via `information_schema.columns`)

Query executed against each of the three tables; results pasted inline (see §5 for diff vs hand-patched type blocks).
```

After writing those two sections, execute the Supabase MCP calls to fill in the parenthetical "Audit must include..." gaps — do not leave placeholder text in the final commit:

1. Call `mcp__supabase__list_migrations` with project `qyxvxleheckijapurisj`; copy the rows for `20260519113322` and `20260519113337` into §2.1 as evidence (table row format above, plus a fenced code block with the raw MCP response trimmed to those two entries).
2. Call `mcp__supabase__list_tables` with `schemas: ["public"]` on `qyxvxleheckijapurisj`; extract the entries for `user_settings`, `teams`, `team_members` (name + columns array + RLS enabled flag) and embed under §2.2 as a fenced JSON block. Do not paste the full response — only the three relevant entries.
3. Call `mcp__supabase__execute_sql` on `qyxvxleheckijapurisj` with this query and store output for §5 (column block written in Task 2):
   ```sql
   SELECT table_name, column_name, data_type, is_nullable, column_default
   FROM information_schema.columns
   WHERE table_schema = 'public'
     AND table_name IN ('user_settings', 'teams', 'team_members')
   ORDER BY table_name, ordinal_position;
   ```

Save the SQL output verbatim (as a fenced code block) in a scratch buffer at the bottom of the audit doc under a temporary `<!-- SCRATCH §5 SQL -->` HTML comment. Task 2 will move it into §5.

Threat model alignment: this task only reads metadata via MCP (no credentials written, no row data accessed). T-14-01 (audit doc disclosure) is mitigated by content scoping — no PII, no row contents, only schema metadata.
  </action>
  <verify>
    <automated>test -f .planning/research/user-settings-audit.md && grep -q "^# user_settings Audit — Phase 14" .planning/research/user-settings-audit.md && grep -q "^## 1. TSC Baseline" .planning/research/user-settings-audit.md && grep -q "^## 2. Live Schema Evidence" .planning/research/user-settings-audit.md && grep -q "TypeScript: No errors found" .planning/research/user-settings-audit.md && grep -q "20260519113322" .planning/research/user-settings-audit.md && grep -q "20260519113337" .planning/research/user-settings-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/research/user-settings-audit.md` exits 0
    - `grep -c "^## " .planning/research/user-settings-audit.md` ≥ 2 (sections 1 and 2 present)
    - `grep -q "tsc_baseline_errors: 0" .planning/research/user-settings-audit.md` exits 0
    - `grep -q "20260519113322" .planning/research/user-settings-audit.md` exits 0 (live user_settings migration version)
    - `grep -q "20260519113337" .planning/research/user-settings-audit.md` exits 0 (live teams migration version)
    - `grep -q "qyxvxleheckijapurisj" .planning/research/user-settings-audit.md` exits 0 (project id referenced)
    - `grep -c "user_settings\|teams\|team_members" .planning/research/user-settings-audit.md` ≥ 6 (all three tables named at least twice)
    - No `(Audit must include` placeholder text remains in committed sections 1–2 (`! grep -q "Audit must include" .planning/research/user-settings-audit.md` or text only present inside the scratch buffer for §5)
  </acceptance_criteria>
  <done>Sections 1 (TSC Baseline) and 2 (Live Schema Evidence) of the audit doc are written with real MCP output embedded. SQL output for §5 stored in a `<!-- SCRATCH §5 SQL -->` comment for Task 2 to consume.</done>
</task>

<task type="auto">
  <name>Task 2: Document consumer call sites, hand-patch diff, and path decision</name>
  <files>.planning/research/user-settings-audit.md</files>
  <read_first>
    - /Users/davideloreti/virtuna-engine-hardening/.planning/research/user-settings-audit.md (current state after Task 1, includes SCRATCH §5 SQL)
    - /Users/davideloreti/virtuna-engine-hardening/src/app/api/profile/route.ts
    - /Users/davideloreti/virtuna-engine-hardening/src/app/api/profile/avatar/route.ts
    - /Users/davideloreti/virtuna-engine-hardening/src/app/api/settings/notifications/route.ts
    - /Users/davideloreti/virtuna-engine-hardening/src/hooks/queries/use-profile.ts
    - /Users/davideloreti/virtuna-engine-hardening/src/hooks/queries/use-team.ts
    - /Users/davideloreti/virtuna-engine-hardening/src/hooks/queries/use-creator-profile.ts
    - /Users/davideloreti/virtuna-engine-hardening/src/types/database.types.ts (lines 1166-1226 team_members+teams; lines 1479-1521 user_settings)
    - /Users/davideloreti/virtuna-engine-hardening/.planning/phases/14-type-hygiene-user-settings-resolution/14-PATTERNS.md (consumer surface excerpts)
    - /Users/davideloreti/virtuna-engine-hardening/.planning/phases/14-type-hygiene-user-settings-resolution/14-CONTEXT.md (D-03, D-05 item-by-item checklist)
  </read_first>
  <action>
Append sections 3–6 to `.planning/research/user-settings-audit.md`, then remove the `<!-- SCRATCH §5 SQL -->` buffer once §5 consumes its contents.

### Section 3 — Consumer Call Sites (9 grep hits)

Run this exact command and use the output to populate the table:

```bash
grep -rn "user_settings\|from\(\"user_settings\"\|UserSettings" src/app/api/ src/hooks/queries/ src/types/ 2>/dev/null | grep -v "database.types.ts"
```

Then resolve to exactly 9 enumerated call sites. For each call site, record:

| # | File | Line | Operation | Fields touched | Reachable from deployed UI? | Breakage if table absent |
|---|------|------|-----------|----------------|-----------------------------|---------------------------|

Use these starter facts from PATTERNS.md (verify each by re-reading the file):
- `src/app/api/profile/route.ts:31` — `.from("user_settings").select("*").eq("user_id", user.id)` (GET) — fields: read-all — reached by `use-profile.ts` hook `useProfile()` query
- `src/app/api/profile/route.ts:100` — `.from("user_settings").upsert({ user_id, ...settingsData }, { onConflict: "user_id" })` (PATCH) — fields: display_name, company, role, avatar_url, notification_email_updates, notification_test_results, notification_weekly_digest, notification_marketing — reached by `useProfileMutation()`
- `src/app/api/profile/avatar/route.ts:61` — `.from("user_settings").upsert({ user_id, avatar_url }, { onConflict: "user_id" })` — fields: avatar_url — reached by `useAvatarUpload()` mutation in `use-profile.ts`
- `src/app/api/settings/notifications/route.ts` — `UserSettingsInsert` typedef + upsert; fields: notification_* booleans — reached by `useNotificationPrefsMutation()` in `use-profile.ts`

Remaining hits (run the grep above to enumerate). Each row's "Reachable from deployed UI?" answer must come from tracing the React Query hook back to the component that calls it — record component path (e.g. `src/app/(authed)/settings/page.tsx`) as evidence.

Breakage column rationale: since live schema has the table, "breakage if absent" is hypothetical — record what would break (e.g. "5xx on `useProfile()` query, settings panel renders empty state"). This is the audit's risk-traceability column.

### Section 4 — Hook → Route → Column Trace

Three rows minimum (per D-05):

| React Query Hook | API Route Hit | Table Columns Touched |
|---|---|---|
| `use-profile.ts` `useProfile()` | `GET /api/profile` | `user_settings.*` (read-all join with `auth.users.email`) |
| `use-profile.ts` `useProfileMutation()` | `PATCH /api/profile` | `user_settings.{display_name, company, role, notification_*}` |
| `use-profile.ts` `useAvatarUpload()` | `POST /api/profile/avatar` | `user_settings.avatar_url` |
| `use-profile.ts` `useNotificationPrefsMutation()` | `PATCH /api/settings/notifications` | `user_settings.notification_*` |
| `use-team.ts` `useTeam()` | `GET /api/team` | `teams.*`, `team_members.*` joined |
| `use-team.ts` `useInviteTeamMember()` | `POST /api/team/invite` | `team_members.{invited_email, role, status, team_id}` |
| `use-team.ts` `useRemoveTeamMember()` | `DELETE /api/team/members/[id]` | `team_members.id` |
| `use-creator-profile.ts` `useCreatorProfile()` | `/api/profile/creator-profile` | `creator_profiles.*` (out of `user_settings` scope but in same audit per D-05) |

Verify each row by reading the named hook file (paths in `<read_first>`). If a hook is not present in the source, mark the row "NOT FOUND — investigate" and surface as a deviation (do not silently delete the row).

### Section 5 — Hand-Patch vs Live Schema Diff

Move the contents of `<!-- SCRATCH §5 SQL -->` (placed by Task 1) into a fenced `sql` block here. Then for each of the three tables, produce a side-by-side check:

```
## 5.1 user_settings

Hand-patched block (current src/types/database.types.ts lines 1479–1517):
- avatar_url: string | null
- company: string | null
- display_name: string | null
- notification_email_updates: boolean
- notification_marketing: boolean
- notification_test_results: boolean
- notification_weekly_digest: boolean
- role: string | null
- updated_at: string
- user_id: string

Live information_schema.columns (from §2.3 query):
[paste filtered SQL rows for user_settings here]

DIFF:
- Column count: <N> hand-patched vs <N> live — MATCH | MISMATCH (if mismatch, list deltas)
- Nullability per column: MATCH | MISMATCH
- Type mapping (e.g. text → string, boolean → boolean): MATCH | MISMATCH
- Defaults: documented (do not affect TS types but record for migration reference)

Result: IDENTITY CONFIRMED | DRIFT DETECTED — if drift, log columns and recommendation.
```

Repeat the same 5.x sub-section structure for `teams` (5.2) and `team_members` (5.3) using the hand-patched ranges 1207–1227 (teams) and 1166–1206 (team_members) from PATTERNS.md.

Also re-check (per D-08) these tables, which had columns added in commits `1c590ac`, `37a7fa4`, `8eec2a9`, `5ee7693`:
- `creator_profiles`
- `analysis_results`
- `platt_parameters`
- `trending_sounds`

For each, run a brief grep of the type file vs an information_schema query (extend the SQL in §2.3 with `OR table_name IN ('creator_profiles', 'analysis_results', 'platt_parameters', 'trending_sounds')`) and record under §5.4 as "No drift detected" or list deltas. These four tables are autogen-from-live now; this section confirms no regression.

### Section 6 — Path Decision

```markdown
## 6. Path Decision

**Decision: Path (a) — migrate, no new SQL work needed.**

Justification chain (each point cites evidence):
1. Live schema already has `user_settings`, `teams`, `team_members` applied (§2.1, §2.2 — Supabase MCP `list_migrations` rows `20260519113322` and `20260519113337`).
2. All 9 consumer call sites are reached from deployed UI via React Query hooks (§3 + §4) — none are dead routes.
3. Hand-patched type blocks match live schema with zero drift (§5.1, §5.2, §5.3 — IDENTITY CONFIRMED).
4. `tsc --noEmit` baseline is already 0 errors (§1) — no error count to drive a different decision.

Path (b) — rip out — is rejected because consumers are live and the table exists. Rip-out would delete working features.

D-03 / D-04 recap: no new SQL migration is written or applied in Phase 14. Phase 14 work is reduced to type regen (Plan 02) plus this audit doc.

Phase 14 exit gates (per D-11) — Plan 02 must satisfy:
1. `pnpm exec tsc --noEmit` → 0 errors
2. `pnpm build` → green
3. `pnpm vitest run` → unchanged from baseline
4. This audit doc checked in (this plan)
5. `database.types.ts` regen committed atomically as `feat(14): regenerate database.types.ts from live schema` (Plan 02)
```

Remove the `<!-- SCRATCH §5 SQL -->` comment after consuming its contents into §5.
  </action>
  <verify>
    <automated>grep -q "^## 3. Consumer Call Sites" .planning/research/user-settings-audit.md && grep -q "^## 4. Hook → Route → Column Trace" .planning/research/user-settings-audit.md && grep -q "^## 5. Hand-Patch vs Live Schema Diff" .planning/research/user-settings-audit.md && grep -q "^## 6. Path Decision" .planning/research/user-settings-audit.md && ! grep -q "SCRATCH §5 SQL" .planning/research/user-settings-audit.md && grep -q "Path (a) — migrate" .planning/research/user-settings-audit.md && pnpm exec tsc --noEmit 2>&1 | tail -1 | grep -qE "No errors found|Found 0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## " .planning/research/user-settings-audit.md` ≥ 6 (sections 1–6 all present)
    - `grep -c "user_settings" .planning/research/user-settings-audit.md` ≥ 9 (one per grep hit minimum, per quality_gate hint)
    - `grep -c "src/app/api/profile/route.ts\|src/app/api/profile/avatar/route.ts\|src/app/api/settings/notifications/route.ts" .planning/research/user-settings-audit.md` ≥ 3 (all three named in §3)
    - `grep -c "use-profile.ts\|use-team.ts\|use-creator-profile.ts" .planning/research/user-settings-audit.md` ≥ 3 (all three hooks named in §4)
    - `grep -q "Path (a)" .planning/research/user-settings-audit.md` exits 0
    - `grep -q "IDENTITY CONFIRMED\|DRIFT DETECTED" .planning/research/user-settings-audit.md` exits 0 (per-table diff verdict written in §5)
    - `! grep -q "SCRATCH" .planning/research/user-settings-audit.md` (scratch buffer consumed)
    - `wc -l .planning/research/user-settings-audit.md | awk '{print $1}'` ≥ 120 (min_lines from must_haves)
    - `pnpm exec tsc --noEmit 2>&1 | tail -1` matches `No errors found` or `Found 0 errors` (D-02 gate held)
  </acceptance_criteria>
  <done>Audit doc complete with all 6 D-05 content items; tsc still 0 errors; path (a) decision logged with evidence chain referencing §1–§5; scratch buffer removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner → Supabase MCP | Read-only metadata queries (`list_migrations`, `list_tables`, `information_schema.columns`); no row data accessed |
| Audit doc → git repo | Audit committed to public-by-default repo (project is on GitHub) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | I (Information disclosure) | `user-settings-audit.md` content | mitigate | Audit doc contains only schema metadata (column names, types, RLS status). No row data, no PII, no secrets. Verify by grep before commit: `grep -iE "password|secret|token|email.*@" .planning/research/user-settings-audit.md` must return no matches outside section headers. |
| T-14-02 | I | Supabase MCP credentials | accept | MCP server-side credentials, not handled by this plan. Already in scope of MCP runtime trust boundary; documented in `.planning/MILESTONE.md`. |
| T-14-03 | T (Tampering) | Audit doc claims vs reality | mitigate | Every claim in audit doc cites either an MCP output, a grep result, or a file:line reference. Reviewers can re-run the commands embedded in §1/§2/§3 to verify. |
| T-14-04 | R (Repudiation) | Path decision provenance | mitigate | §6 decision chain references D-03 + specific evidence sections; audit is committed via signed/hooked commit (project enforces `git config core.hooksPath .githooks`). |
</threat_model>

<verification>
- `test -f .planning/research/user-settings-audit.md` exits 0
- All 6 D-05 sections present (`## 1.` through `## 6.`)
- `pnpm exec tsc --noEmit` returns 0 errors (re-run as TYPES-05 sanity check at plan end)
- Path decision in §6 explicitly says "Path (a)" matching D-03
- No PII/secrets in audit doc (`grep -iE "password|secret|token|api[_-]key" .planning/research/user-settings-audit.md` returns no matches in body text)
</verification>

<success_criteria>
- [ ] `.planning/research/user-settings-audit.md` exists with frontmatter (phase, audited, status, findings object)
- [ ] All 6 required sections present per D-05
- [ ] All 9 user_settings grep hits enumerated in §3 with file:line + reachability + fields-touched + breakage columns
- [ ] §5 contains per-table IDENTITY CONFIRMED or DRIFT DETECTED verdict for user_settings, teams, team_members
- [ ] §5.4 covers the four D-08 drift-check tables (creator_profiles, analysis_results, platt_parameters, trending_sounds)
- [ ] §6 logs Path (a) decision with evidence chain referencing earlier sections
- [ ] `pnpm exec tsc --noEmit` still returns 0 errors at plan exit (D-02 gate)
- [ ] No scratch HTML comments remain in committed file
- [ ] Threat register validated (no PII leaked in audit doc body)
</success_criteria>

<output>
After completion, create `.planning/phases/14-type-hygiene-user-settings-resolution/14-01-SUMMARY.md` per execute-plan workflow.
</output>
