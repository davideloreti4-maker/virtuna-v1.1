---
phase: 04-live-audience-node-the-killer-feature
plan: 11
type: execute
wave: 6
depends_on: [04-10, 04-09, 04-01]
files_modified:
  - .planning/phases/04-live-audience-node-the-killer-feature/04-VERIFICATION-NOTES.md
autonomous: false
requirements: [R1.2, R2.2, R2.3]
user_setup:
  - service: supabase
    why: "BLOCKING schema push — applies the audience_overrides migration to the linked Supabase project"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (required for non-TTY db push)"
must_haves:
  truths:
    - "supabase db push --linked succeeds; analysis_results.analysis_override JSONB column exists on linked DB"
    - "creator_persona_weights table exists with RLS policies cpw_select_own + cpw_upsert_own"
    - "Full Phase 4 test suite green: npx vitest run src/components/board/audience src/app/api/analyze/[id]/override src/lib/engine/wave3/__tests__/weighted-aggregator-client.test.ts src/hooks/queries/__tests__/use-analysis-stream.filmstrips.test.ts"
    - "npx next build (or equivalent) succeeds with zero TypeScript errors"
    - "Mobile portrait verification: heatmap drawer + persona inspector + weight override drawer all open as bottom sheets on iPhone-class viewport"
    - "60fps target on iPhone 13+, 45fps on iPhone 11+ during streaming choreography (manual device test, human verify)"
  artifacts:
    - path: ".planning/phases/04-live-audience-node-the-killer-feature/04-VERIFICATION-NOTES.md"
      provides: "Manual verification record (FPS measurement, mobile screenshot review, supabase push log)"
      contains: "supabase db push"
  key_links:
    - from: "Local migration file"
      to: "Linked Supabase project"
      via: "supabase db push --linked"
      pattern: "supabase db push"
---

<objective>
Final integration & ship gate for Phase 4. Three tasks: (1) BLOCKING schema push (forces the audience_overrides migration onto the linked Supabase project — Phase 4 CANNOT pass verification without it per the orchestrator's blocking requirement), (2) end-to-end automated test sweep across all Phase 4 surfaces + Next build, (3) manual checkpoint for mobile portrait + performance verification per ROADMAP success criteria.

Output: 1 verification notes file capturing the manual check results + a green CI surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (§Phase 4 success criteria)
@.planning/REQUIREMENTS.md (§R1.2 mobile portrait + NF1 perf tiers)
@.planning/phases/04-live-audience-node-the-killer-feature/04-CONTEXT.md
@supabase/migrations/20260527000000_audience_overrides.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] supabase db push --linked + verify columns/tables</name>
  <files>.planning/phases/04-live-audience-node-the-killer-feature/04-VERIFICATION-NOTES.md</files>
  <read_first>
    - supabase/migrations/20260527000000_audience_overrides.sql (Plan 04-01 — migration file to apply)
    - Existing migration list: `ls supabase/migrations/ | tail -5` — confirm migration is newest by timestamp
    - .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-03-PLAN.md (look for the prior BLOCKING schema push task — mirror the verification approach)
  </read_first>
  <action>
    Ensure `SUPABASE_ACCESS_TOKEN` is exported in the environment (`echo $SUPABASE_ACCESS_TOKEN | wc -c` must return > 1). If missing, instruct user to set it from Supabase Dashboard → Account → Access Tokens, then re-run this task.

    Run:
    ```bash
    cd /Users/davideloreti/virtuna-result-surface
    supabase db push --linked
    ```

    Capture the full stdout + stderr.

    On success, verify the schema landed by querying the live DB. Use the Supabase MCP tools or a direct SQL execution:
    ```bash
    # Via Supabase CLI (preferred):
    supabase db diff --linked --schema public 2>&1 | head -50
    # Should show empty diff after a successful push.
    ```

    Or via psql / mcp__supabase tools, run these SELECT statements and capture results:
    ```sql
    -- Verify column exists
    SELECT column_name, data_type
    FROM information_schema.columns
    WHERE table_name = 'analysis_results' AND column_name = 'analysis_override';
    -- Expect: 1 row, analysis_override, jsonb

    -- Verify table exists
    SELECT table_name FROM information_schema.tables
    WHERE table_schema = 'public' AND table_name = 'creator_persona_weights';
    -- Expect: 1 row

    -- Verify RLS enabled
    SELECT relname, relrowsecurity FROM pg_class
    WHERE relname = 'creator_persona_weights';
    -- Expect: relrowsecurity = true

    -- Verify policies
    SELECT policyname FROM pg_policies
    WHERE tablename = 'creator_persona_weights';
    -- Expect: cpw_select_own, cpw_upsert_own
    ```

    Create `.planning/phases/04-live-audience-node-the-killer-feature/04-VERIFICATION-NOTES.md` with a section:

    ```markdown
    # Phase 4 Verification Notes

    ## Schema Push (Task 1 — BLOCKING)

    **Migration:** `supabase/migrations/20260527000000_audience_overrides.sql`
    **Applied:** <ISO timestamp>
    **Linked project:** <output of `supabase projects list --linked` or equivalent>

    ### Push log (stdout)
    <verbatim output of supabase db push --linked>

    ### Schema verification queries (run via Supabase MCP / CLI)
    <output of each SELECT showing column / table / RLS / policies as expected>

    Status: GREEN
    ```

    If `supabase db push --linked` fails for any reason (auth, conflict, syntax), STOP — do NOT continue to Task 2/3. Report the failure verbatim and mark this checkpoint blocked.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-result-surface && test -f .planning/phases/04-live-audience-node-the-killer-feature/04-VERIFICATION-NOTES.md && grep -cE "Schema Push.*BLOCKING|Status: GREEN" .planning/phases/04-live-audience-node-the-killer-feature/04-VERIFICATION-NOTES.md | awk '$1>=2 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push --linked` exits with code 0 (or an idempotent "no changes to apply" success message if a prior run already pushed)
    - Verification queries confirm: `analysis_override` JSONB column present on `analysis_results`; `creator_persona_weights` table present; RLS enabled; both policies (cpw_select_own, cpw_upsert_own) registered
    - `.planning/phases/04-live-audience-node-the-killer-feature/04-VERIFICATION-NOTES.md` exists with the push log + verification query results + "Status: GREEN"
  </acceptance_criteria>
  <done>Schema is live on the linked Supabase project; verification record committed; Phase 4 unblocked for downstream consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Full Phase 4 test sweep + Next build + lint</name>
  <files>.planning/phases/04-live-audience-node-the-killer-feature/04-VERIFICATION-NOTES.md</files>
  <read_first>
    - .planning/phases/04-live-audience-node-the-killer-feature/04-VALIDATION.md (full test map)
    - CLAUDE.md (build + test commands)
  </read_first>
  <action>
    Run the full Phase 4 test sweep:

    ```bash
    cd /Users/davideloreti/virtuna-result-surface
    npx vitest run \
      src/components/board/audience \
      src/lib/engine/wave3/__tests__/weighted-aggregator-client.test.ts \
      src/app/api/analyze/\[id\]/override/__tests__ \
      src/hooks/queries/__tests__/use-analysis-stream.filmstrips.test.ts \
      --reporter=verbose 2>&1 | tee /tmp/phase4-vitest.log
    ```

    Then full type check + build:
    ```bash
    npx tsc --noEmit 2>&1 | tee /tmp/phase4-tsc.log
    npx next build 2>&1 | tee /tmp/phase4-build.log
    ```

    And lint:
    ```bash
    npm run lint 2>&1 | tee /tmp/phase4-lint.log
    ```

    Append to `.planning/phases/04-live-audience-node-the-killer-feature/04-VERIFICATION-NOTES.md`:

    ```markdown
    ## Automated Test Sweep (Task 2)

    ### Vitest summary
    - Test files passed: <N from log>
    - Tests passed: <M from log>
    - Tests failed: 0
    - Tests skipped (it.todo): <S from log>

    ### TypeScript
    - `npx tsc --noEmit`: <exit code; "0 errors" expected>

    ### Next build
    - `npx next build`: <success / fail with summary>
    - Bundle delta (Audience chunk): <approximate kB added>

    ### Lint
    - `npm run lint`: <exit code 0 expected>

    Status: GREEN
    ```

    If ANY of vitest / tsc / next build / lint fails, STOP. Report verbatim and mark this checkpoint blocked. Triage points to the corresponding earlier plan (vitest failures → component plan; tsc → type plan; build → integration plan).
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-result-surface && grep -cE "Vitest summary|Status: GREEN" .planning/phases/04-live-audience-node-the-killer-feature/04-VERIFICATION-NOTES.md | awk '$1>=2 {exit 0} {exit 1}' && npx vitest run src/components/board/audience --reporter=basic 2>&1 | grep -cE "0 failed" | awk '$1>=1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/board/audience src/lib/engine/wave3/__tests__/weighted-aggregator-client.test.ts src/app/api/analyze/\[id\]/override/__tests__ src/hooks/queries/__tests__/use-analysis-stream.filmstrips.test.ts` exits 0 with `0 failed`
    - `npx tsc --noEmit` exits 0 with no errors
    - `npx next build` exits 0 (succeeds)
    - `npm run lint` exits 0 (lint clean)
    - VERIFICATION-NOTES.md "Automated Test Sweep" section present with all four sub-sections (Vitest, TypeScript, Next build, Lint) marked GREEN
  </acceptance_criteria>
  <done>Full Phase 4 surface green: tests + types + build + lint. Verification record updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual mobile portrait + perf verification</name>
  <what-built>
    - AudienceNode plugged into the board at `/analyze`
    - Live streaming choreography (skeletons → row reveal → curve morph → markers)
    - Mobile portrait responsive behavior (heatmap drawer + persona inspector + weight override drawer all open as bottom sheets)
    - Weight override with client-side recompute (slider drag → curve morphs in real time)
    - Anti-virality dual-trigger visual treatment (orange band on curve, ⚠ chips below filmstrip)
  </what-built>
  <how-to-verify>
    Use any existing dev test fixture or a manually triggered analysis. Verification steps:

    **A. Desktop functional check (~5 min)**
    1. Start dev server: `npm run dev` (likely runs Next dev). Open http://localhost:3000/analyze in Chrome (desktop window 1440×900).
    2. Trigger a test analysis using whichever flow the project uses (existing TikTok URL paste / dev fixture / mock SSE — refer to STATE.md / Phase 2 work for the canonical dev path).
    3. Verify visual order top→bottom inside the Audience group frame: chips → filmstrip → curve+markers → "Show personas ▾" affordance.
    4. Tap "Show personas ▾" — heatmap grid expands inline; all 10 rows render in PERSONA_SLOT_ORDER (6 FYP, 2 niche, 1 loyalist, 1 cross-niche).
    5. Tap the weights badge (e.g. "Weighted: 65/20/10/5") — right-side WeightOverrideDrawer opens. Drag any slider — the curve animates continuously, headline chips update live, anti-virality treatment may appear/disappear.
    6. Click a heatmap cell — TapPopover appears anchored at click point with persona + segment + attention + reason (when present).
    7. Click a row label — PersonaInspector opens as right-side panel with archetype + Pass 1 verdict + segment_reasons + sparkline.

    **B. Mobile portrait check (~5 min)**
    1. Open Chrome DevTools → toggle device toolbar → iPhone 13 Pro (390×844) or use a real device via tunneling.
    2. Repeat steps 3-7 above. Expected differences:
       - Step 4: heatmap "Show personas" opens BOTTOM SHEET (max-height 70dvh), NOT inline expansion
       - Step 5: WeightOverrideDrawer opens BOTTOM SHEET
       - Step 7: PersonaInspector opens BOTTOM SHEET
    3. Verify the Audience node fits in viewport (chips + filmstrip + curve visible without horizontal scroll; heatmap accessed via drawer affordance).
    4. Verify pinch-zoom on the heatmap (when drawer open) does NOT conflict with Konva board pan — board behind drawer stays still during heatmap zoom.

    **C. Performance check (~10 min, real device only — DevTools emulation does NOT count)**
    1. Open Safari Web Inspector → Timelines on an iPhone 13 (or simulator with high-end profile if no device available).
    2. Connect device, navigate to deployed preview / local LAN URL.
    3. Trigger analysis. Record Timeline during the full Pass 2 stream (~10s window).
    4. Verify FPS stays ≥ 60 during the row reveal + curve morph + weight slider drag.
    5. If no iPhone 13 available, document with iPhone 11 (or closest) and confirm ≥ 45fps.
    6. If FPS drops below thresholds, the perf-tier auto-degradation should kick in (UI-SPEC §Perf-tier) — verify "Optimized for your device" toast appears.

    **D. Reduced-motion check (~2 min)**
    1. DevTools → Rendering → Emulate CSS media feature `prefers-reduced-motion: reduce`.
    2. Trigger analysis. Expected: no curve morph (jump-cut to final), no L→R cell wave (cells appear at full saturation immediately), markers fade in via opacity only (no scale animation).
  </how-to-verify>
  <resume-signal>
    After completing A/B/C/D, append a section to `.planning/phases/04-live-audience-node-the-killer-feature/04-VERIFICATION-NOTES.md`:

    ```markdown
    ## Manual Verification (Task 3)

    ### A. Desktop functional check — [PASS / FAIL with notes]
    ### B. Mobile portrait — [PASS / FAIL with screenshot path or device used]
    ### C. Performance (real device) — [PASS / FAIL with device model + observed FPS]
    ### D. Reduced-motion — [PASS / FAIL with notes]

    Status: [GREEN / RED — describe issues]
    ```

    Type `approved` if all four pass, or describe specific issues (referencing which plan to revisit). If RED on any item, identify the responsible plan (A/B issues → Plan 04-10 integration; C issues → Plan 04-06 RAF or Plan 04-07 CSS; D issues → Plan 04-06 reduced-motion test was insufficient).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local migration → Linked Supabase project | DDL change applied via authenticated CLI; idempotent (IF NOT EXISTS guards) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-23 | Tampering | Migration applied to wrong project | mitigate | `--linked` flag requires explicit project link; SUPABASE_ACCESS_TOKEN is user-scoped |
| T-04-24 | DoS | Long-running ALTER TABLE on a large table | accept | `ADD COLUMN IF NOT EXISTS` of a JSONB defaulting null is instant in Postgres (no rewrite) |
</threat_model>

<verification>
- Schema verification queries confirm DDL landed
- Full test suite, tsc, build, lint all green
- VERIFICATION-NOTES.md captures push log + test summary + manual notes
- Human checkpoint approved
</verification>

<success_criteria>
- Migration live on Supabase project
- Phase 4 test suite + Next build + lint all GREEN
- Manual mobile + perf verification documented and approved
- Phase 4 ROADMAP success criteria all met (or any RED items identified for follow-up)
</success_criteria>

<output>
After completion, create `.planning/phases/04-live-audience-node-the-killer-feature/04-11-SUMMARY.md`.
</output>
