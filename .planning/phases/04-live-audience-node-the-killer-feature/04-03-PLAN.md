---
phase: 04-live-audience-node-the-killer-feature
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/hooks/queries/use-analysis-stream.ts
autonomous: true
requirements: [R1.2, R2.2]
must_haves:
  truths:
    - "useAnalysisStream return shape includes filmstrips: Record<number, string>"
    - "filmstrip_segment_ready SSE event populates filmstrips state via setState updater"
    - "No existing consumer of useAnalysisStream regresses (all Phase 1/2 tests still green)"
  artifacts:
    - path: "src/hooks/queries/use-analysis-stream.ts"
      provides: "Extended dispatch table — handles filmstrip_segment_ready + returns filmstrips map"
      contains: "filmstrip_segment_ready"
  key_links:
    - from: "src/components/board/audience/Filmstrip.tsx (Plan 04-05)"
      to: "useAnalysisStream().filmstrips"
      via: "destructure return key"
      pattern: "const \\{ filmstrips.*\\} = useAnalysisStream"
---

<objective>
Wave 1 foundation: extend the existing Phase 1 `useAnalysisStream` hook to dispatch `filmstrip_segment_ready` SSE events into a new live `filmstrips: Record<number, string>` map (Open Question 5 resolution per RESEARCH §"Engine Data Contract"). This is the ONLY change to the shared streaming hook; all other Phase 4 work consumes the existing partial/result/phase/stages surface unchanged.

Isolated into its own plan because (a) it modifies a Phase 1 shared file (regression risk), (b) Phase 4 consumers in Wave 2+ depend on the new key, (c) keeping the change small and reviewable mitigates the cross-feature blast radius.

Purpose: surface filmstrip keyframes live during streaming (per O-4) without forcing consumers to subscribe to a second EventSource.
Output: single-file modification + accompanying regression-safe test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-live-audience-node-the-killer-feature/04-CONTEXT.md
@.planning/phases/04-live-audience-node-the-killer-feature/04-RESEARCH.md
@.planning/phases/04-live-audience-node-the-killer-feature/04-PATTERNS.md
@src/hooks/queries/use-analysis-stream.ts
@src/lib/engine/events.ts

<interfaces>
<!-- Existing return shape (verified at use-analysis-stream.ts lines 88-100 and 399). -->

```typescript
export interface AnalysisStreamReturn {
  start: (id: string) => void;
  result: PredictionResult | null;
  stages: StageEvent[];
  partial: { personas: PersonaStreamingPartial[] };
  panelReady: Record<string, boolean>;
  phase: 'idle' | 'streaming' | 'complete' | 'error';
  error: Error | null;
  reconnect: () => void;
  analysisId: string | null;
  // Phase 4 addition (this plan):
  filmstrips: Record<number, string>; // segment_idx → signed keyframe URL
}
```

StageEvent variant we're adding to dispatch (events.ts line 14):
```typescript
| { type: "filmstrip_segment_ready"; segment_idx: number; keyframe_uri: string }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dispatch filmstrip_segment_ready + expose filmstrips in return</name>
  <files>src/hooks/queries/use-analysis-stream.ts</files>
  <read_first>
    - src/hooks/queries/use-analysis-stream.ts ENTIRE file (≤500 lines) — pay attention to lines 88-100 (interface), 121-130 (state declarations), 153-225 (dispatch table), 399 (return)
    - src/lib/engine/events.ts (StageEvent union)
    - .planning/phases/04-live-audience-node-the-killer-feature/04-PATTERNS.md §"use-analysis-stream.ts (extend dispatch)" (lines 939-973)
    - .planning/phases/04-live-audience-node-the-killer-feature/04-RESEARCH.md §"Open question 5" (line 648) — resolution: extend dispatch + add 10th return key (option a)
  </read_first>
  <behavior>
    - When `filmstrip_segment_ready` event arrives, `setFilmstrips(prev => ({ ...prev, [segment_idx]: keyframe_uri }))` runs
    - The hook return shape gains exactly one new key `filmstrips: Record<number, string>` (defaults to `{}`)
    - On `start(id)` (new analysis), filmstrips is reset to `{}`
    - On `reconnect()`, filmstrips is preserved (don't lose keyframes received before drop)
    - All existing return keys remain unchanged; consumers that don't destructure `filmstrips` continue working
  </behavior>
  <action>
    1. Add new state hook near existing partial state (likely around lines 121-130):
       ```typescript
       const [filmstrips, setFilmstrips] = useState<Record<number, string>>({});
       ```

    2. In the dispatch `useCallback` (lines 153-225), inside the `'stage'` event handler, AFTER the existing `pass2_persona_end` branch, ADD:
       ```typescript
       } else if (ev.type === 'filmstrip_segment_ready') {
         setFilmstrips((prev) => ({
           ...prev,
           [ev.segment_idx]: ev.keyframe_uri,
         }));
       }
       ```
       Use type narrowing via `ev.type === 'filmstrip_segment_ready'` so TS infers the segment_idx/keyframe_uri fields (no `as` cast needed if the StageEvent union narrows correctly).

    3. In the `start(id)` callback, add `setFilmstrips({})` alongside other state resets.

    4. In `reconnect()`, do NOT reset filmstrips (the comment should say `// preserve filmstrips across reconnect — segments delivered before drop must persist`).

    5. In the `AnalysisStreamReturn` interface (lines 88-100), add the new key as the LAST field:
       ```typescript
       filmstrips: Record<number, string>;
       ```
       with a JSDoc comment: `/** Live map of segment_idx → signed keyframe URL. Populated by filmstrip_segment_ready SSE events (Phase 4). */`

    6. In the final `return` statement (line 399), add `filmstrips` to the returned object.

    7. Add a test file `src/hooks/queries/__tests__/use-analysis-stream.filmstrips.test.ts` (NEW — separate from existing tests so we don't touch existing test fixtures). Use `renderHook` from `@testing-library/react` + the `STREAMING_FIXTURE` from Plan 04-01. Assertions:
       - Initial `filmstrips` is `{}`
       - After firing a `filmstrip_segment_ready` event with `{ segment_idx: 3, keyframe_uri: 'https://example/k3.jpg' }`, `filmstrips[3] === 'https://example/k3.jpg'`
       - Multiple events accumulate (segment 3 + segment 5 → both present)
       - `start('new_id')` resets to `{}`

       Use the existing mock EventSource pattern from any existing use-analysis-stream test; if none, mock `global.EventSource` minimally to dispatch events synchronously.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-result-surface && grep -c "filmstrip_segment_ready" src/hooks/queries/use-analysis-stream.ts | awk '$1>=1 {exit 0} {exit 1}' && grep -c "filmstrips: Record<number, string>" src/hooks/queries/use-analysis-stream.ts | awk '$1>=2 {exit 0} {exit 1}' && npx vitest run src/hooks/queries/__tests__ --reporter=basic 2>&1 | grep -cE "0 failed" | awk '$1>=1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "filmstrip_segment_ready" src/hooks/queries/use-analysis-stream.ts` returns ≥ 1
    - `grep -c "setFilmstrips" src/hooks/queries/use-analysis-stream.ts` returns ≥ 2 (one in dispatch, one in start reset)
    - `grep -c "filmstrips: Record<number, string>" src/hooks/queries/use-analysis-stream.ts` returns ≥ 1 (in interface)
    - `grep -c "filmstrips," src/hooks/queries/use-analysis-stream.ts` returns ≥ 1 (in return object)
    - `npx vitest run src/hooks/queries/__tests__` exits 0 (existing Phase 1 tests + new filmstrips test all green)
    - New test file `src/hooks/queries/__tests__/use-analysis-stream.filmstrips.test.ts` exists with ≥ 4 it() blocks (initial empty, single update, accumulate, reset on start)
    - `npx tsc --noEmit` reports zero errors
  </acceptance_criteria>
  <done>Hook extended additively, 4+ new tests green, all existing Phase 1 streaming tests still green, no `as` casts added (proper type narrowing).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE EventSource → React state | filmstrip_segment_ready arrives from server engine via EventSource; URLs are server-generated signed URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | keyframe_uri values in dispatch | accept | URLs are server-generated signed URLs (Phase 3 storage.ts); React renders them via `<img src>` which itself sanitizes URL schemes |
| T-04-07 | Information disclosure | filmstrips persists across reconnect | accept | Same analysis context; reconnect implies same authenticated session |
</threat_model>

<verification>
- `npx vitest run src/hooks/queries/__tests__` exits 0 (existing + new tests green)
- `grep -rn "useAnalysisStream" src/` shows no breakage in existing consumers (they don't destructure filmstrips — backwards compatible)
</verification>

<success_criteria>
- Single-file additive change to a Phase 1 shared hook
- New `filmstrips` return key populated live from SSE
- ≥ 4 new tests cover the new state machine
- Zero existing-test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-live-audience-node-the-killer-feature/04-03-SUMMARY.md`.
</output>
