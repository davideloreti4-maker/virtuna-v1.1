---
phase: 06-predict-verb
plan: 07
type: execute
wave: 4
depends_on: ["06-04", "06-06"]
files_modified:
  - src/lib/tools/chain-handoff.ts
  - src/lib/tools/profile-blocks.ts
  - src/lib/tools/runners/simulate-runner.ts
  - src/components/thread/reaction-distribution-block.tsx
autonomous: false
requirements: [PRED-01]
must_haves:
  truths:
    - "The Simulate reaction-distribution card renders a 'Predict an outcome →' CTA that POSTs to /api/tools/predict and renders the gauge in the SAME open thread (D-06)"
    - "The CTA carries the panel audienceId the user just simulated (additive optional field), and renders only when the simulate was a panel (not a person) (D-03/A3)"
    - "The full Simulate → Predict flow completes end-to-end in one thread, human-verified in a real browser (the gauge reads as a band+range, never an oracle)"
  artifacts:
    - path: "src/lib/tools/chain-handoff.ts"
      provides: "'predict' SkillId + the simulate→predict CHAIN_HANDOFFS entry"
      contains: "Predict an outcome"
    - path: "src/components/thread/reaction-distribution-block.tsx"
      provides: "the additive Predict chain-CTA on the Simulate card"
      contains: "handoffsFor"
---

<objective>
Wire the minimal D-06 trigger so the thread works end-to-end: add `"predict"` to the chain SSOT and append the `simulate→predict` handoff, then make the Phase-5 Simulate `reaction-distribution` card actually RENDER that chain-CTA (a real additive UI edit — the card renders NO chain CTA today). Carry the panel `audienceId` the user just simulated via an additive optional field, and render the CTA only when the simulate was a panel (predicting from a person simulate is nonsensical — D-03). Close with a real-browser human-verify of the one-thread Simulate → Predict wow (the P5 precedent — vitest cannot catch the gauge's visual honesty or a bundle leak).

Purpose: PRED-01 reachable + human-verifiable end-to-end (D-06 minimal trigger). NO front-door picker / mode menu / ambient reactor (all P7); creator composer byte-identical.
Output: the chain-CTA on the Simulate card + the GREEN `chain-handoff.test.ts`; an approved end-to-end thread.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-predict-verb/06-RESEARCH.md
@.planning/phases/06-predict-verb/06-PATTERNS.md
@.planning/phases/06-predict-verb/06-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: chain-handoff simulate→predict entry + audienceId carry + the Simulate-card CTA</name>
  <files>src/lib/tools/chain-handoff.ts, src/lib/tools/profile-blocks.ts, src/lib/tools/runners/simulate-runner.ts, src/components/thread/reaction-distribution-block.tsx</files>
  <read_first>
    - src/lib/tools/chain-handoff.ts (the `SkillId` union at lines 52-60, the `CHAIN_HANDOFFS` entries + the profile→simulate entry at 235-241, `handoffsFor` at 255-257)
    - src/components/thread/profile-read-block.tsx (THE CTA analog — `buildSimulateRequest` pure seam at 52-57, `handoffsFor(...).find` at 78, the `fetch` + `Simulating…`/disabled state at 92-113, the neutral cream CTA styling at 260-275 — never coral)
    - src/components/thread/reaction-distribution-block.tsx (the GAP — footer at lines 153-156 has ONLY `<SaveAffordance>`; add the CTA here; read its props/`subjectKind` to gate render on panel-only)
    - src/lib/tools/profile-blocks.ts (`ReactionDistributionBlockSchema.props` at 80-114 — add the optional `audienceId`; `.strict()`-safe since declared)
    - src/lib/tools/runners/simulate-runner.ts (populate `audienceId` — `audience.id` is in scope at line ~151; do NOT change any other behavior — byte-stable simulate output otherwise)
    - .planning/phases/06-predict-verb/06-PATTERNS.md § "Chain handoff simulate → predict" (the two GAPs + the recommended optional-field fix A3)
  </read_first>
  <action>
    Four additive edits:
    1. `chain-handoff.ts`: add `"predict"` to the `SkillId` union; append `{ from: "simulate", to: "predict", ctaLabel: "Predict an outcome →", endpoint: "/api/tools/predict", anchorFrom: "card" }` to `CHAIN_HANDOFFS` (mirror the profile→simulate entry). This turns the Wave-0 `chain-handoff.test.ts` assertion GREEN.
    2. `profile-blocks.ts`: add `audienceId: z.string().optional()` to `ReactionDistributionBlockSchema.props` (additive, `.strict()`-safe). Do NOT touch `PredictionGaugeBlockSchema`.
    3. `simulate-runner.ts`: populate `audienceId: audience.id` on the assembled reaction-distribution block (additive only — no other change; the rest of the simulate output stays byte-stable, and its existing tests must stay green).
    4. `reaction-distribution-block.tsx`: render the predict chain-CTA in the footer beside `SaveAffordance`. Copy the `profile-read-block.tsx` fetch+state pattern: `const handoff = handoffsFor("simulate").find(h => h.to === "predict")`; on click POST `{ audienceId: block.props.audienceId, scenario }` to `handoff.endpoint` with a `Predicting…`/disabled state; on success the gauge block renders in the SAME open thread (the existing thread refresh/poll surfaces it — mirror how the Simulate CTA already lands in-thread). Render the CTA ONLY when the simulate was a panel (gate on the block's panel signal / `subjectKind === "panel"`; a person simulate shows no predict CTA — D-03). Styling: neutral cream secondary text button (mirror "Develop this →"/"Simulate…"), NEVER coral. If `audienceId` is absent, omit the CTA (back-compat fallback).
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/tools/__tests__/chain-handoff.test.ts src/lib/tools/runners/__tests__/simulate-runner.test.ts src/components/reading/__tests__/reskin-matte.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `handoffsFor("simulate").find(h => h.to === "predict")` returns an entry with `ctaLabel === "Predict an outcome →"` and `endpoint === "/api/tools/predict"` (behavior — D-06; chain-handoff.test.ts GREEN)
    - `ReactionDistributionBlockSchema.safeParse({ ...validPanelProps, audienceId: "x" }).success === true` and the existing simulate-runner tests stay green (additive, no regression)
    - `reaction-distribution-block.tsx` calls `handoffsFor` and POSTs to `/api/tools/predict` (source assertion)
    - `grep -ci "rgba(255,127,80\|#FF7F50\|coral" src/components/thread/reaction-distribution-block.tsx` returns `0`; `reskin-matte.test.ts` green (neutral cream CTA, F-07)
    - The CTA renders only for a panel simulate (source/behavior assertion — D-03)
  </acceptance_criteria>
  <done>The Simulate card renders the Predict CTA carrying the panel audienceId; chain GREEN; matte; simulate output back-compat.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: end-to-end one-thread Simulate → Predict human-verify (real browser)</name>
  <action>Blocking human-verify checkpoint (no code change). After Task 1 lands and the full suite + `next build` are green, run the app in a real browser and walk the how-to-verify steps to confirm the end-to-end Simulate → Predict one-thread wow and the gauge's visual honesty (which vitest cannot assert). Pause for the operator; resume on "approved".</action>
  <what-built>
    The full Predict verb: scenario → analyst panel reasoning → `aggregatePredict` (band + panel-spread range + confidence + factors) → a `prediction-gauge` card persisted to the one open thread, reachable via the "Predict an outcome →" CTA on the Simulate card. Always Directional, always shows assumptions + receipts.
  </what-built>
  <how-to-verify>
    1. Run `next dev` (heap cap ≥ 3072 — long Playwright waits OOM at 768; memory `lane-shell-premium-thread`). Auth via `e2e/create-test-user.ts`.
    2. In a thread, run a Simulate against the Analyst Panel (or any panel-type General audience) so the `reaction-distribution` card renders. Confirm the new `Predict an outcome →` CTA appears (neutral cream, never coral) on a PANEL simulate, and is ABSENT on a person simulate.
    3. Click it; drop/confirm a scenario. Confirm the `prediction-gauge` card renders IN THE SAME THREAD with: a band WORD (cream, not colored), a `~min–max%` caption (tilde, en-dash, no decimal, no single value), a `Confidence: {word}` pill, a FEATHERED span (no needle/tick/dot), the factors each naming an analyst, a collapsible panel drill (qualitative lean words, no per-analyst numbers), an Assumptions section with `Judged against:`, an always-on Directional caveat, `SIM-1 Flash` + the Directional TrustBadge.
    4. Honesty check: removing color in DevTools, the forecast still reads completely from words + the `~min–max%` caption (F-01/F-03). Zero terracotta accent on the card (F-03).
    5. Person-SIM reject: trigger Predict against a person audience (e.g. via the API) → confirm an inline 400 nudge ("Predict needs a panel"), never a 500/toast/crash (D-08/F-06).
    6. `next build` compiles (judge "no NEW errors on touched paths"; ignore the pre-existing earnings-chart.tsx:98 recharts baseline — Pitfall 6). Confirm the creator/Socials composer path is unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" to close PRED-01/02/03 + Phase 6, or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| card → API | The CTA POSTs `audienceId` + scenario to `/api/tools/predict`; the route re-authenticates + re-resolves under RLS (the card is not trusted) |
| persisted block JSON → renderer | The additive `audienceId` is validated by `ReactionDistributionBlockSchema` on rehydration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-21 | Tampering | the chain CTA fetch | mitigate | the card passes only `audienceId` + scenario; the route (06-06) re-auths, CSRF-guards, RLS-resolves, and re-applies the D-08 400 guards — the client cannot bypass them |
| T-06-22 | Tampering (honesty) | ReactionDistributionBlockSchema audienceId | mitigate | additive optional field; `.strict()`-safe; no smuggled aggregate; simulate output otherwise byte-stable |
| T-06-23 | UX honesty | person-simulate predict | mitigate | CTA gated to panel simulates only (D-03); a person path is rejected at the route 400 as defense-in-depth |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/lib/tools/__tests__/chain-handoff.test.ts src/lib/tools/runners/__tests__/simulate-runner.test.ts src/components/reading/__tests__/reskin-matte.test.ts` exits 0.
- Phase gate: full suite `node ./node_modules/vitest/vitest.mjs run` green + `next build` compiles + the Task-2 human-verify approved.
</verification>

<success_criteria>
- The Simulate → Predict flow completes end-to-end in one thread (D-06), human-verified in a real browser.
- The CTA carries the simulated panel's audienceId, renders panel-only, neutral cream (F-07); chain SSOT GREEN.
- Creator/Socials composer byte-identical; no P7 surface leaked in; gauge reads honestly (no oracle).
</success_criteria>

## Artifacts this plan produces
- `"predict"` SkillId + the `simulate→predict` entry in `chain-handoff.ts`; optional `audienceId` on `ReactionDistributionBlockSchema.props` (`profile-blocks.ts`); `audienceId` populated in `simulate-runner.ts`; the Predict chain-CTA in `reaction-distribution-block.tsx`.

<output>
Create `.planning/phases/06-predict-verb/06-07-SUMMARY.md` when done.
</output>
