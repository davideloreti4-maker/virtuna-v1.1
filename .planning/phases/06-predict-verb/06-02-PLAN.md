---
phase: 06-predict-verb
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - src/lib/engine/flash/predict-aggregate.ts
autonomous: true
requirements: [PRED-01]
must_haves:
  truths:
    - "The displayed range is the min–max of the panel's per-analyst leans — derived only from aggregation, never a model field (D-01)"
    - "Two different panel disagreement profiles produce two different ranges (panel-grounded, not a decorative fixed band→range map) (D-01)"
    - "Confidence is the tightness of that spread — tight→High, wide→Low — pure, no extra call, no model self-report (D-05)"
  artifacts:
    - path: "src/lib/engine/flash/predict-aggregate.ts"
      provides: "aggregatePredict — the pure honest collapse (band/range/confidence/factors)"
      contains: "export function aggregatePredict"
---

<objective>
The heart of the phase: the ONE genuinely novel module. `predict-aggregate.ts` collapses the panel's per-analyst ordinal leans into a likelihood BAND (center) + a coarse RANGE (panel spread, the single sanctioned numeric) + CONFIDENCE (range tightness) + clustered FACTORS (receipts). Pure, leaf-isolated, zero network — it takes ONLY `PredictAnalyst[]`, which makes the D-01 honesty guarantee structural: the range cannot come from a model field because no model field is in scope.

Purpose: satisfy D-01 (panel-derived range, never fabricated/model-emitted) and D-05 (confidence = tightness) with the same pure-derivation discipline as `aggregateFlash` / `two-audience-read.buildDelta`.
Output: `aggregatePredict` — turns the Wave-0 `predict-aggregate.test.ts` GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-predict-verb/06-RESEARCH.md
@.planning/phases/06-predict-verb/06-PATTERNS.md
@src/lib/engine/flash/predict-schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: aggregatePredict — pure panel-derived band/range/confidence/factors</name>
  <files>src/lib/engine/flash/predict-aggregate.ts</files>
  <behavior>
    - range = exact { min: Math.min(...positions), max: Math.max(...positions) } where positions = each analyst's lean mapped through LEAN_POS (D-01 derivation)
    - two distributions with different spreads → different ranges (panel-grounded; NOT a fixed band→center→range lookup)
    - confidence: spread ≤ 15 → "High"; ≤ 40 → "Medium"; else "Low" (D-05 tightness)
    - band = bandFromCenter(median(positions)) ∈ {Likely, Lean yes, Lean no, Toss-up, Unlikely}
    - factors = analysts.map → { analystArchetype, driver, direction } (D-04 receipts, every factor names its analyst)
    - same input → identical output (pure)
  </behavior>
  <read_first>
    - src/lib/engine/flash/flash-aggregate.ts (THE analog — named test-locked thresholds like `STRONG_THRESHOLD`/`MIXED_THRESHOLD` at lines 124-177; pure, return type carries NO score)
    - src/lib/engine/flash/two-audience-read.ts (the `buildDelta` pure-derivation discipline at lines 114-136 — interpretation computed FROM a code-constant rank map, never a model field)
    - src/lib/engine/flash/predict-schema.ts (imports `PredictAnalyst` type — DO NOT re-declare it here)
    - .planning/phases/06-predict-verb/06-RESEARCH.md § "The Novel Aggregation" (LEAN_POS, the D-01 honesty nuance A1, confidence thresholds A2)
    - .planning/phases/06-predict-verb/06-PATTERNS.md § "predict-aggregate.ts" (the exact collapse to build, lines 164-176)
  </read_first>
  <action>
    Create `src/lib/engine/flash/predict-aggregate.ts` importing ONLY `PredictAnalyst` from `./predict-schema` (leaf isolation — no qwen/pipeline/aggregator imports). Export, per D-01/D-05 and A1/A2:
    - `const LEAN_POS = { strongly_no:10, lean_no:35, toss_up:50, lean_yes:65, strongly_yes:90 } as const;` — the per-analyst ordinal→position map (this is NOT the rejected "fixed band→range map" — it maps each analyst's lean, so the range moves with actual disagreement).
    - Named, test-locked confidence thresholds (mirror `STRONG_THRESHOLD` naming): e.g. `CONF_TIGHT = 15`, `CONF_MID = 40`.
    - `median(nums: number[])` and `bandFromCenter(center: number): "Likely"|"Lean yes"|"Lean no"|"Toss-up"|"Unlikely"` helpers (pure; document the center cutoffs).
    - `export function aggregatePredict(analysts: PredictAnalyst[]): { band, range: { min, max }, confidence, factors }` exactly as the behavior block specifies. The range is the ONLY numeric; it is `Math.min`/`Math.max` of the mapped positions — never read from any field other than the analyst leans. Factors map each analyst's `factor`/`factorDirection` → `{ analystArchetype: a.archetype, driver: a.factor, direction: a.factorDirection }`.
    Match the field names exactly to `PredictionGaugeBlockSchema.props` (06-04) so the 06-05 runner can assemble without adapters.
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/engine/flash/__tests__/predict-aggregate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `predict-aggregate.ts exports function aggregatePredict` (source assertion)
    - `aggregatePredict(tightPanel).range !== aggregatePredict(splitPanel).range` as objects (behavior — panel-grounded, D-01)
    - For a panel whose mapped positions are [35,65], `range === {min:35, max:65}` (behavior — derivation, D-01)
    - A spread ≤15 yields `confidence === "High"`; a spread >40 yields `confidence === "Low"` (behavior — D-05)
    - `grep -c "qwen\|pipeline\|getQwenClient\|aggregateScores" src/lib/engine/flash/predict-aggregate.ts` returns `0` (leaf isolation — no model call)
    - Command above exits 0 (predict-aggregate.test.ts GREEN)
  </acceptance_criteria>
  <done>aggregatePredict pure, leaf-isolated, GREEN against the Wave-0 derivation/confidence guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none external) | Pure function over already-validated `PredictAnalyst[]`; no I/O, no network, no untrusted string sink |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering (honesty / false precision) | aggregatePredict | mitigate | Function takes ONLY the analyst array; range = `Math.min`/`Math.max` of mapped leans — structurally impossible to source a number from a model field (D-01). Wave-0 test asserts derivation + panel-grounding. |
| T-06-04 | Tampering (oracle precision) | LEAN_POS / range | accept | Ordinal→position map is a fixed code constant (A1); range still moves with real disagreement. Risk accepted because it is panel-grounded and the renderer (06-04) never shows a decimal/single value (F-01). |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/lib/engine/flash/__tests__/predict-aggregate.test.ts` exits 0.
- `grep -rc "import" src/lib/engine/flash/predict-aggregate.ts` shows only `./predict-schema` as a project import (leaf isolation).
</verification>

<success_criteria>
- The range is provably the min–max of the panel's leans (D-01), confidence is its tightness (D-05), factors name their analyst (D-04 receipts).
- Pure, same-input-same-output, zero network; field names match `PredictionGaugeBlockSchema` for adapter-free assembly in 06-05.
</success_criteria>

## Artifacts this plan produces
- `src/lib/engine/flash/predict-aggregate.ts` — `aggregatePredict`, `LEAN_POS`, `bandFromCenter`, `median`, named confidence thresholds.

<output>
Create `.planning/phases/06-predict-verb/06-02-SUMMARY.md` when done.
</output>
