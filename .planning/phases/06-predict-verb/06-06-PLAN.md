---
phase: 06-predict-verb
plan: 06
type: execute
wave: 3
depends_on: ["06-05"]
files_modified:
  - src/app/api/tools/predict/route.ts
autonomous: true
requirements: [PRED-01, PRED-03]
must_haves:
  truths:
    - "POST /api/tools/predict mirrors the simulate spine: auth 401 → csrfGuard 415/403 → scenario cap 400 → getAudience under session (RLS) → run → persist to the one open thread (PRED-01)"
    - "A non-General audience → 400 (predict_requires_general_panel); a person-marked audience → 400 + nudge (predict_requires_panel) — never a 500 throw (D-08/D-03, folds WR-03)"
    - "The default template-analyst panel is NOT mis-rejected; internal failures return a generic 500 that never echoes err.message (Pitfall 3 / WR-02)"
  artifacts:
    - path: "src/app/api/tools/predict/route.ts"
      provides: "POST handler — security spine + D-08 400 guards + one-thread persist"
      contains: "export async function POST"
---

<objective>
Build `/api/tools/predict/route.ts` by cloning `simulate/route.ts` VERBATIM, then add the D-08 400 guards BEFORE the try block. The route is the untrusted HTTP boundary: it authenticates, CSRF-guards, caps the scenario, resolves the panel audience under the session (RLS), rejects a non-General/person audience as a 400 (folding the open P5 WR-03 fix), passes the (instruction-isolated, D-07) scenario to `runPredict`, and persists the gauge block to the one open thread.

Purpose: PRED-01 end-to-end run path; PRED-03 honest rejection (400, not a 500 leak); closes WR-03 (D-08) and the person-SIM reject (D-03).
Output: `POST` handler — turns the Wave-0 `route.test.ts` GREEN (401/415/400-empty/400-non-general/400-person/500-generic + template-analyst not mis-rejected).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-predict-verb/06-RESEARCH.md
@.planning/phases/06-predict-verb/06-PATTERNS.md
@src/lib/tools/runners/predict-runner.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: predict/route.ts — security spine clone + D-08 400 guards + one-thread persist</name>
  <files>src/app/api/tools/predict/route.ts</files>
  <read_first>
    - src/app/api/tools/simulate/route.ts (THE analog — clone the spine VERBATIM at lines 36-101: auth 401 BEFORE any DB/LLM, `csrfGuard`, scenario parse + `MAX_MESSAGE_LENGTH` cap, `getAudience` under session → null → 400 `audience_not_found`, the try { normalizeStimulus → createOpenThreadLazy → runPredict → insertMessage → Response.json({block}) } catch { console.error; generic 500 } — NEVER echo err.message)
    - src/lib/tools/runners/predict-runner.ts (`runPredict` + the shared `readSubjectKind` helper for the person guard)
    - src/lib/audience/audience-repo.ts (`getAudience` RLS-scoped; `SENTINEL_IDS` short-circuit so `template-analyst` resolves with no DB row, lines 223/421-423)
    - src/lib/engine/stimulus/normalize.ts (`normalizeStimulus({ kind:"text", text:scenario })` — Zod-validated, tier-tagged)
    - src/lib/threads/threads.ts + src/lib/threads/messages.ts (`createOpenThreadLazy`, `insertMessage` re-validate at write + KC stamp)
    - src/lib/http/csrf-guard.ts + src/lib/kc/kc-stamp.ts (the guard + stamp used in the analog)
    - .planning/phases/06-predict-verb/06-PATTERNS.md § "predict/route.ts" (the D-08 fold; guards go AFTER getAudience resolves, BEFORE the try/run)
    - .planning/phases/06-predict-verb/06-UI-SPEC.md § "Copywriting Contract" (the person-SIM rejection copy: heading "Predict needs a panel", warning tone, 400 not 500)
  </read_first>
  <action>
    Create `src/app/api/tools/predict/route.ts`. Clone the simulate spine verbatim, renaming `message`→`scenario` (accept `scenario` and/or `message` from the body), endpoint label `[/api/tools/predict]`, generic 500 string `"Predict failed"`. Cap the scenario with `MAX_MESSAGE_LENGTH = 2000` (empty/whitespace → 400 `scenario is required`; over-cap → 400). After `getAudience(supabase, audienceId)` resolves (null → 400 `audience_not_found`), add the D-08 400 guards BEFORE the try/run:
    - `if (audience.mode !== "general") return Response.json({ error: "predict_requires_general_panel" }, { status: 400 });`
    - `if (readSubjectKind(audience) === "person") return Response.json({ error: "predict_requires_panel", message: "Predict needs a panel — try the Analyst Panel." }, { status: 400 });` (import `readSubjectKind` from the runner — Pitfall 3: marker-absent general audiences are NOT "person", so `template-analyst` passes).
    Then the try block: `normalizeStimulus({ kind:"text", text: scenario })` → `createOpenThreadLazy(user.id)` → `runPredict({ audience, stimulus })` → `insertMessage(openThread.id, "assistant", [block], kcStamp().kcGenVersion)` → `Response.json({ block })`; the catch logs and returns the generic 500, NEVER echoing `err.message` (WR-02). D-07 is upheld structurally — the route hands the scenario to `runPredict`/`runPredictPanel` which isolates it in the delimited USER block; the route concatenates nothing into a prompt.
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/app/api/tools/predict/__tests__/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `route.ts exports async function POST` (source assertion)
    - No authed user → 401; missing/bad CSRF → 415/403; empty scenario → 400 (behavior)
    - `audience.mode !== "general"` → 400 `predict_requires_general_panel`; person-marked audience → 400 `predict_requires_panel` with the nudge message — NEVER a 500 (behavior — D-08/D-03)
    - A `template-analyst` audience (mode general, `custom_context:[]`) passes the guards (behavior — Pitfall 3)
    - A thrown internal error returns `{ status: 500 }` with NO `err.message` in the body — `grep -c "err.message\|error.message" src/app/api/tools/predict/route.ts` returns `0` (WR-02)
    - Command above exits 0 (route.test.ts GREEN)
  </acceptance_criteria>
  <done>The route runs the predict path end-to-end, rejects non-panel audiences as 400, leaks no error detail; GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (POST body) | The scenario + audienceId are untrusted client input crossing into auth/DB/LLM |
| session → audience (RLS) | The panel audience is resolved under the session; cross-tenant access must be impossible |
| internal error → client | Error detail must not leak to the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Spoofing / Elevation | auth.getUser 401 | mitigate | `auth.getUser()` 401 BEFORE any DB/LLM; `user_id` session-derived, never from the body (CR-01) |
| T-06-15 | Tampering (CSRF) | csrfGuard | mitigate | `csrfGuard(request)` 415/403 (clone simulate/route.ts:51) |
| T-06-16 | DoS | scenario cap | mitigate | `MAX_MESSAGE_LENGTH = 2000` server-side cap; empty/oversize → 400 |
| T-06-17 | Info Disclosure | cross-tenant audience | mitigate | `getAudience` RLS-scoped under session; bad id → 400 `audience_not_found`, never raw weights |
| T-06-18 | Tampering/Elevation (LLM01) | scenario → prompt | mitigate | D-07: route concatenates nothing; the scenario is isolated downstream in the delimited USER block (run-predict-panel) |
| T-06-19 | Info Disclosure (error leakage) | catch block | mitigate | generic 500 `"Predict failed"`; never echo `err.message` (WR-02) |
| T-06-20 | Tampering (honesty / WR-03) | D-08 400 guards | mitigate | non-General → 400; person-marked → 400 + nudge BEFORE the try — never the runner's throw→500 path (folds WR-03) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (RESEARCH "Package Legitimacy Audit: not applicable"); no install task, no registry surface |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/app/api/tools/predict/__tests__/route.test.ts` exits 0.
- Wave gate (run after Wave 3 merge): full suite + `next build` (judge "compiles + no NEW errors on touched paths").
- `grep -c "predict_requires_general_panel\|predict_requires_panel" src/app/api/tools/predict/route.ts` === 2 (both D-08 guards present).
</verification>

<success_criteria>
- The route executes the predict run path and persists the gauge block to the one open thread (PRED-01).
- Non-General/person audiences are rejected as 400 with honest copy (D-08/D-03); WR-03 is closed.
- No auth/CSRF/RLS/DoS/error-leak gap; security_enforcement HIGH items all mitigated.
</success_criteria>

## Artifacts this plan produces
- `src/app/api/tools/predict/route.ts` — the `POST` handler.

<output>
Create `.planning/phases/06-predict-verb/06-06-SUMMARY.md` when done.
</output>
