---
phase: 06-predict-verb
plan: 05
type: execute
wave: 2
depends_on: ["06-02", "06-03", "06-04"]
files_modified:
  - src/lib/tools/runners/predict-runner.ts
autonomous: true
requirements: [PRED-01, PRED-03]
must_haves:
  truths:
    - "runPredict(input, deps?) orchestrates: resolve panel → steer (buildAudienceRepaint) → runPredictPanel → aggregatePredict → assemble a validated prediction-gauge block (PRED-01)"
    - "Every assembled block carries tier:'Directional', model:'sim1-flash', a non-empty caveat, assumptions, and successCriterion from the panel — never an oracle (PRED-03/D-04)"
    - "person/panel is read from the persisted __subject_kind marker (never persona count); a shared readSubjectKind helper is exported for the route's 400 guard (D-03)"
  artifacts:
    - path: "src/lib/tools/runners/predict-runner.ts"
      provides: "runPredict + exported readSubjectKind helper + PredictRunInput/PredictRunDeps"
      contains: "export async function runPredict"
---

<objective>
Build the verb orchestrator `predict-runner.ts` by cloning `simulate-runner.ts` exactly: the injectable `deps.flash` zero-network seam, the deterministic `__subject_kind` marker resolution, the `resolveTier` Directional guard, and the `.strict()` validate-on-assemble. It calls `runPredictPanel` (not the binary flash), collapses via `aggregatePredict`, and assembles a `prediction-gauge` block always carrying `tier:"Directional"`, `model:"sim1-flash"`, a non-empty caveat, the assumptions, and the panel's `successCriterion`.

Purpose: PRED-01 orchestration (panel + scenario → band/range/confidence/factors) + PRED-03 honesty (Directional + receipts + caveat always). Export a shared `readSubjectKind(audience)` helper so the route (06-06) can reject a person SIM at 400 BEFORE the runner runs (D-03/D-08).
Output: `runPredict` — turns the Wave-0 `predict-runner.test.ts` GREEN (zero-network via `deps.flash`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-predict-verb/06-RESEARCH.md
@.planning/phases/06-predict-verb/06-PATTERNS.md
@src/lib/engine/flash/predict-schema.ts
@src/lib/engine/flash/predict-aggregate.ts
@src/lib/engine/flash/run-predict-panel.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: predict-runner.ts — orchestration, shared readSubjectKind, tier guard, .strict() assemble</name>
  <files>src/lib/tools/runners/predict-runner.ts</files>
  <behavior>
    - runPredict({ audience, stimulus }, { flash }) with an injected flash returning a fixed PredictPanelResult makes ZERO network calls
    - the assembled block validates against PredictionGaugeBlockSchema and carries tier:"Directional", model:"sim1-flash", a non-empty caveat
    - band/range/confidence/factors come from aggregatePredict(result.analysts) (never re-rolled, never fabricated)
    - successCriterion = audience.success_criterion ?? null; assumptions derived from the scenario premises
    - readSubjectKind(audience) returns "person" ONLY on an explicit __subject_kind note==="person"; a marker-absent mode:"general" audience (template-analyst, custom_context:[]) is NOT "person" (Pitfall 3)
    - panel[] carries each analyst's archetype + qualitative lean + reasoning (the drill)
  </behavior>
  <read_first>
    - src/lib/tools/runners/simulate-runner.ts (THE analog — clone the IO contract + `deps.flash` seam at lines 51-66, the `resolveSubjectKind` marker resolution at 45-83, the body tier-guard/steer/run/aggregate/validate at 147-224, the private `validate()` safeParse-or-throw)
    - src/lib/engine/flash/predict-aggregate.ts (`aggregatePredict` — the collapse; field names already match the block)
    - src/lib/engine/flash/run-predict-panel.ts (`runPredictPanel` — the call; this is `deps.flash`'s default)
    - src/lib/tools/profile-blocks.ts (`PredictionGaugeBlockSchema` — the validate target; the always-on caveat copy lives in UI-SPEC Copywriting Contract)
    - src/lib/audience/resolve-tier.ts (`resolveTier` — Directional-by-rule; the defensive throw)
    - src/lib/engine/flash/build-reaction-panel.ts (`buildAudienceRepaint` — General/empty → no-op steer)
    - src/lib/audience/audience-types.ts (`Audience`, `CalibratedPersona`, the `__subject_kind` marker convention)
    - .planning/phases/06-predict-verb/06-PATTERNS.md § "predict-runner.ts" (Pitfall 3 landmine; the lift-to-shared-helper note)
  </read_first>
  <action>
    Create `src/lib/tools/runners/predict-runner.ts` cloning `simulate-runner.ts`. Export `PredictRunInput { audience: Audience; stimulus: Stimulus; subjectKind?: SubjectKind }`, `PredictRunDeps { flash?: typeof runPredictPanel }`, and a SHARED exported `readSubjectKind(audience): SubjectKind` (lift the marker resolution; the route imports this for its 400 guard). `readSubjectKind`: explicit override wins; else read `custom_context.find(c => c.persona_evidence_link === "__subject_kind")?.note`; return `"person"` ONLY on an explicit `"person"` note, `"panel"` on `"panel"`, and DEFAULT marker-absent `mode:"general"` audiences to `"panel"` (NOT "person" — Pitfall 3, so the default Analyst Panel is never rejected). `export async function runPredict(input, deps = {})`: `const flash = deps.flash ?? runPredictPanel;` `const tier = resolveTier(audience); if (tier !== "Directional") throw …` (KEEP as defense-in-depth — the user-facing reject is the route's 400, D-08); `const repaint = buildAudienceRepaint(audience);` call `flash(stimulus.content, audience.personas/panel, repaint)`; `const { band, range, confidence, factors } = aggregatePredict(result.analysts);` assemble a `prediction-gauge` block: `audienceName = audience.name`, `scenario = stimulus.content`, the aggregate fields, `panel = result.analysts.map(a => ({ archetype, lean, reasoning }))`, `assumptions` from scenario premises, `successCriterion = audience.success_criterion ?? null`, `caveat` = the UI-SPEC always-on Directional caveat string, `model:"sim1-flash"`, `tier:"Directional"`. Validate via a private `validate(block)` = `PredictionGaugeBlockSchema.safeParse` (throw on failure). Wave-0 test exercises it with injected `deps.flash` (zero-network).
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/tools/runners/__tests__/predict-runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `predict-runner.ts exports async function runPredict` and `export function readSubjectKind` (source assertion)
    - The runner test passes with an injected `deps.flash` and makes no network call (behavior — zero-network)
    - The assembled block has `tier === "Directional"`, `model === "sim1-flash"`, non-empty `caveat` (behavior — PRED-03)
    - `readSubjectKind({ mode:"general", custom_context:[] })` !== `"person"` (behavior — Pitfall 3; the default panel is not rejected)
    - `grep -c "FlashResultSchema\|runFlashTextMode\|aggregateFlash" src/lib/tools/runners/predict-runner.ts` returns `0` (uses the predict leaves, not the binary)
    - Command above exits 0 (predict-runner.test.ts GREEN)
  </acceptance_criteria>
  <done>runPredict orchestrates the predict leaves, assembles a validated Directional gauge block, exports the shared marker helper; GREEN zero-network.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audience (RLS-resolved) → runner | The audience is resolved under the session by the route; the runner trusts the resolved object but defends tier honesty |
| model result → block | The panel result is validated upstream; the assembled block is re-validated at `.strict()` before return |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering (honesty) | runPredict assemble | mitigate | block always carries `tier:"Directional"`, `model:"sim1-flash"`, non-empty caveat; validated via `PredictionGaugeBlockSchema.safeParse` (throws) — never an oracle (PRED-03) |
| T-06-12 | Spoofing / wrong-audience | resolveTier defensive throw | mitigate | runner keeps `resolveTier !== "Directional"` throw as defense-in-depth; the user-facing reject is the route 400 (D-08) |
| T-06-13 | Tampering (mis-classification) | readSubjectKind | mitigate | reject only on an EXPLICIT `note:"person"`; marker-absent general → "panel" (Pitfall 3) — never persona-count inference (D-03) |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/lib/tools/runners/__tests__/predict-runner.test.ts` exits 0.
- `grep -n "deps.flash ?? runPredictPanel" src/lib/tools/runners/predict-runner.ts` present (injectable seam).
</verification>

<success_criteria>
- The runner produces a validated Directional `prediction-gauge` block from panel + scenario (PRED-01), with receipts + assumptions + always-on caveat (PRED-03/D-04).
- The shared `readSubjectKind` helper enables the route's 400 person-reject without persona-count inference (D-03).
- Zero-network unit test green; binary flash schema/aggregate untouched.
</success_criteria>

## Artifacts this plan produces
- `src/lib/tools/runners/predict-runner.ts` — `runPredict`, exported `readSubjectKind`, `PredictRunInput`/`PredictRunDeps`.

<output>
Create `.planning/phases/06-predict-verb/06-05-SUMMARY.md` when done.
</output>
