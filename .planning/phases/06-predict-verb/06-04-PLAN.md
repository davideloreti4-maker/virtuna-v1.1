---
phase: 06-predict-verb
plan: 04
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - src/lib/tools/profile-blocks.ts
  - src/lib/tools/blocks.ts
  - src/lib/tools/block-registry.ts
  - src/components/thread/prediction-gauge-block.tsx
  - src/components/thread/message-blocks.tsx
autonomous: true
requirements: [PRED-02, PRED-03]
must_haves:
  truths:
    - "A prediction-gauge result card renders the band WORD + panel-derived ~min–max% range + confidence + factors (receipts) + panel drill + assumptions + an always-on Directional caveat (PRED-02)"
    - "The block schema is .strict() — it rejects a smuggled point-score / 0-100 / extra field; tier is literally Directional and model literally sim1-flash (PRED-03 / D-01)"
    - "The gauge reads WITHOUT color: band word + ~min–max% caption + confidence word carry 100% of meaning; the feathered span is decorative, no needle (F-01/F-02/F-03)"
  artifacts:
    - path: "src/lib/tools/profile-blocks.ts"
      provides: "PredictionGaugeBlockSchema (.strict()) + type"
      contains: "PredictionGaugeBlockSchema"
    - path: "src/components/thread/prediction-gauge-block.tsx"
      provides: "PredictionGaugeBlockRenderer — feathered span, no needle"
      contains: "PredictionGaugeBlockRenderer"
    - path: "src/lib/tools/block-registry.ts"
      provides: "prediction-gauge registered in BLOCK_REGISTRY"
      contains: "prediction-gauge"
---

<objective>
Add the ONE new block type `prediction-gauge`: its `.strict()` schema (PRED-03 honesty contract), its registration across the 3 wiring files, and its client renderer (PRED-02, the honest gauge from 06-UI-SPEC Surface 1). Schema + registration + renderer + message-blocks wiring land together so the wave-gate `next build`/tsc stays green (the `BLOCK_COMPONENTS` map is `Record<BlockType, …>` — adding a union member without its renderer breaks tsc).

Purpose: render probability + factors + confidence as a bounded, fuzzy, panel-derived likelihood — never an oracle dial (the load-bearing F-01/F-02 decision). Bundle-leak-safe (Pitfall 4): the renderer imports the block TYPE only.
Output: `PredictionGaugeBlockSchema` + `PredictionGaugeBlockRenderer`, registered and wired; the Wave-0 `prediction-gauge-block.test.tsx` GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-predict-verb/06-RESEARCH.md
@.planning/phases/06-predict-verb/06-PATTERNS.md
@.planning/phases/06-predict-verb/06-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PredictionGaugeBlockSchema (.strict()) + 3-file registration</name>
  <files>src/lib/tools/profile-blocks.ts, src/lib/tools/blocks.ts, src/lib/tools/block-registry.ts</files>
  <read_first>
    - src/lib/tools/profile-blocks.ts (THE analogs — `ReactionDistributionBlockSchema` at lines 80-114 and `ProfileReadBlockSchema` at 31-71: both `.strict()`, `caveat` always present, `model`/`tier` provenance literals; the physical home for the overflow since blocks.ts is at the 500-line limit)
    - src/lib/tools/blocks.ts (the P5 re-export at lines 17-26 + the `BlockUnionSchema` discriminated union at 464-478)
    - src/lib/tools/block-registry.ts (the `BLOCK_REGISTRY` entries at lines 29-50 + `validateBlock` at 58-75 — no edit to validateBlock)
    - .planning/phases/06-predict-verb/06-RESEARCH.md § "Recommended PredictionGaugeBlockSchema" (the exact field list, cross-checked vs UI-SPEC Surface 1)
    - .planning/phases/06-predict-verb/06-UI-SPEC.md § "Surface 1" (every rendered field the schema must satisfy)
  </read_first>
  <action>
    Add `PredictionGaugeBlockSchema` to `profile-blocks.ts` (NOT blocks.ts — 500-line limit; profile-blocks.ts exists for exactly this overflow). Mirror `ReactionDistributionBlockSchema`'s `.strict()` provenance pattern. Fields per RESEARCH/UI-SPEC: `type: z.literal("prediction-gauge")`; `props.strict()` with `audienceName` (min 1), `scenario` (min 1), `band: z.enum(["Likely","Lean yes","Lean no","Toss-up","Unlikely"])`, `range: z.object({ min: int 0-100, max: int 0-100 })` (the ONLY numeric), `confidence: z.enum(["High","Medium","Low"])`, `factors: z.array(z.object({ analystArchetype min1, driver min1, direction enum["for","against"] })).min(1)` (every factor names its analyst — D-04), `panel: z.array(z.object({ archetype min1, lean enum(LEANS), reasoning min1 })).min(1)` (the drill), `assumptions: z.array(z.string()).default([])`, `successCriterion: z.string().nullable()`, `caveat: z.string().min(1)` (always-on — D-04/F-04), `model: z.literal("sim1-flash")`, `tier: z.literal("Directional")`. `.strict()` on `props` rejects a smuggled `score`. Export `type PredictionGaugeBlock = z.infer<...>`. The schema MUST allow `min === max` (the renderer enforces the feather, F-01).
    Then register: (1) `blocks.ts` — re-export `PredictionGaugeBlockSchema` + `PredictionGaugeBlock` (mirror the P5 re-export) and add the schema to `BlockUnionSchema`. (2) `block-registry.ts` — import the schema, add `"prediction-gauge": { schema: PredictionGaugeBlockSchema as z.ZodType }` to `BLOCK_REGISTRY`.
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/thread/__tests__/prediction-gauge-block.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `profile-blocks.ts exports PredictionGaugeBlockSchema` and `blocks.ts` re-exports it (source assertion)
    - `block-registry.ts` contains `"prediction-gauge"` in `BLOCK_REGISTRY` (source assertion)
    - `PredictionGaugeBlockSchema.safeParse({ type:"prediction-gauge", props:{ ...valid, score:73 } }).success === false` (.strict — PRED-03/D-01)
    - `wc -l src/lib/tools/blocks.ts` stays ≤ 500 (the overflow lives in profile-blocks.ts)
    - The schema portion of prediction-gauge-block.test.tsx passes
  </acceptance_criteria>
  <done>Schema in profile-blocks.ts, registered in blocks.ts + block-registry.ts, .strict() rejects smuggled fields.</done>
</task>

<task type="auto">
  <name>Task 2: prediction-gauge-block.tsx renderer (feathered span, no needle) + message-blocks wiring</name>
  <files>src/components/thread/prediction-gauge-block.tsx, src/components/thread/message-blocks.tsx</files>
  <read_first>
    - src/components/thread/reaction-distribution-block.tsx (THE analog — the matte 12px shell + provenance header at lines 48-63, the `CONFIDENCE`-style data-color map at 26-30, the collapsible panel-drill toggle idiom at 106-148, the `SaveAffordance` footer at 153-156; TYPE-ONLY import discipline at 20-23 — Pitfall 4)
    - src/components/thread/profile-read-block.tsx (the caveat-always + multi-section layout pattern)
    - src/components/thread/message-blocks.tsx (the `BLOCK_COMPONENTS` map at lines 32-46 — add the entry; TS enforces completeness)
    - src/components/audience/trust-badge.tsx + src/components/thread/save-affordance.tsx (already client-safe; import directly)
    - .planning/phases/06-predict-verb/06-UI-SPEC.md § "Surface 1" + "Non-Blocking Flags F-01..F-05" (the exact section order, copy, and the feathered-span spec)
    - src/components/reading/__tests__/reskin-matte.test.ts (the matte/no-coral guard the card must keep green)
  </read_first>
  <action>
    Create `src/components/thread/prediction-gauge-block.tsx` (`'use client'`). Import ONLY: `useState` from react; `type { PredictionGaugeBlock }` from `@/lib/tools/blocks` (TYPE-ONLY — never the runner/route/engine, Pitfall 4); `TrustBadge`; `SaveAffordance`; phosphor `CaretDown`/`CaretUp`. Build `PredictionGaugeBlockRenderer({ block })` per UI-SPEC Surface 1 section order:
    1. provenance header (panel name left; `SIM-1 Flash` + `<TrustBadge tier="Directional" />` right) — mirror reaction-distribution-block 48-63 verbatim.
    2. scenario lead `On: {scenario}` (quiet, clamped ~2 lines).
    3. THE GAUGE: band WORD as Display (`text-2xl font-semibold leading-none text-foreground` — CREAM, never band-colored, F-03) + `~{min}–{max}%` caption (`text-xs text-foreground-muted`, tilde, en-dash, NO decimal, NO single value) + `Confidence: {word}` pill (colored by the confidence semantic token only). Below: an `h-2 rounded-full bg-white/[0.06]` rail with four zone labels; the FEATHERED SPAN child positioned `left:{min}% right:{100-max}%` filled with the confidence token via a horizontal gradient fading to transparent at BOTH ends (~0.28 opacity). NO needle/tick/pointer/dot. `min===max` → minimum-width feather centered on the value (F-01), still tilde + feather + word.
    4. factors — head `What's driving this`; each `factors[]` row = driver line (semibold lead) + `— {Archetype}` chip + `for`/`against` tag. EVERY factor names its analyst (D-04/F-05).
    5. panel drill — collapsible (`aria-expanded`, default collapsed) summary `The panel — {N} analysts` + `↓ Show`/`↑ Hide`; expanded rows = archetype + qualitative lean WORD (`leans yes`, never a number, F-01) + a `blockquote` reasoning quote. Reuse the reaction-distribution-block toggle idiom verbatim.
    6. assumptions — head `Assumptions`; the premises list + `Judged against: {successCriterion}`. NEUTRAL panel (6% hairline + `white/[0.04]`), never the legacy coral panel.
    7. the always-on Directional caveat (quiet 12px, never a tooltip, F-04).
    8. footer: `<SaveAffordance item_type="read" title={audienceName} snapshot={block.props} />` + optional `Run another scenario →` neutral cream text button.
    Add an `aria-label` summarizing the forecast (UI-SPEC Accessibility). Confidence color map: High→`var(--color-success)`, Medium→`var(--color-warning)`, Low→`var(--color-error)` (NOT accent). Then wire `message-blocks.tsx`: import `PredictionGaugeBlockRenderer` and add `"prediction-gauge": PredictionGaugeBlockRenderer` to `BLOCK_COMPONENTS`.
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/thread/__tests__/prediction-gauge-block.test.tsx src/components/reading/__tests__/reskin-matte.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `prediction-gauge-block.tsx exports PredictionGaugeBlockRenderer` and `message-blocks.tsx` maps `"prediction-gauge"` to it (source assertion)
    - The renderer imports the block as a TYPE only — `grep -c "from '@/lib/tools/runners\|from '@/lib/engine\|/api/tools/predict/route" src/components/thread/prediction-gauge-block.tsx` returns `0` (Pitfall 4 bundle-leak)
    - `grep -ci "needle\|<input type=\"range\"\|rotate(" src/components/thread/prediction-gauge-block.tsx` returns `0` (no dial/needle — F-02)
    - `reskin-matte.test.ts` stays green (no coral/glass — F-03)
    - The test asserts the band WORD + `~min–max%` caption render by text and `min===max` still renders a span (F-01)
  </acceptance_criteria>
  <done>The honest gauge renders (feathered span, no needle), readable without color, bundle-leak-safe, matte; GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted block JSON → renderer | The block is re-validated by `validateBlock` on rehydration (D-14); the renderer trusts only validated props |
| server schema → client bundle | A `'use client'` renderer must not pull a server-only chain into the browser bundle (Pitfall 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering (honesty) | PredictionGaugeBlockSchema | mitigate | `.strict()` rejects a smuggled `score`/point-estimate/0-100; `tier`/`model` are literals; range is the only numeric (D-01/PRED-03) |
| T-06-09 | Info Disclosure / build break | prediction-gauge-block.tsx | mitigate | TYPE-ONLY block import + client-safe TrustBadge/SaveAffordance; NEVER runner/route/engine; wave-gate `next build` (judge "compiles + no NEW errors on touched paths", ignore the pre-existing earnings-chart baseline — Pitfall 4/6) |
| T-06-10 | Tampering (false precision) | gauge render | mitigate | feathered span only, no needle/dial/tick/decimal/single-value; `min===max` → feather not tick (F-01/F-02) |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/components/thread/__tests__/prediction-gauge-block.test.tsx src/components/reading/__tests__/reskin-matte.test.ts` exits 0.
- Wave gate (run after Wave 1 merge): `node ./node_modules/vitest/vitest.mjs run` full suite + `next build` — judge "compiles + no NEW errors on touched paths" (ignore the pre-existing earnings-chart.tsx:98 recharts baseline).
- `grep -c "prediction-gauge" src/components/thread/message-blocks.tsx` ≥ 1.
</verification>

<success_criteria>
- The `prediction-gauge` card renders the full honest forecast (PRED-02), readable without color, no needle (F-01/F-02).
- The `.strict()` schema rejects smuggled scores; tier/model literals + always-on caveat (PRED-03).
- Bundle-leak-safe; matte-only; `next build` compiles with no new errors on touched paths.
</success_criteria>

## Artifacts this plan produces
- `PredictionGaugeBlockSchema` + `PredictionGaugeBlock` in `profile-blocks.ts`; re-export + union member in `blocks.ts`; `"prediction-gauge"` in `BLOCK_REGISTRY`; `PredictionGaugeBlockRenderer` in `prediction-gauge-block.tsx`; `BLOCK_COMPONENTS` entry in `message-blocks.tsx`.

<output>
Create `.planning/phases/06-predict-verb/06-04-SUMMARY.md` when done.
</output>
