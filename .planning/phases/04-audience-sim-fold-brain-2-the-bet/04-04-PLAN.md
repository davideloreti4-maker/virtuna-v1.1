---
phase: 04-audience-sim-fold-brain-2-the-bet
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - scripts/ab-fold-referee.ts
autonomous: true
requirements: [R7, R10]
nyquist_compliant: true

must_haves:
  truths:
    - "The referee runs BOTH paths (behavioralSource:\"personas\" 10-pass + \"fold\") on each video, 2 runs each (R8 noise band), and prints a per-video table + overall verdict"
    - "It computes the 3-metric composite (D-03): behavioral parity ±5, diversity >=0.8x, drop-point agreement +-1 for >=6/10 archetypes"
    - "It asserts the fold path issues exactly 1 audience-sim LLM call vs 20 for the 10-pass (R7) from the stage-event stream"
    - "It is the WRONG-harness-free new script (modeled on measure-pipeline.ts, NOT corpus/eval-harness.ts) and enforces a cost cap"
  artifacts:
    - path: "scripts/ab-fold-referee.ts"
      provides: "Complete 3-metric composite A/B referee (R10)"
      contains: "behavioral parity"
  key_links:
    - from: "scripts/ab-fold-referee.ts"
      to: "src/lib/engine/aggregator.ts"
      via: "aggregateScores(pipelineResult, onEvent, {behavioralSource}) for both paths"
      pattern: "behavioralSource"
---

<objective>
Complete `scripts/ab-fold-referee.ts` (skeleton from Plan 01): implement the D-03 3-metric composite over the 6 fixed videos, run both paths x2 runs each (R8 band), assert the R7 call-count win, and emit a per-video table + advisory overall verdict.

Purpose: R10 — the fold is proven, not assumed, before it can go live. This is the gate. RESEARCH Pitfall 2 — this is a NEW script on the measure-pipeline.ts scaffold; the corpus eval-harness (bucket-classifier macro-F1) is the WRONG harness and stays dormant. The deviation from CONTEXT D-04 ("revive eval-harness") is intentional and documented in the script header (added in Plan 01).
Output: A runnable referee that produces the composite report. Running it + the production-flip decision is Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-audience-sim-fold-brain-2-the-bet/04-RESEARCH.md
@.planning/phases/04-audience-sim-fold-brain-2-the-bet/04-VALIDATION.md

<interfaces>
<!-- The referee consumes these — all exist after Plan 03. -->

aggregateScores(pipelineResult, onStageEvent, { behavioralSource: "personas" | "fold" }) returns a result with:
  .behavioral_predictions.completion_pct etc (the behavioral_score driver)
  .heatmap.personas[].attentions[] (curves) + .heatmap.personas[].swipe_predicted_at (drop point)

runPredictionPipeline(input, { requestId, bypassCache:true, onStageEvent, useFold:true }) — Plan 03 added the fold flag

Stage events: the fold emits a single "wave_3_fold" stage; the 10-pass emits "wave_3" (x10 pass1) + "wave_3_pass2" (x10). Count from the onStageEvent stream.

The 3-metric composite (D-03 / VALIDATION):
  1. behavioral parity: |fold.behavioral_score - tenpass.behavioral_score| <= 5   (avg over the 2 runs)
  2. diversity: fold.avgCurveRange >= 0.8 * tenpass.avgCurveRange   (measure-pipeline.ts:146-160 formula)
  3. drop-point agreement: per archetype |fold.swipe_segment - tenpass.swipe_segment| <= 1; pass if >=6/10 agree
  "reproduce" = all 3 bands met; "beat" = parity + diversity >=1.0x + tighter/comparable drop points
  Advisory per D-05 — print numbers + verdict, do NOT hard-exit-1 on a single-video miss.

behavioral_score: derive the comparable scalar the same way measure-pipeline.ts prints it (the behavioral term of the blend); reuse that exact extraction.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the 3-metric composite + R7 call-count assertion</name>
  <files>scripts/ab-fold-referee.ts</files>
  <read_first>
    - scripts/ab-fold-referee.ts (the Plan 01 skeleton — VIDEO_SET, the both-paths x 2-runs loop, COST_CAP_CENTS, the eval-harness deviation note)
    - scripts/measure-pipeline.ts (FULL — the avg-curve-range metric 146-160, the behavioral_score print line, per-archetype curve printout, stage-event onEvent handler)
    - src/lib/engine/aggregator.ts (the "fold" branch from Plan 03 — what aggregateScores returns for each source)
    - .planning/phases/04-audience-sim-fold-brain-2-the-bet/04-RESEARCH.md the "The 3-metric composite" + "Validation Architecture" + Pitfall 2 (don't revive the corpus harness)
    - .planning/phases/04-audience-sim-fold-brain-2-the-bet/04-VALIDATION.md (the per-task verify map + the composite definition)
  </read_first>
  <action>
    Fill in the Plan-01 skeleton's "TODO Plan 04" block.
    Per video: run runPredictionPipeline(input, {bypassCache:true, useFold:true, onStageEvent}), then for each of 2 runs compute both aggregateScores(..., {behavioralSource:"personas"}) and aggregateScores(..., {behavioralSource:"fold"}). If the fold call must re-run per aggregate, structure so the single fold LLM call is shared within a video to keep the R7 count honest — document the chosen structure.
    Extract per path: behavioral_score (the scalar the blend uses — reuse measure-pipeline.ts's extraction), avgCurveRange (copy the measure-pipeline.ts:146-160 formula VERBATIM over heatmap.personas[].attentions), and per-archetype swipe_segment (the segment index of swipe_predicted_at, or the curve's top-dropoff index).
    Average each metric over the 2 runs (R8 band). Compute the 3 composite checks: (1) parity = Math.abs(foldBeh - tenBeh) <= 5; (2) diversity = foldAvgRange >= 0.8 * tenAvgRange (also compute the >=1.0x "beat" flag); (3) dropAgreement = count archetypes with |foldSeg - tenSeg| <= 1, pass if >=6.
    R7 assertion: from the captured stage events, assert the fold path emitted exactly 1 audience-sim stage ("wave_3_fold") while the 10-pass emitted 20 ("wave_3" pass1 x10 + "wave_3_pass2" x10). Print foldCalls=1 tenPassCalls=20 per video.
    Output a per-video table (video id | parity delta | diversity ratio | drop agreement n/10 | verdict reproduce/beat/miss) + an overall verdict line. Roll fold + 10-pass cost_cents into totalCostCents; warn (do not abort) on COST_CAP_CENTS exceed.
    ADVISORY (D-05): print the full numbers + a clear verdict; do NOT process.exit(1) on a single-video miss — exit 0 with the report so a human makes the call (Plan 05). Reserve exit 1 for infra failures (upload/API error), matching measure-pipeline.ts:175-178.
    Confirm the script does NOT import from src/lib/engine/corpus/ (Pitfall 2).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "ab-fold-referee" | head; echo "---metrics present---"; grep -E -c "behavioral parity|avgCurveRange|0\.8|wave_3_fold" scripts/ab-fold-referee.ts; echo "EXPECTED: no type errors + composite metrics present (the real-API run is Plan 05)"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/ab-fold-referee.ts compiles (npx tsc --noEmit reports no errors for it).
    - All three composite metrics are implemented: `grep -E -c "0\.8|behavioral parity|swipe" scripts/ab-fold-referee.ts` >= 1 each; the parity uses `<= 5`, diversity uses `0.8`, drop agreement uses `>= 6` (`grep -c ">= 6\|>=6\|>= 6" scripts/ab-fold-referee.ts` >= 1).
    - The avgCurveRange formula matches measure-pipeline.ts:146-160 (Math.max - Math.min per persona, mean): `grep -c "Math.max\|Math.min" scripts/ab-fold-referee.ts` >= 2.
    - R7 call-count assertion present: `grep -c "wave_3_fold" scripts/ab-fold-referee.ts` >= 1 and it compares fold=1 vs 10-pass=20.
    - 2-runs-per-video loop present (R8 band): the loop runs each path twice and averages.
    - No corpus harness import: `grep -c "corpus/eval" scripts/ab-fold-referee.ts` reflects only the deviation comment, never an actual import statement.
    - Advisory: no `process.exit(1)` on a metric miss — exit 1 only on infra error.
  </acceptance_criteria>
  <done>The referee computes the full 3-metric composite over the 6 videos x both paths x 2 runs, asserts the R7 1-vs-20 call count, prints a per-video table + advisory verdict, enforces the cost cap, and exits 0 with the report (no harness revival).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| referee script -> DashScope API | real-API A/B (6 x 2 paths x 2 runs ~= 48 runs) — cost runaway is the live concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-COST | DoS (self-inflicted) | ab-fold-referee.ts real-API loop | mitigate | COST_CAP_CENTS + running totalCostCents accumulator with console.warn on exceed (wired Plan 01, enforced here); cost rolled from fold + 10-pass cost_cents |
| T-04-07 | Repudiation | a brittle single-threshold gate blocking a qualitatively-good fold | accept | Composite is advisory (D-05) — the script prints numbers + verdict but never hard-blocks; the human (Plan 05) makes the final call |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH audit = N/A) |
</threat_model>

<verification>
- `npx tsc --noEmit` — no errors in ab-fold-referee.ts.
- Composite metrics present (parity <=5, diversity 0.8x, drop >=6/10) + the R7 1-vs-20 assertion.
- No corpus/eval-harness import (Pitfall 2 — the deviation is documented, not revived).
- Cost cap wired; advisory exit semantics (no exit-1 on metric miss).
</verification>

<success_criteria>
- The referee produces the D-03 3-metric composite report over 6 videos x both paths x 2 runs (R10).
- It proves the R7 call-count win (fold=1 vs 10-pass=20).
- It is a fresh measure-pipeline-based script; the corpus harness stays dormant (Pitfall 2 deviation documented).
</success_criteria>

<output>
Create `.planning/phases/04-audience-sim-fold-brain-2-the-bet/04-04-SUMMARY.md` when done.
</output>
