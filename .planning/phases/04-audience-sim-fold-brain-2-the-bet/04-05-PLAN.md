---
phase: 04-audience-sim-fold-brain-2-the-bet
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - src/lib/engine/pipeline.ts
  - src/lib/engine/aggregator.ts
autonomous: false
requirements: [R7, R10]
nyquist_compliant: true

must_haves:
  truths:
    - "The A/B referee has been run on the 6 fixed videos (2 runs each) and the D-03 composite report is captured"
    - "A human has reviewed the per-video table + verdict and signed off on the production flip (advisory gate, D-05) OR chosen the shadow/defer path (D-10)"
    - "If signed off: production audience-sim runs the fold (behavioralSource:\"fold\" + useFold default ON) and the R7 call-count win is banked; the 10-pass stays dormant-not-deleted (one-flag rollback, D-09)"
    - "The flip is a single, separately-revertable change (one flag) — reverting restores the 10-pass exactly"
  artifacts:
    - path: "src/lib/engine/pipeline.ts"
      provides: "fold default-ON flip (or shadow per D-10) — separately revertable"
      contains: "useFold"
    - path: "src/lib/engine/aggregator.ts"
      provides: "production behavioralSource default flip to fold (if signed off)"
      contains: "fold"
  key_links:
    - from: "src/lib/engine/aggregator.ts"
      to: "src/lib/engine/pipeline.ts"
      via: "the flip changes BOTH the behavioralSource default AND the useFold gate in one revertable change"
      pattern: "fold"
---

<objective>
Run the A/B referee (R10), present the D-03 composite to the user for the advisory sign-off (D-05), and — only if signed off — execute the single, separately-revertable production flip (fold ON by default). If borderline, take the D-10 shadow path and defer the flip to P5.

Purpose: This is the bet's gate (R10) and the call-count payoff (R7). D-09 — flip in P4, gated on the A/B pass, keeping the 10-pass dormant-not-deleted for one-flag rollback. The flip is the ONLY task in this phase that changes production behavior, and it is gated behind both an automated composite and a human checkpoint (D-05 advisory).
Output: A captured referee report; a recorded sign-off decision; and either the flip (fold default ON) or the shadow/defer record (D-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-audience-sim-fold-brain-2-the-bet/04-RESEARCH.md
@.planning/phases/04-audience-sim-fold-brain-2-the-bet/04-VALIDATION.md

<interfaces>
<!-- All exist after Plan 03 + Plan 04. -->

scripts/ab-fold-referee.ts — runs 6 videos x both paths x 2 runs, prints the D-03 composite + verdict (Plan 04)

The flip surface (both default to OFF/10-pass after Plan 03):
  pipeline.ts: useFold gate (ENGINE_USE_FOLD / opts.useFold) — currently default OFF
  aggregator.ts: behavioralSource ?? "deepseek" — production default

The flip = make the fold the production default in ONE revertable change. The 10-pass call site (runWave3Pass2) stays present (D-09 dormant-not-deleted).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the A/B referee + present the D-03 composite for sign-off</name>
  <what-built>
    The complete referee (Plan 04) runs the fold vs the 10-pass on the 6 fixed videos (2 runs each, R8 band) and emits the 3-metric composite: behavioral parity (+-5), diversity (>=0.8x), drop-point agreement (+-1 for >=6/10), plus the R7 call-count proof (fold=1 vs 10-pass=20).
  </what-built>
  <how-to-verify>
    1. Run: `npx tsx scripts/ab-fold-referee.ts` (real API — ~48 runs, several minutes; watch the cost-cap warnings).
    2. Review the per-video table + overall verdict:
       - Behavioral parity within +-5 on each video?
       - Diversity ratio >=0.8x (>=1.0x = "beat")?
       - Drop-point agreement >=6/10 archetypes per video?
       - Call count: fold=1 vs 10-pass=20 (R7) confirmed?
    3. Apply the advisory judgment (D-05): if the fold is inside all bands on most videos and the curves look qualitatively good, sign off on the flip. If it misses on 1 video but curves are clearly good, your call. If diversity collapses (homogenization, the medium-high risk), consider the D-02 ~5-core fallback OR the D-10 shadow path.
    4. Reply with the decision:
       - "FLIP" — make the fold production default (Task 2 runs).
       - "SHADOW" — keep 10-pass production, fold logged/compared only, defer the flip to P5 (D-10). (Task 2 records this instead of flipping.)
       - "FALLBACK-5" — diversity failed; collapse to the 5 cores (D-02) — this re-opens Plan 02's archetype list and is a replan trigger, not a flip.
  </how-to-verify>
  <resume-signal>Reply FLIP, SHADOW, or FALLBACK-5 (with the referee verdict pasted or summarized).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute the production flip (or record the shadow/defer decision)</name>
  <files>src/lib/engine/pipeline.ts, src/lib/engine/aggregator.ts</files>
  <read_first>
    - src/lib/engine/pipeline.ts (the useFold gate from Plan 03 + the runWave3Pass2 dormant call site)
    - src/lib/engine/aggregator.ts (the behavioralSource default + the "fold" branch from Plan 03)
    - .planning/phases/04-audience-sim-fold-brain-2-the-bet/04-RESEARCH.md Open Question 2 (default state at P4 merge) + D-09 / D-10
  </read_first>
  <action>
    Branch on the Task 1 decision:
    IF "FLIP": Make the fold the production default in ONE cohesive, separately-revertable change. Flip the useFold gate default to ON in pipeline.ts AND set the production behavioralSource to "fold" (either change the aggregator default, or — cleaner — have the analyze route / pipeline pass behavioralSource:"fold" explicitly). Keep the change a single logical unit so `git revert <flip-commit>` restores the 10-pass exactly. Do NOT delete runWave3Pass2 (D-09 dormant-not-deleted — one-flag rollback). Add a comment at the flip site: "P4 D-09 flip — A/B signed off [date]; revert THIS commit to roll back to the 10-pass (still present)." Bump ENGINE_VERSION (e.g. 3.4.0) to invalidate the engine cache key. Commit the flip as its own commit, message `feat(04): flip audience-sim production to fold (R7/R10, D-09)`.
    IF "SHADOW" (D-10): Leave production on the 10-pass (defaults unchanged). Keep useFold available behind the flag so the fold can be logged/compared in shadow. Record in STATE.md + the SUMMARY that the flip is deferred to P5 with the referee verdict. No ENGINE_VERSION bump. Do not modify the production default.
    IF "FALLBACK-5": Do NOT flip. Record the diversity failure + the D-02 decision; this triggers a replan of Plan 02 (collapse STABLE_FOLD_SYSTEM_PROMPT + FoldResponseSchema to the 5 cores: tough_crowd, lurker, high_engager, sharer, loyalist). Note it in the SUMMARY as a phase outcome requiring `/gsd-plan-phase 4 --gaps` or a Plan 02 revision.
    In all branches: verify the engine still builds + the existing engine tests stay green (no 10-pass test regressions — it is dormant-not-deleted, not removed).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "---10-pass preserved---"; grep -c "runWave3Pass2" src/lib/engine/pipeline.ts; echo "EXPECTED: build succeeds + runWave3Pass2 call site still present (D-09 rollback)"</automated>
  </verify>
  <acceptance_criteria>
    - Build succeeds (`npm run build` exits 0).
    - The 10-pass is preserved (`grep -c "runWave3Pass2" src/lib/engine/pipeline.ts` >= 1) — dormant-not-deleted (D-09), so the flip is one-flag-revertable.
    - IF FLIP: the production default routes the fold (`grep -c "fold" src/lib/engine/aggregator.ts` reflects the production path; useFold default flipped ON in pipeline.ts); ENGINE_VERSION bumped; the flip is a single commit a `git revert` would cleanly undo.
    - IF SHADOW: production default unchanged (`behavioralSource ?? "deepseek"` and useFold default OFF preserved); the deferral + referee verdict recorded in the SUMMARY + STATE.md.
    - IF FALLBACK-5: no production change; the D-02 replan trigger recorded; Plan 02 revision noted.
    - Existing engine test suite stays green (no 10-pass regressions): `npx vitest run src/lib/engine/wave3/__tests__/ 2>&1 | tail -3`.
  </acceptance_criteria>
  <done>The decision from Task 1 is executed: either the fold is the separately-revertable production default (FLIP, 10-pass dormant), or the shadow/defer (SHADOW) / fallback (FALLBACK-5) decision is recorded with the referee verdict; the build is green and the 10-pass rollback path is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| referee verdict -> production flip | a flip on insufficient evidence ships a homogenized audience-sim to users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | flipping production on a brittle/insufficient A/B result | mitigate | Blocking human checkpoint (Task 1) gates the flip on the D-03 composite + qualitative review (D-05 advisory); D-10 shadow path for borderline |
| T-04-09 | DoS | no rollback after the flip regresses production | mitigate | 10-pass dormant-not-deleted (runWave3Pass2 preserved); the flip is a single revertable commit (D-09 one-flag rollback) |
| T-04-COST | DoS (self-inflicted) | the real-API referee run | mitigate | COST_CAP_CENTS enforced by the referee (Plan 04); the run is a one-time gated checkpoint, not a hot path |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH audit = N/A) |
</threat_model>

<verification>
- `npx tsx scripts/ab-fold-referee.ts` run + the D-03 composite captured (Task 1 checkpoint).
- Human sign-off recorded (FLIP / SHADOW / FALLBACK-5).
- `npm run build` green; `runWave3Pass2` preserved (D-09 rollback).
- Existing engine tests green (no 10-pass regression).
</verification>

<success_criteria>
- R10: the fold is proven against the 10-pass via the 3-metric composite + human sign-off (advisory, D-05).
- R7: if flipped, the 1-vs-20 call-count win is banked in production; the 10-pass stays dormant-not-deleted (D-09 one-flag rollback).
- The flip (if taken) is a single, separately-revertable change.
</success_criteria>

<output>
Create `.planning/phases/04-audience-sim-fold-brain-2-the-bet/04-05-SUMMARY.md` when done. Record the referee verdict + the sign-off decision (FLIP/SHADOW/FALLBACK-5) + whether production was flipped.
</output>
