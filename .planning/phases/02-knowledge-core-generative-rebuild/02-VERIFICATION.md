---
phase: 02-knowledge-core-generative-rebuild
verified: 2026-06-17T19:36:00Z
status: passed
score: 3/3 must-haves verified
overrides_applied: 0
---

# Phase 2: Knowledge-Core Generative Rebuild Verification Report

**Phase Goal:** Rebuild the Knowledge-Core from scratch for *generation* (not scoring) — a shared generative base plus per-mode slices (Ideas, Hooks, chat) and a tight per-request grounding-assembly mechanism — as a content/curation workstream first, code second. THE value, the long pole.
**Verified:** 2026-06-17T19:36:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | A generation-grade KC exists — authored fresh as shared base + per-mode slices (Ideas, Hooks, chat), NOT retrofitted from scoring core or stale `training-data.json` | ✓ VERIFIED | All 4 corpus files are real authored craft prose (base 303L, ideas 263L, hooks 357L, chat 108L). base.md carries Voice & Stance, 6 Universal Craft Principles (each with named mechanism), 6 Anti-Generic Guardrails, Value Bar. hooks.md has 6 mechanism-distinct archetypes with template/performance/failure each. No apollo-core import anywhere in `src/lib/kc/`. KC_GEN_VERSION `gen.1.0.0` decoupled from ENGINE_VERSION. Content is greenfield generative-craft, not scoring rubric. |
| 2 | Each tool assembles a tight curated context slice (niche + relevant craft frame + specific input) rather than the whole profile/KC; per-mode grounding-assembly mechanism in place to avoid signal dilution | ✓ VERIFIED | `assembler.ts` (GROUND-02): MODE_ROLES filters per mode (idea=6 roles, hooks=5 no-goals, chat=3); BUNDLE_CHAR_CAP=4000 hard cap with lowest-priority-role drop; cold-start honest baseline flag (never fabricated); `<<<USER_CONTENT>>>` injection fence with sentinel-strip; by-role lookup via PROFILE_ROLE_MAP (v6.1 redesign isolated to one table). 28/28 assembler tests green incl. role-leak guard, cap, cold-start, fence. |
| 3 | Rebuilt KC measurably outperforms scoring-core baseline on generative eval *(D-12 relaxes to owner-blind rank vs raw-LLM + current-KC; no eval artifact)* | ✓ VERIFIED | `scripts/kc-gate.ts` produces 3×N blind shuffled outputs (A=new-KC via KC_IDEAS_SYSTEM_PROMPT+assembleBundle, B=current-KC via APOLLO_SYSTEM_PROMPT, C=raw-LLM). Fisher-Yates `shuffleArms` (Goodhart guard), decode key recorded separately. It is a throwaway script + owner judgment, NOT a persisted eval harness — exactly as D-12 specifies. Pass condition is owner blind-rank (per SUMMARY 02-04, owner judged new-KC clearly better). |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `.planning/corpus/base.md` | Full-depth domain-general BASE | ✓ VERIFIED | 303 lines. Voice & Stance, Universal Craft Principles (6, mechanism-named), Anti-Generic Guardrails (6 prohibitions), Value Bar, Output Discipline (scaffolding-private). Real authored prose. |
| `.planning/corpus/ideas.md` | Full-depth Ideas slice | ✓ VERIFIED | 263 lines. Mode Job, Topic×Take×Format anatomy, 4 Craft Archetypes, Failure Modes, Reasoning Scaffold, Actionability Contract. |
| `.planning/corpus/hooks.md` | Full-depth Hooks slice (proven shape) | ✓ VERIFIED | 357 lines. 6 archetypes (BOLD/GAP/CONTRARIAN/RESEARCH/NARRATIVE/QUESTION). Multi-modal correction present (channel-agnostic: spoken/visual/audio/caption/edit, lines 58-64). |
| `.planning/corpus/chat.md` | Thin chat stance-slice riding BASE (D-14) | ✓ VERIFIED | 108 lines — thin relative to hooks (357), rides the BASE. Mode Job, Craft Patterns, Failure Modes, Exemplars, Actionability. |
| `scripts/regen-kc.ts` | Byte-stable .md→TS compiler (D-01) | ✓ VERIFIED | escapeForTemplate + readFileSync(4 corpus files) + writeFileSync(compiled.ts). Re-run with unchanged input = byte-identical output (verified by diff). |
| `src/lib/kc/kc-version.ts` | KC_GEN_VERSION decoupled (D-06) | ✓ VERIFIED | Exports KC_GEN_VERSION="gen.1.0.0", no ENGINE_VERSION coupling. |
| `src/lib/kc/compiled.ts` | Generated byte-stable per-mode constants + system prompts | ✓ VERIFIED | 1069 lines. KC_BASE (18.7K chars), KC_IDEAS/HOOKS/CHAT_SYSTEM_PROMPT (35.7K/42.3K/24.6K chars) = BASE+slice composed. Reflects corpus (channel-agnostic + scaffolding-private strings present). |
| `src/lib/kc/profile-role-map.ts` | Role→column lookup (D-05) | ✓ VERIFIED | PROFILE_ROLE_MAP, 6 roles, real DB columns (niche_primary, target_audience, primary_goal, etc.). |
| `src/lib/kc/assembler.ts` | Per-request live-tier grounding assembler (GROUND-02) | ✓ VERIFIED | Exports AssemblerInput, MODE_ROLES, assembleBundle. Per-mode filter + cap + cold-start + fence. CR-01/CR-02 overflow fix present (fenceSectionsWithinBudget keeps sentinels intact). Input validated via zod at boundary. |
| `src/lib/kc/assembler.test.ts` | Behavior tests | ✓ VERIFIED | 28 tests, all green. Covers role filtering, role-leak guard, length cap, cold-start, injection fence, CR-01/CR-02 overflow-integrity regression (3 tests), wins/flops honesty. |
| `scripts/kc-gate.ts` | Throwaway blind 3×N gate (D-12) | ✓ VERIFIED | 3-arm blind shuffle wired to KC_IDEAS_SYSTEM_PROMPT + assembleBundle. Throwaway script, not eval harness. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| regen-kc.ts | corpus/*.md | readFileSync | ✓ WIRED | Reads all 4 corpus files at regen time. |
| regen-kc.ts | compiled.ts | writeFileSync escaped template | ✓ WIRED | Writes byte-stable escaped constants; confirmed byte-identical on re-run. |
| corpus base+ideas | compiled.ts | regen recompile → KC_IDEAS_SYSTEM_PROMPT | ✓ WIRED | Authored prose present in compiled constant. |
| corpus hooks+chat | compiled.ts | regen recompile → KC_HOOKS/CHAT_SYSTEM_PROMPT | ✓ WIRED | Both prompts compiled byte-stable, carry authored prose. |
| assembler.ts | creator_profiles (by role) | PROFILE_ROLE_MAP role→column | ✓ WIRED | Lookup table used; v6.1 redesign isolated. |
| assembler.ts | user text (ask/overrides/anchor) | `<<<USER_CONTENT>>>` fence | ✓ WIRED | Fence + sentinel-strip; overflow-integrity guaranteed. |
| kc-gate.ts | compiled.ts + assembler.ts | new-KC = KC_IDEAS_SYSTEM_PROMPT + assembled bundle | ✓ WIRED | require()s both; arm A built from them. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Regen byte-stable | `npx tsx scripts/regen-kc.ts` + diff | BYTE-IDENTICAL, no drift | ✓ PASS |
| KC tests pass | `npx vitest run src/lib/kc` | 28/28 passed | ✓ PASS |
| compiled exports non-trivial prompts | tsx require compiled | 4 system prompts, 18.7K–42.3K chars each | ✓ PASS |
| KC_GEN_VERSION present | tsx require kc-version | `gen.1.0.0` | ✓ PASS |
| No apollo-core import | grep src/lib/kc | 0 matches | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| GROUND-01 | 02-01,03,04,05 | Generation-grade KC authored fresh (base + slices), not retrofitted | ✓ SATISFIED | 4 corpus files (1031 lines authored craft) → compiled.ts byte-stable; greenfield; no apollo-core. |
| GROUND-02 | 02-02 | Per-mode grounding assembly (tight slice, no whole-profile dilution) | ✓ SATISFIED | assembler.ts MODE_ROLES + cap + cold-start + fence; 28 tests green. |

No orphaned requirements — both Phase-2 IDs declared in plans and mapped in REQUIREMENTS.md. (Note: REQUIREMENTS.md status table lines 112-113 still read "Pending" — stale tracking entry, not a goal gap; implementation verified present. Recommend flipping to Done.)

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| base.md / compiled.ts | 203/217 | "skeleton" / "[MECHANISM:]" | ℹ️ Info | NOT a debt marker — prose teaching the model NOT to leak scaffolding tags. Intended content. |
| assembler.ts | 44 | "placeholder sized conservatively" | ℹ️ Info | Refers to a tuned cap constant value, not unimplemented code. BUNDLE_CHAR_CAP=4000 is functional. |
| scripts/kc-gate.ts | 138-157, 170 | `as never` cast, empty-response substitution (WR-01/WR-02) | ⚠️ Warning | Confined to throwaway dev gate script (not product code). Carried as Phase-3 note per phase context. Not a phase-goal blocker. |

No 🛑 blockers. No unreferenced TBD/FIXME/XXX in deliverables. The 2 prior Criticals (CR-01/CR-02 injection-fence overflow) are confirmed FIXED in commit `1b35b361` — verified by reading `fenceSectionsWithinBudget` (sentinels always emitted intact) + 3 passing regression tests.

### Human Verification Required

None. The one inherently-human gate (D-12 owner blind-rank of new-KC vs current-KC vs raw-LLM) was already performed by the owner this session (per 02-04-SUMMARY and 02-05 replication proceeding from the gate pass). The phase goal — KC exists as a generation core with all four modes authored + a working grounding seam — is fully verifiable in the codebase and is achieved.

### Gaps Summary

No gaps. All 3 ROADMAP success criteria are observably true in the codebase. The generative Knowledge-Core genuinely exists: 1031 lines of authored craft prose across BASE + 3 slices, compiled byte-stable into 4 non-trivial system prompts, with a working per-request grounding assembler (GROUND-02) that filters per mode, caps length, degrades honestly on cold-start, and injection-fences user text with an overflow-proof guarantee. Both Criticals from code review are fixed and regression-tested. The two warnings are isolated to the throwaway gate script and are correctly carried as Phase-3 notes. Phase goal achieved.

---

_Verified: 2026-06-17T19:36:00Z_
_Verifier: Claude (gsd-verifier)_
