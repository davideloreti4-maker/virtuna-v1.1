---
phase: 02-knowledge-core-generative-rebuild
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .planning/corpus/base.md
  - .planning/corpus/ideas.md
  - src/lib/kc/compiled.ts
autonomous: false
requirements: [GROUND-01]

must_haves:
  truths:
    - "The BASE file is authored to full depth and is genuinely domain-general (passes the D-00a reviewer test)"
    - "The Ideas slice is authored to full depth with all five D-04 SLICE sections plus the value-forcing add-ons"
    - "The owner has red-lined the drafted craft to the taste bar (owner is the arbiter, not the typist)"
    - "Authored content is greenfield — never lifted from the scoring KC"
    - "The Ideas system prompt compiles byte-stable from BASE + Ideas slice"
  artifacts:
    - path: ".planning/corpus/base.md"
      provides: "Full-depth domain-general BASE (D-00/D-00a) — Voice & Stance, Universal Craft Principles, Anti-Generic Guardrails, Value Bar"
      min_lines: 40
    - path: ".planning/corpus/ideas.md"
      provides: "Full-depth Ideas slice (D-15 pilot) — Mode Job, Craft Patterns, Failure Modes, Exemplar Patterns, Actionability Contract + diversity + flops-as-negative"
      min_lines: 40
    - path: "src/lib/kc/compiled.ts"
      provides: "Recompiled byte-stable KC_BASE + KC_IDEAS_SLICE + KC_IDEAS_SYSTEM_PROMPT carrying the authored prose"
      contains: "KC_IDEAS_SYSTEM_PROMPT"
  key_links:
    - from: ".planning/corpus/base.md + ideas.md"
      to: "src/lib/kc/compiled.ts"
      via: "scripts/regen-kc.ts recompile"
      pattern: "KC_IDEAS_SYSTEM_PROMPT"
---

<objective>
Author the BASE + the Ideas pilot slice (D-15) to FULL DEPTH — the owner-curated
content workstream that IS the milestone's compounding value. This is the
"narrow & deep first" half of D-11: prove the full shape (base + slice + value-forcing)
end-to-end on the immediate next consumer (Ideas, Phase 3) before replicating to other
modes. Per D-09/D-10 the owner is the PRIMARY spine and taste arbiter; the research
agent + Claude DRAFT craft into the fixed D-04 template from owner steer + the RESEARCH
external-craft notes, and the OWNER RED-LINES to the taste bar. Claude NEVER originates
craft (first-principles = connective tissue only). After curation, recompile.

Purpose: GROUND-01 — a generation-grade KC authored fresh (D-08 greenfield), the
domain-general base + the Ideas slice, structurally value-forcing (D-04) so it refuses
to emit generic output.
Output: full-depth .planning/corpus/base.md + ideas.md + recompiled compiled.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-knowledge-core-generative-rebuild/02-CONTEXT.md
@.planning/phases/02-knowledge-core-generative-rebuild/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DRAFT the BASE + Ideas slice craft into the template (draft only — owner curates next)</name>
  <files>.planning/corpus/base.md, .planning/corpus/ideas.md</files>
  <read_first>
    - .planning/corpus/base.md, .planning/corpus/ideas.md (the Plan-01 skeletons being filled)
    - .planning/phases/02-knowledge-core-generative-rebuild/02-RESEARCH.md (Priority 1A universal mechanisms -> BASE; 1D anti-slop -> BASE Anti-Generic Guardrails + Value Bar; 1B idea-resonance + depth-match failure mode -> Ideas slice; Priority 4A intra-batch diversity; 4B flops-as-negative-grounding; the tier-sort summary table)
    - .planning/phases/02-knowledge-core-generative-rebuild/02-CONTEXT.md (D-00/D-00a north star + reviewer test; D-04 fixed schema; D-08 greenfield; D-09 sources hierarchy)
  </read_first>
  <action>
    DRAFT craft prose into base.md and ideas.md from the RESEARCH Priority-1 notes treated as
    raw material / steer (NOT copy-pasted — D-09), tier-sorted per the RESEARCH tier-sort table:
    BASE gets 1A universal "why viewers keep watching" mechanisms (curiosity gap/open loop,
    curiosity stacking, pattern interrupt, prediction/anticipation, looping, transformation
    promise) under Universal Craft Principles, and 1D anti-slop signals under Anti-Generic
    Guardrails + the Value Bar / Self-Rejection Standard (named mechanism · concrete instantiation ·
    fit-to-creator, self-reject if generic/mechanism-less/niche-agnostic). Ideas slice gets 1B
    idea-resonance (topic specificity > category, the take not just the topic, audience-desire
    targeting), the depth-match note under Failure Modes, the Gold-Standard Exemplar Patterns as
    AUTHORED-FRESH ABSTRACTED patterns (NOT scraped clips — D-04 / training-data.json ban), and an
    Actionability Contract requiring per item: named mechanism · why-it-fits-this-creator ·
    ready-to-use form. Bake the two value-forcing add-ons into the Ideas Actionability Contract:
    intra-batch diversity (N outputs must fire DIFFERENT named mechanisms, never N variants of one)
    and flops-as-negative-grounding (steer toward wins AND away from flops). Apply the D-00a
    reviewer test to EVERY base line — if a statement only makes sense for Ideas, move it to the
    slice. Do NOT open KNOWLEDGE_CORE / apollo-core (D-08). This is a DRAFT for owner red-lining;
    mark it as draft pending curation at the top of each file.
  </action>
  <verify>
    <automated>grep -q "Universal Craft Principles" .planning/corpus/base.md && grep -q "Value Bar" .planning/corpus/base.md && grep -q "Actionability Contract" .planning/corpus/ideas.md && grep -Eqi "diversity|distinct mechanism" .planning/corpus/ideas.md && grep -Eqi "flop" .planning/corpus/ideas.md && [ "$(grep -vc '^[[:space:]]*$' .planning/corpus/base.md)" -ge 40 ] && [ "$(grep -vc '^[[:space:]]*$' .planning/corpus/ideas.md)" -ge 40 ]</automated>
  </verify>
  <acceptance_criteria>
    - base.md has authored prose under all four BASE sections, >= 40 non-blank lines
    - base.md Universal Craft Principles names at least the curiosity-gap/open-loop and pattern-interrupt mechanisms
    - base.md contains a Value Bar / Self-Rejection Standard requiring named mechanism + concrete instantiation + fit-to-creator
    - ideas.md has authored prose under all five SLICE sections, >= 40 non-blank lines
    - ideas.md Actionability Contract requires per item: named mechanism, why-it-fits-this-creator, ready-to-use form
    - ideas.md encodes intra-batch diversity (distinct mechanisms) AND flops-as-negative-grounding
    - No scoring-shaped language leaked: `grep -ci "Strong/Mid/Weak\|dock for\|rubric" .planning/corpus/ideas.md` returns 0 (D-08)
    - Files marked as draft-pending-curation at the top
  </acceptance_criteria>
  <done>BASE + Ideas slice drafted to full depth in the fixed D-04 shape, domain-general base, value-forcing Ideas contract, greenfield — ready for owner red-line.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner red-lines the drafted BASE + Ideas slice to the taste bar (D-10)</name>
  <what-built>
    A full-depth DRAFT of .planning/corpus/base.md (domain-general BASE) and .planning/corpus/ideas.md
    (the Ideas pilot slice) in the locked D-04 template, drafted from owner steer + the RESEARCH
    external-craft notes (never copy-pasted), structurally value-forcing (intra-batch diversity +
    flops-as-negative-grounding + the named-mechanism Actionability Contract).
  </what-built>
  <how-to-verify>
    1. Open .planning/corpus/base.md — confirm every line is genuinely domain-general (apply the
       D-00a reviewer test: "does this make sense for scoring/decode/script too?"). Move any
       mode-specific statement into a slice. Red-line the craft to your taste bar — depth, accuracy,
       voice. You are the arbiter; the draft is connective tissue under your expertise (D-09).
    2. Open .planning/corpus/ideas.md — confirm the craft patterns, failure modes, and exemplar
       patterns match how you actually evaluate an idea; confirm the Actionability Contract forces
       specific + mechanism-backed + shoot-ready + grounded output. Red-line freely.
    3. Edit the .md files directly (they are the prose SSOT). Remove the draft-pending markers when satisfied.
  </how-to-verify>
  <resume-signal>Type "curated" when the BASE + Ideas slice meet your taste bar, or describe the red-lines to apply.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Recompile the curated BASE + Ideas slice to byte-stable constants</name>
  <files>src/lib/kc/compiled.ts</files>
  <read_first>
    - scripts/regen-kc.ts (the compiler from Plan 01 — run it; do not modify)
    - .planning/corpus/base.md, .planning/corpus/ideas.md (the curated prose now being compiled)
  </read_first>
  <action>
    Run scripts/regen-kc.ts to recompile the curated base.md + ideas.md into src/lib/kc/compiled.ts.
    Verify the regenerated KC_IDEAS_SYSTEM_PROMPT now carries the authored prose (not the skeleton)
    and remains byte-stable on a second run. Do not hand-edit compiled.ts. Confirm no template
    holes / no per-request data leaked from the prose into the constant.
  </action>
  <verify>
    <automated>npx tsx scripts/regen-kc.ts && cp src/lib/kc/compiled.ts /tmp/kc-p3.ts && npx tsx scripts/regen-kc.ts && diff /tmp/kc-p3.ts src/lib/kc/compiled.ts && grep -q "Actionability Contract" src/lib/kc/compiled.ts && grep -c "Date.now\|Math.random" src/lib/kc/compiled.ts | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/regen-kc.ts` runs clean and compiled.ts carries the authored Ideas prose (contains "Actionability Contract")
    - Two consecutive runs produce byte-identical output (diff empty)
    - compiled.ts contains no Date.now / Math.random: count is 0
    - `npx tsc --noEmit` passes for compiled.ts
  </acceptance_criteria>
  <done>The curated BASE + Ideas slice are compiled into byte-stable constants; KC_IDEAS_SYSTEM_PROMPT carries the authored craft.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external craft research → authored .md | Public-blog material curated by the owner; risk of copy-paste / unvetted craft entering the SSOT |
| scoring KC → authored .md | Risk of re-importing scoring-biased craft (D-08 breach) |
| curated .md → compiled.ts | Author prose embedded into the byte-stable constant (escape handled by the Plan-01 compiler) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | external craft → BASE/slice | mitigate | Owner curation gate (Task 2 blocking checkpoint) — research is steer, never authoritative copy-paste (D-09) |
| T-02-08 | Elevation | scoring-craft re-import (D-08) | mitigate | Author from owner steer + Priority-1 notes only; never open KNOWLEDGE_CORE; grep for scoring-shaped language in acceptance |
| T-02-09 | Tampering | BASE leaking task-specificity (D-00a) | mitigate | Owner applies the D-00a reviewer test in Task 2; mode-named statements move to a slice |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- base.md + ideas.md authored to full depth, owner-curated, greenfield, value-forcing
- compiled.ts recompiles byte-stable carrying the authored Ideas prose
- `npm test` engine suite stays green (this plan touches no engine code)
- Verifier note: GROUND-01 success-criterion 3 is verified by the D-12 owner-blind gate (Plan 04), NOT a statistical eval artifact (D-12 intentional relaxation)
</verification>

<success_criteria>
- The BASE + Ideas pilot slice exist at full depth, owner-approved, in the fixed D-04 shape (GROUND-01 narrow-and-deep half of D-11)
- The Ideas system prompt compiles byte-stable
- The proven shape is ready for the D-12 gate (Plan 04) before replication (Plan 05)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

New/filled symbols and content created here (exclude from drift verification — they are new):
- Authored content: full-depth `.planning/corpus/base.md` (domain-general BASE) + `.planning/corpus/ideas.md` (Ideas pilot slice)
- Recompiled constants in `src/lib/kc/compiled.ts`: `KC_BASE`, `KC_IDEAS_SLICE`, `KC_IDEAS_SYSTEM_PROMPT` now carry authored prose
- The value-forcing contract content: intra-batch-diversity rule + flops-as-negative-grounding + the named-mechanism Actionability Contract (Ideas)
</artifacts_produced>

<output>
Create `.planning/phases/02-knowledge-core-generative-rebuild/02-03-SUMMARY.md` when done
</output>
