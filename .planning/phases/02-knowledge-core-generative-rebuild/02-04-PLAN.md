---
phase: 02-knowledge-core-generative-rebuild
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - scripts/kc-gate.ts
autonomous: false
requirements: [GROUND-01]

must_haves:
  truths:
    - "A throwaway gate script produces, per prompt, three blind generations: new-KC, current-KC, and raw-LLM (no KC)"
    - "Outputs are presented unlabeled/shuffled so the owner ranks without knowing which is which (Goodhart guard)"
    - "The owner judges the new-KC clearly better in a blind rank (the gate pass condition)"
    - "The gate is a script + owner judgment, NOT an eval harness — no persisted scored pipeline"
  artifacts:
    - path: "scripts/kc-gate.ts"
      provides: "Throwaway blind-gate generator: 3xN unlabeled outputs for owner ranking + optional Flash sanity delta (D-12)"
      contains: "shuffle"
  key_links:
    - from: "scripts/kc-gate.ts"
      to: "src/lib/kc/compiled.ts + src/lib/kc/assembler.ts"
      via: "new-KC generation = KC_IDEAS_SYSTEM_PROMPT (system) + assembled bundle (user)"
      pattern: "KC_IDEAS_SYSTEM_PROMPT"
---

<objective>
Run the D-12 thin owner-blind gate on the Ideas pilot — the real mid-point de-risk
checkpoint before authoring effort is spent replicating to other modes (D-11). On a
handful of real prompts, blind-rank new-KC vs current-KC vs raw-LLM-no-KC (D-13). This
is the means, NOT the value: a THROWAWAY script that produces unlabeled outputs for the
owner to rank — NOT an eval harness, NOT a revival of the dormant corpus/ eval machinery
(D-12). An optional quick SIM-1 Flash delta is a sanity number only; owner judgment is
the gate. Pass = owner judges the new-KC clearly better against BOTH baselines.

Purpose: GROUND-01 — confirm generative craft over generic competence (the rebuilt KC
out-generates both raw-LLM and the current KC) before replicating the proven shape.
This gate gates Plan 05 (Hooks + chat replication).
Output: scripts/kc-gate.ts + the owner's recorded blind-rank verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-knowledge-core-generative-rebuild/02-CONTEXT.md
@.planning/phases/02-knowledge-core-generative-rebuild/02-RESEARCH.md
@src/lib/kc/assembler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the throwaway blind-gate generator script (D-12 — NOT a harness)</name>
  <files>scripts/kc-gate.ts</files>
  <read_first>
    - scripts/build-corpus.ts (the dotenv + tsconfig-paths/register script-runner pattern to reuse, lines 1-35)
    - src/lib/engine/flash/run-flash-text-mode.ts (the Qwen call envelope :80-90 — getQwenClient, QWEN_SEED, temperature 0; FLASH_MODEL env :44 for the optional sanity delta)
    - src/lib/kc/assembler.ts (assembleBundle — produces the new-KC user message)
    - src/lib/kc/compiled.ts (KC_IDEAS_SYSTEM_PROMPT — the new-KC system prompt)
    - src/lib/engine/apollo-core.ts (READ ONLY to use APOLLO_SYSTEM_PROMPT as the "current-KC" baseline arm — this is the gate's comparison baseline, NOT authored craft; reading it here does NOT violate D-08, which governs the AUTHORED slices, not the gate baseline)
    - .planning/phases/02-knowledge-core-generative-rebuild/02-RESEARCH.md (Priority 4C the four-step minimal gate; Pitfall 5 gate-becoming-a-harness warning)
  </read_first>
  <action>
    Create scripts/kc-gate.ts as a THROWAWAY script (mirror the build-corpus.ts:1-35 runner
    toolchain). It takes a handful (5-10) of representative Ideas asks (owner-supplied via CLI
    arg or a small inline default list, optionally drawn from a realistic profile). For EACH
    prompt it produces THREE generations (D-13): (a) new-KC = Qwen call with system =
    KC_IDEAS_SYSTEM_PROMPT and user = assembleBundle(input); (b) current-KC = Qwen call using the
    existing scoring KC APOLLO_SYSTEM_PROMPT pointed at the idea task (baseline arm); (c) raw-LLM =
    Qwen call with NO KC system prompt, profile-only in the user message. SHUFFLE the three outputs
    per prompt and present them UNLABELED (arm order randomized per prompt) so the owner ranks
    blind (the Goodhart guard, D-13). Print the blind set to stdout or write a single throwaway
    .txt for the owner to read; record the per-prompt arm-to-label key separately so the owner
    blind ranks can be decoded AFTER ranking. OPTIONAL: run each generation through the existing
    Flash runner (runFlashRunner with the content text and the "idea" framing) and append the
    band/fraction as a sanity number — clearly labeled "sanity only, not the gate." Do NOT persist
    scored results, do NOT compute aggregate metrics, do NOT build a test suite (Pitfall 5 / D-12).
    Use Qwen-only (no Gemini/DeepSeek). The script is run once at this midpoint, then discarded.
  </action>
  <verify>
    <automated>grep -q "KC_IDEAS_SYSTEM_PROMPT" scripts/kc-gate.ts && grep -Eqi "shuffle|randomi" scripts/kc-gate.ts && grep -Eq "assembleBundle|assembler" scripts/kc-gate.ts && [ -z "$(grep -i 'training_corpus\|persistResults\|aggregateScore' scripts/kc-gate.ts)" ]</automated>
  </verify>
  <acceptance_criteria>
    - scripts/kc-gate.ts references KC_IDEAS_SYSTEM_PROMPT and the assembler (new-KC arm)
    - It produces three arms per prompt: new-KC, current-KC (APOLLO_SYSTEM_PROMPT), raw-LLM (no KC) — D-13
    - It shuffles/randomizes arm order per prompt and presents unlabeled output (Goodhart guard): contains shuffle/randomize logic
    - No persistence/aggregate-score/test-suite machinery: `grep -ci "training_corpus\|persistResults\|aggregateScore\|expect(" scripts/kc-gate.ts` returns 0
    - Qwen-only: `grep -ci "gemini\|deepseek" scripts/kc-gate.ts` returns 0
    - `npx tsc --noEmit` reports no error in scripts/kc-gate.ts
  </acceptance_criteria>
  <done>A throwaway script produces 3xN blind, shuffled, unlabeled Ideas generations for owner ranking, with an optional Flash sanity number — no harness.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner blind-ranks the gate output and renders the verdict (D-12/D-13)</name>
  <what-built>
    scripts/kc-gate.ts generated, per representative Ideas prompt, three blind, shuffled,
    unlabeled generations: the new general-craft KC, the current scoring KC pointed at the task,
    and a raw-LLM with no KC (profile-only). Optionally a SIM-1 Flash band per output as a sanity
    number (not the gate).
  </what-built>
  <how-to-verify>
    1. Run the gate: `npx tsx scripts/kc-gate.ts` (add your real Ideas asks if not using the defaults).
    2. Read the blind, unlabeled outputs WITHOUT decoding which arm is which. For each prompt, rank
       the three (best -> worst) on your taste bar: is it specific, mechanism-backed, shoot-ready,
       grounded — or generic slop?
    3. Decode with the recorded arm-to-label key. Confirm the gate pass condition (D-13): the
       new-KC arm is CLEARLY better than BOTH (a) the raw-LLM-no-KC arm AND (b) the current-KC arm.
       The optional Flash band is a sanity check only — your blind judgment is the gate.
    4. If the new-KC is NOT clearly better, capture the red-lines (which craft is thin / which
       failure mode leaked) and loop back to Plan 03 authoring before replicating to other modes.
  </how-to-verify>
  <resume-signal>Type "gate passed" if the new-KC is clearly better than both baselines, or describe the deficiencies to fix in the BASE/Ideas authoring before replication.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate prompts → Qwen | Owner-supplied asks flow into three Qwen calls; same injection surface as the assembler (reuse the fence) |
| gate output → owner judgment | The blind decision boundary; arm labels must be hidden until after ranking (Goodhart guard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Spoofing | tuning the KC to the Flash metric | mitigate | The Flash band is labeled sanity-only; the owner blind rank is the independent gate (D-13 Goodhart guard) |
| T-02-11 | Repudiation | scope creep into an eval harness | mitigate | Script is throwaway; acceptance forbids persistence/aggregate-scores/test-suite (Pitfall 5 / D-12) |
| T-02-12 | Tampering | gate prompt injection into Qwen | mitigate | Reuse the assembler fence for the new-KC arm user message; gate is dev-only, not a product path |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (reuses existing Qwen client) |
</threat_model>

<verification>
- kc-gate.ts produces 3xN blind shuffled unlabeled Ideas outputs; no harness machinery
- Owner blind-rank verdict recorded: new-KC clearly better than BOTH raw-LLM and current-KC (gate pass)
- `npm test` engine suite stays green (gate is a throwaway script, no engine change)
- Verifier note: this gate IS the verification of GROUND-01 success-criterion 3 (D-12 relaxes the ROADMAP "statistical eval" to "owner blind comparison") — do NOT look for a statistical eval artifact
</verification>

<success_criteria>
- GROUND-01 success-criterion 3 met via the D-12 owner-blind gate: the rebuilt KC measurably out-generates BOTH baselines per the owner's blind rank
- The proven shape is validated before replication (Plan 05 unblocked)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

New symbols/files created here (exclude from drift verification — they are new):
- Script: `scripts/kc-gate.ts` (throwaway blind-gate generator — three arms: new-KC, current-KC, raw-LLM)
- The gate verdict record (owner blind-rank result) — captured in the plan SUMMARY, not a persisted harness artifact
</artifacts_produced>

<output>
Create `.planning/phases/02-knowledge-core-generative-rebuild/02-04-SUMMARY.md` when done
</output>
