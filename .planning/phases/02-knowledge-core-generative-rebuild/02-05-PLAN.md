---
phase: 02-knowledge-core-generative-rebuild
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - .planning/corpus/hooks.md
  - .planning/corpus/chat.md
  - src/lib/kc/compiled.ts
autonomous: false
requirements: [GROUND-01]

must_haves:
  truths:
    - "The Hooks slice is authored to full depth, replicating the gate-proven Ideas shape"
    - "A thin chat stance-slice exists that mostly rides the BASE (D-14), proving the base generalizes to conversation"
    - "The owner has red-lined Hooks + chat to the taste bar"
    - "All four per-mode system prompts compile byte-stable from BASE + their slice"
    - "Phase 2 v1 slice coverage is complete: BASE + Ideas + Hooks (full) + chat (thin)"
  artifacts:
    - path: ".planning/corpus/hooks.md"
      provides: "Full-depth Hooks slice replicating the proven shape — archetypes, timing anchors, failure modes, actionability + diversity"
      min_lines: 40
    - path: ".planning/corpus/chat.md"
      provides: "Thin chat stance-slice riding the BASE (D-14)"
      min_lines: 12
    - path: "src/lib/kc/compiled.ts"
      provides: "All four per-mode system prompts compiled byte-stable (KC_HOOKS_SYSTEM_PROMPT, KC_CHAT_SYSTEM_PROMPT carry authored prose)"
      contains: "KC_HOOKS_SYSTEM_PROMPT"
  key_links:
    - from: ".planning/corpus/hooks.md + chat.md"
      to: "src/lib/kc/compiled.ts"
      via: "scripts/regen-kc.ts recompile"
      pattern: "KC_HOOKS_SYSTEM_PROMPT"
---

<objective>
Replicate the gate-proven shape (D-11/D-15) to the remaining v1 slices: author the
Hooks slice to FULL DEPTH and a THIN chat stance-slice that mostly rides the BASE (D-14).
This runs ONLY after the Ideas pilot passed the D-12 gate (Plan 04) — the mid-point
de-risk before spending authoring effort on every mode. Hooks is the close runner-up
(hardest craft / sharpest signal) and replicates the Ideas pattern. Chat is base-heavy
and sequenced last (P5 polish); a thin stance now proves the base generalizes to
conversation. Per D-09/D-10 the owner curates; Claude/research draft into the fixed
D-04 shape. After curation, recompile all per-mode constants.

Purpose: GROUND-01 — complete the v1 slice coverage (BASE + Ideas + Hooks full + chat
thin) as one foundation workstream, all riding the proven domain-general base.
Output: full-depth hooks.md + thin chat.md + recompiled compiled.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-knowledge-core-generative-rebuild/02-CONTEXT.md
@.planning/phases/02-knowledge-core-generative-rebuild/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DRAFT the Hooks slice (full depth) + thin chat stance-slice, replicating the proven shape</name>
  <files>.planning/corpus/hooks.md, .planning/corpus/chat.md</files>
  <read_first>
    - .planning/corpus/ideas.md (the GATE-PROVEN Ideas slice — the shape to replicate)
    - .planning/corpus/base.md (the curated BASE the chat slice mostly rides on, D-14)
    - .planning/corpus/hooks.md, .planning/corpus/chat.md (the Plan-01 skeletons being filled)
    - .planning/phases/02-knowledge-core-generative-rebuild/02-RESEARCH.md (Priority 1C hook archetype table — Bold Statement, Curiosity Gap, Contrarian, Research/Deep-Dive, Personal Narrative, Question-as-near-failure; timing anchors ~1.5s/3s/10-14 words; Priority 4A diversity)
    - .planning/phases/02-knowledge-core-generative-rebuild/02-CONTEXT.md (D-04 schema; D-07 TikTok-first with inline Reels/Shorts notes; D-14 thin chat; D-08 greenfield)
  </read_first>
  <action>
    DRAFT hooks.md to full depth replicating the gate-proven Ideas shape in the locked D-04 SLICE
    schema. Craft Patterns / Archetypes = the RESEARCH 1C hook archetypes (Bold Statement,
    Curiosity Gap, Contrarian/"wait what", Research/Deep-Dive, Personal Narrative) each with its
    mechanism; flag Question as the overused near-failure-mode under Failure Modes alongside empty
    ragebait and depth-mismatch. Add the timing anchors (algorithm first decision ~1.5s; strong
    hook lands in first 3s / ~10-14 words) TikTok-first with short inline "Reels/Shorts differs
    here" notes only where craft materially changes (D-07). Gold-Standard Exemplar Patterns =
    authored-fresh ABSTRACTED hook patterns (NOT scraped clips — D-04 ban). Actionability Contract:
    named mechanism, why-it-fits-this-creator, the ACTUAL hook line (ready-to-use form, never a
    description) + intra-batch diversity (each of N hooks fires a DIFFERENT archetype, mechanism-
    tagged so duplicates are visibly wrong — RESEARCH 4A) + flops-as-negative-grounding. DRAFT
    chat.md as a THIN stance-slice (D-14): a short Mode Job + a brief stance note that chat rides
    the BASE craft for conversation, with the other SLICE sections kept minimal/deferred-to-P5.
    Apply the D-00a check (anything domain-general belongs in BASE, not these slices). Do NOT open
    KNOWLEDGE_CORE / apollo-core (D-08). Mark both as draft-pending-curation.
  </action>
  <verify>
    <automated>grep -q "Actionability Contract" .planning/corpus/hooks.md && grep -Eqi "archetype|bold statement|curiosity gap" .planning/corpus/hooks.md && grep -Eqi "first 3s|1.5s|first-2s|timing" .planning/corpus/hooks.md && grep -Eqi "thin|rides the base|deferred to P5" .planning/corpus/chat.md && test "$(grep -vc '^[[:space:]]*$' .planning/corpus/hooks.md)" -ge 40</automated>
  </verify>
  <acceptance_criteria>
    - hooks.md has authored prose under all five SLICE sections, at least 40 non-blank lines
    - hooks.md Craft Patterns names at least Bold Statement + Curiosity Gap archetypes with mechanisms
    - hooks.md Failure Modes flags Question-as-overused and empty-ragebait and depth-mismatch
    - hooks.md carries the first-2s/3s timing anchors, TikTok-first with inline Reels/Shorts notes
    - hooks.md Actionability Contract requires the actual hook line + per-item mechanism tag + intra-batch diversity + flops-as-negative
    - chat.md is a thin stance-slice riding the BASE (at least 12 non-blank lines, marked thin/deferred-to-P5)
    - No scoring-shaped language leaked: `grep -ci "Strong/Mid/Weak\|dock for\|rubric" .planning/corpus/hooks.md` returns 0 (D-08)
    - Files marked draft-pending-curation
  </acceptance_criteria>
  <done>Hooks drafted to full depth (proven shape) + thin chat stance-slice drafted; both greenfield, value-forcing, ready for owner red-line.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner red-lines the Hooks + thin chat slices to the taste bar (D-10)</name>
  <what-built>
    A full-depth DRAFT of .planning/corpus/hooks.md (replicating the gate-proven Ideas shape — hook
    archetypes, timing anchors, failure modes, value-forcing Actionability Contract with the actual
    hook line + intra-batch diversity + flops-as-negative) and a THIN .planning/corpus/chat.md
    stance-slice that mostly rides the BASE (D-14).
  </what-built>
  <how-to-verify>
    1. Open .planning/corpus/hooks.md — confirm the archetypes, timing anchors, and failure modes
       match how you actually evaluate a hook; confirm the Actionability Contract forces a
       ready-to-use hook LINE (not a description) and forces N distinct archetypes. Red-line to
       your taste bar (hooks are the sharpest-signal craft).
    2. Open .planning/corpus/chat.md — confirm it is genuinely thin and rides the BASE (D-14); it
       only needs a stance, not full chat polish (that lands in P5).
    3. Apply the D-00a check: move any domain-general statement into base.md, not these slices.
    4. Edit the .md files directly; remove the draft-pending markers when satisfied.
  </how-to-verify>
  <resume-signal>Type "curated" when Hooks (full) + chat (thin) meet your taste bar, or describe the red-lines to apply.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Recompile all four per-mode system prompts byte-stable</name>
  <files>src/lib/kc/compiled.ts</files>
  <read_first>
    - scripts/regen-kc.ts (the compiler from Plan 01 — run it; do not modify)
    - .planning/corpus/base.md, .planning/corpus/ideas.md, .planning/corpus/hooks.md, .planning/corpus/chat.md (all curated prose now being compiled)
  </read_first>
  <action>
    Run scripts/regen-kc.ts to recompile all four corpus files into src/lib/kc/compiled.ts. Verify
    KC_HOOKS_SYSTEM_PROMPT and KC_CHAT_SYSTEM_PROMPT now carry their authored prose, all four
    per-mode prompts are present, and the output stays byte-stable on a second run. Do not hand-edit
    compiled.ts. Confirm no template holes / no per-request data leaked into any constant.
  </action>
  <verify>
    <automated>npx tsx scripts/regen-kc.ts && cp src/lib/kc/compiled.ts /tmp/kc-p5.ts && npx tsx scripts/regen-kc.ts && diff /tmp/kc-p5.ts src/lib/kc/compiled.ts && grep -q "KC_HOOKS_SYSTEM_PROMPT" src/lib/kc/compiled.ts && grep -q "KC_CHAT_SYSTEM_PROMPT" src/lib/kc/compiled.ts && test "$(grep -c 'Date.now\|Math.random' src/lib/kc/compiled.ts)" = 0</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/regen-kc.ts` runs clean; compiled.ts carries the authored Hooks + chat prose
    - All four per-mode system prompts present: KC_IDEAS_SYSTEM_PROMPT, KC_HOOKS_SYSTEM_PROMPT, KC_CHAT_SYSTEM_PROMPT (and KC_BASE)
    - Two consecutive runs are byte-identical (diff empty)
    - compiled.ts contains no Date.now / Math.random (count is 0)
    - `npx tsc --noEmit` passes for compiled.ts
  </acceptance_criteria>
  <done>All four per-mode system prompts compile byte-stable carrying authored craft; v1 slice coverage complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external craft research → authored .md | Public-blog hook material curated by the owner; copy-paste / unvetted craft risk |
| scoring KC → authored .md | Risk of re-importing scoring-biased hook craft (D-08 breach) |
| curated .md → compiled.ts | Author prose embedded into the byte-stable constants (escape handled by the Plan-01 compiler) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | external craft → Hooks/chat slice | mitigate | Owner curation gate (Task 2 blocking checkpoint) — research is steer, never authoritative copy-paste (D-09) |
| T-02-14 | Elevation | scoring-craft re-import (D-08) | mitigate | Author from owner steer + Priority-1C notes only; never open KNOWLEDGE_CORE; grep for scoring-shaped language in acceptance |
| T-02-15 | Tampering | chat slice over-authored (D-14 breach) | mitigate | Acceptance caps chat as thin/base-riding; owner confirms thinness in Task 2 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- hooks.md authored to full depth (proven shape), chat.md thin (D-14), both owner-curated + greenfield
- compiled.ts recompiles byte-stable with all four per-mode system prompts carrying authored prose
- `npm test` engine suite stays green (this plan touches no engine code)
- v1 slice coverage complete: BASE + Ideas + Hooks (full) + chat (thin) (D-14)
</verification>

<success_criteria>
- GROUND-01 fully satisfied: a generation-grade KC exists as a domain-general BASE + per-mode slices (Ideas, Hooks, chat), authored fresh, value-forcing, gate-proven on the Ideas pilot and replicated to Hooks + thin chat
- All per-mode prompts compile byte-stable
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 05)

New/filled symbols and content created here (exclude from drift verification — they are new):
- Authored content: full-depth `.planning/corpus/hooks.md` + thin `.planning/corpus/chat.md`
- Recompiled constants in `src/lib/kc/compiled.ts`: `KC_HOOKS_SLICE`, `KC_CHAT_SLICE`, `KC_HOOKS_SYSTEM_PROMPT`, `KC_CHAT_SYSTEM_PROMPT` now carry authored prose
- The Hooks archetype set + timing anchors + value-forcing Actionability Contract content (hooks)
</artifacts_produced>

<output>
Create `.planning/phases/02-knowledge-core-generative-rebuild/02-05-SUMMARY.md` when done
</output>
