---
phase: 02-knowledge-core-generative-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/corpus/base.md
  - .planning/corpus/ideas.md
  - .planning/corpus/hooks.md
  - .planning/corpus/chat.md
  - scripts/regen-kc.ts
  - src/lib/kc/kc-version.ts
  - src/lib/kc/compiled.ts
autonomous: true
requirements: [GROUND-01]

must_haves:
  truths:
    - "The .planning/corpus/ directory exists with one base file + one file per slice (ideas, hooks, chat)"
    - "Running the regen script compiles the corpus .md files into a byte-stable TS constant module with no per-request data"
    - "Re-running the regen script with unchanged .md input produces byte-identical compiled.ts output (deterministic, no drift)"
    - "The new KC module carries its own version constant decoupled from ENGINE_VERSION"
    - "The new KC module never imports apollo-core"
  artifacts:
    - path: ".planning/corpus/base.md"
      provides: "BASE prose SSOT skeleton with the four locked D-04 BASE sections"
      contains: "Voice & Stance"
    - path: ".planning/corpus/ideas.md"
      provides: "Ideas SLICE skeleton with the five locked D-04 SLICE sections"
      contains: "Mode Job"
    - path: "scripts/regen-kc.ts"
      provides: "Scripted .md -> byte-stable TS constant compiler (D-01)"
      contains: "escapeForTemplate"
    - path: "src/lib/kc/kc-version.ts"
      provides: "KC_GEN_VERSION constant decoupled from ENGINE_VERSION (D-06)"
      exports: ["KC_GEN_VERSION"]
    - path: "src/lib/kc/compiled.ts"
      provides: "Generated byte-stable per-mode constants + per-mode assembled system prompts (D-03)"
      contains: "KC_BASE"
  key_links:
    - from: "scripts/regen-kc.ts"
      to: ".planning/corpus/*.md"
      via: "readFileSync at regen time"
      pattern: "readFileSync"
    - from: "scripts/regen-kc.ts"
      to: "src/lib/kc/compiled.ts"
      via: "writeFileSync of escaped template constants"
      pattern: "writeFileSync"
---

<objective>
Stand up the thin CODE spine for the generative Knowledge-Core so the content
authoring workstream (Plan 02) has a compile target. This is a recombination of
three existing working patterns: the apollo-core.ts byte-stable constant concat,
the flash-prompts.ts system-prefix/volatile-user split, and the scripts/build-corpus.ts
script-runner toolchain. NO craft content is authored here — only the structure,
the fixed D-04 template skeletons (section headings, no prose), the scripted regen
compiler (D-01), and the decoupled version constant (D-06).

Purpose: De-risk the long pole by separating the small, well-defined code surface
from the long content/curation workstream. The spine must enforce the byte-stability
contract structurally before any prose lands.
Output: .planning/corpus/ skeleton .md files, scripts/regen-kc.ts, src/lib/kc/kc-version.ts,
and the generated src/lib/kc/compiled.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-knowledge-core-generative-rebuild/02-CONTEXT.md
@.planning/phases/02-knowledge-core-generative-rebuild/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create corpus skeleton .md files with the locked D-04 template shape</name>
  <files>.planning/corpus/base.md, .planning/corpus/ideas.md, .planning/corpus/hooks.md, .planning/corpus/chat.md</files>
  <read_first>
    - .planning/phases/02-knowledge-core-generative-rebuild/02-CONTEXT.md (D-04 fixed schema: BASE + SLICE section names; D-00a reviewer test; D-14 thin chat)
    - .planning/phases/02-knowledge-core-generative-rebuild/02-RESEARCH.md (Priority 1 tier-sort table; Priority 4A intra-batch diversity; Priority 4B flops-as-negative-grounding)
    - src/lib/engine/apollo-core.ts (file SHAPE + JSDoc contract to MIRROR — do NOT read or lift KNOWLEDGE_CORE craft content; D-08 greenfield)
  </read_first>
  <action>
    Create .planning/corpus/ and four prose-markdown SSOT files as SKELETONS ONLY (section
    headings + one-line authoring-intent notes per section — NO authored craft prose; that is
    Plan 02/05 owner-curated work, D-09/D-10). Lock the exact D-04 shape:
    base.md headings: `# BASE` then `## Voice & Stance`, `## Universal Craft Principles`,
    `## Anti-Generic Guardrails`, `## Value Bar / Self-Rejection Standard`.
    ideas.md, hooks.md headings: `# {Mode} Slice` then `## Mode Job`, `## Craft Patterns / Archetypes`,
    `## Failure Modes`, `## Gold-Standard Exemplar Patterns`, `## Actionability Contract`.
    chat.md: same SLICE headings but flag it as the THIN stance-slice (D-14) — a top note
    "Base-heavy; thin stance only, full polish deferred to P5" and minimal section stubs.
    Under Actionability Contract in each slice, add a placeholder note for the two value-forcing
    add-ons (intra-batch diversity + flops-as-negative-grounding, D-04). Add a top-of-file
    D-00a reviewer-test reminder in base.md ("if a statement only makes sense for one mode, it
    belongs in a slice"). Do NOT author craft prose — the headings + intent notes are the
    compile-target contract; prose is owner-curated downstream.
  </action>
  <verify>
    <automated>test -f .planning/corpus/base.md && test -f .planning/corpus/ideas.md && test -f .planning/corpus/hooks.md && test -f .planning/corpus/chat.md && grep -q "Voice & Stance" .planning/corpus/base.md && grep -q "Mode Job" .planning/corpus/ideas.md && grep -q "Actionability Contract" .planning/corpus/hooks.md</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist under .planning/corpus/
    - base.md contains the four exact BASE section headings (Voice & Stance, Universal Craft Principles, Anti-Generic Guardrails, Value Bar / Self-Rejection Standard)
    - ideas.md and hooks.md each contain the five exact SLICE headings (Mode Job, Craft Patterns / Archetypes, Failure Modes, Gold-Standard Exemplar Patterns, Actionability Contract)
    - chat.md is marked as the thin stance-slice (contains the string "thin stance" or "deferred to P5")
    - No authored craft prose present — `grep -ci "curiosity gap\|bold statement\|contrarian" .planning/corpus/base.md` returns 0 (skeleton only; craft lands in Plan 02/05)
  </acceptance_criteria>
  <done>Four corpus .md skeleton files exist with the locked D-04 headings and no authored craft prose.</done>
</task>

<task type="auto">
  <name>Task 2: Write the scripted regen compiler (D-01) producing byte-stable compiled.ts</name>
  <files>scripts/regen-kc.ts, src/lib/kc/compiled.ts</files>
  <read_first>
    - scripts/build-corpus.ts (the dotenv + tsconfig-paths/register script-runner pattern to reuse, lines 1-35)
    - src/lib/engine/apollo-core.ts (the escape rule at :4-6 — escape backticks and dollar-braces; the byte-stable concat at :254; the JSDoc BYTE-STABILITY CONTRACT to replicate verbatim in the generated header)
    - .planning/phases/02-knowledge-core-generative-rebuild/02-RESEARCH.md (Priority 2A/2B; Code Example "Scripted regen"; Open Q2 — assemble per-mode system prompt at COMPILE time)
  </read_first>
  <action>
    Create scripts/regen-kc.ts: a Node/tsx script that reads each .planning/corpus/*.md, applies
    an `escapeForTemplate(md)` that escapes backslash, backtick, and `${` (the apollo-core.ts:4-6
    escape rule, now scripted to remove the manual lockstep hazard at apollo-core.ts:26), and
    writes src/lib/kc/compiled.ts. The generated module MUST export, per mode: the raw slice
    constant (`KC_BASE`, `KC_IDEAS_SLICE`, `KC_HOOKS_SLICE`, `KC_CHAT_SLICE`) AND the assembled
    per-mode system prompt computed at COMPILE TIME (D-03, RESEARCH Open Q2) as
    `KC_IDEAS_SYSTEM_PROMPT = ${KC_BASE}\n\n---\n\n${KC_IDEAS_SLICE}` (and hooks, chat). Prepend
    a generated-file header: a "GENERATED by scripts/regen-kc.ts — do not hand-edit" line plus
    the verbatim BYTE-STABILITY CONTRACT note (no Date.now()/Math.random()/per-request data).
    The script writes the SAME bytes on re-run for unchanged input (deterministic ordering, no
    timestamps in output). Run it once to produce the initial compiled.ts from the skeletons.
    Reuse the build-corpus.ts runner toolchain; do NOT add a new dependency. compiled.ts MUST
    NOT import apollo-core (D-08 structural enforcement).
  </action>
  <verify>
    <automated>npx tsx scripts/regen-kc.ts && cp src/lib/kc/compiled.ts /tmp/kc-first.ts && npx tsx scripts/regen-kc.ts && diff /tmp/kc-first.ts src/lib/kc/compiled.ts && grep -q "KC_IDEAS_SYSTEM_PROMPT" src/lib/kc/compiled.ts && [ -z "$(grep -l 'apollo-core' src/lib/kc/compiled.ts 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/regen-kc.ts` runs without error and writes src/lib/kc/compiled.ts
    - Re-running on unchanged .md input produces byte-identical output: `diff` of two consecutive runs is empty
    - compiled.ts exports KC_BASE, KC_IDEAS_SLICE, KC_HOOKS_SLICE, KC_CHAT_SLICE and KC_IDEAS_SYSTEM_PROMPT, KC_HOOKS_SYSTEM_PROMPT, KC_CHAT_SYSTEM_PROMPT
    - compiled.ts contains no `Date.now`, no `Math.random`, and no un-escaped template hole: `grep -c "Date.now\|Math.random" src/lib/kc/compiled.ts` returns 0
    - `grep -L apollo-core src/lib/kc/compiled.ts` returns the file path (no apollo-core import — D-08)
    - scripts/regen-kc.ts contains `escapeForTemplate` and escapes backtick + `${`
  </acceptance_criteria>
  <done>regen-kc.ts compiles the corpus .md into a deterministic, byte-stable, apollo-core-free compiled.ts with per-mode raw slices + compile-time assembled system prompts.</done>
</task>

<task type="auto">
  <name>Task 3: Define the decoupled KC_GEN_VERSION constant (D-06)</name>
  <files>src/lib/kc/kc-version.ts</files>
  <read_first>
    - src/lib/engine/version.ts (the ENGINE_VERSION shape to MATCH for simplicity — and the boundary to stay DECOUPLED from; do NOT import or touch it)
    - src/lib/engine/corpus/corpus-version.ts (the DB-row-count precedent that is explicitly NOT the model — D-06 wants a simpler standalone constant, RESEARCH 2C)
  </read_first>
  <action>
    Create src/lib/kc/kc-version.ts exporting `export const KC_GEN_VERSION = "gen.1.0.0";` with a
    JSDoc stating it is the generative KC content version, DECOUPLED from ENGINE_VERSION (the
    protected Max-scoring boundary), bumped on any deliberate change to BASE or any SLICE .md,
    and used to stamp output provenance. Do NOT model it on corpus-version.ts (that is a DB-row
    training-corpus concept, RESEARCH 2C). Do NOT import version.ts. STATE EXPLICITLY in the
    JSDoc that the *persistence/output-stamping wiring* (where the stamp lands on a persisted
    message/block) is DEFERRED to Phase 3 where outputs are first persisted in product
    (RESEARCH Open Q1 / A3) — Phase 2 only defines the constant.
  </action>
  <verify>
    <automated>grep -q 'export const KC_GEN_VERSION' src/lib/kc/kc-version.ts && [ -z "$(grep 'ENGINE_VERSION\|corpus-version\|apollo-core' src/lib/kc/kc-version.ts)" ] && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v 'error' | head -1 || npx tsc --noEmit 2>&1 | grep 'kc-version' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/kc/kc-version.ts exports KC_GEN_VERSION as a string constant
    - The file does not import or reference ENGINE_VERSION, version.ts, corpus-version.ts, or apollo-core: `grep -c "ENGINE_VERSION\|corpus-version\|apollo-core" src/lib/kc/kc-version.ts` returns 0
    - The JSDoc explicitly states output-stamping wiring is deferred to Phase 3
    - `npx tsc --noEmit` reports no error in src/lib/kc/kc-version.ts
  </acceptance_criteria>
  <done>KC_GEN_VERSION exists as a standalone constant, decoupled from ENGINE_VERSION, with deferral of stamp-wiring to P3 documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus .md → compiled.ts | Author-controlled prose embedded into a TS template literal; unescaped backtick/`${` could break the byte-stable string or inject code |
| build-time script | scripts/regen-kc.ts runs in the dev/CI toolchain; reads local files only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | corpus .md → compiled.ts template literal | mitigate | escapeForTemplate escapes backslash, backtick, and `${` before embedding (D-01 escape rule, apollo-core.ts:4-6) |
| T-02-02 | Tampering | compiled.ts byte-stability | mitigate | No Date.now()/Math.random()/per-request data in the generated output; deterministic re-run verified by diff |
| T-02-03 | Elevation | D-08 greenfield breach | mitigate | compiled.ts must not import apollo-core — grep -L verification in Task 2 acceptance |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH: no new dependencies; Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- Four corpus skeleton .md files exist with locked D-04 headings, no craft prose
- regen-kc.ts produces a deterministic, byte-stable compiled.ts (diff-empty on re-run)
- compiled.ts exports per-mode raw slices + compile-time assembled system prompts, no apollo-core import
- KC_GEN_VERSION constant exists, decoupled from ENGINE_VERSION
- `npm test` engine suite stays green (this plan touches no engine code)
</verification>

<success_criteria>
- The corpus SSOT structure + scripted regen compiler + version constant exist (GROUND-01 code spine)
- The byte-stability contract is structurally enforced (deterministic compile, no volatile data, no apollo-core import)
- Authoring (Plan 02) has a working compile target
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New symbols/files created here (exclude from drift verification — they are new):
- Files: `.planning/corpus/base.md`, `.planning/corpus/ideas.md`, `.planning/corpus/hooks.md`, `.planning/corpus/chat.md`
- Script: `scripts/regen-kc.ts` (exports `escapeForTemplate` helper)
- Constant: `KC_GEN_VERSION` (in `src/lib/kc/kc-version.ts`)
- Generated constants (in `src/lib/kc/compiled.ts`): `KC_BASE`, `KC_IDEAS_SLICE`, `KC_HOOKS_SLICE`, `KC_CHAT_SLICE`, `KC_IDEAS_SYSTEM_PROMPT`, `KC_HOOKS_SYSTEM_PROMPT`, `KC_CHAT_SYSTEM_PROMPT`
- Template schema: the D-04 fixed BASE section set (Voice & Stance · Universal Craft Principles · Anti-Generic Guardrails · Value Bar / Self-Rejection Standard) and SLICE section set (Mode Job · Craft Patterns / Archetypes · Failure Modes · Gold-Standard Exemplar Patterns · Actionability Contract)
</artifacts_produced>

<output>
Create `.planning/phases/02-knowledge-core-generative-rebuild/02-01-SUMMARY.md` when done
</output>
