---
phase: 02-knowledge-core-generative-rebuild
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/kc/profile-role-map.ts
  - src/lib/kc/assembler.ts
  - src/lib/kc/assembler.test.ts
autonomous: true
requirements: [GROUND-02]

must_haves:
  truths:
    - "Each mode assembles a tight curated context slice (only the roles that mode needs), never the whole profile/KC"
    - "The assembled bundle stays under a hard length cap; lowest-priority roles drop past the cap"
    - "A thin or absent profile degrades to universal-craft + platform baseline, flagged honestly, never fabricated"
    - "User-controlled text (ask, overrides, anchor) is injection-fenced before entering the prompt"
    - "Profile data is referenced by semantic role, not card shape, so the v6.1 profile redesign touches one lookup table"
    - "The assembler output is a per-request user message (live grounding), distinct from the runner's static slice-binding"
  artifacts:
    - path: "src/lib/kc/profile-role-map.ts"
      provides: "Role -> column lookup isolating the v6.1 profile redesign (D-05 by-role constraint)"
      contains: "PROFILE_ROLE_MAP"
    - path: "src/lib/kc/assembler.ts"
      provides: "Per-request live-tier grounding assembler producing the volatile user message (GROUND-02 / D-05)"
      exports: ["AssemblerInput", "MODE_ROLES"]
    - path: "src/lib/kc/assembler.test.ts"
      provides: "Behavior tests: per-mode role filtering, length cap, cold-start degradation, injection fence"
      contains: "describe"
  key_links:
    - from: "src/lib/kc/assembler.ts"
      to: "creator_profiles (by semantic role)"
      via: "PROFILE_ROLE_MAP role->column lookup"
      pattern: "PROFILE_ROLE_MAP"
    - from: "src/lib/kc/assembler.ts"
      to: "user-supplied text (ask/overrides/anchor)"
      via: "<<<USER_CONTENT>>> injection fence"
      pattern: "USER_CONTENT"
---

<objective>
Build the single code seam of GROUND-02: the per-request live-tier grounding
assembler. Each mode declares a field-map of which live sources it needs; the
assembler pulls only those (under a hard length cap), degrades honestly on a thin
profile, fences user-supplied text, and emits the VOLATILE user message that feeds
the Phase-1 tool-runner. This is essentially a per-mode-field-mapped generalization
of the working formatCreatorContext (creator.ts:262) — reuse its null-guard,
cold-start, and injection-fence patterns; do NOT fork creator.ts (engine-scoped,
feeds the protected path).

Purpose: GROUND-02 — hand each tool a tight curated slice (niche + relevant craft
frame + the specific input), never the whole profile/KC, to avoid signal dilution.
Output: src/lib/kc/profile-role-map.ts + src/lib/kc/assembler.ts + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-knowledge-core-generative-rebuild/02-CONTEXT.md
@.planning/phases/02-knowledge-core-generative-rebuild/02-RESEARCH.md
@src/lib/tools/tool-runner.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the semantic-role profile map (D-05 by-role constraint)</name>
  <files>src/lib/kc/profile-role-map.ts</files>
  <read_first>
    - src/lib/engine/creator.ts (formatCreatorContext :262-360 — the working semantic-role null-guarded mapping precedent; lines :299-353 per-field push-if-present)
    - src/lib/schemas/creator-profile.ts (validated profile field shapes + sanitizeText + the 500-char pain_points cap precedent)
    - src/types/database.types.ts (creator_profiles columns :713-750 — current column bindings)
    - .planning/phases/02-knowledge-core-generative-rebuild/02-RESEARCH.md (Priority 3C semantic-role to column table; 3D per-mode role lists)
  </read_first>
  <action>
    Create src/lib/kc/profile-role-map.ts. Define a Role union: niche, audience, goals, wins,
    flops, platform. Export a single PROFILE_ROLE_MAP typed as Record of Role to a formatting
    function over the creator_profiles row that returns a formatted string or null when absent
    (null-guarded, per creator.ts:299-353). Current 9-card bindings (RESEARCH 3C): niche =
    niche_primary plus optional niche_sub; audience = target_audience JSON formatted as
    age/gender/geo/language parts; goals = primary_goal plus optional creator_stage; wins =
    past_wins (creator-self-reported, directional); flops = past_flops (same); platform =
    target_platforms[0] OR the per-request platform param with the param winning (D-07). Keep ALL
    column knowledge in this ONE file so the v6.1 profile redesign touches only this table (D-05
    by-role, NOT by-card). Do NOT import creator.ts (engine-scoped); replicate patterns fresh.
  </action>
  <verify>
    <automated>grep -q "PROFILE_ROLE_MAP" src/lib/kc/profile-role-map.ts && grep -Eq "niche|audience|goals|wins|flops|platform" src/lib/kc/profile-role-map.ts && [ -z "$(grep 'engine/creator' src/lib/kc/profile-role-map.ts)" ]</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/kc/profile-role-map.ts exports PROFILE_ROLE_MAP keyed by all six roles (niche, audience, goals, wins, flops, platform)
    - Each role maps to a function returning string-or-null (null-guarded for absent fields)
    - File does not import src/lib/engine/creator.ts or apollo-core: `grep -c "engine/creator\|apollo-core" src/lib/kc/profile-role-map.ts` returns 0
    - `npx tsc --noEmit` reports no error in profile-role-map.ts
  </acceptance_criteria>
  <done>PROFILE_ROLE_MAP isolates all current creator_profiles column knowledge behind six semantic roles in one file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the per-request live-tier assembler with field-map, cap, cold-start, fence</name>
  <files>src/lib/kc/assembler.ts, src/lib/kc/assembler.test.ts</files>
  <read_first>
    - src/lib/kc/profile-role-map.ts (the PROFILE_ROLE_MAP from Task 1 this consumes)
    - src/lib/engine/creator.ts (cold-start degradation :282-298 — the "(using platform baseline)" honest flag; injection fence :330-337 and stripUserContentSentinels :252-254)
    - src/lib/tools/tool-runner.ts (the KnowledgeBundle / ToolInput contract this output feeds; the static-runner-field vs per-request ambiguity)
    - .planning/phases/02-knowledge-core-generative-rebuild/02-RESEARCH.md (Priority 3A static-slice-binding vs per-request split resolution = A4; 3D field-map + length cap; 4B flops-as-negative-grounding honesty caveat)
  </read_first>
  <behavior>
    - Ideas mode pulls roles niche+audience+goals+wins+flops+platform; Hooks pulls niche+audience+platform+wins+flops; Chat pulls niche+audience+platform (RESEARCH 3D)
    - A mode never emits roles outside its MODE_ROLES set (signal-dilution guard)
    - Assembled output length is at most a named hard cap constant; past the cap, lowest-priority roles are dropped (not truncated mid-field)
    - Absent/thin profile -> output contains the honest baseline flag string (e.g. "using {platform} baseline"), never a fabricated value
    - ask / overrides / anchor are wrapped in <<<USER_CONTENT>>> ... <<<END_USER_CONTENT>>> and sentinel-stripped first
    - wins/flops are surfaced as "creator-reported, directional" (no fabricated mechanism behind a URL — honesty spine, RESEARCH 4B)
  </behavior>
  <action>
    Write src/lib/kc/assembler.test.ts FIRST (RED) covering the six behaviors above, then
    src/lib/kc/assembler.ts (GREEN). Export the typed input shape AssemblerInput with fields:
    ask (string), platform (tiktok | instagram | youtube), mode (idea | hooks | chat), optional
    overrides (string), optional anchor (string) — the D-05 input shape, extending the Phase-1
    ToolInput so P3 wires rather than retrofits (D-07 forward-wire). Export MODE_ROLES as a
    Record of mode to Role[] with the lists above. Export the main assembler function
    (e.g. assembleBundle(input, profileRow)) returning the per-request USER MESSAGE string.
    RESOLVE the RESEARCH A4 ambiguity explicitly in a doc comment: the runner knowledgeBundle
    field = static slice-binding (which compiled KC slice this tool uses); the assembler output =
    per-request live grounding (the user message) — NEVER mutate the module-level runner const.
    Define a named hard length-cap constant (e.g. BUNDLE_CHAR_CAP) with a comment that the VALUE
    is tuned post-authoring (after BASE+Ideas exist, the pilot in Plan 03) and sized so the live
    tier stays the cheap-varying part vs the warm prefix — not hardcoded blind. Reuse the
    <<<USER_CONTENT>>> fence + sentinel-strip pattern for ask/overrides/anchor. Cold-start: when
    the profile row is null/thin, emit universal-craft + a "{platform} baseline" honest flag
    (mirror creator.ts:282-298). Validate the input with zod at the function boundary (input
    validation at boundaries per CLAUDE.md). Do NOT import creator.ts or apollo-core.
  </action>
  <verify>
    <automated>npx vitest run src/lib/kc/assembler.test.ts && grep -q "AssemblerInput" src/lib/kc/assembler.ts && grep -q "MODE_ROLES" src/lib/kc/assembler.ts && grep -q "USER_CONTENT" src/lib/kc/assembler.ts && [ -z "$(grep 'engine/creator\|apollo-core' src/lib/kc/assembler.ts)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/kc/assembler.test.ts` passes all six behavior tests
    - assembler.ts exports AssemblerInput, MODE_ROLES, and the assemble function
    - MODE_ROLES.idea = [niche,audience,goals,wins,flops,platform]; MODE_ROLES.hooks = [niche,audience,platform,wins,flops]; MODE_ROLES.chat = [niche,audience,platform]
    - A mode output contains no role outside its MODE_ROLES set (test asserts goals/anchor do not leak into chat)
    - Cold-start (null profile) output contains the honest baseline flag string and no fabricated profile value
    - ask/overrides/anchor appear inside <<<USER_CONTENT>>> fences: `grep -c "USER_CONTENT" src/lib/kc/assembler.ts` is >= 1
    - A named hard length-cap constant exists (e.g. BUNDLE_CHAR_CAP) with a tuned-post-authoring comment
    - assembler.ts does not import engine/creator or apollo-core: `grep -c "engine/creator\|apollo-core" src/lib/kc/assembler.ts` returns 0
    - The A4 static-slice-binding vs per-request split is documented in a doc comment
  </acceptance_criteria>
  <done>The assembler produces a per-mode, capped, cold-start-honest, injection-fenced live user message; the static-vs-per-request split is explicit; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer free-text (ask/overrides/anchor) → prompt | Untrusted user input flows into a Qwen prompt; prompt-injection risk |
| creator_profiles row → prompt | DB-stored profile (some free-text fields) embedded into the user message |
| per-request assembler → tool-runner | The boundary where live grounding becomes the model user message |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | ask/overrides/anchor → user message | mitigate | Wrap in <<<USER_CONTENT>>> fence + stripUserContentSentinels before embedding (reuse creator.ts:330-337,252-254); zod-validate input at the function boundary |
| T-02-05 | Information Disclosure | whole-profile dilution | mitigate | Per-mode MODE_ROLES field-map pulls only needed roles + hard length cap (D-05); a mode never emits roles outside its set |
| T-02-06 | Spoofing | fabricated profile on cold-start | mitigate | Cold-start degrades to platform baseline with an honest flag; wins/flops surfaced as "creator-reported, directional" — never a fabricated mechanism (honesty spine) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH: no new dependencies; reuses repo zod) |
</threat_model>

<verification>
- assembler.test.ts passes (per-mode filtering, cap, cold-start, fence)
- PROFILE_ROLE_MAP isolates column knowledge behind six roles
- No import of engine/creator or apollo-core in either file (DDD bounded-context isolation + D-08 hygiene)
- `npm test` engine suite stays green (this plan touches no engine code)
</verification>

<success_criteria>
- GROUND-02 satisfied: each tool can assemble a tight per-mode curated context slice, never the whole profile/KC
- Cold-start honest degradation + injection fence + hard cap all enforced and tested
- The static-slice-binding vs per-request-live-grounding split is explicit (resolves RESEARCH A4)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New symbols/files created here (exclude from drift verification — they are new):
- File: `src/lib/kc/profile-role-map.ts` — exports `PROFILE_ROLE_MAP`, the `Role` union
- File: `src/lib/kc/assembler.ts` — exports `AssemblerInput`, `MODE_ROLES`, the assemble function, `BUNDLE_CHAR_CAP` (named cap constant)
- File: `src/lib/kc/assembler.test.ts`
- Field-map structure: `MODE_ROLES` (idea/hooks/chat → Role[])
- The per-request live-grounding input shape `{ask, platform, mode, overrides?, anchor?}` (extends Phase-1 ToolInput; D-07 forward-wire)
</artifacts_produced>

<output>
Create `.planning/phases/02-knowledge-core-generative-rebuild/02-02-SUMMARY.md` when done
</output>
