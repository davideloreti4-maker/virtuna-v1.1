---
phase: 05-profile-simulate-wow
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/lib/tools/runners/simulate-runner.ts
  - src/app/api/tools/simulate/route.ts
  - src/lib/tools/runners/__tests__/simulate-runner.test.ts
  - src/app/api/tools/simulate/__tests__/route.test.ts
autonomous: true
requirements: [SIMU-01, SIMU-02, SIMU-03]
must_haves:
  truths:
    - "runSimulate runs the drafted-message stimulus through a General audience via the EXISTING Flash engine — buildAudienceRepaint → runFlashTextMode → aggregateFlash — never re-rolling band math (SIMU-01 / Don't-Hand-Roll)"
    - "The card branches on subjectKind: person → a single read (lead persona verdict+reasoning+quote, band/fraction SUPPRESSED); panel → band+fraction+clustered themes+per-persona drill (D-02 / Pitfall 2 — no 'distribution of one')"
    - "The emitted reaction-distribution block is bands-only with tier:'Directional' by rule (resolveTier); behavioral-core is NOT imported here (Pitfall 5 — behavioral prompt rides only the Profile READ)"
    - "The drafted message is instruction-isolated before the Flash call (D-08); the simulate route caps it and resolves the audience via getAudience under the session (RLS-scoped, never raw weights)"
    - "Both Profile and Simulate blocks append to the SAME open thread (SIMU-03 — one continuous thread)"
  artifacts:
    - path: "src/lib/tools/runners/simulate-runner.ts"
      provides: "runSimulate + SimulateRunInput"
      contains: "runSimulate"
    - path: "src/app/api/tools/simulate/route.ts"
      provides: "POST /api/tools/simulate (auth→csrf→cap→getAudience→normalizeStimulus→runSimulate→insertMessage)"
      exports: ["POST"]
  key_links:
    - from: "src/lib/tools/runners/simulate-runner.ts"
      to: "runFlashTextMode + aggregateFlash + buildAudienceRepaint"
      via: "lift the per-audience read from two-audience-read.ts, drop the 2-audience delta"
      pattern: "runFlashTextMode|aggregateFlash"
    - from: "src/app/api/tools/simulate/route.ts"
      to: "insertMessage (same open thread)"
      via: "persist the reaction-distribution block to the user's thread"
      pattern: "insertMessage"
---

<objective>
Build the Simulate verb: `runSimulate` runs a drafted message through a General audience on the existing Flash engine and emits a `reaction-distribution` card; the `/api/tools/simulate` route wires it into the SAME thread so Profile→Simulate is one continuous flow.

Purpose: SIMU-01/02/03. `runTwoAudienceRead` is the closest analog — lift its per-audience read, drop the 2-audience delta. The honesty line is D-02: a person gets a single honest read (no fabricated crowd — Pitfall 2), a panel gets the distribution + clustered themes. Bands-only + Directional-by-rule + reuse `aggregateFlash` (never re-roll). This route is the second untrusted boundary — the drafted message + the resolved audience's `success_criterion`/`custom_context` are isolated (D-08).
Output: `runSimulate` + the simulate route, person/panel-aware, bands-only, persisting to the open thread.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-profile-simulate-wow/05-CONTEXT.md
@.planning/phases/05-profile-simulate-wow/05-RESEARCH.md
@.planning/phases/05-profile-simulate-wow/05-PATTERNS.md
@src/lib/engine/flash/two-audience-read.ts
@src/app/api/tools/read/route.ts
@src/lib/engine/stimulus/normalize.ts
</context>

## Artifacts this phase produces (source-grounding exclusion set)

This plan creates: `runSimulate`, type `SimulateRunInput` (simulate-runner.ts); `POST` at `/api/tools/simulate`. Consumes 05-01 (`ReactionDistributionBlockSchema`) + existing engine (`runFlashTextMode`, `aggregateFlash`, `buildAudienceRepaint`, `resolveTier`). See 05-01-PLAN.md for the full phase-wide set.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: simulate-runner — Flash read → person/panel reaction-distribution block</name>
  <files>src/lib/tools/runners/simulate-runner.ts, src/lib/tools/runners/__tests__/simulate-runner.test.ts</files>
  <read_first>
    - src/lib/engine/flash/two-audience-read.ts (lines 61-104 — `readForAudience`: repaint → runFlashTextMode → aggregateFlash → per-persona drill; lift this, DROP `buildDelta` L114-136 + the 2-audience framing)
    - src/lib/engine/flash/flash-aggregate.ts (lines 101-124 — `FlashBand` + `aggregateFlash`; reuse, never re-roll)
    - src/lib/engine/flash/build-reaction-panel.ts (`buildAudienceRepaint` — the General/empty → undefined no-op)
    - src/lib/audience/resolve-tier.ts (`resolveTier` — Directional-by-rule)
    - .planning/phases/05-profile-simulate-wow/05-RESEARCH.md (Pattern 4 reaction-distribution shape + Open Q1 person-mode mechanic) and 05-PATTERNS.md (simulate-runner.ts section)
  </read_first>
  <behavior>
    - runSimulate for a panel audience returns a block with `band`+`fraction` (from aggregateFlash, not re-rolled) + `themes[]` + `reactions[]`, and `read` null/absent.
    - runSimulate for a person audience returns a block with `read` (lead persona verdict+reasoning+quote) and `band`/`fraction` SUPPRESSED (null/absent) — Pitfall 2.
    - The emitted block has `tier: "Directional"` (resolveTier on the General audience) and `model: "sim1-flash"`.
    - The drafted message is isolated as data before the Flash call; behavioral-core is NOT imported.
  </behavior>
  <action>
    Create `runSimulate(input: SimulateRunInput, deps?)` where `SimulateRunInput = { audience: Audience; stimulus: Stimulus; subjectKind?: "person"|"panel" }`. Lift `readForAudience`'s core: build `audienceRepaint` via the General/empty→undefined no-op, run `runFlashTextMode(stimulus.content, "idea", { niche:null, contentType:null }, repaint)`, `aggregateFlash(result.personas)` → `{band, fraction}`. Branch on `subjectKind` (derive from `audience` shape if not passed; default person per D-02): person → pick the lead/highest-share persona's `{verdict, reasoning, quote}` as the single `read`, SUPPRESS `band`/`fraction` (Pitfall 2 — no "7/10" for one human); panel → keep `band`+`fraction`, cluster `themes[]` (group personas by reaction-frame label, one representative quote each), and map per-persona `reactions[]` (`{archetype, verdict, quote}`). Instruction-isolate `stimulus.content` (and any resolved `success_criterion`) before the Flash call (D-08). Emit a `ReactionDistributionBlockSchema`-valid block: `audienceName`, `subjectKind`, `model: "sim1-flash"`, `tier: resolveTier(audience)`. Do NOT import behavioral-core (Pitfall 5). Write simulate-runner.test.ts covering the `<behavior>` cases with a mocked `runFlashTextMode`.
  </action>
  <acceptance_criteria>
    - `node ./node_modules/vitest/vitest.mjs run src/lib/tools/runners/__tests__/simulate-runner.test.ts` exits 0.
    - A person-audience test asserts `read` present and `band`/`fraction` null/absent (no crowd — Pitfall 2); a panel test asserts `band`+`fraction`+`themes` present and `read` absent.
    - A test asserts `fraction` equals what `aggregateFlash` returns (not re-rolled) and `tier === "Directional"`.
    - `grep -cE "behavioral-core|BEHAVIORAL_" src/lib/tools/runners/simulate-runner.ts` === 0 (Pitfall 5).
    - The emitted block passes `ReactionDistributionBlockSchema.safeParse`.
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/tools/runners/__tests__/simulate-runner.test.ts</automated>
  </verify>
  <done>runSimulate reuses the Flash engine, branches person/panel honestly, emits a bands-only Directional reaction-distribution block, and never imports the behavioral core.</done>
</task>

<task type="auto">
  <name>Task 2: /api/tools/simulate route — security spine + same-thread persistence</name>
  <files>src/app/api/tools/simulate/route.ts, src/app/api/tools/simulate/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/tools/read/route.ts (auth→csrf→cap→getAudience(RLS)→run→insertMessage skeleton; the `getAudience` under-session resolution L112-141 — never raw weights)
    - src/app/api/tools/read/__tests__/route.test.ts (route-test convention)
    - src/lib/audience/audience-repo.ts (`getAudience` signature)
    - src/lib/engine/stimulus/normalize.ts (`normalizeStimulus` for the text path)
  </read_first>
  <action>
    Create `POST /api/tools/simulate` mirroring read/route.ts: `auth.getUser()` 401 → `csrfGuard` → parse body `{ audienceId, message }`. Cap `message` with `MAX_MESSAGE_LENGTH` (e.g. 2000) — 400 on empty/oversize. Resolve the audience via `getAudience(supabase, audienceId)` under the session (RLS-scoped — reject 400 `audience_not_found` if it does not resolve; never trust raw weights). `normalizeStimulus({ kind:"text", text: message })` → `Stimulus`. `createOpenThreadLazy(user.id)` → `runSimulate({ audience, stimulus })` → `insertMessage(openThread.id, "assistant", [block], kcStamp().kcGenVersion)` (re-validates) → `Response.json({ block })`; the SAME open thread the profile-read was appended to (SIMU-03). try/catch → 500. Write route.test.ts: 401 unauthenticated, 400 on empty/over-cap message, 400 on an unresolvable audienceId, 200 returning `{block}` with `block.type === "reaction-distribution"` on the happy path (mock supabase + getAudience + runSimulate).
  </action>
  <acceptance_criteria>
    - `node ./node_modules/vitest/vitest.mjs run src/app/api/tools/simulate/__tests__/route.test.ts` exits 0.
    - Unauthenticated POST returns 401 BEFORE getAudience/runSimulate (mock-not-called); over-cap message returns 400; an unresolvable audienceId returns 400.
    - Happy path returns `{ block }` with `block.type === "reaction-distribution"` and calls `insertMessage` once on the open thread.
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/app/api/tools/simulate/__tests__/route.test.ts</automated>
  </verify>
  <done>The simulate route enforces auth/csrf/cap/RLS-audience resolution and persists a reaction-distribution block to the same open thread.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/tools/simulate | Untrusted drafted message + an audienceId cross into the server |
| drafted message + audience success_criterion → Flash prompt | User free-text reaches the Flash steering prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Spoofing / Elevation | simulate route auth + audience resolution | mitigate | `auth.getUser()` 401 + `csrfGuard` before any run; audience resolved via `getAudience` under the session (RLS-scoped), never trusted as raw body weights; bad id → 400 |
| T-05-15 | Tampering | drafted message / success_criterion → Flash prompt | mitigate | D-08 instruction isolation before the Flash call; the message is data, not steering instructions |
| T-05-16 | DoS | uncapped drafted message | mitigate | `MAX_MESSAGE_LENGTH` route cap, 400 on oversize |
| T-05-17 | Tampering | smuggled 0-100 in the reaction-distribution block | mitigate | `.strict()` bands-only schema (05-01); `aggregateFlash` band math reused, never re-rolled |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs (D-05 zero-new-deps) — Package Legitimacy Gate N/A |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/lib/tools/runners src/app/api/tools/simulate` green.
- Flash engine suite stays green (no re-roll regression): `node ./node_modules/vitest/vitest.mjs run src/lib/engine/flash`.
- `npx tsc --noEmit` introduces no new errors on touched files.
</verification>

<success_criteria>
SIMU-01/02 land: a drafted message runs through a General audience on the existing Flash engine and renders a person/panel-aware, bands-only, Directional reaction-distribution card persisted to the SAME thread — the back half of the SIMU-03 one-thread wow.
</success_criteria>

<output>
Create `.planning/phases/05-profile-simulate-wow/05-05-SUMMARY.md` when done.
</output>
