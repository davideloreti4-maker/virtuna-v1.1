---
phase: 05-profile-simulate-wow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/engine/behavioral-core.ts
  - src/lib/engine/__tests__/behavioral-core.test.ts
autonomous: true
requirements: [PROF-02]
must_haves:
  truths:
    - "behavioral-core.ts is a byte-stable cached system prompt mirroring apollo-core.ts — NO per-request interpolation (Date.now/Math.random/user bytes), so Qwen input-cache fires across Profiles (D-05)"
    - "The behavioral brain is HARVESTED read-only from feat/chat-ethics-gate corpus via git show — the branch is NEVER merged and NONE of its engine logic code is reused (D-05)"
    - "The D-04 light ethics guardrail is embedded at the prompt layer: 'a read to inform YOUR decision', refuse manipulate/coerce/stalk/dox, honesty caveat — no heavy classifier"
    - "Forensic depth is tier-gated in the prompt: FORENSIC_MAX_DIRECTIVE permits deception-likelihood/micro-expression-timestamp reads (video only), FORENSIC_FLASH_DIRECTIVE forbids them on text (D-03 — signal doesn't exist in text)"
  artifacts:
    - path: "src/lib/engine/behavioral-core.ts"
      provides: "BEHAVIORAL_CORE + ethics block + tier-gated forensic directives + BEHAVIORAL_SYSTEM_PROMPT_FLASH/_MAX (+ optional scanForExcludedCoaching backstop)"
      contains: "BEHAVIORAL_SYSTEM_PROMPT_FLASH"
      min_lines: 60
  key_links:
    - from: "src/lib/engine/behavioral-core.ts"
      to: "BEHAVIORAL_SYSTEM_PROMPT_MAX"
      via: "assembled prefix = BEHAVIORAL_CORE + ethics block + FORENSIC_MAX_DIRECTIVE"
      pattern: "BEHAVIORAL_SYSTEM_PROMPT_MAX"
---

<objective>
Build the NEW cached behavioral system prompt — the cognition layer ("who this person is") that grounds the Profile READ — by harvesting the distilled behavioral corpus from the parked `feat/chat-ethics-gate` branch and embedding it exactly the way `apollo-core.ts` embeds the craft `KNOWLEDGE_CORE`.

Purpose: PROF-02's forensic READ needs a behavioral brain distinct from the in-`src` Apollo craft core. D-05 locks: HARVEST the corpus-as-constant + the prompt-layer ethics block; reuse NONE of the branch's engine logic. D-04 ethics is a LIGHT prompt-layer guardrail. D-03 forensic depth is tier-gated so a thin chat never over-claims a micro-expression read.
Output: `behavioral-core.ts` with a byte-stable `BEHAVIORAL_CORE`, the ethics block, two tier-gated forensic directives, and the two assembled `BEHAVIORAL_SYSTEM_PROMPT_FLASH/_MAX` prompts that 05-04's profile-runner consumes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-profile-simulate-wow/05-CONTEXT.md
@.planning/phases/05-profile-simulate-wow/05-RESEARCH.md
@.planning/phases/05-profile-simulate-wow/05-PATTERNS.md
@src/lib/engine/apollo-core.ts
</context>

## Artifacts this phase produces (source-grounding exclusion set)

This plan creates: `BEHAVIORAL_CORE`, `BEHAVIORAL_ETHICS_BLOCK`, `FORENSIC_FLASH_DIRECTIVE`, `FORENSIC_MAX_DIRECTIVE`, `BEHAVIORAL_SYSTEM_PROMPT_FLASH`, `BEHAVIORAL_SYSTEM_PROMPT_MAX`, `EXCLUDE_REGISTRY`, `scanForExcludedCoaching` (all in `src/lib/engine/behavioral-core.ts`). See 05-01-PLAN.md for the full phase-wide artifact set.

<tasks>

<task type="auto">
  <name>Task 1: Harvest the behavioral corpus + assemble the byte-stable system prompts</name>
  <files>src/lib/engine/behavioral-core.ts, src/lib/engine/__tests__/behavioral-core.test.ts</files>
  <read_first>
    - src/lib/engine/apollo-core.ts (lines 1-60 — the byte-stability CONTRACT doc + the `KNOWLEDGE_CORE`→`APOLLO_SYSTEM_PROMPT` three-export assembly shape to mirror)
    - .planning/phases/05-profile-simulate-wow/05-RESEARCH.md (Pattern 1, lines 225-253 — the recommended export shape + the exact ethics/forensic directive copy)
    - .planning/phases/05-profile-simulate-wow/05-PATTERNS.md (behavioral-core.ts section — the escape rules + "distinct from apollo-core" note)
    - feat/chat-ethics-gate:.planning/corpus/BEHAVIORAL-CORE.md (read via `git show feat/chat-ethics-gate:.planning/corpus/BEHAVIORAL-CORE.md` — the corpus body; do NOT `git merge`)
  </read_first>
  <action>
    Read the corpus body with `git show feat/chat-ethics-gate:.planning/corpus/BEHAVIORAL-CORE.md` (read-only — NEVER `git merge` the branch; Out-of-Scope lock). Create `src/lib/engine/behavioral-core.ts` mirroring apollo-core.ts's three-export shape: `BEHAVIORAL_CORE` (the corpus body embedded verbatim as a build-time template-literal constant — escape every backtick `` \` `` and `${` per the apollo-core regeneration rule; NO interpolation of `Date.now()`/`Math.random()`/per-request data so the cache prefix stays byte-identical), `BEHAVIORAL_ETHICS_BLOCK` (the D-04 light guardrail — copy adapted from RESEARCH Pattern 1: "a behavioral read to inform YOUR decision", explain-don't-prescribe, refuse manipulate/coerce/stalk/dox, "DIRECTIONAL from limited evidence" caveat), `FORENSIC_FLASH_DIRECTIVE` (text tier: do NOT claim micro-expression/deception-timestamp reads — that signal does not exist in text) and `FORENSIC_MAX_DIRECTIVE` (video tier: may surface deception likelihood as Low/Medium/High band word NEVER a number + micro-expression cues with timestamps, citing only observed cues — D-03). Assemble `BEHAVIORAL_SYSTEM_PROMPT_FLASH = BEHAVIORAL_CORE + "\n\n---\n\n" + BEHAVIORAL_ETHICS_BLOCK + "\n\n" + FORENSIC_FLASH_DIRECTIVE` and `BEHAVIORAL_SYSTEM_PROMPT_MAX` with the MAX directive — keep the CORE+ethics prefix shared so the cache fires on the common prefix (only the tier suffix differs — Pitfall 5). Add a behavioral-core.test.ts asserting: both system prompts include the never-coach refusal line, the "directional / limited evidence" caveat line, FLASH contains the text-tier forbid-forensic sentence, MAX contains the deception-likelihood band-word sentence; and a byte-stability check that the file source contains no `Date.now(`/`Math.random(`/`new Date(` (grep === 0 inside the module body).
  </action>
  <acceptance_criteria>
    - `behavioral-core.ts exports BEHAVIORAL_SYSTEM_PROMPT_FLASH` and `BEHAVIORAL_SYSTEM_PROMPT_MAX` (grep both === present).
    - `node ./node_modules/vitest/vitest.mjs run src/lib/engine/__tests__/behavioral-core.test.ts` exits 0 with the never-coach line, caveat line, and tier-gated directive assertions passing.
    - `grep -cE "Date\.now\(|Math\.random\(|new Date\(" src/lib/engine/behavioral-core.ts` === 0 (byte-stable, cache-safe).
    - No `git merge` occurred: `git log --oneline -1` HEAD is unchanged from plan start (harvest was read-only via `git show`).
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/engine/__tests__/behavioral-core.test.ts</automated>
  </verify>
  <done>behavioral-core.ts assembles two tier-gated, byte-stable behavioral prompts with the D-04 guardrail; corpus harvested read-only, branch unmerged.</done>
</task>

<task type="auto">
  <name>Task 2: Add the discretionary non-model coaching backstop (EXCLUDE_REGISTRY + scanForExcludedCoaching)</name>
  <files>src/lib/engine/behavioral-core.ts, src/lib/engine/__tests__/behavioral-core.test.ts</files>
  <read_first>
    - feat/chat-ethics-gate:src/lib/chat/ethics-gate.ts (read via `git show feat/chat-ethics-gate:src/lib/chat/ethics-gate.ts` — harvest the `EXCLUDE_REGISTRY` names + the pure `scanForExcludedCoaching` regex; SKIP `gateStreamBuffer`/`gateAsyncDeltas` streaming logic — irrelevant per D-05)
    - .planning/phases/05-profile-simulate-wow/05-RESEARCH.md (Standard Stack "Harvest" table — `scanForExcludedCoaching` is an OPTIONAL no-cost regex backstop, NOT the gate; D-04 keeps the prompt-layer as the lock)
  </read_first>
  <action>
    Harvest (read-only via `git show`) the `EXCLUDE_REGISTRY` list of never-coach names and the pure `scanForExcludedCoaching(text): boolean` regex from the branch's ethics-gate.ts — port ONLY the data + the pure regex function into behavioral-core.ts (no streaming tripwire, no model call). This is the discretionary belt-and-suspenders backstop the profile-runner (05-04) can call AFTER the READ to catch overt weaponization that slipped the prompt guardrail; D-04 keeps the prompt layer as the primary lock. Extend behavioral-core.test.ts asserting `scanForExcludedCoaching` trips on an obvious weaponization request and passes a normal behavioral-read request.
  </action>
  <acceptance_criteria>
    - `behavioral-core.ts exports scanForExcludedCoaching` and `EXCLUDE_REGISTRY` (grep === present).
    - `node ./node_modules/vitest/vitest.mjs run src/lib/engine/__tests__/behavioral-core.test.ts` exits 0; `scanForExcludedCoaching` returns true on a coaching/weaponization string, false on a neutral read.
    - No streaming/model-call symbol ported: `grep -cE "gateStreamBuffer|gateAsyncDeltas|chat\.completions" src/lib/engine/behavioral-core.ts` === 0.
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/engine/__tests__/behavioral-core.test.ts</automated>
  </verify>
  <done>A pure, no-cost coaching backstop is available for the runner; zero branch engine/streaming code was ported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parked branch corpus → in-repo constant | A string is harvested from an unmerged branch into shipped source |
| forensic READ → end user | The behavioral read could be weaponized against a named real person |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering / supply-chain | harvested BEHAVIORAL_CORE corpus | mitigate | Harvested READ-ONLY via `git show` (branch never merged); the executor reads the corpus body before embedding; no branch engine/streaming code is ported (grep gate === 0) |
| T-05-04 | (brand/safety) Repudiation/Elevation | the forensic READ prompt | mitigate | D-04 prompt-layer guardrail embedded in BEHAVIORAL_ETHICS_BLOCK ("read for YOUR decision", refuse manipulate/coerce/stalk/dox, directional caveat) + the discretionary `scanForExcludedCoaching` regex backstop |
| T-05-05 | Information disclosure | over-claim on thin text (false deception read) | mitigate | FORENSIC_FLASH_DIRECTIVE forbids micro-expression/deception reads on the text tier; the deep layer rides Max/video only (D-03); deception likelihood is a band WORD never a number |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs (D-05 zero-new-deps) — Package Legitimacy Gate N/A |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/lib/engine/__tests__/behavioral-core.test.ts` green.
- `npx tsc --noEmit` introduces no new errors on the new file.
- Branch HEAD unchanged (no merge); behavioral-core.ts contains no per-request interpolation.
</verification>

<success_criteria>
A byte-stable, tier-gated behavioral system prompt exists with the D-04 light guardrail baked in, harvested cleanly from the branch corpus without merging or porting engine code. 05-04's profile-runner can import `BEHAVIORAL_SYSTEM_PROMPT_FLASH/_MAX` directly.
</success_criteria>

<output>
Create `.planning/phases/05-profile-simulate-wow/05-02-SUMMARY.md` when done.
</output>
