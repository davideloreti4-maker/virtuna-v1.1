---
phase: 5
slug: profile-simulate-wow
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-28
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for the THREE new user-facing surfaces of "Profile → Simulate Wow".
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Scope (IN):** (1) the hero `profile-read` card, (2) the `reaction-distribution` card, (3) the additive
> "drop a chat / screenshot" composer affordance. Nothing else (front-door picker, mode menu, ambient
> reactor, Predict = P6/P7 — D-09).
>
> **Grounding:** this is NOT a greenfield design. Both cards are **siblings** of the existing thread result
> cards. Source of truth = `src/app/globals.css` (`@theme`) + `docs/DESIGN-SYSTEM.md`. The two cards must read
> as the same family as `account-read-block.tsx`, `multi-audience-read-block.tsx`, `band-block.tsx`,
> `personas-block.tsx`. `docs/subsystems/ui-skill-cards.md` is referenced in the brief but **does not exist in
> this worktree** — its principles (matte · warm-cream · single proof-unit · visible Lens entry · forward-chain
> CTA primary · zero coral · 9:16 media) are honored here via the live analog components instead.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (mature in-house flat-warm charcoal system; no shadcn init) |
| Preset | not applicable |
| Component library | Radix primitives via local `@/components/ui/*` (e.g. `Badge`) — primitives only, not a visual layer |
| Icon library | `@phosphor-icons/react` (e.g. `BookmarkSimple`, `Check`) |
| Font | Inter (`--font-sans`) for ALL chrome + card content; Newsreader (`--font-serif`) for voice-moments ONLY — **not used on these two cards** |

**shadcn gate result:** SKIPPED with cause. `components.json` is absent and the project runs a mature, LOCKED
design system (`globals.css @theme` + `docs/DESIGN-SYSTEM.md`). Introducing shadcn would fork the token system
and violate the LOCKED accent-dosage rule. Registry safety gate: not applicable (no registry in play).

---

## Spacing Scale

The codebase uses a 4-based Tailwind scale. Declared values for this phase (multiples of 4):

| Token | Value | Usage in these surfaces |
|-------|-------|-------------------------|
| xs | 4px | icon↔label gaps (`gap-1`/`gap-1.5`), micro-label left nudge (`ml-0.5`) |
| sm | 8px | chip padding, verdict-badge padding (`px-2 py-0.5`), label↔value gaps (`gap-2`) |
| md | 12px | inter-block stack gap (`gap-3`), card footer padding (`py-3`), persona-row padding |
| lg | 16px | card horizontal padding (`px-4`), evidence-row gaps (`gap-4`) |
| xl | 20px | section card body padding (`p-5`), inter-section stack gap (`gap-5`) |
| 2xl | 24px | hero header padding on the Max-tier expanded card |

Exceptions:
- **`11px` micro-label bottom margin** (`mb-[11px]`) — inherited verbatim from `ReadingSection`; do NOT "round" it, it is a shared-component constant.
- **`6px` format/meter bar height** — only if a proportion meter is reused; not a spacing token.

---

## Typography

Inter throughout. Declared roles for these surfaces:

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Display | 24px (`text-2xl`) | semibold (600) | 1.0 (`leading-none`) | the band WORD on the panel-variant reaction card (mirrors `band-block`) |
| Heading | 15px (`text-[15px]`) | semibold (600) | 1.2 (tight) | card lead line — subject name, verdict line, section heads |
| Body | 14px (`text-sm`) | regular (400) | 1.5 (`leading-relaxed`) | reasoning, how-they-react, list items, evidence prose |
| Label | 10px (`text-[10px]`) | medium (500) | 1.2 | quiet UPPERCASE section labels, `tracking-[0.14em]`, `text-foreground-muted` |

Secondary / inherited sizes (do not introduce new ones beyond these): `13px` (`text-[13px]`) inside dense
read panels, `12px` (`text-xs`) for provenance/meta + the honesty caveat, `16px` quotes only if a quote needs
emphasis (default quotes are 14px italic).

**Weights — exactly two are load-bearing for this phase:** regular (400) for body, semibold (600) for emphasis.
`medium` (500) is the inherited interactive-chrome weight (buttons, labels, badges) carried from the design
system — it is not a new declaration, reuse as-is.

**Emphasis without color (LOCKED rule):** hierarchy comes from weight, size, italic (quotes), and spacing —
never from paint. No accent-colored headings, no accent CTA text.

---

## Color

60 / 30 / 10 split, mapped to the flat-warm charcoal tokens:

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#262624` (`--color-background` / charcoal-app) | thread canvas behind the cards |
| Secondary (30%) | `#1e1d1b` (`--color-surface` / `--color-surface-thread`) + `white/[0.02]–[0.04]` lifts | the card surfaces, persona-drill panels, footer rows |
| Accent (10%) | `#d97757` (`--color-accent`, terracotta) | **see reserved-for — effectively ZERO on these two cards** |
| Destructive | `var(--color-error)` (`oklch(0.60 0.20 25)`, distinct from terracotta hue ~33) | error states, refusal banner, "scrolls/resistant" Weak verdict text |

**Accent reserved for (explicit):**
- **Neither card uses the terracotta accent.** Per the LOCKED near-zero-dosage rule (`DESIGN-SYSTEM.md` §"Accent
  dosage"), a screen with zero accent is the norm. Primary CTA = neutral cream text. Save check = cream. Bands =
  semantic data tokens (below), which are **not** the accent.
- The single sanctioned exception if ever needed: ONE liveness/presence dot (e.g. a "now in your library" pulse).
  Default = omit it. Do not reach for accent to make the wow "pop" — that instinct is the thing being removed.

**Band / data colors (sanctioned, NOT accent)** — reuse the EXACT `BAND_COLOR` map from `band-block.tsx`, never re-roll, never coral:
- Strong → `var(--color-success)` · Mixed → `var(--color-warning)` · Weak → `var(--color-error)`
- Deception likelihood (forensic, Max only): Low → `--color-success` · Medium → `--color-warning` · High → `--color-error`. **Band WORD only, never a number.**

**Borders / depth:** rest `white/[0.06]` (`--color-border`), hover `white/[0.10]`. Matte only — no glass gradient,
no glow, no inset white-shine. Cards are flat (solid charcoal + hairline border). `--shadow-float` is for floating
surfaces only (the composer / its drop overlay), not the result cards. Radius: cards 12px (or 18px when reusing
`ReadingSection`'s `READING_CARD` shell), inputs/buttons 8px, badges/pills full-round.

**⚠️ Divergence from the closest analog (recorded, intentional):** `multi-audience-read-block.tsx` paints its
`.read` panel with a LEGACY coral `rgba(255,127,80,0.30)` border (`READ_PANEL_STYLE`). That predates the LOCKED
dosage rule and is stale. **Do NOT copy it.** The `profile-read` "how they react" / Read panel uses a NEUTRAL
treatment (6% hairline + `white/[0.04]` lift + semibold lead) — zero accent. (Flag F-03.)

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (`profile-read`) | `Simulate a message to {name} →` (forward-chain handoff; `{name}` = `subjectName`; falls back to `Simulate a message to them →` when unnamed) |
| Secondary action (`profile-read`) | `Save` → `Saved` (cream `SaveAffordance`, `item_type="read"`) |
| Saved-SIM provenance line (`profile-read`) | `Saved to your library as {name}` (the bake is now a General SIM; PROF-03) |
| Honesty caveat (`profile-read`, D-04) | `This is a behavioral read to inform your decision — directional, drawn from limited evidence.` (quiet 12px `text-foreground-muted`, always present) |
| Tier / provenance tag (both cards) | `SIM-1 Flash` or `SIM-1 Max` (12px muted) + `TrustBadge tier="Directional"` |
| Primary action (`reaction-distribution`) | `Save` → `Saved` (`item_type="read"`); optional secondary text-CTA `Try another message →` |
| Person-variant verdict line | `{name} is likely to be {verdict}` (verdict = e.g. *receptive / on the fence / resistant*) — single read, NO fraction |
| Panel-variant verdict line | `{Band} pull · {fraction}` (e.g. `Mixed pull · 6/10 react`) — fraction ONLY in panel mode |
| Empty / thin-evidence (`profile-read`) | heading `Not enough to read them yet` (warning tone, never error/coral) · body `We couldn't find enough in what you shared to read this person honestly. Add more of the conversation, or a clearer screenshot, and try again.` |
| Error state (profile run failed) | `That read didn't come through. Try again, or share a bit more of the conversation.` |
| Error state (simulate run failed) | `Couldn't run that one. Try again in a moment.` |
| Weaponization refusal (D-04) | heading `I can't help build that` · body `I can explain why someone behaves the way they do — to inform your own decision — but I won't hand over a playbook to manipulate, coerce, stalk, or expose anyone.` |
| Destructive confirmation | none — this phase ships no destructive action (no delete; renaming a saved SIM is non-destructive inline edit) |
| Composer affordance — control label | `Attach a chat or screenshot` (icon-button `aria-label`; visible hint on hover/focus) |
| Composer affordance — drop hint | `Drop a chat export, screenshot, or short clip` (drag-over overlay) |
| Composer affordance — unsupported file | `That file type isn't supported yet — use a .txt/.md export, an image, or a short video.` (`.docx`/`.pdf` are out per D-09) |

Voice: plain, second-person, calm. No hype, no "AI magic", no exclamation. The wow is the *substance* of the
read, never the chrome shouting about it.

---

## Surface Contracts

> These are the load-bearing per-surface specs the planner/executor build from. Visual layout sits ON TOP of the
> RESEARCH.md Zod field lists — schemas are NOT redefined here.

### Surface 1 — `profile-read` card (hero, PROF-02)

Closest analog: `account-read-block.tsx` (a per-entity Read composed of `ReadingSection` units). The card is a
**vertical stack of `ReadingSection` units** (`gap-5`), STATIC (no model-generated UI), savable, with a footer
forward-chain CTA.

Section order (top → bottom):
1. **Hero / identity header** — `ReadingSection` label `A read on this person` (or `…this group` when `subjectKind==="panel"`).
   Body: `subjectName` at Heading (15/600); a quiet `subjectKind` chip (`person` / `panel`) using `--color-surface-elevated`;
   the `SIM-1 Flash|Max` tag + `TrustBadge "Directional"` right-aligned. Traits as cream chips, `commStyle` one line,
   `drivers` as a short list. Identity is factual, neutral — no accent.
2. **Behavioral tells (evidence-quoted)** — label `What gives them away`. Each `tells[]` entry = a tell line (14/regular,
   `text-foreground-secondary`) with its verbatim `evidence` quote directly beneath, italic 14 muted (`"…"`). The quote is
   the PROVENANCE / trust mechanism (TRUST-02) — it must be visually bound to its claim, never floated separately.
3. **How they'll react (goal-scoped)** — label `How they'll react`. `howTheyReact` prose; a quiet `goalScope` line above
   it framing the target (`On your goal: {goalScope}`). NEUTRAL panel (6% border + `white/[0.04]`), NOT the legacy coral panel.
4. **Deeper forensic read (Max/video ONLY)** — label `Deeper read (from the video)`. Rendered **only when `forensic` is
   present** (null/absent on Flash → section omitted entirely; never a teaser, never "upgrade to see"). `deceptionLikelihood`
   as a band WORD (Low/Medium/High) in the `BAND_COLOR` token, never a number. `cues[]` as rows: `{timestamp}` (tabular,
   muted) · `{observation}` → `{inference}`. Recommend this section start **collapsed** behind a disclosure to keep the
   hero calm and avoid over-claim (Flag F-01).
5. **Honesty caveat** — the D-04 caveat line, quiet 12px muted, always visible (not behind a tooltip).
6. **Footer action row** — top hairline (`border-t white/[0.06]`, `px-4 py-3`, `gap-4`): `SaveAffordance` (cream) on the
   left, the primary `Simulate a message to {name} →` forward-chain CTA as neutral cream-secondary text (mirror the
   `idea-card` "Develop this →" button exactly — text button, not a filled coral button), carrying `savedAudienceId`.

### Surface 2 — `reaction-distribution` card (SIMU-02), two variants on `subjectKind`

Closest analog: `multi-audience-read-block.tsx` (per-audience read) minus the 2-audience delta; `band-block.tsx` for the
band glyph/word; `personas-block.tsx` for the collapsible drill. **Branch the WHOLE layout on `subjectKind`** — never
show a fraction or a crowd for a person.

**Person variant (`subjectKind==="person"`, `read` present, `band`/`fraction` suppressed):**
- A single read. Verdict line: `{audienceName} is likely to be {read.verdict}` (Heading 15). The verdict word may borrow
  the success/warning/error band tone (receptive→success, on-the-fence→warning, resistant→error) — data color only.
- `read.reasoning` prose (14/1.5). ONE representative `read.quote` italic 14 muted. **No `N/10`, no panel grid, no glyph
  crowd** (Pitfall 2 — "distribution of one" is forbidden).

**Panel variant (`subjectKind==="panel"`, `band`+`fraction`+`themes`+`reactions` present):**
- Verdict header: `{band}` WORD (Display 24 or Heading 15, `BAND_COLOR`) + `{fraction}` (e.g. `6/10 react`, muted) — reuse
  `band-block` styling exactly; fraction comes from `aggregateFlash`, never re-rolled.
- **Clustered themes** — `themes[]` as labeled rows: `{label}` (semibold 14) + a representative `{quote}` italic muted.
  This is the panel's proof unit.
- **Per-persona drill** — collapsible (reuse the `personas-block` / `multi-audience-read` "Audience reactions — {stop}/{total}"
  toggle pattern): each `reactions[]` row = archetype (capitalized) + stop/scroll verdict badge + first-person quote.
- Footer: provenance (`SIM-1 Flash|Max` + `TrustBadge "Directional"`) + `SaveAffordance`.

Both variants: bands/words only, ZERO 0-100 number (the `.strict()` schema enforces it; the UI must not invent a meter
or gauge either), zero accent.

### Surface 3 — additive composer affordance (D-07, PROJECT.md locked)

Closest reference: `src/components/app/home/composer.tsx` + `composer-controls.tsx`. **ADDITIVE ONLY — wrap, never
restructure.** The creator (Socials) composer stays byte-identical.

- An **attach / drop control**: a single icon-button (Phosphor `Paperclip` or `Plus`, cream-muted chrome) mounted
  alongside existing composer controls — NOT a new row, NOT a redesign. `aria-label="Attach a chat or screenshot"`.
- A **drag-and-drop overlay** over the existing composer surface that appears only on drag-over: matte `--color-surface`
  panel + `--shadow-float` + 6% dashed-ish hairline, copy `Drop a chat export, screenshot, or short clip`. Dismisses on
  drop/leave.
- Accepts `.txt`/`.md`, image, short video ONLY (D-09). Unsupported type → inline muted error (copy above); never a
  blocking modal.
- Selected-attachment chip: a compact removable chip (filename + `×`) in the composer, `--color-surface-elevated`,
  cream text, neutral. No accent.
- Touch target ≥ 44px for the icon-button (accessibility exception to the spacing scale).

---

## Interaction Contracts

| Interaction | Contract |
|-------------|----------|
| Forward-chain CTA | `Simulate a message to {name} →` reuses the existing `chain-handoff` handoff; seeds the just-saved `savedAudienceId`; same thread (SIMU-03). Disabled/`Simulating…` state during the run, mirror `idea-card` `Develop this →`. |
| Save | `SaveAffordance` flips `Save → Saved` with a CREAM (never coral) check; `snapshot = block.props` so the shelf re-renders the same typed card without re-fetch. |
| Forensic disclosure (Max) | collapsed by default; `Deeper read (from the video) ↓ / ↑` toggle; absent entirely on Flash tier (no teaser). |
| Persona drill (panel) | collapsible, default collapsed, `↓ Show / ↑ Hide`, summary `{stop}/{total} react` — reuse the existing toggle idiom. |
| Composer drop | drag-over reveals overlay; drop/leave dismisses; unsupported type → inline muted error, no modal. |
| Loading | both cards arrive as completed thread blocks (non-streamed `{ block }` route). Use the existing thread loading/skeleton chrome; no new spinner language. |
| Empty / error / refusal | render in-card (thin fallback, error line, refusal banner) — never a toast, never a coral alert; warning tone for thin, `--color-error` for hard errors/refusal. |
| Reduced motion | no essential motion; the drop overlay + disclosures respect `prefers-reduced-motion` (no slide, instant toggle). |

---

## Accessibility

- All bands/verdicts carry a text WORD, never color-only (Strong/Mixed/Weak, Low/Medium/High) — colorblind-safe by construction.
- Evidence quotes use real `"…"` and are programmatically associated with their tell (same list item).
- Forensic `cues[]` timestamps are `tabular-nums`, read as `at {timestamp}, {observation}, suggesting {inference}` for SR.
- Interactive controls: visible focus ring (`focus-visible:ring-1 ring-white/10`), ≥44px touch target on the attach button.
- Card sections use `aria-label` summarizing band + fraction + tier (mirror `band-block` `aria-label`).

---

## Non-Blocking Flags (grounded recommendations)

- **F-01 — Forensic layer honesty (deception likelihood).** Render as a band WORD (Low/Medium/High) in the
  success/warning/error tokens, NEVER a percentage or number (avoids false precision; matches bands-only honesty).
  Per-cue rows: `{timestamp} · {observation} → {inference}`. **Recommend collapsed-by-default** behind a disclosure so the
  hero stays calm and the heaviest claims require a deliberate open. Max-tier only; omit the section entirely on Flash.
- **F-02 — Person vs panel switch.** Branch the entire layout on `subjectKind`. Person = single read (verdict word +
  reasoning + one quote, NO fraction/crowd). Panel = band word + fraction + clustered themes + collapsible drill. This is
  the D-02 honesty line; do not soft-merge the two into one template.
- **F-03 — Reject the legacy coral Read panel.** The closest analog (`multi-audience-read-block`) uses a stale coral
  border that violates the LOCKED near-zero-dosage rule. The new `profile-read` "how they react" panel must use NEUTRAL
  emphasis (6% hairline + `white/[0.04]` + semibold lead). Keep `reskin-matte.test.ts` green; do not introduce any
  `rgba(255,127,80,*)` / `#FF7F50` / coral literals.
- **F-04 — Caveat is always visible.** The D-04 honesty caveat is product copy, not a tooltip. Render it inline, quiet,
  on every `profile-read` (and as a one-line directional note on the simulate card). It is part of the trust contract.
- **F-05 — Composer affordance stays minimal.** One icon-button + a drag overlay + a removable chip. No front-door
  redesign, no audience picker, no mode menu (all P7). The creator composer path must remain byte-identical.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| (none) | none — all UI is in-repo React + local primitives | not applicable |

No shadcn, no third-party registry, no new dependency (D-05 zero-new-deps posture). Registry vetting gate: not applicable.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
