---
phase: 05-profile-simulate-wow
plan: 06
type: execute
wave: 3
depends_on: ["05-04", "05-05"]
files_modified:
  - src/components/app/home/composer.tsx
autonomous: false
requirements: [PROF-01, SIMU-03]
must_haves:
  truths:
    - "A minimal, ADDITIVE 'drop a chat / screenshot' affordance (attach icon-button + drag overlay + removable chip) is added to the existing composer and posts to /api/tools/profile (D-07 / IN-01..03 inbox wiring deferred from P4 D-01)"
    - "The creator (Socials) composer path stays BYTE-IDENTICAL — additive only, no restructure of the tool selector / submit flow (PROJECT.md + vision §15.2 locked)"
    - "The affordance accepts .txt/.md, image, and short video ONLY — .docx/.pdf rejected with an inline muted error (D-09)"
    - "The full Profile-a-chat → Simulate-a-reply flow completes end-to-end in ONE thread (SIMU-03), human-verified in a real browser pass"
    - "P5 ships NO front-door Audience picker, NO Mode-scoped skill menu, NO generalized ambient reactor (all P7 — D-09 tight cut line)"
  artifacts:
    - path: "src/components/app/home/composer.tsx"
      provides: "additive attach/drop affordance wired to /api/tools/profile; creator path untouched"
      contains: "tools/profile"
  key_links:
    - from: "src/components/app/home/composer.tsx"
      to: "/api/tools/profile"
      via: "the attach/drop control posts the evidence stimulus"
      pattern: "tools/profile"
    - from: "profile-read card CTA"
      to: "/api/tools/simulate"
      via: "the chain-handoff CTA (05-01) seeds savedAudienceId — same thread"
      pattern: "Simulate a message"
---

<objective>
Wire the visible inbox affordance that turns the two new verbs into a usable one-thread flow: a minimal additive "drop a chat / screenshot" control on the existing composer that posts to `/api/tools/profile`, then human-verify the full Profile→Simulate wow end-to-end in a real browser.

Purpose: P4 (D-01) deferred the visible inbox affordance to its consumer = P5. D-07 locks it minimal + additive; the creator composer stays byte-identical (PROJECT.md + vision §15.2). The forward-chain CTA on the profile-read card (already wired in 05-01) carries `savedAudienceId` to `/api/tools/simulate` in the SAME thread, completing SIMU-03. Because vitest-in-node cannot catch Next client/server bundle leaks (the GSI P3 BUILD-01 precedent), the end-to-end flow needs a full `npm run build` at the wave gate — but the per-task loop stays tight on `npx tsc --noEmit`.
Output: the additive composer affordance + a human-verified one-thread Profile→Simulate flow.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-profile-simulate-wow/05-CONTEXT.md
@.planning/phases/05-profile-simulate-wow/05-RESEARCH.md
@.planning/phases/05-profile-simulate-wow/05-PATTERNS.md
@.planning/phases/05-profile-simulate-wow/05-UI-SPEC.md
</context>

## Artifacts this phase produces (source-grounding exclusion set)

This plan edits `composer.tsx` (additive only — no new exported symbol of note). It consumes the routes from 05-04 (`/api/tools/profile`) and 05-05 (`/api/tools/simulate`) and the chain CTA from 05-01. See 05-01-PLAN.md for the full phase-wide artifact set.

<tasks>

<task type="auto">
  <name>Task 1: Add the minimal additive "drop a chat / screenshot" composer affordance</name>
  <files>src/components/app/home/composer.tsx</files>
  <read_first>
    - src/components/app/home/composer.tsx (the existing `VideoUpload` import + `file` state path + the `handleTestHook`/`handleWriteScript` handoff handlers — the precedent for wiring a NEW path WITHOUT touching the creator path)
    - .planning/phases/05-profile-simulate-wow/05-UI-SPEC.md (Surface 3 — attach icon-button + drag overlay + removable chip + the exact copy + the ≥44px touch target; F-05 stays-minimal)
    - .planning/phases/05-profile-simulate-wow/05-PATTERNS.md (composer.tsx section — the HARD additive-only constraint)
  </read_first>
  <action>
    Add an additive attach/drop affordance to the existing composer (wrap, never restructure — PROJECT.md locked): a single icon-button (Phosphor `Paperclip`/`Plus`, cream-muted chrome, `aria-label="Attach a chat or screenshot"`, ≥44px touch target) mounted ALONGSIDE existing controls; a drag-over overlay (matte `--color-surface` + `--shadow-float`, copy `Drop a chat export, screenshot, or short clip`, dismiss on drop/leave); a removable selected-attachment chip (filename + ×, `--color-surface-elevated`, cream). Accept `.txt`/`.md`, image, short video ONLY; an unsupported type (`.docx`/`.pdf` — D-09) shows the inline muted error from the UI-SPEC, never a blocking modal. On submit, build the matching `StimulusInput` (text/file_text/image/video) and POST to `/api/tools/profile`; the returned `profile-read` block renders in the thread (registered in 05-01). The creator (Socials) tool-selector / submit flow is BYTE-IDENTICAL — do not alter any existing handler; the new path is a sibling. NO front-door Audience picker, NO Mode-scoped skill menu, NO ambient reactor (P7 — D-09). Zero coral literals (keep `reskin-matte.test.ts` green).
  </action>
  <acceptance_criteria>
    - `grep -c "tools/profile" src/components/app/home/composer.tsx` ≥ 1 (the affordance posts to the profile route).
    - `npx tsc --noEmit` introduces NO new errors on composer.tsx (the fast per-task loop gate).
    - reskin-matte guard green: `grep -rcE "rgba\(255,127,80|#FF7F50" src/components/app/home/composer.tsx` === 0.
    - WAVE GATE (not the per-task loop): `npm run build` exits 0 — catches Next client/server bundle leaks vitest-in-node misses (GSI P3 BUILD-01 precedent). Run once at the wave gate, not on every per-task iteration.
    - The existing composer tests (if any) stay green; no creator-path handler signature changed (diff is additive — reviewer/grep confirms no edit inside the existing tool-selector/submit handlers).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "app/home/composer" | grep -c error</automated>
  </verify>
  <done>A minimal additive evidence-drop affordance posts to /api/tools/profile; the creator composer path is byte-identical; tsc is clean and the wave-gate build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the end-to-end one-thread Profile → Simulate wow (SIMU-03)</name>
  <what-built>
    The full PMF on-ramp: drop a chat → a forensic profile-read card (with evidence quotes + Directional badge) → "Simulate a message to [them]" CTA → draft a reply → a reaction-distribution card — all in ONE continuous thread. Both new routes (/api/tools/profile, /api/tools/simulate), both new cards, and the additive composer affordance. The full `npm run build` (wave-gate bundle-leak check) has passed before this checkpoint.
  </what-built>
  <how-to-verify>
    1. Run the dev server; authenticate (use `e2e/create-test-user.ts` per the GSI auth precedent).
    2. In the composer, use the new attach control to drop a chat `.txt` export (or paste chat text). Confirm a `profile-read` card renders with: subject name, behavioral tells each bound to a verbatim evidence quote, "how they'll react", the always-visible honesty caveat, the `SIM-1 Flash`/`SIM-1 Max` tag + `Directional` badge. (For a person-video, confirm the forensic section appears ONLY then; for a chat it is ABSENT — no teaser.)
    3. Confirm the card shows "Saved to your library as {name}" and the saved SIM appears in the General library.
    4. Click "Simulate a message to {name} →"; draft a reply; confirm a `reaction-distribution` card renders IN THE SAME THREAD — person variant = single read (NO N/10 fraction), panel variant = band word + fraction + themes.
    5. Confirm: zero terracotta-accent paint on either card, zero 0-100 number anywhere, no surface-hopping (one thread throughout).
    6. Confirm the creator (Socials) composer path is unchanged (run a normal Socials test — byte-identical behavior).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issues found (which step, what rendered vs expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client upload (composer) → server route | The drop control stages a file/text; the real trust boundary is the server route (05-04/05-05), which re-validates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18 | Tampering | composer file/type acceptance | mitigate | Client accepts `.txt`/`.md`/image/short-video only with an inline reject; the AUTHORITATIVE validation (MIME/size/cap/storagePath) is server-side in the 05-04/05-05 routes — the client control is a convenience, never the gate |
| T-05-19 | (regression) Tampering | creator (Socials) composer path | mitigate | Additive-only edit; no existing handler signature changed; `next build` + reskin-matte guard + a Socials smoke confirm byte-identical creator behavior |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs (D-05 zero-new-deps) — Package Legitimacy Gate N/A |
</threat_model>

<verification>
- `npm run build` succeeds at the WAVE gate (catches Next client/server bundle leaks vitest-in-node misses — GSI P3 BUILD-01 precedent).
- `npx tsc --noEmit` clean on composer.tsx (the fast per-task loop gate); reskin-matte guard green.
- Human-verify checkpoint approved (the one-thread Profile→Simulate flow + creator path byte-identical).
</verification>

<success_criteria>
The PMF on-ramp works end-to-end in one thread (SIMU-03) with a minimal additive composer affordance, the creator composer byte-identical, the tight P5 cut line held (no P7 surfaces), and a human has confirmed the wow in a real browser.
</success_criteria>

<output>
Create `.planning/phases/05-profile-simulate-wow/05-06-SUMMARY.md` when done.
</output>
</content>
