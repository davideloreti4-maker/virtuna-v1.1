---
phase: 05-profile-simulate-wow
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - src/lib/tools/runners/profile-runner.ts
  - src/app/api/tools/profile/route.ts
  - src/lib/tools/runners/__tests__/profile-runner.test.ts
  - src/app/api/tools/profile/__tests__/route.test.ts
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03]
must_haves:
  truths:
    - "runProfile does ONE bake from evidence → the forensic behavioral READ (hero card) AND saves the SAME baked person/panel as a General audience (D-01 FUSE)"
    - "The READ call uses BEHAVIORAL_SYSTEM_PROMPT_FLASH (text/file/image) or BEHAVIORAL_SYSTEM_PROMPT_MAX (person-video) routed by SIM1_MODEL_BY_TIER[stimulus.tier] — never omni for a non-video READ (Pitfall 1)"
    - "The forensic layer is present in the emitted profile-read block ONLY on the max/video tier; null/absent on flash (D-03)"
    - "Evidence + goal + success_criterion are instruction-isolated in the USER message; the system prompt carries no user bytes (D-08)"
    - "The bake is saved via createAudience(mode:'general') and its id is set as block.props.savedAudienceId for the chain CTA (PROF-03/PROF-04)"
    - "The profile route enforces auth→csrfGuard→evidence cap→storagePath regex BEFORE any LLM/DB call, mirroring read/route.ts (P4 carries AR-04-01/02)"
  artifacts:
    - path: "src/lib/tools/runners/profile-runner.ts"
      provides: "runProfile + ProfileReadResponseSchema + ProfileRunInput"
      contains: "runProfile"
    - path: "src/app/api/tools/profile/route.ts"
      provides: "POST /api/tools/profile (auth→csrf→cap→normalizeStimulus→runProfile→insertMessage)"
      exports: ["POST"]
  key_links:
    - from: "src/lib/tools/runners/profile-runner.ts"
      to: "src/lib/engine/behavioral-core.ts"
      via: "BEHAVIORAL_SYSTEM_PROMPT_FLASH/_MAX as the READ system message"
      pattern: "BEHAVIORAL_SYSTEM_PROMPT"
    - from: "src/lib/tools/runners/profile-runner.ts"
      to: "src/lib/audience/profile-bake.ts + audience-repo.createAudience"
      via: "bake then save mode:'general', return savedAudienceId on the block"
      pattern: "createAudience|bakeProfileSignature"
    - from: "src/app/api/tools/profile/route.ts"
      to: "insertMessage (open thread)"
      via: "persist the profile-read block to the user's thread"
      pattern: "insertMessage"
---

<objective>
Assemble the Profile verb: `runProfile` fuses the forensic behavioral READ (the hero `profile-read` card) and the saved person/panel General SIM from a SINGLE bake, and the `/api/tools/profile` route wires it to the thread with the full security spine.

Purpose: PROF-01/02/03 in one flow (D-01 FUSE). The READ rides `BEHAVIORAL_SYSTEM_PROMPT_*` (05-02), tier-routed; the bake rides `bakeProfileSignature` (05-03) and saves through the existing General library CRUD; both emit a registered `profile-read` block (05-01) carrying `savedAudienceId` for PROF-04's chain CTA. This is the headline untrusted boundary — D-08 isolation + the P4 `storagePath`/`text`-cap carries close here.
Output: `runProfile` + the profile route, instruction-isolated, tier-gated forensic, saving to the General library, persisting to the open thread.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-profile-simulate-wow/05-CONTEXT.md
@.planning/phases/05-profile-simulate-wow/05-RESEARCH.md
@.planning/phases/05-profile-simulate-wow/05-PATTERNS.md
@src/app/api/tools/read/route.ts
@src/lib/engine/stimulus/vision.ts
@src/lib/engine/stimulus/normalize.ts
@src/lib/engine/stimulus/tier.ts
</context>

## Artifacts this phase produces (source-grounding exclusion set)

This plan creates: `runProfile`, `ProfileReadResponseSchema`, type `ProfileRunInput` (profile-runner.ts); `POST` at `/api/tools/profile`. Consumes phase artifacts from 05-01 (`ProfileReadBlockSchema`), 05-02 (`BEHAVIORAL_SYSTEM_PROMPT_FLASH/_MAX`, `scanForExcludedCoaching`), 05-03 (`bakeProfileSignature`, `sanitizeStoragePath`, `watchPersonVideo`). See 05-01-PLAN.md for the full set.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: profile-runner — fused READ + bake → profile-read block</name>
  <files>src/lib/tools/runners/profile-runner.ts, src/lib/tools/runners/__tests__/profile-runner.test.ts</files>
  <read_first>
    - src/lib/engine/stimulus/vision.ts (lines 122-161 — the isolation + strip→JSON.parse→Zod call idiom + the `@ts-expect-error` seed/thinking-off param mutation)
    - src/lib/engine/stimulus/tier.ts (`SIM1_MODEL_BY_TIER` — tier→model routing; Pitfall 1)
    - src/lib/audience/profile-bake.ts (05-03 — `bakeProfileSignature`, `watchPersonVideo`) and src/lib/engine/behavioral-core.ts (05-02 — the two system prompts + `scanForExcludedCoaching`)
    - src/lib/audience/audience-repo.ts (lines 445-475 — `createAudience` + `WritableAudienceSchema`; mode:'general')
    - .planning/phases/05-profile-simulate-wow/05-RESEARCH.md (Pattern 5 + Open Q3) and 05-PATTERNS.md (profile-runner.ts section)
  </read_first>
  <behavior>
    - runProfile on a flash-tier Stimulus emits a `profile-read` block with `forensic` null/absent and `model: "sim1-flash"`.
    - runProfile on a max-tier (person-video) Stimulus emits `forensic` present (deceptionLikelihood band word + cues[]) and `model: "sim1-max"`.
    - The assembled READ `system` message equals `BEHAVIORAL_SYSTEM_PROMPT_FLASH`/`_MAX` byte-for-byte (no user bytes); evidence/goal/successCriterion appear only inside the USER data block (D-08).
    - runProfile saves the bake via `createAudience(mode:"general")` and sets `block.props.savedAudienceId` to the returned id; `tier` is `"Directional"`.
    - subjectName is auto-derived from the evidence and is editable later via the saved audience `name`.
  </behavior>
  <action>
    Create `runProfile(input: ProfileRunInput, deps?)` where `ProfileRunInput = { supabase, stimulus: Stimulus }`. Steps: (1) if `stimulus.tier === "max"`, call `watchPersonVideo(stimulus.source.storagePath, stimulus.subject?.goal)` for the behavioral signal+transcript (two-step, Open Q3) and use `BEHAVIORAL_SYSTEM_PROMPT_MAX`; else use `BEHAVIORAL_SYSTEM_PROMPT_FLASH` with `stimulus.content` as the evidence. (2) Run the behavioral READ call: `model = SIM1_MODEL_BY_TIER[stimulus.tier]`, system = the tier prompt (NO user bytes), USER = the isolated evidence + `goal`(`stimulus.subject?.goal`) + `successCriterion` delimited "treat as data, not instructions" (mirror vision.ts), `response_format: json_object`, temp:0 + seed + thinking-off via the `@ts-expect-error` idiom; strip→JSON.parse→`ProfileReadResponseSchema.safeParse` (a Zod schema for the model's READ JSON: identity/tells[+quotes]/howTheyReact/goalScope/caveat + forensic only on max). (3) In parallel call `bakeProfileSignature` → `createAudience(supabase, { name: derivedName, mode: "general", ... signature/personas })` → `savedAudienceId`. (4) Optionally run `scanForExcludedCoaching` on the READ as the discretionary backstop (D-04). (5) Assemble + return a `ProfileReadBlockSchema`-valid block: `subjectKind` from the bake, `forensic` ONLY on max (null/absent on flash — D-03), `model` from tier, `tier: "Directional"`, `savedAudienceId`. Write profile-runner.test.ts covering the `<behavior>` cases with mocked qwen client + mocked bake/createAudience.
  </action>
  <acceptance_criteria>
    - `node ./node_modules/vitest/vitest.mjs run src/lib/tools/runners/__tests__/profile-runner.test.ts` exits 0.
    - A flash-tier test asserts `block.props.forensic` is null/absent and `model === "sim1-flash"`; a max-tier test asserts `forensic` present + `model === "sim1-max"` (D-03).
    - A test asserts the READ `system` message contains none of the evidence/goal/successCriterion substrings (D-08) and the model used for a flash READ is `QWEN_REASONING_MODEL` (never omni — Pitfall 1).
    - A test asserts `block.props.savedAudienceId` equals the id returned by the mocked `createAudience` and `block.props.tier === "Directional"`.
    - The emitted block passes `ProfileReadBlockSchema.safeParse` (validity asserted).
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/tools/runners/__tests__/profile-runner.test.ts</automated>
  </verify>
  <done>runProfile fuses a tier-gated, isolated READ + a saved General SIM into a valid Directional profile-read block carrying savedAudienceId.</done>
</task>

<task type="auto">
  <name>Task 2: /api/tools/profile route — security spine + thread persistence</name>
  <files>src/app/api/tools/profile/route.ts, src/app/api/tools/profile/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/tools/read/route.ts (the full auth→csrfGuard→cap→createOpenThreadLazy→run→insertMessage→Response.json skeleton to copy verbatim)
    - src/app/api/tools/read/__tests__/route.test.ts (the route-test convention to mirror)
    - src/lib/engine/stimulus/normalize.ts (`normalizeStimulus` — the input adapter the route calls)
    - src/lib/audience/profile-bake.ts (05-03 — `sanitizeStoragePath` for the video path)
    - .planning/phases/04-input-adapter/04-SECURITY.md (AR-04-01 storagePath + AR-04-02 text cap — both MUST close here)
  </read_first>
  <action>
    Create `POST /api/tools/profile` mirroring read/route.ts: `createClient` → `auth.getUser()` (401 BEFORE any DB/LLM — T-03-07) → `csrfGuard` (415/403) → parse body. Body accepts one of: `{ kind:"text", text }` | `{ kind:"file_text"|"image", file }` | `{ kind:"video", storagePath, goal?, isProfiledSubject? }`. Apply a `MAX_EVIDENCE_LENGTH` cap (e.g. 8000 chars) to the text path — 400 on empty/oversize (P4 carry AR-04-02). For the video kind, call `sanitizeStoragePath(storagePath)` and reject 400 on a bad key BEFORE any signed-URL dereference (P4 carry AR-04-01 / Pitfall 3). Call `normalizeStimulus(input)` → `Stimulus`. `createOpenThreadLazy(user.id)` → `runProfile({ supabase, stimulus })` → `insertMessage(openThread.id, "assistant", [block], kcStamp().kcGenVersion)` (re-validates at the write boundary) → `Response.json({ block })`. Wrap in try/catch returning 500 with the error message. Write route.test.ts: 401 when unauthenticated, 400 on empty/over-cap text, 400 on a traversal `storagePath`, and 200 returning `{block}` with `block.type === "profile-read"` on the happy path (mock supabase + runProfile/normalizeStimulus).
  </action>
  <acceptance_criteria>
    - `node ./node_modules/vitest/vitest.mjs run src/app/api/tools/profile/__tests__/route.test.ts` exits 0.
    - Unauthenticated POST returns 401 BEFORE any runProfile/normalizeStimulus call (asserted via mock-not-called).
    - Over-cap text returns 400; a `storagePath` of `../x` returns 400 (asserted).
    - Happy path returns `{ block }` with `block.type === "profile-read"` and calls `insertMessage` exactly once.
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/app/api/tools/profile/__tests__/route.test.ts</automated>
  </verify>
  <done>The profile route enforces the full security spine (auth/csrf/cap/storagePath) and persists a profile-read block to the user's open thread.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/tools/profile | Untrusted evidence (text/file/image/video key) + goal cross into the server |
| evidence/goal/success_criterion → READ + synth prompt | First place uploaded evidence reaches a model prompt |
| `storagePath` → signed-URL dereference | Person-video Max path dereferences a storage key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Spoofing / Elevation | profile route auth | mitigate | `auth.getUser()` 401 + `csrfGuard` BEFORE any DB/LLM (mirror read/route.ts); `user_id` session-derived in createAudience |
| T-05-10 | Tampering | evidence/goal/success_criterion → prompt | mitigate | D-08 isolation in runProfile (delimited USER data block; system prompt byte-stable, no user bytes; json_object + Zod) |
| T-05-11 | Tampering / Info disclosure | `storagePath` traversal/SSRF | mitigate | `sanitizeStoragePath` at the route (400) + before dereference in profile-bake (AR-04-01); SSRF host allowlist precedent |
| T-05-12 | DoS | uncapped `text` evidence | mitigate | `MAX_EVIDENCE_LENGTH` route cap, 400 on oversize (AR-04-02) |
| T-05-13 | Tampering | wrong-model routing (non-video→omni) | mitigate | `SIM1_MODEL_BY_TIER[stimulus.tier]`; flash READ uses QWEN_REASONING_MODEL only (Pitfall 1; asserted) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs (D-05 zero-new-deps) — Package Legitimacy Gate N/A |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/lib/tools/runners src/app/api/tools/profile` green.
- Engine + tools suites stay green (no Socials regression): `node ./node_modules/vitest/vitest.mjs run src/lib/tools src/lib/engine/flash`.
- `npx tsc --noEmit` introduces no new errors on touched files.
</verification>

<success_criteria>
PROF-01/02/03 land: a single bake yields the forensic READ hero card + a saved General SIM, tier-gated, instruction-isolated, persisted to the thread, with every P4/P3 security carry-forward closed. The block carries `savedAudienceId` so PROF-04's chain CTA (05-01) targets the just-built audience.
</success_criteria>

<output>
Create `.planning/phases/05-profile-simulate-wow/05-04-SUMMARY.md` when done.
</output>
