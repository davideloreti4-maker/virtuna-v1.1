---
phase: 03-general-population-honesty-layer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/audience/enrich-signature.ts
  - scripts/rebake-determinism.ts
autonomous: false
requirements: [POP-01]
must_haves:
  truths:
    - "The qwen-3.7-plus synthesis bake runs with enable_thinking:false (greedy temp:0 only)"
    - "A live double-bake of the frozen khaby fixture yields signatureEqual(a,b) === true"
    - "Synthesis quality does not regress vs the socials control (A/B recorded)"
  artifacts:
    - path: "scripts/rebake-determinism.ts"
      provides: "Live double-bake harness (paid) re-created from the torn-down spike probe"
    - path: "src/lib/audience/enrich-signature.ts"
      provides: "Determinism close-out: thinking-mode dropped on the synth call"
  key_links:
    - from: "scripts/rebake-determinism.ts"
      to: "src/lib/audience/enrich-signature.ts"
      via: "enrichSignature(...) with real defaultSynthesize x2"
      pattern: "enrichSignature"
---

<objective>
Close the Phase 2 NO-GO (conditional) determinism leg before any General-surface work begins (D-01). Drop thinking-mode on the `qwen-3.7-plus` synthesis bake in `enrich-signature.ts` (`enable_thinking:false`, drop `thinking_budget`), then CONFIRM the fix with a live, paid double-bake harness asserting `signatureEqual(a,b) === true` plus an A/B of synth quality against the socials control.

Purpose: `temp:0` greedy decoding is the real determinism lever; thinking-mode staging was the documented residual-jitter source (RESEARCH Pitfall 3). This is the Wave-0 GATE — the rest of Phase 3 (migration, repo, UI) does not begin until this passes. The existing `signature-determinism.test.ts` is a zero-network REPLAY test and CANNOT prove this (Pitfall 3) — a real Qwen call is mandatory.
Output: edited synth call + a re-created `scripts/rebake-determinism.ts` harness + recorded `signatureEqual:true` evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-general-population-honesty-layer/03-CONTEXT.md
@.planning/phases/02-trustworthy-sim-spike/SPIKE-VERDICT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop thinking-mode on the synthesis bake (D-01)</name>
  <read_first>
    - src/lib/audience/enrich-signature.ts (lines 30-58 for QWEN_REASONING_MODEL/QWEN_SEED/SYNTH_MAX_TOKENS consts; lines 338-368 for defaultSynthesize)
    - .planning/phases/03-general-population-honesty-layer/03-CONTEXT.md (D-01 lock)
    - .planning/phases/02-trustworthy-sim-spike/SPIKE-VERDICT.md (§Fallback option 1)
  </read_first>
  <action>
    In `defaultSynthesize` (the synth call at ~lines 343-356), set `enable_thinking: false` and REMOVE the `thinking_budget: 2000` line. Keep `temperature: 0` (351) and `seed: QWEN_SEED` (352) exactly as-is. Update the `model: QWEN_REASONING_MODEL` trailing comment and the `enable_thinking` comment to reference D-01 (greedy temp:0 is the determinism lever; thinking-mode was the Pitfall-3 jitter source). `SYNTH_MAX_TOKENS` (6000) may stay — output budget is unaffected; do not retune it in this task. Do NOT touch any other call (the watch/omni call at line ~301 keeps its config). Do NOT bump ENGINE_VERSION. Do NOT alter SynthSchema or the orchestrator.
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/audience/__tests__/signature-determinism.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "enable_thinking" src/lib/audience/enrich-signature.ts` shows `enable_thinking: false` on the defaultSynthesize call and NO `thinking_budget` line remains in that block.
    - `grep -c "thinking_budget" src/lib/audience/enrich-signature.ts` returns 0 (no stray thinking_budget reference) OR any remaining match is a header comment only (`grep -v '^[[:space:]]*\*' ... | grep -c thinking_budget` == 0).
    - `temperature: 0` and `seed: QWEN_SEED` still present in defaultSynthesize.
    - The kept replay gate `signature-determinism.test.ts` still passes (proves no assembly/normalization regression — NOT the D-01 proof itself).
    - ENGINE_VERSION unchanged (`git diff` shows no ENGINE_VERSION edit).
  </acceptance_criteria>
  <done>The synth bake runs greedy temp:0 with thinking-mode off; the replay gate is still green.</done>
</task>

<task type="auto">
  <name>Task 2: Re-create the live double-bake harness under scripts/</name>
  <read_first>
    - .planning/phases/03-general-population-honesty-layer/03-PATTERNS.md (§ NEW scripts/<re-bake-harness>.ts — restore instructions)
    - src/lib/audience/signature-equality.ts (normalizeSignature / signatureEqual / stableStringify)
    - src/lib/audience/enrich-signature.ts (enrichSignature orchestrator signature, lines 370+)
  </read_first>
  <action>
    Restore the torn-down spike probe as a minimal re-bake harness. Run `git show 362ef8df^:scripts/spike/trustworthy-sim-probe.ts` and `git show 362ef8df^:scripts/spike/fixtures/socials-bundle.fixture.json` (and `git show 362ef8df^:scripts/spike/chat-bundle-adapter.ts` if the probe imports it) to recover the source, then write a trimmed `scripts/rebake-determinism.ts`. The harness: loads the frozen socials-bundle fixture (Apify-free — no re-scrape), calls `enrichSignature` with the real `defaultSynthesize` TWICE on the identical frozen input, then asserts `signatureEqual(normalizeSignature(a), normalizeSignature(b)) === true` and prints both normalized signatures on mismatch. Add a second pass that prints the synth `summary` + persona `reaction_frame`s from one bake so the operator can A/B quality vs the socials control. Gate on `DASHSCOPE_API_KEY` (throw a clear message if unset). Keep it a standalone `tsx`/`node`-runnable script — do NOT add it to the vitest suite (it is paid + live). Vendor the fixture under `scripts/fixtures/` or reference the restored path; do NOT reintroduce the full `scripts/spike/` tree.
  </action>
  <verify>
    <automated>test -f scripts/rebake-determinism.ts &amp;&amp; grep -q "signatureEqual" scripts/rebake-determinism.ts &amp;&amp; grep -q "enrichSignature" scripts/rebake-determinism.ts &amp;&amp; echo HARNESS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/rebake-determinism.ts` exists and imports `enrichSignature` + `signatureEqual`/`normalizeSignature`.
    - It reads `DASHSCOPE_API_KEY` and aborts with a clear message when unset (grep `DASHSCOPE_API_KEY`).
    - It bakes the frozen fixture exactly twice and compares normalized signatures (no Apify/network scrape path).
    - No NEW tsc errors on the harness file: `! npx tsc --noEmit 2>&1 | grep -E 'scripts/rebake-determinism\.ts'` exits 0 (the repo tsc baseline is non-zero — earnings-chart.tsx / hooks-runner.test.ts — so gate on no-new-errors on OUR path, not a clean full-repo run).
  </acceptance_criteria>
  <done>A standalone paid double-bake harness exists and type-checks, ready for the live gate run.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <what-built>Determinism fix (thinking-mode dropped) + a live double-bake harness.</what-built>
  <how-to-verify>
    1. Ensure `DASHSCOPE_API_KEY` is set in the environment (budget ~10 Qwen calls / &lt;$0.50, spike actuals).
    2. Run the harness twice-baking the frozen fixture: `node --import tsx scripts/rebake-determinism.ts` (or the repo's tsx runner).
    3. Confirm the harness prints `signatureEqual: true`.
    4. Eyeball the A/B output: the `summary` + reactor `reaction_frame`s read coherently vs the socials control (no quality collapse — A2 guard).
    5. Paste the `signatureEqual:true` line + a one-line quality judgement into the plan SUMMARY as the D-01 GATE evidence.
  </how-to-verify>
  <acceptance_criteria>
    - Recorded evidence shows a REAL paid Qwen run (not the replay test) with `signatureEqual(a,b) === true`.
    - Synth quality judged non-regressed vs the socials control (A2).
    - If `signatureEqual:false`: STOP the phase and escalate — fall back to SPIKE-VERDICT §Fallback option 3 (bounded prose tolerance) before any Wave-1 work proceeds.
  </acceptance_criteria>
  <resume-signal>Type "approved — signatureEqual:true recorded" or report the mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script → DashScope (Qwen) API | Outbound paid LLM call; reads `DASHSCOPE_API_KEY` from env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `scripts/rebake-determinism.ts` reads `DASHSCOPE_API_KEY` | mitigate | Read the key from `process.env` only; never hardcode/log it; the harness is gitignored-safe (no secret literals). React/log output prints signatures, not the key |
| T-03-02 | Denial of Service (cost) | unbounded paid bakes | accept | Bounded to exactly 2 bakes per run; manual human-approved gate; spike actuals &lt;$0.50 |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs this plan (no new deps) |
</threat_model>

<verification>
- Determinism gate: live `signatureEqual:true` recorded (human-approved checkpoint).
- Regression: `signature-determinism.test.ts` green (no assembly/normalization drift).
- No ENGINE_VERSION bump; no change to any call other than the synth call.
</verification>

<success_criteria>
The synth bake is thinking-mode-free, a live double-bake confirms `signatureEqual:true` with no quality regression, and the rest of Phase 3 is cleared to proceed.
</success_criteria>

## Artifacts this plan produces

- `scripts/rebake-determinism.ts` (NEW — paid live double-bake harness)
- Edit to `src/lib/audience/enrich-signature.ts` (`enable_thinking:false`, `thinking_budget` removed)

<output>
Create `.planning/phases/03-general-population-honesty-layer/03-01-SUMMARY.md` when done — include the verbatim `signatureEqual:true` gate evidence.
</output>
</content>
