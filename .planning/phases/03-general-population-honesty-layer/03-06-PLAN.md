---
phase: 03-general-population-honesty-layer
plan: 06
type: execute
wave: 3
depends_on: ["03-02", "03-04"]
files_modified:
  - src/app/api/audiences/route.ts
  - src/app/api/audiences/[id]/route.ts
  - src/components/audience/audience-form.tsx
autonomous: true
requirements: [POP-05, POP-02, TRUST-02]
must_haves:
  truths:
    - "A user can author and edit a General audience's success_criterion via the form"
    - "A user can add/edit custom_context entries (user-added grounding), rendered distinctly from scraped grounding"
    - "The POST and PATCH audiences routes accept + validate success_criterion, custom_context, and mode"
    - "POP-03 save/name in P3 = the existing audience-form create + edit path (name required, PATCH/POST) round-trips a mode='general' audience through these routes — the form gains success_criterion + custom_context fields but NO mode toggle. Net-new General-from-scratch is Profile/P5; the front-door mode-creation picker is P7 (CONTEXT). No new front-door UI here."
  artifacts:
    - path: "src/components/audience/audience-form.tsx"
      provides: "success-criterion textarea + custom-context add/edit affordance + payload wiring"
    - path: "src/app/api/audiences/route.ts"
      provides: "CreateAudienceSchema accepting the new fields"
    - path: "src/app/api/audiences/[id]/route.ts"
      provides: "PatchAudienceSchema accepting the new fields"
  key_links:
    - from: "src/components/audience/audience-form.tsx"
      to: "src/app/api/audiences/[id]/route.ts"
      via: "PATCH payload { success_criterion, custom_context }"
      pattern: "success_criterion"
---

<objective>
Deliver the success-criterion author/edit affordance (POP-05/POP-02) and the first-class custom-context input/edit (D-07/TRUST-02). Extend the two audiences route schemas (`CreateAudienceSchema`, `PatchAudienceSchema`) to accept + sanitize + cap `success_criterion`, `custom_context`, and `mode`, then add a success-criterion textarea and a custom-context add/edit list to `audience-form.tsx`, wiring both into the existing POST/PATCH payload.

Purpose: the route schemas are separate from the repo's `WritableAudienceSchema` (they live in `route.ts` / `[id]/route.ts` with their own sanitizeText) — they MUST also gain the new fields or the form payload is silently dropped at the API boundary. Custom-context strengthens provenance (tagged, visible), never fakes it — render it distinctly from scraped grounding.
Output: extended route schemas + form fields + payload wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-general-population-honesty-layer/03-CONTEXT.md
@.planning/phases/03-general-population-honesty-layer/03-PATTERNS.md
@src/app/api/audiences/route.ts
@src/app/api/audiences/[id]/route.ts
@src/components/audience/audience-form.tsx
@src/lib/audience/audience-types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Accept success_criterion + custom_context + mode at the API boundary (POP-05, V5)</name>
  <read_first>
    - src/app/api/audiences/route.ts (CreateAudienceSchema 46-56; sanitizeText 41-44)
    - src/app/api/audiences/[id]/route.ts (PatchAudienceSchema 48-60; sanitizeText 31-34)
    - src/lib/audience/audience-repo.ts (WritableAudienceSchema — keep the two schemas consistent)
    - .planning/phases/03-general-population-honesty-layer/03-RESEARCH.md (Security Domain V5 — caps + plain-text)
  </read_first>
  <action>
    In BOTH `route.ts` (`CreateAudienceSchema`) and `[id]/route.ts` (`PatchAudienceSchema`), add: `mode: z.enum(["socials","general"]).optional()`; `success_criterion: z.string().max(2000).transform(sanitizeText).nullable().optional()`; `custom_context: z.array(z.object({ source: z.literal("user"), note: z.string().max(2000).transform(sanitizeText), persona_evidence_link: z.string().max(120).optional() })).max(50).optional()`. Reuse each file's existing `sanitizeText` (strips control chars + trims). Keep the existing fields untouched. These flow through `createAudience`/`updateAudience` → `audienceToRow` (already field-additive from 03-04). Do NOT thread `success_criterion` into any scorer (`DomainPackScoring` is untouched — D-02); store + surface only.
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/app/api/audiences/__tests__/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both `CreateAudienceSchema` and `PatchAudienceSchema` include `mode`, `success_criterion` (`.max(2000)` + `sanitizeText`), and `custom_context` (array, `.max(50)`, `source: z.literal("user")`, `note` `.max(2000)` + `sanitizeText`).
    - No change to `DomainPackScoring` (grep shows no scorer import added to the routes).
    - The existing route test suite stays green (or is extended to cover the new fields).
  </acceptance_criteria>
  <done>The POST/PATCH routes accept, sanitize, and cap the new fields; the scorer is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: success-criterion + custom-context input/edit in the form (POP-05/D-07/TRUST-02)</name>
  <read_first>
    - src/components/audience/audience-form.tsx (goalLabel state 47; payload 68-74; PATCH/POST 76-89)
    - src/lib/audience/audience-types.ts (CustomContext shape)
    - .planning/phases/03-general-population-honesty-layer/03-PATTERNS.md (§ audience-form.tsx — mirror the goalLabel field idiom)
    - docs/DESIGN-SYSTEM.md (flat-warm inputs — 8px radius, 6% borders)
  </read_first>
  <action>
    In `audience-form.tsx`: add `const [successCriterion, setSuccessCriterion] = useState(existing?.success_criterion ?? "")` and a `custom_context` editor state `const [customContext, setCustomContext] = useState<CustomContext[]>(existing?.custom_context ?? [])`. Render a labelled multi-line textarea "Success criterion — what 'good' means for this audience" (optional, maxLength 2000) mirroring the existing goal `Input` styling. Render a "User-added grounding" sub-section: a list of current `custom_context` notes (each editable + removable) plus an "Add grounding" control that appends `{ source: "user", note: "" }`; visually tag this block distinctly from scraped grounding (a small "user-added" label). Add both to the submit `payload`: `success_criterion: successCriterion.trim() || null` and `custom_context: customContext.filter(c => c.note.trim().length > 0)`. Render all free text as plain React children (no `dangerouslySetInnerHTML`). Keep the calibration-flow branch + existing fields (incl. the required `name` field — the POP-03 save/name affordance) unchanged.
  </action>
  <verify>
    <automated>! npx tsc --noEmit 2>&1 | grep -E 'src/components/audience/audience-form\.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "successCriterion" src/components/audience/audience-form.tsx` shows state + a textarea + inclusion in the submit payload.
    - The form renders a distinct "user-added grounding" custom-context add/edit/remove affordance bound to `customContext` state.
    - The payload includes `success_criterion` and `custom_context` (empty notes filtered).
    - No `dangerouslySetInnerHTML` anywhere in the file (grep negative).
    - No NEW tsc errors on the touched form: `! npx tsc --noEmit 2>&1 | grep -E 'src/components/audience/audience-form\.tsx'` exits 0 (repo tsc baseline is non-zero — gate on no-new-errors on OUR path, not a clean full-repo run).
  </acceptance_criteria>
  <done>A user can author + edit the success criterion and add/edit user-added grounding, persisted via the existing POST/PATCH path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → POST/PATCH /api/audiences (route Zod) | Untrusted free-text crosses into persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | Stored XSS via free-text `success_criterion` / `custom_context.note` | mitigate | Route zod caps length + `sanitizeText` strips control chars; rendered as plain React text (escaped); never `dangerouslySetInnerHTML` |
| T-03-13 | Elevation / Tampering | Mass-assignment of `user_id`/`is_general`/`mode` via POST/PATCH body | mitigate | Route schema is an allowlist (unknown keys dropped); `user_id` injected from session in `createAudience`/`updateAudience` (never body); `mode` constrained by zod enum + DB CHECK |
| T-03-14 | Denial of Service | Unbounded `custom_context` array | mitigate | `.max(50)` array cap + `.max(2000)` per-note cap in route zod |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs this plan |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/app/api/audiences/__tests__/route.test.ts` green — the primary behavioral gate.
- No NEW tsc errors on the touched form: `! npx tsc --noEmit 2>&1 | grep -E 'src/components/audience/audience-form\.tsx'` exits 0 (do NOT gate on a clean full-repo `tsc --noEmit` — baseline is non-zero).
- Both route schemas + the form carry the new fields; no scorer wiring (D-02 respected).
</verification>

<success_criteria>
A user can author and edit a General audience's success-criterion and add user-added grounding through the form, validated + sanitized at the API boundary and rendered distinctly from scraped grounding.
</success_criteria>

## Artifacts this plan produces

- `CreateAudienceSchema` + `PatchAudienceSchema` extensions (`mode`, `success_criterion`, `custom_context`)
- success-criterion textarea + custom-context add/edit affordance in `audience-form.tsx`

<output>
Create `.planning/phases/03-general-population-honesty-layer/03-06-SUMMARY.md` when done.
</output>
</content>
