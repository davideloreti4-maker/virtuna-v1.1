---
phase: 03-general-population-honesty-layer
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-04"]
files_modified:
  - src/components/audience/audience-display.ts
  - src/components/audience/trust-badge.tsx
  - src/components/audience/audience-card.tsx
  - src/components/audience/audience-manager.tsx
  - src/components/audience/__tests__/honesty-render.test.tsx
autonomous: true
requirements: [TRUST-01, TRUST-02, POP-03]
must_haves:
  truths:
    - "Each audience card shows a Validated or Directional badge derived from resolveTier"
    - "A grounded persona shows its evidence inline; an evidence-free persona renders a distinct muted 'no evidence — Directional' state"
    - "General templates (analyst/hiring) appear as a distinct browseable section in the audience manager"
    - "A behavioral render test in-phase proves the headline honesty deliverable: TrustBadge renders Validated for a mode='socials' fixture and Directional for mode='general'; an evidence-free persona renders the muted ungrounded affordance while an evidence-bearing one shows its quote"
    - "POP-03 browse/reuse in P3 = the General baseline + analyst/hiring templates are surfaced + selectable in the existing manager (this plan). The save/name half is the existing manager CRUD (03-04/03-06); net-new General-from-scratch is Profile/P5, front-door mode-creation is P7 (CONTEXT) — no new front-door UI here."
  artifacts:
    - path: "src/components/audience/trust-badge.tsx"
      provides: "TrustBadge component (Validated/Directional) over the flat-warm Badge primitive"
      exports: ["TrustBadge"]
    - path: "src/components/audience/audience-display.ts"
      provides: "isPersonaGrounded predicate + general-templates grouping bucket"
      contains: "isPersonaGrounded"
    - path: "src/components/audience/__tests__/honesty-render.test.tsx"
      provides: "in-phase render test for the honesty layer (badge tier + grounded/ungrounded persona affordance)"
  key_links:
    - from: "src/components/audience/audience-card.tsx"
      to: "src/lib/audience/resolve-tier.ts"
      via: "TrustBadge tier={resolveTier(audience)}"
      pattern: "resolveTier"
---

<objective>
Make the honesty layer visible at a glance on the audience surface (TRUST-01/TRUST-02/POP-03). Add the `isPersonaGrounded` predicate + a general-templates grouping bucket to `audience-display.ts`, build a flat-warm `TrustBadge` (Validated/Directional) over the existing Badge primitive, mount it on the audience card beside the status chip, render persona evidence INLINE with a distinct muted ungrounded state, and surface the new General templates as a browseable section in the manager. Lock the headline deliverable with an in-phase render test (UI-SPEC was `--skip-ui`'d for this phase — there is NO downstream `/gsd-ui-phase` to catch a honesty-render regression, so it lands here).

Purpose: "honest at a glance" is the bar (D-05) — grounded vs ungrounded and Validated vs Directional must read instantly, not behind a click/expand. Reuse the locked flat-warm design system (`@/components/ui/badge`, 6% borders, 12px radius, cream text, terracotta accent — NO coral/glass/glow). No new visual language.
Output: display helpers + TrustBadge + card honesty render + manager templates section + the honesty render test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-general-population-honesty-layer/03-CONTEXT.md
@.planning/phases/03-general-population-honesty-layer/03-PATTERNS.md
@docs/DESIGN-SYSTEM.md
@src/components/audience/audience-display.ts
@src/components/audience/audience-card.tsx
@src/components/audience/audience-status-chip.tsx
@src/components/audience/audience-manager.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: isPersonaGrounded predicate + general-templates grouping (D-05/D-08, A6)</name>
  <read_first>
    - src/components/audience/audience-display.ts (getPersonaRoster 33-38; getCalibrationStatus 57-65; groupAudiences 109-124)
    - src/lib/audience/audience-types.ts (SignaturePersona.evidence 117-127; CalibratedPersona 63-76)
    - .planning/phases/03-general-population-honesty-layer/03-RESEARCH.md (§ Code Examples isPersonaGrounded; Pitfall 5)
  </read_first>
  <action>
    In `audience-display.ts` add and export `isPersonaGrounded(p: { evidence?: string }): boolean` returning `typeof p.evidence === "string" && p.evidence.trim().length > 0` (mirrors the spike's empty-evidence predicate). Extend the `groupAudiences` return type with a new `generalTemplates: Audience[]` bucket and route audiences with `mode === 'general'` (the analyst/hiring templates) into it BEFORE the `is_preset` check, so they do not mix into the existing socials `templates` bucket (A6). Keep `baseline`/`templates`/`yours` semantics unchanged for socials constants. Add a `getTemplateProvenanceLabel(audience)` helper returning "Authored template — Directional" for `mode==='general'` templates (used by the card subline).
  </action>
  <verify>
    <automated>! npx tsc --noEmit 2>&1 | grep -E 'src/components/audience/'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function isPersonaGrounded" src/components/audience/audience-display.ts` == 1; it returns true only for a non-empty trimmed evidence string.
    - `groupAudiences` returns a `generalTemplates` array and routes `mode==='general'` audiences there (not into `templates`).
    - The socials `templates` bucket still receives `is_preset` audiences only.
    - No NEW tsc errors on the touched components: `! npx tsc --noEmit 2>&1 | grep -E 'src/components/audience/'` exits 0 (repo tsc baseline is non-zero — gate on no-new-errors on OUR paths, not a clean full-repo run).
  </acceptance_criteria>
  <done>Display helpers expose the grounded predicate + a distinct general-templates bucket.</done>
</task>

<task type="auto">
  <name>Task 2: TrustBadge component (Validated/Directional) (D-06 / TRUST-01)</name>
  <read_first>
    - src/components/audience/audience-status-chip.tsx (full — the Badge wrapper + variant-record idiom to mirror)
    - src/lib/audience/resolve-tier.ts (TrustTier type from 03-02)
    - docs/DESIGN-SYSTEM.md (flat-warm badge variants; no coral/glass)
  </read_first>
  <action>
    Create `src/components/audience/trust-badge.tsx` exporting `TrustBadge({ tier, className }: { tier: TrustTier; className?: string })`. Mirror `AudienceStatusChip`: wrap `@/components/ui/badge` `Badge` with `size="sm"`. Map `Validated` → a calm confident variant (e.g. `default`) and `Directional` → a muted/secondary variant — pick from the EXISTING flat-warm badge variants only (no new primitive, no coral). Label text "Validated" / "Directional". Import `TrustTier` type-only from `@/lib/audience/resolve-tier`. Keep it presentation-only (no resolveTier call inside — the caller passes the resolved tier).
  </action>
  <verify>
    <automated>! npx tsc --noEmit 2>&1 | grep -E 'src/components/audience/'</automated>
  </verify>
  <acceptance_criteria>
    - `trust-badge.tsx` exports `TrustBadge` and imports `Badge` from `@/components/ui/badge` (reuses the primitive — grep).
    - It renders the literal labels "Validated" and "Directional"; no coral/`#FF7F50`/glass classes (grep negative).
    - No NEW tsc errors on the touched components: `! npx tsc --noEmit 2>&1 | grep -E 'src/components/audience/'` exits 0.
  </acceptance_criteria>
  <done>A flat-warm TrustBadge renders the two tiers from a passed-in TrustTier.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mount badge + inline evidence + ungrounded state on the card; surface templates in the manager; lock with an in-phase render test (D-05/D-06/POP-03, TRUST-01/02)</name>
  <read_first>
    - src/components/audience/audience-card.tsx (header render 98-124; imports 7-15; getCalibrationStatus usage 48)
    - src/components/audience/audience-manager.tsx (groupAudiences consumer 177; section rendering 326-353; SectionLabel 39)
    - src/components/audience/audience-display.ts (getPersonaRoster, isPersonaGrounded, getTemplateProvenanceLabel from Task 1)
    - src/lib/audience/resolve-tier.ts (resolveTier)
    - src/components/audience/__tests__/persona-edit.test.tsx (the happy-dom + @testing-library/react render idiom + the vitest-environment happy-dom pragma to copy)
  </read_first>
  <behavior>
    - Rendering `TrustBadge tier={resolveTier(socialsFixture)}` shows "Validated"; `tier={resolveTier(generalFixture)}` shows "Directional".
    - Rendering an audience card whose roster persona has a non-empty `evidence` shows that evidence quote inline.
    - Rendering an audience card whose roster personas are all evidence-free shows the muted "no evidence — Directional" affordance and does NOT show an evidence quote.
  </behavior>
  <action>
    In `audience-card.tsx`: import `resolveTier` and `TrustBadge`, compute `const tier = resolveTier(audience)`, and render `<TrustBadge tier={tier} />` adjacent to the existing `<AudienceStatusChip status={status} />` in the header (line ~103). Below the temp bar, render persona provenance: map `getPersonaRoster(audience)` and for each persona, if `isPersonaGrounded(persona)` show its `evidence` quote inline (small muted cream text, truncated), else render a single distinct muted "no evidence — Directional" line for the card (do not repeat per-persona — one ungrounded affordance is enough at card scale). For `mode==='general'` template cards, show `getTemplateProvenanceLabel(audience)` as the provenance subline. Keep the change additive — do not alter existing layout classes beyond inserting the badge + provenance block. In `audience-manager.tsx`: destructure the new `generalTemplates` bucket from `groupAudiences` and render a new `<section>` with `<SectionLabel>General templates</SectionLabel>` between the existing Templates and Yours sections, mapping `generalTemplates.map(renderAudienceCard)`. Mode-sectioning proper (Socials/General split) stays P7 — this is just surfacing the bucket.

    Then create `src/components/audience/__tests__/honesty-render.test.tsx` (vitest, opt into happy-dom via the top-of-file vitest-environment pragma + `@testing-library/react` `render`/`screen`, mirroring `persona-edit.test.tsx` exactly). Build three lightweight fixtures: a `mode:'socials'` Audience, a `mode:'general'` Audience whose roster persona carries a non-empty `evidence`, and a `mode:'general'` Audience whose roster personas are all evidence-free. Assert: (a) `render(<TrustBadge tier={resolveTier(socialsFixture)} />)` shows "Validated" and `resolveTier(generalFixture)` → a "Directional" badge; (b) rendering `<AudienceCard audience={groundedFixture} … />` shows the evidence quote text; (c) rendering `<AudienceCard audience={ungroundedFixture} … />` shows "no evidence — Directional" and NOT an evidence quote. Supply whatever `AudienceCard` props are required (mirror persona-edit.test fixtures); mock nothing external beyond what that test mocks. Run via the project runner only (`node ./node_modules/vitest/vitest.mjs run` — NOT `npm test`/`npx vitest`).
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/audience/__tests__/honesty-render.test.tsx</automated>
    <automated>! npx tsc --noEmit 2>&1 | grep -E 'src/components/audience/'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "resolveTier" src/components/audience/audience-card.tsx` shows the call; `TrustBadge` is rendered in the card header.
    - The card renders an inline evidence quote when `isPersonaGrounded` is true and a muted "no evidence — Directional" affordance when no roster persona is grounded.
    - `audience-manager.tsx` renders a "General templates" section bound to the `generalTemplates` bucket.
    - `honesty-render.test.tsx` exists and asserts (a) Validated vs Directional badge by `resolveTier(mode)`, (b) inline evidence quote for a grounded persona, (c) the muted ungrounded affordance (and no quote) for an evidence-free card — and passes via `node ./node_modules/vitest/vitest.mjs run …`.
    - The existing reskin guard test stays green (`node ./node_modules/vitest/vitest.mjs run src/components/reading/__tests__/reskin-matte.test.ts`).
    - No NEW tsc errors on the touched components: `! npx tsc --noEmit 2>&1 | grep -E 'src/components/audience/'` exits 0.
  </acceptance_criteria>
  <done>Validated/Directional and grounded/ungrounded read at a glance on the card, are browseable in the manager, and are locked by an in-phase render test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored audience strings → rendered DOM | `evidence`, `success_criterion`, `custom_context.note`, template copy rendered in React |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | Stored XSS via persona `evidence` / template free-text in the card | mitigate | Rendered as plain React text children (auto-escaped); never `dangerouslySetInnerHTML`; length already capped at the repo zod boundary |
| T-03-11 | Information Disclosure | Mis-badging a general audience as Validated | mitigate | Badge derives strictly from `resolveTier(audience)` (locked never-Validated-for-general rule); card passes the resolved tier, no override; render test asserts general→Directional |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs this plan |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/components/audience/__tests__/honesty-render.test.tsx` green — the in-phase headline-deliverable gate.
- `node ./node_modules/vitest/vitest.mjs run src/components/reading/__tests__/reskin-matte.test.ts` green (no coral/glass regression).
- No NEW tsc errors on the touched components: `! npx tsc --noEmit 2>&1 | grep -E 'src/components/audience/'` exits 0 (do NOT gate on a clean full-repo `tsc --noEmit` — baseline is non-zero).
</verification>

<success_criteria>
The audience card wears a tier badge and surfaces persona provenance honestly (grounded inline, ungrounded muted), General templates are a distinct browseable section, and an in-phase render test locks the badge-tier + grounded/ungrounded behavior — all in the locked flat-warm system.
</success_criteria>

## Artifacts this plan produces

- `src/components/audience/trust-badge.tsx` (NEW) — `TrustBadge`
- `isPersonaGrounded`, `getTemplateProvenanceLabel`, `generalTemplates` bucket in `audience-display.ts`
- Tier badge + inline-evidence + ungrounded render on `audience-card.tsx`
- "General templates" section in `audience-manager.tsx`
- `src/components/audience/__tests__/honesty-render.test.tsx` (NEW) — in-phase honesty render test

<output>
Create `.planning/phases/03-general-population-honesty-layer/03-05-SUMMARY.md` when done.
</output>
</content>
