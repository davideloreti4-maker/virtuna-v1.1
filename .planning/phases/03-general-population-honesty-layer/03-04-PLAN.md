---
phase: 03-general-population-honesty-layer
plan: 04
type: execute
wave: 2
depends_on: ["03-02", "03-03"]
files_modified:
  - src/lib/audience/audience-repo.ts
  - src/lib/audience/__tests__/audience-repo-mode.test.ts
autonomous: true
requirements: [POP-01, POP-03, POP-04]
must_haves:
  truths:
    - "mode, success_criterion, and custom_context round-trip losslessly through rowToAudience/audienceToRow"
    - "GENERAL_AUDIENCE.mode === 'socials' and every PRESET_AUDIENCES entry is mode 'socials'"
    - "Two new General templates (analyst + hiring) exist as virtual constants, mode 'general', signature null, prepended in listAudiences, refused on write/delete"
    - "POP-03 save/name in P3 = create + update a mode='general' audience round-trips through createAudience/updateAudience (the existing manager CRUD path; rename = updateAudience name PATCH). Net-new General-from-scratch is Profile/P5; the front-door mode-creation picker is P7 (CONTEXT). No new front-door UI is added here."
  artifacts:
    - path: "src/lib/audience/audience-repo.ts"
      provides: "row mappers + WritableAudienceSchema for new fields; GENERAL_TEMPLATES (ANALYST_AUDIENCE, HIRING_AUDIENCE)"
      contains: "GENERAL_TEMPLATES"
    - path: "src/lib/audience/__tests__/audience-repo-mode.test.ts"
      provides: "round-trip + template presence + sentinel-skip + mode='general' save/name (create+update) tests"
  key_links:
    - from: "src/lib/audience/audience-repo.ts listAudiences"
      to: "GENERAL_TEMPLATES"
      via: "prepend [GENERAL_AUDIENCE, ...PRESET_AUDIENCES, ...GENERAL_TEMPLATES, ...userRows]"
      pattern: "GENERAL_TEMPLATES"
---

<objective>
Wire the new domain fields through the audience repo seam and ship the two General template panels (D-04/D-07/D-08). Extend `AudienceRow`, `rowToAudience`, `audienceToRow`, and `WritableAudienceSchema` for `mode` / `success_criterion` / `custom_context`; stamp the existing virtual constants `mode='socials'` (the collision trap); and add `GENERAL_TEMPLATES = [ANALYST_AUDIENCE, HIRING_AUDIENCE]` as `mode='general'`, `signature:null`, sentinel-id virtual constants runnable with zero setup. This is also the data-layer half of POP-03: a `mode='general'` audience saves (createAudience), lists (listAudiences), and renames (updateAudience) through the existing CRUD — no front-door mode picker (that is P7).

Purpose: `rowToAudience`/`audienceToRow` + Zod are the ONLY seam between DB rows and the `Audience` object — new fields must pass through losslessly and securely. The templates mirror `PRESET_AUDIENCES` exactly so they never touch the DB (zero-setup, regression-gate-free).
Output: extended repo mappers + zod + two template constants + a round-trip/template/sentinel/save-name test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-general-population-honesty-layer/03-CONTEXT.md
@.planning/phases/03-general-population-honesty-layer/03-PATTERNS.md
@src/lib/audience/audience-repo.ts
@src/lib/audience/audience-types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map new fields through the repo + stamp constants mode='socials' (D-04/07, Pitfall 1)</name>
  <read_first>
    - src/lib/audience/audience-repo.ts (AudienceRow 155-176; rowToAudience 179-204; audienceToRow 207-235; WritableAudienceSchema 135-150; GENERAL_AUDIENCE 36-53; PRESET_AUDIENCES 64-99)
    - src/lib/audience/audience-types.ts (the new mode/success_criterion/custom_context/CustomContext from 03-02)
    - .planning/phases/03-general-population-honesty-layer/03-RESEARCH.md (Security Domain — V5 zod caps; Pitfall 1)
  </read_first>
  <behavior>
    - A row with `mode='general'`, a `success_criterion` string, and a `custom_context` array maps to the matching Audience fields and back without loss.
    - `GENERAL_AUDIENCE.mode === 'socials'`; each `PRESET_AUDIENCES[i].mode === 'socials'`.
    - WritableAudienceSchema accepts `mode` (enum), `success_criterion` (capped string, nullable), and `custom_context` (array of `{source:'user', note (capped), persona_evidence_link?}`); rejects an over-length note.
  </behavior>
  <action>
    In `audience-repo.ts`: add `mode: string`, `success_criterion: string | null`, `custom_context: unknown[]` to `AudienceRow`. In `rowToAudience` add `mode: row.mode as Audience["mode"]`, `success_criterion: row.success_criterion ?? null`, `custom_context: (row.custom_context as Audience["custom_context"]) ?? []`. In `audienceToRow` add (mirroring the existing guards): `if (a.mode !== undefined) row.mode = a.mode;`, `if ("success_criterion" in a) row.success_criterion = a.success_criterion ?? null;`, `if ("custom_context" in a) row.custom_context = a.custom_context ?? [];`. `user_id` stays session-derived — do NOT add it to the writable surface. Extend `WritableAudienceSchema` with `mode: z.enum(["socials","general"]).optional()`, `success_criterion: z.string().max(2000).nullable().optional()`, `custom_context: z.array(z.object({ source: z.literal("user"), note: z.string().max(2000), persona_evidence_link: z.string().optional() })).optional()`. Add `mode: "socials"` to `GENERAL_AUDIENCE` and to BOTH `PRESET_AUDIENCES` entries, with an asserting comment per Pitfall 1: these run the socials pack; ONLY the new analyst/hiring templates are `mode:'general'`. Keep the `(supabase as any)` cast convention.
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/audience/__tests__/audience-repo-mode.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mode: \"socials\"" src/lib/audience/audience-repo.ts` shows it on GENERAL_AUDIENCE and both presets.
    - `WritableAudienceSchema` includes `mode`, `success_criterion` (`.max(2000)`), and `custom_context` (array with `source: z.literal("user")` + `note` `.max(2000)`).
    - `audienceToRow` does NOT set `user_id` from the input object (still `sessionUserId` only).
    - No NEW tsc errors on the touched file: `! npx tsc --noEmit 2>&1 | grep -E 'src/lib/audience/audience-repo\.ts'` exits 0 (repo tsc baseline is non-zero — gate on no-new-errors for OUR path, not a clean full-repo run).
  </acceptance_criteria>
  <done>New fields round-trip through the repo, zod caps free-text, and the existing constants are correctly mode='socials'.</done>
</task>

<task type="auto">
  <name>Task 2: Add GENERAL_TEMPLATES (analyst + hiring) virtual constants (D-08)</name>
  <read_first>
    - src/lib/audience/audience-repo.ts (PRESET_AUDIENCES 64-99; SENTINEL_IDS/VIRTUAL_BY_ID 101-111; listAudiences 245-261; getAudience 269-290; deleteAudience 375-392)
    - src/lib/audience/audience-types.ts (CalibratedPersona shape — templates author personas here, NO evidence, signature stays null)
    - .planning/phases/03-general-population-honesty-layer/03-RESEARCH.md (Pitfall 5 — template personas are CalibratedPersona[] with no evidence → ungrounded-by-design; A6 — is_template/general bucket)
    - .planning/phases/03-general-population-honesty-layer/03-PATTERNS.md (§ audience-repo.ts virtual-constant pattern)
  </read_first>
  <action>
    Add two virtual-constant audiences mirroring `PRESET_AUDIENCES`: `ANALYST_AUDIENCE` (sentinel id `template-analyst`) and `HIRING_AUDIENCE` (sentinel id `template-hiring`), exported as `GENERAL_TEMPLATES: Audience[]`. Each: `mode: "general"`, `is_general:false`, `is_preset:false`, `signature: null` / `signature` omitted (KEEP null — regression-gate-free per Pitfall 5), `user_id:"__virtual__"`, `custom_context: []`, a representative `success_criterion` string (analyst: e.g. "Surfaces the sharpest risk + the strongest counter-argument"; hiring: e.g. "Flags the strongest signal and the biggest gap against the role"), and `personas` authored as a small `CalibratedPersona[]` panel (archetype/repaint/temperature/disposition/share, shares summing ~1.0, NO `evidence` field — they render ungrounded-by-design per D-05). Author defensible 3-4 persona panels each (analyst: e.g. skeptic / strategist / contrarian; hiring: e.g. manager / peer / skeptic). Extend `SENTINEL_IDS` and `VIRTUAL_BY_ID` to include both template ids, and update the `listAudiences` prepend to `[GENERAL_AUDIENCE, ...PRESET_AUDIENCES, ...GENERAL_TEMPLATES, ...userRows]`. `getAudience` already short-circuits via VIRTUAL_BY_ID; `deleteAudience` already throws on SENTINEL_IDS — confirm both now cover the template ids. Do NOT add a DB seed row.
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/audience/__tests__/audience-repo-mode.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `GENERAL_TEMPLATES` is exported and contains exactly two entries with ids `template-analyst` and `template-hiring`, both `mode:'general'`.
    - Both templates have `signature` null/absent and personas with NO `evidence` field (grep the constants — ungrounded-by-design).
    - `SENTINEL_IDS` and `VIRTUAL_BY_ID` include both template ids; `listAudiences` prepend includes `...GENERAL_TEMPLATES`.
    - The test asserts both templates appear in a mocked `listAudiences` result and `deleteAudience(template-analyst)` rejects.
  </acceptance_criteria>
  <done>analyst + hiring templates are zero-setup virtual constants, runnable-ready, refused on write/delete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: audience-repo-mode round-trip + template + sentinel + save/name test (POP-03)</name>
  <read_first>
    - src/lib/audience/__tests__/signature-determinism.test.ts (mock-first structure + runner-quirk header lines 16-17)
    - src/lib/audience/audience-repo.ts (the functions under test — incl. createAudience/updateAudience for the save/name round-trip)
  </read_first>
  <action>
    Create `src/lib/audience/__tests__/audience-repo-mode.test.ts` (vitest, runner-quirk header). Cover: (a) a fabricated `AudienceRow` with `mode:'general'`, a `success_criterion`, and a `custom_context` array → `rowToAudience` → `audienceToRow` round-trips all three fields losslessly; (b) `WritableAudienceSchema` rejects a `custom_context[0].note` over 2000 chars and accepts a valid one; (c) `GENERAL_AUDIENCE.mode === 'socials'` and both presets are 'socials'; (d) `GENERAL_TEMPLATES` has the two `mode:'general'` entries with null signature + evidence-free personas; (e) `deleteAudience` throws for `template-analyst`/`template-hiring` (mock the supabase client; assert it never reaches the DB); (f) POP-03 save/name: with a mocked supabase client, `createAudience({ ..., mode:'general', name:'My General' })` writes a row whose `mode` is `'general'` (assert the mode reaches the insert payload via the mock), and `updateAudience(id, { name:'Renamed General' })` issues the update with the new name — proving a General audience saves + renames through the existing CRUD (no front-door picker). Mock the Supabase client for create/update/list/delete the same way the existing repo tests do (no live DB).
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/audience/__tests__/audience-repo-mode.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists and all cases (a)-(f) pass.
    - The round-trip assertion compares the three new fields before/after `rowToAudience`+`audienceToRow`.
    - `deleteAudience` sentinel cases assert a throw without a DB call.
    - Case (f) asserts a `mode='general'` create + a name update both reach the mocked client — the POP-03 save/name path.
    - `node ./node_modules/vitest/vitest.mjs run src/lib/audience` is fully green.
  </acceptance_criteria>
  <done>The repo seam is regression-locked for the new fields + templates + sentinel guards + the General save/name (create+rename) path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request body → createAudience/updateAudience (repo Zod) | Untrusted client input validated before any DB write |
| virtual constants → write paths | Sentinel ids must never be persisted/mutated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Elevation / Tampering | Mass-assignment of `user_id`/`mode`/`is_general` via request body | mitigate | `audienceToRow` keeps `user_id` session-derived (never from input); `mode` validated against a zod enum; `WritableAudienceSchema` is the allowlist — unknown keys are dropped |
| T-03-08 | Tampering | Stored XSS via free-text `success_criterion` / `custom_context.note` | mitigate | zod caps length (`.max(2000)`); values are plain strings rendered by React (escaped) downstream; never `dangerouslySetInnerHTML` |
| T-03-09 | Tampering | Writing to a virtual constant (template/General) | mitigate | SENTINEL_IDS guard in `getAudience`/`deleteAudience`; templates carry sentinel ids → write paths short-circuit; test asserts delete throws |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs this plan |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/lib/audience` green (new repo test + kept gate + resolver) — the primary behavioral gate.
- No NEW tsc errors on the touched file: `! npx tsc --noEmit 2>&1 | grep -E 'src/lib/audience/audience-repo\.ts'` exits 0 (do NOT gate on a clean full-repo `tsc --noEmit` — baseline is non-zero).
- grep gates confirm constants are mode='socials' and templates are mode='general' with null signature.
</verification>

<success_criteria>
New fields round-trip through the repo with capped validation, the existing constants stay socials, analyst + hiring templates are zero-setup virtual constants refused on write/delete, and a General audience saves + renames through the existing CRUD (POP-03).
</success_criteria>

## Artifacts this plan produces

- `GENERAL_TEMPLATES` (NEW) — `ANALYST_AUDIENCE`, `HIRING_AUDIENCE` virtual constants (ids `template-analyst`, `template-hiring`)
- `AudienceRow` + `rowToAudience` + `audienceToRow` + `WritableAudienceSchema` extensions for `mode`/`success_criterion`/`custom_context`
- `mode: "socials"` stamped on `GENERAL_AUDIENCE` + `PRESET_AUDIENCES`
- `src/lib/audience/__tests__/audience-repo-mode.test.ts` (NEW)

<output>
Create `.planning/phases/03-general-population-honesty-layer/03-04-SUMMARY.md` when done.
</output>
</content>
