---
phase: 03-general-population-honesty-layer
plan: 03
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - supabase/migrations/20260627000000_audience_general.sql
autonomous: false
requirements: [POP-01]
user_setup:
  - service: supabase
    why: "Apply the additive audiences migration to the live project DB"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (enables non-TTY `supabase db push`)"
must_haves:
  truths:
    - "audiences has a mode column (text NOT NULL DEFAULT 'socials' CHECK IN ('socials','general'))"
    - "The weight-sum CHECK is gated to mode='socials' only; existing Socials rows still satisfy it byte-stable"
    - "audiences has success_criterion text + custom_context jsonb NOT NULL DEFAULT '[]'"
    - "The migration is applied to the live DB (supabase db push) before phase verification"
  artifacts:
    - path: "supabase/migrations/20260627000000_audience_general.sql"
      provides: "Additive mode column + mode-gated weight CHECK + success_criterion + custom_context"
      contains: "audiences_weights_sum_check"
  key_links:
    - from: "supabase/migrations/20260627000000_audience_general.sql"
      to: "public.audiences"
      via: "ALTER TABLE ... ADD COLUMN IF NOT EXISTS"
      pattern: "ADD COLUMN IF NOT EXISTS mode"
---

<objective>
Generalize the `audiences` schema along the new `mode` axis with an additive migration (D-04), and add the `success_criterion` + `custom_context` storage (D-03/D-07) — then apply it to the live DB. Socials rows stay byte-stable: the existing unconditional weight-sum CHECK is dropped and recreated GATED to `mode='socials'` via logical implication, with the 4-weight predicate copied VERBATIM (same `< 0.01` epsilon) so previously-valid Socials rows pass identically.

Purpose: schema invariants belong in the DB; the gated CHECK is the byte-stability guarantee. This migration is foundational — repo, UI, and resolver work read these columns. The [BLOCKING] push is mandatory because build + type checks pass WITHOUT it (types come from config, not the live DB), creating a false-positive verification state.
Output: one additive migration file, applied to the project DB, with existing rows verified `mode='socials'`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-general-population-honesty-layer/03-CONTEXT.md
@.planning/phases/03-general-population-honesty-layer/03-RESEARCH.md
@supabase/migrations/20260619000000_audiences.sql
@supabase/migrations/20260624000000_audience_signature.sql
@supabase/migrations/20260601000000_add_mode_to_analysis_results.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the additive audience_general migration (D-04/03/07)</name>
  <read_first>
    - supabase/migrations/20260619000000_audiences.sql (lines 45-53 — the VERBATIM 4-weight predicate to copy into the gated branch)
    - supabase/migrations/20260601000000_add_mode_to_analysis_results.sql (the ADD COLUMN ... NOT NULL DEFAULT ... CHECK backfill idiom)
    - supabase/migrations/20260624000000_audience_signature.sql (the ADD COLUMN IF NOT EXISTS additive discipline)
    - .planning/phases/03-general-population-honesty-layer/03-RESEARCH.md (Pattern 1, Pattern 2; Pitfall 4)
    - .planning/phases/03-general-population-honesty-layer/03-PATTERNS.md (§ NEW migration — exact migration body)
  </read_first>
  <action>
    Create `supabase/migrations/20260627000000_audience_general.sql`. Steps in order:
    (1) ADD COLUMN IF NOT EXISTS `mode` text NOT NULL DEFAULT 'socials' CHECK (mode IN ('socials','general')) — the DEFAULT backfills every existing row to 'socials' in the same statement (no separate UPDATE).
    (2) ADD COLUMN IF NOT EXISTS `success_criterion` text, and ADD COLUMN IF NOT EXISTS `custom_context` jsonb NOT NULL DEFAULT '[]'.
    (3) DROP CONSTRAINT IF EXISTS audiences_weights_sum_check.
    (4) Recreate it GATED: ADD CONSTRAINT audiences_weights_sum_check CHECK ( mode <> 'socials' OR ( VERBATIM predicate from 20260619000000_audiences.sql:47-52 ) ) — copy the fyp/niche/loyalist/cross_niche bounds plus the ABS sum-minus-1.0 less-than-0.01 epsilon byte-for-byte (Pitfall 4).
    Add COMMENT ON COLUMN lines for mode/success_criterion/custom_context mirroring the existing comment style. Keep the four weight cols NOT-NULL-with-their-existing-defaults (do NOT drop NOT NULL). Do NOT touch analysis_results, creator_persona_weights, the weight column definitions, or RLS policies (they already cover new columns). Header comment must state: additive, socials byte-stable, mode first-class, never alter weight cols.
  </action>
  <verify>
    <automated>grep -q "mode <> 'socials' OR" supabase/migrations/20260627000000_audience_general.sql; grep -q "ADD COLUMN IF NOT EXISTS mode text NOT NULL DEFAULT 'socials'" supabase/migrations/20260627000000_audience_general.sql; grep -q "custom_context" supabase/migrations/20260627000000_audience_general.sql; echo MIGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260627000000_audience_general.sql` exists.
    - Contains `ADD COLUMN IF NOT EXISTS mode text NOT NULL DEFAULT 'socials'` with `CHECK (mode IN ('socials','general'))`.
    - The recreated `audiences_weights_sum_check` uses the `mode <> 'socials' OR ( ... )` implication form.
    - The 4-weight predicate inside the OR-branch is byte-identical to `20260619000000_audiences.sql:47-52` (same `< 0.01` epsilon, same column bounds) — `grep -F "ABS((fyp + niche + loyalist + cross_niche) - 1.0) < 0.01"` matches in both files.
    - Adds `success_criterion text` and `custom_context jsonb NOT NULL DEFAULT '[]'`.
    - No edit to `analysis_results`, the weight column DEFINITIONS, or RLS (grep shows no `DROP POLICY` / `analysis_results` / `ALTER COLUMN fyp`).
  </acceptance_criteria>
  <done>The additive, mode-gated migration exists with the weight predicate copied verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply the migration to the live DB (supabase db push)</name>
  <read_first>
    - supabase/migrations/20260627000000_audience_general.sql (the file written in Task 1)
    - .planning/STATE.md (worktree + Supabase context)
  </read_first>
  <action>
    Apply the schema change to the project DB. Ensure `SUPABASE_ACCESS_TOKEN` is exported (non-TTY workaround), then run `supabase db push`. This is BLOCKING: it must run AFTER the migration file is finalized and BEFORE phase verification, because tsc/build pass without it (types come from config, not the live DB) — skipping it produces a false-positive green. After the push, run a read-back query confirming existing rows are `mode='socials'` and still satisfy the gated CHECK, e.g. `supabase db query "select mode, count(*) from public.audiences group by mode;"` (or the equivalent read via the Supabase SQL endpoint). If `supabase db push` requires an interactive prompt that cannot be suppressed, STOP and surface it at the checkpoint below for manual application. Do NOT regenerate `database.types.ts` here — the repo uses the `(supabase as any)` cast convention until a later regen (RESEARCH Runtime State Inventory).
  </action>
  <verify>
    <automated>supabase migration list 2>/dev/null | grep -q 20260627000000 || echo "PUSH_PENDING — verify at checkpoint"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completed (or was applied manually) — the `20260627000000` migration appears as applied in `supabase migration list`.
    - A read-back shows existing audience rows are `mode='socials'` (no row defaulted to 'general').
    - No row violates the recreated gated CHECK (push did not error on ADD CONSTRAINT).
  </acceptance_criteria>
  <done>The migration is live on the project DB and existing Socials rows are intact and byte-stable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The additive audience_general migration, pushed to the live Supabase project.</what-built>
  <how-to-verify>
    1. Confirm `supabase migration list` shows `20260627000000_audience_general` as applied (Remote column populated).
    2. In the Supabase SQL editor (or `supabase db query`), run: `select mode, count(*) from public.audiences group by mode;` — confirm all existing rows are `mode='socials'`.
    3. Run: `select conname from pg_constraint where conname = 'audiences_weights_sum_check';` — confirm the gated constraint exists.
    4. Confirm `success_criterion` and `custom_context` columns exist: `select column_name from information_schema.columns where table_name='audiences' and column_name in ('mode','success_criterion','custom_context');` returns all three.
  </how-to-verify>
  <acceptance_criteria>
    - Migration applied remotely; all pre-existing rows `mode='socials'`; the three new columns present; gated CHECK present.
  </acceptance_criteria>
  <resume-signal>Type "approved — migration applied, rows socials-stable" or report the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → Postgres (Supabase) | DDL applied to the shared project DB; `SUPABASE_ACCESS_TOKEN` authorizes the push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering / Denial of Service | Migration breaks Socials byte-stability or fails on existing rows | mitigate | Gated CHECK copies the weight predicate verbatim (same epsilon); ADD COLUMN IF NOT EXISTS + DEFAULT backfills in-place; existing rows are known-clean (A3) so the validating ADD CONSTRAINT passes without scan violation |
| T-03-05 | Elevation of Privilege | New columns escape RLS | accept | Existing `audiences_all_own` / `audiences_select_own` RLS is row-level — new columns inherit it; no new policy needed (RESEARCH V4) |
| T-03-06 | Information Disclosure | `SUPABASE_ACCESS_TOKEN` leakage | mitigate | Token read from env only, never committed/logged; push runs locally |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs this plan |
</threat_model>

<verification>
- Migration file matches the additive + gated-CHECK + verbatim-predicate contract (grep gates above).
- `supabase migration list` shows the migration applied remotely.
- Read-back confirms existing rows `mode='socials'` and the three new columns exist.
</verification>

<success_criteria>
The audiences table carries `mode` (gated weight CHECK), `success_criterion`, and `custom_context` on the live DB; existing Socials audiences are byte-stable and unmigrated to general.
</success_criteria>

## Artifacts this plan produces

- `supabase/migrations/20260627000000_audience_general.sql` (NEW) — `mode` column, mode-gated `audiences_weights_sum_check`, `success_criterion` column, `custom_context` column

<output>
Create `.planning/phases/03-general-population-honesty-layer/03-03-SUMMARY.md` when done — record the `supabase migration list` applied state + the row-count read-back.
</output>
