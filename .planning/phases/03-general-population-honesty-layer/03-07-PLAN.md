---
phase: 03-general-population-honesty-layer
plan: 07
type: execute
wave: 4
depends_on: ["03-02", "03-05"]
files_modified:
  - src/lib/tools/blocks.ts
  - src/components/thread/multi-audience-read-block.tsx
autonomous: true
requirements: [TRUST-01]
must_haves:
  truths:
    - "The run/result Read card shows a Validated vs Directional badge for the active audience"
    - "The badge is presentation-only and derives from the audience tier — no new run path is introduced"
  artifacts:
    - path: "src/lib/tools/blocks.ts"
      provides: "Additive presentation-only tier/mode field on the multi-audience-read result block"
    - path: "src/components/thread/multi-audience-read-block.tsx"
      provides: "TrustBadge mount on the run/result card"
  key_links:
    - from: "src/components/thread/multi-audience-read-block.tsx"
      to: "src/components/audience/trust-badge.tsx"
      via: "TrustBadge tier={...}"
      pattern: "TrustBadge"
---

<objective>
Satisfy the "each run" half of TRUST-01 (D-06, RESEARCH A5): surface the Validated/Directional badge on the run/result card. Add a small additive, presentation-only `tier` field to the `MultiAudienceReadBlock` schema (the active run-result block) and mount `TrustBadge` on its renderer.

Purpose: TRUST-01 requires the badge on "each audience AND each run." The only live run path in P3 is the existing Socials Read; the badge rides ON the result block (mirroring the `BandBlock` model-tag provenance idiom) so it survives scroll-away. No new run path (D-02) — this is presentation-only plumbing.
Output: additive block field + badge mount on the Read card.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-general-population-honesty-layer/03-CONTEXT.md
@.planning/phases/03-general-population-honesty-layer/03-PATTERNS.md
@src/lib/tools/blocks.ts
@src/components/thread/multi-audience-read-block.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an additive presentation-only tier field to the run-result block (A5)</name>
  <read_first>
    - src/lib/tools/blocks.ts (BandBlockSchema 34-43 — the model-tag provenance idiom; MultiAudienceReadBlockSchema 338-364 — note the per-entry `.strict()`; RemixCardBlock.audienceName 242-245)
    - .planning/phases/03-general-population-honesty-layer/03-PATTERNS.md (§ blocks.ts — A5)
    - src/lib/audience/resolve-tier.ts (TrustTier)
  </read_first>
  <action>
    In `MultiAudienceReadBlockSchema`, add a TOP-LEVEL optional presentation field on `props` (alongside `model`): `tier: z.enum(["Validated","Directional"]).optional()`. CRITICAL: add it on `props`, NOT inside the per-audience entry object — that entry is `.strict()` and would reject the extra key (and would also conflate per-audience with run-level). Add a comment that `tier` is the run-level honesty badge (TRUST-01), presentation-only, derived from the active audience's resolved tier upstream — it carries NO score and is additive (older payloads omit it). Do NOT change the band/fraction honesty spine, the `.strict()` entries, or the `model` provenance tag. If the upstream emitter that builds this block already has the active audience in scope, set `tier` from `resolveTier(activeAudience)`; if not, leave it optional and let the renderer fall back (Task 2).
  </action>
  <verify>
    <automated>! npx tsc --noEmit 2>&1 | grep -E 'src/lib/tools/blocks\.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `MultiAudienceReadBlockSchema.props` includes `tier: z.enum(["Validated","Directional"]).optional()` at the TOP level (not inside the `.strict()` audience entry).
    - The per-audience entry retains `.strict()` (grep still shows `.strict()`).
    - No score/band field added; `model: z.literal("sim1-flash")` unchanged.
    - No NEW tsc errors on the touched file: `! npx tsc --noEmit 2>&1 | grep -E 'src/lib/tools/blocks\.ts'` exits 0 (repo tsc baseline is non-zero — gate on no-new-errors on OUR path, not a clean full-repo run).
  </acceptance_criteria>
  <done>The run-result block carries an optional, presentation-only tier field without breaking the bands-only honesty spine.</done>
</task>

<task type="auto">
  <name>Task 2: Mount TrustBadge on the Read result card (TRUST-01 "each run")</name>
  <read_first>
    - src/components/thread/multi-audience-read-block.tsx (props + header render 34-70)
    - src/components/audience/trust-badge.tsx (TrustBadge from 03-05)
    - src/lib/audience/resolve-tier.ts (resolveTier / TrustTier)
  </read_first>
  <action>
    In `multi-audience-read-block.tsx`, import `TrustBadge`. Render `<TrustBadge tier={block.props.tier ?? "Directional"} />` in the card header region (near the model/provenance area), so every Read shows its trust tier. The fallback to "Directional" is the honest default when the upstream emitter has not set `tier` (never silently imply Validated). Keep the existing band/glyph/Lever layout and color discipline intact (no coral added beyond the existing reserved usage). Presentation-only — do NOT add a run path, fetch, or scorer call.
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/app/api/tools/read/__tests__/route.test.ts</automated>
    <automated>! npx tsc --noEmit 2>&1 | grep -E 'src/lib/tools/blocks\.ts|src/components/thread/multi-audience-read-block\.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TrustBadge" src/components/thread/multi-audience-read-block.tsx` shows the import + mount.
    - The badge reads `block.props.tier` and falls back to `"Directional"` (never defaults to "Validated").
    - No new fetch/run/scorer call added (grep shows no new network/scoring import).
    - `node ./node_modules/vitest/vitest.mjs run src/app/api/tools/read/__tests__/route.test.ts` stays green (block schema still validates), and no NEW tsc errors on the touched files: `! npx tsc --noEmit 2>&1 | grep -E 'src/lib/tools/blocks\.ts|src/components/thread/multi-audience-read-block\.tsx'` exits 0.
  </acceptance_criteria>
  <done>Every Read result card wears a Validated/Directional badge, honestly defaulting to Directional when unknown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM/tool-emitted block → Zod validation → render | Result-block payload validated before render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Spoofing / Information Disclosure | A run mis-badged Validated when its audience is Directional | mitigate | `tier` enum is constrained to Validated/Directional; renderer falls back to "Directional" (never Validated) when absent; upstream sets it from `resolveTier` (locked rule) |
| T-03-16 | Tampering | Smuggling a score into the bands-only block via the new field | mitigate | `tier` is a 2-value enum (no numeric); per-audience entries keep `.strict()`; honesty spine unchanged |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs this plan |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/app/api/tools/read/__tests__/route.test.ts` green (block schema accepts the additive field) — the primary behavioral gate.
- No NEW tsc errors on the touched files: `! npx tsc --noEmit 2>&1 | grep -E 'src/lib/tools/blocks\.ts|src/components/thread/multi-audience-read-block\.tsx'` exits 0 (do NOT gate on a clean full-repo `tsc --noEmit` — baseline is non-zero).
</verification>

<success_criteria>
Every run/result Read card surfaces a Validated vs Directional badge derived from the audience tier, presentation-only, with no new run path.
</success_criteria>

## Artifacts this plan produces

- `props.tier` additive enum field on `MultiAudienceReadBlockSchema`
- `TrustBadge` mount on `multi-audience-read-block.tsx`

<output>
Create `.planning/phases/03-general-population-honesty-layer/03-07-SUMMARY.md` when done.
</output>
</content>
