---
phase: 03-general-population-honesty-layer
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - src/lib/audience/audience-types.ts
  - src/lib/audience/resolve-tier.ts
  - src/lib/audience/__tests__/resolve-tier.test.ts
autonomous: true
requirements: [POP-01, POP-02, TRUST-01]
must_haves:
  truths:
    - "The Audience domain object carries a first-class `mode: 'socials' | 'general'` axis"
    - "An audience exposes an optional editable `success_criterion` and a `custom_context[]`"
    - "resolveTier(audience) returns Validated for socials (pack-calibrated) and Directional for general / no-pack, never Validated for general"
  artifacts:
    - path: "src/lib/audience/audience-types.ts"
      provides: "Audience.mode, Audience.success_criterion?, Audience.custom_context?, CustomContext interface"
      contains: "interface CustomContext"
    - path: "src/lib/audience/resolve-tier.ts"
      provides: "tierFromCalibration + resolveTier (TrustTier)"
      exports: ["resolveTier", "tierFromCalibration", "TrustTier"]
    - path: "src/lib/audience/__tests__/resolve-tier.test.ts"
      provides: "Tier truth table (socials→Validated; general/undefined/{}/{baselineRef:''}→Directional)"
  key_links:
    - from: "src/lib/audience/resolve-tier.ts"
      to: "src/lib/engine/packs/socials.ts"
      via: "SOCIALS_PACK.calibration.baselineRef"
      pattern: "SOCIALS_PACK.calibration"
---

<objective>
Lay the domain-model foundation for the General population + honesty layer: add the first-class `mode` axis, the editable `success_criterion`, and the top-level `custom_context[]` to the `Audience` type (D-04/D-03/D-07), and productionize the spike-proven tier RULE into a real `resolveTier` resolver + truth-table test (D-06). Pure TypeScript — no DB, no UI. Every downstream plan (repo, UI, run badge) imports these.

Purpose: interface-first ordering — define the contracts once so the repo + components build against them without a scavenger hunt. The resolver keys off `DomainPack.calibration` (the pack), NEVER `Audience.calibration` (scrape provenance) — the locked rule from the spike.
Output: extended `Audience` type + `CustomContext` + `resolve-tier.ts` + its test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-general-population-honesty-layer/03-CONTEXT.md
@.planning/phases/03-general-population-honesty-layer/03-PATTERNS.md
@src/lib/audience/audience-types.ts
@src/lib/audience/__tests__/signature-determinism.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mode + success_criterion + custom_context + CustomContext to Audience (D-04/03/07)</name>
  <read_first>
    - src/lib/audience/audience-types.ts (Audience interface lines 184-236; CalibratedPersona 63-76; SignatureProvenance 151-158 — the additive-optional idiom at 219/226)
    - .planning/phases/03-general-population-honesty-layer/03-CONTEXT.md (D-04 first-class mode; D-07 custom_context; D-03 success_criterion)
    - .planning/phases/03-general-population-honesty-layer/03-RESEARCH.md (Pitfall 2 — custom_context is TOP-LEVEL, not nested in SignatureProvenance)
  </read_first>
  <behavior>
    - `Audience` has a required `mode` field typed `"socials" | "general"`.
    - `Audience` has optional `success_criterion?: string | null` and `custom_context?: CustomContext[] | null` (additive-optional so every existing Audience literal stays valid).
    - `CustomContext` is exported with `source: "user"`, `note: string`, optional `persona_evidence_link?: string`.
    - `custom_context` is a TOP-LEVEL Audience field (NOT added to `SignatureProvenance`) — works when `signature` is null.
  </behavior>
  <action>
    In `audience-types.ts`, add a new exported `interface CustomContext` ( `source: "user"`, `note: string`, `persona_evidence_link?: string` ) above the `Audience` interface. Add three fields to `Audience`: `mode: "socials" | "general"` (required — first-class axis per D-04, with a doc comment asserting it is NOT derived from `is_general`), `success_criterion?: string | null` (free-text "what good means" per D-03), `custom_context?: CustomContext[] | null` (top-level per D-07 — comment that it is conceptually provenance-level but physically top-level so it survives `signature: null`, citing Pitfall 2). Do NOT add `custom_context` to `SignatureProvenance`. Do NOT widen `DomainPack`. The two new optional fields mirror the existing `creator_persona?`/`signature?` additive guardrail idiom (lines 219/226).
  </action>
  <verify>
    <automated>! npx tsc --noEmit 2>&1 | grep -E 'src/lib/audience/(audience-types|resolve-tier)\.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mode:" src/lib/audience/audience-types.ts` shows `mode: "socials" | "general"` on Audience.
    - `grep -c "interface CustomContext" src/lib/audience/audience-types.ts` == 1.
    - `grep -n "custom_context" src/lib/audience/audience-types.ts` shows it on `Audience`, and `grep -n "custom_context" ...` does NOT appear inside the `SignatureProvenance` interface body (top-level only — Pitfall 2).
    - No NEW tsc errors in the touched files: `! npx tsc --noEmit 2>&1 | grep -E 'src/lib/audience/(audience-types|resolve-tier)\.ts'` exits 0 (the repo tsc baseline is non-zero — earnings-chart.tsx / hooks-runner.test.ts — so this asserts no new error on OUR paths, not a clean full-repo run).
  </acceptance_criteria>
  <done>Audience carries mode + success_criterion + custom_context; CustomContext is exported; types compile with no new errors on the touched files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote resolveTier + write the truth-table test (D-06 / TRUST-01)</name>
  <read_first>
    - src/lib/audience/__tests__/signature-determinism.test.ts (lines 105-126 — the test-local predicate + truth table to promote verbatim)
    - src/lib/engine/packs/socials.ts (lines 67-70 — calibration.baselineRef the socials branch reads)
    - .planning/phases/03-general-population-honesty-layer/03-PATTERNS.md (§ resolve-tier.ts — production shape)
  </read_first>
  <behavior>
    - `tierFromCalibration({baselineRef:"..."})` → "Validated"; `tierFromCalibration(undefined | {} | {baselineRef:""})` → "Directional".
    - `resolveTier({mode:"socials"})` → "Validated" (reads SOCIALS_PACK.calibration).
    - `resolveTier({mode:"general"})` → "Directional", and is never "Validated".
  </behavior>
  <action>
    Create `src/lib/audience/resolve-tier.ts` exporting `type TrustTier = "Validated" | "Directional"`, `tierFromCalibration(calibration?: { baselineRef?: string }): TrustTier` (returns "Validated" iff `calibration?.baselineRef` is truthy, the verbatim spike predicate), and `resolveTier(audience: Pick<Audience, "mode">): TrustTier` that returns `tierFromCalibration(SOCIALS_PACK.calibration)` when `audience.mode === "socials"` else `"Directional"`. Import `Audience` type-only from `./audience-types` and `SOCIALS_PACK` from `@/lib/engine/packs/socials`. Add a comment: no General pack exists in P3 (D-02) → general resolves Directional by rule, never Validated; do NOT widen DomainPack. Create `src/lib/audience/__tests__/resolve-tier.test.ts` porting the truth table from `signature-determinism.test.ts:112-126` plus the `resolveTier({mode})` cases. Use the runner-quirk header (run via `node ./node_modules/vitest/vitest.mjs run`).
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/audience/__tests__/resolve-tier.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `resolve-tier.ts` exports `resolveTier`, `tierFromCalibration`, `TrustTier`.
    - `resolve-tier.ts` references `SOCIALS_PACK.calibration` (grep) and does NOT reference `audience.calibration` (must key off the pack, not scrape provenance).
    - The test asserts: socials→Validated; general→Directional; `resolveTier({mode:"general"})` !== "Validated"; `tierFromCalibration(undefined|{}|{baselineRef:""})`→Directional.
    - `node ./node_modules/vitest/vitest.mjs run src/lib/audience/__tests__/resolve-tier.test.ts` passes.
  </acceptance_criteria>
  <done>resolveTier is a production export with a green truth-table test that locks the never-Validated-for-general rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure type + pure-predicate module; no I/O, no request boundary, no persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | `resolveTier` mis-tiering a general audience as Validated | mitigate | Truth-table test asserts `resolveTier({mode:"general"}) !== "Validated"`; resolver has no path to Validated for non-socials |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs this plan |
</threat_model>

<verification>
- No NEW tsc errors on the touched files: `! npx tsc --noEmit 2>&1 | grep -E 'src/lib/audience/(audience-types|resolve-tier)\.ts'` exits 0 (the repo tsc baseline is non-zero by design — do NOT gate on a clean full-repo `tsc --noEmit`).
- `node ./node_modules/vitest/vitest.mjs run src/lib/audience` green (resolver + kept determinism gate) — the primary behavioral gate.
</verification>

<success_criteria>
The Audience contract carries mode/success_criterion/custom_context, CustomContext is defined, and resolveTier productionizes the locked tier rule with a passing truth table.
</success_criteria>

## Artifacts this plan produces

- `Audience.mode`, `Audience.success_criterion`, `Audience.custom_context` (new fields on existing interface)
- `CustomContext` interface (NEW)
- `src/lib/audience/resolve-tier.ts` (NEW) — `resolveTier`, `tierFromCalibration`, `TrustTier`
- `src/lib/audience/__tests__/resolve-tier.test.ts` (NEW)

<output>
Create `.planning/phases/03-general-population-honesty-layer/03-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
