---
phase: 12-accuracy-benchmark-acceptance-gate
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - src/lib/engine/version.ts
autonomous: false
requirements:
  - BENCH-02
  - BENCH-03
  - BENCH-04
  - BENCH-06

must_haves:
  truths:
    - "Benchmark results compared against all acceptance gates (BENCH-02..06) with PASS/FAIL disposition"
    - "BENCH-06 hard gate enforced: any negative signal contribution blocks milestone"
    - "ENGINE_VERSION flipped from '3.0.0-dev' to '3.0.0' ONLY if gates pass"
    - "Summary markdown report generated with all metrics, gates, and decision"
    - "User explicitly reviews and approves before merge"
  artifacts:
    - path: "src/lib/engine/version.ts"
      provides: "Engine version constant — flipped to 3.0.0 after gates pass"
      contains: "3.0.0"
    - path: ".planning/baseline-report-v3.md"
      provides: "Human-readable summary of benchmark results, gate dispositions, and go/no-go decision"
      contains: "macro_f1, ECE, per-signal contribution"
  key_links:
    - from: "src/lib/engine/version.ts"
      to: "global ENGINE_VERSION constant"
      via: "export const ENGINE_VERSION = \"3.0.0\""
      pattern: "ENGINE_VERSION"
    - from: ".planning/research/*.json"
      to: ".planning/baseline-report-v3.md"
      via: "Summary markdown reads JSON reports and computes gate dispositions"
      pattern: "macro_f1"
---

<objective>
Evaluate benchmark results against acceptance gates, flip ENGINE_VERSION, and present final report for user sign-off.

Purpose: D-03 flips ENGINE_VERSION only after gates pass. D-04/D-05 define gate behavior (user decides near-miss, hard block on negative signal). D-06/D-07 define minimal sign-off process.

Output: Flipped ENGINE_VERSION (if gates pass), summary markdown report, user sign-off checkpoint.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/v2.1-baseline.md
@.planning/phases/12-accuracy-benchmark-acceptance-gate/12-CONTEXT.md
@.planning/phases/12-accuracy-benchmark-acceptance-gate/12-RESEARCH.md

<interfaces>
From src/lib/engine/corpus/eval-config.ts:
```typescript
export function requiredImprovementFor(baselineMacroF1: number): number {
  if (baselineMacroF1 <= 0.4) return 0.15;
  if (baselineMacroF1 <= 0.55) return 0.1;
  return 0.07;
}
```

From src/lib/engine/version.ts:
```typescript
export const ENGINE_VERSION = "3.0.0-dev";
// Phase 12 acceptance gate flips to "3.0.0" with one-line edit
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Compute gate dispositions and generate summary report</name>
  <files>.planning/baseline-report-v3.md (created)</files>
  <action>
    Read the benchmark report(s) from Plan 02 and evaluate each acceptance gate.

    **A. Determine which report to use:**
    - If `.planning/research/full-v3.json` exists (full run completed): use this
    - Otherwise: use `.planning/research/smoke-v3.json` (partial — note "smoke-only" in report)

    **B. Read the v2.1 rebaseline:**
    Read `.planning/research/v2.1-rebaseline.json` for the apples-to-apples baseline.

    **C. Compute target threshold:**
    ```javascript
    const rebaselineMacroF1 = v2rebaseline.macro_f1;  // e.g., 0.294
    const improvement = requiredImprovementFor(rebaselineMacroF1); // 0.15 if <= 0.4
    const targetMacroF1 = Math.round(rebaselineMacroF1 * (1 + improvement) * 10000) / 10000;
    ```

    **D. Evaluate each gate:**

    | Gate | Check | Disposition |
    |------|-------|-------------|
    | BENCH-02 | `v3Report.macro_f1 >= targetMacroF1` | PASS/FAIL |
    | BENCH-02 (per-niche) | Every niche: `v3Report.per_niche_f1[n] >= rebaselineReport.per_niche_f1[n] - 0.05` | PASS/FAIL per niche |
    | BENCH-03 | `v3Report.cost_cents_avg <= 7.5` ($0.075) | PASS/FAIL |
    | BENCH-04 | `v3Report.ece < v2rebaseline.ece` | PASS/FAIL |
    | BENCH-06 | All `v3Report.signal_contribution[signal] > 0` | PASS/FAIL — list which signals negative |

    **E. Generate summary markdown** at `.planning/baseline-report-v3.md` with format:

    ```markdown
    # Engine v3 Benchmark Report

    **Date:** {run_at from report}
    **Engine version:** 3.0.0-dev (pre-flip)
    **Corpus:** full.2026-05-11 ({rows_processed} rows)
    **Run type:** {full / smoke-only}

    ## Headline Numbers

    | Metric | v2.1 (rebaseline) | v3 | Gate Target | Status |
    |--------|-------------------|-----|-------------|--------|
    | macro_f1 | {v2} | {v3} | ≥{target} | {PASS/FAIL} |
    | ECE | {v2.ece} | {v3.ece} | <{v2.ece} | {PASS/FAIL} |
    | Cost avg (¢) | {v2.cost} | {v3.cost} | ≤7.5¢ | {PASS/FAIL} |
    | Viral recall | {v2.viral_recall} | {v3.viral_recall} | — | — |
    | Under precision | {v2.under_precision} | {v3.under_precision} | — | — |

    ## Per-Niche F1

    | Niche | v2.1 | v3 | Regression Gate (±5pp) | Status |
    |-------|------|-----|-----------------------|--------|
    | {niche} | {v2} | {v3} | ≥{v2 - 0.05} | {PASS/FAIL} |

    ## Signal Contribution (Leave-One-Out)

    | Signal | Contribution | Status |
    |--------|-------------|--------|
    | {signal} | {delta} | {POSITIVE / NEGATIVE — BLOCKING} |

    ## Gate Summary

    - BENCH-02 (macro_f1 ≥{target}): {PASS/NEAR-MISS/FAIL}
    - BENCH-03 (cost ≤$0.075): {PASS/FAIL}
    - BENCH-04 (ECE reduction): {PASS/FAIL}
    - BENCH-06 (all signals positive): {PASS/FAIL — per D-05 hard gate}
    - Overall: {PASS / FAIL / NEAR-MISS}
    ```

    **F. If any signal has negative contribution (BENCH-06):** Per D-05, this is a HARD GATE. The milestone blocks. List the offending signal(s) and note they must be disabled or fixed.

    **G. Check the v2.1 rebaseline vs original baseline:**
    Compare `.planning/research/v2.1-rebaseline.json` values with the original `.planning/research/v2.1-baseline.md` values. If they differ meaningfully (e.g., macro_f1 change > 0.01), note the drift in the summary.
  </action>
  <verify>
    <automated>test -f .planning/baseline-report-v3.md && grep -q "macro_f1" .planning/baseline-report-v3.md && grep -q "Signal Contribution" .planning/baseline-report-v3.md && echo "Report generated"</automated>
  </verify>
  <done>
    "Summary markdown generated at .planning/baseline-report-v3.md with all gate dispositions"
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Flip ENGINE_VERSION (conditionally)</name>
  <files>src/lib/engine/version.ts</files>
  <action>
    **Only flip if ALL of the following are true:**
    1. BENCH-02 pass OR near-miss accepted by user (D-04)
    2. BENCH-03 pass (cost ≤ $0.075)
    3. BENCH-04 pass (ECE reduced vs rebaseline)
    4. BENCH-06 pass — NO negative signal contributions (D-05 hard gate)

    **If all gates pass:**
    Edit `src/lib/engine/version.ts`:
    - Change `export const ENGINE_VERSION = "3.0.0-dev";`
    - To: `export const ENGINE_VERSION = "3.0.0";`
    - Do NOT change any other file

    **If near-miss (D-04 scenario):**
    Do NOT flip yet — defer to user decision in Task 3. The summary report must indicate "NEAR-MISS" status.

    **If hard gate blocks (BENCH-06 negative signal or BENCH-03 non-compliant):**
    Do NOT flip. Document in summary. Proceed to Task 3 for user review.

    **Verify the flip:**
    ```bash
    node -e "const v = require('./src/lib/engine/version'); console.log(v.ENGINE_VERSION);"
    ```
    Expected: `3.0.0` (if flipped) or `3.0.0-dev` (if blocked).

    Run the test suite to confirm the version change doesn't break anything:
    ```bash
    npm test
    ```
    Expected: all tests pass (ENGINE_VERSION is imported but tests reference `version.ts` directly or use the constant — no behavioral change from the flip).
  </action>
  <verify>
    <automated>node -e "const v = require('./src/lib/engine/version'); const expected = v.ENGINE_VERSION === '3.0.0' || v.ENGINE_VERSION === '3.0.0-dev'; console.assert(expected, 'version should be 3.0.0 or 3.0.0-dev: ' + v.ENGINE_VERSION); console.log('ENGINE_VERSION = ' + v.ENGINE_VERSION);"</automated>
  </verify>
  <done>
    "ENGINE_VERSION flipped to 3.0.0 (if gates passed) or documented why it wasn't flipped"
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Present final report for user sign-off</name>
  <what-built>
    Benchmark summary at `.planning/baseline-report-v3.md`. ENGINE_VERSION state: `{3.0.0 / 3.0.0-dev}`.
  </what-built>
  <how-to-verify>
    Present the full summary to the user. Include:

    1. **Overall verdict:** {PASS / FAIL / NEAR-MISS}
    2. **Key metrics comparison** (v2.1 rebaseline vs v3):
       - macro_f1: {v2} → {v3} (target: ≥{target})
       - ECE: {v2} → {v3}
       - Cost avg: {v2}¢ → {v3}¢ (target: ≤7.5¢)
    3. **Signal contributions:**
       - {list each signal's contribution delta}
       - {If any negative: flagged per D-05 hard gate}
    4. **BENCH-06 assessment:** {all positive / negative signal(s) found — milestone blocks}
    5. **ENGINE_VERSION:** {flipped to 3.0.0 / remains 3.0.0-dev}

    **If ALL gates pass:**
    ```
    All acceptance gates pass. ENGINE_VERSION flipped to 3.0.0.
    Ready for milestone merge.
    Please review the report at .planning/baseline-report-v3.md and confirm:
    - [ ] Approve the benchmark results
    - [ ] Confirm milestone/engine-foundation is ready to merge
    ```

    **If near-miss (per D-04):**
    ```
    Benchmark narrowly misses target (macro_f1 = {actual} vs target ≥{target}).
    Per D-04, you decide:
    - [ ] Soft gate — accept with note, flip to 3.0.0, ship
    - [ ] Hard gate — block milestone, do not flip
    ```

    **If hard gate blocks (D-05):**
    ```
    BENCH-06: negative signal contribution detected ({signal}: {delta}).
    Per D-05, this is a hard block. Milestone cannot ship until resolved.
    Options:
    1. Disable the offending signal and re-run benchmark
    2. Fix the signal and re-run
    3. Override (not recommended — D-05 is non-negotiable)
    ```
  </how-to-verify>
  <resume-signal>User provides explicit approval or instructions. "Approved" or specific next steps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Benchmark report → summary generation | Summary reads JSON report files — trust boundary is file system read |
| User approval → ENGINE_VERSION flip | Human confirms version change — checkpoint gates this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | I | ENGINE_VERSION constant | mitigate | Single-line edit, verified by node import check + test suite |
| T-12-08 | E | Gate computation | mitigate | All comparisons are computed from JSON reports — re-runnable and auditable |
| T-12-09 | D | User sign-off | mitigate | Checkpoint requires explicit user approval (D-07) |
</threat_model>

<verification>
1. `.planning/baseline-report-v3.md` exists with gate dispositions for all BENCH requirements
2. `ENGINE_VERSION` is either `"3.0.0"` or `"3.0.0-dev"` — correct per gate outcome
3. `npm test` passes (regression check after version flip)
4. User has reviewed and explicitly approved or given instructions
</verification>

<success_criteria>
- Benchmark summary markdown generated with all metrics and gate dispositions
- ENGINE_VERSION flipped to 3.0.0 (if all gates pass) or documented decision
- User explicitly reviews and approves/denies milestone readiness
- Ready to commit: updated `src/lib/engine/version.ts` + `.planning/baseline-report-v3.md`
</success_criteria>

<output>
After completion, create `.planning/phases/12-accuracy-benchmark-acceptance-gate/12-03-SUMMARY.md`
</output>
