---
phase: 12-accuracy-benchmark-acceptance-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - BENCH-04
  - BENCH-05

must_haves:
  truths:
    - "platt_parameters table has ≥1 row with non-null a, b, sample_count after training"
    - "All 1191 existing tests pass with zero failures"
    - "Phase 11 human-verify items acknowledged as acceptable for Phase 12 start"
  artifacts:
    - path: "src/lib/engine/corpus/cli/train-platt.ts"
      provides: "Platt training CLI — executed against full.2026-05-11 corpus"
      contains: "runEvalOverCorpus"
    - path: ""
      provides: "platt_parameters row in Supabase with fitted A/B params"
      contains: "a, b, fitted_at, sample_count"
  key_links:
    - from: "supabase/migrations/20260520000000_phase10_platt_parameters.sql"
      to: "platt_parameters (Supabase table)"
      via: "train-platt.ts writes row via supabase.from('platt_parameters').insert()"
      pattern: "platt_parameters"
---

<objective>
Train Platt calibration parameters on the corpus and confirm the codebase is ready for benchmark execution.

Purpose: BENCH-04 requires calibration loss reduction vs v2.1 baseline (ECE = 0.3715). Platt scaling params must be fitted first. BENCH-05 requires zero regressions — confirm the test baseline before benchmark runs.

Output: Trained Platt parameters in `platt_parameters` table. Confirmed test suite passes. Phase 11 state verified acceptable.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/v2.1-baseline.md
@.planning/phases/12-accuracy-benchmark-acceptance-gate/12-CONTEXT.md
@.planning/phases/12-accuracy-benchmark-acceptance-gate/12-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Train Platt calibration parameters</name>
  <files>src/lib/engine/calibration.ts (read-only); platt_parameters table in Supabase</files>
  <action>
    Run the Platt training CLI to fit and persist calibration parameters:

    ```bash
    npx tsx src/lib/engine/corpus/cli/train-platt.ts --version full.2026-05-11
    ```

    The CLI:
    1. Runs `runEvalOverCorpus` over the full `full.2026-05-11` corpus to collect predicted vs actual score pairs
    2. Fits Platt scaling (gradient descent on cross-entropy loss) via `fitPlattScaling()`
    3. Inserts a row into `platt_parameters` table with (a, b, fitted_at, sample_count)

    Expected output: "Fitted Platt parameters: a={value}, b={value}, samples={count}" followed by "Inserted row {id} into platt_parameters"

    If dry-run mode exists (`--dry-run`), run once in dry-run to see params before persisting. Then run without dry-run.

    **If CLI errors**: resolve before proceeding. Common issues:
    - Missing `.env.local` with `SUPABASE_SERVICE_ROLE_KEY` — copy from existing scripts pattern
    - Corpus version `full.2026-05-11` doesn't exist in thresholds — verify in `thresholds.ts`
    </action>
  <verify>
    <automated>npx tsx -e "
    const { createServiceClient } = require('./src/lib/supabase/service');
    const supabase = createServiceClient();
    supabase.from('platt_parameters').select('*').order('created_at', { ascending: false }).limit(1).then(({data}) => {
      if (data && data.length > 0 && data[0].a !== null) {
        console.log('PASS: platt_parameters has ' + data[0].sample_count + ' samples, a=' + data[0].a + ', b=' + data[0].b);
        process.exit(0);
      } else {
        console.log('FAIL: no platt_parameters row found');
        process.exit(1);
      }
    });
    "</automated>
  </verify>
  <done>
    "platt_parameters table has ≥1 row with non-null a and b values and sample_count > 0"
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Confirm test suite and Phase 11 readiness</name>
  <files>none</files>
  <action>
    Run two checks:

    **A. Full test suite:**
    ```bash
    npm test
    ```
    Expected: 1191+ tests pass (88 files). 0 failures. If fewer tests pass than the known baseline (1191 as of Phase 11 completion), investigate regressions before proceeding.

    **B. Phase 11 status check:**
    Review the Phase 11 SUMMARY files (11-01 through 11-05) to confirm:
    - All 5 plans completed (verified by SUMMARYs existence)
    - The only remaining items are human-verify visual checks (signal chips, data disclosure, settings toggle, goal recheck banner) which are UI-level and don't block Phase 12 benchmark execution

    Read:
    ```bash
    cat .planning/phases/11-existing-ui-integration-privacy-policy/11-05-SUMMARY.md
    ```

    Note in the plan summary that Phase 11 human-verify items are acknowledged as non-blocking for the benchmark phase. The benchmark operates on the eval pipeline, not the UI layer.

    **If test failures found** — diagnose and fix before proceeding. Known pre-existing issues (like Phase 11 SUMMARY mentions of `src/app/api/analyze/route.test.ts` pre-existing failure) should be documented but are non-blocking if they are pre-existing.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <done>
    "npm test passes with ≥1191 tests, 0 failures. Phase 11 human-verify items acknowledged as non-blocking."
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| train-platt CLI → Supabase | Service role client writes to platt_parameters — requires SUPABASE_SERVICE_ROLE_KEY in .env.local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | E | cli/train-platt.ts | mitigate | Run with --dry-run first to preview params before DB write |
| T-12-02 | I | platt_parameters INSERT | accept | CLI runs as service role — table has no RLS policies for writes (intentional per Phase 10 design) |
</threat_model>

<verification>
1. `platt_parameters` has ≥1 row with non-null a, b — verify via Supabase query
2. `npm test` passes with ≥1191 tests, 0 failures
3. Phase 11 human-verify deferred items documented as non-blocking
</verification>

<success_criteria>
- Platt calibration parameters trained and persisted (BENCH-04 foundation)
- Test suite confirmed green (BENCH-05)
- Phase 11 status acknowledged — benchmark execution can begin
</success_criteria>

<output>
After completion, create `.planning/phases/12-accuracy-benchmark-acceptance-gate/12-01-SUMMARY.md`
</output>
