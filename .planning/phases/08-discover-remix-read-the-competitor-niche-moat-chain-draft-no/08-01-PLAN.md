---
phase: 08-discover-remix-read-the-competitor-niche-moat-chain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/audience/goal-intent.ts
  - src/lib/audience/temperature-disposition.ts
  - src/lib/audience/__tests__/goal-intent.test.ts
  - src/lib/audience/__tests__/temperature-disposition.test.ts
autonomous: true
requirements: [AUD-W0]
must_haves:
  truths:
    - "GOAL_INTENT_BIAS maps each goal intent to a real WEIGHT_PRESETS mix (no [ASSUMED] placeholder), and the default-path mix is unchanged so the engine regression gate stays green"
    - "TEMPERATURE_DISPOSITION assigns every one of the 10 archetypes a meaningful temperature×disposition label, so low-disposition personas can be named for who-it's-NOT-for"
    - "The audience-regression-gate test still passes (ENGINE_VERSION === 3.19.0, General → DEFAULT mix at source 'default')"
  artifacts:
    - path: "src/lib/audience/goal-intent.ts"
      provides: "Tuned GOAL_INTENT_BIAS (real values, [ASSUMED] marker removed)"
      contains: "GOAL_INTENT_BIAS"
    - path: "src/lib/audience/temperature-disposition.ts"
      provides: "Tuned TEMPERATURE_DISPOSITION lens (real values, [ASSUMED] marker removed)"
      contains: "TEMPERATURE_DISPOSITION"
    - path: "src/lib/audience/__tests__/temperature-disposition.test.ts"
      provides: "Assertion that low-disposition archetypes exist and are stable"
  key_links:
    - from: "src/lib/audience/goal-intent.ts"
      to: "src/components/board/audience/audience-constants.ts"
      via: "WEIGHT_PRESETS import"
      pattern: "WEIGHT_PRESETS"
---

<objective>
W0 — close persona-tuning debt (#2): replace the `[ASSUMED]` placeholder values in the deterministic persona-bias tables so the multi-audience compare (W4) renders signal, not noise (D-02). Tuning is editing constants + re-asserting the existing regression gate — no model call, no `ENGINE_VERSION` change.

Purpose: D-02 makes this a HARD prerequisite. With `[ASSUMED]` values, the W4 side-by-side compare would render placeholder noise as signal, and the who-it's-NOT-for line (D-10) would name meaningless segments. This unblocks W4, read-only personas in the UI, and the weak-signal fallback.

Output: Tuned `GOAL_INTENT_BIAS` + `TEMPERATURE_DISPOSITION` tables with `[ASSUMED]` markers removed and tests asserting the values are real and stable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-CONTEXT.md
@.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tune GOAL_INTENT_BIAS — real WEIGHT_PRESETS mapping, default path unchanged</name>
  <read_first>
    - src/lib/audience/goal-intent.ts (the file being modified — carries the [ASSUMED] structure-not-values marker)
    - src/components/board/audience/audience-constants.ts (WEIGHT_PRESETS — the only legal value source; default = {fyp:0.65,niche:0.20,loyalist:0.10,cross_niche:0.05})
    - src/lib/audience/__tests__ (existing test conventions)
    - src/lib/engine/__tests__/audience-regression-gate.test.ts (asserts General → DEFAULT mix at source 'default' + ENGINE_VERSION 3.19.0 — the invariant this task must not break)
    - .planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-RESEARCH.md (§W0 Persona-Tuning Targets, Pitfall 4)
  </read_first>
  <behavior>
    - GOAL_INTENT_BIAS["grow"] resolves to a WEIGHT_PRESETS mix biased to cold-reach (fyp-heavy)
    - GOAL_INTENT_BIAS["sell"] and ["authority"] resolve to niche-leaning mixes (converter/scout)
    - GOAL_INTENT_BIAS["nurture"] resolves to a loyalist-leaning (established) mix
    - Every value is a reference to a WEIGHT_PRESETS preset (no inline-invented mix); each preset sums to 1.0
    - biasForGoalIntent(intent) returns the mapped preset for all four GoalIntent values
  </behavior>
  <action>
    Tune the four GOAL_INTENT_BIAS entries in src/lib/audience/goal-intent.ts to their final values, sourcing every value from WEIGHT_PRESETS (imported from @/components/board/audience/audience-constants) per the §W0 constraint "do NOT invent new mixes." Keep the existing key→preset structure (grow→new_creator, sell/authority→niche_heavy, nurture→established) unless a value change is justified inline; the decision here is to LOCK the values, not the keys. Remove the `[ASSUMED]` / "structure-not-values … tuned in the post-P7 refinement run" comment markers and replace with a one-line rationale per entry. Do NOT touch WEIGHT_PRESETS.default (regression-gate anchor {0.65,0.20,0.10,0.05}). Do NOT change STRONG_THRESHOLD/MIXED_THRESHOLD in flash-aggregate.ts (Pitfall 4 — those are calibrated, not [ASSUMED], and shift every gate). Add/extend src/lib/audience/__tests__/goal-intent.test.ts asserting each intent maps to a preset whose weights sum to 1.0 and that no [ASSUMED] marker remains in the source.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-tools && pnpm test -- goal-intent && grep -L -i 'ASSUMED' src/lib/audience/goal-intent.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- goal-intent` passes
    - `grep -i ASSUMED src/lib/audience/goal-intent.ts` returns no match (marker removed)
    - WEIGHT_PRESETS.default in audience-constants.ts still reads `{ fyp: 0.65, niche: 0.20, loyalist: 0.10, cross_niche: 0.05 }` (unchanged)
  </acceptance_criteria>
  <done>GOAL_INTENT_BIAS carries final tuned values from WEIGHT_PRESETS, the [ASSUMED] marker is gone, and the goal-intent test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tune TEMPERATURE_DISPOSITION lens + re-assert the regression gate</name>
  <read_first>
    - src/lib/audience/temperature-disposition.ts (the file being modified — TEMPERATURE_DISPOSITION carries [ASSUMED per D-02])
    - src/lib/audience/audience-types.ts (Temperature, Disposition, Archetype, CalibratedPersona.disposition — the consumer of this lens)
    - src/lib/engine/__tests__/audience-regression-gate.test.ts (the gate to re-run)
    - .planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-RESEARCH.md (§W0 Persona-Tuning Targets — "tune so low-disposition personas are meaningful"; feeds who-NOT-for D-10 + verbatim grouping D-11)
  </read_first>
  <behavior>
    - TEMPERATURE_DISPOSITION has exactly one entry per Archetype (the _ExhaustiveCheck stays satisfied)
    - At least 2 of the 10 archetypes carry a low/negative disposition (skeptic/scroller-leaning), so who-it's-NOT-for (D-10) can name a real segment
    - At least 2 archetypes carry a hot/positive disposition (loyalist/engaged-leaning)
    - labelForArchetype(archetype) returns the assigned temperature×disposition for every archetype
  </behavior>
  <action>
    Finalize the 10-archetype TEMPERATURE_DISPOSITION map in src/lib/audience/temperature-disposition.ts so the temperature×disposition labels are meaningful presentation values (this is a presentation-only lens — it never enters the scoring path, so it does not touch ENGINE_VERSION). Ensure a spread: some archetypes low-disposition (so who-it's-NOT-for D-10 names a real "scrolls past" segment), some hot (so verbatim grouping D-11 has a "stopped the scroll" cluster). Remove the `[ASSUMED] per D-02` marker; replace with a one-line rationale. Keep the exhaustive `_ExhaustiveCheck` type guard intact. Add/extend src/lib/audience/__tests__/temperature-disposition.test.ts asserting (a) every Archetype has an entry, (b) at least 2 low-disposition + at least 2 hot/positive entries exist. Then run the engine regression gate to confirm the General no-op + ENGINE_VERSION invariant survives this W0 edit.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-tools && pnpm test -- temperature-disposition && pnpm test -- audience-regression-gate && grep -L -i 'ASSUMED' src/lib/audience/temperature-disposition.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- temperature-disposition` passes (exhaustive coverage + low/hot disposition spread asserted)
    - `pnpm test -- audience-regression-gate` passes (ENGINE_VERSION === "3.19.0", General → DEFAULT mix at source 'default')
    - `grep -i ASSUMED src/lib/audience/temperature-disposition.ts` returns no match
  </acceptance_criteria>
  <done>The temperature×disposition lens is final and meaningful, the regression gate is green, and W0 unblocks W4.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01 — W0)

- `GOAL_INTENT_BIAS` (tuned values, `[ASSUMED]` removed) — `src/lib/audience/goal-intent.ts`
- `TEMPERATURE_DISPOSITION` (tuned values, `[ASSUMED]` removed) — `src/lib/audience/temperature-disposition.ts`
- New/extended tests: `src/lib/audience/__tests__/goal-intent.test.ts`, `src/lib/audience/__tests__/temperature-disposition.test.ts`
- No new symbols, no new files in `src/lib/` beyond tests; no migration; `ENGINE_VERSION` unchanged.
</artifacts_produced>

<verification>
- `pnpm test -- goal-intent temperature-disposition audience-regression-gate` all green.
- No `[ASSUMED]` markers remain in either tuned file.
- `WEIGHT_PRESETS.default` and `flash-aggregate.ts` thresholds unchanged.
</verification>

<success_criteria>
W0 complete when the two persona-bias tables carry real values (no `[ASSUMED]`), low-disposition personas are nameable for D-10/D-11, and the engine regression gate confirms the General no-op + `ENGINE_VERSION 3.19.0` are untouched.
</success_criteria>

<output>
Create `.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-01-SUMMARY.md` when done.
</output>
