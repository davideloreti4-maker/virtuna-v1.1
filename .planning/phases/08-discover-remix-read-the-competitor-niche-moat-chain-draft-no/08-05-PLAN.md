---
phase: 08-discover-remix-read-the-competitor-niche-moat-chain
plan: 05
type: execute
wave: 4
depends_on: ["08-01", "08-03", "08-04"]
files_modified:
  - src/lib/tools/blocks.ts
  - src/lib/tools/block-registry.ts
  - src/components/thread/multi-audience-read-block.tsx
  - src/components/thread/message-blocks.tsx
  - src/lib/engine/flash/who-not-for.ts
  - src/lib/engine/flash/__tests__/who-not-for.test.ts
  - src/lib/tools/__tests__/blocks.test.ts
autonomous: true
requirements: [READ-01, READ-03]
must_haves:
  truths:
    - "A new multi-audience-read typed block renders a single-audience concept Read: aggregate verdict line, a grade glyph + one-line interpretation + Lever line, and an expandable per-audience persona drill"
    - "Who-its-NOT-for is derived from low-disposition personas with no extra model call and shown on the single-audience Read (D-10), the cheap presentation piece D-04 keeps in P8"
    - "The Read carries only a band word + fraction + SIM-1 Flash provenance, never a fabricated 0-100 score"
  artifacts:
    - path: "src/components/thread/multi-audience-read-block.tsx"
      provides: "Static Read card renderer (single-audience form), sketch 005 .read spine"
    - path: "src/lib/engine/flash/who-not-for.ts"
      provides: "deriveWhoNotFor(personas) from low-disposition CalibratedPersona (D-10), no model call"
      exports: ["deriveWhoNotFor"]
    - path: "src/lib/tools/blocks.ts"
      provides: "multi-audience-read Zod schema in BlockUnionSchema (bands only, no score)"
      contains: "multi-audience-read"
  key_links:
    - from: "src/components/thread/message-blocks.tsx"
      to: "src/components/thread/multi-audience-read-block.tsx"
      via: "BLOCK_COMPONENTS registration"
      pattern: "multi-audience-read|MultiAudienceRead"
    - from: "src/lib/engine/flash/who-not-for.ts"
      to: "src/lib/audience/audience-types.ts"
      via: "CalibratedPersona.disposition (low-disposition selection)"
      pattern: "disposition"
---

<objective>
W3, the single-audience concept Read landing (READ-01) plus the cheap presentation pieces that fall out of already-emitted data (READ-03: who-its-NOT-for D-10). Per D-04, who-its-NOT-for + the verbatim quote panel are KEPT in P8 (they are cheap, the data already exists), while comment seeding is DEFERRED out of P8, this plan therefore builds who-not-for and leaves comment seeding entirely unplanned. This introduces the new multi-audience-read typed block in its single-audience form, the static-card landing point of every chain BEFORE filming. It follows the LOCKED sketch 005 .read interpret+lever spine but ships a static card only (the live cloud is P9, do not build it here).

Purpose: The Read is the moat, the foresight payoff. W3 lands the Read as a real typed card with a grade glyph, one-line interpretation, Lever, and a who-its-NOT-for line derived from low-disposition personas (now meaningful after W0 tuning).

Output: the multi-audience-read block schema + single-audience renderer, the pure deriveWhoNotFor helper, and registration in the dispatcher. W4 (Plan 06) extends this same block to the 2-audience compare + verbatim wall.

Honesty spine (load-bearing): bands only (Strong/Mixed/Weak) + fraction + model sim1-flash. NEVER a numeric 0-100 score (Pitfall 5). who-its-NOT-for uses already-emitted persona dispositions, no extra model call (D-10, no fabrication risk).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-CONTEXT.md
@.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-RESEARCH.md
@.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-UI-SPEC.md
@.planning/sketches/005-audience-scale/index.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deriveWhoNotFor + multi-audience-read schema (single-audience form, bands only)</name>
  <read_first>
    - src/lib/audience/audience-types.ts (CalibratedPersona {archetype, repaint, temperature, disposition}; Disposition union; the low-disposition source for who-not-for)
    - src/lib/engine/flash/flash-aggregate.ts (aggregateFlash -> {band, fraction}; FlashBand Strong/Mixed/Weak; STRONG_THRESHOLD/MIXED_THRESHOLD, the band math to reuse, do not re-roll)
    - src/lib/engine/flash/flash-schema.ts (FlashPersona {archetype, verdict stop|scroll, quote})
    - src/lib/tools/blocks.ts (file being modified; BandBlockSchema as the honesty template, band + fraction + model sim1-flash, NO score; BlockUnionSchema)
    - src/lib/tools/block-registry.ts (file being modified; validateBlock registration)
    - .planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-UI-SPEC.md (New Blocks B1: verdict row, the Read coral panel with grade glyph + interpretation + Lever, who-its-NOT-for line Scrolls-past-segment, per-audience persona drill; provenance SIM-1 Flash, no 0-100)
    - .planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-RESEARCH.md (Pitfall 5 honesty; D-10 who-not-for from low-disposition, no model call)
  </read_first>
  <behavior>
    - deriveWhoNotFor(personas: CalibratedPersona[]) returns the segment label(s) for the lowest-disposition personas (the scrolls-past segment), computed purely from disposition, no model call
    - The multi-audience-read schema carries, per audience (1 in this plan, up to 2 in W4): an audience name, an aggregate band + fraction (NOT a score), the interpretation line, the Lever line, the who-not-for segment, and the per-persona drill data (archetype + verdict + quote)
    - The schema forbids any numeric 0-100 score field; provenance is model sim1-flash (mirror BandBlockSchema)
  </behavior>
  <action>
    Create src/lib/engine/flash/who-not-for.ts exporting deriveWhoNotFor(personas: CalibratedPersona[]) that selects the lowest-disposition persona segment(s) and returns the Scrolls-past label text (D-10), purely from disposition, no model call. Add a multi-audience-read Zod schema to BlockUnionSchema in src/lib/tools/blocks.ts: an array of per-audience read entries (cap usage at 1 here; the array shape is W4-ready for 2 per D-09) each with name, band enum (Strong/Mixed/Weak), fraction string, interpretation string, lever string, whoNotFor string, and personas array (archetype + verdict + quote). Mirror BandBlockSchema honesty: include model z.literal sim1-flash and NO numeric score field (Pitfall 5). Register the new type in block-registry.ts. Add src/lib/engine/flash/__tests__/who-not-for.test.ts (low-disposition personas yield a non-empty segment; all-hot personas yield an empty/none segment) and extend src/lib/tools/__tests__/blocks.test.ts asserting the multi-audience-read schema validates a single-audience payload and rejects any payload carrying a numeric score.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-tools && pnpm test -- who-not-for blocks</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test -- who-not-for passes: low-disposition personas produce a segment, all-hot produce none
    - pnpm test -- blocks passes: multi-audience-read validates a single-audience payload and rejects a numeric score field
    - grep -n 'z.number' on the multi-audience-read schema region shows no 0-100 score field (bands only, source assertion)
  </acceptance_criteria>
  <done>The Read block schema and the pure who-not-for derivation exist, honesty-spine-clean (bands only), with the array shape W4-ready.</done>
</task>

<task type="auto">
  <name>Task 2: multi-audience-read renderer (single-audience) + dispatcher registration</name>
  <read_first>
    - src/components/thread/message-blocks.tsx (file being modified; BLOCK_COMPONENTS map, validateBlock dispatch, UnsupportedBlock fallback; add the new component key)
    - src/components/thread/band-block.tsx (BAND_COLOR map Strong=success/Mixed=warning/Weak=error, REUSE this exact map for the verdict band coloring, do not re-roll)
    - src/components/thread/personas-block.tsx (the {archetype, verdict, quote} persona expand/list shape to REUSE for the per-audience drill)
    - src/lib/tools/blocks.ts (multi-audience-read schema + type from Task 1)
    - .planning/sketches/005-audience-scale/index.html (.read block: grade glyph + .tx interpretation + .lever; coral border rgba(255,127,80,0.30), bg rgba(255,127,80,0.08), radius 12px, the static-card spine, NO live cloud/toggle/chat)
    - .planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-UI-SPEC.md (B1 layout; Color: coral reserved for the Lever + the .read interpretation highlight only; verdict bands use semantic success/warning/error, never coral; Typography grade 15px/600, interpretation 16px/600)
  </read_first>
  <behavior>
    - The renderer shows the single-audience verdict line (Audience: band n/10), the coral-bordered Read panel (grade glyph by band + one-line interpretation + Lever line), the Scrolls-past who-not-for line, and a collapsible per-audience persona drill (archetype + verdict + quote)
    - Band words use the band-block BAND_COLOR map (success/warning/error), never coral; coral is reserved for the Lever + interpretation highlight only
    - The card is fully static, no live cloud, no scale toggle, no chat (P9 owns those)
    - An invalid/unknown payload falls back to UnsupportedBlock via the dispatcher
  </behavior>
  <action>
    Create src/components/thread/multi-audience-read-block.tsx rendering the single-audience Read per UI-SPEC B1 + sketch 005 .read: a verdict line (band colored via the reused band-block BAND_COLOR map), the coral-bordered Read panel (grade glyph mapped from band: Strong/Mixed/Weak, the one-line interpretation, and the Lever line, coral border/bg/radius from the sketch), the Scrolls-past who-not-for line, and a collapsible per-audience persona drill reusing the personas-block {archetype,verdict,quote} shape. Keep coral to the Lever + interpretation highlight only; verdict bands use the semantic status tokens (never coral). Build it as a static card, do NOT add any live cloud, scale toggle, or chat (P9). Register the component in BLOCK_COMPONENTS in src/components/thread/message-blocks.tsx keyed to the new block type so the dispatcher renders it (and falls back to UnsupportedBlock for invalid payloads).
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-tools && pnpm exec tsc --noEmit 2>&1 | grep -v __tests__ | grep -iE 'multi-audience|message-blocks' || echo no-read-block-type-errors</automated>
  </verify>
  <acceptance_criteria>
    - multi-audience-read-block.tsx is registered in BLOCK_COMPONENTS (grep multi-audience-read message-blocks.tsx returns a match)
    - Verdict band coloring imports/reuses the band-block BAND_COLOR map, not a re-rolled coral map (source assertion)
    - No live-cloud / scale-toggle / chat code in the renderer (grep for cloud/scale/chat returns no interactive-cloud affordance)
    - pnpm exec tsc --noEmit shows no new read-block type errors
  </acceptance_criteria>
  <done>The single-audience Read renders as a static card with a grade glyph + interpretation + Lever + who-not-for + persona drill, honoring the coral reservation and no-live-cloud constraint.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 05, W3 single-audience Read)

- deriveWhoNotFor(personas) (src/lib/engine/flash/who-not-for.ts, new) (D-10)
- multi-audience-read Zod schema in BlockUnionSchema (src/lib/tools/blocks.ts) + block-registry registration (bands only, array shape W4-ready)
- MultiAudienceReadBlock renderer, single-audience form (src/components/thread/multi-audience-read-block.tsx, new) + BLOCK_COMPONENTS registration (src/components/thread/message-blocks.tsx)
- Tests: who-not-for.test.ts, extended blocks.test.ts
- No migration; bands only, no fabricated score. Comment seeding NOT built (deferred per D-04).
</artifacts_produced>

<verification>
- who-not-for + blocks tests green; schema rejects numeric scores.
- Renderer registered in the dispatcher; verdict bands use the reused BAND_COLOR map; coral reserved for Lever + interpretation.
- Static card only, no live cloud (P9 boundary respected).
</verification>

<success_criteria>
W3 complete when a concept lands a static single-audience Read card (verdict + grade glyph + interpretation + Lever + Scrolls-past who-not-for + persona drill), honesty-spine-clean (bands only, SIM-1 Flash provenance), with the block shape already array-ready for the W4 2-audience compare.
</success_criteria>

<output>
Create `.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-05-SUMMARY.md` when done.
</output>
