---
phase: 08-discover-remix-read-the-competitor-niche-moat-chain
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/lib/tools/runners/remix-runner.ts
  - src/lib/tools/runners/hooks-runner.ts
  - src/lib/tools/runners/script-runner.ts
  - src/lib/tools/runners/chat-runner.ts
  - src/app/api/tools/remix/run/route.ts
  - src/app/api/tools/hooks/route.ts
  - src/app/api/tools/script/route.ts
  - src/app/api/tools/chat/route.ts
  - src/components/thread/remix-card-block.tsx
  - src/lib/tools/blocks.ts
  - src/lib/tools/runners/__tests__/steer-closure.test.ts
autonomous: true
requirements: [REMIX-01, AUD-STEER]
must_haves:
  truths:
    - "The remix, hooks, script, and chat runners each accept an optional active Audience and ground generation on it via buildAudienceGroundingLine + resolveAudienceWeights, replicating the shipped 07-04 ideas-runner shape"
    - "For the General/null audience every runner delegates byte-identically to the existing profile path (DEFAULT no-op), so the engine regression gate stays green and ENGINE_VERSION stays 3.19.0"
    - "When the active audience is not General, the remix card shows an as-your-{audience} tag near the adapted hook (muted, not coral); General shows no tag"
  artifacts:
    - path: "src/lib/tools/runners/remix-runner.ts"
      provides: "RemixPipelineInput.audience steer + audience niche into the Flash panel (REMIX-01)"
      contains: "audience"
    - path: "src/lib/tools/runners/hooks-runner.ts"
      provides: "HooksPipelineInput.audience steer (AUD-STEER)"
      contains: "audience"
    - path: "src/lib/tools/runners/script-runner.ts"
      provides: "ScriptPipelineInput.audience steer (AUD-STEER)"
      contains: "audience"
    - path: "src/lib/tools/runners/chat-runner.ts"
      provides: "ChatPipelineInput.audience steer (AUD-STEER)"
      contains: "audience"
  key_links:
    - from: "src/app/api/tools/remix/run/route.ts"
      to: "src/lib/audience/audience-repo.ts"
      via: "getAudience(active_audience_id) -> runner input"
      pattern: "getAudience|active_audience_id"
    - from: "src/lib/tools/runners/remix-runner.ts"
      to: "src/lib/audience/audience-grounding.ts"
      via: "buildAudienceGroundingLine (delegates to buildGroundingLine for General)"
      pattern: "buildAudienceGroundingLine"
---

<objective>
W2, close the steer-everywhere debt (#1) and ship audience-steered Remix (D-03, REMIX-01). P7 wired the calibrated Audience into the react/SIM path everywhere, but position-1 STEER is proven only in the ideas-runner (07-04). The hooks, script, chat, and remix runners still ground generation on the generic creator profile (profileRow.niche_primary only), verified in RESEARCH, the half-moat: predict-for-audience, generate-for-generic on 5 of 6 skills. This plan replicates the exact 07-04 import-and-input shape across the four remaining runners + their routes. No new design.

Purpose: A Read that predicts against your audience while the generation targets a generic one is a half-moat. Closing steer makes every skill generate FOR the active audience.

Output: audience steer on remix/hooks/script/chat runners + their routes, the remix-card as-your-{audience} tag, and a consolidated steer-closure test asserting the General no-op is byte-identical (regression gate stays green).

Constraint (load-bearing): General audience = DEFAULT no-op MUST be preserved. resolveAudienceWeights([]) / is_general MUST return source default with no analysis_override (Pitfall 3). Do NOT bump ENGINE_VERSION (Anti-pattern, Pitfall 4), this is additive text/generation-path steer only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-CONTEXT.md
@.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-RESEARCH.md
@.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-UI-SPEC.md
@src/lib/tools/runners/ideas-runner.ts
@src/app/api/tools/ideas/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Steer the four runners (replicate the 07-04 ideas-runner shape)</name>
  <read_first>
    - src/lib/tools/runners/ideas-runner.ts (THE reference, copy this exact shape: IdeasPipelineInput.audience optional Audience-or-null; resolveAudienceWeights(audience ? [audience] : []); audienceRepaint map when not is_general; buildAudienceGroundingLine in the STEER path; line 247 runIdeasPipeline body)
    - src/lib/tools/runners/remix-runner.ts (file being modified; RemixPipelineInput {url,profileRow,requestId}; line 149 niche = profileRow.niche_primary; line 171 panel niche; runFlashTextMode call)
    - src/lib/tools/runners/hooks-runner.ts (file being modified; HooksPipelineInput line 87; line 287 niche = profileRow.niche_primary; panel line 288; runFlashTextMode line 292)
    - src/lib/tools/runners/script-runner.ts (file being modified; ScriptPipelineInput line 73; line 251 niche = profileRow.niche_primary)
    - src/lib/tools/runners/chat-runner.ts (file being modified; ChatPipelineInput line 43; runChatPipeline line 121; assembleBundle mode chat; profile-only today)
    - src/lib/audience/audience-grounding.ts (buildAudienceGroundingLine, delegates to buildGroundingLine for General/null, the no-op guarantee)
    - src/lib/audience/resolve-audience-weights.ts (resolveAudienceWeights(Audience[]); [] or is_general returns DEFAULT mix at source default)
    - src/lib/audience/audience-types.ts (Audience, is_general, personas CalibratedPersona[] with archetype+repaint)
    - .planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-RESEARCH.md (Pattern 1 the copy-this shape; Pitfall 3 General must resolve source default)
  </read_first>
  <behavior>
    - Each runner input gains an optional audience field (Audience or null), defaulting to null
    - When audience is null OR is_general: grounding delegates to buildGroundingLine and resolveAudienceWeights returns the DEFAULT mix at source default, behavior byte-identical to today
    - When audience is a calibrated (non-general) Audience: buildAudienceGroundingLine grounds on it, resolveAudienceWeights([audience]) supplies the panel weights, and the audienceRepaint map (archetype to repaint) feeds runFlashTextMode where the runner scores (remix/hooks)
    - remix-runner additionally passes the audience niche into the Flash panel (REMIX-01, replacing the profileRow.niche_primary-only panel)
  </behavior>
  <action>
    For each of remix-runner.ts, hooks-runner.ts, script-runner.ts, chat-runner.ts: add an optional audience field to the pipeline input interface (Audience or null), import buildAudienceGroundingLine + resolveAudienceWeights + the Audience type exactly as ideas-runner does, and replace the grounding-line call with buildAudienceGroundingLine(audience, platform, profileRow). Where the runner runs Flash (remix, hooks), resolve resolveAudienceWeights(audience ? [audience] : []) into the panel and build the audienceRepaint map (Object.fromEntries of personas.map archetype to repaint) when audience is non-general, passing it to runFlashTextMode (mirror ideas-runner lines 284-296). For remix-runner specifically, also derive the panel niche from the audience when present instead of profileRow.niche_primary alone (REMIX-01). Keep every General/null path delegating to the existing profile path, do NOT inject any analysis_override for General (Pitfall 3). Do NOT touch ENGINE_VERSION. Create src/lib/tools/runners/__tests__/steer-closure.test.ts asserting, per runner, that a null/General audience produces the same grounding + DEFAULT weights as today (the no-op), and that a calibrated audience produces audience-grounded output (different grounding line, audience weights present).
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-tools && pnpm test -- steer-closure && pnpm test -- audience-regression-gate</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test -- steer-closure passes: each runner no-ops byte-identically for General/null and grounds on a calibrated audience otherwise
    - pnpm test -- audience-regression-gate passes (ENGINE_VERSION 3.19.0, General to DEFAULT at source default) AFTER the four-runner edits
    - grep -n buildAudienceGroundingLine src/lib/tools/runners/{remix,hooks,script,chat}-runner.ts returns a match in all four files
    - grep -n ENGINE_VERSION src/lib/tools/runners/*.ts shows no version bump introduced by this plan
  </acceptance_criteria>
  <done>All four runners steer on the active audience with a preserved General no-op, replicating the 07-04 shape, and the regression gate is green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire active audience into the four routes (mirror the ideas route)</name>
  <read_first>
    - src/app/api/tools/ideas/route.ts (THE reference; lines 53 import getAudience+GENERAL_AUDIENCE, 136-150 load active_audience_id from openThread, NULL=General no DB query, getAudience fallback to General on failure, 175-179 pass audience into the runner)
    - src/app/api/tools/remix/run/route.ts (file being modified; line 37 createOpenThreadLazy, 131-137 profileRow + openThread, runRemixPipeline call)
    - src/app/api/tools/hooks/route.ts (file being modified; line 133 openThread, 156 runHooksPipeline)
    - src/app/api/tools/script/route.ts (file being modified; line 120 openThread, 141 runScriptPipeline)
    - src/app/api/tools/chat/route.ts (file being modified; line 113 openThread, 159 runChatPipeline)
    - src/lib/audience/audience-repo.ts (getAudience, GENERAL_AUDIENCE virtual constant short-circuit)
  </read_first>
  <behavior>
    - Each route reads openThread.active_audience_id (NULL means General, no DB query), loads the audience via getAudience when non-null, and passes audience into its runner input
    - getAudience failure or not-found falls back to GENERAL_AUDIENCE (non-fatal, no regression)
    - The audience load happens after auth and openThread resolution, before the runner call (mirrors the ideas route ordering)
  </behavior>
  <action>
    In each of remix/run/route.ts, hooks/route.ts, script/route.ts, chat/route.ts: import getAudience + GENERAL_AUDIENCE + the Audience type (mirror ideas/route.ts line 53-54). After openThread is resolved, read active_audience_id off it (typed as string|null; NULL=General, no DB query), call getAudience(supabase, activeAudienceId) when non-null, fall back to GENERAL_AUDIENCE on load failure (non-fatal), and pass audience into the runner input added in Task 1. Do not read audience id from the request body (session/thread only). Keep the existing profileRow load and all other route behavior unchanged.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-tools && pnpm exec tsc --noEmit 2>&1 | grep -v __tests__ | grep -iE 'tools/(remix|hooks|script|chat)' || echo no-route-type-errors</automated>
  </verify>
  <acceptance_criteria>
    - grep -n getAudience src/app/api/tools/remix/run/route.ts src/app/api/tools/hooks/route.ts src/app/api/tools/script/route.ts src/app/api/tools/chat/route.ts returns a match in all four
    - Each route passes audience into its runner call (source assertion)
    - No route reads an audience id from the request body (session/thread only)
    - pnpm exec tsc --noEmit shows no new route type errors
  </acceptance_criteria>
  <done>All four routes load the per-thread active audience and feed it to their steered runner, with a safe General fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Remix card as-your-{audience} steer tag (D-03 UI)</name>
  <read_first>
    - src/components/thread/remix-card-block.tsx (file being modified; adaptedHook headline, Borrowed chip + Develop-into-hooks CTA carry the only coral; BAND_COLOR usage)
    - src/lib/tools/blocks.ts (file being modified; RemixCardBlockSchema props at line 193-219; add an optional steer tag field)
    - .planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-UI-SPEC.md (New Blocks B3: as-your-{audience} tag near the adapted-hook headline, small + muted + NOT coral; omit for General; coral stays on Borrowed chip + CTA)
  </read_first>
  <behavior>
    - When the remix ran under a non-general active audience, the remix card renders a muted as-your-{audience} tag near the adapted hook headline
    - When General (no steer), no tag renders, the card is byte-identical to today (preserves the regression-safe no-op visually)
    - The tag is muted/foreground, never coral (coral remains only on the Borrowed chip + Develop CTA, one-coral law)
  </behavior>
  <action>
    Add an optional audience-name field to RemixCardBlockSchema.props in src/lib/tools/blocks.ts (optional string; absent/empty means General no-tag). Populate it in remix-runner (Task 1) only when the active audience is non-general. In src/components/thread/remix-card-block.tsx render a small muted as-your-{name} tag adjacent to the adaptedHook headline when the field is present, styled per UI-SPEC (muted foreground, NOT coral). Leave the Borrowed chip and Develop-into-hooks CTA as the only coral elements (one-coral law). When the field is absent, render exactly as today.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-tools && pnpm exec tsc --noEmit 2>&1 | grep -v __tests__ | grep -iE 'remix-card|blocks' || echo no-remix-card-type-errors</automated>
  </verify>
  <acceptance_criteria>
    - RemixCardBlockSchema.props has an optional audience-name field (source assertion in blocks.ts)
    - remix-card-block.tsx renders the as-your tag only when the field is present, and the tag is not coral-styled (source assertion: tag class is muted, not the coral accent)
    - With no audience-name field the card output is unchanged (General no-op preserved)
    - pnpm exec tsc --noEmit shows no new remix-card type errors
  </acceptance_criteria>
  <done>The remix card surfaces the steer visibly for a calibrated audience and stays identical for General, honoring the one-coral law.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04, W2 steer closure)

- audience field on RemixPipelineInput / HooksPipelineInput / ScriptPipelineInput / ChatPipelineInput + buildAudienceGroundingLine + resolveAudienceWeights steer in all four runners (src/lib/tools/runners/{remix,hooks,script,chat}-runner.ts)
- getAudience + active_audience_id wiring in all four routes (src/app/api/tools/{remix/run,hooks,script,chat}/route.ts)
- optional audience-name prop on RemixCardBlockSchema (src/lib/tools/blocks.ts) + as-your-{audience} tag render (src/components/thread/remix-card-block.tsx)
- steer-closure regression test (src/lib/tools/runners/__tests__/steer-closure.test.ts)
- No new files in src/lib beyond the test; ENGINE_VERSION unchanged; no migration.
</artifacts_produced>

<verification>
- steer-closure + audience-regression-gate both green after the four-runner + four-route edits.
- All four runners ground via buildAudienceGroundingLine; General no-op byte-identical.
- Remix card shows the steer tag only for a calibrated audience; one coral per card preserved.
</verification>

<success_criteria>
W2 complete when every generation skill (remix, hooks, script, chat) generates FOR the active calibrated audience (not the generic profile), the General audience remains a byte-identical DEFAULT no-op (regression gate green, ENGINE_VERSION 3.19.0), and audience-steered Remix surfaces its steer in the card.
</success_criteria>

<output>
Create `.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-04-SUMMARY.md` when done.
</output>
