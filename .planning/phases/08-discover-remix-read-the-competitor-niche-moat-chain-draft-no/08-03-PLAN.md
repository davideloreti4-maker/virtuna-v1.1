---
phase: 08-discover-remix-read-the-competitor-niche-moat-chain
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/lib/tools/blocks.ts
  - src/lib/tools/block-registry.ts
  - src/lib/tools/chain-handoff.ts
  - src/components/discover/discover-grid.tsx
  - src/components/discover/outlier-tile.tsx
  - src/components/discover/discover-entry.tsx
  - src/app/(app)/discover/page.tsx
  - src/app/(app)/discover/discover-client.tsx
  - src/lib/tools/__tests__/chain-handoff.test.ts
autonomous: true
requirements: [DISC-02]
must_haves:
  truths:
    - "Discover is its own browsable grid view: one input box classifies @handle/URL to profile mode vs free text to niche mode, with the baseline label updating live"
    - "Tiles render the VideoCard metrics grid + an outlier-multiplier badge labelled vs own / vs niche (never a bare multiplier) + a source tag, ~20-30 per pull over the DottedGrid surface"
    - "Each tile primary CTA Remix-to-Read launches the discover-remix chain via the CHAIN_HANDOFFS registry, no card-component edit needed"
    - "Loading (tile skeletons, no fake percent), empty/zero-result, and error+retry states all render per the copy contract"
  artifacts:
    - path: "src/app/(app)/discover/page.tsx"
      provides: "Discover route (RSC) mounting the browsable grid view"
    - path: "src/components/discover/outlier-tile.tsx"
      provides: "Outlier tile = VideoCard metrics + multiplier badge + source tag + Remix-to-Read CTA"
    - path: "src/components/discover/discover-grid.tsx"
      provides: "Responsive tile grid over DottedGrid + loading/empty/error states"
    - path: "src/lib/tools/chain-handoff.ts"
      provides: "discover SkillId + discover-remix CHAIN_HANDOFFS entry"
      contains: "discover"
  key_links:
    - from: "src/components/discover/outlier-tile.tsx"
      to: "src/lib/tools/chain-handoff.ts"
      via: "discover-remix handoff launch on the Remix-to-Read CTA"
      pattern: "CHAIN_HANDOFFS|discover"
    - from: "src/app/(app)/discover/discover-client.tsx"
      to: "src/app/api/discover/route.ts"
      via: "POST fetch on input submit"
      pattern: "/api/discover"
---

<objective>
W1 frontend, the Discover front door (D-13/D-14/D-15/D-16): a browsable own grid view with one-entry-two-modes classification, outlier tiles reusing the VideoCard metrics grid, and a chain-coupled Remix-to-Read tile CTA that drops into the thread chain. No saving/watchlist (that is P10).

Purpose: Discover is the new funnel-top Blort/Sandcastles stop short of. The tile CTA is Remix-to-Read (not rewrite-for-me), the grid is browsable but the ACTION launches the moat chain.

Output: the (app)/discover route + grid/tile/entry components, the discover SkillId + discover-remix chain entry, and the new outlier-grid typed-block schema registered in the union + registry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-CONTEXT.md
@.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-RESEARCH.md
@.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: outlier-grid typed-block schema + discover-remix chain wiring (D-13/D-14)</name>
  <read_first>
    - src/lib/tools/blocks.ts (file being modified; BlockUnionSchema discriminatedUnion type; RemixCardBlockSchema as the shape template; model sim1-flash is the honesty convention for SIM cards, NOTE outlier tiles carry NO SIM model field, they are measured data)
    - src/lib/tools/block-registry.ts (file being modified; validateBlock + BLOCK_REGISTRY; new block type must register here so validateBlock accepts it)
    - src/lib/tools/chain-handoff.ts (file being modified; SkillId union, ChainHandoff interface from/to/anchorFrom/endpoint, CHAIN_HANDOFFS array; existing remix-hooks entry as the template; append-no-card-edit contract)
    - .planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-UI-SPEC.md (New Blocks B2 outlier-grid; tile fields metrics + multiplier badge n-times-vs-own|niche + source tag + Remix-to-Read CTA)
    - .planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-RESEARCH.md (Pitfall 5, outlier multiplier is measured scrape data allowed with its baseline label; a SIM score is forbidden)
  </read_first>
  <action>
    In src/lib/tools/blocks.ts add an outlier-grid Zod schema to BlockUnionSchema: a tile shape carrying the VideoData-derived metrics (views, saves, shares, comments), the outlier multiplier as a number, a baselineLabel enum of vs-own or vs-niche so the renderer NEVER shows a bare multiplier (D-05), a source tag, and the video reference Remix needs. Do NOT include any band, score, or model sim1-flash field, Discover tiles are measured data not SIM output (Pitfall 5). Register the new type in src/lib/tools/block-registry.ts so validateBlock accepts it. In src/lib/tools/chain-handoff.ts add discover to the SkillId union and append a from-discover to-remix ChainHandoff entry (mirror the existing remix-hooks entry; the tile CTA Remix-to-Read launches it, no card-component edit). Extend src/lib/tools/__tests__/chain-handoff.test.ts asserting the discover-remix entry resolves and discover is a valid SkillId.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-tools && pnpm test -- chain-handoff</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test -- chain-handoff passes with the discover-remix entry asserted
    - grep -n discover src/lib/tools/chain-handoff.ts shows discover in the SkillId union AND a CHAIN_HANDOFFS entry
    - The outlier-grid schema in blocks.ts has a baselineLabel field and NO model sim1-flash literal (source assertion)
  </acceptance_criteria>
  <done>The outlier-grid block validates, and the discover-remix handoff is registered for the tile CTA to launch.</done>
</task>

<task type="auto">
  <name>Task 2: outlier-tile + discover-grid components (reuse VideoCard / DottedGrid / states)</name>
  <read_first>
    - src/components/competitors/detail/video-card.tsx (REUSE; 4-col Eye/Heart/MessageCircle/Share2 metrics grid, hover bg-white-0.02, engagement pill; extend with the multiplier badge + CTA, do NOT re-roll)
    - src/components/app/dotted-grid.tsx (REUSE; 24px dotted base surface; tiles are a normal responsive CSS grid laid OVER it, not drag-pannable)
    - src/components/competitors/competitor-empty-state.tsx (REUSE shape; empty/zero state)
    - src/components/competitors/scrape-error-banner.tsx (REUSE; error + Retry)
    - src/components/competitors/competitor-card-skeleton.tsx (REUSE; tile skeleton for the slow Apify pull)
    - .planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-UI-SPEC.md (Typography multiplier 20px 600 tabular-nums; Color multiplier badge neutral NOT coral, coral ONLY on the Remix-to-Read CTA, one coral per tile; Spacing tile min-width ~160-172px; Copywriting all tile/state copy)
  </read_first>
  <action>
    Create src/components/discover/outlier-tile.tsx composing the existing VideoCard metrics grid (reuse, do NOT rebuild the 4-col grid) plus (a) the outlier-multiplier badge rendered as n-times-vs-own or n-times-vs-niche (20px 600 tabular-nums, neutral foreground, NEVER coral, NEVER a bare multiplier per D-05/UI-SPEC), (b) the source sub-tag (Your-channel or Competitor for profile mode per D-15, niche label for niche mode), and (c) the single coral primary CTA Remix-to-Read that launches the discover-remix handoff (Task 1). Enforce the board-kit accent law: exactly one coral element per tile (the CTA). Create src/components/discover/discover-grid.tsx: a responsive CSS grid (tile min-width ~160-172px so ~2-up at 390px) over DottedGrid rendering ~20-30 tiles, plus the three states, loading (tile skeletons via competitor-card-skeleton, NO fake percent), empty/zero-result, error+retry (reuse ScrapeErrorBanner), using the exact UI-SPEC Copywriting strings.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-tools && pnpm exec tsc --noEmit 2>&1 | grep -v __tests__ | grep -i discover || echo no-discover-type-errors</automated>
  </verify>
  <acceptance_criteria>
    - outlier-tile.tsx imports and reuses VideoCard (source assertion: grep video-card returns a match in src/components/discover/)
    - The multiplier badge text always includes vs-own or vs-niche, never a bare multiplier
    - discover-grid.tsx renders loading/empty/error states reusing competitor-card-skeleton + ScrapeErrorBanner
    - pnpm exec tsc --noEmit shows no new discover type errors
  </acceptance_criteria>
  <done>Outlier tiles reuse the VideoCard metrics grid with a labelled multiplier + single coral CTA, and the grid handles all three states over DottedGrid.</done>
</task>

<task type="auto">
  <name>Task 3: (app)/discover route + client (entry, classify, fetch, render)</name>
  <read_first>
    - src/app/(app)/competitors/page.tsx + competitors-client.tsx (REUSE the RSC-page + client-component split pattern for an (app) grid view)
    - src/app/(app)/layout.tsx (AppShell mount point, navigation surface)
    - src/lib/discover/classify-input.ts (classifyDiscoverInput, created 08-02, drives the live mode hint)
    - src/components/discover/discover-grid.tsx + outlier-tile.tsx (Task 2)
    - src/app/api/discover/route.ts (POST /api/discover, created 08-02, the fetch target)
    - .planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-UI-SPEC.md (Copywriting: input placeholder, mode hints profile/niche, empty-state heading/body)
  </read_first>
  <action>
    Create src/components/discover/discover-entry.tsx: the single input box with the placeholder copy, calling classifyDiscoverInput on change to update the live mode hint (profile mode ranked vs their own baseline, or niche mode ranked vs niche median, per UI-SPEC). Create src/app/(app)/discover/page.tsx as an RSC mounting the client, and src/app/(app)/discover/discover-client.tsx (client component) that owns the input state, POSTs to /api/discover on submit, manages loading/error/empty/result states, and renders discover-grid with the returned tiles. Mirror the competitors page/client split. Do NOT add any save/watchlist affordance (P10). Wire the route into the AppShell navigation if the nav is a static list (otherwise leave routing to the existing (app) router).
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-tools && pnpm exec tsc --noEmit 2>&1 | grep -v __tests__ | grep -i discover || echo no-discover-route-type-errors</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/discover/page.tsx and discover-client.tsx exist; the client POSTs to /api/discover (grep /api/discover returns a match)
    - The entry input uses classifyDiscoverInput to drive a live mode hint (source assertion)
    - No save/watchlist/shelf affordance present (grep save/watchlist/shelf returns no user-facing save action)
    - pnpm exec tsc --noEmit shows no new discover/app-route type errors
  </acceptance_criteria>
  <done>The Discover view is a live browsable route: type/paste an input, see classified mode, pull, and a ranked outlier grid with chain-launching tiles.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03, W1 frontend)

- outlier-grid typed-block schema added to BlockUnionSchema (src/lib/tools/blocks.ts); registered in src/lib/tools/block-registry.ts
- discover SkillId + discover-remix CHAIN_HANDOFFS entry (src/lib/tools/chain-handoff.ts)
- OutlierTile component (src/components/discover/outlier-tile.tsx, new)
- DiscoverGrid component + loading/empty/error states (src/components/discover/discover-grid.tsx, new)
- DiscoverEntry component, one-entry-two-modes input (src/components/discover/discover-entry.tsx, new)
- (app)/discover route (src/app/(app)/discover/page.tsx + discover-client.tsx, new)
- Extended chain-handoff test (src/lib/tools/__tests__/chain-handoff.test.ts)
</artifacts_produced>

<verification>
- chain-handoff test green with the discover-remix entry.
- Tiles reuse VideoCard; multiplier badge always carries its baseline label; one coral per tile (the CTA).
- Discover route POSTs to /api/discover and renders the three states; no save/watchlist.
</verification>

<success_criteria>
W1 frontend complete when a user can open the Discover view, paste a @handle/URL or type a niche, see the mode classified live, pull ~20-30 outlier tiles over the DottedGrid surface, and click Remix-to-Read to launch the chain, with all loading/empty/error states honest (no fake progress).
</success_criteria>

<output>
Create `.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-03-SUMMARY.md` when done.
</output>
