---
phase: 08-discover-remix-read-the-competitor-niche-moat-chain
plan: 06
type: execute
wave: 5
depends_on: ["08-01", "08-04", "08-05"]
files_modified:
  - src/lib/engine/flash/two-audience-read.ts
  - src/components/thread/multi-audience-read-block.tsx
  - src/components/thread/verbatim-wall.tsx
  - src/lib/tools/blocks.ts
  - src/app/api/tools/read/route.ts
  - src/lib/engine/flash/__tests__/two-audience-read.test.ts
autonomous: true
requirements: [READ-02, READ-03]
must_haves:
  truths:
    - "A concept is scored against two explicitly-picked audiences (default pair: the active calibrated audience vs General), producing two per-audience verdicts side by side with the delta as the one-line Read + Lever"
    - "The verbatim quote wall fans already-emitted per-persona quotes into a focus-group wall grouped by stop/scroll verdict, each quote tagged with its audience, the sharpest pulled as a lead"
    - "who-its-NOT-for falls out of the 2-audience compare (the segment that scrolls for one audience) and is shown alongside the single-audience derivation"
  artifacts:
    - path: "src/lib/engine/flash/two-audience-read.ts"
      provides: "runTwoAudienceRead(concept, [audA, audB]) -> per-audience aggregate + delta interpretation + lever (READ-02)"
      exports: ["runTwoAudienceRead"]
    - path: "src/components/thread/verbatim-wall.tsx"
      provides: "Verbatim focus-group quote wall grouped by verdict, audience-tagged (D-11)"
    - path: "src/app/api/tools/read/route.ts"
      provides: "POST /api/tools/read - pick-2 audiences, two resolves, multi-audience-read block"
      exports: ["POST"]
  key_links:
    - from: "src/lib/engine/flash/two-audience-read.ts"
      to: "src/lib/audience/resolve-audience-weights.ts"
      via: "resolveAudienceWeights([aud]) per audience (array-shaped, second resolve)"
      pattern: "resolveAudienceWeights"
    - from: "src/app/api/tools/read/route.ts"
      to: "src/components/thread/multi-audience-read-block.tsx"
      via: "multi-audience-read block emitted with 2 audiences"
      pattern: "multi-audience-read"
---

<objective>
W4, the pulled-forward killer feature (D-08/D-09): score one concept against TWO audiences side by side (default pair: active calibrated audience vs General, doubling as proof calibration moves the verdict), with the DELTA as the one-line Read + Lever (wins for growth, bombs for buyers). Plus the verbatim focus-group quote wall (D-11) over already-emitted persona quotes. This extends the multi-audience-read block (W3 / Plan 05) from 1 to 2 audiences, the array shape was built W3-ready, and adds a second resolve + the static compare card. P8 ships a STATIC side-by-side card, NOT the live interactive cloud (P9 owns that, do not redesign it here).

Purpose: wins-for-growth, bombs-for-buyers is the moat headline, but only if the numbers are real (W0 made them real). The 2-audience delta is the foresight payoff. The verbatim wall is pure presentation over data Flash already emits.

Output: runTwoAudienceRead compute, the 2-audience compare render in the existing block, the verbatim wall component, and a POST /api/tools/read route that picks 2 audiences and emits the multi-audience-read block.

Constraints: cap at 2 audiences for v1 legibility (D-09), object stays audience_ids array-ready for N later. Verbatim + who-not-for are presentation over already-emitted scrollQuote + per-persona verdict/disposition, NO extra model call (D-10/D-11). Bands only, no fabricated score (Pitfall 5).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-CONTEXT.md
@.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-RESEARCH.md
@.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-UI-SPEC.md
@.planning/sketches/005-audience-scale/index.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runTwoAudienceRead compute + read route (pick-2, default active-vs-General)</name>
  <read_first>
    - src/lib/audience/resolve-audience-weights.ts (resolveAudienceWeights(Audience[]), already array-shaped; W4 resolves each audience SEPARATELY, one [aud] resolve per audience, the second resolve is additive)
    - src/lib/engine/flash/flash-aggregate.ts (aggregateFlash -> {band, fraction}; band math to reuse per audience)
    - src/lib/engine/flash/run-flash-text-mode.ts (runFlashTextMode(text, mode, panel, audienceRepaint), run per audience with that audience weights/repaint)
    - src/lib/audience/audience-repo.ts (getAudience, GENERAL_AUDIENCE; the default pair is activeAudience vs GENERAL_AUDIENCE per D-09)
    - src/lib/engine/flash/who-not-for.ts (deriveWhoNotFor from Plan 05; reuse per audience)
    - src/lib/tools/blocks.ts (multi-audience-read schema from Plan 05; the array-of-2 payload to emit)
    - src/app/api/tools/ideas/route.ts (the auth + openThread + active_audience_id + insertMessage route template to mirror)
    - .planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-RESEARCH.md (Code Examples: two-audience Read resolve; D-08 verdict+delta+lever; D-09 cap 2, default pair)
  </read_first>
  <behavior>
    - runTwoAudienceRead(concept, [audA, audB]) resolves each audience separately (resolveAudienceWeights([aud]) per audience), runs Flash per audience, aggregates per audience to {band, fraction}, and computes the delta one-line interpretation + Lever from the two verdicts
    - The default pair is the active calibrated audience vs GENERAL_AUDIENCE (D-09); the route caps the pick at 2 (object stays array-ready for N)
    - who-not-for is derived per audience (reuse deriveWhoNotFor); the compare surfaces the segment that scrolls for one audience
    - Output is a multi-audience-read block with exactly 2 per-audience entries; bands only, no numeric score
  </behavior>
  <action>
    Create src/lib/engine/flash/two-audience-read.ts exporting runTwoAudienceRead(concept, audiences: Audience[] capped to 2): for each audience, resolveAudienceWeights([aud]) + runFlashTextMode with that audience weights+repaint + aggregateFlash to {band, fraction} + deriveWhoNotFor(audience.personas). Compute the delta interpretation line + Lever from the two aggregates (the wins-for-X, bombs-for-Y framing, D-08). Return the multi-audience-read block payload (2 entries, bands only). Create src/app/api/tools/read/route.ts (POST): mirror the ideas route security spine (auth first, session/thread only, never body for the audience id); resolve the default pair = active audience (from thread active_audience_id) vs GENERAL_AUDIENCE (D-09), allow an explicit second pick capped at 2; call runTwoAudienceRead; insertMessage the multi-audience-read block to the open thread. Add src/lib/engine/flash/__tests__/two-audience-read.test.ts asserting 2 entries returned, each with a band + fraction (no score), a delta interpretation present, and the default pair defaulting to active-vs-General.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-tools && pnpm test -- two-audience-read</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test -- two-audience-read passes: 2 per-audience entries, each band + fraction (no numeric score), a delta interpretation line, default pair active-vs-General
    - runTwoAudienceRead calls resolveAudienceWeights once per audience (the second resolve is real, source assertion)
    - POST /api/tools/read authenticates before any Flash run and reads the audience id from session/thread, never the body (source assertion)
  </acceptance_criteria>
  <done>A concept is read against 2 audiences with a real delta verdict + Lever, defaulting to active-vs-General, emitted as a bands-only block.</done>
</task>

<task type="auto">
  <name>Task 2: 2-audience compare render + verbatim quote wall (D-08/D-11)</name>
  <read_first>
    - src/components/thread/multi-audience-read-block.tsx (file being modified; the single-audience renderer from Plan 05; extend its verdict row + Read panel to render 2 audiences side by side; the persona drill becomes per-audience collapsible)
    - src/components/thread/remix-card-block.tsx (the italic 14px blockquote with 2px left border border-white-0.12, REUSE verbatim for the verbatim quote wall styling)
    - src/lib/engine/flash/flash-schema.ts (FlashPersona {archetype, verdict stop|scroll, quote}, the quote source for the wall; no extra model call, D-11)
    - .planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-UI-SPEC.md (B1 two-audience verdict row capped at 2; the Read coral panel with the DELTA interpretation; verbatim section labels Stopped-the-scroll / Scrolled-past grouped by verdict, each quote tagged with its audience, sharpest pulled as lead; coral reservation, band semantic colors)
    - .planning/sketches/005-audience-scale/index.html (.read static spine, the compare must stay a static card, no live cloud)
  </read_first>
  <behavior>
    - The block renders 2 per-audience verdict lines side by side (e.g. Growth: Strong 8/10 and Buyers: Weak 3/10), the coral Read panel showing the DELTA interpretation + Lever, and per-audience collapsible persona drills
    - The verbatim wall groups already-emitted persona quotes by stop/scroll verdict (Stopped-the-scroll / Scrolled-past), each quote tagged with its audience, the sharpest quote pulled as a lead, reusing the remix-card blockquote styling, no new generation
    - The whole card is static (no live cloud / scale toggle / chat, P9 boundary)
    - For a single-audience payload (Plan 05 path) the renderer still works (1 entry), the 2-entry path is additive
  </behavior>
  <action>
    Extend src/components/thread/multi-audience-read-block.tsx to render the 2-audience compare when the payload has 2 entries: two verdict lines side by side (band-colored via the reused band-block BAND_COLOR map), the coral Read panel showing the DELTA one-liner + Lever (D-08), and a per-audience collapsible persona drill. Keep the single-audience (1-entry) path working. Create src/components/thread/verbatim-wall.tsx: group the per-persona quotes by stop/scroll verdict into Stopped-the-scroll / Scrolled-past sections, tag each quote with its audience, pull the sharpest as a lead, reusing the remix-card italic blockquote styling (2px left border) verbatim, no new generation (D-11). Mount the verbatim wall inside the Read block. Keep coral reserved for the Lever + interpretation highlight only; verdict bands use semantic success/warning/error. Static card only, no live cloud.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-tools && pnpm exec tsc --noEmit 2>&1 | grep -v __tests__ | grep -iE 'multi-audience|verbatim' || echo no-compare-render-type-errors</automated>
  </verify>
  <acceptance_criteria>
    - The Read block renders 2 verdict lines + a DELTA interpretation + Lever for a 2-entry payload, and still renders the 1-entry single-audience form
    - verbatim-wall.tsx groups quotes by stop/scroll verdict and tags each with its audience, reusing the remix-card blockquote styling (source assertion)
    - No new model call in the verbatim wall (it consumes already-emitted quotes, source assertion)
    - Coral stays on the Lever/interpretation only; verdict bands use semantic colors; no live-cloud code present
    - pnpm exec tsc --noEmit shows no new compare/verbatim type errors
  </acceptance_criteria>
  <done>The Read card compares 2 audiences with a delta verdict + Lever and a focus-group verbatim wall, all static and honesty-clean.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 06, W4 multi-audience compare)

- runTwoAudienceRead(concept, audiences) (src/lib/engine/flash/two-audience-read.ts, new) (READ-02)
- POST /api/tools/read (src/app/api/tools/read/route.ts, new) pick-2 + default active-vs-General
- 2-audience compare render extension (src/components/thread/multi-audience-read-block.tsx)
- VerbatimWall component (src/components/thread/verbatim-wall.tsx, new) (D-11)
- multi-audience-read schema usage extended to 2 entries (src/lib/tools/blocks.ts, shape already array-ready from Plan 05)
- Test: two-audience-read.test.ts
- No migration; bands only, no fabricated score; presentation reuses already-emitted persona quotes (no extra model call).
</artifacts_produced>

<verification>
- two-audience-read test green: 2 entries, bands only, delta present, default pair active-vs-General.
- Compare render + verbatim wall reuse existing band map + blockquote styling; no extra model call.
- Static card only; coral reserved for Lever + interpretation; P9 live-cloud boundary respected.
</verification>

<success_criteria>
W4 complete when one concept reads against 2 audiences side by side (default active-vs-General) with a real delta verdict + Lever (wins for X, bombs for Y) and a focus-group verbatim wall, all on the static multi-audience-read card, honesty-spine-clean, with the live interactive cloud explicitly deferred to P9.
</success_criteria>

<output>
Create `.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-06-SUMMARY.md` when done.
</output>
