---
phase: 04-input-adapter
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - src/lib/engine/stimulus/tier.ts
  - src/lib/engine/stimulus/ingest.ts
autonomous: true
requirements: [IN-02, IN-01]
nyquist_compliant: true

must_haves:
  truths:
    - "The SIM-1 tier auto-resolves from the input kind — video→max (omni audio sensor), everything else→flash (qwen3.7-plus) — and is never a user choice."
    - "A `.txt`/`.md` upload is read to text in-memory with zero parser dependencies (D-05)."
    - "Bad extension, wrong MIME, and oversize uploads are rejected BEFORE any read/decode (V5/V12)."
    - "An uploaded filename is never used as a filesystem/storage path (Pitfall 3)."
  artifacts:
    - path: "src/lib/engine/stimulus/tier.ts"
      provides: "resolveSim1Tier(kind) pure rule + named model mapping"
      contains: "export function resolveSim1Tier"
    - path: "src/lib/engine/stimulus/ingest.ts"
      provides: "readTextFile(file), text size/type/ext caps"
      contains: "export async function readTextFile"
  key_links:
    - from: "src/lib/engine/stimulus/tier.ts"
      to: "src/lib/engine/qwen/client.ts"
      via: "QWEN_OMNI_MODEL (max) / QWEN_REASONING_MODEL (flash) named constants"
      pattern: "QWEN_(OMNI|REASONING)_MODEL"
---

<objective>
Implement the two leaf modules that have no model I/O: the pure SIM-1 tier rule (IN-02) and the `.txt`/`.md` ingestion + upload validation (IN-01 file path / V5 / V12). Turns the Wave-0 `tier.test.ts` and `ingest.test.ts` from RED→GREEN.

Purpose: The tier rule is the audio-presence discriminator surfaced as a pure function (D-03); the ingest module is the untrusted-bytes boundary hardened per ASVS V5/V12. Both are independent leaf modules — parallel with Plan 03 (vision), zero file overlap.
Output: `stimulus/tier.ts` + `stimulus/ingest.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-input-adapter/04-CONTEXT.md
@.planning/phases/04-input-adapter/04-RESEARCH.md
@.planning/phases/04-input-adapter/04-PATTERNS.md
@src/lib/engine/stimulus/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `tier.ts` — the pure SIM-1 tier rule (IN-02 / D-03)</name>
  <files>src/lib/engine/stimulus/tier.ts</files>
  <read_first>
    - src/lib/engine/qwen/client.ts:36,41 — `QWEN_OMNI_MODEL = "qwen3.5-omni-flash"` (SIM-1 Max, audio sensor) + `QWEN_REASONING_MODEL = "qwen3.7-plus"` (SIM-1 Flash, deaf/vision-capable). NOTE client.ts:42 — `QWEN_FAST_MODEL`/`qwen3.6-flash` RETIRED, do NOT reference.
    - src/lib/engine/stimulus/types.ts — `StimulusKind`, `Sim1Tier`.
    - .planning/phases/04-input-adapter/04-RESEARCH.md § "Tier rule (IN-02) — pure" + Pitfall 1 (omni-flash ≠ SIM-1 Flash).
    - src/lib/engine/stimulus/__tests__/tier.test.ts — the RED stub this task turns GREEN.
  </read_first>
  <behavior>
    - `resolveSim1Tier("video")` → `"max"`.
    - `resolveSim1Tier("text")` / `"file_text"` / `"image"` → `"flash"`.
    - Exhaustive over `StimulusKind` (no fallthrough); never reads user state — the tier is computed, never chosen.
  </behavior>
  <action>
    Create `src/lib/engine/stimulus/tier.ts` exporting `resolveSim1Tier(kind: StimulusKind): Sim1Tier` returning `kind === "video" ? "max" : "flash"` (audio-presence discriminator, D-03). Encode the tier→model mapping ONCE here as named constants — e.g. `SIM1_MODEL_BY_TIER = { max: QWEN_OMNI_MODEL, flash: QWEN_REASONING_MODEL }` imported from `../qwen/client` — so no model id is ever inlined elsewhere (Pitfall 1: never route images/text to `QWEN_OMNI_MODEL`). Add a header comment stating the audio discriminator and the omni-flash vs SIM-1-Flash naming-collision warning. Pure module, no I/O, no side effects.
  </action>
  <acceptance_criteria>
    - `tier.ts` contains `export function resolveSim1Tier`.
    - `grep -c QWEN_OMNI_MODEL src/lib/engine/stimulus/tier.ts` ≥ 1 (mapping centralized here, not inlined in vision.ts).
    - `node ./node_modules/vitest/vitest.mjs run src/lib/engine/stimulus/__tests__/tier.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/engine/stimulus/__tests__/tier.test.ts</automated>
  </verify>
  <done>`resolveSim1Tier` returns max for video, flash for all else; model mapping centralized; tier.test.ts GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `ingest.ts` — `.txt`/`.md` read + upload validation (IN-01 / V5 / V12)</name>
  <files>src/lib/engine/stimulus/ingest.ts</files>
  <read_first>
    - src/app/api/profile/avatar/route.ts:17-47 — the validate-then-read template (MIME allowlist, `file.size` cap, `${user.id}/<fixed>.${ext}` storage-key pattern; the ONLY safe path interpolation).
    - src/lib/engine/stimulus/types.ts — `StimulusInput`.
    - .planning/phases/04-input-adapter/04-RESEARCH.md § "Read a `.txt`/`.md` upload" Code Example + Pitfall 3 (filename-as-path) + § Security Domain (V5/V12 controls).
    - src/lib/engine/stimulus/__tests__/ingest.test.ts — the RED stub this task turns GREEN.
  </read_first>
  <behavior>
    - `.txt` and `.md` `File` → returns the UTF-8 text, trimmed.
    - Extension NOT in `{.txt,.md}` → throws (reject).
    - `file.type` present but not in the text MIME allowlist (`text/plain`, `text/markdown`, `""` tolerated for `.md`) → throws.
    - `file.size > MAX_TEXT_BYTES` (1 MB) → throws BEFORE calling `file.text()`.
    - `file.name` is never interpolated into any path.
  </behavior>
  <action>
    Create `src/lib/engine/stimulus/ingest.ts` exporting `readTextFile(file: File): Promise<string>` plus the caps/allowlists as named constants: `MAX_TEXT_BYTES = 1 * 1024 * 1024`, `TEXT_EXT = new Set([".txt",".md"])`, `ALLOWED_TEXT = new Set(["text/plain","text/markdown",""])`. Order the checks: derive `ext` via `file.name.slice(file.name.lastIndexOf("."))` lowercased and reject if not in `TEXT_EXT`; reject if `file.type` is truthy and not in `ALLOWED_TEXT` (allowlist MIME AND extension — never trust one alone); reject if `file.size > MAX_TEXT_BYTES` — all BEFORE `return (await file.text()).trim()` (zero parser deps, D-05). Keep the text caps/allowlists local to this module (each leaf module owns its own caps, per RESEARCH Code Examples — no cross-module sharing, so Plan 03 vision stays independent for parallel execution). NEVER use `file.name` as a path (Pitfall 3) — text is read fully in-memory, no storage. Keep the file under 500 lines. Mirror `createLogger` logging if useful (extract.ts:11 style), optional.
  </action>
  <acceptance_criteria>
    - `ingest.ts` contains `export async function readTextFile` and exports `MAX_TEXT_BYTES`, `TEXT_EXT`, `ALLOWED_TEXT`.
    - No occurrence of `storage.upload(` or `file.name` used as a path argument in `ingest.ts` (grep returns 0).
    - `node ./node_modules/vitest/vitest.mjs run src/lib/engine/stimulus/__tests__/ingest.test.ts` exits 0 (incl. bad-ext / bad-MIME / oversize rejection cases).
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/engine/stimulus/__tests__/ingest.test.ts</automated>
  </verify>
  <done>`.txt`/`.md` read in-memory with size/MIME/ext rejection before read; no filename-as-path; ingest.test.ts GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upload → lib | Untrusted file bytes (name, MIME, size, content) enter `readTextFile`/`validateUpload`. This is the P4 ingestion surface (the HTTP route is P5; D-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | `file.name` → path traversal | mitigate | Never interpolate `file.name` into a path; text read fully in-memory (no storage path at all). Acceptance greps for `storage.upload(`/filename-as-path = 0. |
| T-04-02-02 | Denial of Service | oversize / decompression upload | mitigate | `file.size > MAX_TEXT_BYTES` (1 MB) rejected BEFORE `file.text()` (Plan 03 vision owns its own image cap pre-decode). |
| T-04-02-03 | Spoofing | disguised file (bad MIME / ext mismatch) | mitigate | Allowlist extension AND MIME (`text/plain`/`text/markdown`/`""`); reject when either fails — never trust one alone (V12). |
| T-04-02-SC | Supply chain | npm installs | accept | Zero new packages (D-05). No install surface. |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/lib/engine/stimulus/__tests__/tier.test.ts` and `.../ingest.test.ts` both exit 0.
- `grep` confirms model mapping centralized in `tier.ts` and no filename-as-path in `ingest.ts`.
</verification>

<success_criteria>
`resolveSim1Tier` resolves the SIM-1 tier purely from kind (IN-02, never a user choice); `readTextFile` ingests `.txt`/`.md` in-memory with V5/V12 hardening (IN-01); both Wave-0 tests GREEN.
</success_criteria>

<artifacts_produced>
This plan: `src/lib/engine/stimulus/tier.ts` (`resolveSim1Tier`, `SIM1_MODEL_BY_TIER`), `src/lib/engine/stimulus/ingest.ts` (`readTextFile`, `MAX_TEXT_BYTES`, `TEXT_EXT`, `ALLOWED_TEXT`). See 04-01 for the full phase artifact set (excluded from drift verification).
</artifacts_produced>

<output>
Create `.planning/phases/04-input-adapter/04-02-SUMMARY.md` when done.
</output>
