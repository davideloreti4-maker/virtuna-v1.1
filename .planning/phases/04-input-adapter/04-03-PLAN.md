---
phase: 04-input-adapter
plan: 03
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - src/lib/engine/stimulus/vision.ts
autonomous: true
requirements: [IN-03]
nyquist_compliant: true

must_haves:
  truths:
    - "A screenshot/image is read into text via `qwen3.7-plus` vision (QWEN_REASONING_MODEL) — NEVER omni (omni is the audio-only sensor, D-04)."
    - "The image is sent as an in-memory base64 `data:` URL (no Supabase round-trip, no persisted PII artifact)."
    - "The model output is stripped → JSON.parse → Zod-validated before becoming `Stimulus.content` (V5)."
    - "Untrusted image content is isolated in the USER content array, never the system prompt (prompt-injection isolation)."
  artifacts:
    - path: "src/lib/engine/stimulus/vision.ts"
      provides: "readImageWithVision(file) → text via qwen3.7-plus image_url call"
      contains: "export async function readImageWithVision"
  key_links:
    - from: "src/lib/engine/stimulus/vision.ts"
      to: "src/lib/engine/qwen/client.ts"
      via: "getQwenClient().chat.completions.create({ model: QWEN_REASONING_MODEL, ... })"
      pattern: "QWEN_REASONING_MODEL"
---

<objective>
Implement the IN-03 image reader: `readImageWithVision(file)` validates an uploaded screenshot, frames it as a base64 `data:` URL, and reads it into text via a `qwen3.7-plus` multimodal call — a pure ASSEMBLY of three patterns already live in `wave3/` + `qwen/` (no new dependency, no client wrapper). Turns the Wave-0 `vision.test.ts` RED→GREEN (mocked).

Purpose: D-04 — semantic screenshot read (understands chat-bubble layouts), reusing the reasoning model's existing vision capability. Independent module — parallel with Plan 02 (tier/ingest), zero file overlap.
Output: `stimulus/vision.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-input-adapter/04-CONTEXT.md
@.planning/phases/04-input-adapter/04-RESEARCH.md
@.planning/phases/04-input-adapter/04-PATTERNS.md
@src/lib/engine/stimulus/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `vision.ts` — screenshot → text via qwen3.7-plus (IN-03 / D-04)</name>
  <files>src/lib/engine/stimulus/vision.ts</files>
  <read_first>
    - src/lib/engine/wave3/persona-prompts-pass2.ts:106-134 — the EXACT `{type:"image_url", image_url:{url}}` content-item shape + trailing `{type:"text"}` item.
    - src/lib/engine/wave3/fold.ts:317-339 — `QWEN_REASONING_MODEL` driven with a multimodal `content[]` array + `response_format:{type:"json_object"}`.
    - src/lib/engine/pipeline.ts:626-636 — the DashScope extension param-mutation + `@ts-expect-error` convention (`params.seed = QWEN_SEED`, `params.enable_thinking = false`, `ai.chat.completions.create(params as never)`).
    - src/lib/engine/qwen/omni-analysis.ts:291-297 — the strip→`JSON.parse`→Zod sequence (`stripModelOutput`).
    - src/lib/engine/qwen/client.ts:7,36,41 — `getQwenClient`, `QWEN_OMNI_MODEL` (DO NOT use for images), `QWEN_REASONING_MODEL` (USE this).
    - .planning/phases/04-input-adapter/04-RESEARCH.md § "Image → base64 data URL → vision read" Code Example — defines `ALLOWED_IMG`/`MAX_IMG_BYTES` LOCAL to this module (no import from Plan 02, so this plan stays independent for parallel Wave-1 execution).
    - .planning/phases/04-input-adapter/04-RESEARCH.md § Pattern 1 + "Image → base64 data URL → vision read" + Pitfalls 1/2/4 + Assumption A2.
    - src/lib/engine/stimulus/__tests__/vision.test.ts — the RED stub (mocks `getQwenClient`) this task turns GREEN.
  </read_first>
  <behavior>
    - Rejects a non-image MIME (not in `ALLOWED_IMG`) and an oversize image (`> MAX_IMG_BYTES`) BEFORE base64-encoding.
    - Builds the call with `model === QWEN_REASONING_MODEL` (asserted; NEVER `QWEN_OMNI_MODEL`).
    - User content = `[{type:"image_url", image_url:{url: "data:<mime>;base64,..."}}, {type:"text", text: <read instruction>}]`; the system prompt contains NO untrusted image bytes.
    - Sets `enable_thinking = false` + `seed = QWEN_SEED` via the `@ts-expect-error` mutation convention; `response_format: json_object`.
    - Parses via `stripModelOutput` → `JSON.parse` → Zod; returns the read text string.
  </behavior>
  <action>
    Create `src/lib/engine/stimulus/vision.ts` exporting `readImageWithVision(file: File): Promise<string>` plus a `STIMULUS_VISION_SYSTEM_PROMPT` constant and a small Zod `VisionReadSchema` (e.g. `{ content: string }`). Validate the image against `ALLOWED_IMG = new Set(["image/png","image/jpeg","image/webp"])` + `MAX_IMG_BYTES = 10 * 1024 * 1024` (defined LOCAL to `vision.ts` per RESEARCH Code Example — no import from `ingest.ts`) before decoding; build `dataUrl = \`data:${file.type};base64,${Buffer.from(await file.arrayBuffer()).toString("base64")}\``. Assemble the call exactly per Pattern 1: `getQwenClient()`, `params = { model: QWEN_REASONING_MODEL, messages: [{role:"system", content: STIMULUS_VISION_SYSTEM_PROMPT}, {role:"user", content: [{type:"image_url", image_url:{url: dataUrl}}, {type:"text", text:"Read this screenshot. Return JSON: { content: <verbatim/semantic text> }."}]}], response_format: { type:"json_object" as const }, temperature: 0 }`, then `// @ts-expect-error` mutate `params.seed = QWEN_SEED` and `params.enable_thinking = false`, `await ai.chat.completions.create(params as never)`. Strip→parse→Zod-validate the response into the read text (mirror omni-analysis.ts:291-297). Wrap the model call in `Sentry.captureException` on failure (mirror pipeline.ts logging). Keep the system prompt free of untrusted bytes (injection isolation). Keep under 500 lines. Keep the function signature STABLE so P5 wires it with no re-cut. If the gated A2 live smoke (Plan 01 `vision.test.ts`) reveals base64 `data:` URLs are rejected by the model, fall back to the avatar Storage→signed-URL path — but keep `readImageWithVision(file): Promise<string>` unchanged.
  </action>
  <acceptance_criteria>
    - `vision.ts` contains `export async function readImageWithVision` and references `QWEN_REASONING_MODEL`.
    - `grep -c QWEN_OMNI_MODEL src/lib/engine/stimulus/vision.ts` === 0 (images NEVER routed to omni — Pitfall 1/D-04).
    - `vision.ts` contains `image_url`, `enable_thinking`, and `stripModelOutput` (assembly + parse sequence present).
    - `node ./node_modules/vitest/vitest.mjs run src/lib/engine/stimulus/__tests__/vision.test.ts` exits 0 (mocked unit assertions; the gated A2 live smoke skips unless `DASHSCOPE_API_KEY`).
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/engine/stimulus/__tests__/vision.test.ts</automated>
  </verify>
  <done>`readImageWithVision` reads a screenshot via `qwen3.7-plus` (base64 data URL, never omni), Zod-validates the output, isolates untrusted content in the user array; vision.test.ts GREEN; signature stable for P5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upload → lib | Untrusted image bytes (MIME, size) enter `readImageWithVision`. |
| lib → DashScope model | Untrusted image content crosses to `qwen3.7-plus` — a prompt-injection / info-disclosure surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Denial of Service | oversize base64 image (DashScope per-image cap) | mitigate | Reject `file.size > MAX_IMG_BYTES` (10 MB) BEFORE base64-encoding (base64 inflates ~33%). |
| T-04-03-02 | Spoofing | disguised image MIME | mitigate | Allowlist `image/png|jpeg|webp` (`ALLOWED_IMG`); `data:${file.type};base64,` framing only for allowlisted MIME. |
| T-04-03-03 | Tampering / Info Disclosure | prompt injection via screenshot content | mitigate | Untrusted image isolated in the USER content array, never the system prompt; `response_format: json_object` + Zod-validate the model read before use. (Deeper user-text→scorer injection is P5's carry-forward — not solved here.) |
| T-04-03-04 | Info Disclosure | stored screenshot PII | mitigate | In-memory base64 (no Supabase round-trip, no persisted artifact) for the transient read. |
| T-04-03-05 | Tampering | image routed to wrong model | mitigate | Hard rule: `model: QWEN_REASONING_MODEL` only; acceptance greps `QWEN_OMNI_MODEL` count === 0 (Pitfall 1 / D-04). |
| T-04-03-SC | Supply chain | npm installs | accept | Zero new packages (D-05) — reuses `openai`/`zod` already in repo. |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/lib/engine/stimulus/__tests__/vision.test.ts` exits 0.
- `grep` confirms `QWEN_OMNI_MODEL` count === 0 in `vision.ts` and the `image_url`/`enable_thinking`/`stripModelOutput` assembly is present.
- Gated A2 live smoke confirms base64 acceptance (or triggers the documented Storage→signed-URL fallback) without changing the function signature.
</verification>

<success_criteria>
`readImageWithVision` reads screenshots into text via `qwen3.7-plus` vision (IN-03 / D-04), hardened per V5/V12, with a P5-stable signature; vision.test.ts GREEN.
</success_criteria>

<artifacts_produced>
This plan: `src/lib/engine/stimulus/vision.ts` (`readImageWithVision`, `STIMULUS_VISION_SYSTEM_PROMPT`, `VisionReadSchema`, `ALLOWED_IMG`, `MAX_IMG_BYTES`). See 04-01 for the full phase artifact set (excluded from drift verification).
</artifacts_produced>

<output>
Create `.planning/phases/04-input-adapter/04-03-SUMMARY.md` when done.
</output>
