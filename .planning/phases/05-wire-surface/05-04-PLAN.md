---
phase: 05-wire-surface
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - src/components/app/simulation/results-panel.tsx
  - src/components/app/simulation/__tests__/results-panel.predicted-engagement-null.test.tsx
  - src/components/app/simulation/EngagementRangeCard.tsx
autonomous: true
requirements: [R11, R5]
must_haves:
  truths:
    - "The dead fake-engagement UI (TikTokResultCard predicted_engagement path) is removed from results-panel (D-08 strip)"
    - "The grounded R11 range (from Plan 02) renders as a history-relative range with confidence, never a false-precise point (R11, D-06)"
    - "The range is read from the LIVE result (result.predicted_engagement / analyzeMutation.data) only — it is NOT persisted this phase, so on permalink/DB reload the engagement card is correctly absent; reload-persistence is DEFERRED to the per-creator-avg_views follow-up (D-06)"
    - "When predicted_engagement is null (no creator baseline), no engagement card renders at all (honesty, R9)"
    - "The predicted-engagement null test is updated for the new range shape (the dead point-shape 'renders when present' case is removed/repurposed)"
  artifacts:
    - path: "src/components/app/simulation/results-panel.tsx"
      provides: "Dead TikTokResultCard block removed; grounded range card wired in its place"
    - path: "src/components/app/simulation/EngagementRangeCard.tsx"
      provides: "Range display for the grounded R11 EngagementRange (lo-hi + confidence + basis label)"
      min_lines: 20
  key_links:
    - from: "src/components/app/simulation/results-panel.tsx"
      to: "result.predicted_engagement (EngagementRange)"
      via: "EngagementRangeCard render, gated on non-null"
      pattern: "predicted_engagement"
    - from: "src/components/app/simulation/EngagementRangeCard.tsx"
      to: "range.lo / range.hi / range.confidence"
      via: "range display, no single-point rendering"
      pattern: "lo|hi"
---

<objective>
Close out the surface: strip the dead fake-engagement UI (D-08) and replace it with the grounded R11 range display built in Plan 02. The `TikTokResultCard` predicted_engagement path (results-panel.tsx:159-166) is dead today (the field was null since P1) and must go; in its place, render the honest history-relative range (lo-hi + confidence) when a creator baseline exists, and nothing when it does not.

Purpose: R11 (surface the grounded engagement estimate as a range) + D-08 (strip the dead fake-engagement UI; honesty by deletion, R9).
Output: removed dead block + import, a small `EngagementRangeCard`, and the updated null test reflecting the new range shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-wire-surface/05-CONTEXT.md
@.planning/phases/05-wire-surface/05-RESEARCH.md
@.planning/phases/05-wire-surface/05-PATTERNS.md

@.planning/phases/05-wire-surface/05-02-SUMMARY.md
@.planning/phases/05-wire-surface/05-03-SUMMARY.md

<interfaces>
Extracted from codebase, no exploration needed.

src/components/app/simulation/results-panel.tsx:15, 159-166 -- DEAD block to remove:
  line 15:  import { TikTokResultCard } from './tiktok-result-card';   // DELETE
  lines 159-166:  {result.predicted_engagement && ( <TikTokResultCard ... engagement={result.predicted_engagement} /> )}   // REPLACE with EngagementRangeCard
  // results-panel reads the LIVE result object (analyzeMutation.data) — the range is live-only; predicted_engagement has NO DB column (not persisted this phase), so a permalink reload simply has no range to show. Do NOT add persistence here.

src/lib/engine/types.ts (after Plan 02) -- the grounded range shape:
  predicted_engagement: EngagementRange | null;   // { lo, hi, confidence, basis }  (was the point-shape PredictedEngagement)

src/components/app/simulation/__tests__/results-panel.predicted-engagement-null.test.tsx -- existing test:
  "does NOT render when null" cases stay valid; the "renders when present" case used the POINT shape -> update for the range card.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EngagementRangeCard + strip dead TikTokResultCard path + update null test</name>
  <read_first>
    - src/components/app/simulation/results-panel.tsx:15,150-175 (the import + the dead predicted_engagement JSX block)
    - src/components/app/simulation/__tests__/results-panel.predicted-engagement-null.test.tsx (FULL, the existing null-safety cases + the now-dead "renders when present" point-shape case)
    - .planning/phases/05-wire-surface/05-PATTERNS.md (section results-panel.tsx strip dead engagement UI: delete block + import; replace with range if R11 built)
    - .planning/phases/05-wire-surface/05-02-SUMMARY.md (the EngagementRange shape Plan 02 shipped: lo/hi/confidence/basis)
    - ./CLAUDE.md (Raycast card: bg-transparent, border white/[0.06], 12px radius; Inter; range is history-relative, never a point)
  </read_first>
  <behavior>
    - Test: when result.predicted_engagement is null/absent, NO engagement card renders (keep + adapt the existing null cases).
    - Test: when result.predicted_engagement is a populated EngagementRange, EngagementRangeCard renders the lo-hi range (both bounds present) + the confidence + the basis label, and NEVER a single bare point number.
    - Test: TikTokResultCard is no longer imported or rendered by results-panel (the dead path is gone).
  </behavior>
  <action>
    Three edits:
    (a) Create src/components/app/simulation/EngagementRangeCard.tsx: a small presentational card taking the EngagementRange (lo, hi, confidence, basis). Render "lo-hi" (e.g. "8k-40k") + a confidence indicator + the basis label (e.g. "vs your follower tier"). History-relative framing per D-06; NEVER render a single point. Raycast card styling per CLAUDE.md (bg-transparent, 6% border, 12px radius, Inter). Keep under 500 lines (it is tiny).
    (b) results-panel.tsx: delete the import at line 15 (TikTokResultCard) and replace the dead JSX block at lines 159-166 with {result.predicted_engagement && <EngagementRangeCard range={result.predicted_engagement} />}. The non-null gate preserves the honesty contract (nothing renders without a baseline).
    (c) Update results-panel.predicted-engagement-null.test.tsx: keep the "does NOT render when null/absent" cases (now asserting no EngagementRangeCard); replace the old point-shape "renders when present" case with one that passes a populated EngagementRange and asserts the range card renders lo + hi (not a point). Remove the dead TikTokResultCard import from the test if present.
    Do NOT delete tiktok-result-card.tsx itself in this plan unless grep proves it has zero other consumers (run the grep; if other consumers exist, leave the file and only remove the results-panel usage).
  </action>
  <verify>
    <automated>npx vitest run src/components/app/simulation/__tests__/results-panel.predicted-engagement-null.test.tsx && grep -c TikTokResultCard src/components/app/simulation/results-panel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The updated null test is GREEN: no card when null; EngagementRangeCard with lo+hi when a range is present.
    - grep -c TikTokResultCard src/components/app/simulation/results-panel.tsx returns 0 (dead path fully removed).
    - grep -n "EngagementRangeCard" src/components/app/simulation/results-panel.tsx shows the new gated render.
    - EngagementRangeCard renders both range bounds and never a single point (assert in test).
    - npx tsc --noEmit zero new errors.
  </acceptance_criteria>
  <done>Dead fake-engagement UI stripped; grounded R11 range surfaced as a history-relative range card gated on a real baseline; null test updated for the range shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| predicted_engagement (EngagementRange) -> DOM | the grounded range computed engine-side (Plan 02) rendered in results-panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | EngagementRangeCard numeric display | mitigate | render-only of the already-clamped engine-side range (Plan 02 clamps lo/hi >= 0); the card does no arithmetic; null-gated so no baseline renders nothing (no fabricated number, R9) |
| T-05-11 | Info disclosure | results-panel render | accept | unchanged data path; React escapes text; no new fetch/persist; the strip only removes a dead component usage |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | N/A, zero external installs in P5 (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- npx vitest run src/components/app/simulation/__tests__/results-panel.predicted-engagement-null.test.tsx -- GREEN.
- grep -c TikTokResultCard src/components/app/simulation/results-panel.tsx -- 0.
- npx tsc --noEmit -- zero new errors.
- npm test (full suite) before phase gate.
- Manual/live (phase gate): a real run for a known-follower creator shows the range card with lo-hi; a no-creator run shows no engagement card.
</verification>

<success_criteria>
- Dead fake-engagement UI removed (D-08); TikTokResultCard no longer referenced by results-panel.
- Grounded R11 range surfaced as a history-relative lo-hi range with confidence (R11, D-06), null-gated for honesty (R9).
- Null test updated for the new range shape.
</success_criteria>

<output>
Create `.planning/phases/05-wire-surface/05-04-SUMMARY.md` when done.
</output>
