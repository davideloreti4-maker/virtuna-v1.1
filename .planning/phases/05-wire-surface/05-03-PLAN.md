---
phase: 05-wire-surface
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/board/InsightHeroFrame.tsx
  - src/components/board/__tests__/InsightHeroFrame.test.tsx
  - src/lib/engine/panel-mapping.ts
  - src/components/board/board-types.ts
  - src/components/board/board-constants.ts
  - src/components/board/Board.tsx
autonomous: true
requirements: [R4, R5]
must_haves:
  truths:
    - "The insight-hero is the board hero: read + 3 verbatim-grounded rewrites (original struck-through + copyable variant) then 6 dimensions then retention heatmap then score band then flop warning, in that priority order (D-08)"
    - "The hero reads apollo_reasoning via DUAL-READ (live top-level OR variants.apollo) so it does NOT blank on permalink reload (Pitfall 2 / regression WPk976kozfWs)"
    - "Each dimension renders its band AND its numeric score; old band-only rows (no score) still render defensively (D-01 read side)"
    - "At least one rewrite is labelled with the fold heatmap biggest-drop timestamp (targets the 0:02 dip), joined at render time, no engine change (D-07, R4)"
    - "The score band is reused from verdict-derive (confidenceRange/bandLabel/bandTone), demoted BELOW the insight (D-02, D-08)"
    - "The hero paints when the Apollo (wave_2) call lands via a new insight_hero panel id (D-03 progressive reveal)"
  artifacts:
    - path: "src/components/board/InsightHeroFrame.tsx"
      provides: "Net-new insight-hero frame reading apollo_reasoning (dual-read), rendering the D-08 surface set"
      min_lines: 80
    - path: "src/lib/engine/panel-mapping.ts"
      provides: "insight_hero panel id mapped to wave_2 for progressive reveal"
      contains: "insight_hero"
    - path: "src/components/board/Board.tsx"
      provides: "InsightHeroFrame mounted into the board layout (mirrors ContentAnalysisFrame registration)"
      contains: "InsightHeroFrame"
  key_links:
    - from: "src/components/board/InsightHeroFrame.tsx"
      to: "row.variants.apollo ?? row.apollo_reasoning"
      via: "dual-read useMemo (ContentAnalysisFrame template)"
      pattern: "variants.*apollo|apollo_reasoning"
    - from: "src/components/board/InsightHeroFrame.tsx"
      to: "audience-derive findBiggestDrop/buildInsight"
      via: "D-07 drop-point label on the retention rewrite"
      pattern: "findBiggestDrop|buildInsight"
    - from: "src/lib/engine/panel-mapping.ts"
      to: "STAGE_TO_PANEL.wave_2"
      via: "insight_hero added to the wave_2 panel list"
      pattern: "wave_2.*insight_hero"
---

<objective>
Build the insight-hero frame, the declared HERO of P5 (D-08) and the largest net-new surface. apollo_reasoning (rewrites + dimensions + composite) is persisted to variants.apollo today but ZERO board components read it. This plan surfaces it: the read + 3 verbatim-grounded rewrites (original struck-through + copyable variants), the 6 cited dimensions (band + numeric score from D-01), the score band (D-02, reused verdict-derive), the fold retention drop-point join (D-07, R4 at surface-time), and the progressive-reveal panel wiring (D-03).

Purpose: R4 (audience-aware insight, a rewrite cites an archetype bounce point, satisfied at the UI layer per D-07) + R5 (the honest expert score band demoted below the insight) + D-08 (insight is the hero, score never competes for top attention).
Output: InsightHeroFrame.tsx (net-new), its dual-read test, the insight_hero panel id, and the board-layout registration.

CRITICAL (planning context): DUAL-READ is mandatory. The hero MUST read live top-level AND variants.apollo or it blanks on permalink reload (documented WPk976kozfWs regression). Copy ContentAnalysisFrame.tsx:79-99 verbatim for the merge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-wire-surface/05-CONTEXT.md
@.planning/phases/05-wire-surface/05-RESEARCH.md
@.planning/phases/05-wire-surface/05-PATTERNS.md

@.planning/phases/05-wire-surface/05-01-SUMMARY.md

@src/components/board/content-analysis/ContentAnalysisFrame.tsx

<interfaces>
Contracts the hero consumes. Extracted from codebase, no exploration needed.

src/lib/engine/types.ts:756-792 -- Apollo output (after Plan 01: dimensions carry numeric score):
  ApolloDimension = name (6 enum), band: strong|mid|weak, score: number(0-100), lever, evidence
  ApolloRewrite   = original (verbatim hook), variant, lever_fixed (the lever, DIFFERENT per rewrite)
  apollo_reasoning = rewrites[2-3], dimensions[6], composite_score, ceiling_capper, confidence_scope, platform_note?

src/components/board/content-analysis/ContentAnalysisFrame.tsx:79-99 -- DUAL-READ template (copy for apollo):
  const v = row?.variants?.craft ?? {}; ... field: v.<k> ?? row?.<k> ?? null
  For apollo: const apollo = row?.variants?.apollo ?? row?.apollo_reasoning ?? null

src/components/board/verdict/verdict-derive.ts:19-109 -- D-02 band (REUSE, no edit):
  confidenceRange(score, confidence) -> { lo, hi }   width = clamp(round((1-confidence)*22), 3, 22)
  bandLabel(score) -> "High potential" | "Solid contender" | "Needs work"
  bandTone(score)  -> good | warn | crit

src/components/board/audience/audience-derive.ts -- D-07 join (REUSE, no edit):
  findBiggestDrop(normalizedCurve: number[]) -> { index, delta, fromIndex } | null   (line 111)
  buildInsight(segments, drop, groups) -> { lead, time, tail, addendum }   .time = "0:02" mm:ss (line 155)
  formatTime(seconds) -> "0:02"   (line 49)
  result.heatmap: HeatmapPayload | null; heatmap.weighted_curve: number[]; heatmap.segments[].t_start

src/lib/engine/panel-mapping.ts:10-37 -- D-03 reveal (ADD insight_hero):
  PANEL_IDS = [..., "reasoning", ...]   ADD "insight_hero"
  STAGE_TO_PANEL.wave_2 = ["reasoning"]   ADD "insight_hero" -> ["reasoning","insight_hero"]
  panelReadyFromStages(...) already built, no reducer change

src/components/board/Board.tsx:39, 516 -- frame mount analog:
  import { ContentAnalysisFrame } from './content-analysis/ContentAnalysisFrame';
  {layout.id === 'content-analysis' && <ContentAnalysisFrame camera={camera} layout={layout} />}

src/components/board/board-constants.ts:44, 74, 83-89, 147-162 -- GROUP_FRAMES bounds + AUTO_HEIGHT_FRAMES set + layout math (content-analysis is the analog row)
src/components/board/board-types.ts -- GroupId union (add 'insight-hero')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: InsightHeroFrame component -- dual-read + D-08 surface set + D-07 join + D-02 band</name>
  <read_first>
    - src/components/board/content-analysis/ContentAnalysisFrame.tsx (FULL, the dual-read + frame structure template; copy lines 79-99 verbatim for apollo)
    - src/components/board/verdict/verdict-derive.ts:19-109 (confidenceRange/bandLabel/bandTone, import, do not reimplement)
    - src/components/board/audience/audience-derive.ts:49,111,155-188 (findBiggestDrop/buildInsight/formatTime, import for D-07)
    - src/lib/engine/types.ts:756-792 (ApolloDimension w/ score, ApolloRewrite, apollo_reasoning, HeatmapPayload)
    - .planning/phases/05-wire-surface/05-PATTERNS.md (section InsightHeroFrame.tsx net-new: render order, dual-read, copyable+struck-through, drop-point label, old-row defensive render)
    - .planning/phases/05-wire-surface/05-RESEARCH.md (Pitfall 2 permalink blank; Q4 rewrite/heatmap join; Q5 surface set)
    - ./CLAUDE.md (Raycast: 6% borders white/[0.06], 10% hover, 12px card radius, Inter; client component only when interactive; GlassPanel; backdrop-filter via inline style not class)
  </read_first>
  <behavior>
    - Test: given a LIVE PredictionResult with top-level apollo_reasoning, the frame renders all 3 rewrites and 6 dimensions.
    - Test: given a PERMALINK row with apollo_reasoning == null but variants.apollo populated, the frame renders identically (dual-read, Pitfall 2 / WPk976kozfWs). This is the load-bearing regression test.
    - Test: each rewrite renders the original struck-through and a copy affordance for the variant.
    - Test: each dimension renders its band AND numeric score; a dimension with score === undefined (legacy row) still renders (band only, no crash).
    - Test (D-07): with a heatmap whose biggest drop is at a known segment, the retention-lever rewrite (lever_fixed references retention) is labelled with the drop mm:ss (targets the 0:02 dip). When heatmap is null, no label and no crash.
    - Test (D-02): the score band renders bandLabel/range from overall_score + confidence and sits BELOW the rewrites+dimensions (assert DOM order, insight before band).
  </behavior>
  <action>
    Create src/components/board/InsightHeroFrame.tsx ('use client', interactive: clipboard copy + reveal state). Mirror ContentAnalysisFrame for data access:
    - DUAL-READ: const apollo = row?.variants?.apollo ?? (row as PredictionResult)?.apollo_reasoning ?? null; in a useMemo keyed on [row?.variants?.apollo, row?.apollo_reasoning]. Read row from the same hook ContentAnalysisFrame uses (useAnalysisStream / usePermalinkAnalysis).
    - Render in D-08 priority order: (1) hero read (apollo.confidence_scope / Apollo interpretation text + ceiling_capper); (2) 3 rewrites, del-wrapped struck-through original + a Copy button calling navigator.clipboard.writeText(rewrite.variant) + the variant text; (3) 6 dimensions, name + band + numeric score (defensive: typeof dim.score === 'number' ? dim.score : null) + lever; (4) retention heatmap (pass-through to the existing AudienceNode/RetentionChart, do NOT reimplement the chart); (5) score band via confidenceRange+bandLabel+bandTone from verdict-derive (D-02, demoted below the insight); (6) flop/anti-virality warning if result.anti_virality_gated.
    - D-07 join: compute biggest drop from result.heatmap.weighted_curve via findBiggestDrop, map to mm:ss via buildInsight().time / formatTime(segment.t_start), attach the label to the rewrite whose lever_fixed includes retention. If none matches or heatmap null, no label.
    - Raycast styling per CLAUDE.md (6% borders, 12px radius, Inter, GlassPanel for the frame shell; backdrop-filter inline). Keep the file under 500 lines, extract a RewriteItem / DimensionRow subcomponent inline or to a small sibling if it grows.
    Write the test FIRST (InsightHeroFrame.test.tsx) covering the seven behaviors with a live fixture AND a permalink (variants.apollo) fixture. The dual-read case is mandatory.
  </action>
  <verify>
    <automated>npx vitest run src/components/board/__tests__/InsightHeroFrame.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All seven behavior tests GREEN, including the permalink dual-read case (variants.apollo only) and the legacy no-score dimension case.
    - grep -n "variants?.apollo" src/components/board/InsightHeroFrame.tsx confirms dual-read.
    - grep -n "findBiggestDrop" src/components/board/InsightHeroFrame.tsx confirms the D-07 join uses existing helpers (no reimplemented drop math).
    - grep -n "confidenceRange" src/components/board/InsightHeroFrame.tsx confirms D-02 band reuse (no new banding).
    - No new top-level DB column / no engine edit (UI-only surface).
  </acceptance_criteria>
  <done>InsightHeroFrame renders the full D-08 surface set with dual-read, copyable struck-through rewrites, scored dimensions, the D-07 drop-point label, and the demoted D-02 band, all green incl. the permalink regression test.</done>
</task>

<task type="auto">
  <name>Task 2: Register insight_hero panel (D-03) + mount the frame in the board layout</name>
  <read_first>
    - src/lib/engine/panel-mapping.ts:10-71 (PANEL_IDS, STAGE_TO_PANEL, panelReadyFromStages)
    - src/components/board/board-types.ts (GroupId union)
    - src/components/board/board-constants.ts:44,74,83-89,147-162 (GROUP_FRAMES bounds, AUTO_HEIGHT_FRAMES, layout math, content-analysis is the analog row)
    - src/components/board/Board.tsx:39,468-516 (frame import + the layout.id === 'content-analysis' conditional render)
    - .planning/phases/05-wire-surface/05-PATTERNS.md (section panel-mapping.ts add insight_hero panel id)
  </read_first>
  <action>
    Two registrations:
    (a) Progressive reveal (D-03): add "insight_hero" to PANEL_IDS (panel-mapping.ts) and to STAGE_TO_PANEL.wave_2 so the list becomes ["reasoning","insight_hero"]. The Apollo stage paints the hero at ~70s. No change to panelReadyFromStages (already built). In InsightHeroFrame, read panelReady["insight_hero"] and render a light loading affordance while "loading" (Claude's discretion per CONTEXT, plain progressive with a light affordance, NOT a full skeleton).
    (b) Board mount: add an 'insight-hero' member to the GroupId union (board-types.ts); register it in GROUP_FRAMES (board-constants.ts) with bounds + add to AUTO_HEIGHT_FRAMES (mirror the content-analysis entry shape and the layout math at :147-162); import InsightHeroFrame in Board.tsx and render it with the {layout.id === 'insight-hero' && <InsightHeroFrame camera={camera} layout={layout} />} conditional, mirroring the content-analysis line at :516. Keep the hero visually prominent per D-08 (insight is the hero) but defer deep visual polish to the UI/UX milestone (CONTEXT scope note).
    Run the board layout tests after wiring to confirm no layout regression.
  </action>
  <verify>
    <automated>grep -n insight_hero src/lib/engine/panel-mapping.ts && grep -rn InsightHeroFrame src/components/board/Board.tsx && npx vitest run src/components/board/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "insight_hero" src/lib/engine/panel-mapping.ts shows it in PANEL_IDS AND in STAGE_TO_PANEL.wave_2.
    - grep -n "insight-hero" src/components/board/board-types.ts and board-constants.ts show the GroupId + GROUP_FRAMES + AUTO_HEIGHT_FRAMES registration.
    - grep -n "InsightHeroFrame" src/components/board/Board.tsx shows the import + conditional render.
    - The existing board layout tests pass GREEN (no layout regression).
  </acceptance_criteria>
  <done>insight_hero panel paints on the Apollo stage (D-03); InsightHeroFrame is mounted in the board layout mirroring ContentAnalysisFrame; layout tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted apollo_reasoning -> board render | JSONB read from variants.apollo (and live top-level) rendered in the hero |
| LLM rewrite text -> DOM | rewrite.original/variant + dimension lever/evidence are model text rendered as content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | dual-read merge (variants.apollo) | mitigate | read-only render; the dual-read defensively falls back (variants.apollo ?? top-level ?? null) so a missing/partial shape renders empty, never crashes (Pitfall 2 guard) |
| T-05-08 | Info disclosure / XSS | rewrite + dimension text in DOM | accept | React escapes text content by default; no dangerouslySetInnerHTML; clipboard writes the variant string only. Apollo text already passes the engine's input-validation boundary upstream |
| T-05-09 | Tampering | dim.score read from untrusted JSONB | mitigate | render defensively (typeof dim.score === 'number'); the numeric bound is enforced engine-side in Plan 01 (zod + clamp); the UI does not trust it for logic, only display |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | N/A, zero external installs in P5 (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- npx vitest run src/components/board/__tests__/InsightHeroFrame.test.tsx -- all seven behavior cases GREEN incl. permalink dual-read.
- npx vitest run src/components/board/__tests__/ -- board layout tests green (no regression from the new frame).
- npx tsc --noEmit -- zero new errors.
- npm test (full suite) before wave merge.
- Manual/live (phase gate): a real run shows the hero on the fresh board AND on permalink reload; the insight is visually the hero with the score band demoted; a rewrite carries the drop-point label.
</verification>

<success_criteria>
- The insight-hero renders the full D-08 surface set (read, 3 rewrites struck-through+copyable, 6 scored dimensions, heatmap, demoted D-02 band, flop warning) with dual-read (no permalink blank).
- A rewrite is labelled with the fold heatmap biggest-drop mm:ss (D-07, R4 satisfied at surface-time).
- The hero paints on the Apollo stage via the insight_hero panel (D-03); it is the board hero, score demoted (D-08).
</success_criteria>

<output>
Create `.planning/phases/05-wire-surface/05-03-SUMMARY.md` when done.
</output>
