---
phase: 05-wire-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/engine/types.ts
  - src/lib/engine/apollo-core.ts
  - src/lib/engine/deepseek.ts
  - src/lib/engine/version.ts
  - src/lib/engine/__tests__/deepseek.test.ts
  - src/lib/engine/__tests__/aggregator.test.ts
autonomous: true
requirements: [R5, R7, R8]
must_haves:
  truths:
    - "Apollo composite_score is the deterministic weighted SUM of the 6 dimension anchors (D-01) — same video twice yields an identical composite"
    - "Each Apollo dimension carries a zod-bounded numeric score derived from its strong/mid/weak band (D-01)"
    - "Corpus §4 instructs band-anchored numeric dimension scores + a deterministic hook-weighted sum — the holistic-composite instruction is inverted (D-01)"
    - "ENGINE_VERSION is bumped past 3.7.0 so stale holistic-score rows do not serve from cache (D-01)"
  artifacts:
    - path: "src/lib/engine/types.ts"
      provides: "ApolloDimensionSchema with bounded numeric score field riding inside dimensions[]"
      contains: "score: z.number().min(0).max(100)"
    - path: "src/lib/engine/deepseek.ts"
      provides: "Post-parse deterministic weighted-sum overwrite of composite_score + clamp"
      contains: "HOOK_WEIGHT"
    - path: "src/lib/engine/apollo-core.ts"
      provides: "Inverted §4 output contract (band→numeric anchor + arithmetic composite)"
    - path: "src/lib/engine/version.ts"
      provides: "Bumped ENGINE_VERSION (cache invalidation)"
      contains: "ENGINE_VERSION ="
  key_links:
    - from: "src/lib/engine/deepseek.ts"
      to: "data.dimensions[].score"
      via: "weighted reduce → data.composite_score overwrite"
      pattern: "dimensions.*reduce|composite_score = Math"
    - from: "src/lib/engine/aggregator.ts"
      to: "deepseek.composite_score"
      via: "apollo_score blend term (unchanged — now reads a sum)"
      pattern: "composite_score"
---

<objective>
Land the D-01 rubric-sum engine contract: each Apollo dimension emits a band-anchored numeric `score`, and the 0–100 composite becomes a deterministic hook-weighted SUM of those six anchors instead of a separately-asked holistic judgment. This kills the ±5 swing on identical input (R8) and matches the banked P3 "score = composite of named dimensions, not a black box" principle.

Purpose: R5 (honest expert score, de-noised) + R8 (determinism). The composite stops being a provider-nondeterministic holistic guess and becomes byte-deterministic arithmetic over fixed band anchors.
Output: extended `ApolloDimensionSchema` (numeric score), inverted corpus §4, post-parse deterministic sum in `deepseek.ts`, bumped `ENGINE_VERSION`, and the Wave 0 test scaffolds (determinism + sum-identity + threading) the rest of the phase depends on.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-wire-surface/05-CONTEXT.md
@.planning/phases/05-wire-surface/05-RESEARCH.md
@.planning/phases/05-wire-surface/05-PATTERNS.md
@.planning/phases/05-wire-surface/05-VALIDATION.md

<interfaces>
<!-- Current Apollo output contract the executor edits. Extracted from codebase — no exploration needed. -->

src/lib/engine/types.ts:756-790 (CURRENT — band only, holistic composite):
  ApolloDimensionSchema = z.object({
    name: z.enum(["hook","retention","clarity","share_pull","substance","credibility"]),
    band: z.enum(["strong","mid","weak"]),   // D-01: ADD numeric `score` after this
    lever: z.string().min(1),
    evidence: z.string().min(1),
  })
  // DeepSeekResponseSchema.dimensions: z.array(ApolloDimensionSchema).length(6)
  // DeepSeekResponseSchema.composite_score: z.number().min(0).max(100)   // becomes a sum

src/lib/engine/deepseek.ts:141-172 — existing post-parse backstop:
  - data.composite_score = Math.min(100, Math.max(0, data.composite_score));  // clamp at :149
  - the R2 verbatim-hook backstop loop follows; D-01 sum inserts AFTER the clamp, BEFORE the rewrites check

src/lib/engine/apollo-core.ts:188-190 — §4 instruction to INVERT (CURRENT):
  "Grade each dimension on the 3-band scale ... not a 0–100 per dimension."
  "Give one composite 0–100: a holistic, hook-weighted judgment ... do NOT present the composite as arithmetic — it is a weighted judgment, not a sum of parts."
  Preserve lines 183-185 (hook ~80% weighting) UNCHANGED.

src/lib/engine/aggregator.ts:726 — reads composite_score (NO change needed; now a sum):
  const apollo_score = deepseek?.composite_score ?? 0;

src/lib/engine/version.ts:26 — ENGINE_VERSION = "3.7.0"  (D-23 cache invariant, line 23)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffolds — rubric-sum determinism + sum-identity + threading guard</name>
  <read_first>
    - src/lib/engine/__tests__/deepseek.test.ts (extend — existing post-parse + R2 backstop tests live here)
    - src/lib/engine/__tests__/aggregator.test.ts (extend — existing apollo_reasoning assembly tests live here)
    - src/lib/engine/deepseek.ts:141-172 (the post-parse block the new sum will occupy)
    - src/lib/engine/types.ts:756-790 (schema being extended)
    - .planning/phases/05-wire-surface/05-VALIDATION.md (Wave 0 Requirements list)
  </read_first>
  <behavior>
    - Test (deepseek.test.ts): given a parsed response with 6 dimensions each carrying a numeric `score`, `composite_score` post-parse == the deterministic hook-weighted sum (hook 0.80, the 5 body dims share 0.20) rounded + clamped — NOT the LLM-emitted holistic value (assert by feeding a deliberately-wrong emitted composite and proving it is overwritten by the sum).
    - Test (deepseek.test.ts determinism): the SAME dimension-score input parsed twice yields the byte-identical composite (the de-noise guarantee, R8).
    - Test (deepseek.test.ts schema): a response whose dimensions carry `score` in [0,100] parses; a dimension with `score` > 100 or < 0 is rejected by the zod bound (V5). A legacy band-only dimension (no `score`) is handled by the planned read-defensiveness — assert the schema decision (required-with-clamp vs optional) explicitly.
    - Test (aggregator.test.ts threading): after `aggregateScores`, `result.apollo_reasoning.dimensions[0].score` is a finite number (assembly-hop guard — proves the new field rides through assembly non-null).
  </behavior>
  <action>
    Add the four test cases above to the EXISTING files (do not create new test files — `deepseek.test.ts` and `aggregator.test.ts` already exist). These are RED scaffolds: they MUST reference the not-yet-added `score` field and the not-yet-written sum logic, so they fail until Tasks 2–4 land. Use the existing fixtures/builders in each file (the deepseek tests already build a parsed response object; extend its dimensions with `score`). For the determinism test, call the post-parse path twice with identical input and assert equal composites. Name the sum constant expectations to match Task 3 (HOOK_WEIGHT 0.80, body weight 0.20/5). Do NOT use watch mode.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/deepseek.test.ts src/lib/engine/__tests__/aggregator.test.ts 2>&1 | grep -E "score|composite|sum|threading" | head</automated>
  </verify>
  <acceptance_criteria>
    - The new test cases exist and FAIL with a clear reason tied to the missing `score` field / missing sum (RED state expected before Tasks 2–4).
    - `grep -n "score" src/lib/engine/__tests__/deepseek.test.ts` shows the new dimension-score assertions.
    - The threading test in aggregator.test.ts asserts `apollo_reasoning.dimensions[0].score` is finite.
  </acceptance_criteria>
  <done>Four Wave 0 cases added to the two existing test files, RED against the unbuilt contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bounded numeric score to ApolloDimensionSchema (types.ts)</name>
  <read_first>
    - src/lib/engine/types.ts:756-790 (ApolloDimensionSchema + DeepSeekResponseSchema + ApolloDimension type)
    - src/lib/engine/types.ts:282-300 (apollo_reasoning optional-field pattern on PredictionResult — the additive-extension analog)
    - .planning/phases/05-wire-surface/05-PATTERNS.md (§ "types.ts — add bounded `score`" + § "Zod schema extension — additive")
  </read_first>
  <action>
    In `ApolloDimensionSchema` (types.ts:756) add a `score: z.number().min(0).max(100)` field immediately after `band` (V5 — LLM output is untrusted; the zod bound is the boundary defense). The `score` is the numeric anchor mapped from the strong/mid/weak band; keep `band` for UI display (D-02 / D-08 render both). Do NOT add a parallel top-level field and do NOT touch the persist path — the field rides inside the existing `dimensions[]` array which already flows through `apollo_reasoning → variants.apollo` (assembly-hop rule). The `ApolloDimension` type (line 763, `z.infer`) updates automatically. Leave `DeepSeekResponseSchema.composite_score` as `z.number().min(0).max(100)` — it stays a number, just now produced as a sum. Per CLAUDE.md keep the file under 500 lines (it is large already — add only the one field, no restructuring).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "types.ts" | head; grep -n "score: z.number().min(0).max(100)" src/lib/engine/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "score: z.number().min(0).max(100)" src/lib/engine/types.ts` returns exactly one line inside ApolloDimensionSchema.
    - tsc reports zero new errors attributable to types.ts.
    - The schema-parse Wave 0 case from Task 1 (valid score parses, out-of-range rejected) now passes.
  </acceptance_criteria>
  <done>ApolloDimensionSchema carries a bounded numeric score; type inference updated; no parallel column added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deterministic rubric-sum in deepseek.ts post-parse + invert corpus §4</name>
  <read_first>
    - src/lib/engine/deepseek.ts:141-172 (post-parse backstop — exact insertion point after the :149 clamp)
    - src/lib/engine/apollo-core.ts:183-219 (§4 block: line 183-185 hook-weight to PRESERVE; line 186 "Grading discipline … holistic, not mechanical … don't tally satisfied principles" to RECONCILE; lines 188-190 output contract to INVERT)
    - .planning/phases/05-wire-surface/05-PATTERNS.md (§ "deepseek.ts — post-parse rubric-sum site" + § "apollo-core.ts — invert §4")
    - .planning/phases/05-wire-surface/05-RESEARCH.md (Pitfall 5 — must edit corpus AND schema AND post-parse together)
  </read_first>
  <action>
    Two edits in one task (Pitfall 5 — they must land together or the LLM emits inconsistent scores vs composite):
    (a) `deepseek.ts`: after the existing `data.composite_score = Math.min(100, Math.max(0, data.composite_score))` clamp at line 149 and BEFORE the rewrites-length check, compute the deterministic weighted sum over `data.dimensions[].score` and OVERWRITE `data.composite_score`. Use `HOOK_WEIGHT = 0.80` for the dimension named "hook" and `BODY_WEIGHT = 0.20 / 5` for each of the other 5; `data.composite_score = Math.min(100, Math.max(0, Math.round(sum)))`. The existing clamp idiom is the defense-in-depth pattern — keep the clamp on the sum output. Do NOT re-ask the LLM for a holistic composite.
    (b) `apollo-core.ts` §4: invert the output-contract bullets (lines 188-190) AND reconcile the grading-discipline line (line 186) so the whole §4 block is internally consistent with band→fixed-anchor + arithmetic composite:
      - Lines 188-190 (output contract): each dimension now gets a 3-band grade AND a numeric score anchored to that band (Strong/Mid/Weak → high/mid/low fixed numeric anchors); the composite IS a deterministic hook-weighted sum that TypeScript computes from the per-dimension scores. Remove the "do not present the composite as arithmetic / not a sum of parts" and "do not fabricate per-dimension numeric sub-scores" instructions (they now contradict D-01).
      - Line 186 (grading discipline): the current "holistic, *not* mechanical — don't tally satisfied principles … numbers that *inform* a band, not gates that auto-set it" language tells the LLM to grade the COMPOSITE holistically, which re-introduces the ±5 swing D-01 kills. Reconcile it: keep the honest-grading intent at the PER-DIMENSION band level (judge each dimension's band by craft, not a mechanical principle-tally), but make explicit that the COMPOSITE is no longer a holistic judgment — it is the deterministic sum of the band-anchored scores. The band→fixed-anchor mapping is the gate, not an informer. Do NOT leave any line implying the composite is a holistic/weighted-judgment whole.
      Preserve the §2.0a hook ~80% weighting language (lines 183-185) byte-for-byte and the §4.1 platform-calibration + confidence-scope lines unchanged. The corpus is a byte-stable cached prompt — change only the contradicting lines (186 + 188-190).
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/deepseek.test.ts 2>&1 | tail -20; grep -n "HOOK_WEIGHT" src/lib/engine/deepseek.ts; grep -c "do **not** present the composite as arithmetic" src/lib/engine/apollo-core.ts</automated>
  </verify>
  <acceptance_criteria>
    - The Task 1 sum-identity test passes: post-parse `composite_score` == the hook-weighted sum, overwriting a deliberately-wrong emitted value.
    - The Task 1 determinism test passes: identical dimension scores → identical composite, twice.
    - `grep -n "HOOK_WEIGHT" src/lib/engine/deepseek.ts` shows the weight constant.
    - The inverted corpus no longer instructs "do not present the composite as arithmetic" (grep count for the old phrase is 0).
    - The §4 grading-discipline line (186) no longer frames the COMPOSITE as a holistic judgment: `grep -c "holistic" src/lib/engine/apollo-core.ts` within §4 shows no surviving instruction that the composite is holistic/a weighted judgment of the whole (per-dimension band honesty may remain; the composite-as-sum statement is present). §4 is internally consistent — band→fixed-anchor + arithmetic composite, no latent holistic-composite drift.
  </acceptance_criteria>
  <done>composite_score is a deterministic weighted sum of the band-anchored dimension scores; corpus §4 agrees with the schema + post-parse arithmetic.</done>
</task>

<task type="auto">
  <name>Task 4: Bump ENGINE_VERSION (cache invalidation)</name>
  <read_first>
    - src/lib/engine/version.ts:20-26 (ENGINE_VERSION + D-23 cache invariant comment)
    - .planning/phases/05-wire-surface/05-RESEARCH.md (Pitfall 3 — stale cache serves pre-D-01 scores)
  </read_first>
  <action>
    Bump `ENGINE_VERSION` from `"3.7.0"` to `"3.8.0"` (version.ts:26). D-01 changes the score for the same video, and the prediction cache keys on `hash::ENGINE_VERSION::userId` (D-23 invariant) — the bump auto-invalidates all 3.7.0 L1+L2 cached rows on next analyze call. Update the version-history comment block above the constant to note the D-01 rubric-sum reason (mirror the existing 3.7.0 omni-flash comment style). MANDATORY and must land in the same commit as Tasks 2–3.
  </action>
  <verify>
    <automated>grep -n 'ENGINE_VERSION = "3.8.0"' src/lib/engine/version.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'ENGINE_VERSION = "3.8.0"' src/lib/engine/version.ts` returns the bumped constant.
    - The version-history comment names the D-01 rubric-sum cache-invalidation reason.
  </acceptance_criteria>
  <done>ENGINE_VERSION = "3.8.0"; stale holistic-score rows can no longer serve from cache.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM (Apollo/Qwen) → engine | Apollo's per-dimension `score` is untrusted model output crossing into the score derivation |
| engine cache → user | A stale cache key could serve a pre-D-01 score (integrity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `ApolloDimensionSchema.score` (deepseek.ts parse) | mitigate | zod `.min(0).max(100)` on the new field (V5) + post-parse clamp on the summed composite (deepseek.ts:149 idiom) — defense-in-depth even after schema validation |
| T-05-02 | Tampering (integrity) | prediction cache (version.ts) | mitigate | bump ENGINE_VERSION 3.7.0→3.8.0 — keys on `hash::ENGINE_VERSION::userId`, auto-invalidates stale holistic-score rows (D-23) |
| T-05-03 | Tampering | corpus §4 prompt (apollo-core.ts) | accept | byte-stable cached prompt; only the two contradicting bullets change; no user input flows into the corpus |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | N/A this phase — RESEARCH §"Package Legitimacy Audit" confirms zero external installs in P5; no [ASSUMED]/[SUS] packages to gate |
</threat_model>

<verification>
- `npx vitest run src/lib/engine/__tests__/deepseek.test.ts src/lib/engine/__tests__/aggregator.test.ts` — all new Wave 0 cases GREEN.
- `npx tsc --noEmit` — zero new errors.
- `npm test` (full suite) before wave merge — threading regressions surface here.
- Manual/live (phase gate, not this plan): one real `/analyze` run proving `variants.apollo.dimensions[].score` non-null in the DB row; `scripts/measure-pipeline.ts` ≤90s with fold ∥ Apollo unchanged.
</verification>

<success_criteria>
- composite_score == deterministic hook-weighted sum of the 6 band-anchored dimension scores (D-01); identical input → identical composite (R8).
- ApolloDimensionSchema carries a zod-bounded `score`; corpus §4 + post-parse + schema all agree (no Pitfall 5 inconsistency).
- ENGINE_VERSION bumped to 3.8.0 (cache invalidation, D-01).
- Wave 0 determinism + sum-identity + threading scaffolds GREEN.
</success_criteria>

<output>
Create `.planning/phases/05-wire-surface/05-01-SUMMARY.md` when done.
</output>
