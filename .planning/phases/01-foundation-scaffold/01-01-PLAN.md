---
phase: 01-foundation-scaffold
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/app/(marketing)/layout.tsx
  - src/app/(marketing)/motionTokens.ts
  - src/app/(marketing)/_components/.gitkeep
  - src/app/(marketing)/_data/.gitkeep
  - src/app/(marketing)/_hooks/.gitkeep
  - src/app/(marketing)/v3/.gitkeep
  - public/landing/placeholders/.gitkeep
autonomous: true
requirements: [FOUND-02, FOUND-03, FOUND-06, FOUND-07, MOTION-04, MOTION-05, POLISH-07]
must_haves:
  truths:
    - "Single animation runtime: `pnpm why framer-motion` reports motion@^12.29.2 (no duplicate framer-motion chunk)"
    - "Marketing routes no longer emit duplicate <html>/<body> (only root layout owns them)"
    - "Marketing layout no longer carries the stale 'Artificial Societies' title"
    - "motionTokens.ts exports the shared durations/easings/staggerDelays/viewportThresholds constants"
    - "_components/, _data/, _hooks/, v3/ directories exist under src/app/(marketing)/"
    - "public/landing/placeholders/ directory exists"
  artifacts:
    - path: "package.json"
      provides: "pnpm.overrides alias for framer-motion → motion@^12.29.2"
      contains: "\"framer-motion\": \"npm:motion@^12.29.2\""
    - path: "src/app/(marketing)/layout.tsx"
      provides: "Fragment-style marketing layout with Header (no <html>/<body>)"
      contains: "MarketingLayout"
    - path: "src/app/(marketing)/motionTokens.ts"
      provides: "Shared motion constants for all landing animations"
      contains: "export const motionTokens"
    - path: "src/app/(marketing)/_components/.gitkeep"
      provides: "Route-private components directory"
    - path: "src/app/(marketing)/_data/.gitkeep"
      provides: "Route-private data directory"
    - path: "src/app/(marketing)/_hooks/.gitkeep"
      provides: "Route-private hooks directory"
    - path: "src/app/(marketing)/v3/.gitkeep"
      provides: "Staging route directory placeholder"
    - path: "public/landing/placeholders/.gitkeep"
      provides: "Placeholder asset namespace"
  key_links:
    - from: "package.json pnpm.overrides"
      to: "node_modules motion runtime"
      via: "pnpm install resolution"
      pattern: "framer-motion.*npm:motion"
    - from: "src/app/(marketing)/layout.tsx"
      to: "src/app/layout.tsx (root)"
      via: "removed duplicate <html>/<body> declarations"
      pattern: "MarketingLayout"
---

<objective>
Foundation pre-work that every other Phase 1 plan depends on: alias framer-motion to motion via pnpm.overrides (single animation runtime), fix the duplicate `<html>`/`<body>` bug + stale "Artificial Societies" title in the marketing layout, create the shared motionTokens.ts constants file, and scaffold the four route-private directories (`_components/`, `_data/`, `_hooks/`, `v3/`) plus the `public/landing/placeholders/` namespace.

Purpose: Implements REQ FOUND-02 (layout bug + stale title), FOUND-03 (pnpm.overrides single-runtime), FOUND-06 (_components/_data dirs), FOUND-07 (placeholder dir), MOTION-04 (Lightning CSS workaround pattern locked via motionTokens consumers), MOTION-05 (oklch dark-color guardrail referenced from comment in motionTokens for downstream awareness), POLISH-07 (motionTokens.ts shared constants). Without these, Plans 02/03/05 cannot land cleanly — they would either (a) introduce a second animation runtime, (b) inherit the broken marketing layout, or (c) hardcode magic numbers that diverge across sections.

Output: Modified package.json (+ pnpm-lock.yaml), simplified marketing layout, new motionTokens.ts, four placeholder dirs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-scaffold/01-UI-SPEC.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@package.json
@src/app/layout.tsx
@src/app/(marketing)/layout.tsx
@src/components/layout/header.tsx

<interfaces>
<!-- Root layout (src/app/layout.tsx) ALREADY owns <html lang="en"> + <body className="min-h-screen bg-background font-sans antialiased"> + Inter font wiring. -->
<!-- Marketing layout MUST become a fragment that only wraps children with <Header />. -->

From src/app/layout.tsx (existing, do not modify here):
```typescript
export const viewport: Viewport = { width: "device-width", initialScale: 1 };
export const metadata: Metadata = { metadataBase: new URL("https://virtuna.ai"), title: "...", ... };
// <html lang="en" className={`${inter.variable}`}>
// <body className="min-h-screen bg-background font-sans antialiased">{children}<DevClickToComponent /></body>
```

From src/components/layout/header.tsx:
```typescript
export function Header(): JSX.Element  // existing app/dashboard chrome — keep wiring it from marketing layout
```

motionTokens.ts contract (per UI-SPEC.md § motionTokens.ts Contract — copy verbatim):
```typescript
export const motionTokens = {
  durations: { fast: 0.15, normal: 0.2, slow: 0.3, slower: 0.5, hero: 0.8 },
  easings: {
    outCubic: [0.215, 0.61, 0.355, 1] as const,
    outQuart: [0.165, 0.84, 0.44, 1] as const,
    inOut: [0.42, 0, 0.58, 1] as const,
    spring: [0.34, 1.56, 0.64, 1] as const,
  },
  staggerDelays: { children: 0.08, tight: 0.04, wide: 0.15 },
  viewportThresholds: { default: 0.2, hero: 0, early: 0.1, late: 0.4 },
  triggerOnce: true,
} as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pnpm.overrides alias + run pnpm install + verify single runtime</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (full file — confirm current dependencies block and absence of any existing "pnpm" or "overrides" top-level key)
    - .planning/research/STACK.md (§ 1 Magic UI — confirms motion/react import path; § Pitfall references)
    - .planning/research/PITFALLS.md (Pitfall 1 framer-motion vs motion dual-runtime — implements the documented workaround)
    - .planning/phases/01-foundation-scaffold/01-UI-SPEC.md (§ framer-motion Alias Contract — verification command)
  </read_first>
  <action>
    Edit package.json to add a top-level "pnpm" block per FOUND-03 / D-pinned UI-SPEC. The exact JSON to insert (sibling to "dependencies" / "devDependencies", at the end of the file before the closing `}`):

    ```json
    "pnpm": {
      "overrides": {
        "framer-motion": "npm:motion@^12.29.2"
      }
    }
    ```

    Use the Edit tool — do NOT rewrite the whole file. Place the "pnpm" block after the existing "devDependencies" object (mind the trailing comma on the preceding closing brace).

    After the edit:
    1. Run `pnpm install` from repo root to regenerate pnpm-lock.yaml under the new override resolution.
    2. Verify single runtime with `pnpm why framer-motion` — output MUST show framer-motion resolved to `motion@12.29.x` (the override flattened the legacy package name into the modern one). If pnpm still lists a second separate framer-motion install, the override failed — fix before proceeding.
    3. Run `pnpm exec tsc --noEmit` to confirm TypeScript still compiles — the alias must not break any existing `from "framer-motion"` imports (motion re-exports the same surface).

    Pitfall to avoid: do NOT add the override to "devDependencies" or "dependencies" — it must be under a top-level `"pnpm": { "overrides": {} }` block exactly as shown. Wrong placement is silently ignored by pnpm.

    Per UI-SPEC § framer-motion Alias Contract: implements FOUND-03 + MOTION-05 prerequisite (single-runtime guarantees motion's `MotionConfig reducedMotion="user"` actually controls everything downstream).
  </action>
  <verify>
    <automated>pnpm why framer-motion 2>&1 | grep -E "motion@12\.29" && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"framer-motion": "npm:motion@\^12.29.2"' package.json` returns 1
    - `pnpm why framer-motion` output contains the substring `motion@12.29` (not a separate `framer-motion@12.29.3` install)
    - `pnpm exec tsc --noEmit` exits 0 (no new TypeScript errors from the alias)
    - pnpm-lock.yaml diff shows framer-motion entries resolving to the motion package
  </acceptance_criteria>
  <done>package.json has the pnpm.overrides block; pnpm install completed; single runtime verified by `pnpm why framer-motion`; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix duplicate &lt;html&gt;/&lt;body&gt; bug + stale title in marketing layout</name>
  <files>src/app/(marketing)/layout.tsx</files>
  <read_first>
    - src/app/(marketing)/layout.tsx (full current file — confirms duplicate &lt;html&gt; + stale "Artificial Societies | Human Behavior, Simulated" title + redundant `import "../globals.css"`)
    - src/app/layout.tsx (full current file — confirms root already owns &lt;html lang="en"&gt; + &lt;body&gt; + globals.css + Inter font + metadata)
    - .planning/research/PITFALLS.md (Pitfall 12 — Duplicate &lt;html&gt;/&lt;body&gt; root cause)
    - .planning/research/ARCHITECTURE.md (§ 1 verified existing state table — confirms this is the bug to fix)
    - .planning/phases/01-foundation-scaffold/01-UI-SPEC.md (§ Component Inventory — Existing components row for layout.tsx)
    - src/components/layout/header.tsx (confirm Header export signature; keep wiring it so dashboard chrome doesn't disappear from current live landing during transition)
  </read_first>
  <action>
    Rewrite `src/app/(marketing)/layout.tsx` per FOUND-02 so it becomes a fragment-style passthrough that only wraps children with the existing `<Header />`. Concretely:

    1. REMOVE the `import { Inter } from "next/font/google"` line — root layout owns the font.
    2. REMOVE the `import "../globals.css"` line — root layout already imports it.
    3. REMOVE the local `const inter = Inter({...})` initialization.
    4. REMOVE the entire `<html lang="en" className={...}>` and `<body className="...">` wrapper. Only the root may declare them.
    5. REPLACE the stale `metadata` export. Per UI-SPEC.md § Copywriting Contract, the page-level &lt;title&gt; for Phase 1 is `"Virtuna | Predict viral before you post"` and that lives on `/v3/page.tsx` (handled in Plan 05). The marketing layout itself should NOT export a title that overrides root — delete the local `metadata` export entirely so the root layout's metadata cascades through. (Per Next.js: nested layouts inherit parent metadata unless they explicitly override; deleting the export = inherit.)
    6. The final component body should return a React fragment containing `<Header />` and `{children}`. Keep `MarketingLayout` as the default export name.

    Final file shape (target — write exactly this content via Edit, do not rewrite-via-Write if Edit can do it; this layout file is short enough that Write is acceptable too):

    ```tsx
    import { Header } from "@/components/layout/header";

    export default function MarketingLayout({
      children,
    }: Readonly<{ children: React.ReactNode }>) {
      return (
        <>
          <Header />
          {children}
        </>
      );
    }
    ```

    Pitfall to avoid: do NOT delete `(marketing)/layout.tsx` outright — it provides the route-group wrapper and the Header. Just slim it to a fragment.

    Per UI-SPEC § Component Inventory > Existing components (modify) row. Implements FOUND-02 (both halves: duplicate-html fix AND stale-title removal — by deletion of the metadata export, root's "Virtuna | AI Content Intelligence..." takes over for the live `/` route until cutover; the new `/v3` route ships its own page-level title in Plan 05).
  </action>
  <verify>
    <automated>grep -L "&lt;html" src/app/\(marketing\)/layout.tsx &amp;&amp; grep -L "Artificial Societies" src/app/\(marketing\)/layout.tsx &amp;&amp; grep -L "import.*globals.css" src/app/\(marketing\)/layout.tsx &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<html' src/app/\(marketing\)/layout.tsx` returns 0
    - `grep -c '<body' src/app/\(marketing\)/layout.tsx` returns 0
    - `grep -c 'Artificial Societies' src/app/\(marketing\)/layout.tsx` returns 0
    - `grep -c "import.*globals.css" src/app/\(marketing\)/layout.tsx` returns 0
    - `grep -c "import.*Header" src/app/\(marketing\)/layout.tsx` returns 1
    - `grep -c "export default function MarketingLayout" src/app/\(marketing\)/layout.tsx` returns 1
    - `pnpm exec tsc --noEmit` exits 0
    - `pnpm dev` (manual spot-check, not part of automated gate) emits no "<html> cannot appear as a child of <html>" hydration warning when navigating to `/`
  </acceptance_criteria>
  <done>Marketing layout is a fragment that mounts Header + children only; no duplicate HTML root elements; no stale title; root metadata cascades down; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Create motionTokens.ts + scaffold _components/_data/_hooks/v3/placeholders directories</name>
  <files>src/app/(marketing)/motionTokens.ts, src/app/(marketing)/_components/.gitkeep, src/app/(marketing)/_data/.gitkeep, src/app/(marketing)/_hooks/.gitkeep, src/app/(marketing)/v3/.gitkeep, public/landing/placeholders/.gitkeep</files>
  <read_first>
    - .planning/phases/01-foundation-scaffold/01-UI-SPEC.md (§ motionTokens.ts Contract — copy the entire code block verbatim)
    - .planning/research/ARCHITECTURE.md (§ 1 Component co-location convention — confirms the four directory names)
    - .planning/research/PITFALLS.md (Pitfall 3 oklch L &lt; 0.15 — referenced in the file header comment so future contributors are aware before adding color tokens here)
    - CLAUDE.md (Tailwind v4 oklch quirk + Lightning CSS backdrop-filter rules — referenced in file header)
  </read_first>
  <action>
    Create one TypeScript constants file and five `.gitkeep` placeholder files. Use the Write tool for each.

    File 1 — `src/app/(marketing)/motionTokens.ts`:
    Copy the contract from UI-SPEC.md § motionTokens.ts Contract VERBATIM (the values are locked: durations 0.15/0.2/0.3/0.5/0.8; easings outCubic/outQuart/inOut/spring with the four bezier tuples; staggerDelays 0.08/0.04/0.15; viewportThresholds 0.2/0/0.1/0.4; triggerOnce: true). Use `as const` on the outer object so the type narrows to readonly literal tuples (required for motion/react's easing parameter type).

    Add a JSDoc header above the export per POLISH-07 traceability:

    ```typescript
    /**
     * motionTokens.ts — Shared motion constants for Landing v1.
     *
     * Per POLISH-07: every section animation imports from here for cross-section consistency.
     * Per MOTION-05 / CLAUDE.md: when adding color tokens, use HEX for L < 0.15 (Tailwind v4 oklch
     *   compilation drift) — pure number constants here are unaffected.
     * Per MOTION-04 / CLAUDE.md: keyframe additions to globals.css that use backdrop-filter or
     *   mask-image must be applied via inline style={} in the consuming component (Lightning CSS
     *   strips utility classes); this file holds duration/easing tokens only.
     */
    ```

    File 2-6 — Empty `.gitkeep` files at:
    - `src/app/(marketing)/_components/.gitkeep`
    - `src/app/(marketing)/_data/.gitkeep`
    - `src/app/(marketing)/_hooks/.gitkeep`
    - `src/app/(marketing)/v3/.gitkeep`
    - `public/landing/placeholders/.gitkeep`

    Each `.gitkeep` is a zero-byte file (or single-line file containing only a newline) — its sole purpose is to make git track an otherwise-empty directory. Write empty content `""` via the Write tool.

    Per UI-SPEC § Component Inventory > New files (non-component) + ARCHITECTURE.md § 1 Component co-location convention. Implements FOUND-06 (_components + _data dirs), FOUND-07 (public/landing/placeholders), POLISH-07 (motionTokens.ts).
  </action>
  <verify>
    <automated>test -f src/app/\(marketing\)/motionTokens.ts &amp;&amp; test -d src/app/\(marketing\)/_components &amp;&amp; test -d src/app/\(marketing\)/_data &amp;&amp; test -d src/app/\(marketing\)/_hooks &amp;&amp; test -d src/app/\(marketing\)/v3 &amp;&amp; test -d public/landing/placeholders &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/app/\(marketing\)/motionTokens.ts` exits 0
    - `grep -c "export const motionTokens" src/app/\(marketing\)/motionTokens.ts` returns 1
    - `grep -c "as const" src/app/\(marketing\)/motionTokens.ts` returns at least 5 (outer object + 4 easing tuples)
    - `grep -cE "0\.215, 0\.61, 0\.355, 1" src/app/\(marketing\)/motionTokens.ts` returns 1 (outCubic bezier)
    - `grep -cE "fast: 0\.15" src/app/\(marketing\)/motionTokens.ts` returns 1
    - `grep -cE "triggerOnce: true" src/app/\(marketing\)/motionTokens.ts` returns 1
    - `ls src/app/\(marketing\)/_components/.gitkeep src/app/\(marketing\)/_data/.gitkeep src/app/\(marketing\)/_hooks/.gitkeep src/app/\(marketing\)/v3/.gitkeep public/landing/placeholders/.gitkeep` all resolve
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>motionTokens.ts exports the locked constants object with `as const`; five `.gitkeep` files exist; tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supply chain → npm install | pnpm.overrides alias resolves `framer-motion` to a remote `motion` package; integrity hash in pnpm-lock.yaml is the only verification |
| dev → production CSS pipeline | Lightning CSS in `next build` strips certain CSS properties silently; affects components in later plans but seeded here via motionTokens header comment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | package.json pnpm.overrides | mitigate | Lockfile-pinned: `pnpm install` writes a hash-verified entry to pnpm-lock.yaml. Verification command `pnpm why framer-motion` confirms the override resolved to the expected `motion@12.29.x` and not a substituted package. |
| T-01-02 | D (Denial of Service) | dev experience after layout edit | mitigate | After removing &lt;html&gt;/&lt;body&gt; from `(marketing)/layout.tsx`, root layout retains them — `pnpm exec tsc --noEmit` gates against accidental deletion of the wrapper file itself, and the verify step keeps `<Header />` wired so dashboard chrome is preserved. |
| T-01-03 | I (Information Disclosure) | stale "Artificial Societies" title | mitigate | Stale brand title removed in Task 2; reduces risk of leaking abandoned brand identity in search engine cache. |
| T-01-04 | S (Spoofing) | _components/_data/_hooks/v3/ placeholder dirs | accept | Empty directory scaffolds with .gitkeep — no executable content, no attack surface. |
</threat_model>

<verification>
End-of-plan smoke checks (in addition to per-task automated gates):

```bash
# Single runtime
pnpm why framer-motion 2>&1 | grep -q "motion@12.29" && echo "OK: single runtime"

# Marketing layout is a fragment
[ "$(grep -c '<html' src/app/\(marketing\)/layout.tsx)" -eq 0 ] && echo "OK: no duplicate html"

# Stale title gone
[ "$(grep -c 'Artificial Societies' src/app/\(marketing\)/layout.tsx)" -eq 0 ] && echo "OK: stale title removed"

# motionTokens.ts shape
grep -q "export const motionTokens" src/app/\(marketing\)/motionTokens.ts && echo "OK: motionTokens exported"

# Directories exist (gitkeep-tracked)
for d in _components _data _hooks v3; do
  [ -d "src/app/(marketing)/$d" ] && echo "OK: dir $d"
done
[ -d "public/landing/placeholders" ] && echo "OK: placeholders dir"

# TypeScript compiles
pnpm exec tsc --noEmit && echo "OK: tsc clean"
```

All five `OK:` lines + `OK: tsc clean` must print.
</verification>

<success_criteria>
- pnpm.overrides aliases framer-motion → motion@^12.29.2; `pnpm why framer-motion` confirms single runtime
- `src/app/(marketing)/layout.tsx` is a fragment-style component (no <html>, no <body>, no globals.css re-import, no stale "Artificial Societies" title); still wires `<Header />` + children
- `src/app/(marketing)/motionTokens.ts` exports the locked constants object (durations/easings/staggerDelays/viewportThresholds + triggerOnce) with `as const`
- All four route-private directories (`_components/`, `_data/`, `_hooks/`, `v3/`) and the `public/landing/placeholders/` asset namespace exist (tracked via `.gitkeep`)
- `pnpm exec tsc --noEmit` exits 0 — no type regressions from the alias or layout edit
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-scaffold/01-01-SUMMARY.md` per the standard summary template.
</output>
