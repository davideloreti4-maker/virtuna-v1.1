---
phase: 01-foundation-scaffold
plan: "05"
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - src/app/(marketing)/v3/page.tsx
  - src/app/(marketing)/v3/MotionRoot.tsx
autonomous: true
requirements: [FOUND-01, FOUND-04, FOUND-05, FOUND-09, META-03, PERF-13]
must_haves:
  truths:
    - "User can navigate to `/v3` in the browser/Vercel preview and see all 9 placeholder section blocks render in order (Hero → Demo → How It Works → Three Surfaces → Comparison → The Science → Social Proof → Pricing → Final CTA) without console errors or hydration warnings"
    - "Page title displayed in the browser tab is exactly 'Virtuna | Predict viral before you post' (no 'Artificial Societies' residue)"
    - "Skip-to-content link appears on first Tab keypress before any other focusable element (PERF-11 — the link itself ships in Plan 02; this plan wires it into the page)"
    - "<html lang='en'> is set (provided by root layout — this plan verifies cascade through grep)"
    - "Page-root MotionConfig reducedMotion='user' wraps all 9 SectionShell instances (single kill-switch for motion/react animations across all phases)"
    - "Page <main id='main-content'> wraps the 9 sections — skip-link target lands here"
    - "Page metadata exports include OG title/description matching UI-SPEC § SEO Baseline placeholder OG description (≤ 160 chars)"
    - "Pre-install collision check is documented as part of FOUND-05 — existing src/components/ui/marquee.tsx + src/hooks/useCountUp.ts are NOT touched; no Magic UI Marquee or NumberTicker is installed in Phase 1 (deferred to Phase 2+)"
  artifacts:
    - path: "src/app/(marketing)/v3/page.tsx"
      provides: "/v3 staging route page assembling 9 SectionShell placeholders under MotionConfig + GrainOverlay + SkipToContent + LandingHeader + main landmark"
      exports: ["default", "metadata"]
      min_lines: 80
    - path: "src/app/(marketing)/v3/MotionRoot.tsx"
      provides: "Client-component wrapper around motion's MotionConfig reducedMotion='user' (lets page.tsx remain a server component while still wrapping client children)"
      exports: ["MotionRoot"]
      min_lines: 15
  key_links:
    - from: "src/app/(marketing)/v3/page.tsx"
      to: "src/app/(marketing)/_components/SectionShell.tsx"
      via: "imports SectionShell + renders 9 instances with id slugs matching LandingHeader anchors"
      pattern: "from.*_components/SectionShell"
    - from: "src/app/(marketing)/v3/page.tsx"
      to: "src/app/(marketing)/_components/LandingHeader.tsx"
      via: "imports LandingHeader + renders once at top"
      pattern: "from.*_components/LandingHeader"
    - from: "src/app/(marketing)/v3/page.tsx"
      to: "src/app/(marketing)/_components/SkipToContent.tsx"
      via: "imports SkipToContent + renders first inside <body>"
      pattern: "from.*_components/SkipToContent"
    - from: "src/app/(marketing)/v3/page.tsx"
      to: "src/app/(marketing)/_components/GrainOverlay.tsx"
      via: "imports GrainOverlay + renders once for cross-cutting noise layer"
      pattern: "from.*_components/GrainOverlay"
    - from: "src/app/(marketing)/v3/MotionRoot.tsx"
      to: "motion/react"
      via: "imports MotionConfig from motion/react (NOT framer-motion)"
      pattern: "from ['\"]motion/react['\"]"
    - from: "src/app/(marketing)/v3/page.tsx <main id='main-content'>"
      to: "src/app/(marketing)/_components/SkipToContent.tsx href='#main-content'"
      via: "skip-link target attribute"
      pattern: "id=\"main-content\""
---

<objective>
Assemble the `/v3` staging route page that ties together everything from Plans 01-04: import LandingHeader, SkipToContent, GrainOverlay, and SectionShell, render the 9 placeholder sections in narrative order under a page-root `MotionConfig reducedMotion="user"` wrapper, set the locked page title ("Virtuna | Predict viral before you post"), and document the FOUND-05 collision-check decision (skip Magic UI Marquee + NumberTicker in Phase 1).

Purpose: Implements FOUND-01 (9 placeholder sections visible at `/v3` — the single most important deliverable of Phase 1), FOUND-04 (Magic UI/Aceternity install pattern locked — concretely: nothing installed in Phase 1 because no section component needs it yet beyond optional DotPattern, deferred), FOUND-05 (pre-install collision-check documented via the explicit decision NOT to install Magic UI Marquee or NumberTicker), FOUND-09 (MotionConfig reducedMotion="user" page-root wrapper), META-03 (page metadata title + OG description placeholder for Landing v1 identity), PERF-13 (verifies <html lang="en"> via grep on root layout — no edit needed, root already correct).

Output: Two new files — `src/app/(marketing)/v3/page.tsx` (the route page) and `src/app/(marketing)/v3/MotionRoot.tsx` (a tiny client wrapper so page.tsx can remain a server component).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-scaffold/01-UI-SPEC.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md
@src/app/layout.tsx
@src/app/(marketing)/_components/SectionShell.tsx
@src/app/(marketing)/_components/GrainOverlay.tsx
@src/app/(marketing)/_components/SkipToContent.tsx
@src/app/(marketing)/_components/LandingHeader.tsx
@src/app/(marketing)/motionTokens.ts

<interfaces>
<!-- Plan 02 provides SectionShell + GrainOverlay + SkipToContent. -->
<!-- Plan 03 provides LandingHeader. -->
<!-- Plan 01 provides motionTokens (consumed by future phases, not Phase 1 directly) and the pnpm.overrides alias. -->

SectionShell signature (from Plan 02):
```typescript
export interface SectionShellProps {
  id: 'hero' | 'demo' | 'how-it-works' | 'surfaces' | 'comparison' | 'science' | 'social-proof' | 'pricing' | 'final-cta';
  label: string;
  shipsInPhase: number;
  variant: 'odd' | 'even';
  isHero?: boolean;
}
export function SectionShell(props: SectionShellProps): JSX.Element;
```

GrainOverlay signature (from Plan 02):
```typescript
export function GrainOverlay(): JSX.Element; // no props
```

SkipToContent signature (from Plan 02):
```typescript
export function SkipToContent(): JSX.Element; // no props; href="#main-content"
```

LandingHeader signature (from Plan 03):
```typescript
export function LandingHeader(): JSX.Element; // no props
```

MotionConfig contract (from motion package, aliased from framer-motion via Plan 01 pnpm.overrides):
```typescript
import { MotionConfig } from 'motion/react';
<MotionConfig reducedMotion="user">{children}</MotionConfig>
// `reducedMotion="user"` reads OS prefers-reduced-motion media query
// per UI-SPEC § MotionConfig Root Contract + PITFALLS.md Pitfall 5
```

The 9 sections in narrative order with phase-ship mapping (from ROADMAP.md):
| Order | id | label | shipsInPhase | variant | isHero |
|---|---|---|---|---|---|
| 1 | hero | Hero | 2 | odd | true |
| 2 | demo | Demo | 4 | even | false |
| 3 | how-it-works | How It Works | 5 | odd | false |
| 4 | surfaces | Three Surfaces | 6 | even | false |
| 5 | comparison | Comparison | 7 | odd | false |
| 6 | science | The Science | 8 | even | false |
| 7 | social-proof | Social Proof | 9 | odd | false |
| 8 | pricing | Pricing | 10 | even | false |
| 9 | final-cta | Final CTA | 2 | odd | false |

(Hero ships in Phase 2 per ROADMAP; Final CTA also ships in Phase 2 as the Hero bookend per ROADMAP "Phase 2: Hero Shell + Final CTA Bookend + Vision Beat".)

Existing root layout (src/app/layout.tsx) already provides `<html lang="en">` + `<body>` + Inter font + global metadata — verified in Plan 01's reading. This plan does NOT modify root layout.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MotionRoot.tsx (client wrapper around MotionConfig)</name>
  <files>src/app/(marketing)/v3/MotionRoot.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-scaffold/01-UI-SPEC.md (§ MotionConfig Root Contract — copy exact import path `motion/react` and prop `reducedMotion="user"`)
    - .planning/research/PITFALLS.md (Pitfall 5 reduced-motion — confirms MotionConfig root pattern; Pitfall 1 framer-motion vs motion — confirms import path `motion/react` only)
  </read_first>
  <action>
    Create `src/app/(marketing)/v3/MotionRoot.tsx` as a CLIENT component (must be `"use client"` because `MotionConfig` is a client primitive).

    Concrete file content (write verbatim):
    ```tsx
    'use client';

    import { MotionConfig } from 'motion/react';
    import type { ReactNode } from 'react';

    /**
     * Page-root MotionConfig wrapper for Landing v1 (FOUND-09 / UI-SPEC § MotionConfig Root Contract).
     *
     * `reducedMotion="user"` makes motion/react read the OS prefers-reduced-motion media query
     * automatically — a single kill-switch for all motion/react-powered animations on the landing.
     *
     * Import path is `motion/react` (NOT `framer-motion`), per PITFALLS.md Pitfall 1 and the
     * pnpm.overrides alias set in Plan 01.
     */
    export function MotionRoot({ children }: { children: ReactNode }) {
      return <MotionConfig reducedMotion="user">{children}</MotionConfig>;
    }
    ```

    Why this separate file exists:
    - `page.tsx` (Task 2 below) is a server component that exports `metadata` (server-only). Server components can render client component children, but cannot import client-only APIs like `MotionConfig` directly.
    - Wrapping `MotionConfig` in a tiny client component lets `page.tsx` stay server-side while still putting the MotionConfig at the page root surrounding the 9 SectionShell children.

    Per UI-SPEC § MotionConfig Root Contract. Implements FOUND-09 (single kill-switch wrapper).
  </action>
  <verify>
    <automated>grep -c "^export function MotionRoot" src/app/\(marketing\)/v3/MotionRoot.tsx && grep -c '"use client"' src/app/\(marketing\)/v3/MotionRoot.tsx && grep -c "from 'motion/react'" src/app/\(marketing\)/v3/MotionRoot.tsx && grep -c "framer-motion" src/app/\(marketing\)/v3/MotionRoot.tsx && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"use client"' src/app/\(marketing\)/v3/MotionRoot.tsx` returns 1
    - `grep -c "from 'motion/react'" src/app/\(marketing\)/v3/MotionRoot.tsx` returns 1 (import path MUST be `motion/react`, not `framer-motion`)
    - `grep -c "from .framer-motion." src/app/\(marketing\)/v3/MotionRoot.tsx` returns 0
    - `grep -c 'reducedMotion="user"' src/app/\(marketing\)/v3/MotionRoot.tsx` returns 1
    - `grep -c "MotionConfig" src/app/\(marketing\)/v3/MotionRoot.tsx` returns at least 2 (import + usage)
    - `grep -c "^export function MotionRoot" src/app/\(marketing\)/v3/MotionRoot.tsx` returns 1
    - File line count ≥ 15
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>MotionRoot.tsx exists as a client wrapper around MotionConfig reducedMotion="user", imported from `motion/react`; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create v3/page.tsx (the /v3 route assembling everything)</name>
  <files>src/app/(marketing)/v3/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-scaffold/01-UI-SPEC.md (§ Copywriting Contract — page <title> "Virtuna | Predict viral before you post" FINAL; § SEO Baseline > Open Graph placeholder row — OG description ≤ 160 chars verbatim: "AI that scores your TikToks before TikTok does. Simulate audiences, get a viral score in 30 seconds.")
    - .planning/phases/01-foundation-scaffold/01-UI-SPEC.md (§ Accessibility Baseline — main landmark `<main id="main-content">` wraps the 9 sections; nav landmark provided by LandingHeader)
    - .planning/phases/01-foundation-scaffold/01-UI-SPEC.md (§ Section Shell Visual Contract — section order Hero/Demo/How It Works/Surfaces/Comparison/Science/Social Proof/Pricing/Final CTA matches LandingHeader anchor order from Plan 03; isHero true for hero, false for others; variant alternates odd/even)
    - src/app/layout.tsx (confirm root provides <html lang="en"> + Inter font — no edit needed; verify cascade only)
    - src/app/(marketing)/_components/SectionShell.tsx (just created — confirm SectionShellProps signature)
    - src/app/(marketing)/_components/GrainOverlay.tsx (just created — confirm no props)
    - src/app/(marketing)/_components/SkipToContent.tsx (just created — confirm href="#main-content")
    - src/app/(marketing)/_components/LandingHeader.tsx (just created — confirm 9 anchor href values match the 9 SectionShell ids)
    - src/app/(marketing)/v3/MotionRoot.tsx (created in Task 1 — confirm MotionRoot export signature)
    - src/components/ui/marquee.tsx (Task 3 will document the collision-check decision; for now, confirm the file exists)
    - src/hooks/useCountUp.ts (same — confirm existence)
    - .planning/research/PITFALLS.md (Pitfall 9 — collision check for marquee.tsx + useCountUp.ts informs Task 3 of this plan)
  </read_first>
  <action>
    Create `src/app/(marketing)/v3/page.tsx` as a SERVER component (no `"use client"` directive). It exports a default page function AND a `metadata` object.

    File structure (write verbatim — adjust only the layout container classes if needed for Tailwind v4 compatibility):

    ```tsx
    import type { Metadata } from 'next';
    import { LandingHeader } from '@/app/(marketing)/_components/LandingHeader';
    import { GrainOverlay } from '@/app/(marketing)/_components/GrainOverlay';
    import { SkipToContent } from '@/app/(marketing)/_components/SkipToContent';
    import { SectionShell } from '@/app/(marketing)/_components/SectionShell';
    import { MotionRoot } from './MotionRoot';

    /**
     * Landing v1 — `/v3` staging route (FOUND-01).
     *
     * Renders the 9 placeholder section shells in narrative order under a single
     * MotionConfig reducedMotion="user" page-root wrapper (FOUND-09). Skip-to-content link
     * lands on <main id="main-content"> (PERF-11). GrainOverlay paints SVG noise behind
     * all content (POLISH-01). LandingHeader is fixed-top with anchor links matching
     * section ids (FOUND-08).
     *
     * Page title locks "Virtuna | Predict viral before you post" (META-03 / FOUND-02
     * stale-title fix complement — once /v3 cuts over to root in Phase 11, this title
     * replaces the legacy "Artificial Societies" residue site-wide).
     *
     * FOUND-05 collision-check decision:
     *   - src/components/ui/marquee.tsx EXISTS — Magic UI Marquee NOT installed in Phase 1.
     *     Deferred to Phase 9 (Social Proof) per ROADMAP; collision will be re-evaluated
     *     before install per UI-SPEC § Pre-install collision checks.
     *   - src/hooks/useCountUp.ts EXISTS — Magic UI NumberTicker NOT installed in Phase 1.
     *     Deferred to Phase 4/5/8 (where it's actually consumed) per ROADMAP.
     *   - Magic UI DotPattern is OPTIONAL per UI-SPEC § Magic UI/Aceternity installs row —
     *     deferred until a section actually needs the texture (no section in Phase 1
     *     requires it; placeholder shells use alternating background per § Section Shell).
     */
    export const metadata: Metadata = {
      title: 'Virtuna | Predict viral before you post',
      description: 'AI that scores your TikToks before TikTok does. Simulate audiences, get a viral score in 30 seconds.',
      openGraph: {
        title: 'Virtuna | Predict viral before you post',
        description: 'AI that scores your TikToks before TikTok does. Simulate audiences, get a viral score in 30 seconds.',
        url: 'https://virtuna.ai/',
        siteName: 'Virtuna',
        locale: 'en_US',
        type: 'website',
      },
      twitter: {
        card: 'summary_large_image',
        title: 'Virtuna | Predict viral before you post',
        description: 'AI that scores your TikToks before TikTok does. Simulate audiences, get a viral score in 30 seconds.',
      },
      alternates: {
        canonical: 'https://virtuna.ai/',
      },
    };

    export default function LandingV3Page() {
      return (
        <>
          <SkipToContent />
          <GrainOverlay />
          <LandingHeader />
          <main id="main-content" className="pt-16">
            <MotionRoot>
              <SectionShell id="hero" label="Hero" shipsInPhase={2} variant="odd" isHero />
              <SectionShell id="demo" label="Demo" shipsInPhase={4} variant="even" />
              <SectionShell id="how-it-works" label="How It Works" shipsInPhase={5} variant="odd" />
              <SectionShell id="surfaces" label="Three Surfaces" shipsInPhase={6} variant="even" />
              <SectionShell id="comparison" label="Comparison" shipsInPhase={7} variant="odd" />
              <SectionShell id="science" label="The Science" shipsInPhase={8} variant="even" />
              <SectionShell id="social-proof" label="Social Proof" shipsInPhase={9} variant="odd" />
              <SectionShell id="pricing" label="Pricing" shipsInPhase={10} variant="even" />
              <SectionShell id="final-cta" label="Final CTA" shipsInPhase={2} variant="odd" />
            </MotionRoot>
          </main>
        </>
      );
    }
    ```

    Critical wiring notes (verify each):
    - **DOM order is non-negotiable**: `<SkipToContent />` MUST be the first child of the returned fragment so it is the first focusable element on Tab. `<GrainOverlay />` next (decorative, aria-hidden, sits behind via z-index -1 — placement doesn't affect a11y tree). `<LandingHeader />` third (rendered above content; fixed-position so document flow doesn't shift). `<main id="main-content">` last (the skip-link target).
    - `<main id="main-content" className="pt-16">` — the `pt-16` (= 64px) offsets the fixed Header height so first section content isn't hidden behind the header. Match Header height from Plan 03.
    - The 9 SectionShell instances appear in this exact order and use these exact id slugs (matching LandingHeader anchor hrefs from Plan 03). variant alternates `odd/even/odd/even/odd/even/odd/even/odd` (9 entries → starts/ends odd). `isHero` is true ONLY on the first section.
    - `shipsInPhase` values come from ROADMAP.md mapping (Hero → Phase 2, Demo → 4, How → 5, Surfaces → 6, Comparison → 7, Science → 8, Social Proof → 9, Pricing → 10, Final CTA → 2 [bookend with Hero in Phase 2]).
    - `MotionRoot` wraps the SectionShell children. SkipToContent / GrainOverlay / LandingHeader sit OUTSIDE the MotionConfig — they have no motion behavior and don't need it.
    - Metadata title is the LOCKED "Virtuna | Predict viral before you post" per UI-SPEC § Copywriting Contract. Description is the locked OG description per UI-SPEC § SEO Baseline row.
    - Canonical link via `alternates.canonical: 'https://virtuna.ai/'` — points to root, not /v3, because /v3 is staging and should not be the SEO canonical (per ARCHITECTURE.md § 7 "Per-page metadata for /"). Robots.ts (Plan 04) doesn't disallow /v3 because we want preview crawlability for Vercel preview links, but the canonical URL signals to search engines that the real page lives at /.
    - Import paths use `@/app/(marketing)/_components/...` — the `@/` alias matches the existing tsconfig/components.json baseUrl convention in the codebase (verify in tsconfig.json if needed; if `@/` alias doesn't resolve through that path due to parentheses in `(marketing)`, fall back to relative imports `../_components/SectionShell`).

    Per UI-SPEC § Copywriting Contract + § SEO Baseline + § Accessibility Baseline + § MotionConfig Root Contract + § Section Shell Visual Contract. Implements FOUND-01, FOUND-04 (no installs needed in Phase 1), FOUND-09, META-03 (page title + canonical + OG description).
  </action>
  <verify>
    <automated>grep -c "^export default function LandingV3Page" src/app/\(marketing\)/v3/page.tsx && grep -c "^export const metadata" src/app/\(marketing\)/v3/page.tsx && grep -c "Virtuna | Predict viral before you post" src/app/\(marketing\)/v3/page.tsx && grep -c 'id="main-content"' src/app/\(marketing\)/v3/page.tsx && grep -c "<MotionRoot>" src/app/\(marketing\)/v3/page.tsx && grep -c "SectionShell" src/app/\(marketing\)/v3/page.tsx && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"use client"' src/app/\(marketing\)/v3/page.tsx` returns 0 (server component)
    - `grep -c "^export default function LandingV3Page" src/app/\(marketing\)/v3/page.tsx` returns 1
    - `grep -c "^export const metadata" src/app/\(marketing\)/v3/page.tsx` returns 1
    - `grep -c "Virtuna | Predict viral before you post" src/app/\(marketing\)/v3/page.tsx` returns at least 3 (title + openGraph.title + twitter.title)
    - `grep -c "AI that scores your TikToks before TikTok does" src/app/\(marketing\)/v3/page.tsx` returns at least 3 (description + openGraph.description + twitter.description)
    - `grep -c "canonical: 'https://virtuna.ai/'" src/app/\(marketing\)/v3/page.tsx` returns 1
    - `grep -c "Artificial Societies" src/app/\(marketing\)/v3/page.tsx` returns 0 (must NOT carry the stale brand title)
    - `grep -c 'id="main-content"' src/app/\(marketing\)/v3/page.tsx` returns 1
    - `grep -c "<SkipToContent" src/app/\(marketing\)/v3/page.tsx` returns 1
    - `grep -c "<GrainOverlay" src/app/\(marketing\)/v3/page.tsx` returns 1
    - `grep -c "<LandingHeader" src/app/\(marketing\)/v3/page.tsx` returns 1
    - `grep -c "<MotionRoot>" src/app/\(marketing\)/v3/page.tsx` returns 1
    - `grep -c "</MotionRoot>" src/app/\(marketing\)/v3/page.tsx` returns 1
    - All 9 SectionShell id slugs present (one per `<SectionShell` line):
      - `grep -c 'id="hero"' returns 1`
      - `grep -c 'id="demo"' returns 1`
      - `grep -c 'id="how-it-works"' returns 1`
      - `grep -c 'id="surfaces"' returns 1`
      - `grep -c 'id="comparison"' returns 1`
      - `grep -c 'id="science"' returns 1`
      - `grep -c 'id="social-proof"' returns 1`
      - `grep -c 'id="pricing"' returns 1`
      - `grep -c 'id="final-cta"' returns 1`
    - Total `<SectionShell` occurrences: `grep -c "<SectionShell" src/app/\(marketing\)/v3/page.tsx` returns 9
    - `grep -c "isHero" src/app/\(marketing\)/v3/page.tsx` returns 1 (only the hero section gets it)
    - SkipToContent appears BEFORE LandingHeader in source order (verify via line numbers): `grep -n "SkipToContent\|LandingHeader" src/app/\(marketing\)/v3/page.tsx | head -2` shows SkipToContent first
    - `grep -c "pt-16" src/app/\(marketing\)/v3/page.tsx` returns 1 (main offset for fixed Header)
    - File line count ≥ 80
    - `pnpm exec tsc --noEmit` exits 0
    - Root layout still provides lang="en" (sanity-check, not modified): `grep -c 'lang="en"' src/app/layout.tsx` returns 1
  </acceptance_criteria>
  <done>v3/page.tsx exists as a server component exporting LandingV3Page + metadata; assembles SkipToContent → GrainOverlay → LandingHeader → main[id=main-content] → MotionRoot → 9 SectionShell instances in narrative order with correct variants/isHero/shipsInPhase mapping; page title locked "Virtuna | Predict viral before you post"; canonical points to https://virtuna.ai/; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-test /v3 render via dev server + verify <html lang="en"> + verify FOUND-05 collision decisions</name>
  <files></files>
  <read_first>
    - src/app/layout.tsx (confirm root <html lang="en"> still in place — PERF-13)
    - src/components/ui/marquee.tsx (confirm existing legacy file NOT modified)
    - src/hooks/useCountUp.ts (confirm existing legacy file NOT modified)
    - package.json (confirm no @magicui/marquee or @magicui/number-ticker added — Phase 1 defers per FOUND-05 collision decision)
    - src/app/(marketing)/v3/page.tsx (just created — confirm collision decision is documented in JSDoc)
  </read_first>
  <action>
    Run a sequence of static + runtime smoke checks to prove the /v3 route renders cleanly and that no FOUND-05 collisions occurred:

    1. **PERF-13 — Verify <html lang="en"> in root layout (no modification, just verification):**
       ```bash
       grep -c 'lang="en"' src/app/layout.tsx
       ```
       Must return at least 1. If it returns 0, that's a regression — fix by adding `lang="en"` to the `<html>` tag in `src/app/layout.tsx` (single attribute add, no other change).

    2. **FOUND-05 — Verify no Magic UI collisions introduced:**
       ```bash
       # Existing legacy files must still exist and be unmodified
       test -f src/components/ui/marquee.tsx
       test -f src/hooks/useCountUp.ts
       # No Magic UI marquee or number-ticker package added
       grep -c "@magicui/marquee\|@magicui/number-ticker" package.json
       ```
       The grep MUST return 0 — no @magicui/marquee or @magicui/number-ticker added in Phase 1 (deferred per FOUND-05 collision decision; documented in v3/page.tsx JSDoc).

       If anyone DID install one of these in error, the existing legacy files would either be overwritten (data loss) or duplicated as `marquee-1.tsx` — abort and revert.

    3. **TypeScript end-to-end gate:**
       ```bash
       pnpm exec tsc --noEmit
       ```
       Must exit 0.

    4. **Dev server smoke test (FOUND-01 truth #1):**
       ```bash
       # Start dev server in background
       pnpm dev > /tmp/v3-devserver.log 2>&1 &
       DEV_PID=$!
       # Wait for ready signal (poll up to 30s)
       for i in {1..30}; do
         if grep -q "Ready in\|started server on\|Local:" /tmp/v3-devserver.log 2>/dev/null; then
           break
         fi
         sleep 1
       done
       # Fetch /v3 and check for the locked page title + 9 section ids
       curl -s http://localhost:3000/v3 -o /tmp/v3-page.html
       # Kill dev server
       kill $DEV_PID 2>/dev/null
       wait $DEV_PID 2>/dev/null
       # Static checks on rendered HTML
       grep -c "Virtuna | Predict viral before you post" /tmp/v3-page.html
       for slug in hero demo how-it-works surfaces comparison science social-proof pricing final-cta; do
         grep -q "id=\"$slug\"" /tmp/v3-page.html && echo "OK: rendered $slug"
       done
       # Verify lang attribute
       grep -c 'lang="en"' /tmp/v3-page.html
       # Verify no "Artificial Societies" residue
       grep -c "Artificial Societies" /tmp/v3-page.html
       ```
       Expected outcomes:
       - Title grep returns ≥ 1
       - All 9 "OK: rendered {slug}" lines print
       - lang="en" present in rendered HTML
       - "Artificial Societies" returns 0
       - No hydration warnings or React errors in /tmp/v3-devserver.log

    5. **Sitemap/robots runtime check (relies on Plan 04 being merged first):**
       ```bash
       # If dev server still running OR restart it briefly
       curl -s http://localhost:3000/sitemap.xml | grep -c "virtuna.ai"
       curl -s http://localhost:3000/robots.txt | grep -c "Allow: /"
       ```
       Both should return ≥ 1. If Plan 04 hasn't merged yet at the time this plan executes, defer this check to the phase-level verifier (this task can still pass).

    If any check fails, surface the failure to the user before declaring this task done. The dev-server smoke test is the single most important verification of Phase 1 — it proves "FOUND-01 truth #1: User can navigate to /v3 and see all 9 placeholder section blocks render in order without console errors."

    Implements FOUND-05 collision audit verification, PERF-13 verification, FOUND-01 runtime confirmation.
  </action>
  <verify>
    <automated>grep -c 'lang="en"' src/app/layout.tsx &amp;&amp; test -f src/components/ui/marquee.tsx &amp;&amp; test -f src/hooks/useCountUp.ts &amp;&amp; [ "$(grep -c '@magicui/marquee\|@magicui/number-ticker' package.json)" -eq 0 ] &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'lang="en"' src/app/layout.tsx` returns at least 1 (PERF-13)
    - `test -f src/components/ui/marquee.tsx` exits 0 (legacy file untouched)
    - `test -f src/hooks/useCountUp.ts` exits 0 (legacy hook untouched)
    - `grep -c '@magicui/marquee' package.json` returns 0 (FOUND-05 — deferred)
    - `grep -c '@magicui/number-ticker' package.json` returns 0 (FOUND-05 — deferred)
    - `pnpm exec tsc --noEmit` exits 0
    - Dev server runtime check (manual or automated in execute-phase): `curl http://localhost:3000/v3` returns HTML containing the locked title, lang="en", all 9 section ids, no "Artificial Societies" string, no hydration warnings in stderr
    - Sitemap/robots runtime check (when Plan 04 has merged): `curl /sitemap.xml` returns valid XML with virtuna.ai; `curl /robots.txt` returns `Allow: /` + sitemap directive
  </acceptance_criteria>
  <done>Root layout retains lang="en"; legacy marquee.tsx + useCountUp.ts unmodified; no Magic UI marquee/number-ticker installed in Phase 1; tsc passes; dev server serves /v3 with locked title + all 9 sections + no residue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crawler request → /v3 metadata | OG title/description and canonical link are served publicly; canonical points to / (production), not /v3 (staging) |
| user keyboard → SkipToContent → #main-content | DOM fragment navigation, no JS handler, scoped to document |
| MotionConfig prefers-reduced-motion | Reads OS media query — read-only, not user-tampered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | I (Information Disclosure) | metadata.alternates.canonical | mitigate | Canonical points to `https://virtuna.ai/` (production), not `https://virtuna.ai/v3` (staging) — prevents search engines from indexing the staging URL when Vercel preview is publicly accessible. |
| T-01-18 | T (Tampering) | MotionConfig reducedMotion | accept | `reducedMotion="user"` is a static prop; cannot be tampered client-side. Pitfall 5 mitigation. |
| T-01-19 | E (Elevation of Privilege) | /v3 staging route | mitigate | /v3 is a separate route group entry; Phase 11 cutover overwrites root `(marketing)/page.tsx` with the staged content. Until then, live root landing remains untouched (PITFALLS.md Pitfall 11 — route collision avoided by staging at /v3). |
| T-01-20 | I (Information Disclosure) | OG description placeholder copy | accept | OG description is marketing copy ("AI that scores your TikToks before TikTok does. Simulate audiences, get a viral score in 30 seconds.") — public, no PII. Per UI-SPEC § SEO Baseline. |
| T-01-21 | D (Denial of Service) | 9 SectionShell server-rendered blocks | accept | Server-rendered server components; zero client JS for SectionShell, GrainOverlay, SkipToContent (LandingHeader + MotionRoot are minimal client wrappers). Total client JS for Phase 1 well within PERF-09 200 KB budget (no Magic UI / Aceternity components installed yet). |
| T-01-22 | S (Spoofing) | LandingHeader Sign up CTA href="/login" | accept | Routes to internal /login — matches FOUND-08 spec (Sign up flow routes through login). Not user-controlled. |
</threat_model>

<verification>
End-of-plan + end-of-phase smoke checks:

```bash
# All Plan 05 files exist
ls src/app/\(marketing\)/v3/page.tsx src/app/\(marketing\)/v3/MotionRoot.tsx

# Page is server, MotionRoot is client
[ "$(grep -c '"use client"' src/app/\(marketing\)/v3/page.tsx)" -eq 0 ] && echo "OK: page.tsx is server"
grep -q '"use client"' src/app/\(marketing\)/v3/MotionRoot.tsx && echo "OK: MotionRoot is client"

# MotionConfig wires correctly
grep -q "from 'motion/react'" src/app/\(marketing\)/v3/MotionRoot.tsx && echo "OK: motion/react import"
grep -q 'reducedMotion="user"' src/app/\(marketing\)/v3/MotionRoot.tsx && echo "OK: reducedMotion user"

# All 9 sections present in order
for slug in hero demo how-it-works surfaces comparison science social-proof pricing final-cta; do
  grep -q "id=\"$slug\"" src/app/\(marketing\)/v3/page.tsx && echo "OK: section $slug"
done

# Locked title + description
grep -q "Virtuna | Predict viral before you post" src/app/\(marketing\)/v3/page.tsx && echo "OK: title locked"
grep -q "AI that scores your TikToks before TikTok does" src/app/\(marketing\)/v3/page.tsx && echo "OK: OG description"

# No "Artificial Societies" residue anywhere new
[ "$(grep -c 'Artificial Societies' src/app/\(marketing\)/v3/page.tsx)" -eq 0 ] && echo "OK: no stale title"

# A11y wiring
grep -q 'id="main-content"' src/app/\(marketing\)/v3/page.tsx && echo "OK: skip target"

# Canonical points to prod root
grep -q "canonical: 'https://virtuna.ai/'" src/app/\(marketing\)/v3/page.tsx && echo "OK: canonical /"

# FOUND-05 collision-decision honored
[ "$(grep -c '@magicui/marquee\|@magicui/number-ticker' package.json)" -eq 0 ] && echo "OK: no Magic UI collisions"
test -f src/components/ui/marquee.tsx && echo "OK: legacy marquee preserved"
test -f src/hooks/useCountUp.ts && echo "OK: legacy useCountUp preserved"

# PERF-13 root layout lang
[ "$(grep -c 'lang="en"' src/app/layout.tsx)" -ge 1 ] && echo "OK: lang=en"

# TypeScript
pnpm exec tsc --noEmit && echo "OK: tsc clean"

# Runtime (manual or automated in execute-phase):
# pnpm dev (background); curl http://localhost:3000/v3 ; assert title + 9 ids + lang
# curl http://localhost:3000/sitemap.xml ; assert virtuna.ai entries
# curl http://localhost:3000/robots.txt ; assert Allow: / + Sitemap directive
```

All `OK:` lines (~18 lines) must print.
</verification>

<success_criteria>
- `src/app/(marketing)/v3/page.tsx` exists as a server component
- `src/app/(marketing)/v3/MotionRoot.tsx` exists as a client wrapper around `MotionConfig reducedMotion="user"` from `motion/react`
- Page renders SkipToContent → GrainOverlay → LandingHeader → `<main id="main-content" className="pt-16">` → MotionRoot wrapping 9 SectionShell instances (in narrative order: hero, demo, how-it-works, surfaces, comparison, science, social-proof, pricing, final-cta)
- Section ids match LandingHeader anchor href values (Plan 03 contract)
- `metadata` export sets title "Virtuna | Predict viral before you post", description "AI that scores your TikToks before TikTok does. Simulate audiences, get a viral score in 30 seconds.", canonical "https://virtuna.ai/", openGraph + twitter mirror the same title/description
- No "Artificial Societies" string anywhere new
- Root `src/app/layout.tsx` still has `lang="en"` (PERF-13 verified, not modified)
- FOUND-05 collision-check honored: `src/components/ui/marquee.tsx` and `src/hooks/useCountUp.ts` untouched; no `@magicui/marquee` or `@magicui/number-ticker` in package.json
- `pnpm exec tsc --noEmit` exits 0
- After execute-phase dev-server smoke test: `curl http://localhost:3000/v3` returns HTML containing the locked title, lang="en", all 9 section ids in order, no console errors / hydration warnings in dev-server log
- After Plan 04 + Plan 05 both merged: `curl http://localhost:3000/sitemap.xml` returns valid XML; `curl http://localhost:3000/robots.txt` returns valid robots syntax
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-scaffold/01-05-SUMMARY.md` per the standard summary template.
</output>
