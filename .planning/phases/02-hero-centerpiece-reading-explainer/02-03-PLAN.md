---
phase: 02-hero-centerpiece-reading-explainer
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/components/numen-landing/reading-loop.tsx
  - src/components/numen-landing/hero.tsx
autonomous: true
requirements: [HERO-01, HERO-02, HERO-04, CTA-01]
must_haves:
  truths:
    - "The hero renders a single <h1> + subhead + a 'Try Numen' CTA routing to #cta, above the fold, mobile-first"
    - "The full-bleed real keyframe replays through StageBlock as a calm auto-reveal loop that dwells on the verdict throne"
    - "Under prefers-reduced-motion the loop paints the completed end-state at rest — no auto-cycle, no translate"
  artifacts:
    - path: "src/components/numen-landing/reading-loop.tsx"
      provides: "HERO-02/HERO-04 — the StageBlock-driven reveal loop over the real keyframe + verdict throne"
      min_lines: 40
      contains: "useReducedMotion; StageBlock; if (reduce) return"
    - path: "src/components/numen-landing/hero.tsx"
      provides: "HERO-01/CTA-01 — the H1/subhead/CTA column mounting ReadingLoop"
      min_lines: 30
      contains: "href=\"#cta\"; Try Numen"
  key_links:
    - from: "src/components/numen-landing/reading-loop.tsx"
      to: "@/components/numen/stage-reveal"
      via: "import { StageBlock } + useReducedMotion from motion/react"
      pattern: "StageBlock"
    - from: "src/components/numen-landing/reading-loop.tsx"
      to: "src/components/numen-landing/verdict-throne.tsx"
      via: "final stage renders <VerdictThrone />"
      pattern: "VerdictThrone"
    - from: "src/components/numen-landing/reading-loop.tsx"
      to: "public/images/landing/hero/keyframe.webp"
      via: "next/image static import"
      pattern: "next/image"
    - from: "src/components/numen-landing/hero.tsx"
      to: "#cta anchor (page.tsx slot)"
      via: "next/link href=#cta"
      pattern: "href=.#cta."
---

<objective>
Wave 2 — build the recorded stage-reveal LOOP over the real keyframe + verdict
throne (HERO-02/HERO-04), and the hero text column (H1/subhead/CTA) that mounts
it (HERO-01/CTA-01). This is the load-bearing interaction of the phase.

Purpose: D-01 resolves D-L2 as a recorded calm auto-reveal loop (not live, not a
static appear). HERO-04 is the trap: StageBlock zeroes translate under reduced
motion, but the loop CONTROLLER is separate and must independently NOT auto-cycle
and paint the end-state.
Output: `reading-loop.tsx` + `hero.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/VOICE.md
@.planning/phases/02-hero-centerpiece-reading-explainer/02-UI-SPEC.md
@.planning/phases/02-hero-centerpiece-reading-explainer/02-PATTERNS.md

<interfaces>
<!-- Consumed contracts — extracted from in-repo source. Use directly. -->

From src/components/numen/stage-reveal.tsx ("use client"; the ONLY motion):
```
import { AnimatePresence, motion, useReducedMotion } from "motion/react";
export interface StageBlockProps { show: boolean; children: React.ReactNode; }
export function StageBlock({ show, children })
// {show,children} ONLY — no internal sequencing; reduced-motion zeroes translate.
```

From src/components/numen-landing/verdict-throne.tsx (Wave 1 output):
```
export function VerdictThrone({ className }?: { className?: string })
// good band + label + why, no naked number, plate-backed if APCA required it.
```

From src/components/numen-landing/nav.tsx (CTA + lifecycle analog):
```
const FOCUS_RING =
  "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2 focus-visible:ring-offset-bg";
// CTA: <Link href="#cta" ...>Try Numen</Link>   (label LOCKED, matches footer.tsx:84)
// useState/useEffect + return () => clearInterval(id) cleanup discipline (nav.tsx:33-59)
```

The CURRENT inline hero markup to LIFT into hero.tsx — src/app/(marketing)/page.tsx:24-42
(H1 "Know if your content will land — before you post." + subhead + CTA h-11).

next/image static import (Next 16.1.5) — use `preload` (NOT the deprecated `priority`),
`placeholder="blur"`, auto width/height from the static import → zero CLS.
Reduced-motion test target: src/components/numen-landing/__tests__/reading-loop.test.tsx (RED → GREEN).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the ReadingLoop controller (HERO-02 / HERO-04)</name>
  <files>src/components/numen-landing/reading-loop.tsx</files>
  <behavior>
    - Reduced-motion ON: all stages shown at rest (verdict throne label present), NO setInterval started, NO non-zero translate transform on any element.
    - Reduced-motion OFF: stages reveal in sequence via StageBlock, the loop dwells on the verdict stage (≥ ~4s), then resets and replays — calm, no bounce/flicker.
    - The final stage renders <VerdictThrone />; the keyframe renders via next/image with preload + blur.
  </behavior>
  <read_first>
    - src/components/numen/stage-reveal.tsx (StageBlock API + reduced-motion internals — read in full)
    - src/components/numen-landing/nav.tsx (client useState/useEffect/clearInterval cleanup — lines 33-59)
    - src/components/numen-landing/verdict-throne.tsx (Wave 1 — the final stage)
    - src/components/numen-landing/__tests__/reading-loop.test.tsx (RED target — make GREEN)
    - .planning/phases/02-hero-centerpiece-reading-explainer/02-PATTERNS.md (§reading-loop.tsx — reduced-motion controller pattern, lines 79-122)
    - .planning/phases/02-hero-centerpiece-reading-explainer/02-RESEARCH.md (Pattern 1 next/image preload + Pattern 2 loop + Pitfall 2/4, lines 127-189, 248-263)
    - .planning/phases/02-hero-centerpiece-reading-explainer/02-UI-SPEC.md (§Hero Interaction & Motion Contract, lines 122-132)
  </read_first>
  <action>
    Create `src/components/numen-landing/reading-loop.tsx` with `"use client"`.
    Static-import the keyframe: `import heroKeyframe from "@/../public/images/landing/hero/keyframe.webp"` (or the shipped extension from Plan 02-02) and render it via `next/image` with `preload` (NOT `priority` — deprecated in Next 16.1.5), `placeholder="blur"` (blurDataURL auto from static import), and `className="w-full h-auto"`; give the full-bleed container an explicit aspect ratio (e.g. `aspect-[16/9]` or matching the real still) so it reserves height before paint (Pitfall 4 — zero CLS). The alt text is factual + in voice (UI-SPEC §Copywriting: "A real Reading of a creator video showing an honest 'likely to land' verdict and why.").

    Define a STAGES list of ~3 stages: (1) the keyframe, (2) a short real stage-read excerpt line (VOICE-clean, plain language about *their* video, no engine jargon — Claude's discretion within VOICE, e.g. "Watching how the first seconds land…"), (3) the `<VerdictThrone />`. Drive a `revealed` index with `useReducedMotion()` from `motion/react`:
      const reduce = useReducedMotion();
      const [revealed, setRevealed] = useState(reduce ? STAGES.length : 0);
      useEffect(() => { if (reduce) return; /* setInterval advancing i, reset at end, dwell longer on verdict */ return () => clearInterval(id); }, [reduce]);
    Render `STAGES.map((s, idx) => <StageBlock key={idx} show={reduce ? true : idx < revealed}>{...}</StageBlock>)`.

    HERO-04 HARD (Pitfall 2): under reduced motion the controller MUST `return` before any interval AND initialize `revealed` to `STAGES.length` so the completed end-state paints directly — no auto-cycle, no flicker. Cadence/dwell/pause-on-verdict are Claude's discretion (CONTEXT) but MUST stay calm: no bounce, no flashing, no rapid cycling. Never embed the forbidden bouncy easing `cubic-bezier(0.34, 1.56, 0.64, 1)` (StageBlock owns the easing; do not re-declare motion curves here). Color by token name only; the loop renders inside `.numen-surface` (mounted by the marketing layout), so do NOT portal it out.
  </action>
  <verify>
    <automated>pnpm test -- reading-loop</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- reading-loop` GREEN: under the mocked `useReducedMotion=>true`, the verdict throne label renders (end-state painted) AND no element carries a non-zero translate transform.
    - File starts with `"use client"`; imports `StageBlock` from `@/components/numen/stage-reveal` and `useReducedMotion` from `motion/react`.
    - The effect contains `if (reduce) return;` before any `setInterval`, and `useState(reduce ? STAGES.length : 0)`.
    - Keyframe rendered via `next/image` with `preload` and `placeholder="blur"` (NOT `priority`); container has an explicit aspect ratio.
    - Final stage renders `<VerdictThrone />`; no hex literals in JSX.
  </acceptance_criteria>
  <done>ReadingLoop replays the real keyframe → stage-read → verdict throne as a calm StageBlock loop, and paints the static end-state under reduced motion with no auto-cycle.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Hero column (HERO-01 / CTA-01) mounting ReadingLoop</name>
  <files>src/components/numen-landing/hero.tsx</files>
  <read_first>
    - src/app/(marketing)/page.tsx (the inline hero markup to lift — lines 24-42)
    - src/components/numen-landing/nav.tsx (FOCUS_RING + CTA pattern — lines 30-31, 110-118)
    - src/components/numen-landing/reading-loop.tsx (Task 1 — the artifact it mounts)
    - src/components/numen-landing/__tests__/hero.test.tsx (RED target — make GREEN)
    - .planning/phases/02-hero-centerpiece-reading-explainer/02-PATTERNS.md (§hero.tsx — lifted markup + FOCUS_RING + single-h1, lines 34-75)
    - .planning/phases/02-hero-centerpiece-reading-explainer/02-UI-SPEC.md (§Container full-bleed exception + §Typography + §Copywriting, lines 60-86, 147-171)
  </read_first>
  <action>
    Create `src/components/numen-landing/hero.tsx` with `"use client"` (it mounts
    the client ReadingLoop). LIFT the existing inline hero markup from
    `page.tsx:24-42`: the H1 ("Know if your content will land — before you post.",
    `text-text text-4xl md:text-6xl font-bold tracking-tight`), the subhead
    (`text-base md:text-lg leading-relaxed text-text-muted`), and the CTA. Declare
    the `FOCUS_RING` constant VERBATIM from nav.tsx:30-31. The CTA is
    `<Link href="#cta" className={...h-11... bg-accent ... text-bg ... ${FOCUS_RING}}>Try Numen</Link>`
    — label "Try Numen" and `href="#cta"` are LOCKED (must match nav.tsx:117 +
    footer.tsx:84). Use `h-11` (≥44px) for the hero CTA. Do NOT deep-link `/analyze`.

    Layout (UI-SPEC §Container): the H1/subhead/CTA text column stays in the
    `max-w-6xl` gutter (widen from the current `max-w-3xl` to the shared gutter or
    a readable measure per UI-SPEC), and `<ReadingLoop />` is mounted as the
    full-bleed artifact — it may extend beyond the gutter (the one deliberate
    full-bleed break, HERO-02). The hero renders its OWN single `<h1>` and is
    placed by page.tsx inside `SectionShell id="hero"` with NO `heading=` prop
    (single-h1 rule, D-10) — do NOT emit a second h1/h2 here.

    accent (`bg-accent`) is reserved for the CTA fill + focus ring ONLY — never on
    the H1, the verdict band, or borders. Color by token name only.
  </action>
  <verify>
    <automated>pnpm test -- hero</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- hero` GREEN: exactly one `<h1>`; subhead present; a CTA `<a href="#cta">Try Numen</a>` with the focus-ring class and `h-11`/`min-h-11`.
    - `FOCUS_RING` matches nav.tsx:30-31 verbatim; CTA label "Try Numen" + `href="#cta"` exactly (no `/analyze` deep-link).
    - `<ReadingLoop />` is mounted; the text column is in the `max-w-6xl` gutter while the artifact is the full-bleed element.
    - No second h1/h2 emitted; no hex literals in JSX; `bg-accent` only on the CTA.
  </acceptance_criteria>
  <done>Hero renders the single-h1 text column + locked CTA and mounts the full-bleed ReadingLoop — HERO-01 + CTA-01 satisfied above the fold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none runtime) | Presentation over a static asset: no input, no fetch, no auth. The only safety control is the reduced-motion gate (vestibular harm). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | (a11y/safety) | reading-loop auto-cycle | mitigate | Reduced-motion → static end-state, no auto-cycle (HERO-04); asserted by reading-loop.test.tsx mocking `useReducedMotion=>true`. |
| T-02-05 | Tampering | hero copy strings | mitigate | Static JSX literals + React escaping; never `dangerouslySetInnerHTML`. |
</threat_model>

<verification>
- `pnpm test -- reading-loop hero` GREEN.
- `pnpm test && pnpm tsx scripts/check-apca.ts` GREEN at wave merge.
- Manual (live dev server): hero keyframe is the LCP element, loop is calm, reduced-motion paints the static end-state (kill dev server + clear `.next/` if CSS stale).
</verification>

<success_criteria>
- HERO-01 (h1+subhead+CTA above fold), HERO-02 (real keyframe full-bleed), HERO-04 (calm loop + reduced-motion end-state), CTA-01 (Try Numen → #cta) all satisfied.
</success_criteria>

<output>
Create `.planning/phases/02-hero-centerpiece-reading-explainer/02-03-SUMMARY.md` when done
</output>
