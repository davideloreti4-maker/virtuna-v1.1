---
phase: 02-hero-centerpiece-reading-explainer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/numen-landing/__tests__/hero.test.tsx
  - src/components/numen-landing/__tests__/reading-loop.test.tsx
  - src/components/numen-landing/__tests__/verdict-throne.test.tsx
  - src/components/numen-landing/__tests__/how-it-works.test.tsx
  - src/components/numen-landing/__tests__/voice.test.tsx
  - scripts/check-apca.ts
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, HERO-04, READ-01, READ-02, CTA-01]
must_haves:
  truths:
    - "Running `pnpm test` shows the 5 Phase-2 component tests RED (failing imports) — the Nyquist scaffold exists before implementation"
    - "Running `pnpm tsx scripts/check-apca.ts` measures the verdict-good label on the band `#7faf7a` against Lc ≥ 60 (not only band-on-base)"
  artifacts:
    - path: "src/components/numen-landing/__tests__/hero.test.tsx"
      provides: "HERO-01 / HERO-02 / CTA-01 render assertions (RED)"
      contains: "useReducedMotion is NOT mocked here; asserts h1, subhead, CTA href=#cta label 'Try Numen'"
    - path: "src/components/numen-landing/__tests__/reading-loop.test.tsx"
      provides: "HERO-04 reduced-motion assertion (RED)"
      contains: "vi.mock('motion/react', useReducedMotion: () => true)"
    - path: "src/components/numen-landing/__tests__/verdict-throne.test.tsx"
      provides: "HERO-03 band + label + why, NO naked number (RED)"
      contains: "container.textContent not.toMatch /\\d+\\s*\\/\\s*100|\\d+\\s*%/"
    - path: "src/components/numen-landing/__tests__/how-it-works.test.tsx"
      provides: "READ-01 / READ-02 three-step render (RED)"
      contains: "3 step titles; asserts real content per step (not icon-only)"
    - path: "src/components/numen-landing/__tests__/voice.test.tsx"
      provides: "VOICE Rules 1-2 forbidden-copy scan (RED)"
      contains: "banned regex array: %, viral, guaranteed, Apollo, fold, Omni, pipeline"
    - path: "scripts/check-apca.ts"
      provides: "extended APCA gate with verdict-good-label-on-band pairing"
      contains: "label-on-#7faf7a pairing target 60"
  key_links:
    - from: "all 5 test files"
      to: "@/components/numen-landing/{hero,reading-loop,verdict-throne,how-it-works}"
      via: "dynamic import() (RED until Wave 1-3 ships the components)"
      pattern: "await import\\(.@/components/numen-landing"
---

<objective>
Wave 0 — author the Nyquist test scaffolds and extend the APCA gate BEFORE any
component ships, so every Phase-2 requirement has an automated verify that is RED
now and goes GREEN as Waves 1–3 implement against it.

Purpose: The validation contract (02-VALIDATION.md) mandates all 5 component
tests + the label-on-band APCA pairing exist as Wave 0. No 3 consecutive tasks
without automated verify across the phase.
Output: 5 failing (RED) test files under `src/components/numen-landing/__tests__/`
+ one new pairing in `scripts/check-apca.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/VOICE.md
@.planning/phases/02-hero-centerpiece-reading-explainer/02-VALIDATION.md
@.planning/phases/02-hero-centerpiece-reading-explainer/02-PATTERNS.md

<interfaces>
<!-- Contracts the tests assert against. Extracted from in-repo source. Wave 1-3 implements these. -->

From src/components/numen/stage-reveal.tsx (the reduced-motion test mock target):
```
import { AnimatePresence, motion, useReducedMotion } from "motion/react";
export interface StageBlockProps { show: boolean; children: React.ReactNode; }
export function StageBlock({ show, children }: StageBlockProps)
```

From src/components/numen/verdict-swatch.tsx:
```
export function VerdictSwatch({ verdict, size, children, className }: VerdictSwatchProps)
// verdict: "good" | "mixed" | "bad"  (literal tv classes bg-verdict-good etc.)
```

Test analog (reduced-motion mock + render harness) — tests/numen/stage-reveal.test.ts:1,14-39:
```
/** @vitest-environment happy-dom */
import { describe, it, expect, vi } from "vitest";
import { render } from "@testing-library/react";
vi.mock("motion/react", async (importOriginal) => {
  const actual = await importOriginal<typeof import("motion/react")>();
  return { ...actual, useReducedMotion: () => true };
});
// render(...) + getByText(...) + container.querySelectorAll("*") translate scan
```

Forbidden-copy source-scan analog — tests/numen/stage-reveal.test.ts:41-48 (banned-string scan).

APCA gate shape — scripts/check-apca.ts:41-61 (PAIRINGS array; BASE="#1a1714";
`lc(textHex, bgHex)` helper measures abs APCA Lc). Existing verdict-good row:
`{ label: "verdict-good", text: "#7faf7a", target: 45, role: "verdict band label (large/heavy)" }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 3 render-assertion test scaffolds (hero, verdict-throne, how-it-works)</name>
  <files>src/components/numen-landing/__tests__/hero.test.tsx, src/components/numen-landing/__tests__/verdict-throne.test.tsx, src/components/numen-landing/__tests__/how-it-works.test.tsx</files>
  <read_first>
    - tests/numen/stage-reveal.test.ts (render harness + happy-dom pragma — lines 1, 14-39)
    - tests/numen/primitives.test.ts (dynamic-import variant pattern — lines 44-58)
    - .planning/phases/02-hero-centerpiece-reading-explainer/02-PATTERNS.md (§Test files — render harness + no-number assertion, lines 186-216)
    - .planning/phases/02-hero-centerpiece-reading-explainer/02-UI-SPEC.md (§Copywriting Contract — exact copy strings to assert, lines 147-171)
  </read_first>
  <action>
    Create three `.tsx` test files, each starting with the `/** @vitest-environment happy-dom */` pragma (line 1, mandatory for render tests). Use `import { render } from "@testing-library/react"` and `await import("@/components/numen-landing/<component>")` dynamic imports so the file is RED until the component ships (mirror primitives.test.ts:44-58). Do NOT mock `motion/react` in these three files (that mock is reading-loop's job).

    hero.test.tsx (HERO-01, HERO-02, CTA-01): render `<Hero />`; assert exactly one `<h1>` via `container.querySelectorAll("h1").length === 1`; assert the H1 text contains "land" and the subhead text is present; assert a CTA `<a>` with `href="#cta"` and text "Try Numen" exists (`getByText("Try Numen")`, and that link's `getAttribute("href") === "#cta"`); assert the CTA carries the focus-ring class token `focus-visible:ring-accent` and a min-height utility for ≥44px (`h-11` or `min-h-11`). For HERO-02 asset presence, assert the hero contains an `<img>` (next/image renders an img) with a non-empty `alt` — keep this loose (next/image render detail) and let the real-asset rights check live in the Plan-02 checkpoint.

    verdict-throne.test.tsx (HERO-03): render `<VerdictThrone />`; assert the band label text "This will likely land." (or the shipped label) renders; assert the one-line why renders (text contains "hook" per the UI-SPEC example, or assert a non-empty muted paragraph sibling); CRITICAL — assert NO naked number: `expect(container.textContent).not.toMatch(/\d+\s*\/\s*100|\d+\s*%/)`.

    how-it-works.test.tsx (READ-01, READ-02): render `<HowItWorks />`; assert three step titles render (e.g. getByText for "Upload", "Numen reads it", "Your verdict" per UI-SPEC copy contract); assert each step shows REAL content not icon-only — assert step 1 contains an `<img>` (keyframe), and step 3 contains the verdict band label text (reuses VerdictThrone). Assert step titles are `<h3>` (`container.querySelectorAll("h3").length >= 3`), no `<h1>`/`<h2>` emitted by the component itself.

    Keep assertions matched to the EXACT copy strings in 02-UI-SPEC.md §Copywriting Contract so Wave 1-3 has a precise GREEN target. Where a string is "refinable" per D-08a, assert on the stable substring ("land", "hook", "Upload") rather than the whole sentence.
  </action>
  <verify>
    <automated>pnpm test -- hero verdict-throne how-it-works 2>&1 | grep -E "FAIL|Cannot find module|failed" </automated>
  </verify>
  <acceptance_criteria>
    - All three files exist with the happy-dom pragma on line 1.
    - `pnpm test` reports these three suites RED with a module-resolution / missing-export failure (the components do not exist yet) — NOT a syntax error in the test itself.
    - verdict-throne.test.tsx contains the `not.toMatch(/\d+\s*\/\s*100|\d+\s*%/)` no-naked-number assertion.
    - how-it-works.test.tsx asserts 3 step titles AND real content (img in step 1, band label in step 3).
  </acceptance_criteria>
  <done>Three RED render-test scaffolds exist; each fails only because its target component is not yet implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Author the reduced-motion + voice-scan test scaffolds (reading-loop, voice)</name>
  <files>src/components/numen-landing/__tests__/reading-loop.test.tsx, src/components/numen-landing/__tests__/voice.test.tsx</files>
  <read_first>
    - tests/numen/stage-reveal.test.ts (reduced-motion mock lines 18-24; translate scan lines 33-39; forbidden-string scan lines 41-48)
    - .planning/phases/02-hero-centerpiece-reading-explainer/02-PATTERNS.md (§Test files — reduced-motion mock VERBATIM + forbidden-token scan, lines 188-213)
    - .planning/VOICE.md (Hard Rules 1-2 + Do/Don't table — the ban list, lines 29-77)
    - .planning/phases/02-hero-centerpiece-reading-explainer/02-RESEARCH.md (Pitfall 2 — reduced-motion loop must not auto-cycle, lines 248-252)
  </read_first>
  <action>
    reading-loop.test.tsx (HERO-04): start with the happy-dom pragma; COPY VERBATIM the `vi.mock("motion/react", ...)` block from stage-reveal.test.ts:18-24 returning `useReducedMotion: () => true`. Then `await import("@/components/numen-landing/reading-loop")`, render `<ReadingLoop />`. Assert the reduced-motion END-STATE: (a) the verdict band label text renders (the final stage is painted directly — `getByText` of the throne label), proving all stages are shown at rest; (b) no element carries a non-zero translate transform — reuse the translate-scan loop from stage-reveal.test.ts:33-39 (`container.querySelectorAll("*")`, assert `el.style.transform` does NOT match `/translate(Y|3d)?\([^)]*[1-9]/`). Add a comment noting the controller's own no-auto-cycle is covered by the end-state-painted assertion (a running interval would start at revealed=0, hiding the throne).

    voice.test.tsx (cross — VOICE Rules 1-2): happy-dom pragma; render the three copy-bearing components together — `<Hero />`, `<VerdictThrone />`, `<HowItWorks />` — into one container (or three separate renders, concatenating `container.textContent`). Build the banned-pattern array from VOICE.md: `[/%/, /\bviral\b/i, /virality/i, /guaranteed/i, /\bApollo\b/, /\bfold\b/, /\bOmni\b/i, /\bpipeline\b/, /\bmodel\b/i, /accuracy/i, /predict/i]`. For each regex assert `expect(text).not.toMatch(re)`. This is RED until the components ship (dynamic imports fail), and stays GREEN only if all shipped copy passes VOICE.
  </action>
  <verify>
    <automated>pnpm test -- reading-loop voice 2>&1 | grep -E "FAIL|Cannot find module|failed"</automated>
  </verify>
  <acceptance_criteria>
    - reading-loop.test.tsx contains the verbatim `useReducedMotion: () => true` mock and the translate-scan assertion regex `/translate(Y|3d)?\([^)]*[1-9]/`.
    - voice.test.tsx asserts `not.toMatch` for at least the `%`, `viral`, `guaranteed`, `Apollo`, `fold`, `Omni`, `pipeline` patterns.
    - Both suites are RED due to missing component modules (not test syntax errors).
  </acceptance_criteria>
  <done>Two RED test scaffolds exist: HERO-04 reduced-motion end-state + VOICE forbidden-copy scan.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the APCA gate with a verdict-good label-on-band pairing</name>
  <files>scripts/check-apca.ts</files>
  <read_first>
    - scripts/check-apca.ts (the full PAIRINGS array + `lc(textHex, bgHex)` helper — lines 34-83)
    - .planning/phases/02-hero-centerpiece-reading-explainer/02-RESEARCH.md (Open Q2 + Assumption A3 — label-on-#7faf7a, lines 337-350)
    - .planning/phases/02-hero-centerpiece-reading-explainer/02-UI-SPEC.md (§Color verdict throne contract — Lc ≥ 60 on band, plate fallback, lines 108-112)
  </read_first>
  <action>
    The existing gate measures every pairing as `lc(p.text, BASE)` where BASE is `#1a1714` — it only proves the band color vs the page base (Lc ≥ 45), NOT the LABEL color sitting ON the band. Add a SECOND measurement that checks the throne label color against the band background `#7faf7a` at target Lc ≥ 60 (the `VerdictSwatch` on-swatch gate, UI-SPEC §Color).

    Implement WITHOUT breaking the existing single-base loop: after the main PAIRINGS loop, add a small explicit check block that calls `lc(LABEL_HEX, "#7faf7a")` and asserts `>= 60`, logging a PASS/FAIL line in the same format and setting `failed = true` on miss. Use the body/dark text token `#f0ebe3` as the candidate label color first (the throne label is `text-text` per UI-SPEC §Typography). If `lc("#f0ebe3", "#7faf7a") < 60`, the gate FAILS — that is the SIGNAL that the throne must use the `bg-panel` plate fallback (UI-SPEC-sanctioned), so leave the failing assertion in place and note in a code comment that a sub-60 result means Plan 02's VerdictThrone must back the label with a `bg-panel` plate rather than placing it directly on the band. Do NOT relax the target below 60. Do NOT change any existing hex or existing pairing.
  </action>
  <verify>
    <automated>pnpm tsx scripts/check-apca.ts 2>&1 | grep -iE "verdict-good|label|7faf7a|Lc"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-apca.ts` includes a new check measuring a label color on `#7faf7a` with target 60, printed in the existing PASS/FAIL log format.
    - No existing hex value or existing pairing was modified.
    - The script's exit code correctly reflects pass/fail of the new pairing (non-zero if it fails) — this result decides Plan 02's plate-vs-on-band throne composition.
  </acceptance_criteria>
  <done>APCA gate now validates the on-band label contrast (Lc ≥ 60); its result is the gate for the verdict-throne composition decision in Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test/config-only plan: no runtime input, no fetch, no auth, no rendered production surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | test copy strings | accept | Static string literals; no `dangerouslySetInnerHTML`; React escaping. No HIGH threat in a test-scaffold plan. |
</threat_model>

<verification>
- `pnpm test` shows all 5 new Phase-2 suites RED (missing component modules) — the intended Wave 0 state.
- `pnpm tsx scripts/check-apca.ts` runs and reports the new on-band label pairing.
- No production component shipped yet (Waves 1–3 own that).
</verification>

<success_criteria>
- 5 RED test files exist under `src/components/numen-landing/__tests__/`.
- `scripts/check-apca.ts` extended with the label-on-`#7faf7a` Lc ≥ 60 pairing.
- Every Phase-2 requirement (HERO-01..04, READ-01, READ-02, CTA-01) now has at least one automated assertion waiting to go GREEN.
</success_criteria>

<output>
Create `.planning/phases/02-hero-centerpiece-reading-explainer/02-01-SUMMARY.md` when done
</output>
