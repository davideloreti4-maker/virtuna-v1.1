---
phase: 02-hero-centerpiece-reading-explainer
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - public/images/landing/hero/keyframe.webp
  - src/components/numen-landing/verdict-throne.tsx
autonomous: false
requirements: [HERO-02, HERO-03]
must_haves:
  truths:
    - "A real, rights-clear creator-video keyframe (NOT stock, NOT fake browser chrome) lives in public/images/landing/hero/ ready for static import"
    - "The verdict throne renders the good (muted green) band + a confident label + a specific one-line why, with NO naked number and NO percentage"
  artifacts:
    - path: "public/images/landing/hero/keyframe.webp"
      provides: "the ONE real creator keyframe (HERO-02 authenticity asset)"
      contains: "a genuine creator-video still — human-supplied, rights-clear"
    - path: "src/components/numen-landing/verdict-throne.tsx"
      provides: "the HERO-03 verdict band + label + why, built on VerdictSwatch"
      min_lines: 20
      contains: "VerdictSwatch verdict=\"good\""
  key_links:
    - from: "src/components/numen-landing/verdict-throne.tsx"
      to: "@/components/numen/verdict-swatch"
      via: "import { VerdictSwatch }"
      pattern: "VerdictSwatch verdict=.good."
    - from: "verdict-throne label/why copy"
      to: ".planning/VOICE.md Rule 3"
      via: "band + why, never a naked number"
      pattern: "(?!.*\\d+\\s*%)(?!.*\\d+\\s*/\\s*100)"
---

<objective>
Wave 1 — land the ONE real creator keyframe (human checkpoint) and build the
presentational verdict throne (HERO-03) that Waves 2–3 reuse in both the hero
loop and the explainer's third step.

Purpose: HERO-02 requires a REAL creator-video still (no stock, no fake chrome);
sourcing it is a rights/content decision a human must confirm. HERO-03 requires
the verdict to read as a calibrated band + one-line why, never a naked number —
the throne component encodes that contract once and is reused everywhere.
Output: `public/images/landing/hero/keyframe.webp` + `verdict-throne.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/VOICE.md
@.planning/phases/02-hero-centerpiece-reading-explainer/02-UI-SPEC.md
@.planning/phases/02-hero-centerpiece-reading-explainer/02-PATTERNS.md

<interfaces>
<!-- The throne consumes these. Extracted from in-repo source — use directly, no exploration. -->

From src/components/numen/verdict-swatch.tsx (the band base — reuse, never fork):
```
export function VerdictSwatch({ verdict, size, children, className }: VerdictSwatchProps)
// verdict: "good" | "mixed" | "bad"   size: "sm" | "md" | "lg"
// literal tv classes: good -> "bg-verdict-good"  (greppable; NEVER bg-${verdict})
// on-swatch children MUST meet APCA Lc >= 60
```

From src/components/numen/surface.tsx (the solid plate, if the APCA gate forces a backing):
```
export function Surface({ children, className, style, as }: SurfaceProps)
// base already: rounded-[12px] border border-border bg-panel + soft shadow
```

cn() merge util — src/lib/utils.ts (caller className merged last, override wins).

Token names ONLY (resolve under .numen-surface): text-text, text-text-muted,
border-border, bg-panel, bg-verdict-good. NEVER hardcode hex in JSX.

Locked copy register (02-UI-SPEC.md §Copywriting Contract, refinable per D-08a):
  label: "This will likely land."
  why:   "Strong hook in the first 2 seconds — tighten the middle and it lands."
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Supply ONE rights-clear real creator keyframe</name>
  <what-built>
    The hero artifact (HERO-02) MUST be a REAL creator-video still — never stock,
    never a fake browser/window chrome, never a fabricated diagram (REQUIREMENTS
    + UI-SPEC §Asset authenticity). This is the one residual CONTENT dependency
    the research flagged (02-RESEARCH Open Q1 / A4): app-capture from the sibling
    `/analyze` is NOT viable (auth-gated + verdict UI unshipped), so a human must
    supply the still directly.
  </what-built>
  <how-to-verify>
    1. Place ONE rights-clear real creator-video keyframe at
       `public/images/landing/hero/keyframe.webp` (webp preferred for weight;
       jpg acceptable). It must be a genuine still from a real creator video that
       you own or have rights to use.
    2. Confirm it is NOT: a stock photo, a screenshot of a fake browser/app window,
       or an abstract/fabricated diagram.
    3. Note its intrinsic pixel dimensions (next/image static import derives
       width/height/blur from the file — a landscape ~16:9 or vertical ~9:16 still
       both work; the loop container sets the aspect ratio).
    4. Reply with the creator/niche shown (drives the verdict why copy register)
       and confirm rights are clear.
  </how-to-verify>
  <resume-signal>Reply "approved — keyframe in place" (+ the niche shown) or describe what is still needed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Build the verdict throne (HERO-03) on VerdictSwatch</name>
  <files>src/components/numen-landing/verdict-throne.tsx</files>
  <read_first>
    - src/components/numen/verdict-swatch.tsx (the band base — props + literal classes, read in full)
    - src/components/numen/surface.tsx (the bg-panel plate, if the APCA gate forced a backing)
    - src/components/numen-landing/__tests__/verdict-throne.test.tsx (the RED target from Plan 02-01 — make it GREEN)
    - scripts/check-apca.ts (run it first — its label-on-#7faf7a result decides plate-vs-on-band, per Plan 02-01 Task 3)
    - .planning/phases/02-hero-centerpiece-reading-explainer/02-PATTERNS.md (§verdict-throne.tsx — composition, lines 126-156)
    - .planning/phases/02-hero-centerpiece-reading-explainer/02-UI-SPEC.md (§Color verdict throne contract + §Copywriting Contract, lines 108-112, 147-171)
    - .planning/VOICE.md (Rule 3 — band + why, never a number, lines 55-60)
  </read_first>
  <action>
    Create `src/components/numen-landing/verdict-throne.tsx` as a PRESENTATIONAL
    (no `"use client"` needed) component that renders the GOOD verdict band per
    D-03: a `VerdictSwatch verdict="good" size="md"` swatch, a bold confident
    label, and a muted specific one-line why. Reuse `VerdictSwatch` from
    `@/components/numen/verdict-swatch` — NEVER hand-roll a colored div, NEVER
    interpolate `bg-${verdict}` (Tailwind v4 can't see dynamic strings; the literal
    classes live in VerdictSwatch precisely to be greppable).

    Composition (PATTERNS §verdict-throne): a container that holds a row of
    `VerdictSwatch good` + bold label (`text-sm md:text-base font-bold text-text`),
    then a why line below (`mt-2 text-sm md:text-base leading-relaxed text-text-muted`).
    Copy (02-UI-SPEC §Copywriting, refinable per D-08a but must pass VOICE Rule 3):
      label = "This will likely land."
      why   = "Strong hook in the first 2 seconds — tighten the middle and it lands."
    The why MUST name one real strength + one fixable note (luma discipline) — no
    flattery, no %, no number, no engine jargon (VOICE Rules 1-3).

    PLATE DECISION (gated on Plan 02-01 Task 3 / the APCA gate): run
    `pnpm tsx scripts/check-apca.ts` FIRST. If the label-on-`#7faf7a` pairing
    PASSES Lc ≥ 60, the label may sit directly on the band. If it FAILS (the
    expected `text-text` on `#7faf7a` case), back the throne on a SOLID
    `bg-panel`/`border-border` plate — use `Surface` or a div with
    `rounded-[12px] border border-border bg-panel p-4 md:p-6` — so the label sits
    on the opaque plate, NOT on the band, and NOT glass-over-photo (Lightning CSS
    strips backdrop-filter; UI-SPEC forbids glass-over-photo). Color by token NAME
    only — no hex in JSX. Accept an optional `className` prop merged LAST via `cn()`
    so the hero/explainer can size/position it.

    Do NOT render any number, `/100`, or `%` anywhere — the verdict-throne test
    asserts `not.toMatch(/\d+\s*\/\s*100|\d+\s*%/)`. The throne is NOT a heading
    (no h1/h2); label is a `<span>`/`<p>`, the why is a `<p>`.
  </action>
  <verify>
    <automated>pnpm test -- verdict-throne && pnpm tsx scripts/check-apca.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- verdict-throne` is GREEN: label + why render, NO naked number/percentage.
    - The component imports and uses `VerdictSwatch verdict="good"` — no hand-rolled band, no `bg-${...}` interpolation.
    - No hex literal appears in the JSX (all color via token utilities `text-text`, `text-text-muted`, `bg-panel`, `border-border`, `bg-verdict-good`).
    - If the APCA gate's label-on-band pairing fails, the label sits on a `bg-panel` plate (verified by reading the rendered markup — label is a child of the plate, not the swatch).
    - Copy passes VOICE self-check (Rules 1-3) — re-checked by the Plan 02-01 voice test once Hero/HowItWorks ship.
  </acceptance_criteria>
  <done>VerdictThrone renders the good band + confident label + specific why, plate-backed if the APCA gate required it, with zero naked numbers — reusable by the hero loop and explainer step 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → CDN/static (`public/`) | The supplied keyframe is a static build input; verify it is rights-clear + a real still (content/legal, not a code attack surface). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | verdict-throne copy | mitigate | Static JSX string literals + React escaping; never `dangerouslySetInnerHTML`. |
| T-02-03 | Information disclosure | supplied keyframe | accept | Human checkpoint confirms rights-clear + no private/identifying content; public marketing still by design. |
</threat_model>

<verification>
- Keyframe present at `public/images/landing/hero/keyframe.webp`, human-confirmed real + rights-clear.
- `pnpm test -- verdict-throne` GREEN.
- `pnpm tsx scripts/check-apca.ts` runs; throne composition matches its label-on-band result.
</verification>

<success_criteria>
- HERO-02 asset dependency satisfied (real keyframe in `public/`).
- HERO-03 verdict throne renders band + label + why, no naked number, APCA-safe.
</success_criteria>

<output>
Create `.planning/phases/02-hero-centerpiece-reading-explainer/02-02-SUMMARY.md` when done
</output>
