---
phase: 02-hero-centerpiece-reading-explainer
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/components/numen-landing/how-it-works.tsx
  - src/app/(marketing)/page.tsx
autonomous: true
requirements: [READ-01, READ-02]
must_haves:
  truths:
    - "A visitor reads a 3-step explainer (upload → engine reads → verdict + why) under #how-it-works, each step demonstrated with REAL content not an abstract diagram"
    - "The #hero and #how-it-works slots in page.tsx are filled with <Hero /> and <HowItWorks />; the page still has exactly one <h1>"
  artifacts:
    - path: "src/components/numen-landing/how-it-works.tsx"
      provides: "READ-01/READ-02 — the 3-step real-content explainer"
      min_lines: 30
      contains: "grid md:grid-cols-3; VerdictThrone; next/image keyframe"
    - path: "src/app/(marketing)/page.tsx"
      provides: "wired hero + explainer slots"
      contains: "<Hero />; <HowItWorks />"
  key_links:
    - from: "src/app/(marketing)/page.tsx"
      to: "src/components/numen-landing/hero.tsx + how-it-works.tsx"
      via: "imports + SectionShell slot fill"
      pattern: "<Hero|<HowItWorks"
    - from: "src/components/numen-landing/how-it-works.tsx"
      to: "src/components/numen-landing/verdict-throne.tsx"
      via: "step 3 reuses <VerdictThrone />"
      pattern: "VerdictThrone"
    - from: "src/components/numen-landing/how-it-works.tsx"
      to: "public/images/landing/hero/keyframe.webp"
      via: "step 1 reuses the real keyframe (next/image)"
      pattern: "next/image"
---

<objective>
Wave 3 — build the 3-step Reading explainer reusing the SAME real artifacts
(READ-01/READ-02), and wire both new components into the two empty page slots.

Purpose: READ-02 requires each step to show REAL content (the same keyframe /
stage-read / verdict band), never an abstract diagram or icon-only card. This is
the final integration: page.tsx fills `#hero` with `<Hero />` and `#how-it-works`
with `<HowItWorks />` while preserving the single-h1 invariant.
Output: `how-it-works.tsx` + the in-place `page.tsx` edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/VOICE.md
@.planning/phases/02-hero-centerpiece-reading-explainer/02-UI-SPEC.md
@.planning/phases/02-hero-centerpiece-reading-explainer/02-PATTERNS.md

<interfaces>
<!-- Consumed contracts — extracted from in-repo source. Use directly. -->

From src/components/numen/surface.tsx (the card container — reuse, don't re-declare its base):
```
export function Surface({ children, className, style, as }: SurfaceProps)
// base: rounded-[12px] border border-border bg-panel + soft shadow (already set)
```

From src/components/numen-landing/verdict-throne.tsx (Wave 1) — step 3 reuses <VerdictThrone />.
From src/components/numen-landing/hero.tsx (Wave 2) — page.tsx mounts <Hero />.

From src/components/numen-landing/section-shell.tsx (the slot — read in full):
```
SectionShell({ id, heading?, children, className })
// heading -> internal <h2>; hero passes NO heading (renders own <h1>).
```

The CURRENT page.tsx slots to fill — src/app/(marketing)/page.tsx:24-45:
  line 24: <SectionShell id="hero" className="pt-28 pb-24 md:pt-40 md:pb-32"> ... inline markup ... </SectionShell>
  line 45: <SectionShell id="how-it-works" heading="How the Reading works" />  (currently self-closing, EMPTY)

Locked explainer copy (02-UI-SPEC §Copywriting, refinable per D-08a):
  H2 "How the Reading works"
  Step 1 "Upload" / "Drop in your video — that's all it needs."
  Step 2 "Numen reads it" / "It watches your video the way your sharpest audience would."
  Step 3 "Your verdict" / "An honest call on whether it'll land — and the one thing to fix."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 3-step explainer reusing the real artifacts (READ-01 / READ-02)</name>
  <files>src/components/numen-landing/how-it-works.tsx</files>
  <read_first>
    - src/components/numen/surface.tsx (the card container — read in full)
    - src/components/numen-landing/verdict-throne.tsx (Wave 1 — step 3 reuse)
    - src/components/numen-landing/footer.tsx (closest static multi-column layout analog)
    - src/components/numen-landing/__tests__/how-it-works.test.tsx (RED target — make GREEN)
    - .planning/phases/02-hero-centerpiece-reading-explainer/02-PATTERNS.md (§how-it-works.tsx — Surface grid + h3 + real content, lines 160-182)
    - .planning/phases/02-hero-centerpiece-reading-explainer/02-UI-SPEC.md (§3-Step Explainer Contract + §Copywriting, lines 136-171)
    - .planning/VOICE.md (Rules 1-3 — step 2 "reads it" with NO engine jargon)
  </read_first>
  <action>
    Create `src/components/numen-landing/how-it-works.tsx` as a server component
    (RSC — NO `"use client"`; D-06 clickable-to-hero is DEFERRED, keep it static).
    Render a 3-column responsive grid: mobile single stack (`grid gap-6`), desktop
    `md:grid-cols-3 md:gap-8`, with `mt-8` top spacing under the SectionShell h2.
    Each step is a `Surface` card (`className="p-4 md:p-6"` — do NOT re-declare its
    base radius/border/bg). Each step title is an `<h3>` (`text-base md:text-lg
    font-medium text-text`) — correct level under the `#how-it-works` `<h2>` from
    SectionShell, no skip; the component emits NO h1/h2 itself.

    Each step shows REAL content (READ-02 — hard, NOT icon-only, NOT a diagram):
      Step 1 "Upload": render the SAME real keyframe (static-import via next/image,
        small thumbnail, `placeholder="blur"`, reuse the Plan 02-02 asset path) +
        body "Drop in your video — that's all it needs."
      Step 2 "Numen reads it": render a real stage-read excerpt (the same short
        VOICE-clean line used in the loop's stage 2, or an equivalent real read
        line) + body "It watches your video the way your sharpest audience would."
        — NO engine jargon (Rule 1: no Apollo/fold/Omni/model/pipeline).
      Step 3 "Your verdict": reuse `<VerdictThrone />` (the same good band + label +
        why) + body "An honest call on whether it'll land — and the one thing to fix."

    Copy is refinable per D-08a but must pass VOICE Rules 1-3 (the Plan 02-01 voice
    test scans this component's rendered text). Color by token name only; renders
    inside `.numen-surface`. lucide-react step icons are OPTIONAL and must NOT
    replace the real content — prefer the real artifact over a glyph.
  </action>
  <verify>
    <automated>pnpm test -- how-it-works voice</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- how-it-works` GREEN: 3 step titles render as `<h3>`; step 1 contains an `<img>` (real keyframe); step 3 renders the verdict band label (VerdictThrone reuse).
    - `pnpm test -- voice` GREEN: rendered explainer text contains no forbidden VOICE tokens (`%`, viral, guaranteed, engine terms).
    - Component is RSC (no `"use client"`); emits no h1/h2; reuses `Surface` + `VerdictThrone` (no hand-rolled card or band).
    - No hex literals in JSX; real content per step (no icon-only step).
  </acceptance_criteria>
  <done>The 3-step explainer demonstrates upload → reads → verdict with the SAME real artifacts — READ-01 + READ-02 satisfied, static (D-06 deferred).</done>
</task>

<task type="auto">
  <name>Task 2: Wire Hero + HowItWorks into the page slots (integration)</name>
  <files>src/app/(marketing)/page.tsx</files>
  <read_first>
    - src/app/(marketing)/page.tsx (read in full — the slots + the inline hero markup to replace, + the now-unused FOCUS_RING/Link)
    - src/components/numen-landing/hero.tsx (Wave 2 — what fills #hero)
    - src/components/numen-landing/how-it-works.tsx (Task 1 — what fills #how-it-works)
    - src/components/numen-landing/__tests__/hero.test.tsx + page-level single-h1 assertion (02-VALIDATION cross-row)
    - .planning/phases/02-hero-centerpiece-reading-explainer/02-PATTERNS.md (§page.tsx MODIFY — slot fill, lines 221-232)
  </read_first>
  <action>
    Edit `src/app/(marketing)/page.tsx` in place. Replace the inline hero markup
    (lines 25-41, the `<div className="mx-auto flex max-w-3xl ...">` block) with a
    single `<Hero />` child INSIDE the existing `<SectionShell id="hero"
    className="pt-28 pb-24 md:pt-40 md:pb-32">` — keep the slot wrapper, keep NO
    `heading=` (single-h1 rule). Fill the currently self-closing `#how-it-works`
    slot (line 45) with `<HowItWorks />` as children while KEEPING
    `heading="How the Reading works"`:
      <SectionShell id="how-it-works" heading="How the Reading works">
        <HowItWorks />
      </SectionShell>
    Add the two imports (`import { Hero } from "@/components/numen-landing/hero";`
    and `import { HowItWorks } from "@/components/numen-landing/how-it-works";`).

    Remove the now-unused `FOCUS_RING` constant and the `Link` import IF they are
    no longer referenced elsewhere in the file (the hero markup that used them moved
    into hero.tsx) — verify the other slots (honesty/gallery/proof/cta) do not use
    them before deleting; if any still does, leave them. Do NOT touch the other 4
    SectionShell slots (#honesty, #gallery, #proof, #cta) — those are Phase 3. The
    page stays a server component (Hero/HowItWorks are imported components).

    After the edit the page MUST still have exactly one `<h1>` (Hero's), and
    `#how-it-works` MUST still emit its `<h2>`.
  </action>
  <verify>
    <automated>pnpm test && pnpm tsx scripts/check-apca.ts && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports + renders `<Hero />` (inside `#hero`, no `heading=`) and `<HowItWorks />` (inside `#how-it-works`, keeps `heading=`).
    - The full suite is GREEN: all 5 Phase-2 component tests pass, the page has exactly one `<h1>`, APCA gate passes, `pnpm build` succeeds.
    - The other 4 SectionShell slots (#honesty/#gallery/#proof/#cta) are untouched.
    - No unused imports left dangling (lint clean): `pnpm lint` has no new errors for this file.
  </acceptance_criteria>
  <done>The hero + explainer fill their slots; the whole Phase-2 suite + APCA gate + build are green; single-h1 preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none runtime) | Static marketing markup; no input/fetch/auth/data view. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | explainer copy strings | mitigate | Static JSX literals + React escaping; never `dangerouslySetInnerHTML`. |
| T-02-07 | (a11y) | heading levels | mitigate | Single `<h1>` (hero) + `<h2>` (#how-it-works) + step `<h3>` — no level skip; asserted by the page single-h1 + how-it-works h3 tests. |
</threat_model>

<verification>
- `pnpm test && pnpm tsx scripts/check-apca.ts && pnpm build` GREEN (phase gate).
- Manual (live dev server): explainer shows the SAME real artifacts as the hero, mobile stacks then desktop 3-up; single h1 confirmed in DOM (kill dev server + clear `.next/` if stale).
</verification>

<success_criteria>
- READ-01 (3-step upload→reads→verdict) + READ-02 (real content per step) satisfied.
- Both slots wired; full Phase-2 test suite + APCA gate + build green; single-h1 preserved.
</success_criteria>

<output>
Create `.planning/phases/02-hero-centerpiece-reading-explainer/02-04-SUMMARY.md` when done
</output>
