---
phase: 03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/lib/engine/aggregator.ts
  - src/lib/engine/version.ts
  - src/app/api/analyze/route.ts
  - src/lib/engine/__tests__/aggregator.test.ts
autonomous: false
requirements: [R5]
must_haves:
  truths:
    - "The live score blend is behavioral 40 + Apollo composite (renormalized) — the gemini term no longer feeds raw_overall_score; Apollo does not own the entire 0–100 (full rederivation deferred to P5) (D-04, D-05)"
    - "Apollo's verbatim feed reaches the reasoner (verbatim threaded from aggregator into reasonWithDeepSeek)"
    - "Apollo's rewrites + composite persist on a real run; ENGINE_VERSION bump invalidates stale cached scores"
  artifacts:
    - path: "src/lib/engine/aggregator.ts"
      provides: "behavioral + apollo blend; verbatim threaded into DeepSeekInput; confidence reuses Apollo-vs-behavioral agreement"
      contains: "apollo"
    - path: "src/lib/engine/version.ts"
      provides: "ENGINE_VERSION 3.3.0"
      contains: "3.3.0"
    - path: "src/app/api/analyze/route.ts"
      provides: "variants.apollo read-merge-write persist of rewrites/composite"
      contains: "variants"
  key_links:
    - from: "src/lib/engine/aggregator.ts"
      to: "deepseekResult.reasoning.composite_score"
      via: "apollo term in raw_overall_score"
      pattern: "composite_score"
    - from: "src/lib/engine/aggregator.ts"
      to: "src/lib/engine/deepseek.ts reasonWithDeepSeek"
      via: "verbatim passed on DeepSeekInput.verbatim"
      pattern: "verbatim"
    - from: "src/app/api/analyze/route.ts"
      to: "analysis_results.variants.apollo"
      via: "read-merge-write (mirrors craft/decode persist)"
      pattern: "variants"
---

<objective>
Wire Apollo into the live score + persistence: rewire `aggregator.ts` so the blend is `behavioral 40 + Apollo composite` (renormalized, gemini retired from `raw_overall_score` — D-04), thread the P2 verbatim payload into `reasonWithDeepSeek` so Apollo's rewrites can quote the real line, persist Apollo's rewrites/composite into `variants.apollo`, and bump `ENGINE_VERSION` 3.2.0 → 3.3.0 so stale cached scores invalidate.

Purpose: Delivers R5-partial (Apollo's composite is now the live score term, structurally unchanged derivation so STATE.md's determinism band holds — D-04). Depends on Plan 02 (composite_score field on the schema). DO NOT make Apollo own the whole 0–100 (D-05 — behavioral stays its own term until P4).
Output: Rewired blend, verbatim feed wired, variants.apollo persist, version bump, green aggregator tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-CONTEXT.md
@.planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-RESEARCH.md
@.planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-PATTERNS.md

<interfaces>
<!-- deepseek.ts (Plan 02) — reasoning now carries the Apollo composite + DeepSeekInput.verbatim -->
DeepSeekInput.verbatim?: VerbatimPayload | null   // added in Plan 02
deepseekResult.reasoning.composite_score: number  // 0–100, the Apollo term

<!-- aggregator.ts anchors -->
aggregator.ts:71-80 SCORE_WEIGHTS { behavioral:0.40, gemini:0.35 } ; SCORE_WEIGHT_KEYS ["behavioral","gemini"]
aggregator.ts:131-141 applyCtaPenalty (fired ~:722, modifies gemini_score)
aggregator.ts:157-181 selectWeights — renorm 0.40/0.35 → 0.533/0.467
aggregator.ts:194 calculateConfidence (signal-availability + agreement check; HARD-03 LOW floor)
aggregator.ts:534-551 verbatim pluck (hook from geminiResult.analysis.hook_verbatim) ; segments after :866
aggregator.ts:683-757 blend math: behavioral_score, gemini_score, raw_overall_score

<!-- route.ts anchors -->
route.ts:176-190 decode variants read-merge-write (the pattern to copy for variants.apollo)
route.ts:512-540 buildInsertRow ; :642 JSON INSERT ; :863-865 SSE UPSERT onConflict:id ; :907/:911 SSE score persist
route.ts:383 .eq("user_id", user.id) — V4 access control, preserve

<!-- version.ts:11 ENGINE_VERSION = "3.2.0" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire the score blend to behavioral + Apollo and thread verbatim into the reasoner (D-04/D-05)</name>
  <files>src/lib/engine/aggregator.ts, src/lib/engine/__tests__/aggregator.test.ts</files>
  <read_first>
    - src/lib/engine/aggregator.ts:60-200 (SCORE_WEIGHTS, selectWeights, applyCtaPenalty, calculateConfidence)
    - src/lib/engine/aggregator.ts:530-560 (verbatim hook pluck) + :680-760 (blend math)
    - src/lib/engine/__tests__/aggregator.test.ts (existing — extend; the "blend uses behavioral + apollo" + "gemini retired" cases scaffolded red in Plan 01)
    - .planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-RESEARCH.md (Composite Score Wiring, lines 219-236; Confidence Derivation 276-281; Open Q2 374-377)
    - .planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-PATTERNS.md (aggregator.ts section, lines 154-194)
  </read_first>
  <behavior>
    - Test: "blend uses behavioral + apollo" — with both signals present, raw_overall_score == round(behavioral_score*0.533 + composite_score*0.467) (renorm split unchanged)
    - Test: "gemini retired from raw_overall_score" — gemini_score is still computed + surfaced on the result but is NOT a term in raw_overall_score
    - Test: when Apollo present + behavioral missing, full weight to Apollo (selectWeights edge); and vice versa
    - Test: verbatim threaded — reasonWithDeepSeek receives DeepSeekInput.verbatim sourced from the plucked hook (mock + capture)
    - Test: confidence reuses Apollo-vs-behavioral direction agreement (gemini agreement check removed); HARD-03 LOW floor on dual failure preserved
  </behavior>
  <action>
    In src/lib/engine/aggregator.ts: (a) rewire `SCORE_WEIGHTS` (:71) to `{ behavioral: 0.40, apollo: 0.35 }` — KEEP the 0.35 VALUE on the apollo term (RESEARCH:230) so selectWeights renorm stays 0.533/0.467 and STATE.md's "derivation structurally unchanged" determinism band holds; update `SCORE_WEIGHT_KEYS` to `["behavioral","apollo"]`. (b) Rename the `gemini` weight key to `apollo` in `selectWeights` (:157-181) and the blend; keep `gemini` as a provenance-only flag in SignalAvailability (like ml/rules/trends). (c) In the blend math (:683-757) add `const apollo_score = deepseekResult.reasoning.composite_score;` and set `raw_overall_score = clamp(round(behavioral_score * weights.behavioral + apollo_score * weights.apollo))`. KEEP `gemini_score` computed + surfaced on PredictionResult for back-compat, but REMOVE it from raw_overall_score. (d) DROP the separate `applyCtaPenalty`-on-gemini math from the blend path (Open Q2 recommendation — gemini left the blend; CTA surfaces as Apollo §2.4 critique evidence). (e) Thread verbatim: source the already-plucked hook (:534-551) into the new `DeepSeekInput.verbatim` (Plan 02) when calling `reasonWithDeepSeek`. (f) In `calculateConfidence` (:194) replace the gemini-vs-behavioral agreement check with Apollo-composite-vs-behavioral direction agreement; keep the signal-availability component + HARD-03 LOW floor. DO NOT make Apollo own the whole 0–100 (D-05 — behavioral stays its own 40 term).
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/aggregator.test.ts -t "blend uses behavioral + apollo" src/lib/engine/__tests__/aggregator.test.ts -t "gemini retired"</automated>
  </verify>
  <acceptance_criteria>
    - both named aggregator cases GREEN (were red after Plan 01)
    - `grep -q "apollo: 0.35\|apollo:0.35" src/lib/engine/aggregator.ts` (weight value preserved on apollo term)
    - gemini no longer a blend term: in raw_overall_score math `grep -A3 "raw_overall_score" src/lib/engine/aggregator.ts` shows apollo_score not gemini_score
    - verbatim threaded: `grep -q "verbatim" src/lib/engine/aggregator.ts` at the reasonWithDeepSeek call site
    - `npm test` full suite green (existing aggregator regression tests adapt or stay green); `npm run build` succeeds
  </acceptance_criteria>
  <done>The live score is behavioral 40 + Apollo composite (renorm, structurally unchanged); verbatim feeds the reasoner; confidence uses Apollo agreement; D-05 honored (behavioral stays separate).</done>
</task>

<task type="auto">
  <name>Task 2: Persist Apollo rewrites/composite into variants.apollo + bump ENGINE_VERSION 3.2.0→3.3.0</name>
  <files>src/app/api/analyze/route.ts, src/lib/engine/version.ts</files>
  <read_first>
    - src/app/api/analyze/route.ts:159-200 (decode variants read-merge-write — the pattern to copy) + :512-545 (buildInsertRow) + :860-925 (SSE UPSERT + score persist)
    - src/app/api/analyze/route.ts:383 (.eq("user_id", user.id) — V4 access control to preserve)
    - src/lib/engine/version.ts (ENGINE_VERSION :11 + lineage doc-comment)
    - .planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-RESEARCH.md (Open Q1 369-372; Pitfall 3 301-305; Runtime State Inventory 205-217)
    - .planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-PATTERNS.md (route.ts section 249-271; version.ts 275-277)
  </read_first>
  <action>
    (a) In src/app/api/analyze/route.ts, persist Apollo's rewrites + dimensions + composite into `variants.apollo` using the EXACT read-merge-write pattern from the decode persist (:176-190): read current `variants`, spread to preserve sibling writers (craft/remix race), `update({ variants: { ...current, apollo } })`. Source `apollo` from `finalResult.reasoning` (rewrites/dimensions/composite_score/confidence_scope). The composite already surfaces via `overall_score` (the blend output, Task 1) — variants.apollo is the rewrites/dimensions payload (no migration needed, mirrors P2 craft — Open Q1). PRESERVE `.eq("user_id", user.id)` on the persist (V4 access control, route.ts:383/:936). Apply on the score-mode persist path only (NOT remix). (b) In src/lib/engine/version.ts bump `ENGINE_VERSION = "3.2.0"` → `"3.3.0"` (:11) and update the lineage doc-comment block (:1-10) noting the P3 Apollo blend rewire as the invalidation reason (Pitfall 3 — skipping the bump serves stale pre-Apollo scores).
  </action>
  <verify>
    <automated>grep -q '"3.3.0"' src/lib/engine/version.ts &amp;&amp; grep -q "variants.*apollo\|apollo.*variants" src/app/api/analyze/route.ts &amp;&amp; npm test 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"3.3.0"' src/lib/engine/version.ts` (version bumped) and `grep -c '"3.2.0"' src/lib/engine/version.ts` returns 0
    - route persists variants.apollo via read-merge-write: `grep -q "variants" src/app/api/analyze/route.ts` at a new apollo persist site that spreads `...current` (no wholesale variants overwrite)
    - V4 access control preserved: `grep -c '.eq("user_id", user.id)' src/app/api/analyze/route.ts` unchanged from baseline
    - `npm test` full suite green; `npm run build` succeeds
  </acceptance_criteria>
  <done>Apollo rewrites/composite persist on a real run via variants.apollo without clobbering craft/remix; ENGINE_VERSION 3.3.0 invalidates stale cached scores.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live verification — R2 verbatim rewrite + R8 determinism band + R6 latency on a real video</name>
  <what-built>The full P3 Apollo flow on a real video: deepseek.ts reframed to the shared core, blend = behavioral + Apollo, verbatim-grounded rewrites, persisted to variants.apollo, ENGINE_VERSION 3.3.0. This checkpoint verifies the live behaviors the unit tests mock: R2 verbatim-grounded rewrite on a real run, R8 determinism band on re-run, and R6 latency under cap.</what-built>
  <action>Human runs the live verification steps below (smoke harness + re-run + latency check + persisted-row check) and confirms R2/R8/R6 hold on a real video. All implementation is already done in Tasks 1-2 + Plans 01-03; this task is verification only — no code is written here.</action>
  <how-to-verify>
    1. Run the validated smoke harness on a real .mp4: `npx tsx scripts/apollo-core-smoke.ts` (or `npx tsx scripts/measure-pipeline.ts` for the full E2E) on a fixed video. Confirm the output contains a `rewrites[]` whose `original` equals the verbatim hook line (R2), 6 dimensions each with a §2 lever + quoted evidence, and a composite 0–100.
    2. Re-run the SAME video and confirm the composite lands within a ±1–2 band of the first run (R8 — determinism is a tolerance band, NOT byte-identity per STATE.md 2026-06-04; Apollo is a single call so drift should be small).
    3. Confirm E2E latency is under the 300s Vercel cap with headroom (R6, target ≤90s) — `measure-pipeline.ts` prints the total.
    4. Re-analyze a known video and confirm the persisted `variants.apollo` carries the rewrites and `overall_score` reflects the new behavioral+apollo blend (not a stale pre-Apollo number — ENGINE_VERSION 3.3.0 should have invalidated it).
  </how-to-verify>
  <resume-signal>Type "approved" if R2 (rewrite.original == verbatim hook on a real run), R8 (composite within ±1–2 band on re-run), and R6 (under cap) all hold; otherwise describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apollo reasoning output → aggregator blend → persisted overall_score | model-derived composite drives the user-visible score |
| finalResult.reasoning → analysis_results.variants JSONB | untrusted model JSON persisted; must not clobber sibling writers or cross user boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | variants wholesale-overwrite clobbers concurrent craft/remix writes | mitigate | Read-merge-write spread (...current) per route.ts:176-190 — preserves sibling variants |
| T-03-10 | Elevation of Privilege | Apollo persist writes across user boundary | mitigate | Preserve `.eq("user_id", user.id)` on the persist (V4, route.ts:383/:936) |
| T-03-11 | Repudiation/Info | stale pre-Apollo cached score served as the new blend | mitigate | ENGINE_VERSION 3.2.0→3.3.0 bump invalidates L1/L2 cache (Pitfall 3); checkpoint step 4 confirms |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new packages (RESEARCH audit: not applicable); no install task |
</threat_model>

<verification>
- R5-partial: overall_score = behavioral 40 + Apollo composite (renorm), gemini retired from the blend; structurally unchanged → STATE.md determinism band holds (D-04).
- D-05: Apollo does NOT own the whole 0–100; behavioral stays its own 40 term.
- R8/R6/R2 (live): confirmed at the checkpoint on a real video (determinism band, under cap, verbatim-grounded rewrite).
- Pitfall 3: ENGINE_VERSION 3.3.0 invalidates stale scores.
- V4: user_id scoping preserved on persist.
</verification>

<success_criteria>
- Blend rewired to behavioral + Apollo (renorm), gemini surfaced but retired from raw_overall_score.
- verbatim threaded into reasonWithDeepSeek; variants.apollo persists rewrites/composite (read-merge-write).
- ENGINE_VERSION 3.3.0; full `npm test` + `npm run build` green; live checkpoint approved (R2/R8/R6).
</success_criteria>

<output>
Create `.planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-04-SUMMARY.md` when done.
</output>
