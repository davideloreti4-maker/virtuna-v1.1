---
phase: 03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/engine/remix/decode-prompts.ts
  - src/lib/engine/remix/adapt.ts
  - src/lib/engine/__tests__/remix-core-grounding.test.ts
autonomous: true
requirements: [R12]
must_haves:
  truths:
    - "Remix decode + adapt are grounded in the SAME shared knowledge core as score-mode Apollo"
    - "decode + adapt output contracts (4 beats + luck, concepts[3]) are unchanged — /api/remix/adapt + Remix board stay green"
  artifacts:
    - path: "src/lib/engine/remix/decode-prompts.ts"
      provides: "DECODE_SYSTEM_PROMPT re-grounded on core §5, contract preserved"
      contains: "KNOWLEDGE_CORE"
    - path: "src/lib/engine/remix/adapt.ts"
      provides: "ADAPT_SYSTEM_PROMPT re-grounded on core §6+§2, concepts[3] preserved"
      contains: "KNOWLEDGE_CORE"
  key_links:
    - from: "src/lib/engine/remix/decode-prompts.ts"
      to: "src/lib/engine/apollo-core.ts"
      via: "DECODE_SYSTEM_PROMPT prepends KNOWLEDGE_CORE"
      pattern: "KNOWLEDGE_CORE"
    - from: "src/lib/engine/remix/adapt.ts"
      to: "src/lib/engine/apollo-core.ts"
      via: "ADAPT_SYSTEM_PROMPT prepends KNOWLEDGE_CORE"
      pattern: "KNOWLEDGE_CORE"
---

<objective>
Ground Remix's two reasoning surfaces in the SAME knowledge core as score-mode Apollo (R12): swap `decode.ts`'s bespoke framework for core §5 (decode lens) and `adapt.ts`'s `ADAPT_SYSTEM_PROMPT` for core §6+§2 — re-grounding the KNOWLEDGE, preserving the output CONTRACTS (D-11/D-12). One brain across modes; no divergent knowledge base.

Purpose: Delivers R12. Parallel with Plan 02 (different files — remix/ only). D-13 verified by Plan 01's static assert (§5 beats map 1:1 onto BEAT_IDS). Output schemas stay intact so /api/remix/adapt + the Remix board do not break.
Output: Re-grounded decode/adapt system prompts referencing KNOWLEDGE_CORE; remix contracts unchanged; remix tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-CONTEXT.md
@.planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-RESEARCH.md
@.planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-PATTERNS.md
@.planning/corpus/KNOWLEDGE-CORE.md

<interfaces>
<!-- apollo-core.ts (Plan 01) — import the shared core -->
export const KNOWLEDGE_CORE: string; // byte-stable, zero interpolation

<!-- decode-prompts.ts:27 DECODE_SYSTEM_PROMPT (re-ground framework, KEEP contract) -->
<!-- decode-prompts.ts:97 buildDecodeContext (KEEP unchanged) -->
<!-- decode-types.ts:27-33 BEAT_IDS = [hook_pattern, structure_pacing, the_turn, emotional_beat] (D-13: == §5 beats) -->
<!-- decode.ts:67 retry nudge on USER message (the correct pattern) -->

<!-- adapt.ts:30 ADAPT_SYSTEM_PROMPT (re-ground), :56-65 AdaptConcept(s)ZodSchema length(3) (KEEP), :110 generateAdaptConcepts, :119-127 extraInstruction currently on SYSTEM (MOVE to USER) -->

<!-- KNOWLEDGE-CORE.md §5 Decode Lens line 176; §6 Rewrite line 186; §2 frameworks line 31 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-ground DECODE_SYSTEM_PROMPT on core §5 (preserve the 4-beat + luck contract)</name>
  <files>src/lib/engine/remix/decode-prompts.ts, src/lib/engine/__tests__/remix-core-grounding.test.ts</files>
  <read_first>
    - src/lib/engine/remix/decode-prompts.ts (FULL — DECODE_SYSTEM_PROMPT :27, buildDecodeContext :97; the Voice Contract / Beat Requirements / Luck taxonomy / JSON Output Schema blocks to PRESERVE)
    - src/lib/engine/remix/decode-types.ts:27-33 (BEAT_IDS — D-13 1:1 map to §5)
    - src/lib/engine/apollo-core.ts (Plan 01 — import KNOWLEDGE_CORE)
    - .planning/corpus/KNOWLEDGE-CORE.md §5 (lines 176-185 — the decode lens)
    - .planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-PATTERNS.md (decode-prompts section, lines 197-211)
  </read_first>
  <behavior>
    - Test: DECODE_SYSTEM_PROMPT contains KNOWLEDGE_CORE (R12 "decode prompt references shared core") — already scaffolded red in Plan 01's remix-core-grounding.test.ts
    - Test: DECODE_SYSTEM_PROMPT still instructs EXACTLY 4 beats and the luck taxonomy (contract preserved)
    - Test (regression): DecodeResult Zod contract unchanged — existing decode-route + decode tests stay green
  </behavior>
  <action>
    In src/lib/engine/remix/decode-prompts.ts, change `DECODE_SYSTEM_PROMPT` (line 27) to prepend the shared core: `import { KNOWLEDGE_CORE } from "../apollo-core";` then `DECODE_SYSTEM_PROMPT = `${KNOWLEDGE_CORE}\n\n---\n\n` + "Apply the §5 Decode Lens. Separate repeatable craft from unrepeatable luck. Return EXACTLY 4 beats (hook_pattern §2.1 · structure_pacing §2.2 · the_turn §2.2 head-fake · emotional_beat §2.3) ..." + [the existing voice contract + JSON Output Schema block UNCHANGED]`. KEEP unchanged (D-12): the Voice Contract, Beat Requirements, Luck taxonomy, the JSON Output Schema block, and `buildDecodeContext` (:97). The §5 lens drops nothing — D-13 verified §5's 4 beats map 1:1 onto BEAT_IDS and luck[] is grounded in §5's "repeatable vs luck" paragraph. Preserve the byte-stable prefix: the dynamic per-request content stays in `buildDecodeContext` (user message); the retry nudge stays on the USER message (decode.ts:67 already correct).
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/remix-core-grounding.test.ts -t "decode" src/lib/engine/__tests__/decode-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "KNOWLEDGE_CORE" src/lib/engine/remix/decode-prompts.ts` (R12 verify)
    - remix-core-grounding.test.ts decode case GREEN (was red after Plan 01)
    - DECODE_SYSTEM_PROMPT still references 4 beats: `grep -q "4 beats\|EXACTLY 4" src/lib/engine/remix/decode-prompts.ts`
    - DecodeResult Zod schema UNTOUCHED: `git diff src/lib/engine/remix/decode-types.ts` empty
    - existing decode-route tests green; `npm run build` succeeds
  </acceptance_criteria>
  <done>decode is grounded in the shared core §5 (R12); the 4-beat + luck output contract is byte-for-byte the same (D-12).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-ground ADAPT_SYSTEM_PROMPT on core §6+§2 (preserve concepts[3]; move nudge to user message)</name>
  <files>src/lib/engine/remix/adapt.ts, src/lib/engine/__tests__/remix-core-grounding.test.ts</files>
  <read_first>
    - src/lib/engine/remix/adapt.ts (FULL — ADAPT_SYSTEM_PROMPT :30, AdaptConcept(s)ZodSchema :56-65, generateAdaptConcepts :110, extraInstruction :119-127, buildAdaptUserContent :77-98)
    - src/lib/engine/apollo-core.ts (Plan 01 — import KNOWLEDGE_CORE)
    - .planning/corpus/KNOWLEDGE-CORE.md §6 (line 186) + §2 (line 31)
    - .planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-PATTERNS.md (adapt.ts section, lines 215-229)
  </read_first>
  <behavior>
    - Test: ADAPT_SYSTEM_PROMPT contains KNOWLEDGE_CORE (R12 "adapt prompt references shared core")
    - Test: AdaptConceptsZodSchema still requires exactly 3 concepts (contract preserved)
    - Test (regression): generateAdaptConcepts returns 3 concepts on a valid mock response (existing adapt test pattern stays green)
  </behavior>
  <action>
    In src/lib/engine/remix/adapt.ts, change `ADAPT_SYSTEM_PROMPT` (line 30) to prepend the shared core: `import { KNOWLEDGE_CORE } from "@/lib/engine/apollo-core";` then `ADAPT_SYSTEM_PROMPT = `${KNOWLEDGE_CORE}\n\n---\n\n` + "Apply the §6 Rewrite Lens + §2 frameworks for FORMAT adaptation. " + [the existing RULES + OUTPUT JSON shape from adapt.ts:33-50 UNCHANGED]`. KEEP unchanged (D-12): `AdaptConceptZodSchema` (:56-61), `AdaptConceptsZodSchema.length(3)` (:63-65), `buildAdaptUserContent` (:77-98), and the `generateAdaptConcepts` call structure (temp0+seed, json_object, Sentry-on-final-failure). FIX the byte-stability bug: the retry `extraInstruction` is currently concatenated onto the SYSTEM message (adapt.ts:127 `ADAPT_SYSTEM_PROMPT + extraInstruction`) — now that the core is in the prefix, MOVE the nudge to the USER message (align with decode.ts:67 + deepseek.ts:471) so the byte-stable prefix is preserved across the retry and the DashScope prefix-cache hits.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/remix-core-grounding.test.ts -t "adapt"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "KNOWLEDGE_CORE" src/lib/engine/remix/adapt.ts` (R12 verify)
    - remix-core-grounding.test.ts adapt case GREEN (was red after Plan 01)
    - AdaptConceptsZodSchema still `.length(3)`: `grep -q "length(3)" src/lib/engine/remix/adapt.ts`
    - The retry nudge is NO LONGER on the system message: `grep -c "ADAPT_SYSTEM_PROMPT + extraInstruction" src/lib/engine/remix/adapt.ts` returns 0; the nudge now appends to the user content
    - existing adapt tests green; `npm test` full suite green; `npm run build` succeeds
  </acceptance_criteria>
  <done>adapt is grounded in the shared core §6+§2 (R12); concepts[3] contract preserved (D-12); the cached prefix survives the retry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference video content → decode/adapt user message | creator-supplied reference content crosses into the LLM user message |
| decode/adapt LLM output → Zod parse → /api/remix/adapt → Remix board | untrusted model JSON crosses into typed app state (contract MUST stay stable) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | prompt injection via reference content into decode/adapt | mitigate | Dynamic content stays in buildDecodeContext/buildAdaptUserContent (user role); core in system prefix; output Zod-validated structured JSON |
| T-03-07 | Tampering | re-grounding silently breaks decode/adapt output contract → /api/remix/adapt 500s | mitigate | D-12: schemas UNTOUCHED; regression tests (decode-route + adapt) gate the change; full `npm test` green required |
| T-03-08 | DoS | retry nudge on system message busts prefix cache (latency) | mitigate | Move nudge to user message (Task 2) — preserves byte-stable prefix across retry |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new packages (RESEARCH audit: not applicable); no install task |
</threat_model>

<verification>
- R12: both DECODE_SYSTEM_PROMPT and ADAPT_SYSTEM_PROMPT reference KNOWLEDGE_CORE (grep + remix-core-grounding.test.ts green); no divergent knowledge base.
- D-11: frameworks swapped to core §5 / §6+§2.
- D-12: decode (4 beats + luck) + adapt (concepts[3]) output schemas unchanged; /api/remix/adapt stays green.
- D-13: §5 beats == BEAT_IDS (static assert from Plan 01).
</verification>

<success_criteria>
- decode + adapt system prompts both reference the shared core.
- Remix output contracts byte-for-byte preserved; full `npm test` green; `npm run build` green.
- adapt retry nudge moved to user message (byte-stable prefix preserved).
</success_criteria>

<output>
Create `.planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-03-SUMMARY.md` when done.
</output>
