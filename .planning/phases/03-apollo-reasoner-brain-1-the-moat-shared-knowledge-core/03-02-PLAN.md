---
phase: 03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/engine/types.ts
  - src/lib/engine/deepseek.ts
  - src/lib/engine/creator-rules.ts
  - src/lib/engine/_dormant/engine/creator-rules.ts
  - src/lib/engine/__tests__/deepseek.test.ts
autonomous: true
requirements: [R2, R5]
must_haves:
  truths:
    - "Apollo is grounded in the shared knowledge core, not the generic 5-step framework"
    - "Apollo emits 6 dimensions + a composite 0–100 + 2–3 hook rewrites in one deterministic call"
    - "Every rewrite's `original` equals the verbatim hook line (R2 verify), backstopped in TS"
    - "creator-rules.ts prompt-injection constants are dormant — no dual knowledge base"
  artifacts:
    - path: "src/lib/engine/deepseek.ts"
      provides: "Apollo reasoner: APOLLO_SYSTEM_PROMPT prefix + extended output + verbatim feed + backstop"
      contains: "APOLLO_SYSTEM_PROMPT"
    - path: "src/lib/engine/types.ts"
      provides: "Extended DeepSeekResponseSchema (dimensions/composite_score/rewrites/confidence_scope)"
      contains: "composite_score"
    - path: "src/lib/engine/_dormant/engine/creator-rules.ts"
      provides: "Dormanted creator-rules prompt-injection constants"
  key_links:
    - from: "src/lib/engine/deepseek.ts"
      to: "src/lib/engine/apollo-core.ts"
      via: "import APOLLO_SYSTEM_PROMPT as the system message"
      pattern: "APOLLO_SYSTEM_PROMPT"
    - from: "src/lib/engine/deepseek.ts"
      to: "VerbatimPayload.hook"
      via: "rewrite.original filled from the fed verbatim hook in the user message"
      pattern: "verbatim"
---

<objective>
Reframe `deepseek.ts` into the Apollo Reasoner: swap the generic 5-step `STABLE_SYSTEM_PROMPT` for the shared `APOLLO_SYSTEM_PROMPT` (apollo-core.ts), extend `DeepSeekResponseSchema` additively with the §4 output contract (6 dimensions + composite_score + rewrites + confidence_scope), thread P2 verbatim into the user message so rewrites quote the real hook line (R2), add the post-parse backstop, and dormant `creator-rules.ts` (D-01) now that its missing number is in the core (D-02 satisfied by Plan 01).

Purpose: Delivers R2 (quote-grounded rewrites) + R5-partial (Apollo's composite as the new score term, consumed by Plan 04). Single deterministic call (D-10). One brain SSOT (D-01).
Output: Extended schema, reframed reasoner, dormanted creator-rules, green deepseek tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-CONTEXT.md
@.planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-RESEARCH.md
@.planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-PATTERNS.md
@.planning/corpus/KNOWLEDGE-CORE.md

<interfaces>
<!-- apollo-core.ts (created Plan 01) — import the system prefix -->
export const APOLLO_SYSTEM_PROMPT: string; // KNOWLEDGE_CORE + "\n\n---\n\n" + APOLLO_INSTRUCTION, byte-stable

<!-- types.ts:29-36 VerbatimPayload (P2, do NOT redefine) -->
export interface VerbatimPayload { hook?: { spoken_words; on_screen_text }; segments?: [{ idx, spoken_text, on_screen_text }] }

<!-- types.ts:726-732 DeepSeekResponseSchema (extend ADDITIVELY) -->
DeepSeekResponseSchema = z.object({ behavioral_predictions, component_scores, suggestions(min1), warnings(default[]), confidence: enum[high,medium,low] })

<!-- deepseek.ts anchors -->
deepseek.ts:436 DeepSeekInput { input, gemini_analysis, rule_result, trend_enrichment, creator_context? }
deepseek.ts:451 reasonWithDeepSeek(context: DeepSeekInput)
deepseek.ts:383 buildDeepSeekUserMessage(context, calibration)
deepseek.ts:291 parseDeepSeekResponse — safeParse(DeepSeekResponseSchema)
deepseek.ts:479-491 the create() call — system=STABLE_SYSTEM_PROMPT (SWAP), temp:0, seed:QWEN_SEED, response_format json_object
deepseek.ts:471-474 retry nudge on the USER message (preserve)
deepseek.ts:314 loadCalibrationData (REMOVE); deepseek.ts:387-431 viral-differentiators/duration block in user message (REMOVE)
deepseek.ts:111-282 circuit breaker (KEEP); :544-546 MAX_RETRIES backoff (KEEP); :501-508 cost (KEEP); :555 Sentry (KEEP)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend DeepSeekResponseSchema additively with the §4 Apollo output contract</name>
  <files>src/lib/engine/types.ts, src/lib/engine/__tests__/deepseek.test.ts</files>
  <read_first>
    - src/lib/engine/types.ts:626-745 (SuggestionSchema, BehavioralPredictionsSchema, ComponentScoresSchema, DeepSeekResponseSchema — mirror local z.object/z.enum/.min().max() idioms)
    - src/lib/engine/types.ts:29-36 (VerbatimPayload — reuse, do NOT redefine)
    - .planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-RESEARCH.md (Output Schema Extension, lines 243-266)
    - .planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-PATTERNS.md (types.ts section, lines 112-151)
    - .planning/corpus/KNOWLEDGE-CORE.md §4 (lines 144-175 — the output contract being encoded)
  </read_first>
  <behavior>
    - Test: a valid Apollo JSON (6 dimensions, composite 0–100, 2–3 rewrites, confidence_scope) parses successfully against the extended schema ("apollo schema validates")
    - Test: composite_score outside 0–100 fails parse
    - Test: rewrites.length === 1 fails (min 2); rewrites.length === 4 fails (max 3)
    - Test: dimensions.length !== 6 fails
    - Test: existing behavioral_predictions/component_scores still required (additive, no regression)
  </behavior>
  <action>
    In src/lib/engine/types.ts, add `ApolloDimensionSchema = z.object({ name: z.enum(["hook","retention","clarity","share_pull","substance","credibility"]), band: z.enum(["strong","mid","weak"]), lever: z.string().min(1), evidence: z.string().min(1) })` and `ApolloRewriteSchema = z.object({ original: z.string().min(1), variant: z.string().min(1), lever_fixed: z.string().min(1) })`. Extend DeepSeekResponseSchema (types.ts:726) ADDITIVELY with: `dimensions: z.array(ApolloDimensionSchema).length(6)`, `composite_score: z.number().min(0).max(100)`, `ceiling_capper: z.string().min(1)`, `confidence_scope: z.string().min(1)`, `rewrites: z.array(ApolloRewriteSchema).min(2).max(3)`, `platform_note: z.string().optional()`. KEEP `behavioral_predictions` + `component_scores` REQUIRED (D-05: behavioral term stays its own term in P3; per RESEARCH Open Q3 the Apollo call keeps emitting the 7 component_scores for the behavioral term — additive, no removal). KEEP `confidence` enum (already present, D-06). Do NOT refactor decode/adapt schemas (D-12 — out of scope, different files). Export the two new sub-schemas + their inferred types beside the existing exports.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/deepseek.test.ts -t "apollo schema validates"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/engine/__tests__/deepseek.test.ts -t "apollo schema validates"` GREEN
    - `grep -q "composite_score" src/lib/engine/types.ts` and `grep -q "ApolloRewriteSchema" src/lib/engine/types.ts`
    - behavioral_predictions + component_scores still present in DeepSeekResponseSchema: `grep -c "behavioral_predictions\|component_scores" src/lib/engine/types.ts` ≥ 2
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>The Apollo output contract is encoded additively; deepseek.ts can parse against it and aggregator (Plan 04) can read composite_score.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reframe deepseek.ts — APOLLO_SYSTEM_PROMPT prefix, verbatim feed, output backstop, calibration cleanup</name>
  <files>src/lib/engine/deepseek.ts, src/lib/engine/__tests__/deepseek.test.ts</files>
  <read_first>
    - src/lib/engine/deepseek.ts (FULL — the file being reframed in place; especially :40 STABLE_SYSTEM_PROMPT, :291 parseDeepSeekResponse, :312-335 loadCalibrationData, :383-434 buildDeepSeekUserMessage, :436-442 DeepSeekInput, :451 reasonWithDeepSeek, :471-491 call + retry)
    - src/lib/engine/apollo-core.ts (Plan 01 — import APOLLO_SYSTEM_PROMPT)
    - src/lib/engine/remix/decode.ts:95-125 (safeParse + retry + post-parse backstop pattern to mirror)
    - .planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-PATTERNS.md (deepseek.ts section, lines 58-108)
    - .planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-RESEARCH.md (Verbatim Feed Trace 268-274; Pitfall 4 307-311; Infra Preservation 283-285)
  </read_first>
  <behavior>
    - Test: the system message passed to the Qwen client === APOLLO_SYSTEM_PROMPT (byte-stable prefix, mock the client and capture messages[0].content) — "apollo uses shared core prefix"
    - Test: given a fed verbatim hook, every returned rewrite.original equals that hook after whitespace-normalize — "rewrite original matches verbatim hook" (R2)
    - Test: when the model paraphrases original, the backstop overwrites it from the fed verbatim hook (TS, not model) — "rewrite original backstopped on mismatch"
    - Test: the 2–3 rewrites name distinct lever_fixed values — "rewrites fix distinct §2 levers" (D-08)
    - Test: composite_score is clamped to 0–100 and dimensions.length===6 asserted post-parse
  </behavior>
  <action>
    Reframe src/lib/engine/deepseek.ts in place (KEEP the file name — deliberate per CONTEXT). (a) Replace the `STABLE_SYSTEM_PROMPT` reference in the create() call (deepseek.ts:483) with `APOLLO_SYSTEM_PROMPT` imported from "./apollo-core"; delete the old STABLE_SYSTEM_PROMPT literal (:40-109). (b) Extend `DeepSeekInput` (:436) with `verbatim?: VerbatimPayload | null`. (c) In `buildDeepSeekUserMessage` (:383) emit the verbatim block (hook spoken_words/on_screen_text + segments) in the VOLATILE user message ONLY — never the system prefix; instruct "copy the hook line VERBATIM into each rewrite's `original`" (Pitfall 4). (d) Add a post-parse backstop in/after `parseDeepSeekResponse` (:291) per decode.ts:95-125: assert `dimensions.length===6`, clamp `composite_score` to 0–100, assert `rewrites.length>=2`, and for each rewrite normalize-whitespace-compare `original` to the fed `verbatim.hook.spoken_words` (fallback `on_screen_text`) — on mismatch set `original` from the fed verbatim in TS (not the model). (e) REMOVE the calibration scaffolding: `loadCalibrationData()` (:314), the DeepSeekCalibrationData schema/FALLBACK blocks, the call to it (:462), and the viral-differentiators + duration-sweet-spot block in buildDeepSeekUserMessage (:387-431) — the core §2.0a/§4.1 supersedes it (R5/R9 drop "top X%"); stop reading calibration-baseline.json. (f) PRESERVE: circuit breaker (:111-282), MAX_RETRIES backoff (:544-546), calculateCost + soft-cap warn (:501-508), Sentry (:555), TIMEOUT_MS, and the retry-nudge-on-USER-message (:471-474) — never move the nudge to the system prefix. Pass `verbatim` from the caller (aggregator wires it in Plan 04; for now accept it on DeepSeekInput optionally).
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/deepseek.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/engine/__tests__/deepseek.test.ts` ALL GREEN (including "rewrite original matches verbatim hook", "rewrites fix distinct §2 levers", "apollo uses shared core prefix")
    - `grep -q "APOLLO_SYSTEM_PROMPT" src/lib/engine/deepseek.ts` and `grep -c "STABLE_SYSTEM_PROMPT" src/lib/engine/deepseek.ts` returns 0 (old prefix gone)
    - `grep -c "loadCalibrationData" src/lib/engine/deepseek.ts` returns 0 (calibration removed)
    - No "top X%" / percentile string built into the user message: `grep -vi '^\s*//' src/lib/engine/deepseek.ts | grep -ci "top.*%\|percentile"` returns 0
    - Circuit breaker preserved: `grep -q "isCircuitOpen\|circuit" src/lib/engine/deepseek.ts`
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>deepseek.ts is the Apollo Reasoner: shared-core prefix, verbatim-grounded rewrites (R2), backstopped output, calibration scaffolding gone, infra intact, single deterministic call (D-10).</done>
</task>

<task type="auto">
  <name>Task 3: Dormant creator-rules.ts prompt-injection constants (D-01)</name>
  <files>src/lib/engine/creator-rules.ts, src/lib/engine/_dormant/engine/creator-rules.ts</files>
  <read_first>
    - src/lib/engine/creator-rules.ts (FULL — the constants being dormanted)
    - .planning/corpus/KNOWLEDGE-CORE.md §2.0a (confirm the outlier number is present — Plan 01 Task 1 GATE)
    - src/lib/engine/_dormant/engine/ (the dormant convention dir — P1/P2 precedent)
    - .planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-PATTERNS.md (creator-rules dormant section, lines 233-245)
  </read_first>
  <action>
    GATE CHECK first: confirm `grep "5× follower count" .planning/corpus/KNOWLEDGE-CORE.md` exits 0 (D-02 satisfied by Plan 01) — if not, STOP and flag. Then `git mv src/lib/engine/creator-rules.ts src/lib/engine/_dormant/engine/creator-rules.ts` per the P1/P2 dormant convention. Find and remove all live importers of `CREATOR_RULES_BLOCK`/`CREATOR_RULES_NUMERIC`/`CREATOR_RULES_CONSENSUS`/etc. — grep `grep -rn "creator-rules\"" src --include=*.ts | grep -v _dormant | grep -v creator-rulebook`; the only live consumers should already be dormant (stage11 moved to _dormant in P1). DO NOT TOUCH `creator-rulebook.ts` (D-03 — separate deterministic UI checker; verify with `grep -rn "creator-rulebook" src` that it is untouched). No dual knowledge base (D-01): apollo-core.ts is the single SSOT prompt; creator-rules constants no longer feed any live prompt.
  </action>
  <verify>
    <automated>test ! -f src/lib/engine/creator-rules.ts &amp;&amp; test -f src/lib/engine/_dormant/engine/creator-rules.ts &amp;&amp; npm test 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/engine/creator-rules.ts` no longer exists; `src/lib/engine/_dormant/engine/creator-rules.ts` exists (git mv)
    - No live (non-dormant) import of creator-rules: `grep -rn "from.*creator-rules\"" src --include=*.ts | grep -v _dormant | grep -v rulebook` returns nothing
    - creator-rulebook.ts UNTOUCHED (D-03): `git status src/lib/engine/creator-rulebook.ts` shows no change
    - `npm test` full suite green (no live consumer broke)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>creator-rules.ts prompt constants are dormant with zero silent number loss; the core is the single brain SSOT (D-01); creator-rulebook.ts untouched (D-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| video transcript/verbatim → Apollo user message | creator-supplied content (the hook line, segment text) crosses into the LLM user message |
| Apollo LLM output → Zod parse → persisted JSONB | untrusted model JSON crosses into typed app state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | prompt injection via verbatim hook/segment text into Apollo | mitigate | Verbatim stays in the user (data) role, never the system prefix; APOLLO_INSTRUCTION instructs "do not invent criteria outside the core"; output is structured JSON only (not executed) — low risk |
| T-03-04 | Tampering | malformed/oversized Apollo JSON breaks parse | mitigate | Zod safeParse + retry-nudge-on-user-message + post-parse backstop; TIMEOUT_MS + MAX_RETRIES + circuit breaker preserved |
| T-03-05 | Spoofing | model paraphrases the hook → rewrite.original ≠ verbatim (R2 false pass) | mitigate | TS backstop overwrites original from the fed verbatim hook on whitespace-normalized mismatch (Pitfall 4) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new packages (RESEARCH audit: not applicable); no install task |
</threat_model>

<verification>
- R2: deepseek.test.ts "rewrite original matches verbatim hook" GREEN; rewrites name distinct levers (D-08).
- R5-partial: composite_score present + clamped; available for the Plan 04 blend.
- D-01/D-02: creator-rules dormant only after the core carries the outlier number; no dual knowledge base.
- D-10: single deterministic call (temp0+seed) preserved — no 4th call added.
- D-03/D-12: creator-rulebook.ts + decode/adapt schemas untouched (different files; full suite green).
</verification>

<success_criteria>
- deepseek.ts uses APOLLO_SYSTEM_PROMPT, emits 6 dims + composite + 2–3 quote-grounded rewrites, backstopped.
- Calibration/percentile scaffolding removed; infra (breaker/retries/cost/Sentry) intact.
- creator-rules.ts dormant; full `npm test` green; `npm run build` green.
</success_criteria>

<output>
Create `.planning/phases/03-apollo-reasoner-brain-1-the-moat-shared-knowledge-core/03-02-SUMMARY.md` when done.
</output>
