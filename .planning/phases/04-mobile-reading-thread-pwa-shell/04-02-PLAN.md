---
phase: 04-mobile-reading-thread-pwa-shell
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/reading/throne.tsx
  - src/components/reading/blocks/verdict-block.tsx
  - src/components/reading/reading-thread.tsx
  - src/components/reading/__tests__/use-analysis-stream-mock.tsx
  - src/components/reading/__tests__/throne.test.tsx
  - src/components/reading/__tests__/reading-thread.test.tsx
  - src/components/reading/blocks/__tests__/verdict-block.test.tsx
autonomous: true
requirements: [READ-01, READ-02, READ-03, READ-04, READ-05]
must_haves:
  truths:
    - "The Reading is the thread's first turn — no blank prompt before the structured turn"
    - "The throne is reserved from t0 with a content-free 'forming the read…' placeholder (no band, no number)"
    - "Placeholder slots reveal as their driving stage reaches panelReady==='ready'; real block content fills only at phase==='complete'"
    - "At complete the throne crystallizes ONCE via one StageBlock motion with the correct band swatch"
    - "Expert-insight renders at slot 2 (under the throne) regardless of its late wave_2 completion"
  artifacts:
    - path: "src/components/reading/reading-thread.tsx"
      provides: "the crux container — drives useAnalysisStream, owns curated order, placeholder↔content"
      contains: "useAnalysisStream"
    - path: "src/components/reading/throne.tsx"
      provides: "reserved forming placeholder ↔ crystallized verdict (one motion)"
    - path: "src/components/reading/blocks/verdict-block.tsx"
      provides: "verdict body render with the demoted /100 supporting chip"
  key_links:
    - from: "src/components/reading/reading-thread.tsx"
      to: "src/hooks/queries/use-analysis-stream.ts"
      via: "useAnalysisStream() consumption (phase, panelReady, result)"
      pattern: "useAnalysisStream"
    - from: "src/components/reading/reading-thread.tsx"
      to: "src/lib/reading/view-model.ts"
      via: "toReadingBlocks(canonicalFromLive(result)) at complete only"
      pattern: "toReadingBlocks\\(canonicalFromLive"
    - from: "src/components/reading/reading-thread.tsx"
      to: "src/lib/reading/stage-slots.ts"
      via: "CURATED_ORDER drives the slot list"
      pattern: "CURATED_ORDER"
---

<objective>
Build the crux: the throne (reserved "forming" placeholder that crystallizes the verdict ONCE at
`complete`), the verdict block body (the demoted `/100` supporting chip), and the
`reading-thread.tsx` container that drives the existing `useAnalysisStream`, reveals content-free
placeholder slots keyed to `panelReady`, and fills real block content only at `phase==='complete'`.

Purpose: this is THE load-bearing decision of the phase (RESEARCH crux — the view-model is
all-at-complete). It satisfies READ-01 (pronounce first), READ-02 (stage-reveal placeholders, NOT
token streaming), READ-03 (throne forms then crystallizes last), READ-04 (band + why + confidence
language), and READ-05 (curated order, Apollo pinned at slot 2). It reshapes the EXISTING SSE
stream — it does NOT build new transport, new motion, or a new view-model.
Output: `throne.tsx`, `verdict-block.tsx`, `reading-thread.tsx`, a shared stream mock, RED-first tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mobile-reading-thread-pwa-shell/04-RESEARCH.md
@.planning/phases/04-mobile-reading-thread-pwa-shell/04-PATTERNS.md
@.planning/phases/04-mobile-reading-thread-pwa-shell/04-UI-SPEC.md
@.planning/phases/04-mobile-reading-thread-pwa-shell/04-01-SUMMARY.md

<interfaces>
<!-- Contracts extracted from the codebase + Plan 01 output. -->

From src/hooks/queries/use-analysis-stream.ts (the EXISTING hook — reshape its output, do NOT modify):
- `useAnalysisStream({ initialData? }): { start, result: PredictionResult|null, stages, partial,
  panelReady: Record<PanelId, "idle"|"loading"|"ready"|"error">, phase, error, reconnect,
  analysisId, filmstrips, abort, reset }`
- `type AnalysisStreamPhase = "idle"|"analyzing"|"reconnecting"|"polling"|"complete"|"error"`
- `initialData` with `overall_score != null` → hook starts in `'complete'` and never opens the
  stream (the resting-document short-circuit — used by Plan 03's [id] route).

From src/lib/reading/view-model.ts:
- `toReadingBlocks(c: CanonicalReading): ReadingBlock[]` (PURE, all-at-complete)
- `canonicalFromLive(result: PredictionResult): CanonicalReading` (live adapter — reads top-level
  fields only present in the `complete` payload). NEVER call on a null/partial result (Pitfall 3).
- The `verdict` block is ALWAYS emitted first by toReadingBlocks; `analysis-degraded` (if any) is
  emitted second (above expert-insight).

From src/lib/reading/verdict-bands.ts:
- `type VerdictBand = { id: 'high'|'solid'|'needs-work'; label: string; min: number }`, `VERDICT_BANDS`,
  `bandFor(score)`, `inDeadBand(score, buffer?)`, `DEAD_BAND_FLOOR=5`.
- BAND-ID SEAM the plan MUST bridge: band ids are `high|solid|needs-work` but `VerdictSwatch` takes
  `good|mixed|bad`. "Mixed signals" (amber, `mixed`) is a FIRST-CLASS fourth throne state.

From src/components/numen/stage-reveal.tsx:
- `export function StageBlock({ show, children })` — the ONE motion (DS-07). Reduced-motion handled
  inside (`useReducedMotion() !== false` fails safe). Do NOT roll new motion.

From src/components/numen/verdict-swatch.tsx:
- `export function VerdictSwatch({ verdict: 'good'|'mixed'|'bad', size, children, className })`.
  Literal class strings; never `bg-${verdict}`.

From src/components/numen/surface.tsx: `Surface` (panel-2 for the throne, `min-h` to reserve slot).
From src/lib/reading/stage-slots.ts (Plan 01): `CURATED_ORDER` (8 slots, throne excluded, expert-insight first).
From src/components/reading/block-view.tsx (Plan 01): `BlockView` (dispatches non-verdict kinds).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: verdict-block.tsx (demoted /100 chip) + throne.tsx (forming↔crystallize) + RED tests</name>
  <files>src/components/reading/blocks/verdict-block.tsx, src/components/reading/throne.tsx, src/components/reading/__tests__/throne.test.tsx, src/components/reading/blocks/__tests__/verdict-block.test.tsx</files>
  <read_first>
    - src/components/numen/stage-reveal.tsx (the ONE crystallization motion — reuse `StageBlock`, never roll new motion; PATTERNS §throne.tsx)
    - src/components/numen/verdict-swatch.tsx (the band swatch — `good|mixed|bad`; the band-id→swatch seam)
    - src/lib/reading/verdict-bands.ts (band ids `high|solid|needs-work` + `inDeadBand`; the "Mixed signals" fourth state)
    - src/lib/reading/block-types.ts (the `verdict` block shape: `{ band, why, confidenceLanguage, score }`)
    - .planning/phases/04-mobile-reading-thread-pwa-shell/04-UI-SPEC.md §"Throne reveal" + §"Demoted /100" + §"Typography" (serif `why` 20px; band label sans 30px) + §"Copywriting Contract" (forming/band/confidence copy)
    - .planning/phases/04-mobile-reading-thread-pwa-shell/04-RESEARCH.md §"Pattern 2: Throne reserved-slot crystallization" + Pitfall 9 (forming APCA) + Pitfall 8 (reduced-motion)
    - .planning/phases/04-mobile-reading-thread-pwa-shell/04-PATTERNS.md §throne.tsx (band→swatch map + forming placeholder + skeleton-breathe)
  </read_first>
  <behavior>
    - verdict-block: given a `verdict` block `{ band, why, confidenceLanguage, score }`, renders the
      `/100` as a quiet 14px supporting chip in the BODY (`{score} / 100`, `text-numen-text-muted`,
      hairline border) — never accent, never larger than the band label, never in a headline.
    - throne (phase !== 'complete'): renders a reserved `panel-2 Surface` with `min-h` ≥ crystallized
      height showing "Forming the read…" (`text-numen-text-muted`) — NO VerdictSwatch, NO band word,
      NO number, NO spinner. One `animate-skeleton-breathe` pulse, SUPPRESSED under reduced motion.
      The pulse APCA-safe (pulse a non-text container OR verified at 0.4 opacity — Pitfall 9).
    - throne (phase === 'complete' with a verdict block): crystallizes via ONE `StageBlock show` —
      the `VerdictSwatch` (band→swatch mapped), the band label (sans 30px), the one-sentence `why`
      (serif 20px), and the `confidenceLanguage` 14px caption. Crystallizes exactly once.
    - band→swatch mapping: `high`→`good`, `needs-work`→`bad`; "Mixed signals" (dead-band/anti-virality,
      surfaced by the view-model) → `mixed` (amber, first-class). The mapping is a pure helper.
    - reduced-motion: throne uses StageBlock (translate zeroed) and suppresses the breathe pulse.
  </behavior>
  <action>
    Create `src/components/reading/blocks/verdict-block.tsx` rendering the verdict body (the demoted
    `/100` chip per the UI-SPEC "Demoted /100" decision). Create `src/components/reading/throne.tsx`
    per RESEARCH Pattern 2: a reserved `Surface` (panel-2, `min-h` to avoid layout shift) that shows
    the "Forming the read…" placeholder until `phase === 'complete'`, then crystallizes the verdict
    via ONE `StageBlock show`. Write a pure `bandToSwatch(band: VerdictBand['id'] | 'mixed'): 'good'|'mixed'|'bad'`
    helper bridging the band-id seam (`high→good`, `needs-work→bad`, mixed→mixed) and the band LABEL
    from `VERDICT_BANDS[].label` (+ "Mixed signals" literal). The band label is sans 30px (it is a
    label, not prose); the `why` is the throne's ONE serif moment (20px). The forming placeholder uses
    `animate-skeleton-breathe` (globals.css, 1.2s) suppressed under reduced motion; ensure the forming
    line passes APCA at its lowest pulse opacity (or pulse a non-text dot/bar — Pitfall 9; run
    `npx tsx scripts/check-apca.ts` if a new text/bg pairing is introduced). Use `text-numen-*` /
    `bg-numen-*` tokens only; no legacy coral; no `--font-bold`. First write `throne.test.tsx` +
    `verdict-block.test.tsx` (RED): assert (a) pre-complete throne has NO band word and NO `/100`
    digits and a "Forming the read…" line; (b) at complete with a `mixed` verdict the throne shows
    "Mixed signals" + a `mixed` swatch; (c) the verdict body chip reads `71 / 100` for the fixture's
    score; (d) the throne crystallizes only once (no double swatch). Implement to GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/throne.test.tsx src/components/reading/blocks/__tests__/verdict-block.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/reading/__tests__/throne.test.tsx src/components/reading/blocks/__tests__/verdict-block.test.tsx` exits 0
    - throne.tsx imports `StageBlock` from `@/components/numen/stage-reveal` and `VerdictSwatch` from `@/components/numen/verdict-swatch`
    - The forming-state test asserts NO band label word and no `/ 100` appears before `complete`
    - The mixed-state test asserts `VerdictSwatch` receives `verdict="mixed"` and the label "Mixed signals"
    - `grep -c "bg-accent\|border-border\b\|font-bold" src/components/reading/throne.tsx src/components/reading/blocks/verdict-block.tsx` == 0
  </acceptance_criteria>
  <done>Throne forms then crystallizes once with the correct band swatch (incl. first-class "Mixed signals"); verdict body shows the demoted /100 chip; reduced-motion + APCA respected; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reading-thread.tsx crux container + stream mock + RED tests</name>
  <files>src/components/reading/reading-thread.tsx, src/components/reading/__tests__/use-analysis-stream-mock.tsx, src/components/reading/__tests__/reading-thread.test.tsx</files>
  <read_first>
    - src/app/(app)/analyze/[id]/result-card.tsx (THE analog — drives `useAnalysisStream`, iterates slots by `panelReady`; PATTERNS §reading-thread.tsx — copy hook-consumption, replace the panel grid with the curated single-column order)
    - src/hooks/queries/use-analysis-stream.ts (the return shape + the `initialData` short-circuit at lines ~80-116)
    - src/lib/reading/view-model.ts (lines 129-190 — `toReadingBlocks`/`canonicalFromLive`; all-at-complete)
    - src/lib/reading/stage-slots.ts (Plan 01 — `CURATED_ORDER`)
    - src/components/reading/block-view.tsx + throne.tsx (Plan 01 + Task 1 — what the container composes)
    - src/components/numen/stage-reveal.tsx (`StageBlock` wraps each slot reveal)
    - src/app/globals.css (line ~356 — the `.numen-surface` scope class the root mounts under)
    - .planning/phases/04-mobile-reading-thread-pwa-shell/04-RESEARCH.md §"Crux Question" + §"Pattern 1" + §"Pattern 3 (aria-live once)" + Pitfall 3
    - .planning/phases/04-mobile-reading-thread-pwa-shell/04-UI-SPEC.md §"Thread shell" + §"Block order & pacing" + §"Accessibility"
  </read_first>
  <behavior>
    - On mount/start the structured Reading turn renders immediately — the throne ("forming") + the
      curated placeholder slots ARE the first turn; there is never a blank prompt (READ-01).
    - Pre-complete: for each slot in `CURATED_ORDER`, a content-free placeholder reveals via
      `StageBlock show={panelReady[slot.panelKey] === 'ready'}`. `canonicalFromLive` is NEVER called
      while `result` is null/partial.
    - At `phase === 'complete'`: `blocks = toReadingBlocks(canonicalFromLive(result))`; the throne
      crystallizes; each non-verdict block fills its slot via `BlockView` wrapped in one `StageBlock`;
      omitted blocks render nothing (honest omit-discipline, D-14).
    - Expert-insight renders at slot index 0 of CURATED_ORDER (directly under the throne) regardless
      of `wave_2` completing late (READ-05). `analysis-degraded`, when present, renders under the
      throne ABOVE expert-insight.
    - aria-live: ONE `aria-live="polite"` region, empty until complete, receives the verdict text
      ONCE at crystallization (never per-block — Pattern 3 / no announce storm).
    - error phase: a calm in-thread error Surface ("That didn't go through…") — never a red toast.
  </behavior>
  <action>
    Create `src/components/reading/reading-thread.tsx` ("use client") modeled on `result-card.tsx`'s
    hook consumption but rendering the curated single-column thread instead of the panel grid. Wrap
    the whole surface in `<div className="numen-surface">` (load-bearing scope; warm tokens resolve
    only inside it — legacy coral `bg-accent`/`border-border` MUST NOT appear). Accept props for the
    live path (calls `start`) and the resting path (`initialData` passthrough so the hook
    short-circuits to `complete`). Derive `const blocks = phase === 'complete' && result ?
    toReadingBlocks(canonicalFromLive(result)) : []` — guard so `canonicalFromLive` is never called on
    a partial result (Pitfall 3). Render `<Throne phase={phase} block={blocks.find(b => b.kind ===
    'verdict')} />` first; then (if present) the `analysis-degraded` block; then map `CURATED_ORDER`:
    pre-complete → `<StageBlock show={panelReady[panelKey]==='ready'}><BlockPlaceholder kind={kind} /></StageBlock>`,
    post-complete → `const block = blocks.find(b => b.kind === kind); return block ? <StageBlock show><BlockView block={block} /></StageBlock> : null`.
    Add the single polite `aria-live` region (empty until complete, verdict text once). Reuse the
    analog's in-thread error pattern but restyle to `.numen-surface` tokens + the UI-SPEC error copy
    (NOT the analog's `red-500/*`). The pinned-header "Analyze new" `IconButton` + the inert composer
    are Plan 03's responsibility — leave a clearly-marked slot/prop for them; do NOT build the composer
    here. Create a `BlockPlaceholder` (content-free skeleton + plain-language label, no fake data).
    First write `use-analysis-stream-mock.tsx` (a test util that lets a test drive `phase`/`panelReady`/
    `result` deterministically — `vi.mock('@/hooks/queries/use-analysis-stream')`) and
    `reading-thread.test.tsx` (RED): assert (a) at `phase='analyzing'` the throne forming line renders
    and NO block content / NO `canonicalFromLive` crash; (b) toggling `panelReady.hook_decomp='ready'`
    reveals the hook placeholder; (c) at `phase='complete'` with the real fixture as `result`, the
    expert-insight heading renders at DOM-order position immediately after the throne (slot 2), and the
    verdict band label renders; (d) exactly one `aria-live` region carries the verdict text. Implement to GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/reading-thread.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/reading/__tests__/reading-thread.test.tsx` exits 0
    - `grep -c "toReadingBlocks(canonicalFromLive" src/components/reading/reading-thread.tsx` >= 1 AND it is guarded by `phase === "complete"` (the same line/block references phase complete)
    - `grep -c "CURATED_ORDER" src/components/reading/reading-thread.tsx` >= 1
    - `grep -c "numen-surface" src/components/reading/reading-thread.tsx` >= 1
    - `grep -c "aria-live" src/components/reading/reading-thread.tsx` == 1 (one region — no storm)
    - The order test asserts expert-insight is the first slot after the throne (READ-05)
    - `grep -c "red-500\|bg-accent\|border-border\b" src/components/reading/reading-thread.tsx` == 0
  </acceptance_criteria>
  <done>The thread pronounces first, reveals content-free placeholders on stage readiness, fills real blocks + crystallizes the throne only at complete, Apollo at slot 2, one aria-live region; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream → render | live engine `StageEvent`/`complete` payload drives the client thread |
| engine `result` → blocks | engine-authored text rendered in the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure (confident lie) | mid-stream throne / blocks fabricating data | mitigate | The crux discipline: NO real content before `complete`; throne stays "forming" (no band/number); `canonicalFromLive` never called on a partial result (test-asserted). This is the Phase-3 anti-confident-lie doctrine enforced in render. |
| T-04-04 | Tampering (XSS) | reading-thread rendering engine `result` text | mitigate | default JSX escaping; no `dangerouslySetInnerHTML` (inherited from Plan 01 block discipline) |
| T-04-05 | Denial of Service (a11y) | aria-live storm on every revealing block | mitigate | exactly ONE polite aria-live region, announce verdict once (grep-asserted == 1) |

> No package-manager installs in this plan → no T-04-SC entry (Serwist gate is Plan 04). PWA caching is Plan 04 (T-04-CACHE there).
</threat_model>

<verification>
- `npx vitest run src/components/reading src/lib/reading` green (incl. existing 37/37 reading suite)
- `npx tsc --noEmit` clean
- Crux discipline test-proven: no `canonicalFromLive` on partial result; throne forming pre-complete; one aria-live region
</verification>

<success_criteria>
- READ-01: the Reading is the first turn, no blank prompt
- READ-02: placeholder slots reveal on stage readiness; content fills only at complete (NOT token streaming)
- READ-03: throne reserved → forms → crystallizes ONCE at complete
- READ-04: verdict = band + why + confidence language; /100 demoted to a body chip
- READ-05: curated order with expert-insight pinned at slot 2
</success_criteria>

<output>
Create `.planning/phases/04-mobile-reading-thread-pwa-shell/04-02-SUMMARY.md` when done
</output>
