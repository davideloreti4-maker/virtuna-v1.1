---
phase: 05-develop-predict-lineage
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/board/adapt/AdaptConceptCard.tsx
  - src/components/board/adapt/AdaptFrameBody.tsx
autonomous: true
requirements: [DEVELOP-01]
must_haves:
  truths:
    - "Each of the 3 Adapt concept cards shows a 'Develop & predict →' trigger"
    - "Clicking Develop on ONE concept opens exactly one /api/analyze stream (zero before click)"
    - "On the child's started event the user is navigated to /analyze/[childId]"
    - "The submitted child carries parent_id = the source remix analysis id"
    - "The other concepts are NOT scored unless separately clicked"
  artifacts:
    - path: "src/components/board/adapt/AdaptConceptCard.tsx"
      provides: "onDevelop trigger on each concept card"
      contains: "onDevelop"
    - path: "src/components/board/adapt/AdaptFrameBody.tsx"
      provides: "Develop stream + brief assembly + navigate-on-started"
      contains: "developStream"
  key_links:
    - from: "src/components/board/adapt/AdaptConceptCard.tsx"
      to: "AdaptFrameBody handleDevelop"
      via: "onDevelop callback prop"
      pattern: "onDevelop"
    - from: "src/components/board/adapt/AdaptFrameBody.tsx"
      to: "/api/analyze"
      via: "developStream.start with input_mode=text, mode=score, parent_id=analysisId"
      pattern: "parent_id: analysisId"
    - from: "AdaptFrameBody developStream.analysisId"
      to: "/analyze/[childId]"
      via: "router.push on started (D-01 Board.tsx pattern)"
      pattern: "router.push"
---

<objective>
Add the per-concept "Develop & predict →" trigger (D-03) to `AdaptConceptCard`, and wire it in `AdaptFrameBody` to: assemble a `content_text` brief from the concept fields (D-04/D-05), submit ONE `/api/analyze` stream with `input_mode='text'`, `mode='score'`, and `parent_id` = the source remix analysis id (D-06/D-07), then navigate to `/analyze/[childId]` on the `started` event (D-01) — reusing the exact Board.tsx pattern. Enforce C3 discipline (D-02): zero streams until click, exactly one stream per click, other concepts untouched.

Purpose: Develop is the payoff that closes the loop — it bridges a remix analysis back into the core scoring engine, producing one real scored child board per click. No new engine path; the existing pipeline is reused unchanged.

Output: onDevelop trigger on each card + Develop stream wiring in the frame body.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-develop-predict-lineage/05-CONTEXT.md
@.planning/phases/05-develop-predict-lineage/05-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- AdaptConcept (src/lib/engine/remix/decode-types.ts:184) — fields for brief assembly -->
export interface AdaptConcept {
  hook: string;            // D-05 child label / recognizable identifier
  angle: string;
  who_its_for: string;
  format_borrowed: string; // chip text
}

<!-- AdaptFrameBody current state (src/components/board/adapt/AdaptFrameBody.tsx):
     - line 59: const { data: permalinkData, id: analysisId } = usePermalinkAnalysis();
       → `analysisId` IS the source remix analysis id (stable, pre-known) → this is parent_id (D-07)
     - line 60: stream = useAnalysisStream(...)   ← the REMIX/adapt stream; do NOT reuse for Develop
     - lines 232-244: success render maps `adapt` → <AdaptConceptCard key={i} concept={concept} />
     - adapt generation uses a MUTATION hook (useAdaptConcepts), NOT stream.start — Develop is a NEW separate stream -->

<!-- useAnalysisStream return (src/hooks/queries/use-analysis-stream.ts) -->
start: (input: AnalysisStreamInput) => Promise<void>;
phase: "idle" | "analyzing" | "reconnecting" | "polling" | "complete" | "error";
analysisId: string | null;   // populated from `started` SSE frame
// AnalysisStreamInput now includes optional parent_id (added in 05-01)

<!-- Board.tsx navigate-on-started pattern (src/components/board/Board.tsx ~298-305) — copy verbatim:
const prevAnalysisIdRef = useRef<string | null>(stream.analysisId);
useEffect(() => {
  const id = stream.analysisId;
  if (id && prevAnalysisIdRef.current === null) router.push(`/analyze/${id}`);
  prevAnalysisIdRef.current = id;
}, [stream.analysisId, router]); -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add onDevelop trigger to AdaptConceptCard (Raycast secondary action)</name>
  <files>src/components/board/adapt/AdaptConceptCard.tsx</files>
  <read_first>
    - src/components/board/adapt/AdaptConceptCard.tsx (full file, 57 lines — card anatomy + Raycast rules in the doc comment)
    - CLAUDE.md §"Raycast Design Language Rules" (secondary action: bg-transparent, no border change on hover, text-white → foreground on hover; 8px button radius; full-width tap target ≥44px for mobile)
    - .planning/phases/05-develop-predict-lineage/05-PATTERNS.md §AdaptConceptCard (exact trigger markup + extended props)
  </read_first>
  <action>
    Extend `AdaptConceptCardProps` with two optional props: `onDevelop?: () => void;` (fires Develop for this concept) and `isPending?: boolean;` (disables the trigger while a stream is starting). Render the trigger only when `onDevelop` is provided: after the "Who it's for" row and before the closing `</article>`, add a divider (`<div className="border-t border-white/[0.04]" />`) then a full-width `<button type="button">` that: calls `onDevelop` on click, is `disabled={isPending}`, reads `text-xs font-medium text-white/55` → `hover:text-foreground transition-colors`, lays out `flex items-center justify-between` with label "Develop & predict" and a plain-text `→` arrow (`text-white/30`, no icon dependency), and applies `opacity-40 pointer-events-none` when `isPending`. Full-width (`w-full`) so the tap target satisfies the mobile card-stack ≥44px requirement (D-14 forward-compat). NO coral accent (Raycast: coral reserved for the format_borrowed chip only), NO border change on hover.
  </action>
  <verify>
    <automated>grep -c "onDevelop\|isPending" src/components/board/adapt/AdaptConceptCard.tsx | { read n; [ "$n" -ge 2 ] && echo OK || echo FAIL; }; npx tsc --noEmit 2>&1 | grep "AdaptConceptCard" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `AdaptConceptCardProps` declares `onDevelop?: () => void` and `isPending?: boolean`
    - Trigger markup is gated on `onDevelop &&` (cards rendered without the callback show no Develop button — preserves Phase 4 callers)
    - Button text contains "Develop & predict"; arrow is plain text `→` (no @phosphor-icons import added)
    - Button is `w-full` and uses `disabled={isPending}`
    - No `text-accent`/`bg-accent` applied to the trigger; no `border` color change on `hover`
    - `npx tsc --noEmit` clean for AdaptConceptCard.tsx
  </acceptance_criteria>
  <done>Each card optionally renders a Raycast-conformant Develop trigger gated on onDevelop.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Develop stream in AdaptFrameBody (brief assembly + one-stream-per-click + navigate)</name>
  <files>src/components/board/adapt/AdaptFrameBody.tsx</files>
  <read_first>
    - src/components/board/adapt/AdaptFrameBody.tsx (full file — line 59 analysisId source, line 60 existing remix stream, lines 232-244 success render mapping concepts)
    - src/components/board/Board.tsx lines 295-350 (navigate-on-started useEffect + stream.start input shape — the verbatim D-01 pattern)
    - .planning/phases/05-develop-predict-lineage/05-PATTERNS.md §AdaptFrameBody (developStream, handleDevelop brief assembly, already-fired guard, card pass-down)
  </read_first>
  <action>
    Add a SECOND `useAnalysisStream` instance dedicated to Develop, separate from the existing remix stream on line 60:
    - `import { useRouter } from 'next/navigation';` and `import { useCallback } from 'react';` (extend existing react import).
    - `const router = useRouter();`
    - `const developStream = useAnalysisStream({ initialData: null });`
    - `const developPrevIdRef = useRef<string | null>(null);`
    - Navigate-on-started effect (verbatim D-01 / Board.tsx pattern): in a useEffect keyed on `[developStream.analysisId, router]`, read `const id = developStream.analysisId; if (id && developPrevIdRef.current === null) router.push('/analyze/' + id); developPrevIdRef.current = id;`. This guarantees navigation fires exactly once on the child's started frame.
    - `handleDevelop` via `useCallback((concept: AdaptConcept) => {...}, [analysisId, developStream])`: guard `if (!analysisId) return;`. Assemble the brief (D-04 — full concept context, not just the hook): join `concept.hook`, `concept.angle`, and `'Format: ' + concept.format_borrowed` with `\n\n`. Call `developStream.start({ input_mode: 'text', content_text: brief, content_type: 'video', mode: 'score', parent_id: analysisId })` — `mode: 'score'` so the child is a STANDARD scored analysis (D-06), `parent_id: analysisId` is the source remix id known BEFORE the child's started frame (D-07, NOT derived from the child SSE). The hook fires exactly one POST per call (C3 / D-02); do NOT loop or prefetch.
    - In the success render (lines 232-244) pass to each card: `onDevelop={() => handleDevelop(concept)}` and `isPending={developStream.phase === 'analyzing' || developStream.phase === 'reconnecting' || developStream.phase === 'polling'}`. The label for the child in Recent (D-05) is `concept.hook` carried implicitly as the brief's first line / content_text prefix — no separate label field needed.

    Do NOT alter the existing remix `stream` or adapt-generation mutation logic. The Develop stream is additive and only starts on an explicit card click — preserving zero-streams-until-click (D-02).
  </action>
  <verify>
    <automated>grep -c "developStream\|parent_id: analysisId\|router.push" src/components/board/adapt/AdaptFrameBody.tsx | { read n; [ "$n" -ge 3 ] && echo OK || echo "FAIL $n"; }; npx tsc --noEmit 2>&1 | grep "AdaptFrameBody" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - A second `useAnalysisStream` (`developStream`) exists, distinct from the line-60 remix `stream`
    - `handleDevelop` assembles `content_text` from hook + angle + format_borrowed (3 fields joined) — not the hook alone (D-04)
    - `developStream.start(...)` is called with `input_mode: 'text'`, `mode: 'score'`, `parent_id: analysisId` (D-06/D-07)
    - Navigate effect: `router.push('/analyze/' + id)` fires once when `developPrevIdRef.current === null` (D-01)
    - Each `AdaptConceptCard` in the success render receives `onDevelop` and `isPending`
    - No loop/prefetch over concepts that would start more than one stream — start is only reachable via a single card's onClick (C3/D-02)
    - `npx tsc --noEmit` clean for AdaptFrameBody.tsx
  </acceptance_criteria>
  <done>Clicking Develop on one card assembles a brief, opens exactly one scored child stream with parent_id, and navigates to the child board on started.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adapt card click → developStream.start → POST /api/analyze | Client-initiated scored submission carrying parent_id (the user's own source remix id) |
| router.push('/analyze/[childId]') | Navigation to a child board owned by the same user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Denial of service (cost/latency) | per-concept Develop trigger | mitigate | C3 discipline (D-02): the only path to `developStream.start` is a single card's onClick; no loop, no prefetch, no bulk. `isPending` disables all triggers while a stream is in flight, preventing rapid duplicate submissions. Existing DAILY_LIMITS/429 branch in route.ts still gates per-user cost. |
| T-05-04 | Tampering | parent_id sent from client = analysisId | mitigate | parent_id is the user's OWN source remix id (from usePermalinkAnalysis on the current board the user already owns). Even so, the value is treated as untrusted input: validated by AnalysisInputSchema (05-01) and ownership re-checked server-side on the chip read (05-03). |
| T-05-05 | Information disclosure | content_text brief assembled from concept | accept | The brief contains only the user's own niche-adapted concept fields (hook/angle/format) — no source caption, no third-party content (Phase 4 already excludes source content from concepts per ADAPT-02). Low risk. |
| T-05-SC | Tampering | npm installs | mitigate | No new dependencies; arrow is plain text (no icon package added). slopcheck N/A. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Each rendered concept card shows a "Develop & predict →" trigger.
- Behavior: before any click, `developStream.phase === 'idle'` and zero POST /api/analyze fired by Develop (C3). After one click, exactly one POST; the other cards remain idle.
- The POST body carries `input_mode:'text'`, `mode:'score'`, `parent_id` = current board id.
- Navigation to `/analyze/[childId]` occurs on the child's `started` frame.
</verification>

<success_criteria>
- DEVELOP-01: clicking Develop on one concept creates exactly one child analysis via the existing /api/analyze SSE pipeline and navigates to its board; other concepts unscored unless separately developed.
- The child carries parent_id = source remix id (known before started; passed in request body).
- No bulk/auto scoring; zero streams until click, then exactly one (C3/D-02).
</success_criteria>

<output>
Create `.planning/phases/05-develop-predict-lineage/05-02-SUMMARY.md` when done.
</output>
