---
phase: 05-develop-predict-lineage
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/api/analysis/[id]/route.ts
  - src/components/board/RemixedFromChip.tsx
  - src/components/board/InputResultCard.tsx
  - src/components/sidebar/Sidebar.tsx
  - src/hooks/queries/use-analysis-stream.ts
autonomous: true
requirements: [DEVELOP-02]
must_haves:
  truths:
    - "A developed child board shows a 'Remixed from \"[caption]\"' chip linking to /analyze/[parentId] (D-09)"
    - "/api/analysis/[id]?summary returns only id + caption + created_at, ownership-scoped (D-10)"
    - "Remix/decode source rows (null score) show a render-side 'Remix' tag in Recent instead of a number; rows already surface from the backend (D-11/D-12)"
    - "A remix/decode permalink reload still renders its content (frame-level dual-read; not gated on overall_score)"
    - "Developed-child polling no longer times out at 90s (360s ceiling); the live-poll path also completes on the variants.remix marker (D-13)"
  artifacts:
    - path: "src/app/api/analysis/[id]/route.ts"
      provides: "?summary minimal parent-summary branch"
      contains: "summary"
    - path: "src/components/board/RemixedFromChip.tsx"
      provides: "the remixed-from chip component"
      contains: "Remixed from"
    - path: "src/hooks/queries/use-analysis-stream.ts"
      provides: "lifted polling ceiling + variants.remix live-poll fallback marker"
      contains: "POLLING_CEILING_MS"
  key_links:
    - from: "src/components/board/InputResultCard.tsx"
      to: "/api/analysis/[parentId]?summary"
      via: "RemixedFromChip fetches parent summary when permalinkData.parent_id != null"
      pattern: "parent_id"
    - from: "src/components/board/RemixedFromChip.tsx"
      to: "/analyze/[parentId]"
      via: "anchor href"
      pattern: "/analyze/"
    - from: "src/hooks/queries/use-analysis-stream.ts"
      to: "polling ceiling"
      via: "POLLING_CEILING_MS = 360_000 replaces the 90s setTimeout"
      pattern: "POLLING_CEILING_MS"
---

<objective>
Make lineage visible and harden remix-row behavior: add a `?summary` branch to `/api/analysis/[id]` returning a minimal ownership-scoped parent summary (D-10); render a "Remixed from '[caption]'" chip on the child score board linking to `/analyze/[parentId]` (D-09); add a "Remix" tag to null-score source rows in the sidebar Recent list (D-11/D-12); and lift the polling ceiling in `use-analysis-stream` so developed children don't false-timeout (D-13), plus apply a harmless live-poll completion-marker fallback for remix rows.

Hydration note (correction): permalink-reload hydration for remix/decode rows is ALREADY handled at the frame level by Phase 3's dual-read — `DecodeShellNode.tsx` (~181-189) renders from `permalinkData.variants.remix` regardless of stream phase, with the skeleton gated on `isDecoding`. On a fresh permalink reload the stream hook starts in phase `idle` (its `completedFromInitial` gate at line 127 keys on `overall_score != null`, which is null for remix rows), so `pollEnabled` is false and the line-412 poll gate never runs. Therefore editing the line-412 gate does NOT change permalink-reload hydration. The line-412 OR-marker (`variants.remix != null`) is applied only as a harmless fallback for the LIVE-polling path; it must not be claimed as a permalink-reload fix. If a true stream-level "complete" phase were ever desired for remix permalinks, the correct site is the `completedFromInitial` gate at line 127 — out of scope here.

Purpose: Lineage must be honest and navigable in both directions — parent_id down (05-01/05-02), chip up (this plan). The Recent "Remix" tag makes un-scored source rows legible (D-11/D-12), and the 360s ceiling lift (D-13) is genuinely required because developed children run the full 90-332s pipeline.

Output: ?summary route branch + RemixedFromChip + chip mount in InputResultCard + Recent "Remix" tag + polling-ceiling lift (+ live-poll completion fallback marker).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-develop-predict-lineage/05-CONTEXT.md
@.planning/phases/05-develop-predict-lineage/05-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- /api/analysis/[id]/route.ts GET (lines 10-39): auth + ownership query.
     `_request: Request` must become `request: Request` to read `.url` for the ?summary check.
     Insert the ?summary branch AFTER the auth + ownership SELECT (so it stays scoped to user.id),
     BEFORE the existing enrichment block (~line 41+). -->
const { data, error } = await supabase
  .from("analysis_results").select("*")
  .eq("id", id).eq("user_id", user.id).is("deleted_at", null).single();
// → add: if (new URL(request.url).searchParams.has("summary")) return Response.json({ id, caption, created_at })

<!-- InputResultCard.tsx (line 65): already calls `usePermalinkAnalysis()` → `permalinkData`.
     After 05-01 types regen, permalinkData.parent_id is available. The chip mounts at the
     TOP of the card's returned JSX (the `input` frame renders on EVERY score board incl. children). -->
const { data: permalinkData } = usePermalinkAnalysis();

<!-- Sidebar.tsx recentBoards (lines 358-365 + render 553-592):
     recentBoards = (historyData ?? []).slice(0,8) as Array<{id; content_text?; overall_score?; created_at?}>
     Render shows a score chip: `{board.overall_score != null ? Math.round(board.overall_score) : '—'}` (line 588).
     ADD variants to the cast + replace the '—' fallback with a "Remix" tag when the row is a remix source. -->

<!-- use-analysis-stream.ts hydration map (IMPORTANT — corrected understanding):
     - line 127 `completedFromInitial`: gates on initialData.overall_score != null → null for remix rows →
       fresh permalink reload starts phase 'idle'. This is the gate that decides permalink-reload completion.
     - line 397 `pollEnabled = (phase === "polling")`: false when phase is 'idle' →
     - lines 411-420 poll gate: only runs while polling → NEVER runs on a fresh remix permalink reload.
       So the line-412 edit is a LIVE-POLL fallback only, NOT a permalink-reload fix.
     - lines 422-432: 90s ceiling `setTimeout(..., 90_000)` — must lift to 360s (D-13, genuinely needed).
     Permalink-reload hydration for remix rows is handled at the FRAME level by Phase 3's dual-read
     (DecodeShellNode.tsx ~181-189 reads permalinkData.variants.remix regardless of stream phase). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ?summary branch to /api/analysis/[id] + RemixedFromChip + mount on child board</name>
  <files>src/app/api/analysis/[id]/route.ts, src/components/board/RemixedFromChip.tsx, src/components/board/InputResultCard.tsx</files>
  <read_first>
    - src/app/api/analysis/[id]/route.ts lines 10-39 (auth + ownership query — the `?summary` branch inserts after this, before enrichment), lines 175-178 (catch/error pattern)
    - src/components/board/InputResultCard.tsx lines 50-66, 217-251 (Props, usePermalinkAnalysis on line 65, the returned JSX top where the chip mounts)
    - .planning/phases/05-develop-predict-lineage/05-PATTERNS.md §analysis/[id] ?summary + §dual-read chip
    - CLAUDE.md §"Raycast Design Language Rules" (chip: 6% border, Inter, 12px/rounded, no glow/tint; muted secondary text)
  </read_first>
  <action>
    In `src/app/api/analysis/[id]/route.ts`: change the handler's first param from `_request: Request` to `request: Request`. AFTER the existing auth + ownership SELECT (the query already filters `.eq("user_id", user.id)`), and BEFORE the enrichment block (~line 41), add: `if (new URL(request.url).searchParams.has("summary")) { return Response.json({ id: data.id, caption: (data.content_text ?? "").slice(0, 120) || null, created_at: data.created_at }); }`. This returns ONLY id + caption + created_at (D-10) — never the full row — and inherits the same user_id ownership scoping (a forged parent_id for another user's row 404s at the SELECT). Keep the existing catch/error pattern unchanged.

    Create `src/components/board/RemixedFromChip.tsx` ('use client'): props `{ parentId: string }`. It fetches `/api/analysis/${parentId}?summary` (a small useQuery or useEffect+fetch; reuse the project's query-key convention if convenient), and renders an anchor `href={'/analyze/' + parentId}` styled as a Raycast chip: `inline-flex items-center gap-1.5 rounded-lg border border-white/[0.06] px-2 py-1 text-xs font-medium text-white/55 hover:text-foreground hover:bg-white/[0.04] transition-colors w-fit`. Text reads `Remixed from "{caption}"` where caption is the summary's caption (truncate to ~40 chars with an ellipsis; fall back to `Remixed from source` if caption is null). No coral, no glow. While loading, render the chip with placeholder text `Remixed from …` (don't block the board). On fetch error, render the fallback `Remixed from source` link (the lineage link still works even if the caption fails). data-testid="remixed-from-chip".

    In `src/components/board/InputResultCard.tsx`: read `parent_id` off the existing `permalinkData` (line 65 — after 05-01 types regen it is typed). At the TOP of the card's main returned JSX (the `showResult` branch return at ~217, before the hero row), conditionally render `{(permalinkData as { parent_id?: string | null } | null)?.parent_id && <RemixedFromChip parentId={permalinkData.parent_id} />}`. Import RemixedFromChip. The chip appears only on developed children (parent_id != null) — ordinary analyses show nothing.
  </action>
  <verify>
    <automated>grep -c "summary" src/app/api/analysis/\[id\]/route.ts | { read n; [ "$n" -ge 1 ] && echo OK || echo FAIL; }; grep -c "Remixed from" src/components/board/RemixedFromChip.tsx; grep -c "RemixedFromChip\|parent_id" src/components/board/InputResultCard.tsx; npx tsc --noEmit 2>&1 | grep -E "route.ts|RemixedFromChip|InputResultCard" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `/api/analysis/[id]?summary` returns exactly `{ id, caption, created_at }` (NOT the full row) and only after the user_id-scoped SELECT (ownership preserved); a non-owned id still 404s
    - handler param is `request` (not `_request`) and reads `new URL(request.url)`
    - `RemixedFromChip.tsx` exists, renders an anchor to `/analyze/[parentId]`, text "Remixed from", Raycast chip styling (border-white/[0.06], no accent/glow), data-testid="remixed-from-chip"
    - InputResultCard mounts the chip ONLY when `permalinkData.parent_id` is truthy
    - `npx tsc --noEmit` clean for all three files
  </acceptance_criteria>
  <done>Child boards show a navigable "remixed from" chip backed by an ownership-scoped minimal parent summary.</done>
</task>

<task type="auto">
  <name>Task 2: Recent "Remix" tag for null-score source rows + polling ceiling lift (+ live-poll completion fallback)</name>
  <files>src/components/sidebar/Sidebar.tsx, src/hooks/queries/use-analysis-stream.ts</files>
  <read_first>
    - src/components/sidebar/Sidebar.tsx lines 358-365 (recentBoards cast), lines 553-592 (Recent render + score chip at 586-589 showing '—' for null score)
    - src/hooks/queries/use-analysis-stream.ts line 127 (`completedFromInitial` — the gate that decides permalink-reload completion; keys on overall_score, so remix rows start 'idle'), line 397 (`pollEnabled` keys on phase==='polling'), lines 411-420 (poll completion gate — live-poll path only), lines 422-432 (90s ceiling)
    - src/components/board/decode/DecodeShellNode.tsx lines 181-189 (frame-level dual-read — already hydrates remix permalink reload regardless of stream phase)
    - .planning/phases/05-develop-predict-lineage/05-PATTERNS.md §use-sidebar-queries (isRemix derivation: overall_score == null && variants.remix != null) + §use-analysis-stream (ceiling 360s)
    - CLAUDE.md §"Raycast Design Language Rules" (badge: muted, small, no accent — e.g. text-[10px] uppercase tracking-widest text-white/45 or a rounded-full bg-white/[0.06] pill)
  </read_first>
  <action>
    Recent "Remix" tag (D-11/D-12): in `src/components/sidebar/Sidebar.tsx`, extend the `recentBoards` cast (lines 360-365) to also read `variants?: { remix?: unknown } | null`. In the render (the score chip at ~586-589), compute `const isRemix = board.overall_score == null && (board.variants as { remix?: unknown } | null)?.remix != null;` and when `isRemix` is true, render a small "Remix" badge (Raycast: `text-[10px] uppercase tracking-widest text-white/45`, or a `rounded-full bg-white/[0.06] px-1.5 py-0.5 text-[10px] text-white/45` pill) INSTEAD of the `'—'` score fallback. Scored rows (overall_score != null) keep the numeric chip unchanged. data-testid="sidebar-remix-tag" on the badge. This distinguishes the un-scored remix source from scored analyses (D-11) without a blank slot. Backend already returns all non-deleted rows so remix rows already surface (D-12) — this is render-side only.

    Polling ceiling lift (D-13, the genuinely-required fix): in `src/hooks/queries/use-analysis-stream.ts`, replace the `90_000` literal in the ceiling setTimeout (lines 422-432) with a named constant `const POLLING_CEILING_MS = 360_000;` (declare near the top of the hook body or just above the effect) and use it in the setTimeout. Developed children run the full ~90-332s pipeline; the old 90s ceiling false-timed-out long analyses. Add a comment citing D-13.

    Live-poll completion fallback (harmless, NOT a permalink fix): also modify the poll completion gate (lines 411-420) to complete when `overall_score != null` OR `variants.remix != null`, reading variants defensively: `const d = pollQuery.data; const isScoreComplete = d?.overall_score != null; const isRemixComplete = (d as { variants?: { remix?: unknown } } | undefined)?.variants?.remix != null; if (isScoreComplete || isRemixComplete) { setResult(d as PredictionResult); setPhase("complete"); if (analysisId) queryClient.setQueryData(queryKeys.analysis.detail(analysisId), d); }`. IMPORTANT — add a comment that this gate only runs on the LIVE-polling path (phase==='polling'); it does NOT run on a fresh permalink reload (which starts 'idle' via `completedFromInitial` at line 127). Permalink-reload hydration of remix rows is already handled at the frame level by Phase 3's dual-read (DecodeShellNode ~181-189). Do NOT claim this edit fixes permalink reload. Do NOT touch the `completedFromInitial` gate at line 127 (out of scope).
  </action>
  <verify>
    <automated>grep -c "isRemix\|sidebar-remix-tag" src/components/sidebar/Sidebar.tsx | { read n; [ "$n" -ge 1 ] && echo OK || echo FAIL; }; grep -c "variants" src/hooks/queries/use-analysis-stream.ts; grep -c "360_000\|POLLING_CEILING_MS" src/hooks/queries/use-analysis-stream.ts | { read n; [ "$n" -ge 1 ] && echo "ceiling OK" || echo "ceiling FAIL"; }; npx tsc --noEmit 2>&1 | grep -E "Sidebar|use-analysis-stream" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar Recent renders a "Remix" badge (data-testid="sidebar-remix-tag") for rows where `overall_score == null && variants.remix != null`; scored rows keep the numeric chip
    - The old `90_000` ceiling literal is gone; `POLLING_CEILING_MS = 360_000` is used in the setTimeout
    - The live-poll completion gate (lines 411-420) transitions to "complete" when `overall_score != null` OR `variants.remix != null`, with a comment noting it is a live-poll fallback (not a permalink-reload fix) and that frame-level dual-read handles permalink reload
    - The `completedFromInitial` gate (line 127) is NOT modified
    - `grep -v '^[[:space:]]*//' src/hooks/queries/use-analysis-stream.ts | grep -c "90_000"` == 0
    - `npx tsc --noEmit` clean for both files
  </acceptance_criteria>
  <done>Remix source rows show a "Remix" tag in Recent; developed children no longer false-timeout at 90s (360s ceiling); the live-poll path also completes on the variants.remix marker. Permalink-reload hydration remains handled by Phase 3's frame-level dual-read (unchanged).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/analysis/[id]?summary | Untrusted id (a parent_id chosen by the client) requesting parent metadata |
| RemixedFromChip → /analyze/[parentId] | Navigation to a board owned by the same user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information disclosure | GET /api/analysis/[id]?summary | mitigate | The ?summary branch runs AFTER the existing `.eq("user_id", user.id).is("deleted_at", null).single()` SELECT — a user can only summarize THEIR OWN rows; a forged/cross-user parent_id 404s before any data is returned. The response is restricted to id + caption (first 120 chars of content_text) + created_at — no scores, personas, video paths, or PII beyond the user's own caption (D-10). |
| T-05-07 | Information disclosure | RemixedFromChip caption render | mitigate | Caption is rendered as a React text child (auto-escaped, no dangerouslySetInnerHTML) — no XSS from a caption containing markup. Truncated to ~40 chars in the chip. |
| T-05-08 | Spoofing | Recent "Remix" tag | accept | The tag is a render-side classification of the user's own rows (overall_score null + variants.remix). No trust boundary crossed; purely cosmetic. |
| T-05-SC | Tampering | npm installs | mitigate | No new dependencies introduced. slopcheck N/A. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `GET /api/analysis/[ownId]?summary` returns `{id, caption, created_at}` only; `GET /api/analysis/[otherUserId]?summary` returns 404.
- A child board (parent_id set) renders the "Remixed from …" chip linking to /analyze/[parentId]; an ordinary board renders no chip.
- Recent shows "Remix" tag on null-score remix rows, numeric score on scored rows.
- A remix/decode permalink reload still renders its decode content via Phase 3's frame-level dual-read (this is NOT changed by the line-412 edit; verified by the existing DecodeShellNode permalink behavior).
- Developed-child polling tolerates >90s (up to 360s) without erroring.
</verification>

<success_criteria>
- DEVELOP-02 (chip): child board shows a working "remixed from" chip linking back; parent summary via ?summary.
- DEVELOP-02 (Recent): developed children render normally; remix sources show a "Remix" tag (D-11) and appear in Recent (D-12).
- DEVELOP-02 (hydration/timeout): polling ceiling lifted to 360s (D-13) so developed children don't false-timeout; remix permalink-reload content continues to render via Phase 3's frame-level dual-read (unchanged); the live-poll path additionally completes on the variants.remix marker.
</success_criteria>

<output>
Create `.planning/phases/05-develop-predict-lineage/05-03-SUMMARY.md` when done.
</output>
