---
phase: 05-develop-predict-lineage
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - src/components/board/FrameErrorBoundary.tsx
  - src/components/board/Board.tsx
  - src/components/board/BoardMobile.tsx
  - src/components/board/decode/DecodeShellNode.tsx
  - src/components/board/adapt/AdaptShellNode.tsx
autonomous: false
requirements: [DEVELOP-01, DEVELOP-02]
must_haves:
  truths:
    - "Decode and Adapt frames are wrapped in an error boundary that contains a render throw"
    - "Decode and Adapt conform to Raycast styling (6% borders, no glow/tint, correct radii)"
    - "Decode and Adapt render correctly in the mobile card-stack at <768px, Develop trigger tappable"
    - "The grade-mode (score) board and existing analyze flow are unchanged — no regression"
  artifacts:
    - path: "src/components/board/FrameErrorBoundary.tsx"
      provides: "reusable render-error boundary for board frames"
      contains: "componentDidCatch"
  key_links:
    - from: "src/components/board/Board.tsx"
      to: "DecodeShellNode + AdaptShellNode"
      via: "FrameErrorBoundary wrap at the remix-frame mount sites"
      pattern: "FrameErrorBoundary"
    - from: "src/components/board/BoardMobile.tsx"
      to: "DecodeShellNode + AdaptShellNode"
      via: "FrameErrorBoundary wrap in the mobile remix card render"
      pattern: "FrameErrorBoundary"
---

<objective>
Own the phase-level regression + polish (D-14): create a reusable `FrameErrorBoundary`, wrap the Decode and Adapt frame mounts (desktop Board.tsx + mobile BoardMobile.tsx), sweep both new frames for Raycast styling conformance, verify the mobile card-stack (<768px) renders Decode + Adapt with the Develop trigger tappable, and confirm the grade-mode (score) board + existing analyze flow are entirely unchanged.

Purpose: This is the last plan in the milestone. It hardens the two new frames (error containment, design-language conformance, mobile) and proves no remix change leaked into the score path. It depends on 05-02 (Develop trigger) and 05-03 (chip) so the polish/regression sweep covers the finished surface.

Output: FrameErrorBoundary + wrapped mounts + Raycast/mobile sweep + grade-mode regression confirmation (checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-develop-predict-lineage/05-CONTEXT.md
@.planning/phases/05-develop-predict-lineage/05-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Desktop frame mounts — src/components/board/Board.tsx ~516-517:
  {layout.id === 'decode' && <DecodeShellNode />}
  {layout.id === 'adapt' && <AdaptShellNode camera={camera} layout={layout} />}

  Mobile frame mounts — src/components/board/BoardMobile.tsx ~134-138:
  case 'decode': return <DecodeShellNode />;
  case 'adapt':  return <AdaptShellNode camera={CARD_CAMERA} layout={layout} />;

  No ErrorBoundary component exists anywhere in src — this plan creates one. -->

<!-- AdaptShellNode (src/components/board/adapt/AdaptShellNode.tsx) — data-testid="adapt-shell"
     DecodeShellNode (src/components/board/decode/DecodeShellNode.tsx) — data-testid="decode-shell",
       has DecodeErrorState() at lines 60-74 (a render state, NOT a boundary) -->

<!-- Raycast rules (CLAUDE.md): borders white/[0.06], hover white/[0.1]; card radius 12px (rounded-xl),
     button radius 8px; Inter; NO glow, NO colored tint (coral reserved for accents only). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FrameErrorBoundary and wrap Decode + Adapt mounts (desktop + mobile)</name>
  <files>src/components/board/FrameErrorBoundary.tsx, src/components/board/Board.tsx, src/components/board/BoardMobile.tsx</files>
  <read_first>
    - src/components/board/Board.tsx lines 510-520 (the frame overlay mount switch — decode/adapt at ~516-517)
    - src/components/board/BoardMobile.tsx lines 130-145 (the remix card render switch — decode/adapt cases)
    - src/components/board/decode/DecodeShellNode.tsx lines 60-74 (DecodeErrorState — reuse its honest copy tone for the boundary fallback)
    - CLAUDE.md §"Raycast Design Language Rules" (fallback must be on-brand: muted, no glow)
  </read_first>
  <action>
    Create `src/components/board/FrameErrorBoundary.tsx` ('use client') — a class component (React error boundaries must be class-based) with `static getDerivedStateFromError(error)` returning `{ hasError: true }` and `componentDidCatch(error, info)`. Props: `{ frameLabel: string; children: React.ReactNode }`. On caught error, render an honest Raycast-styled fallback (NOT "fix this"/alarmist): a `role="alert"` block reading `"{frameLabel} couldn't render"` (`text-sm font-medium text-foreground`) + a muted secondary line `"The rest of the board is unaffected."` (`text-xs font-medium text-foreground-muted`), no coral, no glow, optional `border border-white/[0.06] rounded-xl p-4`. Log via `console.error('[FrameErrorBoundary]', frameLabel, error)`. data-testid="frame-error-boundary". The boundary must CONTAIN the throw so a Decode/Adapt render error never blanks the whole board (D-14 error boundaries).

    In `src/components/board/Board.tsx`: import FrameErrorBoundary and wrap the two remix mounts (~516-517):
    decode → `<FrameErrorBoundary frameLabel="Decode"><DecodeShellNode /></FrameErrorBoundary>`
    adapt → `<FrameErrorBoundary frameLabel="Adapt"><AdaptShellNode camera={camera} layout={layout} /></FrameErrorBoundary>`
    Do NOT wrap the score-mode frames (verdict/actions/input/engine/audience/content-analysis) — scope the boundary to the two NEW frames only (keeps the regression surface minimal, D-14).

    In `src/components/board/BoardMobile.tsx`: import FrameErrorBoundary and wrap the `case 'decode'` and `case 'adapt'` returns the same way (frameLabel "Decode"/"Adapt"). Leave the score-mode cases untouched.
  </action>
  <verify>
    <automated>grep -cE "componentDidCatch|getDerivedStateFromError" src/components/board/FrameErrorBoundary.tsx; grep -c "FrameErrorBoundary" src/components/board/Board.tsx; grep -c "FrameErrorBoundary" src/components/board/BoardMobile.tsx; npx tsc --noEmit 2>&1 | grep -E "FrameErrorBoundary|Board.tsx|BoardMobile" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `FrameErrorBoundary.tsx` is a class component with both `getDerivedStateFromError` and `componentDidCatch`; renders a `role="alert"` Raycast fallback (no coral, no glow); data-testid="frame-error-boundary"
    - Board.tsx wraps BOTH decode and adapt mounts in `<FrameErrorBoundary>`; score-mode frames are NOT wrapped
    - BoardMobile.tsx wraps the decode and adapt cases in `<FrameErrorBoundary>`; score-mode cases untouched
    - `npx tsc --noEmit` clean for all three files
  </acceptance_criteria>
  <done>A render throw inside Decode or Adapt is contained by the boundary on both desktop and mobile; the rest of the board renders.</done>
</task>

<task type="auto">
  <name>Task 2: Raycast styling + mobile sweep on Decode and Adapt frames</name>
  <files>src/components/board/decode/DecodeShellNode.tsx, src/components/board/adapt/AdaptShellNode.tsx</files>
  <read_first>
    - src/components/board/decode/DecodeShellNode.tsx (full file — verify borders/radii/colors against CLAUDE.md; existing isDecoding guard ~172-190 and m3 dual-read)
    - src/components/board/adapt/AdaptShellNode.tsx (full file) + src/components/board/adapt/AdaptConceptCard.tsx (the Develop trigger added in 05-02 — confirm w-full ≥44px tap target for mobile)
    - src/components/board/BoardMobile.tsx (MOBILE_ORDER_REMIX includes decode+adapt; CARD_CAMERA passed to AdaptShellNode)
    - CLAUDE.md §"Raycast Design Language Rules" (6% borders, 10% hover, 12px card / 8px button radius, Inter, no glow/tint)
  </read_first>
  <action>
    Audit `DecodeShellNode.tsx` and `AdaptShellNode.tsx` (and the AdaptConceptCard surface they render) against the Raycast rules and fix ANY deviations found: borders must be `white/[0.06]` (hover `white/[0.1]`), card radius `rounded-xl` (12px), button radius 8px, NO `glow`/`shadow`-glow effects, NO colored background tint (coral reserved for accent chips only). If a frame already conforms, make no cosmetic change (do not churn conformant code) and record "conformant, no change" in the SUMMARY. Confirm the Develop trigger added in 05-02 is `w-full` (full-width tap target ≥44px) so it is tappable in the mobile card stack — if its rendered height is below 44px on mobile, add vertical padding (e.g. `py-2`) without changing the desktop look materially.

    Do NOT modify any score-mode component (verdict/actions/input/engine/audience/content-analysis) — D-14 requires the score path stay byte-identical. Confirm DecodeShellNode/AdaptShellNode are mounted ONLY in the remix branch (Board.tsx + BoardMobile.tsx remix cases) and never under boardMode='score'.
  </action>
  <verify>
    <automated>grep -rnE "white/\[0\.08\]|white/\[0\.12\]|drop-shadow|blur-|shadow-\[0 0" src/components/board/decode/DecodeShellNode.tsx src/components/board/adapt/AdaptShellNode.tsx src/components/board/adapt/AdaptConceptCard.tsx || echo "no non-Raycast border/glow tokens found"; npx tsc --noEmit 2>&1 | grep -E "DecodeShellNode|AdaptShellNode" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - No non-Raycast border opacities (white/[0.08], white/[0.12]) and no glow tokens (drop-shadow / coral box-shadow glow) in DecodeShellNode, AdaptShellNode, or AdaptConceptCard — or each remaining instance is documented as intentional in the SUMMARY
    - The Develop trigger is full-width with a ≥44px effective tap target on mobile
    - No score-mode component file is modified by this task
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Both new frames conform to Raycast styling and the Develop trigger is mobile-tappable; score components untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Full regression — typecheck, lint, test suite, grade-path freeze</name>
  <files>(no source edits — verification + fix-on-fail only)</files>
  <read_first>
    - .planning/phases/05-develop-predict-lineage/05-CONTEXT.md §D-14 (grade-mode regression sweep scope)
    - package.json scripts (test = `vitest run`, lint = `eslint`, build = `next build`)
  </read_first>
  <action>
    Run the full regression gate and fix any failures introduced by Phases 3-5 wiring:
    - `npx tsc --noEmit` — zero errors.
    - `npm run lint` — zero new errors in changed files.
    - `npm test` (vitest run) — full suite green; pay attention to Board/BoardMobile/InputResultCard/Sidebar/Adapt tests.
    - `npm run build` — succeeds (catches RSC/client-boundary regressions).
    Confirm the grade-mode (score) path is unchanged: a `mode='score'` submission renders Verdict + Actions + Input + Engine + Audience + Content Craft (NO Decode/Adapt frames), and the existing analyze flow behaves identically. Verify via the existing score-path tests (Board.test / BoardMobile.test) staying green. If any failure traces to a Phase 5 change, fix it; if a failure is pre-existing/unrelated, document it in the SUMMARY (do not silently absorb scope).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint 2>&1 | tail -5 && npm test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm test` reports all suites passing (or any failure is documented as pre-existing + unrelated in the SUMMARY with evidence)
    - `npm run build` succeeds
    - Score-path tests (Board/BoardMobile score mode) remain green — no Decode/Adapt leak into mode='score'
  </acceptance_criteria>
  <done>tsc, lint, tests, and build all pass; grade-mode path proven unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — Develop loop, chip, Recent tag, mobile (D-14)</name>
  <files>(none — human verification of the running app)</files>
  <action>
    Pause for human verification of the full Develop & lineage loop on the running app. The executor presents the verification steps below; the user runs them and approves or reports failures. The loop spans Plans 05-01..05-04: per-concept Develop trigger, parent_id lineage, "remixed from" chip, Recent "Remix" tag, error boundaries, mobile card-stack, and the Raycast sweep. This is a blocking gate — do not mark the phase complete until the user approves.
  </action>
  <what-built>The full Develop & lineage loop (Plans 05-01..05-04): per-concept Develop trigger, parent_id lineage, "remixed from" chip, Recent "Remix" tag, error boundaries, mobile card-stack, and the Raycast sweep.</what-built>
  <how-to-verify>
    1. Start the dev server. Submit a Remix-mode TikTok URL; wait for Decode + Adapt to render. On a desktop viewport, confirm each of the 3 Adapt concept cards shows a "Develop & predict →" trigger.
    2. Click Develop on ONE concept. Confirm: exactly one new analysis starts, the browser navigates to `/analyze/[childId]`, and the OTHER concepts did NOT start scoring (C3 — watch the network tab: exactly one POST /api/analyze).
    3. On the child board, confirm a "Remixed from '[caption]'" chip is visible and clicking it returns to the source remix board (`/analyze/[parentId]`).
    4. Open the sidebar Recent list. Confirm the source remix row shows a "Remix" tag (not a score number) and the developed child shows a normal score.
    5. Reload the source remix permalink — confirm it hydrates to a complete state (no perpetual skeleton).
    6. Resize to <768px (or use device emulation). Confirm Decode + Adapt appear as cards in the stack, in order, and the Develop trigger is tappable.
    7. (Optional) Confirm a thrown error inside a frame shows the FrameErrorBoundary fallback, not a blank board.
    8. Submit a Score-mode analysis — confirm the grade board renders Verdict + Actions (no Decode/Adapt) exactly as before (no regression).
  </how-to-verify>
  <verify>
    <human-check>All 8 steps pass: Develop loop (1 stream/click), chip navigates, Recent tag correct, remix permalink hydrates, mobile cards + tappable trigger, error boundary contains, score path unchanged.</human-check>
  </verify>
  <acceptance_criteria>
    - One Develop click → exactly one POST /api/analyze → navigation to child board (DEVELOP-01, C3)
    - Child board chip links back to the source (DEVELOP-02)
    - Recent shows "Remix" tag on source, score on child (DEVELOP-02)
    - Mobile card-stack renders Decode + Adapt with tappable Develop trigger (D-14)
    - Score-mode board unchanged (D-14 regression)
  </acceptance_criteria>
  <done>User has approved all 8 verification steps; the Develop & lineage loop and the grade-mode regression are confirmed.</done>
  <resume-signal>Type "approved" once all steps verify, or describe the failing step(s).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Decode/Adapt frame render → FrameErrorBoundary | A render-time throw inside an untrusted/external-data-driven frame is contained, not propagated to the whole board |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Denial of service (UI) | Decode/Adapt render throw | mitigate | FrameErrorBoundary catches render errors in the two new frames so a single frame's failure (e.g. malformed variants payload) cannot blank the entire board — the rest of the board (and the score path) keeps rendering (D-14). |
| T-05-10 | Information disclosure | FrameErrorBoundary fallback copy | accept | The fallback shows only a generic "{frame} couldn't render" message; the raw error is logged to console (dev tooling), not rendered to the DOM — no stack/PII leak to the page. |
| T-05-SC | Tampering | npm installs | mitigate | No new dependencies introduced (error boundary uses React's built-in class lifecycle). slopcheck N/A. |
</threat_model>

<verification>
- FrameErrorBoundary contains a thrown error in Decode/Adapt (desktop + mobile); board otherwise renders.
- Decode/Adapt conform to Raycast tokens (no non-6% borders, no glow).
- `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build` all pass.
- Grade-mode (score) board unchanged — Verdict + Actions present, no Decode/Adapt; score-path tests green.
- Human checkpoint confirms the full Develop → child → chip → Recent → mobile loop.
</verification>

<success_criteria>
- D-14: grade-mode regression confirmed (score board + analyze flow unchanged); Decode + Adapt pass Raycast styling, render error boundaries, and verify on the mobile card-stack at <768px.
- DEVELOP-01 + DEVELOP-02 end-to-end loop verified by the human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/05-develop-predict-lineage/05-04-SUMMARY.md` when done. Record the Raycast audit result (conformant / fixes made) and the regression gate outputs.
</output>
