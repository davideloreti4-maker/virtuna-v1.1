---
phase: 02-omni-verbatim
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/lib/engine/qwen/omni-analysis.ts
  - src/lib/engine/aggregator.ts
  - src/lib/engine/types.ts
  - src/app/api/analyze/route.ts
  - src/lib/engine/version.ts
  - src/types/database.types.ts
  - supabase/migrations/20260604000000_persist_engine_verbatim_phase2.sql
autonomous: false
requirements: [R1, R8]
must_haves:
  truths:
    - "Model-emitted hook_verbatim survives the omni-analysis assembly hop onto geminiResult.analysis (the emotion_arc bug does not recur)"
    - "The aggregator plucks verbatim and threads it onto PredictionResult; absence is non-fatal (null)"
    - "Both route persistence sites (INSERT buildInsertRow and SSE safety-net UPDATE) write the verbatim column"
    - "A dedicated verbatim JSONB column exists on the live analysis_results table"
    - "database.types.ts declares verbatim on Row/Insert/Update"
    - "ENGINE_VERSION is bumped to 3.2.0 so stale pre-verbatim cached rows do not serve"
  artifacts:
    - path: "supabase/migrations/20260604000000_persist_engine_verbatim_phase2.sql"
      provides: "ADD COLUMN IF NOT EXISTS verbatim JSONB on analysis_results"
      contains: "verbatim"
    - path: "src/lib/engine/types.ts"
      provides: "verbatim field on PredictionResult"
      contains: "verbatim"
    - path: "src/lib/engine/version.ts"
      provides: "ENGINE_VERSION = 3.2.0"
      contains: "3.2.0"
  key_links:
    - from: "src/lib/engine/qwen/omni-analysis.ts assembly literal"
      to: "src/lib/engine/aggregator.ts pluck"
      via: "geminiResult.analysis.hook_verbatim read by as-unknown-as cast"
      pattern: "hook_verbatim"
    - from: "src/lib/engine/aggregator.ts"
      to: "src/app/api/analyze/route.ts"
      via: "finalResult.verbatim persisted at INSERT (:594) and UPDATE (:921)"
      pattern: "verbatim"
---

<objective>
Thread verbatim through the remaining three hops (assembly to aggregator to PredictionResult to DB) following the emotion_arc precedent line-by-line, persist it to a dedicated verbatim JSONB column (Option A), and bump the cache key. After this plan the full pipeline carries verbatim end-to-end and a real run will populate the column — Plan 03 proves it.

Persistence decision (Claude's Discretion, locked to Option A): a dedicated `verbatim` JSONB column on analysis_results, mirroring emotion_arc's migration `20260531000000`. Rationale: verbatim is produced inline by the aggregator exactly like emotion_arc, so it rides the SAME row write with zero new write-race surface (unlike Option B which adds a 4th concurrent writer to the variants blob); R1's "persists non-empty verbatim" is query-clean against a top-level column; and verbatim is first-class P3/P4 sensor data, not ephemeral board state. The migration is minimal (one ADD COLUMN IF NOT EXISTS, no backfill — historical rows stay NULL).

Additive-only (D-01): every edit ADDS. No score-blend input removed/repointed.

Purpose: make verbatim survive all four hops AND both persistence sites — the exact places the emotion_arc bug nulled 26/26 rows.
Output: assembly thread, aggregator pluck+thread, PredictionResult field, migration + applied column + db types, both route inserts, ENGINE_VERSION 3.2.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-omni-verbatim/02-CONTEXT.md
@.planning/phases/02-omni-verbatim/02-RESEARCH.md
@.planning/phases/02-omni-verbatim/02-PATTERNS.md
@.planning/phases/02-omni-verbatim/02-01-SUMMARY.md

<interfaces>
<!-- Extracted from current repo source (verified 2026-06-04). The emotion_arc precedent is the line-by-line template across all sites below. -->

src/lib/engine/qwen/omni-analysis.ts:
- :236-258 assembly literal `const analysis = { ... } as GeminiVideoAnalysis`
- :249-257 emotion_arc do-not-remove annotation + `emotion_arc: data.emotion_arc` at :257
- :263 log.info("omni analysis complete", { ..., emotion_arc_points })  ← add verbatim_present here

src/lib/engine/aggregator.ts:
- :518-531 emotion_arc pluck: `let emotion_arc = null; try { const arcRaw = (geminiResult.analysis as unknown as { emotion_arc? })?.emotion_arc; ... } catch { ... }`
- :925 area: emotion_arc threaded onto the assembled PredictionResult
- :45 / :959 ENGINE_VERSION import + use

src/lib/engine/types.ts:
- :9  import type { EmotionArcPoint } from "./qwen/schemas"
- :268 emotion_arc?: EmotionArcPoint[] | null  ← add verbatim? after this

src/app/api/analyze/route.ts:
- :594 INSERT (buildInsertRow): emotion_arc: (finalResult.emotion_arc ?? null) as unknown as Json
- :921 UPDATE (SSE safety-net): emotion_arc: (finalResult.emotion_arc ?? null) as unknown as null

src/lib/engine/version.ts:
- :9 export const ENGINE_VERSION = "3.1.0"  ← bump to "3.2.0"

src/types/database.types.ts:
- :197 Row:    emotion_arc: Json | null
- :251 Insert: emotion_arc?: Json | null
- :305 Update: emotion_arc?: Json | null

supabase/migrations/20260531000000_persist_engine_emitted_columns.sql  ← minimal ADD COLUMN template
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread verbatim through assembly to aggregator to PredictionResult</name>
  <files>src/lib/engine/qwen/omni-analysis.ts, src/lib/engine/aggregator.ts, src/lib/engine/types.ts</files>
  <read_first>
    - src/lib/engine/qwen/omni-analysis.ts (:236-258 assembly literal; :249-257 emotion_arc annotation; :263 log line)
    - src/lib/engine/aggregator.ts (:518-531 emotion_arc pluck; :925 thread-onto-result)
    - src/lib/engine/types.ts (:9 import, :268 emotion_arc field)
    - .planning/phases/02-omni-verbatim/02-PATTERNS.md (sections 2, 3, 4 — exact threading code)
  </read_first>
  <behavior>
    - Model emits hook_verbatim -> after analyzeVideoWithOmni, (geminiResult.analysis as unknown as { hook_verbatim? }).hook_verbatim is defined (the assembly-hop test from Plan 01 turns/stays GREEN).
    - Model omits hook_verbatim -> analysis.hook_verbatim undefined, no crash; aggregator yields verbatim = null.
    - PredictionResult carries a verbatim field plucked by the aggregator.
    - Per-segment verbatim rides on data.segments through normalizeSegments untouched.
  </behavior>
  <action>
    Hop 2 (omni-analysis.ts assembly :236-258): add `hook_verbatim: data.hook_verbatim` to the `const analysis = { ... } as GeminiVideoAnalysis` literal, directly mirroring the `emotion_arc: data.emotion_arc` line at :257 and respecting its do-not-remove annotation. Per-segment verbatim already rides on data.segments (normalizeSegments passes unknown fields through) — do not strip it. Add a `verbatim_present: data.hook_verbatim ? true : false` key to the log.info at :263 (mirrors emotion_arc_points) so a real run can distinguish "model emitted none" from "assembly dropped it". Hop 3 (aggregator.ts :518-531): add a verbatim pluck block immediately after the emotion_arc pluck, using the identical `(geminiResult.analysis as unknown as { hook_verbatim?: { spoken_words?: string | null; on_screen_text?: string | null } })?.hook_verbatim` narrow cast wrapped in try/catch (verbatim = null on absence/throw — non-fatal). Thread `verbatim` onto the assembled PredictionResult near :925 alongside `emotion_arc`. Hop into types (types.ts): add `verbatim?: { hook?: { spoken_words?: string | null; on_screen_text?: string | null } } | null` to PredictionResult after the emotion_arc field at :268 (inline type acceptable, or a VerbatimPayload interface near EmotionArcPoint). Do NOT add verbatim to the base GeminiVideoAnalysis interface — it rides the `as` cast exactly like emotion_arc. Additive only.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/omni-analysis-verbatim.test.ts 2>&1 | tail -20</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -E 'omni-analysis|aggregator|types.ts' || echo "no new type errors in touched files"</automated>
  </verify>
  <done>Full verbatim test green including the assembly-hop regression case. PredictionResult exposes verbatim. tsc clean for the three touched files.</done>
</task>

<task type="auto">
  <name>Task 2: Add verbatim JSONB column — migration + db types + both route persistence sites</name>
  <files>supabase/migrations/20260604000000_persist_engine_verbatim_phase2.sql, src/types/database.types.ts, src/app/api/analyze/route.ts</files>
  <read_first>
    - supabase/migrations/20260531000000_persist_engine_emitted_columns.sql (minimal ADD COLUMN template)
    - src/types/database.types.ts (:197 Row, :251 Insert, :305 Update emotion_arc entries)
    - src/app/api/analyze/route.ts (:594 INSERT, :921 UPDATE emotion_arc persist sites)
    - .planning/phases/02-omni-verbatim/02-PATTERNS.md (sections 5, 7, 8 — persistence + migration + db types)
  </read_first>
  <action>
    Migration: create supabase/migrations/20260604000000_persist_engine_verbatim_phase2.sql with `ALTER TABLE public.analysis_results ADD COLUMN IF NOT EXISTS verbatim JSONB;` plus a COMMENT ON COLUMN documenting the shape ({ hook { spoken_words, on_screen_text }, per-segment via segments[].{spoken_text,on_screen_text} }), the null-for-no-speech contract, the [inaudible] marker, no-backfill policy, and P3/P4 consumers. Mirror 20260531000000 exactly — no backfill, no DROP. db types: add `verbatim: Json | null` to the Row (after :197), `verbatim?: Json | null` to Insert (after :251) and Update (after :305) sections of database.types.ts (hand-add each, mirroring the emotion_arc line in that section). Route persistence — add verbatim to BOTH sites (the emotion_arc bug Pitfall 2: streamed runs use the UPDATE path): at INSERT/buildInsertRow (:594) add `verbatim: (finalResult.verbatim ?? null) as unknown as Json,`; at the SSE safety-net UPDATE (:921) add `verbatim: (finalResult.verbatim ?? null) as unknown as null,`. Additive only — touch no existing column.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260604000000_persist_engine_verbatim_phase2.sql && grep -c 'ADD COLUMN IF NOT EXISTS verbatim' supabase/migrations/20260604000000_persist_engine_verbatim_phase2.sql</automated>
    <automated>grep -c 'verbatim' src/types/database.types.ts</automated>
    <automated>grep -c 'verbatim:' src/app/api/analyze/route.ts</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -E 'route.ts|database.types' || echo "no new type errors"</automated>
  </verify>
  <done>Migration file exists with the ADD COLUMN statement. database.types.ts has verbatim in all three (Row/Insert/Update) = grep count >= 3. route.ts has verbatim at both insert sites = grep count >= 2. tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [BLOCKING]: Apply the verbatim migration to the live database</name>
  <what-built>
    The migration file supabase/migrations/20260604000000_persist_engine_verbatim_phase2.sql adds a `verbatim JSONB` column to analysis_results. database.types.ts and the route inserts already reference it. BUT the live DB still lacks the column — types come from the generated file, not the live schema, so build/types pass falsely while a real insert would fail or silently drop. This column MUST exist on the live DB BEFORE Plan 03's real-run R1 proof, or the proof is a false positive.
  </what-built>
  <how-to-verify>
    Apply the migration to the live Supabase project, then confirm the column exists. Preferred (this environment): use the Supabase MCP `apply_migration` tool with the SQL from the migration file (name `persist_engine_verbatim_phase2`). Alternative: `supabase db push` (requires SUPABASE_ACCESS_TOKEN in env). After applying, verify with the Supabase MCP `list_tables` (or `execute_sql`): confirm `analysis_results` now has a `verbatim` column of type `jsonb`. This task is autonomous: false because applying to the live DB and/or supplying SUPABASE_ACCESS_TOKEN may require interactive confirmation that cannot be supplied non-interactively.
  </how-to-verify>
  <acceptance_criteria>
    - The live analysis_results table has a column `verbatim` of type jsonb (confirmed via list_tables / information_schema query).
    - The migration is recorded as applied (appears in the migrations history / supabase_migrations).
  </acceptance_criteria>
  <resume-signal>Type "applied" once the verbatim column is confirmed on the live DB, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Bump ENGINE_VERSION to 3.2.0 (cache invalidation)</name>
  <files>src/lib/engine/version.ts</files>
  <read_first>
    - src/lib/engine/version.ts (:6 invariant comment, :9 ENGINE_VERSION = "3.1.0")
    - .planning/phases/02-omni-verbatim/02-RESEARCH.md (Cache Key section — single constant gates L1 + L2)
  </read_first>
  <action>
    Change ENGINE_VERSION from "3.1.0" to "3.2.0" at version.ts:9. Update the adjacent comment if it names the prior version so it reflects 3.1.0 -> 3.2.0. This single constant gates both the L1 in-memory cacheKey() template and the L2 Supabase .eq("engine_version", ENGINE_VERSION) filter — bumping it auto-invalidates all stale pre-verbatim cached rows so they never serve a verbatim-less result. No other cache change needed (no separate Omni-level cache exists). Additive/version-only edit.
  </action>
  <verify>
    <automated>grep -c 'ENGINE_VERSION = "3.2.0"' src/lib/engine/version.ts</automated>
  </verify>
  <done>version.ts exports ENGINE_VERSION = "3.2.0". grep count = 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Qwen-Omni model to backend | Verbatim strings (already capped by Plan 01 Zod) flow through assembly/aggregator into the DB write. |
| Backend to Supabase (analysis_results row) | Verbatim is written to the user's own RLS-governed row; no new access surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-03 | Information disclosure | verbatim persisted to analysis_results | accept | Verbatim rides the same user_id-scoped, RLS-governed row the existing pipeline already writes; no new exposure surface (RESEARCH Security Domain V4 = no change). |
| T-2-04 | Tampering | assembly-hop drop (the emotion_arc bug) | mitigate | Verbatim added to the assembly literal at :257 alongside emotion_arc; the Plan 01 assembly-hop test gates it; verbatim_present log signal added for real-run diagnosis. |
| T-2-05 | Tampering | streamed runs persist via UPDATE not INSERT | mitigate | Verbatim added to BOTH route sites (:594 INSERT and :921 UPDATE) per Pitfall 2; grep verify asserts >=2 occurrences. |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Standard Stack: zero installs). |
</threat_model>

<verification>
- `npx vitest run src/lib/engine/__tests__/omni-analysis-verbatim.test.ts` — assembly-hop case green.
- `npx tsc --noEmit` — clean across omni-analysis, aggregator, types, route, database.types.
- Migration applied to live DB (Task 3 acceptance): verbatim jsonb column present.
- `grep -c 'verbatim:' src/app/api/analyze/route.ts` >= 2 (both sites).
- `grep -c 'ENGINE_VERSION = "3.2.0"' src/lib/engine/version.ts` = 1.
- Additive-only guard: git diff shows only added lines for verbatim; no removed/repointed existing fields or score-blend inputs.
</verification>

<success_criteria>
- Verbatim survives all four hops (schema -> assembly -> aggregator -> PredictionResult) and reaches BOTH persistence sites.
- A live verbatim JSONB column exists (migration applied before Plan 03).
- ENGINE_VERSION = 3.2.0 invalidates stale cache.
- Determinism untouched; no existing field removed (D-01).
</success_criteria>

<output>
Create `.planning/phases/02-omni-verbatim/02-02-SUMMARY.md` when done.
</output>
