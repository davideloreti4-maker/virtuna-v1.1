---
phase: 02-omni-verbatim
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified: []
autonomous: false
requirements: [R1, R8, R12]
must_haves:
  truths:
    - "A real Omni run on a speech-bearing video persists non-empty verbatim (hook_verbatim.spoken_words + per-segment text) in the analysis_results.verbatim column (R1)"
    - "A real run on a silent video persists null verbatim — NOT [inaudible], NOT a description (D-02 honesty contract)"
    - "E2E latency on a real run stays under the cap with verbatim live (R6 headroom)"
    - "The remix path (omniOutputToStructuralInput + remix __tests__) stays green — verbatim is additive and invisible to its fixed allowlist (R12 no-regression)"
    - "temperature: 0 + seed: QWEN_SEED at the Omni call site remain unchanged (R8)"
  artifacts:
    - path: ".planning/phases/02-omni-verbatim/02-03-SUMMARY.md"
      provides: "R1 real-run proof record (DB query results for speech + silent videos) + latency number + remix-green attestation"
      contains: "verbatim"
  key_links:
    - from: "scripts/measure-pipeline.ts real run"
      to: "analysis_results.verbatim column"
      via: "Omni -> aggregator -> route INSERT/UPDATE -> DB"
      pattern: "verbatim"
---

<objective>
Prove the whole thing actually works on a real run — the deliverable R1 cares about ("verbatim persists on a real run", not "schema compiles"). This is the exact hop the emotion_arc bug hid in: everything can be green in unit tests and still null on every persisted row. Plan 03 runs a live analyze on a speech-bearing video and a silent video, queries the verbatim column, and confirms the honesty contract on real model output. It also confirms the remix path did not regress and that latency stayed under cap.

Depends on Plan 02 fully landed INCLUDING the applied migration (Task 02-02-3) — without the live column the real-run proof is a false positive.

Purpose: R1 acceptance (the phase target), R6 latency headroom, R12 no-regression, R8 determinism guard — all on live evidence.
Output: a SUMMARY recording the DB query results, latency, and remix-green attestation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-omni-verbatim/02-CONTEXT.md
@.planning/phases/02-omni-verbatim/02-RESEARCH.md
@.planning/phases/02-omni-verbatim/02-VALIDATION.md
@.planning/phases/02-omni-verbatim/02-01-SUMMARY.md
@.planning/phases/02-omni-verbatim/02-02-SUMMARY.md

<interfaces>
<!-- Verification entry points (verified 2026-06-04). -->

scripts/measure-pipeline.ts
- Real E2E harness: `npx tsx scripts/measure-pipeline.ts "/path/to/video.mp4"` runs runPredictionPipeline + aggregateScores exactly as /api/analyze and reconstructs a wall-clock timeline. Confirms latency under cap.

src/lib/engine/remix/decode.ts:176-212
- omniOutputToStructuralInput(omni): reads a FIXED allowlist (hook_decomposition, factors, video_signals, emotion_arc, content_summary, overall_impression) + omni.segments + omni.wave0Result. Returns null only if hook_decomposition || factors || video_signals missing. Verbatim is invisible to it.

src/lib/engine/remix/__tests__/
- omni-to-structural.test.ts, decode.test.ts, adapt.test.ts — must stay green.
src/app/api/analyze/__tests__/decode-route.test.ts — remix route test, must stay green.

src/lib/engine/qwen/omni-analysis.ts:195-196
- temperature: 0 / seed: QWEN_SEED — R8 guard target.

DB query: select id, verbatim from analysis_results where id = '<run id>';  (Supabase MCP execute_sql, or supabase client)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remix no-regression + R8 determinism guard (automated)</name>
  <files></files>
  <read_first>
    - src/lib/engine/remix/decode.ts (:176-212 omniOutputToStructuralInput fixed allowlist)
    - src/lib/engine/remix/__tests__/omni-to-structural.test.ts, decode.test.ts
    - .planning/phases/02-omni-verbatim/02-RESEARCH.md (Remix No-Regression Surface section)
  </read_first>
  <action>
    Run the remix test suite and confirm it stays green — verbatim is additive and invisible to omniOutputToStructuralInput's fixed allowlist, so these must pass untouched (R12 no-regression; consumption of verbatim by decode is P3, out of scope). Run the full verbatim unit suite (Plan 01/02) too. Assert R8: the Omni call-site sampling config is unchanged via grep on omni-analysis.ts. If any remix test fails, STOP and report — that is a real regression, not an expected change.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/remix/__tests__/ src/app/api/analyze/__tests__/decode-route.test.ts 2>&1 | tail -15</automated>
    <automated>npx vitest run src/lib/engine/__tests__/omni-analysis-verbatim.test.ts 2>&1 | tail -10</automated>
    <automated>grep -v '^#' src/lib/engine/qwen/omni-analysis.ts | grep -c 'temperature: 0\|seed: QWEN_SEED'</automated>
  </verify>
  <acceptance_criteria>
    - Remix __tests__ + decode-route test all pass (R12 no-regression).
    - Verbatim unit suite passes (carry-forward from Plans 01/02).
    - grep count for temperature: 0 / seed: QWEN_SEED = 2 (R8 unchanged).
  </acceptance_criteria>
  <done>All three automated checks pass; no remix regression.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 [R1 PROOF]: Real run — verbatim persists (speech) and is null (silent) in the live DB</name>
  <what-built>
    The full additive verbatim path: Omni emits verbatim, it survives all four hops + both route persistence sites, and the live analysis_results.verbatim JSONB column (migration applied in Plan 02 Task 3) stores it. This is the R1 acceptance: "a video run persists non-empty verbatim fields" — proven on a real DashScope/Qwen run, not a mock. It also validates the D-02 honesty contract on real model output: silent content yields null, never a fabricated or bracketed paraphrase.
  </what-built>
  <how-to-verify>
    Requires a live DASHSCOPE_API_KEY + Supabase access (env-dependent — hence autonomous: false).
    1. Speech-bearing video: run `npx tsx scripts/measure-pipeline.ts "<path-to-speech-video>"`. Capture the run/analysis id and the wall-clock total. Query the row: `select id, verbatim from analysis_results where id = '<id>';` (Supabase MCP execute_sql or the supabase client). Confirm verbatim is non-empty: hook_verbatim.spoken_words is a real transcribed line AND at least one segment carries spoken_text or on_screen_text. Confirm the omni log emitted verbatim_present: true (distinguishes "model emitted none" from "assembly dropped it").
    2. Silent / music-only / slideshow video: run the same harness. Query the row. Confirm verbatim.hook_verbatim.spoken_words is null (NOT "[inaudible]", NOT "[music plays]" or any description). A null here is a PASS, not a regression (D-02.2).
    3. Latency (R6): confirm each run's E2E total stays under the cap with verbatim live (target headroom per R6; the D-04.4 caps bound the extra output tokens). Record the number.
    Record all results (ids, the verbatim JSON for each, latency, verbatim_present log) in 02-03-SUMMARY.md.
  </how-to-verify>
  <acceptance_criteria>
    - Speech video: analysis_results.verbatim is non-empty — hook_verbatim.spoken_words is a non-null transcribed string; >=1 segment has non-null spoken_text or on_screen_text. (R1)
    - Silent video: verbatim.hook_verbatim.spoken_words === null, and is NOT the string "[inaudible]" and NOT a sound description. (D-02)
    - Both runs' E2E latency recorded and under the cap. (R6)
    - verbatim_present: true logged on the speech run. (assembly-hop confirmed live)
    - Results captured in 02-03-SUMMARY.md with the actual query output.
  </acceptance_criteria>
  <resume-signal>Type "verified" with the speech-run id + the verbatim JSON + latency, and the silent-run null confirmation — or describe the failure (e.g. null on a speech video = the assembly/persistence drop recurred).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live Qwen-Omni model to DB | Real model output (verbatim) is persisted; this plan only reads/verifies, adds no new write surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-06 | Tampering/Integrity | verbatim nulled on real persisted rows (the emotion_arc bug recurring undetected) | mitigate | Real-run DB query (Task 2) is the explicit detector; verbatim_present log distinguishes model-omission from assembly-drop. This is the mitigation the unit tests cannot provide. |
| T-2-07 | Spoofing/Integrity | model fabricates verbatim for silent content | mitigate | Task 2 silent-video check asserts null, not a bracketed/described paraphrase (D-02). |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | No new packages (verification-only plan). |
</threat_model>

<verification>
- `npx vitest run src/lib/engine/remix/__tests__/ ...` — remix green (R12).
- Real-run DB query — speech video verbatim non-empty (R1); silent video null, not [inaudible] (D-02).
- Latency recorded under cap (R6).
- R8 grep — sampling unchanged.
- `npm test` full suite green before phase sign-off (per VALIDATION.md phase gate).
</verification>

<success_criteria>
- R1 proven on a real run: verbatim persists non-empty for speech, null for silence.
- R6 latency under cap with verbatim live.
- R12 remix path green (no regression).
- R8 determinism config untouched.
- Phase is independently shippable to main (additive, no downstream dependency).
</success_criteria>

<output>
Create `.planning/phases/02-omni-verbatim/02-03-SUMMARY.md` when done — include the real-run ids, the verbatim JSON for both videos, latency numbers, and the remix-green attestation.
</output>
