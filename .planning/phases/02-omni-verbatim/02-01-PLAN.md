---
phase: 02-omni-verbatim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/engine/qwen/schemas.ts
  - src/lib/engine/qwen/omni-analysis.ts
  - src/lib/engine/__tests__/omni-analysis-verbatim.test.ts
autonomous: true
requirements: [R1, R8]
must_haves:
  truths:
    - "OmniAnalysisZodSchema accepts a model response carrying hook_verbatim + per-segment spoken_text/on_screen_text"
    - "OmniAnalysisZodSchema still validates a response WITHOUT any verbatim fields (backward-compat)"
    - "The exported SegmentSchema (SegmentGrid) carries spoken_text/on_screen_text so per-segment verbatim survives the normalizeSegments transport into the aggregator"
    - "Zod rejects an over-cap verbatim string (>280 hook / >500 segment)"
    - "The Omni system prompt instructs verbatim emission + all four D-04 fidelity rules + the D-02 null-vs-[inaudible] contract"
    - "A failing-first verbatim test file exists guarding schema accept/reject, prompt literals, the assembly hop, and the null-vs-[inaudible] contract"
    - "temperature: 0 + seed: QWEN_SEED at the Omni call site are unchanged"
  artifacts:
    - path: "src/lib/engine/qwen/schemas.ts"
      provides: "hook_verbatim top-level field + per-segment spoken_text/on_screen_text on BOTH the INLINE OmniAnalysisZodSchema.segments shape (validator) AND the exported SegmentSchema (transport)"
      contains: "hook_verbatim"
    - path: "src/lib/engine/qwen/omni-analysis.ts"
      provides: "verbatim instructions + fidelity/absence rules in buildSystemPrompt"
      contains: "hook_verbatim"
    - path: "src/lib/engine/__tests__/omni-analysis-verbatim.test.ts"
      provides: "Wave 0 regression test: schema, prompt literal, assembly hop, null-vs-[inaudible]"
      contains: "hook_verbatim"
  key_links:
    - from: "src/lib/engine/qwen/omni-analysis.ts buildSystemPrompt"
      to: "OmniAnalysisZodSchema"
      via: "JSON template field names match Zod field names (hook_verbatim, spoken_text, on_screen_text)"
      pattern: "hook_verbatim"
    - from: "OmniAnalysisZodSchema.segments inline shape"
      to: "exported SegmentSchema / SegmentGrid"
      via: "per-segment spoken_text/on_screen_text declared on BOTH so normalizeSegments preserves them to the aggregator"
      pattern: "spoken_text"
---

<objective>
Add the verbatim CONTRACTS — the Zod schema fields and the system-prompt instructions that make Omni emit the actual words — plus the Wave 0 regression test that guards every downstream hop. This is contracts-first: Plan 02 threads these fields through assembly to aggregator to PredictionResult to persistence against the shape defined here, and the test written here is what proves the assembly hop (the emotion_arc bug) does not recur.

Additive-only (D-01): every edit ADDS fields. No existing 0-10 judgment field, score-blend input, or sampling parameter is removed or repointed.

Purpose: R1 begins at the sensor — Omni must be told to transcribe and the schema must accept the transcription. Without the contract, there is nothing to thread. Critically, per-segment verbatim must be declared on the EXPORTED SegmentSchema too (not just the inline validator shape) — `SegmentGrid = z.infer<SegmentSchema>` is the type that rides through `normalizeSegments` into the aggregator (`pipelineResult.segments`), so a field absent from SegmentSchema is stripped before persistence (the segment-axis version of the emotion_arc drop).
Output: extended OmniAnalysisZodSchema (inline segments + hook_verbatim), extended SegmentSchema, extended buildSystemPrompt, and omni-analysis-verbatim.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-omni-verbatim/02-CONTEXT.md
@.planning/phases/02-omni-verbatim/02-RESEARCH.md
@.planning/phases/02-omni-verbatim/02-PATTERNS.md
@.planning/phases/02-omni-verbatim/02-VALIDATION.md

<interfaces>
<!-- Extracted from current repo source (verified 2026-06-04). Use directly — no codebase exploration needed. -->

src/lib/engine/qwen/schemas.ts:
- :56  EmotionArcPointSchema = z.object({...})            ← precedent shape
- :67  SegmentSchema = z.object({ t_start, t_end, visual_event(max 200), audio_event(max 200), scene_boundary_reason(max 300).optional(), is_hook_zone?, idx? })
       ← EXPORTED shape. :76 `export type SegmentGrid = z.infer<typeof SegmentSchema>`.
       normalizeSegments returns SegmentGrid[] and pipelineResult.segments is SegmentGrid[] — so per-segment verbatim MUST be declared HERE too, or it is stripped before the aggregator sees it.
- :120 OmniAnalysisZodSchema = z.object({...})
- :152 emotion_arc: z.array(EmotionArcPointSchema).optional()   ← .optional() backward-compat precedent
- :157 segments: z.array(z.object({ t_start, t_end, visual_event(max 200), audio_event(max 200), scene_boundary_reason(max 300).optional() })).optional()
       ← INLINE shape: the VALIDATOR Omni output is parsed against (Pitfall 4). Extend this for parse-time acceptance.
- :106 audio_description max 280, :45 rationale max 300   ← string-cap idiom precedent

src/lib/engine/qwen/normalize-segments.ts:
- normalizeSegments(data.segments, durationSeconds): SegmentGrid[] — reconstructs segment objects (some paths spread `...nextSeg`, synthetic fallback paths at :182/:195/:211 build fresh). Per-segment verbatim must survive this (Plan 02 Task 1 handles the preserve; the SegmentSchema declaration here makes SegmentGrid carry the field).

src/lib/engine/qwen/omni-analysis.ts:
- :54-147 buildSystemPrompt — JSON template + "Rules for ..." blocks
- :114 "emotion_arc": [...] template block; :135-138 "emotion_arc is REQUIRED..." rules; :141 "Rules for segments:"
- :195 temperature: 0   :196 seed: QWEN_SEED   ← MUST stay unchanged (R8)
- :221 normalizeSegments call; :236-258 assembly literal (Plan 02 territory — do not edit here)

src/lib/engine/__tests__/omni-analysis-emotion-arc.test.ts:
- :36-86 makeOmniResponse fixture helper
- :88-162 schema validation tests
- :170-208 assembly-hop regression block (THE template to clone)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the verbatim Wave 0 regression test (failing-first)</name>
  <files>src/lib/engine/__tests__/omni-analysis-verbatim.test.ts</files>
  <read_first>
    - src/lib/engine/__tests__/omni-analysis-emotion-arc.test.ts (the exact clone source — fixture :36-86, schema tests :88-162, assembly regression :170-208)
    - src/lib/engine/qwen/schemas.ts (:120 OmniAnalysisZodSchema, :157 inline segments, :67 exported SegmentSchema)
    - .planning/phases/02-omni-verbatim/02-VALIDATION.md (Per-Task Verification Map — the behaviors this test must cover)
  </read_first>
  <behavior>
    - Schema ACCEPT: a response with hook_verbatim {spoken_words, on_screen_text} + segments[].{spoken_text, on_screen_text} parses via OmniAnalysisZodSchema.safeParse to success.
    - Schema BACKWARD-COMPAT: a response with NO verbatim fields still parses to success (.optional()).
    - SegmentSchema ACCEPT: the exported SegmentSchema parses a segment carrying spoken_text/on_screen_text (so SegmentGrid carries them through normalizeSegments).
    - Schema CAP REJECT: spoken_words longer than 280, or segment spoken_text longer than 500, gives safeParse failure.
    - PROMPT LITERAL: buildSystemPrompt() output string contains "hook_verbatim", "spoken_text", "on_screen_text", and the fidelity-rule cues ("[inaudible]", "translate", "null").
    - ASSEMBLY HOP (the regression): after analyzeVideoWithOmni runs against a mocked model emitting hook_verbatim + per-segment text, (out.geminiResult.analysis as unknown as { hook_verbatim? }).hook_verbatim is defined and carries the spoken_words string, AND out.segments[0] carries the spoken_text it emitted (proving per-segment survives normalizeSegments). Mirror emotion_arc test :170-208.
    - ASSEMBLY BACKWARD-COMPAT: model omits hook_verbatim, analysis.hook_verbatim is undefined (no crash).
    - NULL-vs-[inaudible] CONTRACT: silent fixture gives hook_verbatim.spoken_words === null (NOT "[inaudible]", NOT a description like "[music plays]"); unclear-speech fixture gives hook_verbatim.spoken_words containing "[inaudible]" and NOT null.
  </behavior>
  <action>
    Clone omni-analysis-emotion-arc.test.ts to omni-analysis-verbatim.test.ts. Reuse its mock setup (mocked OpenAI client) and adapt its makeOmniResponse fixture helper to take flags withVerbatim, silent, hasUnclearSpeech and inject hook_verbatim plus segments[0].spoken_text/on_screen_text accordingly. Field names MUST be exactly hook_verbatim, spoken_words, on_screen_text, spoken_text. Use cap values 280 (hook) and 500 (segment) in the over-cap reject tests. Add a SegmentSchema-accept case (import the exported SegmentSchema, parse a segment with spoken_text/on_screen_text). The assembly-hop test reads hook_verbatim off geminiResult.analysis via the as-unknown-as narrow cast AND reads spoken_text off out.segments[0] — the identical access paths the aggregator will use in Plan 02 (hook off analysis, per-segment off pipelineResult.segments). This test is written BEFORE the schema/prompt edits in Tasks 2-3, so the schema/prompt-dependent cases (accept, prompt-literal, assembly) START RED and turn GREEN as Tasks 2-3 land. Do not weaken assertions to make them pass prematurely.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/omni-analysis-verbatim.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists with all behaviors above as discrete it(...) cases, including the SegmentSchema-accept and the per-segment-survives-assembly cases. Schema/prompt/assembly cases fail (RED) until Tasks 2-3 land; the JSON-parse-only null-vs-[inaudible] fixture assertions may already pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend OmniAnalysisZodSchema + exported SegmentSchema with verbatim fields</name>
  <files>src/lib/engine/qwen/schemas.ts</files>
  <read_first>
    - src/lib/engine/qwen/schemas.ts (:152 emotion_arc .optional() precedent; :157 INLINE segments object — the validator; :67 exported SegmentSchema / :76 SegmentGrid — the transport type; :106/:45 cap idiom)
    - src/lib/engine/qwen/normalize-segments.ts (returns SegmentGrid[]; confirms per-segment fields must live on SegmentSchema to survive)
    - .planning/phases/02-omni-verbatim/02-PATTERNS.md (section 1 schemas.ts — exact field shapes)
  </read_first>
  <behavior>
    - hook_verbatim accepts { spoken_words, on_screen_text } each a string max 280, nullable, optional.
    - Each segment in the INLINE OmniAnalysisZodSchema.segments array accepts spoken_text + on_screen_text, each a string max 500, nullable, optional (parse-time acceptance of model output).
    - The exported SegmentSchema ALSO accepts spoken_text + on_screen_text (same shape) so SegmentGrid carries them through normalizeSegments into the aggregator.
    - A response/segment without any of these fields still validates.
    - Over-cap strings are rejected.
  </behavior>
  <action>
    Add a top-level hook_verbatim field to OmniAnalysisZodSchema (after the emotion_arc field at :152), shaped z.object with spoken_words and on_screen_text each z.string().max(280).nullable().optional(), the whole object .optional(). Add spoken_text and on_screen_text, each z.string().max(500).nullable().optional(), to the INLINE z.object inside OmniAnalysisZodSchema.segments at :157 — the validator the Omni output is parsed against (Pitfall 4: editing only the wrong shape silently strips the field at parse). ALSO add the SAME two fields (spoken_text, on_screen_text, z.string().max(500).nullable().optional()) to the EXPORTED SegmentSchema at :67. This is the BLOCKER fix: SegmentGrid = z.infer<SegmentSchema> is the type normalizeSegments returns and pipelineResult.segments carries — a field absent from SegmentSchema is stripped before the aggregator can pluck per-segment verbatim (the segment-axis emotion_arc drop). Both shapes must declare the fields: the inline shape for parse-time acceptance, SegmentSchema for transport. Cap values 280/500 per D-04.4. The .nullable() is mandatory so silence persists as honest null (D-02), distinct from absent. The .optional() mirrors emotion_arc backward-compat (D-02.3). Do not touch Wave0FieldsSchema or any existing field. Additive only.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/omni-analysis-verbatim.test.ts -t "schema\|Segment" 2>&1 | tail -15</automated>
  </verify>
  <done>Schema accept, SegmentSchema-accept, backward-compat, and over-cap-reject tests from Task 1 pass. npx tsc --noEmit reports no new errors in schemas.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend buildSystemPrompt with verbatim instructions + fidelity rules</name>
  <files>src/lib/engine/qwen/omni-analysis.ts</files>
  <read_first>
    - src/lib/engine/qwen/omni-analysis.ts (:54-147 buildSystemPrompt; :114 emotion_arc template block; :135-138 "emotion_arc is REQUIRED" rules block; :141 "Rules for segments:"; :195-196 temp0+seed — DO NOT TOUCH)
    - .planning/phases/02-omni-verbatim/02-PATTERNS.md (section 2 omni-analysis.ts — JSON template + "Rules for verbatim")
  </read_first>
  <behavior>
    - buildSystemPrompt() output contains the JSON template keys "hook_verbatim", "spoken_words", "on_screen_text", "spoken_text".
    - Output contains all four D-04 rules as prose: transcribe in spoken language / do NOT translate (D-04.1); use [inaudible] ONLY for present-but-unintelligible speech and null when there is no speech/text (D-02 / D-04.2); preserve casing, punctuation, emoji (D-04.3); cap cues ~280 hook / ~500 segment (D-04.4).
    - temperature: 0 and seed: QWEN_SEED at :195-196 are byte-identical to before.
  </behavior>
  <action>
    In the JSON template inside buildSystemPrompt, add a hook_verbatim block with spoken_words and on_screen_text keys (after the emotion_arc block, ~:117) and add spoken_text + on_screen_text keys to the template segments[] example object. Add a "Rules for verbatim:" block (mirroring the existing "emotion_arc is REQUIRED..." rules at :135-138 and "Rules for segments:" at :141) encoding D-04.1 (original language, do NOT translate), D-02/D-04.2 ([inaudible] ONLY for present-but-unintelligible speech; null when there is no speech/text — NEVER describe sound, NEVER write a music description), D-04.3 (preserve casing/punctuation/emoji in on_screen_text), D-04.4 (cap hook ~280, segment ~500). Discretion (per RESEARCH Open Q1): emit hook_verbatim AND segment text both (cheap; decouples P3 from segment indexing). Optional anchoring example allowed. Verbatim instructions live in the SYSTEM prompt (stable/cached slot) — do not move them to the volatile user prompt. DO NOT modify the sampling params at :195-196 (R8). Do not touch the assembly literal at :236-258 — that is Plan 02.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/omni-analysis-verbatim.test.ts -t "prompt" 2>&1 | tail -15</automated>
    <automated>grep -v '^#' src/lib/engine/qwen/omni-analysis.ts | grep -c 'temperature: 0\|seed: QWEN_SEED'</automated>
  </verify>
  <done>Prompt-literal tests pass. The grep count for temperature: 0 / seed: QWEN_SEED is unchanged (2). npx tsc --noEmit clean for this file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Qwen-Omni model to backend | Model-generated verbatim strings are untrusted input; they cross into the Zod parse at omni-analysis.ts before assembly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering/DoS | hook_verbatim + per-segment text fields (token budget) | mitigate | z.string().max(280)/max(500) ceilings (D-04.4) on every new field, on BOTH the inline shape and SegmentSchema; Zod rejects oversized output, triggering MAX_RETRIES re-prompt rather than persisting unbounded strings. Tested by the over-cap reject case in Task 1. |
| T-2-02 | Spoofing/Integrity | model fabricates a verbatim line for silent content | mitigate | Prompt absence-contract (D-02): null for no speech/text, never a paraphrase; [inaudible] reserved for present-but-unintelligible. Distinct-state assertion in Task 1. (Downstream [inaudible]-leak into rewrites is OUT OF SCOPE — flagged to P3.) |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Standard Stack: zero installs). No legitimacy gate required. |
</threat_model>

<verification>
- `npx vitest run src/lib/engine/__tests__/omni-analysis-verbatim.test.ts` — all cases green after Tasks 2-3.
- `npx tsc --noEmit` — no new type errors.
- R8 guard: `grep -v '^#' src/lib/engine/qwen/omni-analysis.ts | grep -c 'temperature: 0\|seed: QWEN_SEED'` returns 2 (unchanged).
- Additive-only guard: `git diff src/lib/engine/qwen/schemas.ts` shows only ADDED lines for verbatim (on both the inline shape and SegmentSchema), no removed/repointed existing fields.
</verification>

<success_criteria>
- OmniAnalysisZodSchema accepts verbatim, rejects over-cap, validates without it.
- Exported SegmentSchema carries spoken_text/on_screen_text so SegmentGrid transports per-segment verbatim through normalizeSegments.
- buildSystemPrompt emits the four field names + all four D-04 rules + the D-02 contract.
- omni-analysis-verbatim.test.ts exists and is green for schema + prompt cases; the assembly-hop case is wired against the exact access paths Plan 02 will satisfy (hook off analysis, per-segment off segments).
- Sampling params untouched (R8).
</success_criteria>

<output>
Create `.planning/phases/02-omni-verbatim/02-01-SUMMARY.md` when done.
</output>
