---
phase: 03-honesty-moat-gallery-proof-conversion
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/numen-landing/verdict-throne.tsx
  - src/components/numen-landing/reading-gallery.tsx
  - public/images/landing/gallery/
autonomous: false
requirements: [GALLERY-01, GALLERY-02, CONTENT-02]
must_haves:
  truths:
    - "A visitor browses ≥3 gallery cards across ≥3 distinct creator niches, each a content centerpiece (real still + band + why), not a feature diagram"
    - "The gallery shows a verdict RANGE — at least one good, one mixed, one bad band (honesty by breadth), never all-good"
    - "Each card uses an opaque Surface plate (no glass-over-photo) and every image has a non-empty factual in-voice alt"
    - "D-06: the gallery ships structurally complete now on placeholder stills across ≥3 niches (final rights-cleared assets deferred to Phase 4); D-07: stills come from user-supplied short-video keyframes via a non-blocking checkpoint, reusing VerdictThrone/VerdictSwatch in luma staging"
  artifacts:
    - path: "src/components/numen-landing/reading-gallery.tsx"
      provides: "GALLERY-01/02 luma gallery, ≥3 Surface cards, VerdictThrone range + PillChip niche tags"
      contains: "VerdictThrone"
    - path: "src/components/numen-landing/verdict-throne.tsx"
      provides: "parametrized verdict+label+why (default good = Phase-2 copy) so cards show a range"
      contains: "verdict"
  key_links:
    - from: "reading-gallery.tsx"
      to: "public/images/landing/gallery/*.webp + hero/keyframe.webp"
      via: "next/image static imports on Surface plates"
      pattern: "next/image"
    - from: "reading-gallery.tsx"
      to: "VerdictThrone (good/mixed/bad)"
      via: "one throne per card with varying verdict prop"
      pattern: "verdict="
---

<objective>
Build the `#gallery` luma real-Readings gallery (GALLERY-01/02): a responsive grid of ≥3 gallery-quality cards across ≥3 distinct creator niches, each a real still + a verdict band + one-line why + a niche tag — showing a verdict RANGE (good/mixed/bad) for honesty by breadth (D-06/Pattern 2). Parametrize `VerdictThrone` so the band+why+plate contract stays in one place while cards vary. Source placeholder stills via the Phase-2 ffmpeg→cwebp pipeline (D-07, non-blocking).

Purpose: prove specificity over claims — real verdicts on real videos, including the ones that need work (the gallery must NOT only show wins).
Output: a parametrized `VerdictThrone`, `reading-gallery.tsx`, and `public/images/landing/gallery/` stills — GREEN against the Plan-01 gallery scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-honesty-moat-gallery-proof-conversion/03-UI-SPEC.md
@.planning/phases/03-honesty-moat-gallery-proof-conversion/03-PATTERNS.md
@.planning/phases/03-honesty-moat-gallery-proof-conversion/03-RESEARCH.md
@.planning/VOICE.md

<interfaces>
<!-- Current VerdictThrone is hard-coded to good + fixed copy. Task 1 parametrizes it. -->

CURRENT src/components/numen-landing/verdict-throne.tsx:
```
export interface VerdictThroneProps { className?: string }
export function VerdictThrone({ className }): JSX.Element  // hard-coded verdict="good" + Phase-2 label/why
```

TARGET (parametrized — default preserves Phase-2 hero/explainer call sites):
```
export interface VerdictThroneProps {
  verdict?: "good" | "mixed" | "bad";   // default "good"
  label?: string;                        // default "This will likely land."
  why?: string;                          // default Phase-2 why line
  className?: string;
}
```

From src/components/numen/{surface,pill-chip,verdict-swatch}.tsx:
```
Surface({ children, className, style, as }): JSX.Element            // opaque plate, bg-panel + border-border
PillChip({ children, icon, intent, onClick, disabled, className })  // intent="instant" niche tag ("use client")
VerdictSwatch({ verdict, size, children, className })               // literal classes, used INSIDE VerdictThrone
```

Existing still (reuse as niche-1, 720×1280 WebP):
```
import heroKeyframe from "@/../public/images/landing/hero/keyframe.webp";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parametrize VerdictThrone (verdict + label + why; default = Phase-2 good)</name>
  <files>src/components/numen-landing/verdict-throne.tsx</files>
  <behavior>
    - `<VerdictThrone />` with no props renders EXACTLY the current Phase-2 output (verdict="good", "This will likely land.", the Phase-2 why) — existing hero + how-it-works call sites unchanged.
    - `<VerdictThrone verdict="mixed" label="This could go either way." why="..." />` renders the mixed band + the passed label + why.
    - `<VerdictThrone verdict="bad" .../>` renders the bad band.
    - Band uses VerdictSwatch literal classes (no `bg-${verdict}` interpolation); label stays on the SOLID bg-panel plate (APCA mitigation preserved).
  </behavior>
  <read_first>
    - src/components/numen-landing/verdict-throne.tsx (the file being modified — preserve the plate + APCA-mitigation doc comment + the bg-panel/border-border plate)
    - src/components/numen/verdict-swatch.tsx (the `verdict` prop it forwards — good/mixed/bad, literal classes)
    - src/components/numen-landing/how-it-works.tsx (a call site that uses `<VerdictThrone />` with NO props — must keep working after the change)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-UI-SPEC.md (§Color "VerdictThrone reuse caveat" — option (a) parametrize is preferred; APCA Lc ≥ 60 plate discipline for ALL three bands)
    - src/components/numen-landing/__tests__/voice.test.tsx (renders `<VerdictThrone />` no-props — must stay GREEN)
  </read_first>
  <action>Edit `verdict-throne.tsx`: extend `VerdictThroneProps` with optional `verdict?: "good"|"mixed"|"bad"` (default `"good"`), `label?: string` (default `"This will likely land."`), `why?: string` (default the existing Phase-2 line "Strong hook in the first 2 seconds — tighten the middle and it lands."). Forward `verdict` to `<VerdictSwatch verdict={verdict} size="md" />`; render `{label}` in the existing bold `text-sm md:text-base font-bold text-text` span and `{why}` in the existing muted `<p>`. Keep the SOLID `bg-panel border-border rounded-[12px]` plate and the doc comment's APCA rationale (the label stays on the plate, not the band, for all three verdicts). Do NOT change the no-props rendered output. Color by token name only; no `bg-${verdict}` (VerdictSwatch owns the literal classes).</action>
  <verify>
    <automated>npx vitest run src/components/numen-landing/__tests__/voice.test.tsx src/components/numen-landing/__tests__/hero.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `VerdictThroneProps` now declares optional `verdict`, `label`, `why` (`grep -c 'verdict?\|label?\|why?' src/components/numen-landing/verdict-throne.tsx` ≥ 3).
    - No `bg-${` interpolation introduced (`grep -c 'bg-\${' ...` = 0); the band still goes through `VerdictSwatch verdict={verdict}`.
    - All existing tests that render `<VerdictThrone />` no-props (voice + hero/how-it-works) still pass GREEN (no-props output byte-identical in intent).
    - The bg-panel plate + APCA doc comment preserved (`grep -c 'bg-panel' ...` ≥ 1).
  </acceptance_criteria>
  <done>VerdictThrone parametrized with verdict/label/why defaulting to the Phase-2 good copy; existing call sites unchanged; ready for the gallery to show a range.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Source ≥2 more niche stills (non-blocking, D-07)</name>
  <files>public/images/landing/gallery/</files>
  <what-built>The gallery needs stills across ≥3 distinct niches. niche-1 reuses the existing `public/images/landing/hero/keyframe.webp` (comedy-skit, 720×1280). This checkpoint requests ~2 more short creator clips (distinct niches, e.g. fitness, cooking) so the gallery ships on REAL distinct stills. Per D-07 this is NON-BLOCKING: if you supply nothing, Task 3 varies the existing keyframe across cards and the gallery still ships structurally complete.</what-built>
  <read_first>
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-RESEARCH.md (Target 4 — the exact ffmpeg→cwebp commands + the 720×1280 / -q 82 target spec)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-CONTEXT.md (D-07 — non-blocking, vary the existing still if no clips)
  </read_first>
  <action>If the user supplies clip paths: run `mkdir -p public/images/landing/gallery`, then per clip run `ffmpeg -ss 00:00:01.5 -i "<clip>" -frames:v 1 -q:v 2 /tmp/gallery-frame.png -y` followed by `cwebp -q 82 -resize 720 0 /tmp/gallery-frame.png -o public/images/landing/gallery/niche-<name>.webp` (RESEARCH Target 4 — matches the existing 720×1280 hero still spec), and verify each output with `file`. If the user replies "proceed with placeholders", create no files and record the decision in the SUMMARY as a one-line note — Task 3 will vary `hero/keyframe.webp` across the cards. Per D-07 this checkpoint is NON-BLOCKING: the gallery ships either way; do not gate Task 3 on it.</action>
  <how-to-verify>
    1. If you have ~2 short creator videos (distinct niches), drop their file paths here, OR place the files somewhere readable and name them.
    2. The executor runs the ffmpeg→cwebp extraction (see <action>) into the net-new `public/images/landing/gallery/` dir.
    3. Confirm each output is a 720-wide portrait WebP (`file public/images/landing/gallery/*.webp`).
    4. If you supply NOTHING: reply "proceed with placeholders" — Task 3 varies `keyframe.webp` across the ≥3 cards and emits real-shaped band+why per niche. The gallery is NOT gated on this.
  </how-to-verify>
  <acceptance_criteria>
    - EITHER ≥2 new `public/images/landing/gallery/niche-*.webp` files exist (720-wide portrait WebP, verified via `file`), OR the user explicitly approved proceeding on the varied keyframe (recorded in the SUMMARY as a one-line decision).
    - The gallery never renders an empty/"no readings" state regardless of which path is taken.
  </acceptance_criteria>
  <resume-signal>Type "approved" / supply clip paths, or "proceed with placeholders".</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build reading-gallery.tsx (≥3 cards, verdict range, luma staging)</name>
  <files>src/components/numen-landing/reading-gallery.tsx</files>
  <behavior>
    - Renders ≥3 cards in a responsive grid (single col mobile, `md:grid-cols-3` desktop); `getAllByRole("img")` length ≥ 3.
    - Each card = Surface plate → next/image real still → a VerdictThrone band+label+why → a PillChip niche tag.
    - The cards span a verdict RANGE: at least one good, one mixed, one bad band present in the markup (bg-verdict-good + bg-verdict-mixed + bg-verdict-bad all appear).
    - Every `<img>` has a non-empty factual in-voice alt; no glass-over-photo (opaque Surface only).
  </behavior>
  <read_first>
    - src/components/numen-landing/how-it-works.tsx (EXACT structural analog: `mt-8 grid gap-6 md:grid-cols-3 md:gap-8`, Surface card with `overflow-hidden rounded-[12px] border border-border` image wrap, next/image with `placeholder="blur"` + `sizes`, the heroKeyframe import path)
    - src/components/numen/surface.tsx (the opaque plate — bg-panel, border-border; NOT glass)
    - src/components/numen/pill-chip.tsx (niche tag — `intent="instant"`, "use client" but safe to render inside an RSC as a child)
    - src/components/numen-landing/verdict-throne.tsx (now parametrized by Task 1 — pass verdict/label/why per card)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-UI-SPEC.md (§2 gallery card anatomy, §Color verdict RANGE rule "at minimum one mixed and one bad card", §Copywriting gallery niche tags + verdict labels + per-card why lines + image alts, "no card hover theater")
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-CONTEXT.md (D-06/D-07 — placeholder stills, vary keyframe if no clips)
    - src/components/numen-landing/__tests__/reading-gallery.test.tsx (the Plan-01 scaffold to turn GREEN: ≥3 imgs, non-empty alts, all three verdict tokens)
  </read_first>
  <action>Create `src/components/numen-landing/reading-gallery.tsx` as a static RSC, exporting `ReadingGallery()`, emitting NO heading (the `#gallery` h2 is on the slot). Lead with a `mt-6 md:mt-8` intro `<p>` from UI-SPEC §Copywriting ("Real verdicts on real videos — the good, the mixed, and the ones that need work. We don't only show you the wins."). Then a `mt-8 grid gap-6 md:grid-cols-3 md:gap-8` grid copying the how-it-works grid. Import `heroKeyframe` from `@/../public/images/landing/hero/keyframe.webp` and, IF Task 2 produced them, the `@/../public/images/landing/gallery/niche-*.webp` stills; otherwise reuse heroKeyframe across cards (D-07). Define a typed card data array of ≥3 entries (recommend 4 per UI-SPEC default), each `{ src, alt, niche, verdict, label, why }`, spanning all three verdicts — at minimum one `good`, one `mixed`, one `bad`. Per card render a `<Surface className="flex flex-col gap-4 p-4 md:p-6">` with: an `overflow-hidden rounded-[12px] border border-border` wrap holding `<Image src={...} alt={...} placeholder="blur" className="h-auto w-full" sizes="(min-width: 768px) 320px, 100vw" />`; a `<VerdictThrone verdict={verdict} label={label} why={why} />`; a `<PillChip intent="instant">{niche}</PillChip>` niche tag. Copy per UI-SPEC §Copywriting: niche tags ("Comedy"/"Fitness"/"Cooking" or the real niches), verdict labels (good "This will likely land." · mixed "This could go either way." · bad "This needs work first."), specific one-line whys (one real strength + one fixable note each — luma specificity, VOICE-clean, never a number), and factual in-voice alts ("A real Reading of a comedy creator's video showing an honest verdict and why."). NO card hover theater (at most `hover:bg-panel-2`, no translate, no border change, no scroll-reveal). Color by token name only; no hex; no `backdrop-filter` class; no `bg-${verdict}` (the throne/swatch own literal classes).</action>
  <verify>
    <automated>npx vitest run src/components/numen-landing/__tests__/reading-gallery.test.tsx 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - reading-gallery.test.tsx (Plan 01) passes GREEN: ≥3 imgs, every img non-empty alt, all three verdict tokens present.
    - `grep -c 'VerdictThrone' src/components/numen-landing/reading-gallery.tsx` ≥ 3 (or a `.map` over ≥3 entries) AND the data spans `good`+`mixed`+`bad` (the test's three-token assertion confirms at least one of each).
    - Every Image has a non-empty `alt` (`grep -c 'alt=' ...` ≥ 3, no `alt=""`).
    - Uses `Surface` plates and `next/image`; no glass (`grep -c 'backdrop-filter\|backdropFilter\|Glass' ...` = 0); no hex (`grep -cE '#[0-9a-fA-F]{6}' ...` = 0); no `<h1`/`<h2`.
  </acceptance_criteria>
  <done>reading-gallery.tsx renders ≥3 luma cards across ≥3 niches with a good/mixed/bad verdict range on opaque Surface plates, real-shaped band+why+niche tag per card, all imgs alt-tagged; Plan-01 gallery scaffold GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time still extraction → public/ | ffmpeg/cwebp run locally by the agent on user-supplied clips; output committed to `public/`. No runtime trust boundary; no user input at request time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | gallery stills (real creator content) | accept | Stills are intentionally public marketing assets; user supplies clips they consent to publish (D-07). No PII beyond the depicted public content. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No installs — next/image, lucide, tailwind-variants already present (RESEARCH: zero new packages). ffmpeg/cwebp are system binaries verified on PATH. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run src/components/numen-landing/__tests__/reading-gallery.test.tsx` GREEN.
- The Plan-01 voice gate ban scan for `reading-gallery` (its slice) goes GREEN (no banned token in gallery copy).
- `file public/images/landing/gallery/*.webp` (if clips supplied) → 720-wide portrait WebP; OR SUMMARY records "proceeded on varied keyframe".
- `npm run build` succeeds.
</verification>

<success_criteria>
- GALLERY-01: ≥3 cards across ≥3 distinct niches.
- GALLERY-02: each card is a content centerpiece (Surface + real still + band+why), not a feature diagram; verdict RANGE proves honesty by breadth.
- CONTENT-02: every gallery string passes VOICE.
</success_criteria>

<output>
Create `.planning/phases/03-honesty-moat-gallery-proof-conversion/03-03-SUMMARY.md` when done
</output>
