---
phase: 03-honesty-moat-gallery-proof-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/__tests__/actions.test.ts
  - src/lib/__tests__/waitlist-count.test.ts
  - src/components/numen-landing/__tests__/honesty-comparison.test.tsx
  - src/components/numen-landing/__tests__/reading-gallery.test.tsx
  - src/components/numen-landing/__tests__/social-proof.test.tsx
  - src/components/numen-landing/__tests__/proof-placement.test.tsx
  - src/app/(marketing)/__tests__/page-headings.test.tsx
  - src/components/numen-landing/__tests__/voice.test.tsx
autonomous: true
requirements: [CTA-02, PROOF-01, PROOF-02, TRUST-01, TRUST-02, GALLERY-01, GALLERY-02, CONTENT-02]
must_haves:
  truths:
    - "Every Phase-3 requirement has a RED test scaffold that fails on the missing module, not on an assertion typo"
    - "The voice gate extends to the four new copy-bearing components and positively asserts the sanctioned rival strings live only in honesty-comparison"
    - "PROOF-02 'one source, two surfaces' and the single-h1 invariant each have an automated scaffold"
  artifacts:
    - path: "src/app/(marketing)/__tests__/actions.test.ts"
      provides: "CTA-02 server-action seam: dup-as-success, honeypot, invalid-email"
      contains: "joinWaitlist"
    - path: "src/lib/__tests__/waitlist-count.test.ts"
      provides: "PROOF-01 count read: RPC number passthrough, error→0"
      contains: "getWaitlistCount"
    - path: "src/components/numen-landing/__tests__/honesty-comparison.test.tsx"
      provides: "TRUST-01/02 semantic table + scoped rival strings"
      contains: "scope"
    - path: "src/components/numen-landing/__tests__/reading-gallery.test.tsx"
      provides: "GALLERY-01/02 ≥3 cards, alts, verdict range"
      contains: "getAllByRole"
    - path: "src/components/numen-landing/__tests__/social-proof.test.tsx"
      provides: "PROOF-01/D-09 threshold guard, never '0 creators'"
      contains: "THRESHOLD"
    - path: "src/components/numen-landing/__tests__/proof-placement.test.tsx"
      provides: "PROOF-02/D-10 one-source-two-surfaces: ProofStrip + SocialProof get the SAME count"
      contains: "count"
    - path: "src/app/(marketing)/__tests__/page-headings.test.tsx"
      provides: "single-h1 invariant: exactly one level-1 heading after slots filled"
      contains: "level: 1"
  key_links:
    - from: "voice.test.tsx"
      to: "the four new components + honesty-comparison"
      via: "render + ban-scan others, positive-assert rival strings in honesty-comparison only"
      pattern: "honesty-comparison"
    - from: "proof-placement.test.tsx"
      to: "ProofStrip + SocialProof"
      via: "render both with one count value, assert both surface it"
      pattern: "count"
---

<objective>
Wave 0 — author the RED Nyquist test scaffolds for every Phase-3 requirement seam BEFORE any implementation, and extend the page-wide voice gate to cover the four new copy-bearing components. This is the feedback infrastructure all later waves sample against (RESEARCH §Validation Architecture / 03-VALIDATION.md).

Purpose: every later task gets a fast automated signal; the voice gate stays meaningful even though the comparison section intentionally contains the sanctioned rival strings (RESEARCH Pitfall 5 / D-05). PROOF-02 (one source, two surfaces) and the single-h1 invariant get explicit automated coverage rather than manual-only checks.
Output: seven new test files + an extended voice.test.tsx, all RED (failing on missing modules), ready to go GREEN as Waves 2–3 ship.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-honesty-moat-gallery-proof-conversion/03-RESEARCH.md
@.planning/phases/03-honesty-moat-gallery-proof-conversion/03-PATTERNS.md
@.planning/phases/03-honesty-moat-gallery-proof-conversion/03-VALIDATION.md
@.planning/VOICE.md

<interfaces>
<!-- The seams these tests target. Executor must NOT invent different signatures; -->
<!-- Waves 2–3 implement against exactly these shapes. -->

From src/app/(marketing)/actions.ts (Plan 05, not yet built — test imports it):
```
export type WaitlistState =
  | { status: "idle" }
  | { status: "success" }
  | { status: "error"; message: string };
export async function joinWaitlist(prev: WaitlistState, formData: FormData): Promise<WaitlistState>;
```

From src/lib/waitlist-count.ts (Plan 05):
```
export const getWaitlistCount: () => Promise<number>;  // unstable_cache wrapped
```

From the components (Plans 02/03/05) — prop contracts the tests assert against:
```
HonestyComparison()                        // RSC, no props — semantic <table>
ReadingGallery()                           // RSC, no props — ≥3 Surface cards
SocialProof({ count }: { count: number })  // RSC — D-09 threshold guard
ProofStrip({ count }: { count: number })   // RSC — D-10 early thin strip
CtaSection()                               // RSC — hosts WaitlistForm
```

Existing voice gate ban list (src/components/numen-landing/__tests__/voice.test.tsx, EXTEND):
```
const banned: RegExp[] = [/%/, /\bviral\b/i, /virality/i, /guaranteed/i,
  /\bApollo\b/, /\bfold\b/, /\bOmni\b/i, /\bpipeline\b/, /\bmodel\b/i, /accuracy/i, /predict/i];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the data-seam RED scaffolds (actions + count read)</name>
  <files>src/app/(marketing)/__tests__/actions.test.ts, src/lib/__tests__/waitlist-count.test.ts</files>
  <read_first>
    - src/components/numen-landing/__tests__/voice.test.tsx (the `/** @vitest-environment happy-dom */` header + import-inside-test pattern this repo uses for RED scaffolds)
    - src/app/(onboarding)/signup/actions.ts (the action shape the future joinWaitlist mirrors — `createClient` from `@/lib/supabase/server`, `formData.get`, `{ error }` destructure)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-RESEARCH.md (Target 2 joinWaitlist contract + §Validation Req→Test rows for CTA-02; Target 3 getWaitlistCount error→0)
    - vitest.config.ts (the `@/` alias + happy-dom + staticImageStub are already configured — confirm, do not re-add)
  </read_first>
  <action>Create `src/app/(marketing)/__tests__/actions.test.ts` mocking `@/lib/supabase/server`'s `createClient` (vi.mock) so `from("waitlist").insert(...)` is a spy. Three RED unit tests against `joinWaitlist` (imported dynamically inside each test so the file is RED on the missing module, per repo scaffold style): (1) honeypot — `FormData` with `company` set → resolves `{ status: "success" }` AND the insert spy is NOT called; (2) invalid email ("notanemail") → `{ status: "error" }` AND insert NOT called; (3) duplicate — insert spy returns `{ error: { code: "23505" } }` → `{ status: "success" }` (dup-as-success, no leak). Create `src/lib/__tests__/waitlist-count.test.ts` mocking `@/lib/supabase/server` so `rpc("waitlist_count")` is a spy: (1) rpc returns `{ data: 42, error: null }` → `getWaitlistCount()` resolves `42`; (2) rpc returns `{ data: null, error: {...} }` → resolves `0` (fail-soft). Use `vi.mock` factory + dynamic `await import(...)` inside each `it`. These import modules that do not exist yet — the files MUST be RED now.</action>
  <verify>
    <automated>npx vitest run "src/app/(marketing)/__tests__/actions.test.ts" "src/lib/__tests__/waitlist-count.test.ts" 2>&1 | grep -qE "Cannot find module|Failed to (load|resolve) (url|import)|actions|waitlist-count" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and reference `joinWaitlist` / `getWaitlistCount` respectively (`grep -l joinWaitlist src/app/\(marketing\)/__tests__/actions.test.ts` and `grep -l getWaitlistCount src/lib/__tests__/waitlist-count.test.ts` both match).
    - Running the two files via vitest FAILS (RED) because the target modules `@/app/(marketing)/actions` and `@/lib/waitlist-count` do not exist yet — failure is a module-resolution error, NOT an assertion-shape typo.
    - actions.test.ts contains all three cases: a honeypot assertion, an invalid-email assertion, and a `23505`→success assertion (`grep -c "23505\|company\|notanemail" ...` ≥ 3).
  </acceptance_criteria>
  <done>actions.test.ts + waitlist-count.test.ts exist, both RED on missing target modules, covering CTA-02 (honeypot/invalid/dup) and PROOF-01 (number passthrough / error→0).</done>
</task>

<task type="auto">
  <name>Task 2: Write the component RED scaffolds (honesty, gallery, social-proof)</name>
  <files>src/components/numen-landing/__tests__/honesty-comparison.test.tsx, src/components/numen-landing/__tests__/reading-gallery.test.tsx, src/components/numen-landing/__tests__/social-proof.test.tsx</files>
  <read_first>
    - src/components/numen-landing/__tests__/voice.test.tsx (happy-dom header + `render(...).container` + dynamic-import scaffold idiom)
    - src/components/numen-landing/how-it-works.tsx (the static-RSC analog the three components mirror — grid, Surface cards, next/image alts)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-UI-SPEC.md (§1 semantic table contract; §2 gallery ≥3 cards + verdict RANGE; §3 D-09 threshold + never "0 creators")
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-RESEARCH.md (§Validation Req→Test rows for TRUST/GALLERY/PROOF)
  </read_first>
  <action>Create three `/** @vitest-environment happy-dom */` test files, each dynamically importing its (not-yet-built) component so it is RED now. `honesty-comparison.test.tsx`: render `HonestyComparison`; assert (a) exactly one `<table>` with a `<caption>` and 2 column `<th scope="col">` headers carrying "Numen" and "Virality-score tools"; (b) the rendered text DOES contain the sanctioned rival strings (positive assertion — they are SUPPOSED to be here, D-05). `reading-gallery.test.tsx`: render `ReadingGallery`; assert (a) `getAllByRole("img")` length ≥ 3; (b) every img has a non-empty `alt`; (c) the rendered markup contains all three verdict band classes `bg-verdict-good`, `bg-verdict-mixed`, `bg-verdict-bad` (verdict RANGE, not all-good — query container.innerHTML). `social-proof.test.tsx`: render `SocialProof` with `count={3}` → assert text does NOT contain "0 creators" AND contains the qualitative-anchor phrase fragment "first" (below-threshold path); render with `count={5000}` → assert text contains "5,000" (above-threshold `toLocaleString`). Define `THRESHOLD` expectation = 50 in a comment so the implementer matches. All three RED on missing modules.</action>
  <verify>
    <automated>npx vitest run src/components/numen-landing/__tests__/honesty-comparison.test.tsx src/components/numen-landing/__tests__/reading-gallery.test.tsx src/components/numen-landing/__tests__/social-proof.test.tsx 2>&1 | grep -qE "Cannot find module|Failed to (load|resolve)" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist; each fails (RED) because its target component module does not exist yet (module-resolution failure, not a render assertion failure).
    - honesty-comparison.test.tsx asserts a single `<table>` + a `<caption>` + the two scoped column headers (`grep -c "scope=\"col\"\|caption\|table" ...` ≥ 3).
    - reading-gallery.test.tsx asserts img count ≥ 3 and references all three verdict tokens (`grep -c "bg-verdict-good\|bg-verdict-mixed\|bg-verdict-bad" ...` = 3).
    - social-proof.test.tsx contains a "0 creators" negative assertion and a `toLocaleString`/"5,000" above-threshold assertion (`grep -c "0 creators\|5,000\|toLocaleString" ...` ≥ 2).
  </acceptance_criteria>
  <done>honesty-comparison / reading-gallery / social-proof test scaffolds exist, all RED on missing modules, encoding the UI-SPEC §1/§2/§3 contracts (semantic table, ≥3 cards + verdict range, D-09 threshold guard).</done>
</task>

<task type="auto">
  <name>Task 3: Write the PROOF-02 + single-h1 invariant scaffolds (proof-placement, page-headings)</name>
  <files>src/components/numen-landing/__tests__/proof-placement.test.tsx, src/app/(marketing)/__tests__/page-headings.test.tsx</files>
  <read_first>
    - src/components/numen-landing/__tests__/voice.test.tsx (happy-dom header + dynamic-import RED-scaffold idiom)
    - src/app/(marketing)/page.tsx (the slots/heading structure the page-headings test renders; hero owns the only h1 — D-10 single-h1 invariant doc comment)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-RESEARCH.md (§Validation Req→Test: the `proof-placement.test.tsx` PROOF-02/D-10 row + the `single-h1` row; Target 3 one-source-two-surfaces shape)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-UI-SPEC.md (§3 D-10 two surfaces / one source; single-h1 invariant in Accessibility Baseline)
  </read_first>
  <action>Create two `/** @vitest-environment happy-dom */` test files, both RED on the not-yet-built modules. `proof-placement.test.tsx` (PROOF-02 / D-10 — one source, two surfaces): dynamically import `ProofStrip` and `SocialProof`; render BOTH with the same explicit prop `count={1234}`; assert each rendered output surfaces that same number (e.g. both `container.textContent` include "1,234" via `toLocaleString`) — proving both surfaces read from ONE count value, not two independent queries. Add a comment noting the page (Plan 05 Task 3) threads a single `getWaitlistCount()` read to both; this scaffold guards the contract at the component level. `page-headings.test.tsx` (single-h1 invariant): dynamically import the `(marketing)` `HomePage` from `@/app/(marketing)/page`; render it (it is `async` after Plan 05 — `render(await HomePage())` inside the `it`); assert `getAllByRole("heading", { level: 1 }).length === 1` (the hero h1 is the page's ONLY top-level heading after all four slots fill). Both files MUST be RED now (ProofStrip/SocialProof and the wired page do not exist yet).</action>
  <verify>
    <automated>npx vitest run src/components/numen-landing/__tests__/proof-placement.test.tsx "src/app/(marketing)/__tests__/page-headings.test.tsx" 2>&1 | grep -qE "Cannot find module|Failed to (load|resolve)" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and are RED on missing modules (ProofStrip/SocialProof for proof-placement; the wired async page for page-headings).
    - proof-placement.test.tsx renders BOTH `ProofStrip` and `SocialProof` with the SAME `count` prop and asserts both surface that number (`grep -c "ProofStrip\|SocialProof" ...` = 2 and a shared-count assertion present).
    - page-headings.test.tsx asserts exactly one level-1 heading (`grep -c "level: 1" ...` ≥ 1 and an `=== 1`/`toHaveLength(1)` assertion).
  </acceptance_criteria>
  <done>proof-placement (PROOF-02 one-source-two-surfaces) + page-headings (single-h1) scaffolds exist, both RED on missing modules; the two warnings now have automated coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Extend the voice gate to the four new components (scoped rival strings)</name>
  <files>src/components/numen-landing/__tests__/voice.test.tsx</files>
  <behavior>
    - Ban scan still passes for hero + verdict-throne + how-it-works (carried, unchanged).
    - Ban scan EXTENDS to reading-gallery, social-proof, proof-strip, cta-section — none of them may contain any banned token.
    - honesty-comparison is EXCLUDED from the negative ban scan; instead a positive assertion confirms the sanctioned rival strings ("viral score" / "virality score" / "accuracy" / "guaranteed") appear ONLY there.
    - File is RED now (the four new components + honesty-comparison do not exist yet) and goes GREEN only when every shipped copy string obeys VOICE.
  </behavior>
  <read_first>
    - src/components/numen-landing/__tests__/voice.test.tsx (the file being modified — the existing `banned` regex array + the render-and-scan structure)
    - .planning/VOICE.md (Hard Rules 1–2 ban list this gate enforces)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-PATTERNS.md (§voice.test.tsx EXTEND block; RESEARCH Pitfall 5 — scope rival strings to honesty-comparison)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-CONTEXT.md (D-05 — the one sanctioned home of the rival strings)
  </read_first>
  <action>Edit `voice.test.tsx`. Keep the existing `banned` array and the existing hero+throne+how-it-works ban scan. Add a second `it` that dynamically imports `ReadingGallery`, `SocialProof` (pass `count={3}`), `ProofStrip` (pass `count={3}`), and `CtaSection`, renders each, concatenates their `container.textContent`, and asserts NONE of the `banned` patterns match (these four must be VOICE-clean). Add a third `it` that imports `HonestyComparison`, renders it, and POSITIVELY asserts its text DOES contain the sanctioned rival labels (at least "viral score" and one of "accuracy"/"guaranteed") — proving the strings are present-and-scoped here, not leaked elsewhere. Do NOT add honesty-comparison to the negative ban scan. The file stays RED until Plans 02/03/05 ship all five components.</action>
  <verify>
    <automated>npx vitest run src/components/numen-landing/__tests__/voice.test.tsx 2>&1 | grep -qE "Cannot find module|Failed to (load|resolve)" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - voice.test.tsx now references all five Phase-3 components (`grep -c "reading-gallery\|social-proof\|proof-strip\|cta-section\|honesty-comparison" src/components/numen-landing/__tests__/voice.test.tsx` ≥ 5).
    - The negative ban scan covers the four clean components but NOT honesty-comparison; a separate positive assertion exists for honesty-comparison's rival strings.
    - The file is RED now (module-resolution failure on the missing components), confirming the gate will only pass once real shipped copy obeys VOICE.
  </acceptance_criteria>
  <done>voice.test.tsx extended: four new components in the ban scan, honesty-comparison positively asserted as the sole home of the sanctioned rival strings; RED until Waves 2–3 ship.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → mocked Supabase | Tests mock `@/lib/supabase/server`; no real DB or network crosses here. No live trust boundary in this plan (scaffolds only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | actions.test.ts dup-as-success assertion | mitigate | The scaffold ENCODES the no-enumeration-leak contract (23505→success) so Plan 05 cannot ship a leak GREEN — the test is the guard. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan (vitest + testing-library already present per RESEARCH Package Legitimacy Audit: zero new packages). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run` over the seven new files + voice.test.tsx → ALL RED on missing modules (expected this wave; they go GREEN as Waves 2–3 ship).
- No production code touched — only `__tests__/` files. `npm run build` unaffected.
- Confirm no new package added: `git diff --stat package.json` shows no change.
</verification>

<success_criteria>
- Seven RED test scaffolds exist (actions, waitlist-count, honesty-comparison, reading-gallery, social-proof, proof-placement, page-headings) + voice.test.tsx extended, each failing on the missing target module (not on an assertion typo).
- Every Phase-3 requirement (CTA-02, PROOF-01/02, TRUST-01/02, GALLERY-01/02, CONTENT-02) has at least one sampling seam, AND PROOF-02 (one source, two surfaces) + the single-h1 invariant each have an explicit automated scaffold.
- The voice gate scopes the sanctioned rival strings to honesty-comparison and bans them in the other four components.
</success_criteria>

<output>
Create `.planning/phases/03-honesty-moat-gallery-proof-conversion/03-01-SUMMARY.md` when done
</output>
