---
phase: 03-honesty-moat-gallery-proof-conversion
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/numen-landing/honesty-comparison.tsx
autonomous: true
requirements: [TRUST-01, TRUST-02, CONTENT-02]
must_haves:
  truths:
    - "A visitor sees a 2-column comparison contrasting Numen (band + why) against an unnamed 'Virality-score tools' tier"
    - "The sanctioned rival strings ('viral score' / '95% accuracy' / 'guaranteed views') appear ONLY in this component, labelling the rejected category — never as a Numen claim"
    - "The comparison is a semantic <table> with a sr-only caption and scoped column/row headers"
  artifacts:
    - path: "src/components/numen-landing/honesty-comparison.tsx"
      provides: "TRUST-01/02 kero comparison move, RSC, VerdictSwatch Numen column"
      contains: "scope=\"col\""
  key_links:
    - from: "honesty-comparison.tsx"
      to: "VerdictSwatch (good band)"
      via: "import + render in the Numen 'what you get' cell"
      pattern: "VerdictSwatch"
---

<objective>
Build the `#honesty` kero comparison move (TRUST-01/02): a semantic `<table>` contrasting Numen's calibrated honest verdict (band + one-line why) against an unnamed generic "Virality-score tools" tier. This is the ONE component on the page where the sanctioned rival strings ("viral score", "95% accuracy", "guaranteed views") appear — labelling the rejected category, never as a Numen claim (D-04/D-05).

Purpose: make the anti-snake-oil posture EXPLICIT (the moat made visible), turning the voice baseline into a side-by-side contrast.
Output: `honesty-comparison.tsx` (static RSC) — goes GREEN against the Plan-01 honesty-comparison + voice scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-honesty-moat-gallery-proof-conversion/03-UI-SPEC.md
@.planning/phases/03-honesty-moat-gallery-proof-conversion/03-PATTERNS.md
@.planning/VOICE.md

<interfaces>
<!-- Primitives this component consumes (DS-01 — consume, never fork). -->

From src/components/numen/verdict-swatch.tsx:
```
export function VerdictSwatch(props: { verdict?: "good"|"mixed"|"bad"; size?: "sm"|"md"|"lg"; children?: ReactNode; className?: string }): JSX.Element
// literal classes only; bg-verdict-good for the Numen "what you get" band
```

From src/lib/utils.ts:
```
export function cn(...inputs): string  // caller className merged LAST
```

Token-name classes available in `.numen-surface` scope (color by NAME only — Phase-4 hex swap, DS-01):
  bg-bg, bg-panel, bg-panel-2, text-text, text-text-muted, border-border
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build honesty-comparison.tsx (semantic table, Numen vs rival tier)</name>
  <files>src/components/numen-landing/honesty-comparison.tsx</files>
  <behavior>
    - Renders exactly one `<table>` with a `sr-only` `<caption>` "Numen compared to virality-score tools" and a `<thead>` with two `<th scope="col">` data headers: "Numen" and "Virality-score tools".
    - Three `<tbody>` rows, each with a `<th scope="row">` dimension; the Numen cell shows a VerdictSwatch band + one-line why (never a number); the rival cell states the rejected pattern plainly in text-text-muted.
    - The rendered text DOES contain the sanctioned rival strings ("viral score" + "95% accuracy" + "guaranteed views") — they live ONLY here.
    - The Numen cells contain NO banned token (no "%", no "viral", no number-as-verdict).
  </behavior>
  <read_first>
    - src/components/numen-landing/how-it-works.tsx (the static-RSC analog: `mt-8` lead-in, Surface plates, token-name color, NO "use client", NO h1/h2 — this component emits no heading; the slot's h2 is already in page.tsx)
    - src/components/numen/verdict-swatch.tsx (the band primitive for the Numen "what you get" cell — `verdict="good"`, literal classes, never `bg-${verdict}`)
    - src/components/numen-landing/verdict-throne.tsx (the band+label+why plate idiom: VerdictSwatch + bold label + muted why on bg-panel/border-border)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-UI-SPEC.md (§1 Section Contract: table structure, the 3 dimension rows, rival-cell copy, "tasteful contrast not a teardown", optional quiet Lucide Check/X as text-text-muted markers; §Copywriting Contract honesty rows)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-CONTEXT.md (D-04 unnamed generic tier; D-05 sanctioned-strings HARD scoping)
    - .planning/VOICE.md (Rules 1–3 + do/don't — every Numen cell must self-check)
    - src/components/numen-landing/__tests__/honesty-comparison.test.tsx (the Plan-01 scaffold this must turn GREEN — table/caption/scoped-headers + positive rival-string assertion)
  </read_first>
  <action>Create `src/components/numen-landing/honesty-comparison.tsx` as a static RSC (no `"use client"`), exporting `HonestyComparison()`, emitting NO heading of its own (the `#honesty` h2 lives on the SectionShell slot in page.tsx). Lead with a `mt-6 md:mt-8` intro `<p>` from UI-SPEC §Copywriting ("Other tools hand you a number and call it certainty. Numen tells you the truth about your video — and the one thing to fix."). Then a semantic `<table>` inside the shared `mx-auto max-w-6xl` rhythm: `<caption className="sr-only">Numen compared to virality-score tools</caption>`; `<thead>` row = an empty/dimension `<th scope="col">` corner + `<th scope="col">Numen</th>` + `<th scope="col">Virality-score tools</th>`; three `<tbody>` rows with `<th scope="row">` dimensions ("What you get", "How it reads", "What it promises"). Numen cells: row 1 = a `VerdictSwatch verdict="good" size="md"` + a bold `text-sm md:text-base font-bold text-text` label "An honest verdict you can act on." on a `bg-panel border-border rounded-[12px]` plate; rows 2–3 = VOICE-clean why-lines ("Reads your video like your sharpest audience would.", "The one thing to fix — and when it'll land."). Rival cells (`text-text-muted`, plainly stated, NOT screaming): "A viral score out of 100.", "A black-box percentage.", "95% accuracy. Guaranteed views." Color every element by token NAME only (`bg-panel`, `text-text`, `text-text-muted`, `border-border`) — zero hex in JSX. Cell padding `p-4`, hairline `border-border` rules. Optional: quiet Lucide `Check` (Numen) / `X` (rival) as `text-text-muted` cell markers — NOT accent/verdict-colored. On mobile the table may stack via responsive classes but the semantic `<table>` markup is preserved. Use `cn()` for any className merge. The sanctioned rival strings live ONLY in this file.</action>
  <verify>
    <automated>npx vitest run src/components/numen-landing/__tests__/honesty-comparison.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - honesty-comparison.test.tsx (Plan 01) passes GREEN.
    - `grep -c 'scope="col"' src/components/numen-landing/honesty-comparison.tsx` ≥ 2 and `grep -c 'scope="row"' ...` = 3 and exactly one `<table` and one `<caption`.
    - The file imports and renders `VerdictSwatch` with `verdict="good"` (`grep -c 'VerdictSwatch' ...` ≥ 1); NO `bg-${` interpolation (`grep -c 'bg-\${' ...` = 0).
    - The sanctioned rival strings are present here (`grep -ic 'viral score\|accuracy\|guaranteed' ...` ≥ 2); the Numen cells contain NO `%` and NO naked number-as-verdict.
    - No hex color in JSX (`grep -cE '#[0-9a-fA-F]{6}' src/components/numen-landing/honesty-comparison.tsx` = 0); no `"use client"`.
    - Component emits no `<h1>`/`<h2>` (`grep -cE '<h1|<h2' ...` = 0).
  </acceptance_criteria>
  <done>honesty-comparison.tsx renders the semantic-table kero comparison; Numen column is band+why VOICE-clean; rival column carries the sanctioned strings as the rejected category; Plan-01 honesty scaffold GREEN; tokens by name only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Static RSC, no user input, no network, no DB. No trust boundary crossed in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | honesty-comparison rival strings | accept | The "% accuracy"/"viral score" strings are INTENTIONAL here (D-05) and scoped by the voice gate (Plan 01) so they cannot leak into Numen claims elsewhere. No security risk — a positioning concern handled by the gate. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No installs (lucide-react already in deps per UI-SPEC). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run src/components/numen-landing/__tests__/honesty-comparison.test.tsx` GREEN.
- Voice gate (Plan 01) positive assertion for honesty-comparison's rival strings satisfied (run voice.test.tsx — its honesty assertion goes GREEN; the four-component ban scan may still be RED until Plans 03/05 ship — expected).
- `npm run build` succeeds (static RSC, no new deps).
</verification>

<success_criteria>
- TRUST-01: visitor sees Numen-vs-rival 2-column comparison.
- TRUST-02: comparison framing present; "% accuracy"/"viral score" appear ONLY here as the rejected category.
- CONTENT-02: every Numen string passes VOICE (band + why, second person, no hype).
</success_criteria>

<output>
Create `.planning/phases/03-honesty-moat-gallery-proof-conversion/03-02-SUMMARY.md` when done
</output>
