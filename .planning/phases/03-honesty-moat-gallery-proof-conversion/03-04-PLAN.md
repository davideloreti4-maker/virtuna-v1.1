---
phase: 03-honesty-moat-gallery-proof-conversion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260612000000_waitlist.sql
  - src/types/database.types.ts
  - package.json
autonomous: false
requirements: [CTA-02, PROOF-01]
user_setup:
  - service: supabase
    why: "Landing waitlist insert + live-count read run against the remote virtuna-v1.1 project"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard → Project Settings → API (must be set in the LANDING deploy env)"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project Settings → API (anon/public key)"
    dashboard_config:
      - task: "Apply the waitlist migration to the remote project (MCP apply_migration or CLI db push)"
        location: "Supabase project virtuna-v1.1"
must_haves:
  truths:
    - "A `waitlist` table exists with anon INSERT-only RLS (no public SELECT of rows) and a UNIQUE email + source CHECK constraint"
    - "A `waitlist_count()` SECURITY DEFINER function returns a bare count(*) and is EXECUTE-granted to anon"
    - "database.types.ts includes the waitlist table and waitlist_count function so .from('waitlist') and .rpc('waitlist_count') type-check"
  artifacts:
    - path: "supabase/migrations/20260612000000_waitlist.sql"
      provides: "CTA-02/PROOF-01 table + RLS + SECURITY DEFINER count RPC + grant"
      contains: "SECURITY DEFINER"
    - path: "src/types/database.types.ts"
      provides: "regenerated types incl. waitlist + waitlist_count"
      contains: "waitlist"
  key_links:
    - from: "waitlist_count() SECURITY DEFINER"
      to: "public.waitlist count(*)"
      via: "aggregate-only read that bypasses RLS safely (mirrors compute_niche_percentiles)"
      pattern: "count\\(\\*\\)"
---

<objective>
[BLOCKING] Create the net-new `waitlist` Supabase table + RLS + `waitlist_count()` RPC, APPLY the migration to the remote `virtuna-v1.1` project, and REGENERATE `database.types.ts` — so the Wave-3 server action (`.from("waitlist").insert`) and count read (`.rpc("waitlist_count")`) type-check and run. This is the schema-push gate: build/type checks pass WITHOUT the push (types come from the committed file, not the live DB), so the push is mandatory and must happen here, before any code wires the table.

Purpose: the persistence backbone for CTA-02 (records the signup) + PROOF-01 (live count), solving "count over RLS-hidden rows" with the in-repo `compute_niche_percentiles` SECURITY DEFINER precedent.
Output: the migration file, the applied table+RPC on the remote project, regenerated types, and a `db:types` convenience script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-honesty-moat-gallery-proof-conversion/03-RESEARCH.md
@.planning/phases/03-honesty-moat-gallery-proof-conversion/03-PATTERNS.md

<interfaces>
<!-- The exact SQL contract Wave 3 codes against. -->

Target table + RPC (RESEARCH Target 1 — full SQL):
```
public.waitlist(id uuid pk, email text NOT NULL UNIQUE, source text NOT NULL DEFAULT 'landing', created_at timestamptz)
  CHECK source IN ('landing-hero','landing-footer-cta','landing')
  RLS ENABLED; POLICY INSERT TO anon,authenticated WITH CHECK (true); no SELECT/UPDATE/DELETE policy (default-deny)
public.waitlist_count() RETURNS bigint LANGUAGE sql SECURITY DEFINER SET search_path = public
  AS $$ SELECT count(*) FROM public.waitlist $$;
GRANT EXECUTE ON FUNCTION public.waitlist_count() TO anon, authenticated;
```

Apply config (VERIFIED): `supabase/config.toml` → project_id = "virtuna-v1.1", major_version = 17; project is REMOTE (not locally linked).
Regen target: `src/types/database.types.ts` (already has a Functions block — compute_niche_percentiles — confirming RPCs are generated).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the waitlist migration (table + insert-only RLS + count RPC + grant)</name>
  <files>supabase/migrations/20260612000000_waitlist.sql</files>
  <read_first>
    - supabase/migrations/20260531000001_niche_percentiles_rpc.sql (the SECURITY DEFINER + `SET search_path = public` + `LANGUAGE sql` aggregate-RPC idiom to copy EXACTLY for waitlist_count)
    - supabase/migrations/20260216000000_referral_clicks_insert_policy.sql (the `... FOR INSERT TO ... WITH CHECK (...)` insert-only policy idiom)
    - supabase/migrations/20260213140000_referral_tables.sql (the ENABLE ROW LEVEL SECURITY idiom)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-RESEARCH.md (Target 1 — the complete, verified SQL: table, UNIQUE email, source CHECK, RLS, INSERT policy, SECURITY DEFINER count, GRANT)
  </read_first>
  <action>Create `supabase/migrations/20260612000000_waitlist.sql` (timestamp sorts after the latest `20260607000000_analysis_chats.sql`). Contents per RESEARCH Target 1, verbatim intent: `CREATE TABLE IF NOT EXISTS public.waitlist` with `id UUID PK DEFAULT gen_random_uuid()`, `email TEXT NOT NULL UNIQUE`, `source TEXT NOT NULL DEFAULT 'landing'`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`; add `CONSTRAINT waitlist_source_check CHECK (source IN ('landing-hero','landing-footer-cta','landing'))`; `ALTER TABLE public.waitlist ENABLE ROW LEVEL SECURITY`; `CREATE POLICY "Anyone can join the waitlist" ON public.waitlist FOR INSERT TO anon, authenticated WITH CHECK (true)` (NO select/update/delete policy → RLS default-deny hides rows from the anon aggregate); `CREATE OR REPLACE FUNCTION public.waitlist_count() RETURNS BIGINT LANGUAGE sql SECURITY DEFINER SET search_path = public AS $$ SELECT count(*) FROM public.waitlist; $$;` followed by `GRANT EXECUTE ON FUNCTION public.waitlist_count() TO anon, authenticated;`. Add a header comment explaining the insert-only + SECURITY-DEFINER-count rationale (mirrors compute_niche_percentiles).</action>
  <verify>
    <automated>test -f supabase/migrations/20260612000000_waitlist.sql && grep -qi 'SECURITY DEFINER' supabase/migrations/20260612000000_waitlist.sql && grep -qi 'ENABLE ROW LEVEL SECURITY' supabase/migrations/20260612000000_waitlist.sql && grep -qi 'FOR INSERT' supabase/migrations/20260612000000_waitlist.sql && echo MIGRATION-OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `supabase/migrations/20260612000000_waitlist.sql` and sorts after `20260607000000_analysis_chats.sql` (`ls supabase/migrations/ | tail -1` = the waitlist file).
    - Contains: `CREATE TABLE ... public.waitlist`, `email TEXT NOT NULL UNIQUE`, the `source` CHECK constraint, `ENABLE ROW LEVEL SECURITY`, a single `FOR INSERT ... WITH CHECK (true)` policy and NO SELECT/UPDATE/DELETE policy, `waitlist_count()` with `SECURITY DEFINER` + `SET search_path = public` returning `count(*)`, and `GRANT EXECUTE ... TO anon`.
    - No SELECT policy on waitlist (`grep -ic 'FOR SELECT' supabase/migrations/20260612000000_waitlist.sql` = 0) — count is read ONLY via the definer RPC.
  </acceptance_criteria>
  <done>The waitlist migration exists with anon insert-only RLS, UNIQUE email, source CHECK, and a SECURITY DEFINER count RPC granted to anon.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply the migration to the remote project + regenerate types</name>
  <files>src/types/database.types.ts</files>
  <what-built>The migration file exists but the table does NOT yet live on the remote `virtuna-v1.1` project, and `database.types.ts` has no `waitlist`/`waitlist_count`. This applies the migration and regenerates types. The project is REMOTE (not locally linked), so apply via Supabase MCP `apply_migration` (preferred) or CLI `db push` (needs SUPABASE_ACCESS_TOKEN + link). Types regen needs an access token too — hence a human-gated step.</what-built>
  <read_first>
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-RESEARCH.md (Target 5 — apply paths + the gen-types command + the hard sequencing: apply → regen → grep-verify → THEN wire)
    - supabase/config.toml (project_id = virtuna-v1.1)
  </read_first>
  <action>Apply the migration to the remote `virtuna-v1.1` project, then regenerate types — this is the [BLOCKING] schema push. Apply via Supabase MCP `apply_migration` (name `waitlist`, SQL from `supabase/migrations/20260612000000_waitlist.sql`) — preferred since the project is not locally linked — OR via CLI `supabase db push` if a `SUPABASE_ACCESS_TOKEN` + linked project is available. Then regenerate `src/types/database.types.ts` via `supabase gen types typescript --project-id virtuna-v1.1 --schema public > src/types/database.types.ts` (or MCP `generate_typescript_types` then overwrite the file). Confirm the table + RPC are live by invoking `waitlist_count()` (returns a bigint, likely 0) and confirming an anon SELECT on `waitlist` returns no rows. If interactive auth is required and cannot be suppressed, complete it and re-run; report which apply path was used in the SUMMARY. Do NOT hand-edit the generated types file.</action>
  <how-to-verify>
    1. Apply the migration to the remote project — ONE of:
       - Supabase MCP `apply_migration` with name `waitlist` and the SQL from `supabase/migrations/20260612000000_waitlist.sql` (preferred — goes straight to remote), OR
       - CLI (if a SUPABASE_ACCESS_TOKEN + linked project): `supabase db push`.
    2. Regenerate types — ONE of:
       - `supabase gen types typescript --project-id virtuna-v1.1 --schema public > src/types/database.types.ts`, OR
       - Supabase MCP `generate_typescript_types` then overwrite `src/types/database.types.ts`.
    3. Verify the table + RPC live: query `waitlist_count()` via MCP/CLI → returns a bigint (likely 0). Confirm an anon SELECT on `waitlist` returns no rows (RLS).
    4. If applying requires interactive auth that cannot be suppressed, complete the auth and re-run; report the path used.
  </how-to-verify>
  <acceptance_criteria>
    - `grep -c waitlist src/types/database.types.ts` ≥ 2 (the `waitlist` Tables entry + the `waitlist_count` Functions entry) — verified AFTER regen.
    - `grep -c waitlist_count src/types/database.types.ts` ≥ 1.
    - The remote `waitlist_count()` RPC returns a number (bigint) when invoked (MCP/CLI) — the table is live.
    - An anon SELECT on `public.waitlist` returns zero rows (RLS default-deny holds — emails are not harvestable).
  </acceptance_criteria>
  <resume-signal>Type "applied" once the migration is live and types regenerated, or report the blocker (auth/access token).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Add the db:types convenience script + verify regen sticks</name>
  <files>package.json</files>
  <read_first>
    - package.json (the existing `scripts` block — add one line, do not reorder)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-RESEARCH.md (Target 5 — the exact gen-types command for the script)
  </read_first>
  <action>Add a `"db:types": "supabase gen types typescript --project-id virtuna-v1.1 --schema public > src/types/database.types.ts"` entry to the `scripts` block in `package.json` so future regens are one command. Then confirm the regenerated `database.types.ts` from Task 2 is committed-ready: it must already contain `waitlist` (Tables) + `waitlist_count` (Functions). Do NOT hand-edit `database.types.ts` — it is generated.</action>
  <verify>
    <automated>grep -q '"db:types"' package.json && grep -q 'waitlist_count' src/types/database.types.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v 'waitlist' | grep -c 'error TS' | grep -q '^0$' && echo TYPES-OK || echo "check tsc output"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `scripts` has `db:types` (`grep -c '"db:types"' package.json` = 1).
    - `database.types.ts` contains both `waitlist` and `waitlist_count` (`grep -c 'waitlist' src/types/database.types.ts` ≥ 2).
    - `npx tsc --noEmit` introduces no NEW type errors attributable to the regen (the file is valid generated TS).
  </acceptance_criteria>
  <done>`db:types` script added; regenerated types confirmed to include waitlist + waitlist_count; Wave 3 can now type-check `.from("waitlist")` and `.rpc("waitlist_count")`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon client → public.waitlist | Untrusted public visitors INSERT here; RLS must allow insert but forbid reading rows. |
| anon client → waitlist_count() | Public count read; the RPC must expose ONLY a bigint, never any row/email. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | anon SELECT on waitlist (email harvesting) | mitigate | Insert-only RLS + NO select policy (default-deny) → anon SELECT returns zero rows; count comes only from the SECURITY DEFINER aggregate. Verified in Task 2 acceptance (anon SELECT returns 0 rows). |
| T-03-05 | Information Disclosure | waitlist_count() leaking rows | mitigate | Function returns a bare `BIGINT count(*)`, never a row/email; `SET search_path = public` pins resolution (linter guidance). |
| T-03-06 | Tampering | source column arbitrary write | mitigate | DB `CHECK (source IN (...))` backstops the server-side literal allowlist (Plan 05). |
| T-03-07 | Elevation of Privilege | SECURITY DEFINER misuse | mitigate | Function body is a single read-only `count(*)`; no dynamic SQL, no writes; `search_path` pinned — cannot be repurposed. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; supabase CLI already on PATH (RESEARCH: zero new packages). No legitimacy checkpoint required. |
</threat_model>

<verification>
- Migration file present + sorts last; contains insert-only RLS + SECURITY DEFINER count + grant; NO select policy.
- Remote apply confirmed: `waitlist_count()` returns a number; anon SELECT on waitlist returns 0 rows.
- `database.types.ts` regenerated with `waitlist` + `waitlist_count`; `npx tsc --noEmit` clean (no NEW errors).
- `db:types` script present.
</verification>

<success_criteria>
- CTA-02 (backbone): the `waitlist` table + insert-only RLS exist live on the remote project.
- PROOF-01 (backbone): `waitlist_count()` SECURITY DEFINER RPC returns an RLS-safe count, granted to anon.
- Types regenerated so Wave 3 wiring type-checks — the schema-push gate is satisfied BEFORE any code consumes the table.
</success_criteria>

<output>
Create `.planning/phases/03-honesty-moat-gallery-proof-conversion/03-04-SUMMARY.md` when done
</output>
