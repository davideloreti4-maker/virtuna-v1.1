---
phase: 03-honesty-moat-gallery-proof-conversion
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-04"]
files_modified:
  - src/app/(marketing)/actions.ts
  - src/lib/waitlist-count.ts
  - src/components/numen-landing/waitlist-form.tsx
  - src/components/numen-landing/cta-section.tsx
  - src/components/numen-landing/proof-strip.tsx
  - src/components/numen-landing/social-proof.tsx
  - src/app/(marketing)/page.tsx
autonomous: false
requirements: [CTA-02, PROOF-01, PROOF-02, CONTENT-02]
must_haves:
  truths:
    - "A visitor can submit their email in a real form near the footer that records the signup to the waitlist table"
    - "A duplicate email returns the SAME success state — never revealing the email already exists"
    - "The live waitlist count appears on two surfaces (early strip + #proof) from ONE cached read; below threshold it shows a qualitative anchor, never '0 creators'"
    - "All four sections fill their slots and the page keeps exactly one <h1>"
  artifacts:
    - path: "src/app/(marketing)/actions.ts"
      provides: "CTA-02 joinWaitlist server action — honeypot, validation, dup-as-success, source allowlist"
      exports: ["joinWaitlist", "WaitlistState"]
    - path: "src/lib/waitlist-count.ts"
      provides: "PROOF-01 cached RPC count read, error→0"
      exports: ["getWaitlistCount"]
    - path: "src/components/numen-landing/waitlist-form.tsx"
      provides: "CTA-02 client form, useActionState, honeypot, success/dup/error states"
      contains: "useActionState"
    - path: "src/components/numen-landing/social-proof.tsx"
      provides: "PROOF-01/02/D-09 count block + placeholder-ready testimonials"
      contains: "count"
    - path: "src/components/numen-landing/proof-strip.tsx"
      provides: "PROOF-02/D-10 early thin credibility strip"
      contains: "count"
    - path: "src/components/numen-landing/cta-section.tsx"
      provides: "CTA-02 RSC wrapper hosting the footer waitlist form"
      contains: "WaitlistForm"
  key_links:
    - from: "waitlist-form.tsx"
      to: "joinWaitlist server action"
      via: "useActionState(joinWaitlist, { status: 'idle' })"
      pattern: "joinWaitlist"
    - from: "actions.ts"
      to: "public.waitlist (insert)"
      via: "createClient().from('waitlist').insert + 23505→success"
      pattern: "23505"
    - from: "page.tsx"
      to: "getWaitlistCount"
      via: "single await, threaded to ProofStrip + SocialProof"
      pattern: "getWaitlistCount"
---

<objective>
Close the conversion + proof loop (CTA-02 / PROOF-01/02): the `joinWaitlist` server action + the cached `getWaitlistCount` read, the `"use client"` waitlist form, the `#cta` wrapper, the early proof strip + `#proof` block (D-10 — one source, two surfaces), and the `page.tsx` wiring that reads the count ONCE and fills all four remaining slots. This is the only plan that touches `page.tsx` and the only `"use client"` island on the page.

Purpose: the landing now records real signups and shows honest social proof — the page earns belief AND converts.
Output: 6 net-new files + the edited `page.tsx`, turning the Plan-01 actions/count/social-proof scaffolds GREEN and completing the four section slots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-honesty-moat-gallery-proof-conversion/03-RESEARCH.md
@.planning/phases/03-honesty-moat-gallery-proof-conversion/03-PATTERNS.md
@.planning/phases/03-honesty-moat-gallery-proof-conversion/03-UI-SPEC.md
@.planning/VOICE.md

<interfaces>
<!-- Plan 04 already shipped the types; .from("waitlist") + .rpc("waitlist_count") now type-check. -->

The action + read contracts the Plan-01 tests assert (build to these EXACTLY):
```
// src/app/(marketing)/actions.ts
export type WaitlistState = { status:"idle" } | { status:"success" } | { status:"error"; message:string };
export async function joinWaitlist(prev: WaitlistState, formData: FormData): Promise<WaitlistState>;

// src/lib/waitlist-count.ts
export const getWaitlistCount: () => Promise<number>;  // unstable_cache, revalidate 60, error→0
```

From src/lib/supabase/server.ts:  export async function createClient()  // anon RSC client
From src/components/numen/{surface,pill-chip}.tsx: Surface, PillChip (as in Plan 03)

Current page.tsx slots to fill (heading-only): #honesty, #gallery, #proof, #cta.
ProofStrip mounts as a NEW sibling between #hero and #how-it-works (D-10 early strip — NOT a SectionShell).
Token-name classes only; accent (bg-accent/text-accent) reserved for the submit button fill + focus ring ONLY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the data layer — joinWaitlist action + getWaitlistCount cached read</name>
  <files>src/app/(marketing)/actions.ts, src/lib/waitlist-count.ts</files>
  <behavior>
    - joinWaitlist: honeypot `company` filled → `{ status:"success" }` with NO insert; invalid email → `{ status:"error" }` with NO insert; valid email → inserts `{ email, source }` and returns `{ status:"success" }`; unique-violation `23505` → `{ status:"success" }` (dup-as-success, no leak); other insert error → `{ status:"error", message }`.
    - source is a server-side literal allowlist ("landing-hero" | "landing-footer-cta"), never trusted raw from the hidden field.
    - getWaitlistCount: rpc("waitlist_count") number → that number; error or non-number → 0; wrapped in unstable_cache (revalidate 60).
  </behavior>
  <read_first>
    - src/app/(onboarding)/signup/actions.ts (the server-action shape to mirror: `"use server"`, `createClient` from `@/lib/supabase/server`, `formData.get`, `{ error }` destructure — but DROP `redirect`, return state instead)
    - src/lib/supabase/server.ts (the anon RSC `createClient` both files use)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-RESEARCH.md (Target 2 full joinWaitlist code incl. EMAIL_RE + 23505 mapping + honeypot + source allowlist; Target 3 full getWaitlistCount unstable_cache code)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-PATTERNS.md (§actions.ts + §waitlist-count.ts divergence notes; the sequence gate — types already regenerated in Plan 04)
    - src/app/(marketing)/__tests__/actions.test.ts + src/lib/__tests__/waitlist-count.test.ts (the Plan-01 scaffolds these must turn GREEN)
  </read_first>
  <action>Create `src/app/(marketing)/actions.ts` (`"use server"`): export the `WaitlistState` union and `joinWaitlist(prev, formData)` per RESEARCH Target 2 — honeypot check on `company` (filled → silent success, no insert), trim+lowercase `email`, validate against an EMAIL_RE regex (`/^[^\s@]+@[^\s@]+\.[^\s@]+$/`), resolve `source` via a server-side allowlist (`formData.get("source") === "landing-hero" ? "landing-hero" : "landing-footer-cta"`), `createClient()` from `@/lib/supabase/server`, `await supabase.from("waitlist").insert({ email, source })`, map `error.code === "23505"` → `{ status:"success" }` and any other error → `{ status:"error", message:"Something went wrong. Please try again." }`, invalid email → `{ status:"error", message:"Enter a valid email address." }`. Create `src/lib/waitlist-count.ts`: export `getWaitlistCount` = `unstable_cache(async () => { const supabase = await createClient(); const { data, error } = await supabase.rpc("waitlist_count"); if (error || typeof data !== "number") return 0; return data; }, ["waitlist-count"], { revalidate: 60, tags: ["waitlist-count"] })` per RESEARCH Target 3. All copy strings pass VOICE.</action>
  <verify>
    <automated>npx vitest run "src/app/(marketing)/__tests__/actions.test.ts" "src/lib/__tests__/waitlist-count.test.ts" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Both Plan-01 scaffolds (actions.test.ts + waitlist-count.test.ts) pass GREEN.
    - actions.ts: `"use server"`, maps `23505`→success (`grep -c '23505' ...` ≥ 1), has the honeypot `company` check, validates email server-side, and resolves `source` from a literal allowlist (`grep -c 'landing-hero\|landing-footer-cta' ...` ≥ 2). NO `redirect` import.
    - waitlist-count.ts: wraps the rpc read in `unstable_cache` (`grep -c 'unstable_cache' ...` = 1) with `revalidate: 60`; returns 0 on error.
    - `npx tsc --noEmit` clean for these two files (types from Plan 04 present).
  </acceptance_criteria>
  <done>joinWaitlist records signups with honeypot + validation + dup-as-success + source allowlist; getWaitlistCount reads the cached RLS-safe count (error→0). Plan-01 data scaffolds GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the form + cta wrapper + proof surfaces (proof-strip, social-proof)</name>
  <files>src/components/numen-landing/waitlist-form.tsx, src/components/numen-landing/cta-section.tsx, src/components/numen-landing/proof-strip.tsx, src/components/numen-landing/social-proof.tsx</files>
  <behavior>
    - waitlist-form: `"use client"`, useActionState(joinWaitlist, {status:"idle"}); renders a hidden `source` input, a sr-only honeypot `company`, a `type="email" required` field, an accent submit ("Join the waitlist"→"Joining…" while pending). Success (and duplicate) swaps the form for "You're on the list." (role="status", aria-live="polite"); invalid-email error shows "Enter a valid email address." (role="alert"), field preserved.
    - social-proof({count}): D-09 guard — count ≥ 50 → "{count.toLocaleString()} creators are on the list."; count < 50 → "Be one of the first creators to read your content honestly." NEVER "0 creators". Plus a placeholder-ready testimonial row that renders nothing (or one neutral line) from an empty array — NEVER a fabricated quote.
    - proof-strip({count}): a thin `py-4 md:py-6 border-y border-border` band (NOT a section) with the compact count line (same D-09 guard) + optional niche PillChips.
    - cta-section: RSC wrapper, intro lead + `<WaitlistForm source="landing-footer-cta" />` in a max-w-md container.
  </behavior>
  <read_first>
    - src/app/(onboarding)/signup/signup-form.tsx (the useActionState + form-action precedent — copy the `"use client"` + `useActionState` + `isPending` disable wiring; DIVERGE: native input/button + token classes, NOT the @/components/ui/* glass kit; render inline state instead of redirect)
    - src/components/numen-landing/hero.tsx (the carried accent CTA treatment for the submit button: `inline-flex h-11 items-center rounded-lg bg-accent px-5 ... text-bg`; the focus-visible ring string)
    - src/components/numen-landing/verdict-throne.tsx (the `bg-panel border-border rounded-[12px] p-4 md:p-6` plate idiom for the count block backing)
    - src/components/numen-landing/how-it-works.tsx (the RSC wrapper-hosting-a-child idiom for cta-section + the Surface card idiom for testimonials)
    - src/components/numen/{surface,pill-chip}.tsx (Surface plate + PillChip niche tags)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-UI-SPEC.md (§3 proof contract: thin strip vs full block, D-09 threshold=50 + qualitative anchor, placeholder testimonials; §4 cta form contract: layout, honeypot, ALL form states table, focus ring; §Copywriting all the strings; §Color accent reserved for submit + ring only)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-PATTERNS.md (§waitlist-form.tsx + §social-proof/proof-strip + §cta-section divergence notes)
    - src/components/numen-landing/__tests__/social-proof.test.tsx (the Plan-01 scaffold social-proof must turn GREEN)
  </read_first>
  <action>Create four files under `src/components/numen-landing/`. `waitlist-form.tsx` (`"use client"`): import `joinWaitlist`/`WaitlistState` from `@/app/(marketing)/actions`; `useActionState(joinWaitlist, { status: "idle" })`; props `{ source: "landing-hero" | "landing-footer-cta" }`; `<form action={action}>` with a hidden `<input type="hidden" name="source" value={source} />`, a honeypot `<input type="text" name="company" tabIndex={-1} autoComplete="off" aria-hidden="true" className="sr-only" />`, a labelled `<input type="email" name="email" required autoComplete="email" placeholder="you@example.com">` (token-class field: `bg-panel border-border text-text rounded-lg min-h-11` + the focus-visible ring), and a submit `<button>` using the carried accent treatment (`bg-accent text-bg min-h-11 rounded-lg`, label "Join the waitlist" / "Joining…" when pending, optional Lucide `Loader2` that does NOT spin under reduced-motion). On `state.status === "success"` swap the form for a `role="status" aria-live="polite"` "You're on the list." (may fade via StageBlock, reduced-motion=static); on error show `role="alert"` text-text-muted message, field preserved. Desktop inline (`flex gap-2`), mobile stacked (`flex-col gap-2`), `max-w-md`. `cta-section.tsx` (RSC): a `mt-6 md:mt-8` lead `<p>` ("Get an honest verdict on your content before you post. Join the waitlist — we'll let you know when it's your turn.") + `<WaitlistForm source="landing-footer-cta" />` in a `max-w-md` container; emits no heading (the `#cta` h2 is on the slot). `social-proof.tsx` (RSC, props `{ count: number }`): the D-09 guard with THRESHOLD=50 (count≥50 → "{count.toLocaleString()} creators are on the list." else the qualitative anchor "Be one of the first creators to read your content honestly." — never "0 creators"), the count figure on a `bg-panel border-border` plate styled `text-3xl md:text-4xl font-bold tracking-tight text-text`, plus a placeholder-ready testimonial row: map over an EMPTY testimonial array → renders nothing (or one neutral non-quote credibility line), NEVER a fabricated quote; testimonial card (when populated) = Surface plate + quote (`text-text`) + attribution (`text-text-muted`). `proof-strip.tsx` (RSC, props `{ count: number }`): a thin `py-4 md:py-6` `border-y border-border` band (NOT a `py-24` section) with the compact count line (same D-09 guard, "{count} creators on the waitlist" above threshold) + optional niche `PillChip`s. Color by token name only; accent ONLY on the submit button + focus rings. All copy passes VOICE.</action>
  <verify>
    <automated>npx vitest run src/components/numen-landing/__tests__/social-proof.test.tsx 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - social-proof.test.tsx (Plan 01) passes GREEN (threshold guard, never "0 creators", toLocaleString above threshold).
    - waitlist-form.tsx: `"use client"` + `useActionState` (`grep -c 'useActionState' ...` = 1); has the sr-only honeypot `company` (`grep -c 'name="company"' ...` = 1) and a hidden `source` input; submit uses `bg-accent` + `text-bg`; success copy "You're on the list." with `role="status"`.
    - social-proof.tsx + proof-strip.tsx both take a `count` prop and both contain the D-09 guard (`grep -c 'THRESHOLD\|toLocaleString' ...` ≥ 1 each); neither renders the literal "0 creators".
    - cta-section.tsx renders `<WaitlistForm source="landing-footer-cta" />` (`grep -c 'landing-footer-cta' ...` = 1).
    - No fabricated testimonial quote text in social-proof (testimonial array is empty/placeholder); no hex (`grep -cE '#[0-9a-fA-F]{6}'` = 0 across the four files); accent appears ONLY on the form submit/ring.
  </acceptance_criteria>
  <done>The waitlist form (with honeypot + all states), the cta wrapper, and both proof surfaces (count guard + placeholder testimonials) are built; social-proof scaffold GREEN; accent scoped to the submit button.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Wire page.tsx (fill four slots, one count read, single h1) + confirm deploy env</name>
  <files>src/app/(marketing)/page.tsx</files>
  <read_first>
    - src/app/(marketing)/page.tsx (the file being modified — the seven ordered slots; #honesty/#gallery/#proof/#cta are heading-only; the single-h1 invariant doc comment)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-PATTERNS.md (§page.tsx — make async, read count once, thread to ProofStrip + SocialProof, mount ProofStrip between #hero and #how-it-works)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-RESEARCH.md (Target 3 — the async page shape + the caching caveat: read ONLY via getWaitlistCount, never createClient() in the page body; Open Question 1 / A2 — landing deploy env must carry NEXT_PUBLIC_SUPABASE_URL + anon key)
    - .planning/phases/03-honesty-moat-gallery-proof-conversion/03-UI-SPEC.md (single-h1 invariant; the carried slot headings)
  </read_first>
  <what-built>page.tsx becomes `async`, reads the waitlist count ONCE via `getWaitlistCount()`, threads it to both the early `ProofStrip` (mounted between #hero and #how-it-works) and the `#proof` `SocialProof` (one source, two surfaces — D-10), and fills the four currently-heading-only slots with `HonestyComparison` (Plan 02), `ReadingGallery` (Plan 03), `SocialProof`, and `CtaSection`. Exactly one `<h1>` (hero) is preserved. This checkpoint also confirms the LANDING deploy env carries the Supabase anon URL+key so the live count + insert work in prod (RESEARCH A2).</what-built>
  <action>Edit `src/app/(marketing)/page.tsx`: make `HomePage` `async`; at the top `const waitlistCount = await getWaitlistCount();` (import from `@/lib/waitlist-count` — do NOT call `createClient()` in the page body). Mount `<ProofStrip count={waitlistCount} />` as a sibling BETWEEN the `#hero` SectionShell and the `#how-it-works` SectionShell (it is a thin band, NOT a SectionShell). Fill the four slots: `<SectionShell id="honesty" heading="An honest verdict, not a hype score."><HonestyComparison /></SectionShell>`, `<SectionShell id="gallery" heading="Real Readings, real creators."><ReadingGallery /></SectionShell>`, `<SectionShell id="proof" heading="Creators who trust the Reading."><SocialProof count={waitlistCount} /></SectionShell>`, `<SectionShell id="cta" heading="See what your next video is really saying."><CtaSection /></SectionShell>`. Keep the carried slot headings + the single-h1 invariant (hero owns the only h1; no new h1/h2 in the filled bodies). Import the five new components.</action>
  <how-to-verify>
    1. Run the dev server (`npm run dev`); if CSS/token changes don't appear, kill it + clear `.next/` + restart (CLAUDE.md cache landmine).
    2. Visit the landing root. Confirm in order: hero → early proof strip (count line or qualitative anchor, never "0 creators") → how-it-works → #honesty comparison table → #gallery (≥3 cards, mixed verdicts) → #proof (count + no fake testimonial) → #cta waitlist form.
    3. Submit a real email in the #cta form → "You're on the list." Submit the SAME email again → identical "You're on the list." (no "already exists"). Submit an invalid email → "Enter a valid email address."
    4. Verify the row landed: query `waitlist_count()` (MCP/CLI) → incremented; confirm `source = 'landing-footer-cta'`.
    5. Reduced-motion: enable `prefers-reduced-motion: reduce` → the success swap is a static appear (no translate), the pending spinner does not spin.
    6. DEPLOY ENV: confirm the landing's Vercel project has `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` set (RESEARCH A2) — dev works locally regardless; this gates the live count + insert in prod.
  </how-to-verify>
  <verify>
    <automated>npx vitest run src/components/numen-landing/__tests__/voice.test.tsx 2>&1 | tail -6 && grep -c '<SectionShell id="hero"' src/app/\(marketing\)/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx is `async`, imports `getWaitlistCount`, and calls it exactly ONCE (`grep -c 'getWaitlistCount' src/app/\(marketing\)/page.tsx` = 1 for the call site import; no `createClient` in the page).
    - All four slots are filled (`grep -c 'HonestyComparison\|ReadingGallery\|SocialProof\|CtaSection' ...` ≥ 4) and `ProofStrip` mounts as a sibling between #hero and #how-it-works.
    - The page renders exactly one `<h1>` (hero) — `grep -c 'heading=' ...` shows the four non-hero slots keep their heading prop and #hero has none.
    - The FULL voice gate (Plan 01) passes GREEN — all five Phase-3 components VOICE-clean except the scoped rival strings in honesty-comparison.
    - Live manual check passed: form records a signup (count increments, source correct), dup-as-success works, invalid-email error works, reduced-motion static; deploy env confirmed (or flagged as a launch-checklist item if the landing deploys standalone without the keys).
  </acceptance_criteria>
  <done>page.tsx is async, reads the count once and threads it to both proof surfaces, fills all four slots (honesty/gallery/proof/cta) with a single preserved h1; the full Phase-3 voice gate is GREEN and the live form flow (record / dup-as-success / invalid) + reduced-motion are verified; deploy env confirmed or flagged.</done>
  <resume-signal>Type "approved" after the live page + form flow verify, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → joinWaitlist (server action) | Untrusted email + source cross here; validated/sanitized server-side. |
| RSC → waitlist_count() RPC | Cached count read; no row data crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | dup-email error revealing membership | mitigate | `23505` → identical success state; success and duplicate render the SAME copy (verified by actions.test.ts + the live dup check). |
| T-03-09 | Input Validation | malformed/injected email | mitigate | Server-side EMAIL_RE before insert; parameterized supabase insert (no raw SQL). Honeypot drops bots silently. |
| T-03-10 | Tampering | `source` hidden-field tampering | mitigate | Server-side literal allowlist (only two values accepted) + DB CHECK backstop (Plan 04). |
| T-03-11 | Denial of Service | bot mass-signup | mitigate | Honeypot floor (D-02); UNIQUE(email) bounds duplicates. Rate-limit optional (Claude's discretion, not required this phase) — accepted residual: low-value pre-launch target. |
| T-03-12 | Information Disclosure | count read leaking rows / going dynamic | mitigate | Read ONLY via `getWaitlistCount` (unstable_cache + SECURITY DEFINER RPC); never `createClient()` in the page body; never `from().select(count)` on anon. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No installs — React/Next/lucide already present (RESEARCH: zero new packages). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run` over actions.test.ts, waitlist-count.test.ts, social-proof.test.ts, voice.test.tsx → ALL GREEN (the full Phase-3 suite).
- `npm test` full suite green before `/gsd:verify-work`.
- `npm run build` succeeds; page is async, count read cached (one Supabase round-trip per revalidate window).
- Live: form records a signup (count increments, correct source), dup-as-success identical, invalid-email error inline, reduced-motion static; single h1 preserved.
- Deploy env confirmed or flagged (RESEARCH A2).
</verification>

<success_criteria>
- CTA-02: a real near-footer waitlist form records the signup; dup-as-success; source-attributed.
- PROOF-01: live waitlist count rendered from a cached RLS-safe read.
- PROOF-02: credibility anchored early (proof strip) + the fuller #proof block, one source two surfaces; D-09 never shows "0 creators".
- CONTENT-02: every form/proof/cta string passes VOICE; the full voice gate is GREEN.
</success_criteria>

<output>
Create `.planning/phases/03-honesty-moat-gallery-proof-conversion/03-05-SUMMARY.md` when done
</output>
