---
phase: 14-kc-grounding-quality-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/engine/wave3/niche-resolver.ts
  - src/lib/engine/wave3/__tests__/niche-resolver.test.ts
  - src/lib/tools/runners/ideas-runner.ts
  - src/lib/tools/runners/hooks-runner.ts
  - src/lib/engine/flash/flash-aggregate.ts
  - src/lib/engine/flash/__tests__/slop-vs-strong.test.ts
autonomous: true
requirements: [KCQ-06, KCQ-01, KCQ-05]
must_haves:
  truths:
    - "A creator whose niche_primary holds a sub-slug (e.g. personal-finance) or free text gets niche-instantiated SIM personas, not the generic fallback"
    - "The text SIM gate can say no — obvious slop lands Weak, a known-great item lands Strong, with a real discrimination margin"
    - "The General (null-niche) path stays byte-identical — the STABLE_FLASH_SYSTEM_PROMPT fallback and the 61/0 flash baseline are untouched"
    - "selectPersonaSlots and NICHE_INSTANTIATION bytes are unchanged — no ENGINE_VERSION bump, the Max video path is unperturbed"
  artifacts:
    - path: "src/lib/engine/wave3/niche-resolver.ts"
      provides: "resolveNicheKey(nichePrimary) — free-text/sub-slug → top-level NICHE_INSTANTIATION key or null"
      exports: ["resolveNicheKey"]
    - path: "src/lib/engine/wave3/__tests__/niche-resolver.test.ts"
      provides: "Resolver unit coverage: direct hit, sub-slug→parent, prose miss→null"
  key_links:
    - from: "src/lib/tools/runners/ideas-runner.ts"
      to: "resolveNicheKey"
      via: "panel niche field built from resolveNicheKey(profileRow?.niche_primary)"
      pattern: "resolveNicheKey"
    - from: "src/lib/tools/runners/hooks-runner.ts"
      to: "resolveNicheKey"
      via: "panel niche field built from resolveNicheKey(profileRow?.niche_primary)"
      pattern: "resolveNicheKey"
---

<objective>
The moat spine (D-02 hard sequence: KCQ-06 → KCQ-05). The text SIM is silently niche-blind in production — not because persona-registry is unwired (it shipped in 07-04) but because `profileRow.niche_primary` is free text (`z.string().max(64)`) that frequently holds a sub-slug like `personal-finance` or prose, which fails the exact top-level slug match in `selectPersonaSlots` (`persona-registry.ts:436`) and silently falls back to `"general TikTok"` + generic instantiation (`:437-438,509-510`). Result: flat 6/6/6/6/5, "all Mixed" — the gate can't say no.

This plan adds `resolveNicheKey` at the RUNNER layer (per D-02 / Pitfall 2 — NEVER inside the shared engine function, which the Max video path imports at `pipeline.ts:771`), wires it into the Ideas + Hooks panels (KCQ-06 + KCQ-01 are the same niche-layer fix per RESEARCH Verification 7), re-validates the slop-vs-strong gate with real resolved niches (KCQ-06 threshold recalibration), and formalizes the now-discriminating `aggregateFlash` band as the named gate (KCQ-05 — closes the chain).

Purpose: turn the SIM from a flat passer into a gate that clears the slop floor — the durable-moat centerpiece.
Output: `resolveNicheKey` + runner wiring + re-validated thresholds, all on the text path, no ENGINE_VERSION bump.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-kc-grounding-quality-loop-expansion-the-durable-moat-not-yet/14-CONTEXT.md
@.planning/phases/14-kc-grounding-quality-loop-expansion-the-durable-moat-not-yet/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: resolveNicheKey at the runner/lib layer (KCQ-06 + KCQ-01)</name>
  <read_first>
    - src/lib/engine/wave3/persona-registry.ts:431-528 (selectPersonaSlots exact-slug match :436; makeSlot generic fallback :509-510; NICHE_INSTANTIATION keys are the 10 top-level slugs)
    - src/lib/niches/taxonomy.ts:53-298 (NICHE_TREE: top-level slug + label + subs[]; e.g. education > personal-finance :115)
    - src/lib/tools/runners/ideas-runner.ts:283-284 (panel build site)
    - src/lib/tools/runners/hooks-runner.ts:312-313 (panel build site)
    - 14-RESEARCH.md §Pattern 1 + Code Examples (resolveNicheKey shape) + Pitfall 2 (Max-path byte-stability)
  </read_first>
  <action>
    Create `src/lib/engine/wave3/niche-resolver.ts` exporting `resolveNicheKey(nichePrimary: string | null): string | null`. Import `NICHE_TREE` from `@/lib/niches/taxonomy` and the `NICHE_INSTANTIATION` keyset from `persona-registry.ts` (export the keyset or a `isNicheInstantiationKey(slug)` guard from persona-registry WITHOUT changing selectPersonaSlots or the NICHE_INSTANTIATION table bytes — add a pure read-only export only). Resolution order: (1) null/empty → null; (2) normalize trim+lowercase, direct top-level hit → return it; (3) sub-slug → walk NICHE_TREE, find the parent whose `subs[]` contains the slug, return parent.slug if it is an instantiation key; (4) keyword/contains fallback against top-level labels/slugs → nearest top-level; (5) no match → null (honest generic fallback — never fabricate a niche). Resolver MUST be pure + deterministic (no Date.now/random) to preserve cache warmth. Do NOT touch `selectPersonaSlots`, `makeSlot`, `NICHE_INSTANTIATION`, or `NICHE_TREE` data — adding a read-only key export is the only persona-registry edit allowed. Keep the file under 500 lines.
    Then wire into both runners: in ideas-runner.ts replace `const niche = profileRow?.niche_primary ?? null;` (`:283`) with `const niche = resolveNicheKey(profileRow?.niche_primary ?? null);`, and the same at hooks-runner.ts `:312`. The panel shape `{ niche, contentType: null }` is unchanged. Add `resolveNicheKey` to each runner's declared import surface in the header isolation comment.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/wave3/__tests__/niche-resolver.test.ts && npx vitest run src/lib/tools/runners</automated>
  </verify>
  <acceptance_criteria>
    - `resolveNicheKey("personal-finance")` returns `"education"`; `resolveNicheKey("fitness")` returns `"fitness"`; `resolveNicheKey("my weird prose niche")` returns null; `resolveNicheKey(null)` returns null — asserted in niche-resolver.test.ts.
    - `git diff src/lib/engine/wave3/persona-registry.ts` shows ONLY an added read-only key export (no change to lines 238-362 or 431-528 logic).
    - Both runners import and call `resolveNicheKey` at the panel build site: `grep -c resolveNicheKey src/lib/tools/runners/ideas-runner.ts src/lib/tools/runners/hooks-runner.ts` each ≥ 1.
    - `npx vitest run src/lib/tools/runners` exits 0.
  </acceptance_criteria>
  <done>resolveNicheKey exists, unit-tested, wired into both runner panels; persona-registry data bytes unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Re-validate slop-vs-strong gate with resolved niches + formalize the gate (KCQ-06 recal + KCQ-05)</name>
  <read_first>
    - src/lib/engine/flash/flash-aggregate.ts:18-93 (STRONG_THRESHOLD=6, MIXED_THRESHOLD=3, aggregateFlash, the "no recalibration needed" comment at :45-48 — now known incomplete per Pitfall 3)
    - src/lib/engine/flash/__tests__/slop-vs-strong.test.ts:70-214 (pure HALF 1 fixtures :70-171; LIVE HALF 2 :176-214, DASHSCOPE_API_KEY-gated, hardcoded FITNESS_PANEL)
    - 14-RESEARCH.md Critical Verification 3 (recalibration method) + Pitfall 3 + Assumptions note (5 of 10 NICHE_INSTANTIATION cells are [PLACEHOLDER]: gaming, tech-gadgets, food-cooking, fashion-style, music-performance — calibrate on a non-placeholder niche: fitness/beauty/education/comedy/lifestyle)
  </read_first>
  <action>
    Correct the stale calibration comment in flash-aggregate.ts:45-48 (the "no recalibration needed; the key fix was the niche panel" claim) to reflect that the niche panel only discriminates once `resolveNicheKey` lands at the runner layer, and that the LIVE half validates the resolved-niche path. Keep `STRONG_THRESHOLD`/`MIXED_THRESHOLD` as named exported constants (the gate-floor contract `band !== "Weak"`, i.e. stops ≥ MIXED_THRESHOLD, is KCQ-05's formalized gate — leave the values at 6/3 unless the LIVE recalibration below shows slop does not land < MIXED or strong does not land ≥ STRONG, in which case adjust and update the test assertions in lockstep).
    Extend slop-vs-strong.test.ts: keep the pure HALF-1 assertions unchanged (margin ≥ 3, slop band Weak, strong band non-Weak). For the LIVE HALF-2, route the SLOP_HOOK and STRONG_HOOK through a niche resolved by `resolveNicheKey` (use a non-placeholder niche — fitness) so the test exercises the production resolution path, not a hand-built FITNESS_PANEL; assert `strongStops - slopStops >= 2`. Add an assertion pair locking `STRONG_THRESHOLD === <final>` and `MIXED_THRESHOLD === <final>` so any future drift fails loud. The General/null-niche path MUST keep returning STABLE_FLASH_SYSTEM_PROMPT unchanged — do not touch that branch (Pitfall 4). The LIVE half stays DASHSCOPE_API_KEY-gated (pure half always runs).
    Run the LIVE recalibration locally with DASHSCOPE_API_KEY present per Critical Verification 3 step 3 (≥5 known-slop, ≥5 known-strong fitness items); record the observed stop-counts and the final threshold decision in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/flash</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/engine/flash` exits 0 (full flash suite green, incl. slop-vs-strong pure half + flash-prompts byte-stable).
    - slop-vs-strong.test.ts LIVE half resolves its niche via `resolveNicheKey` (grep shows the import/call), not a hand-built panel.
    - The test contains explicit `STRONG_THRESHOLD === N` and `MIXED_THRESHOLD === N` equality assertions (drift gate).
    - General/null-niche path unchanged: `flash-prompts.test.ts` byte-stable assertion passes; `grep -n STABLE_FLASH_SYSTEM_PROMPT src/lib/engine/flash/run-flash-text-mode.ts` branch logic untouched in the diff.
    - SUMMARY records the LIVE recalibration stop-counts + final threshold values (Claude's discretion per D-04, validated not asserted).
  </acceptance_criteria>
  <done>Gate re-validated with real resolved niches; thresholds locked + drift-asserted; KCQ-05 gate formalized; General path byte-identical.</done>
</task>

</tasks>

<verification>
- `npm test` full suite green (flash + kc + runners + tools).
- No ENGINE_VERSION bump: `grep ENGINE_VERSION src/lib/engine/version.ts` still "3.19.0" (RESEARCH Verification 10 — niche resolution lives at the runner layer, engine bytes untouched).
- `npm run build` + `npm run lint` clean.
</verification>

<success_criteria>
- A production creator with a sub-slug/free-text niche gets niche-instantiated personas (no silent generic fallback).
- The slop-vs-strong LIVE half proves the gate discriminates on the resolved-niche production path.
- General/null path and the Max video path are byte-identical; no ENGINE_VERSION bump.
</success_criteria>

<artifacts_this_phase_produces>
NEW symbols (exclude from source-grounding drift verification): `resolveNicheKey` (+ its module `niche-resolver.ts`), the read-only NICHE_INSTANTIATION key export/guard added to persona-registry, the threshold drift-assertion pair in slop-vs-strong.test.ts.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/14-kc-grounding-quality-loop-expansion-the-durable-moat-not-yet/14-01-SUMMARY.md` when done.
</output>
