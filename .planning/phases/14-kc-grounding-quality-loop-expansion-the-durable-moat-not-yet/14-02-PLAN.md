---
phase: 14-kc-grounding-quality-loop
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/lib/engine/flash/rubric-critic.ts
  - src/lib/engine/flash/__tests__/rubric-critic.test.ts
  - src/lib/tools/runners/ideas-runner.ts
  - src/lib/tools/runners/hooks-runner.ts
  - src/lib/tools/blocks.ts
  - src/lib/tools/runners/__tests__/best-of-n.test.ts
autonomous: true
requirements: [KCQ-02, KCQ-04, KCQ-07]
must_haves:
  truths:
    - "Ideas and Hooks over-generate, then a parallel Flash rubric-critic judges each candidate against the Value Bar; only candidates that pass the rubric AND clear the SIM band ship"
    - "When EVERY candidate fails the rubric floor (rare), the runner regenerates once — never an unbounded serial loop"
    - "Each surviving card carries a predictedFailureMode field, populated for opt-in drill-reveal, null when the item passes cleanly"
    - "Latency stays ~1x — over-generation + critique are parallel (Promise.all), never serial"
  artifacts:
    - path: "src/lib/engine/flash/rubric-critic.ts"
      provides: "critiqueAgainstRubric(item, framing, panel) → { pass, predictedFailureMode } via one Flash json_object call against the BASE Value Bar"
      exports: ["critiqueAgainstRubric"]
    - path: "src/lib/tools/blocks.ts"
      provides: "predictedFailureMode optional field on IdeaCardBlockSchema + HookCardBlockSchema"
  key_links:
    - from: "src/lib/tools/runners/ideas-runner.ts"
      to: "critiqueAgainstRubric"
      via: "parallel critic call alongside runFlashTextMode, combined gate criterion"
      pattern: "critiqueAgainstRubric"
    - from: "src/lib/tools/runners/hooks-runner.ts"
      to: "critiqueAgainstRubric"
      via: "parallel critic call alongside runFlashTextMode, combined gate criterion"
      pattern: "critiqueAgainstRubric"
---

<objective>
The quality loop (KCQ-02 best-of-N rubric critique + KCQ-04 flop pass + KCQ-07 thin runtime reject), Ideas + Hooks ONLY (D-09). Today's gate is a SIM stop/scroll band only — not a rubric critique (RESEARCH Verification 4). This plan inserts a PARALLEL Flash rubric-critic (D-05/D-08: independent judge, parallel not serial — the "2-4× latency" objection only applies to serial regeneration) that judges each candidate against the BASE Value Bar (Test A named mechanism / Test B non-fakeable concrete / Test C fit + Prohibition 6 trope test, `base.md:260-303`). The same critic call returns `predictedFailureMode` (KCQ-04 shared call per RESEARCH Pattern 3 / Verification 5 — cheapest), and the trope clause covers KCQ-07's thin runtime reject (corpus floor already covers most). Combined gate (KCQ-05 formalized in 14-01): keep iff `band !== "Weak"` AND `verdict.pass`. Regenerate ONCE only when ALL candidates fail (D-06, conditional, no unbounded loop).

Purpose: execute the Value Bar at runtime, not just author it — "rightness" over discipline.
Output: a shared parallel Flash rubric-critic + best-of-N + flop-pass wiring in both runners, text path only, no ENGINE_VERSION bump.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-kc-grounding-quality-loop-expansion-the-durable-moat-not-yet/14-CONTEXT.md
@.planning/phases/14-kc-grounding-quality-loop-expansion-the-durable-moat-not-yet/14-RESEARCH.md
@.planning/phases/14-kc-grounding-quality-loop-expansion-the-durable-moat-not-yet/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flash rubric-critic — shared best-of-N + flop call (KCQ-02 + KCQ-04 + KCQ-07)</name>
  <behavior>
    - Test 1: a candidate naming a concrete mechanism + a non-fakeable concrete + creator fit → `{ pass: true, predictedFailureMode: null }`.
    - Test 2: a generic niche-trope candidate (fails Prohibition 6 / Test B find-and-replace) → `{ pass: false, predictedFailureMode: <non-empty string> }`.
    - Test 3: a Flash/transport error inside the critic → resolves to a fail-safe verdict (`pass: false` or a marked warning) — never throws into the Promise.all, mirroring the existing `runFlashTextMode(...).catch(() => null)` resilience.
  </behavior>
  <read_first>
    - .planning/corpus/base.md:260-303 (Prohibition 6 trope test + Value Bar Test A/B/C — the rubric source to extend, verbatim)
    - src/lib/engine/flash/run-flash-text-mode.ts:89-125 (the json_object Flash call pattern, temperature:0 + seed, panel resolution, HARD ISOLATION from engine modules :11-13 — the critic must mirror this isolation)
    - src/lib/engine/flash/flash-schema.ts (FlashPersona shape, json_object parsing conventions)
    - 14-RESEARCH.md Critical Verification 5 + Pattern 3 + Code Examples (RubricVerdict shape) + Open Question 2 (recommend a SEPARATE parallel critic call, not an extended SIM call — preserves D-08 independence)
  </read_first>
  <action>
    Create `src/lib/engine/flash/rubric-critic.ts` exporting `critiqueAgainstRubric(item: string, framing: "idea" | "hook", panel: NichePanel): Promise<RubricVerdict>` where `RubricVerdict = { pass: boolean; predictedFailureMode: string | null }`. It makes ONE Flash `json_object` call (own SIM-1 Flash model, `temperature: 0`, `seed: QWEN_SEED`, AbortController timeout) with a system prompt that encodes the Value Bar Test A/B/C + Prohibition 6 trope test (extend the BASE rubric prose, Claude's discretion on exact wording per D-04). The model returns `{ pass: boolean, predictedFailureMode: string | null }` — `predictedFailureMode` is the "if this flops for this audience, here's why" texture (KCQ-04), null when pass. Maintain HARD ISOLATION: import ONLY from qwen/client, flash-schema, and the niche panel type — NEVER from pipeline/aggregator/version/wave3 engine internals (mirror run-flash-text-mode.ts:11-13). On transport/parse error, resolve to a fail-safe verdict (do not throw). Keep the file under 500 lines. This is the independent-judge critic (D-08) — a separate call from the SIM band, runnable in the same Promise.all so wall-clock stays ~1x (D-05).
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/flash/__tests__/rubric-critic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `critiqueAgainstRubric` exported; returns `{ pass, predictedFailureMode }`; null failureMode when pass, non-empty when fail — asserted in rubric-critic.test.ts.
    - Critic imports ONLY qwen/client + flash-schema + panel type: `grep -n "from \"@/lib/engine" src/lib/engine/flash/rubric-critic.ts` shows no pipeline/aggregator/version/wave3 import.
    - Error path resolves (no throw): the error test passes.
    - `npx vitest run src/lib/engine/flash` full suite still exits 0.
  </acceptance_criteria>
  <done>Shared rubric-critic exists, isolated, fail-safe, unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Wire best-of-N + flop pass into Ideas + Hooks runners + card schema (KCQ-02/04/07)</name>
  <read_first>
    - src/lib/tools/runners/ideas-runner.ts:282-393 (the Promise.all SIM loop :301-309, gate-drop :336-337, MAX_SURVIVORS=3, block build :346-374)
    - src/lib/tools/runners/hooks-runner.ts:311-452 (Promise.all SIM :327-335, gate :360, rank+trim :377-390, block build :395-425)
    - src/lib/tools/blocks.ts:77-138 (IdeaCardBlockSchema + HookCardBlockSchema — add predictedFailureMode here)
    - 14-RESEARCH.md Pattern 2 (parallel critique between generate and gate) + Pitfall 5 (never serial) + D-06 (regenerate only on all-fail)
  </read_first>
  <action>
    Add an optional `predictedFailureMode: z.string().nullable().optional()` field to both `IdeaCardBlockSchema.props` and `HookCardBlockSchema.props` in blocks.ts (optional → existing persisted blocks + rehydration stay valid, no migration). In each runner, alongside the existing `runFlashTextMode(...)` call inside the SAME `Promise.all`, add a parallel `critiqueAgainstRubric(item, framing, panel)` call per candidate (use the same `panel` built from `resolveNicheKey` in 14-01, and the same seed item — `idea.seedHook` / `hook.seedHook ?? hook.hookLine`). Combine the gate criterion (KCQ-05 formalized): keep a candidate iff `band !== "Weak"` AND `verdict.pass`. Carry `verdict.predictedFailureMode` onto the built card block props. Implement conditional regeneration (D-06): if ZERO candidates pass the combined gate, regenerate ONCE (one more parallel over-generate + critique pass), then proceed with whatever survives (may be 0 — no unbounded loop, preserves the existing "0 blocks is valid" contract). Keep ranking (Hooks) and MAX_SURVIVORS/MAX_HOOKS trims unchanged. Keep the FLYWHEEL-02 pin logic untouched. Update each runner's header isolation comment to declare the new `rubric-critic` dependency.
  </action>
  <verify>
    <automated>npx vitest run src/lib/tools/runners src/app/api/tools src/lib/tools</automated>
  </verify>
  <acceptance_criteria>
    - `predictedFailureMode` is an optional nullable field on both card schemas: `grep -c predictedFailureMode src/lib/tools/blocks.ts` ≥ 2; existing block fixtures still validate (rehydration unbroken).
    - Both runners call `critiqueAgainstRubric` inside the candidate Promise.all and gate on `pass` AND `band !== "Weak"`: `grep -c critiqueAgainstRubric src/lib/tools/runners/ideas-runner.ts src/lib/tools/runners/hooks-runner.ts` each ≥ 1.
    - Conditional single regeneration exists (regenerate path triggers only when no candidate passes) — covered by best-of-n.test.ts; no serial per-candidate loop.
    - `npx vitest run src/lib/tools/runners src/app/api/tools` exits 0.
  </acceptance_criteria>
  <done>Both runners run parallel rubric-critique, combined gate, single conditional regen; cards carry predictedFailureMode.</done>
</task>

</tasks>

<verification>
- `npm test` full suite green.
- Latency posture preserved: critic runs inside the existing Promise.all (parallel) — no new awaited serial stage. Grep confirms no `await critiqueAgainstRubric` inside a per-candidate `for` loop.
- No ENGINE_VERSION bump; no Max video-path file touched.
- `npm run build` + `npm run lint` clean.
</verification>

<success_criteria>
- Ideas + Hooks emit only candidates that pass the Value Bar rubric AND clear the SIM band.
- All-fail triggers exactly one regeneration; common path stays ~1x latency.
- Each card carries predictedFailureMode for the opt-in reveal built in 14-04.
</success_criteria>

<artifacts_this_phase_produces>
NEW symbols (exclude from source-grounding drift verification): `critiqueAgainstRubric` + `RubricVerdict` (module `rubric-critic.ts`), the `predictedFailureMode` field on IdeaCardBlockSchema + HookCardBlockSchema.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/14-kc-grounding-quality-loop-expansion-the-durable-moat-not-yet/14-02-SUMMARY.md` when done.
</output>
