---
phase: 14-kc-grounding-quality-loop
plan: 04
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - src/components/thread/idea-card-block.tsx
  - src/components/thread/hook-card-block.tsx
  - src/components/thread/__tests__/idea-card-block.test.tsx
autonomous: true
requirements: [KCQ-09, KCQ-04]
must_haves:
  truths:
    - "Each idea/hook card surfaces an inline plain-language made-for-you rationale (the whyItFits grounding line), visibly separating Numen from a generic chatbot"
    - "Each card's predicted failure mode is available on-demand via a drill/expand affordance — never shoved in the user's face, never silent-only"
    - "Cards keep leading with the sharp scroll-quote, not the fraction (D-03)"
    - "All card UI is the fixed numen-rework typed renderer — no model-generated markup"
  artifacts:
    - path: "src/components/thread/idea-card-block.tsx"
      provides: "inline whyItFits rationale micro-copy + opt-in predictedFailureMode reveal"
      contains: "whyItFits"
    - path: "src/components/thread/hook-card-block.tsx"
      provides: "opt-in predictedFailureMode reveal (whyItFits already present where applicable)"
  key_links:
    - from: "src/components/thread/idea-card-block.tsx"
      to: "IdeaCardBlock.props.predictedFailureMode"
      via: "drill/expand reveal rendered only when present"
      pattern: "predictedFailureMode"
---

<objective>
The surface lane (KCQ-09 inline made-for-you rationale + KCQ-04 opt-in flop reveal) — minimal presentational edits to the EXISTING fixed renderers (no new design surface, no UI-SPEC). KCQ-09 (thin, D-04): surface the existing `whyItFits` grounding line (`buildGroundingLine`/`buildAudienceGroundingLine`, already on the card props) as inline plain-language micro-copy — personalization trust + a steering lever, NOT source-citation (D-14). Full field-legibility surface is deferred to P12 (D-04). KCQ-04 reveal (D-10): the `predictedFailureMode` field (added to the card schema + populated by the rubric-critic in 14-02) renders as an opt-in drill/expand affordance — available on-demand, never always-visible, never silent-only. Cards keep leading with the scroll-quote, not the fraction (D-03). Fixed typed renderer only — no model-generated UI (CLAUDE.md / REQUIREMENTS line 207); flat-warm THEME-06 Raycast tokens (6% borders, 10% hover, 12px radius, coral accents only).

Purpose: make the moat legible — "made for you" + honest failure-mode foresight — without breaking the positive-but-honest tone.
Output: inline rationale + opt-in flop reveal on the idea/hook card renderers. Pure client render; no engine, no ENGINE_VERSION bump.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-kc-grounding-quality-loop-expansion-the-durable-moat-not-yet/14-CONTEXT.md
@.planning/phases/14-kc-grounding-quality-loop-expansion-the-durable-moat-not-yet/14-RESEARCH.md
@.planning/phases/14-kc-grounding-quality-loop-expansion-the-durable-moat-not-yet/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline made-for-you rationale on the idea/hook cards (KCQ-09)</name>
  <read_first>
    - src/components/thread/idea-card-block.tsx:1-50 (renderer constraints — model emits validated props only, THIS component owns layout; whyItFits is already a prop; BAND_COLOR; THEME-06 tokens)
    - src/components/thread/hook-card-block.tsx (face/expand layout, scrollQuote-led)
    - src/lib/tools/blocks.ts:77-138 (IdeaCardBlock.whyItFits :83; HookCardBlock has no whyItFits — see action)
    - 14-RESEARCH.md Pattern 5 (whyItFits already produced; render inline) + D-04 (thin, NOT citation; full surface deferred to P12)
  </read_first>
  <action>
    On the idea card renderer, surface the existing `whyItFits` prop as inline plain-language micro-copy on the card FACE (e.g. a subtle "Made for you — because {whyItFits}" line, Claude's discretion on exact micro-copy/placement per D-04), visually distinct from the scroll-quote (which still LEADS per D-03) and from the band chip. Use THEME-06 tokens (muted text, no coral except existing accents). For the hook card: `HookCardBlock` has no `whyItFits` prop (hooks inherit the idea's grounding); do NOT add a schema field — if a grounding line is not present on the hook block, render no rationale line on hooks (KCQ-09 primary surface is Ideas + Hooks where the line exists; do not fabricate one). This is presentational only — no new prop, no model markup. Update idea-card-block.test.tsx to assert the rationale line renders from whyItFits and the scroll-quote still leads.
  </action>
  <verify>
    <automated>npx vitest run src/components/thread/__tests__/idea-card-block.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The idea card renders the `whyItFits` line inline on the face: test asserts the rationale text appears and is derived from the `whyItFits` prop (not hardcoded).
    - Scroll-quote still leads the card (D-03): test asserts scrollQuote renders above/before the band fraction.
    - No new schema field added; no model-generated markup (`grep -c "dangerouslySetInnerHTML" src/components/thread/idea-card-block.tsx` = 0).
  </acceptance_criteria>
  <done>Inline made-for-you rationale renders from whyItFits on the idea card; scroll-quote still leads.</done>
</task>

<task type="auto">
  <name>Task 2: Opt-in predicted-failure-mode reveal on the idea/hook cards (KCQ-04)</name>
  <read_first>
    - src/components/thread/idea-card-block.tsx (the existing expand/disclosure pattern — useState tap-to-expand for mechanism/seedHook)
    - src/components/thread/hook-card-block.tsx (same expand pattern)
    - src/lib/tools/blocks.ts (predictedFailureMode optional nullable field added in 14-02)
    - 14-RESEARCH.md Pattern 3 + D-10 (internal gate + opt-in reveal; not always-visible, not silent-only) + 14-02-SUMMARY.md (confirms the field is populated)
  </read_first>
  <action>
    On both the idea and hook card renderers, when `block.props.predictedFailureMode` is present (non-null), render an opt-in drill/expand affordance (reuse the existing tap-to-expand disclosure pattern — e.g. a small "If this could flop →" / "Why it might miss" toggle inside the expanded section) that reveals the failure-mode text on demand. When `predictedFailureMode` is null/absent, render NOTHING (no empty affordance). It must NOT be always-visible on the face (preserves the positive-but-honest tone, D-10) and must NOT be silent-only (it IS reachable via the drill). THEME-06 tokens — use `--color-warning` for the affordance accent, never coral, never error-red (honesty-spine tone). Pure client render; no engine call. Add a test asserting the affordance renders only when predictedFailureMode is present and is gated behind the disclosure (not on the face).
  </action>
  <verify>
    <automated>npx vitest run src/components/thread/__tests__/idea-card-block.test.tsx; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The flop affordance renders ONLY when `predictedFailureMode` is non-null, and only inside the expand/disclosure (not on the always-visible face): test covers both present and null cases.
    - Affordance uses `--color-warning` (not coral/error): `grep -n "color-warning" src/components/thread/idea-card-block.tsx` returns a hit in the flop affordance.
    - Both idea + hook renderers reference `predictedFailureMode`: `grep -c predictedFailureMode src/components/thread/idea-card-block.tsx src/components/thread/hook-card-block.tsx` each ≥ 1.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Opt-in flop reveal renders on drill when present, hidden when absent, warning-toned, fixed renderer.</done>
</task>

</tasks>

<verification>
- `npm test` full suite green.
- No model-generated UI anywhere in the touched renderers; all layout owned by the components.
- No ENGINE_VERSION bump (pure client render).
- `npm run build` + `npm run lint` clean.
</verification>

<success_criteria>
- Idea/hook cards show an inline made-for-you rationale and an opt-in flop reveal.
- Tone stays positive-but-honest: flop reveal is drill-only, scroll-quote still leads.
</success_criteria>

<artifacts_this_phase_produces>
CHANGED renderers only (no new exported symbols). The inline rationale line + opt-in flop affordance are presentational additions to idea-card-block.tsx / hook-card-block.tsx consuming existing/14-02 props.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/14-kc-grounding-quality-loop-expansion-the-durable-moat-not-yet/14-04-SUMMARY.md` when done.
</output>
