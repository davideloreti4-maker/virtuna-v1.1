---
phase: 14-kc-grounding-quality-loop
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/kc/assembler.ts
  - src/lib/kc/profile-role-map.ts
  - src/lib/kc/__tests__/assembler.test.ts
  - .planning/corpus/hooks.md
  - src/lib/kc/compiled.ts
  - src/lib/kc/kc-version.ts
  - src/components/command-bar/ExpertChatThread.tsx
autonomous: false
requirements: [KCQ-08, HONESTY-01]
user_setup: []
must_haves:
  truths:
    - "Voice survives the BUNDLE_CHAR_CAP drop — it is no longer the first role dropped for idea/hooks/script/remix, and the grounding carries an explicit write-in-this-voice instruction"
    - "The owner's 26 hook templates ground Hooks generation as a silent private-reasoning exemplar layer under the archetype table — never emitted verbatim, no citation pills"
    - "The corpus change recompiles byte-stable via regen-kc.ts and bumps KC_GEN_VERSION (not ENGINE_VERSION)"
    - "The fake §N citation pills are gone from the chat — no decorative citation render, no static tooltip map, with the real-code fallback and board-frame pills intact"
  artifacts:
    - path: ".planning/corpus/hooks.md"
      provides: "26-template exemplar block under the archetype table, framed private-reasoning-only (D-17)"
      contains: "private reasoning"
    - path: "src/lib/kc/kc-version.ts"
      provides: "bumped KC_GEN_VERSION (minor) for the authoring update"
    - path: "src/components/command-bar/ExpertChatThread.tsx"
      provides: "citation-pill render removed (HONESTY-01)"
  key_links:
    - from: ".planning/corpus/hooks.md"
      to: "src/lib/kc/compiled.ts"
      via: "regen-kc.ts recompile of KC_HOOKS_SLICE"
      pattern: "KC_HOOKS_SLICE"
---

<objective>
The corpus + voice + honesty lane (independent of the SIM chain — parallelizable). Three pieces: (1) **KCQ-08 voice calibration** — voice is plumbed (`assembler.ts:111-116`, `writing_voice_sample` shipped N1) but appended last and drops first under `BUNDLE_CHAR_CAP=4000`, so output often does not sound like the creator. Promote voice priority + add an explicit style-match instruction (D-11/D-12). (2) **The owner's 26 hook templates (D-16/17/18)** — fold them UNDER the `hooks.md` archetype table as a silent private-reasoning exemplar layer (owner-owned, pre-curated — the cheapest grounding win), via the compile pipeline; private reasoning only, never emitted, no pills (D-17). (3) **HONESTY-01** — delete the fake `§N` citation pills in `ExpertChatThread.tsx` (decorative, no retrieval behind them; honesty-spine violation; delete, no re-light — D-13/D-14).

The 26 templates are an EXECUTION-TIME owner input (not in the repo — RESEARCH Verification 8 / A1), so Task 2 pauses for the owner to supply them + the map-before-merge review (D-18) → this plan is `autonomous: false`.

Purpose: make output sound like the creator + ground hooks on owned exemplars + remove a dishonest UI affordance.
Output: voice reorder + style instruction, the 26-template corpus fold-in (KC_GEN_VERSION bump), citation-pill deletion. No ENGINE_VERSION bump.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-kc-grounding-quality-loop-expansion-the-durable-moat-not-yet/14-CONTEXT.md
@.planning/phases/14-kc-grounding-quality-loop-expansion-the-durable-moat-not-yet/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Voice priority promotion + style-match instruction (KCQ-08)</name>
  <read_first>
    - src/lib/kc/assembler.ts:100-116 (MODE_ROLES — voice LAST for idea/hooks/script/remix, excluded for chat) + :176-186 isProfileThin + :277-286 the tail-first cap-drop loop
    - src/lib/kc/profile-role-map.ts:140-160 (formatVoice — the STYLE-only fence header to strengthen)
    - 14-RESEARCH.md Critical Verification 6 + Pattern 4 (concrete landing points) + Pitfall 4 (General/null path must stay byte-stable; voice touches the volatile user message only, not the cached system prompt)
  </read_first>
  <action>
    In `assembler.ts` MODE_ROLES (`:110-116`) move `voice` OUT of the tail position for `idea`, `hooks`, `script`, `remix` so a routine cap-drop never silently removes it — place it ahead of `wins`/`flops`/`platform` (Claude's discretion on exact rank within D-12, but voice must NOT be the last element of those four role arrays). Leave `chat` unchanged (voice intentionally excluded). In `profile-role-map.ts` `formatVoice` (`:151-160`), strengthen the instruction header from "emulate the STYLE, rhythm, and tone only" to an explicit directive: "Write in this voice — match its sentence rhythm, vocabulary register, and tone" while KEEPING the existing "do NOT reuse specific content or claims" honesty clause and the `<<<USER_CONTENT>>>` fence intact. (Optional few-shot per D-12(c) is allowed but not required — if added, frame the sample as a worked example inside the existing fence.) These changes affect the VOLATILE user message only — do not touch `STABLE_FLASH_SYSTEM_PROMPT` or any compiled system prompt (Pitfall 4 cache discipline). Update assembler.test.ts to assert voice is no longer tail-positioned for the four modes and survives a representative cap-drop.
  </action>
  <verify>
    <automated>npx vitest run src/lib/kc</automated>
  </verify>
  <acceptance_criteria>
    - `voice` is not the last element of MODE_ROLES.idea/hooks/script/remix: assembler.test.ts asserts position; chat array unchanged.
    - `formatVoice` carries the explicit "Write in this voice" directive AND retains the "do NOT reuse specific content" clause + the fence: `grep -n "Write in this voice" src/lib/kc/profile-role-map.ts` returns a hit; fence sentinels still present.
    - `npx vitest run src/lib/kc` exits 0; no compiled.ts / system-prompt bytes changed by this task (voice lives in the volatile path).
  </acceptance_criteria>
  <done>Voice survives the cap-drop with an explicit style-match instruction; General/cached path untouched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The compile-pipeline slot under the hooks.md archetype table is ready for the owner's 26 hook templates (D-16). Per D-18 (map-before-merge) the owner supplies the list and confirms clean fit against the archetype table before the fold-in.</what-built>
  <how-to-verify>
    Owner action required (Claude cannot supply these — they are owner-owned and not in the repo, RESEARCH A1):
    1. Paste the 26 hook templates here.
    2. Map-before-merge (D-18): for each template, confirm it is a concrete INSTANCE of a mechanism already named in the hooks.md archetype table (BOLD / GAP / CONTRARIAN / RESEARCH / NARRATIVE / QUESTION) — flag any that contradict or duplicate, so they fold cleanly UNDER the table without re-litigating archetypes.
  </how-to-verify>
  <resume-signal>Paste the 26 templates + confirm the map-before-merge fit (or note adjustments), then type "templates ready".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Fold the 26 templates into hooks.md + recompile + KC_GEN_VERSION bump (D-16/17/18)</name>
  <read_first>
    - .planning/corpus/hooks.md:1-7 (no-[SLUG]-leak header discipline), :54-62 (archetype table), :268-310 (Hook Reasoning Scaffold → Clean Deliverable — the private-reasoning framing to mirror), :327+ (INTRA-BATCH DIVERSITY RULE — slot the block alongside)
    - src/lib/kc/compiled.ts:1-10 (generated-file header), KC_HOOKS_SLICE (:587), KC_HOOKS_SYSTEM_PROMPT (:1312)
    - scripts/regen-kc.ts:1-49 (byte-deterministic recompile) + src/lib/kc/kc-version.ts (KC_GEN_VERSION = "gen.1.0.0", decoupled from ENGINE_VERSION)
    - 14-RESEARCH.md Critical Verification 8 (exact fold-in slot + pipeline steps) + D-17 (private reasoning, never verbatim)
  </read_first>
  <action>
    Add the owner-supplied 26 templates as a concrete exemplar block UNDER the archetype table in `.planning/corpus/hooks.md` (after the archetype/mechanism definitions, alongside the INTRA-BATCH DIVERSITY RULE near `:327`). Frame them explicitly as private reasoning: "Concrete instances of the mechanisms above — pattern inspiration for internal reasoning ONLY; NEVER emit verbatim, as a fill-in-the-blank scaffold, or as a `[SLUG]` tag" (D-17), mirroring the existing "Hook Reasoning Scaffold → Clean Deliverable" discipline. Do NOT introduce mad-libs/template-fill output (the anti-slop enemy P2 fought). Then run `npx tsx scripts/regen-kc.ts` to regenerate `compiled.ts` (KC_HOOKS_SLICE / KC_HOOKS_SYSTEM_PROMPT) — do NOT hand-edit compiled.ts. Bump `KC_GEN_VERSION` in `kc-version.ts` from `gen.1.0.0` to a MINOR bump (`gen.1.1.0`) for this authoring update. Do NOT touch ENGINE_VERSION (decoupled per the kc-version.ts header). Keep flash + kc tests green and the compiled output structurally byte-stable (no interpolation drift).
  </action>
  <verify>
    <automated>npx tsx scripts/regen-kc.ts && npx vitest run src/lib/kc src/lib/engine/flash</automated>
  </verify>
  <acceptance_criteria>
    - hooks.md contains the 26-template block framed private-reasoning-only: `grep -c "NEVER emit\|private reasoning" .planning/corpus/hooks.md` ≥ 1; no `[SLUG]`-emit instruction introduced.
    - `compiled.ts` is freshly regenerated (re-running regen-kc.ts produces no further diff — byte-stable): `npx tsx scripts/regen-kc.ts && git diff --quiet src/lib/kc/compiled.ts` after the commit.
    - `KC_GEN_VERSION` bumped to `gen.1.1.0`: `grep -n "gen.1.1.0" src/lib/kc/kc-version.ts` returns a hit; ENGINE_VERSION unchanged.
    - `npx vitest run src/lib/kc src/lib/engine/flash` exits 0.
  </acceptance_criteria>
  <done>26 templates folded in as silent grounding, recompiled byte-stable, KC_GEN_VERSION bumped, tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the fake §N citation pills (HONESTY-01)</name>
  <read_first>
    - src/components/command-bar/ExpertChatThread.tsx:6 (JSDoc "§citation pills inline" line), :26-38 (CORPUS_SECTIONS map), :331-337 (insertCitationMarkers), :363-377 (§cite: pill branch in the code component), :389 (the {insertCitationMarkers(content)} call site), :40-43 VALID_FRAMES / parseFrameTag (SEPARATE board-frame feature — LEAVE INTACT)
    - 14-RESEARCH.md Critical Verification 9 (exact deletion targets + confirmation that CORPUS_SECTIONS/insertCitationMarkers are file-local with no other importers) + D-13/D-14 (delete, no re-light)
  </read_first>
  <action>
    In `ExpertChatThread.tsx`, delete: the `CORPUS_SECTIONS` map (`:26-38`); the `insertCitationMarkers` function (`:331-337`) AND its call site — change `{insertCitationMarkers(content)}` (`:389`) to `{content}`; and the `if (text.startsWith('§cite:')) {...}` pill-render branch inside the `code` component (`:363-377`), KEEPING the real-code fallback (`:378-385`). Update the JSDoc header line (`:6`) that documents "§citation pills inline" to remove that claim. Do NOT touch `VALID_FRAMES` / `parseFrameTag` / the board-frame pills (`:40-43,402+`) — a separate feature. No replacement, no positive re-light (D-14). Raycast tokens unchanged.
  </action>
  <verify>
    <automated>grep -c "CORPUS_SECTIONS\|insertCitationMarkers\|§cite" src/components/command-bar/ExpertChatThread.tsx; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Zero citation-pill references remain: `grep -c "CORPUS_SECTIONS\|insertCitationMarkers\|§cite" src/components/command-bar/ExpertChatThread.tsx` returns 0.
    - The code component renders `{content}` directly (no insertCitationMarkers wrapper); the real-code fallback branch is intact.
    - `VALID_FRAMES`/`parseFrameTag` board-frame pills untouched: `grep -c "VALID_FRAMES\|parseFrameTag" src/components/command-bar/ExpertChatThread.tsx` unchanged from baseline.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Fake §N citation pills fully removed; board-frame pills + real-code render intact; build green.</done>
</task>

</tasks>

<verification>
- `npm test` full suite green (kc + flash).
- No ENGINE_VERSION bump; KC_GEN_VERSION bumped exactly once (Task 2).
- compiled.ts only ever changed via regen-kc.ts (no hand-edits).
- `npm run build` + `npm run lint` clean.
</verification>

<success_criteria>
- Generation reflects the creator's voice (voice survives the cap, carries a style directive).
- Hooks ground on the 26 owned exemplars silently (no pills, never emitted).
- The chat no longer renders fabricated citation pills.
</success_criteria>

<artifacts_this_phase_produces>
NEW/CHANGED symbols (exclude from source-grounding drift verification): the reordered MODE_ROLES arrays, the strengthened formatVoice header, the 26-template exemplar block in hooks.md + its recompiled KC_HOOKS_SLICE bytes, the bumped KC_GEN_VERSION ("gen.1.1.0"). HONESTY-01 is pure deletion (no new symbol).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/14-kc-grounding-quality-loop-expansion-the-durable-moat-not-yet/14-03-SUMMARY.md` when done.
</output>
