---
phase: 07-audience-as-front-door-surface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/audience-lens/audience-presence.tsx
  - src/components/audience-lens/__tests__/audience-presence.test.tsx
autonomous: true
requirements: [UX-01, UX-03]
must_haves:
  truths:
    - "The switcher popover renders `── Socials ──` and (only when a General audience is owned) `── General ──` section headers via groupAudiences()."
    - "Each row shows a neutral Directional/Validated trust badge from resolveTier (no accent)."
    - "A `+ Build an audience` row at the bottom invokes the host onBuildAudience callback."
    - "A creator who owns zero General audiences NEVER sees a `── General ──` header (byte-identical Socials)."
    - "A General audience drives the reactor; a person-SIM (1 persona) presents as a single reactor; the buildAudienceRepaint undefined→no-op branch is untouched."
  artifacts:
    - path: "src/components/audience-lens/audience-presence.tsx"
      provides: "groupAudiences-sectioned switcher + trust badge + onBuildAudience row + person-SIM single-reactor framing"
      contains: "groupAudiences"
    - path: "src/components/audience-lens/__tests__/audience-presence.test.tsx"
      provides: "mode-section + Build-row + General-reactor assertions"
      contains: "Build an audience"
  key_links:
    - from: "AudiencePresence switcher"
      to: "groupAudiences(audiences)"
      via: "replace the flat audiences.map with sectioned render"
      pattern: "groupAudiences"
    - from: "switcher rows"
      to: "resolveTier(audience)"
      via: "leaf import, never the pack barrel (BUILD-01)"
      pattern: "resolveTier"
---

<objective>
Promote the LIVE `AudiencePresence` switcher into the Mode-sectioned front-door picker (UX-01 / D-02) and generalize the ambient reactor to General audiences (UX-03 / D-06) — reusing the existing mechanism, with the creator/Socials render byte-identical.

Per RESEARCH Assumption A1: the correct surface is the live `AudiencePresence` switcher (presence.tsx L307–359), NOT the dead `audience-chip.tsx` (resurrecting it doubles the picker — treat its markup as reference only). Per D-06 / RESEARCH Pitfall 4: "reacts live as you draft" is satisfied by the existing focus/ask reactor (`AbortController` throttle); this plan does NOT add a per-keystroke loop.

Purpose: make the generality built in P3/P5/P6 reachable from the front door, sectioned by Mode, without touching the byte-identical creator path.
Output: a sectioned switcher with a Build entry + trust badges + person-SIM single-reactor framing + extended guard test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-audience-as-front-door-surface/07-CONTEXT.md
@.planning/phases/07-audience-as-front-door-surface/07-RESEARCH.md
@.planning/phases/07-audience-as-front-door-surface/07-PATTERNS.md
@.planning/phases/07-audience-as-front-door-surface/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section the switcher via groupAudiences + trust badges + `+ Build an audience` row</name>
  <read_first>
    - src/components/audience-lens/audience-presence.tsx — the file being modified: switcher popover (L307–359), the flat `audiences.map` rows (L320–347), the section-header pattern (L314–316), the footer `Manage audiences` Link (L348–357), `AudiencePresenceProps` (L66–90), the props destructure (L92–105).
    - src/components/audience/audience-display.ts — `groupAudiences()` (L126–146): returns `{ baseline, templates, generalTemplates, yours }`; routes `mode==='general'` FIRST (Pitfall 5 — keeps the `mode:'socials'` baseline in Socials).
    - src/lib/audience/resolve-tier.ts — `resolveTier(audience)` (L40); import the LEAF, never the pack barrel (BUILD-01 bundle-leak discipline).
    - 07-UI-SPEC.md `### S1` + `## Copywriting Contract` (section headers `Socials`/`General`, `+ Build an audience`, `Directional`/`Validated`); PATTERNS.md `### audience-presence.tsx`.
  </read_first>
  <action>
    Add an optional `onBuildAudience?: () => void` prop to `AudiencePresenceProps` and the destructure. Replace the flat `audiences.map(...)` inside the switcher popover with a sectioned render: call `groupAudiences(audiences)` → `{ baseline, templates, generalTemplates, yours }`. Render a `── Socials ──` header (reuse the L314–316 header markup: 11px/600/uppercase/muted) over `baseline + templates + yours.filter(a => a.mode === "socials")`. Render a `── General ──` header over `generalTemplates + yours.filter(a => a.mode === "general")` ONLY when that combined set is non-empty (byte-identical guard: a creator with zero General audiences sees no General header). Keep the existing row markup verbatim (Users icon, name, sub-meta, Check) and ADD a right-aligned neutral trust badge using `resolveTier(a)` rendering the literal text `Directional`/`Validated` in 11px/500 muted (`var(--color-foreground-muted)`) — NO accent. Replace/augment the footer `Manage audiences` Link region: keep a `+ Build an audience` row (plain `Plus` glyph + cream label, NO accent) that calls `onBuildAudience?.()` then closes the switcher (`setSwitcherOpen(false)`); keep the existing `Manage audiences` Link below it (or fold it — discretion, but do not remove the ability to reach `/audience`). Preserve the existing selection logic (`on = a.is_general ? isGeneral : a.id === selectedAudienceId`) and `handleSelect`.
  </action>
  <acceptance_criteria>
    - `grep -c "groupAudiences" src/components/audience-lens/audience-presence.tsx` ≥ 1.
    - Test: with a Socials-only audience list, the popover renders no `── General ──`/`General` section header (byte-identical).
    - Test: with at least one `mode:'general'` audience, both `Socials` and `General` section headers render and the General audience appears under General.
    - Test: a `+ Build an audience` control is present and clicking it fires the `onBuildAudience` mock.
    - Each rendered row shows `Directional` or `Validated` text; no `text-accent`/`#d97757`/coral added (the existing liveness dot at L300–302 is the ONLY sanctioned accent and is untouched).
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/audience-lens/__tests__/audience-presence.test.tsx src/components/reading/__tests__/reskin-matte.test.ts</automated>
  </verify>
  <done>Switcher is Mode-sectioned via groupAudiences with trust badges + a `+ Build an audience` row; no General header when none owned; reskin-matte green.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize the reactor — person-SIM single-reactor framing (reuse the existing mechanism)</name>
  <read_first>
    - src/components/audience-lens/audience-presence.tsx — `personas`/`isGeneral`/`rosterCount` (L109–112), `pulseText` (L120–123), `peekDots`/`heroDots` (L125–126), the liveness dot (L298–303), the reactor view body below L360.
    - src/lib/engine/flash/build-reaction-panel.ts — `buildAudienceRepaint(audience)` (L68–99): returns `undefined` for `is_general`/empty personas → byte-identical no-op. DO NOT change this branch.
    - src/lib/engine/__tests__/audience-regression-gate.test.ts — BLOCKING gate (ENGINE_VERSION `3.20.0`, General injects no override) — must stay green.
    - 07-UI-SPEC.md `### S5` — General panel-SIM uses the same multi-persona presentation; a person-SIM (1 persona) presents as a single reactor; mechanism UNCHANGED.
  </read_first>
  <action>
    The reactor view is already audience-agnostic and is driven by `audience.personas` + `focus`. Generalize the framing for General audiences WITHOUT changing the reaction mechanism: when the active audience is a General person-SIM (exactly one persona — detect via `audience.mode === "general" && personas.length === 1`), present a SINGLE reactor (one dot / one identity reacting "as that one person") rather than a swarm — e.g. clamp `rosterCount`/the dot build to 1 and frame `pulseText` for a single reactor. For a General panel-SIM (>1 persona) keep the existing multi-persona presentation. Do NOT touch the `is_general`/empty no-op path, do NOT alter `buildAudienceRepaint`, and do NOT add a keystroke debounce — reuse the existing focus/`AbortController` ask throttle (D-06 "reusing the existing mechanism"). Keep the single liveness dot as the only accent. The Socials render path must be unchanged for `is_general` and multi-persona socials audiences.
  </action>
  <acceptance_criteria>
    - Test: a General audience with personas drives the reactor view (the presence renders its roster/pulse for a `mode:'general'` audience, not a crash/empty).
    - Test: a General person-SIM (1 persona) presents a single reactor (assert one reactor dot / single-reactor pulse copy), not the multi-dot swarm.
    - `node ./node_modules/vitest/vitest.mjs run src/lib/engine/__tests__/audience-regression-gate.test.ts` stays GREEN (ENGINE_VERSION 3.20.0; General injects no override).
    - No diff to `buildAudienceRepaint` and no new accent introduced.
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/audience-lens/__tests__/audience-presence.test.tsx src/lib/engine/__tests__/audience-regression-gate.test.ts</automated>
  </verify>
  <done>General audiences drive the reactor; person-SIM presents as a single reactor; engine regression gate stays green; mechanism unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Extend audience-presence.test.tsx (sections + Build row + General reactor)</name>
  <read_first>
    - src/components/audience-lens/__tests__/audience-presence.test.tsx — the existing test (render helpers, audience fixtures); extend, don't weaken.
    - 07-VALIDATION.md `## Wave 0 Requirements` (extend this test for sections + Build row + General reactor).
  </read_first>
  <action>
    Add test cases covering Task 1 + Task 2: (a) Socials-only list → no General header; (b) mixed list with a `mode:'general'` audience → both `Socials` and `General` headers, General audience under General, trust badge text present; (c) `+ Build an audience` row present and fires `onBuildAudience`; (d) a General audience renders the reactor and a 1-persona General audience shows the single-reactor framing. Build `mode:'general'` audience fixtures inline (reuse existing fixture shape; set `mode:'general'`, `personas` length 1 and >1 as needed). Provide the `onBuildAudience` mock via props.
  </action>
  <acceptance_criteria>
    - `node ./node_modules/vitest/vitest.mjs run src/components/audience-lens/__tests__/audience-presence.test.tsx` passes with the new cases (use the vitest.mjs form, not the fake-PASS shim).
    - The test file references `Build an audience` and a `mode:'general'` fixture.
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/audience-lens/__tests__/audience-presence.test.tsx</automated>
  </verify>
  <done>Extended guard test green, asserting the sections, Build row, trust badges, and General reactor.</done>
</task>

</tasks>

<artifacts_produced>
New symbols this plan creates:
- `onBuildAudience` optional prop on `AudiencePresenceProps`
- The `+ Build an audience` switcher row (calls onBuildAudience)
- Mode-sectioned switcher render (Socials/General via groupAudiences)
- Per-row trust badge (resolveTier text) in the switcher
- Person-SIM single-reactor framing branch
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| audience list → render | Audience names/sub-meta render as React children (auto-escaped); no new untrusted sink. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | switcher row labels | mitigate | Audience name/success-criterion render as plain React children (default escaping); do NOT add dangerouslySetInnerHTML to the sectioned rows. |
| T-07-02-02 | Information disclosure | trust badge | mitigate | `resolveTier` is Directional-by-rule for General; never silently render Validated for a General SIM — import the leaf resolver (single source of truth, BUILD-01 import discipline avoids leaking the pack barrel into the client bundle). |
| T-07-02-03 | Elevation | reactor mechanism | accept | No new server call; reuses the existing audience-agnostic reactor + AbortController throttle; cost stays bounded. |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/components/audience-lens/__tests__/audience-presence.test.tsx` green.
- `node ./node_modules/vitest/vitest.mjs run src/lib/engine/__tests__/audience-regression-gate.test.ts` green (BLOCKING).
- `node ./node_modules/vitest/vitest.mjs run src/components/reading/__tests__/reskin-matte.test.ts` green.
</verification>

<success_criteria>
The switcher is the Mode-sectioned front-door picker with a Build entry + trust badges; the reactor is generalized to General (person-SIM single reactor); the byte-identical Socials/creator path and the engine regression gate are preserved.
</success_criteria>

<output>
Create `.planning/phases/07-audience-as-front-door-surface/07-02-SUMMARY.md` when done.
</output>
