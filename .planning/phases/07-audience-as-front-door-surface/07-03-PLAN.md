---
phase: 07-audience-as-front-door-surface
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/audience/audience-repo.ts
  - src/lib/audience/__tests__/build-clone.test.ts
autonomous: true
requirements: [UX-04]
must_haves:
  truths:
    - "Cloning a GENERAL_TEMPLATE produces a saved owned General SIM (mode:'general') via createAudience."
    - "The sentinel id (e.g. 'template-analyst') and virtual user_id ('__virtual__') are stripped — never inserted."
    - "The clone's name is the derived editable name (capped to the WritableAudienceSchema 80-char limit)."
  artifacts:
    - path: "src/lib/audience/audience-repo.ts"
      provides: "cloneTemplateAudience helper"
      exports: ["cloneTemplateAudience"]
    - path: "src/lib/audience/__tests__/build-clone.test.ts"
      provides: "clone → createAudience(mode:'general', sentinel stripped) assertions"
      contains: "cloneTemplateAudience"
  key_links:
    - from: "cloneTemplateAudience"
      to: "createAudience(supabase, {...cloneable, mode:'general'})"
      via: "strip SENTINEL_IDS + virtual fields, then save"
      pattern: "createAudience"
---

<objective>
Add the template clone-and-edit lib helper (UX-04 / D-03 "From a template" path): `cloneTemplateAudience` reads a `GENERAL_TEMPLATES` entry, strips its sentinel id + virtual fields, and saves it through the existing `createAudience` as an owned, editable General SIM (the moat object — turning today's select-only presets into owned SIMs).

This is the lib leaf for the Build chooser's template path. The chooser UI + composer wiring is the Wave-3 plan 07-05; this plan ships the validated save helper independently (no UI, no file overlap), so it runs in parallel.

Purpose: one validated save target for the template Build path; never insert a sentinel id; access control via the existing session-derived `user_id`.
Output: `cloneTemplateAudience` + a unit test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-audience-as-front-door-surface/07-CONTEXT.md
@.planning/phases/07-audience-as-front-door-surface/07-RESEARCH.md
@.planning/phases/07-audience-as-front-door-surface/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cloneTemplateAudience helper in audience-repo.ts</name>
  <read_first>
    - src/lib/audience/audience-repo.ts — the file being modified: `createAudience` (L445–475, session-derived user_id CR-01, Zod-validated), `GENERAL_TEMPLATES` (L117–220, `template-analyst` L119 / `template-hiring` L170, each carries `id` + `user_id:'__virtual__'` + `mode:'general'`), `SENTINEL_IDS` (L223), `WritableAudienceSchema` (L258–288, accepts mode/success_criterion/custom_context/personas/persona_weights/signature; name ≤ 80).
    - PATTERNS.md `### audience-repo.ts` (clone pattern: strip id/user_id/created_at/updated_at, preserve mode:'general'); RESEARCH `### Pattern 3` + `## Don't Hand-Roll`.
  </read_first>
  <behavior>
    - cloneTemplateAudience(supabase, 'template-analyst') → calls createAudience with an input whose `mode === 'general'`, whose `id`/`user_id`/`created_at`/`updated_at` are absent, and returns the saved Audience.
    - An unknown templateId → throws (no silent createAudience call).
    - A provided name longer than 80 chars is truncated to ≤ 80 before save (or relies on the schema cap — assert the saved input name ≤ 80).
    - Default name (no name arg) derives from the template name (e.g. "Analyst Panel").
  </behavior>
  <action>
    Export `async function cloneTemplateAudience(supabase: SupabaseClient, templateId: string, name?: string): Promise<Audience>`. Implementation: `const tpl = GENERAL_TEMPLATES.find(t => t.id === templateId)`; throw `Error("unknown template")` if absent. Destructure to drop the non-writable/sentinel fields: `const { id, user_id, created_at, updated_at, ...cloneable } = tpl;` (these satisfy `SENTINEL_IDS` stripping — never pass them on). Build the input `{ ...cloneable, name: (name ?? tpl.name).slice(0, 80) }` (mode:'general' is carried in `cloneable`). Call and return `createAudience(supabase, input)` — which validates via `WritableAudienceSchema` and derives `user_id` from session (CR-01, never client-trusted). Do NOT add a new insert path; reuse `createAudience` verbatim.
  </action>
  <acceptance_criteria>
    - `grep -c "export async function cloneTemplateAudience" src/lib/audience/audience-repo.ts` === 1.
    - Test (mocked supabase + spied createAudience): clone of `template-analyst` calls createAudience with `input.mode === 'general'` and with `input.id`/`input.user_id` undefined.
    - Test: unknown templateId throws and createAudience is NOT called.
    - Test: an 80+ char name is saved truncated to ≤ 80.
    - `node ./node_modules/typescript/bin/tsc --noEmit` shows no NEW error on audience-repo.ts vs baseline.
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/audience/__tests__/build-clone.test.ts</automated>
  </verify>
  <done>`cloneTemplateAudience` saves an owned `mode:'general'` SIM via createAudience with the sentinel id/virtual user_id stripped and the name capped.</done>
</task>

<task type="auto">
  <name>Task 2: build-clone.test.ts unit test</name>
  <read_first>
    - src/lib/audience/__tests__/audience-repo.test.ts — existing test conventions (how supabase + auth.getUser are mocked); mirror that mock style in the new file.
    - src/lib/audience/__tests__/audience-repo-mode.test.ts — existing `mode` assertions to mirror.
    - 07-VALIDATION.md `## Wave 0 Requirements` (add template-clone test → mode:'general', sentinel stripped).
  </read_first>
  <action>
    Create `src/lib/audience/__tests__/build-clone.test.ts` exercising `cloneTemplateAudience` with a mocked SupabaseClient (auth.getUser returns a user; the insert chain returns a row). Spy on / intercept `createAudience` input (or assert the row payload) to verify `mode:'general'`, absent `id`/`user_id` in the writable input, the derived/truncated name, and the unknown-template throw. Use the repo's documented runner (`node ./node_modules/vitest/vitest.mjs run`).
  </action>
  <acceptance_criteria>
    - `node ./node_modules/vitest/vitest.mjs run src/lib/audience/__tests__/build-clone.test.ts` passes (NOT the fake-PASS shim).
    - The test asserts the sentinel id is never present in the createAudience/insert input.
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/audience/__tests__/build-clone.test.ts src/lib/audience/__tests__/audience-repo.test.ts</automated>
  </verify>
  <done>New build-clone.test.ts green; existing audience-repo tests stay green.</done>
</task>

</tasks>

<artifacts_produced>
New symbols this plan creates:
- `cloneTemplateAudience(supabase, templateId, name?)` exported from audience-repo.ts
- `src/lib/audience/__tests__/build-clone.test.ts`
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client Build action → audiences table | A template clone becomes an owned DB row; the write must stay under session RLS with no client-trusted identity. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Elevation (IDOR) | cloneTemplateAudience | mitigate | Reuse `createAudience` — `user_id` is derived from `supabase.auth.getUser()` (CR-01), never from input; RLS `audiences_all_own` scopes the insert. The helper accepts no `user_id`/`id`. |
| T-07-03-02 | Tampering | sentinel id reuse | mitigate | Strip `id` (SENTINEL_IDS like `template-analyst`) and `user_id:'__virtual__'` before save — never insert a sentinel; test asserts absence. |
| T-07-03-03 | Tampering (stored XSS / DoS) | name / success_criterion / custom_context | mitigate | `createAudience` validates via `WritableAudienceSchema` (name ≤ 80, success_criterion/note ≤ 2000, custom_context array ≤ 50) — the Zod boundary caps the cloned fields. |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/lib/audience/__tests__/build-clone.test.ts` green.
- `node ./node_modules/vitest/vitest.mjs run src/lib/audience/__tests__/audience-repo.test.ts src/lib/audience/__tests__/audience-repo-mode.test.ts` green (no regression).
</verification>

<success_criteria>
`cloneTemplateAudience` is the validated, session-scoped save target for the template Build path; sentinel ids never leak into the DB; name capped.
</success_criteria>

<output>
Create `.planning/phases/07-audience-as-front-door-surface/07-03-SUMMARY.md` when done.
</output>
