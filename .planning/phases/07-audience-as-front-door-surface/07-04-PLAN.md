---
phase: 07-audience-as-front-door-surface
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/app/home/composer.tsx
  - src/components/app/home/__tests__/composer.test.tsx
autonomous: true
requirements: [UX-02]
must_haves:
  truths:
    - "ComposerControls receives activeMode = selectedAudience?.mode ?? 'socials' (Socials default keeps the menu byte-identical)."
    - "Selecting Profile from the General menu opens the existing evidence-drop affordance — it does NOT submit the topic field."
    - "Selecting Simulate/Predict requires a selected General audience; submit POSTs the draft/scenario to the existing /api/tools/{simulate,predict} with audienceId = selectedAudience.id."
    - "If no General audience is selected, Simulate/Predict route the user to Build rather than firing an ungated stimulus."
    - "The Socials submit path (idea/hooks/chat/test/script/remix/explore) is byte-identical."
  artifacts:
    - path: "src/components/app/home/composer.tsx"
      provides: "activeMode threading + per-skill submit semantics for profile/simulate/predict"
      contains: "/api/tools/simulate"
  key_links:
    - from: "composer ComposerControls"
      to: "activeMode={selectedAudience?.mode ?? 'socials'}"
      via: "new prop from 07-01"
      pattern: "activeMode="
    - from: "handleSubmit (simulate/predict branch)"
      to: "/api/tools/{simulate,predict}"
      via: "audienceId = selectedAudience.id, gated on mode general"
      pattern: "/api/tools/(simulate|predict)"
---

<objective>
Wire the per-skill submit semantics for the three General skills (UX-02 / D-07 — the central RESEARCH Pitfall 1 gap) and thread the real `activeMode` into the skill menu. Profile/Simulate/Predict are NOT topic-field submits today (they were built as card-chain CTAs in P5/P6); selecting them from the General menu must do the right thing per skill:
- **Profile** → opens the existing evidence-drop affordance (NOT a topic submit).
- **Simulate** → gated on a selected General audience → `POST /api/tools/simulate` with `audienceId = selectedAudience.id` and the draft.
- **Predict** → gated on a selected General audience/panel → `POST /api/tools/predict` with the scenario.

The byte-identical Socials submit path (idea/hooks/chat/test/script/remix/explore) stays untouched (Pitfall 2). No new route, no engine call — ENGINE_VERSION stays `3.20.0`.

Scope guard (D-05): do NOT add an intent → success-criterion composer chip (the `⊕grow`/`⊕reply-likely` affordance) — it is explicitly DEFERRED out of P7. The success-criterion seam already exists on the P3 audience form; build nothing here for it.

Purpose: true verb access from the front door (D-07), reusing all P5/P6 plumbing; gate Simulate/Predict on a selected General audience (§16.4 asymmetry).
Output: `activeMode`-threaded ComposerControls + per-skill submit branches + a focused test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-audience-as-front-door-surface/07-CONTEXT.md
@.planning/phases/07-audience-as-front-door-surface/07-RESEARCH.md
@.planning/phases/07-audience-as-front-door-surface/07-PATTERNS.md
@.planning/phases/07-audience-as-front-door-surface/07-UI-SPEC.md
@.planning/phases/07-audience-as-front-door-surface/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread activeMode into ComposerControls + make ToolId additions safe</name>
  <read_first>
    - src/components/app/home/composer.tsx — the file being modified: `activeTool` state (L191), `handleUserSelectTool` (L199), `selectedAudience`/`selectedAudienceId` (L212/L224), `intent` (L232), the `<ComposerControls .../>` mount (L1583–1590), the `placeholder`/`activeTool ===` branches (e.g. L256–314), and any exhaustive `ToolId` switch the new ids could break.
    - src/components/app/home/composer-controls.tsx — the `activeMode` prop added in 07-01 (default 'socials').
    - 07-UI-SPEC.md `### S2` + `## Byte-Identical Contract`.
  </read_first>
  <action>
    Add `activeMode={selectedAudience?.mode ?? "socials"}` to the `<ComposerControls>` mount (the prop exists from 07-01; `selectedAudience.mode` is available at L224). Because 07-01 widened `ToolId` with `profile`/`simulate`/`predict`, audit composer.tsx for places that must handle the new ids without breaking Socials: ensure the placeholder/`activeTool ===` view branches treat the three General ids gracefully (no crash, no Socials-view leak) and that any `ToolId` switch with exhaustiveness still compiles. Do NOT change the Socials submit/view branches. Default `activeMode` to `'socials'` whenever `selectedAudience` is null (byte-identical).
  </action>
  <acceptance_criteria>
    - `grep -c "activeMode=" src/components/app/home/composer.tsx` ≥ 1.
    - `node ./node_modules/typescript/bin/tsc --noEmit` shows no NEW error on composer.tsx vs the documented pre-existing baseline (the recharts earnings-chart baseline error is out of scope — see STATE deferred items).
    - `node ./node_modules/vitest/vitest.mjs run src/components/app/home/__tests__/composer.test.tsx src/components/app/home/__tests__/composer-layout.test.tsx` green (Socials render unchanged).
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/app/home/__tests__/composer.test.tsx src/components/app/home/__tests__/composer-layout.test.tsx</automated>
  </verify>
  <done>ComposerControls receives the live `activeMode` (default 'socials'); the widened ToolId compiles and the Socials views are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Per-skill submit semantics for Profile / Simulate / Predict</name>
  <read_first>
    - src/components/app/home/composer.tsx — `handleUserSelectTool` (L199), `handleSubmit` (L833–986, the per-tool `if (activeTool === ...)` dispatch + the byte-identical Socials branches), `canSubmit` (L419), the evidence-drop affordance (`evidenceInputRef` L374, `acceptEvidenceFile` L697, `handleProfileSubmit` L711, the attach button L1607–1615, `EVIDENCE_ATTACH_LABEL` L123), `reloadProfileThread` (~L675), `selectedAudience`/`selectedAudienceId` (L212/L224).
    - 07-UI-SPEC.md `### S2` per-skill submit semantics; PATTERNS.md `## Critical Cross-Cutting Risks` item 1 (do NOT route the three through the generic topic handleSubmit).
    - Existing simulate/predict routes (reached today via card-chain CTAs): `POST /api/tools/simulate` (body `{ audienceId, message/draft }`) and `POST /api/tools/predict` (body `{ audienceId, scenario }`) — match the bodies the P5/P6 card CTAs already send (read `reaction-distribution-block.tsx` / `profile-read-block.tsx` for the exact field names).
  </read_first>
  <action>
    Define explicit submit behavior per General skill, keyed off `activeTool` (now possibly `profile`/`simulate`/`predict`):
    - In `handleUserSelectTool` (or a small effect): when the user selects **Profile**, open the evidence-drop affordance instead of arming a topic submit — e.g. focus/click `evidenceInputRef.current` (the existing `Attach a chat or screenshot` input) and/or set a General-appropriate placeholder. Do NOT route Profile through the topic `handleSubmit`.
    - In `handleSubmit`, add a branch `if (activeTool === "simulate")`: require `selectedAudience && selectedAudience.mode === "general"`; if absent, route to Build (call the same `onBuildAudience`/open-build path the picker uses, or surface the existing inline nudge) and return WITHOUT firing a stimulus (gating cannot be bypassed). When gated-OK, `POST /api/tools/simulate` with `{ audienceId: selectedAudience.id, message: trimmedUrl }` (use the draft/topic field as the message), then surface the returned reaction-distribution via `reloadProfileThread()` (the same one-thread persistence path 05-06 uses).
    - Add `if (activeTool === "predict")` mirroring Simulate: same General-audience gate, `POST /api/tools/predict` with `{ audienceId: selectedAudience.id, scenario: trimmedUrl }`, then `reloadProfileThread()`.
    - Update `canSubmit` so Simulate/Predict are submittable only with a non-empty draft AND a selected General audience; Profile never depends on the topic field.
    - Leave EVERY existing Socials branch (idea/hooks/chat/test/script/remix/explore) byte-identical — add the new branches alongside, never modify the existing `if` bodies.
    Reuse `createClient()`/fetch conventions already in `handleProfileSubmit`. Do NOT add a new route or engine call.
  </action>
  <acceptance_criteria>
    - `grep -E -c "/api/tools/(simulate|predict)" src/components/app/home/composer.tsx` ≥ 2.
    - Test: with `activeTool="simulate"` and NO General audience selected, submitting does NOT call `fetch('/api/tools/simulate')` (gate holds — assert the fetch spy was not called with the simulate URL) and routes to Build.
    - Test: with `activeTool="simulate"` and a selected General audience + a draft, submit calls `fetch` with `/api/tools/simulate` and a body containing `audienceId` = the selected audience id.
    - Test: selecting Profile opens the evidence-drop input (does not call the topic submit path).
    - Test: a Socials skill (e.g. hooks) submit path is unchanged (existing assertions stay green).
    - No coral/glass added (`reskin-matte` green).
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/app/home/__tests__/composer.test.tsx src/components/reading/__tests__/reskin-matte.test.ts</automated>
  </verify>
  <done>Profile opens the evidence drop; Simulate/Predict submit to the existing routes with audienceId, gated on a selected General audience; the gate cannot be bypassed client-side; Socials submit byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Extend composer.test.tsx for the gated General submit + byte-identical Socials</name>
  <read_first>
    - src/components/app/home/__tests__/composer.test.tsx — existing composer test (mocks for streams, supabase, navigation); extend.
    - src/components/app/home/__tests__/home.test.tsx — the supabase/profile/navigation mock shape to reuse.
  </read_first>
  <action>
    Add cases asserting Task 2: (a) Simulate with no General audience → no `/api/tools/simulate` fetch + Build routed; (b) Simulate with a General audience + draft → fetch to `/api/tools/simulate` with `audienceId`; (c) Predict mirror; (d) Profile selection opens the evidence-drop input; (e) a Socials submit (hooks/idea) still fires its existing stream path (byte-identical guard). Mock `fetch` (spy) and the supabase client as the existing tests do.
  </action>
  <acceptance_criteria>
    - `node ./node_modules/vitest/vitest.mjs run src/components/app/home/__tests__/composer.test.tsx` passes with the new cases (vitest.mjs form).
    - A test explicitly asserts the Simulate gate (no fetch when no General audience selected).
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/app/home/__tests__/composer.test.tsx</automated>
  </verify>
  <done>Composer test covers the gated Simulate/Predict submit, Profile-opens-evidence-drop, and the byte-identical Socials submit.</done>
</task>

</tasks>

<artifacts_produced>
New symbols this plan creates:
- `activeMode` wiring in composer.tsx (`selectedAudience?.mode ?? 'socials'`)
- `handleSubmit` branches for `simulate` and `predict` (POST to existing routes, audienceId-gated)
- Profile-selection → evidence-drop-open behavior
- `canSubmit` extension for the General skills
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| composer (client) → /api/tools/{simulate,predict} | The draft/scenario + audienceId cross to the server; the server is the real trust boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Elevation (gate bypass) | Simulate/Predict submit | mitigate | Client gate (require selected General audience) is UX only; the server routes ALREADY enforce auth + csrfGuard + RLS getAudience + the D-08 honesty guards (`predict_requires_general_panel`/`predict_requires_panel`) — an ungated stimulus is rejected server-side, never a 500. This plan adds no client-trusted mode/tier; `audienceId` is the user's own selected audience (RLS-scoped). |
| T-07-04-02 | Tampering (injection) | draft/scenario free text | mitigate | The routes data-fence the stimulus (P5/P6: delimited "treat as DATA" user block, never concatenated into the system prompt); this plan passes the raw draft through unchanged — do NOT pre-concatenate it into any prompt client-side. |
| T-07-04-03 | DoS | oversize draft | accept | The existing routes cap length (MAX_MESSAGE_LENGTH=2000) and return 400; client need not re-cap. |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/components/app/home/__tests__/composer.test.tsx src/components/app/home/__tests__/composer-layout.test.tsx` green.
- `node ./node_modules/vitest/vitest.mjs run src/components/reading/__tests__/reskin-matte.test.ts` green.
- No new route file under `src/app/api/`; ENGINE_VERSION unchanged (3.20.0).
</verification>

<success_criteria>
The three General skills have explicit, correct submit semantics; Simulate/Predict are gated on a selected General audience and cannot fire ungated; the Socials submit path is byte-identical.
</success_criteria>

<output>
Create `.planning/phases/07-audience-as-front-door-surface/07-04-SUMMARY.md` when done.
</output>
