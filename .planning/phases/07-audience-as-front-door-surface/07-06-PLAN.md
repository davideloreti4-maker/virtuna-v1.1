---
phase: 07-audience-as-front-door-surface
plan: 06
type: execute
wave: 4
depends_on: ["07-05"]
files_modified:
  - src/components/app/home/home-starter.tsx
  - src/components/app/home/composer.tsx
  - src/app/(app)/home/page.tsx
  - src/components/app/home/__tests__/home.test.tsx
autonomous: false
requirements: [UX-05]
must_haves:
  truths:
    - "The empty home renders the 3 locked chips verbatim: 'Test an idea on your audience', 'Profile a chat', 'Predict an outcome'."
    - "A one-tap 'See it in action' demo runs the pre-seeded profile-chat through Profile→Read (POST /api/tools/profile with a canned fixture); 'Dismiss' is a muted text link."
    - "The demo is show-once: a localStorage flag (set on first run OR dismiss) hides it on the next mount."
    - "Tapping a chip drives the relevant composer flow (Test → idea submit on the active audience; Profile → evidence drop; Predict → Predict flow gated on a General audience)."
    - "The greeting + composer are unchanged; chips + demo are an additive block in the previously-empty region."
  artifacts:
    - path: "src/components/app/home/home-starter.tsx"
      provides: "3 starter chips + first-run demo + localStorage show-once"
      min_lines: 40
    - path: "src/components/app/home/__tests__/home.test.tsx"
      provides: "3-chips + one-tap-demo + show-once assertions (replaces NO-chips/NO-demo)"
      contains: "Profile a chat"
  key_links:
    - from: "HomeStarter demo CTA"
      to: "/api/tools/profile"
      via: "canned fixture POST then thread reload"
      pattern: "/api/tools/profile"
    - from: "composer empty region"
      to: "<HomeStarter>"
      via: "rendered when !hasConversationContent"
      pattern: "HomeStarter"
---

<objective>
Unlock the home empty state (UX-05 / D-04): render the greeting + composer + the three LOCKED chips, plus a one-tap, show-once first-run demo that runs a PRE-SEEDED sample chat through Profile→Read — the cold-start wow with no chat-export friction (VISION §15.5). The home was deliberately locked empty in P5 (D-18/D-25, "demo is Phase 5"); P7 is where it unlocks.

Per the home.test.tsx render model (it mounts `<HomeGreeting/> + <Composer/>` directly), the starter block is rendered by the composer in its no-conversation region so the chip handlers wire to composer-internal flows directly (no risky cross-sibling plumbing) and the test sees the chips. The greeting + composer themselves stay unchanged.

This is the LAST plan of the phase — it ends with the mandatory real authed `/home` browser pass (vitest cannot catch Next client/server bundle leaks).

Purpose: deliver the §15.5 cold-start wow + the locked chips; close UX-05.
Output: `home-starter.tsx` + composer empty-region wiring + updated home.test.tsx + the browser-pass checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-audience-as-front-door-surface/07-CONTEXT.md
@.planning/phases/07-audience-as-front-door-surface/07-RESEARCH.md
@.planning/phases/07-audience-as-front-door-surface/07-PATTERNS.md
@.planning/phases/07-audience-as-front-door-surface/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HomeStarter component — 3 locked chips + first-run demo + localStorage show-once</name>
  <read_first>
    - src/app/(app)/home/page.tsx — the LOCKED-empty doc comment (L20–25, "NO starter chips (D-18), NO demo affordance (D-25)") to drop.
    - src/components/app/home/home-page-layout.tsx — the `!hasConversation` empty block (L37–46) for the visual placement reference (chips below greeting).
    - src/components/app/home/composer.tsx — `handleProfileSubmit`/`reloadProfileThread` (L711/~L675) for the canned-fixture demo POST pattern (the demo reuses the same `POST /api/tools/profile` + thread reload), `EVIDENCE_*` constants (L123).
    - localStorage show-once precedent: `src/hooks/use-lens-scale.ts`, `src/components/.../society-selector.tsx` (grep `localStorage`).
    - 07-UI-SPEC.md `### S4` + `## Copywriting Contract` — the 3 chips verbatim, `See it in action`, `Dismiss`, `#2f2e2b` lifted surface, 8px radius, 6% border, NO accent, neutral-cream demo CTA.
  </read_first>
  <action>
    Create `src/components/app/home/home-starter.tsx` (`"use client"`) exporting `HomeStarter` with props `{ onChipTest: () => void; onChipProfile: () => void; onChipPredict: () => void; onDemoComplete?: () => void; }`. Render the three LOCKED chips verbatim — `Test an idea on your audience`, `Profile a chat`, `Predict an outcome` — as `button`s in a single wrapping row (16px gap; each chip 14/400/cream, `#2f2e2b` surface, 8px radius, 6% border / 10% hover, 8×12 padding, NO accent), wired to `onChipTest`/`onChipProfile`/`onChipPredict`. Below the chips render the first-run demo: a neutral-cream (`--color-action`) `See it in action` button + a muted `Dismiss` text link (12/400/muted). Gate the demo block behind a `localStorage` flag key `numen.home.demo.seen` — render the demo only when the flag is absent; set the flag on tap OR on Dismiss (show-once). On `See it in action`: `POST /api/tools/profile` with a small CANNED chat fixture (a few lines of a believable conversation as `{ kind: "text", text: <fixture> }` or the file_text base64 shape the route accepts — match `handleProfileSubmit`'s body), then set the flag and call `onDemoComplete?.()` so the composer reloads the open thread and surfaces the profile-read card. Keep all of it matte, no accent. The chips always render (only the demo is show-once).
  </action>
  <acceptance_criteria>
    - `src/components/app/home/home-starter.tsx` exists and exports `HomeStarter`.
    - The three chip strings appear verbatim (`grep -c "Test an idea on your audience" ... === 1` and same for the other two).
    - `grep -c "numen.home.demo.seen" src/components/app/home/home-starter.tsx` ≥ 1 and `grep -c "/api/tools/profile" src/components/app/home/home-starter.tsx` ≥ 1.
    - No `text-accent`/`#d97757`/coral in the file (dosage); `reskin-matte` green.
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/app/home/__tests__/home.test.tsx src/components/reading/__tests__/reskin-matte.test.ts</automated>
  </verify>
  <done>HomeStarter renders the 3 locked chips + a show-once one-tap profile demo (canned fixture → /api/tools/profile), matte + no accent.</done>
</task>

<task type="auto">
  <name>Task 2: Render HomeStarter in the composer empty region + wire chip handlers; unlock page.tsx doc</name>
  <read_first>
    - src/components/app/home/composer.tsx — `hasConversationContent` (L380–400) for the empty-region gate, `setActiveTool`/`handleUserSelectTool` (L199), the evidence-drop entry (`evidenceInputRef` L374), `selectedAudience` (L224), `reloadProfileThread` (~L675), the render tree where the composer form is returned.
    - src/components/app/home/home-starter.tsx — the props from Task 1.
    - src/app/(app)/home/page.tsx — the doc comment to update.
  </read_first>
  <action>
    In composer.tsx, render `<HomeStarter>` in the composer's no-conversation region (gate on `!hasConversationContent`), placed so it reads below the greeting (top of the empty composer area per UI-SPEC S4). Wire the chip handlers to composer-internal flows: `onChipTest` → select the appropriate idea/test flow on the active audience (e.g. `handleUserSelectTool('idea')` or focus the draft field — match §S4 "Test → idea submit on the active audience"); `onChipProfile` → open the evidence-drop (`evidenceInputRef.current?.click()`); `onChipPredict` → select Predict (`handleUserSelectTool('predict')`), which already gates on a selected General audience (07-04) and routes to Build when none. Pass `onDemoComplete={() => void reloadProfileThread()}`. Update `src/app/(app)/home/page.tsx`: drop the "NO starter chips (D-18), NO demo affordance (D-25)" lock from the doc comment and note the P7 unlock (D-04) — the chips/demo now render in the composer empty region. Keep the greeting + composer markup byte-identical; the starter is an additive block.
  </action>
  <acceptance_criteria>
    - `grep -c "HomeStarter" src/components/app/home/composer.tsx` ≥ 1.
    - page.tsx doc comment no longer asserts "NO starter chips"/"NO demo affordance" as a lock.
    - Test: the chips render on the empty home (no conversation) and do NOT render once conversation content exists.
    - `node ./node_modules/vitest/vitest.mjs run src/components/app/home/__tests__/composer.test.tsx` green (Socials submit + existing behavior unchanged).
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/app/home/__tests__/home.test.tsx src/components/app/home/__tests__/composer.test.tsx</automated>
  </verify>
  <done>HomeStarter mounts in the composer empty region with chip handlers wired to the real flows; page.tsx lock dropped; greeting/composer byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Update home.test.tsx — 3 chips + one-tap demo + show-once (replace NO-chips/NO-demo)</name>
  <read_first>
    - src/components/app/home/__tests__/home.test.tsx — the file being modified: the `Home()` render helper (L68–76, renders HomeGreeting + Composer), the now-obsolete "locked omissions" block (L109–137, asserts NO chips / NO demo), the existing mocks (supabase/profile/navigation/streams).
    - 07-VALIDATION.md `## Wave 0 Requirements` (UPDATE home.test → 3 chips + one-tap demo + show-once).
  </read_first>
  <action>
    Replace the obsolete `describe('Home — locked omissions (D-18 / D-25)')` block. Keep the greeting + glyph + composer assertions. ADD: (a) the 3 locked chips render verbatim on the empty home; (b) the first-run demo CTA `See it in action` + `Dismiss` render when the show-once flag is absent; (c) show-once — with `localStorage` `numen.home.demo.seen` set (seed it in the test), the demo does NOT render on mount, but the chips still render; (d) tapping `See it in action` posts to `/api/tools/profile` (spy fetch) and sets the flag. Mock `localStorage` (happy-dom provides it; clear it in `beforeEach`) and `fetch`. Do NOT weaken the byte-identical greeting/composer assertions.
  </action>
  <acceptance_criteria>
    - `node ./node_modules/vitest/vitest.mjs run src/components/app/home/__tests__/home.test.tsx` passes with the new cases (vitest.mjs form, not the fake-PASS shim).
    - The test asserts all three chip strings and the show-once behavior (demo hidden when the flag is set, chips still shown).
    - The obsolete NO-chips/NO-demo assertions are removed (not commented out).
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/app/home/__tests__/home.test.tsx</automated>
  </verify>
  <done>home.test.tsx asserts the 3 chips + one-tap demo + show-once; greeting/composer assertions preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Real authed /home browser pass (full P7 surface)</name>
  <what-built>The full Audience-as-Front-Door surface: the Mode-sectioned picker (07-02), the mode-scoped skill menu + per-skill submit (07-01/07-04), the Build chooser (07-05), and the home empty state chips + first-run demo (this plan). vitest (node) cannot catch Next client/server bundle leaks (the BUILD-01 class that a prior GSI phase hit) — a real browser pass is mandatory before /gsd-verify-work.</what-built>
  <how-to-verify>
    1. Start dev: `node ./node_modules/next/dist/bin/next dev` (the npx wrapper breaks dev; see MEMORY). Auth via `e2e/create-test-user.ts`.
    2. Open `/home`. Confirm: the 3 chips render verbatim; the `See it in action` demo runs the pre-seeded chat → a Profile→Read card appears in-thread; reload → the demo is gone (show-once) but chips remain.
    3. Open the audience picker (the AudiencePresence switcher): with Socials-only audiences, NO `── General ──` header; `+ Build an audience` present. Build a General SIM via the template path → it appears under `── General ──` and becomes active.
    4. With a General audience active: the skill menu shows Profile/Simulate/Predict (not Hooks/Test). Profile opens the evidence drop; Simulate/Predict gate on the selected General audience (no ungated stimulus fires). Switch back to a Socials audience → the menu is byte-identical (Hooks/Test/Remix/Chat in the original order).
    5. Confirm zero console errors (no bundle-leak / hydration error), zero coral/glass, and the single liveness dot is the only accent.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (console errors, byte-identical regressions, missing surfaces).</resume-signal>
</task>

</tasks>

<artifacts_produced>
New symbols this plan creates:
- `src/components/app/home/home-starter.tsx` — `HomeStarter` component (3 chips + demo + show-once)
- localStorage key `numen.home.demo.seen`
- `<HomeStarter>` mount + chip handlers in composer.tsx (empty region)
- Updated home/page.tsx doc comment (P7 unlock)
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| demo CTA (client) → /api/tools/profile | The canned fixture crosses to the profile route; the route is the trust boundary (auth + caps already enforced). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Tampering | demo fixture | accept | The canned fixture is a static in-repo string (no user input); the profile route re-validates + caps (MAX_EVIDENCE_LENGTH) regardless. |
| T-07-06-02 | Tampering (stored XSS) | chip labels / greeting | mitigate | All copy renders as React children (escaped); no dangerouslySetInnerHTML added to HomeStarter. |
| T-07-06-03 | — (bundle leak / build break) | HomeStarter / composer imports | mitigate | Import only client-safe leaves (no engine pack barrel) — the browser pass (Task 4) catches any Next client/server bundle leak vitest cannot. |
</threat_model>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/components/app/home/__tests__/home.test.tsx src/components/app/home/__tests__/composer.test.tsx` green.
- `node ./node_modules/vitest/vitest.mjs run src/components/reading/__tests__/reskin-matte.test.ts` green.
- Full suite `node ./node_modules/vitest/vitest.mjs run` green before /gsd-verify-work.
- Task 4 human-verify approved (real authed /home browser pass, zero console errors).
</verification>

<success_criteria>
The home empty state seeds the 3 locked chips + a one-tap show-once profile-chat demo (the cold-start wow); the greeting/composer are byte-identical; the full P7 surface passes a real browser check.
</success_criteria>

<output>
Create `.planning/phases/07-audience-as-front-door-surface/07-06-SUMMARY.md` when done.
</output>
