---
phase: 11-explore-audience-curated-discovery
plan: 06
type: execute
wave: 4
depends_on: ["11-05"]
files_modified:
  - src/components/thread/explore-thread-view.tsx
  - src/components/thread/explore-thread-view.test.tsx
autonomous: true
requirements: [EXPLORE-01, EXPLORE-02, EXPLORE-04, EXPLORE-05]
must_haves:
  truths:
    - "ExploreThreadView owns its own idle state (like ChatThreadView) and shows 3 audience-derived quick-action cards (Top performers in my niche today / What competitors shipped / Surprise me) when no grid is streaming/persisted (EXPLORE-04, D-06/D-07)"
    - "Tapping a quick-action runs a preset pull via use-explore-stream start(params) — never auto-fires on render"
    - "The 'What competitors shipped' card degrades to a quiet 'Track an account first' disabled sub-state when no tracked accounts exist — never a fabricated competitor feed (honesty)"
    - "Tapping a tile's 'Remix → Read' launches the existing discover→remix chain handoff verbatim and surfaces the remix-card by reloading the open thread in place (NOT router.push — Explore renders in /home, RESEARCH Q2)"
    - "Tapping '+ Track account' POSTs /api/tracked-accounts and marks the tile tracked; the real persona reaction appears ONLY via the reused remix-card LensTrigger after Remix (no fabricated reaction on the grid, D-02)"
  artifacts:
    - path: "src/components/thread/explore-thread-view.tsx"
      provides: "ExploreThreadView — idle quick-actions + streaming/persisted grid + onRemix(in-place reload)/onTrack handlers + progress + error"
      exports: ["ExploreThreadView"]
      min_lines: 120
  key_links:
    - from: "src/components/thread/explore-thread-view.tsx"
      to: "src/lib/tools/chain-handoff.ts"
      via: "handoffsFor('discover').find(h => h.to === 'remix') — reuse, no fork (D-04/D-05)"
      pattern: "handoffsFor"
    - from: "src/components/thread/explore-thread-view.tsx"
      to: "src/components/thread/outlier-grid-block.tsx"
      via: "renders the grid via MessageBlocks + supplies onRemix/onTrack to OutlierGridBlockRenderer"
      pattern: "onRemix"
---

<objective>
Build `ExploreThreadView` (EXPLORE-01/02/04/05): the thread view that owns Explore's idle quick-actions (D-07), renders the streaming + persisted outlier grid, and owns the `onRemix` and `onTrack` handlers. The tap reuses the shipped `discover→remix` chain verbatim (D-04/D-05) and surfaces the remix-card by reloading the open thread in place (RESEARCH Q2 — Explore renders in `/home`, so reload, not `router.push`). The on-tap real reaction comes free from the reused remix-card's LensTrigger — this view adds NO reaction UI to the grid.

Purpose: EXPLORE-04 (audience-aware quick-actions so a blank thread is never intimidating) + the in-thread tap → Remix → Read loop (EXPLORE-02). Honesty (D-02): no fabricated competitor feed (card 2 degrades), no fabricated reaction on the grid (real reaction is lazy, via the remix-card).
Output: `explore-thread-view.tsx` (idle + streaming + persisted + handlers), with a render/idle test.
</objective>

<artifacts_this_phase_produces>
This plan creates:
- `src/components/thread/explore-thread-view.tsx` — NEW: `ExploreThreadView` props { persistedBlocks, streamingBlocks, stages, isStreaming, error, platform, audience (for quick-action copy + degrade), hasTrackedAccounts, onQuickAction(params), onRetry, onThreadReload }. Renders: idle 3-card quick-action screen (when no content), ProgressChecklist while streaming, the grid via MessageBlocks (streaming + persisted) with `onRemix`/`onTrack` supplied, SkillRunError on error. Owns `handleRemix` (verbatim discover→remix handoff + in-place thread reload) and `handleTrack` (POST /api/tracked-accounts + mark tracked).
- `src/components/thread/explore-thread-view.test.tsx` — NEW: idle-shows-quick-actions, card-2-degrades-without-tracked-accounts, no-auto-fire-on-render.
(11-07 mounts this in composer.tsx, supplies the audience + hasTrackedAccounts + onThreadReload, and gates showExploreView unconditionally like chat.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-RESEARCH.md
@.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExploreThreadView — idle quick-actions + grid + onRemix(reload)/onTrack (EXPLORE-04/02)</name>
  <read_first>
    - src/components/thread/hooks-thread-view.tsx (the thread-view template: the `max-w-[760px] mx-auto flex flex-col gap-6 px-4 py-6` column, ProgressChecklist while streaming, MessageBlocks for streaming + persisted bodies, the SkillRunError block + onRetry — clone this structure; ExploreThreadView ADDS the idle quick-actions like ChatThreadView)
    - src/components/thread/chat-thread-view.tsx (the idle-ownership precedent — a skill view that ALWAYS shows when its chip is active and owns its own empty/idle state; mirror this for D-07)
    - src/app/(app)/discover/discover-client.tsx (the VERBATIM `handleRemix` pattern: `handoffsFor("discover").find(h => h.to === "remix")` → `fetch(handoff.endpoint, { method:"POST", body: JSON.stringify({ url: tile.videoUrl, platform }) })` → set remixPendingId; the standalone client does `router.push("/home")` — DO NOT copy that; reload in place instead, see below)
    - src/components/app/home/composer.tsx (the `handleDevelopRemix` in-place thread-reload pattern lines ~384-410: after the chain POST, `GET /api/threads/open` → filter blocks → setPersisted — Explore's onRemix surfaces the remix-card via an `onThreadReload` callback the composer supplies, RESEARCH Q2)
    - src/lib/tools/chain-handoff.ts (CHAIN_HANDOFFS — confirm `discover→remix` is registered with endpoint `/api/tools/remix/run`; reuse, do NOT add an explore→remix entry — D-04/D-05)
    - src/components/discover/outlier-tile.tsx (OutlierTileData — the tile shape the handlers receive, incl. videoUrl, platformVideoId, trackable, trackHandle) [from 11-05]
    - .planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-UI-SPEC.md (§Surface 3 — the 3 quick-action cards copy LOCKED + the card-2 degrade; the card visual spec: transparent bg, 6% border, 12px radius, 20px padding, title 16px/600 + sub 14px/400 muted, phosphor line-icon, hover bg-white/[0.02], active=terracotta border+tint; §Copywriting — idle heading "Find what your audience would actually bite on." + body; §Surface 5 loading copy)
  </read_first>
  <action>
    Create `src/components/thread/explore-thread-view.tsx` (`"use client"`), cloning the HooksThreadView column + adding the ChatThreadView-style idle ownership.

    Props: `{ persistedBlocks: OutlierGridBlock[]; streamingBlocks: OutlierGridBlock[]; stages: StageState[]; isStreaming: boolean; error: string | null; platform: string; audience: Audience | null; hasTrackedAccounts: boolean; onQuickAction: (params: { niche?: string; accounts?: string; timeWindow?: string; serendipity?: number }) => void; onRetry?: () => void; onThreadReload?: () => void }`.

    Derive `isIdle = !isStreaming && streamingBlocks.length === 0 && persistedBlocks.length === 0 && !error`.

    IDLE state (D-07/EXPLORE-04): render the heading "Find what your audience would actually bite on." (16px/600) + the body copy (verbatim from UI-SPEC §Copywriting), then 3 quick-action cards (copy LOCKED, UI-SPEC §Surface 3):
    - Card 1 "Top performers in my niche today" · "Fresh outliers, scored for your audience" → on tap `onQuickAction({ niche: <derive from audience.name/goal_label, fallback "" >, timeWindow: "today" })`. Always enabled.
    - Card 2 "What competitors shipped" · "Recent posts from accounts you track" → if `hasTrackedAccounts` on tap `onQuickAction({ accounts: <tracked>, timeWindow: "week" })`; ELSE render a quiet DISABLED sub-state reading "Track an account first" (NO pull fired — honesty, never a fabricated competitor feed).
    - Card 3 "Surprise me" · "Widen beyond your niche — something unexpected" → on tap `onQuickAction({ niche: <audience niche or "">, serendipity: 1 })`. Always enabled.
    Card visuals per UI-SPEC §Surface 3 (transparent bg, `border-white/[0.06]`, 12px radius, 20px padding, title + muted sub, a phosphor line-icon compass/people/sparkle, hover `bg-white/[0.02]`, active = terracotta border+tint). Cards stack 1-up ≤640px, 2–3-up wider via the auto-fill minmax pattern. The quick-action cards NEVER auto-fire — they run ONLY on click.

    STREAMING / CONTENT state: ProgressChecklist while `isStreaming && stages.length > 0`; the loading lead line "Pulling outliers and scoring them for your audience… this can take a few minutes." (UI-SPEC §Surface 5, NO fake %); render the grid by passing the streaming + persisted block bodies to `MessageBlocks`, supplying the grid block's `onRemix` + `onTrack` via the `OutlierGridBlockRenderer` props path. Track per-tile state with local `remixPendingId` + `trackPendingId` + `trackedIds: Set<string>`.

    `handleRemix(tile)` (D-04/D-05, RESEARCH Q2): `const handoff = handoffsFor("discover").find(h => h.to === "remix"); if (!handoff?.endpoint) return;` → `setRemixPendingId(tile.platformVideoId)` → `await fetch(handoff.endpoint, { method:"POST", headers:{"Content-Type":"application/json"}, body: JSON.stringify({ url: tile.videoUrl, platform }) })` → on success call `onThreadReload?.()` (the composer reloads the open thread in place to surface the persisted remix-card — NOT router.push). On error clear remixPendingId. CTA label stays "Remix → Read" (LOCKED, D-05 — already the tile's string).

    `handleTrack(tile)` (EXPLORE-05): if `!tile.trackable || !tile.trackHandle` return → `setTrackPendingId(tile.platformVideoId)` → `await fetch("/api/tracked-accounts", { method:"POST", headers:{"Content-Type":"application/json"}, body: JSON.stringify({ platform, handle: tile.trackHandle, source_video_id: tile.platformVideoId }) })` → on success add `tile.trackHandle` to `trackedIds`; clear pending either way.

    SkillRunError on `error && !isStreaming` with `onRetry` (reuse the HooksThreadView SkillRunError shape — "nothing was charged"). The grid renderer must receive `onRemix={handleRemix}`, `onTrack={handleTrack}`, `remixPendingId`, `trackPendingId`, `trackedIds` (the props added to OutlierGridBlockRenderer in 11-05). Do NOT render any persona quote/reaction here (D-02 — the real reaction is the reused remix-card's LensTrigger, which appears after the remix-card persists).
  </action>
  <verify>
    <automated>npx vitest run src/components/thread/explore-thread-view.test.tsx</automated>
  </verify>
  <done>ExploreThreadView owns its idle state with 3 LOCKED-copy quick-action cards (card 2 degrading to "Track an account first" without tracked accounts), runs presets only on tap (no auto-fire), renders the streaming+persisted grid with onRemix/onTrack wired, launches the reused discover→remix handoff + in-place thread reload (no router.push), POSTs Track writes, shows no fabricated reaction on the grid, and the test suite is green.</done>
</task>

</tasks>

<verification>
- `explore-thread-view.test.tsx` passes: idle renders 3 quick-action cards; card 2 shows the disabled "Track an account first" sub-state when `hasTrackedAccounts=false`; no `onQuickAction` fires on mount (only on click).
- Reuse-not-fork: `grep -q "handoffsFor(\"discover\")" src/components/thread/explore-thread-view.tsx` succeeds AND `grep -n "explore.*remix\|router.push" src/components/thread/explore-thread-view.tsx` shows NO new explore→remix chain entry and NO router.push (reload via onThreadReload instead).
- No fabricated reaction on the grid: the view renders no `<blockquote>`/persona-quote markup (the real reaction lives in the reused remix-card LensTrigger).
- `npx tsc --noEmit` reports no new errors.
</verification>

<success_criteria>
ExploreThreadView delivers EXPLORE-04's audience-aware idle quick-actions (honest card-2 degrade) and the in-thread tap → Remix → Read loop by reusing the shipped chain verbatim + reloading the open thread in place — with the on-tap real reaction inherited free from the remix-card, no fabricated reaction on the grid, and behaviour locked by a test.
</success_criteria>

<output>
Create `.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-06-SUMMARY.md` when done.
</output>
