---
phase: 11-explore-audience-curated-discovery
plan: 07
type: execute
wave: 5
depends_on: ["11-06"]
files_modified:
  - src/components/app/home/composer.tsx
  - src/components/app/home/composer-controls.tsx
autonomous: true
requirements: [EXPLORE-01, EXPLORE-02, EXPLORE-04]
must_haves:
  truths:
    - "The Explore skill pill is enabled (SKILLS explore.enabled true) so /explore is selectable in the composer + slash menu"
    - "Selecting Explore mounts ExploreThreadView (showExploreView is unconditional when activeTool === 'explore', mirroring chat) so the idle quick-actions show"
    - "The Explore submit branch calls explore.start(params) and returns — it NEVER arms pendingNavRef or calls stream.start (Pitfall 1: Explore must never navigate to /analyze/[id])"
    - "Persisted outlier-grid blocks rehydrate on mount (filter b.type === 'outlier-grid') and the params popover supplies niche/accounts/time-window/serendipity to explore.start"
    - "After a tile Remix, the open thread reloads in place (onThreadReload refetches GET /api/threads/open and re-filters outlier-grid + remix-card blocks) — no router.push (RESEARCH Q2)"
  artifacts:
    - path: "src/components/app/home/composer.tsx"
      provides: "Explore stream mount + showExploreView gate + submit branch + persisted load + ExploreThreadView render + onThreadReload + params popover wiring"
      contains: "useExploreStream"
    - path: "src/components/app/home/composer-controls.tsx"
      provides: "SKILLS explore.enabled flipped to true; the params 'Search' popover trigger (or its wiring) beside the audience control"
      contains: "explore"
  key_links:
    - from: "src/components/app/home/composer.tsx"
      to: "src/components/thread/explore-thread-view.tsx"
      via: "mounts <ExploreThreadView> in threadContent; supplies audience + hasTrackedAccounts + onQuickAction + onThreadReload"
      pattern: "ExploreThreadView"
    - from: "src/components/app/home/composer.tsx"
      to: "explore.start"
      via: "handleSubmit explore branch — start(params) then return, NEVER pendingNavRef"
      pattern: "explore.start"
---

<objective>
Wire Explore into the composer (EXPLORE-01/02/04): enable the skill pill, mount `useExploreStream` + `ExploreThreadView`, add the submit branch (Pitfall-1-guarded so Explore never navigates to `/analyze`), rehydrate persisted `outlier-grid` blocks on mount, surface the params popover (niche/accounts/time-window/serendipity), and supply the in-place thread-reload that surfaces a remix-card after a tile tap (RESEARCH Q2). This is the wave that makes `/explore` a live, selectable in-thread skill.

Purpose: EXPLORE-01 (Explore skill in-thread + customizable params + serendipity valve) + EXPLORE-04 (audience-aware quick-actions reachable) become live. The composer is the dispatch hub every other skill mounts through; Explore mirrors the chat/hooks branches exactly, plus the chat-style unconditional idle gate.
Output: an extended `composer.tsx` (stream + view + submit branch + persisted load + onThreadReload + params popover) and `composer-controls.tsx` (explore enabled + the params trigger).
</objective>

<artifacts_this_phase_produces>
This plan extends:
- `src/components/app/home/composer.tsx` — `const explore = useExploreStream()`; `showExploreView = activeTool === "explore"` (unconditional, like chat); a `handleSubmit` explore branch calling `explore.start(params)` + return (no pendingNavRef); persisted `outlier-grid` load in the mount effect; `<ExploreThreadView>` in `threadContent` with audience/hasTrackedAccounts/onQuickAction/onThreadReload; a `reloadOpenThread()` helper (refetch GET /api/threads/open → re-filter blocks incl. `remix-card`) wired to onThreadReload; `hasThread` includes explore content; the params popover state + its `onRun` calling `explore.start`.
- `src/components/app/home/composer-controls.tsx` — `SKILLS` `explore.enabled: false → true`; the params "Search" popover trigger beside the audience control (or its trigger hook — the popover body content per UI-SPEC §Surface 4 may live in composer.tsx or here; pick the placement that mirrors the existing audience/intent popovers).
(EXPLORE-03's fit bar + EXPLORE-05's Track write already render via 11-05's tile + 11-06's handlers — this plan only connects the composer.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-RESEARCH.md
@.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable the Explore skill + params popover trigger (composer-controls.tsx)</name>
  <read_first>
    - src/components/app/home/composer-controls.tsx (the SKILLS array — the `explore` row currently `enabled: false` (group:"creator", model:"Flash", icon compass, command "/explore"); flip it to true. The existing audience + intent icon-only controls + the Popover shell — the pattern the params "Search" trigger mirrors; the `Ico` "search" icon already defined)
    - .planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-UI-SPEC.md (§Surface 4 — the params popover form factor: an upward popover beside the composer skill pill, the same `bottom-[calc(100%+12px)]`/`bg-[#211f1d]`/6% border/12px radius/`max-h-[60vh]` shell; contents: Niche/keywords input · Accounts input · Time-window segmented (Today/Week/Month) · Serendipity slider (On-niche ⟷ Surprise me) · "Run Explore" apply (terracotta, the popover's one accent); §Copywriting — the param labels/placeholders + "Search" trigger label + "Run Explore")
  </read_first>
  <action>
    In `src/components/app/home/composer-controls.tsx`:
    1. Flip the `explore` row in `SKILLS` from `enabled: false` to `enabled: true` (leave group/model/command/icon as-is — already correct). This lights up the skill pill row + the `/explore` slash entry.
    2. Add a params "Search" control to the left cluster (beside the audience control), shown ONLY when `activeTool === "explore"` (it is Explore-specific). It is an icon-only borderless control (the `ctl` class + `Ico name="search"`), `aria-label="Search"`. Tapping it toggles a params popover (reuse the `Popover` shell). The popover body per UI-SPEC §Surface 4: a "Niche or keywords" input (placeholder "e.g. gym beginners, myth-busting"), an "Accounts" input (placeholder "@handle, @handle"), a "Time window" segmented control (Today/This week/This month), a "Serendipity" labeled range slider (ends "On-niche" ⟷ "Surprise me", helper "Slide right to widen beyond your niche.", terracotta active track), and a "Run Explore" apply button (terracotta — the popover's one accent). Reuse THEME-06 input styling (42px height, `rgba(255,255,255,0.05)` bg, 8px radius).
    3. Expose the params via a new prop on `ComposerControlsProps`: `onRunExplore?: (params: { niche?: string; accounts?: string; timeWindow?: string; serendipity?: number }) => void` (the apply button calls it + closes the popover). Keep params popover local state inside ComposerControls. Do NOT make the skill pill a submit (Pitfall 5 — selection is never a submit).

    Note: if it is cleaner to keep the params popover body in composer.tsx (next to where `explore.start` lives), expose just the trigger here and a callback — either placement is acceptable as long as the trigger sits beside the audience control and only shows for the Explore skill. Mirror the existing audience/intent popover idiom.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "composer-controls" || echo "composer-controls typechecks clean"; grep -q "id: \"explore\".*enabled: true\|explore.*enabled: true" src/components/app/home/composer-controls.tsx && echo "explore enabled"</automated>
  </verify>
  <done>The explore SKILLS row is enabled:true (selectable in the pill + slash menu); an Explore-only "Search" params popover (niche/accounts/time-window/serendipity slider/"Run Explore") sits beside the audience control reusing the THEME-06 Popover idiom; the apply button calls onRunExplore; composer-controls typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Mount Explore stream + view + submit branch + reload (composer.tsx)</name>
  <read_first>
    - src/components/app/home/composer.tsx (the dispatch hub: the per-skill stream mounts (useHooksStream/useChatStream etc.) + `show*View` gates + the `showChatView = activeTool === 'chat'` UNCONDITIONAL idle pattern (mirror for explore); the mount-effect `loadPersistedBlocks` that filters blocks by type + sets persisted state; the `handleSubmit` per-skill branches (each calls `skill.start(...)` + `return`, NEVER pendingNavRef); the navigate-on-id guard + the Pitfall-1 comments at lines ~442-466; the `handleDevelopRemix` in-place thread-reload (GET /api/threads/open → filter → setPersisted) — the reload pattern for onThreadReload; the `threadContent` JSX where each *ThreadView mounts; `hasThread`; the audiences list + selectedAudience already fetched here; PLACEHOLDER_BY_TOOL already has an `explore` entry)
    - src/components/thread/explore-thread-view.tsx (ExploreThreadView's exact props: persistedBlocks/streamingBlocks/stages/isStreaming/error/platform/audience/hasTrackedAccounts/onQuickAction/onRetry/onThreadReload) [from 11-06]
    - src/hooks/queries/use-explore-stream.ts (useExploreStream's API: start(params)/stop/reset/toBlocks/stages/isStreaming/error) [from 11-05]
    - .planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-RESEARCH.md (§Architecture Pattern 1 row 9 — the composer dispatch checklist: add useExploreStream, showExploreView gate, persisted load filter b.type==='outlier-grid', handleSubmit branch that NEVER arms pendingNavRef (Pitfall 1), mount <ExploreThreadView>; §Pitfall 1 — the navigate guard is Test-exclusive)
  </read_first>
  <action>
    Extend `src/components/app/home/composer.tsx`:

    1. Import + mount: `import { useExploreStream } from "@/hooks/queries/use-explore-stream";` `import { ExploreThreadView } from "@/components/thread/explore-thread-view";`. Add `const explore = useExploreStream();` and `const exploreBlocks = explore.toBlocks();`. Add persisted state `const [persistedExploreBlocks, setPersistedExploreBlocks] = useState<any[]>([]);`.

    2. View gate (mirror chat's unconditional idle): `const showExploreView = activeTool === "explore";` (unconditional so the idle quick-actions show even with no content — EXPLORE-04/D-07, exactly like `showChatView`).

    3. Persisted load: in the existing `loadPersistedBlocks` mount effect, add `const outlierGridBlocks = allBlocks.filter((b) => b.type === 'outlier-grid'); setPersistedExploreBlocks(outlierGridBlocks);`.

    4. Reload helper (RESEARCH Q2): extract or add `const reloadOpenThread = useCallback(async () => { ... GET /api/threads/open → flatten blocks → setPersistedExploreBlocks(filter outlier-grid) AND setPersistedRemixBlocks(filter remix-card) ... }, [])`. The remix-card filter is what surfaces the freshly-persisted Read after a tile Remix without `router.push`. (Reuse the exact fetch/flatten shape from `handleDevelopRemix`.)

    5. hasThread: add `explore.isStreaming || exploreBlocks.length > 0 || persistedExploreBlocks.length > 0 || showExploreView` to the `hasThread` OR-chain (showExploreView like showChatView keeps the thread layout when Explore is active).

    6. handleSubmit branch (Pitfall 1 — CRITICAL): add an `if (activeTool === "explore") { ... }` branch BEFORE the Test path. It must call `explore.start({ niche: trimmedUrl || undefined })` (a typed field send → niche pull) and `return` — it MUST NOT set `pendingNavRef.current` and MUST NOT call `stream.start` (Explore never navigates to /analyze/[id]). Clear the input. Add a code comment mirroring the other branches' "NEVER arms pendingNavRef" note.

    7. Mount the view in `threadContent`: `{showExploreView && (<ExploreThreadView persistedBlocks={persistedExploreBlocks} streamingBlocks={exploreBlocks} stages={explore.stages} isStreaming={explore.isStreaming} error={explore.error} platform={platform} audience={selectedAudience} hasTrackedAccounts={hasTrackedAccounts} onQuickAction={(params) => void explore.start(params)} onRetry={() => void explore.start({})} onThreadReload={() => void reloadOpenThread()} />)}`.

    8. hasTrackedAccounts: add a lightweight mount fetch `GET /api/tracked-accounts` → `setHasTrackedAccounts((data.accounts ?? []).length > 0)` (silent on 401, like the audiences fetch). This drives card-2's degrade honestly.

    9. Params popover wiring: connect ComposerControls' `onRunExplore` (from Task 1) to `(params) => void explore.start(params)` (if the popover body lives in composer.tsx instead, render it there and call `explore.start`). Either way the submit Button's `loading`/`canSubmit` should include `explore.isStreaming` (mirror the other skills) and `canSubmit` for explore = `!submitting && !explore.isStreaming`.

    Do NOT change the navigate-on-id effect (it stays Test-exclusive). Do NOT auto-fire any explore pull on render (quick-actions + Run Explore + field-send fire only on explicit user action — D-05 spine).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "home/composer.tsx" || echo "composer typechecks clean"; grep -q "explore.start" src/components/app/home/composer.tsx && ! grep -A3 "activeTool === \"explore\"" src/components/app/home/composer.tsx | grep -q "pendingNavRef" && echo "explore branch does not arm nav"</automated>
  </verify>
  <done>The composer mounts useExploreStream + ExploreThreadView; showExploreView is unconditional (idle quick-actions show); the handleSubmit explore branch calls explore.start + returns without arming pendingNavRef/stream.start (Pitfall 1); persisted outlier-grid blocks rehydrate; reloadOpenThread surfaces remix-card + outlier-grid after a tap (no router.push); hasTrackedAccounts drives card-2 degrade; the params popover calls explore.start; composer typechecks clean.</done>
</task>

</tasks>

<verification>
- Explore is selectable: `grep -q "explore.*enabled: true\|enabled: true.*explore" src/components/app/home/composer-controls.tsx`.
- Pitfall 1 honored: the explore submit branch never arms navigation — `grep -A4 'activeTool === "explore"' src/components/app/home/composer.tsx` shows `explore.start` + `return` and NO `pendingNavRef`/`stream.start`.
- In-place reload, not navigate: `grep -q "reloadOpenThread\|/api/threads/open" src/components/app/home/composer.tsx` succeeds and the explore path does not call `router.push("/analyze")`.
- `npm run build` succeeds (or `npx tsc --noEmit` reports no new errors across composer.tsx + composer-controls.tsx) — this is the integration point where Explore becomes reachable.
</verification>

<success_criteria>
`/explore` is a live, selectable in-thread skill: the pill is enabled, the idle quick-actions render (chat-style unconditional gate), the params popover + field-send drive `explore.start`, submits never navigate to /analyze (Pitfall 1), persisted grids rehydrate, and a tile Remix surfaces the Read via in-place thread reload (no router.push) — the whole composer typechecks/builds.
</success_criteria>

<output>
Create `.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-07-SUMMARY.md` when done.
</output>
