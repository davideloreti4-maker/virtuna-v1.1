---
phase: 11-explore-audience-curated-discovery
plan: 05
type: execute
wave: 3
depends_on: ["11-01", "11-04"]
files_modified:
  - src/hooks/queries/use-explore-stream.ts
  - src/components/discover/outlier-tile.tsx
  - src/components/thread/outlier-grid-block.tsx
autonomous: true
requirements: [EXPLORE-02, EXPLORE-03, EXPLORE-05]
must_haves:
  truths:
    - "use-explore-stream consumes POST /api/tools/explore via fetch + getReader (NOT EventSource), parsing stage/content/done/error frames and exposing start(params)/stop/reset/toBlocks"
    - "Each result tile renders the measured multiplier (neutral) AND a 3-segment fit bar + 'FIT · {Strong|Fair|Weak}' + 'Fit for your audience · predicted' — the bar is OMITTED entirely when fit is null/absent (degrade, D-02), never an empty/zero bar"
    - "The fit bar is DATA — neutral cream + score-zone tone (green/amber/muted), NEVER coral; coral stays reserved for the one 'Remix → Read' CTA (one-accent law)"
    - "The tile's '+ Track account' button appears only when trackable; it POSTs the handle and toggles to 'Tracking' on success; the on-tap reaction stays the reused remix-card's LensTrigger (no fabricated reaction on the grid, D-02)"
    - "OutlierGridBlockRenderer passes onRemix / onTrack / fit / trackable through to the grid + tile (the static-reference renderer is upgraded to wire the CTAs, Pitfall 2)"
  artifacts:
    - path: "src/hooks/queries/use-explore-stream.ts"
      provides: "useExploreStream — SSE consumer (mirrors use-hooks-stream): start/stop/reset/toBlocks + stages"
      exports: ["useExploreStream"]
      min_lines: 90
    - path: "src/components/discover/outlier-tile.tsx"
      provides: "OutlierTile extended with the fit bar + '+ Track account' button (read order: multiplier → fit → metrics → track → CTA)"
      contains: "FIT ·"
    - path: "src/components/thread/outlier-grid-block.tsx"
      provides: "OutlierGridBlockRenderer wiring onRemix/onTrack/fit/trackable through to DiscoverGrid/OutlierTile (no longer a static reference)"
      contains: "onRemix"
  key_links:
    - from: "src/hooks/queries/use-explore-stream.ts"
      to: "/api/tools/explore"
      via: "fetch POST + res.body.getReader() frame parsing"
      pattern: "/api/tools/explore"
    - from: "src/components/discover/outlier-tile.tsx"
      to: "tile.fit.level"
      via: "fit bar fill tone by level (success/warning/muted), omitted when fit==null"
      pattern: "fit"
---

<objective>
Build the Explore client primitives (EXPLORE-02/03/05): (1) `use-explore-stream.ts` — the SSE consumer that drives the thread view, cloned from `use-hooks-stream.ts`; (2) extend `OutlierTile` with the prescribed fit bar + "+ Track account" button per the UI-SPEC; (3) upgrade `OutlierGridBlockRenderer` from a static in-thread reference into a live renderer that passes `onRemix`/`onTrack`/`fit`/`trackable` through to the grid (Pitfall 2). The on-tap real reaction stays the reused remix-card's LensTrigger — this plan adds NO reaction UI to the grid (D-02).

Purpose: EXPLORE-03's audience-fit estimate becomes visible as the honest 3-segment bar (the moat tell vs competitors' borrowed view counts); EXPLORE-05's Track write gets its tile affordance; EXPLORE-02's eager signal is the bar (the lazy real reaction comes free in 11-06 via the reused chain). Honesty hard-bind (D-02): the bar carries NO quote/verdict/persona voice, omits entirely on no-signal, and is NEVER coral.
Output: `use-explore-stream.ts`, an extended `outlier-tile.tsx`, an upgraded `outlier-grid-block.tsx`.
</objective>

<artifacts_this_phase_produces>
This plan creates / extends:
- `src/hooks/queries/use-explore-stream.ts` — NEW: `useExploreStream()` returning `{ streamingBlocks?/toBlocks, statusMessage, isStreaming, error, isDone, stages, start(params), stop, reset }` (fetch+getReader, parses the explore SSE frames).
- `src/components/discover/outlier-tile.tsx` — EXTENDED: `OutlierTileData` gains `fit?: { level: FitLevel } | null`, `trackable?`, `trackHandle?`; the tile renders the fit bar (between multiplier and metrics) + the "+ Track account" button (above the CTA), with new optional `onTrack` + `trackPending`/`tracked` props.
- `src/components/thread/outlier-grid-block.tsx` — EXTENDED: `OutlierGridBlockRenderer` accepts + forwards `onRemix`/`onTrack`/`remixPendingId` to `DiscoverGrid`, and its inline block interface widens to include the new tile fields.
(11-06 mounts useExploreStream + supplies the onRemix/onTrack handlers in ExploreThreadView. The fit bar's exact tones/copy are PRESCRIBED in 11-UI-SPEC §Color + §Copywriting.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-RESEARCH.md
@.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: use-explore-stream.ts — SSE consumer (mirrors use-hooks-stream)</name>
  <read_first>
    - src/hooks/queries/use-hooks-stream.ts (the EXACT SSE consumer to clone: fetch + res.body.getReader() (NOT EventSource — BLOCKER-1), the \n\n frame split, the event/data line parse, the isMountedRef mount-guard + abortRef, the stage upsert-by-name into StageState[], reset/stop, the toBlocks() shape)
    - src/lib/tools/blocks.ts (OutlierGridBlock type — the block this hook's toBlocks() produces; the tiles[] prop shape incl. fit/trackable) [extended in 11-01]
    - src/components/thread/progress-checklist.tsx (StageState type — imported for the stages array)
    - .planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-RESEARCH.md (§Architecture Pattern 1 row 4 — start(params), stop, reset, toBlocks; handle stage/content/done/error; the explore SSE has NO per-tile score events, unlike hooks — content carries the whole grid block)
  </read_first>
  <action>
    Create `src/hooks/queries/use-explore-stream.ts` exporting `useExploreStream`, cloning the `use-hooks-stream` structure but SIMPLER (Explore streams ONE outlier-grid block in the content event — no per-card score events, no followup).

    State: `streamingBlock: OutlierGridBlock | null`, `statusMessage`, `isStreaming`, `error`, `isDone`, `stages: StageState[]`. Refs: `abortRef`, `isMountedRef` (the WR-05 unmount guard verbatim from use-hooks-stream).

    `start(params: { niche?: string; accounts?: string; timeWindow?: string; serendipity?: number })`: abort prior, reset state, `setIsStreaming(true)`, `fetch("/api/tools/explore", { method:"POST", headers:{"Content-Type":"application/json"}, body: JSON.stringify(params), signal })`. On `!res.ok` throw the parsed error. Read via `res.body.getReader()` + the same `\n\n` frame split + event/data line parse as use-hooks-stream. Handle event types:
    - `stage`: upsert into `stages` by name (verbatim the use-hooks-stream stage upsert).
    - `content`: `data.blocks` is an array containing the single outlier-grid block — set `streamingBlock` to `data.blocks[0]` (guard shape: only accept `block.type === "outlier-grid"`).
    - `done`: setIsDone(true), clear status.
    - `error`: throw new Error(message).
    Catch AbortError (return), else setError. `finally` setIsStreaming(false) (guarded by isMountedRef).

    Expose `toBlocks(): OutlierGridBlock[]` → `streamingBlock ? [streamingBlock] : []` (for MessageBlocks rendering). Plus `stop`, `reset`. Add a header: SSE consumer for /api/tools/explore; fetch+getReader (NOT EventSource, BLOCKER-1); one grid block per run (no per-tile score events).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "use-explore-stream" || echo "use-explore-stream typechecks clean"</automated>
  </verify>
  <done>useExploreStream uses fetch+getReader (no EventSource), parses stage/content/done/error frames, exposes start(params)/stop/reset/toBlocks/stages, surfaces the single outlier-grid block from the content event, guards unmount via isMountedRef, and typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend OutlierTile — fit bar + "+ Track account" (EXPLORE-03/05, honesty + one-accent law)</name>
  <read_first>
    - src/components/discover/outlier-tile.tsx (the tile to extend: the current `OutlierTileData` interface, the multiplier badge row, the VideoCard metrics grid, the existing "Remix → Read" CTA + its `#FF7F50` styling + `onRemix`/`remixPending` props — the read order to insert into)
    - src/lib/discover/explore-rank.ts (the FitLevel type to import for the tile's `fit` field) [from 11-01]
    - .planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-UI-SPEC.md (§Color → "Fit-score visual encoding (PRESCRIBED)" — the EXACT spec: `h-1.5 rounded-full` full-width bar on `bg-white/[0.06]` track, 3 quantized levels; Strong→`--color-success`, Fair→`--color-warning`, Weak→`--color-foreground-muted`; label `text-[11px] font-semibold uppercase tracking-wide text-foreground-muted` reading "FIT · {Strong|Fair|Weak}" + sub-line "Fit for your audience · predicted"; placement BELOW the multiplier badge, ABOVE the metrics grid; OMIT entirely when no fit signal — never an empty/zero bar; NEVER coral. §Surface 1 read order. §Copywriting — track copy "+ Track account" / "Tracking" with a check)
  </read_first>
  <action>
    Extend `src/components/discover/outlier-tile.tsx`.

    Type: add to `OutlierTileData` — `fit?: { level: FitLevel } | null` (import `FitLevel` from `@/lib/discover/explore-rank`), `trackable?: boolean`, `trackHandle?: string`.

    Props: add to `OutlierTileProps` — `onTrack?: (tile: OutlierTileData) => void`, `trackPending?: boolean`, `tracked?: boolean`.

    Fit bar (insert BETWEEN the multiplier badge row and the `<VideoCard>` — §Surface 1 read order step 2): render ONLY when `tile.fit != null`. A `<div>` with a thin track (`h-1.5 rounded-full` on `bg-white/[0.06]`, full width) and a fill segment whose width maps to the level (Weak ≈ 1/3, Fair ≈ 2/3, Strong = full) and whose color maps by level: Strong → `var(--color-success)`, Fair → `var(--color-warning)`, Weak → `var(--color-foreground-muted)`. Beneath: a label `<span className="text-[11px] font-semibold uppercase tracking-wide text-foreground-muted">FIT · {level}</span>` and a quiet sub-line "Fit for your audience · predicted". DEGRADE (D-02): when `tile.fit == null` render NOTHING here (no empty bar, no "fit unavailable" placeholder — UI-SPEC §Copywriting "thin-fit honesty"). NEVER use coral/`#FF7F50`/`--color-accent` on the fit bar — it is DATA (UI-SPEC §Color "NOT accent" rule).

    Track button (insert ABOVE the "Remix → Read" CTA — §Surface 1 read order step 4): render ONLY when `tile.trackable`. A quiet borderless text-button (cream-muted, `text-xs font-medium`, left-aligned), label "+ Track account" → on click `onTrack?.(tile)`; while `trackPending` show a disabled state; when `tracked` show "Tracking" with a check (`Ico name="check"` or a "✓"). It is NON-accent (secondary to Remix — never coral).

    Keep the existing "Remix → Read" CTA EXACTLY as-is (the single coral element, the one-accent law — UI-SPEC §Color accent-reserved #1). Do NOT add a second accent.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "outlier-tile" || echo "outlier-tile typechecks clean"; grep -q "FIT ·" src/components/discover/outlier-tile.tsx && grep -q "Track account" src/components/discover/outlier-tile.tsx && echo "tile copy present"</automated>
  </verify>
  <done>OutlierTile renders a 3-level fit bar (success/warning/muted, never coral) with "FIT · {level}" + "predicted" sub-line, omitted entirely when fit==null; a "+ Track account" button appears only when trackable and toggles to "Tracking" when tracked; the "Remix → Read" CTA remains the lone coral element; the tile typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade OutlierGridBlockRenderer to wire the CTAs (Pitfall 2)</name>
  <read_first>
    - src/components/thread/outlier-grid-block.tsx (the CURRENT static renderer — its inline `OutlierGridBlock` interface (lines ~19-25) + the header comment "No remix CTA wiring here" / "static record"; it renders `<DiscoverGrid state="results">` with NO callbacks — this is the Pitfall-2 surface to fix)
    - src/components/discover/discover-grid.tsx (DiscoverGrid props — confirm it already accepts `onRemix`/`remixPendingId`/`onRetry`; check whether it forwards an `onTrack` prop to OutlierTile, and add the forward if missing — the grid is the pass-through layer)
    - src/components/discover/outlier-tile.tsx (the extended tile from Task 2 — the onTrack/trackPending/tracked props the grid must forward) [Task 2]
    - .planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-RESEARCH.md (§Pitfall 2 — EXTEND the renderer to accept + pass onRemix/onTrack/fit/trackable; the thread view owns the handlers)
  </read_first>
  <action>
    Upgrade `src/components/thread/outlier-grid-block.tsx` so the in-thread block renders LIVE tiles (not a static reference).

    Widen the inline `OutlierGridBlock` interface to include the new tile fields (`fit?`, `trackable?`, `trackHandle?`) — or import `OutlierGridBlock`/`OutlierTileData` types directly from blocks.ts / outlier-tile.tsx instead of the local interface (preferred — single source of truth).

    Add optional props to `OutlierGridBlockRenderer`: `onRemix?: (tile: OutlierTileData) => void`, `onTrack?: (tile: OutlierTileData) => void`, `remixPendingId?: string | null`, `trackPendingId?: string | null`, `trackedIds?: Set<string>` (so the thread view can drive per-tile pending/tracked state). Pass them through to `<DiscoverGrid ... onRemix={onRemix} remixPendingId={remixPendingId} />`.

    If `DiscoverGrid` does not yet forward `onTrack`/`trackPendingId`/`tracked` to each `OutlierTile`, add that forwarding in `discover-grid.tsx` (map each tile → `<OutlierTile ... onTrack={onTrack} trackPending={trackPendingId === tile.platformVideoId} tracked={trackedIds?.has(tile.trackHandle ?? "")} />`). Update the renderer's header comment: this block is now LIVE in the Explore thread (onRemix/onTrack wired by ExploreThreadView); it remains a static reference ONLY when those callbacks are omitted (Discover page reuse stays unchanged — callbacks optional).

    Do NOT change `message-blocks.tsx` BLOCK_COMPONENTS dispatch or `block-registry.ts` (the "outlier-grid" key + dispatch are unchanged — Pattern 3).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "outlier-grid-block|discover-grid" || echo "grid renderer typechecks clean"</automated>
  </verify>
  <done>OutlierGridBlockRenderer accepts + forwards onRemix/onTrack/remixPendingId/trackPendingId/trackedIds to DiscoverGrid→OutlierTile; DiscoverGrid forwards the track props to each tile; omitting the callbacks preserves the static-reference behavior for the Discover page; message-blocks/block-registry unchanged; everything typechecks clean.</done>
</task>

</tasks>

<verification>
- `use-explore-stream.ts` uses fetch+getReader (no EventSource): `grep -q "getReader" src/hooks/queries/use-explore-stream.ts && ! grep -q "EventSource" src/hooks/queries/use-explore-stream.ts`.
- The fit bar is never coral: `grep -nE "#FF7F50|--color-accent|coral" src/components/discover/outlier-tile.tsx` shows those tokens ONLY on the existing "Remix → Read" CTA block, not in the fit-bar markup.
- The fit bar omits on null: the tile guards `tile.fit != null` before rendering the bar (no empty/zero bar path exists).
- `npx tsc --noEmit` reports no new errors across the three files.
</verification>

<success_criteria>
The Explore client primitives are ready to mount: an SSE consumer hook, a tile that honestly renders the audience-fit bar (degrading to nothing on no-signal, never coral) plus a Track-account affordance, and an upgraded grid-block renderer that wires Remix/Track through to the tiles — all typechecking, with the one-accent law and honesty spine preserved.
</success_criteria>

<output>
Create `.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-05-SUMMARY.md` when done.
</output>
