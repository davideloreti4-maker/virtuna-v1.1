---
phase: 11-explore-audience-curated-discovery
plan: 08
type: execute
wave: 6
depends_on: ["11-02", "11-03", "11-04", "11-07"]
files_modified:
  - src/lib/database.types.ts
  - src/lib/tracked-accounts/tracked-accounts-repo.ts
autonomous: false
requirements: [EXPLORE-05]
must_haves:
  truths:
    - "The tracked_accounts migration is applied to the live Supabase project (table + RLS exist) so the Track→Explore loop persists for real"
    - "database.types.ts is regenerated post-push so tracked_accounts is typed; the interim (supabase as any) casts in tracked-accounts-repo.ts are removed where the regen makes them unnecessary"
    - "The engine + KC regression suites stay green and ENGINE_VERSION remains '3.19.0' — Explore made NO video-scoring change (the protected SIM-1 Max path is untouched, Pitfall 6)"
    - "End-to-end Explore works against live data: pick /explore → quick-action/params pull → fit-scored grid → Remix → Read with a real persona reaction → Track account persists"
  artifacts:
    - path: "src/lib/database.types.ts"
      provides: "Regenerated types including the tracked_accounts table"
      contains: "tracked_accounts"
  key_links:
    - from: "src/lib/tracked-accounts/tracked-accounts-repo.ts"
      to: "src/lib/database.types.ts"
      via: "typed tracked_accounts rows (casts dropped post-regen)"
      pattern: "tracked_accounts"
---

<objective>
The mandatory BLOCKING wave (per the schema-push requirement): apply the `tracked_accounts` migration to the live Supabase project, regenerate `database.types.ts`, drop the interim `(supabase as any)` casts the regen makes unnecessary, run the engine + KC regression gate (ENGINE_VERSION must stay "3.19.0"), and complete an end-to-end UAT of the full Explore flow against live data. This mirrors the exact sequencing prior phases used (07-06, 10-07: "live schema push + types regen + engine regression gate").

Purpose: build + type checks pass WITHOUT the push (types come from `database.types.ts`, not the live DB) — so the phase CANNOT pass verification without this wave, which would otherwise be a false-positive state. Explore reuses scoring and changes NO video-scoring bytes, so the protected SIM-1 Max path must remain byte-identical (regression gate proves it).
Output: a live `tracked_accounts` table, regenerated types, cast cleanup, a green regression gate, and a passed end-to-end UAT.

`autonomous: false` — `supabase db push` may require interactive confirmation in a non-TTY shell, and the UAT is a human-verified end-of-phase check.
</objective>

<artifacts_this_phase_produces>
This plan finalizes:
- The live `tracked_accounts` table (applied from 11-02's migration).
- `src/lib/database.types.ts` — REGENERATED to include `tracked_accounts`.
- `src/lib/tracked-accounts/tracked-accounts-repo.ts` — cast cleanup (drop `(supabase as any)` where the regen types resolve it).
(No new symbols — this wave applies + verifies what 11-02..11-07 built. EXPLORE-06 stays DEFERRED, D-09.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-RESEARCH.md
@.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply tracked_accounts migration to live Supabase + regenerate types</name>
  <read_first>
    - supabase/migrations/20260620090000_tracked_accounts.sql (the migration to apply) [from 11-02]
    - .planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-RESEARCH.md (§Architecture Pattern 6 "Note on the existing migration push pattern" — prior phases pushed in a final BLOCKING wave + regenerated database.types.ts; the (supabase as any) interim convention)
    - .planning/STATE.md (the live project ref context: prior pushes targeted Supabase project qyxvxleheckijapurisj per the 07-06 note — confirm the active project before pushing)
  </read_first>
  <action>
    Apply the `tracked_accounts` migration to the live Supabase project, then regenerate types.

    PUSH (preferred — CLI): run `supabase db push` (the project is linked; this applies pending migrations incl. `20260620090000_tracked_accounts.sql`). If the shell is non-TTY and the push needs confirmation, set `SUPABASE_ACCESS_TOKEN` in the env first (do NOT hardcode it — read from the existing env/secret). ALTERNATIVE (if CLI push cannot run non-interactively): apply the exact DDL via the Supabase MCP `apply_migration` tool (name `tracked_accounts`, body = the migration SQL).

    VERIFY the table exists: query the live DB (e.g. Supabase MCP `list_tables` or `select` against `public.tracked_accounts`) and confirm the table + the `tracked_all_own` RLS policy are present.

    REGENERATE TYPES: regenerate `src/lib/database.types.ts` from the live schema (the project's existing types-gen command — e.g. `supabase gen types typescript --linked > src/lib/database.types.ts`, matching how prior phases regenerated). Confirm `tracked_accounts` now appears in the generated types.

    This task is `checkpoint:human-action` + `gate="blocking"` because `supabase db push` / token setup may need human authorization in this environment. After the push + regen succeed, the executor proceeds to the cast cleanup + gate.
  </action>
  <verify>
    <automated>grep -q "tracked_accounts" src/lib/database.types.ts && echo "tracked_accounts present in regenerated types"</automated>
  </verify>
  <done>The tracked_accounts table + tracked_all_own RLS policy exist on the live Supabase project; database.types.ts is regenerated and contains tracked_accounts; the verify command confirms the type is present.</done>
</task>

<task type="auto">
  <name>Task 2: Drop interim (supabase as any) casts in tracked-accounts-repo + typecheck</name>
  <read_first>
    - src/lib/tracked-accounts/tracked-accounts-repo.ts (the `(supabase as any)` casts added in 11-03 — remove them where the regenerated tracked_accounts type now resolves the query; keep the repo's behavior identical)
    - src/lib/shelf/shelf-repo.ts (the post-regen cast convention reference — how typed Supabase queries read once the table is in database.types.ts)
  </read_first>
  <action>
    Now that `database.types.ts` includes `tracked_accounts`, remove the interim `(supabase as any)` casts in `src/lib/tracked-accounts/tracked-accounts-repo.ts` where the typed client resolves the `from("tracked_accounts")` calls. Keep behavior byte-identical (same list/create/delete logic, same session-derived user_id, same idempotent upsert). If a residual cast is genuinely still needed (e.g. an `.upsert` onConflict typing gap), leave it with a one-line comment explaining why — do not fight the type system unnecessarily. Re-run the repo's unit test to confirm no behavior regression.
  </action>
  <verify>
    <automated>npx vitest run src/lib/tracked-accounts/tracked-accounts-repo.test.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "tracked-accounts-repo" || echo "repo typechecks clean post-regen"</automated>
  </verify>
  <done>The repo uses the regenerated tracked_accounts types (interim (supabase as any) casts dropped where resolvable); the repo unit test still passes; the repo typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING gate] Engine + KC regression suite green, ENGINE_VERSION "3.19.0", full build</name>
  <read_first>
    - src/lib/engine/version.ts (ENGINE_VERSION = "3.19.0" — MUST remain unchanged; Explore made no video-scoring change, Pitfall 6)
    - .planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-RESEARCH.md (§Pitfall 6 — Explore makes NO video-scoring call; the fit math lives at the runner/route layer; keep the engine + KC regression suites green; preserve same-video SIM-1 Max score-identity)
    - .planning/STATE.md (the prior BLOCKING-gate pattern: 07-06 "full suite green", 10-07 "engine regression gate (ENGINE_VERSION unchanged)" — the same gate to re-run here)
  </read_first>
  <action>
    Run the BLOCKING regression gate (mirrors 07-06 / 10-07 final waves):
    1. Confirm `ENGINE_VERSION === "3.19.0"` in `src/lib/engine/version.ts` (unchanged — Explore touched no video-scoring bytes). If anything bumped it, that is a defect — revert.
    2. Run the engine regression suite + the KC suite: `npm test` (or the project's engine/KC test commands). They MUST be green — Explore added a pure fit module + a non-scoring SSE route + UI; the protected SIM-1 Max path is untouched, so same-video score-identity is preserved by construction.
    3. Run the full build: `npm run build` — MUST succeed (Explore is now reachable; the regenerated types must compile end-to-end).
    4. Run the lint pass: `npm run lint` — MUST pass (per CLAUDE.md: lint before commit).

    If any suite fails, fix the regression before this wave can complete (the phase cannot pass verification on a red gate). Document the suite counts in the SUMMARY (e.g. "engine+KC NNNN green, ENGINE_VERSION 3.19.0").
  </action>
  <verify>
    <automated>grep -q '"3.19.0"' src/lib/engine/version.ts && npm run build</automated>
  </verify>
  <done>ENGINE_VERSION is "3.19.0" (unchanged); the engine + KC regression suites are green; the full build succeeds; lint passes; the protected SIM-1 Max video-scoring path is byte-identical (Explore made no scoring change).</done>
</task>

<task type="auto">
  <name>Task 4: End-to-end Explore UAT (human-verified, end-of-phase)</name>
  <read_first>
    - .planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-UI-SPEC.md (the full surface contract to verify against: the fit bar, the 3 quick-action cards + card-2 degrade, the params popover + serendipity slider, the "Remix → Read" CTA, the "+ Track account" affordance, the honesty rules — no fabricated reaction on the grid, no fake %, no empty fit bar)
    - .planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-CONTEXT.md (the locked decisions D-01..D-09 the UAT confirms in behavior: honest fit eager / real reaction lazy / Remix-then-Read / CTA wording / quick-actions+serendipity / watchlist producer)
  </read_first>
  <action>
    Walk the full Explore flow against live data and confirm each behavior. This is the end-of-phase human-verified check (the project runs human_verify_mode=end-of-phase, so verification is a human-check, not an automated-only gate).

    Steps to confirm:
    1. Select `/explore` (pill + `/` slash menu both work) → the idle screen shows the heading + 3 quick-action cards.
    2. With NO tracked accounts: card 2 "What competitors shipped" shows the quiet "Track an account first" disabled sub-state (no pull fires, no fabricated feed).
    3. Tap "Top performers in my niche today" (or open the params popover, set a niche + serendipity, "Run Explore") → the loading line shows "Pulling outliers and scoring them for your audience… this can take a few minutes" with NO fake % → a fit-scored grid renders.
    4. With a CALIBRATED audience active: tiles show the 3-segment fit bar + "FIT · {Strong|Fair|Weak}" + "predicted" (never coral). With General active: the fit bar is OMITTED entirely (no empty bar), only the measured multiplier shows.
    5. Tap "Remix → Read" on a profile-mode tile → the existing remix chain runs → a remix-card persists and surfaces in-thread (in place, no jump to /analyze) → the remix-card's "See how the room reacted" LensTrigger shows a REAL persona reaction (D-02 — first persona voice in the flow, not fabricated on the grid).
    6. Tap "+ Track account" on a profile-mode tile → it toggles to "Tracking ✓" → confirm a row exists in `tracked_accounts` (live DB) → re-running Explore + tapping Track again does NOT duplicate (idempotent). Re-load card 2 now offers the competitors pull (hasTrackedAccounts true).
    7. Reload the page → the persisted outlier-grid + remix-card rehydrate (THREAD-07).

    Record PASS/PARTIAL/FAIL per step in the SUMMARY. Any honesty-spine violation (fabricated reaction on a grid tile, an empty/zero fit bar, a fake %, coral on the fit bar) is a hard FAIL → file a gap.
  </action>
  <verify>
    <human-check>
      Confirm the 7 UAT steps above against live data: /explore idle quick-actions render; card-2 degrades honestly without tracked accounts; a pull streams (no fake %) into a fit-scored grid; the fit bar shows on calibrated audiences (never coral) and omits on General; "Remix → Read" produces a real in-thread Read + real persona reaction via the reused remix-card LensTrigger (no fabricated reaction on the grid); "+ Track account" persists an idempotent tracked_accounts row; reload rehydrates. Report PASS/PARTIAL/FAIL per step.
    </human-check>
  </verify>
  <done>The full Explore flow works end-to-end against live data with the honesty spine intact (no fabricated grid reaction, no empty fit bar, no fake %, coral only on the CTA); the Track→Explore loop persists idempotently; reload rehydrates; the UAT is recorded PASS (or gaps filed for any FAIL).</done>
</task>

</tasks>

<verification>
- `database.types.ts` contains `tracked_accounts` (push + regen succeeded).
- `ENGINE_VERSION` is unchanged: `grep -q '"3.19.0"' src/lib/engine/version.ts`.
- The engine + KC regression suites are green and `npm run build` succeeds (the protected SIM-1 Max path is byte-identical — Explore made no scoring change).
- The tracked-accounts repo unit test still passes after cast cleanup.
- The end-of-phase UAT is recorded (PASS, or gaps filed).
</verification>

<success_criteria>
The tracked_accounts table is live and typed, the interim casts are dropped, the engine/KC regression gate is green with ENGINE_VERSION frozen at 3.19.0, the full build passes, and the end-to-end Explore flow is human-verified against live data with the honesty spine intact — closing Phase 11.
</success_criteria>

<output>
Create `.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-08-SUMMARY.md` when done.
</output>
