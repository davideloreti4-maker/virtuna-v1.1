---
phase: 11-explore-audience-curated-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260620090000_tracked_accounts.sql
autonomous: true
requirements: [EXPLORE-05]
must_haves:
  truths:
    - "A tracked_accounts migration file exists defining a flat, typed, RLS-own-rows-only watchlist table (mirrors saved_items) — NOT applied to the live DB in this plan (push is the BLOCKING final wave 11-08)"
    - "The table is flat + Library-compatible (D-08): no folder_id, no tags, no CMS — P12 surfaces/manages it with no rework"
    - "Tracking the same (user, platform, handle) twice is idempotent via a UNIQUE constraint (no duplicate rows)"
  artifacts:
    - path: "supabase/migrations/20260620090000_tracked_accounts.sql"
      provides: "tracked_accounts table + RLS policy + indexes (the watchlist producer rail, EXPLORE-05 / D-08)"
      contains: "CREATE TABLE IF NOT EXISTS public.tracked_accounts"
  key_links:
    - from: "supabase/migrations/20260620090000_tracked_accounts.sql"
      to: "auth.users"
      via: "user_id FK ON DELETE CASCADE + RLS auth.uid() = user_id (mirrors saved_items)"
      pattern: "auth.uid"
---

<objective>
Author the `tracked_accounts` migration file — the durable, flat-typed watchlist rail that Explore's "+ Track account" write persists into (EXPLORE-05 producer half / D-08). This plan WRITES the migration only; it is applied to the live database in the BLOCKING final wave (11-08), mirroring how prior phases (07-06, 10-07) sequenced live schema push + types regen into a final gated wave.

Purpose: D-08 says P11 ships the minimal "Track this account" WRITE that persists a row "flat + typed + Library-compatible" so P12 builds the Library management UI with no rework. RESEARCH Open Q1 recommends a NEW dedicated table (not overloading `saved_items`) because a tracked account is an input HANDLE, not a saved block snapshot — and the `saved_items` migration comment explicitly says "P12 EXTENDS with separate tables." This plan lays that table.
Output: `supabase/migrations/20260620090000_tracked_accounts.sql`.

Note: EXPLORE-06 (comment seeding) is intentionally DEFERRED again (D-09) — it is NOT planned in Phase 11. Do not add a comments/seeding table.
</objective>

<artifacts_this_phase_produces>
This plan creates:
- `supabase/migrations/20260620090000_tracked_accounts.sql` — NEW: the `tracked_accounts` table (id, user_id, platform, handle, source_video_id, created_at), a UNIQUE(user_id, platform, handle) idempotency constraint, RLS own-rows-only policy, and a user index.
(11-03 builds the repo + route over this table; 11-08 applies it to live prod + regenerates database.types.ts.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the tracked_accounts migration (EXPLORE-05 / D-08)</name>
  <read_first>
    - supabase/migrations/20260619100200_saved_items.sql (the EXACT RLS idiom to mirror: auth.users FK ON DELETE CASCADE, ENABLE ROW LEVEL SECURITY, FOR ALL TO authenticated USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id), the user index, the FLAT-by-construction header comment "P12 EXTENDS with separate tables, never reworks this shape")
    - .planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-RESEARCH.md (§Architecture Pattern 6 — the exact migration DDL: columns, platform CHECK, UNIQUE(user_id, platform, handle), the RLS policy tracked_all_own, the index tracked_accounts_user_idx)
  </read_first>
  <action>
    Create `supabase/migrations/20260620090000_tracked_accounts.sql` mirroring the `saved_items` migration structure exactly.

    Table definition (use these exact columns and constraints): `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`; `user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE`; `platform text NOT NULL DEFAULT 'tiktok' CHECK (platform IN ('tiktok','instagram','youtube'))`; `handle text NOT NULL` (comment: the @handle tracked, no '@', lowercased); `source_video_id text` (comment: the outlier tile this was tracked from — provenance); `created_at timestamptz NOT NULL DEFAULT now()`; and a table-level `UNIQUE (user_id, platform, handle)` (comment: idempotent track — no dup rows). Wrap the CREATE in `IF NOT EXISTS`.

    Add a `COMMENT ON TABLE public.tracked_accounts` stating: flat-typed watchlist input State (EXPLORE-05 / D-08, the Explore PRODUCER half). FLAT by construction — no folder_id, no tags, no CMS. P12 surfaces/manages it (Library) with no rework. One row per (user, platform, handle); UNIQUE makes Track idempotent.

    Enable RLS and add the own-rows-only policy mirroring `saved_all_own`: `ALTER TABLE public.tracked_accounts ENABLE ROW LEVEL SECURITY;` then `DROP POLICY IF EXISTS tracked_all_own ...` then `CREATE POLICY tracked_all_own ON public.tracked_accounts FOR ALL TO authenticated USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);`.

    Add the user index: `CREATE INDEX IF NOT EXISTS tracked_accounts_user_idx ON public.tracked_accounts (user_id);`.

    Do NOT add folder_id, tags, a snapshot column, or any comment-seeding table (EXPLORE-06 deferred, D-09). Do NOT run `supabase db push` in this plan — applying the migration to live prod is the BLOCKING final wave (11-08).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260620090000_tracked_accounts.sql && grep -q "CREATE TABLE IF NOT EXISTS public.tracked_accounts" supabase/migrations/20260620090000_tracked_accounts.sql && grep -q "auth.uid() = user_id" supabase/migrations/20260620090000_tracked_accounts.sql && grep -qi "UNIQUE (user_id, platform, handle)" supabase/migrations/20260620090000_tracked_accounts.sql && echo "MIGRATION OK"</automated>
  </verify>
  <done>The migration file exists with the tracked_accounts table (UNIQUE(user_id,platform,handle), platform CHECK, source_video_id provenance), RLS own-rows policy tracked_all_own, and the user index; it is flat (no folder_id/tags/snapshot); it is NOT pushed to live prod (deferred to 11-08); the verify command prints MIGRATION OK.</done>
</task>

</tasks>

<verification>
- The migration file parses as valid SQL DDL (CREATE TABLE + RLS + index present).
- It mirrors `saved_items` RLS idiom: `grep -q "FOR ALL TO authenticated" ...` and `grep -q "auth.uid() = user_id" ...` both succeed.
- It is flat: `grep -qi "folder_id\|tags" supabase/migrations/20260620090000_tracked_accounts.sql` returns nothing.
- The migration is NOT applied to the live DB in this plan (no `supabase db push` invoked).
</verification>

<success_criteria>
A flat, typed, RLS-own-rows-only `tracked_accounts` migration exists, idempotent via UNIQUE(user_id,platform,handle), Library-compatible (P12 extends, no rework), with the live-prod push deferred to the BLOCKING wave 11-08.
</success_criteria>

<output>
Create `.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-02-SUMMARY.md` when done.
</output>
