---
phase: 11-explore-audience-curated-discovery
plan: 03
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - src/lib/tracked-accounts/tracked-accounts-repo.ts
  - src/app/api/tracked-accounts/route.ts
  - src/lib/tracked-accounts/tracked-accounts-repo.test.ts
autonomous: true
requirements: [EXPLORE-05]
must_haves:
  truths:
    - "createTrackedAccount persists one watchlist row with user_id ALWAYS derived from the session (CR-01) — never from the request body"
    - "Tracking the same (platform, handle) twice does not error and does not duplicate (idempotent upsert on the UNIQUE constraint)"
    - "POST/GET/DELETE /api/tracked-accounts are auth-first (401 unauthenticated) and csrfGuard-protected on the mutating methods (mirrors /api/saved)"
  artifacts:
    - path: "src/lib/tracked-accounts/tracked-accounts-repo.ts"
      provides: "listTrackedAccounts / createTrackedAccount / deleteTrackedAccount (flat-typed repo, mirrors shelf-repo)"
      exports: ["listTrackedAccounts", "createTrackedAccount", "deleteTrackedAccount"]
      min_lines: 70
    - path: "src/app/api/tracked-accounts/route.ts"
      provides: "POST (write) / GET (list, for P12) / DELETE (remove) — auth-first + csrfGuard"
      exports: ["POST", "GET", "DELETE"]
  key_links:
    - from: "src/app/api/tracked-accounts/route.ts"
      to: "src/lib/tracked-accounts/tracked-accounts-repo.ts"
      via: "createTrackedAccount / listTrackedAccounts / deleteTrackedAccount"
      pattern: "createTrackedAccount"
    - from: "src/lib/tracked-accounts/tracked-accounts-repo.ts"
      to: "tracked_accounts table"
      via: "session-derived user_id insert + UNIQUE upsert (CR-01)"
      pattern: "auth.getUser"
---

<objective>
Build the watchlist write half (EXPLORE-05 producer / D-08): a flat-typed `tracked-accounts-repo.ts` (mirroring `shelf-repo.ts`) and a `/api/tracked-accounts` route (mirroring `/api/saved`). The tile's "+ Track account" button (built in 11-05) POSTs here; P12 consumes GET for its Library surface. The producer goes live immediately so the track→explore loop works the moment P11 ships.

Purpose: D-08 — P11 ships the minimal Track WRITE; the row is flat + typed + Library-compatible. Honesty + security spine (CR-01): user_id is ALWAYS session-derived, never from the body; the write is idempotent so tracking twice is a no-op.
Output: `tracked-accounts-repo.ts` (list/create/delete), `/api/tracked-accounts/route.ts` (POST/GET/DELETE), and a repo unit test.
</objective>

<artifacts_this_phase_produces>
This plan creates:
- `src/lib/tracked-accounts/tracked-accounts-repo.ts` — NEW: `listTrackedAccounts(supabase)`, `createTrackedAccount(supabase, input)` (zod-validated, session-derived user_id, idempotent upsert), `deleteTrackedAccount(supabase, id)`; domain types `TrackedAccount` + `TrackedAccountInput`.
- `src/app/api/tracked-accounts/route.ts` — NEW: `POST` (write one row), `GET` (list for P12), `DELETE` (remove by id) — all auth-first; csrfGuard on POST/DELETE.
- `src/lib/tracked-accounts/tracked-accounts-repo.test.ts` — NEW: validation + session-derived-user_id proof (mocked SupabaseClient).
(11-05 wires the tile "+ Track account" button to POST this route. 11-08 regenerates database.types.ts so the interim `(supabase as any)` casts can drop.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tracked-accounts-repo.ts — flat-typed watchlist repo (EXPLORE-05 / CR-01)</name>
  <read_first>
    - src/lib/shelf/shelf-repo.ts (the EXACT idiom to mirror: SupabaseClient param, a zod input schema, `safeParse` → throw "invalid ... input" on failure, CR-01 `auth.getUser()` to derive user_id and throw "unauthenticated" when absent, `(supabase as any)`-style insert, the list/create/delete trio shape)
    - supabase/migrations/20260620090000_tracked_accounts.sql (the table columns this repo writes: user_id, platform, handle, source_video_id; the UNIQUE(user_id, platform, handle) the idempotent upsert targets) [from 11-02]
    - .planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-RESEARCH.md (§Architecture Pattern 6 — repo names listTrackedAccounts/createTrackedAccount/deleteTrackedAccount; idempotent via the unique constraint / upsert; user_id ALWAYS session-derived; `(supabase as any)` interim cast convention until 11-08 types regen)
  </read_first>
  <action>
    Create `src/lib/tracked-accounts/tracked-accounts-repo.ts` mirroring `shelf-repo.ts`.

    Domain types: `interface TrackedAccount { id: string; user_id: string; platform: "tiktok"|"instagram"|"youtube"; handle: string; source_video_id: string | null; created_at: string }` and the writable `interface TrackedAccountInput { platform: "tiktok"|"instagram"|"youtube"; handle: string; source_video_id?: string | null }` (NO user_id — CR-01, session-derived).

    Zod schema `TrackedAccountInputSchema`: `platform: z.enum(["tiktok","instagram","youtube"])`, `handle: z.string().min(1).max(100)`, `source_video_id: z.string().nullable().optional()`. In `createTrackedAccount`, BEFORE validating, normalize the handle: strip a leading "@" and lowercase (`handle.replace(/^@/, "").trim().toLowerCase()`) so the UNIQUE constraint dedupes case/`@` variants — matches the migration comment "(no '@', lowercased)".

    `listTrackedAccounts(supabase): Promise<TrackedAccount[]>` — select * from tracked_accounts ordered by created_at desc (RLS scopes to the user). Throw on error.

    `createTrackedAccount(supabase, input): Promise<TrackedAccount>` — `safeParse` the input (throw `invalid tracked account input: ...` on failure → the route maps this to 400). Derive user_id via `auth.getUser()` (CR-01; throw "unauthenticated" if absent). Insert with `{ ...parsed.data, handle: normalizedHandle, user_id }` using an IDEMPOTENT upsert: `.upsert(payload, { onConflict: "user_id,platform,handle" }).select("*").single()` so re-tracking the same account returns the existing row instead of erroring on the UNIQUE constraint. Use the `(supabase as any)` cast convention (database.types.ts has no tracked_accounts type until 11-08).

    `deleteTrackedAccount(supabase, id): Promise<void>` — delete by id (RLS scopes to owner). Throw on error.

    Add a file header: flat-typed watchlist repo (EXPLORE-05 / D-08), mirrors shelf-repo; user_id ALWAYS session-derived (CR-01); FLAT — no folders/tags; P12 EXTENDS, never reworks.
  </action>
  <behavior>
    - createTrackedAccount with a body containing user_id IGNORES it — the persisted user_id comes from the mocked session (CR-01 proof).
    - createTrackedAccount with handle "@GymBeginner" persists handle "gymbeginner" (strip @, lowercase).
    - createTrackedAccount with an invalid platform (e.g. "linkedin") throws an error whose message starts with "invalid tracked account input".
    - createTrackedAccount with no authenticated session throws "unauthenticated".
    - The insert call uses an upsert with onConflict "user_id,platform,handle" (idempotency).
  </behavior>
  <verify>
    <automated>npx vitest run src/lib/tracked-accounts/tracked-accounts-repo.test.ts</automated>
  </verify>
  <done>tracked-accounts-repo.ts exports listTrackedAccounts/createTrackedAccount/deleteTrackedAccount; user_id is session-derived (never from input); handle is normalized (@-stripped, lowercased); the write is an idempotent upsert on the UNIQUE constraint; invalid input throws a 400-mappable error; the test suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: /api/tracked-accounts route — POST/GET/DELETE, auth-first + csrfGuard (EXPLORE-05)</name>
  <read_first>
    - src/app/api/saved/route.ts (the EXACT route idiom to mirror: per-method `createClient()` → `auth.getUser()` 401 gate, POST body `request.json().catch(() => null)` guard, delegating validation/user_id to the repo, 201 on create, the zod-failure → 400 mapping, DELETE reads `?id=`)
    - src/app/api/tools/hooks/route.ts (the `csrfGuard(request)` call pattern — Content-Type 415 + cross-origin 403 on mutating POSTs; import from `@/lib/http/csrf-guard`)
    - src/lib/tracked-accounts/tracked-accounts-repo.ts (the repo functions this route delegates to) [Task 1]
  </read_first>
  <action>
    Create `src/app/api/tracked-accounts/route.ts` mirroring `/api/saved/route.ts`, delegating to the Task-1 repo.

    `POST` — `createClient()` → `auth.getUser()` (401 if no user) → `csrfGuard(request)` (return the guard Response if truthy) → parse `request.json().catch(() => null)` (400 "Invalid body" if not an object) → `createTrackedAccount(supabase, body)` → `Response.json({ account }, { status: 201 })`. In the catch, if the error message starts with "invalid tracked account input" return 400 with that message; if it is "unauthenticated" return 401; else log + 500.

    `GET` — `auth.getUser()` (401) → `listTrackedAccounts(supabase)` → `Response.json({ accounts })`. (This is the read P12's Library surface consumes; P11 only needs it to exist.)

    `DELETE` — `auth.getUser()` (401) → `csrfGuard(request)` → read `?id=` (`400 "id query param is required"` if absent) → `deleteTrackedAccount(supabase, id)` → `Response.json({ success: true })`.

    Match the existing /api/saved error-logging style (`console.error("[tracked-accounts] ...")`). Do NOT accept user_id from the body anywhere (CR-01).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "tracked-accounts/route" || echo "tracked-accounts route typechecks clean"</automated>
  </verify>
  <done>POST writes one row (201) via the repo, GET lists, DELETE removes by id; all methods 401 unauthenticated; POST+DELETE call csrfGuard; user_id is never read from the body; zod failures map to 400; the route typechecks clean.</done>
</task>

</tasks>

<verification>
- `tracked-accounts-repo.test.ts` passes (session-derived user_id, handle normalization, invalid-input throw, upsert idempotency).
- The route delegates to the repo and never references user_id from the body: `grep -n "user_id" src/app/api/tracked-accounts/route.ts` shows no body-sourced user_id.
- `npx tsc --noEmit` reports no new errors in the repo or route.
- Build is not broken: `npm run build` is not required here (types come from database.types.ts which lacks the table until 11-08 — the `(supabase as any)` cast holds the surface, matching the audience-repo/shelf-repo interim convention).
</verification>

<success_criteria>
A flat-typed watchlist write path is live (repo + route), CR-01-safe (session-derived user_id), idempotent (upsert on UNIQUE), auth-first + csrfGuard-protected, with a GET that P12's Library will consume — all typechecking, with the repo's behaviour locked by a unit test.
</success_criteria>

<output>
Create `.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-03-SUMMARY.md` when done.
</output>
