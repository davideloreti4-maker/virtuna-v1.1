---
phase: 11-explore-audience-curated-discovery
plan: 04
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/lib/tools/runners/explore-runner.ts
  - src/app/api/tools/explore/route.ts
  - src/lib/tools/runners/explore-runner.test.ts
autonomous: true
requirements: [EXPLORE-01, EXPLORE-02, EXPLORE-03]
must_haves:
  truths:
    - "POST /api/tools/explore pulls outliers (apidojo, reusing the Discover pull/rank stack), audience-fit-re-ranks them (rankWithAudienceFit), and streams an outlier-grid block over SSE following the skill-chain contract (auth → csrfGuard → openThread → getAudience → stream → persist + KC stamp)"
    - "The fit re-rank is pure deterministic math at the route/runner layer — NO SIM call on the grid, NO video-scoring call, ENGINE_VERSION untouched (D-02/D-03, Pitfall 6)"
    - "The active audience is loaded from openThread.active_audience_id (NEVER the request body, CR-01); General/preset/thin degrades every tile to fit:null"
    - "Profile-mode tiles carry trackable:true + trackHandle (the normalized pull input handle); niche-mode tiles carry trackable:false (no author handle exists — RESEARCH Q3)"
  artifacts:
    - path: "src/lib/tools/runners/explore-runner.ts"
      provides: "runExplorePipeline: pull → rankOutliers → rankWithAudienceFit → build validated outlier-grid block (no SIM call)"
      exports: ["runExplorePipeline"]
      min_lines: 70
    - path: "src/app/api/tools/explore/route.ts"
      provides: "Explore SSE route (mirrors hooks route): auth/csrf/audience/stream/persist + KC stamp"
      exports: ["POST"]
  key_links:
    - from: "src/lib/tools/runners/explore-runner.ts"
      to: "src/lib/discover/explore-rank.ts"
      via: "rankWithAudienceFit(rankOutliers(videos,mode), audience, serendipity)"
      pattern: "rankWithAudienceFit"
    - from: "src/lib/tools/runners/explore-runner.ts"
      to: "src/lib/tools/blocks.ts"
      via: "OutlierGridBlockSchema.safeParse on the built block (D-14 belt-and-suspenders)"
      pattern: "OutlierGridBlockSchema.safeParse"
    - from: "src/app/api/tools/explore/route.ts"
      to: "openThread.active_audience_id"
      via: "getAudience(supabase, activeAudienceId) — never from body (CR-01)"
      pattern: "active_audience_id"
---

<objective>
Build the Explore server half (EXPLORE-01/02/03): lift the `/api/discover` pull/rank logic into a new SSE route `/api/tools/explore` that follows the canonical skill-chain contract, runs the audience-fit re-rank (11-01's `rankWithAudienceFit`), threads the profile-mode handle so "+ Track account" has a source, builds the extended `outlier-grid` block, and persists it to the open thread. Everything below this route already exists (the tap → Remix → Read chain is reused, not built).

Purpose: EXPLORE-01 (Explore skill in-thread, customizable params + serendipity valve) + EXPLORE-03 (audience-relative scoring) live here; EXPLORE-02's eager fit signal is emitted on the tiles (the on-tap real reaction comes free from the reused remix-card in 11-05/06). Hard constraint: the fit math is pure at the route/runner layer — NO SIM call on the grid, ENGINE_VERSION stays "3.19.0" (D-02/D-03, Pitfall 6).
Output: `explore-runner.ts` (pull→rank→fit→build block, validated), `/api/tools/explore/route.ts` (the SSE route), and a runner unit test.
</objective>

<artifacts_this_phase_produces>
This plan creates:
- `src/lib/tools/runners/explore-runner.ts` — NEW: `runExplorePipeline({ params, audience, mode, normalizedInput })` → pull via `createScrapingProvider().scrapeVideos` → `rankOutliers` → `rankWithAudienceFit(ranked, audience, serendipity)` → build the `outlier-grid` block with per-tile `fit` + `trackable` + `trackHandle` → `OutlierGridBlockSchema.safeParse` (D-14). NO SIM call.
- `src/app/api/tools/explore/route.ts` — NEW: `POST` SSE route mirroring `hooks/route.ts` (auth → csrfGuard → param parse+cap → createOpenThreadLazy → getAudience from active_audience_id → checkUserCap → cache/scrape via the runner → SSE stage/content/done → insertMessage + kcStamp).
- `src/lib/tools/runners/explore-runner.test.ts` — NEW: fit-threading + trackable-by-mode + safeParse-validity proof (mocked provider + a stub audience).
(11-05 builds the stream hook + the tile fit-bar/Track button; 11-06 the thread view; 11-07 the composer wiring. EXPLORE-06 comment seeding is DEFERRED — D-09.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-RESEARCH.md
@.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: explore-runner.ts — pull → rank → audience-fit → validated block (EXPLORE-03, no SIM call)</name>
  <read_first>
    - src/app/api/discover/route.ts (the pull/rank logic to lift: `createScrapingProvider().scrapeVideos(normalized, SCRAPE_LIMIT)` → `rankOutliers(videos, mode).slice(0, MAX_TILES)` → map to tiles with a `source` tag; MAX_TILES=30; the niche-vs-profile `source` tagging)
    - src/lib/discover/explore-rank.ts (rankWithAudienceFit / FitLevel / FitRankedOutlier — the 11-01 fit module this runner calls AFTER rankOutliers) [from 11-01]
    - src/lib/tools/blocks.ts (the EXTENDED OutlierGridBlockSchema + OutlierGridBlock type — the exact tile prop shape incl. fit/trackable/trackHandle to emit, and the no-band/no-model honesty contract) [extended in 11-01]
    - src/app/(app)/discover/discover-client.tsx (the `toTileData` shape mapping RankedOutlier → tile props incl. `source`/`baselineLabel`; the displaySource niche-vs-Competitor logic to reuse)
    - src/lib/scraping/types.ts (VideoData — confirm no author handle field exists; this is WHY niche-mode tiles are not trackable — RESEARCH Q3)
  </read_first>
  <action>
    Create `src/lib/tools/runners/explore-runner.ts` exporting `runExplorePipeline`. Signature: `runExplorePipeline(opts: { audience: Audience; mode: "profile"|"niche"; normalizedInput: string; serendipity: number }): Promise<{ block: OutlierGridBlock }>`.

    Body:
    1. Pull: `const provider = createScrapingProvider(); const videos = await provider.scrapeVideos(opts.normalizedInput, 30);` (reuse the Discover SCRAPE_LIMIT/MAX_TILES band — RESEARCH §Don't Hand-Roll: do NOT re-roll the scrape).
    2. Rank (measured, honest): `const ranked = rankOutliers(videos, opts.mode).slice(0, 30);`
    3. Audience-fit re-rank (EXPLORE-03 / D-01): `const fitRanked = rankWithAudienceFit(ranked, opts.audience, opts.serendipity);` — PURE math, NO SIM call (Pitfall 6).
    4. Determine the Track handle source (RESEARCH Q3, resolved): profile-mode pulls are trackable with the handle = the pull input. Compute `const trackHandle = opts.mode === "profile" ? opts.normalizedInput.replace(/^@/, "").toLowerCase() : undefined;` and `const trackable = opts.mode === "profile";` (niche-mode tiles carry no author handle on VideoData → not trackable in P11; note this with a `// RESEARCH Q3: niche-mode video items expose no author handle → profile-mode Track only` comment).
    5. Build the `outlier-grid` block: `{ type: "outlier-grid", props: { mode: opts.mode, tiles: fitRanked.map((t) => ({ platformVideoId, videoUrl, caption, views, likes, comments, shares, saves, durationSeconds, postedAt: t.postedAt.toISOString(), multiplier: t.multiplier, baselineLabel: t.baselineLabel, source: <reuse discover source tag>, fit: t.fit, trackable, trackHandle: trackable ? trackHandle : undefined })) } }`. Map `source` the same way Discover does (niche pulls tag the niche query, profile pulls tag "Competitor"/the channel — reuse `displaySource` logic).
    6. D-14 belt-and-suspenders: `const parsed = OutlierGridBlockSchema.safeParse(block); if (!parsed.success) throw new Error("explore block validation failed: " + parsed.error.message);` (mirrors hooks-runner safeParse — Pitfall 4). Return `{ block: parsed.data }`.

    Add a file header: pull→rank→audience-fit→build, NO SIM call on the grid (D-02/D-03), ENGINE_VERSION untouched (3.19.0). Honesty: tiles carry fit (estimate) + measured multiplier ONLY — never a band/model/quote.
  </action>
  <behavior>
    - Given a calibrated audience + a mocked provider returning videos, the built block validates against OutlierGridBlockSchema and each tile has `fit.level` in {Strong,Fair,Weak}.
    - Given a General audience, every tile has `fit: null` (degrade flows through from rankWithAudienceFit).
    - mode "profile" → every tile has `trackable: true` and `trackHandle` = the normalized input (no @, lowercase).
    - mode "niche" → every tile has `trackable: false` and `trackHandle` undefined.
    - The runner imports nothing from `@/lib/engine` scoring and makes no SIM/Flash call (the only network is the provider scrape).
  </behavior>
  <verify>
    <automated>npx vitest run src/lib/tools/runners/explore-runner.test.ts</automated>
  </verify>
  <done>runExplorePipeline pulls (reusing scrapeVideos), ranks via rankOutliers, fit-re-ranks via rankWithAudienceFit, builds a schema-valid outlier-grid block with per-tile fit + mode-appropriate trackable/trackHandle, validates via safeParse, and makes ZERO SIM calls; the test suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: /api/tools/explore/route.ts — SSE route on the skill-chain contract (EXPLORE-01/02)</name>
  <read_first>
    - src/app/api/tools/hooks/route.ts (the EXACT SSE route skeleton to clone: auth gate → csrfGuard → body parse + server-side length caps → load creator_profiles → createOpenThreadLazy → load active audience from openThread.active_audience_id via getAudience with GENERAL_AUDIENCE fallback (CR-01 — id NEVER from body) → ReadableStream with `send(event,data)` → stage events around the real pipeline → content event → done → insertMessage(openThread.id,"assistant",[block],kcStamp().kcGenVersion) → sseHeaders())
    - src/app/api/discover/route.ts (the daily-cap + cache reuse: `checkUserCap(user.id)` 429 friendly-error shape, `getCachedDiscover`/`setCachedDiscover`/`recordUserPull`, `classifyDiscoverInput`, MAX_INPUT_LENGTH=200; RESEARCH Pitfall 5 — the cache/cap are in-memory, reuse as-is and document it)
    - src/lib/tools/runners/explore-runner.ts (runExplorePipeline — the runner this route calls) [Task 1]
    - .planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-RESEARCH.md (§Architecture Pattern 1 row 2 + the SSE stage contract: stages "Pulling outliers" → "Scoring for your audience" → done; NO fake %; the route may inline the runner but Explore has no Flash/gate loop so a thin runner is fine; §Pitfall 5)
    - .planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-UI-SPEC.md (§Copywriting — the loading line "Pulling outliers and scoring them for your audience… this can take a few minutes"; honesty: no fake progress %)
  </read_first>
  <action>
    Create `src/app/api/tools/explore/route.ts` cloning the hooks-route shape, but the body work delegates to `runExplorePipeline` (no Flash/gate loop).

    Auth + guard: `createClient()` → `auth.getUser()` (401) → `csrfGuard(request)`.

    Body params (EXPLORE-01 customizable params + serendipity valve): parse `{ niche?, accounts?, timeWindow?, serendipity? }` (or a single `input` string for the quick-action presets). Server-side caps (independent of client, mirror hooks/discover): cap `niche`/`accounts` strings at 200 chars; coerce `serendipity` to a number clamped 0..1 (default 0). Determine the pull input string: prefer `accounts` (a handle → profile pull) else `niche` (→ niche pull); if both empty return 400 "input is required". Run `classifyDiscoverInput(input)` → `{ mode, normalized }`.

    Open thread + audience (CR-01): `createOpenThreadLazy(user.id)` → read `openThread.active_audience_id` → if non-null `getAudience(supabase, id)` (fallback `GENERAL_AUDIENCE`, non-fatal try/catch) → `activeAudience`. Audience id is NEVER read from the body.

    Daily cap + cache (reuse, Pitfall 5): `checkUserCap(user.id)` → 429 friendly-error shape if not allowed. Check `getCachedDiscover(normalized, mode)` — note the cache stores raw tiles; for Explore, recompute the fit re-rank per-request even on a cache hit (the audience can differ), OR cache by (input, mode, day) and re-run fit on the cached `RankedOutlier[]`. Simplest honest path: on cache hit, still call `rankWithAudienceFit` on the cached ranked tiles before building the block (fit depends on the active audience, not just the pull). Document the in-memory limitation in a comment (real rate-limiting is HARDEN-01).

    SSE stream (mirror hooks): `ReadableStream` with `send(event,data)`. Emit `send("stage", { name: "Pulling outliers", status: "active" })` before the scrape; after the scrape `send("stage", { name: "Pulling outliers", status: "done" })`; `send("stage", { name: "Scoring for your audience", status: "active" })` around the fit re-rank, then done. Call `runExplorePipeline({ audience: activeAudience, mode, normalizedInput: normalized, serendipity })`. `send("content", { blocks: [block] })`. On a real scrape, `recordUserPull(user.id)` + `setCachedDiscover(...)`. `send("done", { count: block.props.tiles.length })`. On error `send("error", { message })`. In `finally`, close the controller. Persist: `await insertMessage(openThread.id, "assistant", [block], kcStamp().kcGenVersion)` (KC stamp like every skill route, D-10). Return `new Response(stream, { headers: sseHeaders() })`.

    NO Flash/SIM call anywhere in this route (Pitfall 6). NO fake progress `%` (UI-SPEC honesty — the pull is genuinely minutes). The route MUST NOT navigate or arm any nav (it is an SSE producer only).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "tools/explore/route" || echo "explore route typechecks clean"</automated>
  </verify>
  <done>POST /api/tools/explore is auth-first + csrfGuard, parses capped params + serendipity, loads the active audience from active_audience_id (never body), enforces the daily cap, pulls+ranks+fit-scores via runExplorePipeline (no SIM call), streams Pulling→Scoring stages + the outlier-grid block content-first with no fake %, persists with a KC stamp, and typechecks clean.</done>
</task>

</tasks>

<verification>
- `explore-runner.test.ts` passes (fit threading, trackable-by-mode, safeParse validity, zero SIM calls).
- No SIM/engine-scoring import in the route or runner: `grep -rE "runPredictionPipeline|aggregateScores|runFlash|ENGINE_VERSION|runWave3" src/app/api/tools/explore/route.ts src/lib/tools/runners/explore-runner.ts` returns nothing.
- Audience id is never read from the body: `grep -n "active_audience_id" src/app/api/tools/explore/route.ts` is present; `grep -n "body.*audience" src/app/api/tools/explore/route.ts` returns nothing.
- `npx tsc --noEmit` reports no new errors in the route or runner.
</verification>

<success_criteria>
The Explore server half is live: a skill-chain SSE route that pulls outliers, scores them for the active audience with pure math (no SIM call, ENGINE_VERSION frozen), threads the profile-mode Track handle, builds + validates + persists the extended outlier-grid block with a KC stamp, and streams honest (no-fake-%) progress — runner behaviour locked by a unit test.
</success_criteria>

<output>
Create `.planning/phases/11-explore-audience-curated-discovery-expansion-not-yet-discuss/11-04-SUMMARY.md` when done.
</output>
