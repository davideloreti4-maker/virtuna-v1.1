---
phase: 01-engine-pack-seam
plan: 01
subsystem: testing
tags: [vitest, npm, engine, baseline, infra]

# Dependency graph
requires:
  - phase: 00-engine-rework (on main)
    provides: ENGINE_VERSION 3.20.0, the pre-seam Socials engine + 91-file vitest engine suite
provides:
  - Restored node_modules (typescript, vitest 4.1.9, zod) so tsc + vitest run real tests
  - A recorded GREEN pre-seam baseline (91 files / 1151 passed + 20 skipped, ~13s) the refactor is measured against
  - Confirmation ENGINE_VERSION is pinned 3.20.0 (cache-key invariant)
affects: [01-02 domain-pack types, 01-03 socials pack, 01-04 smoke gate, 01-05 route rewire, 01-06 harness rewire]

# Tech tracking
tech-stack:
  added: []  # no new packages — deps restored from main-vetted package.json
  patterns:
    - "Run engine tests via `node ./node_modules/vitest/vitest.mjs run` (NOT npm test/npx vitest — fake PASS(0) when deps absent, RESEARCH Pitfall 3)"

key-files:
  created:
    - .planning/phases/01-engine-pack-seam/01-01-SUMMARY.md
  modified:
    - .planning/phases/01-engine-pack-seam/01-VALIDATION.md

key-decisions:
  - "package-lock.json is gitignored + was absent → deps resolved fresh from the main-vetted package.json ranges; the plan's 'byte-unchanged tracked-lockfile restore' is N/A in this worktree (not a blocker — goal is a working test runner)"

patterns-established:
  - "Engine-suite feedback latency ~13s — sample after each task commit on touched tests, full engine suite per wave"

requirements-completed: [PACK-04]

# Metrics
duration: 9min
completed: 2026-06-26
---

# Phase 01 Plan 01: Wave 0 Enabler — Restore deps + green pre-seam baseline Summary

**node_modules restored from the main-vetted package.json (vitest 4.1.9); engine suite captured GREEN at 91 files / 1151 passed + 20 skipped (~13s) with ENGINE_VERSION pinned 3.20.0 — the reference the pack-seam refactor is measured against.**

## Performance

- **Duration:** ~9 min (7 min of which was `npm install`)
- **Started:** 2026-06-26T12:53:06Z
- **Completed:** 2026-06-26T13:02:24Z
- **Tasks:** 2
- **Files modified:** 1 tracked (01-VALIDATION.md) + node_modules/package-lock.json (gitignored)

## Accomplishments
- Restored `node_modules` via `npm install` — 1003 packages; vitest now runs real tests (kills RESEARCH Pitfall 3's fake PASS(0)/FAIL(0) trap)
- Captured GREEN pre-seam engine baseline: `src/lib/engine` = 91 files, 1151 passed + 20 skipped, 12.65s (vitest) / ~13s wall-clock
- Confirmed `ENGINE_VERSION === "3.20.0"` via both gate tests (`version.test.ts` + `audience-regression-gate.test.ts`) — the cache-key invariant the phase keeps pinned
- Populated VALIDATION.md runtime fields + Wave 0 checkboxes; set `wave_0_complete: true`

## Task Commits

1. **Task 1: Restore node_modules from package.json** — no tracked commit (node_modules + package-lock.json are gitignored; environment-only restore, no tracked files changed — not force-adding gitignored content per the SDK skip contract)
2. **Task 2: Capture green pre-seam baseline + record runtime** — `0effdea1` (test)

**Plan metadata:** see final docs commit (this SUMMARY + STATE.md + ROADMAP.md)

## Files Created/Modified
- `.planning/phases/01-engine-pack-seam/01-VALIDATION.md` — runtime fields (~13s), Wave 0 checkboxes marked done, task-status rows → green, `wave_0_complete: true`
- `node_modules/` (gitignored) — restored deps
- `package-lock.json` (gitignored) — freshly generated by npm install

## Decisions Made
- Engine tests run only via `node ./node_modules/vitest/vitest.mjs run` (project memory + RESEARCH Pitfall 3). Verified the runner reports non-zero counts, confirming a real run.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] package-lock.json absent + gitignored — plan assumed a tracked vetted lockfile to restore**
- **Found during:** Task 1 (npm install)
- **Issue:** The plan frontmatter (`files_modified: package-lock.json`) and Task 1's done-criteria assert `package-lock.json` is "byte-unchanged in git status" — a tracked-lockfile restore. In this worktree `package-lock.json` did not exist (`shasum` failed) and is gitignored alongside `node_modules`. So `npm install` resolved fresh from the `package.json` semver ranges (the same ranges vetted on `main`) rather than restoring a pinned tracked lockfile.
- **Fix:** Proceeded with the fresh resolve — the plan's real goal (a working vitest runner + green baseline) is fully achieved. Did NOT force-add the gitignored lockfile/node_modules. Recorded the divergence in VALIDATION.md Wave 0 notes + the task-status table.
- **Files modified:** none (informational) — VALIDATION.md note only
- **Verification:** `git status` shows package-lock.json absent (gitignored); the byte-unchanged assertion is vacuously N/A; engine suite green
- **Committed in:** `0effdea1` (Task 2 — VALIDATION.md note)

---

**Total deviations:** 1 auto-fixed (1 blocking-assumption reconciliation)
**Impact on plan:** No scope change. Threat T-01-SC disposition shifts from "lockfile byte-unchanged" to "fresh resolve from main-vetted package.json"; supply-chain surface is unchanged (no new packages added; deps are the same package.json ranges already vetted on main). No source code touched. Baseline integrity intact.

## Issues Encountered
- `npm install` took ~7 min (1003 packages, cold cache) and emitted benign warnings: 1 EBADENGINE (`@pmndrs/detect-gpu` wants node >=24; running v22.22.3 — not used by the engine suite), deprecation notices, and `2 vulnerabilities (1 moderate, 1 high)` from transitive deps. None block the baseline; no `npm audit fix --force` run (would mutate deps — out of scope for a baseline restore).

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- Wave 0 complete: test runner live, baseline green, ENGINE_VERSION confirmed 3.20.0.
- Plan 02 (DomainPack types, Wave 1) is unblocked — `tsc --noEmit` and vitest both functional.
- Concern to carry: every downstream `<verify>` must use `node ./node_modules/vitest/vitest.mjs run` (never `npm test`/`npx vitest`).

## Self-Check: PASSED

- FOUND: `.planning/phases/01-engine-pack-seam/01-01-SUMMARY.md`
- FOUND: `.planning/phases/01-engine-pack-seam/01-VALIDATION.md`
- FOUND: commit `0effdea1`
- FOUND: `node_modules/vitest`

---
*Phase: 01-engine-pack-seam*
*Completed: 2026-06-26*
