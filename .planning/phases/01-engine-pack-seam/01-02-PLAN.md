---
phase: 01-engine-pack-seam
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - src/lib/engine/domain-pack.ts
  - src/lib/engine/__tests__/domain-pack.contract.test.ts
autonomous: true
requirements: [PACK-03]
must_haves:
  truths:
    - "A typed DomainPack contract exists with all 7 domain-spec fields (D-05), plus the run + id dispatch members"
    - "The scoring sub-shape matches aggregateScores' verbatim signature (arity + option shape), proven at compile time before the pack is built"
  artifacts:
    - path: "src/lib/engine/domain-pack.ts"
      provides: "DomainPack interface + PopulationsSpec/GroundingSpec/ReactionFrameSpec/CalibrationSpec/OutputSchemaSpec/DomainPackScoring/StimulusType spec types"
      contains: "interface DomainPack"
      min_lines: 40
    - path: "src/lib/engine/__tests__/domain-pack.contract.test.ts"
      provides: "Compile-time + light runtime probe that the scoring sub-shape matches the real aggregateScores signature"
  key_links:
    - from: "src/lib/engine/domain-pack.ts"
      to: "src/lib/engine/aggregator.ts (AggregateScoresOptions), pipeline.ts (PipelineResult/PipelineOptions), types.ts (AnalysisInput/PredictionResult), events.ts (StageEventCallback)"
      via: "type-only imports"
      pattern: "import type"
---

<objective>
Define the full 7-field typed `DomainPack` interface NOW (D-05) — `populations / grounding / stimulusTypes / reactionFrame / scoring / outputSchema / calibration` — plus the structural `id` and `run` dispatch members that satisfy ROADMAP SC#1's "the core run path runs `pack[mode].run(...)`". Implement ONLY the type contract this plan; `SOCIALS_PACK` populates it in Plan 03. The contract is the expensive, hard-to-re-cut part (D-05) — get it precise so P3 (General) / P6 (Predict) packs do not need a re-cut.

Purpose: PACK-03 — the schema must be defined and precise enough that `tsc --noEmit` later proves `SOCIALS_PACK satisfies DomainPack`. Under-typed deferred specs would force a mid-milestone re-cut (D-05 forbids).
Output: `src/lib/engine/domain-pack.ts` (pure types, no runtime logic) + a contract probe test.
</objective>

<artifacts_this_phase_produces>
This plan creates: `DomainPack`, `DomainPackScoring`, `PopulationsSpec`, `GroundingSpec`, `ReactionFrameSpec`, `CalibrationSpec`, `OutputSchemaSpec`, `StimulusType` (all in `src/lib/engine/domain-pack.ts`).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-engine-pack-seam/01-CONTEXT.md
@.planning/phases/01-engine-pack-seam/01-RESEARCH.md
@.planning/phases/01-engine-pack-seam/01-PATTERNS.md
@src/lib/engine/types.ts
@src/lib/engine/aggregator.ts
@src/lib/engine/pipeline.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the DomainPack interface + 7 spec types</name>
  <files>src/lib/engine/domain-pack.ts</files>
  <read_first>
    - .planning/phases/01-engine-pack-seam/01-PATTERNS.md §"domain-pack.ts" (the `interface`-not-class idiom; the 7-field shape with the exact existing types each field references)
    - .planning/phases/01-engine-pack-seam/01-RESEARCH.md §"DomainPack 7-Field Map" (per-field Socials value + module)
    - src/lib/engine/aggregator.ts:497-524 (AggregateScoresOptions + aggregateScores signature — the scoring.run shape to mirror VERBATIM)
    - src/lib/engine/pipeline.ts:55 (PipelineResult), :137 (PipelineOptions), :332 (runPredictionPipeline signature — the run() shape)
    - src/lib/engine/types.ts (AnalysisInput, PredictionResult)
    - src/lib/engine/events.ts:17 (StageEventCallback)
  </read_first>
  <behavior>
    - `DomainPack["scoring"].run` has the EXACT arity/shape of `aggregateScores`: `(pipelineResult: PipelineResult, onStageEvent?: StageEventCallback, options?: AggregateScoresOptions) => Promise<PredictionResult>`.
    - `DomainPack["run"]` has the EXACT shape of `runPredictionPipeline`: `(input: AnalysisInput, opts?: PipelineOptions) => Promise<PipelineResult>`.
    - All 7 D-05 domain-spec fields are present and non-`any` (deferred specs are thin but precise — they must not be `unknown`/`any`).
    - `stimulusTypes` is `readonly StimulusType[]` where `StimulusType = "text" | "tiktok_url" | "video_upload"` (the `input_mode` enum, the orthogonal STIMULUS axis — NOT the pack/domain key).
  </behavior>
  <action>
    Create `src/lib/engine/domain-pack.ts` as PURE TYPES (no runtime values — `export interface` + `export type` only; mirror the `types.ts` idiom, NOT a class/DI container per D-05 + PATTERNS §"Typed-object-not-class"). Use `import type` for all cross-module references so this file adds no runtime coupling.

    Define:
    - `export type StimulusType = "text" | "tiktok_url" | "video_upload";` (the input_mode enum = stimulus axis, orthogonal to the pack/domain key — D-08 + RESEARCH anti-pattern "Collapsing input_mode into the pack key").
    - `export interface DomainPackScoring { systemPrompt: string; run(pipelineResult: PipelineResult, onStageEvent?: StageEventCallback, options?: AggregateScoresOptions): Promise<PredictionResult>; }` — the NEW SEAM (D-06). `run` mirrors `aggregateScores` (aggregator.ts:520) VERBATIM; `systemPrompt` is the referenced `APOLLO_SYSTEM_PROMPT`.
    - The 7 D-05 domain-spec types, thin-but-precise (Socials-only fields populated in Plan 03; General/Predict deferred): `PopulationsSpec` (e.g. `{ kind: "socials"; description: string }` — references Audience + persona archetypes conceptually, fully consumed P3), `GroundingSpec` (`{ systemPrompt: string; knowledgeCore: string }` → APOLLO_SYSTEM_PROMPT + KNOWLEDGE_CORE), `ReactionFrameSpec` (type its two members via `typeof` type-only imports of the REAL functions: `selectPersonaSlots` from `./wave3/persona-registry` and `buildAudienceRepaint` from `./flash/build-reaction-panel` — so tsc proves the pack references the real ones; D-05 "do not under-type"), `CalibrationSpec` (`{ kind: "socials"; baselineRef: string }` → calibration-baseline.json + realized-signature, P3+), `OutputSchemaSpec` (`{ name: "PredictionResult"; requiredKeys: readonly string[] }` → types.ts PredictionResult).
    - `export interface DomainPack { id: "socials"; run(input: AnalysisInput, opts?: PipelineOptions): Promise<PipelineResult>; populations: PopulationsSpec; grounding: GroundingSpec; stimulusTypes: readonly StimulusType[]; reactionFrame: ReactionFrameSpec; scoring: DomainPackScoring; outputSchema: OutputSchemaSpec; calibration: CalibrationSpec; }`

    NOTE the documented interpretation (resolves RESEARCH Assumption A1): `run` (orchestration) + the 7 spec fields are the contract; the genuinely socials-specific scoring lives ONLY in `scoring` (= `aggregateScores`, Plan 03). `grounding`/`reactionFrame`/`populations`/`calibration` are populated by REFERENCE this phase (declarative); they are read live by the pipeline only in P3 — do NOT thread the pack into `pipeline.ts`/`deepseek.ts` (Cut Line A, scoped; threading would break ~60 existing test callers and breach D-08). Add a header doc-comment stating this lock.
  </action>
  <verify>
    <automated>node ./node_modules/typescript/bin/tsc --noEmit -p tsconfig.json 2>&1 | grep -q "domain-pack" && exit 1 || exit 0</automated>
  </verify>
  <done>`domain-pack.ts` compiles clean (no tsc errors mention it); all 7 D-05 spec fields + `id` + `run` are present; `scoring.run` and `run` mirror the real signatures; no `any`/`unknown` in the spec types.</done>
  <acceptance_criteria>
    - File is pure types (no runtime exports)
    - 7 domain-spec fields + id + run present
    - scoring.run arity matches aggregateScores verbatim
    - ReactionFrameSpec members typed via `typeof` of the real functions
    - Header doc-comment records the Cut Line A scope lock
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contract probe — prove the scoring shape matches aggregateScores</name>
  <files>src/lib/engine/__tests__/domain-pack.contract.test.ts</files>
  <read_first>
    - src/lib/engine/__tests__/audience-regression-gate.test.ts (the self-contained BLOCKING-gate test idiom)
    - src/lib/engine/aggregator.ts:520 (aggregateScores — the reference the probe binds against)
  </read_first>
  <behavior>
    - A compile-time `satisfies DomainPackScoring["run"]` (or equivalent typed binding) against the real `aggregateScores` reference passes — proving the seam wrap in Plan 03 will type-check.
    - A light runtime assertion confirms the deferred spec types instantiate (a minimal object typed as each Spec compiles + is defined).
  </behavior>
  <action>
    Create `src/lib/engine/__tests__/domain-pack.contract.test.ts`. Import `aggregateScores` (aggregator.ts) and the `DomainPack`/`DomainPackScoring` types. Add a compile-time binding that asserts `aggregateScores` is assignable to `DomainPackScoring["run"]` (e.g. `const _scoringRunProbe: DomainPackScoring["run"] = aggregateScores;`) — this is the de-risk for Plan 03 (if the interface's `scoring.run` shape drifts from the real `aggregateScores`, this fails at compile/test time, not in Plan 03). Add one `it(...)` that constructs a minimal object for each deferred spec type (`PopulationsSpec`, `GroundingSpec`, `CalibrationSpec`, `OutputSchemaSpec`) and `expect(...).toBeDefined()` — confirming the deferred specs are usable, not under-typed. Mirror the BLOCKING-gate doc-comment header from audience-regression-gate.test.ts naming PACK-03.
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/engine/__tests__/domain-pack.contract.test.ts 2>&1 | grep -q "passed"</automated>
  </verify>
  <done>The contract probe is green; the `aggregateScores → DomainPackScoring["run"]` binding compiles; deferred specs instantiate.</done>
  <acceptance_criteria>
    - Probe test green via the vitest binary
    - Compile-time scoring-shape binding present and passing
    - Each deferred spec type has a runtime instantiation assertion
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure type definitions — no runtime, no input, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-RR | Tampering (regression) | DomainPack scoring shape vs real aggregateScores | mitigate | Task 2's compile-time binding catches any drift between the interface and the verbatim `aggregateScores` signature before the pack is built (PACK-02 integrity). |

No new external attack surface (types only). The only risk is a contract/regression mismatch, mitigated by the Task 2 probe.
</threat_model>

<verification>
- `tsc --noEmit` clean for `domain-pack.ts`.
- `domain-pack.contract.test.ts` green.
- All 7 D-05 fields present and precisely typed.
</verification>

<success_criteria>
- The full 7-field `DomainPack` contract exists (PACK-03).
- The scoring sub-shape provably matches `aggregateScores` (de-risks Plan 03).
- Interface is pure types, no `pipeline.ts`/`deepseek.ts` threading (Cut Line A scope lock recorded).
</success_criteria>

<output>
Create `.planning/phases/01-engine-pack-seam/01-02-SUMMARY.md` when done.
</output>
