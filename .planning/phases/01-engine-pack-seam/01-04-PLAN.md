---
phase: 01-engine-pack-seam
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - src/lib/engine/__tests__/pack-seam-smoke.test.ts
autonomous: true
requirements: [PACK-04]
must_haves:
  truths:
    - "A representative PipelineResult fixture fed to pack.scoring.run returns a structurally valid PredictionResult without throwing"
    - "overall_score is finite and within the sane band [0,100]; engine_version === '3.20.0'"
    - "The core dispatch surface (packs/index.ts) holds zero scoring logic (static no-import check)"
  artifacts:
    - path: "src/lib/engine/__tests__/pack-seam-smoke.test.ts"
      provides: "The D-03 BLOCKING smoke + structural gate for the phase"
      contains: "pack.scoring.run"
      min_lines: 40
  key_links:
    - from: "src/lib/engine/__tests__/pack-seam-smoke.test.ts"
      to: "src/lib/engine/packs (resolvePack/SOCIALS_PACK), __tests__/factories.ts (makePipelineResult)"
      via: "fixture → pack.scoring.run assertion"
      pattern: "makePipelineResult|resolvePack|SOCIALS_PACK"
---

<objective>
Author the **D-03 BLOCKING smoke + structural gate** — the phase's load-bearing safety check. It feeds a representative `PipelineResult` fixture (from `factories.ts`, video + text variants) to `pack.scoring.run` and asserts the run COMPLETES and the returned `PredictionResult` is STRUCTURALLY valid (all required keys present, `overall_score` finite ∈ [0,100], `engine_version === "3.20.0"`) — NOT exact values. Plus a static check that the dispatch surface holds zero scoring logic.

Purpose: PACK-04 read through the D-02/D-03 lens — the byte-identical golden-master rig is SUPERSEDED. This is cheap insurance the refactor didn't break Socials (crashes, dropped fields, wiring errors), NOT a brittle exact-value harness. Do NOT build a golden-master/byte-equality fixture rig.
Output: `src/lib/engine/__tests__/pack-seam-smoke.test.ts` (BLOCKING — the phase cannot pass with this red).
</objective>

<artifacts_this_phase_produces>
This plan creates: `src/lib/engine/__tests__/pack-seam-smoke.test.ts` (the D-03 gate).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-engine-pack-seam/01-CONTEXT.md
@.planning/phases/01-engine-pack-seam/01-RESEARCH.md
@.planning/phases/01-engine-pack-seam/01-PATTERNS.md
@.planning/phases/01-engine-pack-seam/01-VALIDATION.md
@src/lib/engine/__tests__/audience-regression-gate.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the BLOCKING pack-seam smoke + structural gate</name>
  <files>src/lib/engine/__tests__/pack-seam-smoke.test.ts</files>
  <read_first>
    - .planning/phases/01-engine-pack-seam/01-PATTERNS.md §"pack-seam-smoke.test.ts" (the BLOCKING-gate structure; the static no-import regex idiom from explore-runner.test.ts:300-319 — strip comments first; what the D-03 smoke asserts)
    - .planning/phases/01-engine-pack-seam/01-RESEARCH.md §"What the D-03 smoke asserts" + §"Validation Architecture"
    - src/lib/engine/__tests__/audience-regression-gate.test.ts (mirror the BLOCKING header + structure)
    - src/lib/engine/__tests__/factories.ts:260 (makePipelineResult — confirm it covers both video and text/url foldOutcome/personaBehavioralAggregate shapes; RESEARCH Wave 0 gap)
  </read_first>
  <behavior>
    - `pack.scoring.run(makePipelineResult())` resolves WITHOUT throwing (assert for a video fixture AND a text/url fixture variant).
    - The returned `PredictionResult` has ALL required keys present (assert KEYS, not values): `overall_score`, `confidence`, `confidence_label`, `behavioral_predictions`, `factors`, `signal_availability`, `engine_version`, `input_mode`.
    - `overall_score` is a finite number in `[0, 100]` (the sane band).
    - `engine_version === "3.20.0"`.
    - `resolvePack("socials")` returns a pack with all 7 spec fields + `run` + `scoring` present.
    - Static: `packs/index.ts` source (comments stripped) does NOT match `/aggregateScores/` — the dispatch surface holds zero scoring logic (PACK-01 structural proof).
  </behavior>
  <action>
    Create `src/lib/engine/__tests__/pack-seam-smoke.test.ts` modeled on `audience-regression-gate.test.ts` (self-contained BLOCKING anchor; header doc-comment naming PACK-01..04 and stating "The phase CANNOT pass with this red."). Import `resolvePack`/`SOCIALS_PACK` from `../packs`, `makePipelineResult` from `./factories`, `ENGINE_VERSION` from `../version`.

    Assertions (structural, NOT byte-identical — D-03):
    1. `const pack = resolvePack("socials");` then assert `pack.id === "socials"` and every DomainPack field is present (`pack.populations`, `grounding`, `stimulusTypes`, `reactionFrame`, `scoring`, `outputSchema`, `calibration`, and `typeof pack.run === "function"`, `typeof pack.scoring.run === "function"`).
    2. `await pack.scoring.run(makePipelineResult())` returns without throwing; repeat with a text/url-shaped fixture variant (override `input_mode`/payload via `makePipelineResult({...})`). If `makePipelineResult` does not cover a needed shape, extend the fixture via overrides ONLY (do not hand-roll a raw mock — RESEARCH §"Don't Hand-Roll").
    3. Assert all required keys present via `expect(Object.keys(result)).toEqual(expect.arrayContaining([...]))` (keys, not values).
    4. `expect(Number.isFinite(result.overall_score)).toBe(true)` and `0 <= overall_score <= 100`.
    5. `expect(result.engine_version).toBe("3.20.0")` and `expect(ENGINE_VERSION).toBe("3.20.0")`.
    6. Static no-scoring-logic check on the dispatcher: `fs.readFileSync` of `src/lib/engine/packs/index.ts`, strip block + line comments FIRST (`/\/\*[\s\S]*?\*\//g` then `/\/\/.*$/gm`), then `expect(code).not.toMatch(/aggregateScores/)` (mirror explore-runner.test.ts:316; strip comments so prose/token mentions don't trip the matcher).

    If the live `aggregateScores` makes network/LLM calls on a fixture, mock the LLM layer with `vi.mock` the way `pipeline.test.ts`/`route.test.ts` do (RESEARCH Assumption A3 — offline, deterministic; the gate is structural, not a live end-to-end run).
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/engine/__tests__/pack-seam-smoke.test.ts 2>&1 | grep -q "passed"</automated>
  </verify>
  <done>The BLOCKING smoke is GREEN: `pack.scoring.run` returns a structurally valid PredictionResult (keys present, `overall_score ∈ [0,100]`, `engine_version === "3.20.0"`) for video + text fixtures; the dispatcher static check passes.</done>
  <acceptance_criteria>
    - Smoke green via the vitest binary
    - Asserts keys (not values) + sane-band overall_score + engine_version
    - Static no-import check on packs/index.ts present and passing
    - Uses factories.ts fixtures (no hand-rolled PipelineResult)
    - No golden-master/byte-equality assertions (D-02)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only; offline fixtures + mocked LLM layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-RR | Tampering (regression) | the seam wrap output | mitigate | This gate IS the primary mitigation for the phase's refactor-regression risk: it proves Socials still completes + returns a structurally valid, sane-band result post-seam (D-03). |
| T-01-CP | Tampering | ENGINE_VERSION drift | mitigate | The gate asserts `engine_version === "3.20.0"` on the result AND `ENGINE_VERSION === "3.20.0"` — catches an accidental bump (Pitfall 2). |

No new external attack surface (test-only). The gate is the documented refactor-regression mitigation, not an external-threat control.
</threat_model>

<verification>
- `pack-seam-smoke.test.ts` GREEN (video + text variants).
- Required keys present; `overall_score ∈ [0,100]`; `engine_version === "3.20.0"`.
- Dispatcher static no-scoring-import check passes.
</verification>

<success_criteria>
- The D-03 light smoke + structural gate exists and is green (PACK-04, relaxed per D-02/D-03).
- No byte-identical/golden-master rig (explicitly avoided).
- The gate BLOCKS the phase if Socials regresses.
</success_criteria>

<output>
Create `.planning/phases/01-engine-pack-seam/01-04-SUMMARY.md` when done.
</output>
