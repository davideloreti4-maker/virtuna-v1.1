---
phase: 01-engine-pack-seam
plan: 05
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - src/app/api/analyze/route.ts
autonomous: true
requirements: [PACK-01]
must_haves:
  truths:
    - "Both route.ts call sites (JSON + SSE) run orchestration via pack.run and scoring via pack.scoring.run"
    - "route.ts imports neither aggregateScores nor runPredictionPipeline directly — only resolvePack"
    - "The SSE branch preserves its aggregateMs timing + onStageEvent wiring; behaviour unchanged"
  artifacts:
    - path: "src/app/api/analyze/route.ts"
      provides: "Pack-dispatched run path (JSON + SSE branches)"
  key_links:
    - from: "src/app/api/analyze/route.ts"
      to: "src/lib/engine/packs (resolvePack)"
      via: "pack.run + pack.scoring.run dispatch"
      pattern: "pack\\.scoring\\.run|resolvePack"
---

<objective>
Route the production analyze endpoint's TWO call sites — the JSON branch (route.ts:790 + 809) and the SSE branch (route.ts:1000 + 1027) — through the pack. Replace the direct `runPredictionPipeline(...)` + `aggregateScores(...)` two-call sequence with `pack.run(...)` + `pack.scoring.run(...)`, and remove the now-dead direct imports (route.ts:9 runPredictionPipeline, route.ts:10 aggregateScores). Behaviour is unchanged — the pack references are identity wraps of the same functions.

Purpose: PACK-01 — "no socials-specific logic on the core run path" for the production route. After this, route.ts reaches scoring only via `pack.scoring.run` (the socials `overall_score` math is behind the seam).
Output: `src/app/api/analyze/route.ts` (both branches dispatched via the pack).
</objective>

<artifacts_this_phase_produces>
This plan modifies (no new symbols): `src/app/api/analyze/route.ts` — 2 call sites rewired to `pack.run` + `pack.scoring.run`; 2 direct imports removed.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-engine-pack-seam/01-RESEARCH.md
@.planning/phases/01-engine-pack-seam/01-PATTERNS.md
@src/lib/engine/packs/index.ts
@src/app/api/analyze/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch the JSON branch through the pack</name>
  <files>src/app/api/analyze/route.ts</files>
  <read_first>
    - .planning/phases/01-engine-pack-seam/01-RESEARCH.md §"Seam Cut Map" (call site #1 + #2) + §"Code Examples" (route.ts JSON dispatch after seam)
    - .planning/phases/01-engine-pack-seam/01-PATTERNS.md §"4 call sites → pack dispatch" (per-site edits table)
    - src/app/api/analyze/route.ts:9-10 (imports), :787-812 (JSON branch two-call)
  </read_first>
  <action>
    In the JSON branch (`if (wantsJSON)`, ~route.ts:787): add `const pack = resolvePack("socials");` (import `resolvePack` from `@/lib/engine/packs`). Change `pipelineResult = await runPredictionPipeline(validated, {...})` (line 790) to `pipelineResult = await pack.run(validated, {...})` — keep the SAME opts object verbatim (`requestId`, `bypassCache`, `userId: user.id`, `audience: activeAudience`). Change `const result = await aggregateScores(pipelineResult, undefined)` (line 809) to `const result = await pack.scoring.run(pipelineResult, undefined)`. Preserve the surrounding try/catch (orphan cleanup) and the `tAgg`/`aggregateMs` timing exactly. Do NOT alter behaviour — these are identity wraps.
  </action>
  <verify>
    <automated>node ./node_modules/typescript/bin/tsc --noEmit -p tsconfig.json 2>&1 | grep -q "route.ts" && exit 1 || exit 0</automated>
  </verify>
  <done>The JSON branch calls `pack.run` + `pack.scoring.run`; opts + timing preserved; tsc clean for route.ts.</done>
  <acceptance_criteria>
    - JSON branch uses pack.run + pack.scoring.run
    - opts object unchanged
    - tsc clean
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Dispatch the SSE branch + remove dead imports</name>
  <files>src/app/api/analyze/route.ts</files>
  <read_first>
    - src/app/api/analyze/route.ts:1000 (SSE runPredictionPipeline), :1023-1031 (SSE aggregateScores with onStageEvent + aggregateMs)
    - .planning/phases/01-engine-pack-seam/01-RESEARCH.md §"Seam Cut Map" (call site #2 — preserve aggregateMs timing; keep two entrypoints, Open Q2)
  </read_first>
  <action>
    In the SSE branch (~route.ts:1000): add `const pack = resolvePack("socials");` (reuse the Task-1 import). Change `runPredictionPipeline(validated, {...})` (line 1000) to `pack.run(validated, {...})` keeping the opts verbatim. Change the `aggregateScores(pipelineResult, (event: StageEvent) => { send("stage", event); })` call (lines 1027-1030) to `pack.scoring.run(pipelineResult, (event: StageEvent) => { send("stage", event); })` — preserve the `tAgg`/`aggregateMs` timing block (route.ts:1023-1031) exactly (RESEARCH Open Q2: two entrypoints preserve the route's inter-call timing). Then REMOVE the now-dead direct imports: `runPredictionPipeline` (route.ts:9) and `aggregateScores` (route.ts:10). Leave comment mentions (lines 327, 396, 1023) untouched — only the import statements and live call expressions change. Confirm no other live `runPredictionPipeline(` / `aggregateScores(` call expression remains in route.ts.
  </action>
  <verify>
    <automated>! grep -E "^import.*(aggregateScores|runPredictionPipeline)" src/app/api/analyze/route.ts && grep -q "pack.scoring.run" src/app/api/analyze/route.ts && node ./node_modules/vitest/vitest.mjs run src/lib/engine/__tests__/pack-seam-smoke.test.ts src/app/api/analyze/__tests__ 2>&1 | grep -q "passed"</automated>
  </verify>
  <done>The SSE branch calls `pack.run` + `pack.scoring.run` with timing preserved; route.ts has no direct `aggregateScores`/`runPredictionPipeline` import; the route tests + pack-seam smoke are green.</done>
  <acceptance_criteria>
    - SSE branch uses pack.run + pack.scoring.run, aggregateMs preserved
    - No `^import` of aggregateScores or runPredictionPipeline in route.ts
    - route __tests__ + pack-seam smoke green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/analyze (existing) | Unchanged — same zod `AnalysisInputSchema` boundary, same `user.id` auth; only the internal two-call indirection changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-RR | Tampering (regression) | route two-call → pack dispatch | mitigate | Identity wraps (`pack.run`=runPredictionPipeline, `pack.scoring.run`=aggregateScores); opts/timing preserved verbatim. The route `__tests__` + the D-03 smoke (Plan 04) guard against wiring regressions. |
| T-01-ID | Information Disclosure | request auth / SSRF re-host block | accept | This plan touches only the two-call dispatch in route.ts; `user.id` auth and the pipeline's tiktok re-host token handling are untouched (RESEARCH §"Security Domain"). |

No new external attack surface — the endpoint's input/auth boundary is unchanged.
</threat_model>

<verification>
- Both branches dispatch via `pack.run` + `pack.scoring.run`.
- `route.ts` has no direct `aggregateScores`/`runPredictionPipeline` import.
- route `__tests__` + pack-seam smoke green; tsc clean.
</verification>

<success_criteria>
- The production run path holds no direct socials-scoring import (PACK-01).
- SSE timing/eventing behaviour unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-engine-pack-seam/01-05-SUMMARY.md` when done.
</output>
