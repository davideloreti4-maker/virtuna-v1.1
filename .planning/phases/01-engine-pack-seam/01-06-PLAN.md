---
phase: 01-engine-pack-seam
plan: 06
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - src/lib/engine/corpus/eval-runner.ts
  - src/lib/engine/learning/predict.ts
autonomous: true
requirements: [PACK-01]
must_haves:
  truths:
    - "Both harness call sites (eval-runner, learning/predict) run orchestration via pack.run and scoring via pack.scoring.run"
    - "Neither harness imports aggregateScores directly; eval-runner/predict keep their ENGINE_VERSION import"
    - "The behavioralSource option threading in eval-runner is preserved verbatim"
  artifacts:
    - path: "src/lib/engine/corpus/eval-runner.ts"
      provides: "Pack-dispatched eval batch path"
    - path: "src/lib/engine/learning/predict.ts"
      provides: "Pack-dispatched learning/predict path"
  key_links:
    - from: "src/lib/engine/corpus/eval-runner.ts, src/lib/engine/learning/predict.ts"
      to: "src/lib/engine/packs (resolvePack)"
      via: "pack.run + pack.scoring.run dispatch"
      pattern: "pack\\.scoring\\.run|resolvePack"
---

<objective>
Route the two non-route call sites — `corpus/eval-runner.ts` (line 123 + 127) and `learning/predict.ts` (line 70 + 71) — through the pack, mirroring the production route. Replace `runPredictionPipeline(...)` + `aggregateScores(...)` with `pack.run(...)` + `pack.scoring.run(...)`, drop the direct `aggregateScores` import while KEEPING the `ENGINE_VERSION` import (both files import `aggregateScores, ENGINE_VERSION` from the aggregator — split the import, keep ENGINE_VERSION).

Purpose: PACK-01 — all 4 call sites must route through `pack[mode]` (RESEARCH Pitfall 1: if only route.ts is migrated, these harnesses still import `aggregateScores` directly → "no socials logic on core" violated for those entrypoints). This plan closes sites #3 and #4.
Output: `corpus/eval-runner.ts` + `learning/predict.ts` dispatched via the pack.
</objective>

<artifacts_this_phase_produces>
This plan modifies (no new symbols): `src/lib/engine/corpus/eval-runner.ts` + `src/lib/engine/learning/predict.ts` — call sites #3 and #4 rewired to `pack.run` + `pack.scoring.run`; direct `aggregateScores` imports removed (ENGINE_VERSION import retained).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-engine-pack-seam/01-RESEARCH.md
@.planning/phases/01-engine-pack-seam/01-PATTERNS.md
@src/lib/engine/packs/index.ts
@src/lib/engine/corpus/eval-runner.ts
@src/lib/engine/learning/predict.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch eval-runner through the pack (preserve behavioralSource)</name>
  <files>src/lib/engine/corpus/eval-runner.ts</files>
  <read_first>
    - .planning/phases/01-engine-pack-seam/01-RESEARCH.md §"Seam Cut Map" (call site #3 — `pack.scoring.run(pr, undefined, {behavioralSource})`)
    - src/lib/engine/corpus/eval-runner.ts:4-5 (imports), :123-131 (two-call with the conditional behavioralSource option)
  </read_first>
  <action>
    Import `resolvePack` from `@/lib/engine/packs`. Add `const pack = resolvePack("socials");` before the batch loop's call site. Change `const pipelineResult = await runPredictionPipeline(input)` (line 123) to `await pack.run(input)`. Change `const prediction = await aggregateScores(pipelineResult, undefined, opts.behavioralSource ? { behavioralSource: opts.behavioralSource } : undefined)` (lines 127-131) to `await pack.scoring.run(pipelineResult, undefined, opts.behavioralSource ? { behavioralSource: opts.behavioralSource } : undefined)` — preserve the conditional EXACTLY (it avoids passing `{ behavioralSource: undefined }`; behaviour-identical, RESEARCH note). Edit the import at line 5 (`import { aggregateScores, ENGINE_VERSION } from "@/lib/engine/aggregator";`): REMOVE `aggregateScores`, KEEP `ENGINE_VERSION` (`import { ENGINE_VERSION } from "@/lib/engine/aggregator";`). Also remove the now-dead `runPredictionPipeline` import (line 4) since it is reached via `pack.run`.
  </action>
  <verify>
    <automated>! grep -E "^import.*aggregateScores" src/lib/engine/corpus/eval-runner.ts && grep -q "ENGINE_VERSION" src/lib/engine/corpus/eval-runner.ts && grep -q "pack.scoring.run" src/lib/engine/corpus/eval-runner.ts && node ./node_modules/typescript/bin/tsc --noEmit -p tsconfig.json 2>&1 | grep -q "eval-runner.ts" && exit 1 || exit 0</automated>
  </verify>
  <done>eval-runner dispatches via `pack.run` + `pack.scoring.run`; the behavioralSource conditional is preserved; `aggregateScores` import removed, `ENGINE_VERSION` import retained; tsc clean.</done>
  <acceptance_criteria>
    - pack.run + pack.scoring.run used; behavioralSource conditional verbatim
    - no `^import` of aggregateScores; ENGINE_VERSION import retained
    - tsc clean
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Dispatch learning/predict through the pack</name>
  <files>src/lib/engine/learning/predict.ts</files>
  <read_first>
    - .planning/phases/01-engine-pack-seam/01-RESEARCH.md §"Seam Cut Map" (call site #4 — `pack.scoring.run(pr)`)
    - src/lib/engine/learning/predict.ts:14-15 (imports), :70-71 (two-call)
  </read_first>
  <action>
    Import `resolvePack` from `@/lib/engine/packs` (relative `../packs`). Add `const pack = resolvePack("socials");` before the call. Change `const pipelineResult = await runPredictionPipeline(input)` (line 70) to `await pack.run(input)` and `const prediction = await aggregateScores(pipelineResult)` (line 71) to `await pack.scoring.run(pipelineResult)`. Edit the import at line 15 (`import { aggregateScores, ENGINE_VERSION } from "../aggregator";`): REMOVE `aggregateScores`, KEEP `ENGINE_VERSION`. Remove the now-dead `runPredictionPipeline` import (line 14). Leave the doc-comment references (lines 5-12) untouched.
  </action>
  <verify>
    <automated>! grep -E "^import.*aggregateScores" src/lib/engine/learning/predict.ts && grep -q "ENGINE_VERSION" src/lib/engine/learning/predict.ts && grep -q "pack.scoring.run" src/lib/engine/learning/predict.ts && node ./node_modules/vitest/vitest.mjs run src/lib/engine 2>&1 | grep -q "passed"</automated>
  </verify>
  <done>learning/predict dispatches via `pack.run` + `pack.scoring.run`; `aggregateScores` import removed, `ENGINE_VERSION` retained; the full engine suite (incl. pack-seam smoke + regression gate) is green.</done>
  <acceptance_criteria>
    - pack.run + pack.scoring.run used
    - no `^import` of aggregateScores; ENGINE_VERSION import retained
    - full `src/lib/engine` suite green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal batch/learning harnesses; no external input, endpoint, or network change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-RR | Tampering (regression) | harness two-call → pack dispatch | mitigate | Identity wraps; the behavioralSource conditional preserved verbatim. The full `src/lib/engine` suite (Task 2 verify) + the D-03 smoke guard against wiring regressions across all 4 call sites (Pitfall 1). |
| T-01-CP | Tampering | ENGINE_VERSION import retained for cache keying | accept | `ENGINE_VERSION` import is deliberately retained (both harnesses use it for cache keys); only `aggregateScores` is removed. |

No new external attack surface — internal harness indirection only.
</threat_model>

<verification>
- Both harnesses dispatch via `pack.run` + `pack.scoring.run`.
- Neither imports `aggregateScores`; both retain `ENGINE_VERSION`.
- Full `src/lib/engine` suite green (closes Pitfall 1 — all 4 call sites migrated).
</verification>

<success_criteria>
- All 4 call sites route through `pack[mode]` (PACK-01 complete across route + harnesses).
- behavioralSource option behaviour preserved.
</success_criteria>

<output>
Create `.planning/phases/01-engine-pack-seam/01-06-SUMMARY.md` when done.
</output>
</content>
</invoke>
