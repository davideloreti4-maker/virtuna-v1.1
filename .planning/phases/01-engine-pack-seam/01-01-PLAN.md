---
phase: 01-engine-pack-seam
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package-lock.json
autonomous: true
requirements: [PACK-04]
must_haves:
  truths:
    - "node_modules is present; vitest runs real tests (not fake PASS(0)/FAIL(0))"
    - "The pre-seam engine suite is green — a baseline the refactor is measured against"
    - "ENGINE_VERSION is confirmed pinned at 3.20.0"
  artifacts:
    - path: "node_modules"
      provides: "Installed deps (typescript, vitest, zod) restored from the existing lockfile"
    - path: "package-lock.json"
      provides: "Unchanged lockfile (restore, not resolve-drift)"
  key_links: []
---

<objective>
Wave 0 enabler. This worktree has NO `node_modules` — nothing (tsc, vitest) runs until deps are restored. Install from the existing vetted lockfile, then capture a GREEN baseline of the engine test suite so the rest of the phase has a known-good reference. No source code changes.

Purpose: Every downstream task's `<verify>` depends on a working test runner. Pitfall 3 (RESEARCH §"Common Pitfalls"): `npm test` / `npx vitest` emit fake PASS(0)/FAIL(0) when `node_modules` is absent — a red suite looks green. This plan removes that trap.
Output: Restored `node_modules`; a recorded baseline (suite green + ENGINE_VERSION === "3.20.0") + measured runtime for VALIDATION.md.
</objective>

<artifacts_this_phase_produces>
Phase-wide symbol inventory (created across plans 01–06; this plan creates none of the source symbols, it enables them):
- `DomainPack` interface + spec types (`PopulationsSpec`, `GroundingSpec`, `ReactionFrameSpec`, `CalibrationSpec`, `OutputSchemaSpec`, `DomainPackScoring`, `StimulusType`) — `src/lib/engine/domain-pack.ts` (Plan 02)
- `SOCIALS_PACK: DomainPack` const — `src/lib/engine/packs/socials.ts` (Plan 03)
- `resolvePack(mode): DomainPack` dispatch fn — `src/lib/engine/packs/index.ts` (Plan 03)
- `pack-seam-smoke.test.ts` (D-03 BLOCKING gate) — `src/lib/engine/__tests__/pack-seam-smoke.test.ts` (Plan 04)
- 4 call sites rewired to `pack.run(...)` + `pack.scoring.run(...)` (Plans 05–06)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engine-pack-seam/01-CONTEXT.md
@.planning/phases/01-engine-pack-seam/01-RESEARCH.md
@.planning/phases/01-engine-pack-seam/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore node_modules from the existing lockfile</name>
  <files>package-lock.json (unchanged — restore only)</files>
  <read_first>
    - .planning/phases/01-engine-pack-seam/01-RESEARCH.md §"Environment Availability" + §"Package Legitimacy Audit" (N/A — no new packages; restoring the on-`main`-vetted lockfile)
    - CLAUDE.md (root) §"Setup" — `git config core.hooksPath .githooks`
  </read_first>
  <action>
    Run `npm install` at the worktree root to restore `node_modules` from the existing `package-lock.json`. This adds NO new packages (RESEARCH Package Legitimacy Audit = N/A; deps already vetted on `main`); it restores the lockfile-pinned versions (typescript ^5, vitest ^4.0.18, zod ^4.3.6). Do NOT edit `package.json` or upgrade anything. After install, verify `git status` shows `package-lock.json` unchanged (a clean restore, not a resolution drift) — if the lockfile changed, stop and report.
  </action>
  <verify>
    <automated>test -d node_modules/vitest && node ./node_modules/vitest/vitest.mjs run src/lib/engine/__tests__/version.test.ts 2>&1 | grep -E "Test Files|Tests " | grep -vq "Tests 0"</automated>
  </verify>
  <done>`node_modules/` exists; `node ./node_modules/vitest/vitest.mjs run src/lib/engine/__tests__/version.test.ts` reports a non-zero test count (real run, not fake PASS(0)); `package-lock.json` is unchanged in `git status`.</done>
  <acceptance_criteria>
    - `node_modules/vitest/vitest.mjs` resolves
    - version.test.ts runs and reports > 0 tests
    - package-lock.json byte-unchanged
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Capture the green pre-seam baseline + record runtime</name>
  <files>.planning/phases/01-engine-pack-seam/01-VALIDATION.md (record runtime only)</files>
  <read_first>
    - src/lib/engine/__tests__/version.test.ts (asserts ENGINE_VERSION === "3.20.0")
    - src/lib/engine/__tests__/audience-regression-gate.test.ts (the BLOCKING regression-gate idiom this phase mirrors)
  </read_first>
  <action>
    Run the engine test suite via the CORRECT runner — `node ./node_modules/vitest/vitest.mjs run src/lib/engine` — and confirm it is GREEN before any refactor. This is the reference the seam is measured against (D-03: cheap insurance the refactor didn't break Socials). Confirm `version.test.ts` + `audience-regression-gate.test.ts` both assert `ENGINE_VERSION === "3.20.0"` and pass. Record the measured wall-clock runtime of the engine suite into 01-VALIDATION.md "Test Infrastructure" (Estimated runtime) and "Sampling Rate" (Max feedback latency). Do NOT use `npm test` / `npx vitest` (Pitfall 3).
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/engine/__tests__/version.test.ts src/lib/engine/__tests__/audience-regression-gate.test.ts 2>&1 | grep -q "passed"</automated>
  </verify>
  <done>The engine suite runs green via the vitest binary; ENGINE_VERSION is verified `3.20.0`; runtime recorded in 01-VALIDATION.md.</done>
  <acceptance_criteria>
    - `src/lib/engine` suite is green on the unmodified tree
    - ENGINE_VERSION assertion passes in both gate tests
    - VALIDATION.md runtime fields populated
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supply chain (npm registry → lockfile) | The only boundary this plan touches; install of already-pinned, already-vetted deps |

This phase introduces ZERO new external attack surface (no new inputs, endpoints, auth, network calls, or schema). Per the security note: the relevant risk is a refactor regression, not an external threat. This plan's only risk is supply-chain drift on install.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install` (lockfile restore) | accept | No NEW packages (RESEARCH audit = N/A); restores the `main`-vetted `package-lock.json`. Task 1 asserts the lockfile is byte-unchanged after install — drift → stop + report. |
| T-01-CP | Tampering | `prediction_cache` keyed on `ENGINE_VERSION` | mitigate | Baseline confirms `ENGINE_VERSION === "3.20.0"`; the phase keeps it pinned so cache rows stay valid (Pitfall 2). |
</threat_model>

<verification>
- `node_modules/` restored; `package-lock.json` unchanged.
- `node ./node_modules/vitest/vitest.mjs run src/lib/engine` is green.
- ENGINE_VERSION confirmed `3.20.0`.
</verification>

<success_criteria>
- The vitest binary runs real tests (non-zero counts).
- The pre-seam engine suite is green — the refactor baseline.
- VALIDATION.md runtime fields populated.
</success_criteria>

<output>
Create `.planning/phases/01-engine-pack-seam/01-01-SUMMARY.md` when done.
</output>
