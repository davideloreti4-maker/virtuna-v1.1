---
phase: 01-strip-to-senses
plan: 06
type: execute
wave: 5
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05]
files_modified:
  - src/components/app/simulation/insights-section.tsx
  - src/components/app/simulation/__tests__/insights-section.test.tsx
  - src/lib/engine/version.ts
autonomous: false
requirements: [R5, R6, R8, R9, R12]
must_haves:
  truths:
    - "insights-section hides (renders nothing) when counterfactuals suggestions are empty/null — no FALLBACK_ITEM fake advice (D4.1)"
    - "ENGINE_VERSION bumped to 3.1.0 so stale cached results (with old percentile labels / fabricated engagement) are invalidated"
    - "Post-strip pre/post score delta measured + documented (D3.2); E2E latency under 300s (R6); same-video determinism holds (R8)"
    - "Remix decode/adapt path intact post-strip (R12)"
    - "Full suite + build green"
  artifacts:
    - path: "src/lib/engine/version.ts"
      provides: "ENGINE_VERSION = 3.1.0 (cache invalidation)"
      contains: "3.1.0"
    - path: "src/components/app/simulation/insights-section.tsx"
      provides: "no FALLBACK_ITEM fake advice — hidden on empty/null"
  key_links:
    - from: "src/lib/engine/version.ts"
      to: "cache/prediction-cache.ts"
      via: "ENGINE_VERSION key bump invalidates L1+L2"
      pattern: "3.1.0"
---

<objective>
Steps 8-10 (final) of the validated order of operations: apply the one real UI edit (D4.1 — hide `FALLBACK_ITEM` instead of surfacing fake fallback advice), bump the cache key so stale outputs are invalidated, then run the BLOCKING phase gates — pre/post score delta (D3.2), latency (R6), determinism (R8), remix intact (R12), full suite + build.

Purpose: This plan ships the user-facing honesty edit (no fabricated fallback advice), invalidates the now-stale cache (outputs changed: no percentile labels, no jitter engagement — reverify #13), and PROVES the strip is correct against the Plan 01 pre-strip baseline. The phase cannot pass without these gates.
Output: FALLBACK_ITEM hidden, ENGINE_VERSION=3.1.0, a documented score delta, a sub-cap latency number, a passing remix smoke, and a green full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ENGINE-MAP.md
@.planning/phases/01-strip-to-senses/01-RESEARCH.md
@.planning/phases/01-strip-to-senses/01-01-SUMMARY.md
@.planning/phases/01-strip-to-senses/01-04-SUMMARY.md
@.planning/phases/01-strip-to-senses/01-05-SUMMARY.md

<interfaces>
<!-- Anchors verified 2026-06-04. RE-GREP SYMBOLS, not line numbers. -->

insights-section.tsx:
- FALLBACK_ITEM (~44) — the fake "advice" item
- `const items = suggestions.length === 0 ? [FALLBACK_ITEM] : suggestions` (~61) — TARGET: empty -> render nothing (no fallback). The panel already null-guards `result.counterfactuals` (results-panel.tsx:206), so this only fires on EMPTY suggestions; D4.1 wants no fabricated fallback advice shown either way.

version.ts:
- `export const ENGINE_VERSION = "3.0.0";` (~9) -> "3.1.0". Consumed by cache/prediction-cache.ts (L1 key `${contentHash}::${ENGINE_VERSION}::${userId}` + L2 `.eq("engine_version", ENGINE_VERSION)`). One-line bump auto-invalidates L1+L2 (RESEARCH Claim 11).

measure harness (extended in Plan 01):
- scripts/measure-pipeline.ts now logs OVERALL_SCORE. Run twice on the SAME fixed video used for the Plan 01 baseline; diff overall_score (D3.2); read total latency (R6); confirm determinism (R8).

Remix tests (R12): src/app/api/remix/__tests__/* + src/app/api/analyze/__tests__/decode-route.test.ts + scripts/smoke-tiktok-pipeline.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hide FALLBACK_ITEM (no fake fallback advice) + cache bump to 3.1.0</name>
  <files>src/components/app/simulation/insights-section.tsx, src/components/app/simulation/__tests__/insights-section.test.tsx, src/lib/engine/version.ts</files>
  <read_first>
    - src/components/app/simulation/insights-section.tsx (re-grep FALLBACK_ITEM, `suggestions.length === 0`)
    - src/components/app/simulation/results-panel.tsx (confirm `{result.counterfactuals && (<SuggestionsSection .../>)}` already null-guards)
    - src/lib/engine/version.ts (re-grep ENGINE_VERSION)
    - .planning/phases/01-strip-to-senses/01-RESEARCH.md Claim 6 (FALLBACK_ITEM is the only real UI edit) + Claim 11 (cache bump one-liner) + planning_context user_decisions #6 (null-render, no design contract)
  </read_first>
  <behavior>
    - When counterfactuals suggestions are empty (or null), insights-section renders nothing — FALLBACK_ITEM is never shown as if it were real advice (D4.1)
    - When suggestions exist, they render unchanged
    - ENGINE_VERSION === "3.1.0"
  </behavior>
  <action>
    In insights-section.tsx, change the empty/zero-suggestions branch so it renders nothing (return null or an empty fragment) instead of `[FALLBACK_ITEM]`. Remove FALLBACK_ITEM if it becomes unused. KEEP the component itself (P3 wires Apollo rewrites into it). In version.ts, bump ENGINE_VERSION from "3.0.0" to "3.1.0" (outputs changed — no more percentile labels / fabricated engagement; reverify #13). Add/extend an insights-section test asserting empty suggestions -> nothing rendered, and non-empty -> items rendered.
  </action>
  <verify>
    <automated>grep -q '"3.1.0"' src/lib/engine/version.ts && npx vitest run src/components/app/simulation/__tests__/insights-section.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"3.1.0"' src/lib/engine/version.ts` matches (cache bumped)
    - insights-section renders nothing on empty/null suggestions (no FALLBACK_ITEM surfaced) — asserted by test
    - `npx vitest run src/components/app/simulation/__tests__/insights-section.test.tsx` exits 0
  </acceptance_criteria>
  <done>FALLBACK_ITEM no longer shown as advice; ENGINE_VERSION=3.1.0; insights-section test green.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Full suite + build + remix regression (R12) green post-strip</name>
  <files>(verification only — full build + test suite + remix tests)</files>
  <read_first>
    - .planning/phases/01-strip-to-senses/01-RESEARCH.md Claim 10 (remix branches before the blend; cannot structurally regress — run existing remix tests) + Validation Architecture (Req->Test map)
    - src/app/api/remix/__tests__/ + src/app/api/analyze/__tests__/decode-route.test.ts
  </read_first>
  <action>
    Run the FULL test suite + build to prove the whole strip is green: `npm run build` then `npm test`. Then run the remix regression set explicitly: the remix unit/integration tests + decode-route test. These prove R12 (remix decode/adapt path intact — it shares Omni only and imports no cut module). If any remix test fails, the strip regressed something shared — STOP and diagnose (expected: zero remix impact per RESEARCH Claim 10). Record the green result.
  </action>
  <verify>
    <automated>npm run build && npm test && npx vitest run src/app/api/remix src/app/api/analyze/__tests__/decode-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npm test` exits 0 (full suite, dormant excluded)
    - Remix tests (`src/app/api/remix` + decode-route.test.ts) exit 0 — R12 proven
  </acceptance_criteria>
  <done>Full suite + build green; remix path proven intact (R12).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] E2E gates — score delta (D3.2), latency (R6), determinism (R8), remix smoke (R12)</name>
  <files>(verification only — measure-pipeline + remix smoke; no files modified)</files>
  <action>Run the four E2E proof gates described below against the Plan 01 pre-strip baseline and record results in the SUMMARY. No source is modified.</action>
  <what-built>The E2E proof gates: pre/post score delta (D3.2), latency under cap (R6), determinism (R8), and a remix smoke. These require live DashScope + Supabase E2E runs (real API budget + interactive auth for the smoke), so they run as a blocking checkpoint that compares against the Plan 01 pre-strip baseline.</what-built>
  <how-to-verify>
    1. Score delta (D3.2) — run `npx tsx scripts/measure-pipeline.ts /path/to/fixed-video.mp4` (the SAME video used for the Plan 01 baseline). Read the logged OVERALL_SCORE. Diff against the Plan 01 baseline overall_score. EXPECT ~0 to a small UPWARD nudge (the trends 0.10 redistribution — RESEARCH Pitfall 6). Document the exact delta and label it the honesty correction. A large unexpected shift means investigate, do not ship silently.
    2. Latency (R6) — from that same run, read total wall-clock. MUST be < 300s; target <=90s. Record it (compare to the pre-strip baseline to show the cut's latency win).
    3. Determinism (R8) — run the harness a second time on the same video; OVERALL_SCORE must be byte-identical to run 1.
    4. Remix smoke (R12) — POST /api/analyze with mode:'remix' + a tiktok_url (exercises resolve-and-rehost -> Omni -> decode), then POST /api/remix/adapt with the decode result (or run scripts/smoke-tiktok-pipeline.ts). Confirm it completes without error.
    Record all four results in the SUMMARY: score delta (with direction + honesty-correction note), latency, determinism pass/fail, remix smoke pass/fail.
  </how-to-verify>
  <acceptance_criteria>
    - Pre/post score delta measured against the Plan 01 baseline and documented (D3.2) — direction noted, large shifts flagged
    - E2E total latency < 300s recorded (R6); compared to pre-strip baseline
    - Same-video-twice OVERALL_SCORE identical (R8)
    - Remix smoke completes end-to-end without error (R12)
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded score delta, post-strip latency, determinism result, and remix smoke result — or describe the blocker (e.g. unexpected score shift, latency over cap, remix failure).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cache key -> served result | ENGINE_VERSION gates which cached rows are served to clients |
| client -> /api/analyze (remix smoke) | untrusted tiktok_url + mode; Zod-validated upstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering / Information disclosure | stale cache serving fabricated output | mitigate | ENGINE_VERSION bump to 3.1.0 invalidates L1 + L2 (key includes ENGINE_VERSION) so no pre-strip row with old percentile labels / fabricated engagement is served post-deploy. Verify the bump lands before deploy. |
| T-06-02 | Information disclosure | FALLBACK_ITEM fake advice | mitigate | Hiding the fallback removes ungrounded "advice" from user-facing output — a fabrication-disclosure reduction. |
| T-06-03 | Spoofing/Elevation | remix smoke against live route | accept | Smoke uses the existing authed route + Zod validation; no new surface; verification-only. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan. If proposed, halt. |

No high-severity threats — the cache bump is itself the mitigation for stale-fabricated-output leakage; the UI edit reduces a fabrication surface; the E2E gates are read-only proofs.
</threat_model>

<verification>
- `grep -q '"3.1.0"' src/lib/engine/version.ts`
- insights-section test green (empty -> nothing rendered)
- `npm run build` + `npm test` green; remix tests green
- E2E (checkpoint): score delta documented, latency < 300s, determinism identical, remix smoke passes
</verification>

<success_criteria>
- R9: no fabricated fallback advice shown; cache invalidated so no stale fabrication serves
- R5: score + confidence render post-strip (suite-proven)
- R6: E2E under 300s (latency win documented)
- R8: same-video determinism holds
- R12: remix decode/adapt intact (tests + smoke)
- D3.2: pre/post score delta measured + documented as the honesty correction
</success_criteria>

<output>
Create `.planning/phases/01-strip-to-senses/01-06-SUMMARY.md` when done. MUST record: the pre/post score delta (vs Plan 01 baseline) with honesty-correction note, post-strip latency, determinism result, and remix smoke result. This is the phase-gate evidence.
</output>
