---
phase: 01-strip-to-senses
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - src/lib/engine/aggregator.ts
  - src/lib/engine/stage10-critique.ts
  - src/lib/engine/deepseek.ts
  - src/lib/engine/__tests__/aggregator.test.ts
  - src/lib/engine/__tests__/aggregator-anti-virality.test.ts
  - src/lib/engine/__tests__/stage10-critique.test.ts
  - src/lib/engine/__tests__/weighted-aggregator.test.ts
autonomous: true
requirements: [R5, R9]
must_haves:
  truths:
    - "SCORE_WEIGHT_KEYS = [behavioral, gemini] — dead keys (ml, rules, trends, audio, retrieval, platform_fit) removed"
    - "Scoring math reduces to behavioral*w.behavioral + gemini*w.gemini; selectWeights handles the 2-key path"
    - "stage10 vestigial flags.push strings removed + Check#4 audio/retrieval sub-conditions removed; confidence_adjustment KEPT (R5)"
    - "deepseek 'top X%' framing + *_percentile schema fields + percentile benchmark block removed; calibration-baseline.json KEPT (still feeds differentiators + duration)"
    - "score + confidence still render; build + touched tests green"
  artifacts:
    - path: "src/lib/engine/aggregator.ts"
      provides: "behavioral+gemini-only blend; dead keys + unreachable redistribution removed; confidence path intact"
      contains: "behavioral"
    - path: "src/lib/engine/deepseek.ts"
      provides: "reasoning intact, percentile-label framing removed (labels-only cut)"
  key_links:
    - from: "src/lib/engine/aggregator.ts"
      to: "result.confidence"
      via: "stage10 confidence_adjustment preserved"
      pattern: "confidence_adjustment"
    - from: "src/lib/engine/deepseek.ts"
      to: "calibration-baseline.json"
      via: "differentiators + duration still read (labels removed)"
      pattern: "calibration"
---

<objective>
Steps 3-5 of the validated order of operations: cut the dead keys from the aggregator blend (now safe because Plan 02/03 removed their call sites), scope-delete the stage10 vestigial flags (KEEPING confidence_adjustment), and remove deepseek's "top X%" percentile-label framing (labels only — KEEPING calibration-baseline.json which still feeds differentiators + duration).

Purpose: With the cut sources gone, the blend can be reduced to its only live signals — behavioral + gemini — so the math is honest (R9) while the score + confidence still render (R5). The deepseek cut removes the fabricated corpus-rank framing without touching reasoning.
Output: behavioral+gemini-only blend; stage10 flags-only deletion (confidence preserved); deepseek label-only cut; updated tests. The pre/post score delta this introduces is MEASURED in Plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ENGINE-MAP.md
@.planning/phases/01-strip-to-senses/01-RESEARCH.md
@.planning/phases/01-strip-to-senses/01-02-SUMMARY.md
@.planning/phases/01-strip-to-senses/01-03-SUMMARY.md

<interfaces>
<!-- Anchors verified 2026-06-04. RE-GREP SYMBOLS, not line numbers. -->

aggregator.ts:
- SCORE_WEIGHT_KEYS (~90) = ["behavioral","gemini","ml","rules","trends","audio","retrieval","platform_fit"] -> TARGET ["behavioral","gemini"]
- SCORE_WEIGHTS (~70-79) trim to behavioral + gemini
- selectWeights (~167-277) redistribution -> simplify to the 2-key path
- scoring sum (~942-962) -> behavioral_score*w.behavioral + ctaPenaltyApplied_gemini_score*w.gemini
- SignalAvailability dead flags (~803-852) -> remove ml/rules/trends/audio/retrieval/platform_fit flags
- platform_fit/audio/retrieval/ml/trends/rules reads (~847,930-934,959,1227) -> remove
- applyCtaPenalty (~142) — KEEP (modifies the LIVE gemini term; removing it would shift the live score — NOT an honesty correction; RESEARCH Open Q1 + Pitfall 7). D3.3's "scaffolding evaporates" does NOT apply to CTA penalty.
- stage10 critique (deriveCritique/applyCritiqueAdjustment) — KEEP the confidence path (Task 2 owns it)

stage10-critique.ts:
- flags.push(...) strings (~64,77,83,98) -> DELETE (vestigial — never persisted/rendered; reverify #14)
- Check #4 sub-conditions sa.audio !== true && "audio" / sa.retrieval !== true && "retrieval" (~109-110) -> REMOVE (those keys are gone)
- confidence_adjustment + clamp [-0.20,0] (~45) -> KEEP (LIVE input to result.confidence per D1.4/R5 — this session resolved stage10 FLAGS-ONLY)
- Check #1 (gemini vs behavioral gap) + Check #2 (score vs factors) -> KEEP

deepseek.ts:
- STABLE_SYSTEM_PROMPT step instructing "top X%" framing (~75) -> REMOVE the percentile-framing instruction
- output schema *_percentile string fields (~84-90: completion/share/comment/save_percentile) -> REMOVE these schema fields
- percentile benchmark block in buildDeepSeekUserMessage (~434-437) -> REMOVE
- loadCalibrationData / calibration-baseline.json (~314-335, read at ~320) -> KEEP (still feeds viral differentiators ~429-430 + duration sweet-spot ~432; RESEARCH Claim 5 / Pitfall 4)
- 5-step component-score derivation -> DO NOT TOUCH (becomes Apollo in P3)
- temp0+seed (~493-494) -> DO NOT TOUCH (R8)
- stable-system + volatile-user cache split -> PRESERVE
- NOTE: the persona-path *_percentile (wave3/aggregator.ts "high intent"/"moderate intent") is HONEST (WR-05) -> DO NOT TOUCH. A repo-wide _percentile grep will still match the persona path; scope assertions to deepseek.ts only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cut dead keys from the aggregator blend to behavioral + gemini only</name>
  <files>src/lib/engine/aggregator.ts</files>
  <read_first>
    - src/lib/engine/aggregator.ts (re-grep SCORE_WEIGHT_KEYS, SCORE_WEIGHTS, selectWeights, SignalAvailability, applyCtaPenalty, and the scoring-sum block)
    - .planning/phases/01-strip-to-senses/01-RESEARCH.md Claim 4 (blend separability + CTA penalty caveat) + Pitfall 6 (expect small upward nudge from trends 0.10 redistribution = honesty correction) + Pitfall 7 (do NOT remove CTA penalty)
  </read_first>
  <behavior>
    - SCORE_WEIGHT_KEYS === ["behavioral","gemini"]
    - The scoring sum references only behavioral_score and the CTA-penalty-applied gemini_score
    - selectWeights returns a valid 2-key distribution summing ~1.0
    - No reference to ml/rules/trends/audio/retrieval/platform_fit weight, availability flag, or score read remains in the blend math
    - applyCtaPenalty STILL applied to the gemini term (unchanged)
  </behavior>
  <action>
    Reduce SCORE_WEIGHT_KEYS to ["behavioral","gemini"] and trim SCORE_WEIGHTS accordingly. Simplify selectWeights to the 2-key redistribution path (delete the now-unreachable multi-source redistribution branches). Reduce the scoring sum to behavioral_score*w.behavioral + ctaPenaltyApplied_gemini_score*w.gemini. Remove the dead SignalAvailability flags (ml/rules/trends/audio/retrieval/platform_fit) and every read of those sources in the blend (platform_fit mean, ml score, rule_score, trend_score, audio_score, retrieval term). DELETE only now-unreachable redistribution/calibration scaffolding (D3.3) — but KEEP applyCtaPenalty (it modifies the live gemini term; removing it shifts the live score and is NOT an honesty correction per RESEARCH Pitfall 7/Open Q1). Keep the stage10 confidence path intact (Task 2 owns it). Do not touch optimal_post (kept honest per Claim 13).
  </action>
  <verify>
    <automated>grep -A1 "SCORE_WEIGHT_KEYS =" src/lib/engine/aggregator.ts; npx tsc --noEmit -p tsconfig.json && grep -q "applyCtaPenalty" src/lib/engine/aggregator.ts</automated>
  </verify>
  <acceptance_criteria>
    - SCORE_WEIGHT_KEYS is exactly ["behavioral", "gemini"] (inspect the array literal)
    - No active "ml"/"trends"/"audio"/"retrieval"/"platform_fit" key remains inside SCORE_WEIGHT_KEYS or SCORE_WEIGHTS (residual comment text acceptable)
    - grep -q "applyCtaPenalty" src/lib/engine/aggregator.ts matches (CTA penalty preserved)
    - npx tsc --noEmit -p tsconfig.json reports no NEW errors
  </acceptance_criteria>
  <done>Blend is behavioral+gemini only; CTA penalty preserved; dead keys + unreachable redistribution removed; compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scope-delete stage10 vestigial flags (KEEP confidence_adjustment)</name>
  <files>src/lib/engine/stage10-critique.ts</files>
  <read_first>
    - src/lib/engine/stage10-critique.ts (re-grep flags.push, confidence_adjustment, sa.audio, sa.retrieval)
    - .planning/phases/01-strip-to-senses/01-RESEARCH.md Claim 12 (flags vestigial; confidence_adjustment LIVE) + Pitfall 5 (do NOT kill the whole stage)
    - planning_context user_decisions #1 (stage10 -> FLAGS-ONLY; KEEP deriveCritique + confidence_adjustment)
  </read_first>
  <behavior>
    - stage10-critique.ts has NO flags.push(...) calls and emits no flags strings
    - confidence_adjustment + its [-0.20,0] clamp are UNCHANGED
    - Check #1 (gemini vs behavioral) + Check #2 (score vs factors) intact
    - Check #4's sa.audio/sa.retrieval sub-conditions removed (those keys no longer exist)
    - aggregator's applyCritiqueAdjustment still applies the confidence penalty
  </behavior>
  <action>
    Remove the vestigial flags.push(...) strings (never persisted, never rendered — reverify #14) and the flags array if it becomes unused, AND remove the Check #4 sub-conditions that reference the now-removed sa.audio / sa.retrieval availability keys. KEEP deriveCritique, the confidence_adjustment computation, and the [-0.20,0] clamp — it is a LIVE input to result.confidence (D1.4/R5; this session resolved stage10 as FLAGS-ONLY). KEEP Check #1 + Check #2. If removing flags changes deriveCritique's return shape, update the type so confidence_adjustment still flows; do NOT remove the adjustment.
  </action>
  <verify>
    <automated>grep -c "flags.push" src/lib/engine/stage10-critique.ts | grep -qx 0 && grep -q "confidence_adjustment" src/lib/engine/stage10-critique.ts && npx vitest run src/lib/engine/__tests__/stage10-critique.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "flags.push" src/lib/engine/stage10-critique.ts returns 0
    - grep -q "confidence_adjustment" src/lib/engine/stage10-critique.ts matches (confidence path preserved)
    - No sa.audio / sa.retrieval reference remains in stage10-critique.ts
    - npx vitest run src/lib/engine/__tests__/stage10-critique.test.ts exits 0
  </acceptance_criteria>
  <done>stage10 flags removed; confidence_adjustment + Checks #1/#2 preserved; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove deepseek 'top X%' percentile labels (KEEP calibration JSON) + update aggregator tests</name>
  <files>src/lib/engine/deepseek.ts, src/lib/engine/__tests__/aggregator.test.ts, src/lib/engine/__tests__/aggregator-anti-virality.test.ts, src/lib/engine/__tests__/weighted-aggregator.test.ts</files>
  <read_first>
    - src/lib/engine/deepseek.ts (re-grep percentile, top X, calibration, loadCalibrationData, buildDeepSeekUserMessage, and the differentiators/duration usage)
    - .planning/phases/01-strip-to-senses/01-RESEARCH.md Claim 5 (labels-only scope; calibration JSON ALSO feeds differentiators + duration — KEEP it) + Pitfall 4
    - planning_context user_decisions #3 (DEEPSEEK FRAMING ONLY; KEEP persona-path intent labels)
    - src/lib/engine/__tests__/aggregator.test.ts + aggregator-anti-virality.test.ts + weighted-aggregator.test.ts (re-grep dead-key + percentile assertions)
  </read_first>
  <behavior>
    - deepseek's STABLE_SYSTEM_PROMPT no longer instructs "top X%" framing
    - The deepseek output schema no longer declares completion/share/comment/save *_percentile string fields
    - buildDeepSeekUserMessage no longer injects the percentile benchmark block
    - calibration-baseline.json is STILL loaded and STILL feeds viral differentiators + duration sweet-spot
    - The 5-step component-score derivation, temp0+seed, and the cache split are UNCHANGED
    - The wave3 persona-path "intent" labels are UNTOUCHED
  </behavior>
  <action>
    In deepseek.ts: remove the "top X%" percentile-framing instruction from the stable system prompt, remove the four *_percentile schema field declarations, and remove the percentile-benchmark block from buildDeepSeekUserMessage. KEEP loadCalibrationData + the calibration-baseline.json read and the differentiators + duration-sweet-spot blocks it feeds (labels-only cut per D1.2; do NOT delete the JSON or the whole calibration load — RESEARCH Pitfall 4). Do NOT touch the persona-path "high intent"/"moderate intent" labels (wave3, honest per WR-05) or the 5-step reasoning or temp0+seed. Then update aggregator.test.ts / aggregator-anti-virality.test.ts / weighted-aggregator.test.ts: remove assertions on the now-removed dead blend keys + the deepseek percentile fields; assert the blend is behavioral+gemini and that score + confidence still derive. Keep the score/confidence-render assertions.
  </action>
  <verify>
    <automated>grep -c "_percentile" src/lib/engine/deepseek.ts | grep -qx 0 && grep -q "calibration" src/lib/engine/deepseek.ts && npx vitest run src/lib/engine/__tests__/aggregator.test.ts src/lib/engine/__tests__/aggregator-anti-virality.test.ts src/lib/engine/__tests__/weighted-aggregator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "_percentile" src/lib/engine/deepseek.ts returns 0 (deepseek's percentile schema fields gone; the persona-path labels in wave3 are a different file and untouched)
    - grep -q "calibration" src/lib/engine/deepseek.ts matches (calibration load preserved for differentiators + duration)
    - The three named aggregator test files pass (npx vitest run exits 0)
    - No change to the 5-step reasoning or temp0+seed in deepseek.ts
  </acceptance_criteria>
  <done>deepseek percentile labels removed (labels-only); calibration JSON kept; aggregator tests assert behavioral+gemini blend and pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine -> persisted score/confidence | the blend output is written to the prediction row + returned to client; no untrusted input enters here |
| deepseek -> LLM | system+user prompt assembled server-side; calibration JSON is a static local file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | reducing the blend to 2 keys | mitigate | The cut introduces a documented score shift (Pitfall 6 honesty correction) — Plan 06 measures + records the pre/post delta so no silent score change ships. No input guard is in the blend. |
| T-04-02 | Information disclosure | removing "top X%" framing | mitigate | Removes a FABRICATED corpus-rank claim from user-facing output — strictly reduces a false-precision disclosure. |
| T-04-03 | Tampering | keeping calibration-baseline.json | accept | Static local file feeding differentiators + duration; no user input; KEEP per D1.2 labels-only scope. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan. If proposed, halt (no Package Legitimacy audit in RESEARCH). |

No high-severity threats — blend simplification + label removal; no guard removed, no new input. The only behavior change (score delta) is gated by Plan 06's measure + cache bump.
</threat_model>

<verification>
- npx tsc --noEmit -p tsconfig.json clean
- SCORE_WEIGHT_KEYS == [behavioral, gemini]; applyCtaPenalty + confidence_adjustment preserved (grep)
- deepseek _percentile schema fields gone; calibration load kept (grep)
- stage10/aggregator/anti-virality/weighted tests green
</verification>

<success_criteria>
- R9: dead blend keys + fabricated percentile labels removed
- R5: score + confidence still derive and render (confidence_adjustment + CTA penalty preserved)
- Build + touched tests green; score-delta to be measured in Plan 06
</success_criteria>

<output>
Create `.planning/phases/01-strip-to-senses/01-04-SUMMARY.md` when done. Note the expected score-delta direction (small upward nudge from trends 0.10 redistribution per Pitfall 6) for Plan 06 to confirm.
</output>
