---
phase: 01-strip-to-senses
plan: 05
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03, 01-04]
files_modified:
  - src/lib/engine/_dormant/engine/ml.ts
  - src/lib/engine/_dormant/engine/audio-fingerprint.ts
  - src/lib/engine/_dormant/engine/trends.ts
  - src/lib/engine/_dormant/engine/fuzzy.ts
  - src/lib/engine/_dormant/engine/rules.ts
  - src/lib/engine/_dormant/engine/stage11-counterfactuals.ts
  - src/lib/engine/_dormant/engine/stage11-counterfactuals-prompts.ts
  - src/lib/engine/_dormant/engine/wave4/platform-fit.ts
  - src/lib/engine/_dormant/engine/wave4/platform-fit-prompts.ts
  - src/app/api/cron/retrain-ml/route.ts
  - vercel.json
autonomous: true
requirements: [R6, R9]
must_haves:
  truths:
    - "ml, audio-fingerprint, trends (+fuzzy), rules, stage11-counterfactuals (+prompts), wave4/platform-fit (+prompts) moved under src/lib/engine/_dormant/ with their tests"
    - "No active-tree file imports any moved module (grep proof); retrain-ml cron route dormanted + its vercel.json schedule removed"
    - "npm run build + npm test green (dormant glob excludes the moved tree)"
  artifacts:
    - path: "src/lib/engine/_dormant/engine/ml.ts"
      provides: "dormanted ml module (was aggregator-called every run, now removed)"
    - path: "vercel.json"
      provides: "cron list with retrain-ml schedule removed"
  key_links:
    - from: "active engine tree"
      to: "_dormant/ modules"
      via: "zero imports (grep proof)"
      pattern: "_dormant"
---

<objective>
Step 7 of the validated order of operations: now that every call site is removed (Plans 02-04) and the creator-rules.test.ts cross-import is fixed (Plan 01), physically move the dead modules + their tests into `src/lib/engine/_dormant/`. Handle the `retrain-ml` cron route (it imports ml.ts) by moving it to dormant AND removing its `vercel.json` schedule (confirmed configured: `0 3 * * 1`).

Purpose: The dormant move is reversible (holds Apollo-corpus seed material; M-restorable) and the tsconfig + vitest `**/_dormant/**` exclude already exists — so the moved tree neither compiles nor runs, removing dead machinery without losing the source (D2). Doing the move LAST among the cuts guarantees no live import is stranded (reverify #9/#10).
Output: the seven module groups (+ tests) relocated under `_dormant/`, the cron route dormanted, the vercel cron entry removed, and a grep proof that no active-tree import reaches into `_dormant/`. Build + full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ENGINE-MAP.md
@.planning/phases/01-strip-to-senses/01-RESEARCH.md
@.planning/phases/01-strip-to-senses/01-02-SUMMARY.md
@.planning/phases/01-strip-to-senses/01-03-SUMMARY.md
@.planning/phases/01-strip-to-senses/01-04-SUMMARY.md
@.planning/_archive/01-pre-apollo-strip-retrieval-trending/01-CONTEXT.md

<interfaces>
<!-- Anchors verified 2026-06-04. RE-GREP importers before each move. -->

Modules to move (source -> _dormant target; exact dir layout is Claude's discretion per D2.4 — keep a parallel engine/ subtree under _dormant/):
- src/lib/engine/ml.ts (+ __tests__/ml.test.ts)
- src/lib/engine/audio-fingerprint.ts
- src/lib/engine/trends.ts (+ __tests__/trends.test.ts) — trends imports ./fuzzy
- src/lib/engine/fuzzy.ts (+ __tests__/fuzzy.test.ts) — NO other active importer once trends moves
- src/lib/engine/rules.ts (+ __tests__/rules.test.ts) — DORMANT WHOLE MODULE (regex+semantic are inline branches of one function — NOT splittable; RESEARCH Claim 2; rule_score already weight 0)
- src/lib/engine/stage11-counterfactuals.ts + stage11-counterfactuals-prompts.ts (+ __tests__/stage11-counterfactuals.test.ts)
- src/lib/engine/wave4/platform-fit.ts + wave4/platform-fit-prompts.ts (+ wave4/__tests__/platform-fit*.test.ts — these test the platform-fit MODULE and travel with it)

NOTE: __tests__/aggregator-platform-fit.test.ts does NOT travel here. It imports selectWeights from the KEPT ../aggregator (not the platform-fit module) and asserts a platform_fit weight KEY — it is handled (deleted) in Plan 04 Task 3 when the key is removed. Do NOT look for it under wave4; it is gone before this plan runs.

Pre-move guards (already done by earlier plans — VERIFY by grep before moving):
- aggregator.ts no longer imports ml / stage11-counterfactuals (Plan 02)
- pipeline.ts no longer imports audio-fingerprint / trends / rules / wave4/platform-fit (Plan 03)
- analyze/route.ts no longer imports stage11-counterfactuals (Plan 02)
- creator-rules.test.ts no longer imports the stage11/platform-fit prompt files (Plan 01)
- NO active-tree aggregator test still mocks ../ml or ../stage11-counterfactuals (Plan 04 Task 3 removed every such mock from aggregator-cta-penalty/optimal-post/audio.test.ts) — so moving ml.ts + stage11-counterfactuals.ts strands no test mock.

Remaining live importer to handle HERE:
- src/app/api/cron/retrain-ml/route.ts imports ml.ts (trainModel/stratifiedSplit). Move the route under _dormant/ (or delete) AND remove its vercel.json cron entry { "path": "/api/cron/retrain-ml", "schedule": "0 3 * * 1" } — CONFIRMED present in vercel.json (assumption A3 resolved: a schedule IS configured).

tsconfig.json:38 + vitest.config.ts already exclude **/_dormant/** — NO config edit needed; verify the moved files land under the glob.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-move import-graph proof + dormant the no-live-importer modules (audio, trends, fuzzy, rules, stage11)</name>
  <files>src/lib/engine/_dormant/engine/ (git mv targets: audio-fingerprint, trends, fuzzy, rules, stage11-counterfactuals + prompts + tests)</files>
  <read_first>
    - .planning/phases/01-strip-to-senses/01-RESEARCH.md Claim 1 (per-module importer table) + Claim 2 (rules whole-module) + Validated Order of Operations step 7 + Pitfall 1/2
    - .planning/_archive/01-pre-apollo-strip-retrieval-trending/01-CONTEXT.md (the move-not-delete + tests-travel-with-source + _dormant convention)
    - tsconfig.json (confirm `**/_dormant/**` in exclude) + vitest.config.ts (confirm exclude)
  </read_first>
  <action>
    First, prove the import graph is clean (reverify #10): grep the ACTIVE tree (excluding _dormant + the modules being moved) for any importer of each module about to move — audio-fingerprint, trends, fuzzy, rules, stage11-counterfactuals (+ its prompts). There must be ZERO active importers (Plans 02/03/01/04 cleared them — including the aggregator test ../stage11 mocks removed in Plan 04). If any remain, STOP and fix the call site first. Then `git mv` each module AND its co-located test into `src/lib/engine/_dormant/engine/` (mirror the wave4/ subdir for platform-fit in the next task). Move rules.ts WHOLE (regex+semantic inline, not splittable — RESEARCH Claim 2). Move trends.ts together with fuzzy.ts (trends imports fuzzy; fuzzy has no other live importer). Use `git mv` so history follows. Do NOT edit the modules' internals — they are dormant, not compiled.
  </action>
  <verify>
    <automated>for m in audio-fingerprint trends fuzzy rules stage11-counterfactuals; do echo "== $m =="; grep -rln "from \"\\.\\?\\./.*$m\"\|from '\\.\\?\\./.*$m'" src --include=*.ts --include=*.tsx | grep -v "_dormant" || echo "no active importer"; done; test -f src/lib/engine/_dormant/engine/rules.ts && test -f src/lib/engine/_dormant/engine/stage11-counterfactuals.ts</automated>
  </verify>
  <acceptance_criteria>
    - Zero active-tree (non-_dormant) importers of audio-fingerprint, trends, fuzzy, rules, stage11-counterfactuals (grep proof in SUMMARY)
    - audio-fingerprint.ts, trends.ts, fuzzy.ts, rules.ts, stage11-counterfactuals.ts (+ prompts) and their tests now live under src/lib/engine/_dormant/engine/
    - Files moved via `git mv` (history preserved)
  </acceptance_criteria>
  <done>Five module groups dormanted with tests; no active importer remains (grep-proven).</done>
</task>

<task type="auto">
  <name>Task 2: Dormant platform-fit + ml; dormant the retrain-ml cron route + remove its vercel.json schedule</name>
  <files>src/lib/engine/_dormant/engine/ml.ts, src/lib/engine/_dormant/engine/wave4/platform-fit.ts, src/app/api/cron/retrain-ml/route.ts, vercel.json</files>
  <read_first>
    - src/app/api/cron/retrain-ml/route.ts (re-grep imports of ../../../../lib/engine/ml or @/lib/engine/ml)
    - vercel.json (re-grep the retrain-ml cron entry — confirmed: { "path": "/api/cron/retrain-ml", "schedule": "0 3 * * 1" })
    - .planning/phases/01-strip-to-senses/01-RESEARCH.md Claim 1 (ml has the cron importer) + Open Q4 (dormant the route) + Assumption A3 (vercel cron IS configured)
    - planning_context user_decisions #4 (DORMANT THE ROUTE; disable the Vercel schedule)
  </read_first>
  <action>
    Move wave4/platform-fit.ts + wave4/platform-fit-prompts.ts (+ their wave4/__tests__/platform-fit*.test.ts — the MODULE's own unit tests travel with it) into src/lib/engine/_dormant/engine/wave4/ via git mv. Do NOT move __tests__/aggregator-platform-fit.test.ts — it does not exist after Plan 04 (it tested the kept aggregator's platform_fit key and was deleted there); if it is somehow still present, STOP and flag it (Plan 04 should have removed it). Handle ml.ts's last live importer: the retrain-ml cron route. Per the session decision, dormant the cron route (git mv src/app/api/cron/retrain-ml/ into _dormant/, or delete it — it trains a disabled 0-weight model) so ml.ts has no active importer, then git mv ml.ts (+ ml.test.ts) into _dormant/engine/. Remove the `{ "path": "/api/cron/retrain-ml", "schedule": "0 3 * * 1" }` object from the `crons` array in vercel.json so Vercel stops scheduling a now-dormant route. Re-grep to confirm ml has zero active importers BEFORE moving it.
  </action>
  <verify>
    <automated>grep -rln "engine/ml\"\|engine/ml'" src/app --include=*.ts | grep -v _dormant || echo "no active ml importer"; grep -c "retrain-ml" vercel.json | grep -qx 0 && test -f src/lib/engine/_dormant/engine/ml.ts && test -f src/lib/engine/_dormant/engine/wave4/platform-fit.ts</automated>
  </verify>
  <acceptance_criteria>
    - ml.ts + ml.test.ts and wave4/platform-fit* (+ the module's own wave4 tests) now live under src/lib/engine/_dormant/engine/
    - No active-tree importer of ml.ts remains (cron route moved/deleted)
    - `grep -c "retrain-ml" vercel.json` returns 0 (cron schedule removed)
    - retrain-ml route no longer present in the active src/app/api tree
  </acceptance_criteria>
  <done>platform-fit + ml dormanted; retrain-ml cron route dormanted + vercel schedule removed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove _dormant exclusion — full build + suite green, no active import reaches in</name>
  <files>(verification only — build/test/grep; optional _dormant/README.md)</files>
  <read_first>
    - tsconfig.json + vitest.config.ts (confirm `**/_dormant/**` exclude is present — RESEARCH Claim 14: already done)
    - .planning/phases/01-strip-to-senses/01-RESEARCH.md Claim 14 (exclude already configured) + reverify #9
  </read_first>
  <action>
    Run the full build + test suite to prove the dormant move is clean: `npm run build` must succeed (tsconfig excludes _dormant, so the moved tree is not type-checked) and `npm test` must pass (vitest excludes _dormant, so the moved tests do not run). Then grep the entire active `src/` tree to prove NO non-dormant file imports anything under `_dormant/` (the only allowed mentions are comments). If the build surfaces a dangling import to a moved module, fix that call site (it means an earlier plan missed one). Optionally add a short `src/lib/engine/_dormant/README.md` documenting the move-not-delete convention (planner discretion, non-blocking).
  </action>
  <verify>
    <automated>npm run build && npm test && (grep -rln "from \"[^\"]*_dormant" src --include=*.ts --include=*.tsx | grep -v "/_dormant/" | grep . && exit 1 || echo "no active import into _dormant")</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npm test` exits 0 (full suite, dormant tests excluded)
    - No active-tree (non-_dormant) file imports any path containing `_dormant` (grep returns empty)
  </acceptance_criteria>
  <done>Build + full suite green post-move; _dormant exclusion proven; no stranded import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel cron -> /api/cron/retrain-ml | scheduled invocation of a now-dormant route |
| active engine tree -> _dormant/ | excluded-from-compile boundary (tsconfig/vitest glob) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information disclosure / Elevation | dormanting retrain-ml route | mitigate | Moving the route out of src/app/api removes its HTTP surface entirely; removing the vercel.json schedule stops scheduled invocation. No orphaned unauthenticated endpoint is left exposed (the route file leaves the routable tree). VERIFY the route is gone from src/app/api after the move. |
| T-05-02 | Tampering | git mv of dead modules | accept | Source preserved in _dormant/ + git history; excluded from compile/test; reversible. No behavior change to the live path. |
| T-05-03 | Denial of service | removed weekly cron | accept | The cron trained a disabled 0-weight model; removing it has no functional impact and reduces scheduled load. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan (file moves + config edit). If proposed, halt. |

No high-severity threats — dormanting the cron route REMOVES an HTTP surface rather than adding one; confirm the route file leaves src/app/api so no unauthenticated endpoint is orphaned.
</threat_model>

<verification>
- `npm run build` + `npm test` green
- grep: zero active-tree importers of any moved module (per module) and zero active imports into _dormant/
- `grep -c retrain-ml vercel.json` == 0; retrain-ml route absent from src/app/api
- moved files exist under src/lib/engine/_dormant/engine/ (history preserved via git mv)
</verification>

<success_criteria>
- R9: dead machinery (ml/audio/trends/fuzzy/rules/stage11/platform-fit) dormanted, not live
- R6: rule-semantic + platform_fit LLM calls fully gone from the build
- No stranded import; no orphaned cron route; full suite + build green
</success_criteria>

<output>
Create `.planning/phases/01-strip-to-senses/01-05-SUMMARY.md` when done. MUST record the per-module grep proof (zero active importers) and confirm retrain-ml removed from both src/app/api and vercel.json.
</output>
