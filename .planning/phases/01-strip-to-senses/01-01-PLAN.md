---
phase: 01-strip-to-senses
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/measure-pipeline.ts
  - src/lib/engine/__tests__/creator-rules.test.ts
  - src/components/board/verdict/__tests__/verdict-derive.platform-fit-null.test.ts
  - src/components/app/simulation/__tests__/results-panel.predicted-engagement-null.test.tsx
autonomous: false
requirements: [R6, R8]
must_haves:
  truths:
    - "measure-pipeline.ts logs result.overall_score so a pre/post score delta can be diffed"
    - "A pre-strip baseline overall_score + total latency is captured for a fixed video"
    - "creator-rules.test.ts no longer imports stage11/platform-fit prompt files (cut-module cross-import removed)"
    - "trending_sounds / scraped_videos / outcomes confirmed 0 rows before any dormant move"
    - "Determinism: same fixed video run twice yields identical overall_score (temp0+seed already live)"
  artifacts:
    - path: "scripts/measure-pipeline.ts"
      provides: "E2E latency timeline + overall_score logging for score-delta proof"
      contains: "overall_score"
    - path: "src/lib/engine/__tests__/creator-rules.test.ts"
      provides: "KEPT creator-rules test with cut-module imports removed/inlined"
    - path: "src/components/board/verdict/__tests__/verdict-derive.platform-fit-null.test.ts"
      provides: "null-input test confirming verdict-derive platform_fit null-safety"
    - path: "src/components/app/simulation/__tests__/results-panel.predicted-engagement-null.test.tsx"
      provides: "test asserting absent predicted_engagement → TikTokResultCard not rendered"
  key_links:
    - from: "scripts/measure-pipeline.ts"
      to: "aggregateScores result"
      via: "log overall_score after pipeline run"
      pattern: "overall_score"
---

<objective>
Wave 0 scaffolding + read-only verification gates. This plan touches NO production engine source — it only (a) extends the measurement harness so the score-delta is observable, (b) removes a cut-module cross-import from a KEPT test so later dormant moves don't break compile, (c) adds the two missing null-degrade tests, and (d) runs the read-only DB-row-count + determinism + baseline-latency gates that the strip must be measured against.

Purpose: Establish the measurement baseline and clear the `creator-rules.test.ts` landmine BEFORE any source is cut, so every later wave stays green-at-each-step and the D3.2 score delta has a pre-strip anchor.
Output: Extended `measure-pipeline.ts`, a fixed-video baseline (overall_score + latency, recorded in the SUMMARY), reconciled `creator-rules.test.ts`, two new null-degrade tests, and documented DB-row-count + determinism results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ENGINE-MAP.md
@.planning/phases/01-strip-to-senses/01-RESEARCH.md
@.planning/phases/01-strip-to-senses/01-VALIDATION.md

<interfaces>
<!-- Anchors verified 2026-06-04. Line numbers DRIFT — re-grep SYMBOLS, not lines. -->

scripts/measure-pipeline.ts — runs the REAL engine (`runPredictionPipeline` + `aggregateScores`) like /api/analyze; uploads a local video; reconstructs a wall-clock timeline from StageEvents. Currently logs latency, NOT overall_score. The aggregateScores result is in scope. Run: `npx tsx scripts/measure-pipeline.ts [/path/to/video.mp4]`. Requires `.env.local` (Supabase + DASHSCOPE_API_KEY).

src/lib/engine/__tests__/creator-rules.test.ts — KEPT module's test. Imports `STABLE_COUNTERFACTUALS_SYSTEM_PROMPT` (from `../stage11-counterfactuals-prompts`) and `STABLE_PLATFORM_FIT_SYSTEM_PROMPT` (from `../wave4/platform-fit-prompts`) and asserts `CREATOR_RULES_BLOCK` is embedded in BOTH. Those prompt files move to _dormant/ in Plan 05.

src/components/board/verdict/verdict-derive.ts — `(result.platform_fit as { fit_score?: number } | null | undefined)?.fit_score` with a `typeof fit === 'number'` guard. ALREADY null-safe.

src/components/app/simulation/results-panel.tsx — `{result.predicted_engagement && (<TikTokResultCard .../>)}` (ALREADY null-guarded) and `{result.counterfactuals && (<SuggestionsSection .../>)}` (ALREADY null-guarded).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend measure-pipeline.ts to log overall_score (Wave 0 harness)</name>
  <files>scripts/measure-pipeline.ts</files>
  <read_first>
    - scripts/measure-pipeline.ts (re-grep `aggregateScores` and the result variable name; do NOT rely on line numbers)
    - .planning/phases/01-strip-to-senses/01-RESEARCH.md §"Claim 8" (the harness already has the aggregateScores result in scope; it just doesn't print overall_score)
  </read_first>
  <action>
    In scripts/measure-pipeline.ts, after the `aggregateScores` result is computed, add a clearly-labeled log line that prints `result.overall_score` (and `result.confidence` if present) alongside the existing total-latency line. Do NOT change what the harness runs or its inputs — additive logging only. Use a stable, greppable label (e.g. `OVERALL_SCORE=`) so the pre/post diff in Plan 06 can parse it. Preserve the existing StageEvent timeline output.
  </action>
  <verify>
    <automated>grep -n "overall_score" scripts/measure-pipeline.ts</automated>
  </verify>
  <acceptance_criteria>
    - scripts/measure-pipeline.ts contains a log statement referencing `overall_score`
    - The harness still compiles: `npx tsc --noEmit -p tsconfig.json` reports no NEW errors in measure-pipeline.ts (this file may be excluded from tsconfig `scripts` glob — if so, `npx tsx --check scripts/measure-pipeline.ts` or a dry parse suffices)
    - No change to the harness's engine call (`runPredictionPipeline`/`aggregateScores`) or its CLI args
  </acceptance_criteria>
  <done>measure-pipeline.ts emits a greppable overall_score line; engine invocation unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile creator-rules.test.ts cross-imports (landmine fix)</name>
  <files>src/lib/engine/__tests__/creator-rules.test.ts</files>
  <read_first>
    - src/lib/engine/__tests__/creator-rules.test.ts (re-grep `STABLE_COUNTERFACTUALS_SYSTEM_PROMPT`, `STABLE_PLATFORM_FIT_SYSTEM_PROMPT`, `CREATOR_RULES_BLOCK`)
    - .planning/phases/01-strip-to-senses/01-RESEARCH.md §"Claim 1" cross-cut landmine + §"Pitfall 2"
  </read_first>
  <action>
    creator-rules.test.ts (a KEPT module's test) imports `STABLE_COUNTERFACTUALS_SYSTEM_PROMPT` from `../stage11-counterfactuals-prompts` and `STABLE_PLATFORM_FIT_SYSTEM_PROMPT` from `../wave4/platform-fit-prompts` — both prompt files move to _dormant/ in Plan 05, which would break this test at compile. Remove BOTH cut-module imports and the two assertion blocks that depend on them (the `toContain(CREATOR_RULES_BLOCK)` / `toContain("Lead-Magnet Formula")` / `toContain("[weight 25]")` / `toContain("NEVER average platforms")` checks against those two prompt constants). KEEP every assertion that targets `creator-rules.ts`'s own exports (the `CREATOR_RULES_BLOCK` content / structure tested against the kept module itself). Do NOT inline copies of the cut prompt strings — the point is to stop coupling a kept test to cut modules. The test's purpose (verify the KEPT creator-rules block) must remain intact.
  </action>
  <verify>
    <automated>grep -c "stage11-counterfactuals-prompts\|wave4/platform-fit-prompts" src/lib/engine/__tests__/creator-rules.test.ts | grep -qx 0 && npx vitest run src/lib/engine/__tests__/creator-rules.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "stage11-counterfactuals-prompts\|wave4/platform-fit-prompts" src/lib/engine/__tests__/creator-rules.test.ts` returns 0
    - `npx vitest run src/lib/engine/__tests__/creator-rules.test.ts` exits 0
    - The remaining assertions still target creator-rules.ts's own exports (test is not gutted to a no-op)
  </acceptance_criteria>
  <done>creator-rules.test.ts imports no cut-module prompt file and passes; kept-module coverage preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Add the two missing null-degrade tests (verdict-derive + predicted_engagement)</name>
  <files>src/components/board/verdict/__tests__/verdict-derive.platform-fit-null.test.ts, src/components/app/simulation/__tests__/results-panel.predicted-engagement-null.test.tsx</files>
  <read_first>
    - src/components/board/verdict/verdict-derive.ts (re-grep `platform_fit`, `fit_score`)
    - src/components/app/simulation/results-panel.tsx (re-grep `predicted_engagement`, `TikTokResultCard`)
    - .planning/phases/01-strip-to-senses/01-RESEARCH.md §"Claim 6" (both surfaces ALREADY null-safe — these tests CONFIRM existing behavior, they do not add new handling)
    - An existing sibling test for the import/render conventions (e.g. an existing verdict or results-panel test in the same dirs)
  </read_first>
  <action>
    Add two confirmation tests (these assert EXISTING null-safety; do not modify production code):
    (a) src/components/board/verdict/__tests__/verdict-derive.platform-fit-null.test.ts — call the verdict-derive function with a result whose `platform_fit` is null AND with `platform_fit` absent/undefined; assert it returns without throwing and does not surface a fit-derived value.
    (b) src/components/app/simulation/__tests__/results-panel.predicted-engagement-null.test.tsx — render results-panel (or its TikTokResultCard gate) with `predicted_engagement` absent/null; assert TikTokResultCard is NOT rendered. Use the project's existing React test setup (react-testing-library / vitest jsdom — match the convention of a sibling component test). If results-panel is too heavy to mount, test the render gate at the smallest unit that exercises the `{result.predicted_engagement && ...}` condition.
  </action>
  <verify>
    <automated>npx vitest run src/components/board/verdict/__tests__/verdict-derive.platform-fit-null.test.ts src/components/app/simulation/__tests__/results-panel.predicted-engagement-null.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both new test files exist and `npx vitest run` on them exits 0
    - The verdict-derive test passes null AND undefined platform_fit and asserts no throw
    - The results-panel test asserts TikTokResultCard absent when predicted_engagement is null/absent
    - NO production source file was modified by this task
  </acceptance_criteria>
  <done>Two null-degrade confirmation tests added and green; no production code touched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] Pre-strip baseline gates — DB row counts, determinism, baseline score+latency</name>
  <files>(verification only — no files modified)</files>
  <action>Run the read-only DB-row-count, determinism, and baseline-latency gates described below and record the pre-strip baseline in the SUMMARY. No source is modified.</action>
  <what-built>Read-only DB-row-count + determinism + baseline-latency gates. These require live Supabase/DashScope access (interactive auth may be needed for the MCP and the harness consumes real API budget), so they run here as a blocking gate that records the pre-strip baseline.</what-built>
  <how-to-verify>
    1. DB row counts (reverify #3) — run via `mcp__supabase__execute_sql`:
       `SELECT count(*) FROM trending_sounds;` then `SELECT count(*) FROM scraped_videos;` then `SELECT count(*) FROM outcomes;`
       EXPECT 0 each. If any is non-zero, STOP and flag — trends/audio may not be safely dead before the Plan 05 dormant move.
    2. Determinism (R8) — run `npx tsx scripts/measure-pipeline.ts /path/to/fixed-video.mp4` TWICE on the SAME video; the logged OVERALL_SCORE must be byte-identical across the two runs (temp0+seed are already live — this confirms it).
    3. Baseline latency + score (R6 / D3.2 anchor) — from those same runs, record the total wall-clock latency and the OVERALL_SCORE. This is the PRE-STRIP baseline that Plan 06 diffs against.
    Record all three results (row counts, determinism pass/fail, baseline overall_score + latency) in this plan's SUMMARY.
  </how-to-verify>
  <acceptance_criteria>
    - trending_sounds, scraped_videos, outcomes row counts recorded (expected 0 each)
    - Same-video-twice OVERALL_SCORE confirmed identical (R8)
    - Pre-strip baseline overall_score + total latency recorded in SUMMARY (the D3.2/R6 anchor)
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded row counts + baseline overall_score/latency, or describe the blocker (e.g. non-zero row count, non-deterministic score).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → Supabase (MCP execute_sql) | read-only count queries; no writes, no untrusted input |
| harness → DashScope/Supabase | measure-pipeline runs the real engine with real creds from .env.local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information disclosure | measure-pipeline overall_score logging | accept | Logs to stdout only; no PII; score is already returned to the client. No new surface. |
| T-01-02 | Tampering | read-only SQL counts | accept | SELECT count(*) only; no mutation path; MCP creds unchanged. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — deletion/test/harness work only. If any install is proposed, halt (no Package Legitimacy audit in RESEARCH). |

No high-severity threats — Wave 0 adds logging + tests + read-only queries; no new endpoint, input, or attack surface.
</threat_model>

<verification>
- `npx vitest run src/lib/engine/__tests__/creator-rules.test.ts` exits 0 with no cut-module imports
- Both new null-degrade tests green
- `grep -n overall_score scripts/measure-pipeline.ts` matches
- DB row counts = 0 (recorded); same-video determinism confirmed; baseline overall_score + latency recorded
</verification>

<success_criteria>
- Harness logs overall_score (score-delta now observable)
- creator-rules.test.ts landmine cleared before any dormant move
- predicted_engagement + platform_fit null-safety confirmed by test
- Pre-strip baseline (row counts, determinism, overall_score, latency) recorded for D3.2/R6/R8
</success_criteria>

<output>
Create `.planning/phases/01-strip-to-senses/01-01-SUMMARY.md` when done. MUST record: the pre-strip baseline overall_score, total latency, the three DB row counts, and determinism pass/fail.
</output>
