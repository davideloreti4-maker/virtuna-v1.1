---
phase: 04-proof-conversion
plan: 05
type: execute
wave: 2
depends_on: [04-00, 04-01, 04-02, 04-03, 04-04]
files_modified:
  - src/components/marketing/index.ts
  - src/app/(marketing)/page.tsx
autonomous: true
requirements: [PROOF-01, PROOF-02, CONVERT-01, CONVERT-02, CONVERT-03]

must_haves:
  truths:
    - "The assembled / page renders all five Phase-4 sections in the D-18 order"
    - "The social-proof strip rides directly under #hero; testimonials sit after #features, before #pricing"
    - "The #pricing and #faq stubs are filled in place; the full-bleed CTA band sits just before <Footer/>"
    - "/ stays statically prerendered (○ /) — no client directive leaked to a section root"
    - "NAV_LINKS is unchanged — still exactly 5 links (D-19)"
  artifacts:
    - path: "src/components/marketing/index.ts"
      provides: "Barrel exports for the 5 new sections so page.tsx imports from @/components/marketing"
      contains: "SocialProofStrip"
    - path: "src/app/(marketing)/page.tsx"
      provides: "D-18 assembled scroll — strip + testimonials inserted, #pricing/#faq filled, band before footer"
      contains: "SocialProofStrip"
  key_links:
    - from: "page.tsx"
      to: "@/components/marketing barrel"
      via: "named imports of the 5 new sections"
      pattern: "SocialProofStrip,[\\s\\S]*Testimonials,[\\s\\S]*PricingTeaser,[\\s\\S]*Faq,[\\s\\S]*FinalCtaBand"
    - from: "page.tsx"
      to: "rendered DOM order"
      via: "D-18 section sequence with stable ids/markers"
      pattern: "id=\"social-proof\"[\\s\\S]*id=\"testimonials\"[\\s\\S]*data-section=\"final-cta\""
---

<objective>
Assemble Phase 4 into the page. This is the ONLY plan that edits `src/app/(marketing)/page.tsx`
(page.tsx write-contention — every other Phase-4 plan built file-disjoint components). Extend the
marketing barrel with the 5 new section exports, then wire them into the page in the locked D-18
order: insert the social-proof strip directly under `#hero`; insert testimonials after `#features`
(before `#pricing`); fill the `#pricing` and `#faq` stubs in place; insert the full-bleed CTA band
just before `<Footer/>`. Preserve the locked section rhythm; the CTA band intentionally breaks the
`max-w-5xl` measure (D-12). Keep page.tsx a pure RSC so `/` stays statically prerendered, and leave
`NAV_LINKS` unchanged (D-19). Turn the 04-00 page-order test GREEN and pass the build gate.

Purpose: The page-order RED block from 04-00 is the integration contract; this plan satisfies it.
Output: 2 modified files (barrel + page).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-proof-conversion/04-CONTEXT.md
@.planning/phases/04-proof-conversion/04-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contracts extracted from in-repo sources this session. Use directly. -->

Current page.tsx (src/app/(marketing)/page.tsx) — VERIFIED structure:
  - Pure RSC (no "use client"). Imports sections from "@/components/marketing" (lines 3-9).
  - <MotionConfigShell><Header/><main>...sections...</main><Footer/></MotionConfigShell>
  - LOCKED section rhythm (mirror for inserted full-measure sections):
      <section id="..." className="scroll-mt-20 border-t border-border px-6 py-16 md:py-20">
        <div className="mx-auto max-w-5xl"> <SectionComponent/> </div>
      </section>
  - #hero is the FIRST section (line 46-48, padding-only, no border-t).
  - #features section ENDS at line 87.
  - #pricing STUB lines 89-99: replace the inner `<h2 ...muted>Pricing</h2>` with <PricingTeaser/>.
  - #faq STUB lines 101-111: replace the inner `<h2 ...muted>FAQ</h2>` with <Faq/>.
  - <Footer/> is mounted at line 113.

Current barrel (src/components/marketing/index.ts) — VERIFIED, append after line 11:
  export { Hero } / { HowItWorks } / { SimulationShowcase } / { FeatureBlocks } already present.

New section exports to add (from Waves 1):
  SocialProofStrip → "./proof/social-proof-strip"
  Testimonials     → "./proof/testimonials"
  PricingTeaser    → "./pricing/pricing-teaser"
  Faq              → "./faq/faq"
  FinalCtaBand     → "./cta/final-cta-band"

Stable section ids/markers the 04-00 page-order test asserts (the integration CONTRACT):
  strip section id="social-proof"  ·  testimonials section id="testimonials"  ·  band section data-section="final-cta"
NAV_LINKS (src/lib/nav.ts) — DO NOT extend (D-19); the new sections get NO nav links.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the marketing barrel with the 5 new section exports</name>
  <files>src/components/marketing/index.ts</files>
  <read_first>
    - src/components/marketing/index.ts (the current exports — append after the existing story exports)
    - .planning/phases/04-proof-conversion/04-PATTERNS.md (§ src/components/marketing/index.ts — the append target)
    - src/components/marketing/proof/social-proof-strip.tsx, src/components/marketing/proof/testimonials.tsx, src/components/marketing/pricing/pricing-teaser.tsx, src/components/marketing/faq/faq.tsx, src/components/marketing/cta/final-cta-band.tsx (confirm the exact export names exist from Waves 1)
  </read_first>
  <action>
    Append five named exports to `src/components/marketing/index.ts` (after the existing story
    exports), matching the barrel's existing style:
    `export { SocialProofStrip } from "./proof/social-proof-strip";`
    `export { Testimonials } from "./proof/testimonials";`
    `export { PricingTeaser } from "./pricing/pricing-teaser";`
    `export { Faq } from "./faq/faq";`
    `export { FinalCtaBand } from "./cta/final-cta-band";`
    Do NOT re-export the leaf primitives (TestimonialCard / PricingCard / FaqAccordion) — only the
    page-mounted sections. Do NOT change existing exports.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && npx tsc --noEmit 2>&1 | grep -i "marketing/index" | head -5 || echo "barrel-resolves"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/marketing/index.ts` exports `SocialProofStrip`, `Testimonials`, `PricingTeaser`, `Faq`, `FinalCtaBand` (`grep -Eq 'SocialProofStrip|Testimonials|PricingTeaser|Faq|FinalCtaBand' ...` matches all 5).
    - Existing exports (Hero, HowItWorks, SimulationShowcase, FeatureBlocks, Placeholder, MotionConfigShell) are unchanged.
    - No leaf-primitive re-exports added (no `TestimonialCard`/`PricingCard`/`FaqAccordion` in the barrel).
  </acceptance_criteria>
  <done>The barrel exposes the 5 new sections; page.tsx can import them from `@/components/marketing`.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the 5 sections into page.tsx in D-18 order + verify the static-build gate</name>
  <files>src/app/(marketing)/page.tsx</files>
  <read_first>
    - src/app/(marketing)/page.tsx (the full current structure — the import block, the #hero/#features sections, the #pricing/#faq stubs, the <Footer/> mount)
    - src/components/marketing/__tests__/page-order.test.tsx (the integration contract — the exact stable ids/markers + the D-18 relative order it asserts)
    - .planning/phases/04-proof-conversion/04-PATTERNS.md (§ page.tsx — the D-18 mount plan + the full-bleed band Pattern 3 + the static-build-gate LANDMINE)
    - .planning/phases/04-proof-conversion/04-CONTEXT.md (D-12 full-width band, D-18 order, D-19 nav lock)
  </read_first>
  <action>
    Edit `src/app/(marketing)/page.tsx` (keep it a PURE RSC — no `"use client"`). Add the 5 new
    sections to the barrel import block, then mount them in the D-18 order:

    1. After the `#hero` section (closes line 48) → insert the social-proof strip section. It rides
       high under the hero as a thin trust bar. Use the locked rhythm but a TIGHTER vertical padding
       (it is a strip, not a full section — e.g. `scroll-mt-20 border-t border-border px-6 py-8 md:py-10`)
       with `id="social-proof"` and the `mx-auto max-w-5xl` inner measure wrapping `<SocialProofStrip/>`.
    2. After the `#features` section (closes line 87) → insert the testimonials section using the FULL
       locked rhythm (`scroll-mt-20 border-t border-border px-6 py-16 md:py-20`), `id="testimonials"`,
       `mx-auto max-w-5xl` wrapping `<Testimonials/>` (conversion zone, D-05 — after features, before pricing).
    3. Fill the `#pricing` stub (lines 89-99) → replace the inner `<h2 ...muted>Pricing</h2>` with
       `<PricingTeaser/>` (keep the existing `<section id="pricing" ...>` + `mx-auto max-w-5xl` wrapper).
    4. Fill the `#faq` stub (lines 101-111) → replace the inner `<h2 ...muted>FAQ</h2>` with `<Faq/>`
       (keep the existing `<section id="faq" ...>` + wrapper).
    5. Before `<Footer/>` (line 113) → insert the full-bleed CTA band section. Per D-12 / RESEARCH
       Pattern 3, this section carries NO `max-w-5xl` inner measure — the band owns its own full-bleed
       surface: `<section data-section="final-cta" className="border-t border-border"><FinalCtaBand/></section>`.
       (The band needs no `scroll-mt`/`id` — it has no nav anchor, D-19.)

    Refresh the page docblock to describe the now-complete Phase-4 scroll. Do NOT touch
    `src/lib/nav.ts` (NAV_LINKS unchanged — D-19). Do NOT add `#proof`/`#testimonials`/`#cta` to the
    nav. Verify the static-build gate: `npm run build` must show `○ /` (static) in the route table —
    if any newly-mounted section transitively leaks a client directive to the page root the route
    flips to `ƒ /` and the build gate fails (Pitfall 5; only the FAQ accordion is a client island,
    nested under the RSC `<Faq>`).
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && npx vitest run src/components/marketing/__tests__/page-order.test.tsx 2>&1 | tail -5 && npm run build 2>&1 | grep -E '○ /|ƒ /|^\s*/\s' | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/marketing/__tests__/page-order.test.tsx` exits 0 (D-18 order + D-19 NAV-lock both GREEN).
    - `npm run build` exits 0 and the route table shows `○ /` (static), NOT `ƒ /` / `λ /` (the standing build gate; Pitfall 5).
    - page.tsx renders the D-18 order: `id="social-proof"` after `#hero`; `id="testimonials"` after `#features` and before `#pricing`; `#pricing` body is `<PricingTeaser/>`; `#faq` body is `<Faq/>`; `data-section="final-cta"` section is the last child of `<main>` before `<Footer/>`.
    - page.tsx contains no `"use client"` (`grep -c 'use client' src/app/(marketing)/page.tsx` → 0).
    - `src/lib/nav.ts` is NOT in this plan's changes and `NAV_LINKS.length === 5` still holds (asserted by the page-order test).
    - The full-bleed band section carries NO `max-w-5xl` (it breaks the inner measure per D-12).
  </acceptance_criteria>
  <done>The assembled / renders all 5 Phase-4 sections in D-18 order, the #pricing/#faq stubs are filled, the full-bleed CTA band sits before the footer, / stays ○ static, and NAV_LINKS is unchanged; the page-order test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC page root → FAQ client island | The only client boundary in the assembled page is the FAQ accordion, nested under the RSC `<Faq>` wrapper. The page root stays RSC. |
| marketing page → app signup | All CTAs (pricing cards, CTA band) are outbound `<Link href="/signup">` — hardcoded internal constant, no user-controlled redirect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Denial of Service / build-gate break | client leak to page root | mitigate | Only the FAQ accordion is `"use client"`, mounted as an island under the RSC `<Faq>`; the build gate (`○ /`) in Task 2 verify enforces the page stays statically prerendered. |
| T-04-05-02 | Tampering | NAV anchors | mitigate | `src/lib/nav.ts` is untouched (D-19); the page-order test asserts `NAV_LINKS.length === 5`. No new nav anchors introduced. |
| T-04-05-03 | Tampering | CTA redirect targets | mitigate | All CTAs route to the hardcoded internal `SIGNUP_URL`; no user-controlled redirect param anywhere in the assembled page. |
| T-04-SC | Tampering | npm installs | accept | Zero installs — assembly only wires existing in-repo components. |

No high-severity threats (04-RESEARCH § Security Domain — static, input-free surface).
</threat_model>

<verification>
- Page-order test GREEN (D-18 order + D-19 NAV lock).
- `npm test` full suite green (all 5 section tests + page-order + the prior baseline).
- `npm run build` exits 0 with `○ /` (static-build gate held).
- page.tsx pure RSC; barrel exposes the 5 sections; `src/lib/nav.ts` untouched.
</verification>

<success_criteria>
- All five Phase-4 sections render on `/` in the locked D-18 order.
- `#pricing` / `#faq` stubs filled in place; full-bleed CTA band before the footer.
- `/` stays statically prerendered (`○ /`); `NAV_LINKS` unchanged (exactly 5).
- The page-order RED block from 04-00 is now GREEN.
</success_criteria>

<output>
Create `.planning/phases/04-proof-conversion/04-05-SUMMARY.md` when done.
</output>
