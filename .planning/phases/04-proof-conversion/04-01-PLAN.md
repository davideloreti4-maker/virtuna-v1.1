---
phase: 04-proof-conversion
plan: 01
type: execute
wave: 1
depends_on: [04-00]
files_modified:
  - src/components/marketing/proof/social-proof-strip.tsx
  - src/components/marketing/proof/testimonial-card.tsx
  - src/components/marketing/proof/testimonials.tsx
autonomous: true
requirements: [PROOF-01, PROOF-02]

must_haves:
  truths:
    - "A thin trust strip renders a peer-count stat + an auto-scroll logo marquee of placeholder logos"
    - "The marquee's duplicated copies are hidden from the a11y tree (screen reader reads logos once)"
    - "A static 3-card testimonials grid renders, each card = avatar placeholder + name + @handle + quote + result metric"
    - "Both sections are pure RSC — no \"use client\" at either root — so / stays statically prerendered"
  artifacts:
    - path: "src/components/marketing/proof/social-proof-strip.tsx"
      provides: "PROOF-01 — trust stat + reduced-motion-gated logo marquee (RSC)"
      exports: ["SocialProofStrip"]
    - path: "src/components/marketing/proof/testimonial-card.tsx"
      provides: "PROOF-02 — bespoke flat-warm card (avatar + name + @handle + quote + metric)"
      exports: ["TestimonialCard"]
    - path: "src/components/marketing/proof/testimonials.tsx"
      provides: "PROOF-02 — static 3-up grid of TestimonialCard (RSC + StaggerReveal leaf)"
      exports: ["Testimonials"]
  key_links:
    - from: "social-proof-strip.tsx"
      to: "ui/marquee.tsx + marketing/placeholder.tsx"
      via: "Marquee wrapping Placeholder variant=logo, region aria-hidden"
      pattern: "Marquee[\\s\\S]*variant=\"logo\""
    - from: "testimonials.tsx"
      to: "testimonial-card.tsx"
      via: "map TESTIMONIALS through TestimonialCard inside StaggerRevealItem"
      pattern: "TESTIMONIALS\\.map"
    - from: "testimonial-card.tsx"
      to: "marketing/placeholder.tsx"
      via: "Placeholder variant=avatar"
      pattern: "variant=\"avatar\""
---

<objective>
Build the two PROOF sections as bespoke flat-warm pure-RSC components: a thin social-proof strip
(peer-count trust stat + auto-scroll logo marquee of placeholder logos, riding directly under the
hero) and a static 3-card testimonials grid (each card = avatar placeholder + name + @handle +
drafted quote + result metric). Turn the two PROOF-01/PROOF-02 RED tests from 04-00 GREEN. Do NOT
mount anything into page.tsx — that is 04-05's job (page.tsx write-contention).

Purpose: The proof split (D-01) brackets the scroll — instant credibility under the hero, deeper
testimonials in the conversion zone. Conversion is the brief (D-21): the peer count and result
metrics are the levers.
Output: 3 new component files under `src/components/marketing/proof/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-proof-conversion/04-CONTEXT.md
@.planning/phases/04-proof-conversion/04-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contracts extracted from in-repo sources this session. Use directly — no exploration. -->

Marquee (src/components/ui/marquee.tsx) — VERIFIED:
  export function Marquee(props: { className?, reverse?, pauseOnHover?, vertical?, repeat?=4, children, ...divProps })
  - CSS-driven `animate-marquee`; globally reduced-motion-gated (globals.css L363-369). No JS, no internal aria-hidden.
  - Renders children `repeat=4` times → wrap the whole marquee region in aria-hidden (Pitfall 4).

Placeholder (src/components/marketing/placeholder.tsx) — VERIFIED:
  <Placeholder variant="logo" aspect="3/1" className="..." />   // logo label suppressed (line 167), data-variant="logo"
  <Placeholder variant="avatar" className="h-10 w-10" />        // 1/1 rounded-full, data-variant="avatar"

StaggerReveal motion barrel (src/components/motion/index.ts) — VERIFIED:
  import { StaggerReveal, StaggerRevealItem } from "@/components/motion";   // NAMED export — NOT StaggerReveal.Item

cn (src/lib/utils.ts) — class merge.

Flat-warm tokens (semantic only, never hex / never text-white):
  text-foreground (cream) · text-foreground-secondary · text-foreground-muted
  bg-surface-elevated · border-border · --radius-lg · --color-accent (CORAL — CTA-only, NOT here)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Social-proof strip — trust stat + reduced-motion-gated logo marquee (PROOF-01)</name>
  <files>src/components/marketing/proof/social-proof-strip.tsx</files>
  <behavior>
    - Renders a peer-count trust stat text node matching /2,?000\+? creators/i (D-04, swappable placeholder).
    - Renders a Marquee of >= 4 (use 5-6) <Placeholder variant="logo"> slots → data-variant="logo" count passes.
    - The marquee region is wrapped in aria-hidden="true" so the duplicated repeat=4 copies are read once (Pitfall 4).
    - Pure RSC (no "use client").
  </behavior>
  <read_first>
    - src/components/marketing/proof/__tests__/social-proof-strip.test.tsx (the RED gate this turns GREEN — match its exact assertions)
    - src/components/marketing/story/simulation-showcase.tsx (lines 72-86 — the RSC section shell idiom: `{ className }` prop, sans serif heading)
    - src/components/ui/marquee.tsx (the reused API + the repeat=4 a11y landmine)
    - src/components/marketing/placeholder.tsx (variant="logo" default aspect 3/1, label suppressed, data-variant hook)
    - .planning/phases/04-proof-conversion/04-PATTERNS.md (§ social-proof-strip.tsx — the assignment + LANDMINE block)
    - .planning/phases/04-proof-conversion/04-CONTEXT.md (D-01..D-04, D-18 placement)
  </read_first>
  <action>
    Create `src/components/marketing/proof/social-proof-strip.tsx` as a PURE RSC exporting
    `function SocialProofStrip({ className }: { className?: string })`. Compose, per D-02/D-04:
    a headline peer-count trust stat ("Join 2,000+ creators" — placeholder number, swappable per
    D-04; render it as the strip's one accessible text so the region has a name) + a `<Marquee>`
    (reuse `@/components/ui/marquee`, `pauseOnHover`, e.g. `className="[--duration:40s]"`) of 5-6
    `<Placeholder variant="logo" aspect="3/1" className="h-10 w-28 shrink-0" />` slots (D-03 —
    neutral creator/brand swap slots, NOT real platform marks, NOT press logos).

    a11y LANDMINE (Pitfall 4): the marquee renders its children `repeat=4` times and none are
    aria-hidden. Wrap the entire `<Marquee>` region in a container with `aria-hidden="true"` so a
    screen reader reads the logo wall once (the trust stat carries the strip's accessible meaning).
    The 04-00 test asserts an `aria-hidden="true"` ancestor of the logo placeholders.

    Token discipline: cream tokens only (`text-foreground` / `text-foreground-secondary`), NO coral
    (the strip has no CTA — coral stays precious to the band/pricing CTAs), NO glass/glow/blur,
    `border-border` hairlines, `--radius-lg`. NO serif (the serif voice is reserved to the hero +
    the band close-line, D-13). Keep the stat typographically confident but calm (D-21 — levers are
    structural, not loud). Add NO heading/`<h2>` (the strip rides under the hero as a thin trust bar,
    not a titled section). The strip owns only its content; the section wrapper + scroll rhythm are
    04-05's. Provide the stable section id contract by NOT hardcoding it here — 04-05 sets
    `id="social-proof"` on the wrapping `<section>` (the strip is the inner content).

    Do NOT touch page.tsx or the barrel. Do NOT add `"use client"`.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && npx vitest run src/components/marketing/proof/__tests__/social-proof-strip.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/marketing/proof/__tests__/social-proof-strip.test.tsx` exits 0 (PROOF-01 GREEN).
    - Source contains no `"use client"` (`grep -c 'use client' src/components/marketing/proof/social-proof-strip.tsx` → 0).
    - Source renders `>= 4` `Placeholder variant="logo"` slots and wraps the Marquee in an `aria-hidden="true"` region.
    - No `text-white`, no hardcoded hex, no `text-accent`/coral, no glass/glow/blur in the file (`grep -Eiv '^\s*(//|\*)' src/components/marketing/proof/social-proof-strip.tsx | grep -Eiq 'text-white|text-accent|bg-accent|#[0-9a-f]{6}|backdrop-blur|drop-shadow' && echo VIOLATION || echo clean` → clean).
    - Exports `SocialProofStrip`.
  </acceptance_criteria>
  <done>The social-proof strip renders a peer-count stat + a reduced-motion-gated, a11y-correct logo marquee; PROOF-01 test passes; component is pure RSC and flat-warm.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bespoke flat-warm testimonial card + static 3-card testimonials grid (PROOF-02)</name>
  <files>src/components/marketing/proof/testimonial-card.tsx, src/components/marketing/proof/testimonials.tsx</files>
  <behavior>
    - TestimonialCard renders: a <Placeholder variant="avatar"> + name + @handle (data-testid="testimonial-handle") + quote + result metric (data-testid="testimonial-metric").
    - Testimonials renders EXACTLY 3 cards in a static grid (grid-cols-1 md:grid-cols-3) → 3 avatar placeholders, 3 handles, 3 metrics.
    - Both pure RSC; the only client leaf is the StaggerReveal entrance (named StaggerRevealItem export).
  </behavior>
  <read_first>
    - src/components/marketing/proof/__tests__/testimonials.test.tsx (the RED gate — match its avatar count / handle / metric assertions exactly, including the data-testid names it queries)
    - src/components/marketing/story/feature-blocks.tsx (lines 3, 78-104 — the canonical RSC-section-with-StaggerReveal-grid idiom + the named-export import)
    - .planning/phases/04-proof-conversion/04-RESEARCH.md (§ "Bespoke flat-warm testimonial card anatomy" lines ~427-447 — the verbatim flat-warm card shape)
    - .planning/phases/04-proof-conversion/04-PATTERNS.md (§ testimonial-card.tsx + § testimonials.tsx — the assignment + both LANDMINE blocks + the legal card-surface excerpt)
    - src/components/ui/testimonial-card.tsx (the ANTI-analog — cold brand, anatomy {quote, author:{name,role,company,avatar}} has NO @handle/metric → DO NOT import/copy styling; take only the inset boxShadow + bg-transparent border-border surface idea)
    - src/components/marketing/placeholder.tsx (variant="avatar")
    - .planning/phases/04-proof-conversion/04-CONTEXT.md (D-05, D-06, D-07, D-21)
  </read_first>
  <action>
    Create `testimonial-card.tsx` (RSC, no `"use client"`) exporting
    `function TestimonialCard({ quote, name, handle, metric }: {...})`. Anatomy per D-07 (mirror the
    04-RESEARCH verbatim shape): an outer `<article className="rounded-[--radius-lg] border
    border-border bg-transparent p-6">` with the project's subtle white-5% inset boxShadow
    (`style={{ boxShadow: "rgba(255, 255, 255, 0.05) 0px 1px 0px 0px inset" }}`) — NO featured glow,
    NO oklch, NO coral (cold-brand LANDMINE / Pitfall 2). Inside: the `<blockquote>` quote in
    `text-foreground-secondary`; the result `metric` in `text-foreground` font-medium carrying
    `data-testid="testimonial-metric"` (the conversion lever — keep it, D-07/D-21); then an
    identity row = `<Placeholder variant="avatar" className="h-10 w-10" />` + name (`text-foreground`)
    + `@{handle}` (`text-foreground-muted`) carrying `data-testid="testimonial-handle"`. Emit the two
    `data-testid`s exactly as the 04-00 test queries them.

    Create `testimonials.tsx` (RSC) exporting `function Testimonials({ className }: { className?: string })`,
    mirroring `feature-blocks.tsx`: `import { StaggerReveal, StaggerRevealItem } from "@/components/motion"`
    (NAMED export — StaggerReveal.Item is the RSC-boundary landmine, Pitfall 1). A module-level
    `TESTIMONIALS` const of EXACTLY 3 entries (D-06 — static grid, NOT a carousel, NOT 6+) with
    drafted quotes + names + @handles + result metrics (Claude's discretion on copy per D-21;
    plausible swappable stand-ins like "+2.3M views", "3× my hook rate"). Map them through
    `<TestimonialCard>` inside `<StaggerRevealItem key={t.handle}>`, in
    `<StaggerReveal className="mt-12 grid grid-cols-1 gap-8 md:grid-cols-3">`. Add a sans-serif `<h2>`
    section heading (`text-3xl font-semibold text-foreground`, e.g. "What creators say") — sans the
    Newsreader serif (serif reserved to hero + band close-line).

    Token discipline: cream tokens only, never `text-white`, no hex, no glass/glow/blur, coral NOT
    used (no CTA in this section). Do NOT touch page.tsx or the barrel.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && npx vitest run src/components/marketing/proof/__tests__/testimonials.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/marketing/proof/__tests__/testimonials.test.tsx` exits 0 (PROOF-02 GREEN).
    - `testimonials.tsx` imports the NAMED `StaggerRevealItem` (`grep -q 'StaggerRevealItem' src/components/marketing/proof/testimonials.tsx`) and never uses `StaggerReveal.Item` (`grep -c 'StaggerReveal\.Item' ...` → 0).
    - Exactly 3 testimonial entries render → 3 `[data-variant="avatar"]`, 3 `data-testid="testimonial-handle"`, 3 `data-testid="testimonial-metric"`.
    - Neither file contains `"use client"` (`grep -c 'use client' <both files>` → 0).
    - testimonial-card.tsx imports nothing from `@/components/ui/testimonial-card` and uses no `oklch`/`border-accent`/`shadow-[...glow...]`/`text-white` (`grep -Eiv '^\s*(//|\*)' src/components/marketing/proof/testimonial-card.tsx | grep -Eiq 'ui/testimonial-card|oklch|border-accent|text-white' && echo VIOLATION || echo clean` → clean).
    - Exports `TestimonialCard` and `Testimonials`.
  </acceptance_criteria>
  <done>A bespoke flat-warm testimonial card + a static 3-card grid render with full D-07 anatomy; PROOF-02 test passes; both pure RSC, no cold-brand styling, named StaggerReveal export used.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Static presentational RSC components. No input, no auth, no network, no data fetch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | drafted testimonial copy/metrics | accept | Quotes/names/@handles/metrics are FICTIONAL placeholders (D-21) — no real PII or secrets by construction. Swapped for real assets post-launch (deferred). |
| T-04-01-02 | XSS | quote / metric string children | mitigate | Plain string children only; React escapes by default. No `dangerouslySetInnerHTML` anywhere in either file. |
| T-04-SC | Tampering | npm installs | accept | Zero package installs this plan — all primitives reused in-repo. |

No high-severity threats (04-RESEARCH § Security Domain — static, input-free surface).
</threat_model>

<verification>
- Both PROOF tests GREEN: `npx vitest run src/components/marketing/proof/`.
- No `"use client"` in any of the 3 files (pure RSC; the build gate stays `○ /`).
- Marquee region is `aria-hidden` (logos read once); 3 testimonial cards with full D-07 anatomy.
- Token discipline grep gates clean (no cold-brand tokens, no coral, no hex/glass/glow).
</verification>

<success_criteria>
- PROOF-01 + PROOF-02 RED tests turn GREEN.
- `SocialProofStrip`, `TestimonialCard`, `Testimonials` exported, pure RSC, flat-warm.
- page.tsx + the marketing barrel UNTOUCHED (04-05 owns the wiring).
</success_criteria>

<output>
Create `.planning/phases/04-proof-conversion/04-01-SUMMARY.md` when done.
</output>
