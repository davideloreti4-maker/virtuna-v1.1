---
phase: 04-proof-conversion
plan: 03
type: execute
wave: 1
depends_on: [04-00]
files_modified:
  - src/components/marketing/faq/faq-accordion.tsx
  - src/components/marketing/faq/faq.tsx
autonomous: true
requirements: [CONVERT-03]

must_haves:
  truths:
    - "An FAQ section answers 6 objection-busting creator questions in a single-open accordion"
    - "The accordion is keyboard-accessible (Radix-provided: arrow keys / Enter / Space / focus)"
    - "Only the accordion is a client island; the section wrapper + page root stay RSC (/ stays static)"
    - "The reused Radix accordion's cold-brand tokens are restyled to flat-warm at the call site"
  artifacts:
    - path: "src/components/marketing/faq/faq-accordion.tsx"
      provides: "CONVERT-03 — the lone Phase-4 client island; Radix single-open accordion, 6 items, flat-warm"
      exports: ["FaqAccordion"]
    - path: "src/components/marketing/faq/faq.tsx"
      provides: "CONVERT-03 — RSC section wrapper (heading) mounting the FaqAccordion island"
      exports: ["Faq"]
  key_links:
    - from: "faq.tsx"
      to: "faq-accordion.tsx"
      via: "RSC wrapper mounts the client island"
      pattern: "<FaqAccordion"
    - from: "faq-accordion.tsx"
      to: "ui/accordion.tsx"
      via: "Radix AccordionRoot type=single collapsible, cold tokens overridden via className"
      pattern: "type=\"single\""
---

<objective>
Build the FAQ section: a pure-RSC wrapper (`faq.tsx`) with a sans-serif heading that mounts the
ONE genuinely-interactive Phase-4 piece — a `"use client"` accordion island (`faq-accordion.tsx`)
reusing the Radix `ui/accordion.tsx` for keyboard a11y, restyled flat-warm at the call site, with
6 objection-busting Q&A and `type="single" collapsible` (one panel open at a time). Turn the
CONVERT-03 RED test from 04-00 GREEN. Keep the section wrapper RSC so page.tsx stays RSC and /
stays statically prerendered. Do NOT touch page.tsx (04-05).

Purpose: Single-open keeps focus and avoids a wall of text burying the CTA (D-16). Six answers
turn real buying objections into reasons to try (D-15, conversion-framed).
Output: 2 new component files under `src/components/marketing/faq/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-proof-conversion/04-CONTEXT.md
@.planning/phases/04-proof-conversion/04-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contracts extracted from in-repo sources this session. Use directly. -->

Radix accordion (src/components/ui/accordion.tsx) — VERIFIED, "use client", exports:
  AccordionRoot, AccordionItem, AccordionTrigger, AccordionContent
  - Root accepts Radix props incl. type="single" collapsible (one panel at a time + close-all). [Radix docs]
  - AccordionTrigger renders a real <button> (line 49) → screen.getAllByRole("button") counts the triggers.
  - COLD-BRAND tokens to OVERRIDE via className at the call site (cn()/tailwind-merge wins for conflicts):
      AccordionItem (L36):   "border-white/[0.06] bg-background-elevated/50" → "border-border bg-surface-elevated/50"
      AccordionTrigger (L52): "text-white ... hover:text-white/80"           → "text-foreground ... hover:text-foreground/80"
      AccordionContent (L73): "text-gray-400"                                 → "text-foreground-secondary"
      (the caret icon's text-white is internal; the content/trigger override carries the visible fix)
  - Note: prefer bg-surface-elevated (--color-charcoal-chip) over the cold bg-background-elevated
    (--color-charcoal-sidebar, the SIDEBAR shade) for consistency with the rest of the landing.

The RSC-wrapper-mounts-a-leaf idiom: src/components/marketing/story/how-it-works.tsx (RSC heading + mounts a leaf).

Flat-warm tokens: text-foreground / -secondary / -muted · bg-surface-elevated · border-border. No coral (no CTA here).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FAQ accordion client island — 6 items, single-open, flat-warm restyle (CONVERT-03)</name>
  <files>src/components/marketing/faq/faq-accordion.tsx</files>
  <read_first>
    - src/components/marketing/faq/__tests__/faq.test.tsx (the RED gate — 6 button triggers, single-open contract)
    - src/components/ui/accordion.tsx (the reused Radix structure + the EXACT cold-brand classNames to override — quoted in the interfaces block)
    - .planning/phases/04-proof-conversion/04-RESEARCH.md (§ Pattern 2 lines ~263-292 + § Pitfall 3 — the restyle-at-call-site approach + exact token map)
    - .planning/phases/04-proof-conversion/04-PATTERNS.md (§ faq-accordion.tsx — the assignment + the cold-brand LANDMINE)
    - .planning/phases/04-proof-conversion/04-CONTEXT.md (D-15 the 6 objection topics, D-16 single-open, D-17 reuse Radix)
  </read_first>
  <action>
    Create `src/components/marketing/faq/faq-accordion.tsx` with `"use client"` on line 1 (this is the
    LONE Phase-4 client island — Radix needs React context). Import the Radix parts:
    `import { AccordionRoot, AccordionItem, AccordionTrigger, AccordionContent } from "@/components/ui/accordion"`.
    Export `function FaqAccordion({ className }: { className?: string })`.

    A module-level `FAQ_ITEMS` const of EXACTLY 6 entries (D-15 — the objection set: accuracy
    "how does it know?", platforms "TikTok-only or more?", niche fit, data/privacy, free/price "is
    it free to try?", speed/how-it-works). Drafted conversion-framed answers (Claude's discretion on
    copy per D-21 — turn each objection into a reason to try; plain string answers, never
    `dangerouslySetInnerHTML`).

    Render `<AccordionRoot type="single" collapsible className={className}>` (D-16 — one panel open
    at a time + close-all). Map `FAQ_ITEMS` through `<AccordionItem key value={item.q}>` /
    `<AccordionTrigger>` / `<AccordionContent>`. RESTYLE the cold-brand tokens via `className`
    overrides at each part (do NOT re-implement the a11y — Radix owns keyboard nav, that is untouched):
    - `AccordionItem className="border-border bg-surface-elevated/50"`
    - `AccordionTrigger className="text-foreground hover:text-foreground/80"`
    - `AccordionContent className="text-foreground-secondary"`
    Use `bg-surface-elevated` (not the cold `bg-background-elevated`). No coral (no CTA here). No hex.

    Do NOT edit the shared `ui/accordion.tsx` primitive (a legacy route may depend on its cold
    defaults — RESEARCH option A, surgical call-site override). Do NOT touch page.tsx or the barrel.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && npx vitest run src/components/marketing/faq/__tests__/faq.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/marketing/faq/faq-accordion.tsx` has `"use client"` on line 1 and exports `FaqAccordion`.
    - `FAQ_ITEMS` has exactly 6 entries → 6 Radix `<button>` triggers render (`getAllByRole("button").length === 6`).
    - `AccordionRoot` carries `type="single"` and `collapsible` (`grep -q 'type="single"' ... && grep -q 'collapsible' ...`).
    - The cold tokens are overridden: no `text-white` / `text-gray-400` / `bg-background-elevated` appear in this file (`grep -Eiq 'text-white|text-gray-400|bg-background-elevated' src/components/marketing/faq/faq-accordion.tsx && echo VIOLATION || echo clean` → clean); `bg-surface-elevated`, `text-foreground`, `text-foreground-secondary` ARE used.
    - `src/components/ui/accordion.tsx` is NOT modified (it is not in this plan's files_modified).
    - No `dangerouslySetInnerHTML` (`grep -c dangerouslySetInnerHTML ...` → 0).
  </acceptance_criteria>
  <done>A flat-warm Radix single-open accordion island renders 6 objection-busting Q&A with keyboard a11y intact and cold tokens overridden at the call site.</done>
</task>

<task type="auto">
  <name>Task 2: FAQ RSC section wrapper mounting the island</name>
  <files>src/components/marketing/faq/faq.tsx</files>
  <read_first>
    - src/components/marketing/faq/faq-accordion.tsx (the island this wraps — built in Task 1)
    - src/components/marketing/story/how-it-works.tsx (the RSC-section-mounts-a-leaf idiom: `{ className }` prop, sans-serif `<h2>` heading, no "use client")
    - .planning/phases/04-proof-conversion/04-RESEARCH.md (§ Pattern 2 — the faq.tsx RSC wrapper shape)
    - .planning/phases/04-proof-conversion/04-PATTERNS.md (§ faq.tsx)
  </read_first>
  <action>
    Create `src/components/marketing/faq/faq.tsx` as a PURE RSC (NO `"use client"`) exporting
    `function Faq({ className }: { className?: string })`. It renders a sans-serif `<h2>` heading
    (`text-3xl font-semibold text-foreground`, e.g. "Questions, answered" — sans the Newsreader serif,
    reserved to hero + band) + a one-line cream-secondary subhead (optional), then mounts the ONE
    client island: `<FaqAccordion className="mt-10" />`. This keeps the page root + the section
    heading RSC; only the accordion subtree is client (Pitfall 5 — the static-build gate `○ /`).

    Do NOT touch page.tsx or the barrel.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && npx vitest run src/components/marketing/faq/__tests__/faq.test.tsx 2>&1 | tail -5 && grep -c 'use client' src/components/marketing/faq/faq.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/marketing/faq/faq.tsx` exists, exports `Faq`, and contains NO `"use client"` (`grep -c 'use client' src/components/marketing/faq/faq.tsx` → 0).
    - `faq.tsx` mounts `<FaqAccordion>` (`grep -q 'FaqAccordion' src/components/marketing/faq/faq.tsx`).
    - `npx vitest run src/components/marketing/faq/__tests__/faq.test.tsx` exits 0 (CONVERT-03 GREEN — the test imports `{ Faq }` which renders the island).
    - The heading is sans the serif (no `var(--font-serif)` / `font-serif` in faq.tsx).
  </acceptance_criteria>
  <done>A pure-RSC FAQ wrapper renders a sans-serif heading and mounts the accordion island; the page-mountable section stays RSC; CONVERT-03 test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC page root → client island | The FAQ accordion is the lone `"use client"` boundary. It carries no input, no network, no data — purely expand/collapse local state via Radix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | XSS | FAQ answer string children | mitigate | Plain string answers only; React escapes by default. No `dangerouslySetInnerHTML` anywhere (verified by grep gate). |
| T-04-03-02 | Denial of Service / build-gate break | client leak to page root | mitigate | Only `faq-accordion.tsx` is `"use client"`; `faq.tsx` stays RSC so `/` stays `○` static (verified in 04-05 build gate). |
| T-04-03-03 | Information Disclosure | drafted FAQ copy | accept | Answers are fictional conversion copy (D-21) — no real PII/secrets. |
| T-04-SC | Tampering | npm installs | accept | Zero installs — Radix accordion reused in-repo. |

No high-severity threats (04-RESEARCH § Security Domain).
</threat_model>

<verification>
- CONVERT-03 test GREEN: `npx vitest run src/components/marketing/faq/`.
- `faq.tsx` is RSC (no `"use client"`); only `faq-accordion.tsx` is the island.
- Cold-brand tokens overridden (no `text-white`/`text-gray-400`/`bg-background-elevated`); flat-warm tokens used.
- `ui/accordion.tsx` shared primitive untouched.
</verification>

<success_criteria>
- CONVERT-03 RED test turns GREEN.
- `FaqAccordion` (client island) + `Faq` (RSC wrapper) exported.
- page.tsx + the barrel UNTOUCHED (04-05 owns the wiring).
</success_criteria>

<output>
Create `.planning/phases/04-proof-conversion/04-03-SUMMARY.md` when done.
</output>
