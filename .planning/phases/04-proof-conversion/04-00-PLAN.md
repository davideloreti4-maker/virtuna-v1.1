---
phase: 04-proof-conversion
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/marketing/proof/__tests__/social-proof-strip.test.tsx
  - src/components/marketing/proof/__tests__/testimonials.test.tsx
  - src/components/marketing/pricing/__tests__/pricing-teaser.test.tsx
  - src/components/marketing/faq/__tests__/faq.test.tsx
  - src/components/marketing/cta/__tests__/final-cta-band.test.tsx
  - src/components/marketing/__tests__/page-order.test.tsx
autonomous: true
requirements: [PROOF-01, PROOF-02, CONVERT-01, CONVERT-02, CONVERT-03]
nyquist_role: wave-0-scaffold

must_haves:
  truths:
    - "Five RED section test files exist, one per Phase-4 section, each asserting its requirement's behavior"
    - "Each test file is RED-by-design (module-not-found until its component is built in Wave 1)"
    - "A page-order test asserts D-18 section order and NAV_LINKS.length === 5 (D-19)"
  artifacts:
    - path: "src/components/marketing/proof/__tests__/social-proof-strip.test.tsx"
      provides: "PROOF-01 gate — trust stat + logo marquee count + marquee a11y"
      contains: "data-variant=\"logo\""
    - path: "src/components/marketing/proof/__tests__/testimonials.test.tsx"
      provides: "PROOF-02 gate — 3 cards, full D-07 anatomy"
      contains: "data-variant=\"avatar\""
    - path: "src/components/marketing/pricing/__tests__/pricing-teaser.test.tsx"
      provides: "CONVERT-01 gate — 2 tiers, Most-popular, CTAs→/signup, bullet count"
      contains: "Most popular"
    - path: "src/components/marketing/faq/__tests__/faq.test.tsx"
      provides: "CONVERT-03 gate — 6 items, single-open, button triggers"
      contains: "getAllByRole"
    - path: "src/components/marketing/cta/__tests__/final-cta-band.test.tsx"
      provides: "CONVERT-02 gate — serif close-line, CTA→/signup, gauge echo"
      contains: "/signup"
    - path: "src/components/marketing/__tests__/page-order.test.tsx"
      provides: "Cross gate — D-18 order + NAV_LINKS.length === 5 (D-19)"
      contains: "NAV_LINKS"
  key_links:
    - from: "each test file"
      to: "the not-yet-built component under ../<name>"
      via: "import { Component } from \"../<name>\""
      pattern: "import .* from \"\\.\\./"
---

<objective>
Lay the Nyquist Wave-0 RED test scaffold for Phase 4, mirroring the `03-00` pattern. Create
exactly one failing test file per Phase-4 section (social-proof strip, testimonials, pricing
teaser, FAQ, final CTA band) plus a page-order/NAV-unchanged cross test. Every file is
RED-by-design — its import of the not-yet-built component fails with module-not-found, which IS
the Wave-0 success signal. Wave 1 turns each file GREEN by building the component it imports.

Purpose: Establish the automated feedback contract before any production code, so every Wave-1
task has a real `<automated>` verify command (the Nyquist sampling rule). Bind each phase
requirement to its check at the exact commands 04-VALIDATION.md specifies.
Output: 6 RED test files under the new `proof/ pricing/ faq/ cta/` `__tests__/` subdirs +
`marketing/__tests__/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-proof-conversion/04-VALIDATION.md
@.planning/phases/04-proof-conversion/04-PATTERNS.md

<interfaces>
<!-- Contracts the tests assert against. Extracted from in-repo sources this session. -->
<!-- The Wave-1 components MUST satisfy these; the tests encode them. -->

Test framework (VERIFIED in `src/components/marketing/story/__tests__/feature-blocks.test.tsx`):
- Line 1 of EVERY test file: `/** @vitest-environment happy-dom */`
- `import { describe, it, expect } from "vitest";`
- `import { render, screen } from "@testing-library/react";`
- Imports the component from a RELATIVE path (`../social-proof-strip`, etc.) — RED until built.

Placeholder count hook (VERIFIED `src/components/marketing/placeholder.tsx` line 121):
- `data-variant="logo"` on each logo placeholder; `data-variant="avatar"` on each avatar.
- query: `container.querySelectorAll('[data-variant="logo"]')` / `'[data-variant="avatar"]'`

CTA target (VERIFIED `src/lib/routes.ts`): `SIGNUP_URL = "/signup"`.
- assert: `screen.getAllByRole("link").some(a => a.getAttribute("href") === "/signup")`

Nav lock (VERIFIED `src/lib/nav.ts`): `NAV_LINKS` is exactly 5 entries (D-19).
- import `{ NAV_LINKS } from "@/lib/nav"`; assert `NAV_LINKS.length === 5`.

Radix accordion triggers are `<button>` (VERIFIED `src/components/ui/accordion.tsx` line 49) →
`screen.getAllByRole("button")` counts the 6 FAQ triggers (CONVERT-03 keyboard a11y proxy).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the 5 section RED test files</name>
  <files>src/components/marketing/proof/__tests__/social-proof-strip.test.tsx, src/components/marketing/proof/__tests__/testimonials.test.tsx, src/components/marketing/pricing/__tests__/pricing-teaser.test.tsx, src/components/marketing/faq/__tests__/faq.test.tsx, src/components/marketing/cta/__tests__/final-cta-band.test.tsx</files>
  <read_first>
    - src/components/marketing/story/__tests__/feature-blocks.test.tsx (the EXACT scaffold template — copy the pragma/imports/describe shape verbatim)
    - .planning/phases/04-proof-conversion/04-VALIDATION.md (the per-requirement automated commands + Wave-0 list to mirror)
    - .planning/phases/04-proof-conversion/04-RESEARCH.md (§ "Test idioms to mirror" lines ~505-522 — the exact assertion idioms)
    - .planning/phases/04-proof-conversion/04-PATTERNS.md (§ "04-00 Nyquist test files" — the per-section assertions)
    - src/components/marketing/placeholder.tsx (confirm `data-variant` hook line 121, logo label suppression line 167)
    - src/lib/routes.ts (SIGNUP_URL = "/signup")
  </read_first>
  <action>
    Create five RED-by-design test files, each importing its component from a relative path that
    does NOT yet exist (module-not-found = the Wave-0 signal). Mirror the
    `story/__tests__/feature-blocks.test.tsx` shape exactly: `/** @vitest-environment happy-dom */`
    on line 1, then vitest + testing-library imports, then a `describe` per requirement. Assert
    ONLY against stable tokens / `data-variant` counts / roles / hrefs — NEVER full drafted
    sentences (all copy is swappable per D-21). Per file:

    `proof/__tests__/social-proof-strip.test.tsx` imports `{ SocialProofStrip } from "../social-proof-strip"`.
    Assert (PROOF-01, D-02/D-03/D-04): a trust stat text node matching `/2,?000\+? creators/i`
    (tolerant of copy tweaks per D-04); at least N (use `>= 4`) logo placeholders via
    `container.querySelectorAll('[data-variant="logo"]')`; and the marquee a11y fix (Pitfall 4) —
    assert the marquee region (or its duplicated copies) is hidden from the a11y tree by
    `querySelector('[aria-hidden="true"]')` resolving to a node that contains logo placeholders
    (the strip wraps the `<Marquee>` in `aria-hidden`).

    `proof/__tests__/testimonials.test.tsx` imports `{ Testimonials } from "../testimonials"`.
    Assert (PROOF-02, D-06/D-07): exactly 3 avatar placeholders
    (`querySelectorAll('[data-variant="avatar"]').length === 3`); each card carries an `@handle`
    (assert `getAllByText(/^@/).length >= 3` OR a `data-testid="testimonial-handle"` count of 3);
    and a result metric per card (assert 3 metric nodes via a stable `data-testid="testimonial-metric"`
    OR `getAllByText(/views|hook|rate|×|M\b/i).length >= 3`). Prefer a `data-testid` count for
    handle + metric so the assertion is copy-resilient — the Wave-1 component MUST emit those
    test ids.

    `pricing/__tests__/pricing-teaser.test.tsx` imports `{ PricingTeaser } from "../pricing-teaser"`.
    Assert (CONVERT-01, D-08/D-09/D-10): two tier names present (`getByText(/starter/i)` +
    `getByText(/pro/i)`); the Pro highlight label `getByText(/most popular/i)`; both CTAs route to
    `/signup` (`getAllByRole("link").filter(a => a.getAttribute("href") === "/signup").length >= 2`);
    and each card carries 3-4 bullets (assert via a stable `data-testid="pricing-bullet"` count
    grouped per card, or assert total bullets between 6 and 8 across the two cards). Add the D-10
    static guard as a SEPARATE import-absence test: read the built component source is out of scope
    for a unit test, so instead assert the rendered output contains no checkout affordance — there
    is exactly one CTA target (`/signup`) and no other off-site link. (The hard import-absence guard
    is the grep gate in Task 2 + the build gate in 04-05.)

    `faq/__tests__/faq.test.tsx` imports `{ Faq } from "../faq"`.
    Assert (CONVERT-03, D-15/D-16): exactly 6 accordion triggers via
    `getAllByRole("button").length === 6` (Radix triggers are `<button>`); single-open is a Radix
    `type="single"` behavior — assert the wiring proxy by confirming the rendered root carries the
    Radix single-collapsible contract is hard in a unit test, so assert instead that there are 6
    triggers AND 6 region/content panels exist in the DOM (`querySelectorAll('[data-state]').length >= 6`).
    Single-open keyboard behavior itself is in 04-VALIDATION Manual-Only (live UAT).

    `cta/__tests__/final-cta-band.test.tsx` imports `{ FinalCtaBand } from "../final-cta-band"`.
    Assert (CONVERT-02, D-13/D-14): one CTA → `/signup`
    (`getAllByRole("link").some(a => a.getAttribute("href") === "/signup")`); the serif close-line
    present (assert a node carrying the serif class — the band wraps the close-line in
    `font-[family-name:var(--font-serif)]` or `data-testid="cta-close-line"`; assert the
    `data-testid` for resilience); and the gauge echo present via the ScoreGaugeSkeleton's stable
    accessible name `getByRole("img", { name: /virality score/i })` (VERIFIED aria-label in
    score-gauge-skeleton.tsx line 39).

    Do NOT place implementation code in these files beyond the assertions. No fenced impl. Each
    file is short, RED, and copy-resilient.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && npx vitest run src/components/marketing/proof/ src/components/marketing/pricing/ src/components/marketing/faq/ src/components/marketing/cta/ 2>&1 | grep -Eiq "cannot find module|failed to resolve|no test files|error" && echo "RED-as-expected" || echo "UNEXPECTED-GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist at the exact `files` paths, each with `/** @vitest-environment happy-dom */` on line 1 (`head -1 <file>` shows the pragma for every file).
    - Each file imports its component from the relative `../<name>` path (RED — module-not-found).
    - `npx vitest run` over the four section dirs FAILS with module-resolution errors (the RED signal), NOT with assertion failures inside a resolved module.
    - No production component file is created in this task (Wave 1 owns those).
    - Assertions use stable tokens / `data-variant` counts / roles / hrefs / `data-testid`, never full drafted sentences.
  </acceptance_criteria>
  <done>Five RED section test files exist; the scoped vitest run reports module-not-found (RED-by-design), confirming the scaffold is in place and waiting on Wave 1.</done>
</task>

<task type="auto">
  <name>Task 2: Create the page-order / NAV-unchanged cross test</name>
  <files>src/components/marketing/__tests__/page-order.test.tsx</files>
  <read_first>
    - src/app/(marketing)/page.tsx (the current section order + the #pricing/#faq stubs Wave-1/04-05 fill)
    - src/lib/nav.ts (NAV_LINKS — the locked 5-link set, D-19)
    - src/components/layout/__tests__/footer.test.tsx (the existing anchor-coverage idiom to mirror; confirms no page.test.tsx exists)
    - .planning/phases/04-proof-conversion/04-CONTEXT.md (§ D-18 final section order, § D-19 nav lock)
  </read_first>
  <action>
    Create `marketing/__tests__/page-order.test.tsx`. RESEARCH Open-Q1 confirmed: no `page.test.tsx`
    exists; the footer test already covers all 5 anchors. This file adds the two cross gates that
    no section test owns:

    (1) NAV-unchanged (D-19): `import { NAV_LINKS } from "@/lib/nav"`; assert `NAV_LINKS.length === 5`
    and that its hrefs are exactly `["#how-it-works","#the-simulation","#features","#pricing","#faq"]`
    (assert the set, order-tolerant on the labels but href-set-exact). This fails RED if Wave 1/04-05
    ever adds `#proof`/`#testimonials`/`#cta` to the nav.

    (2) D-18 section order: render the assembled `HomePage` from `src/app/(marketing)/page.tsx`
    (`import HomePage from "@/app/(marketing)/page"`). This is RED now (the new sections are not
    mounted yet) and turns GREEN after 04-05 wires them. Assert the D-18 order by reading the DOM
    order of the section anchor ids + the new sections' stable ids. Use a single ordered query:
    collect the `id` of every `<section>` (or the `data-section` of the new full-bleed band) in
    document order via `container.querySelectorAll("section[id], section[data-section]")`, map to
    their ids, and assert the resulting array, filtered to the known set, equals the D-18 order:
    `hero` → (strip section id, e.g. `social-proof`) → `how-it-works` → `the-simulation` →
    `features` → (testimonials id, e.g. `testimonials`) → `pricing` → `faq` → (CTA band marker).
    Because the strip rides high under hero and the CTA band is full-bleed (may not carry a max-w
    `id`), assert the new sections via the stable ids/`data-section` the Wave-1 components emit
    (define them in this test as the contract: strip section id `social-proof`, testimonials id
    `testimonials`, band section `data-section="final-cta"`). The 04-05 integration MUST use those
    ids/markers.

    Keep assertions resilient: assert the RELATIVE order of the known ids (strip before how-it-works;
    testimonials after features and before pricing; band after faq and last before footer), not
    absolute indices, so unrelated DOM changes don't break it.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && npx vitest run src/components/marketing/__tests__/page-order.test.tsx 2>&1 | grep -Eiq "fail|error|cannot|expect" && echo "RED-as-expected (sections not yet mounted)" || echo "check-output"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/marketing/__tests__/page-order.test.tsx` exists with the happy-dom pragma on line 1.
    - The NAV-unchanged block imports `NAV_LINKS` from `@/lib/nav` and asserts `length === 5` + the exact href set (passes NOW — D-19 holds).
    - The D-18 order block imports the page default export and asserts the RELATIVE order of `social-proof`, `how-it-works`, `the-simulation`, `features`, `testimonials`, `pricing`, `faq`, `final-cta` (RED now; GREEN after 04-05).
    - The test names the stable ids/markers the Wave-1 + 04-05 components MUST emit (`social-proof`, `testimonials`, `data-section="final-cta"`), making them the integration contract.
  </acceptance_criteria>
  <done>The cross test exists; NAV-unchanged passes immediately, the D-18 order block is RED until 04-05 mounts the new sections in order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Wave 0 creates test files only — no runtime surface, no input, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | Information Disclosure | test fixtures / drafted assertions | accept | Tests assert stable tokens only; no real PII/secrets in fixtures (drafted placeholder copy is fictional per D-21). |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Phase 4 installs ZERO packages (04-RESEARCH § Package Legitimacy Audit — empty by construction). No legitimacy gate triggered. |

No high-severity threats. The phase is a static, input-free marketing surface (04-RESEARCH § Security Domain — most ASVS categories N/A).
</threat_model>

<verification>
- `head -1` of every new test file shows `/** @vitest-environment happy-dom */`.
- Scoped `npx vitest run` over the four section dirs reports module-not-found (RED-by-design) — NOT in-module assertion failures.
- `page-order.test.tsx` NAV-unchanged block passes (D-19 holds today); the D-18 order block is RED (sections not yet mounted).
- Full suite baseline unaffected: `npm test` still reports the prior baseline plus the new RED files (RED is expected for Wave 0).
</verification>

<success_criteria>
- 6 RED test files exist at the exact `files_modified` paths.
- Each section test binds its requirement to the exact automated command in 04-VALIDATION.md.
- The page-order test encodes D-18 order + D-19 NAV lock and names the stable section ids/markers Wave 1 + 04-05 must emit.
- No production component is created in Wave 0.
</success_criteria>

<output>
Create `.planning/phases/04-proof-conversion/04-00-SUMMARY.md` when done.
</output>
