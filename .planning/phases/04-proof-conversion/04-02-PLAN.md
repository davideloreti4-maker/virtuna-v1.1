---
phase: 04-proof-conversion
plan: 02
type: execute
wave: 1
depends_on: [04-00]
files_modified:
  - src/components/marketing/pricing/pricing-card.tsx
  - src/components/marketing/pricing/pricing-teaser.tsx
autonomous: true
requirements: [CONVERT-01]

must_haves:
  truths:
    - "A pricing teaser shows two tier cards (Starter + Pro) side by side, Pro highlighted Most popular"
    - "Starter is framed Free to start; Pro shows a placeholder $/mo + a 7-day free trial badge"
    - "Each card has 3-4 benefit bullets and a Try it free CTA routing to /signup"
    - "No Supabase/Whop/CheckoutModal import — pure RSC + Link; / stays statically prerendered"
  artifacts:
    - path: "src/components/marketing/pricing/pricing-card.tsx"
      provides: "CONVERT-01 — bespoke flat-warm tier card (name, price, badge, bullets, CTA→/signup)"
      exports: ["PricingCard"]
    - path: "src/components/marketing/pricing/pricing-teaser.tsx"
      provides: "CONVERT-01 — 2-up tier grid (Starter + Pro, Pro highlighted)"
      exports: ["PricingTeaser"]
  key_links:
    - from: "pricing-card.tsx"
      to: "ui/button.tsx + ui/badge.tsx + src/lib/routes.ts"
      via: "Button asChild → Link href={SIGNUP_URL}; Badge for Most popular / trial"
      pattern: "href=\\{SIGNUP_URL\\}"
    - from: "pricing-teaser.tsx"
      to: "pricing-card.tsx"
      via: "map TIERS through PricingCard, Pro highlighted"
      pattern: "TIERS\\.map"
---

<objective>
Build the pricing teaser as a bespoke flat-warm pure-RSC component: two tier cards (Starter +
Pro) side by side, Pro highlighted "Most popular", each with 3-4 benefit bullets, a placeholder
price, the D-20 risk-reducer microcopy, and a "Try it free" CTA → SIGNUP_URL. Turn the CONVERT-01
RED test from 04-00 GREEN. Mine the real tier MODEL only (D-11) — import NONE of the real
`pricing-section.tsx` machinery (Supabase/Whop/CheckoutModal — D-10). Do NOT touch page.tsx (04-05).

Purpose: A conversion teaser, not the real `/pricing` route. Free entry removes friction (D-09);
the Pro price anchors value; both CTAs route to signup (D-10).
Output: 2 new component files under `src/components/marketing/pricing/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-proof-conversion/04-CONTEXT.md
@.planning/phases/04-proof-conversion/04-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contracts extracted from in-repo sources this session. Use directly. -->

Button (src/components/ui/button.tsx) — VERIFIED:
  <Button asChild variant="primary" size="lg"><Link href={SIGNUP_URL}>Try it free</Link></Button>
  - variant="primary" = coral (CTA-only); asChild requires EXACTLY ONE element child (React.Children.only).
  - "use client" lives in button.tsx itself, but importing/rendering it from an RSC is fine (it's a leaf).

Badge (src/components/ui/badge.tsx) — VERIFIED:
  <Badge variant="accent" size="sm">Most popular</Badge>      // coral: bg-accent/15 text-accent border-accent/25
  <Badge variant="secondary" size="sm">7-day free trial</Badge> // neutral: bg-surface-elevated

SIGNUP_URL (src/lib/routes.ts) = "/signup". Link from "next/link".

Real tier MODEL to MIRROR (mine the shape, NEVER import — D-10/D-11):
  Starter (real $19/mo, 5 sims/mo, Basic trend) → teaser reframe "Free to start", ~3-4 bullets:
    5 simulations/mo · core score + the why · basic trend read
  Pro (real $49/mo, unlimited, advanced + audience insights/referral/calendar/priority/export; 7-day trial)
    → teaser placeholder "$19/mo" (swappable) + "7-day free trial" badge, ~3-4 bullets:
    unlimited simulations · advanced insights · audience deep-dive · priority support
  FORBIDDEN imports into the teaser (D-10): createClient (Supabase), useSubscription, CheckoutModal,
    hasAccessToTier, WHOP_PRODUCT_IDS — all live in the real /pricing route only.

Flat-warm tokens: text-foreground / -secondary / -muted · bg-transparent · border-border · --radius-lg.
  Coral (--color-accent) ONLY on the CTA + the Most-popular badge. Pro highlight = token-legal accent
  ring/border emphasis, NEVER a glow.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bespoke flat-warm pricing card (name, price, badge, bullets, CTA→/signup)</name>
  <files>src/components/marketing/pricing/pricing-card.tsx</files>
  <behavior>
    - Renders tier name + price; an optional Badge (Most popular / 7-day free trial); 3-4 bullet rows (data-testid="pricing-bullet"); one CTA <Link href="/signup"> "Try it free".
    - The D-20 risk-reducer microcopy ("Free to start — no credit card") renders near the CTA.
    - highlighted prop applies a token-legal accent border/ring emphasis (no glow).
    - Pure RSC (no "use client").
  </behavior>
  <read_first>
    - src/components/marketing/pricing/__tests__/pricing-teaser.test.tsx (the RED gate — match its bullet-count data-testid, Most-popular text, and /signup href assertions)
    - .planning/phases/04-proof-conversion/04-PATTERNS.md (§ pricing-card.tsx — the Badge + Button asChild excerpts + the MODEL reference + the forbidden-imports LANDMINE)
    - .planning/phases/04-proof-conversion/04-RESEARCH.md (§ "Reference-only: the REAL tier model" lines ~449-464 — mine the shape only)
    - src/components/ui/button.tsx (asChild → Slot, single-child rule, variant="primary"=coral)
    - src/components/ui/badge.tsx (variant accent/secondary, size sm/md)
    - src/lib/routes.ts (SIGNUP_URL)
    - src/app/(marketing)/pricing/pricing-section.tsx (the ANTI-analog — confirm the forbidden imports at its head; DO NOT import any of it)
    - .planning/phases/04-proof-conversion/04-CONTEXT.md (D-08, D-09, D-10, D-11, D-20)
  </read_first>
  <action>
    Create `src/components/marketing/pricing/pricing-card.tsx` (PURE RSC, no `"use client"`) exporting
    `function PricingCard({ name, price, priceSuffix, badge, badgeVariant, highlighted, bullets, ctaLabel }: {...})`.
    Surface: the same flat-warm card idiom as the testimonial card — `rounded-[--radius-lg] border
    border-border bg-transparent p-6` (or p-8) + the subtle white-5% inset boxShadow. NO glow, NO
    oklch, NO `text-white`. When `highlighted` (Pro), emphasize with a TOKEN-LEGAL accent border/ring
    (e.g. `border-accent/25` or a `ring-1 ring-accent/20`) — NEVER a `shadow-[...glow...]` or blur.

    Render: tier `name` (`text-foreground`), the `price` + suffix (placeholder $/mo for Pro per D-09 —
    swappable; "Free to start" for Starter), an optional `<Badge>` (`variant="accent"` for "Most
    popular" on Pro; `variant="secondary"` for the "7-day free trial" pill), then 3-4 benefit bullets
    each carrying `data-testid="pricing-bullet"` (the 04-00 test counts these; use a check/dot glyph
    that is decorative `aria-hidden`). Below the bullets: the CTA via
    `<Button asChild variant="primary" size="lg"><Link href={SIGNUP_URL}>{ctaLabel}</Link></Button>`
    (coral, the lone accent CTA; `asChild` wraps EXACTLY ONE `<Link>`), and the D-20 risk-reducer
    microcopy "Free to start — no credit card" in `text-foreground-muted` near the CTA.

    LANDMINE (Pitfall 2 + D-10): import NOTHING from `src/app/(marketing)/pricing/pricing-section.tsx`
    or `src/lib/whop/*` or `src/lib/supabase/*` or `@/components/app/checkout-modal` or
    `@/hooks/use-subscription`. Mine the tier MODEL (shape/bullets) only. The card is link-only.

    Do NOT touch page.tsx or the barrel.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && grep -Eiq 'pricing-section|whop|supabase|checkout-modal|use-subscription' src/components/marketing/pricing/pricing-card.tsx && echo FORBIDDEN-IMPORT || echo clean</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/marketing/pricing/pricing-card.tsx` exists, exports `PricingCard`, contains no `"use client"` (`grep -c 'use client' ...` → 0).
    - No forbidden import: `grep -Eiq 'pricing-section|whop|supabase|checkout-modal|use-subscription'` over the file → no match (prints `clean`).
    - The CTA is `<Button asChild ...><Link href={SIGNUP_URL}>...` (single child) and renders an `href="/signup"` anchor.
    - 3-4 `data-testid="pricing-bullet"` rows render; the D-20 microcopy text ("no credit card") is present.
    - No `text-white`, no `oklch`, no `shadow-[...glow...]`, no hex (`grep -Eiv '^\s*(//|\*)' src/components/marketing/pricing/pricing-card.tsx | grep -Eiq 'text-white|oklch|drop-shadow|backdrop-blur|#[0-9a-f]{6}' && echo VIOLATION || echo clean` → clean). Coral only on the CTA + Most-popular badge.
  </acceptance_criteria>
  <done>A bespoke flat-warm tier card renders name + price + optional badge + 3-4 bullets + a /signup CTA + the risk-reducer microcopy; no checkout machinery; pure RSC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pricing teaser — 2-up tier grid (Starter + Pro, Pro highlighted) (CONVERT-01)</name>
  <files>src/components/marketing/pricing/pricing-teaser.tsx</files>
  <behavior>
    - Renders exactly 2 PricingCards in a grid (grid-cols-1 md:grid-cols-2): Starter (Free to start) + Pro (highlighted, Most popular, placeholder $/mo, 7-day free trial badge).
    - Both cards' CTAs route to /signup → getAllByRole("link") has >= 2 hrefs === "/signup".
    - Pure RSC (no "use client").
  </behavior>
  <read_first>
    - src/components/marketing/pricing/__tests__/pricing-teaser.test.tsx (the RED gate — Starter/Pro names, Most-popular, >=2 /signup links, bullet count)
    - src/components/marketing/story/feature-blocks.tsx (lines 78-104 — the RSC section + module-level const + grid map idiom)
    - .planning/phases/04-proof-conversion/04-PATTERNS.md (§ pricing-teaser.tsx + the static-build-gate LANDMINE)
    - .planning/phases/04-proof-conversion/04-CONTEXT.md (D-08, D-09, D-11, D-18, D-20)
  </read_first>
  <action>
    Create `src/components/marketing/pricing/pricing-teaser.tsx` (PURE RSC) exporting
    `function PricingTeaser({ className }: { className?: string })`. A module-level `TIERS` const of
    EXACTLY 2 entries built from the mined model (D-09/D-11):
    - Starter: `price: "Free"`, suffix "to start", `bullets: [5 simulations/mo, core score + the why,
      basic trend read]` (3-4), `ctaLabel: "Try it free"`, no Most-popular badge (optionally a small
      neutral badge).
    - Pro: `price: "$19"` (placeholder, swappable per D-09), suffix "/mo", `badge: "Most popular"`
      (`badgeVariant="accent"`) + a secondary "7-day free trial" pill, `highlighted: true`,
      `bullets: [unlimited simulations, advanced insights, audience deep-dive, priority support]`
      (3-4), `ctaLabel: "Try it free"`.
    Map `TIERS` through `<PricingCard>` in `<div className="mt-12 grid grid-cols-1 gap-8 md:grid-cols-2">`
    (D-08 — two cards side by side). A `StaggerReveal` leaf is optional (calm, 2 cards) — if used,
    import the NAMED `StaggerRevealItem` (Pitfall 1). Add a sans-serif `<h2>` heading
    (`text-3xl font-semibold text-foreground`, e.g. "Simple pricing") — sans the serif.

    LANDMINE (Pitfall 5): NO `"use client"` at this section root — the teaser is pure RSC + `<Link>`.
    A leaked client directive flips `/` from `○` static to dynamic (the standing build gate).

    Do NOT touch page.tsx or the barrel.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && npx vitest run src/components/marketing/pricing/__tests__/pricing-teaser.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/marketing/pricing/__tests__/pricing-teaser.test.tsx` exits 0 (CONVERT-01 GREEN).
    - Renders exactly 2 tier cards; Starter + Pro names present; "Most popular" present on Pro; `>= 2` links with `href="/signup"`.
    - Neither pricing file contains `"use client"` (`grep -c 'use client' src/components/marketing/pricing/*.tsx` → 0).
    - 6-8 total `data-testid="pricing-bullet"` rows (3-4 per card).
    - Exports `PricingTeaser`.
  </acceptance_criteria>
  <done>The pricing teaser renders Starter + Pro side by side, Pro highlighted Most popular with a placeholder price + trial badge, both CTAs → /signup; CONVERT-01 test passes; pure RSC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| marketing page → app signup | Outbound `<Link href="/signup">` only. The hardcoded internal `SIGNUP_URL` constant — no user-controlled redirect param. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | CTA redirect target | mitigate | CTA target is the hardcoded internal constant `SIGNUP_URL = "/signup"` (`src/lib/routes.ts`) — no user input, no open-redirect surface. |
| T-04-02-02 | Elevation of Privilege / heavy-client leak | accidental checkout/auth import | mitigate | D-10 forbids Supabase/Whop/CheckoutModal imports; grep gate (Task 1 verify) + the 04-05 build gate (`○ /`) enforce absence. Teaser is link-only. |
| T-04-02-03 | Information Disclosure | placeholder price/bullets | accept | Price + bullets are fictional swappable placeholders (D-21) — no real pricing logic or secrets. |
| T-04-SC | Tampering | npm installs | accept | Zero installs — Button/Badge/Link reused in-repo. |

No high-severity threats (04-RESEARCH § Security Domain).
</threat_model>

<verification>
- CONVERT-01 test GREEN: `npx vitest run src/components/marketing/pricing/`.
- Forbidden-import grep clean (no Supabase/Whop/CheckoutModal — D-10).
- No `"use client"` in either file (build gate `○ /` preserved).
- Coral confined to the CTA + Most-popular badge; no cold-brand/glow tokens.
</verification>

<success_criteria>
- CONVERT-01 RED test turns GREEN.
- `PricingCard` + `PricingTeaser` exported, pure RSC, flat-warm, link-only.
- page.tsx + the barrel UNTOUCHED (04-05 owns the wiring).
</success_criteria>

<output>
Create `.planning/phases/04-proof-conversion/04-02-SUMMARY.md` when done.
</output>
