---
phase: 04-proof-conversion
plan: 04
type: execute
wave: 1
depends_on: [04-00]
files_modified:
  - src/components/marketing/cta/final-cta-band.tsx
autonomous: true
requirements: [CONVERT-02]

must_haves:
  truths:
    - "A final full-width CTA band renders a Newsreader-serif voice close-line"
    - "The band carries one dominant Try it free CTA routing to /signup + the risk-reducer microcopy"
    - "The lone visual flourish is a subtle muted ScoreGaugeSkeleton echo (no new bespoke visual)"
    - "The band's warm radial stays flat-matte-legal (a low-alpha tone-step, never a coral glow); it is pure RSC"
  artifacts:
    - path: "src/components/marketing/cta/final-cta-band.tsx"
      provides: "CONVERT-02 — full-bleed warm band: serif close-line + CTA→/signup + gauge echo"
      exports: ["FinalCtaBand"]
  key_links:
    - from: "final-cta-band.tsx"
      to: "ui/button.tsx + src/lib/routes.ts"
      via: "Button asChild → Link href={SIGNUP_URL}"
      pattern: "href=\\{SIGNUP_URL\\}"
    - from: "final-cta-band.tsx"
      to: "story/skeletons/score-gauge-skeleton.tsx"
      via: "ScoreGaugeSkeleton rendered small/muted (D-14)"
      pattern: "ScoreGaugeSkeleton"
---

<objective>
Build the final full-width CTA band — the closing ask. A pure-RSC full-bleed band carrying a
Newsreader-serif voice close-line (the landing's serif voice recurs here, D-13 — Phase 4's single
serif moment), one dominant coral "Try it free" CTA → SIGNUP_URL, the D-20 risk-reducer microcopy,
and a subtle muted ScoreGaugeSkeleton echo (D-14 — reuse the Phase-3 primitive, NOT a new visual).
The warm radial "seat" stays flat-matte-legal (a low-alpha tone-step, never a glow). Turn the
CONVERT-02 RED test from 04-00 GREEN. Do NOT touch page.tsx (04-05).

Purpose: Open and close in the same serif voice; tie the close back to the score instrument without
competing with the CTA; the band breaks the page's inner measure for full-width emphasis (D-12).
Output: 1 new component file under `src/components/marketing/cta/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-proof-conversion/04-CONTEXT.md
@.planning/phases/04-proof-conversion/04-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contracts extracted from in-repo sources this session. Use directly. -->

ScoreGaugeSkeleton (src/components/marketing/story/skeletons/score-gauge-skeleton.tsx) — VERIFIED:
  import { ScoreGaugeSkeleton } from "@/components/marketing/story/skeletons";
  <ScoreGaugeSkeleton className="opacity-70 scale-75" />   // "small/muted" per D-14
  - Pure RSC, single { className } prop, fixed 87 "Strong", flat-warm tokens, NO coral.
  - role="img" aria-label="Virality score (sample)" (line 39) → getByRole("img", { name: /virality score/i }).

Button (src/components/ui/button.tsx): <Button asChild variant="primary" size="lg"><Link href={SIGNUP_URL}>Try it free</Link></Button>
SIGNUP_URL (src/lib/routes.ts) = "/signup". Link from "next/link".

Legal flat-warm warm-seat radial (copy VERBATIM from simulation-showcase.tsx lines 92-100 — a low-alpha
CREAM tone-step, NOT a coral glow):
  style={{ background: "radial-gradient(68% 60% at 50% 36%, rgba(236,231,222,0.07), transparent 70%)" }}
  + legal DARK-shadow depth (NOT glow): shadow-[0_40px_80px_-24px_rgba(0,0,0,0.72),...]

Newsreader serif (wired Phase 1) — the close-line is the lone Phase-4 serif moment. Apply the serif
font-family the hero uses for its headline (the serif voice token). Tag the close-line node with
data-testid="cta-close-line" for the test.

Flat-warm tokens: text-foreground / -secondary / -muted · bg-surface-elevated · border-border.
  Coral (--color-accent) ONLY on the CTA. No glass/blur. Radial = warm-charcoal/cream tone-step only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Final full-width CTA band — serif close-line + CTA→/signup + muted gauge echo (CONVERT-02)</name>
  <files>src/components/marketing/cta/final-cta-band.tsx</files>
  <behavior>
    - Renders a Newsreader-serif close-line node (data-testid="cta-close-line").
    - Renders one dominant CTA <Link href="/signup"> "Try it free" + the D-20 microcopy ("no credit card").
    - Renders a muted ScoreGaugeSkeleton echo → getByRole("img", { name: /virality score/i }) resolves.
    - Full-bleed surface (breaks max-w-5xl); inner content mx-auto max-w-3xl text-center; warm radial is a low-alpha cream tone-step (no glow). Pure RSC.
  </behavior>
  <read_first>
    - src/components/marketing/cta/__tests__/final-cta-band.test.tsx (the RED gate — serif close-line data-testid, /signup href, gauge img role)
    - src/components/marketing/story/simulation-showcase.tsx (lines 89-105 — the legal warm-seat radial + layered-dark-shadow depth, the flat-warm way to add a "warm seat" without a glow)
    - src/components/marketing/story/skeletons/score-gauge-skeleton.tsx (the reused gauge — { className }, aria-label, fixed 87 "Strong")
    - src/components/marketing/hero/hero.tsx (the serif close-line precedent — confirm how the hero applies the Newsreader serif to its headline; reuse that exact serif token for the band close-line)
    - .planning/phases/04-proof-conversion/04-PATTERNS.md (§ final-cta-band.tsx — the assignment + the radial-must-stay-flat-matte LANDMINE + the verbatim radial excerpt)
    - src/components/ui/button.tsx (asChild → Link, variant="primary"=coral)
    - src/lib/routes.ts (SIGNUP_URL)
    - .planning/phases/04-proof-conversion/04-CONTEXT.md (D-12, D-13, D-14, D-20)
  </read_first>
  <action>
    Create `src/components/marketing/cta/final-cta-band.tsx` (PURE RSC, no `"use client"`) exporting
    `function FinalCtaBand({ className }: { className?: string })`. It owns its own FULL-BLEED surface
    (D-12 — 04-05 mounts it in a `<section>` with NO `max-w-5xl`, so the band spans the viewport). The
    band surface is a flat-warm tone-step (`bg-surface-elevated` or a tone-step shade) + a hairline
    `border-border` + the legal warm-seat radial (copy the VERBATIM low-alpha CREAM
    `radial-gradient(...rgba(236,231,222,0.07)...)` from simulation-showcase.tsx lines 92-100 on an
    `aria-hidden` `-z-10` layer) — NEVER a coral halo, blur, or glass (Pitfall 6). Depth comes from a
    layered DARK shadow if needed, not a glow.

    Inner content `mx-auto max-w-3xl text-center`:
    - The Newsreader-serif voice close-line (D-13 — Phase 4's single serif moment). Apply the SAME
      serif token the hero headline uses (read hero.tsx to copy the exact serif font-family
      application). Tag the close-line node `data-testid="cta-close-line"` (the 04-00 test queries it).
      Drafted copy is Claude's discretion (D-21) — a confident closing line in the brand voice.
    - One dominant CTA: `<Button asChild variant="primary" size="lg"><Link href={SIGNUP_URL}>Try it free</Link></Button>`
      (coral — the lone accent; `asChild` wraps EXACTLY ONE `<Link>`).
    - The D-20 risk-reducer microcopy "Free to start — no credit card" in `text-foreground-muted` near the CTA.
    - The lone visual flourish: `<ScoreGaugeSkeleton className="opacity-70 scale-75" />` (D-14 — small/muted;
      reuse the Phase-3 primitive, NOT a new bespoke visual, NOT the full hero product-shot). Place it so it
      ties the close back to the instrument without competing with the CTA.

    Token discipline: cream tokens, coral ONLY on the CTA, no hex except the explicitly-commented legal
    radial/shadow rgba (matching the showcase precedent), no glass/glow/blur.

    Do NOT touch page.tsx or the barrel.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && npx vitest run src/components/marketing/cta/__tests__/final-cta-band.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/marketing/cta/__tests__/final-cta-band.test.tsx` exits 0 (CONVERT-02 GREEN).
    - The file exports `FinalCtaBand`, contains no `"use client"` (`grep -c 'use client' ...` → 0).
    - Renders a `data-testid="cta-close-line"` node using the serif token; one `href="/signup"` CTA via `Button asChild` + `Link`; the D-20 microcopy ("no credit card").
    - Renders `<ScoreGaugeSkeleton>` (→ `getByRole("img", { name: /virality score/i })` resolves).
    - The warm radial is the low-alpha CREAM `rgba(236,231,222,0.07)` tone-step (no coral in the radial); no `backdrop-blur`/`drop-shadow`/glow (`grep -Eiv '^\s*(//|\*)' src/components/marketing/cta/final-cta-band.tsx | grep -Eiq 'backdrop-blur|drop-shadow|0_0_.*accent|glow' && echo VIOLATION || echo clean` → clean). Coral confined to the CTA.
  </acceptance_criteria>
  <done>The final CTA band renders a serif close-line + a dominant /signup CTA + the risk-reducer microcopy + a muted gauge echo on a flat-matte warm-seat surface; CONVERT-02 test passes; pure RSC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| marketing page → app signup | Outbound `<Link href="/signup">` only — the hardcoded internal `SIGNUP_URL` constant; no user-controlled redirect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | CTA redirect target | mitigate | CTA target is the hardcoded internal `SIGNUP_URL = "/signup"` (`src/lib/routes.ts`) — no user input, no open-redirect surface. |
| T-04-04-02 | Information Disclosure | drafted serif close-line copy | accept | Fictional brand-voice copy (D-21) — no real PII/secrets. |
| T-04-04-03 | XSS | close-line / microcopy string children | mitigate | Plain string children; React escapes by default. No `dangerouslySetInnerHTML`. |
| T-04-SC | Tampering | npm installs | accept | Zero installs — Button/Link/ScoreGaugeSkeleton reused in-repo. |

No high-severity threats (04-RESEARCH § Security Domain).
</threat_model>

<verification>
- CONVERT-02 test GREEN: `npx vitest run src/components/marketing/cta/`.
- No `"use client"` (pure RSC; build gate `○ /` preserved when 04-05 mounts it).
- Serif close-line present + tagged; one /signup CTA; muted gauge echo present.
- Warm radial is the legal low-alpha cream tone-step; coral confined to the CTA; no glow/blur.
</verification>

<success_criteria>
- CONVERT-02 RED test turns GREEN.
- `FinalCtaBand` exported, pure RSC, full-bleed, flat-matte-legal, serif close-line + gauge echo.
- page.tsx + the barrel UNTOUCHED (04-05 owns the wiring).
</success_criteria>

<output>
Create `.planning/phases/04-proof-conversion/04-04-SUMMARY.md` when done.
</output>
