---
phase: 12-library-acts-state-ia
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/tools/read/route.ts
  - src/app/api/tools/read/__tests__/route.test.ts
  - src/components/audience/audience-manager.tsx
autonomous: false
requirements: [AUD-EDIT-02]
must_haves:
  truths:
    - "On the /audience surface, a Compare action enters a selection mode where the user picks any two saved audiences"
    - "Selecting exactly two audiences enables 'Compare these two →'; fewer/more keeps it disabled"
    - "Launching Compare runs the P8 multi-audience Read against the chosen arbitrary audience pair and renders the EXISTING compare block/Read (not a redesigned render)"
    - "The /api/tools/read route accepts an explicit two-audience pair (arbitrary audience_ids), resolved RLS-scoped under the session — never as raw weights"
    - "Selection checkboxes/selected-row state are neutral (charcoal-chip / white-[0.06] + cream check) — NOT coral; coral stays only on the Compare-launch CTA"
  artifacts:
    - path: "src/app/api/tools/read/route.ts"
      provides: "Arbitrary-pair compare path — accepts an explicit audienceIds[2] body, each resolved via getAudience under the session"
      contains: "audienceIds"
    - path: "src/components/audience/audience-manager.tsx"
      provides: "Selection mode + Compare action + 'Compare these two →' launch"
      contains: "Compare"
  key_links:
    - from: "src/components/audience/audience-manager.tsx"
      to: "/api/tools/read"
      via: "POST with concept + the two chosen audienceIds, then surface the multi-audience Read"
      pattern: "/api/tools/read"
    - from: "src/app/api/tools/read/route.ts"
      to: "runTwoAudienceRead"
      via: "resolve both explicit audiences (getAudience, RLS-scoped) → runTwoAudienceRead(concept, [a,b])"
      pattern: "runTwoAudienceRead"
---

<objective>
Ship the killer feature (AUD-EDIT-02 / D-05): a **multi-select Compare entry point** on the `/audience` surface that picks **any two saved audiences** (arbitrary pair — not just active-vs-General) and runs **P8's existing multi-audience Read** (08-06) against them. The Read render is REUSED verbatim — the only net-new build is the selection-mode entry point + an arbitrary-pair path on the `/api/tools/read` route (which today resolves the primary audience from the thread). Delivers "wins for growth, bombs for buyers" side-by-side over proven infra.

Purpose: comparing two calibrated audiences on one concept is the audience-surface flagship — the foresight payoff that proves the audience is the moat — and it ships as a thin entry point, not a rebuild.
Output: a `Compare`/selection mode in `audience-manager.tsx`, an arbitrary-pair body path on `/api/tools/read`, and a route test for the new pair path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-library-acts-state-ia-expansion-not-yet-discussed/12-CONTEXT.md
@.planning/phases/12-library-acts-state-ia-expansion-not-yet-discussed/12-UI-SPEC.md
@.planning/phases/08-discover-remix-read-the-competitor-niche-moat-chain-draft-no/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend /api/tools/read to accept an arbitrary explicit two-audience pair</name>
  <read_first>
    - src/app/api/tools/read/route.ts (the file being extended — read the WHOLE route: it currently resolves the PRIMARY audience from the thread (active_audience_id) and an OPTIONAL secondAudienceId from the body, then calls runTwoAudienceRead(concept, [activeAudience, secondAudience]))
    - src/lib/engine/flash/two-audience-read.ts (runTwoAudienceRead — confirm it accepts an arbitrary Audience[] and dedupes by identity / fills General for a single distinct audience; the runner needs NO change)
    - src/lib/audience/audience-repo.ts (getAudience — RLS-scoped resolution incl. virtual sentinel short-circuit; this is how each explicit id is safely resolved, never trusted as raw weights)
    - src/lib/http/csrf-guard.ts (the shipped CSRF guard already applied in this route — keep it)
  </read_first>
  <action>
    Add an explicit-pair path to `src/app/api/tools/read/route.ts` WITHOUT removing the shipped thread-derived default path (Pitfall: keep 08-06's active-vs-General default working).

    Parse an OPTIONAL `audienceIds` field from the body: `const audienceIds = Array.isArray(body.audienceIds) ? body.audienceIds.filter((x) => typeof x === "string") : null;`.

    Branch on it:
    - **Explicit-pair path (NEW, AUD-EDIT-02):** when `audienceIds` has exactly 2 entries, resolve BOTH via `getAudience(supabase, id)` under the session (RLS-scoped — the SAME trust-boundary discipline as the shipped secondAudienceId path; ids are resolved, never trusted as raw weights, CR-01/T-03-08). Pass the two resolved audiences to `runTwoAudienceRead(concept, [first, second])`. If either id fails to resolve (null/throws), return `{ error: "audience_not_found" }` with status 400 (do NOT silently fall back to General for an explicitly-requested pair — that would hide a bad pick). The 2-audience cap (D-09) is already enforced downstream by the runner.
    - **Default path (UNCHANGED):** when `audienceIds` is absent, keep the existing behavior exactly — primary from `thread.active_audience_id`, optional `secondAudienceId` from the body, default second = General.

    Keep auth-first (T-03-07), the CSRF guard, the concept length cap (MAX_CONCEPT_LENGTH), `insertMessage` re-validation + `kcStamp` provenance, and the bands-only honesty spine all UNCHANGED. Do NOT bump `ENGINE_VERSION` (no engine bytes change — this is route-layer audience resolution only). Persist the resulting `multi-audience-read` block to the open thread exactly as the shipped path does, and return `{ block }`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "audienceIds" src/app/api/tools/read/route.ts` returns matches in the parse + the explicit-pair branch.
    - `grep -n "runTwoAudienceRead" src/app/api/tools/read/route.ts` still returns a match (the runner is reused, not replaced).
    - `grep -n "audience_not_found" src/app/api/tools/read/route.ts` returns a match (explicit bad-id rejection, status 400 — no silent General fallback for an explicit pair).
    - The shipped default path is intact: `grep -n "active_audience_id" src/app/api/tools/read/route.ts` and `grep -n "secondAudienceId" src/app/api/tools/read/route.ts` both still return matches.
    - `grep -c "ENGINE_VERSION" src/app/api/tools/read/route.ts` returns 0 (no engine version touched).
    - `npm run build` passes.
  </acceptance_criteria>
  <done>The read route serves both the shipped active-vs-General default and the new arbitrary explicit two-audience pair (each id RLS-resolved), reusing runTwoAudienceRead; bad explicit ids are rejected 400; no engine bytes change.</done>
</task>

<task type="auto">
  <name>Task 2: Route test — arbitrary-pair compare path resolves both ids and rejects bad pairs</name>
  <read_first>
    - src/app/api/tools/read/route.ts (the just-edited route — confirm the exact request/response contract the test must assert: body { concept, audienceIds }, response { block } on success, { error: "audience_not_found" } 400 on a bad id)
    - src/app/api/threads/open/__tests__/route.test.ts (a sibling route test in this repo — mirror its mocking idiom for `@/lib/supabase/server`, `auth.getUser`, and the thread/audience repo functions; reuse the same vitest mock style)
    - src/lib/engine/flash/two-audience-read.ts (so the test can mock runTwoAudienceRead to return a deterministic 2-entry multi-audience-read block without a real Flash call)
  </read_first>
  <action>
    Create `src/app/api/tools/read/__tests__/route.test.ts` (no test exists for this route today). Mock `@/lib/supabase/server` (authed user), `@/lib/audience/audience-repo` (`getAudience` returns a distinct calibrated `Audience` for each of two ids; returns null for an unknown id), `@/lib/threads/threads` (`createOpenThreadLazy`), `@/lib/threads/messages` (`insertMessage` no-op), and `@/lib/engine/flash/two-audience-read` (`runTwoAudienceRead` returns a fixed 2-entry `multi-audience-read` block). Assert:
    - **Arbitrary pair:** POST `{ concept: "<text>", audienceIds: ["aud-a", "aud-b"] }` → 200 with `{ block }`, and `runTwoAudienceRead` was called with the concept and an array of the TWO resolved audiences (verify both ids were resolved via `getAudience`).
    - **Bad explicit id:** POST `{ concept, audienceIds: ["aud-a", "missing"] }` (getAudience returns null for "missing") → status 400 with `{ error: "audience_not_found" }` (no runTwoAudienceRead call).
    - **Default path preserved:** POST `{ concept }` with no `audienceIds` → 200 (the shipped active-vs-General path still runs) — a smoke assertion that the branch did not break the default.
    - **Unauthorized:** no user → 401 (auth-first).
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/app/api/tools/read/__tests__/route.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/app/api/tools/read/__tests__/route.test.ts` succeeds.
    - `node ./node_modules/vitest/vitest.mjs run src/app/api/tools/read/__tests__/route.test.ts` reports all tests PASS (≥ 4 test cases: arbitrary-pair, bad-id-400, default-path, unauthorized-401).
    - The test asserts `runTwoAudienceRead` receives the two resolved audiences for the arbitrary-pair case (not General-as-second).
  </acceptance_criteria>
  <done>The arbitrary-pair compare path is locked by a route test: two explicit ids resolve and run, a bad id returns 400, the shipped default path still passes, and auth is enforced.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Compare selection mode + launch to audience-manager.tsx</name>
  <read_first>
    - src/components/audience/audience-manager.tsx (the file being extended — read the whole component: the header with the "Create audience" button, the audience-row list map, the General badge + ⋯ overflow menu, the delete Dialog)
    - .planning/phases/12-library-acts-state-ia-expansion-not-yet-discussed/12-UI-SPEC.md (§"Audience compare — multi-select entry (D-05)" + §"Copywriting Contract → Audience compare" — the exact Compare/selection copy, the neutral-not-coral selection rule, the "any two saved audiences" constraint, and the render-location discretion)
    - src/components/thread/multi-audience-read-block.tsx (the REUSED Read render — confirm its prop shape `MultiAudienceReadBlockRenderer({ block })` if rendering the result inline; do NOT redesign it)
    - src/lib/audience/audience-types.ts (Audience shape — the rows already carry `id`; the object is audience_ids[]-ready per P7)
  </read_first>
  <action>
    Add a selection mode to `src/components/audience/audience-manager.tsx` (D-05). Net-new build is the entry point ONLY — the Read render is reused.

    (a) **Compare action + selection state:** add a `Compare` action button to the header (next to "Create audience"; use a `Scales` or `Rows` icon from `@phosphor-icons/react`). Toggling it enters `selectionMode`. Add `const [selectionMode, setSelectionMode] = useState(false)` and `const [selectedIds, setSelectedIds] = useState<string[]>([])`. In selection mode the header shows the selection prompt `Pick two audiences to compare.`, a `{n}/2 selected` counter, a `Cancel` button (exits selection mode + clears selectedIds), and the launch CTA `Compare these two →` (disabled unless `selectedIds.length === 2`; disabled hint `Select two audiences`).

    (b) **Per-row selection toggle:** in selection mode each audience row gains a neutral checkbox (or whole-row toggle) that adds/removes its `audience.id` in `selectedIds` (cap at 2 — ignore a 3rd selection or replace-oldest, planner's choice; keep it ≤2). Selection visual state is NEUTRAL: selected row = `bg-white/[0.06]` (or `--color-charcoal-chip`) + a cream check — NOT coral (UI-SPEC: selection ≠ action accent; coral stays on the launch CTA only). Use the shipped `pointer-coarse:` size bump on the toggle for ≥44px touch targets. In selection mode the row's normal `onClick` navigation to `/audience/[id]` is suppressed (clicking a row toggles selection instead).

    (c) **Launch Compare:** `Compare these two →` POSTs to `/api/tools/read` with `{ concept, audienceIds: selectedIds }`. For the `concept`: prompt the user for a concept to compare (a minimal inline text input shown in selection mode, or reuse an existing concept-entry affordance — planner discretion; the concept is REQUIRED by the route, min 1 char). On success, surface the returned `multi-audience-read` block using the EXISTING `MultiAudienceReadBlockRenderer` (render location is planner discretion — inline panel on /audience, a Dialog, or route to /home where the thread rehydrates; the render COMPONENT is fixed, do NOT redesign it). On a non-eligible/insufficiently-calibrated audience error, show the warning-tone note `This audience isn't calibrated enough to compare yet.` (NEVER error-red, NEVER coral).

    Leave the shipped list rendering, General badge, ⋯ overflow menu, and delete Dialog UNCHANGED outside selection mode. The General + preset audiences MAY be selected (the route resolves their virtual sentinels fine), but if the chosen pair collapses to identical identities the runner already degrades gracefully — do not special-case it in the UI.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15 && node ./node_modules/vitest/vitest.mjs run src/lib/audience src/components/audience 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "selectionMode" src/components/audience/audience-manager.tsx` and `grep -n "selectedIds" src/components/audience/audience-manager.tsx` return matches.
    - `grep -n "Compare these two" src/components/audience/audience-manager.tsx` and `grep -n "Pick two audiences to compare" src/components/audience/audience-manager.tsx` return matches.
    - `grep -n "/api/tools/read" src/components/audience/audience-manager.tsx` returns a match AND `grep -n "audienceIds" src/components/audience/audience-manager.tsx` returns a match (the launch passes the chosen pair).
    - `grep -n "MultiAudienceReadBlockRenderer" src/components/audience/audience-manager.tsx` returns a match (the P8 render is reused, not redesigned) — OR, if the planner chose the route-to-/home render location, `grep -n 'router.push("/home")' src/components/audience/audience-manager.tsx` returns a match instead (one of the two MUST be present).
    - The selected-row/checkbox styling contains NO coral token: `grep -nE "selected.*(accent|coral|FF7F50)|(accent|coral|FF7F50).*select" src/components/audience/audience-manager.tsx` returns 0 (selection is neutral).
    - `npm run build` passes and the audience suites are green.
  </acceptance_criteria>
  <done>The /audience surface has a Compare selection mode: pick any two saved audiences (neutral selection), launch "Compare these two →", and the result renders via the reused P8 multi-audience Read. No render redesign; no coral on selection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify the audience Compare flow end-to-end</name>
  <what-built>
    The /audience surface now has a Compare selection mode that picks any two saved audiences and renders the P8 multi-audience Read (wins-for-X / bombs-for-Y) against an arbitrary pair, via an extended /api/tools/read route.
  </what-built>
  <how-to-verify>
    1. Run the dev server and sign in. Ensure you have at least two saved audiences (create a second calibrated/target audience if needed, or use General + one calibrated audience).
    2. On `/audience`, click `Compare`. Confirm selection mode: the prompt `Pick two audiences to compare.`, a `{n}/2 selected` counter, and `Compare these two →` DISABLED.
    3. Select one audience — counter reads `1/2`, CTA still disabled. Select a second — counter reads `2/2`, CTA enabled. Confirm the selected rows show a NEUTRAL highlight + cream check (NO coral on the selection itself; coral only on the enabled `Compare these two →` CTA).
    4. Enter a concept to compare (the inline concept input), then click `Compare these two →`. Confirm the multi-audience Read renders the side-by-side verdict (e.g. "Growth: Strong · Buyers: Weak"), each audience's Read panel + Lever, and the verbatim quote wall — i.e. the EXISTING P8 Read render, not a new design.
    5. Confirm the compared pair is the two you picked (arbitrary pair — not silently active-vs-General).
    6. `Cancel` selection mode → returns to the normal audience list (rows navigate to /audience/[id] again).
    7. Honesty spot-check: if you pick an under-calibrated audience and it can't compare, you see the warning-tone note (never error-red, never coral).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what's off (coral on selection, the wrong pair compared, a redesigned Read render, or a launch error instead of a Read).</resume-signal>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 12-03)

New/changed symbols introduced by this plan (exclude these from source-drift verification — they are net-new here):
- **Route body field:** `audienceIds: [string, string]` accepted by `POST /api/tools/read` (the arbitrary-pair compare path), plus the `audience_not_found` 400 error code.
- **Route test:** `src/app/api/tools/read/__tests__/route.test.ts`.
- **Selection-mode UI (audience-manager.tsx):** `selectionMode` + `selectedIds` state, the `Compare` header action, the selection prompt/counter/`Cancel`, the `Compare these two →` launch CTA, and an inline concept input.
- **Reused, NOT created (exclude from drift):** `runTwoAudienceRead`, `MultiAudienceReadBlockRenderer`, `getAudience`, `createOpenThreadLazy`, `insertMessage`, `kcStamp`, the csrf-guard, the `multi-audience-read` block schema — all shipped in P8 (08-05/08-06) / P5 / P7.
</artifacts_produced>

<verification>
- Build compiles: `npm run build`.
- New read-route test green: `node ./node_modules/vitest/vitest.mjs run src/app/api/tools/read/__tests__/route.test.ts`.
- Audience suites green: `node ./node_modules/vitest/vitest.mjs run src/lib/audience src/components/audience`.
- Engine untouched: `grep -c "ENGINE_VERSION" src/app/api/tools/read/route.ts` returns 0 (no version bump; route-layer audience resolution only).
- Human checkpoint (Task 4) confirms the arbitrary-pair compare renders the reused P8 Read.
- NO migration introduced (audiences already pushed in P7) → no `supabase db push` task required.
</verification>

<success_criteria>
- Compare picks any two saved audiences (arbitrary pair) and renders the reused P8 multi-audience Read — no render redesign.
- /api/tools/read serves both the shipped active-vs-General default and the new explicit pair (each id RLS-resolved, bad ids rejected 400).
- Selection state is neutral (cream check), coral reserved for the Compare-launch CTA; honest warning tone for under-calibrated audiences.
- No ENGINE_VERSION bump; General baseline untouched.
</success_criteria>

<output>
Create `.planning/phases/12-library-acts-state-ia-expansion-not-yet-discussed/12-03-SUMMARY.md` when done.
</output>
