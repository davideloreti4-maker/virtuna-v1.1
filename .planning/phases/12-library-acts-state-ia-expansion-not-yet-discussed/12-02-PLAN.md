---
phase: 12-library-acts-state-ia
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/saved/saved-shelf.tsx
  - src/components/saved/saved-item-card.tsx
  - src/components/thread/multi-audience-read-block.tsx
  - src/app/(app)/library/page.tsx
  - src/app/(app)/saved/page.tsx
autonomous: false
requirements: [LIB-01, LIB-03]
must_haves:
  truths:
    - "A Library surface at /library renders the flat typed grid of saved nouns with the noun-type filter chips (All · Reads · Ideas · Hooks · Scripts · Outliers)"
    - "The Library heading reads 'Library' (not 'Saved'); the empty state reads 'Nothing in your Library yet'"
    - "Deep links to /saved still resolve (redirect to /library — no second store, no broken bookmarks)"
    - "The multi-audience Read card carries a Save affordance so the flagship Read is savable to Library (Save → Saved ✓, cream check)"
    - "A saved Read item shows a 'Use in thread →' affordance that lands the user in the open thread at /home (never an error path); saved Outliers keep their already-shipped discover→remix launch"
  artifacts:
    - path: "src/app/(app)/library/page.tsx"
      provides: "The /library route — in-shell content div rendering SavedShelf-as-Library"
      contains: "SavedShelf"
    - path: "src/components/saved/saved-shelf.tsx"
      provides: "Library heading/subtitle/empty/loading/error copy (relabeled from Saved)"
      contains: "Library"
    - path: "src/components/thread/multi-audience-read-block.tsx"
      provides: "SaveAffordance item_type='read' mounted on the Read card"
      contains: "SaveAffordance"
  key_links:
    - from: "src/components/saved/saved-item-card.tsx"
      to: "the open thread at /home"
      via: "read launch (no POST handoff) does router.push('/home'); outlier keeps the shipped discover→remix POST"
      pattern: "router\\.push\\(\"/home\"\\)"
    - from: "src/app/(app)/saved/page.tsx"
      to: "/library"
      via: "redirect('/library') — deep-link preservation"
      pattern: "redirect\\("
---

<objective>
Land the **Library** State home (LIB-01) and complete the **surface↔thread wiring** (LIB-03) by EXTENDING shipped P10 infrastructure — never reworking it. Library = the P10 `SavedShelf` relabeled `Saved`→`Library` and repointed to `/library` over the SAME `saved_items` store (one scrollable surface, flat noun-type filter chips, NO folders/tags/CMS). LIB-03 closes the two-way loop: mount the shipped `SaveAffordance` on the flagship multi-audience Read card (the one savable noun still missing it), and add the ONE missing launch path so a saved Read is actionable back into the open thread (saved Ideas/Hooks/Scripts/Outliers already launch via the shipped `CHAIN_HANDOFFS` resolution).

Purpose: Library is the first real front door for saved content (`/saved` ships today with NO nav entry), and "Use in thread →" turns every saved noun back into an Act in the single open thread (P5 model) — the Acts/State spine made whole.
Output: a `/library` route + relabeled `SavedShelf`, a `/saved`→`/library` redirect, a Save affordance on the Read card, and a `read`-type launch path in `SavedItemCard`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-library-acts-state-ia-expansion-not-yet-discussed/12-CONTEXT.md
@.planning/phases/12-library-acts-state-ia-expansion-not-yet-discussed/12-UI-SPEC.md
@.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /library route, relabel SavedShelf → Library, redirect /saved</name>
  <read_first>
    - src/components/saved/saved-shelf.tsx (the file being relabeled — read the whole component: heading "Saved", subtitle, FILTERS array, loading/error/EmptyState copy)
    - src/app/(app)/saved/page.tsx (the shipped server page wrapper to clone for /library — in-shell content div, auth guard, max-w-5xl)
    - .planning/phases/12-library-acts-state-ia-expansion-not-yet-discussed/12-UI-SPEC.md (§"Library surface (LIB-01 / D-03)" + §"Copywriting Contract → Library surface" — the exact Library heading/subtitle/empty/loading/error copy + the route-rename-vs-relabel discretion + "preserve deep links")
  </read_first>
  <action>
    Repoint the shipped Saved shelf to `/library` over the same `saved_items` store (D-03). Chosen approach (CONTEXT discretion — pick the physical-rename-with-redirect option for a clean canonical URL):

    (a) **Relabel `SavedShelf` copy** in `src/components/saved/saved-shelf.tsx` to the UI-SPEC Library strings (do NOT rename the component export, do NOT add a second store, do NOT touch the `useSavedItems` hook or the `FILTERS` array structure):
       - `<h1>` `Saved` → `Library`.
       - subtitle → `Everything you've saved — Reads, ideas, hooks, scripts, outliers — ready to pull back into a thread.`
       - loading text `Loading your shelf…` → `Loading your library…`.
       - error text `Couldn't load your shelf. Try again in a moment.` → `Couldn't load your library. Try again in a moment.`.
       - `EmptyState` heading: unfiltered `Nothing saved yet` → `Nothing in your Library yet`; filtered `Nothing of this type yet` (KEEP as-is — already matches UI-SPEC).
       - `EmptyState` body → `Save a Read, idea, hook, or outlier from any thread and it lands here — ready to pull back in.` (keep, already matches).
       - Confirm the `FILTERS` chip array stays `All · Reads · Ideas · Hooks · Scripts · Outliers · Formats` (the `Formats` chip stays rendered — it is not promoted but stays for format items; UI-SPEC note). Flat within each type — do NOT add folder/group/tag UI (ROADMAP guard).

    (b) **Create `src/app/(app)/library/page.tsx`** by cloning `src/app/(app)/saved/page.tsx` verbatim except: rename the default export to `LibraryPage`, update the `metadata` title to `Library | Numen` and description to match the new subtitle. It MUST render the SAME `<SavedShelf />` inside the SAME in-shell wrapper `<div className="mx-auto max-w-5xl px-4 py-6 sm:p-6">` (AppShell owns `<main>` — do NOT nest a second `<main>`, STATE 07-05). Keep the `await createClient()` + `getUser()` + `if (!user) return null` auth guard.

    (c) **Redirect `/saved` → `/library`** to preserve deep links (D-03 "preserve deep links / redirects"). Convert `src/app/(app)/saved/page.tsx` to a server component that calls Next's `redirect("/library")` (import from `next/navigation`) and renders nothing else. (This keeps `/saved` resolving without a second store or a duplicate shelf mount.) Do NOT delete the route file — the redirect IS the route.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "src/app/(app)/library/page.tsx"` succeeds and `grep -n "SavedShelf" "src/app/(app)/library/page.tsx"` returns a match.
    - `grep -n '>Library<' src/components/saved/saved-shelf.tsx` returns a match; `grep -c '>Saved<' src/components/saved/saved-shelf.tsx` returns 0.
    - `grep -n 'Loading your library' src/components/saved/saved-shelf.tsx` and `grep -n 'Nothing in your Library yet' src/components/saved/saved-shelf.tsx` both return matches.
    - `grep -n 'redirect("/library")' "src/app/(app)/saved/page.tsx"` returns a match (deep-link preservation).
    - `grep -c 'useSavedItems' src/components/saved/saved-shelf.tsx` returns exactly 1 (still the single shipped store — no second store introduced).
    - `npm run build` completes with no new type errors.
  </acceptance_criteria>
  <done>/library renders the relabeled flat typed Library surface over the existing saved_items store; /saved redirects to /library; no second store, no folder/tag UI added.</done>
</task>

<task type="auto">
  <name>Task 2: Complete LIB-03 — Save the Read card + launch a saved Read into the open thread</name>
  <read_first>
    - src/components/thread/multi-audience-read-block.tsx (the flagship Read card — read the full `MultiAudienceReadBlockRenderer`; it currently mounts NO SaveAffordance, the LIB-03 gap)
    - src/components/thread/account-read-block.tsx (lines ~180-190 — the shipped `<SaveAffordance item_type="read" .../>` mount pattern to mirror, including the thread_id prop convention)
    - src/components/thread/save-affordance.tsx (the shipped Act→State affordance — confirm its props: item_type, ref_id?, thread_id?, title?, snapshot)
    - src/components/saved/saved-item-card.tsx (the State→Act launcher — read ITEM_TYPE_TO_SKILL, LAUNCH_LABEL, launchHandoffFor, handleUse. CONFIRM: ITEM_TYPE_TO_SKILL already maps `outlier: "discover"` → `launchHandoffFor("outlier")` returns the live `discover→remix` handoff, so a saved outlier ALREADY renders a working "Use in thread →"; only `read` is absent from the map and resolves to `undefined`)
    - src/lib/tools/chain-handoff.ts (the CHAIN_HANDOFFS SSOT — CONFIRM the live `from:"discover", to:"remix"` entry with `endpoint:"/api/tools/remix/run"` powers the outlier launch, and there is NO `read` handoff; this is why only `read` needs the new no-endpoint /home path)
  </read_first>
  <action>
    Close the LIB-03 two-way loop by EXTENDING shipped wiring (no new mechanic, no new store, CHAIN_HANDOFFS stays the SSOT):

    (a) **Mount Save on the flagship Read card.** In `src/components/thread/multi-audience-read-block.tsx`, import `SaveAffordance` from `./save-affordance` and render `<SaveAffordance item_type="read" title={...} snapshot={block.props} />` at the foot of `MultiAudienceReadBlockRenderer` (after the `VerbatimWall` + provenance line). Derive `title` from the compare verdict — use the first audience's name + band (e.g. ``${audiences[0]?.name ?? "Read"}``) so the Library card has a human-readable label; pass `snapshot={block.props}` so the shelf re-renders the SAME typed renderer without a re-fetch (mirrors `account-read-block`). This is the ONLY savable noun still missing the affordance.

    (b) **Make a saved Read actionable into the open thread.** In `src/components/saved/saved-item-card.tsx`, handle ONLY the `read` item type — it is the single type with no entry in `ITEM_TYPE_TO_SKILL`, so `launchHandoffFor("read")` returns `undefined` and a saved Read currently renders NO launch CTA. Since a Read is re-opened (not re-generated), add a fallback launch path: when `launchHandoffFor(item.item_type)` returns `undefined` AND `item.item_type === "read"`, render a `Use in thread →` button whose handler does `router.push("/home")` (the open thread rehydrates and the user continues the single conversation — D-04 "append to the open / create-or-route"; `createOpenThreadLazy` already guarantees an open thread exists server-side on the next skill action). Use the default label `Use in thread →` for the Read type (NOT a per-type "Develop"/"Test" label — those are for re-generable nouns). Do NOT fabricate a re-generation endpoint for Reads (honesty — a saved Read is a record, re-opening the thread is the honest action).

    Do NOT add a new branch for `outlier`: a saved outlier ALREADY launches via the shipped `discover→remix` handoff (`ITEM_TYPE_TO_SKILL.outlier="discover"` → `handoffsFor("discover")[0]` POSTs the snapshot anchor to `/api/tools/remix/run`). Leave that path UNCHANGED — just confirm it still renders the existing "Use in thread →" / "Remix → Read" CTA on a saved outlier card. Keep the existing idea/hook/script launch behavior, the `LAUNCH_LABEL` map, the `anchorFromSnapshot` helper, and the Remove/AlertDialog flow UNCHANGED. The `format` type stays without a launch CTA (no handoff, not in CONTEXT's actionable noun list).

    (c) **Relabel the Remove confirmation copy** in `saved-item-card.tsx` to the Library wording (UI-SPEC §Destructive): `Remove from shelf?` → `Remove from Library?`; the body `…off your shelf.` → `…off your Library.`. Leave the destructive `Remove`/`Keep` buttons + `useDeleteSavedItem` mutation UNCHANGED.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && node ./node_modules/vitest/vitest.mjs run src/components/thread/__tests__ 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SaveAffordance" src/components/thread/multi-audience-read-block.tsx` returns a match AND `grep -n 'item_type="read"' src/components/thread/multi-audience-read-block.tsx` returns a match.
    - In `src/components/saved/saved-item-card.tsx`: `grep -n 'router.push("/home")' src/components/saved/saved-item-card.tsx` returns a match (the read fallback launch), and a saved item of `item_type` `read` now renders a `Use in thread →` control (verify by reading the render branch — the `handoff === undefined && item.item_type === "read"` case renders a button, not nothing).
    - The new fallback is scoped to `read` only: `grep -n 'item_type === "read"' src/components/saved/saved-item-card.tsx` returns a match, and the new `router.push("/home")` branch is NOT gated on `outlier` (a saved outlier still resolves a handoff via the unchanged `launchHandoffFor` path — `ITEM_TYPE_TO_SKILL` still contains `outlier: "discover"`).
    - `grep -n 'Remove from Library?' src/components/saved/saved-item-card.tsx` returns a match; `grep -c 'Remove from shelf?' src/components/saved/saved-item-card.tsx` returns 0.
    - `npm run build` passes; the thread `__tests__` suite passes (no regression from the Read-card Save mount).
    - No new file in `src/lib/tools/` and no new POST route created (CHAIN_HANDOFFS stays the SSOT — `grep -c "CHAIN_HANDOFFS\|handoffsFor" src/components/saved/saved-item-card.tsx` ≥ 1, the existing resolution path is preserved).
  </acceptance_criteria>
  <done>The flagship Read card is savable to Library; a saved Read launches into the open thread via /home; saved Outliers keep their shipped discover→remix launch; Remove copy reads "Library"; no new store/mechanic/endpoint introduced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the literal four-noun nav + Library surface + the save↔use loop</name>
  <what-built>
    The Library State home is live: the sidebar shows the literal four nouns (depends on Plan 12-01 having landed), /library renders the flat typed saved-content surface with noun-type filter chips, the flagship Read card is now savable, a saved Read launches back into the open thread, and saved Ideas/Hooks/Scripts/Outliers keep their shipped launches.
  </what-built>
  <how-to-verify>
    1. Run the dev server (`npm run dev`) and sign in. If CSS changes don't appear, kill the dev server + clear `.next/` + restart (CLAUDE.md known issue).
    2. **Nav literal four nouns:** Confirm the sidebar reads — accent "New Thread" CTA (coral) above the divider, then Settings · Audience · Library in the top group (Library NOT coral, matte active state when on /library), then the "Thread" history section, then Account bottom. The four nav nouns must read literally Thread · Audience · Library · Settings.
    3. **Library surface:** Click `Library`. Confirm the heading reads `Library`, the subtitle mentions "Reads, ideas, hooks, scripts, outliers", and the filter chips read `All · Reads · Ideas · Hooks · Scripts · Outliers` (+ Formats if format items exist). Click a few chips — the grid filters flat (no folders/groups). If empty, confirm the empty state reads `Nothing in your Library yet`.
    4. **Deep link:** Visit `/saved` directly in the address bar — it must redirect to `/library` (no broken page, no duplicate shelf).
    5. **Save loop (Act→State):** In a thread, run a concept Read (the multi-audience Read card). Confirm a `Save` button appears on it; click it → it flips to `Saved ✓` with a CREAM check (NOT coral). Then go to Library → the saved Read appears as a `Read`-tagged card.
    6. **Use loop (State→Act):** On a saved `Read` card in Library, click `Use in thread →` → you land at /home in the open thread (no error). On a saved `Outlier` card, confirm its shipped launch (the discover→remix "Use in thread →" / "Remix → Read") still fires. On a saved `Hook`/`Idea` card, confirm the per-type label (`Test full →` / `Develop →`) still launches as before.
    7. **Honesty/color spot-check:** No coral on the Library nav item, the type-filter chips, or the item-type tags. The Save check is cream. An empty Library is calm (not an error).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what's off (wrong copy, coral where it shouldn't be, a broken redirect, or a launch that errors instead of landing in the thread).</resume-signal>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 12-02)

New/changed symbols introduced by this plan (exclude these from source-drift verification — they are net-new here):
- **Route:** `src/app/(app)/library/page.tsx` (export `LibraryPage`) — the canonical Library surface; `/saved` becomes a `redirect("/library")` stub.
- **Relabeled copy (saved-shelf.tsx):** heading `Library`, the Library subtitle, `Loading your library…`, `Couldn't load your library…`, `Nothing in your Library yet`.
- **Relabeled copy (saved-item-card.tsx):** `Remove from Library?` + "…off your Library." body.
- **New affordance mount:** `<SaveAffordance item_type="read">` on `multi-audience-read-block.tsx` (the flagship Read card).
- **New launch branch (saved-item-card.tsx):** the `read` item type (previously no launch CTA) renders a `Use in thread →` control that routes to `/home` (open-thread re-open; no new endpoint). The `outlier` launch is UNCHANGED (shipped discover→remix handoff).
- **Reused, NOT created (exclude from drift):** `SavedShelf`, `SavedItemCard`, `SaveAffordance`, `useSavedItems`/`useSaveItem`/`useDeleteSavedItem`, `CHAIN_HANDOFFS`/`handoffsFor`, `ITEM_TYPE_TO_SKILL` (incl. the shipped `outlier: "discover"` mapping), `MultiAudienceReadBlockRenderer`, the `saved_items` table + `/api/saved` route, `createOpenThreadLazy` — all shipped in P5/P8/P10.
</artifacts_produced>

<verification>
- Build compiles: `npm run build`.
- Thread card tests green (Read-card Save mount caused no regression): `node ./node_modules/vitest/vitest.mjs run src/components/thread/__tests__`.
- Human checkpoint (Task 3) confirms the literal four-noun nav, the Library surface copy/filters, the /saved→/library redirect, and the save↔use loop end-to-end.
- NO migration introduced (saved_items already pushed in P10) → no `supabase db push` task required.
</verification>

<success_criteria>
- /library is the canonical Library surface (flat, typed, noun-type chips) over the existing saved_items store; /saved redirects to it; deep links preserved.
- The flagship multi-audience Read is savable to Library (Save → Saved ✓, cream check); every other thread output stays savable (shipped).
- A saved Read is actionable back into the open thread; saved Ideas/Hooks/Scripts/Outliers keep their shipped launches; no launch is an error path.
- No second store, no new mechanic, no new endpoint — CHAIN_HANDOFFS remains the SSOT; no folder/tag/CMS UI.
</success_criteria>

<output>
Create `.planning/phases/12-library-acts-state-ia-expansion-not-yet-discussed/12-02-SUMMARY.md` when done.
</output>
