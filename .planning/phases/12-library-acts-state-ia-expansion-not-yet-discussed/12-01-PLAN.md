---
phase: 12-library-acts-state-ia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sidebar/Sidebar.tsx
  - src/components/sidebar/__tests__/Sidebar.a11y.test.tsx
  - src/components/sidebar/__tests__/Sidebar.recent.test.tsx
autonomous: true
requirements: [IA-01]
must_haves:
  truths:
    - "The four nav rows read literally Thread · Audience · Library · Settings"
    - "Tapping the new Library nav item routes to /library and shows the saved-content surface"
    - "The primary CTA reads 'New Thread' (was 'New Simulation') and still triggers a new thread + routes to /home"
    - "The history section header reads 'Thread' (was 'Simulations') and its empty state reads 'No threads yet.'"
    - "The Library nav item's active state is matte white/[0.06] — NOT coral (coral stays on 'New Thread')"
  artifacts:
    - path: "src/components/sidebar/Sidebar.tsx"
      provides: "Relabeled nav copy + new Library NavItem in the Settings·Audience·Library top group"
      contains: "Library"
  key_links:
    - from: "src/components/sidebar/Sidebar.tsx"
      to: "/library"
      via: "router.push on the Library NavItem onClick + isActive=pathname.startsWith('/library')"
      pattern: "router\\.push\\(\"/library\"\\)"
---

<objective>
Crystallize the literal four-noun nav (IA-01 / D-01): add ONE `Library` `NavItem` to the active studio sidebar's top group and relabel the existing copy so the four nav labels read literally **Thread · Audience · Library · Settings**. This is an ADDITIVE + relabel change to `src/components/sidebar/Sidebar.tsx` only — NO sidebar restructure, NO new responsive logic. Preserve all shipped D-12 (Settings/Audience/@handle cluster) and D-13 (Simulations history list, score/remix chips, routing) behavior.

Purpose: The nav is the Acts/State spine made visible — Library is the new State home's front door, and "New Thread" / "Thread" align the literal labels with the single-conversation model shipped in P5.
Output: A relabeled `Sidebar.tsx` with a fourth nav noun (`Library`) wired to `/library`, plus updated sidebar tests for the new label/testid churn.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-library-acts-state-ia-expansion-not-yet-discussed/12-CONTEXT.md
@.planning/phases/12-library-acts-state-ia-expansion-not-yet-discussed/12-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Library NavItem + relabel the four nouns in Sidebar.tsx</name>
  <read_first>
    - src/components/sidebar/Sidebar.tsx (the file being modified — read the whole `NavItem` component, the top-group block at lines ~324-344, the "Simulations" history section at lines ~346-450, and the collapsed-rail tooltip at lines ~435-449)
    - .planning/phases/12-library-acts-state-ia-expansion-not-yet-discussed/12-UI-SPEC.md (§"Sidebar — Library nav item (D-01)" + §"Copywriting Contract → Sidebar IA relabel" — the exact old→new copy table + the accent/active-state rules)
    - src/components/sidebar/SidebarAccountSelector.tsx (the component that follows the top-group NavItems — confirm insertion point is BEFORE it)
  </read_first>
  <action>
    Edit `src/components/sidebar/Sidebar.tsx`:

    (a) **Import a Library icon** from `@phosphor-icons/react` alongside the existing icon imports (`Plus, SlidersHorizontal, ClockCountdown, ...`): add `Books` (the chosen Library icon — must visually read as "saved collection", distinct from the history `ClockCountdown`).

    (b) **Relabel the primary CTA** (lines ~307-318): change the `NavItem` `label` from `"New Simulation"` to `"New Thread"`. Leave its `icon={Plus}`, `accent`, `onClick={() => { triggerNewAnalysis(); router.push("/home"); }}`, and the `⌘N` badge UNCHANGED. This NavItem stays the ONE accent (coral) item in the nav.

    (c) **Add the Library NavItem** inside the top-group `<div className="flex flex-col gap-0.5">` (lines ~326-343), positioned AFTER the `Audience` NavItem and BEFORE `<SidebarAccountSelector .../>`, so the rendered top-group order is Settings · Audience · Library. Use `icon={Books}`, `label="Library"`, `isCollapsed={effectiveCollapsed}`, `isActive={isOnLibrary}`, `onClick={() => router.push("/library")}`. Do NOT pass `accent` (the Library active state must be matte `bg-white/[0.06]`, identical to Settings/Audience — coral here would break the one-accent rule).

    (d) **Add the `isOnLibrary` derived flag** next to the existing `isOnSettings` / `isOnAudience` declarations (lines ~229-230): `const isOnLibrary = pathname.startsWith("/library");`.

    (e) **Relabel the history section** (lines ~346-450): change the `<SectionLabel>Simulations</SectionLabel>` (line ~348) to `<SectionLabel>Thread</SectionLabel>`; change the empty-state copy `No simulations yet.` (line ~358) to `No threads yet.`; change the collapsed-rail tooltip `<TooltipContent side="right">Simulations</TooltipContent>` (line ~447) and its `aria-label="Simulations"` (line ~443) to `Thread`. Do NOT change the `SectionLabel` SIZE/style (it stays the shipped 10px uppercase semibold — only its text changes). Leave `useAnalysisHistory`, the row mapping, score/remix chips, `data-testid` row values (`sidebar-board-label`, `sidebar-score-chip`, `sidebar-remix-tag`), and the `/analyze/[id]` routing UNCHANGED (D-13 preserved).

    Relabel scope is the sidebar ONLY (D-01 / CONTEXT discretion — keep minimal). Do NOT touch the dead legacy `src/components/app/sidebar.tsx`.
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/sidebar/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'label="New Thread"' src/components/sidebar/Sidebar.tsx` returns a match; `grep -c 'New Simulation' src/components/sidebar/Sidebar.tsx` returns 0.
    - `grep -n 'label="Library"' src/components/sidebar/Sidebar.tsx` returns exactly one match, and `grep -n 'router.push("/library")' src/components/sidebar/Sidebar.tsx` returns a match.
    - `grep -n 'isOnLibrary = pathname.startsWith("/library")' src/components/sidebar/Sidebar.tsx` returns a match.
    - `grep -n '<SectionLabel>Thread</SectionLabel>' src/components/sidebar/Sidebar.tsx` returns a match; `grep -c 'Simulations' src/components/sidebar/Sidebar.tsx` returns 0.
    - `grep -n 'No threads yet.' src/components/sidebar/Sidebar.tsx` returns a match.
    - The Library `NavItem` JSX does NOT pass an `accent` prop (verify by reading: the Library NavItem block has no `accent` token, unlike the "New Thread" one which keeps it).
  </acceptance_criteria>
  <done>Sidebar renders four literal nav nouns (New Thread CTA + Settings · Audience · Library top group), the Library item routes to /library with a matte non-coral active state, and the history section reads "Thread". Shipped D-12/D-13 behavior intact.</done>
</task>

<task type="auto">
  <name>Task 2: Update sidebar tests for the relabel + new Library item</name>
  <read_first>
    - src/components/sidebar/__tests__/Sidebar.a11y.test.tsx (the accessibility test — read its current assertions to find any "Simulations"/"New Simulation" label lookups or nav-item-count assertions that the relabel invalidates)
    - src/components/sidebar/__tests__/Sidebar.recent.test.tsx (the history-list test — read its assertions for the "Simulations" section header / empty-state copy)
    - src/components/sidebar/Sidebar.tsx (the just-edited source — confirm the exact new strings the tests must assert)
  </read_first>
  <action>
    Fix the two sidebar test files so they assert the NEW copy and cover the new Library item (Rule 1 — stale assertions become live assertions, do not weaken them):

    (a) In `Sidebar.a11y.test.tsx`: replace any query for the old `New Simulation` label/`aria-label` with `New Thread`; if the test enumerates or counts top-group nav items, update the expectation to include the `Library` item (querying by accessible name `Library`). Keep all a11y assertions (aria-current, focus, tooltip accessible name) intact — add a `Library` nav-item presence assertion (e.g. `getByRole("button", { name: "Library" })` is in the document).

    (b) In `Sidebar.recent.test.tsx`: replace any assertion of the `Simulations` section header text with `Thread`, and any `No simulations yet.` empty-state assertion with `No threads yet.`. Leave the row-rendering / score-chip / remix-tag / `data-testid` assertions UNCHANGED (D-13 behavior is unchanged).

    Do NOT modify `Sidebar.collapse.test.tsx` or `Sidebar.glass-strip.test.tsx` unless they reference the relabeled strings (read them only if the suite run in Task 1 surfaces a failure there).
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/sidebar/ 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `node ./node_modules/vitest/vitest.mjs run src/components/sidebar/` reports all sidebar test files PASS with 0 failures.
    - `grep -rn 'Library' src/components/sidebar/__tests__/Sidebar.a11y.test.tsx` returns at least one match (the new Library item is asserted present).
    - `grep -rc 'New Simulation\|No simulations yet\|>Simulations<' src/components/sidebar/__tests__/Sidebar.a11y.test.tsx src/components/sidebar/__tests__/Sidebar.recent.test.tsx` returns 0 across both files (no stale copy left asserted).
  </acceptance_criteria>
  <done>The sidebar test suite is green against the relabeled nav, asserts the new "New Thread"/"Thread" copy, and covers the presence of the new Library nav item.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 12-01)

New/changed symbols introduced by this plan (exclude these from source-drift verification — they are net-new here):
- **Nav copy:** `New Thread` (relabel of "New Simulation"), `Thread` history section label (relabel of "Simulations"), `No threads yet.` empty state (relabel of "No simulations yet."), `Thread` collapsed-rail tooltip/aria-label.
- **Nav item:** a `Library` `NavItem` in `Sidebar.tsx` (icon `Books` from `@phosphor-icons/react`), routing to `/library`.
- **Derived flag:** `isOnLibrary` (`pathname.startsWith("/library")`) in `Sidebar.tsx`.
- **Route consumed (not created here):** `/library` — the route itself is created in Plan 12-02. Until 12-02 lands, `/library` 404s; this plan's nav item is the consumer.
</artifacts_produced>

<verification>
- Sidebar test suite green: `node ./node_modules/vitest/vitest.mjs run src/components/sidebar/`
- Build compiles: `npm run build` (no type errors introduced by the new icon import / NavItem).
- Visual literal-four-noun check is covered by Plan 12-02's checkpoint (after /library exists), not here — this plan stops at the source + unit-test layer.
</verification>

<success_criteria>
- The four nav nouns read literally Thread · Audience · Library · Settings (New Thread CTA above the divider; Settings · Audience · Library top group; Account bottom-anchored).
- The Library item routes to /library and its active state is matte white/[0.06] (not coral).
- "New Thread" retains the coral accent + ⌘N + new-thread behavior; "Thread" history section retains all D-13 row behavior.
- Sidebar tests pass; no stale "Simulations"/"New Simulation" copy remains in source or tests.
</success_criteria>

<output>
Create `.planning/phases/12-library-acts-state-ia-expansion-not-yet-discussed/12-01-SUMMARY.md` when done.
</output>
