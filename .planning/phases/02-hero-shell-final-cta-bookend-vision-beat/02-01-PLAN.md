---
phase: 02
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/word-rotate.tsx
  - src/components/ui/shimmer-button.tsx
  - src/components/ui/animated-shiny-text.tsx
  - src/components/ui/border-beam.tsx
  - src/components/ui/spotlight.tsx
  - src/app/(marketing)/_components/HeroBookend.tsx
autonomous: true
requirements: [HERO-02, HERO-03, HERO-04, HERO-06, HERO-07, HERO-09, HERO-11, MOTION-01, MOTION-02, MOTION-03]
tags: [magicui, aceternity, hero, motion, reduced-motion]

must_haves:
  truths:
    - "User sees H1 'Predict viral for [WordRotate] before you post.' with rotating word fused inside the sentence"
    - "User sees coral-tinted Spotlight backdrop, ShimmerButton primary CTA, AnimatedShinyText badge, and BorderBeam stroke when motion enabled"
    - "User with prefers-reduced-motion sees static 'creators' word, no shimmer sweep, no BorderBeam render"
    - "WordRotate cycling pauses when Hero section leaves viewport (IntersectionObserver gate)"
    - "Screen reader announces full H1 sentence via aria-label, not individual word changes"
  artifacts:
    - path: "src/components/ui/word-rotate.tsx"
      provides: "Magic UI WordRotate primitive"
    - path: "src/components/ui/shimmer-button.tsx"
      provides: "Magic UI ShimmerButton primitive"
    - path: "src/components/ui/animated-shiny-text.tsx"
      provides: "Magic UI AnimatedShinyText primitive"
    - path: "src/components/ui/border-beam.tsx"
      provides: "Magic UI BorderBeam primitive"
    - path: "src/components/ui/spotlight.tsx"
      provides: "Aceternity Spotlight primitive"
    - path: "src/app/(marketing)/_components/HeroBookend.tsx"
      provides: "Shared inner layout for Hero + Final CTA (Spotlight + badge + H1 with fused WordRotate + sub + dual CTA), with reducedHeight prop"
      contains: "WordRotate, ShimmerButton, AnimatedShinyText, BorderBeam, Spotlight, useReducedMotion, IntersectionObserver"
  key_links:
    - from: "src/app/(marketing)/_components/HeroBookend.tsx"
      to: "motion/react"
      via: "useReducedMotion + import path"
      pattern: 'from "motion/react"'
    - from: "src/app/(marketing)/_components/HeroBookend.tsx"
      to: "src/app/(marketing)/motionTokens.ts"
      via: "import"
      pattern: "motionTokens"
    - from: "src/app/(marketing)/_components/HeroBookend.tsx"
      to: "ShimmerButton coral border"
      via: "single-stop alpha"
      pattern: "rgba\\(255,\\s*127,\\s*80"
---

<objective>
Install the five third-party UI primitives required by Hero + Final CTA (Magic UI WordRotate, ShimmerButton, AnimatedShinyText, BorderBeam; Aceternity Spotlight) and build the shared `HeroBookend` client component that both `HeroSection` and `FinalCtaSection` (Plan 03) will render.

**Purpose:** Establish the single canonical inner-layout for the bookend pattern (D-08 verbatim mirror) so that Hero and Final CTA never drift. All motion gating (prefers-reduced-motion, IntersectionObserver) lives here once, not duplicated per surface.

**Output:** 5 component files in `src/components/ui/` + 1 `HeroBookend.tsx` client component that exports a ready-to-mount inner layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-CONTEXT.md
@.planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-UI-SPEC.md
@.planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-PATTERNS.md
@.planning/phases/01-foundation-scaffold/01-CONTEXT.md
@./CLAUDE.md
@src/app/(marketing)/motionTokens.ts
@src/app/(marketing)/_components/SectionShell.tsx
@src/app/(marketing)/_components/LandingHeader.tsx
@src/components/motion/fade-in.tsx

<interfaces>
<!-- Contracts the executor needs. From Phase 1 codebase. -->

From src/app/(marketing)/motionTokens.ts:
```ts
export const motionTokens = {
  durations: { fast: 0.15, normal: 0.2, slow: 0.3, slower: 0.5, hero: 0.8 },
  easings: {
    outCubic: [0.215, 0.61, 0.355, 1] as const,
    outQuart: [0.165, 0.84, 0.44, 1] as const,
    inOut: [0.42, 0, 0.58, 1] as const,
    spring: [0.34, 1.56, 0.64, 1] as const,
  },
  staggerDelays: { children: 0.08, tight: 0.04, wide: 0.15 },
  viewportThresholds: { default: 0.2, hero: 0, early: 0.1, late: 0.4 },
  triggerOnce: true,
} as const;
```

From `motion/react` (consumed):
```ts
export function useReducedMotion(): boolean | null;
```

H1 fused-WordRotate component contract (from 02-UI-SPEC.md):
```tsx
<h1
  className="font-bold leading-[1.05] tracking-[-0.04em] text-center"
  style={{ fontSize: 'clamp(40px, 5.5vw, 64px)' }}
  aria-label="Predict viral for creators, brands, and founders — before you post."
>
  Predict viral for{" "}
  <span
    className="inline-flex justify-center"
    style={{ minWidth: '290px' /* desktop; tune for mobile via responsive style */ }}
    aria-live="off"
    aria-atomic="true"
  >
    {prefersReduced ? <span>creators</span> : <WordRotate words={["creators","brands","founders"]} duration={2500} />}
  </span>{" "}
  before you post.
</h1>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install 5 UI primitives via shadcn CLI (Magic UI + Aceternity Spotlight)</name>
  <files>
    src/components/ui/word-rotate.tsx,
    src/components/ui/shimmer-button.tsx,
    src/components/ui/animated-shiny-text.tsx,
    src/components/ui/border-beam.tsx,
    src/components/ui/spotlight.tsx
  </files>
  <read_first>
    .planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-UI-SPEC.md (sections "Component Inventory" + "Registry Safety"),
    .planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-PATTERNS.md (section "No Analog Found"),
    components.json (verify preset: style=new-york, baseColor=slate, cssVariables=true)
  </read_first>
  <action>
    Pre-install collision check: run `ls src/components/ui/` and verify NONE of `word-rotate.tsx`, `shimmer-button.tsx`, `animated-shiny-text.tsx`, `border-beam.tsx`, `spotlight.tsx` already exist. If any exist, skip its install (the file is preserved as-is).

    For each missing file, install via shadcn CLI in this exact order (Magic UI first, then Aceternity):
    1. `pnpm dlx shadcn@latest add @magicui/word-rotate --yes --overwrite=false`
    2. `pnpm dlx shadcn@latest add @magicui/shimmer-button --yes --overwrite=false`
    3. `pnpm dlx shadcn@latest add @magicui/animated-shiny-text --yes --overwrite=false`
    4. `pnpm dlx shadcn@latest add @magicui/border-beam --yes --overwrite=false`
    5. `pnpm dlx shadcn@latest add "https://ui.aceternity.com/registry/spotlight.json" --yes --overwrite=false`

    After each install, verify the file lands at `src/components/ui/{name}.tsx`. If it lands elsewhere, manually move it (do NOT leave duplicates). If any install errors due to npm/pnpm registry conflict, retry once; if it fails again, STOP and escalate with the exact error message — do NOT hand-roll the component (per D-XX no-scope-reduction rule).

    After all 5 install, audit each file for forbidden imports:
    - `grep -rn "from .framer-motion." src/components/ui/{word-rotate,shimmer-button,animated-shiny-text,border-beam,spotlight}.tsx` MUST return zero lines. If any file imports `framer-motion`, replace with `motion/react` via sed: `sed -i '' 's|from "framer-motion"|from "motion/react"|g' src/components/ui/{file}.tsx`.
    - `grep -rn "fetch(\|eval(\|process\.env\|dangerouslySetInnerHTML" src/components/ui/{word-rotate,shimmer-button,animated-shiny-text,border-beam,spotlight}.tsx` MUST return zero lines. If any flag fires, STOP and escalate — registry safety regression.

    Per CONTEXT.md D-12 / UI-SPEC § Registry Safety: all 5 components passed safety review on 2026-05-24. No new safety review needed unless installed files differ from review snapshot — flag deviations.
  </action>
  <verify>
    <automated>test -f src/components/ui/word-rotate.tsx && test -f src/components/ui/shimmer-button.tsx && test -f src/components/ui/animated-shiny-text.tsx && test -f src/components/ui/border-beam.tsx && test -f src/components/ui/spotlight.tsx && grep -rEL "from \"framer-motion\"|fetch\(|eval\(|process\.env|dangerouslySetInnerHTML" src/components/ui/{word-rotate,shimmer-button,animated-shiny-text,border-beam,spotlight}.tsx | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist at `src/components/ui/{name}.tsx`
    - `grep -l "from \"framer-motion\"" src/components/ui/{word-rotate,shimmer-button,animated-shiny-text,border-beam,spotlight}.tsx` returns ZERO matches (any framer-motion → motion/react rewrites already applied)
    - `grep -c "fetch(\|eval(\|process\.env\|dangerouslySetInnerHTML" src/components/ui/{word-rotate,shimmer-button,animated-shiny-text,border-beam,spotlight}.tsx | grep -v ":0$"` returns ZERO matches (no forbidden runtime constructs)
    - `pnpm tsc --noEmit` exits 0 (no type errors introduced by installs)
  </acceptance_criteria>
  <done>5 third-party primitives installed cleanly, no framer-motion imports, no XSS / network / env access, TypeScript compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build HeroBookend.tsx shared client component (Spotlight + badge + H1 + sub + dual CTA + reduced-motion gates)</name>
  <files>src/app/(marketing)/_components/HeroBookend.tsx</files>
  <read_first>
    .planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-UI-SPEC.md (sections "Spotlight Contract", "WordRotate Contract", "Dual CTA Contract", "AnimatedShinyText Contract", "Reduced-Motion Policy", "IntersectionObserver Autoplay Gate", "Above-Fold Geometry Contract"),
    .planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-PATTERNS.md (sections "HeroBookend.tsx", "Shared Patterns"),
    .planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-CONTEXT.md (decisions D-01 through D-08),
    src/components/motion/fade-in.tsx (reduced-motion guard pattern),
    src/app/(marketing)/motionTokens.ts (durations, easings, viewportThresholds, triggerOnce),
    src/app/(marketing)/_components/LandingHeader.tsx (backdropFilter inline-style pattern, lines 42-47),
    src/components/ui/word-rotate.tsx (just installed — read its prop signature to wire correctly),
    src/components/ui/shimmer-button.tsx (just installed — read its prop signature),
    src/components/ui/border-beam.tsx (just installed — read its prop signature: duration, size, borderWidth),
    src/components/ui/spotlight.tsx (just installed — read its prop signature: fill, position, size),
    src/components/ui/animated-shiny-text.tsx (just installed — read its prop signature)
  </read_first>
  <action>
    Create `src/app/(marketing)/_components/HeroBookend.tsx` as a `"use client"` component. This is the SHARED inner layout used verbatim by both Hero and Final CTA (per D-08).

    **Top of file:**
    ```tsx
    "use client";

    import { useEffect, useRef, useState } from "react";
    import { useReducedMotion } from "motion/react";
    import { ChevronRight } from "lucide-react";
    import { cn } from "@/lib/utils";
    import { motionTokens } from "@/app/(marketing)/motionTokens";
    import { WordRotate } from "@/components/ui/word-rotate";
    import { ShimmerButton } from "@/components/ui/shimmer-button";
    import { AnimatedShinyText } from "@/components/ui/animated-shiny-text";
    import { BorderBeam } from "@/components/ui/border-beam";
    import { Spotlight } from "@/components/ui/spotlight";

    export interface HeroBookendProps {
      /** Plan 03 sets `true` on the Final CTA instance to use min-h-[60vh] vs 100dvh (D-08 height difference) */
      reducedHeight?: boolean;
      className?: string;
    }
    ```

    **Component body — implement exactly per UI-SPEC contracts (NOT a "v1" or "minimal" version):**

    1. **Spotlight wrapper** (positioned absolutely behind content): pass `fill="rgba(255, 127, 80, 0.12)"` (single-stop coral alpha per MOTION-03). Position via inline style: desktop `top: -20%; right: -10%; width: 800px; height: 600px;`; mobile collapses to `top: -10%; right: -20%; width: 400px; height: 300px;` via responsive className (`md:` prefix). Wrap with `prefersReduced && {}` — render the Spotlight but suppress any internal CSS animation by passing `className="motion-reduce:animate-none"`. If Spotlight's installed source uses `backdrop-filter` in className, wrap its parent with inline `style={{ backdropFilter: 'blur(0px)' }}` to defeat Lightning CSS stripping (per MOTION-04 / PATTERNS.md "Shared Patterns").

    2. **AnimatedShinyText badge** (centered above H1, gap 16px):
       ```tsx
       <div
         className="inline-flex items-center rounded-full border border-white/[0.08] px-4 py-2"
         style={{ background: "rgba(255,255,255,0.03)" }}
       >
         {prefersReduced ? (
           <span className="text-sm font-normal" style={{ letterSpacing: '0.08em' }}>
             ✦ Behavioral prediction engine
           </span>
         ) : (
           <AnimatedShinyText className="text-sm font-normal" style={{ letterSpacing: '0.08em' }}>
             ✦ Behavioral prediction engine
           </AnimatedShinyText>
         )}
       </div>
       ```
       NO "AI" wording — per CONTEXT.md D-01 + § Copywriting Contract "No 'AI' in copy rule".

    3. **H1 with fused WordRotate** — use exact JSX from the `<interfaces>` block above. The `min-width` lock prevents H1 reflow when the word changes (per UI-SPEC "Longest word budget: 'creators' at 64px/700 weight ≈ 290px desktop, ≈ 180px mobile"). Apply `min-w-[180px] md:min-w-[290px]` on the inline-flex span. Pass WordRotate `duration={2500}` (rotation interval) + `framerProps={{ transition: { duration: 0.4, ease: motionTokens.easings.outQuart } }}` if WordRotate's installed signature supports it; if not, set as inline style and document the gap as a TODO comment.

       **IntersectionObserver gate for WordRotate** (per MOTION-02): use the pattern from PATTERNS.md lines 90-101. When `!inView`, render the static word `creators` in place of `<WordRotate />`. When section re-enters viewport, resume — do NOT restart from index 0. If WordRotate primitive has no pause/resume API, wrap it with conditional `inView ? <WordRotate /> : <span>creators</span>` (re-mount on re-enter is acceptable — UI-SPEC explicitly says "resume from current word" but if primitive doesn't expose state, the "resume" requirement degrades to "re-mount on re-enter" — flag in SUMMARY.md).

    4. **Sub-headline** (below H1, gap 24px desktop / 16px mobile):
       ```tsx
       <p className="text-base md:text-xl text-foreground-secondary leading-[1.5] max-w-2xl mx-auto" style={{ letterSpacing: '0.2px' }}>
         Stop guessing what'll hit. Score, refine, ship — in 30 seconds.
       </p>
       ```

    5. **Phase 3 placeholder gap** (per UI-SPEC § Above-Fold Geometry "min-h-[64px]" desktop only): `<div className="hidden md:block min-h-[64px]" aria-hidden="true" />` — reserves vertical space so Phase 3 logo bar drops in without reflow.

    6. **Dual CTA pair** (per UI-SPEC § Dual CTA Contract):
       ```tsx
       <div className="flex flex-col sm:flex-row items-center gap-2 sm:gap-6 w-full sm:w-auto">
         {/* Primary — ShimmerButton wrapping anchor */}
         <a href="#demo" className="w-full sm:w-auto">
           <ShimmerButton
             className="w-full sm:w-auto h-14 px-6 text-base font-bold rounded-[8px]"
             style={{
               border: '1px solid rgba(255, 127, 80, 0.25)',
               color: '#f9f9f9',
             }}
             shimmerColor="rgba(255, 127, 80, 0.25)"
             background="rgba(255, 127, 80, 0.08)"
           >
             <span className="relative z-10">Score your first TikTok</span>
             {!prefersReduced && (
               <BorderBeam
                 size={60}
                 duration={6}
                 borderWidth={1}
                 colorFrom="rgba(255, 127, 80, 0.8)"
                 colorTo="rgba(255, 127, 80, 0)"
               />
             )}
           </ShimmerButton>
         </a>
         {/* Secondary — ghost link */}
         <a
           href="#pricing"
           className="flex items-center justify-center min-h-[44px] text-sm text-foreground-secondary hover:text-foreground focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:rounded-[4px] w-full sm:w-auto"
           style={{ outlineColor: '#FF7F50', transition: 'color 150ms ease-out' }}
         >
           See pricing
           <ChevronRight className="ml-1 w-4 h-4 transition-transform motion-reduce:transform-none group-hover:translate-x-[2px]" />
         </a>
       </div>
       ```
       If ShimmerButton's installed prop names differ from `shimmerColor` / `background`, adapt to the actual signature (read the installed file) and keep coral single-stop alpha values. NEVER use multi-hue gradients (per MOTION-03 / D-XX).

    7. **Container & vertical rhythm** (full component skeleton):
       ```tsx
       export function HeroBookend({ reducedHeight = false, className }: HeroBookendProps) {
         const prefersReduced = useReducedMotion() ?? false;
         const ref = useRef<HTMLDivElement>(null);
         const [inView, setInView] = useState(false);
         useEffect(() => {
           const obs = new IntersectionObserver(
             ([e]) => setInView(e.isIntersecting),
             { threshold: motionTokens.viewportThresholds.hero, rootMargin: "0px" }
           );
           if (ref.current) obs.observe(ref.current);
           return () => obs.disconnect();
         }, []);

         return (
           <div
             ref={ref}
             className={cn(
               "relative w-full flex items-center justify-center px-6",
               reducedHeight ? "min-h-[60vh] py-16" : "min-h-[100dvh] py-12",
               className
             )}
           >
             {/* Spotlight absolutely positioned behind */}
             {/* Inner stack: badge → H1 → sub → gap → CTA pair */}
             <div className="relative z-10 max-w-7xl mx-auto flex flex-col items-center text-center gap-4">
               {/* badge */}
               {/* gap-4 (16px) handled by parent gap */}
               {/* H1 */}
               {/* sub-headline (gap to H1 = 24px via mt-6 override) */}
               {/* Phase 3 placeholder gap */}
               {/* CTA pair (gap to placeholder = 8px via mt-2 override) */}
             </div>
           </div>
         );
       }
       ```
       Per CONTEXT.md "Spotlight position + intensity" planner discretion: use `rgba(255, 127, 80, 0.12)` (12% alpha) — keeps WCAG AA ≥ 4.5:1 on H1 white text per UI-SPEC verification.

    **A11y**: aria-label on `<h1>` provides full sentence (per UI-SPEC). WordRotate span has `aria-live="off"` so changes don't announce. Secondary CTA has visible label "See pricing" — no aria-label needed.

    **NOT in scope (Plan 03 handles):** wrapping HeroBookend in `<section id="hero" aria-label="...">` (HeroSection does that), wrapping in `<section id="final-cta">` (FinalCtaSection does that). HeroBookend exports the INNER layout only.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | grep -c "src/app/(marketing)/_components/HeroBookend.tsx" | tr -d ' ' && grep -c '"use client"' src/app/\(marketing\)/_components/HeroBookend.tsx && grep -c 'from "motion/react"' src/app/\(marketing\)/_components/HeroBookend.tsx && grep -c 'useReducedMotion' src/app/\(marketing\)/_components/HeroBookend.tsx && grep -c 'IntersectionObserver' src/app/\(marketing\)/_components/HeroBookend.tsx && grep -c 'rgba(255, 127, 80' src/app/\(marketing\)/_components/HeroBookend.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/app/(marketing)/_components/HeroBookend.tsx`
    - First line is `"use client";` — grep returns exactly 1
    - Imports `useReducedMotion` from `"motion/react"` (NOT `framer-motion`) — grep `from "motion/react"` returns ≥1, grep `from "framer-motion"` returns 0
    - Contains `IntersectionObserver` JS call — grep returns ≥1
    - Contains the exact H1 sentence segment `Predict viral for` — grep returns ≥1
    - Contains the exact sub-headline `Stop guessing what'll hit` — grep returns ≥1
    - Contains coral single-stop alpha rgba `rgba(255, 127, 80` — grep returns ≥3 occurrences (Spotlight fill, ShimmerButton border, ShimmerButton bg or BorderBeam color)
    - Contains primary CTA destination `href="#demo"` and secondary `href="#pricing"` — grep each returns ≥1
    - Contains `aria-label=` on H1 with full sentence including "creators, brands, and founders" — grep returns ≥1
    - NO multi-hue gradient: `grep -E "linear-gradient.*#FF7F50.*#" src/app/(marketing)/_components/HeroBookend.tsx | grep -v "rgba"` returns 0 matches
    - NO "AI" string in user-facing copy paths (anywhere outside comments): `grep -nE "\bAI\b" src/app/(marketing)/_components/HeroBookend.tsx | grep -v "^\s*//\|^\s*\*"` returns 0 matches
    - `pnpm tsc --noEmit` exits 0 (no type errors)
    - `pnpm lint -- src/app/\(marketing\)/_components/HeroBookend.tsx` exits 0 (or matches Phase 1 baseline of zero new warnings)
  </acceptance_criteria>
  <done>HeroBookend.tsx renders the exact bookend layout (Spotlight + badge + H1 with fused WordRotate + sub + dual CTA + Phase 3 placeholder gap), gated on prefers-reduced-motion (WordRotate→static, BorderBeam→null, AnimatedShinyText→static, Spotlight→static), gated on viewport entry (WordRotate IO), TypeScript clean, lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party-registry → repo | shadcn CLI fetches Magic UI + Aceternity component source from registries (npm + aceternity); installed source becomes part of bundle |
| URL hash → DOM | CTA `href="#demo"` / `href="#pricing"` link to anchors managed by `v3/page.tsx` (Plan 03); no user input flows here |
| prefers-reduced-motion media query | OS-level user setting controls WordRotate cycling / shimmer / BorderBeam — bypass would be motion-attack vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | shadcn registry install | mitigate | Registry safety review timestamped 2026-05-24 in UI-SPEC § Registry Safety; Task 1 re-greps installed files for `fetch(`, `eval(`, `process.env`, `dangerouslySetInnerHTML` post-install and aborts on any flag |
| T-02-02 | Information disclosure (XSS) | WordRotate / AnimatedShinyText / ShimmerButton | mitigate | All Phase 2 copy is statically embedded string literals — no user-derived content, no `dangerouslySetInnerHTML`, no template-string injection. Task 2 acceptance forbids `dangerouslySetInnerHTML` |
| T-02-03 | Open redirect | CTA `href` | accept | Both CTA hrefs are local hash anchors (`#demo`, `#pricing`) — no external URLs, no user-controlled href. No redirect surface |
| T-02-04 | Denial of service (motion-attack bypass) | useReducedMotion gate | mitigate | Three independent guards required: (1) `useReducedMotion()` for WordRotate JS timer, (2) conditional `{!prefersReduced && <BorderBeam />}`, (3) static text replacement for AnimatedShinyText. Acceptance criteria require all three present in component |
| T-02-05 | Repudiation | n/a | accept | No logging / no actions taken; purely presentational |
| T-02-06 | Elevation of privilege | n/a | accept | No auth / no role checks in this layer |
</threat_model>

<verification>
- All 5 third-party primitives present at `src/components/ui/`
- `pnpm tsc --noEmit` exits 0
- `pnpm lint` exits 0 (or no new warnings vs Phase 1 baseline)
- HeroBookend.tsx contains exactly one `"use client"` directive at top
- HeroBookend.tsx imports `useReducedMotion` from `motion/react` (not `framer-motion`)
- HeroBookend.tsx has IntersectionObserver wired to gate WordRotate cycling
- All coral usages are single-stop alpha (no multi-hue gradients) — grep validates
- No "AI" wording in any user-visible copy path
</verification>

<success_criteria>
- HERO-02: H1 follows outcome-pattern, "Predict viral for [word] before you post." present in HeroBookend
- HERO-03: Primary CTA `href="#demo"`, secondary `href="#pricing"` — both grep-verified
- HERO-04: Spotlight rendered with `rgba(255, 127, 80, 0.12)` single-stop alpha
- HERO-06: ShimmerButton + AnimatedShinyText + BorderBeam all rendered in HeroBookend
- HERO-07: prefers-reduced-motion guards present (Spline scope deferred to Phase 3 — HERO-07 in Phase 2 covers WordRotate / shimmer / BorderBeam)
- HERO-09: WordRotate cycles `["creators", "brands", "founders"]` with reduced-motion static fallback
- HERO-11: aria-label on H1 provides full reading order; WordRotate span `aria-live="off"`
- MOTION-01: useReducedMotion guards on every Aceternity/Magic UI primitive
- MOTION-02: IntersectionObserver gate for WordRotate cycling
- MOTION-03: Single-stop coral alpha only (grep validates no multi-hue)
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-01-SUMMARY.md` documenting:
- All 5 installed primitive files + any framer-motion → motion/react rewrites applied
- ShimmerButton / BorderBeam / Spotlight / WordRotate actual installed prop signatures (so Plan 03 + downstream phases can reuse without re-reading source)
- Any deviation from WordRotate "resume from current word" requirement (if primitive lacks pause API and degraded to re-mount-on-re-enter)
- Min-width measurement for "creators" at 64px/700 (desktop) and 40px/700 (mobile) — confirm 290px / 180px or adjust
- Confirmation of all 10 acceptance criteria met
</output>
