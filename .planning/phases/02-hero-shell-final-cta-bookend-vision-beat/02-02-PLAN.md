---
phase: 02
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/_components/VisionBeat.tsx
  - src/app/(marketing)/_components/LandingFooter.tsx
  - src/app/(marketing)/privacy/page.tsx
  - src/app/(marketing)/terms/page.tsx
  - src/app/sitemap.ts
autonomous: true
requirements: [CTA-03, CTA-04, CTA-05, CTA-06, VISION-01]
tags: [server-components, footer, vision, seo, sitemap, stub-routes, accessibility]

must_haves:
  truths:
    - "User sees founder quote 'Virality isn't luck — it's a behavioral signal...' inside Raycast GlassPanel between Pricing and Final CTA"
    - "User sees 4-column footer (Product / Company / Legal / Social) with Sign-in link in Product column"
    - "User sees full-width 'A Numen Machines product' strip with copyright below footer columns"
    - "User clicking 'Privacy Policy' lands on /privacy with H1 + 'Coming soon' body"
    - "User clicking 'Terms of Service' lands on /terms with H1 + 'Coming soon' body"
    - "sitemap.xml lists /privacy and /terms entries with priority 0.3"
    - "All footer text passes WCAG AA contrast (≥ 4.5:1)"
  artifacts:
    - path: "src/app/(marketing)/_components/VisionBeat.tsx"
      provides: "Server-rendered founder quote section wrapped in GlassPanel (D-10, D-11, D-12)"
      contains: "Virality isn't luck"
    - path: "src/app/(marketing)/_components/LandingFooter.tsx"
      provides: "Server-rendered 4-column footer + Numen Machines strip + Sign-in link (D-13..D-16, CTA-03..06)"
      contains: "A Numen Machines product"
    - path: "src/app/(marketing)/privacy/page.tsx"
      provides: "Privacy Policy stub route with canonical (D-14)"
    - path: "src/app/(marketing)/terms/page.tsx"
      provides: "Terms of Service stub route with canonical (D-14)"
    - path: "src/app/sitemap.ts"
      provides: "Extended to include /privacy and /terms (priority 0.3)"
  key_links:
    - from: "src/app/(marketing)/_components/VisionBeat.tsx"
      to: "src/components/primitives/GlassPanel"
      via: "import"
      pattern: "GlassPanel"
    - from: "src/app/(marketing)/_components/LandingFooter.tsx"
      to: "lucide-react"
      via: "icon imports"
      pattern: "Twitter|Linkedin|Music|Instagram"
    - from: "src/app/(marketing)/_components/LandingFooter.tsx"
      to: "/login, /privacy, /terms"
      via: "next/link href"
      pattern: 'href="/login"|href="/privacy"|href="/terms"'
---

<objective>
Build the three static surfaces of Phase 2 that have no motion or interactivity: the Vision Beat founder quote, the 4-column LandingFooter, and the /privacy + /terms stub routes. Extend sitemap.ts to register the new routes.

**Purpose:** All five files are independent of HeroBookend (Plan 01) and the assembly route (Plan 03). They have zero file overlap with Plan 01, so they execute in parallel in Wave 1.

**Output:** 4 new files (VisionBeat, LandingFooter, privacy/page, terms/page) + 1 modified file (sitemap.ts). All server components, no client JS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-CONTEXT.md
@.planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-UI-SPEC.md
@.planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-PATTERNS.md
@./CLAUDE.md
@src/components/primitives/GlassPanel.tsx
@src/app/sitemap.ts
@src/app/(marketing)/coming-soon/page.tsx
@src/app/(marketing)/_components/SectionShell.tsx

<interfaces>
<!-- Contracts from existing Phase 1 / repo. -->

From src/components/primitives/GlassPanel.tsx:
```ts
export interface GlassPanelProps {
  children: ReactNode;
  className?: string;
  style?: CSSProperties;
  as?: "div" | "section" | "article" | "aside";
}
export function GlassPanel(props: GlassPanelProps): JSX.Element;
```
GlassPanel is a `"use client"` component but accepts server-component children — VisionBeat can remain a server component while rendering `<GlassPanel>` as a client island.

From `lucide-react` (already installed via shadcn new-york preset):
```ts
export const Twitter: LucideIcon;     // for X (UI-SPEC § Footer Contract names "Twitter or XIcon")
export const Linkedin: LucideIcon;
export const Music: LucideIcon;       // TikTok placeholder per D-15
export const Instagram: LucideIcon;
```

From `next/link`:
```ts
import Link from "next/link";
<Link href="/privacy">Privacy Policy</Link>
```

From `next` Metadata API (used by stub pages):
```ts
import type { Metadata } from "next";
export const metadata: Metadata = { title, description, alternates: { canonical } };
```

From src/app/sitemap.ts (current shape, lines 1-30):
```ts
import type { MetadataRoute } from 'next';
export default function sitemap(): MetadataRoute.Sitemap {
  const base = 'https://virtuna.ai';
  const now = new Date();
  return [
    { url: `${base}/`, lastModified: now, changeFrequency: 'weekly', priority: 1.0 },
    { url: `${base}/#demo`, lastModified: now, changeFrequency: 'monthly', priority: 0.9 },
    { url: `${base}/#pricing`, lastModified: now, changeFrequency: 'monthly', priority: 0.9 },
  ];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build VisionBeat.tsx server component (founder quote in GlassPanel)</name>
  <files>src/app/(marketing)/_components/VisionBeat.tsx</files>
  <read_first>
    .planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-UI-SPEC.md (section "Vision Beat Contract"),
    .planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-PATTERNS.md (section "VisionBeat.tsx"),
    .planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-CONTEXT.md (D-10, D-11, D-12),
    src/components/primitives/GlassPanel.tsx (zero-config 4-prop contract),
    src/app/(marketing)/_components/SectionShell.tsx (section landmark pattern),
    CLAUDE.md (Raycast 137deg gradient, 5px blur, 12px radius — confirms GlassPanel CSS values)
  </read_first>
  <action>
    Create `src/app/(marketing)/_components/VisionBeat.tsx` as a **server component** (NO `"use client"` directive — content is fully static per D-12). GlassPanel itself is a client island but accepts server-component children, so this composition is legal in Next.js App Router.

    File body — implement exactly per UI-SPEC § Vision Beat Contract (NOT a "v1" or simplified pull-quote — the GlassPanel framing is locked per D-11):

    ```tsx
    import type { JSX } from "react";
    import { GlassPanel } from "@/components/primitives/GlassPanel";

    /**
     * VisionBeat (VISION-01) — founder vision quote between #pricing and #final-cta.
     *
     * Behavioral-science thesis angle (D-10), Raycast GlassPanel container (D-11),
     * plain-text attribution with NO photo (D-12, REQUIREMENTS.md VISION-01).
     *
     * Server component — fully static, no interactivity.
     */
    export function VisionBeat(): JSX.Element {
      return (
        <section
          id="vision-beat"
          aria-label="Founder vision"
          className="bg-background py-16 px-6"
        >
          <div className="max-w-2xl mx-auto">
            <GlassPanel className="p-8">
              <blockquote
                className="text-xl font-normal leading-[1.6] text-foreground italic"
                style={{ letterSpacing: '0.1px' }}
              >
                Virality isn&apos;t luck &mdash; it&apos;s a behavioral signal. We built Virtuna
                to surface that signal before you bet on the post.
              </blockquote>
              <p
                className="mt-4 text-sm font-normal text-foreground-muted not-italic"
                style={{ letterSpacing: '0.2px' }}
              >
                &mdash; Davide Loreti, Founder, Virtuna
              </p>
            </GlassPanel>
          </div>
        </section>
      );
    }
    ```

    Notes:
    - Use `&apos;` and `&mdash;` HTML entities (Phase 1 lint convention — avoids `react/no-unescaped-entities` warnings).
    - Quote text is EXACTLY per D-10: `Virality isn't luck — it's a behavioral signal. We built Virtuna to surface that signal before you bet on the post.` — no rewording.
    - Attribution is EXACTLY per D-12: `— Davide Loreti, Founder, Virtuna` — no role chip, no coral underline, no photo, no signature image.
    - NO decorative large `"` glyph or CSS `::before` quote marks (per UI-SPEC "Quotation mark style" — GlassPanel IS the framing device).
    - GlassPanel passed only `className="p-8"` — DO NOT pass `tint`, `blur`, `opacity`, `innerGlow`, `borderGlow` (those props don't exist on the zero-config GlassPanel per CLAUDE.md).
    - `<section aria-label="Founder vision">` provides landmark for screen readers (per UI-SPEC § Accessibility Contract).
  </action>
  <verify>
    <automated>test -f src/app/\(marketing\)/_components/VisionBeat.tsx && grep -c '"use client"' src/app/\(marketing\)/_components/VisionBeat.tsx | grep -q "^0$" && grep -c 'GlassPanel' src/app/\(marketing\)/_components/VisionBeat.tsx && grep -c "Virality" src/app/\(marketing\)/_components/VisionBeat.tsx && grep -c 'aria-label="Founder vision"' src/app/\(marketing\)/_components/VisionBeat.tsx && grep -c 'Davide Loreti, Founder, Virtuna' src/app/\(marketing\)/_components/VisionBeat.tsx && pnpm tsc --noEmit 2>&1 | grep -c "VisionBeat" | tr -d ' ' | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/app/(marketing)/_components/VisionBeat.tsx`
    - NO `"use client"` directive (`grep -c '"use client"'` returns 0)
    - Imports `GlassPanel` from `@/components/primitives/GlassPanel` (grep returns ≥1)
    - Contains EXACT D-10 quote substring `Virality isn` and `behavioral signal` and `bet on the post` (grep each returns ≥1)
    - Contains EXACT D-12 attribution `Davide Loreti, Founder, Virtuna` (grep returns ≥1)
    - Contains `id="vision-beat"` and `aria-label="Founder vision"` (grep each returns ≥1)
    - Container uses `max-w-2xl mx-auto` and `py-16 px-6` per UI-SPEC
    - GlassPanel receives `className="p-8"` (grep `className="p-8"` returns ≥1)
    - Does NOT pass `tint=`, `blur=`, `opacity=`, `innerGlow=`, `borderGlow=` to GlassPanel (grep each returns 0)
    - `pnpm tsc --noEmit` exits 0 (no type errors in VisionBeat)
  </acceptance_criteria>
  <done>VisionBeat.tsx renders the locked quote + attribution inside Raycast GlassPanel, server-component, TypeScript clean, A11y landmark present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build LandingFooter.tsx server component (4 columns + Sign-in + Numen Machines strip)</name>
  <files>src/app/(marketing)/_components/LandingFooter.tsx</files>
  <read_first>
    .planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-UI-SPEC.md (section "Footer Contract" — full grid, headers, links, social icons, Sign-in, Numen Machines strip, WCAG table),
    .planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-PATTERNS.md (section "LandingFooter.tsx"),
    .planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-CONTEXT.md (D-13, D-14, D-15, D-16),
    src/components/layout/footer.tsx (analog — read its column/link structure but adapt to Raycast tokens and replace phosphor-icons with lucide-react),
    CLAUDE.md (Raycast borders: 6% standard, 4% sub-strips; foreground-muted #848586, foreground-secondary #9c9c9d)
  </read_first>
  <action>
    Create `src/app/(marketing)/_components/LandingFooter.tsx` as a **server component** (CTA-03: no client JS — no `"use client"` directive).

    Implement EXACTLY per UI-SPEC § Footer Contract — no "minimal version", no deferred Sign-in link, no skipping disabled-link treatment.

    **Top of file:**
    ```tsx
    import Link from "next/link";
    import { Twitter, Linkedin, Music, Instagram } from "lucide-react";
    import type { JSX } from "react";

    /**
     * LandingFooter (CTA-03..06, D-13..16) — server-rendered 4-column footer.
     *
     * - 4 columns: Product / Company / Legal / Social (D-13)
     * - Stub routes: /privacy, /terms are real (D-14); About/Careers/Blog are aria-disabled stubs
     * - Sign-in link at bottom of Product column (CTA-06)
     * - Full-width "A Numen Machines product" strip + copyright below grid (CTA-04, D-16)
     * - Social: X / LinkedIn / TikTok / Instagram via lucide-react (D-15)
     * - WCAG AA verified (all text ≥ 4.5:1 contrast — UI-SPEC § Footer Contract WCAG table)
     */
    export function LandingFooter(): JSX.Element { /* ... */ }
    ```

    **Footer outer shell** (per UI-SPEC):
    ```tsx
    <footer
      role="contentinfo"
      className="bg-background border-t"
      style={{ borderTopColor: 'rgba(255, 255, 255, 0.06)' }}
    >
      <div className="max-w-7xl mx-auto px-6 pt-16 pb-8">
        {/* 4-column grid */}
        <nav aria-label="Footer navigation">
          <div className="grid grid-cols-1 gap-8 sm:grid-cols-2 sm:gap-8 md:grid-cols-4 md:gap-16">
            {/* 4 columns here */}
          </div>
        </nav>
        {/* Numen Machines strip */}
      </div>
    </footer>
    ```

    **Column 1 — Product** (active links + Sign-in at bottom per CTA-06):
    - Header: `Product` (14px, 400, foreground-muted, uppercase, letter-spacing 0.08em, mb-4)
    - Links (in order): `Demo` → `#demo`, `How it works` → `#how-it-works`, `Pricing` → `#pricing`, `Three Surfaces` → `#surfaces`
    - Each link: `text-sm text-foreground-secondary hover:text-foreground transition-colors focus-visible:outline focus-visible:outline-2` with inline `outlineColor: '#FF7F50'` and `lineHeight: 1.75`
    - Below the 4 links, divider strip then Sign-in link:
      ```tsx
      <div className="mt-4 pt-4" style={{ borderTop: '1px solid rgba(255,255,255,0.04)' }}>
        <Link
          href="/login"
          className="text-sm text-foreground-muted hover:text-foreground transition-colors"
        >
          Sign in
        </Link>
      </div>
      ```

    **Column 2 — Company** (all disabled per D-14): `About`, `Careers`, `Blog` — each as:
    ```tsx
    <a
      href="#"
      aria-disabled="true"
      tabIndex={-1}
      className="text-sm text-foreground-secondary"
      style={{ opacity: 0.35, cursor: 'default', pointerEvents: 'none', lineHeight: 1.75 }}
    >
      About
    </a>
    ```
    NO tooltip, NO "coming soon" label — visual dimming sufficient (per UI-SPEC).

    **Column 3 — Legal** (real routes — Task 3 creates the stub pages):
    - Header: `Legal`
    - Links: `Privacy Policy` → `/privacy` (next/link), `Terms of Service` → `/terms` (next/link)
    - Same active-link className as Product column

    **Column 4 — Social** (D-15: X / LinkedIn / TikTok / Instagram via lucide-react):
    - Header: `Social`
    - Icons in `<div className="flex gap-2">` row, each:
      ```tsx
      <a
        href="https://x.com/virtuna"
        target="_blank"
        rel="noopener noreferrer"
        aria-label="Follow Virtuna on X"
        className="flex h-11 w-11 items-center justify-center text-foreground-muted hover:text-foreground transition-colors focus-visible:outline focus-visible:outline-2"
        style={{ outlineColor: '#FF7F50' }}
      >
        <Twitter className="w-[18px] h-[18px]" aria-hidden="true" />
      </a>
      ```
      Repeat for LinkedIn (`https://linkedin.com/company/virtuna`, label "Follow Virtuna on LinkedIn", `<Linkedin />`), TikTok (`https://www.tiktok.com/@virtuna`, label "Follow Virtuna on TikTok", `<Music />`), Instagram (`https://instagram.com/virtuna`, label "Follow Virtuna on Instagram", `<Instagram />`).
    - Hit-target 44×44 via `h-11 w-11` (WCAG 2.5.5). Icon visual size 18px via `w-[18px] h-[18px]`.

    **Numen Machines strip** (below the 4-column grid, full-width, per CTA-04 + D-16):
    ```tsx
    <div
      className="mt-12 pt-6 flex flex-col items-center gap-2"
      style={{ borderTop: '1px solid rgba(255, 255, 255, 0.04)' }}
    >
      <a
        href="https://numenmachines.com"
        target="_blank"
        rel="noopener noreferrer"
        className="text-sm text-foreground-muted hover:text-foreground transition-colors"
      >
        A Numen Machines product
      </a>
      <p className="text-sm text-foreground-muted" style={{ opacity: 0.6 }}>
        &copy; 2026 Numen Machines. All rights reserved.
      </p>
    </div>
    ```
    Per D-16: text-only lockup is acceptable for Phase 2 (SVG asset gated on Phase 9 content gate per REQUIREMENTS.md). If `public/landing/numen-machines-lockup.svg` exists at execution time, use `<Image src="/landing/numen-machines-lockup.svg" alt="Numen Machines" width={120} height={16} />` from `next/image` above the text line; otherwise text-only is final for Phase 2.

    **Column header styling** (shared across all 4 columns) — UI-SPEC § Footer Contract:
    ```tsx
    <p
      className="text-sm font-normal text-foreground-muted uppercase mb-4"
      style={{ letterSpacing: '0.08em' }}
    >
      Product
    </p>
    ```
    Use `<p>` (NOT `<h2>`/`<h3>`) — per UI-SPEC § Accessibility Contract "Footer column headers are `<p>` or `<span>` styled as label, not `<h*>` — prevents hierarchy gaps".

    Column wrapper `<ul className="flex flex-col gap-2">` with each link in `<li>` for semantic list (or use `<div className="flex flex-col gap-2">` if ul/li hierarchy conflicts with the Sign-in divider in Product column — planner's call).
  </action>
  <verify>
    <automated>test -f src/app/\(marketing\)/_components/LandingFooter.tsx && grep -c '"use client"' src/app/\(marketing\)/_components/LandingFooter.tsx | grep -q "^0$" && grep -c 'role="contentinfo"' src/app/\(marketing\)/_components/LandingFooter.tsx && grep -c 'aria-label="Footer navigation"' src/app/\(marketing\)/_components/LandingFooter.tsx && grep -c 'href="/login"' src/app/\(marketing\)/_components/LandingFooter.tsx && grep -c 'href="/privacy"' src/app/\(marketing\)/_components/LandingFooter.tsx && grep -c 'href="/terms"' src/app/\(marketing\)/_components/LandingFooter.tsx && grep -c 'A Numen Machines product' src/app/\(marketing\)/_components/LandingFooter.tsx && grep -c 'aria-disabled="true"' src/app/\(marketing\)/_components/LandingFooter.tsx && grep -cE 'from "lucide-react"' src/app/\(marketing\)/_components/LandingFooter.tsx && pnpm tsc --noEmit 2>&1 | grep -c "LandingFooter" | tr -d ' ' | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/app/(marketing)/_components/LandingFooter.tsx`
    - NO `"use client"` directive (server component per CTA-03)
    - Contains `role="contentinfo"` (grep returns ≥1)
    - Contains `aria-label="Footer navigation"` on `<nav>` (grep returns ≥1)
    - Sign-in link present: `href="/login"` (grep returns exactly 1)
    - Legal links present: `href="/privacy"` and `href="/terms"` (grep each returns ≥1)
    - All 3 disabled links have `aria-disabled="true"` AND `tabIndex={-1}` (grep `aria-disabled="true"` returns ≥3; grep `tabIndex={-1}` returns ≥3)
    - All 4 social icons rendered with `target="_blank"` + `rel="noopener noreferrer"` + `aria-label="Follow Virtuna on` prefix (grep `aria-label="Follow Virtuna on` returns ≥4)
    - Imports from `lucide-react` include exactly `Twitter, Linkedin, Music, Instagram` (grep returns ≥1 import line containing all 4)
    - Numen Machines strip present: `A Numen Machines product` text + `https://numenmachines.com` href (grep each returns 1)
    - Column headers use `<p>` (not `<h2>`/`<h3>`) — `grep -E "<h[1-6][^>]*>Product</h[1-6]>"` returns 0
    - `pnpm tsc --noEmit` exits 0 for this file
    - `pnpm lint -- src/app/\(marketing\)/_components/LandingFooter.tsx` exits 0
  </acceptance_criteria>
  <done>LandingFooter.tsx server component renders 4-column grid + Sign-in + Numen Machines strip per locked UI-SPEC, all WCAG/disabled-link attributes present, lint + types clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create /privacy + /terms stub routes and extend sitemap.ts</name>
  <files>
    src/app/(marketing)/privacy/page.tsx,
    src/app/(marketing)/terms/page.tsx,
    src/app/sitemap.ts
  </files>
  <read_first>
    .planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-UI-SPEC.md (section "Stub Page Contract"),
    .planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-PATTERNS.md (sections "src/app/(marketing)/privacy/page.tsx", "src/app/sitemap.ts"),
    .planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-CONTEXT.md (D-14),
    src/app/(marketing)/coming-soon/page.tsx (exact analog — copy structure, adapt copy),
    src/app/sitemap.ts (current shape — extend, don't replace)
  </read_first>
  <action>
    **A. Create `src/app/(marketing)/privacy/page.tsx`** — server component stub (per D-14 + UI-SPEC § Stub Page Contract):

    ```tsx
    import type { Metadata } from "next";

    export const metadata: Metadata = {
      title: "Privacy Policy | Virtuna",
      description: "Privacy Policy for Virtuna.",
      alternates: {
        canonical: "https://virtuna.ai/privacy",
      },
    };

    export default function PrivacyPage() {
      return (
        <main className="flex min-h-screen flex-col items-center justify-center bg-background px-4">
          <div className="text-center max-w-xl">
            <h1 className="text-4xl font-normal text-foreground md:text-5xl">
              Privacy Policy
            </h1>
            <p className="mt-4 text-base text-foreground-muted">
              Coming soon. Last updated: 2026.
            </p>
          </div>
        </main>
      );
    }
    ```

    **B. Create `src/app/(marketing)/terms/page.tsx`** — identical structure to A, with these substitutions:
    - `title: "Terms of Service | Virtuna"`
    - `description: "Terms of Service for Virtuna."`
    - `canonical: "https://virtuna.ai/terms"`
    - `<h1>Terms of Service</h1>`
    - Function name `TermsPage`

    Both files are server components — NO `"use client"` directive. Body copy is intentionally minimal per D-14 ("real minimal Next.js placeholder pages... single H1 + 'Coming soon' body + canonical link... legal page-existence is a basic investor-credibility signal").

    **C. Modify `src/app/sitemap.ts`** — add `/privacy` and `/terms` entries with `priority: 0.3` and `changeFrequency: 'yearly'`. Read the current file first, then extend the returned array. Final file shape:

    ```ts
    import type { MetadataRoute } from 'next';

    export default function sitemap(): MetadataRoute.Sitemap {
      const base = 'https://virtuna.ai';
      const now = new Date();
      return [
        { url: `${base}/`, lastModified: now, changeFrequency: 'weekly', priority: 1.0 },
        { url: `${base}/#demo`, lastModified: now, changeFrequency: 'monthly', priority: 0.9 },
        { url: `${base}/#pricing`, lastModified: now, changeFrequency: 'monthly', priority: 0.9 },
        { url: `${base}/privacy`, lastModified: now, changeFrequency: 'yearly', priority: 0.3 },
        { url: `${base}/terms`, lastModified: now, changeFrequency: 'yearly', priority: 0.3 },
      ];
    }
    ```

    Preserve existing JSDoc comment block at top of sitemap.ts. Do NOT rewrite — extend.

    Verify each stub page resolves: after creation, run `pnpm build` and confirm `.next/server/app/(marketing)/privacy/page.html` and `.next/server/app/(marketing)/terms/page.html` are listed in build output as static routes (○ marker).
  </action>
  <verify>
    <automated>test -f src/app/\(marketing\)/privacy/page.tsx && test -f src/app/\(marketing\)/terms/page.tsx && grep -c '"use client"' src/app/\(marketing\)/privacy/page.tsx | grep -q "^0$" && grep -c '"use client"' src/app/\(marketing\)/terms/page.tsx | grep -q "^0$" && grep -c "canonical: \"https://virtuna.ai/privacy\"" src/app/\(marketing\)/privacy/page.tsx && grep -c "canonical: \"https://virtuna.ai/terms\"" src/app/\(marketing\)/terms/page.tsx && grep -c "Privacy Policy | Virtuna" src/app/\(marketing\)/privacy/page.tsx && grep -c "Terms of Service | Virtuna" src/app/\(marketing\)/terms/page.tsx && grep -c '`${base}/privacy`' src/app/sitemap.ts && grep -c '`${base}/terms`' src/app/sitemap.ts && grep -c "priority: 0.3" src/app/sitemap.ts | awk '{ exit ($1 >= 2) ? 0 : 1 }' && pnpm build 2>&1 | grep -E "(/privacy|/terms)" | grep -c "○\|●" | awk '{ exit ($1 >= 2) ? 0 : 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(marketing)/privacy/page.tsx` exists, NO `"use client"`, exports default function, has canonical `https://virtuna.ai/privacy` in metadata, renders `<h1>Privacy Policy</h1>` + "Coming soon. Last updated: 2026."
    - `src/app/(marketing)/terms/page.tsx` exists, NO `"use client"`, has canonical `https://virtuna.ai/terms` in metadata, renders `<h1>Terms of Service</h1>` + "Coming soon. Last updated: 2026."
    - `src/app/sitemap.ts` contains EXACTLY 5 url entries: `/`, `/#demo`, `/#pricing`, `/privacy`, `/terms` (grep `url:` returns exactly 5)
    - The two new entries have `priority: 0.3` and `changeFrequency: 'yearly'`
    - Original 3 sitemap entries unchanged (still have priority 1.0, 0.9, 0.9 — grep validates)
    - Original sitemap JSDoc comment preserved (grep `FOUND-10` returns ≥1)
    - `pnpm build` exits 0; build output lists `/privacy` and `/terms` as static routes (○ marker)
    - `curl http://localhost:3000/privacy` (after `pnpm dev` or build serve) returns HTML containing "Privacy Policy" and "Coming soon"
  </acceptance_criteria>
  <done>Both stub routes resolve as static server-rendered pages with locked canonical URLs and minimal body; sitemap extended with both routes at priority 0.3; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external-link → new tab | Footer social links + Numen Machines link open in new tabs via `target="_blank"` |
| URL hash → footer link | Footer href `#demo`, `#pricing`, `#how-it-works`, `#surfaces` resolve on same page; `/login`, `/privacy`, `/terms` resolve to internal routes |
| static route render | /privacy and /terms render statically — no user input, no server actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering (reverse tabnabbing) | Footer social links + Numen Machines link | mitigate | All `target="_blank"` anchors set `rel="noopener noreferrer"` — Task 2 acceptance grep validates each social link has both attributes |
| T-02-08 | Open redirect | Footer link hrefs | mitigate | All hrefs are hardcoded string literals (no user-derived URLs, no template interpolation). Internal: `/login`, `/privacy`, `/terms`, hash anchors. External: 5 fixed URLs (numenmachines.com + 4 social). Acceptance criteria require exact literal matches |
| T-02-09 | Information disclosure | Stub pages /privacy /terms | accept | Bodies are static placeholders; no PII, no business logic, no data fetches. "Coming soon" body matches D-14 minimal contract |
| T-02-10 | Information disclosure (XSS in VisionBeat) | VisionBeat blockquote | mitigate | Quote text is a static string literal — no user-derived content, no `dangerouslySetInnerHTML`. HTML entities (`&apos;`, `&mdash;`) escape special chars per Phase 1 lint convention |
| T-02-11 | Denial of service | Footer / Vision Beat | accept | Both server-rendered, no JS bundle impact, no event listeners |
| T-02-12 | Elevation of privilege | /privacy /terms | accept | Pure static rendering; no auth surfaces |
</threat_model>

<verification>
- 5 files created/modified as listed in frontmatter
- All 3 stub-related grep checks pass (canonical, title, sitemap entries)
- VisionBeat + LandingFooter both server components (no `"use client"`)
- `pnpm tsc --noEmit` exits 0
- `pnpm lint` exits 0 (or no new warnings vs baseline)
- `pnpm build` succeeds, /privacy + /terms listed as static routes
- `curl /privacy` and `curl /terms` (with build serve) each return their respective H1 text
</verification>

<success_criteria>
- VISION-01: Founder vision quote between Pricing slot and Final CTA section, attributed to "Davide Loreti, Founder, Virtuna", no photo, GlassPanel container
- CTA-03: 4-column footer, server-rendered, no client JS
- CTA-04: Footer includes "A Numen Machines product" lockup with link to numenmachines.com
- CTA-05: All footer text passes WCAG AA contrast (UI-SPEC § Footer Contract WCAG table — pre-verified at spec time)
- CTA-06: Footer "Sign in" link present at bottom of Product column, links to /login
- /privacy and /terms are real Next.js stub pages with canonical link tags
- Sitemap.ts registers both new routes at priority 0.3, changeFrequency yearly
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-shell-final-cta-bookend-vision-beat/02-02-SUMMARY.md` documenting:
- VisionBeat: server-component status confirmed, GlassPanel client island composition works
- LandingFooter: server-component status confirmed, exact link counts (Product 4+Sign-in, Company 3 disabled, Legal 2, Social 4) verified
- Stub routes: canonical URLs match production format, build output shows static rendering
- Sitemap: extended from 3 to 5 entries, original JSDoc preserved
- Any SVG asset usage (if `numen-machines-lockup.svg` was present at execution time)
- WCAG note: contrast pre-verified in UI-SPEC; no new audit needed unless tokens changed
</output>
