---
phase: 55-glass-docs-regression
plan: 02
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - BRAND-BIBLE.md
  - .planning/BRAND-BIBLE.md  # DELETE or replace with symlink
  - docs/tokens.md
  - docs/components.md
  - docs/component-index.md
  - docs/contributing.md
  - docs/design-specs.json
  - docs/accessibility.md
  - ~/.claude/projects/-Users-davideloreti/memory/MEMORY.md  # DOCS-04
autonomous: true

must_haves:
  truths:
    - "BRAND-BIBLE.md contains 'Virtuna Design System' branding with Raycast-derived design language (not iOS 26 Liquid Glass)"
    - "All font references in docs say Inter (not Funnel Display or Satoshi)"
    - "All border opacity values in docs say 0.06 base / 0.10 hover (not 0.08 or 0.10 base)"
    - "All color tokens in docs match globals.css @theme values exactly"
    - "GradientGlow and GradientMesh do not appear in any design doc as available components"
    - "GlassPanel docs reflect simplified API (no tint/blur/opacity/innerGlow/borderGlow props)"
    - "MEMORY.md Raycast Design Language Rules section updated with final verified values"
  artifacts:
    - path: "BRAND-BIBLE.md"
      provides: "Complete design language reference with Raycast-accurate values"
      contains: "Virtuna Design System"
    - path: "docs/tokens.md"
      provides: "Token reference with correct Inter font and hex color values"
      contains: "Inter"
    - path: "docs/component-index.md"
      provides: "Component index without deleted components"
  key_links:
    - from: "BRAND-BIBLE.md"
      to: "src/app/globals.css"
      via: "Token values documented must match @theme block values"
      pattern: "rgba\\(255,255,255,0\\.06\\)"
---

<objective>
Rewrite BRAND-BIBLE.md from scratch as a complete Virtuna Design System reference with all Raycast-accurate values, and update all design docs to reflect the final component API and correct token values.

Purpose: After Plan 01 changes the component code, all documentation must reflect the actual state. This eliminates the 13+ known doc/code mismatches identified during research and replaces the outdated "iOS 26 Liquid Glass" direction with "Raycast Design Language."

Output: Rewritten BRAND-BIBLE.md, updated tokens.md, components.md, component-index.md, contributing.md, design-specs.json, and accessibility.md.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-glass-docs-regression/55-CONTEXT.md
@.planning/phases/55-glass-docs-regression/55-RESEARCH.md
@.planning/phases/55-glass-docs-regression/55-01-SUMMARY.md
@src/app/globals.css
@src/components/primitives/GlassPanel.tsx
@src/components/ui/card.tsx
@BRAND-BIBLE.md
@docs/tokens.md
@docs/components.md
@docs/component-index.md
@docs/contributing.md
@docs/design-specs.json
@docs/accessibility.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite BRAND-BIBLE.md as complete Virtuna Design System reference</name>
  <files>
    BRAND-BIBLE.md
    .planning/BRAND-BIBLE.md
  </files>
  <action>
    **Rewrite `BRAND-BIBLE.md` at repo root from scratch.** This is a full design language document, not a patch.

    **Structure the document with these sections:**

    1. **Virtuna Design System** -- Title and 2-line overview. "Raycast-derived dark design language with coral (#FF7F50) branding."
    2. **Design Direction** -- Raycast Design Language. Clean, dark, minimal. No colored tinting, no glow effects. Color is used only for accents and interactive elements. Mention Raycast origin in an internal-notes callout.
    3. **Color Tokens** -- Every token from globals.css @theme block, organized by category:
       - Background: `--color-background: #07080a` (body), `--color-surface: #18191a` (elevated), `--color-surface-elevated: #1e1f21` (higher)
       - Foreground: `--color-foreground: #f5f5f5`, `--color-foreground-muted: #848586`, `--color-foreground-subtle: #5a5b5c`
       - Brand: `--color-accent: #FF7F50` (coral)
       - Borders: `--color-border: rgba(255,255,255,0.06)` base, hover `rgba(255,255,255,0.10)`
       - Read actual values from globals.css -- do NOT copy from this plan. Verify every value.
    4. **Typography** -- Font: Inter (all weights via next/font/google). Body letter-spacing: 0.2px. Body line-height: 1.5 (24px at 16px base). Antialiased rendering. Size scale from globals.css.
    5. **Spacing & Radius** -- Radius scale: 4/6/8/12/16/20/24px. Cards = 12px, inputs = 8px, buttons = 8px, header = 16px, modals = 12px.
    6. **Surface Patterns** -- Cards (bg-transparent, 6% border, 12px radius, 5% inset shadow, hover: bg-white/2% only), Glass (GlassPanel: 137deg gradient + 5px blur + 6% border + 15% inset shadow), Modals (solid opaque dark bg, NOT glass), Inputs (white/5% bg, 5% border, 8px radius, 42px height).
    7. **Shadows** -- Button shadow (4-layer `shadow-button`), glass inset shadow, card inset shadow (5%). Document exact CSS values.
    8. **Components Overview** -- List all available primitives (GlassPanel, GlassPill, GlassInput, GlassTextarea, TrafficLights) and UI components (Card, GlassCard, Button, Badge, etc.). Note that GradientGlow and GradientMesh have been removed.
    9. **Do / Don't** -- 5-8 quick rules. Do: use bg-transparent for cards. Don't: add colored glass tints. Do: use inline backdrop-filter. Don't: add translate-y hover effects on cards. Do: use Inter for all text. Don't: use oklch for dark colors in @theme.

    **For .planning/BRAND-BIBLE.md:** Delete the file entirely. The repo root version is canonical. If `.planning/BRAND-BIBLE.md` is referenced anywhere, note that the canonical location is repo root.

    **Critical accuracy requirement:** Read `src/app/globals.css` at execution time and extract every token value directly. Do NOT use hardcoded values from this plan or the research doc -- they may have changed during Plan 01. The document must be a perfect mirror of the actual codebase state.
  </action>
  <verify>
    Grep BRAND-BIBLE.md for known wrong values:
    - `grep -i "satoshi\|funnel display\|liquid glass\|iOS 26" BRAND-BIBLE.md` should return zero matches
    - `grep "0.08" BRAND-BIBLE.md` should return zero matches (border is 0.06, not 0.08)
    - `grep "#18191c" BRAND-BIBLE.md` should return zero matches (surface is #18191a)
    - `grep "#6a6b6c" BRAND-BIBLE.md` should return zero matches (muted is #848586)
    - `grep "GradientGlow\|GradientMesh" BRAND-BIBLE.md` should return zero matches (deleted components)
    - `.planning/BRAND-BIBLE.md` should not exist
  </verify>
  <done>
    BRAND-BIBLE.md is a complete, accurate Virtuna Design System reference. All 13+ known inaccuracies are corrected. Design direction is "Raycast Design Language." Every token value matches globals.css. `.planning/BRAND-BIBLE.md` is deleted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all design docs with correct values and simplified component API</name>
  <files>
    docs/tokens.md
    docs/components.md
    docs/component-index.md
    docs/contributing.md
    docs/design-specs.json
    docs/accessibility.md
  </files>
  <action>
    Read each file and fix all known mismatches identified in research. Read globals.css at execution time for ground truth.

    **docs/tokens.md:**
    - Replace all `Satoshi` references with `Inter`
    - Replace all `Funnel Display` references -- remove entirely (Inter for all)
    - Fix `--font-sans: Satoshi` to `--font-sans: Inter`
    - Remove `--font-display: Funnel Display` (no display font)
    - Fix `--color-surface: #18191c` to `#18191a`
    - Fix `--color-border: rgba(255,255,255,0.08)` to `rgba(255,255,255,0.06)`
    - Fix `foreground-muted: #6a6b6c` to `#848586`
    - Remove any glass blur scale tokens (xs through 2xl) -- replaced by fixed 5px
    - Remove GradientGlow/GradientMesh token references
    - Verify all remaining values against globals.css

    **docs/components.md:**
    - Update GlassPanel section to show simplified API (4 props: children, className, style, as)
    - Remove all tint/blur/opacity/innerGlow/borderGlow documentation
    - Remove GradientGlow component section entirely
    - Remove GradientMesh component section entirely
    - Remove primitives/GlassCard section (or redirect to ui/card.tsx GlassCard)
    - Update any code examples that use removed props

    **docs/component-index.md:**
    - Remove GradientGlow row from component table
    - Remove GradientMesh row from component table
    - Remove primitives/GlassCard row (or mark as removed, redirect to ui/card.tsx)
    - Update GlassPanel description to match simplified API

    **docs/contributing.md:**
    - Remove GradientGlow and GradientMesh from file structure examples
    - Update primitives list to reflect current components
    - Fix any outdated references to removed patterns

    **docs/design-specs.json:**
    - Fix `"fontFamily"` entries: replace `"Funnel Display"` and `"Satoshi"` with `"Inter"`
    - Verify all numeric values (spacing, radius, etc.) match current globals.css

    **docs/accessibility.md:**
    - Verify contrast ratio examples use correct foreground-muted value (#848586)
    - Ensure documented WCAG ratios are still accurate with current token values
    - Update any references to glass blur levels (no longer variable)

    **Also update showcase page surface hex if wrong:**
    - Check `src/app/(marketing)/showcase/page.tsx` line ~49 for `#18191c` -- change to `#18191a` if present

    **Update MEMORY.md (DOCS-04):**
    - Read `~/.claude/projects/-Users-davideloreti/memory/MEMORY.md`
    - Update the "Raycast Design Language Rules" section with final verified values from globals.css
    - Ensure all token values listed match current codebase state
    - Remove any references to GradientGlow/GradientMesh or old glass patterns
    - Add note about GlassPanel simplified to zero-config (no tint/blur/opacity props)
    - Keep the section concise -- MEMORY.md has a 200-line limit
  </action>
  <verify>
    Run these checks across all docs:
    - `grep -r "Satoshi\|Funnel Display" docs/` should return zero matches
    - `grep -r "#18191c" docs/` should return zero matches
    - `grep -r "0\.08" docs/tokens.md` for border values -- should not show border as 0.08
    - `grep -r "GradientGlow\|GradientMesh" docs/` should return zero matches
    - `grep -r "#6a6b6c" docs/` should return zero matches
    - `pnpm build` succeeds (no broken references in showcase pages)
    - MEMORY.md "Raycast Design Language Rules" section contains current values
  </verify>
  <done>
    All 8 design doc files contain correct Raycast-accurate values. MEMORY.md updated with final Raycast values. Font is Inter everywhere. Border is 0.06 everywhere. Surface is #18191a. Muted is #848586. GradientGlow/GradientMesh removed from all docs. GlassPanel documented with simplified 4-prop API.
  </done>
</task>

</tasks>

<verification>
1. `grep -ri "satoshi\|funnel display\|liquid glass\|iOS 26" BRAND-BIBLE.md docs/` -- zero matches
2. `grep -r "GradientGlow\|GradientMesh" BRAND-BIBLE.md docs/` -- zero matches
3. `grep -r "#18191c\|#6a6b6c" BRAND-BIBLE.md docs/ src/app/(marketing)/showcase/page.tsx` -- zero matches
4. BRAND-BIBLE.md contains "Virtuna Design System" heading
5. BRAND-BIBLE.md contains `rgba(255,255,255,0.06)` for borders
6. docs/tokens.md contains `Inter` as the font family
7. docs/component-index.md does not list GradientGlow or GradientMesh
8. `.planning/BRAND-BIBLE.md` does not exist on disk
</verification>

<success_criteria>
- BRAND-BIBLE.md is a complete design language document branded "Virtuna Design System"
- All 13+ known inaccuracies corrected across all doc files
- Every documented token value matches the actual globals.css @theme block
- GlassPanel documented with simplified API (no tint/blur/opacity/innerGlow/borderGlow)
- GradientGlow and GradientMesh removed from all documentation
- .planning/BRAND-BIBLE.md deleted (single canonical location at repo root)
</success_criteria>

<output>
After completion, create `.planning/phases/55-glass-docs-regression/55-02-SUMMARY.md`
</output>
