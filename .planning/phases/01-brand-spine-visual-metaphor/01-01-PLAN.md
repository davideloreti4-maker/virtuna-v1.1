---
phase: 01-brand-spine-visual-metaphor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/reference/BRAND-SPINE.md
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
  - BRAND-04
user_setup: []

must_haves:
  truths:
    - "BRAND-SPINE.md exists at .planning/reference/BRAND-SPINE.md"
    - "Canonical one-liner 'Your audience, simulated.' appears verbatim in the doc"
    - "All 7 required H2 sections are present (One-Liner, Tone, Preferred Verbs, Banned/Replacement, Numen Machines Lockup, Audience Tuning, How to Use)"
    - "Banned -> Replacement table contains at least the four seed entries (viral, AI, go viral, users)"
    - "Audience Tuning per Viewport table maps each of the 7 viewports to a primary audience"
    - "Numen Machines lockup matrix names at least 5 surfaces (hero, footer, OG, in-product, page titles)"
    - "CONTEXT.md decisions covered: D-01 (full glossary + tone, ~3-4 pages), D-02 (required H2 sections), D-03 (Numen Machines lockup matrix), D-04 (BRAND-SPINE.md is source of truth — enforced downstream by Plan 04 vocab-lint script), D-16 (hero copy in REQUIREMENTS.md is draft starting point)"
  artifacts:
    - path: ".planning/reference/BRAND-SPINE.md"
      provides: "Voice + vocabulary + tone source-of-truth for all customer-facing copy in Phase 2-6"
      min_lines: 120
      contains:
        - "Your audience, simulated"
        - "## 2. Tone Descriptors"
        - "## 3. Preferred Verbs"
        - "## 4. Banned"
        - "## 5. Numen Machines Lockup"
        - "## 6. Audience Tuning"
  key_links:
    - from: ".planning/reference/BRAND-SPINE.md §4 Banned -> Replacement"
      to: "scripts/lint-vocab.mjs (Plan 04)"
      via: "vocab table mirrored in BANNED regex array"
      pattern: "viral|go viral|AI|users"
    - from: ".planning/reference/BRAND-SPINE.md §6 Audience Tuning"
      to: "Phase 2-4 section copy plans"
      via: "per-viewport audience lean dictates voice in build phases"
      pattern: "Hero|Demo|How It Works|Three Surfaces|Science|Social Proof|Pricing"
---

<objective>
Codify Virtuna's brand voice, vocabulary guardrails, and audience tuning as a single source-of-truth markdown reference at `.planning/reference/BRAND-SPINE.md`. This document is the input every Phase 2-6 plan researcher and executor reads before writing any UI string.

Purpose: Lock the brand voice (per D-01, D-02, D-03, D-04) so downstream phases cannot drift on tone, vocabulary, or three-audience framing. Closes BRAND-01..04. Without this artifact, Phase 2 hero copy cannot be verified for vocab compliance and Phase 3-4 section copy has no canonical reference for tone.

Output: One new file: `.planning/reference/BRAND-SPINE.md` (~3-4 pages, 120-200 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-brand-spine-visual-metaphor/01-CONTEXT.md
@.planning/phases/01-brand-spine-visual-metaphor/01-RESEARCH.md
@.planning/phases/01-brand-spine-visual-metaphor/01-PATTERNS.md
@.planning/phases/01-brand-spine-visual-metaphor/01-VALIDATION.md
@BRAND-BIBLE.md
@CLAUDE.md

<style_invariants>
<!-- Extracted from BRAND-BIBLE.md (351 lines) — match this style verbatim. -->
- Use double-hyphen ` -- ` separators, NEVER em-dash `—` (BRAND-BIBLE.md:11, 19, 340-346)
- Open with H1 + blockquote one-liner + `---` separator + first H2 (BRAND-BIBLE.md:1-9 pattern)
- 3-column tables for token-style refs: `| X | Y | Z |` no alignment markers (BRAND-BIBLE.md:30-41)
- Italic two-line footer: `*Virtuna Brand Spine v1.0*` / `*Last updated: YYYY-MM-DD*` (BRAND-BIBLE.md:350-351 pattern)
- H3 uses bold-emphasis labels followed by content (BRAND-BIBLE.md:28, 60, 70)
- Inline decision-ID references where applicable: `(per D-02)`, `(per D-03)`, `(per D-04)`
</style_invariants>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 1 Plan 01 introduces no executable code. Output is a markdown document read by humans and grep at planning time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | I (Information Disclosure) | `.planning/reference/BRAND-SPINE.md` | accept | Document is intended to be public-readable inside the repo; no secrets, no PII, no credentials. Threat surface = zero. |

**Per phase security_threat_model:** No code introduced -- markdown document only. Threat surface = zero.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author BRAND-SPINE.md with all 7 required sections</name>
  <files>.planning/reference/BRAND-SPINE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (locked hero copy: pre-headline, H1, sub-headline, subline, CTAs lines 9-14)
    - .planning/PROJECT.md (vocab guardrails: "Avoid 'viral' and 'AI'... Lead with audience + behavioral + simulated language")
    - .planning/phases/01-brand-spine-visual-metaphor/01-CONTEXT.md (D-01..D-04, D-18 — full glossary + tone, table-vs-bullet writer's call)
    - .planning/phases/01-brand-spine-visual-metaphor/01-RESEARCH.md §"Pattern 1: BRAND-SPINE.md — Voice Document Structure" (concrete skeleton lines 240-310)
    - .planning/phases/01-brand-spine-visual-metaphor/01-PATTERNS.md §"`.planning/reference/BRAND-SPINE.md`" (style invariants from BRAND-BIBLE.md and motion-guidelines.md)
    - BRAND-BIBLE.md lines 1-22, 28-41, 348-351 (header pattern, table pattern, footer pattern to mirror)
    - docs/motion-guidelines.md lines 1-15 (section-tone pattern with bold keyword + em-dash gloss — adapt to double-hyphen)
  </read_first>
  <action>
Create `.planning/reference/BRAND-SPINE.md` (NEW FILE) with the following structure verbatim. Reference 01-RESEARCH.md skeleton at lines 240-310 as the canonical seed. Match BRAND-BIBLE.md's style invariants exactly: double-hyphen ` -- `, H1 + blockquote + `---`, 3-column tables, italic two-line footer.

```markdown
# Virtuna Brand Spine

> Source of truth for voice, vocabulary, and tone across all customer-facing surfaces.
> Read this before writing any UI string in Phase 2-6 of the Brand Statement Landing milestone.

---

## 1. The One-Liner (canonical)

**Your audience, simulated.**

This appears verbatim in: deck cover, social bios, OG metadata (`og:description`), footer brand stamp.
NOT a tagline subject to A/B testing. Do not paraphrase.

## 2. Tone Descriptors

- **Calm** -- never breathless. No exclamation marks in body copy. Numerals over hype.
- **Confident** -- declarative sentences. No hedging ("maybe", "we think", "could").
- **Lab-credible** -- research vocabulary (forecast, trained, calibrated, behavioral). Not academic stuffiness.

### Do / Don't

| Do (✓) | Don't (✗) |
|--------|-----------|
| "Predict how your audience will respond." | "AI-powered viral prediction!" |
| "Trained on decades of behavioral research." | "Powered by cutting-edge AI." |
| "Forecast every video before it ships." | "Will your video go viral?" |
| "Virtuna simulates your audience." | "Virtuna's AI tells you what works." |
| "Run a prediction." | "Submit a query." |
| "Self-improving with every outcome." | "Always learning, always adapting!" |

## 3. Preferred Verbs

predict · simulate · forecast · learn · improve · train · respond · watch · land · run · ship

Use these as the spine of every CTA and section headline. When in doubt, lead with one of these verbs.

## 4. Banned -> Replacement Table (per D-02, D-04)

| Banned | Replacement | Reason |
|--------|-------------|--------|
| viral | breakout / high-performing / lands | "viral" weakens $100M positioning |
| AI | behavioral simulation / engine / model | "AI" is generic; we're specific |
| go viral | land with audience / break through | dated creator-economy phrase |
| users | creators (or specific role) | products serve creators, not "users" |
| AI-powered | trained on behavioral research | "AI-powered" is template marketing-speak |
| cutting-edge | (delete; let specifics speak) | hype word — replace with the specific |
| revolutionary | (delete) | hype word |
| game-changing | (delete) | hype word |

This table is enforced at commit time by `scripts/lint-vocab.mjs` (Plan 04). Inline override: `<!-- vocab-lint-disable-next-line -->` for legitimate uses.

## 5. Numen Machines Lockup Pattern (per BRAND-03, D-03)

| Surface | Treatment | Example |
|---------|-----------|---------|
| Landing hero pre-headline | `VIRTUNA · A NUMEN MACHINES PRODUCT` (small mono uppercase) | per HERO-01 |
| Landing footer | `Made by Numen Machines` + lockup mark | per PRICE-05 |
| OG metadata (og:title) | `Virtuna -- A Numen Machines product` | shareable previews |
| In-product chrome | `Virtuna` alone | logged-in user; context already established |
| Page titles (`<title>`) | `Virtuna -- Predict your audience` | tab clarity |

**Rule:** Lead with the lockup on first impression (landing, OG, footer). Drop to Virtuna-alone once the user is in-product.

## 6. Audience Tuning per Viewport (per BRAND-04, D-02)

| Viewport | Primary Audience | Voice Lean | Concrete Example |
|----------|------------------|------------|------------------|
| Hero | All three (creator-led) | Creator-led, lab-credible enough for investors | H1 in second person, subline cites "decades of research" |
| Demo | Creators | Tactile, verb-first | "Paste a TikTok URL.", not "Submit a query." |
| How It Works | Creators + investors | Process diagram language | "video -> analyze -> simulate -> predict" |
| Three Surfaces | Creators | Product-language | "Brand Deals", not "Monetization integrations" |
| The Science | Investors + partners | Lab-credible, citation-led | "1,000-survey replication study" |
| Social Proof | Creators + investors | Quote-led, honest framing | "Early signal from 47 creators" not fake-impressive numbers |
| Pricing | Creators | Direct, no jargon | "$X/mo. 7-day Pro trial." |

## 7. How to use this document

- Plan-phase researchers: read this before creating section-level copy plans.
- Executors: every UI string MUST be cross-checked against §3 (preferred verbs) and §4 (banned table).
- Automated enforcement: `scripts/lint-vocab.mjs` (Plan 04) blocks commits that introduce banned vocabulary.
- Compliance task: each Phase 2-6 plan should include one verification task that confirms section copy obeys §4 banned-list and §6 audience tuning.

---

*Virtuna Brand Spine v1.0*
*Last updated: 2026-05-10*
```

**Concrete values that MUST appear (used by automated verification grep gates):**
- Verbatim string `Your audience, simulated.` (BRAND-01)
- Headers: `## 2. Tone Descriptors`, `## 3. Preferred Verbs`, `## 4. Banned`, `## 5. Numen Machines Lockup`, `## 6. Audience Tuning` (BRAND-02)
- Lockup string `VIRTUNA · A NUMEN MACHINES PRODUCT` (BRAND-03)
- Phrase `Audience Tuning` in section header (BRAND-04)

**Style enforcement:**
- Use double-hyphen ` -- ` exclusively (zero `—` em-dashes — verify with `grep -c "—" .planning/reference/BRAND-SPINE.md` returning `0`)
- H1 + blockquote opener + `---` separator pattern
- Italic two-line footer ending with the date stamp

**Decision discretion choices made (per D-18):**
- §4 uses TABLE (3-column: Banned, Replacement, Reason) — most scannable for executors and matches BRAND-BIBLE.md token-table convention.
- §6 uses 4-column TABLE (Viewport, Primary Audience, Voice Lean, Concrete Example) — matches `01-CONTEXT.md` 4-column tables.

**DO NOT:**
- Reference Figma frames or sketches (D-05 chose written-only).
- Omit any of the 6 banned/replacement seed entries.
- Use em-dash `—` anywhere in the file.
- Add tone DO/DON'T pairs that contradict locked hero copy from REQUIREMENTS.md.
- Mention sound design, light mode, or supporting pages (deferred per CONTEXT.md).
  </action>
  <acceptance_criteria>
    - [ ] File exists: `test -f .planning/reference/BRAND-SPINE.md`
    - [ ] BRAND-01: Canonical one-liner present: `grep -c "Your audience, simulated\." .planning/reference/BRAND-SPINE.md` returns ≥ 1
    - [ ] BRAND-02 (Tone): `grep -c "^## 2\. Tone Descriptors$" .planning/reference/BRAND-SPINE.md` returns 1
    - [ ] BRAND-02 (Verbs): `grep -c "^## 3\. Preferred Verbs$" .planning/reference/BRAND-SPINE.md` returns 1
    - [ ] BRAND-02 (Banned table): `grep -c "^## 4\. Banned" .planning/reference/BRAND-SPINE.md` returns 1
    - [ ] BRAND-03 (Lockup): `grep -c "VIRTUNA · A NUMEN MACHINES PRODUCT" .planning/reference/BRAND-SPINE.md` returns ≥ 1 AND `grep -c "^## 5\. Numen Machines Lockup" .planning/reference/BRAND-SPINE.md` returns 1
    - [ ] BRAND-04: `grep -c "^## 6\. Audience Tuning" .planning/reference/BRAND-SPINE.md` returns 1
    - [ ] All 4 banned-table seeds present: `grep -v '^#' .planning/reference/BRAND-SPINE.md | grep -E "viral|go viral|users|^\| AI " | wc -l` returns ≥ 4
    - [ ] All 7 viewports present in audience tuning table: for each in "Hero" "Demo" "How It Works" "Three Surfaces" "Science" "Social Proof" "Pricing", `grep -c "$viewport" .planning/reference/BRAND-SPINE.md` returns ≥ 1
    - [ ] All 5 lockup surfaces named: `grep -E "Landing hero pre-headline|Landing footer|OG metadata|In-product chrome|Page titles" .planning/reference/BRAND-SPINE.md | wc -l` returns ≥ 5
    - [ ] Style: zero em-dash `—` characters: `grep -c "—" .planning/reference/BRAND-SPINE.md` returns 0
    - [ ] Min length: `wc -l < .planning/reference/BRAND-SPINE.md` returns ≥ 120
    - [ ] Italic footer: `grep -c "^\*Virtuna Brand Spine" .planning/reference/BRAND-SPINE.md` returns 1
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/reference/BRAND-SPINE.md && grep -q "Your audience, simulated\." .planning/reference/BRAND-SPINE.md && grep -q "^## 2\. Tone Descriptors$" .planning/reference/BRAND-SPINE.md && grep -q "^## 3\. Preferred Verbs$" .planning/reference/BRAND-SPINE.md && grep -q "^## 4\. Banned" .planning/reference/BRAND-SPINE.md && grep -q "^## 5\. Numen Machines Lockup" .planning/reference/BRAND-SPINE.md && grep -q "^## 6\. Audience Tuning" .planning/reference/BRAND-SPINE.md && grep -q "VIRTUNA · A NUMEN MACHINES PRODUCT" .planning/reference/BRAND-SPINE.md && [ $(grep -c "—" .planning/reference/BRAND-SPINE.md) -eq 0 ] && [ $(wc -l < .planning/reference/BRAND-SPINE.md) -ge 120 ]</automated>
  </verify>
  <done>
    `.planning/reference/BRAND-SPINE.md` exists with all 7 required H2 sections (One-Liner, Tone, Preferred Verbs, Banned/Replacement, Numen Machines Lockup, Audience Tuning, How to Use), the canonical one-liner verbatim, the 4 seed banned->replacement entries, all 7 viewports in the audience tuning table, all 5 lockup surfaces, zero em-dashes, italic dual-line footer, and minimum 120 lines. Plan-phase researchers in Phase 2-6 can read this file as their copy guardrail.
  </done>
</task>

</tasks>

<verification>
Phase-level checks (run after task completes):

1. **File-exists smoke check (BRAND-01):**
   ```bash
   test -f .planning/reference/BRAND-SPINE.md && grep -q "Your audience, simulated" .planning/reference/BRAND-SPINE.md
   ```

2. **Required sections present (BRAND-02):**
   ```bash
   for s in "## 2. Tone Descriptors" "## 3. Preferred Verbs" "## 4. Banned" "## 5. Numen Machines Lockup" "## 6. Audience Tuning"; do
     grep -q "$s" .planning/reference/BRAND-SPINE.md || { echo "MISSING: $s"; exit 1; }
   done
   ```

3. **Lockup matrix present (BRAND-03):**
   ```bash
   grep -q "VIRTUNA · A NUMEN MACHINES PRODUCT" .planning/reference/BRAND-SPINE.md
   ```

4. **Audience tuning per viewport (BRAND-04):**
   ```bash
   grep -q "Audience Tuning" .planning/reference/BRAND-SPINE.md && \
   for v in "Hero" "Demo" "How It Works" "Three Surfaces" "Science" "Social Proof" "Pricing"; do
     grep -q "$v" .planning/reference/BRAND-SPINE.md || { echo "MISSING viewport: $v"; exit 1; }
   done
   ```

5. **Style invariants (no em-dash, has footer):**
   ```bash
   [ $(grep -c "—" .planning/reference/BRAND-SPINE.md) -eq 0 ] && grep -q "^\*Virtuna Brand Spine" .planning/reference/BRAND-SPINE.md
   ```
</verification>

<success_criteria>
- [ ] `.planning/reference/BRAND-SPINE.md` exists, ≥ 120 lines
- [ ] BRAND-01 verified: canonical one-liner verbatim
- [ ] BRAND-02 verified: 7 H2 sections + 4 banned/replacement seeds + 6 DO/DON'T pairs
- [ ] BRAND-03 verified: Numen Machines lockup matrix with 5 surface entries
- [ ] BRAND-04 verified: 7 viewports tuned in audience matrix
- [ ] Style invariants: zero em-dashes, italic two-line footer, double-hyphen separators
- [ ] Downstream readiness: file is referenceable by Plan 04's `scripts/lint-vocab.mjs` BANNED array (table content matches regex seeds)
</success_criteria>

<output>
After completion, create `.planning/phases/01-brand-spine-visual-metaphor/01-01-SUMMARY.md` with:
- File created (path + line count)
- Required sections checklist (which present, line numbers)
- Banned-table entries enumerated (for Plan 04 mirror reference)
- Audience tuning rows enumerated (for Phase 2-4 plan researchers)
- Style invariants verification (em-dash count, footer present)
- Any decisions made under D-18 discretion (table format chosen)
</output>
