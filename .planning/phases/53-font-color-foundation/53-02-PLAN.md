---
phase: 53-font-color-foundation
plan: 02
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - src/components/ui/typography.tsx
  - src/components/landing/hero-section.tsx
  - src/components/landing/features-section.tsx
  - src/components/landing/faq-section.tsx
  - src/components/landing/stats-section.tsx
  - src/components/landing/feature-card.tsx
  - src/components/layout/footer.tsx
  - src/app/(marketing)/coming-soon/page.tsx
  - src/app/(app)/trending/trending-client.tsx
  - src/app/(marketing)/showcase/page.tsx
  - src/components/landing/testimonial-quote.tsx
  - src/components/ui/input.tsx
autonomous: false

must_haves:
  truths:
    - "No component or page file references font-display class"
    - "No component uses arbitrary font weights font-[350] or font-[450]"
    - "Input component uses semi-transparent background matching Raycast pattern"
    - "Showcase page documents Inter as the font family (not Funnel Display or Satoshi)"
    - "Application builds successfully with zero font-related errors"
  artifacts:
    - path: "src/components/ui/typography.tsx"
      provides: "Heading component without font-display"
      contains: "headingSizeClasses"
    - path: "src/components/ui/input.tsx"
      provides: "Input with Raycast-matching semi-transparent background"
      contains: "bg-white/5"
    - path: "src/app/(marketing)/showcase/page.tsx"
      provides: "Updated font documentation reflecting Inter"
      contains: "Inter"
  key_links:
    - from: "src/components/ui/typography.tsx"
      to: "src/app/globals.css"
      via: "font-sans utility resolves to --font-inter from @theme"
      pattern: "font-sans"
    - from: "src/components/ui/input.tsx"
      to: "Raycast extraction"
      via: "Input bg uses rgba(255,255,255,0.05) matching Raycast"
      pattern: "bg-white/5"
---

<objective>
Remove all font-display class references from 11 component/page files, replace non-standard font weights (font-[350], font-[450]) with Inter-compatible standard weights, fix input background to match Raycast, and update showcase documentation.

Purpose: Completes the font migration started in Plan 01. Without these changes, the build breaks (font-display utility no longer exists) and components render with incorrect font weights.

Output: All 12 files updated, build passing, zero references to font-display/Satoshi/Funnel Display in component code.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-font-color-foundation/53-RESEARCH.md
@.planning/phases/53-font-color-foundation/53-01-SUMMARY.md

Key source files:
@src/components/ui/typography.tsx
@src/components/landing/hero-section.tsx
@src/components/landing/features-section.tsx
@src/components/landing/faq-section.tsx
@src/components/landing/stats-section.tsx
@src/components/landing/feature-card.tsx
@src/components/layout/footer.tsx
@src/app/(marketing)/coming-soon/page.tsx
@src/app/(app)/trending/trending-client.tsx
@src/app/(marketing)/showcase/page.tsx
@src/components/landing/testimonial-quote.tsx
@src/components/ui/input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove font-display from typography, landing components, and pages</name>
  <files>
    src/components/ui/typography.tsx
    src/components/landing/hero-section.tsx
    src/components/landing/features-section.tsx
    src/components/landing/faq-section.tsx
    src/components/landing/stats-section.tsx
    src/components/landing/feature-card.tsx
    src/components/layout/footer.tsx
    src/app/(marketing)/coming-soon/page.tsx
    src/app/(app)/trending/trending-client.tsx
    src/components/landing/testimonial-quote.tsx
  </files>
  <action>
Apply these changes to each file. Read each file first to confirm exact line content before editing.

**typography.tsx (lines 10-11):**
Remove `font-display` from heading size classes:
- Line 10: `"text-display font-semibold leading-none tracking-tight font-display"` → `"text-display font-semibold leading-none tracking-tight"`
- Line 11: `"text-5xl font-semibold leading-tight tracking-tight font-display"` → `"text-5xl font-semibold leading-tight tracking-tight"`

**hero-section.tsx:**
- Remove `font-display` from the h1 className
- Replace `font-[350]` with `font-normal` (Inter 400 is the closest match to Funnel Display 350)
- Replace `font-[450]` with `font-normal` on the p element (Inter 400 matches Satoshi 450)

**features-section.tsx:**
- Remove `font-display` from the h2 className
- Replace `font-[350]` with `font-normal`

**faq-section.tsx:**
- Remove `font-display` from the h2 className
- Replace `font-[350]` with `font-normal`

**stats-section.tsx:**
- Remove `font-display` from the h2 className
- Replace `font-[350]` with `font-normal`

**feature-card.tsx:**
- Remove `font-display` from the h3 className (line ~40)
- Update any comments referencing Funnel Display or display font to reference Inter

**footer.tsx:**
- Remove `font-display` from the h2 className
- Replace `font-[350]` with `font-normal`

**coming-soon/page.tsx:**
- Remove `font-display` from the h1 className
- Replace `font-[350]` with `font-normal`

**trending-client.tsx:**
- Remove `font-display` from the Heading component className (line ~206)

**testimonial-quote.tsx:**
- Update any comments that reference "Satoshi" font to reference "Inter"
- No className changes needed (this file doesn't use font-display class, only has Satoshi in comments)

**Pattern for all font-display removals:** Simply delete ` font-display` from the className string. Do not add `font-sans` -- it's inherited from body.

**Pattern for font-[350] replacements:** Replace `font-[350]` with `font-normal`. Inter at weight 400 provides the closest visual density to Funnel Display at 350.

**Pattern for font-[450] replacements:** Replace `font-[450]` with `font-normal`. Inter at weight 400 matches Satoshi at 450.
  </action>
  <verify>
Run `grep -rn "font-display" src/components/ src/app/ --include="*.tsx" --include="*.ts"` and confirm ONLY `src/app/(marketing)/showcase/page.tsx` matches remain (showcase is updated in Task 2).
Run `grep -rn "font-\[350\]\|font-\[450\]" src/` and confirm zero matches.
Run `npx tsc --noEmit` to verify no TypeScript errors.
  </verify>
  <done>All 10 component/page files have zero font-display class references and zero non-standard font weights (font-[350], font-[450]).</done>
</task>

<task type="auto">
  <name>Task 2: Update showcase font docs and fix input background</name>
  <files>
    src/app/(marketing)/showcase/page.tsx
    src/components/ui/input.tsx
  </files>
  <action>
**showcase/page.tsx:**
Read the file and locate the font documentation sections:

1. Find the font token entries (around lines 113-116) that document `--font-display`. Update them:
   - Remove the `--font-display` entry entirely
   - Update the `--font-sans` entry to show "Inter" (not "Satoshi")
   - If there's a font showcase section showing `font-display` class usage (around line 549), update the code example to use `font-sans` or remove the display font showcase

2. Search for any other references to "Satoshi", "Funnel Display", "font-display" in the file and update:
   - Change font family descriptions to reference Inter
   - Remove any display font showcase sections or update them to show Inter
   - Keep the showcase functional -- just update the content to reflect the new single-font system

**input.tsx:**
Fix the base Input component background to use semi-transparent white matching Raycast's input pattern:

Line 48 (the base styles line):
- FROM: `"flex h-11 w-full rounded-md border bg-surface px-3 text-foreground"`
- TO: `"flex h-11 w-full rounded-md border bg-white/5 px-3 text-foreground"`

This changes the input background from opaque `bg-surface` (#18191a) to semi-transparent `bg-white/5` (rgba(255,255,255,0.05)) matching the Raycast extraction pattern and the existing GlassInput component.

**Important:** Do NOT change the height (h-11 = 44px). COLR-04 requires rgba(255,255,255,0.05) background but the 42px height is a Phase 54 concern (INPT-03). Keep h-11 for now.
  </action>
  <verify>
Run `grep -rn "font-display\|Satoshi\|Funnel" src/app/\(marketing\)/showcase/page.tsx` and confirm zero stale references (or only references in documentation context explaining the migration).
Run `grep "bg-surface" src/components/ui/input.tsx` and confirm zero matches.
Run `grep "bg-white/5" src/components/ui/input.tsx` and confirm the new background class is present.
Run `npx next build 2>&1 | tail -20` to verify the full build passes.
  </verify>
  <done>Showcase page documents Inter as the sole font family with no Funnel Display/Satoshi references. Input component uses bg-white/5 matching Raycast input background pattern. Full build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete font migration from Satoshi + Funnel Display to Inter across the entire codebase, plus color token corrections (gray hex, card gradient 137deg, glass gradient, button-secondary shadow, input background).
  </what-built>
  <how-to-verify>
1. Kill dev server if running, then: `rm -rf .next && npm run dev`
2. Open http://localhost:3000 (marketing/landing page):
   - Verify all text renders in Inter (check in browser DevTools: select any text element, inspect Computed > font-family)
   - Verify body text has comfortable spacing (line-height 1.5)
   - Verify headings are not too thin or too bold (font-normal replacing font-[350])
3. Open http://localhost:3000/trending (app route):
   - Verify heading renders in Inter
   - Verify card backgrounds use the subtle diagonal gradient (137deg)
4. Open http://localhost:3000/showcase:
   - Verify font documentation shows Inter (not Satoshi/Funnel Display)
   - Verify all showcase sections render correctly
5. Open browser DevTools on any page:
   - In Elements panel, check `<html>` element has a class containing `__variable` (Inter CSS variable)
   - In Styles, check `--font-sans` resolves to Inter via `--font-inter`
   - Check body `line-height` is 1.5
   - Check body `letter-spacing` is 0.2px
6. Test any input field (e.g., search on trending page):
   - Verify input has semi-transparent background (not opaque dark)
  </how-to-verify>
  <resume-signal>Type "approved" to confirm visual correctness, or describe any issues for follow-up fixes.</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:

1. `grep -rn "font-display" src/ --include="*.tsx" --include="*.ts" --include="*.css"` returns zero matches
2. `grep -rn "font-\[350\]\|font-\[450\]" src/` returns zero matches
3. `grep -rn "Satoshi\|satoshi" src/ --include="*.tsx" --include="*.ts"` returns zero matches (or only in documentation explaining the migration)
4. `npx next build` completes successfully
5. `ls src/fonts/ 2>/dev/null` returns nothing (directory removed)
6. All gray tokens in globals.css are hex (no oklch for gray scale)
7. Visual verification confirms Inter font rendering across all pages
</verification>

<success_criteria>
- Zero references to font-display class in any component or CSS file
- Zero uses of font-[350] or font-[450] arbitrary weights
- Zero references to Satoshi or Funnel Display in component code
- Input component uses bg-white/5 semi-transparent background
- Showcase page accurately documents Inter as the sole font
- Full Next.js build passes with no errors
- Visual verification confirms correct font rendering on landing, trending, and showcase pages
</success_criteria>

<output>
After completion, create `.planning/phases/53-font-color-foundation/53-02-SUMMARY.md`
</output>
