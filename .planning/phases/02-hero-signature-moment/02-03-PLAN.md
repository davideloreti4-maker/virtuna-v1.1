---
phase: 02-hero-signature-moment
plan: 03
type: execute
wave: 2
depends_on: [02-00, 02-01]
files_modified:
  - src/components/marketing/hero/hero-constants.ts
  - src/components/marketing/hero/composed-still.tsx
  - src/components/marketing/hero/signature-moment-client.tsx
  - src/components/marketing/hero/hero.tsx
autonomous: true
requirements: [HERO-03, HERO-04]
user_setup: []

must_haves:
  truths:
    - "A composed still (phone + settled dot field + SVG arc ring + final score) renders server-side as the universal floor — visible pre-hydration, under reduced-motion, on mobile, and as the at-rest frame"
    - "The arc ring is a clean re-derivation (coral progress stroke, tone-step track, NO glow/tiers/white) whose strokeDashoffset maps from the score"
    - "The stage container exposes role=img + an aria-label announcing the score"
    - "The dynamic(ssr:false) call lives only inside a 'use client' wrapper; Hero (RSC) renders that wrapper; npm run build succeeds (no ssr:false-in-RSC error)"
    - "Under reduced-motion OR mobile, the canvas island does NOT mount and the still stands"
    - "The chosen score is >= 70 so the coral 'will pop' readout is honest"
  artifacts:
    - path: "src/components/marketing/hero/hero-constants.ts"
      provides: "token-derived constants: HERO_SCORE (>=70), particle budget, timing, easing/palette references"
      exports: ["HERO_SCORE"]
      contains: "HERO_SCORE"
    - path: "src/components/marketing/hero/composed-still.tsx"
      provides: "RSC-renderable resolved still: Placeholder phone + static SVG dots + clean SVG arc ring + score number + role=img/aria-label + aspect-lock"
      exports: ["ComposedStill"]
      min_lines: 40
    - path: "src/components/marketing/hero/signature-moment-client.tsx"
      provides: "'use client' boundary: the dynamic(ssr:false) canvas import + manual reduced-motion/mobile/perf gating"
      exports: ["SignatureMomentClient"]
      contains: "'use client'"
    - path: "src/components/marketing/hero/hero.tsx"
      provides: "stage interior wired to <ComposedStill> + <SignatureMomentClient> (Placeholder-only scaffolding replaced)"
      contains: "ComposedStill"
  key_links:
    - from: "src/components/marketing/hero/signature-moment-client.tsx"
      to: "src/components/marketing/hero/signature-canvas.tsx"
      via: "dynamic(() => import('./signature-canvas'), { ssr: false, loading: () => <ComposedStill/> })"
      pattern: "ssr:\\s*false"
    - from: "src/components/marketing/hero/composed-still.tsx"
      to: "src/components/board/verdict/verdict-constants.ts"
      via: "HERO_SCORE (from hero-constants) >= BAND_THRESHOLDS.STRONG drives the honest coral arc"
      pattern: "var\\(--color-accent\\)"
    - from: "src/components/marketing/hero/hero.tsx"
      to: "src/components/marketing/hero/composed-still.tsx"
      via: "Hero renders <ComposedStill score={HERO_SCORE}/> as the SSR floor inside the stage"
      pattern: "ComposedStill"
---

<objective>
Build the **keystone** of Phase 2: `ComposedStill` — the resolved end-state (phone + settled dot field + clean SVG arc ring + final score) that serves as the universal floor (SSR pre-hydration == reduced-motion fallback == mobile render == post-animation rest), D-15. Plus `hero-constants.ts` (the token-derived score/budget/timing/easing), and `SignatureMomentClient` — the `"use client"` boundary that holds the `dynamic(ssr:false)` canvas import (THE landmine fix, Pattern 1) and the manual reduced-motion/mobile/perf gating (Pattern 2). Wire both into the `Hero` stage built in 02-01.

**This plan is sequenced BEFORE the canvas (02-02) because `ComposedStill` is the shared dependency hub:** 02-02's canvas imports it as the `dynamic` `loading` fallback AND must visually converge on its ring end-state. `SignatureMomentClient` references `./signature-canvas` which 02-02 creates — so the dynamic import target is a forward reference resolved when 02-02 lands; until then the build still passes (dynamic imports are not statically resolved at build for ssr:false islands the same way — see action note) and the gates render the still on the test paths.

Purpose: Satisfies HERO-04 (static fallback + lazy/gated boundary) and the still-side of HERO-03 (the resolved arc ring + honest score). Removes the highest project risk (the ssr:false-in-RSC build error) by spelling out the boundary chain.
Output: `hero-constants.ts`, `composed-still.tsx`, `signature-moment-client.tsx`, and an edited `hero.tsx` stage interior. Turns the 02-00 `composed-still.test.tsx`, `signature-moment-client.test.tsx`, and `hero-constants.test.ts` GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-signature-moment/02-RESEARCH.md
@.planning/phases/02-hero-signature-moment/02-UI-SPEC.md
@.planning/phases/02-hero-signature-moment/02-CONTEXT.md

<interfaces>
<!-- THE landmine fix — verified shape from in-repo Board.tsx (lines 1, 71-74). Copy this boundary chain. -->
Board.tsx proof (src/components/board/Board.tsx): line 1 is `'use client'`; lines 71-74:
```
const BoardCanvas = dynamic(
  () => import('./BoardCanvas').then((m) => ({ default: m.BoardCanvas })),
  { ssr: false, loading: () => <Skeleton className="absolute inset-0" /> },
);
```
The RSC `analyze/layout.tsx` simply renders `<Board/>`. SAME shape here: signature-moment-client.tsx ('use client') does the dynamic(ssr:false) with `loading: () => <ComposedStill score={HERO_SCORE}/>`; the RSC hero.tsx renders <SignatureMomentClient/>.

<!-- Gating hooks/perf — verified signatures + SSR-safe still-favoring defaults. -->
From src/hooks/usePrefersReducedMotion.ts: `usePrefersReducedMotion(): boolean` — defaults true (no motion) until mount.
From src/hooks/useIsMobile.ts: `useIsMobile(): boolean` — defaults true (mobile) until mount; breakpoint 768.
From src/lib/perf-tier.ts: `usePerfStore((s) => s.tier): 'high'|'medium'|'low'` (default 'high'); `detectInitialTier(): Promise<PerfTier>` (call in a post-mount effect, never block paint); `startFpsSampler(onDrop): () => void` (drops once on sustained <40fps). Mirror Board.tsx:77-82 + its detect/sampler effect.
Gate: `const animate = !reduced && !isMobile && tier !== 'low'`.

<!-- Clean SVG arc ring — geometry from ViralScoreRing.tsx:87-93, EVERYTHING ELSE STRIPPED. -->
From src/components/viral-results/ViralScoreRing.tsx (REFERENCE ONLY — DO NOT import):
geometry: radius = (SIZE - STROKE) / 2; circumference = 2*PI*radius; offset = circumference - (score/100)*circumference.
STRIP: the glow <filter feGaussianBlur> (lines 144-151), getGlowColor green/yellow/red (45-49), text-white / text-white/50 (184,192), the `framer-motion` import (line 4), the tier label. Use SIZE=240, STROKE=12; track stroke="var(--color-surface-elevated)", progress stroke="var(--color-accent)" strokeLinecap="round".

<!-- Score readout — useCountUp returns MotionValue<string>; MUST render in motion.span. -->
From src/hooks/useCountUp.ts: `useCountUp({ to, duration?, format? }): MotionValue<string>`; already usePrefersReducedMotion-gated (.jump under reduce). Render: `<motion.span>{display}</motion.span>` (import motion from "motion/react"). In the STILL, the number is final/static (no count-up needed there — count-up belongs to the canvas play in 02-02). ComposedStill renders the final score as static text.

From src/components/marketing/placeholder.tsx: `<Placeholder variant="video" aspect="9/16" label="Your TikTok" />`.
From src/components/marketing/hero/hero.tsx (02-01): the bordered stage <div> with the seam comment where the Placeholder scaffolding sits.
Tokens (verified): --color-accent (coral), --color-surface-elevated (ring track / chips), --color-cream-secondary #c2bdb4 / --color-cream-muted #8a857c (base dots), --color-charcoal-chip #2f2e2b (depth), --color-foreground (cream number), text-display 64px, tabular-nums.
BAND_THRESHOLDS.STRONG = 70 (src/components/board/verdict/verdict-constants.ts).
</interfaces>

<!-- DO NOT import from board/*, viral-results/*, hive-demo/*, audience/* — retired aesthetic (glow/glass/old-brand/green-yellow-red/white) + framer-motion. Re-derive clean, token-driven. (UI-SPEC §Component Inventory.) -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hero-constants.ts + ComposedStill (the SSR floor)</name>
  <behavior>
    - Test (hero-constants.test.ts, 02-00): HERO_SCORE >= BAND_THRESHOLDS.STRONG (70) and <= 100.
    - Test (composed-still.test.tsx, 02-00): svg has >=2 circle elements; progress circle stroke-dashoffset matches score within 1px; progress stroke === "var(--color-accent)"; score number text present; getByRole("img") with aria-label matching the score; the stage box has an aspect-ratio / fixed-height style.
  </behavior>
  <read_first>
    - src/components/marketing/hero/__tests__/composed-still.test.tsx (the GREEN target — exact assertions)
    - src/components/marketing/hero/__tests__/hero-constants.test.ts (the GREEN target)
    - src/components/viral-results/ViralScoreRing.tsx (geometry source lines 87-93; what to STRIP)
    - src/components/marketing/placeholder.tsx (the phone, variant="video" aspect="9/16")
    - src/components/board/verdict/verdict-constants.ts (BAND_THRESHOLDS.STRONG)
    - .planning/phases/02-hero-signature-moment/02-RESEARCH.md (§"Re-derived clean SVG arc ring" code example; §"Token-derived constants"; Assumption A1 on the settled-field representation)
    - .planning/phases/02-hero-signature-moment/02-UI-SPEC.md (§Color accent allowlist; §Typography score readout; §A11y accessibility text)
  </read_first>
  <files>src/components/marketing/hero/hero-constants.ts, src/components/marketing/hero/composed-still.tsx</files>
  <action>
    **hero-constants.ts** — export the token-derived constants (NO hardcoded hex in components; values mirror verified `@theme` tokens here only, with comments citing the token names): `HERO_SCORE = 87` (>= BAND_THRESHOLDS.STRONG 70 ⇒ coral is honest, A2); `PARTICLE_COUNT_DESKTOP = 420` (300–500, never >600); `TIMING = { settle: 550, reaction: 800, coalesce: 1200, total: 3500 }` (ms, ≤3.5s); ring geometry constants `RING_SIZE = 240`, `RING_STROKE = 12`; easing token names (`--ease-out-quart` coalesce, `--ease-out-cubic` reaction wave) as string constants for the canvas; palette references (base dots `--color-cream-secondary`/`--color-cream-muted` low alpha, depth `--color-charcoal-chip`, reaction lerp target `--color-accent`). Consumed by both ComposedStill and the 02-02 canvas — single source of truth.

    **composed-still.tsx** — an RSC-renderable component (NO "use client", NO client hooks, NO canvas) `function ComposedStill({ score = HERO_SCORE }: { score?: number })`. It is the universal floor (D-15), so it MUST render on the server with zero JS. Build:
    1. A wrapping `<figure>` (or `<div>`) with `role="img"` and `aria-label` = "A synthetic audience reacts to a video and resolves into a virality score of {score} out of 100." (HERO-04 — the score is announced). Dimension-lock it (inline `aspect-ratio` or fixed height) so it occupies the same box the canvas mounts into (no CLS, Pitfall 3) — match the stage interior from 02-01.
    2. The in-scene **phone**: `<Placeholder variant="video" aspect="9/16" label="Your TikTok" />` positioned in-scene (D-05).
    3. A **settled dot field** — a small STATIC representation (A1): a handful (~30–60) of low-alpha SVG `<circle>`s in cream-secondary/cream-muted with a few charcoal-chip for depth, scattered around the ring. Crisp, server-rendered, ~zero cost. NO white, NO coral on the base dots.
    4. The **clean SVG arc ring** (re-derived, Pattern from RESEARCH): SIZE=RING_SIZE(240), STROKE=RING_STROKE(12); radius=(SIZE-STROKE)/2; circumference=2*PI*radius; offset=circumference-(score/100)*circumference. Two `<circle>`s — track `stroke="var(--color-surface-elevated)"`, progress `stroke="var(--color-accent)"` `strokeLinecap="round"` `strokeDasharray={circumference}` `strokeDashoffset={offset}`, svg `className="-rotate-90"`. NO glow filter, NO gradient, NO tier colors.
    5. The **center number** — static final score as text (cream or coral per UI-SPEC §Color — the resolved score number is coral, the lone-accent allowlist item 3), `tabular-nums`, `text-display` 64px / 600, with a "/100" suffix at a smaller size in cream-muted. (Static here — count-up belongs to the 02-02 canvas play.) Ensure the coral number clears AA on the ring interior (charcoal/surface) — coral-500 on #1e1d1b passes per UI-SPEC §Contrast.

    Reference semantic tokens via CSS-var strings/utilities only; coral confined to the progress arc + the number (allowlist). NO import from board/viral-results/hive-demo/audience.
  </action>
  <verify>
    <automated>npx vitest run src/components/marketing/hero/__tests__/composed-still.test.tsx src/components/marketing/hero/__tests__/hero-constants.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - hero-constants.ts exports HERO_SCORE with value >= 70 and <= 100
    - composed-still.tsx has NO "use client" and imports NO client hook and NO canvas
    - composed-still.tsx renders an <svg> with >=2 <circle> (the dot field adds more; the ring is 2 of them) and the progress circle stroke-dashoffset equals circumference-(score/100)*circumference within 1px
    - the progress circle stroke attribute === "var(--color-accent)"; the track stroke === "var(--color-surface-elevated)"; NO feGaussianBlur / filter / green-yellow-red in the file
    - the wrapper has role="img" and aria-label containing the score value
    - the wrapper has an inline aspect-ratio (or fixed height) style (no-CLS)
    - no hardcoded hex in composed-still.tsx component JSX (grep check, comments excluded)
    - both 02-00 tests (composed-still, hero-constants) are GREEN
  </acceptance_criteria>
  <done>ComposedStill renders the full resolved frame (phone + static dots + clean coral arc ring + final score) server-side with role=img/aria-label + aspect-lock; hero-constants exposes the honest score + budgets. The two tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SignatureMomentClient boundary (ssr:false fix + gating) + wire into Hero stage</name>
  <behavior>
    - Test (signature-moment-client.test.tsx, 02-00): with usePrefersReducedMotion mocked true → NO canvas mounts; with useIsMobile mocked true → NO canvas mounts.
    - Build: npm run build succeeds (no "ssr: false is not allowed with next/dynamic in Server Components" error) — the dynamic(ssr:false) call lives in a 'use client' module.
  </behavior>
  <read_first>
    - src/components/marketing/hero/__tests__/signature-moment-client.test.tsx (the GREEN target — the mocks + canvas-null assertions)
    - src/components/board/Board.tsx (lines 1, 71-74 the dynamic(ssr:false)+loading pattern; lines 77-82 + the detect/sampler effect — the gating shape to mirror)
    - src/hooks/usePrefersReducedMotion.ts, src/hooks/useIsMobile.ts (the gates; SSR-safe defaults)
    - src/lib/perf-tier.ts (detectInitialTier, startFpsSampler, usePerfStore)
    - src/components/marketing/hero/composed-still.tsx (the loading/still fallback created in Task 1)
    - src/components/marketing/hero/hero.tsx (02-01 — the stage interior + seam comment to replace)
    - .planning/phases/02-hero-signature-moment/02-RESEARCH.md (Pattern 1 Client-Only Entrypoint; Pattern 2 gating; Pitfall 1 + Pitfall 5)
  </read_first>
  <files>src/components/marketing/hero/signature-moment-client.tsx, src/components/marketing/hero/hero.tsx</files>
  <action>
    **signature-moment-client.tsx** — line 1 MUST be `'use client'` (Pitfall 1 / Pattern 1). This is the ONLY place the `dynamic(ssr:false)` call may live. At module scope:
    `const SignatureCanvas = dynamic(() => import('./signature-canvas'), { ssr: false, loading: () => <ComposedStill score={HERO_SCORE} /> });`
    (The `./signature-canvas` module is created by 02-02 — a forward reference. The `loading` fallback is the still, so before the canvas chunk loads the user sees the resolved frame, D-16. Add a brief comment noting 02-02 provides the default export.)

    Export `function SignatureMomentClient({ score = HERO_SCORE }: { score?: number })`. Inside, replicate Board.tsx gating (Pattern 2):
    - `const reduced = usePrefersReducedMotion();` `const isMobile = useIsMobile();` `const tier = usePerfStore((s) => s.tier);`
    - In a post-mount `useEffect`, call `detectInitialTier()` and `setTier(...)` (never block first paint); start `startFpsSampler(onDrop)` that, on sustained <40fps, settles to the still (cancel/unmount the canvas — set a state flag), then cancel the sampler on cleanup. (FPS-drop→still is the robustness add, A3 — keep it but it is not the correctness gate.)
    - `const animate = !reduced && !isMobile && tier !== 'low' && !fpsDropped;`
    - **When `animate` is false → return `null`** (render NOTHING — the RSC `ComposedStill` rendered by Hero underneath is already the full frame; Pattern 2 / Pitfall 5: the canvas island must not mount at all under reduced-motion, so the rAF loop and its at-rest drift never start). When `animate` is true → `return <SignatureCanvas score={score} … />` (it lazy-loads with the still as its loading fallback).
    Because both gates default to the still-favoring value pre-mount, first paint is always the still (matches D-16). DO NOT import `framer-motion`; only `motion/react` if needed (the count-up lives in the canvas).

    **hero.tsx (edit the stage interior)** — replace the 02-01 Placeholder-only scaffolding inside the bordered stage with the layered floor + island (the seam marked in 02-01):
    `import { HERO_SCORE } from "./hero-constants"; import { ComposedStill } from "./composed-still"; import { SignatureMomentClient } from "./signature-moment-client";`
    Inside the stage `<div>` render BOTH, stacked in the same dimension-locked box: `<ComposedStill score={HERO_SCORE} />` (the SSR floor, paints immediately) and `<SignatureMomentClient score={HERO_SCORE} />` (the client island; positioned to overlay the still — e.g. an absolutely-positioned canvas over the still, both inside a `relative` stage). The phone Placeholder now lives INSIDE ComposedStill, so remove the standalone Placeholder from hero.tsx. Hero MUST remain an RSC (no "use client") — it only renders the `"use client"` boundary as a child (Pattern 1). Keep the stage's aspect-lock from 02-01.
  </action>
  <verify>
    <automated>npx vitest run src/components/marketing/hero/__tests__/signature-moment-client.test.tsx && npm run build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - signature-moment-client.tsx line 1 === "'use client'"
    - the dynamic(...) call with { ssr: false } appears ONLY in signature-moment-client.tsx (grep: no other hero/*.tsx that lacks 'use client' contains "ssr: false")
    - the dynamic loading fallback is <ComposedStill .../>
    - SignatureMomentClient returns null when the gate is false (reduced-motion / mobile / low-tier)
    - signature-moment-client.test.tsx: NO canvas mounts under mocked usePrefersReducedMotion=true; NO canvas under mocked useIsMobile=true (GREEN)
    - hero.tsx still has NO "use client"; it renders <ComposedStill> and <SignatureMomentClient> stacked in the aspect-locked stage; the standalone Placeholder is removed (it lives in ComposedStill now)
    - npm run build exits 0 (THE landmine gate — no "ssr: false is not allowed ... in Server Components")
  </acceptance_criteria>
  <done>The client boundary holds the dynamic(ssr:false) canvas import with the still as loading fallback and gates mounting by reduced-motion/mobile/perf; Hero (RSC) renders the still + boundary stacked in the no-CLS stage; the build passes. signature-moment-client.test.tsx is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static marketing surface. The only crossing remains the `/signup` link (owned by 02-01). No user input reaches this code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | n/a | ComposedStill + client boundary | accept | No attack surface: no user input, no auth/session, no data persistence, no secrets, no network calls. The phone `<Placeholder>` has no `src` (static poster) and `src` would be a build-time developer path regardless (RESEARCH §Security Domain). ASVS input/auth/session/crypto categories N/A. |
</threat_model>

<verification>
- `npx vitest run src/components/marketing/hero/__tests__/composed-still.test.tsx src/components/marketing/hero/__tests__/hero-constants.test.ts src/components/marketing/hero/__tests__/signature-moment-client.test.tsx` → all GREEN (HERO-03 still-side + HERO-04 gating).
- `npm run build` → exits 0 (the authoritative ssr:false-in-RSC landmine gate).
- `npm test` → full suite stays green (phase is additive).
- Manual (phase gate): OS reduce-motion ON → `/` shows the resolved still, zero motion/drift (Pitfall 5).
</verification>

<success_criteria>
- HERO-04: one composed still serves SSR pre-hydration, reduced-motion, mobile, and at-rest; the canvas island is gated off on those paths; the build proves the ssr:false boundary is correct.
- HERO-03 (still-side): the clean coral arc ring + honest score (>=70) render server-side; the canvas in 02-02 will converge on this exact ring end-state.
- `Hero` stays an RSC; `npm run build` passes.
</success_criteria>

<output>
Create `.planning/phases/02-hero-signature-moment/02-03-SUMMARY.md` when done.
</output>
