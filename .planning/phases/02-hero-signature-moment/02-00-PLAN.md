---
phase: 02-hero-signature-moment
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/marketing/hero/__tests__/hero.test.tsx
  - src/components/marketing/hero/__tests__/composed-still.test.tsx
  - src/components/marketing/hero/__tests__/signature-moment-client.test.tsx
  - src/components/marketing/hero/__tests__/hero-constants.test.ts
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, HERO-04]
user_setup: []

must_haves:
  truths:
    - "Four Wave-0 test files exist under src/components/marketing/hero/__tests__/ and each begins with the happy-dom pragma"
    - "Each test file currently FAILS (RED) because the components it imports do not exist yet"
    - "The test suite (npm test) still passes for all pre-existing tests (the new RED files are the only failures, scoped to this phase dir)"
  artifacts:
    - path: "src/components/marketing/hero/__tests__/hero.test.tsx"
      provides: "RED tests for HERO-01 (verbatim H1 + serif class) and HERO-02 (CTA href, scroll-cue href)"
      contains: "@vitest-environment happy-dom"
    - path: "src/components/marketing/hero/__tests__/composed-still.test.tsx"
      provides: "RED tests for HERO-03 (SVG ring geometry + coral stroke + score number) and HERO-04 (role=img + aria-label + aspect-lock)"
      contains: "@vitest-environment happy-dom"
    - path: "src/components/marketing/hero/__tests__/signature-moment-client.test.tsx"
      provides: "RED tests for HERO-04 gating (reduced-motion → no canvas; mobile → no canvas)"
      contains: "@vitest-environment happy-dom"
    - path: "src/components/marketing/hero/__tests__/hero-constants.test.ts"
      provides: "RED test for HERO-03 score-honesty (HERO_SCORE >= 70)"
      contains: "@vitest-environment happy-dom"
  key_links:
    - from: "hero-constants.test.ts"
      to: "src/components/board/verdict/verdict-constants.ts"
      via: "import { BAND_THRESHOLDS } and assert HERO_SCORE >= BAND_THRESHOLDS.STRONG"
      pattern: "BAND_THRESHOLDS"
---

<objective>
Create the four Wave-0 test files (Nyquist validation scaffold) for the Phase-2 hero, BEFORE any component exists. These tests encode the acceptance criteria for HERO-01..04 as executable assertions. They MUST fail RED initially (the components they import do not exist yet) — that RED state proves the tests are real and gives every downstream task a concrete `<automated>` gate to turn GREEN.

Purpose: Per `02-VALIDATION.md` the repo has NO global vitest config — tests opt into a DOM via the per-file pragma `/** @vitest-environment happy-dom */`. Without these files, downstream tasks would have `<automated>MISSING` gates. This plan removes that gap.
Output: 4 test files under `src/components/marketing/hero/__tests__/`, each RED, each pragma-prefixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-signature-moment/02-VALIDATION.md
@.planning/phases/02-hero-signature-moment/02-RESEARCH.md
@.planning/phases/02-hero-signature-moment/02-UI-SPEC.md

<interfaces>
<!-- Contracts the downstream components WILL expose. Tests are written against these. -->
<!-- Existing convention reference — the ONLY current marketing test, copy its shape exactly: -->
From src/components/marketing/__tests__/placeholder.test.tsx (line 1 is the mandatory pragma):
```
/** @vitest-environment happy-dom */
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
```

Planned exports the tests import (created by 02-01/02-02/02-03):
- `Hero` (default-free named export) from `src/components/marketing/hero/hero.tsx` — RSC composing H1 + subcopy + CTA cluster + stage.
- `ComposedStill` from `src/components/marketing/hero/composed-still.tsx` — props `{ score: number }`. Renders an `<svg>` with two `<circle>`s (track + coral progress), a score number, wrapped in a container with `role="img"` and an `aria-label` containing the score, and an aspect-locked stage box.
- `SignatureMomentClient` from `src/components/marketing/hero/signature-moment-client.tsx` — props `{ score: number }`. `"use client"`. Mounts the lazy canvas ONLY when desktop + motion-OK; otherwise renders nothing (the still underneath stands).
- `HERO_SCORE` (number, >= 70) from `src/components/marketing/hero/hero-constants.ts`.

Existing constants the tests reference:
- `SIGNUP_URL = "/signup"` from `src/lib/routes.ts`.
- `BAND_THRESHOLDS.STRONG = 70` from `src/components/board/verdict/verdict-constants.ts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write hero.test.tsx + hero-constants.test.ts (HERO-01, HERO-02, HERO-03 honesty)</name>
  <read_first>
    - src/components/marketing/__tests__/placeholder.test.tsx (THE convention to copy — pragma on line 1, RTL render/screen)
    - src/lib/routes.ts (SIGNUP_URL value)
    - src/components/board/verdict/verdict-constants.ts (BAND_THRESHOLDS.STRONG = 70)
    - .planning/phases/02-hero-signature-moment/02-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements)
    - .planning/phases/02-hero-signature-moment/02-UI-SPEC.md (§Copywriting Contract — H1 verbatim, CTA, scroll-cue)
  </read_first>
  <files>src/components/marketing/hero/__tests__/hero.test.tsx, src/components/marketing/hero/__tests__/hero-constants.test.ts</files>
  <action>
    Create both files; each begins with `/** @vitest-environment happy-dom */` on line 1.

    `hero.test.tsx` — `import { Hero } from "../hero"` and `import { SIGNUP_URL } from "@/lib/routes"`. Render `<Hero />`. Assert:
    (HERO-01) an `<h1>` exists whose textContent equals the verbatim string "Know if it'll pop before you post" (D-09 LOCKED); the H1 element carries the serif class — assert its `className` matches `/font-serif/` (the `--font-serif` token utility, D-10).
    (HERO-01) a subcopy paragraph is present and non-empty (D-11) — assert at least one element contains the word "Simulation" or "simulates" (the product-noun mechanism line; planner-flexible wording, so assert the noun, not the full sentence).
    (HERO-02) the primary CTA is a link whose `href` === `SIGNUP_URL` (use `screen.getByRole("link", { name: /try it free/i })` and assert `.getAttribute("href") === SIGNUP_URL`).
    (HERO-02) the secondary scroll-cue is a link whose `href` === "#how-it-works" (`getByRole("link", { name: /how it works/i })`).
    Keep assertions resilient to copy tightening: match the H1 verbatim (locked) but match subcopy/scroll-cue by stable tokens.

    `hero-constants.test.ts` — `import { HERO_SCORE } from "../hero-constants"` and `import { BAND_THRESHOLDS } from "@/components/board/verdict/verdict-constants"`. Assert `HERO_SCORE >= BAND_THRESHOLDS.STRONG` and `HERO_SCORE <= 100` (HERO-03 score-honesty — coral "will pop" is truthful only at >= 70).
  </action>
  <verify>
    <automated>npx vitest run src/components/marketing/hero/__tests__/hero.test.tsx src/components/marketing/hero/__tests__/hero-constants.test.ts 2>&1 | grep -qE "Cannot find module|Failed to resolve import|failed" && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - hero.test.tsx line 1 === "/** @vitest-environment happy-dom */"
    - hero-constants.test.ts line 1 === "/** @vitest-environment happy-dom */"
    - hero.test.tsx asserts an h1 with textContent "Know if it'll pop before you post"
    - hero.test.tsx asserts the CTA link href === SIGNUP_URL (imported, not the literal "/signup")
    - hero.test.tsx asserts the scroll-cue link href === "#how-it-works"
    - hero-constants.test.ts asserts HERO_SCORE >= BAND_THRESHOLDS.STRONG (imported, not literal 70)
    - Both files FAIL RED now (the imported modules ../hero and ../hero-constants do not exist yet)
  </acceptance_criteria>
  <done>Both test files exist, pragma-prefixed, and fail RED with module-not-found (proving they will gate the implementation tasks).</done>
</task>

<task type="auto">
  <name>Task 2: Write composed-still.test.tsx + signature-moment-client.test.tsx (HERO-03, HERO-04)</name>
  <read_first>
    - src/components/marketing/__tests__/placeholder.test.tsx (convention)
    - src/hooks/usePrefersReducedMotion.ts (boolean return; the gate to mock)
    - src/hooks/useIsMobile.ts (boolean return; the gate to mock)
    - .planning/phases/02-hero-signature-moment/02-VALIDATION.md (HERO-04 gating rows — reduced-motion/mobile → no canvas)
    - .planning/phases/02-hero-signature-moment/02-RESEARCH.md (Pattern 4 useCountUp; clean SVG ring code example; Pattern 2 gating)
  </read_first>
  <files>src/components/marketing/hero/__tests__/composed-still.test.tsx, src/components/marketing/hero/__tests__/signature-moment-client.test.tsx</files>
  <action>
    Both files begin with `/** @vitest-environment happy-dom */` on line 1.

    `composed-still.test.tsx` — `import { ComposedStill } from "../composed-still"`. Render `<ComposedStill score={87} />`. Assert:
    (HERO-03) an `<svg>` is rendered containing at least two `<circle>` elements; the progress circle has a non-zero `stroke-dasharray` and a `stroke-dashoffset` consistent with score=87 (compute expected: radius=(240-12)/2=114, circumference=2*PI*114, offset=circumference-(87/100)*circumference; assert the rendered `stroke-dashoffset` is within 1px of expected). Geometry per RESEARCH §"Re-derived clean SVG arc ring".
    (HERO-03) the coral progress stroke is token-driven — assert the progress circle's `stroke` attribute === "var(--color-accent)" (NOT a hardcoded hex, NOT white).
    (HERO-03) the score number "87" is rendered as text.
    (HERO-04) the stage/figure container exposes `role="img"` and an `aria-label` whose text contains "87" (the score is announced) — `screen.getByRole("img")` and assert its `aria-label` matches /87/.
    (HERO-04) no-CLS: the stage box is dimension-locked — assert the container (or an ancestor) has an inline `aspect-ratio` style OR a fixed height (query for a style attribute containing "aspect-ratio").

    `signature-moment-client.test.tsx` — `import { SignatureMomentClient } from "../signature-moment-client"`. Use `vi.mock("@/hooks/usePrefersReducedMotion", ...)` and `vi.mock("@/hooks/useIsMobile", ...)` to control the gates. Two cases:
    (HERO-04) when `usePrefersReducedMotion` → true (and mobile false), rendering `<SignatureMomentClient score={87} />` mounts NO canvas — assert `container.querySelector("canvas")` is null.
    (HERO-04) when `useIsMobile` → true (and reduced false), rendering mounts NO canvas — assert `container.querySelector("canvas")` is null.
    Also mock `@/lib/perf-tier` minimally if the component imports it at module scope (return a benign tier so the import resolves). Because the canvas is `dynamic(ssr:false)`, it will not synchronously render in happy-dom anyway — these assertions verify the GATE prevents mounting, which is the correctness property.
  </action>
  <verify>
    <automated>npx vitest run src/components/marketing/hero/__tests__/composed-still.test.tsx src/components/marketing/hero/__tests__/signature-moment-client.test.tsx 2>&1 | grep -qE "Cannot find module|Failed to resolve import|failed" && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - composed-still.test.tsx line 1 === "/** @vitest-environment happy-dom */"
    - signature-moment-client.test.tsx line 1 === "/** @vitest-environment happy-dom */"
    - composed-still.test.tsx asserts svg has >=2 circle elements and the progress circle stroke-dashoffset matches score=87 within 1px
    - composed-still.test.tsx asserts the progress circle stroke === "var(--color-accent)"
    - composed-still.test.tsx asserts getByRole("img") with aria-label matching /87/
    - composed-still.test.tsx asserts an aspect-ratio (or fixed height) style on the stage box
    - signature-moment-client.test.tsx asserts NO canvas mounts when usePrefersReducedMotion mocked true
    - signature-moment-client.test.tsx asserts NO canvas mounts when useIsMobile mocked true
    - Both files FAIL RED now (imported modules do not exist yet)
  </acceptance_criteria>
  <done>Both test files exist, pragma-prefixed, and fail RED with module-not-found.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only plan. No runtime, no network, no user input, no data persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00 | n/a | test scaffolding | accept | No attack surface: these are RED unit-test files. ASVS input/auth/session/crypto categories N/A (static marketing surface, confirmed in RESEARCH §Security Domain). |
</threat_model>

<verification>
- `npx vitest run src/components/marketing/hero/__tests__/` → all four files RED (module-not-found), no pragma/parse errors.
- The four files each have `/** @vitest-environment happy-dom */` on line 1.
- `npm test` → pre-existing 215 test files stay green; the only failures are the four intentional RED hero files.
</verification>

<success_criteria>
- Four test files created under `src/components/marketing/hero/__tests__/`, each pragma-prefixed and RED.
- Every Phase-2 requirement (HERO-01..04) has at least one executable assertion waiting to turn GREEN.
- Downstream plans (02-01/02/03) can cite these exact file paths as their `<automated>` gates.
</success_criteria>

<output>
Create `.planning/phases/02-hero-signature-moment/02-00-SUMMARY.md` when done.
</output>
