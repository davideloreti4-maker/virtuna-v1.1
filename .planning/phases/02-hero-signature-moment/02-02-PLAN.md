---
phase: 02-hero-signature-moment
plan: 02
type: execute
wave: 3
depends_on: [02-00, 02-01, 02-03]
files_modified:
  - src/components/marketing/hero/signature-canvas.tsx
autonomous: false
requirements: [HERO-03]
user_setup: []

must_haves:
  truths:
    - "On desktop with motion enabled, the hero plays the 'crowd → score' moment once on mount: a charcoal/cream particle field settles, pulses coral as it reacts, then coalesces inward into the arc ring as the score counts up"
    - "The animated ring end-state is pixel-consistent with ComposedStill's SVG ring (same geometry, same coral, same final offset)"
    - "After resolving, the field keeps a very slow, low-amplitude drift; the score stays fixed; the moment does not loop"
    - "A subtle replay is available on hover/click of the stage"
    - "The total moment is <= ~3.5s, uses no spring overshoot on the coalesce, and never blocks page scroll"
    - "The canvas mounts only on the desktop+motion-OK path (gated by 02-03); it degrades to the still on FPS drop"
  artifacts:
    - path: "src/components/marketing/hero/signature-canvas.tsx"
      provides: "the bespoke canvas-2D rAF particle field → coral reaction → coalesce → ring handoff → count-up → rest drift; default export for the dynamic import"
      exports: ["default"]
      min_lines: 80
  key_links:
    - from: "src/components/marketing/hero/signature-canvas.tsx"
      to: "src/hooks/useCountUp.ts"
      via: "useCountUp({ to: score }) rendered in a <motion.span> for the center number"
      pattern: "useCountUp"
    - from: "src/components/marketing/hero/signature-canvas.tsx"
      to: "src/components/marketing/hero/hero-constants.ts"
      via: "import HERO_SCORE, PARTICLE_COUNT_DESKTOP, TIMING, RING geometry, easing/palette refs"
      pattern: "TIMING|PARTICLE_COUNT_DESKTOP|RING_SIZE"
---

<objective>
Build the bespoke **canvas-2D signature moment** (HERO-03, the wow): a single `requestAnimationFrame` loop that plays once on mount — a field of ~420 charcoal/cream soft particles settles, pulses/shifts coral as it "reacts" to the phone, then **coalesces inward in one continuous gesture and hands off to the arc ring** while the center number counts up — then rests with a very slow drift. The animated ring end-state must be pixel-consistent with `ComposedStill`'s SVG ring (02-03). This is the `dynamic(ssr:false)` lazy target referenced by `signature-moment-client.tsx`.

**Depends on 02-03:** `ComposedStill` is the canvas's `loading` fallback and the visual target the coalesce converges on; `hero-constants.ts` supplies the score, particle budget, timing, easing tokens, and ring geometry. The gating (mobile/reduced-motion/perf) is already decided in 02-03 — this canvas only ever runs on the desktop+motion-OK path.

This is the keystone craft deliverable at the sandcastles/Linear/Raycast bar — **refined > flashy**. It carries a `checkpoint:human-verify` because the coalesce choreography reading as "calm + refined" is a subjective craft bar that unit tests cannot assert (VALIDATION §Manual-Only).

Purpose: Satisfies HERO-03 (the live signature mechanic). The still-side of HERO-03 (the resolved ring + honest score) already shipped in 02-03; this plan adds the motion.
Output: `signature-canvas.tsx` (the `"use client"` lazy canvas with a default export).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-signature-moment/02-RESEARCH.md
@.planning/phases/02-hero-signature-moment/02-UI-SPEC.md
@.planning/phases/02-hero-signature-moment/02-CONTEXT.md

<interfaces>
<!-- Canvas mechanics to COPY BY HAND from hive-demo (do NOT import it — retired aesthetic). Verified line refs. -->
From src/components/hive-demo/hive-demo-canvas.tsx — the 6 mechanics (RESEARCH Pattern 3):
1. `const dpr = window.devicePixelRatio || 1`; resize backing store to `Math.round(rect.width*dpr)` only when changed (lines 52-60).
2. `ctx.setTransform(dpr,0,0,dpr,0,0)` then draw in CSS px (line 62).
3. `getBoundingClientRect()` fit transform — scale/offsetX/offsetY from content bounds + padding (lines 53,66-71).
4. Idle float via `elapsed * k` feeding `Math.sin/cos` for cheap drift (lines 83-91) → reuse for the D-14 at-rest shimmer.
5. `style={{ touchAction: "auto" }}` so the canvas never blocks scroll (line 155).
6. `animRef = requestAnimationFrame(draw)` in the effect; `cancelAnimationFrame(animRef.current)` in cleanup (lines 145-148).
Pre-compute particle start positions ONCE at module scope (hive-demo does BOUNDS/NODE_MAP lines 9-24) — not per frame.
REJECT its `rgba(255,255,255,…)` strokes/nodes (lines 109,128-137) — that white is the retired look.

<!-- Score count-up — MUST render in motion.span (plain span → "[object Object]"). -->
From src/hooks/useCountUp.ts: `useCountUp({ to, duration?, format? }): MotionValue<string>`; on motion/react; self-gated under reduced-motion (.jump). Use:
```
import { motion } from "motion/react";
const display = useCountUp({ to: score, duration: 1.2 });   // ~coalesce window
<motion.span>{display}</motion.span>
```

<!-- Constants + the still to converge on (02-03). -->
From src/components/marketing/hero/hero-constants.ts: HERO_SCORE (>=70), PARTICLE_COUNT_DESKTOP (420), TIMING {settle:550, reaction:800, coalesce:1200, total:3500}, RING_SIZE (240), RING_STROKE (12), easing token strings (--ease-out-quart coalesce, --ease-out-cubic reaction wave), palette refs (cream-secondary/cream-muted base, charcoal-chip depth, accent coral reaction).
From src/components/marketing/hero/composed-still.tsx (02-03): the SVG arc ring end-state — the canvas coalesce MUST land on the SAME geometry (radius=(240-12)/2=114, circumference, offset=circ-(score/100)*circ) and the SAME coral (var(--color-accent)). Recommended (RESEARCH Open Q1): keep the resolved ring as an SVG overlay (same one present in the still) so the canvas→still handoff is seamless; the count-up <motion.span> lives in the DOM overlay anyway.

Token color reads (no hardcoded hex): read via getComputedStyle on a token element OR mirror the verified values with a comment citing the token — base dots --color-cream-secondary #c2bdb4 / --color-cream-muted #8a857c (alpha 0.25–0.55), depth --color-charcoal-chip #2f2e2b, reaction lerp → --color-accent ≈ #d97757 (peak alpha ~0.9); dot radius 2–4px DPR-scaled.
</interfaces>

<!-- DO NOT import hive-demo / board / viral-results / audience. Copy mechanics by hand; re-derive clean. Easing: --ease-out-quart for the coalesce (decisive arrival), --ease-out-cubic for the reaction wave, gentle sine for rest drift. NEVER --ease-spring on the coalesce (overshoot = flashy, Pitfall/anti-pattern). -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the signature-canvas rAF moment (settle → react → coalesce → ring + count-up → drift)</name>
  <behavior>
    - The component default-exports a "use client" canvas; signature-moment-client.tsx's dynamic import resolves so npm run build succeeds.
    - The full suite stays green (additive); the existing signature-moment-client gating test still passes (canvas still does not mount under reduced-motion/mobile mocks).
    - Manual/craft behavior (human-verified): once-through play ≤~3.5s, no overshoot, coalesce lands on the still's ring, then slow drift; no loop; scroll never blocked.
  </behavior>
  <read_first>
    - src/components/hive-demo/hive-demo-canvas.tsx (the 6 DPR/fit/rAF mechanics to copy by hand — lines cited in interfaces)
    - src/hooks/useCountUp.ts (MotionValue<string> → motion.span; reduced-motion .jump)
    - src/components/marketing/hero/hero-constants.ts (02-03 — score/budget/timing/easing/ring geometry/palette)
    - src/components/marketing/hero/composed-still.tsx (02-03 — the SVG ring end-state to converge on)
    - src/components/marketing/hero/signature-moment-client.tsx (02-03 — the dynamic target's expected default export + props)
    - .planning/phases/02-hero-signature-moment/02-RESEARCH.md (Pattern 3 mechanics; Pattern 4 count-up; §"RESOLVED particle count/timing/easing"; Anti-Patterns; Open Q1 SVG-vs-canvas ring + Open Q2 replay)
    - .planning/phases/02-hero-signature-moment/02-UI-SPEC.md (§Signature Moment Interaction & Motion Contract — the visual source of truth; §Color accent allowlist)
  </read_first>
  <files>src/components/marketing/hero/signature-canvas.tsx</files>
  <action>
    Create `signature-canvas.tsx` with `'use client'` on line 1 and a **default export** (the `dynamic(() => import('./signature-canvas'))` in 02-03 expects `default`). Props: `{ score?: number }` (defaults to HERO_SCORE). Build the bespoke canvas-2D moment — copy the 6 DPR/fit/rAF mechanics from hive-demo BY HAND (do not import it); reject its white aesthetic.

    Structure:
    1. **Pre-compute** the ~`PARTICLE_COUNT_DESKTOP` (420) particle start positions + per-particle phase offsets ONCE at module scope (not per frame). Each particle: a start position (scattered across the field), a target position on the ring's circumference (where it coalesces to), a base color (lerp among cream-secondary/cream-muted/charcoal-chip), and a small radius (2–4px).
    2. **Single rAF loop** keyed off `elapsed` (timestamp − start). Drive the phases from `TIMING`:
       - **settle/gather (~settle ms):** particles ease into the resting field (low-alpha cream/charcoal), gentle.
       - **reaction wave (~reaction ms):** a coral pulse ripples across the field — particles lerp base→`--color-accent` (peak alpha ~0.9) on a wavefront, easing back as the wave passes (`--ease-out-cubic`). This is the lone-accent moment (D-02 — coral ONLY here + on the resolved instrument).
       - **coalesce (~coalesce ms):** particles flow inward along eased paths to their ring-circumference targets — ONE continuous gesture (D-03), `--ease-out-quart` (decisive arrival, NEVER `--ease-spring`). As they arrive, hand off to the arc ring: animate the ring's `strokeDashoffset` from full→the score offset over this window so the crowd "becomes" the gauge (D-03/D-04). Recommended: render the resolved ring as an **SVG overlay** (the same ring geometry as ComposedStill) so the canvas→still handoff is seamless (Open Q1).
       - **count-up:** drive the center number with `useCountUp({ to: score, duration: 1.2 })` rendered in a `<motion.span>` overlay (Pattern 4 — NEVER a plain span). The number lands on `score` as the arc completes.
       - **rest (after total):** switch to a very slow, low-amplitude drift/shimmer (≤~1.5px positional, ≤0.05 alpha, period ~6–10s) via the hive-demo idle-float technique (Mechanic 4); the score + ring stay fixed. **No loop** of the intro (D-13/D-14).
    3. **Replay (D-13):** subtle replay on hover/click of the stage — reset `start` so the once-through plays again. Per RESEARCH Open Q2, the still already shows the full outcome, so replay is an enhancement, not the sole way to perceive the moment; lean: click-to-replay on the stage container, hover as enhancement (give any interactive replay affordance an accessible name like `aria-label="Replay the simulation"` if it is a real control).
    4. **Plumbing:** DPR-aware backing store (Mechanic 1), `setTransform` (2), fit transform (3), `style={{ touchAction: "auto" }}` (5, never block scroll), `cancelAnimationFrame` on unmount (6). Position the canvas absolutely to overlay the still inside the stage's `relative` box (the still from ComposedStill sits underneath; the canvas paints over it, no CLS).
    5. **Colors via tokens** — read coral/cream/charcoal from CSS vars (getComputedStyle on a token-bearing element) or mirror the verified values with a token-citing comment; NEVER hardcode raw hex without the token reference. Coral confined to the reaction + the resolved arc + the number (allowlist). NO white.

    Keep total ≤~3.5s so it never feels like a load-blocker. The aesthetic bar: Claude-calm, soft, refined — the drama is the coalesce + the number, not flashy particles.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -25 && npx vitest run src/components/marketing/hero/</automated>
  </verify>
  <acceptance_criteria>
    - signature-canvas.tsx line 1 === "'use client'" and the module has a default export
    - the score number is rendered via useCountUp inside a <motion.span> (NOT a plain span/div/p)
    - the coalesce uses --ease-out-quart (string from hero-constants); the file contains NO --ease-spring reference on the coalesce path
    - the canvas element has style touchAction:"auto" and the rAF is cancelled on unmount (cancelAnimationFrame in the effect cleanup)
    - NO import from hive-demo / board / viral-results / audience; NO framer-motion import (motion/react only)
    - NO rgba(255,255,255 / pure-white particle color; coral usage is the reaction + resolved ring/number only
    - the resolved ring geometry matches ComposedStill (radius=(RING_SIZE-RING_STROKE)/2, offset from the same score) so the handoff is pixel-consistent
    - npm run build exits 0 (the dynamic import target now resolves)
    - npx vitest run src/components/marketing/hero/ → all phase tests GREEN (gating test still passes — canvas does not mount under reduced-motion/mobile)
  </acceptance_criteria>
  <done>The canvas plays the once-through crowd→score moment (settle→coral reaction→coalesce→ring+count-up→slow drift, ≤~3.5s, no overshoot, replayable) converging on ComposedStill's ring; it overlays the still with no CLS, never blocks scroll, and cleans up its rAF. Build passes; phase tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the signature moment craft (calm + refined)</name>
  <files>src/components/marketing/hero/signature-canvas.tsx, src/app/(marketing)/page.tsx</files>
  <action>This is a verification checkpoint — no code is written. Pause execution and have the human run the dev/build steps in `<how-to-verify>` to confirm the signature moment meets the craft bar (calm + refined, no-CLS mount, reduced-motion/mobile → still). Resume only on explicit approval; route any requested adjustments back into Task 1.</action>
  <what-built>
    The bespoke canvas-2D "crowd → score" signature moment: ~420 charcoal/cream particles settle, pulse coral as they react to the phone, then coalesce inward into the arc ring while the score counts up to the resolved value, then rest with a slow drift. Plays once on mount, replays on hover/click. The composed still is the SSR/reduced-motion/mobile floor it converges on.
  </what-built>
  <how-to-verify>
    1. `npm run dev` → open `/` on a DESKTOP viewport with OS reduced-motion OFF.
    2. Watch the once-through play: field settles → a coral reaction wave ripples across the crowd → the crowd flows inward and coalesces into the arc ring (one continuous gesture, NOT a separate gauge filling beside a static crowd) → the center number counts up to the resolved score → a very slow, barely-perceptible drift begins. Total should feel ≤ ~3.5s and "earned/calm," with NO bouncy spring overshoot on the coalesce.
    3. Confirm the resolved frame (ring + score) exactly matches the still — no jump/flicker at the canvas→rest handoff.
    4. Hover/click the stage → the moment replays subtly. The page still scrolls normally while the canvas is present (no scroll-blocking).
    5. Confirm coral appears ONLY as the reaction accent + the resolved arc + the number — the base field is charcoal/cream, no white, no glow/halo (flat-warm).
    6. `npm run build && npm start` → throttle network → confirm the still paints FIRST and the canvas mounts over it with NO visible layout shift (CLS).
    7. Enable OS reduced-motion (and/or load on a mobile viewport) → confirm the resolved STILL shows with ZERO motion (no drift, no canvas) — the universal floor.
    The bar: sandcastles-grade signature moment at Linear/Raycast craft, inside the flat-warm Claude-calm system. Refined > flashy.
  </how-to-verify>
  <verify>Human confirms the craft bar via the dev + build steps above (subjective; VALIDATION §Manual-Only).</verify>
  <resume-signal>Type "approved" if the moment reads as calm + refined and all checks pass, or describe what to adjust (timing, density, easing, color, coalesce choreography, replay).</resume-signal>
  <done>Human has watched the once-through coalesce on desktop, confirmed no-CLS mount + reduced-motion/mobile → still, and typed "approved" (or adjustments were applied and re-verified).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static marketing surface. Client-only canvas rendering; no user input, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | n/a | signature canvas | accept | No attack surface: the canvas reads only static module-scope data + theme tokens; no user input, no auth/session, no data persistence, no secrets, no network calls (RESEARCH §Security Domain). ASVS input/auth/session/crypto categories N/A. |
</threat_model>

<verification>
- `npm run build` → exits 0 (the `./signature-canvas` dynamic import now resolves; the ssr:false boundary still holds).
- `npx vitest run src/components/marketing/hero/` → all phase tests GREEN (the 02-03 gating test still passes — the canvas does not mount under reduced-motion/mobile mocks).
- `npm test` → full 215-file suite stays green (additive).
- **Human-verify checkpoint (blocking):** the craft bar — calm/refined once-through coalesce, no-CLS mount, reduced-motion/mobile → still (VALIDATION §Manual-Only).
</verification>

<success_criteria>
- HERO-03: the live "crowd → score" signature moment plays once on mount, coalesces the crowd into the ring with a count-up, rests with a slow drift, and is replayable — at the sandcastles/Linear/Raycast craft bar, flat-warm, refined > flashy.
- The animated ring converges pixel-consistently on `ComposedStill`'s SVG ring; no CLS at mount.
- The canvas runs only on the desktop+motion-OK path, degrades to the still on FPS drop, and never blocks scroll.
- `npm run build` passes; the human approves the craft.
</success_criteria>

<output>
Create `.planning/phases/02-hero-signature-moment/02-02-SUMMARY.md` when done.
</output>
