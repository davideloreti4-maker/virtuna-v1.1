---
phase: 02-hero-signature-moment
plan: 01
type: execute
wave: 1
depends_on: [02-00]
files_modified:
  - src/components/marketing/hero/hero.tsx
  - src/components/marketing/hero/index.ts
  - src/components/marketing/index.ts
  - src/app/(marketing)/page.tsx
autonomous: true
requirements: [HERO-01, HERO-02]
user_setup: []

must_haves:
  truths:
    - "Visiting / shows a serif H1 reading exactly 'Know if it'll pop before you post'"
    - "An Inter subcopy line names the Simulation mechanism and the real outputs (watch-through %, Hook, Retention, Shareability, virality score)"
    - "A dominant 'Try it free' CTA links to SIGNUP_URL and a quieter 'See how it works ↓' link scrolls to #how-it-works"
    - "A contained flat-warm stage (6% hairline border, 12px radius, tone-step surface, dimension-locked) sits below the CTA, ready to host the signature moment"
    - "The page remains a Server Component and / stays statically prerendered"
  artifacts:
    - path: "src/components/marketing/hero/hero.tsx"
      provides: "RSC hero: serif H1 + Inter subcopy + CTA cluster + the bordered stage shell"
      exports: ["Hero"]
      min_lines: 40
    - path: "src/components/marketing/hero/index.ts"
      provides: "barrel re-exporting Hero"
      contains: "export"
    - path: "src/components/marketing/index.ts"
      provides: "marketing barrel extended to re-export ./hero"
      contains: "./hero"
    - path: "src/app/(marketing)/page.tsx"
      provides: "the #hero section now renders <Hero /> (stub removed)"
      contains: "Hero"
  key_links:
    - from: "src/app/(marketing)/page.tsx"
      to: "src/components/marketing/hero/hero.tsx"
      via: "import { Hero } from @/components/marketing"
      pattern: "Hero"
    - from: "src/components/marketing/hero/hero.tsx"
      to: "src/lib/routes.ts"
      via: "import { SIGNUP_URL } and pass to the primary Button-as-link"
      pattern: "SIGNUP_URL"
---

<objective>
Build the RSC `Hero` — the serif voice headline (D-09/D-10), the Inter mechanism subcopy (D-11), the CTA cluster (primary "Try it free" → `SIGNUP_URL` + secondary "See how it works ↓" → `#how-it-works`, D-12), and the contained flat-warm **stage shell** (D-07: 6% hairline border, 12px radius, tone-step surface, dimension-locked for no-CLS). Replace the `<section id="hero">` stub in `page.tsx`. Wire the barrels.

This plan **owns the stage container** that 02-03's `ComposedStill` and the client island mount into. The stage's aspect-lock (the no-CLS contract) is decided here. The stage initially holds a single labelled `<Placeholder variant="video">` phone (D-05) as visible scaffolding; 02-03 wires the resolved still + the client boundary into this same stage.

Purpose: Satisfies HERO-01 + HERO-02 and establishes the composition + the dimension-locked arena every later Phase-2 piece converges on.
Output: `hero.tsx` (RSC), `hero/index.ts` barrel, extended `marketing/index.ts`, rewritten `#hero` section in `page.tsx`. Turns the 02-00 `hero.test.tsx` GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-signature-moment/02-UI-SPEC.md
@.planning/phases/02-hero-signature-moment/02-RESEARCH.md
@.planning/phases/02-hero-signature-moment/02-CONTEXT.md

<interfaces>
<!-- Verified against live code — use directly, no codebase exploration needed. -->
From src/lib/routes.ts:
```
export const SIGNUP_URL = "/signup";   // "Try it free" target (HERO-02)
```
From src/components/ui/button.tsx — primary (coral) Button with asChild via Radix Slot, 44px target:
```
<Button variant="primary" asChild><Link href={SIGNUP_URL}>Try it free</Link></Button>
```
From src/components/marketing/placeholder.tsx — the in-scene phone (D-05); aspect-locked, no CLS:
```
<Placeholder variant="video" aspect="9/16" label="Your TikTok" />   // no src for v1 (static poster)
```
Existing token utilities (globals.css, verified): font-serif (--font-serif Newsreader 400), text-display 64px / text-hero 52px / text-4xl 36px, leading-tight 1.1, tracking-tight -0.02em, text-foreground (cream) / text-foreground-secondary (#c2bdb4) / text-foreground-muted (#8a857c), border-border (6%), rounded-[--radius-lg] (12px), bg-surface (tone-step #1e1d1b).
Optional entrance wrappers (Phase-1 base set, on motion/react): FadeInUp, StaggerReveal from @/components/motion.
Current marketing barrel exports: Placeholder, MotionConfigShell (extend it).
</interfaces>

<!-- The stub being replaced (page.tsx is an RSC under <MotionConfigShell>): the #hero <section> currently renders only a "coming in Phase 2" paragraph. Replace its CONTENTS with <Hero/>; keep page.tsx an RSC (no "use client"). -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the RSC Hero (H1 + subcopy + CTA cluster + stage shell)</name>
  <behavior>
    - Test (hero.test.tsx, from 02-00): h1 textContent === "Know if it'll pop before you post"; h1 className matches /font-serif/.
    - Test: subcopy element present, contains the product noun "Simulation"/"simulates".
    - Test: CTA link name /try it free/i has href === SIGNUP_URL.
    - Test: scroll-cue link name /how it works/i has href === "#how-it-works".
  </behavior>
  <read_first>
    - src/components/marketing/hero/__tests__/hero.test.tsx (the GREEN target — exact assertions)
    - src/lib/routes.ts (SIGNUP_URL)
    - src/components/ui/button.tsx (variant="primary", asChild via Slot, size targets)
    - src/components/marketing/placeholder.tsx (variant="video" API, aspect/label props)
    - src/app/(marketing)/page.tsx (the stub + the existing #how-it-works anchor target; the surrounding RSC + MotionConfigShell)
    - .planning/phases/02-hero-signature-moment/02-UI-SPEC.md (§Typography, §Color, §Copywriting Contract, §Composition)
    - .planning/phases/02-hero-signature-moment/02-RESEARCH.md (Appendix 02-01 row; HERO-01/02 support)
  </read_first>
  <files>src/components/marketing/hero/hero.tsx, src/components/marketing/hero/index.ts</files>
  <action>
    Create `hero.tsx` as a Server Component (NO "use client"). Export `function Hero()`. Compose a centered vertical stack (D-06) inside the existing `<section id="hero">` rhythm:

    1. **H1** — a real `<h1>` with the verbatim text "Know if it'll pop before you post" (D-09 LOCKED — do not alter a character). Apply `font-serif` (the `--font-serif` Newsreader utility, D-10), responsive size via the existing tokens (desktop `text-display` 64px → `text-hero` 52px → mobile `text-4xl` 36px), `leading-tight`, `tracking-tight`, cream-primary (`text-foreground`). Constrain measure so it never exceeds 3 lines at 320px (a `max-w-[18ch]`-class measure per UI-SPEC §Typography). This is the landing's ONE reserved serif voice slot.
    2. **Subcopy** — one tight Inter `<p>` (cream-secondary, `text-foreground-secondary`, `text-lg`→`text-base`, `max-w-[60ch]`). Name the mechanism + the real outputs using the product noun **"Simulation"** (D-11/D-23): watch-through %, Hook, Retention, Shareability, and a virality score. Tighten the UI-SPEC suggested line within that intent. NO coral on the subcopy.
    3. **CTA cluster** — primary `<Button variant="primary" asChild>` wrapping a Next `<Link href={SIGNUP_URL}>Try it free</Link>` (HERO-02, coral, inherits shadow-button + 44px target). Beside/under it a quieter secondary scroll-cue: an `<a href="#how-it-works">` or ghost `<Button variant="ghost" asChild>` reading "See how it works ↓" (D-12), cream-muted, visibly subordinate to the primary, with a ≥44px tappable area on mobile. The scroll-cue must NOT compete with the primary (UI-SPEC §Copywriting).
    4. **Stage shell** — a `<div>` (the contained flat-warm scene, D-07): `bg-surface` tone-step, hairline `border border-border` (6%), `rounded-[--radius-lg]` (12px), generous inner padding. **Dimension-lock it** with an inline `aspect-ratio` style (e.g. a wide landscape ratio that comfortably holds a 9/16 phone + a 240px ring) so there is zero CLS when the moment mounts later (Pitfall 3). Inside, for THIS plan, render a single labelled `<Placeholder variant="video" aspect="9/16" label="Your TikTok" />` centered — visible scaffolding proving the scene composes. (02-03 replaces the stage interior with `<ComposedStill>` + `<SignatureMomentClient>`; leave a clear comment marking that seam.)

    Reference semantic tokens only — NO hardcoded hex (Pitfall 4). Coral appears ONLY on the primary CTA in this plan (the lone accent). Optionally wrap the H1/subcopy/CTA entrance in `FadeInUp`/`StaggerReveal` (calm, Phase-1 base set on `motion/react`) — keep it subtle; the moment is the wow, not the copy entrance.

    Create `hero/index.ts` barrel: `export { Hero } from "./hero";` (and any exported types).
  </action>
  <verify>
    <automated>npx vitest run src/components/marketing/hero/__tests__/hero.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - hero.tsx has NO "use client" directive (it is an RSC)
    - hero.tsx renders an <h1> with exact textContent "Know if it'll pop before you post"
    - the <h1> className includes the serif utility (matches /font-serif/)
    - the primary CTA is a Link with href === SIGNUP_URL (imported from @/lib/routes, not a literal)
    - the scroll-cue link href === "#how-it-works"
    - the subcopy contains "Simulation" or "simulates"
    - the stage div has border-border (6%) + rounded-[--radius-lg] + an inline aspect-ratio style (no-CLS)
    - no hardcoded hex color in hero.tsx (grep -v '^\s*//' src/components/marketing/hero/hero.tsx | grep -ciE '#[0-9a-f]{3,6}' returns 0)
    - hero/index.ts re-exports Hero
    - npx vitest run .../hero.test.tsx passes (GREEN)
  </acceptance_criteria>
  <done>The RSC Hero renders the locked serif H1, the mechanism subcopy, the CTA cluster (signup + scroll-cue), and the dimension-locked flat-warm stage holding a labelled phone Placeholder. hero.test.tsx is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Mount Hero in page.tsx + extend the marketing barrel</name>
  <read_first>
    - src/app/(marketing)/page.tsx (the #hero stub to replace; keep it an RSC under MotionConfigShell)
    - src/components/marketing/index.ts (the barrel to extend — currently Placeholder + MotionConfigShell)
    - src/components/marketing/hero/index.ts (the hero barrel created in Task 1)
  </read_first>
  <files>src/app/(marketing)/page.tsx, src/components/marketing/index.ts</files>
  <action>
    Extend `src/components/marketing/index.ts` to re-export the hero: add `export { Hero } from "./hero";` (mirror the existing export style; include the type export if `hero/index.ts` exports one).

    In `src/app/(marketing)/page.tsx`: import `Hero` from `@/components/marketing`. Replace the CONTENTS of the existing `<section id="hero">` — remove the `min-h-[60vh]` "Hero — coming in Phase 2" paragraph and render `<Hero />` inside the section (keep the `id="hero"` anchor; adjust the section's utility classes so the hero composition controls its own spacing/centering per UI-SPEC §Spacing). Do NOT add "use client" to page.tsx — it MUST stay a Server Component so `/` stays statically prerendered. Leave the other stub sections (`#how-it-works`, `#the-simulation`, `#pricing`, `#faq`) untouched.
  </action>
  <verify>
    <automated>npx vitest run src/components/marketing/hero/__tests__/hero.test.tsx && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/marketing/index.ts contains an export from "./hero"
    - page.tsx imports Hero and renders <Hero /> inside <section id="hero">
    - page.tsx has NO "use client" directive
    - the "coming in Phase 2" placeholder text is gone from page.tsx
    - npm run build exits 0 (page.tsx still compiles as an RSC; / prerenders)
    - npx vitest run .../hero.test.tsx still GREEN
  </acceptance_criteria>
  <done>/ renders the composed hero via the RSC Hero; the marketing barrel exports Hero; the build passes and / stays statically prerendered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → app route | The only crossing is `<Link href="/signup">` — a same-origin navigation to the existing app. No data submitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | n/a | static marketing hero | accept | No attack surface: no user input, no auth, no session, no data persistence, no secrets, no network calls beyond the same-origin `/signup` link (RESEARCH §Security Domain). ASVS input/auth/session/crypto categories N/A. The `<Placeholder src>` is a build-time developer path, never end-user input. |
</threat_model>

<verification>
- `npx vitest run src/components/marketing/hero/__tests__/hero.test.tsx` → GREEN (HERO-01 + HERO-02).
- `npm run build` → exits 0; `/` remains a statically prerendered RSC.
- Manual (deferred to phase gate): `npm run dev` → `/` shows the serif H1 + subcopy + coral CTA + scroll-cue + the bordered stage with the labelled phone, composing within the first viewport on desktop (D-08).
</verification>

<success_criteria>
- HERO-01: hybrid-voice serif H1 + Inter mechanism subcopy render on `/`.
- HERO-02: "Try it free" routes to `SIGNUP_URL`; scroll-cue targets `#how-it-works`.
- The dimension-locked flat-warm stage exists and is ready to host the signature moment with no CLS.
- `page.tsx` stays an RSC; build passes.
</success_criteria>

<output>
Create `.planning/phases/02-hero-signature-moment/02-01-SUMMARY.md` when done.
</output>
