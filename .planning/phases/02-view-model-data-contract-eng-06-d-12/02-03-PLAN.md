---
phase: 02-view-model-data-contract-eng-06-d-12
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/lib/reading/from-persisted-row.ts
  - src/app/api/analysis/[id]/route.ts
autonomous: true
requirements: [DATA-02]
must_haves:
  truths:
    - "A single pure deterministic fromPersistedRow(row) → CanonicalReading exists; the same row always normalizes to the same shape"
    - "synthHeatmap() Math.random fabrication and the time/DB-dependent optimal_post_window recompute are GONE — heatmap reads the persisted column only and degrades to null when absent"
    - "The [id] route is a thin caller of fromPersistedRow; its user_id ownership filter is intact"
  artifacts:
    - path: "src/lib/reading/from-persisted-row.ts"
      provides: "Pure deterministic persisted-row → CanonicalReading normalizer (D-11), absorbing the [id] route shims"
      exports: ["fromPersistedRow"]
      contains: "fromPersistedRow"
    - path: "src/app/api/analysis/[id]/route.ts"
      provides: "Thin route delegating to fromPersistedRow; ownership filter preserved"
      contains: "fromPersistedRow"
  key_links:
    - from: "src/app/api/analysis/[id]/route.ts"
      to: "src/lib/reading/from-persisted-row.ts"
      via: "imports + calls fromPersistedRow on the user-scoped row"
      pattern: "from-persisted-row|fromPersistedRow"
    - from: "src/lib/reading/from-persisted-row.ts"
      to: "src/lib/reading/block-types.ts"
      via: "returns CanonicalReading"
      pattern: "CanonicalReading"
---

<objective>
Build the deterministic reload-normalizer `fromPersistedRow(row) → CanonicalReading` (D-11) that resolves the load-bearing audit finding: DATA-02 is FALSE today because `[id]/route.ts` reconstructs four fields at load time — one of them (`synthHeatmap()`) using `Math.random()` for persona ids, another (`optimal_post_window`) time/DB-dependent. This plan absorbs the route's deterministic shims into one pure module and DROPS the non-deterministic ones, then rewires the route to be a thin caller. This is NOT optional polish — DATA-02 cannot be satisfied without it.

Purpose: Make the replay shape deterministic and path-agnostic so the view-model produces identical blocks from live and replay (the contract test in P04 proves it).
Output: `src/lib/reading/from-persisted-row.ts`; a slimmed `[id]/route.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-view-model-data-contract-eng-06-d-12/02-RESEARCH.md
@.planning/phases/02-view-model-data-contract-eng-06-d-12/02-PATTERNS.md
@src/lib/reading/block-types.ts

<interfaces>
<!-- The shims to ABSORB/DROP live in src/app/api/analysis/[id]/route.ts: -->
<!-- KEEP (deterministic): numeric coercion (L56-61: confidence/overall_score may be string → Number.parseFloat) -->
<!-- KEEP (deterministic): analysis_unavailable derive (L141-144: !sa.gemini && !sa.behavioral); ADD partial_analysis (sa.gemini !== sa.behavioral) — NOT in route today -->
<!-- KEEP: defensive variants.* reads (variants.hero / variants.apollo / variants.craft → overall_impression/content_summary) — any may be absent if its async writer raced/failed -->
<!-- DROP: synthHeatmap() (L76-124, Math.random persona ids L102) → read row.heatmap column ONLY; null → leave heatmap null (P04 emits retention-degraded) -->
<!-- DROP: optimal_post_window recompute (L153-172, computeOptimalPostWindow time/DB-dependent) + the creator_profiles lookup → not a Reading block, drop entirely -->
<!-- DROP/prefer-column: confidence_label (>=0.7/0.4) + anti_virality_gated (<0.4) threshold re-derives (L178-190) → prefer persisted column; derive only when genuinely null on old rows, matching engine thresholds -->
<!-- Route auth to PRESERVE: select("*").eq("id",id).eq("user_id",user.id).is("deleted_at",null).single() + the ?summary branch -->
<!-- CanonicalReading shape: from src/lib/reading/block-types.ts (built in 02-02) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the pure deterministic fromPersistedRow normalizer</name>
  <read_first>
    - src/app/api/analysis/[id]/route.ts (FULL — the shim block L56-194: numeric coercion, synthHeatmap, optimal_post_window recompute, analysis_unavailable/confidence_label/anti_virality_gated derives; the extras cast pattern)
    - src/lib/reading/block-types.ts (CanonicalReading — the exact target shape, built in 02-02)
    - src/types/database.types.ts (analysis_results Row L218-327 — flattened columns + variants JSONB bag)
    - src/lib/engine/types.ts (HeroBlock L285, HeatmapPayload, SignalAvailability — input field shapes)
    - .planning/phases/02-view-model-data-contract-eng-06-d-12/02-PATTERNS.md (from-persisted-row.ts section — KEEP/DROP rulings with line refs; defensive variants reads)
    - .planning/phases/02-view-model-data-contract-eng-06-d-12/02-RESEARCH.md (Pitfalls 2/3/5; Runtime State Inventory — the variants race surface)
  </read_first>
  <behavior>
    - fromPersistedRow(row) is PURE + DETERMINISTIC: called twice on the same row object → deep-equal output, with NO Math.random and NO Date/now/DB call anywhere
    - string confidence/overall_score → coerced to number
    - row.heatmap null → CanonicalReading.heatmap is null (NOT synthesized)
    - row.variants.apollo absent → apolloReasoning null (no throw)
    - row.variants.hero absent → hero null (no throw)
    - signal_availability !gemini && !behavioral → analysisUnavailable true; gemini !== behavioral → partialAnalysis true
    - a row missing the entire variants bag → returns a valid CanonicalReading (degraded fields null), never throws
  </behavior>
  <action>
    Create `src/lib/reading/from-persisted-row.ts` exporting pure `fromPersistedRow(row): CanonicalReading`. ABSORB the deterministic shims: numeric coercion (`typeof === 'string' ? Number.parseFloat : value` for confidence/overall_score, Pitfall 5); the `analysis_unavailable` derive (`!sa.gemini && !sa.behavioral`) AND add `partial_analysis` (`sa.gemini !== sa.behavioral`) which the route omits today; defensive `variants.*` reads — `row.variants?.hero`, `row.variants?.apollo`, `row.variants?.craft?.overall_impression`/`content_summary` — each optional-chained, null when its racing writer was absent. For confidenceLabel/antiViralityGated: PREFER the persisted column; only fall back to a threshold derive when the column is genuinely null (old rows), using the engine's own thresholds. DROP entirely: `synthHeatmap()` (read `row.heatmap` column ONLY; null stays null — Pitfall 3); the `optimal_post_window` recompute and its `creator_profiles` lookup (not a Reading block, time/DB-dependent — do not replicate). Return a `CanonicalReading` populated only from the live∩persisted fields; NEVER read `variants.engagement_range`/`predicted_engagement` (D-09). The function does NO DB I/O — it receives an already-fetched, already-authorized row. Add a header comment stating it is pure + deterministic (no Math.random, no Date, no fetch).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && node -e "const s=require('fs').readFileSync('src/lib/reading/from-persisted-row.ts','utf8'); const clean=!/Math\.random|computeOptimalPostWindow|Date\.now|new Date|fetch\(/.test(s); process.exit(clean?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/reading/from-persisted-row.ts` exports pure `fromPersistedRow` returning `CanonicalReading`
    - `grep -E "Math\.random|computeOptimalPostWindow|Date\.now|fetch\(" src/lib/reading/from-persisted-row.ts` → no matches (deterministic, no synth, no recompute, no I/O)
    - File reads `row.heatmap`/`extras.heatmap` directly with no `?? synthHeatmap()` fallback
    - File derives both `analysis_unavailable` and `partial_analysis` from signal_availability
    - `pnpm exec tsc --noEmit` passes
  </acceptance_criteria>
  <done>Pure deterministic normalizer exists; synth + recompute gone; degrades-not-throws on partial rows.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire [id]/route.ts as a thin caller of fromPersistedRow</name>
  <read_first>
    - src/app/api/analysis/[id]/route.ts (FULL — the auth/SELECT block L26-51 to PRESERVE, the ?summary branch, the shim block L56-194 to replace)
    - src/lib/reading/from-persisted-row.ts (the normalizer built in Task 1)
    - .planning/phases/02-view-model-data-contract-eng-06-d-12/02-PATTERNS.md ([id]/route.ts MODIFIED section — becomes thin caller; security V4 note)
    - .planning/phases/02-view-model-data-contract-eng-06-d-12/02-RESEARCH.md (Security Domain — V4 ownership filter must stay; normalizer does no DB I/O)
  </read_first>
  <action>
    Replace the inline shim block (L56-194) in `src/app/api/analysis/[id]/route.ts` with a call to `fromPersistedRow(data)`. KEEP intact: the `select("*").eq("id", id).eq("user_id", user.id).is("deleted_at", null).single()` ownership filter (Security V4 — never widen what the route returns), the `Unauthorized` 401 guard, the error/not-found handling, and the `?summary` branch (leave its current behavior unchanged unless it consumed a dropped shim — if so, source the summary fields from the normalized `CanonicalReading`). Remove the now-unused imports (`computeOptimalPostWindow`, `creator_profiles` lookup, any synthHeatmap-only helpers/types). Return the normalized canonical shape as the route's JSON response. Do not introduce any new DB query.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && node -e "const s=require('fs').readFileSync('src/app/api/analysis/[id]/route.ts','utf8'); const ok=/fromPersistedRow/.test(s)&&/user_id/.test(s)&&!/synthHeatmap|computeOptimalPostWindow/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `[id]/route.ts` imports and calls `fromPersistedRow`
    - `grep -E "synthHeatmap|computeOptimalPostWindow|Math\.random" src/app/api/analysis/[id]/route.ts` → no matches (shims removed)
    - The `.eq("user_id", user.id)` ownership filter and the 401 Unauthorized guard remain present
    - `pnpm exec tsc --noEmit` passes; `pnpm test` shows no NEW failures vs the pre-change baseline (the only intentional behavior change is heatmap-null-instead-of-synth)
  </acceptance_criteria>
  <done>Route delegates to the normalizer; ownership preserved; non-deterministic reconstruction gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted DB row → fromPersistedRow | Untrusted-ish JSONB `variants` bag (possibly-missing/partial fields from racing writers) crosses into the normalizer |
| client → /api/analysis/[id] | Existing authenticated, user-scoped read (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Denial of Service | fromPersistedRow on a partial/missing variants bag | mitigate | Defensive optional-chaining + null-not-throw on every `variants.*` read; a row missing the whole bag returns a valid degraded CanonicalReading (behavior test asserts no throw) |
| T-02-06 | Elevation of Privilege | refactor dropping the route's user_id ownership filter | mitigate | Keep `select("*").eq("user_id", user.id).is("deleted_at", null)` + 401 guard in the route; `fromPersistedRow` performs no DB I/O and receives the already-scoped row (verify grep asserts `user_id` still present) |
| T-02-07 | Tampering | non-deterministic recompute leaking back in | mitigate | Verify grep forbids Math.random/Date.now/computeOptimalPostWindow/fetch in the normalizer — determinism is the DATA-02 precondition |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — pure refactor/consolidation; any dependency proposal is a red flag (RESEARCH: zero new deps) |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` passes.
- `pnpm test` — no new regressions beyond the intended heatmap-degrade change.
- Normalizer contains no Math.random / Date / recompute / fetch; route keeps the ownership filter.
</verification>

<success_criteria>
- A pure deterministic `fromPersistedRow` exists; the replay shape is now deterministic + testable.
- The route is a thin, still-authorized caller; synth + recompute are gone.
- No `lib/engine/` edits; ENGINE_VERSION unchanged.
</success_criteria>

<output>
Create `.planning/phases/02-view-model-data-contract-eng-06-d-12/02-03-SUMMARY.md` when done
</output>
