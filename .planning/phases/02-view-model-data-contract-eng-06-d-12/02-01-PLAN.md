---
phase: 02-view-model-data-contract-eng-06-d-12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/smoke-tiktok-pipeline.ts
  - src/lib/reading/__tests__/view-model.test.ts
  - src/lib/reading/__tests__/verdict.test.ts
  - src/lib/reading/__tests__/identical-render.test.ts
  - src/lib/reading/__tests__/fixtures/README.md
autonomous: true
requirements: [DATA-02]
must_haves:
  truths:
    - "A real (live, persisted) fixture pair for the same analysis id exists on disk — not hand-authored"
    - "The three test files exist and reference the not-yet-built modules (RED state — they fail until later plans implement)"
  artifacts:
    - path: "scripts/smoke-tiktok-pipeline.ts"
      provides: "Capture step that dumps the persisted /api/analysis/[id] row for the same id it just analyzed"
      contains: "api/analysis/"
    - path: "src/lib/reading/__tests__/identical-render.test.ts"
      provides: "DATA-02 deep-equal contract test scaffold"
      contains: "toReadingBlocks"
    - path: "src/lib/reading/__tests__/view-model.test.ts"
      provides: "DATA-01/DATA-03/D-14 taxonomy + degradation test scaffold"
    - path: "src/lib/reading/__tests__/verdict.test.ts"
      provides: "DATA-04 band+why test scaffold"
  key_links:
    - from: "scripts/smoke-tiktok-pipeline.ts"
      to: "src/lib/reading/__tests__/fixtures/"
      via: "writeFileSync of live-<id>.json + persisted-<id>.json"
      pattern: "fixtures"
---

<objective>
Establish the Wave 0 execution prerequisite for the entire phase: a REAL (live, persisted) fixture pair for the SAME analysis id, plus the three failing test scaffolds the rest of the phase makes green. The DATA-02 deep-equal contract test (the crux, D-12) is structurally impossible without a genuine captured pair — all existing repo fixtures are hand-authored mocks with stale shapes (null apollo_reasoning/hero/heatmap, deprecated predicted_engagement point shape) and are explicitly UNACCEPTABLE per success criteria 1+2.

Purpose: Without real captured fixtures, the contract test would either not exist or would pass trivially against a mock — defeating the only proof that live and replay actually converge. Capturing them first (Wave 1) lets the build plans verify against reality from their first commit.
Output: An extended smoke script that also dumps the persisted row; a captured fixture pair in `src/lib/reading/__tests__/fixtures/`; three RED test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-view-model-data-contract-eng-06-d-12/02-RESEARCH.md
@.planning/phases/02-view-model-data-contract-eng-06-d-12/02-PATTERNS.md
@.planning/phases/02-view-model-data-contract-eng-06-d-12/02-VALIDATION.md

<interfaces>
<!-- Smoke script today writes ONLY the live PredictionResult JSON. -->
<!-- scripts/smoke-tiktok-pipeline.ts:
       L21  import { readFileSync, writeFileSync, mkdirSync } from "fs";
       L325 const response = await fetch(endpoint, { ... })   // the analyze POST
       L499-503 validationsDir = resolve(__dirname, "../.planning/phases/13-real-pipeline-validation-production-hardening/validations"); mkdirSync(...)
       L517-518 rawJsonPath = resolve(validationsDir, `video-${videoNum padStart}.json`); writeFileSync(rawJsonPath, JSON.stringify(videoResult, null, 2));
   The analyze response includes the analysis id (the row that was just persisted). -->
<!-- Persisted read path: GET /api/analysis/[id] returns the row JSON (currently via the shims this phase will consolidate). -->
<!-- Test analog (import style + minimal-override factory):
     src/components/board/verdict/__tests__/verdict-derive.test.ts L1-30 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend smoke script to capture the persisted row for the same id</name>
  <read_first>
    - scripts/smoke-tiktok-pipeline.ts (full — understand the analyze POST at L325, how `result`/`videoResult` is obtained, and the writeFileSync at L517-518)
    - src/app/api/analysis/[id]/route.ts (the GET that returns the persisted row; understand the user-scoped auth — the capture must reuse the smoke run's authenticated session/cookies or service client)
    - src/app/api/analyze/route.ts (confirm the `complete` payload / persisted insert exposes the analysis id the capture needs)
    - .planning/phases/02-view-model-data-contract-eng-06-d-12/02-RESEARCH.md (Open Questions §3 + Pitfall 2: snapshot persisted row only AFTER post-`complete` variants writes settle)
  </read_first>
  <action>
    After the existing live-result write (around L517-518), add a capture step that, for the SAME analysis id the smoke run just produced, fetches the persisted row and writes BOTH halves into `src/lib/reading/__tests__/fixtures/`: write the live `PredictionResult` JSON as `live-<id>.json` and the persisted `/api/analysis/[id]` row JSON as `persisted-<id>.json`. Resolve `<id>` from the analyze response/result (the persisted row id). To avoid the write-after-complete race (Pitfall 2), poll/re-fetch the persisted row until `variants.apollo != null` (bounded retry, e.g. up to ~5 attempts with a short delay) BEFORE snapshotting — assert `variants.apollo` is present and abort the capture with a clear message if it never settles. Reuse the smoke run's authenticated session for the GET (same cookies/headers used for the analyze POST), or a service-role client if the script already uses one; do NOT weaken the route's `user_id` ownership filter. Gate the fixture capture behind the existing per-video success path (skip on GATE FAIL / no result). Keep the existing `scripts/validations/`-style live dump untouched — this is an ADDITIONAL write into the test fixtures dir.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('scripts/smoke-tiktok-pipeline.ts','utf8'); const ok = s.includes('api/analysis/') && /fixtures/.test(s) && /variants\.apollo|variants\?\.apollo/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/smoke-tiktok-pipeline.ts` contains a fetch/read of `/api/analysis/<id>` and a `writeFileSync` of both `live-<id>.json` and `persisted-<id>.json` under `src/lib/reading/__tests__/fixtures/`
    - The capture asserts `variants.apollo` is non-null on the persisted row before snapshotting (settle-the-race guard)
    - No change to the route's `.eq("user_id", ...)` ownership filter; capture reuses the authenticated session or service client
    - `pnpm exec tsc --noEmit` (or the repo typecheck) still passes on the modified script
  </acceptance_criteria>
  <done>Smoke script writes a real (live, persisted) fixture pair for the same id, race-guarded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The smoke script now captures a real persisted fixture pair, but RUNNING it requires live infra (API keys, deployed-or-local Supabase + engine) which Claude cannot drive headlessly — the analyze step uploads a video via the dev-server UI.</what-built>
  <how-to-verify>
    1. Ensure the dev stack is up (engine API keys + Supabase reachable) per the smoke script's existing instructions.
    2. Run the smoke pipeline for ONE real video (e.g. `~/Downloads/TikTok Video Downloader.mp4` per project notes), uploading via the dev-server UI when prompted.
    3. Confirm two files now exist under `src/lib/reading/__tests__/fixtures/`: `live-<id>.json` and `persisted-<id>.json` for the SAME `<id>`.
    4. Open `persisted-<id>.json` and confirm `variants.apollo` is present (writes settled) and `heatmap` is present-or-null (real, not synthesized).
  </how-to-verify>
  <resume-signal>Type "captured" once both fixture files exist for the same id, or describe what failed (e.g. infra down, variants never settled).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write the three RED test scaffolds + fixtures README</name>
  <read_first>
    - src/components/board/verdict/__tests__/verdict-derive.test.ts (L1-30 — copy the import style + `(over = {}) => ({...}) as PredictionResult` minimal-override factory; node env default, no happy-dom pragma)
    - .planning/phases/02-view-model-data-contract-eng-06-d-12/02-PATTERNS.md (test-files section: the identical-render.test.ts shape; the minimal-override factory)
    - .planning/phases/02-view-model-data-contract-eng-06-d-12/02-RESEARCH.md (Validation Architecture: Req→Test map; the DATA-02 deep-equal test body)
  </read_first>
  <action>
    Create three test files under `src/lib/reading/__tests__/` that reference the modules later plans build (they will be RED until then — expected):
    1. `identical-render.test.ts` — imports `live-<id>.json` + `persisted-<id>.json` from `./fixtures/`, imports `toReadingBlocks` + `canonicalFromLive` from `../view-model` and `fromPersistedRow` from `../from-persisted-row`; one test asserting `toReadingBlocks(canonicalFromLive(live))` `toEqual` `toReadingBlocks(fromPersistedRow(persisted))` (deep structural equality). Resolve the fixture filenames by globbing the fixtures dir (read whichever `live-*.json`/`persisted-*.json` pair exists) so the test does not hardcode a specific id.
    2. `view-model.test.ts` — DATA-01/DATA-03/D-14: assert `toReadingBlocks` returns the value-bearing block `kind`s for a populated canonical input; assert dropped fields never produce a block (NO `kind:'audio'` ever; no block for `predicted_engagement`/`emotion_arc`/`platform_fit`/`critique`/`rule_score`/`trend_score`/`gemini_score`/`ml_score`); assert D-14 degradation (heatmap null → `retention-degraded` not `retention`; `analysis_unavailable`/`partial_analysis` → `analysis-degraded`; individual null field → block omitted silently).
    3. `verdict.test.ts` — DATA-04: assert the `verdict` block carries band + grounded why + confidence-language (string, not a number headline); assert `/100` appears only as in-body `score`, never as the headline.
    Use the minimal-override factory from the analog for the synthetic cases; only `identical-render.test.ts` uses the REAL fixtures. Add `src/lib/reading/__tests__/fixtures/README.md` documenting that fixtures are REAL captures from the smoke script (not mocks) and how to regenerate.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const f=['identical-render','view-model','verdict'].every(n=>fs.existsSync('src/lib/reading/__tests__/'+n+'.test.ts')); process.exit(f?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist: `src/lib/reading/__tests__/{identical-render,view-model,verdict}.test.ts`
    - `identical-render.test.ts` imports from `./fixtures/` and asserts `toEqual` between the live and replay block arrays
    - `view-model.test.ts` asserts NO `kind:'audio'` block is ever emitted and that dropped-field block kinds are absent
    - `pnpm test src/lib/reading` RUNS the three files (they may FAIL/RED because the modules don't exist yet — that is the expected Wave-0 state; the command must collect them, not error on a syntax/parse fault)
    - `src/lib/reading/__tests__/fixtures/README.md` exists and states fixtures are real captures
  </acceptance_criteria>
  <done>Three test scaffolds collected by vitest, RED pending the build plans; fixtures README present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke script → /api/analysis/[id] | The capture re-reads an authenticated, user-scoped row over HTTP/service client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | persisted-<id>.json committed fixture | mitigate | Capture from a synthetic test video, not real creator PII; reviewer confirms no secrets/tokens in the row JSON before commit |
| T-02-02 | Elevation of Privilege | capture step reusing auth | accept | Capture reuses the smoke run's own session/service client against its own row; route ownership filter untouched — no new privilege path |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan (pure script edit + test files); if any install is proposed it is a red flag for this phase (RESEARCH: zero new deps) |
</threat_model>

<verification>
- `pnpm test src/lib/reading` collects all three test files (RED expected until Wave 2–4).
- Fixture pair `live-<id>.json` + `persisted-<id>.json` exists for the SAME id after the human-action capture.
- `variants.apollo` present in the persisted fixture (race settled).
</verification>

<success_criteria>
- A genuine (live, persisted) fixture pair exists — the single execution prerequisite of the phase is satisfied.
- Three test scaffolds exist and are collected by vitest, pending implementation.
- No `lib/engine/` edits; ENGINE_VERSION unchanged.
</success_criteria>

<output>
Create `.planning/phases/02-view-model-data-contract-eng-06-d-12/02-01-SUMMARY.md` when done
</output>
