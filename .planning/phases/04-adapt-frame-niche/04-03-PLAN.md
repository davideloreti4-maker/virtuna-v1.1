---
phase: 04-adapt-frame-niche
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/board/adapt/AdaptConceptCard.tsx
  - src/components/board/adapt/__tests__/AdaptConceptCard.test.tsx
autonomous: true
requirements: [ADAPT-01]
user_setup: []

must_haves:
  truths:
    - "Each concept renders as a card: hook is the bold headline, format_borrowed is a coral chip prefixed 'Borrowed:', angle + who_its_for are quiet muted sub-rows (D-09)"
    - "Cards conform to Raycast card language: transparent bg, 6% border, 12px radius, inset shadow, hover bg-white/[0.02] only (no translate, no border change); designed for the 3-up all-visible stack with no accordion/tabs (D-08)"
    - "Cards render as <article> elements for correct reading order (DOM, not Konva)"
  artifacts:
    - path: "src/components/board/adapt/AdaptConceptCard.tsx"
      provides: "AdaptConceptCard({ concept }: { concept: AdaptConcept }) — one Raycast card"
      exports: ["AdaptConceptCard"]
      contains: "Borrowed:"
  key_links:
    - from: "src/components/board/adapt/AdaptConceptCard.tsx"
      to: "@/lib/engine/remix/decode-types"
      via: "import type { AdaptConcept }"
      pattern: "AdaptConcept"
---

<objective>
Build the presentational leaf: `AdaptConceptCard`, one Raycast-conformant card rendering a single `AdaptConcept` per D-08/D-09 and the UI-SPEC.

This is an independent UI leaf — it depends only on the contract type (plan 01), not the backend (plan 02), so it runs in parallel with plan 02. Delivers the per-card half of ADAPT-01's render.

Purpose: A pure, testable card component the frame body (plan 04) stacks ×3, with hook-first anatomy and the single coral `format_borrowed` chip that makes "format not content" visible at a glance.
Output: `AdaptConceptCard.tsx` + its filled-in Wave 0 test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-adapt-frame-niche/04-CONTEXT.md
@.planning/phases/04-adapt-frame-niche/04-PATTERNS.md
@.planning/phases/04-adapt-frame-niche/04-UI-SPEC.md
@.planning/phases/04-adapt-frame-niche/04-VALIDATION.md
@.planning/phases/04-adapt-frame-niche/04-01-SUMMARY.md

<interfaces>
<!-- Contract from plan 01. Import, do not redefine. -->
- `AdaptConcept { hook: string; angle: string; who_its_for: string; format_borrowed: string }` from `@/lib/engine/remix/decode-types`

<!-- Design contract (UI-SPEC §Surface Patterns §Concept Card + CLAUDE.md Raycast rules) -->
- Card: `bg-transparent border border-white/[0.06] rounded-xl p-4`, inset shadow `rgba(255,255,255,0.05) 0 1px 0 0 inset`, hover `bg-white/[0.02]` ONLY
- Hook headline: `text-base font-semibold text-foreground`
- format_borrowed chip: `text-xs font-medium text-accent bg-accent/[0.12] rounded-full px-2 py-0.5`, prefix "Borrowed:"
- Divider: `border-t border-white/[0.04]`
- Muted rows (Angle / Who it's for): label `text-xs font-medium text-white/45 uppercase tracking-widest` + value `text-xs font-medium text-foreground-secondary`
- `cn` from `@/lib/utils`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AdaptConceptCard — Raycast card, hook-first anatomy</name>
  <read_first>
    - .planning/phases/04-adapt-frame-niche/04-PATTERNS.md §"AdaptConceptCard.tsx" (card chrome + internal layout excerpt)
    - .planning/phases/04-adapt-frame-niche/04-UI-SPEC.md §Surface Patterns §Concept Card + §Typography + §Color (exact classes, "Who it's for"/"Angle" labels)
    - src/components/board/_kit/FrameHero.tsx (card chrome + typography analog)
    - CLAUDE.md §"Raycast Design Language Rules" (6% border, 12px radius, 2% hover, no translate/border change)
    - src/components/board/adapt/__tests__/AdaptConceptCard.test.tsx (Wave 0 scaffold from plan 01 — make it pass)
  </read_first>
  <files>src/components/board/adapt/AdaptConceptCard.tsx, src/components/board/adapt/__tests__/AdaptConceptCard.test.tsx</files>
  <behavior>
    - Test: renders `concept.hook` as the headline element (semibold, text-base)
    - Test: renders `format_borrowed` inside a chip whose text begins "Borrowed:" and carries the coral accent classes
    - Test: renders an "Angle" muted row with `concept.angle` and a "Who it's for" muted row with `concept.who_its_for`
    - Test: root element is an `<article>` (reading order)
  </behavior>
  <action>
    Create `AdaptConceptCard({ concept }: { concept: AdaptConcept })` per PATTERNS + UI-SPEC. Root `<article>` with the Raycast card classes via `cn(...)` and the inset box-shadow inline style. Internal order top-to-bottom: (1) hook headline `text-base font-semibold text-foreground`; (2) `format_borrowed` chip `<span>` with the coral chip classes, text `Borrowed: {concept.format_borrowed}`; (3) divider `border-t border-white/[0.04]`; (4) Angle muted row — uppercase tracking-widest white/45 label "Angle" + `text-foreground-secondary` value `{concept.angle}`; (5) Who-it's-for muted row — label "Who it's for" + value `{concept.who_its_for}`. Hover is `bg-white/[0.02]` only — NO translate-y, NO border change (CLAUDE.md verified rule). No coral anywhere except the chip (UI-SPEC reserved-for rule). No fenced code in implementation beyond the analog. Fill in the plan-01 test scaffold (happy-dom) and make every case green.
  </action>
  <verify>
    <automated>npx vitest run src/components/board/adapt/__tests__/AdaptConceptCard.test.tsx 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `npx vitest run src/components/board/adapt/__tests__/AdaptConceptCard.test.tsx` all green (hook headline, "Borrowed:" chip, Angle row, Who it's for row, article root)
    - source: card root is `<article>` and uses `border-white/[0.06]` + `rounded-xl` (`grep -E 'article|rounded-xl|border-white/\[0.06\]' src/components/board/adapt/AdaptConceptCard.tsx`)
    - source: the ONLY coral usage is the chip — `grep -c 'text-accent\|bg-accent' src/components/board/adapt/AdaptConceptCard.tsx` matches only chip lines; no `translate-y` and no hover border-change class present
    - source: imports `AdaptConcept` type from `@/lib/engine/remix/decode-types`
  </acceptance_criteria>
  <done>AdaptConceptCard renders hook-first anatomy with the single coral Borrowed chip, conforms to Raycast card rules, and its test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no runtime boundary this plan) | Pure presentational component — receives a typed `AdaptConcept`, renders DOM; no network, no DB, no LLM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Information Disclosure | concept fields rendered to DOM | accept | Concepts are model-generated format guidance for the row owner; no third-party content, no PII; rendered only inside the owner's authed board |
| T-04-09 | Tampering (XSS) | rendering `concept.*` strings | mitigate | React escapes text children by default; no `dangerouslySetInnerHTML` used (verify absent in the component) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase — RESEARCH §Package Legitimacy Audit confirms zero new deps |
</threat_model>

<verification>
- `npx vitest run src/components/board/adapt/__tests__/AdaptConceptCard.test.tsx` green.
- No `dangerouslySetInnerHTML` in the component.
- Raycast conformance: 6% border, 12px radius, hover bg-white/[0.02] only; coral only on the chip.
- No new npm dependencies.
</verification>

<success_criteria>
- AdaptConceptCard renders hook headline + Borrowed chip + Angle + Who-it's-for rows as an <article>.
- Conforms to Raycast card language and the UI-SPEC typography/color contract.
</success_criteria>

<output>
Create `.planning/phases/04-adapt-frame-niche/04-03-SUMMARY.md` when done.
</output>
