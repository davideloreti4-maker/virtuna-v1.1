---
phase: 04-adapt-frame-niche
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - src/hooks/queries/use-adapt-concepts.ts
  - src/components/board/adapt/AdaptFrameBody.tsx
  - src/components/board/adapt/AdaptShellNode.tsx
  - src/components/board/adapt/__tests__/AdaptFrameBody.test.tsx
  - src/components/board/Board.tsx
  - src/components/board/BoardMobile.tsx
autonomous: false
requirements: [ADAPT-01, ADAPT-02]
user_setup: []

must_haves:
  truths:
    - "When a niche is present and decode output is available, the 3 concepts auto-generate once (no extra click) and render as 3 stacked all-visible cards (D-04, D-08)"
    - "When the creator-profile niche is empty (both null), an inline NichePicker appears; on confirm the niche is PATCHed to creator_profile then concepts generate (no cache race) (D-11, D-12)"
    - "On permalink reload with variants.remix.adapt present, cards rehydrate from the persisted row with NO regeneration fetch (D-05)"
    - "A failed adapt generation shows the Adapt-frame error state while the Decode frame still renders (independent failure) (D-06)"
    - "The Adapt frame mounts with its body on both the desktop Konva overlay and the mobile card-stack (<768px) (D-10)"
    - "No regenerate/reroll affordance this phase — auto-generate produces exactly one set of 3 concepts (D-07)"
  artifacts:
    - path: "src/components/board/adapt/AdaptFrameBody.tsx"
      provides: "Self-sourcing frame body: loading → niche-prompt → 3 cards / error; dual-read rehydrate; auto-generate guard"
      exports: ["AdaptFrameBody"]
    - path: "src/hooks/queries/use-adapt-concepts.ts"
      provides: "useAdaptConcepts mutation → POST /api/remix/adapt + invalidate analysis query"
      exports: ["useAdaptConcepts"]
    - path: "src/components/board/adapt/AdaptShellNode.tsx"
      provides: "Mount that renders <AdaptFrameBody> (descriptor body replaced)"
  key_links:
    - from: "src/components/board/adapt/AdaptFrameBody.tsx"
      to: "/api/remix/adapt"
      via: "useAdaptConcepts mutation"
      pattern: "useAdaptConcepts|/api/remix/adapt"
    - from: "src/components/board/adapt/AdaptFrameBody.tsx"
      to: "variants.remix.adapt"
      via: "dual-read: persisted variants ?? live mutation state"
      pattern: "remix.*adapt"
    - from: "src/components/board/adapt/AdaptFrameBody.tsx"
      to: "src/components/app/cards/niche-picker.tsx"
      via: "inline NichePicker + useCreatorProfile/useUpdateCreatorProfile"
      pattern: "NichePicker"
    - from: "src/components/board/Board.tsx"
      to: "src/components/board/adapt/AdaptShellNode.tsx"
      via: "<AdaptShellNode camera layout /> at layout.id==='adapt'"
      pattern: "AdaptShellNode"
---

<objective>
Wire the full Adapt frame: the self-sourcing `AdaptFrameBody` that orchestrates loading → empty-niche prompt → 3 stacked cards → error, the `useAdaptConcepts` mutation hook, and the shell + desktop + mobile mount-point updates. Delivers ADAPT-01 (3 cards rendered end-to-end) and ADAPT-02 (inline niche prompt).

This is the composition wave: it consumes the route (plan 02) and the card (plan 03), reuses `NichePicker` + creator-profile hooks (D-11/D-12), implements the auto-generate-once + dual-read-rehydrate + independent-failure state machine (D-04/D-05/D-06), and ends with a human-verify checkpoint for concept quality + Raycast styling (the two manual-only verifications from VALIDATION).

Purpose: Make the remix board feel complete on load (auto-generate, D-04), survive reload without regeneration (D-05), prompt inline when niche empty (ADAPT-02), and fail independently of Decode (D-06).
Output: hook + frame body + shell/Board/BoardMobile wiring + filled-in AdaptFrameBody test; then a checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-adapt-frame-niche/04-CONTEXT.md
@.planning/phases/04-adapt-frame-niche/04-RESEARCH.md
@.planning/phases/04-adapt-frame-niche/04-PATTERNS.md
@.planning/phases/04-adapt-frame-niche/04-UI-SPEC.md
@.planning/phases/04-adapt-frame-niche/04-VALIDATION.md
@.planning/phases/04-adapt-frame-niche/04-02-SUMMARY.md
@.planning/phases/04-adapt-frame-niche/04-03-SUMMARY.md

<interfaces>
<!-- Contract from plans 01-03. Import, do not redefine. -->
- `AdaptConcept` from `@/lib/engine/remix/decode-types`
- `AdaptConceptCard` from `./AdaptConceptCard` (plan 03) — stack ×3
- POST `/api/remix/adapt` → `{ concepts: AdaptConcept[] }` (plan 02)

<!-- Verified seams (import, do not reimplement) -->
- Self-sourcing + dual-read precedent — `src/components/board/content-analysis/ContentAnalysisFrame.tsx:64-99`
- `useAnalysisStream` — `@/hooks/queries/use-analysis-stream`; `usePermalinkAnalysis` — `@/hooks/queries/use-permalink-analysis`
- `useCreatorProfile` / `useUpdateCreatorProfile` — `@/hooks/queries/use-creator-profile` (mutation invalidates profile cache on success)
- `NichePicker` — `@/components/app/cards/niche-picker` — controlled props: `{ primary: string|null; sub: string|null; onChange: (next:{primary,sub})=>void }`
- niche labels: `getPrimaryLabel(slug)`, `getSubLabel(primary, sub)` — `@/lib/niches/taxonomy`
- TanStack mutation + invalidate precedent — `src/hooks/queries/use-creator-profile.ts:75-101`; `queryKeys` from `@/lib/queries/query-keys`
- already-fired ref precedent — `src/components/board/Board.tsx:182` (streamingAnalysisIdRef)
- Mount points: `Board.tsx:517` (`{layout.id === 'adapt' && <AdaptShellNode />}`), `BoardMobile.tsx:137` (`case 'adapt': return <AdaptShellNode />`)
- Skeleton + Button from existing ui kit; WarningCircle from @phosphor-icons/react (UI-SPEC error/loading states)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useAdaptConcepts mutation hook</name>
  <read_first>
    - .planning/phases/04-adapt-frame-niche/04-PATTERNS.md §"use-adapt-concepts.ts" (mutation + invalidation shape)
    - src/hooks/queries/use-creator-profile.ts:75-101 (mutation + cache invalidation analog)
    - src/lib/queries/query-keys.ts (analysis.byId key)
  </read_first>
  <files>src/hooks/queries/use-adapt-concepts.ts</files>
  <behavior>
    - On mutate: POST /api/remix/adapt with { analysis_id, decode (AdaptInput structural fields), niche }, JSON Content-Type
    - On !res.ok: throw with the server error message
    - On success: invalidate the analysis-by-id query so permalink rehydrate picks up variants.remix.adapt
    - Returns { concepts: AdaptConcept[] }
  </behavior>
  <action>
    Create `useAdaptConcepts(analysisId: string)` returning a `useMutation` per PATTERNS §use-adapt-concepts.ts. `mutationFn` POSTs to `/api/remix/adapt` with `Content-Type: application/json` and a body `{ analysis_id, decode, niche }`; throws on `!res.ok` with the server `error`; returns the parsed `{ concepts }`. `onSuccess` → `queryClient.invalidateQueries({ queryKey: queryKeys.analysis.byId(analysisId) })`. Type the input so `decode` is the 4 structural fields + `repeatable[]` (the AdaptInput minus niche, which is passed alongside) — never include luck/caption (D-01). No fenced code beyond the analog.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E 'use-adapt-concepts' ; test $? -ne 0 && echo "hook compiles clean"</automated>
  </verify>
  <acceptance_criteria>
    - source: posts to `/api/remix/adapt` (`grep -c "/api/remix/adapt" src/hooks/queries/use-adapt-concepts.ts` ≥1) and invalidates `queryKeys.analysis.byId`
    - source: the mutation input `decode` type has no `luck` field
    - test-command: `npx tsc --noEmit` clean for this file
  </acceptance_criteria>
  <done>useAdaptConcepts posts to the endpoint, invalidates the analysis query, and carries only structural decode fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AdaptFrameBody — state machine + dual-read + auto-generate + niche prompt + shell/Board/BoardMobile wiring</name>
  <read_first>
    - .planning/phases/04-adapt-frame-niche/04-PATTERNS.md §"AdaptFrameBody.tsx" (self-sourcing, dual-read, niche-gate, already-fired guard) + §"AdaptShellNode.tsx" (mount-point change) + §Shared Patterns
    - src/components/board/content-analysis/ContentAnalysisFrame.tsx:1-99 (self-sourcing + dual-read shape — copy exactly, swap craft→adapt)
    - src/components/app/cards/niche-picker.tsx:24-119 (controlled API + a11y)
    - src/hooks/queries/use-creator-profile.ts:54-101 (read + update + invalidation)
    - src/components/board/Board.tsx:130-170,486-518 (boardMode derivation; AdaptShellNode mount) + src/components/board/BoardMobile.tsx:37-143 (mobile adapt slot)
    - .planning/phases/04-adapt-frame-niche/04-UI-SPEC.md §Surface Patterns (loading/niche-prompt/error states + copywriting contract) + §Accessibility
    - src/components/board/adapt/__tests__/AdaptFrameBody.test.tsx (Wave 0 scaffold from plan 01 — make it pass)
  </read_first>
  <files>src/components/board/adapt/AdaptFrameBody.tsx, src/components/board/adapt/AdaptShellNode.tsx, src/components/board/Board.tsx, src/components/board/BoardMobile.tsx, src/components/board/adapt/__tests__/AdaptFrameBody.test.tsx</files>
  <behavior>
    - Test: niche empty (niche_primary AND niche_sub both null) → inline NichePicker shown; "Generate concepts" CTA disabled until sub selected
    - Test: niche supplied inline → updateProfile.mutateAsync awaited THEN adapt fires (no race, Pitfall 5); the just-picked niche labels passed directly into the adapt call
    - Test: niche present + decode present + no persisted adapt → adapt fires exactly once (already-fired ref); renders 3 AdaptConceptCard
    - Test: variants.remix.adapt present (permalink) → renders 3 cards with NO mutation/fetch (rehydrate-no-regen, Pitfall 3)
    - Test: adapt failure (mutation error / null) → error state (role="alert", "Couldn't generate concepts"); Decode sibling content NOT affected (D-06)
    - Test: decode absent but niche present → empty state copy "Concepts generate once the source video is decoded."
  </behavior>
  <action>
    Create `AdaptFrameBody({ camera, layout })` mirroring `ContentAnalysisFrame` self-sourcing + dual-read (PATTERNS §AdaptFrameBody.tsx). Self-source via `usePermalinkAnalysis` + `useAnalysisStream`; read decode from `row.variants.remix.decode` (Phase 3's persisted output; until Phase 3 lands this is absent on live and adapt waits — that is correct per D-04, and tests inject it). Dual-read adapt: `row?.variants?.remix?.adapt ?? liveAdaptConcepts` (live = the `useAdaptConcepts` mutation response held in state). Niche gate: `useCreatorProfile`; `nicheEmpty = niche_primary == null && niche_sub == null` (D-11, BOTH null). State machine + UI per UI-SPEC: (a) nicheEmpty → inline `NichePicker` (controlled draft state) + heading/body copy + "Generate concepts" Button (disabled until `sub`), on confirm `await useUpdateCreatorProfile.mutateAsync({niche_primary, niche_sub})` THEN fire adapt with the just-picked labels via `getPrimaryLabel`/`getSubLabel` (Pitfall 5 — pass directly, don't re-read cache); (b) niche present + decode present + adapt absent + live session → auto-fire `useAdaptConcepts` ONCE guarded by an `adaptFiredRef` (Board.tsx:182 pattern), gated so it never fires on permalink-with-persisted-adapt (Pitfall 3); (c) generating → 3 Skeletons in an `aria-live="polite"` container labelled "Generating adapted concepts…"; (d) success → `flex flex-col gap-4` stack of 3 `AdaptConceptCard` under the "ADAPTED FOR YOUR NICHE" FrameHero label; (e) decode absent → empty-state copy; (f) failure → `role="alert"` error block with WarningCircle + "Couldn't generate concepts" / "Try refreshing the page. Decode results are still available." (no retry button, D-07). All copy verbatim from UI-SPEC §Copywriting. Then update `AdaptShellNode.tsx` to accept `{ camera, layout }` and render `<AdaptFrameBody camera={camera} layout={layout} />` (replace the descriptor `<p>`, keep `'use client'` + `data-testid="adapt-shell"` + outer div). Update the two mount points: `Board.tsx:517` → `<AdaptShellNode camera={camera} layout={layout} />`; `BoardMobile.tsx:137` → pass the mobile camera + adapt layout the same way ContentAnalysis is passed there (D-10 mobile mirror). No fenced code beyond the analog. Fill in the plan-01 `AdaptFrameBody.test.tsx` scaffold (happy-dom; mock the hooks + mutation) and make every case green.
  </action>
  <verify>
    <automated>npx vitest run src/components/board/adapt 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `npx vitest run src/components/board/adapt` all green covering niche-prompt, mutateAsync-then-generate (no race), auto-fire-once, rehydrate-no-regen, independent-failure, decode-absent empty state
    - source: dual-read reads `variants.remix.adapt` before live state (`grep -E "remix.*adapt" src/components/board/adapt/AdaptFrameBody.tsx`)
    - source: an `adaptFiredRef`/ref guard prevents re-fire; the auto-fire effect condition excludes the case where persisted adapt is present (Pitfall 3)
    - source: `Board.tsx` and `BoardMobile.tsx` pass `camera` + `layout` to `<AdaptShellNode>` (`grep -n "AdaptShellNode camera" src/components/board/Board.tsx src/components/board/BoardMobile.tsx`)
    - source: error block has `role="alert"`; loading container has `aria-live="polite"`; copy strings match UI-SPEC verbatim
  </acceptance_criteria>
  <done>AdaptFrameBody implements the full state machine with dual-read rehydrate, auto-generate-once, inline niche prompt, and independent-failure error state; shell + desktop + mobile mounts render it; component tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify concept quality + Raycast styling on a real remix (manual-only)</name>
  <action>This is a human-verify checkpoint, not an automated task — no production code is written here. The executor has already shipped the full Adapt frame in Tasks 1-2; pause and have the developer run the manual verification steps below (concept semantics + Raycast visual conformance are the two manual-only checks per 04-VALIDATION.md, not unit-assertable). Resume on approval.</action>
  <what-built>
    The full Adapt frame end-to-end: auto-generated 3 concept cards (hook headline + coral "Borrowed:" chip + Angle/Who-it's-for muted rows), inline NichePicker for empty-niche, loading skeletons, and independent error state — on desktop canvas and mobile card-stack. The two VALIDATION manual-only checks (LLM concept semantics + visual Raycast conformance) are not unit-assertable beyond schema/leak guards.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev`, sign in, choose "Remix a viral video", paste a known viral TikTok URL (use a populated niche on your creator profile).
    2. On the remix board, confirm the Adapt frame auto-generates exactly 3 distinct concepts with NO extra click once Decode completes (D-04). (If Phase 3 Decode is not yet merged in this worktree, inject/seed `variants.remix.decode` from `decode.fixture.ts` to drive generation.)
    3. Read the 3 concepts: each must adapt the source FORMAT/STRUCTURE (open-loop, pacing, the turn) to YOUR niche — none should reproduce the source video's specific subject/content. Each card: bold hook headline, one coral "Borrowed:" chip, quiet Angle + Who-it's-for rows.
    4. Empty-niche path: clear your creator-profile niche, reload — confirm the inline NichePicker appears, picking a niche then "Generate concepts" PATCHes and generates; Decode keeps rendering niche-free meanwhile.
    5. Reload the permalink (`/analyze/[id]`) — confirm the 3 cards rehydrate from the persisted row with NO network call to `/api/remix/adapt` (check Network tab — Pitfall 3).
    6. Resize below 768px — confirm the same 3 cards render in the mobile card-stack Adapt slot (D-10).
    7. Raycast conformance: cards transparent bg, 6% border, 12px radius, hover only dims to bg-white/[0.02] (no lift, no border change); coral appears ONLY on the chip; Inter throughout.
  </how-to-verify>
  <verify><human-check>Developer confirms the 7 verification steps pass on a live remix (desktop + mobile); concepts are format-adapted not content-copied; permalink reload triggers no /api/remix/adapt fetch.</human-check></verify>
  <done>Developer types "approved" after confirming concept quality, Raycast styling, rehydrate-no-regen, mobile mirror, and the empty-niche inline-prompt flow.</done>
  <resume-signal>Type "approved" or describe issues (concept-quality, styling, rehydrate-fetch, mobile, niche-prompt).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (frame body) → /api/remix/adapt | Client triggers generation; server enforces auth/ownership/validation (plan 02) |
| Browser → /api/profile/creator-profile (PATCH) | Inline niche write; CSRF + RLS already enforced server-side |
| NichePicker input | Bounded to the fixed taxonomy (slugs→labels), not free text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | prompt injection via niche string | mitigate | Niche derives from the fixed taxonomy via `getPrimaryLabel`/`getSubLabel` (bounded labels), never arbitrary free text (NichePicker is the only source) |
| T-04-11 | DoS | double / repeated adapt generation | mitigate | `adaptFiredRef` guard + permalink-rehydrate gate fire the mutation at most once; no fire when `variants.remix.adapt` already present (Pitfall 3) |
| T-04-12 | Spoofing/Elevation | niche PATCH from the frame | accept | Reuses existing `/api/profile/creator-profile` PATCH — CSRF (415/403) + RLS already implemented (creator-profile/route.ts:92-211); no new control needed |
| T-04-13 | Information Disclosure | adapt failure leaking Decode/other frame state | mitigate | Independent frame state machine (D-06) — null adapt → Adapt error state only; Decode reads its own `variants.remix.decode` |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase — RESEARCH §Package Legitimacy Audit confirms zero new deps |
</threat_model>

<verification>
- `npx vitest run src/components/board/adapt` green.
- `npm test` green (full suite — grade-mode board + analyze flow unchanged, regression crit 5).
- Network tab: no `/api/remix/adapt` POST on permalink reload (Pitfall 3).
- Human-verify checkpoint approved (concept quality + Raycast styling + mobile).
- No new npm dependencies.
</verification>

<success_criteria>
- 3 concepts auto-generate once and render as stacked cards (ADAPT-01).
- Empty niche → inline NichePicker → PATCH → generate (ADAPT-02).
- Permalink rehydrates from variants.remix.adapt with no regeneration (D-05).
- Adapt failure leaves Decode intact (D-06).
- Mounts on desktop + mobile; grade-mode board unchanged (crit 5).
</success_criteria>

<output>
Create `.planning/phases/04-adapt-frame-niche/04-04-SUMMARY.md` when done.
</output>
