---
phase: 04-adapt-frame-niche
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/lib/engine/remix/adapt.ts
  - src/lib/engine/remix/__tests__/adapt.test.ts
  - src/app/api/remix/adapt/route.ts
  - src/app/api/remix/adapt/__tests__/route.test.ts
autonomous: true
requirements: [ADAPT-01]
user_setup: []

must_haves:
  truths:
    - "generateAdaptConcepts returns exactly 3 concepts, each with hook/angle/who_its_for/format_borrowed, or null on graceful failure"
    - "The adapt input builder physically cannot receive the source caption or luck[] (compile-time + test-enforced)"
    - "POST /api/remix/adapt requires auth, verifies row ownership, Zod-validates the body, and read-merge-writes variants.remix.adapt without clobbering variants.craft or variants.remix.decode"
    - "Adapt runs as a single Qwen JSON-mode call — never runPredictionPipeline, never usage_tracking/DAILY_LIMITS"
  artifacts:
    - path: "src/lib/engine/remix/adapt.ts"
      provides: "generateAdaptConcepts(input: AdaptInput): Promise<AdaptConcept[] | null> + buildAdaptUserContent + Zod schemas"
      exports: ["generateAdaptConcepts"]
      contains: "QWEN_REASONING_MODEL"
    - path: "src/app/api/remix/adapt/route.ts"
      provides: "POST handler: auth → CSRF/Content-Type → Zod body → ownership → adapt.ts → read-merge-write"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/remix/adapt/route.ts"
      to: "src/lib/engine/remix/adapt.ts"
      via: "generateAdaptConcepts(input)"
      pattern: "generateAdaptConcepts"
    - from: "src/lib/engine/remix/adapt.ts"
      to: "@/lib/engine/qwen/client"
      via: "getQwenClient + QWEN_REASONING_MODEL + QWEN_SEED"
      pattern: "getQwenClient"
    - from: "src/app/api/remix/adapt/route.ts"
      to: "analysis_results.variants"
      via: "read-merge-write spreading current AND current.remix"
      pattern: "currentRemix"
---

<objective>
Build the backend slice: the Qwen-only adapt generator (`adapt.ts`) and its `/api/remix/adapt` POST endpoint.

`adapt.ts` is a single `qwen3.6-plus` JSON-mode call mirroring `omni-analysis.ts`/`wave3/pass2.ts` exactly (temp:0, seed:7, maxRetries:0, manual repair loop, stripModelOutput before parse, Zod `.length(3)`). The endpoint authenticates, verifies row ownership, Zod-validates the body, calls the generator, and read-merge-writes `variants.remix.adapt`. Delivers ADAPT-01.

Purpose: Generate exactly 3 format-not-content concepts off a lightweight Qwen path (D-04), persist them safely (D-05, Pitfall 2), and enforce the content-leak + auth + ownership security controls.
Output: `adapt.ts`, `route.ts`, and their two Wave 0 test files filled in (green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-adapt-frame-niche/04-CONTEXT.md
@.planning/phases/04-adapt-frame-niche/04-RESEARCH.md
@.planning/phases/04-adapt-frame-niche/04-PATTERNS.md
@.planning/phases/04-adapt-frame-niche/04-VALIDATION.md
@.planning/phases/04-adapt-frame-niche/04-01-SUMMARY.md

<interfaces>
<!-- Contract from plan 01 (decode-types.ts). Import, do not redefine. -->
- `AdaptInput = Pick<DecodeOutput,'hook_pattern'|'structure'|'the_turn'|'emotional_beat'|'repeatable'> & { niche: string }`
- `AdaptConcept { hook; angle; who_its_for; format_borrowed }`
- `DECODE_FIXTURE` from `@/lib/engine/remix/decode.fixture` (test input)

<!-- Verified engine seams (import, do not reimplement) -->
- `getQwenClient()`, `QWEN_REASONING_MODEL` ("qwen3.6-plus"), `QWEN_SEED` (7) — `@/lib/engine/qwen/client` (maxRetries:0; app owns retry loop)
- `stripModelOutput(raw)` — `@/lib/engine/utils/strip`
- Zod `.length(3)` precedent — `src/lib/engine/qwen/schemas.ts:125`
- Retry+repair loop shape — `src/lib/engine/qwen/omni-analysis.ts:159-291`
- `persistCraftToVariants` read-merge-write — `src/app/api/analyze/route.ts:121-138`
- Auth pattern — `src/app/api/analyze/route.ts:175-183`
- CSRF + Content-Type guards — `src/app/api/profile/creator-profile/route.ts:92-125`
- `createServiceClient` — `@/lib/supabase/service`; `createClient` — `@/lib/supabase/server`; `Json` — `@/types/database.types`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Qwen-only adapt generator (engine/remix/adapt.ts)</name>
  <read_first>
    - .planning/phases/04-adapt-frame-niche/04-PATTERNS.md §"adapt.ts" (the full retry+repair+Zod+strip template, imports, input-builder guard)
    - src/lib/engine/qwen/omni-analysis.ts:149-291 (canonical retry+repair loop)
    - src/lib/engine/wave3/pass2.ts:130-201 (reasoning-model JSON call + explicit count guard)
    - src/lib/engine/qwen/schemas.ts:120-164 (.length(N) Zod precedent)
    - src/lib/engine/remix/__tests__/adapt.test.ts (the Wave 0 scaffold from plan 01 — make it pass)
  </read_first>
  <files>src/lib/engine/remix/adapt.ts, src/lib/engine/remix/__tests__/adapt.test.ts</files>
  <behavior>
    - Test: valid Qwen response → returns exactly 3 AdaptConcept, each field non-empty
    - Test: short/invalid first response then valid → repair attempt (extraInstruction) succeeds, returns 3
    - Test: invalid on all attempts → returns null (graceful, no throw) + Sentry.captureException with tag stage="remix_adapt"
    - Test: buildAdaptUserContent accepts AdaptInput only — feeding DECODE_FIXTURE.luck text is impossible (type) and the produced prompt string contains NO luck[] item text (luck-exclusion, D-01)
    - Test: the prompt user content contains repeatable[] item labels but no caption/content_summary field (no-caption guard, Pitfall 1)
  </behavior>
  <action>
    Create `generateAdaptConcepts(input: AdaptInput): Promise<AdaptConcept[] | null>` mirroring PATTERNS §adapt.ts. Define a stable `ADAPT_SYSTEM_PROMPT` (system role, kept constant for cache prefix) enforcing "exactly 3 DISTINCT concepts, each adapting the source FORMAT/STRUCTURE not its content, grounded in the supplied niche; output strict JSON {concepts:[{hook,angle,who_its_for,format_borrowed}]}". Define `buildAdaptUserContent(input: AdaptInput): string` that serializes ONLY the 4 structural fields + `repeatable[]` + niche — its parameter type is `AdaptInput`, so `luck`/caption are unrepresentable (Pitfall 1). Use `getQwenClient()`, model `QWEN_REASONING_MODEL`, `temperature:0`, `seed:QWEN_SEED`, `response_format:{type:"json_object"}`, an `AbortController` timeout, `MAX_RETRIES=1` loop with `extraInstruction` repair on retry, `stripModelOutput` before `JSON.parse`, `AdaptConceptsZodSchema` (`z.array(...).length(3)`) safeParse + a belt-and-suspenders `concepts.length !== 3` throw (mirrors pass2.ts:197). On exhaustion: `Sentry.captureException(lastError, { tags: { stage: "remix_adapt" } })` then `return null`. Use `createLogger({ module: "engine.remix.adapt" })`. Do NOT touch usage_tracking/DAILY_LIMITS/runPredictionPipeline (D-04). No fenced code — follow the PATTERNS analog. Fill in the plan-01 `adapt.test.ts` scaffold (convert todo→real) and make every case green; mock `getQwenClient`/`chat.completions.create` per the existing engine mock precedent.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/remix/__tests__/adapt.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `npx vitest run src/lib/engine/remix/__tests__/adapt.test.ts` all green, ≥5 cases passing (exactly-3, repair, null-on-fail, no-caption, luck-exclusion)
    - source: `buildAdaptUserContent` parameter type is `AdaptInput` (grep `grep -c "AdaptInput" src/lib/engine/remix/adapt.ts` ≥1)
    - source: `adapt.ts` references `QWEN_REASONING_MODEL`, `QWEN_SEED`, `stripModelOutput`, `.length(3)` and contains NO reference to `runPredictionPipeline`/`usage_tracking`/`DAILY_LIMITS` (`grep -E 'runPredictionPipeline|usage_tracking|DAILY_LIMITS' src/lib/engine/remix/adapt.ts` returns nothing)
    - behavior: luck-exclusion test feeds DECODE_FIXTURE and asserts no `luck[]` item text appears in the prompt string
  </acceptance_criteria>
  <done>adapt.test.ts green; generator returns exactly-3 or null; input builder structurally excludes luck/caption; no scoring-pipeline coupling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/remix/adapt route (auth + ownership + validate + persist)</name>
  <read_first>
    - .planning/phases/04-adapt-frame-niche/04-PATTERNS.md §"route.ts" (imports, auth, CSRF/Content-Type, Zod body schema, read-merge-write, ownership check)
    - src/app/api/analyze/route.ts:1-16,102-145,175-183 (imports, persistCraftToVariants, auth)
    - src/app/api/profile/creator-profile/route.ts:92-145 (CSRF/Content-Type guards + safeParse body validation)
    - src/app/api/remix/adapt/__tests__/route.test.ts (the Wave 0 scaffold from plan 01 — make it pass)
  </read_first>
  <files>src/app/api/remix/adapt/route.ts, src/app/api/remix/adapt/__tests__/route.test.ts</files>
  <behavior>
    - Test: no authenticated user → 401
    - Test: Content-Type not application/json → 415; cross-origin → 403
    - Test: malformed body (missing analysis_id / bad shape) → 400 (Zod safeParse)
    - Test: analysis row belongs to another user → 404 (ownership, never 200)
    - Test: valid request → calls generateAdaptConcepts, read-merge-writes; pre-seeded variants.craft AND variants.remix.decode SURVIVE (Pitfall 2); returns { concepts }
    - Test: generator returns null → 500 (or documented error), variants NOT written
  </behavior>
  <action>
    Create the POST handler per PATTERNS §route.ts. Order: (1) auth via `createClient().auth.getUser()` → 401 (per ADAPT security V2); (2) Content-Type 415 + cross-origin 403 guards (creator-profile pattern); (3) parse JSON body, `AdaptRequestSchema.safeParse` → 400 — schema = `{ analysis_id: z.string().uuid(), decode: { hook_pattern, structure, the_turn, emotional_beat, repeatable: z.array({label,why_repeatable}) }, niche: z.string().min(1).max(200) }` — NOTE `luck` is NOT in the schema (D-01 guard at the wire); (4) ownership check via `createServiceClient().from("analysis_results").select("user_id").eq("id", analysis_id).single()` → 404 if missing or `user_id !== user.id` (ASVS V4); (5) build `AdaptInput` from the validated `decode` + `niche`, call `generateAdaptConcepts` → null → 500 (do NOT write); (6) read-merge-write into `variants.remix.adapt` spreading `current` AND `current.remix` (Pitfall 2, verbatim from analyze/route.ts:121-138), scoped `.eq("id", analysis_id)`; (7) return `Response.json({ concepts })`. Wrap the body in try/catch → 500. Use `nanoid` request id + `createLogger` per analyze/route.ts. Fill in the plan-01 `route.test.ts` scaffold and make every case green (mock supabase server + service clients + generateAdaptConcepts).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/remix/adapt/__tests__/route.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - test-command: `npx vitest run src/app/api/remix/adapt/__tests__/route.test.ts` all green, covering 401 / 415 / 403 / 400 / 404-ownership / merge-preservation / null→500
    - source: `route.ts` calls `supabase.auth.getUser()` and returns 401 before any DB/LLM work
    - source: read-merge-write spreads `current` AND `current.remix` (`grep -c "currentRemix" src/app/api/remix/adapt/route.ts` ≥1); `AdaptRequestSchema` has no `luck` key
    - behavior: merge-preservation test pre-seeds variants.craft + variants.remix.decode, runs the write, asserts all three keys present
  </acceptance_criteria>
  <done>route.test.ts green; endpoint enforces auth + ownership + validation and persists adapt without clobbering craft/decode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/remix/adapt | Untrusted POST body (analysis_id, decode payload, niche) crosses here |
| /api/remix/adapt → DashScope/Alibaba (Qwen) | Adapt input leaves the trust boundary to a third-party LLM (IP boundary) |
| Service client → analysis_results | Privileged write must be scoped to the row owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Elevation/DoS | POST /api/remix/adapt (unauth cost abuse) | mitigate | `supabase.auth.getUser()` → 401 before any LLM/DB work (ASVS V2); mirrors analyze/route.ts:181 |
| T-04-04 | Information Disclosure | adapt prompt → DashScope (source-caption leak) | mitigate | `AdaptRequestSchema` accepts only the 4 structural fields + repeatable[] + niche; no `luck`/caption keys; `buildAdaptUserContent(input: AdaptInput)` makes caption unrepresentable (D-01; aligns with derive-and-drop IP boundary) |
| T-04-05 | Elevation | cross-user variants write | mitigate | Ownership check (`select user_id` → compare `user.id`) → 404 before write; service-client update scoped `.eq("id", analysis_id)`; RLS backstop (ASVS V4) |
| T-04-06 | Tampering | malformed/oversized request body | mitigate | `AdaptRequestSchema.safeParse` → 400; `niche` bounded `.max(200)`; Content-Type 415 + cross-origin 403 guards |
| T-04-07 | Spoofing | CSRF on state-mutating POST | mitigate | Content-Type `application/json` 415 guard + cross-origin 403 guard (creator-profile/route.ts:92-125 pattern) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase — RESEARCH §Package Legitimacy Audit confirms zero new deps |
</threat_model>

<verification>
- `npx vitest run src/lib/engine/remix src/app/api/remix` green.
- `grep -E 'runPredictionPipeline|usage_tracking|DAILY_LIMITS' src/lib/engine/remix/adapt.ts` returns nothing (D-04 lightweight path).
- Route enforces 401 (auth) and 404 (ownership) before any write; read-merge-write preserves craft + decode.
- No new npm dependencies.
</verification>

<success_criteria>
- generateAdaptConcepts returns exactly 3 concepts or null; input builder structurally excludes luck/caption.
- POST /api/remix/adapt: 401 unauth, 404 cross-user, 400 invalid body, 415/403 CSRF guards, persists variants.remix.adapt preserving craft + decode.
- Adapt is a single Qwen call off the scoring path.
</success_criteria>

<output>
Create `.planning/phases/04-adapt-frame-niche/04-02-SUMMARY.md` when done.
</output>
