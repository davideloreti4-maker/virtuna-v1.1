---
phase: 44-verification-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/tokens.md
  - docs/component-index.md
autonomous: true

must_haves:
  truths:
    - "Token reference document lists every token with its value, category, and usage note"
    - "Component index links every component to its source file and showcase page"
  artifacts:
    - path: "docs/tokens.md"
      provides: "Complete token reference with all values, categories, and usage guidance"
    - path: "docs/component-index.md"
      provides: "Component index with links to source files and /showcase pages"
  key_links:
    - from: "docs/tokens.md"
      to: "src/app/globals.css"
      via: "Documents all @theme values"
      pattern: "@theme"
    - from: "docs/component-index.md"
      to: "src/components/ui/index.ts"
      via: "Maps every export to source file and showcase URL"
      pattern: "export"
---

<objective>
Create token reference document and component index.

Purpose: DOC-01 requires a token reference with all values and usage notes. DOC-08 requires a component index linking to showcase and source. These are standalone documentation tasks that can run in parallel with verification scripts.

Output: docs/tokens.md and docs/component-index.md
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
@src/components/ui/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create token reference document</name>
  <files>docs/tokens.md</files>
  <action>
1. Create docs/ directory if it doesn't exist.

2. Create docs/tokens.md by reading src/app/globals.css and documenting EVERY token in the @theme block. Structure:

   ```markdown
   # Design Tokens Reference

   > Source: `src/app/globals.css` | Visual reference: [/showcase](/showcase)

   ## Overview
   [Brief description of two-tier architecture: primitive -> semantic]
   [Note: dark-mode first, oklch for colors where accurate, hex/rgba for dark grays]

   ## Colors
   ### Coral Scale (Brand)
   | Token | Value | Preview | Usage |
   |-------|-------|---------|-------|
   | --color-coral-100 | oklch(...) | | Subtle backgrounds, hover states |
   ...

   ### Gray Scale
   | Token | Value | Preview | Usage |
   ...

   ### Semantic Colors
   #### Backgrounds
   | Token | Value | Maps To | Usage |
   | --color-background | var(--color-gray-950) | #07080a | Page background |
   ...

   #### Text
   ...

   #### Accent
   ...

   #### Status
   ...

   #### Borders
   ...

   #### States
   ...

   ## Typography
   ### Font Families
   | Token | Value | Usage |

   ### Font Sizes
   | Token | Value | Usage |

   ### Font Weights
   ...

   ### Line Heights
   ...

   ### Letter Spacing
   ...

   ## Spacing
   | Token | Value | Usage |
   | --spacing-0 | 0 | Reset |
   | --spacing-1 | 4px | Tight gaps |
   ...

   ## Shadows
   | Token | Value | Usage |
   | --shadow-sm | ... | Subtle elevation |
   ...

   ## Border Radius
   | Token | Value | Usage |

   ## Animation
   ### Durations
   | Token | Value | Usage |

   ### Easings
   | Token | Value | Usage |

   ## Z-Index
   | Token | Value | Usage |

   ## Gradients
   | Token | Value | Usage |
   ```

3. For EVERY token:
   - Read the actual value from globals.css
   - Provide a usage note (what it's for, when to use)
   - For semantic tokens, note which primitive they map to

4. Include a "Quick Reference" section at the top with the most commonly used tokens.

5. Include a note about the Tailwind v4 @theme compilation behavior for dark colors (hex values used instead of oklch for accuracy).
  </action>
  <verify>
    - `wc -l docs/tokens.md` > 100 lines (comprehensive)
    - Every token category from globals.css is represented
    - Usage notes are practical, not just repeating the token name
  </verify>
  <done>Token reference document at docs/tokens.md covers every @theme token with values and usage guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Create component index</name>
  <files>docs/component-index.md</files>
  <action>
1. Create docs/component-index.md by reading src/components/ui/index.ts and mapping every export. Structure:

   ```markdown
   # Component Index

   > Quick reference for all Virtuna design system components

   ## UI Components

   | Component | Source | Showcase | Category | Complexity |
   |-----------|--------|----------|----------|------------|
   | Button | [source](../src/components/ui/button.tsx) | [/showcase/inputs](/showcase/inputs) | Interactive | Full |
   | Card | [source](../src/components/ui/card.tsx) | [/showcase/data-display](/showcase/data-display) | Display | Full |
   ...

   ## Motion Components

   | Component | Source | Showcase | Description |
   |-----------|--------|----------|-------------|
   | FadeIn | [source](../src/components/motion/fade-in.tsx) | [/showcase/utilities](/showcase/utilities) | Scroll-reveal fade |
   ...

   ## Effects Components

   | Component | Source | Showcase | Description |
   |-----------|--------|----------|-------------|
   | NoiseTexture | [source](../src/components/effects/noise-texture.tsx) | [/showcase/utilities](/showcase/utilities) | SVG grain overlay |
   ...

   ## Primitives

   | Component | Source | Description |
   |-----------|--------|-------------|
   | GlassPanel | [source](../src/components/primitives/GlassPanel.tsx) | Configurable glass blur |
   ...
   ```

2. For each component:
   - Link to source file (relative path from docs/)
   - Link to showcase page URL
   - Note the component category and documentation depth tier (Full/Standard/Brief per research)
   - List all named exports (subcomponents, types, variants)

3. Include a "By Category" section grouping components:
   - Interactive: Button, Input, InputField, Select, SearchableSelect, Toggle, Dialog, Tabs, CategoryTabs
   - Display: Badge, Card, GlassCard, Avatar, AvatarGroup, Skeleton, ExtensionCard, TestimonialCard
   - Typography: Heading, Text, Caption, Code
   - Feedback: Spinner, Toast, Kbd, ShortcutBadge
   - Layout: Divider, GlassPanel, Icon
   - Motion: FadeIn, FadeInUp, SlideUp, StaggerReveal, HoverScale, PageTransition, FrozenRouter
   - Effects: NoiseTexture, ChromaticAberration

4. Include an "Exports Quick Reference" section showing exact import paths.
  </action>
  <verify>
    - `cat docs/component-index.md` lists all 21 UI components + motion + effects
    - Every component has source link and showcase link
    - Import paths are accurate
  </verify>
  <done>Component index at docs/component-index.md maps every component to its source, showcase page, and category.</done>
</task>

</tasks>

<verification>
- docs/tokens.md exists and covers every token category from globals.css
- docs/component-index.md exists and maps all components
- No component code modified
- All links use correct relative paths
</verification>

<success_criteria>
- Token reference is comprehensive — a developer can look up any token value and know when to use it
- Component index is complete — every exported component is listed with source and showcase links
</success_criteria>

<output>
After completion, create `.planning/phases/44-verification-documentation/44-03-SUMMARY.md`
</output>
