---
phase: 03-rich-visuals-as-drill-downs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/board/verdict/ScoreDistribution.tsx
  - src/components/board/audience/RetentionChart.tsx
  - src/components/board/content-analysis/CraftFilmstrip.tsx
  - src/components/board/audience/RetentionPlayer.tsx
autonomous: true
requirements: [READ-09]

must_haves:
  truths:
    - "ScoreDistribution, RetentionChart, CraftFilmstrip render flat-warm — no glow, no old-coral, no gradient chrome"
    - "The ScoreDistribution --color-frame latent bug is fixed (resolves to --color-surface, not cold near-black)"
    - "CraftFilmstrip keeps its footage grade/grain/vignette (filmed-video read) while losing UI shine"
  artifacts:
    - path: "src/components/board/verdict/ScoreDistribution.tsx"
      provides: "Score histogram + confidence band, matte (you-marker = solid pin + hairline ring, no glow)"
      contains: "var(--color-accent)"
    - path: "src/components/board/audience/RetentionChart.tsx"
      provides: "Retention curve + niche/ghost overlay, matte (cream curve, flat cream-alpha area, coral drop mark only)"
      contains: "var(--color-accent)"
    - path: "src/components/board/content-analysis/CraftFilmstrip.tsx"
      provides: "Energy-graded filmstrip, matte chrome (no end-cap glow, no inset white-shine) + preserved footage grading"
      contains: "var(--color-accent)"
  key_links:
    - from: "src/components/board/verdict/ScoreDistribution.tsx"
      to: "src/app/globals.css @theme tokens"
      via: "repoints #FF7F50/#161719/gradients → --color-accent/--color-surface"
      pattern: "var\\(--color-(accent|surface|surface-elevated|foreground)"
---

<objective>
Tier-2 re-treat (the real reskin work, D-07): the three charts whose gradient fills, glows, and Canvas-style treatments fight the matte taste bar — `ScoreDistribution`, `RetentionChart`, `CraftFilmstrip` — get an actual visual re-treatment to flat-warm, because a pure token-swap won't catch a gradient or a `box-shadow` glow. Plus `RetentionPlayer` (the heaviest reskin: blur/glass/glows) IF it ships — but it's discretionary/deferred (see 03-05), so reskin it here only to keep the file clean and the matte-lint green, without committing to mounting it.

Purpose: SC-3 — drill-down visuals read flat-warm (no Raycast glass, no glow/shine) consistent with the Reading. The re-treat is TARGETED (kill glows/shines/blurs, flatten gradient *chrome*) — KEEP footage *grading* (filmstrip energy grade/grain/vignette read as filmed video, not UI chrome). Not a from-scratch redesign — geometry/layout/information design preserved.
Output: three matte-clean charts + a matte-clean RetentionPlayer; the `reskin-matte.test.ts` gate (03-01) flips GREEN for these files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-rich-visuals-as-drill-downs/03-RESEARCH.md
@.planning/phases/03-rich-visuals-as-drill-downs/03-UI-SPEC.md

<tokens>
<!-- Verified present in src/app/globals.css @theme (RESEARCH § Token availability). Use these exact names. -->
--color-accent          /* oklch(0.68 0.13 33) ≈ #d97757 — the matured terracotta coral; REPLACES old #FF7F50 */
--color-surface         /* #1e1d1b — panel surface; REPLACES var(--color-frame,#161719) cold-near-black */
--color-surface-elevated/* #2f2e2b — lifted cells/tiles */
--color-foreground      /* #ece7de cream primary */
--color-foreground-secondary /* #c2bdb4 */
--color-foreground-muted/* #8a857c — axes, captions, timestamps */
--color-border          /* rgba(255,255,255,0.06) hairline */
--color-success / --color-warning / --color-error  /* score zones — only for score-derived band color */
--shadow-float          /* the ONE allowed float shadow (popover/floating only) — never a chart glow */
/* NOTE: --color-frame is ABSENT. ScoreDistribution's var(--color-frame,#161719) silently resolves to cold near-black → repoint to var(--color-surface). */
/* Cream-alpha for dots/curve/area where a token doesn't fit: rgba(236,231,222,…) — the exact value persona-cloud.tsx uses. */
</tokens>

<reskin_map>
<!-- Exact violation lines, verified against live code this session (RESEARCH § Reskin Surface Area Tier-2). -->

ScoreDistribution.tsx:
- L72  panel bg `linear-gradient(180deg, rgba(255,255,255,0.022), …)` → flatten to solid --color-surface (or a barely-there cream wash)
- L196–197 lane track inset-shine `inset 0 0 0 1px rgba(255,255,255,0.05), inset 0 1px 1px rgba(0,0,0,0.35)` → flat --color-border fill, drop inset shadow
- L207 lane fill `linear-gradient(90deg, rgba(255,255,255,0.10), 0.22)` → flat cream-alpha
- L240 band `boxShadow: inset 0 0 0 1px rgba(255,127,80,0.22), 0 0 18px rgba(255,127,80,0.10)` → flat --color-accent ~8% tint + hairline coral border, REMOVE the 0 0 18px outer glow
- L314 pin `boxShadow: 0 0 9px rgba(255,127,80,0.5)` → solid coral pin, REMOVE glow
- L325 dot `border: 1.5px solid var(--color-frame, #161719)` → `var(--color-surface)` (the latent bug)
- L326 dot `boxShadow: 0 0 13px rgba(255,127,80,0.7)` → solid coral dot + hairline ring, REMOVE glow
- surfaces L325/L337 → --color-surface / --color-surface-elevated

RetentionChart.tsx:
- L132–135 `<linearGradient id="retentionFill">` white-alpha area → flat cream-alpha rgba(236,231,222,0.06→0)
- L142 area fill uses url(#retentionFill) — keep the fill but with the cream stops above
- L148 ghost line → cream-muted dashed (keep dashed identity)
- L160 lock line `stroke="rgba(255,127,80,0.22)"` → coral low-alpha derived from --color-accent, no shadow
- L169 curve stroke `rgba(255,255,255,0.72)` → cream `rgba(236,231,222,0.72)`
- L175 drop dot `fill="#FF7F50"` → `var(--color-accent)`
- L184/L186 delta label `color:'#FF7F50'` → `var(--color-accent)`
- L237 filmstrip cell border `cell.isDrop ? 'rgba(255,127,80,0.55)' : 'rgba(255,255,255,0.06)'` → coral-from-token / --color-border; KEEP the footage darkening filter (grading)
- L241 drop-cell `boxShadow: rgba(255,127,80,0.14) 0 0 0 1px` → hairline coral ring, drop the glow
- L262 time mark `color:'#FF7F50'` → `var(--color-accent)`

CraftFilmstrip.tsx (Pitfall 4 — KEEP footage grading, KILL chrome shine):
- L58 inset `rgba(255,255,255,.02)` white-shine → hairline or remove
- L66 `boxShadow: inset 0 2px 0 0 rgba(255,255,255,.34)` + L67 top gradient → reduce to hairline / remove (Raycast shine)
- L120 `boxShadow: inset 0 1px 0 0 rgba(255,255,255,.05), …` → remove the white inset highlight
- L150 no-CTA tint `linear-gradient(90deg, transparent, rgba(255,127,80,.14))` → cream-alpha or token coral low-alpha (this is the scalpel edge — keep subtle)
- L155 end-cap `boxShadow: 0 0 10px rgba(255,127,80,.55)` → REMOVE the glow; keep the flat coral scalpel bar (`background: var(--color-accent)` stays)
- L98/L178/L186 caption/axis `text-white/40`, `text-white/[0.22]` → text-foreground-muted / cream-alpha
- L171 bar fill `rgba(244,244,245,.26)` → cream-alpha
- KEEP: L135 energyGradeFilter grade, L141–142 grain (opacity-[0.45]), per-cell vignette — these are footage texture, NOT chrome (Pitfall 4)

RetentionPlayer.tsx (reskin file clean even though mount is deferred to 03-05 discretion):
- L204/L224/L253/L277 `#FF7F50` → `var(--color-accent)`
- L174 `backdrop-filter: blur(2px)` → REMOVE (Lightning CSS strips it + matte forbids) → flat dim
- L216 glass tooltip `#18191a` + inset shine → `--color-surface` flat, no inset
- L148 backplate `#0c0d0f` → charcoal token
- L205 knob ring coral box-shadow → hairline, no glow
</reskin_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-treat ScoreDistribution.tsx to matte flat-warm (+ fix the --color-frame latent bug)</name>
  <files>src/components/board/verdict/ScoreDistribution.tsx</files>
  <read_first>
    - src/components/board/verdict/ScoreDistribution.tsx (the file being modified — read it fully: the histogram geometry, the three modes field/lane/absolute via resolveMode, the you-marker pin/dot/chip, the confidence band, all the cited violation lines L72/L196–197/L207/L240/L314/L325/L326/L337)
    - .planning/phases/03-rich-visuals-as-drill-downs/03-RESEARCH.md (§ Reskin Surface Area Tier-2 ScoreDistribution row + Pitfall 6 on --color-frame)
    - src/app/globals.css (confirm the token names in <tokens> resolve; confirm --color-frame is absent)
  </read_first>
  <action>
    Repoint every hardcoded coral `rgba(255,127,80,…)` / `#FF7F50`-family to `var(--color-accent)`. REMOVE all three `0 0 Npx` outer glows: the confidence band glow (L240 — keep `inset 0 0 0 1px` hairline coral border + a flat `--color-accent` ~8% tint via background, drop the `0 0 18px` halo), the pin glow (L314 — solid coral pin, no shadow), the dot glow (L326 — solid coral dot + a 1px hairline ring only). Fix the latent bug: L325 `var(--color-frame, #161719)` → `var(--color-surface)`. Flatten the panel bg gradient (L72) to solid `var(--color-surface)` or a barely-there cream wash. Flatten the lane track (L196–197) to a flat `var(--color-border)` fill and DROP the inset shadow; lane fill (L207) → flat cream-alpha. Repoint the `#1c1d20`/`#161719` surfaces (L325/L337) to `--color-surface`/`--color-surface-elevated`. Preserve ALL geometry (PLOT_H, BASE, GAP, MAX_STACK, the 10-decile histogram, clampPct, the field/lane/absolute mode logic) — this is a paint-only re-treat. Do NOT change the props interface or `resolveMode` (the internal lane/absolute degrade is the in-panel PanelEmpty equivalent — keep it).
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/reskin-matte.test.ts src/components/board/verdict 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -nE "#FF7F50|0 0 1[0-9]px|0 0 9px|color-frame" -- src/components/board/verdict/ScoreDistribution.tsx` returns NOTHING (all glows + old coral + the absent token gone)
    - `git grep -n "var(--color-surface)" -- src/components/board/verdict/ScoreDistribution.tsx` shows the L325 dot border now resolves to surface
    - `reskin-matte.test.ts` no longer flags ScoreDistribution
    - `npx vitest run src/components/board/verdict` exits 0 (verdict-derive + any ScoreDistribution coverage still green; geometry unchanged)
  </acceptance_criteria>
  <done>ScoreDistribution is matte: solid coral you-marker with hairline ring, no glows, flat surfaces, the --color-frame bug fixed; histogram geometry untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Re-treat RetentionChart.tsx to matte flat-warm</name>
  <files>src/components/board/audience/RetentionChart.tsx</files>
  <read_first>
    - src/components/board/audience/RetentionChart.tsx (the file being modified — read it fully: the SVG curve + the nicheGhostCurve overlay L67–78/L143–152, the drop dot/lock-line/delta, the retentionFill gradient L132–135, the filmstrip row L237/L241, the time axis L262, the cited violation lines)
    - src/components/board/audience/retention-geometry.ts (VB_W/VB_H/PAD_TOP/FLOOR_Y, yForValue, xForIndex — geometry to PRESERVE)
    - .planning/phases/03-rich-visuals-as-drill-downs/03-RESEARCH.md (§ Reskin Surface Area Tier-2 RetentionChart row; Landmine 3 confirms niche/ghost is built-in)
  </read_first>
  <action>
    Repoint every `#FF7F50` (drop dot L175, delta label L184/L186, time mark L262) → `var(--color-accent)`. Recolor the `retentionFill` linearGradient (L132–135) from white-alpha stops to FLAT cream-alpha `rgba(236,231,222,0.06)` → `rgba(236,231,222,0)` (a subtle survival-area fill, no white). Curve stroke (L169) `rgba(255,255,255,0.72)` → cream `rgba(236,231,222,0.72)`. Lock line (L160) `rgba(255,127,80,0.22)` → coral low-alpha derived from the accent token, and DROP any shadow. Ghost line (L148) → cream-muted dashed (KEEP the dashed identity — it's the niche overlay). Filmstrip cell border (L237) → coral-from-token for the drop cell / `var(--color-border)` otherwise; DROP the drop-cell coral `box-shadow` glow (L241) → a 1px hairline coral ring instead. KEEP the footage darkening `filter` on filmstrip cells (that's grading, not chrome — Pitfall 4). White-alpha y-guides/labels → cream-muted. Preserve ALL geometry and the niche/ghost overlay logic (it transplants with the curve — do NOT extract or alter it). Do NOT change the props interface (`curve`, `heatmap`, `drop`, `totalDurationSec`, `filmstrips`, `nicheCompletionPct`, `isLoading`).
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/reskin-matte.test.ts src/components/board/audience 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -nE "#FF7F50|rgba\(255, ?127, ?80|rgba\(255, ?255, ?255, ?0\.72" -- src/components/board/audience/RetentionChart.tsx` returns NOTHING
    - The `retentionFill` gradient stops are cream `rgba(236,231,222,…)`, not white
    - `reskin-matte.test.ts` no longer flags RetentionChart
    - `npx vitest run src/components/board/audience` exits 0 (audience-derive + RetentionPlayer test still green; geometry unchanged)
  </acceptance_criteria>
  <done>RetentionChart is matte: cream curve, flat cream-alpha area, coral drop-mark/lock-line only via token, niche/ghost overlay preserved, footage grading kept.</done>
</task>

<task type="auto">
  <name>Task 3: Re-treat CraftFilmstrip.tsx (kill chrome shine, KEEP footage grading) + RetentionPlayer.tsx</name>
  <files>src/components/board/content-analysis/CraftFilmstrip.tsx, src/components/board/audience/RetentionPlayer.tsx</files>
  <read_first>
    - src/components/board/content-analysis/CraftFilmstrip.tsx (the file being modified — read it fully: the per-cell render with energyGradeFilter L135 + grain L141–142 + vignette, the seam/hook inset shines L58/L66/L120, the no-CTA end-cap L150/L155, the caption/axis text L98/L178/L186, the bar L171)
    - src/components/board/audience/RetentionPlayer.tsx (the file being modified — the playhead/scrubber `#FF7F50`, the `backdrop-filter: blur` L174, the glass tooltip L216, backplate L148, knob ring L205)
    - .planning/phases/03-rich-visuals-as-drill-downs/03-RESEARCH.md (§ Reskin Surface Area Tier-2 CraftFilmstrip + RetentionPlayer rows; Pitfall 4 — the footage-vs-chrome distinction is the crux of this task)
  </read_first>
  <action>
    CraftFilmstrip — KILL chrome, KEEP grading: REMOVE the no-CTA end-cap glow (L155 `box-shadow: 0 0 10px rgba(255,127,80,.55)`) while keeping the flat coral scalpel bar (`background: var(--color-accent)`). Reduce/remove the white-shine inset highlights (L58 `rgba(255,255,255,.02)`, L66 `inset 0 2px 0 0 rgba(255,255,255,.34)` + its L67 top gradient, L120 inset white) to hairline or nothing — these are Raycast shine. Recolor the no-CTA tint (L150) to a token-coral low-alpha or cream-alpha. Repoint caption/axis `text-white/40` (L98/L178) and `text-white/[0.22]` (L186) → `text-foreground-muted` / cream-alpha; bar fill (L171) `rgba(244,244,245,.26)` → cream-alpha. CRITICAL (Pitfall 4): DO NOT touch the energy grade (L135 energyGradeFilter), the film grain (L141–142 opacity-[0.45]), or the per-cell vignette — they read as *filmed video*, are footage treatment not UI chrome, and the EMPTY_CELL_BG/hasFrames gating must stay (degradation). RetentionPlayer — repoint `#FF7F50` (L204/L224/L253/L277) → `var(--color-accent)`; REMOVE `backdrop-filter: blur(2px)` (L174) and replace with a flat dim (Lightning CSS strips it + matte forbids); glass tooltip (L216 `#18191a` + inset shine) → `var(--color-surface)` flat; backplate (L148 `#0c0d0f`) → a charcoal token; knob ring (L205) → hairline, no glow. Preserve RetentionPlayer's geometry-agreement with retention-geometry.ts and its byte-for-byte fallback to RetentionChart when videoSrc is null (do NOT change props or the fallback).
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/reskin-matte.test.ts src/components/board/content-analysis src/components/board/audience 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -nE "0 0 10px|#FF7F50|backdrop-filter|rgba\(255,255,255,\.34" -- src/components/board/content-analysis/CraftFilmstrip.tsx src/components/board/audience/RetentionPlayer.tsx` returns NOTHING
    - `git grep -nE "energyGradeFilter|grain|opacity-\[0\.45\]" -- src/components/board/content-analysis/CraftFilmstrip.tsx` STILL returns the grading (proof footage texture was preserved — Pitfall 4)
    - `reskin-matte.test.ts` no longer flags CraftFilmstrip or RetentionPlayer
    - `npx vitest run src/components/board/content-analysis src/components/board/audience` exits 0
  </acceptance_criteria>
  <done>CraftFilmstrip lost its UI shine + end-cap glow but kept its footage grade/grain/vignette; RetentionPlayer is matte (no blur, no glass, no glow) and still falls back to RetentionChart byte-for-byte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine output → chart render | Charts render already-authorized `PredictionResult` fields (LLM-derived scores). No new boundary — paint-only changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Untrusted LLM score rendered raw (NaN/105) into the chart | accept | `ScoreDistribution`'s `clampPct` already clamps positions; this plan does not change clamping logic (paint-only). The gauge clamp lives in `score-gauge.tsx` (untouched here). |
| READ-10 | Info-disclosure | chart surfaces | accept | These charts receive only whitelisted derived props from the panels (wired in 03-04/03-05); this plan changes color/style only, adds no new field reads. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs (RESEARCH § Package Legitimacy Audit not triggered). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `reskin-matte.test.ts` (from 03-01) flips GREEN for ScoreDistribution, RetentionChart, CraftFilmstrip, RetentionPlayer.
- `npx vitest run src/components/board` exits 0 — derive tests + RetentionPlayer test unaffected (geometry/props preserved).
- The grep acceptance criteria prove: old coral gone, glows gone, blur gone, --color-frame fixed, footage grading preserved.
</verification>

<success_criteria>
- The four heavy charts read flat-warm matte: no `#FF7F50`, no `0 0 Npx` glow, no `backdrop-filter`, no Raycast gradient/inset-shine.
- The `--color-frame` latent bug is fixed (ScoreDistribution dot border → `--color-surface`).
- CraftFilmstrip's footage grade/grain/vignette is demonstrably preserved (Pitfall 4 respected).
- All chart geometry, props, and degradation paths are unchanged — paint-only.
</success_criteria>

<output>
Create `.planning/phases/03-rich-visuals-as-drill-downs/03-02-SUMMARY.md` when done.
Note: confirm the matte-lint is now GREEN for these four files; flag if any glow was load-bearing for legibility (so the UAT gate reviewer knows).
</output>
