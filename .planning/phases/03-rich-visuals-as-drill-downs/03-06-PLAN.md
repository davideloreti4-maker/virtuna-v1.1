---
phase: 03-rich-visuals-as-drill-downs
plan: 06
type: execute
wave: 4
depends_on: [03-04, 03-05]
files_modified: []
autonomous: false
requirements: [READ-09]

must_haves:
  truths:
    - "Full suite is green and the production build is clean BEFORE the human reviews (precondition, same as 01-05) [covers D-07]"
    - "A human opens all 5 drill-down panels on a real running Simulation, mobile + desktop, and confirms matte flat-warm + no throw/grey-cell"
    - "The reskinned drill-downs are signed off (LOCKED) or returned with specific gaps"
  artifacts:
    - path: ".planning/phases/03-rich-visuals-as-drill-downs/03-06-SUMMARY.md"
      provides: "The UAT sign-off record (verdict, locked notes or gap list, RetentionPlayer + filmstrip-on-reload findings)"
      min_lines: 20
  key_links:
    - from: "the 5 drill-down panels (live)"
      to: "the THEME-06 flat-warm taste bar"
      via: "human visual review on a real surface — same shape as 01-05"
      pattern: "human-UAT"
---

<objective>
The blocking D-07 human-UAT gate — the phase exit criterion. The reskinned charts are net-new visual surfaces the Phase-1 shell UAT (THEME-06) never covered, and the milestone has burned twice on craft, so Phase 3 ends with a LIVE human review of the reskinned drill-downs on a real running surface (same shape as the 01-05 / THEME-06 gate): open each of the 5 panels against a real Simulation, mobile + desktop, confirm the matte taste bar (no glow/shine/halo), flat-warm token fidelity, and that nothing throws / grey-cells. This is not optional polish — it locks the phase.

Purpose: SC-3 (reskinned flat-warm, consistent with the Reading) is a visual judgment a grep can't fully make. This gate also resolves the two MEDIUM-confidence open questions on a live surface: the RetentionPlayer include/exclude call (signed-URL `ready` rate on a reloaded upload) and the CraftFilmstrip audio-band richness on reload (`variants` dual-read or not).
Output: a recorded sign-off (LOCKED) or a specific gap list routed back to a `--gaps` plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-rich-visuals-as-drill-downs/03-UI-SPEC.md
@.planning/phases/03-rich-visuals-as-drill-downs/03-VALIDATION.md
@.planning/phases/01-foundation-shell/01-05-PLAN.md

<gate_precondition>
This gate has the SAME precondition as the THEME-06 gate (01-05): full suite green + clean production build BEFORE the human reviews. A red suite or broken build means the surface under review is not the real shipped surface — fix first, review second.

CLAUDE.md dev-server gotcha: when CSS changes don't appear, kill the dev server + clear `.next/` + `node_modules/.cache/` + browser cache and restart (Tailwind v4 / Lightning CSS caching). Do this before the human looks, so a stale-CSS false-negative doesn't read as a reskin failure.

UAT test video (from memory): use `~/Downloads/TikTok Video Downloader.mp4` (short) for a live /analyze run; Playwright upload needs the file copied into `./.playwright-mcp/`. Avoid the 120s clip. A completed Simulation `id` is needed to exercise the permalink-reload degradation paths.
</gate_precondition>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the gate precondition — full suite green + clean build + a real Simulation to review</name>
  <files>(none — runs the suite/build; no source files modified)</files>
  <read_first>
    - .planning/phases/03-rich-visuals-as-drill-downs/03-VALIDATION.md (§ Sampling Rate — "Before the D-07 gate: full suite green AND clean npm run build")
    - .planning/phases/01-foundation-shell/01-05-PLAN.md (the precondition shape this mirrors)
    - CLAUDE.md (the dev-server cache-clear gotcha + the commit format)
  </read_first>
  <action>
    Run the full suite (`npm test`) and confirm it is green and ≥ ~2035 (the baseline per STATE.md; the new panel tests added in 03-01/03-04/03-05 raise it). Run `npm run build` and confirm a clean production build (no type errors, no failed compile). If either fails, STOP and fix (or route to a `--gaps` plan) — do not present a broken surface for review. Then bring up the running app (dev or a preview build) and ensure a REAL completed Simulation exists to review (run a live /analyze on the short UAT video `~/Downloads/TikTok Video Downloader.mp4` if no recent Simulation `id` is available; for the permalink-reload degradation check, note its `/analyze/[id]` URL). Clear the dev-server/`.next/` cache first if any CSS looks stale (CLAUDE.md). Capture the suite count + build status + the Simulation URL(s) for the checkpoint.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5 && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with the full suite green (≥ ~2035)
    - `npm run build` exits 0 (clean production build)
    - A real Simulation `id`/URL is available to review (both a fresh stream AND a permalink reload of the same id, to exercise the degradation paths)
  </acceptance_criteria>
  <done>Suite green, build clean, and a real Simulation (live + reloaded) is ready for the human review — the gate precondition is met.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-07 human-UAT gate — review all 5 reskinned drill-downs on a live surface (mobile + desktop)</name>
  <action>
    AUTOMATION IS COMPLETE (plans 03-01..03-05 wired all 5 panels + reskinned every chart; Task 1 of this plan established the green-suite + clean-build precondition + a real Simulation). This task is a BLOCKING human gate — it PAUSES execution for the human reviewer. Do NOT auto-approve (auto_advance does NOT apply to the phase exit gate; same as the 01-05 / THEME-06 gate). Bring up the running app on the real Simulation from Task 1, walk the reviewer through the <how-to-verify> checklist on BOTH mobile (~390px) and desktop widths, and capture the verdict into 03-06-SUMMARY.md — either "approved" (LOCK the reskinned drill-downs + record the RetentionPlayer include/exclude decision and the CraftFilmstrip audio-band-on-reload finding) OR a specific per-panel gap list (which glow/color/throw, which panel) to route into a `/gsd-plan-phase 3 --gaps` closure plan.
  </action>
  <verify>
    <human-check>The reviewer confirms, per panel, on mobile + desktop: matte taste bar (no glass/glow/shine/halo), flat-warm token fidelity (charcoal surfaces, cream text, hairline borders, score zones only where score-derived, the --color-frame dot-border fix), footage grading preserved on the filmstrip, and no throw / no grey-cell on real + degraded data.</human-check>
  </verify>
  <done>The human has reviewed all 5 drill-downs on a live surface and recorded a verdict — LOCKED (approved) or a specific per-panel gap list — plus the RetentionPlayer include/exclude decision and the filmstrip audio-band finding, in 03-06-SUMMARY.md.</done>
  <what-built>
    All 5 rich-visual drill-downs are wired into the Reading and reskinned to flat-warm:
    - score (NEW) — tap the hero gauge → ScoreDistribution (niche histogram + confidence range)
    - personas — tap the cloud → the full PersonaGraph (200 dots, tap-to-reveal cards on mobile)
    - retention — tap the Retention row → the composed watch-journey (RetentionChart + CraftFilmstrip + SegmentTable, aligned timeline, scrollable)
    - shareability — tap the Share row → behavioral rate tiles (StatTileRow) + share_pull evidence
    - hook — tap the Hook row → 0–10 modality rows (reskin-verified)
    Every transplanted chart was re-treated off the Raycast glass/glow to the locked THEME-06 flat-warm tokens (matte), with footage grading preserved on the filmstrip. The closed-union allow-list grew safely; the reading cluster stayed board-store-free.
  </what-built>
  <how-to-verify>
    On the running app, open the real Simulation from Task 1. Review BOTH mobile width (~390px) AND desktop width — the DrillSheet is a bottom sheet on mobile / right drawer on desktop.

    For EACH of the 5 panels (score, personas, retention, shareability, hook):
    1. Open it from its tap target. Confirm it opens in the one DrillSheet, titled correctly (score → "Score", personas → "Audience", etc.), and the Reading stays behind.
    2. Confirm the MATTE taste bar: NO Raycast glass (137deg gradient), NO blur, NO `0 0` glows/halos, NO old coral `#FF7F50` — coral appears ONLY on the single worst/weakest signal + the "you" marker (persona worst cluster, retention drop mark, score you-pin, weakest stat tile, bad table row).
    3. Confirm flat-warm token fidelity: charcoal surfaces, cream text (never pure white), hairline borders, score zones green/amber/red only where score-derived. Confirm the ScoreDistribution "you" dot border is NOT cold near-black (the `--color-frame` fix).
    4. Confirm the filmstrip still reads as FILMED VIDEO (energy grade / grain / vignette kept) — it should not look like flat swatches.
    5. Confirm nothing throws and there is NO grey-cell fallback box.

    Specifically check the two open questions on the live surface:
    - retention: on the PERMALINK RELOAD of an UPLOADED video, does the RetentionPlayer's signed-URL source resolve (`status==='ready'`)? If yes and the scrubber works cleanly, note "player viable"; if not, confirm the static composition reads fine (it's the SC-2-safe default).
    - retention filmstrip: on the reloaded upload, is the CraftFilmstrip's audio band populated, or flat? A flat band means `emotion_arc`/`audio_signals` need a `variants` dual-read (a follow-up, not a blocker).

    Also exercise a DEGRADED path if available (a Simulation with thin/missing data, or the reloaded id): confirm panels show "Not available for this read." — never a throw, never a grey box, never a fabricated 0.
  </how-to-verify>
  <resume-signal>
    Type "approved" to LOCK the reskinned drill-downs (records the sign-off + the RetentionPlayer/filmstrip findings in the summary), OR describe specific gaps (per-panel: which glow/color/throw) to route into a `/gsd-plan-phase 3 --gaps` closure plan. Note the RetentionPlayer include/exclude decision + the filmstrip-audio-band finding either way.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live surface → human reviewer | The reviewer inspects the real running drill-downs. No untrusted machine input; the only "input" is the human verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering/Info-disclosure | the 5 panels (closed union) | accept | Already mitigated + asserted in 03-04 (`tsc --noEmit` + the closed-union test); this gate visually confirms the right panel opens for the right tap. No new code path. |
| READ-10 | Info-disclosure | drill-down render | accept | Already guarded by `reading.no-cut-data.test.tsx` (03-04/03-05); this gate is a visual confirmation that no cut/jargon field surfaced. No new field reads. |
| T-03-03 | Info-disclosure | signed video URL (RetentionPlayer, if reviewed) | accept | If the reviewer exercises the player, the existing infra never logs the URL; no new handling. The include/exclude call is the gate's output, not new code. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs in this phase (RESEARCH § Package Legitimacy Audit not triggered). No legitimacy checkpoint required. |
</threat_model>

<verification>
- Precondition: `npm test` green (≥ ~2035) + `npm run build` clean before the human reviews.
- The human confirms, per panel, on mobile + desktop: matte taste bar (no glass/glow/shine), flat-warm token fidelity, footage grading preserved, no throw / no grey-cell.
- The RetentionPlayer include/exclude decision and the CraftFilmstrip audio-band-on-reload finding are recorded.
</verification>

<success_criteria>
- Full suite green + clean build established as the precondition (same as 01-05).
- A human reviews all 5 drill-downs on a real running Simulation, mobile + desktop, and either LOCKS them (approved) or returns a specific per-panel gap list.
- The two open questions (RetentionPlayer source `ready` rate; filmstrip audio band on reload) are resolved on the live surface and recorded.
- READ-09 is verified complete behind the blocking gate — the phase can close.
</success_criteria>

<output>
Create `.planning/phases/03-rich-visuals-as-drill-downs/03-06-SUMMARY.md` when done.
Record: the verdict (LOCKED or gaps), the per-panel matte/flat-warm confirmation, the RetentionPlayer include/exclude decision, the CraftFilmstrip audio-band finding (and whether a `variants` dual-read follow-up is needed), and the final suite count + build status.
</output>
