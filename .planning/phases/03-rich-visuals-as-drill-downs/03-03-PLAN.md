---
phase: 03-rich-visuals-as-drill-downs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/board/audience/SegmentTable.tsx
  - src/components/board/_kit/DataTable.tsx
  - src/components/board/_kit/StatTile.tsx
  - src/components/board/_kit/KeyframeImage.tsx
  - src/components/board/_kit/PersonaGraph.tsx
autonomous: true
requirements: [READ-09]

must_haves:
  truths:
    - "SegmentTable, DataTable, StatTile, KeyframeImage read flat-warm (cream text, hairline borders, coral reserved to the worst/marked element)"
    - "PersonaGraph reads flat-warm matte — cream dots/links, flat hover card, no glass, the <animate> pulse gated on reducedMotion"
    - "PersonaGraph has a mobile tap-to-reveal path for the hover card (no hover-only affordance on touch)"
  artifacts:
    - path: "src/components/board/_kit/PersonaGraph.tsx"
      provides: "Full 200-dot persona cloud, matte SVG, reduced-motion-gated, mobile-tappable hover card"
      contains: "reducedMotion"
    - path: "src/components/board/audience/SegmentTable.tsx"
      provides: "Who-leaves cohort table, cream tokens, single coral bad row preserved"
      contains: "text-foreground"
    - path: "src/components/board/_kit/StatTile.tsx"
      provides: "Behavioral rate tiles, cream tokens, accent tile preserved"
      contains: "tone"
  key_links:
    - from: "src/components/board/_kit/PersonaGraph.tsx"
      to: "src/hooks/usePrefersReducedMotion.ts"
      via: "panel passes reducedMotion; <animate> pulse is gated"
      pattern: "reducedMotion"
---

<objective>
Tier-1 token-swaps (mechanical: `SegmentTable`, `DataTable`, `StatTile`, `KeyframeImage` — repoint `text-white/NN` + `rgba(255,255,255,…)` to cream tokens, hairline borders to `--color-border`, keep the coral on the single bad/marked element) PLUS the one Tier-2 re-treat in this file group: `PersonaGraph` (RESEARCH correction — it is SVG, NOT HTML-Canvas; reskin via SVG attrs + token swaps, no Canvas draw constants). PersonaGraph carries the real work here: glass hover card → flat, white-alpha dots/links → cream-alpha, gate the `<animate>` pulse on `reducedMotion`, and add a mobile tap-to-reveal for the hover card.

Purpose: SC-3 — the table/tile/keyframe primitives and the persona cloud read flat-warm consistent with the Reading. Coral stays the scalpel (worst persona cluster, bad table row, accent tile, marked keyframe badge) — never broad tinting.
Output: five matte-clean components; `reskin-matte.test.ts` (03-01) flips GREEN for PersonaGraph + KeyframeImage; the persona panel (wired in 03-04) gets a touch-friendly graph.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-rich-visuals-as-drill-downs/03-RESEARCH.md
@.planning/phases/03-rich-visuals-as-drill-downs/03-UI-SPEC.md

<tokens>
<!-- Verified present in src/app/globals.css @theme. -->
--color-accent --color-surface --color-surface-elevated
--color-foreground (#ece7de) --color-foreground-secondary (#c2bdb4) --color-foreground-muted (#8a857c)
--color-border (rgba(255,255,255,0.06) hairline) --color-border-hover
--shadow-float  /* the ONE allowed float shadow — the PersonaGraph hover card may use AT MOST this; never shadow-xl */
/* Cream-alpha where a token doesn't fit: rgba(236,231,222,…) — the value persona-cloud.tsx already uses (match it for PersonaGraph dots/links). */
</tokens>

<reskin_map>
<!-- Exact lines, verified live (RESEARCH § Reskin Surface Area Tier-1 + Tier-2 PersonaGraph row). -->

SegmentTable.tsx (Tier 1):
- L63 bad-row name `bad ? 'text-accent' : 'text-white/90'` → keep text-accent for bad; `text-white/90` → `text-foreground`
- L66 desc `text-white/40` → `text-foreground-muted`
- L79–82 value `bad ? 'text-accent' : 'text-white/90'` → keep accent; `text-white/90` → `text-foreground`

DataTable.tsx (Tier 1, consumed by SegmentTable):
- L29–32 header/cell `text-white/90`,`/80`,`/70`,`/35` → cream tokens (foreground / -secondary / -muted by weight)
- L50 `border-white/[0.06]` → `--color-border` (or `border-[var(--color-border)]`)
- L59 hover `bg-white/[0.02]` → keep (subtle, acceptable)
- L71 → cream token

StatTile.tsx (Tier 1):
- L31–33 default tile `border-white/[0.06] bg-white/[0.016]` → `--color-border` / `bg-surface-elevated` low-alpha; accent tile (`border-accent/25 bg-accent/[0.035]`) → keep (coral correct)
- L37 caps label `text-white/55` → cream-muted
- L46 value `text-white/95` → `text-foreground`
- L48 unit `text-white/40` → cream-muted
- L51–52 evidence `text-white/55`, `em text-white/75` → cream tokens

KeyframeImage.tsx (Tier 2 — coral halo + near-black):
- L77 fallback `radial` coral glow → flat charcoal token (NO coral halo impersonating footage)
- L54 backplate `#0c0b10`/`#0b0a10` → charcoal token (--color-surface / -elevated)
- L83 bottom-edge dark gradient overlay (legibility scrim over a real photo) → MAY STAY
- L87 play `bg-white/15` → cream-alpha
- L93–103 coral timecode/marked badge → keep `--color-accent`
- L85/L90 `text-white/85/90` → cream tokens

PersonaGraph.tsx (Tier 2 — the real work; it is SVG not Canvas):
- L95 dot fill `accent ? 'var(--color-accent)' : rgba(255,255,255,${…})` → keep accent; cream-alpha `rgba(236,231,222,${…})` for the rest (match persona-cloud L109)
- L151 link `stroke="white"` → cream-alpha stroke
- L160 node fill `rgba(255,255,255,${…})` → cream-alpha
- L164–165 `onMouseEnter`/`onMouseLeave` hover → ADD a touch/tap path (tapped node shows card, tap-elsewhere dismisses)
- L188–194 `<animate>` opacity pulse → already `{!reducedMotion && …}`; ensure the panel PASSES `usePrefersReducedMotion()` (wired in 03-04) AND keep the gate; if it reads busy at UAT it can be dropped
- L208 hover card `bg-[#18191a]/95 border-white/10 shadow-xl` → `--color-surface` + hairline `--color-border`, `shadow-xl` → at most `--shadow-float`
- L215–230 card text `text-white/*` → cream tokens
</reskin_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token-swap SegmentTable, DataTable, StatTile, KeyframeImage to flat-warm</name>
  <files>src/components/board/audience/SegmentTable.tsx, src/components/board/_kit/DataTable.tsx, src/components/board/_kit/StatTile.tsx, src/components/board/_kit/KeyframeImage.tsx</files>
  <read_first>
    - src/components/board/audience/SegmentTable.tsx (the file being modified — the bad-row coral logic L63/L79–82, the desc L66; SlotKey/SegmentGroup/CohortDropFrame prop shape to PRESERVE)
    - src/components/board/_kit/DataTable.tsx (the file being modified — header/cell text L29–32/L50/L59/L71; the shared table primitive SegmentTable renders through)
    - src/components/board/_kit/StatTile.tsx (the file being modified — StatTileData interface, the `tone: 'accent'|'default'` branch L30–33, the cited text lines)
    - src/components/board/_kit/KeyframeImage.tsx (the file being modified — the coral radial fallback L77, backplate L54, the marked/timecode badges L93–103)
    - .planning/phases/03-rich-visuals-as-drill-downs/03-RESEARCH.md (§ Reskin Surface Area Tier-1 table + KeyframeImage row)
  </read_first>
  <action>
    Repoint every `text-white/NN` to the cream token nearest its weight: `/90`/`/95` → `text-foreground`, `/70`–`/80` → `text-foreground-secondary`, `/35`–`/55` → `text-foreground-muted` (per the reskin_map line cites). In SegmentTable KEEP `text-accent` on the bad row (L63/L79–82) — coral is correct for the single worst group; only swap the non-bad `text-white/90` → `text-foreground` and the desc `text-white/40` → `text-foreground-muted`. In DataTable swap `border-white/[0.06]` → `var(--color-border)` and the text classes; KEEP the subtle `bg-white/[0.02]` hover (L59). In StatTile KEEP the accent-tile branch (`border-accent/25 bg-accent/[0.035]`, L31) and swap only the default-tile border/bg → `--color-border`/`bg-surface-elevated` low-alpha + the text classes. In KeyframeImage replace the coral radial-glow fallback (L77) with a flat charcoal token (no coral halo), repoint the `#0c0b10`/`#0b0a10` backplate (L54) to `--color-surface`-family, swap `bg-white/15` (L87) and `text-white/85/90` to cream; KEEP the `--color-accent` marked/timecode badges (L93–103) and the bottom legibility scrim (L83). Preserve ALL component props/interfaces (`SegmentTableProps`, `StatTileData`, the DataTable column shape) — token-swap only, no structural change.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/reskin-matte.test.ts src/components/board/audience src/components/board/verdict 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -nE "text-white/(90|95|80|70|55|40|35)" -- src/components/board/audience/SegmentTable.tsx src/components/board/_kit/DataTable.tsx src/components/board/_kit/StatTile.tsx` returns NOTHING
    - `git grep -n "text-accent" -- src/components/board/audience/SegmentTable.tsx` STILL returns the bad-row coral (proof the scalpel was preserved)
    - `reskin-matte.test.ts` no longer flags KeyframeImage
    - `npx vitest run src/components/board` exits 0 (no behavior change)
  </acceptance_criteria>
  <done>Table/tile/keyframe primitives are cream-on-charcoal with hairline borders; coral preserved only on the bad row, accent tile, and marked keyframe badge.</done>
</task>

<task type="auto">
  <name>Task 2: Re-treat PersonaGraph.tsx — matte SVG + reduced-motion gate</name>
  <files>src/components/board/_kit/PersonaGraph.tsx</files>
  <read_first>
    - src/components/board/_kit/PersonaGraph.tsx (the file being modified — read it FULLY: PersonaGraphProps L27–31, the golden-angle layout, the dot fills L95/L160, the links L151, the `<animate>` pulse L188–194, the hover card L208/L215–230, the hover state L164–165)
    - src/components/reading/persona-cloud.tsx (the P2 static cloud — the cream-alpha value `rgba(236,231,222, …)` at L109 to MATCH; the role=button/keyboard tap pattern L76–95 as the affordance reference)
    - src/hooks/usePrefersReducedMotion.ts (the hook the panel will pass; confirm its return shape)
    - .planning/phases/03-rich-visuals-as-drill-downs/03-UI-SPEC.md (§ Mobile interactivity downgrade — the tap-to-reveal requirement)
  </read_first>
  <action>
    Repoint all white-alpha render values to cream-alpha matching persona-cloud: dot fill (L95) `rgba(255,255,255,${…})` → `rgba(236,231,222,${…})` (KEEP the `accent ? var(--color-accent)` branch — the worst cluster stays coral); node fill (L160) and link stroke (L151 `stroke="white"`) → cream-alpha. Hover card (L208): `bg-[#18191a]/95 border-white/10 shadow-xl` → `bg-surface` + hairline `border-[var(--color-border)]`, and `shadow-xl` → at most `--shadow-float` (the ONE allowed float shadow); card text (L215–230) `text-white/*` → cream tokens. Keep the `<animate>` opacity pulse gated behind `{!reducedMotion && …}` (L188 — it already is); the props default `reducedMotion = false` stays, and 03-04 passes `usePrefersReducedMotion()` from the panel. Add a MOBILE tap path: today the card shows on `onMouseEnter`/`onMouseLeave` (L164–165) — add an `onClick`/`onPointerUp` (or `onTouchStart`) on each node that toggles the hovered index, and a dismiss-on-tap-elsewhere (e.g. an outer transparent backdrop or a container click handler that clears the index), so a tapped node reveals its card and a tap elsewhere dismisses it. No hover-only affordance may gate the card on touch. Preserve the SSR-safe golden-angle layout (mulberry32 seed, VB_W/VB_H) and the props interface (`personas`, `height`, `reducedMotion`, `className`).
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/reskin-matte.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -nE "shadow-xl|bg-\[#18191a\]|stroke=\"white\"|rgba\(255,255,255" -- src/components/board/_kit/PersonaGraph.tsx` returns NOTHING
    - `git grep -nE "reducedMotion|rgba\(236, ?231, ?222" -- src/components/board/_kit/PersonaGraph.tsx` returns the gate + the cream-alpha (proof: gated pulse + cream dots)
    - A tap/pointer handler exists on the nodes (not only `onMouseEnter`/`onMouseLeave`): `git grep -nE "onClick|onPointerUp|onTouchStart" -- src/components/board/_kit/PersonaGraph.tsx` returns a node-level handler
    - `reskin-matte.test.ts` no longer flags PersonaGraph
  </acceptance_criteria>
  <done>PersonaGraph is matte (cream dots/links, flat hover card, no glass/shadow-xl), the pulse respects reduced-motion, and the hover card is tap-revealable on touch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine personas → SVG render | PersonaGraph/SegmentTable render already-authorized derived persona/segment data. Paint + interaction changes only; no new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| READ-10 | Info-disclosure | persona/table surfaces | accept | These components receive whitelisted derived props from the panels (wired in 03-04); this plan changes color/interaction only, adds no new field reads or raw-data spread. |
| T-03-02 | Tampering | persona watch-through value rendered into the SVG/card | accept | Values are clamped at derive time (`buildPersonaNodes`/`clamp01`); this plan does not change derivation. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero package installs (RESEARCH § Package Legitimacy Audit not triggered). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `reskin-matte.test.ts` (03-01) flips GREEN for PersonaGraph + KeyframeImage.
- `npx vitest run src/components/board` exits 0 — derive + existing render tests unaffected.
- The grep criteria prove: white-alpha gone, glass card gone, shadow-xl gone, coral scalpel preserved, the reduced-motion gate intact, and a touch path added.
</verification>

<success_criteria>
- SegmentTable / DataTable / StatTile / KeyframeImage read flat-warm with cream text + hairline borders; coral only on the bad row / accent tile / marked badge.
- PersonaGraph is matte SVG (cream dots/links, flat `--color-surface` hover card, no `shadow-xl`/glass), its `<animate>` pulse gated on `reducedMotion`, and its hover card tap-revealable on mobile.
- No structural/prop changes — token + interaction only.
</success_criteria>

<output>
Create `.planning/phases/03-rich-visuals-as-drill-downs/03-03-SUMMARY.md` when done.
Note: confirm the matte-lint is GREEN for PersonaGraph + KeyframeImage, and record HOW the mobile tap-to-reveal was implemented (so 03-04's panel wiring passes the right props and the UAT reviewer knows the touch flow).
</output>
