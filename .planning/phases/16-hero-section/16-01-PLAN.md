---
phase: 16-hero-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/hero/HeroBackground.tsx
  - src/components/hero/index.ts
autonomous: true

must_haves:
  truths:
    - "Hero background displays animated gradient on near-black (#0A0A0B) base"
    - "Gradient colors drift continuously (10-20 second cycle)"
    - "Purple and cyan colors are visible and blend smoothly"
    - "Animation runs autonomously without user interaction"
  artifacts:
    - path: "src/components/hero/HeroBackground.tsx"
      provides: "Animated gradient background component"
      exports: ["HeroBackground"]
    - path: "src/components/hero/index.ts"
      provides: "Barrel export for hero components"
      exports: ["HeroBackground"]
  key_links:
    - from: "HeroBackground.tsx"
      to: "globals.css"
      via: "oklch gradient colors"
      pattern: "oklch\\(0\\.63.*300\\)|oklch\\(0\\.72.*200\\)"
---

<objective>
Create animated gradient background component for the hero section with slow-drifting purple-to-cyan ambient lighting.

Purpose: Establish the dramatic visual foundation that creates the premium first impression per Raycast-inspired design direction.
Output: HeroBackground component with continuous CSS animation, ready for composition in hero section.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-hero-section/16-CONTEXT.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroBackground component with animated gradient</name>
  <files>src/components/hero/HeroBackground.tsx</files>
  <action>
Create a client component that renders an animated gradient background:

1. **Structure:**
   - Full-width, full-height container with `position: absolute` and `inset: 0`
   - Near-black base color: `bg-bg-base` (oklch(0.13 0.02 264) = #0A0A0B)
   - Overflow hidden to contain gradient blobs
   - z-index: -10 (behind all content)

2. **Gradient blobs (2-3 positioned divs):**
   - Purple blob (oklch(0.63 0.24 300)): positioned top-right area
   - Cyan blob (oklch(0.72 0.15 200)): positioned bottom-left area
   - Each blob: large radial gradient (600-800px), heavily blurred (150-200px), low opacity (0.3-0.5)
   - Use absolute positioning with % values for responsive scaling

3. **CSS Animation:**
   - Define @keyframes in component or globals.css for continuous drift
   - Animation: translateX/Y shift (10-20% movement), 15-20 second duration
   - Each blob animates on different timing for organic feel
   - Use `animation: gradient-drift-1 18s ease-in-out infinite` pattern
   - Alternate direction for natural back-and-forth

4. **Implementation notes:**
   - Use inline styles for dynamic values, className for static
   - Blobs should overlap slightly in the center for color blending
   - No user interaction (pure autonomous animation)
   - Consider reduced-motion: `@media (prefers-reduced-motion: reduce)` to disable animation

Props interface:
```typescript
interface HeroBackgroundProps {
  className?: string;
}
```

Export as named export.
  </action>
  <verify>
`npm run build` succeeds with no TypeScript errors.
Component file exists at src/components/hero/HeroBackground.tsx.
  </verify>
  <done>
HeroBackground component renders animated purple-cyan gradient on near-black base.
Animation runs continuously without user interaction.
Reduced motion preference is respected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add gradient animation keyframes to globals.css</name>
  <files>src/app/globals.css</files>
  <action>
Add keyframes for the gradient drift animation after existing keyframes:

```css
/* Hero gradient drift animations - slow organic movement */
@keyframes gradient-drift-1 {
  0%, 100% {
    transform: translate(0, 0) scale(1);
  }
  33% {
    transform: translate(5%, -8%) scale(1.05);
  }
  66% {
    transform: translate(-3%, 5%) scale(0.95);
  }
}

@keyframes gradient-drift-2 {
  0%, 100% {
    transform: translate(0, 0) scale(1);
  }
  33% {
    transform: translate(-6%, 4%) scale(0.95);
  }
  66% {
    transform: translate(4%, -6%) scale(1.05);
  }
}
```

These provide subtle position and scale changes that create an organic, breathing effect.
  </action>
  <verify>
`npm run build` succeeds (CSS is valid).
Keyframes are defined in globals.css.
  </verify>
  <done>
Gradient drift keyframes available for HeroBackground component.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create barrel export for hero components</name>
  <files>src/components/hero/index.ts</files>
  <action>
Create barrel export file:

```typescript
export { HeroBackground } from "./HeroBackground";
export type { HeroBackgroundProps } from "./HeroBackground";
```

This enables clean imports: `import { HeroBackground } from "@/components/hero"`
  </action>
  <verify>
`npm run build` succeeds.
Import from @/components/hero works (TypeScript resolves correctly).
  </verify>
  <done>
Hero components can be imported from @/components/hero barrel.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. HeroBackground.tsx exports HeroBackground component
3. globals.css contains gradient-drift-1 and gradient-drift-2 keyframes
4. index.ts barrel export works
</verification>

<success_criteria>
- HeroBackground renders animated gradient on near-black base
- Purple and cyan blobs drift continuously (15-20 second cycles)
- Animation is smooth and organic (not mechanical)
- Reduced motion preference disables animation
- Component is ready for composition in HeroSection
</success_criteria>

<output>
After completion, create `.planning/phases/16-hero-section/16-01-SUMMARY.md`
</output>
