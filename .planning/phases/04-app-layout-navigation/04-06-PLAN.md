---
phase: 04-app-layout-navigation
plan: 06
type: execute
wave: 5
depends_on: ["04-03", "04-04", "04-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "App layout visually matches societies.io reference"
    - "Sidebar dimensions and styling are pixel-perfect"
    - "Mobile drawer works smoothly"
    - "Network visualization is visually appealing"
    - "All interactive elements respond correctly"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint for the complete app shell.

Purpose: Ensure all Phase 4 components match the societies.io reference and work correctly before proceeding to Phase 5 (Society Management).

Output: Verified app shell ready for feature development.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.reference/app/sidebar.md
@.reference/app/dashboard.md
@.reference/app/_assets/dashboard-main.png
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete app shell with:
- Route groups for marketing and app layouts
- Sidebar with society selector, view selector, and nav items
- Society selector modal with mock societies
- View selector dropdown with 6 options
- Network visualization with animated dots
- Context bar and filter pills
- Mobile drawer navigation
- Auth guard with skeleton loading
  </what-built>
  <how-to-verify>
**Desktop verification (>768px):**
1. Visit http://localhost:3000/dashboard
2. Verify skeleton shows briefly (~350ms) then dashboard loads
3. Check sidebar:
   - Logo at top left (∧ symbol + "Artificial Societies" optional)
   - "Current Society" label with "Zurich Founders" dropdown below
   - "Current View" label with "Country" dropdown below
   - "Create a new test" button with plus icon
   - Bottom nav: Manage plan, Leave Feedback, Product Guide, Log Out
   - Version 2.1 at bottom
4. Test Society Selector:
   - Click society dropdown - modal opens
   - Shows Personal and Target sections with society cards
   - Select a society - modal closes, trigger updates
   - Press ESC - modal closes
5. Test View Selector:
   - Click view dropdown - menu opens with 6 options
   - Select a view - dropdown closes, trigger updates
   - Click outside - dropdown closes
6. Check main content area:
   - "Switzerland" label at top left (context bar)
   - Filter pills at top right (4 role levels)
   - Animated dots filling the space
   - Dots have 4 colors (indigo, pink, emerald, orange)
7. Test filter pills:
   - Click a pill - it toggles active state
8. Test Log Out:
   - Click "Log Out" - navigates to landing page
9. Compare to .reference/app/_assets/dashboard-main.png:
   - Sidebar width matches (~248px)
   - Colors match (dark theme, zinc tones)
   - Typography matches (font sizes, weights)

**Mobile verification (<768px):**
1. Resize browser to mobile width
2. Sidebar should be hidden
3. Hamburger button visible at top-left
4. Tap hamburger - sidebar slides in from left
5. Dark overlay covers content
6. Tap X or overlay - sidebar closes
7. Test all sidebar interactions work in drawer mode

**Landing page regression check:**
1. Visit http://localhost:3000
2. Header should still be visible
3. Landing page content renders normally
4. Visit /coming-soon - still works

**Build verification:**
1. Run `npm run build` - should pass without errors
2. No TypeScript errors
3. No console errors in browser
  </how-to-verify>
  <resume-signal>
Type "approved" if all checks pass.
If issues found, describe them and I will fix before proceeding.
  </resume-signal>
</task>

</tasks>

<verification>
All visual and functional checks in the checkpoint pass.
</verification>

<success_criteria>
- User confirms app layout matches reference
- All interactive elements work correctly
- Mobile experience is smooth
- No regressions on landing pages
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-app-layout-navigation/04-06-SUMMARY.md`
</output>
