---
phase: 01-ingestion-build-hard-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-ingestion-build-hard-gate/01-INGESTION-SPIKE.md
autonomous: false
requirements: [INGEST-01]
user_setup:
  - service: apify
    why: "Live Clockworks tiktok-scraper run to resolve A1-A5 (the milestone's blocking external contract). Billable per run."
    env_vars:
      - name: APIFY_TOKEN
        source: "Apify Console -> Settings -> Integrations -> API token. NOT present in this worktree (no .env* exists) — must be exported in the shell before the spike runs."
    dashboard_config:
      - task: "Confirm the Apify account has credit/quota for ~6-12 single-video Clockworks runs (≥5 URL matrix, plus the two-video C1 differential)."
        location: "Apify Console -> Billing"

must_haves:
  truths:
    - "A live clockworks/tiktok-scraper run with { postURLs:[url], shouldDownloadVideos:true } returns a dataset item whose mp4 download field name is recorded (confirms or corrects A1/A2)"
    - "The resolved mp4 URL's fetchability/TTL across the ~90-332s pipeline window is measured by curl (confirms A3)"
    - "Two structurally-different viral videos fed through analyzeVideoWithOmni produce materially DIFFERENT structural signal — the C1 caption-only-hallucination guard (criterion 2)"
    - "Resolve-hop + Omni wall-clock is measured against maxDuration=300 (confirms A5; decides inline vs async/re-host strategy for Plan 03)"
    - "A failure-mode taxonomy across ≥5 varied live URLs (canonical, vm. short link, region-locked, 404, carousel, private) is recorded with the per-class dataset shape"
  artifacts:
    - path: ".planning/phases/01-ingestion-build-hard-gate/01-INGESTION-SPIKE.md"
      provides: "The live spike findings that gate Plans 02-03: confirmed input key, exact mp4 field name + index, empty-rate, TTL window, resolve+Omni latency vs 300s, sample Omni output, the C1 two-video differential, failure taxonomy"
      contains: "mediaUrls"
  key_links:
    - from: "live Apify Clockworks run"
      to: "analyzeVideoWithOmni"
      via: "resolved mp4 URL string fed to the Omni call"
      pattern: "analyzeVideoWithOmni"
---

<objective>
Run the live ingestion spike that resolves the milestone's blocking external contract (RESEARCH assumptions A1-A5). This is the HARD GATE: no build task in Plan 02/03 runs until this spike confirms the exact Clockworks input key, the mp4 download field name + index, the resolved-URL TTL, the resolve+Omni latency vs the maxDuration=300 ceiling, and that real (not caption-hallucinated) structural signal reaches Omni.

Purpose: A1-A5 cannot be answered from code alone (RESEARCH § Assumptions Log). The spike's findings directly parameterize Plan 02's schema field name and Plan 03's inline-vs-async/re-host strategy. Running build code against an unconfirmed Apify contract would bake in a wrong field name or a TTL assumption that fails live.
Output: `01-INGESTION-SPIKE.md` — the per-URL result table, exact mp4 field name, empty-rate, TTL window, resolve+Omni latency, sample Omni `segments` + `hook_decomposition`, the C1 two-video differential, and the failure taxonomy. This artifact IS success criteria 1, 2, and 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ingestion-build-hard-gate/01-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- The Omni reuse target — accepts ANY fetchable URL string, returns structured segments. -->
<!-- Source: src/lib/engine/qwen/omni-analysis.ts:54 (VERIFIED in worktree 2026-06-01) -->
analyzeVideoWithOmni(videoUrl: string, opts: { niche?: string; content_type?: string; onEvent?: StageEventCallback })
  => Promise<OmniAnalysisOutput>
OmniAnalysisOutput.segments?: SegmentGrid[]
OmniAnalysisOutput.geminiResult: { analysis: GeminiVideoAnalysis; cost_cents } | null   // analysis.hook_decomposition, analysis.video_signals
OmniAnalysisOutput.signalAvailability: { gemini_hook: boolean; gemini_body: boolean; gemini_cta: boolean }

<!-- The live actor invocation template — Source: src/lib/scraping/apify-provider.ts:51-61 (VERIFIED) -->
this.client.actor("clockworks/tiktok-scraper").call(input, { waitSecs })
this.client.dataset(run.defaultDatasetId).listItems() => { items }
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Supply APIFY_TOKEN for the live billable spike</name>
  <read_first>
    - .planning/phases/01-ingestion-build-hard-gate/01-RESEARCH.md § "Live Actor Test — NOT RUN" and § "Environment Availability"
  </read_first>
  <action>
    PAUSE for developer credentials — this is a human-action checkpoint, not autonomous work. Do not attempt the live Apify run until the developer confirms APIFY_TOKEN (and the Qwen/DashScope key) are exported in the spike shell. See <what-built> / <how-to-verify> / <resume-signal> below.
  </action>
  <what-built>
    Nothing automated yet. RESEARCH verified NO `.env*` file exists in this worktree and `APIFY_TOKEN` is read from the runtime environment only (`apify-provider.ts:16`). A live Clockworks run is billable and unsafe to fire blind, so execution pauses here.
  </what-built>
  <how-to-verify>
    1. Obtain the Apify API token: Apify Console -> Settings -> Integrations -> API token.
    2. Export it in the shell that will run the spike: `export APIFY_TOKEN=<token>`.
    3. Confirm credit for ~6-12 single-video runs (Apify Console -> Billing).
    4. Confirm `DASHSCOPE`/Qwen key is also present in the env (needed for the Omni fidelity step) — same env the existing pipeline uses.
  </how-to-verify>
  <verify>
    <automated>test -n "$APIFY_TOKEN"</automated>
  </verify>
  <acceptance_criteria>
    - `echo -n "$APIFY_TOKEN" | wc -c` returns a non-zero length (token present in env).
    - The Qwen/DashScope key the existing engine reads is present in the same shell.
  </acceptance_criteria>
  <done>APIFY_TOKEN and the Qwen key are exported in the spike shell; the live run can proceed.</done>
  <resume-signal>Type "token set" once APIFY_TOKEN (and the Qwen key) are exported in the spike shell, or "skip-live" to document the spike as deferred (NOT recommended — leaves A1-A5 unresolved and the hard gate open).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run the ≥5-URL Clockworks resolve matrix + measure TTL/latency</name>
  <files>.planning/phases/01-ingestion-build-hard-gate/01-INGESTION-SPIKE.md</files>
  <read_first>
    - .planning/phases/01-ingestion-build-hard-gate/01-RESEARCH.md § "Live Actor Test" (the exact node -e command + URL matrix) and § "Assumptions Log" (A1, A2, A3, A5)
    - src/lib/scraping/apify-provider.ts:51-61 (the .actor().call() + dataset().listItems() shape to mirror)
  </read_first>
  <action>
    Run the live Clockworks resolve across the ≥5 (recommend 6) URL matrix from RESEARCH § Live Actor Test. For EACH URL run the documented one-shot node script with the EXACT input object `{ postURLs:[url], resultsPerPage:1, shouldDownloadVideos:true }` and `{ waitSecs:180 }`, against actor `clockworks/tiktok-scraper`:

    URL matrix (use currently-live URLs):
    1. High-view canonical `https://www.tiktok.com/@<creator>/video/<id>`
    2. `vm.` short link `https://vm.tiktok.com/<short>/`
    3. Region-locked / age-gated video
    4. Deleted / 404 video URL
    5. Photo carousel (no video track) post URL
    6. Private-account video URL

    For each URL record: `count` (item count), the FULL item `keys` array (to discover the actual download-field name if it is NOT `mediaUrls`), `mediaUrls` (or the confirmed field), and `resolveMs`. THE EXACT mp4 FIELD NAME + INDEX you confirm here is what Plan 02 will add to `apifyVideoSchema` — do not assume `mediaUrls[0]` if the live keys say otherwise.

    Then for each SUCCESSFULLY-resolved mp4 URL, measure TTL: `curl -sI "<mp4Url>" | head -5` immediately, then again after ~300s (`sleep 300 && curl -sI ...`). Record whether it is still HTTP 200 after the pipeline window. A short TTL means Plan 03 MUST re-host to the Supabase `videos` bucket (criterion 4); a long TTL means Plan 03 passes the Apify URL straight to Omni.

    Record `resolveMs + (later) Omni wall-clock` against the `maxDuration=300` ceiling (`route.ts:149`). This decides whether Plan 03 runs the resolve inline or moves it async.

    Do NOT write any resolver/pipeline code in this plan — this is measurement only. Do NOT persist any downloaded mp4 anywhere (derive-and-drop applies even to the spike).
  </action>
  <verify>
    <automated>MISSING — live billable external run; cannot be unit-automated. Verified by the recorded result table in 01-INGESTION-SPIKE.md (Task 4) covering all ≥5 URLs with count/keys/mediaUrls/resolveMs/TTL columns populated.</automated>
  </verify>
  <acceptance_criteria>
    - The spike artifact's per-URL table has ≥5 rows, each with: URL class, `count`, item `keys`, the confirmed mp4 field name + value, `resolveMs`, and TTL-after-300s (200 / expired).
    - The exact mp4 download-field name and array index are stated explicitly (e.g. "mediaUrls[0]" or the corrected field) — this is the value Plan 02 hardcodes into `apifyVideoSchema`.
    - The resolve-hop latency is stated against 300s.
  </acceptance_criteria>
  <done>≥5 varied live URLs resolved/classified; mp4 field name confirmed; TTL measured across the 300s window; resolveMs recorded against maxDuration=300.</done>
</task>

<task type="auto">
  <name>Task 3: Omni fidelity check + C1 two-video differential</name>
  <files>.planning/phases/01-ingestion-build-hard-gate/01-INGESTION-SPIKE.md</files>
  <read_first>
    - .planning/phases/01-ingestion-build-hard-gate/01-RESEARCH.md § "Live Actor Test" (the Omni fidelity + C1 guard steps) and § Pitfall C1, plus § Assumptions Log A4
    - src/lib/engine/qwen/omni-analysis.ts:28-47 (OmniAnalysisOutput shape: segments, geminiResult.analysis.hook_decomposition, signalAvailability)
  </read_first>
  <action>
    Write a THROWAWAY script (do not commit it; delete it after the run) that imports `analyzeVideoWithOmni` from `src/lib/engine/qwen/omni-analysis.ts` and feeds it each of TWO structurally-different resolved mp4 URLs from Task 2 (e.g. a fast-cut comedy hook vs a slow talking-head tutorial).

    For each, capture and record in the artifact: non-empty `segments` (length + a sample segment), the populated `hook_decomposition` (eyeball whether `first_words_speech_score`/the spoken-hook fields actually reflect the real video — this resolves A4: whether a separate Qwen ASR transcript is needed for Phase 3, gate it but do NOT build ASR in Phase 1), and `signalAvailability` (expect `gemini_hook`/`gemini_body` true, NOT all-false).

    C1 GUARD (criterion 2): assert the two videos' structural fields differ MATERIALLY — different segment counts/boundaries, different hook_decomposition scores, different content_summary. If the two outputs are near-identical, the model is hallucinating from caption text, NOT reading the video — that means the resolved URL did not actually reach Omni and the build is invalid. Record the side-by-side diff in the artifact.

    Qwen-only constraint: use ONLY `analyzeVideoWithOmni` (which calls the Qwen Omni model). Do NOT introduce Gemini/Whisper/Deepgram.
  </action>
  <verify>
    <automated>MISSING — live Omni call against live resolved URLs; not unit-automatable. Verified by the recorded sample Omni output + the C1 side-by-side differential in 01-INGESTION-SPIKE.md (Task 4).</automated>
  </verify>
  <acceptance_criteria>
    - The artifact contains a sample non-empty `segments` array and a populated `hook_decomposition` for each of the two test videos.
    - The artifact contains an explicit "C1 differential" subsection showing the two videos' structural fields differ materially (not near-identical).
    - The artifact states a verdict on A4: whether Omni's hook fidelity is sufficient for Phase 3's Decode, or whether a (deferred, Qwen-only) ASR transcript is needed — gate only, no ASR build here.
    - `signalAvailability` from the live runs is recorded and is NOT all-false.
  </acceptance_criteria>
  <done>Two structurally-different videos produce materially different Omni structural signal; hook-fidelity verdict recorded; signalAvailability non-false.</done>
</task>

<task type="auto">
  <name>Task 4: Write 01-INGESTION-SPIKE.md (the gating artifact)</name>
  <files>.planning/phases/01-ingestion-build-hard-gate/01-INGESTION-SPIKE.md</files>
  <read_first>
    - .planning/phases/01-ingestion-build-hard-gate/01-RESEARCH.md § "Live Actor Test" final "Deliverable" paragraph (the exact artifact contents)
    - .planning/phases/01-ingestion-build-hard-gate/01-VALIDATION.md § "Manual-Only Verifications"
  </read_first>
  <action>
    Write `.planning/phases/01-ingestion-build-hard-gate/01-INGESTION-SPIKE.md` consolidating Tasks 2-3 with these REQUIRED sections:
    1. "Confirmed Apify contract" — exact actor slug, exact input object, the CONFIRMED mp4 field name + index (the value Plan 02 adds to `apifyVideoSchema`), confirming/correcting A1+A2.
    2. "Per-URL result table" — ≥5 rows: URL class, count, keys, mp4 field value, resolveMs, TTL@300s.
    3. "Failure-mode taxonomy" — one row per class (private, region-locked, carousel, 404, vm. short link) mapping the live dataset shape to the typed error class Plan 02 must throw (empty_dataset / no_media_url / etc.).
    4. "TTL verdict" — does the mp4 URL outlive the pipeline window? → Plan 03 strategy: pass-through vs Supabase re-host (resolves A3 + criterion 4).
    5. "Latency verdict" — resolve+Omni wall-clock vs 300s → Plan 03 strategy: inline vs async (resolves A5).
    6. "Omni fidelity sample" — sample segments + hook_decomposition; A4 verdict (ASR needed for Phase 3 or not).
    7. "C1 differential" — the two-video side-by-side proving real signal (criterion 2).
  </action>
  <verify>
    <automated>test -f .planning/phases/01-ingestion-build-hard-gate/01-INGESTION-SPIKE.md && grep -q "C1 differential" .planning/phases/01-ingestion-build-hard-gate/01-INGESTION-SPIKE.md && grep -q "TTL verdict" .planning/phases/01-ingestion-build-hard-gate/01-INGESTION-SPIKE.md && grep -q "Latency verdict" .planning/phases/01-ingestion-build-hard-gate/01-INGESTION-SPIKE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Confirmed Apify contract\|Per-URL result table\|Failure-mode taxonomy\|TTL verdict\|Latency verdict\|Omni fidelity\|C1 differential" 01-INGESTION-SPIKE.md` returns 7 (all required sections present).
    - The artifact names the exact mp4 field + index that Plan 02 will use.
    - The artifact states the Plan 03 strategy decision (pass-through vs re-host; inline vs async).
  </acceptance_criteria>
  <done>01-INGESTION-SPIKE.md exists with all 7 sections; it unambiguously parameterizes Plan 02 (field name) and Plan 03 (TTL/latency strategy). Criteria 1, 2, 4 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pasted TikTok URL → Apify actor | User-supplied third-party URL crosses into a billable external scrape |
| Apify-resolved mp4 URL → Omni/curl fetch | Attacker-influenced URL (from a third-party scrape) is fetched server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation/DoS | live spike Clockworks runs | accept | Spike is a one-time gated manual run behind APIFY_TOKEN supply (Task 1) with a bounded ≥5-URL matrix; cost-exhaustion mitigation for the PRODUCTION path is built in Plan 03 (T-01-05). |
| T-01-02 | Info Disclosure | curl-fetching the resolved mp4 URL in the spike | accept | Spike fetches only HTTPS TikTok/Apify CDN URLs the actor returns; no internal-host fetch. The enforced SSRF allowlist is built in Plan 02 (T-01-04). The spike is operator-run, not attacker-driven. |
| T-01-03 | (Legal/IP) | downloaded mp4 during spike | mitigate | Spike persists NOTHING durable — no mp4 written to any bucket, throwaway Omni script deleted after the run. Derive-and-drop applies to the spike too. |
</threat_model>

<verification>
- `01-INGESTION-SPIKE.md` exists with all 7 required sections (grep gate in Task 4).
- The artifact resolves A1-A5: confirmed input key, confirmed mp4 field name+index, TTL verdict (A3), latency verdict (A5), Omni hook-fidelity verdict (A4), and the C1 differential (criterion 2).
- No durable artifact (no bucket object, no committed throwaway script) survives the spike.
</verification>

<success_criteria>
- Criterion 1 (real Omni signal) + Criterion 2 (C1 differential) + Criterion 4 (failure taxonomy + TTL + latency) are recorded in 01-INGESTION-SPIKE.md.
- The artifact hands Plan 02 the exact `apifyVideoSchema` mp4 field name and hands Plan 03 the TTL/latency strategy. The gate is closed only when this artifact exists.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion-build-hard-gate/01-01-SUMMARY.md` recording the confirmed Apify contract, the TTL/latency verdicts, and the A4 ASR-needed verdict, so Plans 02-03 (and Phase 3) read the resolved unknowns directly.
</output>
