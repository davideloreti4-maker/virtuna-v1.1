---
phase: 01-ingestion-build-hard-gate
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/lib/engine/pipeline.ts
  - src/app/api/analyze/route.ts
  - src/app/api/analyze/__tests__/derive-and-drop.test.ts
autonomous: true
requirements: [INGEST-01]

must_haves:
  truths:
    - "For input_mode='tiktok_url', the pipeline resolves the URL via resolveVideoUrl and feeds the resolved mp4 URL to analyzeVideoWithOmni — producing non-empty video_signals/segments instead of the caption-only text branch's video_signals:null"
    - "Any downloaded/re-hosted source media is deleted in a finally block; a remix/tiktok_url row NEVER carries a video_storage_path and the videos bucket holds ZERO non-owned objects after the request (derive-and-drop / IP boundary)"
    - "The per-request Apify-cost abuse is bounded by the existing 429 rate-limit pattern (cost-exhaustion mitigation)"
    - "The video_upload signed-URL branch, the text branch, AnalysisInputSchema, and buildInsertRow's video_storage_path rule are UNCHANGED (regression crit 5)"
  artifacts:
    - path: "src/lib/engine/pipeline.ts"
      provides: "additive tiktok_url branch (else-if) that sets the Omni input URL from resolveVideoUrl + a finally that drops any temp/re-host"
      contains: "resolveVideoUrl"
    - path: "src/app/api/analyze/route.ts"
      provides: "(if re-host used) unconditional remix-media cleanup separate from cleanupUploadedStorage; assertion that buildInsertRow keeps video_storage_path null for tiktok_url"
      contains: "video_storage_path"
    - path: "src/app/api/analyze/__tests__/derive-and-drop.test.ts"
      provides: "asserts no video_storage_path on a tiktok_url row + finally-delete invoked on the temp/re-host path"
      contains: "video_storage_path"
  key_links:
    - from: "pipeline.ts tiktok_url branch"
      to: "resolveVideoUrl"
      via: "ApifyScrapingProvider.resolveVideoUrl(validated.tiktok_url)"
      pattern: "resolveVideoUrl"
    - from: "pipeline.ts tiktok_url branch"
      to: "analyzeVideoWithOmni"
      via: "the resolved mp4 URL passed as the videoUrl arg"
      pattern: "analyzeVideoWithOmni"
    - from: "tiktok_url remix path"
      to: "video_storage_path"
      via: "buildInsertRow keeps it null (assert, do not alter)"
      pattern: "video_storage_path"
---

<objective>
Wire the resolver into the engine: add an ADDITIVE `tiktok_url` branch around `pipeline.ts:494-520` that calls `resolveVideoUrl` and feeds the resolved mp4 URL into the EXISTING `analyzeVideoWithOmni` (so a non-owned URL produces real segments instead of the caption-only text branch). Enforce derive-and-drop (delete any temp/re-host in `finally`; never set `video_storage_path` on a tiktok_url row). Bound per-request Apify cost via the existing 429 rate-limit pattern. Freeze the `video_upload`/text paths.

Purpose: this is the build that closes the hard gate. Today `signedVideoUrl` is null for `tiktok_url` (`pipeline.ts:496` is `video_upload`-only), so the multimodal Omni call (`pipeline.ts:520`) is skipped and the request falls to the text branch returning `video_signals:null` (`pipeline.ts:594`). This plan reaches Omni for `tiktok_url` while persisting NOTHING durable for non-owned media (IP boundary, pitfall C4).
Output: the tiktok_url Omni branch + derive-and-drop wiring + the derive-and-drop test + a green regression suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ingestion-build-hard-gate/01-RESEARCH.md
@.planning/phases/01-ingestion-build-hard-gate/01-INGESTION-SPIKE.md
@.planning/phases/01-ingestion-build-hard-gate/01-02-SUMMARY.md

<interfaces>
<!-- The resolver from Plan 02 (read its SUMMARY for the exact final shape). -->
ApifyScrapingProvider.resolveVideoUrl(url: string): Promise<ResolvedVideo>   // { mp4Url, durationSeconds }; throws IngestError{ kind, url }

<!-- EXISTING gate block this branch is ADDITIVE to. Source: pipeline.ts:494-520 (VERIFIED) -->
let signedVideoUrl: string | null = null;
if (validated.input_mode === "video_upload" && validated.video_storage_path) {
  /* createSignedUrl(videos, 3600) → signedVideoUrl */          // DO NOT MODIFY
}
/* ...new tiktok_url else-if goes here... */
if (signedVideoUrl) {                                            // reuse this gate
  const omniOut = await analyzeVideoWithOmni(signedVideoUrl, { niche, onEvent });
}

<!-- text branch the tiktok_url path must STOP falling into. Source: pipeline.ts:568-599 → video_signals:null. DO NOT MODIFY. -->

<!-- buildInsertRow video_storage_path rule — keeps it null for non-video_upload. Source: route.ts:450-455 (VERIFIED). ASSERT, do not alter. -->
video_storage_path: validated.input_mode === "video_upload" && validated.video_storage_path && retentionOptedIn ? validated.video_storage_path : null,

<!-- cost-exhaustion reuse target: 429 branch. Source: route.ts:296-310 (VERIFIED). -->
<!-- cleanup-pattern reuse target: cleanupUploadedStorage (route.ts:40-85) — service.storage.from("videos").remove([path]).catch(...). NEW remix cleanup must be SEPARATE + UNCONDITIONAL (do NOT consult retentionOptedIn). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the additive tiktok_url Omni branch + derive-and-drop finally in pipeline.ts</name>
  <read_first>
    - .planning/phases/01-ingestion-build-hard-gate/01-INGESTION-SPIKE.md § "TTL verdict" + § "Latency verdict" (decides pass-through vs Supabase re-host, and inline vs async)
    - .planning/phases/01-ingestion-build-hard-gate/01-02-SUMMARY.md (the resolveVideoUrl signature + IngestError kinds)
    - src/lib/engine/pipeline.ts:494-560 (the signedVideoUrl gate + the Omni call to reuse; do NOT touch the video_upload sub-block 496-508)
    - src/lib/engine/pipeline.ts:568-599 (the text branch — must STAY for text mode and must NOT be the destination for tiktok_url anymore)
    - .planning/phases/01-ingestion-build-hard-gate/01-RESEARCH.md § Pattern 2 (derive-and-drop finally) + § Regression Surface
  </read_first>
  <behavior>
    - When `input_mode === "tiktok_url"`, the Omni input URL is set from `resolveVideoUrl(validated.tiktok_url).mp4Url` (or its re-hosted Supabase signed URL if the spike's TTL verdict says re-host is required), so `if (signedVideoUrl)` (pipeline.ts:520) is TRUE and `analyzeVideoWithOmni` runs.
    - A `tiktok_url` request no longer reaches the text branch (`precomputedGeminiResult !== null` after Omni succeeds, so the geminiPromise short-circuits at pipeline.ts:565).
    - On a resolver IngestError, the branch records the warning and leaves the Omni URL null (graceful: NO crash; the existing warnings[] path is reused), and NO durable media is written.
    - Any re-hosted/temp object is removed in a `finally` regardless of success/failure.
  </behavior>
  <action>
    In `src/lib/engine/pipeline.ts`, add an ADDITIVE `else if (validated.input_mode === "tiktok_url" && validated.tiktok_url)` branch immediately AFTER the existing `video_upload` block (do not rewrite 496-508). In it:
      1. Instantiate the resolver (`new ApifyScrapingProvider()` — reuses `process.env.APIFY_TOKEN`) and call `resolveVideoUrl(validated.tiktok_url)` inside try/catch. On `IngestError`, push a warning (`warnings.push(...)`) and skip Omni (leave the Omni URL null) — do NOT throw the whole pipeline.
      2. STRATEGY per the spike's TTL verdict: if the mp4 URL outlives the pipeline window, set the Omni input URL directly to `resolved.mp4Url` (preferred — persists nothing). If the spike says TTL is too short, re-host to the `videos` bucket via the existing `createSignedUrl` pattern (`pipeline.ts:497-507`) with a 3600s TTL, track the temp `rehostPath`, and set the Omni URL to the signed URL.
      3. Set the Omni input URL into the SAME variable the gate at line 520 reads (rename `signedVideoUrl` to `omniVideoUrl` across this block + line 520/542 reads, OR assign the existing `signedVideoUrl` — choose the minimal-diff option; the variable feeds `if (signedVideoUrl) analyzeVideoWithOmni(...)`).
      4. DERIVE-AND-DROP: wrap the resolve+(optional re-host)+Omni for the tiktok_url branch so that a `finally` deletes any `rehostPath` via `service.storage.from("videos").remove([rehostPath]).catch(...)` (mirror cleanupUploadedStorage shape) UNCONDITIONALLY — do NOT consult `storage_retention_opted_in`. If the preferred pass-through path is taken (no re-host), there is nothing to delete; assert no bucket write happened.
      5. If the spike's LATENCY verdict says resolve+Omni breaches 300s, follow its recommended async/resolve-first ordering; otherwise run inline.
    Do NOT modify the `video_upload` sub-block, the text branch (568-599), or `AnalysisInputSchema`. The tiktok_url branch is purely additive.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-viral-remix && npx vitest run src/lib/engine</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "resolveVideoUrl" src/lib/engine/pipeline.ts` and `grep -q "tiktok_url" src/lib/engine/pipeline.ts` (the branch exists).
    - `grep -n "input_mode === \"video_upload\" && validated.video_storage_path" src/lib/engine/pipeline.ts` still matches at the original block (video_upload branch unchanged — regression crit 5).
    - `grep -q "\.from(\"videos\")\s*\n*\s*\.remove" src/lib/engine/pipeline.ts` OR the re-host path is absent (pass-through) — i.e. either a finally-delete exists, or no `videos` write exists on the tiktok_url path. The derive-and-drop test (Task 2) is the binding check.
    - `npx vitest run src/lib/engine` is green (existing engine tests + video_upload/text paths unchanged).
  </acceptance_criteria>
  <done>tiktok_url resolves → Omni runs → real segments; resolver failures degrade gracefully via warnings[]; any temp/re-host deleted in finally; video_upload + text branches untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive-and-drop assertion + cost-exhaustion rate-limit reuse + the derive-and-drop test</name>
  <read_first>
    - src/app/api/analyze/route.ts:40-85 (cleanupUploadedStorage/cleanupRawUpload — the shape to mirror; the new remix cleanup must be SEPARATE + unconditional)
    - src/app/api/analyze/route.ts:278-283 (retentionOptedIn — governs OWN uploads ONLY; the remix path must NOT consult it)
    - src/app/api/analyze/route.ts:296-310 (the 429 rate-limit branch to reuse for cost-exhaustion)
    - src/app/api/analyze/route.ts:450-455 (buildInsertRow video_storage_path rule — ASSERT it keeps tiktok_url null; do NOT alter)
    - src/app/api/analyze/__tests__/route.test.ts (existing route test mocking pattern to reuse)
  </read_first>
  <behavior>
    - A tiktok_url analysis writes a row with `video_storage_path === null` (buildInsertRow's existing rule already does this for non-video_upload modes — assert it).
    - If a re-host temp path was created, the finally-delete (`service.storage.from("videos").remove([...])`) is invoked exactly once on the tiktok_url path, regardless of pipeline success/failure.
    - A tiktok_url request from a user already at their DAILY_LIMITS hits the existing 429 branch (cost-exhaustion bound) — paste-spam cannot run unbounded billable Apify calls.
  </behavior>
  <action>
    Cost-exhaustion (RESEARCH § Security Domain, V4): confirm the existing rate-limit/429 branch (`route.ts:296-310`, gated by `DAILY_LIMITS`) already covers ALL input modes including `tiktok_url` (it runs before the stream, mode-agnostic). If it does, no change is needed beyond a code comment marking it as the cost-exhaustion guard for remix. If `tiktok_url` somehow bypasses it, route it through the same check. Do NOT add a second rate limiter — reuse the one pattern.

    Derive-and-drop assertion (pitfall C4): verify `buildInsertRow` (`route.ts:450-455`) keeps `video_storage_path` null for `input_mode === "tiktok_url"` (it already does — the rule is `video_upload && retentionOptedIn`-only). Do NOT alter the rule. If Task 1 used a Supabase re-host, ensure the cleanup is a SEPARATE unconditional path (not entangled with `cleanupUploadedStorage`'s retention gate). Add a brief comment at the cleanup site naming it the remix derive-and-drop boundary.

    Write `src/app/api/analyze/__tests__/derive-and-drop.test.ts` asserting:
      1. For an `input_mode:"tiktok_url"` analysis, the row passed to the DB insert has `video_storage_path === null`.
      2. If a re-host path is exercised, the `videos` bucket `.remove([...])` is called in the finally (spy on the storage client), independent of `storage_retention_opted_in` (assert it deletes even when retention is true — proving it does NOT consult the owned-upload flag).
      3. (Pass-through variant) when no re-host occurs, NO `videos` bucket write happens on the tiktok_url path.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-viral-remix && npx vitest run src/app/api/analyze/__tests__/derive-and-drop.test.ts src/app/api/analyze/__tests__/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/app/api/analyze/__tests__/derive-and-drop.test.ts` passes all three assertions (null video_storage_path; unconditional finally-delete on re-host; no write on pass-through).
    - `npx vitest run src/app/api/analyze/__tests__/route.test.ts` stays green (existing route behavior + video_upload/text unchanged — regression crit 5).
    - `grep -n "video_storage_path" src/app/api/analyze/route.ts | grep -q "450\|451\|452\|453\|454\|455"` OR the original `video_upload && ... && retentionOptedIn` rule is still present verbatim (the buildInsertRow rule was NOT altered).
    - The cost-exhaustion 429 branch (`route.ts:296-310`) is unchanged and covers tiktok_url (mode-agnostic pre-stream check).
  </acceptance_criteria>
  <done>tiktok_url row carries null video_storage_path; re-host (if any) is deleted unconditionally in finally; cost-exhaustion bounded by the existing 429; derive-and-drop test green; route regression green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/analyze (tiktok_url) | user submits an arbitrary third-party URL that triggers a billable scrape + a server-side fetch |
| resolved mp4 URL → analyzeVideoWithOmni / ffmpeg | attacker-influenced URL fetched server-side (allowlist enforced in Plan 02; this plan must not bypass it) |
| downloaded/re-hosted mp4 → Supabase videos bucket | non-owned media must never persist past the request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Denial of Service (cost exhaustion) | per-request Apify spend via paste-spam | mitigate | Reuse the existing `DAILY_LIMITS`/429 pre-stream branch (route.ts:296-310) which is mode-agnostic and already gates tiktok_url; no second limiter. Comment marks it as the remix cost guard. |
| T-01-07 | (Legal/IP) + Info Disclosure | persisted non-owned media | mitigate | Prefer passing the Apify URL straight to Omni (persist nothing). If re-host required: short-TTL signed URL + unconditional finally-delete (NOT gated on storage_retention_opted_in); buildInsertRow keeps video_storage_path null; no media URL returned to the client. Asserted in derive-and-drop.test.ts. |
| T-01-08 | Info Disclosure | re-hosted private video via long-TTL signed URL | mitigate | Any re-host uses the existing 3600s signed-URL TTL (mirror pipeline.ts:500) and is deleted in finally; the signed URL never leaves the server. |

> SSRF on the resolved URL (T-01-04) is mitigated in Plan 02's resolveVideoUrl allowlist; this plan consumes the already-validated URL and must not introduce a second unvalidated fetch path.
</threat_model>

<verification>
- `npx vitest run src/lib/engine` and `npx vitest run src/app/api/analyze` are fully green (build + regression).
- `npm test` (full suite) green at the wave merge — confirms video_upload + text + the whole engine are regression-free (crit 5).
- The derive-and-drop test proves: tiktok_url row has null video_storage_path AND any re-host is deleted unconditionally in finally.
- A tiktok_url request reaches `analyzeVideoWithOmni` (real signal) and no longer falls to the `video_signals:null` text branch.
</verification>

<success_criteria>
- A non-owned TikTok URL produces non-empty video_signals/segments via Omni (criterion 1; the live confirmation is the spike artifact from Plan 01).
- Derive-and-drop enforced: zero non-owned objects persist; no video_storage_path on the remix row (criterion 3 / C4).
- Cost-exhaustion bounded by the existing 429 (security requirement).
- video_upload path, text path, AnalysisInputSchema, and buildInsertRow's video_storage_path rule unchanged (criterion 5).
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion-build-hard-gate/01-03-SUMMARY.md` recording the final tiktok_url branch shape (pass-through vs re-host as chosen per the spike), the derive-and-drop cleanup site, and a regression-freeze confirmation for the video_upload + text paths. This closes the INGEST-01 hard gate for Phases 2-5.
</output>
