---
phase: 05-open-chat-test-reframe
plan: 05
type: execute
wave: 3
depends_on: ["05-03", "05-04"]
files_modified:
  - src/lib/tools/refine.ts
  - src/lib/tools/__tests__/refine.test.ts
  - src/app/api/tools/refine/route.ts
  - src/app/api/tools/refine/__tests__/route.test.ts
  - src/components/app/home/composer.tsx
  - src/components/thread/chat-thread-view.tsx
autonomous: true
requirements: [STUDIO-02, STUDIO-03, THREAD-05]
user_setup: []

must_haves:
  truths:
    - "A refine request ('make hook 1 punchier') re-runs the relevant skill scoped to that one card and produces a NEW, freshly SIM-1-scored card inline (never an untested rewrite)"
    - "The refined card shows its OWN fresh band chip (never reuses the prior score)"
    - "Refine intent is detected by reference to an existing card; a skill runs ONLY on an explicit chip send or a tapped CTA — never a silent auto-fire"
    - "Pure chat can surface a tappable suggested chain-step CTA ('Turn this into hooks →') that never auto-fires on render"
  artifacts:
    - path: "src/lib/tools/refine.ts"
      provides: "detectRefineIntent + buildRefineAnchor — NL refine-intent detection + scoped re-run anchor shape"
      exports: ["detectRefineIntent", "buildRefineAnchor"]
    - path: "src/app/api/tools/refine/route.ts"
      provides: "POST scoped re-run — re-runs the skill for one card → new freshly-scored card inline + chat note"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/tools/refine/route.ts"
      to: "runHooksPipeline / runIdeasPipeline (scoped re-run with instruction + original card anchor)"
      via: "fresh SIM-1 score on the refined card"
      pattern: "runHooksPipeline|runIdeasPipeline"
    - from: "src/components/app/home/composer.tsx chat-send"
      to: "detectRefineIntent → /api/tools/refine"
      via: "refine-detect routes to a scoped re-run instead of a plain chat turn"
      pattern: "detectRefineIntent"
---

<objective>
Ship the studio's **core loop** (STUDIO-02 / D-04): a chat refine request ("make hook 1 punchier", "tighten idea 2") re-runs the relevant skill **scoped to that one card** (carrying the user instruction + the original card as anchor) into a **NEW card embedded inline, freshly SIM-1-scored** (refined output is never an untested rewrite), plus a one-line model chat note. Add the **refine-intent NL detection** + the **tap-to-launch suggested chain-step CTA** (STUDIO-03 / D-05) — a skill runs ONLY on an explicit chip send or a tapped CTA, never a silent auto-fire.

Purpose: D-04 is the integrative payoff — "refine to the user's vision" while the moat holds ("every output tested"). D-05 closes intent routing without a full-NL auto-launch router.
Output: `refine.ts` (detect + anchor), `POST /api/tools/refine` (scoped re-run + fresh score + chat note), composer refine-detect routing, the suggested-CTA surface in the chat view.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-open-chat-test-reframe/05-CONTEXT.md
@.planning/phases/05-open-chat-test-reframe/05-UI-SPEC.md
@.planning/phases/05-open-chat-test-reframe/05-03-SUMMARY.md
@.planning/phases/05-open-chat-test-reframe/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: refine.ts — NL refine-intent detection + scoped re-run anchor (D-04/D-05)</name>
  <files>src/lib/tools/refine.ts, src/lib/tools/__tests__/refine.test.ts</files>
  <read_first>
    - src/lib/tools/blocks.ts (HookCardBlock + IdeaCardBlock prop shapes — the original card anchor carries hookLine / title+angle / seedHook)
    - src/lib/tools/chain-handoff.ts (Plan-04 — the skill-id vocabulary to reuse)
    - src/lib/tools/__tests__/block-registry.test.ts (the vitest test style/imports used in this folder)
    - .planning/phases/05-open-chat-test-reframe/05-CONTEXT.md D-04 (refine = scoped re-run carrying instruction + original card as anchor to a new freshly-scored card) + D-05 (refine intent detected by reference to an existing card — "hook 1", "that idea"; NO full-NL auto-launch router) + Claude's Discretion (refine-intent NL heuristics + scoped-refine payload shape are Claude's discretion)
    - .planning/phases/05-open-chat-test-reframe/05-UI-SPEC.md §Copywriting (recognized patterns "make hook 1 punchier", "tighten idea 2"; no confirmation dialog)
  </read_first>
  <behavior>
    - Test 1: detectRefineIntent("make hook 1 punchier") returns isRefine true, skill "hooks", cardRef 1, instruction preserved.
    - Test 2: detectRefineIntent("tighten idea 2") returns isRefine true, skill "idea", cardRef 2.
    - Test 3: detectRefineIntent("what should I post this week?") returns isRefine false (a plain chat question is NOT a refine — no silent skill fire).
    - Test 4: buildRefineAnchor(originalCardProps, instruction) returns a fenced anchor string embedding the original card content + the user instruction.
  </behavior>
  <action>
    Create detectRefineIntent(text: string) returning { isRefine: boolean; skill?: "hooks" | "idea"; cardRef?: number; instruction?: string } — a bounded heuristic recognizing a refine verb (make/tighten/punch/sharpen/rewrite/redo) AND a card noun ("hook"/"idea") AND an ordinal/number reference ("1", "2", "the first"). It MUST NOT classify a generic question as a refine (Test 3 — predictability + cost honesty, D-05). Create buildRefineAnchor(card, instruction) returning a compact fenced anchor (original hook/idea line + the instruction) for the scoped re-run; accept either HookCardBlock["props"] or IdeaCardBlock["props"]. Pure module — no React, no fetch. Keep under 500 lines.
  </action>
  <verify>
    <automated>npx vitest run src/lib/tools/__tests__/refine.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All 4 behavior tests pass.
    - detectRefineIntent returns isRefine false for "what should I post this week?" (no false positive → no silent fire).
    - Pure module: no react import, no fetch( in the file.
    - npx tsc --noEmit clean.
  </acceptance_criteria>
  <done>Refine intent is detected only on an explicit card reference; a plain question never triggers a skill run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/tools/refine — scoped re-run to a new freshly-scored card inline + chat note (D-04)</name>
  <files>src/app/api/tools/refine/route.ts, src/app/api/tools/refine/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/tools/hooks/route.ts (the auth / profile / open-thread / SSE / persist skeleton to clone; runHooksPipeline call shape; cap constants)
    - src/lib/tools/runners/hooks-runner.ts and src/lib/tools/runners/ideas-runner.ts (runHooksPipeline / runIdeasPipeline signatures — the scoped re-run calls one of these with the refine anchor; the runner ALWAYS re-runs Flash SIM, so the new card is freshly scored by construction — moat honesty D-04)
    - src/lib/tools/refine.ts (Task 1 — buildRefineAnchor)
    - src/lib/threads/messages.ts (insertMessage — persist the new card + the chat note; append-only)
    - src/lib/kc/kc-stamp.ts (kcStamp().kcGenVersion)
    - src/lib/kc/compiled.ts (KC_CHAT_SYSTEM_PROMPT for the one-line chat note voice — D-08)
    - src/app/api/tools/chat/__tests__/route.test.ts (the SSE-route test style/mocks for this repo — clone the auth + mocked-runner + mocked-insertMessage approach)
    - .planning/phases/05-open-chat-test-reframe/05-UI-SPEC.md §Copywriting (refined-card note "Re-ran #1 punchier — fresh SIM-1 score below.") + §Interaction Contract (new card MUST show its own fresh band chip; stacking-vs-replace is planner discretion — choose STACK: append a new card, never overwrite; append-only thread)
  </read_first>
  <behavior>
    - Test 1: POST with no auth session returns 401.
    - Test 2: POST { skill:"hooks", instruction, anchor } with a mocked runHooksPipeline returning one scored card persists a NEW hook-card message (one card) to the open thread AND a markdown chat-note message; SSE emits the new card content + score + the note.
    - Test 3: the persisted new card carries band/fraction from the fresh pipeline run (asserted via the mocked runner being invoked), not copied from the request body.
  </behavior>
  <action>
    Create POST /api/tools/refine: auth gate (401, user_id from session only — never body); parse { skill: "hooks" | "idea", instruction, anchor, cardRef? }, cap instruction (2000) + anchor (5000) server-side; load the creator profile + createOpenThreadLazy(user.id); fold the instruction into the ask and pass buildRefineAnchor content as the anchor; call runHooksPipeline (skill hooks) or runIdeasPipeline (skill idea) and take the top survivor as the ONE refined card; persist that ONE new card via insertMessage(openThread.id, "assistant", [refinedCardBlock], kcStamp().kcGenVersion) — STACK (append, never overwrite the original; append-only per D-04); generate + persist a one-line model chat note (one-shot Qwen call with KC_CHAT_SYSTEM_PROMPT, co-pilot voice D-08) as a SECOND markdown message; stream via SSE the same content / score / followup events the client already understands so the new card renders inline with its OWN fresh band chip. The fresh score is guaranteed because the runner always runs Flash SIM — NEVER copy a prior band (moat honesty). Keep route under 500 lines.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/tools/refine/__tests__/route.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 3 behavior tests pass.
    - route.ts calls runHooksPipeline or runIdeasPipeline (grep present) — the refined card comes from a real pipeline run (fresh SIM), never a band copied from the request.
    - route.ts persists the new card via insertMessage(..., "assistant", ...) and a separate markdown chat-note message (append-only; no update / overwrite of any existing message).
    - Auth gate present before any DB read; instruction + anchor length caps enforced server-side.
    - npx tsc --noEmit clean.
  </acceptance_criteria>
  <done>A scoped refine re-runs the skill for one card and appends a NEW freshly-SIM-scored card + a one-line chat note inline — never an untested rewrite.</done>
</task>

<task type="auto">
  <name>Task 3: Wire refine-detect into the chat send + the suggested chain-step CTA (D-04/D-05/STUDIO-03)</name>
  <files>src/components/app/home/composer.tsx, src/components/thread/chat-thread-view.tsx</files>
  <read_first>
    - src/components/app/home/composer.tsx (the activeTool === "chat" send branch added in Plan 03 — refine-detect routes here BEFORE the plain chat turn; the chat + ideas + hooks stream state)
    - src/lib/tools/refine.ts (detectRefineIntent — Task 1)
    - src/lib/tools/chain-handoff.ts (Plan-04 CHAIN_HANDOFFS / handoffsFor — the suggested CTA reads from here)
    - src/components/thread/chat-thread-view.tsx (Plan 03 — where chat turns render; the suggested CTA renders as a tappable affordance below a pure-chat answer)
    - .planning/phases/05-open-chat-test-reframe/05-UI-SPEC.md §Copywriting (suggested chain-step CTA "Turn this into hooks →" — tappable only, never auto-fires) + §Interaction Contract (no silent auto-fire; pure chat MAY suggest a tappable CTA but never auto-launches) + §Color (chain CTAs use coral accent)
  </read_first>
  <action>
    In the composer chat-send branch: before calling chat.start(ask), run detectRefineIntent(ask); if isRefine, look up the referenced card by cardRef among the current thread's card blocks, build the anchor, POST to /api/tools/refine with { skill, instruction, anchor }, and consume its SSE into the appropriate thread view's streaming state — INSTEAD of a plain chat turn; otherwise fall through to the normal chat.start. CRITICAL: refine fires only because the user explicitly sent a refine-phrased message — this is an explicit send, not an auto-fire (D-05). In chat-thread-view.tsx: when a pure-chat answer completes, render a tappable suggested chain-step CTA sourced from handoffsFor (e.g. "Turn this into hooks →") styled with the coral accent per UI-SPEC; tapping it switches the active chip / fires the corresponding skill via the existing send path — it MUST NEVER auto-fire on render (only on tap). Keep both files under 500 lines.
  </action>
  <verify>
    <automated>grep -q "detectRefineIntent" src/components/app/home/composer.tsx && grep -q "/api/tools/refine" src/components/app/home/composer.tsx && grep -Eq "Turn this into|handoffsFor|CHAIN_HANDOFFS" src/components/thread/chat-thread-view.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - composer.tsx chat-send branch calls detectRefineIntent and, on a refine, POSTs to /api/tools/refine (both strings grep-present).
    - The refine path does NOT set pendingNavRef or call stream.start (no navigation; explicit-send only).
    - chat-thread-view.tsx renders a tappable suggested CTA from the chain-handoff registry that fires only on tap (the onClick handler, not an effect, triggers the skill).
    - npx tsc --noEmit clean for both files.
  </acceptance_criteria>
  <done>A refine-phrased chat message routes to a scoped re-run; pure chat surfaces a tappable next-step CTA that never auto-fires.</done>
</task>

</tasks>

<verification>
- npx vitest run src/lib/tools/__tests__/refine.test.ts and npx vitest run src/app/api/tools/refine green.
- npx tsc --noEmit clean for all touched files.
- git diff --name-only shows NO file under src/lib/engine/ and not src/lib/engine/version.ts (text-path only; the refine re-run uses the existing Flash text path, no video scoring change, no ENGINE_VERSION bump).
- Engine suite still green (npx vitest run src/lib/engine if present).
- No silent auto-fire: every skill run originates from an explicit send or a tap handler (no useEffect fires a skill).
</verification>

<success_criteria>
A creator can type "make hook 1 punchier" and get a NEW, freshly-SIM-1-scored hook card inline with its own band chip plus a one-line chat note; pure chat surfaces a tappable next-step CTA; and no skill ever fires without an explicit send or tap.
</success_criteria>

<output>
Create `.planning/phases/05-open-chat-test-reframe/05-05-SUMMARY.md` when done.
</output>
