---
phase: 05-open-chat-test-reframe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tools/runners/chat-runner.ts
  - src/app/api/tools/chat/route.ts
  - src/app/api/tools/chat/__tests__/route.test.ts
autonomous: true
requirements: [THREAD-03]
user_setup: []

must_haves:
  truths:
    - "A creator can send a chat message in the open thread and receive a streamed, profile-grounded markdown answer (not a generic chatbot)"
    - "A thin/no profile degrades gracefully to niche/platform baselines (open chat still works) and the stream signals a coldStart boolean so the UI can show a one-time nudge"
    - "Chat turns persist as markdown blocks in the single open thread (rehydration is wired in Plan 03)"
  artifacts:
    - path: "src/lib/tools/runners/chat-runner.ts"
      provides: "runChatPipeline — assembleBundle(mode:chat) + KC_CHAT_SYSTEM_PROMPT streamed Qwen turn; isColdStart predicate + coldStart in the result"
      exports: ["runChatPipeline", "isColdStart"]
    - path: "src/app/api/tools/chat/route.ts"
      provides: "POST SSE chat route — token stream + meta{coldStart} signal + markdown-block persistence to the open thread"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/tools/chat/route.ts"
      to: "assembleBundle({mode:'chat'})"
      via: "chat-runner grounding assembly"
      pattern: "mode:\\s*[\"']chat[\"']"
    - from: "src/app/api/tools/chat/route.ts"
      to: "insertMessage(openThread.id, 'assistant', [markdown block])"
      via: "post-stream persistence"
      pattern: "insertMessage"
    - from: "src/app/api/tools/chat/route.ts"
      to: "event: meta { coldStart }"
      via: "structured cold-start signal for the Plan 03 one-time nudge (D-08)"
      pattern: "coldStart"
---

<objective>
Build the open-chat BACKEND (THREAD-03): a chat runner + SSE route that grounds each turn on a tight per-turn slice via `assembleBundle({mode:"chat"})` + `KC_CHAT_SYSTEM_PROMPT`, streams a Qwen markdown answer token-by-token, signals a structured `coldStart` boolean over the stream (D-08 — drives the one-time nudge UI in Plan 03), and persists both the user turn and the assistant answer as `markdown` blocks in the user's single open thread (`type:"open"`, `reading_id IS NULL`).

Purpose: Open chat is the THREAD-03 deliverable — profile-grounded general chat with no anchoring Reading, persisting like any thread. This plan delivers the server + persistence half + the cold-start signal; Plan 03 wires the chip + UI + rehydration + the one-time nudge render.
Output: `runChatPipeline` (+ `isColdStart`), `POST /api/tools/chat` (token stream + `meta{coldStart}` event).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-open-chat-test-reframe/05-CONTEXT.md
@.planning/phases/05-open-chat-test-reframe/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat runner — grounded streamed Qwen turn + cold-start signal (D-07/D-08)</name>
  <files>src/lib/tools/runners/chat-runner.ts</files>
  <read_first>
    - src/lib/tools/runners/hooks-runner.ts (runner structure, import-isolation discipline, assembleBundle call shape, Qwen client usage, timeout/abort pattern)
    - src/lib/tools/runners/ideas-runner.ts (the EXISTING cold-start path / thin-profile predicate Ideas already uses — reuse this same definition for isColdStart; do NOT invent a second "thin profile" rule)
    - src/lib/kc/assembler.ts (assembleBundle signature: `assembleBundle(input: AssemblerInput, profileRow: ProfileRow | null)`; MODE_ROLES.chat = niche/audience/platform; `anchor` carries recent turns for chat; the cold-start honest flag is emitted INSIDE the bundle string already — confirm whether it also exposes a thin-profile boolean to reuse)
    - src/app/api/analyze/[id]/chat/route.ts (the analogous streamed-Qwen pattern: `qwen.chat.completions.create({ stream: true })`, `for await (chunk) ... delta`, fullContent accumulation — REUSE this streaming shape, NOT the analysis grounding)
    - src/lib/kc/compiled.ts lines 1063-1069 (KC_CHAT_SYSTEM_PROMPT export — already compiled = BASE + chat stance-slice)
    - src/lib/engine/qwen/client.ts (getQwenClient, QWEN_REASONING_MODEL — Qwen-only constraint)
  </read_first>
  <action>
    Create `runChatPipeline` exporting an async generator (or callback-driven streamer) the route consumes. Input type `ChatPipelineInput { ask: string; platform: AssemblerInput["platform"]; profileRow: ProfileRow | null; priorTurns: Array<{ role: "user" | "assistant"; text: string }> }`. Build the volatile user message via `assembleBundle({ ask, platform, mode: "chat", ...(priorTurns.length ? { anchor: <serialized recent turns> } : {}) }, profileRow)` — the chat stance-slice + cold-start honest flag ride this with zero schema change (D-07/D-08). System message = `KC_CHAT_SYSTEM_PROMPT`. Call `getQwenClient().chat.completions.create({ model: QWEN_REASONING_MODEL, messages: [{role:"system", content: KC_CHAT_SYSTEM_PROMPT}, {role:"user", content: userMessage}], stream: true, temperature: 0.3 })` (match the grounded chat route temperature). Yield/emit each `delta` token; accumulate `fullContent`. COLD-START SIGNAL (D-08): export a tiny pure helper `isColdStart(profileRow: ProfileRow | null): boolean` that REUSES the existing thin-profile / cold-start predicate the Ideas runner+assembler already apply (true when `profileRow === null` or the profile is thin by that same rule — do NOT author a second, divergent definition). Make `runChatPipeline` return `{ fullContent: string; coldStart: boolean }` where `coldStart = isColdStart(profileRow)`. This surfaces the assembler's honest cold-start state as a structured boolean the route puts on the stream so Plan 03 can gate the one-time nudge UI with the same honesty the bundle string already carries. Qwen-only. Do NOT import the engine scoring core (assembler already forbids it). Keep under 500 lines.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "chat-runner" | grep -q "^0$" && grep -q "coldStart" src/lib/tools/runners/chat-runner.ts && grep -q "isColdStart" src/lib/tools/runners/chat-runner.ts && echo TSPASS</automated>
  </verify>
  <acceptance_criteria>
    - `runChatPipeline` and `isColdStart` exported; source contains the literal `mode: "chat"` (or `mode:'chat'`) in the assembleBundle call (grep `mode:\s*["']chat["']` matches).
    - Source references `KC_CHAT_SYSTEM_PROMPT`, `getQwenClient`, `QWEN_REASONING_MODEL`; contains `stream: true`.
    - `runChatPipeline`'s result carries a `coldStart` boolean derived from `isColdStart`, which reuses the existing Ideas/assembler thin-profile predicate (NOT a second divergent definition) — `coldStart` grep-present.
    - Source contains NO import from `@/lib/engine/flash` or `@/lib/engine/scoring` (grep returns 0).
    - `npx tsc --noEmit` reports no error mentioning `chat-runner`.
  </acceptance_criteria>
  <done>runChatPipeline streams a profile-grounded markdown answer via assembleBundle(mode:chat) + KC_CHAT_SYSTEM_PROMPT, returns the full text for persistence AND a coldStart boolean for the nudge UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/tools/chat SSE route + open-thread persistence + coldStart signal (THREAD-03 / D-08)</name>
  <files>src/app/api/tools/chat/route.ts, src/app/api/tools/chat/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/tools/hooks/route.ts (the SSE route skeleton to clone: auth gate, body parse + cap, profile load `creator_profiles` by user_id, `createOpenThreadLazy(user.id)`, ReadableStream + `send(event,data)` helper, `sseHeaders()`, `insertMessage` persistence, `kcStamp().kcGenVersion`)
    - src/app/api/analyze/[id]/chat/route.ts (token streaming + persist-user-turn-before-streaming ordering; MAX_MESSAGE_LENGTH=2000; per-user rate-limit posture)
    - src/lib/threads/messages.ts (insertMessage signature `insertMessage(threadId, role, blocks, kcGenVersion?)`; markdown block shape `{ type: "markdown", props: { text } }`; loadMessages unwrap shape)
    - src/lib/threads/threads.ts (createOpenThreadLazy, getOpenThread)
    - src/lib/tools/runners/chat-runner.ts (the Task-1 runner this route consumes — returns `{ fullContent, coldStart }`)
  </read_first>
  <behavior>
    - Test 1: POST with no auth session → 401 (auth gate before any DB read).
    - Test 2: POST with `ask` exceeding 2000 chars → 400 (server-side cap, independent of client).
    - Test 3: POST with a valid ask (mocked Qwen stream + mocked threads/messages) → SSE emits `event: token` frames then `event: done`, and `insertMessage` is called with role `"assistant"` and a single `{type:"markdown", props:{text}}` block on the open thread.
    - Test 4: POST with a null/thin profile (mocked runner returns `coldStart:true`) → SSE emits `event: meta { coldStart: true }`; POST with a full profile (`coldStart:false`) → `event: meta { coldStart: false }` (the structured cold-start signal is on the stream, D-08).
  </behavior>
  <action>
    Create `POST /api/tools/chat`: auth gate (401 if no session, user_id from session only — never body); parse `{ ask, platform }`, reject `ask` empty (400 "message is required") or > 2000 chars (400); normalise platform to the tiktok/instagram/youtube enum (default tiktok); load `creator_profiles` row by `user.id` (cold-start safe — null is valid); `createOpenThreadLazy(user.id)`; load prior turns via `loadMessages(openThread.id)`, mapping markdown blocks to `{role, text}` (the running-context anchor for D-01/D-07, bounded to recent turns); persist the USER turn first as `insertMessage(openThread.id, "user", [{type:"markdown",props:{text: ask}}], kcStamp().kcGenVersion)` (mirrors the grounded chat route's persist-user-first ordering); open a ReadableStream with the `send(event,data)` + `sseHeaders()` pattern; emit `event: meta { coldStart }` ONCE up front (from `runChatPipeline`'s `coldStart` — the route can read it before/at first token; if the runner only resolves coldStart with its result, compute it route-side via the exported `isColdStart(profileRow)` so the meta frame can lead the stream — D-08); emit `event: token { delta }` per token from `runChatPipeline`, then `await insertMessage(openThread.id, "assistant", [{ type: "markdown", props: { text: fullContent } }], kcStamp().kcGenVersion)`, then `event: done { }`; on throw emit `event: error { message }`. Keep under 500 lines.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/tools/chat/__tests__/route.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/app/api/tools/chat/__tests__/route.test.ts` passes all 4 behavior tests.
    - route.ts contains `auth.getUser` before any `.from(` DB read; contains `createOpenThreadLazy`; contains `insertMessage` with a `markdown` block for BOTH `"user"` and `"assistant"` roles; contains `event: token`, `event: done`, `event: error`.
    - route.ts emits a `meta` frame carrying `coldStart` (grep `coldStart` present) — the structured cold-start signal Plan 03 gates the one-time nudge on.
    - `npx tsc --noEmit` reports no new errors in route.ts.
  </acceptance_criteria>
  <done>POST /api/tools/chat streams a grounded answer, emits a coldStart meta signal, and persists both turns to the open thread; all behavior tests green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/app/api/tools/chat` green.
- `npx tsc --noEmit` clean for all touched files.
- `git diff --name-only` shows NO engine file (this plan does not import or modify `src/lib/engine/*` scoring).
- `src/lib/engine/version.ts` NOT in the diff (no ENGINE_VERSION bump).
</verification>

<success_criteria>
A POST to /api/tools/chat with a creator's ask returns a streamed, profile-grounded markdown answer, signals a structured `coldStart` boolean over the stream, and persists both the user turn and the assistant turn to the single open thread, degrading gracefully when the profile is thin.
</success_criteria>

<output>
Create `.planning/phases/05-open-chat-test-reframe/05-01-SUMMARY.md` when done.
</output>
