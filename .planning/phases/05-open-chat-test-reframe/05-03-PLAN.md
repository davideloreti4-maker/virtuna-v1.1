---
phase: 05-open-chat-test-reframe
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/hooks/queries/use-chat-stream.ts
  - src/components/thread/chat-thread-view.tsx
  - src/components/app/home/tool-chips.tsx
  - src/components/app/home/composer.tsx
autonomous: true
requirements: [THREAD-03, STUDIO-03]
user_setup: []

must_haves:
  truths:
    - "The Chat chip is live (no longer disabled) and selecting it routes composer sends to the chat stream"
    - "A creator sees their chat turns and the streamed assistant answer interleaved in the open-thread column"
    - "Persisted markdown chat turns rehydrate into the chat view on reload"
    - "An empty open-chat surface shows the 'Ask anything about your content.' empty state"
    - "On a thin-profile turn, a one-time cold-start nudge line renders once (gated on the stream coldStart signal — D-08)"
    - "A failed chat turn renders the UI-SPEC error copy ('That answer didn't come through.') instead of failing silently"
  artifacts:
    - path: "src/hooks/queries/use-chat-stream.ts"
      provides: "useChatStream — fetch+getReader SSE consumer for POST /api/tools/chat (token stream → markdown turns); exposes coldStart + error"
      exports: ["useChatStream"]
    - path: "src/components/thread/chat-thread-view.tsx"
      provides: "ChatThreadView — renders interleaved markdown chat turns (persisted + streaming) via MessageBlocks; one-time cold-start nudge; chat-turn error state"
      exports: ["ChatThreadView"]
  key_links:
    - from: "src/components/app/home/tool-chips.tsx"
      to: "Chat chip enabled:true"
      via: "flip the disabled // P5 chip"
      pattern: "id: \"chat\""
    - from: "src/components/app/home/composer.tsx handleSubmit"
      to: "useChatStream.start"
      via: "activeTool === 'chat' branch (no pendingNavRef, no stream.start)"
      pattern: "activeTool === \"chat\""
    - from: "src/components/app/home/composer.tsx loadPersistedBlocks"
      to: "markdown blocks split → persistedChatBlocks"
      via: "rehydration filter extended to type === 'markdown'"
      pattern: "markdown"
    - from: "src/hooks/queries/use-chat-stream.ts"
      to: "event: meta { coldStart }"
      via: "parse the cold-start signal into hook state for the nudge gate (D-08)"
      pattern: "coldStart"
---

<objective>
Build the open-chat FRONTEND (THREAD-03): a `useChatStream` SSE consumer for `POST /api/tools/chat`, a `ChatThreadView` that renders interleaved markdown chat turns (persisted + streaming) via the fixed renderer library, flip the disabled **Chat** chip live (D-05), wire the composer `activeTool === "chat"` send path (no silent auto-fire, never navigates), extend the open-thread rehydration to surface persisted markdown turns, add the empty-state copy, render the **one-time cold-start nudge** gated on the stream `coldStart` signal (D-08), and render the **chat-turn error state** with UI-SPEC copy.

Purpose: Makes open chat usable in product — the creator can hold a profile-grounded conversation in the one open thread that persists/rehydrates, gets an honest one-time nudge when their profile is thin, and sees a clear retry-able message when a turn fails. First consumer of the generic-chip routing STUDIO-03 generalizes.
Output: `useChatStream` (coldStart + error), `ChatThreadView` (empty state + cold-start nudge + error state), live Chat chip, composer chat-send branch + markdown rehydration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-open-chat-test-reframe/05-CONTEXT.md
@.planning/phases/05-open-chat-test-reframe/05-UI-SPEC.md
@.planning/phases/05-open-chat-test-reframe/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: useChatStream — token-stream SSE consumer → markdown turns + coldStart + error</name>
  <files>src/hooks/queries/use-chat-stream.ts</files>
  <read_first>
    - src/hooks/queries/use-hooks-stream.ts (the fetch+getReader SSE loop to clone: AbortController, decoder, `\n\n` frame split, `event:`/`data:` parse, isMountedRef guard — CRITICAL: fetch+getReader NOT EventSource, POST needs a body / BLOCKER-1)
    - src/app/api/analyze/[id]/chat/route.ts (the token-stream event contract: `event: token { delta }`, `event: done { content }`, `event: error { message }`)
    - src/app/api/tools/chat/route.ts (the Plan-01 route — confirm the exact event names it emits: `meta { coldStart }`, token/done/error)
    - src/lib/tools/blocks.ts MarkdownBlockSchema (`{ type:"markdown", props:{ text } }` — the block shape toBlocks produces)
  </read_first>
  <action>
    Create `useChatStream` returning `{ streamingText: string, isStreaming, error, isDone, coldStart: boolean, start(ask, platform), stop(), reset(), toBlocks() }`. `start` POSTs to `/api/tools/chat` with `{ ask, platform }` via fetch + `res.body.getReader()` (NOT EventSource). Parse `event: meta { coldStart }` into a `coldStart` boolean state (default false; set when the meta frame arrives — D-08). Accumulate `event: token { delta }` into `streamingText`; on `event: done` set isDone + stop streaming; on `event: error` set `error` (string message from the frame — the ChatThreadView renders the UI-SPEC error copy off this). `toBlocks()` returns the in-flight assistant turn as a single `{ type:"markdown", props:{ text: streamingText } }` block array (empty array when streamingText empty). `reset()` clears error + coldStart + streamingText. Mirror use-hooks-stream's mount-guard + abort discipline. Keep under 500 lines.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "use-chat-stream" | grep -q "^0$" && grep -q "getReader" src/hooks/queries/use-chat-stream.ts && grep -q "coldStart" src/hooks/queries/use-chat-stream.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `useChatStream` exported; source uses `fetch('/api/tools/chat'` + `.getReader()` and NOT `EventSource`.
    - Source parses `event: token`, `event: done`, `event: error`, and `event: meta`/`coldStart` (all grep-present).
    - Hook return exposes `coldStart` (boolean) and `error` to consumers (grep `coldStart` present).
    - `toBlocks()` returns markdown-typed blocks (source contains `type: 'markdown'` or `type: "markdown"`).
    - `npx tsc --noEmit` reports no error mentioning `use-chat-stream`.
  </acceptance_criteria>
  <done>useChatStream consumes the chat SSE token stream, exposes the streamed assistant turn as a markdown block, plus a coldStart boolean and an error string for the view.</done>
</task>

<task type="auto">
  <name>Task 2: ChatThreadView (empty state + cold-start nudge + error state) + flip Chat chip live + composer chat-send wiring + markdown rehydration (D-05/D-08)</name>
  <files>src/components/thread/chat-thread-view.tsx, src/components/app/home/tool-chips.tsx, src/components/app/home/composer.tsx</files>
  <read_first>
    - src/components/thread/hooks-thread-view.tsx (the thread-view structure to mirror: persisted + streaming sections, `max-w-[760px] mx-auto gap-6`, MessageBlocks render, status line with `aria-live="polite"`, idle → null)
    - src/components/thread/message-blocks.tsx (MessageBlocks renders markdown via MarkdownBlockRenderer — the fixed renderer; chat turns render through it, NOT model-generated UI)
    - src/components/app/home/tool-chips.tsx lines 41-46 (CHIPS array — `{ id: "chat", ..., enabled: false }` is the chip to flip; MODEL_LABEL.chat = "SIM-1 Flash" already)
    - src/components/app/home/composer.tsx (the WHOLE file — handleSubmit activeTool branches at 249-334; loadPersistedBlocks at 182-206 currently filters only idea-card/hook-card; the showIdeasView/showHooksView gate pattern; threadContent JSX; PLACEHOLDER_BY_TOOL.chat; canSubmit ternary; hasThread signal)
    - .planning/phases/05-open-chat-test-reframe/05-UI-SPEC.md §Copywriting (empty-state heading "Ask anything about your content."; body "Numen grounds every answer on your niche and your audience — not a generic chatbot. Try \"what should I post this week?\" or send an idea to test it."; cold-start nudge "Heads up — your profile is light, so I'm leaning on {niche}/{platform} baselines. Fill in your profile for sharper, you-specific reads."; error — chat turn failed: heading "That answer didn't come through." body "Send it again, or rephrase.") + §Interaction Contract (Chat chip live; no silent auto-fire) + §Color (cold-start nudge + error use muted/secondary cream, NOT coral)
  </read_first>
  <action>
    (1) Create `ChatThreadView` mirroring HooksThreadView: props `{ persistedBlocks, streamingBlocks, isStreaming, coldStart, error, niche?, platform? }` (markdown blocks). Render persisted then streaming markdown turns via `MessageBlocks`; show an `aria-live="polite"` status line while streaming; render the empty-state heading + body copy (UI-SPEC verbatim) when there are no persisted turns and not streaming.
    (1a) ONE-TIME COLD-START NUDGE (D-08): when `coldStart === true`, render the nudge line ONCE per session at the top of the thread column using the UI-SPEC verbatim copy: `Heads up — your profile is light, so I'm leaning on {niche}/{platform} baselines. Fill in your profile for sharper, you-specific reads.` — interpolate `niche`/`platform` from props (fall back to the literal words "your niche"/"your platform" if absent). Gate "once" with a local ref/state flag (e.g. `hasShownNudge`) so it does not re-render on every subsequent turn; muted/secondary cream styling, never coral, no glow.
    (1b) CHAT-TURN ERROR STATE (W2): when `error` is truthy, render the UI-SPEC error block — heading `That answer didn't come through.` + body `Send it again, or rephrase.` — as an inline muted/secondary-cream notice below the last turn (not a card, not coral). It clears on the next successful send (the hook `reset()` on `start`).
    Column `max-w-[760px] mx-auto gap-6`, THEME-06 flat-warm, no glow.
    (2) In tool-chips.tsx flip the Chat chip to `enabled: true` (remove the disabled `// P5` state).
    (3) In composer.tsx: declare `const [persistedChatBlocks, setPersistedChatBlocks] = useState<any[]>([])` (mirror persistedIdeaBlocks); in `loadPersistedBlocks` add `const markdownBlocks = allBlocks.filter((b) => b.type === 'markdown')` and `setPersistedChatBlocks(markdownBlocks)`; instantiate `const chat = useChatStream();`; add `const showChatView = activeTool === "chat";` (the chat view owns its empty state); add a `handleSubmit` branch `if (activeTool === "chat") { const ask = trimmedUrl; setUrl(""); await chat.start(ask, platform); return; }` placed AFTER the hooks branch and BEFORE the Test path — CRITICAL: this branch MUST NOT set `pendingNavRef.current` and MUST NOT call `stream.start` (no navigate, no silent auto-fire — D-05); update `canSubmit` so a chat send requires a non-empty ask (`activeTool === "chat" ? !submitting && !chat.isStreaming && trimmedUrl.length > 0 : <existing>`); render `<ChatThreadView .../>` in `threadContent` gated by `showChatView`, passing `persistedBlocks={persistedChatBlocks}`, `streamingBlocks={chat.toBlocks()}`, `isStreaming={chat.isStreaming}`, `coldStart={chat.coldStart}`, `error={chat.error}`, and `niche`/`platform` from the available composer profile/platform state; include `chat.isStreaming`, `persistedChatBlocks.length > 0`, and `showChatView` in the `hasThread` signal so the layout switches to thread mode. Do NOT auto-launch a skill from chat (suggested CTAs are Plan 05). Keep all files under 500 lines.
  </action>
  <verify>
    <automated>grep -q "Ask anything about your content" src/components/thread/chat-thread-view.tsx && grep -q "Heads up — your profile is light" src/components/thread/chat-thread-view.tsx && grep -q "That answer didn't come through" src/components/thread/chat-thread-view.tsx && grep -q 'activeTool === "chat"' src/components/app/home/composer.tsx && grep -q "b.type === 'markdown'" src/components/app/home/composer.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - chat-thread-view.tsx contains the verbatim empty-state heading `Ask anything about your content.` and the body line containing `not a generic chatbot`.
    - chat-thread-view.tsx contains the verbatim cold-start nudge string `Heads up — your profile is light, so I'm leaning on` AND gates its render on the `coldStart` prop with a once-per-session flag (D-08) — grep `coldStart` present in the view.
    - chat-thread-view.tsx contains the verbatim chat-turn error strings `That answer didn't come through.` and `Send it again, or rephrase.`, rendered only when the `error` prop is truthy (W2).
    - tool-chips.tsx Chat chip entry has `enabled: true`.
    - composer.tsx handleSubmit contains an `activeTool === "chat"` branch that calls `chat.start(` and does NOT contain `pendingNavRef` or `stream.start` inside that branch; the branch returns early (no navigation).
    - composer.tsx `loadPersistedBlocks` filters `b.type === 'markdown'` into a `persistedChatBlocks` state; the `<ChatThreadView>` render passes `coldStart` and `error`.
    - `npx tsc --noEmit` clean for all three files.
  </acceptance_criteria>
  <done>The Chat chip is live, selecting it shows the chat view + empty state, sending streams an interleaved markdown turn, persisted turns rehydrate, a one-time cold-start nudge shows on thin profiles, and a failed turn shows the UI-SPEC error copy — with no navigation and no silent auto-fire.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean for all touched files.
- `git diff --name-only` shows NO file under `src/lib/engine/` and not `src/lib/engine/version.ts`.
- Manual sanity (executor): selecting the Chat chip renders the empty state; sending a message streams an answer that persists on reload; a thin-profile account shows the one-time nudge; a forced stream failure shows "That answer didn't come through."
</verification>

<success_criteria>
A creator can select the live Chat chip, see the empty state, send a message, watch the grounded answer stream into the open-thread column, see both turns rehydrate on reload, receive a one-time honest cold-start nudge when their profile is thin, and see a clear retry-able error when a turn fails — no navigation, no auto-fire.
</success_criteria>

<output>
Create `.planning/phases/05-open-chat-test-reframe/05-03-SUMMARY.md` when done.
</output>
