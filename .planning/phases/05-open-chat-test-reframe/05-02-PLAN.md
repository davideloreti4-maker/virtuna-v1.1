---
phase: 05-open-chat-test-reframe
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/reading/reading-hero.tsx
  - src/components/reading/reading.tsx
  - src/components/app/home/tool-chips.tsx
  - src/components/app/home/composer.tsx
autonomous: true
requirements: [TEST-01]
user_setup: []

must_haves:
  truths:
    - "The Reading hero reads 'Test' with a 'powered by SIM-1 Max' tag (presentation only — score gauge + video path unchanged)"
    - "When 'Test full →' lands, the carried hook/idea renders as a visible brief above the upload affordance"
    - "Entry/chip language matches the Test framing (the Test chip + composer brief copy reflect SIM-1 Max)"
  artifacts:
    - path: "src/components/reading/reading-hero.tsx"
      provides: "Hero relabeled Test + powered-by-SIM-1-Max tag"
      contains: "Test"
  key_links:
    - from: "src/components/reading/reading-hero.tsx"
      to: "'powered by SIM-1 Max' tag treatment"
      via: "hero title relabel (presentation only, no engine import)"
      pattern: "SIM-1 Max"
    - from: "src/components/app/home/composer.tsx testBrief banner"
      to: "Test landing brief copy (D-06 sub-line)"
      via: "carried hook rendered above upload"
      pattern: "SIM-1 Max scores the real thing"
---

<objective>
Reframe the existing Reading as **"Test · powered by SIM-1 Max"** (TEST-01 / D-06) — a PRESENTATION/WIRING change only. Relabel the hero title + add the "powered by SIM-1 Max" tag, align the entry/chip language, and render the carried hook/idea as a visible brief above the upload affordance ("Shoot this hook → upload → SIM-1 Max scores the real thing"). The SIM-1 Max video-scoring path, the score gauge, `ENGINE_VERSION`, and internal nomenclature (`reading_id`, route paths, `analysis_results`) are UNCHANGED (D-06a bounded rename).

Purpose: Test is the landing point of every chain CTA and completes the honest Flash-ceiling → Max-realized ladder. This plan touches only `components/reading/*` + chip/composer copy, fully disjoint from the open-chat plans (Wave 1, parallel).
Output: Relabeled hero, aligned chip/entry language, the Test landing brief above upload.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-open-chat-test-reframe/05-CONTEXT.md
@.planning/phases/05-open-chat-test-reframe/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relabel the Reading hero → Test + powered-by-SIM-1-Max tag (D-06)</name>
  <files>src/components/reading/reading-hero.tsx, src/components/reading/reading.tsx</files>
  <read_first>
    - src/components/reading/reading-hero.tsx (the hero: `<ReadingSection label="The read">` at line 127 is the title; ScoreGauge is the primary anchor — DO NOT touch its score math)
    - src/components/reading/reading-section.tsx (ReadingSection label prop — quiet uppercase label; READING_CARD surface tokens)
    - .planning/phases/05-open-chat-test-reframe/05-UI-SPEC.md §Typography (Test hero title 20-24px semibold cream; the "powered by SIM-1 Max" tag may use subtle coral on text/border, hero title stays cream) + §Copywriting (Test hero title = "Test" with tag "powered by SIM-1 Max")
    - src/app/globals.css (THEME-06 tokens — reuse `--color-foreground`, `--color-accent`, `--color-border`; NO new tokens)
    - .claude project Raycast rules in CLAUDE.md (coral flat matte only, no glow; 6% borders)
  </read_first>
  <action>
    In reading-hero.tsx, change the section label from `"The read"` to `"Test"` and add a small inline "powered by SIM-1 Max" tag adjacent to / under the label using existing THEME-06 tokens (tag text/border may use `--color-accent` subtly per UI-SPEC; the hero title/gauge stay cream — coral is NOT applied to the gauge or score). The tag is presentation chrome only — it must NOT read any engine value, must NOT import from `src/lib/engine/*`, and must NOT alter `score`, `watch`, `dropT`, `finishRate`, or `ScoreGauge`. Bounded rename (D-06a): change the user-facing hero label + this tag only. Do NOT touch `reading_id`, route paths, or `analysis_results`. Keep the file under 500 lines. If reading.tsx carries any user-facing "The read"/"Reading" hero copy that must align, update only the hero-adjacent label string (leave the D-13 honesty-state copy and internal comments alone).
  </action>
  <verify>
    <automated>grep -q "Test" src/components/reading/reading-hero.tsx && grep -q "SIM-1 Max" src/components/reading/reading-hero.tsx && git diff --name-only | grep -qv "src/lib/engine" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - reading-hero.tsx contains the literal `SIM-1 Max` and a `Test` label; no longer renders `label="The read"`.
    - `git diff --name-only` includes NO file under `src/lib/engine/` and NOT `src/lib/engine/version.ts` (no ENGINE_VERSION bump).
    - reading-hero.tsx contains NO import from `@/lib/engine` (grep `from "@/lib/engine` returns 0 new lines).
    - ScoreGauge invocation and the `score`/`watch`/`dropT`/`finishRate` props are byte-identical to before (the score math is untouched).
  </acceptance_criteria>
  <done>The Reading hero reads "Test" with a "powered by SIM-1 Max" tag; the video score path, gauge, and ENGINE_VERSION are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Align Test chip/entry language + finalize the Test landing brief (D-06)</name>
  <files>src/components/app/home/tool-chips.tsx, src/components/app/home/composer.tsx</files>
  <read_first>
    - src/components/app/home/tool-chips.tsx (CHIPS array: Test chip label + MODEL_LABEL.test = "SIM-1 Max"; the active-model field already shows it)
    - src/components/app/home/composer.tsx lines 432-464 (the existing `testBrief` banner — already renders the carried hook above upload; refine its copy to the D-06 contract) + lines 60-73 PLACEHOLDER_BY_TOOL.test
    - .planning/phases/05-open-chat-test-reframe/05-UI-SPEC.md §Copywriting (Test landing brief: heading = the carried hook/idea line verbatim; sub = "Shoot this hook → upload → SIM-1 Max scores the real thing.") + §Interaction Contract (Test reframe row)
  </read_first>
  <action>
    Update the Test brief banner sub-line copy in composer.tsx to the exact D-06 contract string "Shoot this hook → upload → SIM-1 Max scores the real thing" (replace the current "Shooting this hook — upload your video and SIM-1 Max will score the real thing" with the UI-SPEC verbatim line), keeping the carried `testBrief.hookLine` as the heading and the existing dismiss control. Use the THEME-06 secondary surface (6% border) already on the banner; coral is reserved per UI-SPEC (the existing inline `color:'#FF7F50'` on the hook line is acceptable as the accent — leave or replace with `var(--color-accent)` for token-consistency). In tool-chips.tsx, confirm the Test chip carries "SIM-1 Max" as its model label (already true via MODEL_LABEL.test) and ensure the Test chip label/title language reads as the Test framing (no "Reading" string). Do NOT touch the upload/URL submit logic, `pendingNavRef`, or `stream.start` (presentation/copy only). Internal nomenclature unchanged (D-06a).
  </action>
  <verify>
    <automated>grep -q "SIM-1 Max scores the real thing" src/components/app/home/composer.tsx && grep -q '"SIM-1 Max"' src/components/app/home/tool-chips.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - composer.tsx contains the exact sub-line `Shoot this hook → upload → SIM-1 Max scores the real thing`.
    - tool-chips.tsx `MODEL_LABEL.test` resolves to `"SIM-1 Max"` (unchanged) and no chip label/title contains the string `Reading`.
    - No change to `pendingNavRef`, `stream.start`, or the upload/URL handleSubmit branches (diff confined to copy + chip strings).
    - `npx tsc --noEmit` clean for both files.
  </acceptance_criteria>
  <done>The Test chip + entry language reflect SIM-1 Max framing and the landing brief renders the carried hook with the D-06 contract sub-line above the upload.</done>
</task>

</tasks>

<verification>
- `git diff --name-only` shows NO file under `src/lib/engine/` (the protected SIM-1 Max video path is untouched — D-06).
- `src/lib/engine/version.ts` is NOT in the diff (no ENGINE_VERSION bump).
- `npx tsc --noEmit` clean for all touched files.
- The engine test suite remains green (run a quick `npx vitest run src/lib/engine` if present — it must still pass, proving no scoring regression).
</verification>

<success_criteria>
The Reading surface presents as "Test · powered by SIM-1 Max" with aligned chip/entry language and a carried-hook brief above the upload, with zero change to the video-scoring path, the score gauge, or ENGINE_VERSION.
</success_criteria>

<output>
Create `.planning/phases/05-open-chat-test-reframe/05-02-SUMMARY.md` when done.
</output>
