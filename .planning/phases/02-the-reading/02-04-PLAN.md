---
phase: 02-the-reading
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/reading/rewrite-item.tsx
  - src/components/reading/fix-first-list.tsx
  - src/components/reading/deeper-read.tsx
  - src/components/reading/__tests__/rewrite-item.test.tsx
  - src/components/reading/__tests__/fix-first.test.tsx
  - src/components/reading/__tests__/deeper-read.test.tsx
autonomous: true
requirements: [READ-07, READ-08]
must_haves:
  truths:
    - "Fix First shows the top timestamped fix(es) from counterfactuals.suggestions (type='fix', top-3); extra fixes collapse behind an inline 'N more fixes →' expand (not a Sheet)"
    - "Each hook rewrite (apollo_reasoning.rewrites) renders struck-through original + variant + a Copy button that writes the variant to clipboard and shows 'Copied' for 1.5s"
    - "Empty Fix First (zero fixes) shows 'Nothing urgent to fix' / 'This one's solid.' — a win, not a blank (D-14)"
    - "No rewrites (rewrites empty/null) → fixes render WITHOUT the rewrite/Copy chip; no placeholder (D-14)"
    - "Deeper read inline-expands the remaining 3 Apollo dims (clarity / substance / credibility); apollo_reasoning null → renders only what's present, omits missing dims silently (D-13)"
  artifacts:
    - path: "src/components/reading/fix-first-list.tsx"
      provides: "Fix First — top-3 type='fix' suggestions + inline 'N more' expand + D-14 empty win; NO useBoardStore"
      contains: "Nothing urgent to fix"
    - path: "src/components/reading/rewrite-item.tsx"
      provides: "Copyable hook rewrite (struck original + variant + Copy→Copied 1.5s), flat-warm palette"
      contains: "navigator.clipboard.writeText"
    - path: "src/components/reading/deeper-read.tsx"
      provides: "Inline-expand (Accordion) of clarity/substance/credibility dims; degrades on null"
      contains: "Deeper read"
  key_links:
    - from: "src/components/reading/rewrite-item.tsx"
      to: "navigator.clipboard"
      via: "writeText(rewrite.variant) on Copy"
      pattern: "navigator\\.clipboard\\.writeText"
    - from: "src/components/reading/deeper-read.tsx"
      to: "src/components/ui/accordion.tsx"
      via: "inline expand (Radix Accordion, NOT a Sheet)"
      pattern: "accordion"
---

<objective>
Build the **Fix First** cluster and the **Deeper read** expand (READ-07/08) — the actionable bottom of the Reading: timestamped fixes + copyable hook rewrites (the product value, D-15), with the light two-tier disclosures (inline "N more fixes →" and inline "Deeper read") that complement the heavy DrillSheet. All three handle the D-13/D-14 empty/degraded states as correctness, not polish.

Purpose: These are where the creator gets what to change — the copyable rewrites are the literal payload. The inline expands (NOT Sheets) are the deliberate light half of the two-tier system (D-10). They drop `useBoardStore` (dead in the thread) and repoint the OLD palette.
Output: `rewrite-item.tsx`, `fix-first-list.tsx`, `deeper-read.tsx`, each with a test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-the-reading/02-CONTEXT.md
@.planning/phases/02-the-reading/02-RESEARCH.md
@.planning/phases/02-the-reading/02-PATTERNS.md
@.planning/phases/02-the-reading/02-UI-SPEC.md
@.planning/phases/02-the-reading/02-VALIDATION.md

<interfaces>
<!-- Verbatim contracts. Use directly. -->

Data contracts (engine types.ts):
```typescript
// CounterfactualSuggestionItem (L573-579): { type: string; headline: string; detail: string; timestamp_ms: number /*MS — 0 when unavailable*/; signal_anchor: string }
// counterfactuals.suggestions[] — top-level field, SEPARATE from apollo_reasoning (fixes can exist when rewrites don't)
// ApolloRewrite (L855-861): { original: string; variant: string; lever_fixed: string }  — source: apollo_reasoning.rewrites (dual-read variants.apollo ?? apollo_reasoning)
// ApolloDimension (L840-848): { name; band: 'strong'|'mid'|'weak'; score: 0-100; lever; evidence } — Deeper read = clarity, substance, credibility
```

RewriteItem to LIFT NEARLY VERBATIM (InsightHeroFrame.tsx L97-149) — repoint palette:
```
copy: navigator.clipboard.writeText(rewrite.variant).then(()=>{setCopied(true); setTimeout(()=>setCopied(false),1500)}).catch(()=>{/*no-op*/})
<del className="text-white/40">{rewrite.original}</del>   →  text-foreground-muted
<p className="text-white">{rewrite.variant}</p>           →  text-foreground
<button>{copied ? 'Copied' : 'Copy'}</button>             →  Copy is a sanctioned coral surface
```

DimensionRow to BORROW (InsightHeroFrame.tsx L155-180) — repoint BAND_COLOR:
```
BAND_COLOR { strong:'text-emerald-400', mid:'text-amber-400', weak:'text-red-400' }  →  text-success / text-warning / text-error
<span>{DIM_NAME_LABEL[dim.name]}</span> <span className="tabular-nums">{dim.score}</span> <span className={bandColor}>{BAND_LABEL[dim.band]}</span>
```

TopFixesList card + filter to REUSE (TopFixesList.tsx L24, L34-62) — DROP useBoardStore:
```
const fixes = suggestions.filter(s => s.type === 'fix').slice(0, 3);   // top-3 (D-08)
// card: rounded-[8px] border border-white/[0.06] bg-white/[0.02] p-2 > headline (text-xs font-medium) + detail (text-xs text-foreground-muted)
// DROP (L2,L21,L41-54): import useBoardStore; setActivePreset('audience'); the <button onClick={()=>setActivePreset('audience')}> timestamp-jump anchor (dead — no Konva preset)
```

Pure helper for fixCount: src/components/board/verdict/verdict-constants.ts (L67): `fixCount(suggestions)` → min(3, fixes). Use for the "N more fixes →" count.
Timestamp note: if a fix timestamp is shown, the value is `timestamp_ms` (MS) → `formatTime(timestamp_ms / 1000)` or the ms variant — NOT the audience-derive seconds variant (CORRECTION #4).

Vendored: src/components/ui/accordion.tsx (Radix — AccordionRoot/Item/Trigger/Content; handles a11y + reduced-motion) — for the inline expands (NOT a Sheet, D-10).

Test fixtures: `import { makeReadingResult, makeApolloNullResult } from './fixtures/reading-fixture'` (02-01). makeReadingResult has ≥3 type='fix' suggestions + 2 rewrites + the 6 dims.

Tokens: `--color-foreground`/`-secondary`/`-muted` (creams, never white) · `--color-success`/`-warning`/`-error` (dim bands) · `--color-accent` (Copy button) · `--color-border` (6%).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RewriteItem — copyable hook rewrite, flat-warm palette (READ-08)</name>
  <files>src/components/reading/rewrite-item.tsx, src/components/reading/__tests__/rewrite-item.test.tsx</files>
  <read_first>
    - .planning/phases/02-the-reading/02-PATTERNS.md (§rewrite-item.tsx assignment — lift L97-149 verbatim, repoint palette; D-14 no-rewrites)
    - .planning/phases/02-the-reading/02-UI-SPEC.md (§Copywriting > Primary CTA Copy→Copied 1.5s; §Color accent-reserved: Copy is a sanctioned coral surface)
    - src/components/board/InsightHeroFrame.tsx (L97-149 the RewriteItem to lift — the clipboard handler + struck-original + variant + Copy button markup with the OLD palette)
    - src/lib/engine/types.ts (L855-861 ApolloRewrite shape)
  </read_first>
  <behavior>
    - Test: renders the original struck-through (`<del>` with rewrite.original) and the variant text.
    - Test: clicking Copy calls navigator.clipboard.writeText with rewrite.variant.
    - Test: after a successful copy the button label becomes "Copied" then reverts to "Copy" after 1.5s (use fake timers).
    - Test: clipboard rejection (mock writeText to reject) → no throw, button stays "Copy" (no-op catch).
    - Test (a11y): the Copy button has an accessible name (aria-label "Copy rewrite"); axe passes.
  </behavior>
  <action>
    Create `'use client'` `src/components/reading/rewrite-item.tsx` exporting `RewriteItem({ rewrite }: { rewrite: ApolloRewrite })`. Lift the InsightHeroFrame `RewriteItem` (L97-149) nearly verbatim: `const [copied, setCopied] = useState(false)`; `handleCopy()` calls `navigator.clipboard.writeText(rewrite.variant).then(() => { setCopied(true); setTimeout(() => setCopied(false), 1500); }).catch(() => {})`. Markup: a `rounded-[8px] border border-[--color-border] p-3` card with `<del>{rewrite.original}</del>` (repoint `text-white/40` → `text-foreground-muted`), `<p>{rewrite.variant}</p>` (repoint `text-white` → `text-foreground`), and a `<button aria-label="Copy rewrite" onClick={handleCopy}>{copied ? 'Copied' : 'Copy'}</button>` styled as a sanctioned coral surface (border + coral text/hover; Copy is one of the 3 allowed coral affordances). Drop the `dropLabel` prop unless needed by the consumer (keep the component minimal: original + variant + Copy). Repoint ALL `text-white[/x]` → cream tokens; NO glow. Write `rewrite-item.test.tsx` covering the 5 behaviors; mock `navigator.clipboard.writeText` (resolve + reject cases) and use `vi.useFakeTimers()` for the 1.5s revert.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/rewrite-item.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `rewrite-item.test.tsx` exits 0.
    - Clicking Copy invokes `navigator.clipboard.writeText` with the exact `rewrite.variant`.
    - Button text toggles "Copy" → "Copied" → "Copy" (after 1500ms fake-timer advance).
    - A rejected clipboard promise does not throw and leaves the label "Copy".
    - `grep -c "text-white" src/components/reading/rewrite-item.tsx` returns 0 (palette repointed to cream tokens).
  </acceptance_criteria>
  <done>RewriteItem renders a copyable hook rewrite on the flat-warm palette, Copy→Copied→Copy with graceful clipboard failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FixFirstList — top-3 fixes + inline "N more" + D-14 empty win (READ-07/08)</name>
  <files>src/components/reading/fix-first-list.tsx, src/components/reading/__tests__/fix-first.test.tsx</files>
  <read_first>
    - .planning/phases/02-the-reading/02-PATTERNS.md (§fix-first-list.tsx assignment — reuse card+filter, DROP useBoardStore + audience-jump anchor, add D-14 empty state)
    - .planning/phases/02-the-reading/02-RESEARCH.md (Landmine 3 — TopFixesList depends on useBoardStore; the dead audience-jump; formatTime ms-vs-seconds)
    - .planning/phases/02-the-reading/02-UI-SPEC.md (§Copywriting — FIX FIRST heading, "N more fixes →" inline, empty "Nothing urgent to fix"/"This one's solid.", no-rewrites = fixes without Copy chip; §Two-tier — light inline expand NOT a Sheet)
    - src/components/board/verdict/TopFixesList.tsx (L2/L21/L41-54 the useBoardStore + audience-jump to DROP; L24/L34-62 the card markup + fix filter to KEEP)
    - src/components/board/verdict/verdict-constants.ts (L67 fixCount)
    - src/lib/engine/types.ts (L573-591 CounterfactualSuggestionItem + CounterfactualResult)
  </read_first>
  <behavior>
    - Test: given ≥4 type='fix' suggestions, renders the top-3 fix headlines; the rest collapse behind an inline "N more fixes →" affordance; clicking it expands the remaining inline (no Sheet/dialog opens).
    - Test: each fix renders its headline + detail; non-'fix' suggestion types are excluded.
    - Test (D-14 empty): zero fixes (counterfactuals undefined / suggestions empty) → renders "Nothing urgent to fix" + "This one's solid." and NO empty list / no error.
    - Test (D-14 no rewrites): when rewrites empty/null, the fixes render WITHOUT any RewriteItem/Copy chip (no placeholder).
    - Test (with rewrites): when rewrites present, RewriteItem(s) render (delegates to the rewrite-item component).
    - Test: NO useBoardStore — component renders standalone without a board store provider (no throw).
  </behavior>
  <action>
    Create `'use client'` `src/components/reading/fix-first-list.tsx` exporting `FixFirstList({ fixes, rewrites }: { fixes: CounterfactualSuggestionItem[] | undefined; rewrites: ApolloRewrite[] | null | undefined })`. Filter `const fixItems = (fixes ?? []).filter(s => s.type === 'fix')`; show `fixItems.slice(0,3)` as fix cards (reuse the TopFixesList card markup: `rounded-[8px] border border-[--color-border] bg-white/[0.02] p-2`, headline `text-xs font-medium`, detail `text-xs text-foreground-muted`). If `fixItems.length > 3`, render an inline `{fixItems.length - 3} more fixes →` toggle (a `useState` expand or the Accordion) that reveals the remainder IN THE THREAD — NOT a Sheet (D-10). DROP `useBoardStore` and the `setActivePreset('audience')` timestamp-jump anchor entirely (dead in the thread). If a fix timestamp is shown, format `timestamp_ms` as MS (`formatTime(timestamp_ms/1000)` or the ms variant) — NOT the seconds variant. Empty state (D-14): when `fixItems.length === 0`, render heading `Nothing urgent to fix` + body `This one's solid.` (a win — replaces the analog's `return null`). Render the hook rewrites via `<RewriteItem rewrite={r} />` (from Task 1) for each `rewrites?.` item under a `Hook rewrites` section label; when `rewrites` is empty/null, render the fixes WITHOUT the rewrite section (no placeholder — D-14). Section heading `FIX FIRST` (uppercase tracking per UI-SPEC §Typography). Write `fix-first.test.tsx` covering the 6 behaviors with `makeReadingResult` (full), a no-fixes variant, and a no-rewrites variant.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/fix-first.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `fix-first.test.tsx` exits 0.
    - With 4 fixes → exactly 3 visible headlines + a "1 more fixes →" inline affordance; clicking it reveals the 4th inline (no `role="dialog"` appears).
    - Zero fixes → the strings "Nothing urgent to fix" AND "This one's solid." are present; no list rendered.
    - No rewrites → no RewriteItem/Copy button rendered; `grep` the rendered output shows no "Copy" affordance.
    - `grep -c "useBoardStore\|setActivePreset" src/components/reading/fix-first-list.tsx` returns 0 (board coupling severed).
    - Component renders without any board-store provider wrapper (test mounts it bare — no throw).
  </acceptance_criteria>
  <done>FixFirstList shows top-3 timestamped fixes + copyable rewrites with an inline "N more" expand and the D-14 positive empty state, fully decoupled from the board store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DeeperRead — inline-expand the remaining 3 Apollo dims (READ-08, D-13)</name>
  <files>src/components/reading/deeper-read.tsx, src/components/reading/__tests__/deeper-read.test.tsx</files>
  <read_first>
    - .planning/phases/02-the-reading/02-PATTERNS.md (§deeper-read.tsx assignment — DimensionRow markup + Accordion inline expand; D-13 null degrade)
    - .planning/phases/02-the-reading/02-UI-SPEC.md (§Copywriting — "Deeper read" inline affordance; §Two-tier — light inline NOT a Sheet; §Color — dim band colors are score-zone tokens)
    - src/components/board/InsightHeroFrame.tsx (L155-180 DimensionRow + L75-79 BAND_COLOR OLD palette to repoint)
    - src/components/ui/accordion.tsx (the vendored Radix Accordion for the inline expand)
    - src/lib/engine/types.ts (L840-848 ApolloDimension)
  </read_first>
  <behavior>
    - Test: collapsed by default — the dim rows are not visible until the "Deeper read" trigger is activated; activating it reveals clarity / substance / credibility rows inline (no Sheet/dialog).
    - Test: each visible dim row shows its name label + score (tabular-nums) + band word colored by the score-zone token (clarity/substance/credibility from dimensions).
    - Test (D-13 degrade): given makeApolloNullResult() (no dimensions) → the component renders nothing OR an empty/absent expand — it does NOT render fabricated dims and does NOT throw; the hero/rows (other components) are unaffected.
    - Test (partial dims): if only some of the 3 are present, render only those (omit missing silently).
    - Test (a11y): the Accordion trigger/content have correct roles; axe passes.
  </behavior>
  <action>
    Create `'use client'` `src/components/reading/deeper-read.tsx` exporting `DeeperRead({ dimensions }: { dimensions: ApolloDimension[] | null | undefined })`. Select the three remaining dims by name: `clarity`, `substance`, `credibility` from `dimensions`. If none are present (D-13: apollo_reasoning null → no dimensions), return `null` (render only what's present — the hero/rows still resolve from overall_score elsewhere). Use the vendored Radix `Accordion` (`ui/accordion.tsx`) for the INLINE expand — `AccordionTrigger` label `Deeper read`, `AccordionContent` holds the dim rows — NOT a Sheet (D-10, light disclosure). Borrow the InsightHeroFrame `DimensionRow` markup (L155-180): `<span>{DIM_NAME_LABEL[dim.name]}</span> <span className="tabular-nums">{dim.score}</span> <span className={bandColor}>{BAND_LABEL[dim.band]}</span>` — repoint `BAND_COLOR` (L75-79: `strong:text-emerald-400`/`mid:text-amber-400`/`weak:text-red-400`) → `text-success`/`text-warning`/`text-error`. Render only the dims that exist (omit missing silently — partial Apollo). NO supporting-signals heavy charts here (those are Phase-3 drill-downs); Phase 2 deeper-read = the remaining 3 dims + any simple native supporting signals if trivially available, inline. Write `deeper-read.test.tsx` covering the 5 behaviors with `makeReadingResult` (full dims) and `makeApolloNullResult` (degrade).
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/deeper-read.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `deeper-read.test.tsx` exits 0.
    - Collapsed by default; after activating the "Deeper read" trigger, the clarity/substance/credibility rows are visible inline (no `role="dialog"`).
    - Each dim row shows the score (tabular-nums) + a band word in a score-zone token class (`text-success`/`text-warning`/`text-error`).
    - With apollo_reasoning null → `container` renders no dim rows and does not throw (degrade — no fabricated dims).
    - `grep -c "text-emerald-400\|text-amber-400\|text-red-400" src/components/reading/deeper-read.tsx` returns 0 (palette repointed).
    - `grep -c "Sheet\|Dialog\|RetentionChart" src/components/reading/deeper-read.tsx` returns 0 (inline expand only — no heavy/Sheet content).
  </acceptance_criteria>
  <done>DeeperRead inline-expands clarity/substance/credibility on the score-zone palette, degrades to nothing when Apollo is absent, never opens a Sheet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clipboard write | `navigator.clipboard.writeText(rewrite.variant)` — writes engine-generated rewrite text the user requested; no injection surface |
| PredictionResult fields → DOM | only whitelisted fix/rewrite/dim fields rendered; no raw spread (READ-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Information disclosure | fix-first-list.tsx / deeper-read.tsx | mitigate | render only whitelisted fields (`suggestions[].headline/detail/timestamp_ms`, `rewrites[].original/variant`, `dimensions[].name/score/band`); never spread raw PredictionResult; no telemetry/critique/feature_vector touched |
| T-02-09 | Tampering | rewrite-item.tsx clipboard | accept | writes only the in-app `rewrite.variant` string on explicit user click; reads nothing from clipboard; rejection is a no-op catch |
| T-02-10 | Denial of service | deeper-read.tsx degrade | mitigate | null/missing dimensions return null (no throw) — D-13 correctness path hits on permalink reload |
| T-02-SC | Tampering | npm installs | accept | zero new dependencies — nothing to vet |
</threat_model>

<verification>
- `npx vitest run src/components/reading` green (adds rewrite-item, fix-first, deeper-read tests).
- Full suite green at wave merge: `npm test`.
- No `useBoardStore` import anywhere under `src/components/reading/` (the thread is board-store-free).
</verification>

<success_criteria>
- RewriteItem: copyable rewrite, flat-warm palette, Copy→Copied→Copy, graceful clipboard failure.
- FixFirstList: top-3 fixes + inline "N more" expand + D-14 empty win + no-rewrites graceful, NO useBoardStore.
- DeeperRead: inline-expand of clarity/substance/credibility on zone tokens, D-13 degrade to nothing, never a Sheet.
</success_criteria>

<output>
Create `.planning/phases/02-the-reading/02-04-SUMMARY.md` when done.
</output>
