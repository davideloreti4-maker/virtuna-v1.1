---
phase: 02-the-reading
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/reading/persona-cloud.tsx
  - src/components/reading/thumbnail-strip.tsx
  - src/components/reading/anti-virality-header.tsx
  - src/components/reading/__tests__/persona-cloud.test.tsx
  - src/components/reading/__tests__/thumbnail-strip.test.tsx
autonomous: true
requirements: [READ-03, READ-04]
must_haves:
  truths:
    - "The hero shows a lightweight static persona dot-cloud — SVG dots sized by persona weight, the single worst-retention cluster in coral, all others neutral cream, no Canvas/hover/animate"
    - "PersonaCloud renders dots ONLY — it does NOT render watch-through % (watch% is hero-owned, rendered by the container in 02-05 so it survives the empty-personas degraded path)"
    - "When heatmap.personas is empty the cloud renders nothing (omitted), not an empty box"
    - "A video thumbnail strip renders a static signed-URL <img> gated on a real video; no keyframe → no strip and no broken placeholder"
    - "The gate banner reuses board/verdict/AntiViralityHeader as-is and returns null when not gated"
  artifacts:
    - path: "src/components/reading/persona-cloud.tsx"
      provides: "Static SVG dot-cloud ONLY (PersonaGraph golden-angle math, stripped); cloud is a tap target for the Phase-3 PersonaGraph drill-down. Watch% is NOT rendered here (hero-owned, 02-05)."
      contains: "buildPersonaNodes"
    - path: "src/components/reading/thumbnail-strip.tsx"
      provides: "Static keyframe <img> poster gated on real video (KeyframeImage pattern), decorative alt"
      contains: "onError"
    - path: "src/components/reading/anti-virality-header.tsx"
      provides: "The 'Don't post yet' gate banner — re-export/thin-wrap of board AntiViralityHeader (D-04)"
      contains: "AntiViralityHeader"
  key_links:
    - from: "src/components/reading/persona-cloud.tsx"
      to: "src/components/board/audience/audience-derive.ts"
      via: "buildPersonaNodes + worstBadGroupKey (layout + worst-cluster; NOT watch% — that's hero-owned)"
      pattern: "buildPersonaNodes|worstBadGroupKey"
    - from: "src/components/reading/anti-virality-header.tsx"
      to: "src/components/board/verdict/AntiViralityHeader.tsx"
      via: "re-export or thin wrapper"
      pattern: "AntiViralityHeader"
---

<objective>
Build the three hero "atoms" that surround the gauge: the static **PersonaCloud** (READ-04 — dots only), the **ThumbnailStrip** (READ-03 — top-of-thread poster), and the **AntiViralityHeader** gate banner (D-04 — the one clean as-is board reuse). All are pure-helper-fed, prop-driven leaves the container (02-05) composes into the hero grid.

Purpose: These complete the hero's "outcome" surface — the audience read (cloud), the video context (thumbnail), and the go/no-go gate. **Watch-through % is a hero-owned field (D-04/READ-04), rendered ONCE by the container (02-05), NOT by this cloud** — so it still appears when PersonaCloud omits itself on the empty-personas degraded path. They depend only on the Wave-1 fixture and the pure `audience-derive`/`AntiViralityHeader` modules.
Output: `persona-cloud.tsx`, `thumbnail-strip.tsx`, `anti-virality-header.tsx`, with persona-cloud + thumbnail-strip tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-the-reading/02-CONTEXT.md
@.planning/phases/02-the-reading/02-RESEARCH.md
@.planning/phases/02-the-reading/02-PATTERNS.md
@.planning/phases/02-the-reading/02-UI-SPEC.md
@.planning/phases/02-the-reading/02-VALIDATION.md

<interfaces>
<!-- Verbatim contracts. Use directly. -->

Pure helpers (REUSE VERBATIM — import, do NOT copy):
From src/components/board/audience/audience-derive.ts:
```typescript
export function buildPersonaNodes(heatmap: HeatmapPayload | null, simResults: PersonaSimulationResult[] | undefined, badKey: SlotKey | null): PersonaNode[]; // returns [] when heatmap.personas empty (L428)
export function worstBadGroupKey(groups: SegmentGroup[]): SlotKey | null; // the badKey arg (L292)
export function buildSegmentGroups(heatmap: HeatmapPayload, simResults: PersonaSimulationResult[] | undefined): SegmentGroup[]; // (L228) — feeds worstBadGroupKey
// NOTE: averageWatchThrough is consumed by the CONTAINER (02-05) for the hero-owned watch%, NOT here.
```
From src/components/board/_kit/PersonaGraph.tsx — the PersonaNode shape (the cloud data unit):
```typescript
export interface PersonaNode {
  id: string; label: string;
  weight: number;        // 0..1 → node radius
  watchThrough: number;  // 0..1 → fill opacity
  segment?: string; dropAt?: string;
  tone?: 'accent' | 'default'; // 'accent' = coral worst cluster
}
```
PersonaGraph golden-angle layout to COPY VERBATIM (L55-72, SSR-safe deterministic, no Math.random):
`VB_W=320, VB_H=200; golden=Math.PI*(3-Math.sqrt(5)); cx=VB_W/2, cy=VB_H/2, n=max(personas.length,1); maxR=min(VB_W,VB_H)*0.42; per node i: t=(i+0.5)/n, r=Math.sqrt(t)*maxR, a=i*golden; x=cx+cos(a)*r, y=cy+sin(a)*r; rad=4+clamp01(weight)*9` (smaller than PersonaGraph's 6+13 per D-02). Fill rule (L157-160): tone==='accent' → `var(--color-accent)`; else cream `rgba(236,231,222, 0.2 + watchThrough*0.5)` (NOT white). sr-only `<ul>` mirror pattern: PersonaGraph L237-245.

Keyframe pattern (thumbnail):
From src/components/board/_kit/KeyframeImage.tsx (L47-69) — plain `<img>` (NOT next/image), signed dynamic URL, `onError` → failed state, decorative `alt=""`. From src/components/board/_kit (index.ts L14-15): `resolveKeyframeUrl(segment)` real-video gate.

Gate banner (re-export target — clean, flat-warm-compatible already):
From src/components/board/verdict/AntiViralityHeader.tsx — `export function AntiViralityHeader({ result, analysisId }: { result: PredictionResult | null; analysisId: string })`. Token gradient `var(--color-accent)`→`var(--color-warning)` (no glow); self-manages localStorage dismissal; returns `null` when `!result.anti_virality_gated`. Reads `counterfactuals?.suggestions` for fixCount + `COPY.AV_HEADER(n)`.

Engine fields (read-only; READ-10 whitelist): `heatmap.personas[]`, `heatmap.segments[].keyframe_uri`, `persona_simulation_results`, `anti_virality_gated`, `anti_virality_reason`. (Watch% sources `weighted_completion_pct`/`averageWatchThrough` are on the CONTAINER's whitelist, 02-05.)

Test fixture: `import { makeReadingResult } from './fixtures/reading-fixture'` (created in 02-01).

Tokens: `--color-accent` (coral #d97757) · `--color-foreground`/`-secondary`/`-muted` (creams, never pure white) · `--color-surface-elevated` #2f2e2b.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PersonaCloud — static dot-cloud, dots only (READ-04, D-02)</name>
  <files>src/components/reading/persona-cloud.tsx, src/components/reading/__tests__/persona-cloud.test.tsx</files>
  <read_first>
    - .planning/phases/02-the-reading/02-PATTERNS.md (§persona-cloud.tsx assignment — the verbatim golden-angle math + worst-cluster coral + a11y mirror + degradation)
    - .planning/phases/02-the-reading/02-RESEARCH.md (Focus 3 — the full static-cloud recipe, the [] degradation)
    - .planning/phases/02-the-reading/02-UI-SPEC.md (§Visual Primitives > Static persona dot-cloud + §Color accent-reserved list: worst-cluster dots are coral, all others cream)
    - src/components/board/_kit/PersonaGraph.tsx (L37-72 golden-angle/mulberry32 layout; L157-160 fill rule; L237-245 sr-only mirror — strip the 200 dots, links, hover card, <animate>)
    - src/components/board/audience/audience-derive.ts (L228, L292, L422-461 — buildSegmentGroups, worstBadGroupKey, buildPersonaNodes)
  </read_first>
  <behavior>
    - Test: given a heatmap with 4 personas (one in the worst slot), renders exactly 4 `<circle>` dots; the worst-cluster dot fill = var(--color-accent), the others use the cream `rgba(236,231,222,...)` form (assert at least one non-accent fill is cream, not white, not coral).
    - Test: dot radius increases with weight (a higher-weight persona has a larger `r`).
    - Test: PersonaCloud does NOT render watch% — the string "watch" does NOT appear in the cloud's own output (watch% is the container's hero-owned field, 02-05).
    - Test: empty personas (makeReadingResult({ heatmap: {...personas:[]} }) or a null-personas variant) → component renders nothing (container is empty / returns null), NOT an empty box.
    - Test (a11y): sr-only `<ul>` mirrors each persona's segment + watch-through; axe passes; the SVG has role="img".
  </behavior>
  <action>
    Create `'use client'` `src/components/reading/persona-cloud.tsx` exporting `PersonaCloud({ heatmap, simResults, onOpen }: { heatmap: HeatmapPayload | null; simResults: PersonaSimulationResult[] | undefined; onOpen?: () => void })`. (The `'use client'` directive is REQUIRED — the cloud is an interactive tap target wired to `onOpen`.) Derive `badKey = worstBadGroupKey(buildSegmentGroups(heatmap, simResults))` then `nodes = buildPersonaNodes(heatmap, simResults, badKey)`. If `nodes.length === 0` return `null` (omit — the hero still shows the gauge + the hero-owned watch% from the container, which renders regardless). Lay out nodes with the COPIED-VERBATIM golden-angle math (VB 320×200, `golden=Math.PI*(3-Math.sqrt(5))`, `rad=4+clamp01(weight)*9`). Render one `<circle>` per node: `tone==='accent'` → `fill="var(--color-accent)"`, else `fill="rgba(236,231,222, ${0.2 + node.watchThrough*0.5})"` (cream, NEVER white/coral). Wrap the `<svg role="img" aria-label="Audience watch-through by persona">` and add an `sr-only <ul>` mirroring `{label}: {Math.round(watchThrough*100)}% watch-through` per node (PersonaGraph L237-245 pattern). **Do NOT render the "{n}% watch" caption here** — watch% is hero-owned and rendered once by the container (02-05); the cloud is DOTS ONLY (the per-node watch-through still drives fill opacity + the sr-only mirror, but no aggregate watch caption). NO Canvas, NO hover card, NO `<animate>` (that's the Phase-3 PersonaGraph). Make the cloud a ≥44px tap target wired to `onOpen?.()` (Phase-3 seam — the container will pass an opener that mounts the full PersonaGraph in the DrillSheet; in Phase 2 `onOpen` may be undefined → render as a static figure). Write `persona-cloud.test.tsx` covering the 5 behaviors with `makeReadingResult` + an empty-personas variant.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/persona-cloud.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `persona-cloud.test.tsx` exits 0.
    - With 4 personas → exactly 4 `<circle>` elements; the accent dot fill is `var(--color-accent)`; a non-accent dot fill matches `/rgba\(236,\s*231,\s*222/` (cream, not white).
    - The cloud's own rendered output contains no "{n}% watch" caption (watch% is container-owned) — the only "watch-through" text is inside the sr-only `<ul>` mirror.
    - Empty personas → `container.querySelector('svg')` is null (cloud omitted, no empty box).
    - `grep -c "Canvas\|useRef.*canvas\|<animate\|onMouseEnter\|hover" src/components/reading/persona-cloud.tsx` returns 0 (static only).
    - `grep -c "Math.random" src/components/reading/persona-cloud.tsx` returns 0 (deterministic, SSR-safe).
  </acceptance_criteria>
  <done>PersonaCloud renders a static, deterministic, coral-worst-cluster dot-cloud (dots only, no watch caption), omits cleanly on empty data, and exposes an `onOpen` seam for the Phase-3 drill-down.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ThumbnailStrip — static keyframe poster gated on real video (READ-03, D-03)</name>
  <files>src/components/reading/thumbnail-strip.tsx, src/components/reading/__tests__/thumbnail-strip.test.tsx</files>
  <read_first>
    - .planning/phases/02-the-reading/02-PATTERNS.md (§thumbnail-strip.tsx assignment — the signed-`<img>` + failure-fallback idiom + real-video gate)
    - .planning/phases/02-the-reading/02-UI-SPEC.md (§Visual Primitives > Video thumbnail strip — static poster, gated, decorative alt="", inline-playable deferred)
    - src/components/board/_kit/KeyframeImage.tsx (L47-69 — plain `<img>`, `onError`→failed, signed dynamic URL, decorative alt)
    - src/components/board/_kit/index.ts (L14-15 — `resolveKeyframeUrl` export, the real-video gate)
    - .planning/phases/02-the-reading/02-RESEARCH.md (§Security Domain — signed keyframe URL: don't log it; alt="")
  </read_first>
  <behavior>
    - Test: given a heatmap with segments[0].keyframe_uri = a signed URL → renders an `<img>` with that src and `alt=""`.
    - Test: no keyframe (segments empty or keyframe_uri null) → renders nothing (no `<img>`, no placeholder box).
    - Test: on `<img>` onError → the img is removed/hidden (failed state), no broken-image box left behind.
    - Test (a11y): decorative `alt=""` (no a11y violation for missing alt); axe passes.
  </behavior>
  <action>
    Create `'use client'` `src/components/reading/thumbnail-strip.tsx` exporting `ThumbnailStrip({ heatmap }: { heatmap: HeatmapPayload | null })`. (The `'use client'` directive is REQUIRED — it uses `useState` + the `onError` handler.) Resolve the poster src from the first real keyframe — `resolveKeyframeUrl(heatmap?.segments?.[0])` (or the first segment whose `keyframe_uri` is non-null). Gate on a real video: if no resolvable src, return `null` (no strip, no broken placeholder — mirror `resolveKeyframeUrl`'s real-video gate; text/tiktok-url-only modes have no keyframe). Use a `const [failed, setFailed] = useState(false)`; render a plain `<img src={src} alt="" onError={() => setFailed(true)} .../>` (NOT `next/image` — signed dynamic URLs) only while `!!src && !failed`. Static poster only (inline-playable deferred). Do NOT log the signed URL (Security: info disclosure). Style: full-width within the 760px column, rounded, hairline border, object-cover; matte (no glow). Write `thumbnail-strip.test.tsx` covering the 4 behaviors with `makeReadingResult` (real keyframe) and a no-keyframe variant; simulate `fireEvent.error(img)` for the failed-state case.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/thumbnail-strip.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `thumbnail-strip.test.tsx` exits 0.
    - Real keyframe → exactly one `<img>` with `alt=""` and the signed src.
    - No keyframe → `container.querySelector('img')` is null AND no placeholder element rendered.
    - After firing `error` on the img, the img is no longer in the document (failed state, no broken box).
    - `grep -c "next/image\|console.log\|console.info" src/components/reading/thumbnail-strip.tsx` returns 0 (plain img; signed URL never logged).
  </acceptance_criteria>
  <done>ThumbnailStrip renders a static signed-URL poster only for real videos, fails gracefully to nothing, never logs the signed URL.</done>
</task>

<task type="auto">
  <name>Task 3: AntiViralityHeader — gate banner re-export (READ-03/D-04)</name>
  <files>src/components/reading/anti-virality-header.tsx</files>
  <read_first>
    - .planning/phases/02-the-reading/02-PATTERNS.md (§anti-virality-header.tsx assignment — THE clean as-is reuse; place above the gauge, banner does NOT hide the score)
    - .planning/phases/02-the-reading/02-CONTEXT.md (D-04 — gate banner "Don't post yet", score stays visible but subordinated)
    - src/components/board/verdict/AntiViralityHeader.tsx (the component — token gradient, localStorage dismissal, returns null when not gated, takes `{ result, analysisId }`)
    - src/components/board/verdict/__tests__/AntiViralityHeader.test.tsx (its existing tests — confirm the contract; do NOT duplicate them)
    - src/components/board/verdict/verdict-constants.ts (L14-19 — COPY.AV_HEADER(n) / AV_OVERRIDE_LINK, the banner copy)
  </read_first>
  <action>
    Create `src/components/reading/anti-virality-header.tsx` that re-exports the board `AntiViralityHeader` for the Reading namespace — `export { AntiViralityHeader } from '@/components/board/verdict/AntiViralityHeader'`. This is the ONE friction-free board-React reuse (D-04): it is already flat-warm-compatible (token gradient `var(--color-accent)`→`var(--color-warning)`, no glow), self-manages localStorage dismissal, and returns `null` when `!result.anti_virality_gated`. Do NOT fork or restyle it. The container (02-05) wires `<AntiViralityHeader result={data} analysisId={id} />` ABOVE the gauge (D-04) — the banner does NOT hide the score (score stays visible, subordinated). Only add a thin wrapper instead of a bare re-export IF a className/placement adjustment is needed; default to the bare re-export. (A bare re-export module needs no `'use client'` — the board component already declares its own directive.) No new test needed (the board component's own tests cover its behavior — referenced in read_first); the gate-banner-renders-when-gated assertion belongs to the hero composition test in 02-05.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading src/components/board/verdict/__tests__/AntiViralityHeader.test.tsx 2>&1 | tail -6 ; grep -q "AntiViralityHeader" src/components/reading/anti-virality-header.tsx && echo "REEXPORT OK"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/reading/anti-virality-header.tsx` exists and re-exports (or thinly wraps) the board `AntiViralityHeader`.
    - `grep -c "var(--color-accent)\|var(--color-warning)\|FF7F50\|boxShadow\|0 0 10px" src/components/reading/anti-virality-header.tsx` shows NO new glow/hardcoded color added (bare re-export adds none).
    - The existing board `AntiViralityHeader.test.tsx` still passes (no regression to the reused component).
  </acceptance_criteria>
  <done>The Reading namespace exposes the gate banner via a re-export of the clean board component; no fork, no restyle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signed keyframe URL → `<img src>` | signed dynamic URL from storage; must not be logged or persisted in plaintext logs |
| PredictionResult fields → DOM | only whitelisted hero fields rendered; no raw spread (READ-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information disclosure | thumbnail-strip.tsx signed keyframe URL | mitigate | use the existing `resolveKeyframeUrl`/KeyframeImage gate (real-video only); decorative `alt=""`; never `console.log` the signed URL (asserted: no console.* in file) |
| T-02-04 | Information disclosure | persona-cloud.tsx / hero atoms | mitigate | render only whitelisted fields (`heatmap.personas`, `persona_simulation_results`); never spread raw PredictionResult; sr-only mirror exposes only persona label + watch-through (no internal ids/telemetry) |
| T-02-05 | Spoofing | anti-virality-header.tsx | accept | re-export of an already-shipped, tested component behind the existing `(app)` auth gate; no new logic |
| T-02-SC | Tampering | npm installs | accept | zero new dependencies — nothing to vet |
</threat_model>

<verification>
- `npx vitest run src/components/reading` green (adds persona-cloud + thumbnail-strip tests).
- Board `AntiViralityHeader.test.tsx` unaffected (no regression).
- Full suite green at wave merge: `npm test`.
</verification>

<success_criteria>
- PersonaCloud: static deterministic dot-cloud (dots only — no watch caption), coral worst cluster, cream others, omitted on empty data, `onOpen` seam for Phase 3.
- ThumbnailStrip: static signed-URL poster gated on real video, fails to nothing, never logs the URL.
- AntiViralityHeader: clean re-export of the board gate banner (D-04), no fork.
</success_criteria>

<output>
Create `.planning/phases/02-the-reading/02-02-SUMMARY.md` when done.
</output>
