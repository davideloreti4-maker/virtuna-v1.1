---
phase: 02-the-reading
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - src/components/reading/reading.tsx
  - src/components/reading/index.ts
  - src/app/(app)/analyze/layout.tsx
  - src/components/reading/__tests__/reading.test.tsx
  - src/components/reading/__tests__/reading.degraded.test.tsx
  - src/components/reading/__tests__/reading.no-cut-data.test.tsx
autonomous: true
requirements: [READ-01, READ-03, READ-04, READ-08, READ-10]
must_haves:
  truths:
    - "After analysis, the Reading lays out top-to-bottom: thumbnail → (gate banner when gated) → hero (gauge | persona cloud + watch%) → 3 driver rows → Fix First → Deeper read → composer, in that order (READ-01)"
    - "The hero shows the gauge zone-colored, the gate banner above it when anti_virality_gated, and watch-through % exactly once — rendered BY THE CONTAINER (averageWatchThrough, fallback weighted_completion_pct), so it survives even when PersonaCloud omits itself on the empty-personas path (READ-04 hero-owned)"
    - "analysis_unavailable (fake 0) short-circuits to a 'We couldn't analyze this video' state BEFORE the gauge ever sees the 0 (D-13); the string '0' never reaches the score region"
    - "partial_analysis renders a small 'Partial read' annotation; apollo_reasoning null degrades rows/deeper while hero gauge + gate still resolve from overall_score"
    - "Tapping a driver row opens the DrillSheet (bottom mobile / right desktop) with real native panel content; the cloud opens its Phase-3 seam panel"
    - "No cut/jargon data appears anywhere — feature_vector, dead *_score, cost_cents/latency_ms, model names, critique, predicted_engagement never render (READ-10)"
    - "The /analyze layout mounts <Reading/> in place of <Board/>; the no-id composer state and the AppShell 760px column keep working"
  artifacts:
    - path: "src/components/reading/reading.tsx"
      provides: "The container — single usePermalinkAnalysis, dual-read, D-13 gate FIRST, panel state + one DrillSheet, vertical composition, whitelisted field reads"
      contains: "analysis_unavailable"
    - path: "src/components/reading/index.ts"
      provides: "Barrel for the reading components"
      exports: ["Reading"]
    - path: "src/app/(app)/analyze/layout.tsx"
      provides: "Layout restructured to mount <Reading/> (Landmine 0 invert)"
      contains: "Reading"
  key_links:
    - from: "src/components/reading/reading.tsx"
      to: "src/hooks/queries/use-permalink-analysis.ts"
      via: "single usePermalinkAnalysis() at the container; children get props"
      pattern: "usePermalinkAnalysis"
    - from: "src/app/(app)/analyze/layout.tsx"
      to: "src/components/reading/reading.tsx"
      via: "mounts <Reading/> (replaces <Board/>)"
      pattern: "Reading"
    - from: "src/components/reading/reading.tsx"
      to: "src/components/reading/drill-sheet.tsx"
      via: "one DrillSheet instance driven by panel state"
      pattern: "DrillSheet"
---

<objective>
Assemble the Reading: the **container** (`reading.tsx`) that fetches once, runs the D-13 honesty gate FIRST, composes the Wave-2 blocks in the locked vertical order (READ-01), owns the single DrillSheet + panel state, and renders watch-through exactly once; the **barrel**; the **`/analyze` layout restructure** (Landmine 0 — invert the Board mount to a Reading mount); and the **READ-10 no-cut-data enforcement** pass. This is the wave that makes the Reading real on `/analyze/[id]`.

Purpose: Everything upstream is a prop-driven leaf; this container is the brain — the single data source (sever the per-leaf re-subscribe), the honesty short-circuit (correctness, hits on permalink reload + real failures), the disclosure orchestration, and the cut-data discipline. The layout restructure retires the Konva Board and mounts the thread in the Phase-1 shell.
Output: `reading.tsx`, `index.ts`, restructured `analyze/layout.tsx`, and three container-level tests (composition order, degraded states, no-cut-data).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-the-reading/02-CONTEXT.md
@.planning/phases/02-the-reading/02-RESEARCH.md
@.planning/phases/02-the-reading/02-PATTERNS.md
@.planning/phases/02-the-reading/02-UI-SPEC.md
@.planning/phases/02-the-reading/02-VALIDATION.md

@.planning/phases/02-the-reading/02-01-SUMMARY.md
@.planning/phases/02-the-reading/02-02-SUMMARY.md
@.planning/phases/02-the-reading/02-03-SUMMARY.md
@.planning/phases/02-the-reading/02-04-SUMMARY.md

<interfaces>
<!-- Verbatim contracts. The Wave-2 components are now built — read their SUMMARYs for exact prop names. -->

Hero-owned watch% (READ-04) — the container renders it, NOT PersonaCloud:
From src/components/board/audience/audience-derive.ts: `averageWatchThrough(nodes): number | null` (already 0-100; build nodes via `buildPersonaNodes(heatmap, persona_simulation_results, badKey)`), with fallback `Math.round((heatmap?.weighted_completion_pct ?? 0)*100)` ONLY when `averageWatchThrough` is null. Render `{n}% watch` EXACTLY ONCE beside the cloud area — and crucially OUTSIDE the `PersonaCloud` (which returns null on empty personas), so watch% still shows on the degraded path.

Data source (single subscription at the container):
From src/hooks/queries/use-permalink-analysis.ts: `usePermalinkAnalysis(): { id: string | null; data: PredictionResult | null; isLoading: boolean }` (reads useParams; TanStack staleTime:Infinity; one network call shared).

Dual-read (Pitfall 2 — permalink-blank regression WPk976kozfWs; lift from InsightHeroFrame L191-196):
```typescript
const apollo = data?.variants?.apollo ?? data?.apollo_reasoning ?? null;
```

Honesty flags (engine types.ts L369-393, all REQUIRED booleans — present on fresh runs AND derivable on reload):
```typescript
// analysis_unavailable: boolean  — when true, overall_score is a FABRICATED 0. Short-circuit BEFORE the gauge. NEVER pass the 0 to ScoreGauge.
// partial_analysis: boolean       — exactly one signal dead → "Partial read" annotation; everything else renders.
// apollo_reasoning: null | {...}   — null → no rewrites/dimensions. Rows/Deeper degrade; hero gauge + gate STILL resolve from overall_score + anti_virality_gated (never null).
// counterfactuals?.suggestions     — SEPARATE field from apollo_reasoning (fixes can exist when rewrites don't).
```

Whitelisted fields the container may read (READ-10 — never spread raw PredictionResult):
`overall_score`, `anti_virality_gated`, `anti_virality_reason`, `analysis_unavailable`, `partial_analysis`, `apollo_reasoning.dimensions[]`, `apollo_reasoning.rewrites[]`, `variants.apollo` (dual-read), `counterfactuals.suggestions[]`, `heatmap.{personas[], weighted_top_dropoff_t, weighted_completion_pct, segments[]}`, `persona_simulation_results`, `hook_decomposition` (Hook drill-down native content).
BANNED (READ-10 — must NOT render): `feature_vector`, `rule_score`/`trend_score`/`ml_score`/`gemini_score` (dead), `score_weights`, `signal_availability`, `latency_ms`, `cost_cents`, `gemini_model`/`deepseek_model` (model names), `critique`, `predicted_engagement`, dead modules (retrieval/platform_fit/audio_fingerprint).

Wave-2 component props (confirm exact names from each SUMMARY; expected shapes):
```
<ScoreGauge score={overall_score} />                                              (02-01)
<DrillSheet open onOpenChange title>{panel}</DrillSheet>                            (02-01)
<PersonaCloud heatmap simResults onOpen />                                          (02-02)
<ThumbnailStrip heatmap />                                                          (02-02)
<AntiViralityHeader result={data} analysisId={id} />                               (02-02)
<DriverRows dimensions dropT onRowTap />                                            (02-03)
<FixFirstList fixes rewrites />                                                     (02-04)
<DeeperRead dimensions />                                                           (02-04)
```

Native drill-down content (D-12 — real engine data, simple form; NOT stubs, NOT the Phase-3 rich charts):
- Hook panel = `hook_decomposition` modality sub-scores as plain rows/mini-bars + `weakest_modality`.
- Retention panel = drop segments (`dropoff_segment_indices` / `heatmap.segments`) as a text list.
- Shareability panel = the share_pull dimension lever/evidence as plain text.
- Personas panel = the Phase-3 PersonaGraph seam (Phase 2 may show the persona list as native rows; Phase 3 mounts the Canvas graph here).
Closed allow-list for panelId (Security V5): `hook` | `retention` | `shareability` | `personas`.

Route restructure (Landmine 0) — current src/app/(app)/analyze/layout.tsx renders `<Board />` + `<div className="sr-only">{children}</div>`; `[id]/page.tsx` returns null. Invert: replace `<Board/>` with `<Reading/>` (same persistence rationale — survives submit→/analyze/[id]; usePermalinkAnalysis reads useParams so it works from the layout). Keep `[id]/page.tsx` metadata-only. The (app)/layout.tsx `<AppShell>` 760px column must keep working; the no-id `/analyze` composer state is the Phase-1 shell.

Barrel pattern: src/components/board/_kit/index.ts (`export { X } from './x'` named-export convention).

Test fixtures: `makeReadingResult`, `makeUnavailableResult`, `makePartialResult`, `makeApolloNullResult` (02-01). usePermalinkAnalysis must be mocked in tests (it reads useParams + fetch) — `vi.mock('@/hooks/queries/use-permalink-analysis', ...)` returning the fixture.

Tokens: 760px column (Phase 1 D-17) · `--color-foreground-muted` (annotations) · all score-zone + cream tokens as established.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reading container — single source, D-13 gate FIRST, vertical composition, one DrillSheet (READ-01/03/04/08)</name>
  <files>src/components/reading/reading.tsx, src/components/reading/index.ts, src/components/reading/__tests__/reading.test.tsx, src/components/reading/__tests__/reading.degraded.test.tsx</files>
  <read_first>
    - .planning/phases/02-the-reading/02-PATTERNS.md (§reading.tsx assignment — composition + short-circuit order; the dual-read; the whitelist; §index.ts barrel)
    - .planning/phases/02-the-reading/02-RESEARCH.md (the System data flow diagram; Landmine 1 single-source; Landmine 5 honesty-flag null handling; Pitfall 2 dual-read)
    - .planning/phases/02-the-reading/02-UI-SPEC.md (§Copywriting — the exact degraded/error/empty copy strings; §Spacing — 760px column, section gaps; §Visual Primitives — hero grid desktop / stack mobile)
    - .planning/phases/02-the-reading/02-CONTEXT.md (D-01..D-15 — esp. D-03 hero layout, D-04 gate above gauge, D-13 honesty, D-15 no prose)
    - src/components/board/InsightHeroFrame.tsx (L184-196 — the assembly + dual-read logic donor; do NOT import as-is, lift the logic)
    - src/hooks/queries/use-permalink-analysis.ts (the single data source)
    - src/lib/engine/types.ts (L300-393 PredictionResult + honesty flags)
    - The four Wave-2 SUMMARYs (exact prop names for ScoreGauge/DrillSheet/PersonaCloud/ThumbnailStrip/AntiViralityHeader/DriverRows/FixFirstList/DeeperRead)
  </read_first>
  <behavior>
    - Test (order, READ-01): with makeReadingResult, the rendered blocks appear top-to-bottom in order: thumbnail, hero (gauge + cloud), driver rows, Fix First, Deeper read (assert DOM order via querying testids/roles).
    - Test (hero, READ-03/04): the gauge renders the overall_score; watch-through % appears EXACTLY ONCE in the whole Reading (assert a single "{n}% watch" match); when anti_virality_gated, the AntiViralityHeader renders ABOVE the gauge and the score is still present.
    - Test (READ-04 degraded watch%): with `heatmap.personas=[]` but `weighted_completion_pct` present, the hero STILL renders watch% exactly once even though PersonaCloud is absent (no SVG cloud, but the "{n}% watch" caption is present) — the watch% is container-owned, not cloud-owned.
    - Test (D-13 unavailable): makeUnavailableResult() → renders "We couldn't analyze this video" and the gauge is NOT rendered; the literal "0" does NOT appear in the score region (assert the ScoreGauge component is absent).
    - Test (D-13 partial): makePartialResult() → renders the "Partial read" annotation AND the full hero/rows still render.
    - Test (D-13 apollo null): makeApolloNullResult() → the gauge + (gate if any) still render from overall_score; DriverRows/DeeperRead degrade (no fabricated 0); no throw.
    - Test (error): data null (not a degrade flag) → renders the "This Simulation didn't load" error state.
    - Test (disclosure, READ-07): tapping a driver row opens the DrillSheet (a role="dialog" / the sheet content appears) with native panel content; only ONE DrillSheet instance exists.
  </behavior>
  <action>
    Create `'use client'` `src/components/reading/reading.tsx` exporting `Reading()`. Call `usePermalinkAnalysis()` ONCE → `{ id, data, isLoading }`; children receive data as PROPS (sever the InsightHeroFrame per-leaf re-subscribe — no child calls usePermalinkAnalysis/useAnalysisStream). Dual-read `const apollo = data?.variants?.apollo ?? data?.apollo_reasoning ?? null` BEFORE deriving rows/rewrites/dims. Short-circuit ORDER (D-13, gate FIRST): if `isLoading` → a calm loading state; else if `!data` → `<ReadingError/>` ("This Simulation didn't load" / "Refresh, or open another from the sidebar."); else if `data.analysis_unavailable` → `<CouldNotAnalyze/>` ("We couldn't analyze this video" / "The read didn't come back. Try another video or re-upload.") — RETURN BEFORE rendering the gauge; NEVER pass the fabricated 0 to ScoreGauge. When rendering the Reading: if `data.partial_analysis`, render a small `Partial read — based on half the usual signals.` annotation near the hero (`text-foreground-muted`). Compose in the 760px column in the LOCKED vertical order (READ-01): `<ThumbnailStrip heatmap={data.heatmap}/>` → `<AntiViralityHeader result={data} analysisId={id ?? ''}/>` (renders null when not gated; ABOVE the gauge, D-04) → HERO grid (`flex-col md:flex-row` CSS-responsive — NOT useIsMobile-switched): `<ScoreGauge score={data.overall_score}/>` | a cloud column that renders `<PersonaCloud heatmap={data.heatmap} simResults={data.persona_simulation_results} onOpen={() => setPanel('personas')}/>` (dots only — may be null on empty personas) AND, SEPARATELY in the same column, the hero-owned watch% caption `{watch}% watch` computed BY THE CONTAINER: `watch = averageWatchThrough(buildPersonaNodes(data.heatmap, data.persona_simulation_results, worstBadGroupKey(buildSegmentGroups(data.heatmap, data.persona_simulation_results)))) ?? Math.round((data.heatmap?.weighted_completion_pct ?? 0)*100)`. The watch% MUST render regardless of whether PersonaCloud returned null (FIX: on the empty-personas reload the cloud omits but watch% must still show — it is hero-owned per D-04/READ-04). Ensure exactly ONE "{n}% watch" across the whole Reading (the cloud no longer renders it) → `<DriverRows dimensions={apollo?.dimensions} dropT={data.heatmap?.weighted_top_dropoff_t} onRowTap={setPanel}/>` → `<FixFirstList fixes={data.counterfactuals?.suggestions} rewrites={apollo?.rewrites}/>` → `<DeeperRead dimensions={apollo?.dimensions}/>`. Own panel state: `const [panel, setPanel] = useState<'hook'|'retention'|'shareability'|'personas'|null>(null)` driving ONE `<DrillSheet open={!!panel} onOpenChange={(o) => !o && setPanel(null)} title={panelTitle(panel)}>{panelContentFor(panel, data)}</DrillSheet>` — `panelContentFor` renders the D-12 native content (Hook = hook_decomposition modality rows + weakest_modality; Retention = drop segments text list; Shareability = share_pull lever/evidence text; Personas = native persona list, the Phase-3 PersonaGraph seam). READ-10: read ONLY whitelisted fields; never spread `data` into JSX; never render banned fields. NO verdict/horoscope prose anywhere (D-15). Keep the data source abstracted so Phase 4 can swap `usePermalinkAnalysis` → `useAnalysisStream` without a rewrite. Create the barrel `src/components/reading/index.ts` exporting the public components (`Reading`, and the leaves for Phase-3/4/5 reuse). Write `reading.test.tsx` (order + hero + disclosure) and `reading.degraded.test.tsx` (unavailable/partial/apollo-null/error), mocking `usePermalinkAnalysis` with the fixtures.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/reading.test.tsx src/components/reading/__tests__/reading.degraded.test.tsx 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - Both `reading.test.tsx` and `reading.degraded.test.tsx` exit 0.
    - DOM block order is thumbnail → hero(gauge,cloud) → driver rows → Fix First → Deeper read (READ-01).
    - The watch-through string "{n}% watch" matches EXACTLY ONCE in the full Reading render (READ-04 owned-once), rendered by the container (not by PersonaCloud).
    - With `heatmap.personas=[]` but `weighted_completion_pct` present, the hero still renders watch% exactly once and PersonaCloud is absent (no cloud `<svg>`, but the "{n}% watch" caption IS present) — proves watch% is container-owned and survives the empty-personas degraded path.
    - makeUnavailableResult() → "We couldn't analyze this video" is present AND the ScoreGauge is NOT in the document AND the score region contains no literal "0".
    - makePartialResult() → "Partial read" annotation present AND the hero/rows still render.
    - makeApolloNullResult() → gauge renders from overall_score; no throw; DriverRows/DeeperRead degrade (no fabricated 0).
    - `grep -c "usePermalinkAnalysis\|useAnalysisStream" src/components/reading/reading.tsx` returns exactly 1 (single source; children take props).
    - `grep -c "DrillSheet" src/components/reading/reading.tsx` shows a single DrillSheet instance; `setPanel` is the only driver.
  </acceptance_criteria>
  <done>The Reading container fetches once, gates honesty FIRST (the fabricated 0 never reaches the gauge), composes the locked vertical order with watch% shown once, and drives one DrillSheet with real native panel content.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure /analyze layout to mount <Reading/> (Landmine 0)</name>
  <files>src/app/(app)/analyze/layout.tsx</files>
  <read_first>
    - .planning/phases/02-the-reading/02-PATTERNS.md (§analyze/layout.tsx — LANDMINE 0, invert the Board mount; keep [id]/page.tsx metadata-only)
    - .planning/phases/02-the-reading/02-RESEARCH.md (Landmine 0 full detail — render <Reading/> from layout.tsx, the AppShell 760px column + no-id composer interplay, Open Question 1 + recommendation)
    - src/app/(app)/analyze/layout.tsx (CURRENT — the <Board/> + sr-only children mount to invert)
    - src/app/(app)/analyze/[id]/page.tsx (the metadata-only server shell — keep as-is)
    - src/app/(app)/layout.tsx (the <AppShell> wrapper — the 760px column that must keep working; confirm how Reading sits inside it)
    - src/components/reading/index.ts (the barrel exporting Reading — from Task 1)
  </read_first>
  <action>
    Edit `src/app/(app)/analyze/layout.tsx`: replace the `<Board />` mount with `<Reading />` (imported from `@/components/reading`). Keep the `<Suspense fallback={null}>` wrapper and the `{children}` from the page — but since the Reading is now the real UI (not a Konva DOM overlay), render `<Reading />` as the layout body and keep `[id]/page.tsx` returning null (metadata-only). Verify the Reading sits correctly inside the Phase-1 `(app)/layout.tsx` `<AppShell>` 760px column and the no-id `/analyze` composer state (Phase-1 shell) still works — the Reading should render its blocks only when `usePermalinkAnalysis` has data (id present); with no id it should be inert / the composer-centered shell remains. Remove the now-dead `<Board />` import. Do NOT delete `src/components/board/**` (the visuals are Phase-3 drill-down sources) and do NOT delete `/analyze` route files (dormant-but-reachable per the milestone constraint). Update the stale layout comment to reflect the new Reading mount. This is a layout restructure, not a 1-for-1 swap — size accordingly and confirm the composer→/analyze/[id] transition still lands the Reading.
  </action>
  <verify>
    <automated>grep -n "Reading\|Board" 'src/app/(app)/analyze/layout.tsx' ; npx tsc --noEmit 2>&1 | grep -i "analyze/layout\|reading" | head -10 ; npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/analyze/layout.tsx` imports and mounts `<Reading />` from `@/components/reading`; the `<Board />` import is removed.
    - `grep -c "Board" 'src/app/(app)/analyze/layout.tsx'` returns 0 (Board mount retired from this layout).
    - `src/app/(app)/analyze/[id]/page.tsx` still returns null (metadata-only) — unchanged.
    - `npm run build` succeeds (clean build — no type errors from the restructure).
    - `src/components/board/` still exists (Phase-3 sources not deleted); `/analyze` route files still exist (dormant-but-reachable).
  </acceptance_criteria>
  <done>The /analyze layout mounts the Reading thread in place of the Konva Board; the Phase-1 shell/composer/760px column keep working; the build is clean; board sources are preserved for Phase 3.</done>
</task>

<task type="auto">
  <name>Task 3: READ-10 no-cut-data enforcement + full-suite green gate</name>
  <files>src/components/reading/__tests__/reading.no-cut-data.test.tsx</files>
  <read_first>
    - .planning/phases/02-the-reading/02-PATTERNS.md (the "Whitelisted engine fields" block + the anti-pattern "Spreading raw PredictionResult into JSX")
    - .planning/phases/02-the-reading/02-CONTEXT.md (READ-10 cut-data list + brief §2.9 reference)
    - .planning/phases/02-the-reading/02-RESEARCH.md (READ-10 row; §Security Domain cut-data leak mitigation; anti-pattern "never spread raw PredictionResult")
    - src/lib/engine/types.ts (L300-455 — the full PredictionResult so the test asserts the BANNED fields by name: feature_vector, rule_score/trend_score/ml_score/gemini_score, score_weights, signal_availability, latency_ms, cost_cents, gemini_model/deepseek_model, critique, predicted_engagement)
    - src/components/reading/reading.tsx (Task 1 — the container under test)
  </read_first>
  <action>
    Write `src/components/reading/__tests__/reading.no-cut-data.test.tsx` (READ-10 assertion): render the full Reading with a `makeReadingResult` fixture EXTENDED to carry populated banned fields (e.g. `feature_vector` with a recognizable sentinel, `cost_cents: 4.2`, `latency_ms: 31000`, `gemini_model: 'qwen-omni-flash'`, `deepseek_model: 'deepseek-reasoner'`, `critique: { ... }`, `predicted_engagement: { lo, hi, ... }`, `rule_score/trend_score/ml_score/gemini_score` non-null, `score_weights`, `signal_availability`). Assert the rendered DOM (`container.textContent` / queries) contains NONE of: the model-name strings, the `cost_cents`/`latency_ms` numeric sentinels, any `feature_vector` key, the word "critique", "predicted_engagement" / "projected views", or the dead `*_score` values. This is an assert-ABSENT test (the whitelist discipline made testable). If the test FAILS (a banned field leaks), fix `reading.tsx` (and any leaf) to read only whitelisted fields — never spread raw `data` into JSX — until green. Then run the FULL suite as the wave/phase gate: `npm test` must stay green (~1967 tests). If any prior reading component spreads raw fields, repoint it to explicit whitelisted reads. This task's value is the standing READ-10 regression guard.
  </action>
  <verify>
    <automated>npx vitest run src/components/reading/__tests__/reading.no-cut-data.test.tsx 2>&1 | tail -8 ; echo "=== FULL SUITE ===" ; npm test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `reading.no-cut-data.test.tsx` exits 0 and asserts the ABSENCE of every banned field/value in the rendered DOM.
    - The rendered DOM contains no occurrence of the model-name strings (`qwen-omni-flash`/`deepseek-reasoner`), the `cost_cents`/`latency_ms` sentinels, `feature_vector`, `critique`, or `predicted_engagement`.
    - `grep -rc "\.\.\.data\b\|\.\.\.result\b\|JSON.stringify(data" src/components/reading/` shows no raw PredictionResult spread into JSX across the reading components.
    - `npm test` (full suite) exits 0 — ~1967 tests stay green (phase gate).
  </acceptance_criteria>
  <done>A standing READ-10 regression guard proves no cut/jargon field renders, the whitelist discipline holds across all reading components, and the full suite is green — the phase is ready for verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route param `id` → /api/analysis/[id] | IDOR-defended server-side (existing); container only consumes the returned data |
| PredictionResult → DOM | the load-bearing boundary — only whitelisted fields cross; raw spread is forbidden (READ-10) |
| panel state (`?panel=` if synced) → DrillSheet | panelId is a closed union mapped through an allow-list before opening |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Information disclosure | reading.tsx → DOM (cut-data leak) | mitigate | whitelist rendered fields; never spread raw PredictionResult; Task 3 is a standing assert-absent regression guard (READ-10) — feature_vector/telemetry/model-names/critique/predicted_engagement proven absent |
| T-02-12 | Tampering | reading.tsx panel / future ?panel= | mitigate | panelId is the closed union `hook`/`retention`/`shareability`/`personas`; if URL-synced, validate against this allow-list before opening — never reflect the raw param or key untrusted markup off it (Security V5) |
| T-02-13 | Information disclosure | reading.tsx D-13 fabricated-0 | mitigate | analysis_unavailable short-circuits BEFORE the gauge — the fabricated 0 never renders as a real score (honesty correctness, hits on permalink reload) |
| T-02-14 | Spoofing | analyze/layout.tsx | accept | the route is already behind the (app) server `auth.getUser()` gate (Phase 1); the restructure changes only the mounted component, not the auth boundary |
| T-02-SC | Tampering | npm installs | accept | zero new dependencies — nothing to vet (RESEARCH Package Legitimacy Audit: gate not triggered) |
</threat_model>

<verification>
- `npx vitest run src/components/reading` green (adds reading, reading.degraded, reading.no-cut-data tests).
- `npm run build` clean (the layout restructure type-checks + builds).
- `npm test` full suite green (~1967 tests) — the phase gate before /gsd:verify-work.
- `/analyze/[id]` renders the vertical Reading thread inside the Phase-1 AppShell 760px column; `/analyze` (no id) keeps the Phase-1 composer-centered shell.
</verification>

<success_criteria>
- The Reading renders top-to-bottom thumbnail → gate → hero(gauge|cloud+watch%) → 3 rows → Fix First → Deeper read → composer (READ-01), hero zone-colored with gate above + watch% once (READ-02/03/04).
- D-13 honesty: analysis_unavailable short-circuits before the gauge (the 0 never shows); partial_analysis annotates; apollo null degrades rows/deeper while hero/gate resolve.
- Disclosure: one DrillSheet, row tap opens it with real native content (D-12), cloud opens the Phase-3 seam.
- READ-10: no cut/jargon data renders anywhere (standing guard).
- /analyze layout mounts <Reading/> (Board retired from the mount, board sources preserved); clean build; full suite green.
</success_criteria>

<output>
Create `.planning/phases/02-the-reading/02-05-SUMMARY.md` when done.
</output>
