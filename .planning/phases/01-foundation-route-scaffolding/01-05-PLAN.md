---
phase: 01-foundation-route-scaffolding
plan: 05
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified: []
autonomous: false
requirements:
  - SC-1
  - SC-2
  - SC-3
  - SC-4

must_haves:
  truths:
    - "Production build is clean — `pnpm run build` exits 0 with zero TypeScript errors, zero linting errors that fail the build, zero broken imports"
    - "All 4 Wave-0 invariant tests are GREEN — encoded invariants for Plans 02 + 03 hold under the final tree"
    - "Visitor at / sees the header (Virtuna wordmark + 'Sign up free' CTA) over a dark canvas with no rendered body content — confirming SC-1"
    - "Visitor at /showcase scrolls to the Magic UI section and sees coral hover spotlights, coral border beam, coral shine border — no visible violet/amber/pink/black bleeding — confirming SC-2 (Raycast-native vetting)"
    - "Visitor at /pricing sees PricingSection only (no FAQ that previously plagiarized AS copy) — confirming the cross-route cleanup didn't break /pricing"
    - "Chrome DevTools console shows zero errors and zero React hydration warnings on / and /showcase first paint — confirming SC-4"
  artifacts:
    - path: "(no new files — verification-only plan)"
      provides: "Confirmation that all SC-1 through SC-4 success criteria are met"
  key_links:
    - from: "human verification"
      to: "browser DevTools"
      via: "Chrome console + network panel"
      pattern: "(human-eye check; not gateable via grep)"
---

<objective>
Verify Phase 1 deliverables end-to-end against the 4 ROADMAP success criteria (SC-1 through SC-4). This is the final gate before Phase 1 closes. It is a verification-only plan — no source files are modified.

Three automated checks (build, tests, console-error sweep via Next.js build log) plus three manual browser checks (`/`, `/pricing`, `/showcase`) cover the surface area. The autonomous build + test step runs first (autonomous=false on the plan reflects the embedded checkpoint task, not the build step itself).

Purpose: SC-4 (no console errors, no hydration warnings) is the only criterion that cannot be fully automated in this stack — browser DevTools is the canonical signal. The checkpoint task in this plan pauses for the user to perform the 3-route browser smoke and report back.

Output: A SUMMARY.md noting build status, test status, and human verdict for each of the 3 routes. If anything fails, Phase 1 does not close — issues feed back to whichever plan owns the failing assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/MILESTONE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-route-scaffolding/01-CONTEXT.md
@.planning/phases/01-foundation-route-scaffolding/01-VALIDATION.md
@.planning/phases/01-foundation-route-scaffolding/01-01-SUMMARY.md
@.planning/phases/01-foundation-route-scaffolding/01-02-SUMMARY.md
@.planning/phases/01-foundation-route-scaffolding/01-03-SUMMARY.md
@.planning/phases/01-foundation-route-scaffolding/01-04-SUMMARY.md
@CLAUDE.md

<interfaces>
The 4 ROADMAP §"Phase 1" success criteria — verbatim text the executor must score:

SC-1: "Visitor at `/` sees a clean, intentionally-empty marketing route (not the plagiarized AS template) with the existing dark-mode design tokens applied"
- Automated assertion: Plan 03's marketing-shell test covers source-level invariants
- Manual assertion: browser smoke on `/` shows header + dark canvas + nothing else

SC-2: "At least one Magic UI primitive is installed, vetted to feel native to the Raycast aesthetic (6% borders, GlassPanel pattern, Inter font), and exported through a documented integration path"
- Automated assertion: Plan 02's magic-card/border-beam/shine-border tests verify tuned defaults; barrel export exists
- Manual assertion: browser smoke on `/showcase#magic-ui` confirms primitives feel native, not "Magic UI default" (no violet/amber/pink/black bleed)

SC-3: "A documented vetting checklist exists for selectively importing Magic UI / Aceternity / Origin UI / Cult UI components without breaking the Raycast design language"
- Automated assertion: `grep -c "External Library Vetting Checklist" BRAND-BIBLE.md` ≥ 1
- Manual assertion: skim the checklist; confirm 9 gates are present and worded as gates (PASS/FAIL), not as advice

SC-4: "The route renders without console errors and without React hydration warnings on first load"
- Manual-only: open `/` and `/showcase` in Chrome DevTools, watch console for errors and React hydration warnings

Routes to verify:
- `/` — empty marketing shell (Plan 03)
- `/showcase` — design system + Magic UI section (Plan 04)
- `/pricing` — PricingSection only after FAQ detach (Plan 03 Task 1)

Dev server start command: `pnpm dev` (uses `next dev` per `package.json:dev`)
Default port: 3000 (or whatever Next.js picks if 3000 is taken — note the port in the SUMMARY)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final automated verification (build + full Wave-0 test suite + lint)</name>
  <read_first>
    - All prior plan SUMMARYs in `.planning/phases/01-foundation-route-scaffolding/` to understand what each plan claims is done
    - `package.json` scripts section (confirm `build`, `test`, `lint` script names)
  </read_first>
  <files>
    (no files modified — read-only verification)
  </files>
  <action>
Run the following commands in sequence. Capture outputs to the SUMMARY.

**Step 1 — Clean build cache (RESEARCH §Pitfall 4 hygiene):**

```
rm -rf .next/ node_modules/.cache/
```

This is precautionary — Plans 02 and 03 may have left stale cache that masks a real issue. Phase 1 closing should run against a clean build.

**Step 2 — Production build:**

```
pnpm run build
```

Expected: exits 0. Capture any warnings to the SUMMARY (TypeScript warnings, missing type warnings, image-optimization warnings — these don't fail Phase 1 but should be noted).

**Step 3 — Full Wave-0 invariant test suite:**

```
pnpm test src/components/magic-ui/__tests__/
```

Expected: 4 test files PASS — `magic-card.test.ts`, `border-beam.test.ts`, `shine-border.test.ts`, `marketing-shell.test.ts`. Capture the exact assertion count (each file has 5-6 assertions; total ≈ 22).

**Step 4 — Lint (does not gate Phase 1 but flags issues for SUMMARY):**

```
pnpm run lint
```

Expected: clean, OR known pre-existing warnings only. Phase 1 did not introduce new code in shape that should trip lint; any new lint warnings indicate a regression to investigate.

**Step 5 — Cross-route render smoke (build-level):**

The Next.js production build (Step 2) compiles every route. If `/`, `/showcase`, or `/pricing` had a broken import, the build would already have failed in Step 2. Re-confirm this by checking the build log for the route prefixes:

```
pnpm run build 2>&1 | grep -E "^\s+/(showcase|pricing)?" | head -10
```

Expected: 3 route entries — `/`, `/showcase`, `/pricing` — none marked as compilation failures.

If ANY of Steps 2/3 fail, STOP — do not proceed to the checkpoint task. Surface the failure to the operator with the file path and assertion that failed. Phase 1 cannot close on red.
  </action>
  <verify>
    <automated>pnpm run build && pnpm test src/components/magic-ui/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run build` exits 0
    - Build log shows route entries for `/`, `/showcase`, `/pricing` (verify via `pnpm run build 2>&1 | grep -cE "/(showcase|pricing)"` returns ≥ 2)
    - `pnpm test src/components/magic-ui/__tests__/` reports 4 test files PASS, 0 fail (any failure = Phase 1 cannot close)
    - `pnpm run lint` either clean OR only pre-existing warnings (no new lint failures introduced by Phase 1)
    - No file modifications during this task (verify: `git status` shows no changes to source files; only `.next/` directory regenerated if not gitignored)
    - The `.next/` and `node_modules/.cache/` directories cleared before build (verify the build does not reuse stale CSS for the new `@keyframes shine`)
  </acceptance_criteria>
  <done>
    Automated verification complete: build clean, all 4 Wave-0 tests GREEN, lint clean (or known pre-existing only). Phase 1's automated SC criteria (SC-1 partial, SC-2 partial, SC-3) all show GREEN. SC-4 awaits browser smoke (next task).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual browser smoke check — / + /pricing + /showcase</name>
  <read_first>
    - All Wave-0 test outputs from Task 1 (confirm GREEN before starting browser smoke)
    - .planning/phases/01-foundation-route-scaffolding/01-UI-SPEC.md §"Marketing Route Shell Contract" + §"Header Stub Contract" + §"/showcase Route Addition Contract" — these define the visual contract the human is verifying
  </read_first>
  <what-built>
    - Phase 1 has installed + tuned 3 Magic UI primitives (Plan 02), replaced the plagiarized AS template at `/` with an empty Virtuna shell + Virtuna-correct header (Plan 03), detached FAQSection from `/pricing` and deleted `src/components/landing/` (Plan 03), and added a "Magic UI Primitives" section to `/showcase` (Plan 04). Task 1 (automated) confirmed the build is clean and all 4 Wave-0 invariant tests are GREEN.
  </what-built>
  <how-to-verify>
**Step 1 — Start the dev server:**

```
pnpm dev
```

Note the port reported (usually 3000; record actual port in SUMMARY).

**Step 2 — Open Chrome DevTools BEFORE navigating** (so you catch errors during first paint):

1. Open Chrome
2. Hit Cmd+Option+J (macOS) to open DevTools Console
3. Clear the console (Cmd+K)
4. Toggle DevTools to "Preserve log" so navigations don't wipe the log

**Step 3 — Visit `/` (the empty marketing shell):**

Navigate to `http://localhost:3000/`. Verify:

| Check | Expected outcome | Pass/Fail |
|-------|------------------|-----------|
| Header wordmark text | Reads "Virtuna" (NOT "Artificial Societies") | |
| Header primary CTA | Reads "Sign up free", clicking routes to `/signup` | |
| Header primary CTA color | Coral background, dark brown text (accent / accent-foreground tokens) | |
| Header "Sign in" link | Visible, hover changes opacity, routes to `/auth/login` | |
| `<main>` body content | NOTHING. Empty dark canvas. No "coming soon" text, no spinner, no placeholder | |
| Body background color | `#07080a` — very dark, near-black | |
| DevTools Console | 0 errors, 0 React hydration warnings | |
| DevTools Network | Only expected resources load (no 404 for stale `/components/landing/*` paths) | |

**Step 4 — Visit `/pricing` (regression check after FAQSection detach):**

Navigate to `http://localhost:3000/pricing`. Verify:

| Check | Expected outcome | Pass/Fail |
|-------|------------------|-----------|
| Route renders | Page loads without 500 error | |
| Pricing section present | Existing PricingSection content visible | |
| FAQ section absent | NO "Common questions" FAQ at the bottom of the page (was AS-plagiarized; intentionally removed in Phase 1) | |
| Footer present | Existing Footer renders (Phase 7 will swap it later) | |
| DevTools Console | 0 errors, 0 React hydration warnings on this route | |

**Step 5 — Visit `/showcase` and scroll to the Magic UI section:**

Navigate to `http://localhost:3000/showcase`. Scroll to the bottom (or click `#magic-ui` if the page has anchor nav). Verify:

| Check | Expected outcome | Pass/Fail |
|-------|------------------|-----------|
| Section heading | "Magic UI Primitives" visible | |
| Section description | The "Three vetted primitives tuned to the Raycast design language" copy | |
| Magic Card demo (2-column) | 2 cards side-by-side. Hover one — coral radial spotlight should track your cursor smoothly (NOT violet) | |
| Magic Card border on hover | Border arc tracks cursor in coral (NOT violet/pink) | |
| Border Beam demo | One card with a slowly-sweeping coral arc on its 1px border (8s cycle — restrained, NOT high-contrast neon) | |
| Border Beam color | Coral rgba (warm orange) — NOT amber yellow, NOT violet | |
| Shine Border demo | One card with a rotating coral shine sweep on its border (18s cycle — very slow, very subtle) | |
| Shine Border color | Coral palette — NOT black sweep | |
| CodeBlock | Visible at the bottom of the section, shows canonical `import { MagicCard, BorderBeam, ShineBorder } from "@/components/magic-ui";` | |
| Existing sections above | Colors, Typography, Spacing, Radius, Surfaces, Shadows, Gradients — all render unchanged | |
| DevTools Console | 0 errors, 0 React hydration warnings on this route | |
| Reduced-motion (optional) | Set Chrome `prefers-reduced-motion: reduce` via DevTools (Rendering tab); refresh `/showcase`; Border Beam should not render the moving arc, Shine Border arc should freeze, Magic Card spotlight should not track cursor | |

**Step 6 — Final eyeball test (Raycast-nativeness):**

Compare the new Magic UI section to the existing 36-component showcase above it. Does the Magic UI section feel like part of the same design system? Specifically:
- Borders: 1px rgba(255,255,255,0.06) — same as existing cards
- Radius: 12px — same as existing cards
- Background: dark surface — same as existing
- Hover: subtle, not maximalist — same idiom as existing

If anything looks "off" (purple bleed, neon glow, too-fast animation, embedded font), that's a vetting-checklist gate failure to flag in the SUMMARY.

**Step 7 — Stop the dev server:**

Ctrl-C the `pnpm dev` process.

**Step 8 — Report back with the table results.** For each of the ~25 checks above, mark PASS or FAIL. Any FAIL becomes a Phase 1 closing blocker and must be routed back to the owning plan for revision.

If ALL checks PASS: type "approved" (or describe what you saw). Phase 1 closes.
If ANY check FAIL: describe the failure precisely — which route, which check, what you actually saw vs what was expected. We'll route to the owning plan and revise.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all browser checks PASS. Otherwise paste the failing rows from the tables above with a description of what was actually observed.
  </resume-signal>
  <files>(none — verification-only checkpoint task, no source mutations)</files>
  <action>Manual operator-driven verification — see the <how-to-verify> block above for the exact steps and the route-by-route check tables. This task is gated by the operator's response.</action>
  <verify>Operator response to <resume-signal> — typing "approved" indicates all browser checks PASS; any other response routes the listed failures back to the owning plan for revision.</verify>
  <done>All 25 browser-smoke checks across `/`, `/pricing`, and `/showcase` are marked PASS by the operator; Phase 1 closes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build artifact → browser | The production build output is loaded by Chrome. Any console-error or hydration warning is a user-visible signal. |
| Human verification | Manual eyeball test cannot be automated; introduces operator-dependent variance. Mitigated by giving the operator a precise table to score. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T (Tampering) | An executor could skip the human checkpoint and self-approve | accept | The plan is `autonomous: false`; the orchestrator gates on a "approved" response from the operator. Skipping requires affirmatively bypassing the gate, which is documented as wrong. |
| T-05-02 | I (Information disclosure) | DevTools console output is shared in the SUMMARY (could leak internal paths) | accept | Repo is private (per CLAUDE.md). Internal paths in the console are part of normal development workflow. |
| T-05-03 | D (Denial of service) | Dev server crash during smoke check | accept | `pnpm dev` is a routine local-dev command; failure mode is well-understood and operator restarts. No production impact. |
</threat_model>

<verification>
- Task 1 (automated): build + tests + lint all GREEN
- Task 2 (manual): operator reports all 25 browser checks PASS via "approved" response
- Phase 1 SC-1 confirmed: `/` shows header over empty dark canvas
- Phase 1 SC-2 confirmed: Magic UI primitives feel Raycast-native in `/showcase`
- Phase 1 SC-3 confirmed: BRAND-BIBLE.md vetting checklist exists with 9 gates
- Phase 1 SC-4 confirmed: no console errors, no hydration warnings on `/` or `/showcase` first paint
- Phase 1 cross-route invariant confirmed: `/pricing` still renders correctly (PricingSection + Footer, no FAQ)
</verification>

<success_criteria>
- All 4 ROADMAP success criteria for Phase 1 (SC-1, SC-2, SC-3, SC-4) verified — automated where possible, manual where required
- Phase 1 can close cleanly; the milestone progresses to Phase 2 (Hero & Trust Band) which composes against the empty shell + Magic UI primitives Phase 1 delivered
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-route-scaffolding/01-05-SUMMARY.md` covering:
- Task 1 automated results: build exit code, route entries detected in build log, test pass count, lint state (clean vs warnings)
- Task 2 manual browser smoke results: route-by-route table with PASS/FAIL for each of the ~25 checks
- Reduced-motion check result (if performed): did the motion stop in all 3 primitives?
- Final SC scorecard: SC-1, SC-2, SC-3, SC-4 each marked GREEN with a one-line evidence pointer
- Any anomalies that surfaced (console warnings that did not fail the build, intermittent flicker on Magic UI hover, etc.) — flagged for Phase 2 attention or noted as out-of-scope
- Recommendation: ready to close Phase 1 → proceed to `/gsd-discuss-phase 2`
</output>
