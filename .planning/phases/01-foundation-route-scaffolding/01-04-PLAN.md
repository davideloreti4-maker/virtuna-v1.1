---
phase: 01-foundation-route-scaffolding
plan: 04
type: execute
wave: 2
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/app/(marketing)/showcase/page.tsx
autonomous: true
requirements:
  - SC-2

must_haves:
  truths:
    - "The /showcase route renders a new 'Magic UI Primitives' section below the existing 'Gradients' section"
    - "The section composes MagicCard (×2), BorderBeam, and ShineBorder demos using the tuned defaults from Plan 02 — no stock props passed at usage sites"
    - "A CodeBlock under the demos shows the canonical 'import { MagicCard, BorderBeam, ShineBorder } from \"@/components/magic-ui\"' usage so Phase 2-6 executors can copy-paste"
    - "The existing 36-component showcase sections (Colors, Typography, Spacing, Radius, Surfaces, Shadows, Gradients, etc.) render unchanged — no regression"
    - "Build is clean (`pnpm run build` exits 0) and no hydration warnings are introduced"
  artifacts:
    - path: "src/app/(marketing)/showcase/page.tsx"
      provides: "New ShowcaseSection with id='magic-ui' appended after the existing Gradients section"
      contains: "Magic UI Primitives"
  key_links:
    - from: "src/app/(marketing)/showcase/page.tsx"
      to: "src/components/magic-ui/index.ts"
      via: "named import"
      pattern: "import\\s*\\{\\s*MagicCard\\b.*BorderBeam\\b.*ShineBorder\\b.*\\}\\s*from\\s*[\"']@/components/magic-ui[\"']"
    - from: "src/app/(marketing)/showcase/page.tsx"
      to: "src/app/(marketing)/showcase/_components/showcase-section.tsx"
      via: "wrapping ShowcaseSection"
      pattern: "<ShowcaseSection[^>]*id=\"magic-ui\""
---

<objective>
Make Plan 02's primitives visible and eyeball-testable by adding a "Magic UI Primitives" section to the existing `/showcase` route (D-07 — verification surface is the existing showcase, not a separate sandbox). The section composes Magic Card × 2 in a 2-column grid, Border Beam in a single card, Shine Border in a single card, plus a CodeBlock with canonical usage so downstream phases have a copy-paste template.

Purpose: SC-2 verification — proves the tuned primitives feel Raycast-native (6% borders, GlassPanel compat, coral accents) without any stock visual bleed. Plan 05's final manual smoke check inspects this section in a browser to confirm SC-4 (no console errors, no hydration warnings).

Output: A single appended `<ShowcaseSection id="magic-ui">` block at the end of `src/app/(marketing)/showcase/page.tsx`, after the existing "Gradients" section (lines 736-777 in the current file). No other file is modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/MILESTONE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-route-scaffolding/01-CONTEXT.md
@.planning/phases/01-foundation-route-scaffolding/01-RESEARCH.md
@.planning/phases/01-foundation-route-scaffolding/01-UI-SPEC.md
@.planning/phases/01-foundation-route-scaffolding/01-02-SUMMARY.md
@.planning/phases/01-foundation-route-scaffolding/01-03-SUMMARY.md
@CLAUDE.md

<interfaces>
Existing showcase page structure (verified — line offsets refer to the current file):

`src/app/(marketing)/showcase/page.tsx` (top-of-file imports — line 1-7):
```
import { Heading, Text } from "@/components/ui";

import { CodeBlock } from "./_components/code-block";
import { ComponentGrid } from "./_components/component-grid";
import { ShowcaseSection } from "./_components/showcase-section";
import { TokenRow, TokenSwatch } from "./_components/token-swatch";
```

`ShowcaseSection` component contract (from `./_components/showcase-section.tsx`):
```
interface ShowcaseSectionProps {
  title: string;
  description: string;
  children: React.ReactNode;
  id?: string;
}
```

`ComponentGrid` component contract (from `./_components/component-grid.tsx`):
```
interface ComponentGridProps {
  children: React.ReactNode;
  columns?: 2 | 3 | 4;
  className?: string;
}
```

`Text` component (from `@/components/ui`): accepts `size` and `muted` props (used in existing showcase for placeholder labels).

`CodeBlock` component (from `./_components/code-block`): accepts `title?: string` and `code: string` props.

Existing "Gradients" section anchor — last section in the file (line 736 starts with `{/* --- Gradients --- */}` and the `</ShowcaseSection>` for Gradients closes at line 777). The new Magic UI section MUST go AFTER this `</ShowcaseSection>` and BEFORE the closing `</div>` at line 778. The closing `}` at line 780 ends the `ShowcasePage` function.

Magic UI barrel (created by Plan 02): `src/components/magic-ui/index.ts` exports `MagicCard`, `BorderBeam`, `ShineBorder` — named exports.

Tuned defaults (from Plan 02 — already baked into the source files, NOT passed at usage):
- MagicCard: gradientFrom `#FF7F50`, gradientTo `rgba(255,127,80,0.15)`, gradientColor `#18191a`, gradientOpacity `0.6` — usage site can call `<MagicCard>...</MagicCard>` with no props and get coral spotlight
- BorderBeam: colorFrom `rgba(255,127,80,0.9)`, colorTo `rgba(255,127,80,0)`, duration `8`, size `40` — usage site `<BorderBeam />`
- ShineBorder: shineColor `["rgba(255,127,80,0.8)", "rgba(255,127,80,0.15)", "rgba(255,127,80,0.8)"]`, duration `18` — usage site `<ShineBorder />`

UI-SPEC §"/showcase Route Addition Contract" prescribes the EXACT structure: 2-column MagicCard grid → single BorderBeam card → single ShineBorder card → CodeBlock with canonical imports.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the Magic UI Primitives section to /showcase/page.tsx</name>
  <read_first>
    - src/app/(marketing)/showcase/page.tsx (lines 1-7 for current imports + lines 736-780 for the Gradients section + closing div — these are the two insertion zones)
    - src/components/magic-ui/index.ts (confirm named exports — MagicCard, BorderBeam, ShineBorder)
    - .planning/phases/01-foundation-route-scaffolding/01-UI-SPEC.md §"/showcase Route Addition Contract" (section placement, hierarchy, card heights, CodeBlock body)
    - .planning/phases/01-foundation-route-scaffolding/01-RESEARCH.md §"Code Examples" — Example 6 (verbatim block to append, including imports)
    - src/app/(marketing)/showcase/_components/showcase-section.tsx (confirm ShowcaseSection prop names — title, description, children, id)
    - src/app/(marketing)/showcase/_components/component-grid.tsx (confirm ComponentGrid prop — columns: 2 | 3 | 4)
  </read_first>
  <files>
    - src/app/(marketing)/showcase/page.tsx
  </files>
  <action>
This task makes TWO related Edits to `src/app/(marketing)/showcase/page.tsx`. Both Edits must succeed atomically — if Step 1 lands but Step 2 fails, the file won't compile.

**Step 1 — Add the Magic UI import to the import block.**

Find the existing import block (lines 1-7 of the current file):
```
import { Heading, Text } from "@/components/ui";

import { CodeBlock } from "./_components/code-block";
import { ComponentGrid } from "./_components/component-grid";
import { ShowcaseSection } from "./_components/showcase-section";
import { TokenRow, TokenSwatch } from "./_components/token-swatch";
```

Insert (immediately after the `TokenRow, TokenSwatch` import line) a new import line:
```
import { MagicCard, BorderBeam, ShineBorder } from "@/components/magic-ui";
```

Final import section after Step 1 reads:
```
import { Heading, Text } from "@/components/ui";

import { CodeBlock } from "./_components/code-block";
import { ComponentGrid } from "./_components/component-grid";
import { ShowcaseSection } from "./_components/showcase-section";
import { TokenRow, TokenSwatch } from "./_components/token-swatch";
import { MagicCard, BorderBeam, ShineBorder } from "@/components/magic-ui";
```

**Step 2 — Append the new ShowcaseSection JSX at the end of the page.**

Find the closing tag of the existing "Gradients" section. Per the verified grep, the file ends:
```
        </div>
      </ShowcaseSection>      ← line ~777 (closes Gradients ShowcaseSection)
    </div>                    ← line ~778 (closes the page wrapper div)
  );
}                             ← line ~780 (closes ShowcasePage function)
```

Insert the new section block BETWEEN the closing `</ShowcaseSection>` of Gradients and the closing `</div>` of the page wrapper. Use Edit tool to find the unique anchor string (the closing of Gradients section's `</ShowcaseSection>` followed by `    </div>` followed by `  );`) and insert the block before the wrapper `</div>`.

Insert this exact JSX (verbatim — preserves the prescribed visual hierarchy from UI-SPEC):

```tsx
      {/* --- Magic UI Primitives (Phase 1 deliverable) --- */}
      <ShowcaseSection
        id="magic-ui"
        title="Magic UI Primitives"
        description="Three vetted primitives tuned to the Raycast design language. These are the tuned versions — stock defaults have been overridden to use coral accent and Raycast motion timing."
      >
        {/* Magic Card demo — 2-column grid */}
        <ComponentGrid columns={2}>
          <div className="relative h-[200px] overflow-hidden rounded-lg">
            <MagicCard>
              <div className="flex h-full items-center justify-center">
                <Text size="sm" muted>
                  Magic Card — gradient mode
                </Text>
              </div>
            </MagicCard>
          </div>
          <div className="relative h-[200px] overflow-hidden rounded-lg">
            <MagicCard>
              <div className="flex h-full items-center justify-center">
                <Text size="sm" muted>
                  Magic Card — hover tracking
                </Text>
              </div>
            </MagicCard>
          </div>
        </ComponentGrid>

        {/* Border Beam demo — single card */}
        <div className="relative mt-6 h-[140px] overflow-hidden rounded-lg border border-border bg-surface">
          <BorderBeam />
          <div className="flex h-full items-center justify-center">
            <Text size="sm" muted>
              Border Beam — coral sweep
            </Text>
          </div>
        </div>

        {/* Shine Border demo — single card */}
        <div className="relative mt-6 h-[140px] overflow-hidden rounded-lg bg-surface">
          <ShineBorder />
          <div className="flex h-full items-center justify-center">
            <Text size="sm" muted>
              Shine Border — featured card treatment
            </Text>
          </div>
        </div>

        <div className="mt-6">
          <CodeBlock
            title="Tuned usage"
            code={`import { MagicCard, BorderBeam, ShineBorder } from "@/components/magic-ui";

// Magic Card — gradient mode (coral defaults already tuned)
<MagicCard>
  <div className="p-6">Content</div>
</MagicCard>

// Border Beam — drop into a relative + overflow-hidden parent
<div className="relative overflow-hidden rounded-lg border border-border">
  <BorderBeam />
  <div className="p-6">Card content</div>
</div>

// Shine Border — featured/active card treatment
<div className="relative overflow-hidden rounded-lg bg-surface">
  <ShineBorder />
  <div className="p-6">Featured tier</div>
</div>`}
          />
        </div>
      </ShowcaseSection>
```

**Critical notes:**
- DO NOT pass stock props at the usage sites. `<MagicCard>` with no props uses the tuned coral defaults from Plan 02. Same for `<BorderBeam />` and `<ShineBorder />`. UI-SPEC §"/showcase Route Addition Contract" makes this explicit.
- The wrapping `<div className="relative ... overflow-hidden rounded-lg ...">` is required for BorderBeam and ShineBorder per UI-SPEC §GlassPanel compatibility — both primitives are `absolute inset-0` overlays and need a `relative` parent with `overflow-hidden`.
- Use `bg-surface` (the Tailwind v4 token from `@theme`) for the BorderBeam and ShineBorder demo backgrounds — matches the Raycast surface token #18191a.
- The MagicCard demo uses NO `bg-surface` — the spotlight effect renders directly on the section background.
- All Text size="sm" muted labels are showcase-internal copy (not user-facing per UI-SPEC §Copywriting Contract).
- DO NOT modify any other section. The change is purely additive.

After both Edits land, run `pnpm run build` to confirm no compilation errors and `pnpm test src/components/magic-ui/__tests__/` to confirm all 4 Wave-0 invariants remain GREEN (they should — this plan modifies neither the magic-ui sources nor the marketing shell).
  </action>
  <verify>
    <automated>grep -q "from \"@/components/magic-ui\"" src/app/\(marketing\)/showcase/page.tsx && grep -q 'id="magic-ui"' src/app/\(marketing\)/showcase/page.tsx && grep -q "Magic UI Primitives" src/app/\(marketing\)/showcase/page.tsx && grep -q "<MagicCard>" src/app/\(marketing\)/showcase/page.tsx && grep -q "<BorderBeam" src/app/\(marketing\)/showcase/page.tsx && grep -q "<ShineBorder" src/app/\(marketing\)/showcase/page.tsx && pnpm run build && pnpm test src/components/magic-ui/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from "@/components/magic-ui"' src/app/(marketing)/showcase/page.tsx` returns 1 (single named-import line)
    - `grep -c 'MagicCard.*BorderBeam.*ShineBorder' src/app/(marketing)/showcase/page.tsx` returns ≥ 1 (all three on the import line OR all three appear at usage)
    - `grep -c 'id="magic-ui"' src/app/(marketing)/showcase/page.tsx` returns 1
    - `grep -c '"Magic UI Primitives"' src/app/(marketing)/showcase/page.tsx` returns 1
    - `grep -c '<MagicCard>' src/app/(marketing)/showcase/page.tsx` returns 2 (two MagicCard demos in the 2-column grid)
    - `grep -c '<BorderBeam' src/app/(marketing)/showcase/page.tsx` returns 1
    - `grep -c '<ShineBorder' src/app/(marketing)/showcase/page.tsx` returns 1
    - `grep -c 'Magic Card — gradient mode' src/app/(marketing)/showcase/page.tsx` returns 1
    - `grep -c 'Magic Card — hover tracking' src/app/(marketing)/showcase/page.tsx` returns 1
    - `grep -c 'Border Beam — coral sweep' src/app/(marketing)/showcase/page.tsx` returns 1
    - `grep -c 'Shine Border — featured card treatment' src/app/(marketing)/showcase/page.tsx` returns 1
    - `grep -c 'columns={2}' src/app/(marketing)/showcase/page.tsx` returns ≥ 1 (the MagicCard demo grid)
    - `grep -c 'bg-surface' src/app/(marketing)/showcase/page.tsx` returns ≥ 2 (BorderBeam + ShineBorder demo wrappers)
    - The MagicCard, BorderBeam, ShineBorder usage sites pass NO props (verify: `grep -E "<MagicCard\\s+[a-zA-Z]" src/app/(marketing)/showcase/page.tsx` returns no matches — only `<MagicCard>` plain opens. Same for BorderBeam/ShineBorder.)
    - Existing "Gradients" section is preserved: `grep -c '"Gradients"' src/app/(marketing)/showcase/page.tsx` returns ≥ 1
    - Existing section count increases by exactly 1: `grep -c '<ShowcaseSection' src/app/(marketing)/showcase/page.tsx` returns 1 more than before this plan
    - `pnpm run build` exits 0
    - `pnpm test src/components/magic-ui/__tests__/` reports 4/4 test files GREEN (no regression)
  </acceptance_criteria>
  <done>
    /showcase now has a "Magic UI Primitives" section at the bottom (id=magic-ui) demonstrating all 3 tuned primitives + a copy-paste CodeBlock. Existing showcase sections unchanged. Build clean. Plan 05's manual browser smoke check will eyeball-test this in the next wave.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server route → client hydration | The showcase route is RSC; the Magic UI primitives are `'use client'` subtrees that hydrate after RSC payload arrives. New client surface area is added (3 client subtrees: MagicCard ×2 + BorderBeam + ShineBorder). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I (Information disclosure) | Adding 4 client subtrees to /showcase increases hydration cost — could leak unstyled FOUC briefly | accept | RESEARCH §Pitfall 8 documents this as ASSUMED severity; /showcase is a developer-only route, not user-conversion. Plan 05's manual browser smoke check verifies the actual hydration behavior. |
| T-04-02 | T (Tampering) | The CodeBlock body contains template-literal JSX-as-string — if not properly escaped, could leak `${...}` interpolation accidentally | mitigate | The CodeBlock body uses a single backtick template literal with literal `${...}` only inside the inner code example as `${"..."}` SHOULD NOT appear. Verified: the provided code body uses no `${...}` interpolation; it is a literal string of imports/usage examples. The acceptance criteria's grep checks for specific strings ensure the executor pastes the exact body. |
| T-04-03 | D (Denial of service) | Magic Card / Border Beam motion runs at infinite repeat on every showcase visit | mitigate | Plan 02 ensured reduced-motion guards: MagicCard's hover spotlight skips when prefers-reduced-motion is set; BorderBeam returns null; ShineBorder uses motion-safe Tailwind class. /showcase visitors with reduced-motion preferences see static cards. |
| T-04-04 | T (Tampering) | A future contributor could pass stock props (e.g. `<MagicCard gradientFrom="#9E7AFF" />`) at this usage site and re-introduce violet | accept | Wave-0 invariant tests cover SOURCE defaults, not usage-site props. Vetting checklist Gate 1 (Color Audit) catches at code review. This is a process control, not a runtime gate. |
</threat_model>

<verification>
- `/showcase` page now includes a "Magic UI Primitives" section as its last section (after Gradients)
- All 3 primitives render in the demo (verify with browser smoke in Plan 05 — not in this plan)
- Existing showcase sections render unchanged
- `pnpm run build` exits 0
- All 4 Wave-0 invariant tests still GREEN (no regression on Plans 02/03 invariants)
- No file modifications outside `src/app/(marketing)/showcase/page.tsx`
</verification>

<success_criteria>
- SC-2 verification surface in place: the 3 Magic UI primitives are visible at `/showcase#magic-ui` and can be eyeball-tested for Raycast-nativeness
- Build clean
- CodeBlock provides Phase 2-6 executors with copy-paste-ready import + usage patterns (reducing exploration tax in subsequent phases)
- No regression on prior plan invariants
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-route-scaffolding/01-04-SUMMARY.md` covering:
- Confirmation of single-section addition (no other showcase sections modified)
- Final line count of `src/app/(marketing)/showcase/page.tsx` before + after the change
- Confirmation that all 4 Wave-0 tests remain GREEN after the change
- Note any unexpected hydration warning that surfaced during `pnpm run build` (Plan 05 inspects in the browser; this plan only inspects the build log)
</output>
