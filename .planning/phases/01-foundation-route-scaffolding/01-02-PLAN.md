---
phase: 01-foundation-route-scaffolding
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - package.json
  - pnpm-lock.yaml
  - components.json
  - src/components/magic-ui/magic-card.tsx
  - src/components/magic-ui/border-beam.tsx
  - src/components/magic-ui/shine-border.tsx
  - src/components/magic-ui/index.ts
  - src/app/globals.css
autonomous: true
requirements:
  - SC-2
  - SC-2.tuning

must_haves:
  truths:
    - "D-01: Three hero-targeted primitives (Magic Card, Border Beam, Shine Border) installed via shadcn CLI to src/components/magic-ui/ — the locked Phase 1 set that maps forward to Phase 2 hero, Phase 3 bento, Phase 6 pricing"
    - "D-02: Vetting checklist gates any additional Magic UI primitive — only the 3 D-01-locked primitives ship in this plan; further primitives require BRAND-BIBLE.md checklist pass before commit"
    - "D-03: Magic UI is the only external registry consumed this phase — components.json registries field declares ONLY @magicui (no Aceternity / Origin UI / Cult UI registries added)"
    - "D-04: Install path uses `npx shadcn@latest add @magicui/<name>` (or URL form) and components land in src/components/magic-ui/ — components.json aliases.ui is temporarily flipped to @/components/magic-ui for install, then restored to @/components/ui"
    - "next-themes dependency is installed before shadcn add @magicui/magic-card runs (Magic Card hard-codes useTheme from next-themes)"
    - "Magic Card, Border Beam, Shine Border source files land under src/components/magic-ui/ (NOT src/components/ui/) because components.json aliases.ui is flipped before install and restored after"
    - "All 3 primitives are tuned at install time: coral defaults replace stock violet/amber/black per UI-SPEC tuning tables (D-05 — no 'stock now, tune later')"
    - "@keyframes shine is present in src/app/globals.css after Shine Border install (auto-injected by CLI OR manually added per RESEARCH §Pitfall 6)"
    - "Barrel export src/components/magic-ui/index.ts exists with named exports for the 3 primitives"
    - "All 3 wave-0 invariant tests turn GREEN: magic-card.test.ts, border-beam.test.ts, shine-border.test.ts"
  artifacts:
    - path: "package.json"
      provides: "next-themes added to dependencies"
      contains: "next-themes"
    - path: "components.json"
      provides: "registries field set, aliases.ui restored to @/components/ui"
      contains: "@magicui"
    - path: "src/components/magic-ui/magic-card.tsx"
      provides: "Tuned Magic Card primitive (coral spotlight, coral gradient defaults)"
      min_lines: 80
    - path: "src/components/magic-ui/border-beam.tsx"
      provides: "Tuned Border Beam primitive (coral rgba sweep, 8s duration)"
      min_lines: 40
    - path: "src/components/magic-ui/shine-border.tsx"
      provides: "Tuned Shine Border primitive (coral palette array, 18s duration)"
      min_lines: 40
    - path: "src/components/magic-ui/index.ts"
      provides: "Barrel export for the 3 primitives"
      min_lines: 3
    - path: "src/app/globals.css"
      provides: "@keyframes shine and --animate-shine token registered"
      contains: "@keyframes shine"
  key_links:
    - from: "src/components/magic-ui/index.ts"
      to: "magic-card.tsx, border-beam.tsx, shine-border.tsx"
      via: "named re-export"
      pattern: "export\\s*\\{\\s*MagicCard\\s*\\}\\s*from\\s*[\"']\\./magic-card[\"']"
    - from: "src/components/magic-ui/shine-border.tsx"
      to: "src/app/globals.css"
      via: "animate-shine class consumes @keyframes shine"
      pattern: "animate-shine"
    - from: "src/components/magic-ui/magic-card.tsx"
      to: "package.json next-themes dep"
      via: "import useTheme from next-themes"
      pattern: "from\\s+[\"']next-themes[\"']"
---

<objective>
Install and tune the 3 Magic UI primitives Phase 1 locks (D-01: Magic Card, Border Beam, Shine Border) using the shadcn registry CLI (D-04), with coral/Raycast defaults baked in at install time (D-05). This plan executes the technical core of Phase 1: dependency hygiene, registry configuration, install, tune, barrel export, keyframe verification.

Purpose: SC-2 — At least one Magic UI primitive installed, vetted to feel native to the Raycast aesthetic (6% borders, Inter font, GlassPanel-compatible), exported through a documented integration path. This plan delivers all 3 locked primitives plus the integration path; Plan 04 consumes them in `/showcase`.

Output: A new `src/components/magic-ui/` directory with 3 tuned primitive source files + index barrel; `next-themes` added to package.json; `components.json` updated with `registries` field; `@keyframes shine` confirmed in `globals.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/MILESTONE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-route-scaffolding/01-CONTEXT.md
@.planning/phases/01-foundation-route-scaffolding/01-RESEARCH.md
@.planning/phases/01-foundation-route-scaffolding/01-UI-SPEC.md
@CLAUDE.md
@BRAND-BIBLE.md

<interfaces>
Existing patterns the executor should mirror:

From `src/components/ui/index.ts` (existing 36-component barrel — STUDY this pattern): shadcn-style barrel: re-exports named components from sibling files. Magic UI barrel should mirror exactly: named re-export, no default exports.

From `components.json` (current state — verified line-by-line):

```
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/app/globals.css",
    "baseColor": "slate",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui"
  }
}
```

From `src/app/globals.css` (existing keyframes pattern — verified):
- Lines 199-202: `--animate-*` token entries (accordion-down, accordion-up, gradient-x, shimmer)
- Lines 237, 246, 255, 267: existing `@keyframes` blocks at module scope
- The Shine Border install MUST result in `@keyframes shine` plus `--animate-shine` joining this pattern

From `CLAUDE.md` §Raycast Design Language Rules (NON-NEGOTIABLE values):
- Coral accent: `#FF7F50` (NOT oklch — Tailwind v4 oklch inaccuracy at L<0.15)
- Surface: `#18191a`
- Border: `rgba(255,255,255,0.06)` resting and `rgba(255,255,255,0.10)` hover
- Card radius: 12px

From `src/hooks/usePrefersReducedMotion.ts` (EXISTS — confirmed): canonical reduced-motion hook to wire into Magic Card and Border Beam during tuning if not already guarded internally.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add next-themes dependency and configure components.json for Magic UI namespace install</name>
  <read_first>
    - package.json (CONFIRM next-themes is NOT in dependencies today — verified absent)
    - components.json (CURRENT: aliases.ui = "@/components/ui" — must be flipped temporarily)
    - .planning/phases/01-foundation-route-scaffolding/01-RESEARCH.md §"Standard Stack" (next-themes rationale) and §"Pattern 1: shadcn Namespace Registry" (exact diff for components.json)
    - .planning/phases/01-foundation-route-scaffolding/01-RESEARCH.md §"Common Pitfalls" — Pitfall 5 (aliases.ui silently writes to wrong directory)
  </read_first>
  <files>
    - package.json
    - pnpm-lock.yaml (auto-updated by pnpm)
    - components.json
  </files>
  <action>
Step 1 — Install next-themes (Magic Card registry dep):

```
pnpm add next-themes
```

Expected outcome: `next-themes` lands under `dependencies` at a version `>= 0.4.x` (App Router compatible). `pnpm-lock.yaml` updates.

Step 2 — Edit `components.json` to:
1. Add a top-level `registries` field mapping `@magicui` to the Magic UI registry URL template.
2. Temporarily flip `aliases.ui` from `"@/components/ui"` to `"@/components/magic-ui"` so the shadcn CLI writes Magic UI primitives to the new directory (per RESEARCH §Pitfall 5).

Final content of `components.json` AFTER edit (use Edit tool, preserve $schema):

```
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/app/globals.css",
    "baseColor": "slate",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/magic-ui"
  },
  "registries": {
    "@magicui": "https://magicui.design/r/{name}.json"
  }
}
```

DO NOT commit `components.json` in this state — Task 5 (last task in this plan) flips `aliases.ui` BACK to `"@/components/ui"` after installs complete. This task and Task 5 form a bracket — both must complete before any other plan reads `components.json`.

Why both changes in the same task: keeping the two concerns paired makes the failure mode obvious (you either have both fields set or neither — no half-applied state).
  </action>
  <verify>
    <automated>grep -q '"next-themes"' package.json && grep -q '"@magicui"' components.json && grep -q '"ui": "@/components/magic-ui"' components.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"next-themes"' package.json` returns ≥ 1
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('components.json','utf-8')).registries['@magicui'])"` prints `https://magicui.design/r/{name}.json`
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('components.json','utf-8')).aliases.ui)"` prints `@/components/magic-ui` (temporary state — restored in Task 5)
    - `pnpm-lock.yaml` mentions `next-themes` (verify with `grep -c "next-themes:" pnpm-lock.yaml` ≥ 1)
    - No other files modified
  </acceptance_criteria>
  <done>
    next-themes added as a dependency; components.json has `registries["@magicui"]` and `aliases.ui` temporarily set to `@/components/magic-ui` for the install step.
  </done>
</task>

<task type="auto">
  <name>Task 2: shadcn add the 3 Magic UI primitives in a single CLI invocation, then immediately tune Magic Card source</name>
  <read_first>
    - .planning/phases/01-foundation-route-scaffolding/01-UI-SPEC.md §"Magic Card" (tuning table, GlassPanel compat note, reduced-motion spec, SSR safety note — exact prop diffs)
    - .planning/phases/01-foundation-route-scaffolding/01-RESEARCH.md §"Standard Stack" (CLI command form, expected install path, post-install file structure)
    - .planning/phases/01-foundation-route-scaffolding/01-RESEARCH.md §"Common Pitfalls" — Pitfall 1 (next-themes hydration), Pitfall 5 (aliases.ui), Pitfall 7 (motion/react vs framer-motion)
    - components.json (CONFIRM `aliases.ui` is `@/components/magic-ui` from Task 1)
    - src/hooks/usePrefersReducedMotion.ts (to confirm hook signature for the reduced-motion guard, only if Magic Card source ships without one)
  </read_first>
  <files>
    - src/components/magic-ui/magic-card.tsx (CREATED by CLI, then EDITED to apply tuning)
    - src/components/magic-ui/border-beam.tsx (CREATED by CLI — tuned in Task 3)
    - src/components/magic-ui/shine-border.tsx (CREATED by CLI — tuned in Task 4)
  </files>
  <action>
Step 1 — Run a single shadcn CLI invocation that installs all 3 primitives at once:

```
npx shadcn@latest add @magicui/magic-card @magicui/border-beam @magicui/shine-border
```

Expected outcome (verify before proceeding):
- `src/components/magic-ui/magic-card.tsx` exists
- `src/components/magic-ui/border-beam.tsx` exists
- `src/components/magic-ui/shine-border.tsx` exists
- NONE of these landed in `src/components/ui/` (if any did, the alias flip from Task 1 failed — fix `components.json` and rerun the CLI)

If the namespace form fails for any reason, fall back to the URL form (single invocation, same result):

```
npx shadcn@latest add https://magicui.design/r/magic-card.json https://magicui.design/r/border-beam.json https://magicui.design/r/shine-border.json
```

Step 2 — Tune `src/components/magic-ui/magic-card.tsx` per UI-SPEC §Magic Card tuning table.

Apply these exact prop-default diffs in the source file (use Edit tool — find each stock literal, replace with tuned value). Tuning is in-source, not at usage sites — D-05 requires the tuned version IS what lands:

| Prop | Stock default | Tuned value (NEW) |
|------|---------------|-------------------|
| `gradientFrom` | `"#9E7AFF"` | `"#FF7F50"` |
| `gradientTo` | `"#FE8BBB"` | `"rgba(255,127,80,0.15)"` |
| `gradientColor` | `"#262626"` | `"#18191a"` |
| `gradientOpacity` | `0.8` (numeric) | `0.6` (numeric) |

Other rules for Magic Card tuning:
- DO NOT remove the `mounted` state guard or the `useState(false)` + `useEffect(() => setMounted(true), [])` pattern — RESEARCH §Pitfall 1 confirms this is the hydration-mismatch fix.
- DO NOT remove `suppressHydrationWarning` if present.
- Verify the import line reads `from "motion/react"` (NOT `framer-motion`) — if it reads framer-motion (would be surprising — registry source uses motion/react per RESEARCH §State of the Art), correct it.
- Verify `import { useTheme } from "next-themes"` is present (confirms next-themes from Task 1 is wired).
- Prepend a header comment block to the file (UI-SPEC §Component Directory Contract):

```
/**
 * Magic UI — MagicCard
 * Source: https://magicui.design/r/magic-card.json
 * Installed: 2026-05-11 via npx shadcn@latest add @magicui/magic-card
 * Tuned for Virtuna Raycast design language per 01-UI-SPEC.md
 * 'use client' — requires pointer/motion interactivity
 */
```

Reduced-motion guard for Magic Card:
UI-SPEC §Magic Card reduced-motion spec states: "Executor must add: const prefersReducedMotion = usePrefersReducedMotion() (reuse src/hooks/usePrefersReducedMotion.ts verbatim). When true: disable mouseX/mouseY spring tracking, render static card."

Inspect the installed Magic Card source. If it already uses `useReducedMotion()` from `motion/react`, leave that in place — it satisfies the gate. If it has no guard at all, add:

```
import { usePrefersReducedMotion } from "@/hooks/usePrefersReducedMotion";

// inside the component, before motion handlers:
const prefersReducedMotion = usePrefersReducedMotion();

// guard the pointer handler:
function handleMouseMove(e) {
  if (prefersReducedMotion) return;
  // existing spring-tracking logic
}
```

(Keep the smallest possible change set — do not refactor the source structure.)

Do not yet tune Border Beam or Shine Border — those are Task 3 and Task 4 to keep each commit atomic (one primitive per commit if the executor commits incrementally).
  </action>
  <verify>
    <automated>test -f src/components/magic-ui/magic-card.tsx && test -f src/components/magic-ui/border-beam.tsx && test -f src/components/magic-ui/shine-border.tsx && grep -q "#FF7F50" src/components/magic-ui/magic-card.tsx && ! grep -q "#9E7AFF" src/components/magic-ui/magic-card.tsx && ! grep -q "#FE8BBB" src/components/magic-ui/magic-card.tsx && pnpm test src/components/magic-ui/__tests__/magic-card.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/magic-ui/magic-card.tsx` exists
    - File `src/components/magic-ui/border-beam.tsx` exists (stock — tuned in Task 3)
    - File `src/components/magic-ui/shine-border.tsx` exists (stock — tuned in Task 4)
    - `grep -c "#FF7F50" src/components/magic-ui/magic-card.tsx` ≥ 1
    - `grep -c "#9E7AFF" src/components/magic-ui/magic-card.tsx` = 0
    - `grep -c "#FE8BBB" src/components/magic-ui/magic-card.tsx` = 0
    - `grep -c "rgba(255,127,80,0.15)" src/components/magic-ui/magic-card.tsx` ≥ 1
    - `grep -c "#18191a" src/components/magic-ui/magic-card.tsx` ≥ 1
    - `grep -c "useState(false)" src/components/magic-ui/magic-card.tsx` ≥ 1 (mounted guard preserved)
    - `grep -c "setMounted(true)" src/components/magic-ui/magic-card.tsx` ≥ 1
    - `grep -E "from\s+[\"']framer-motion[\"']" src/components/magic-ui/magic-card.tsx` returns no matches
    - `grep -E "from\s+[\"']next-themes[\"']" src/components/magic-ui/magic-card.tsx` returns ≥ 1 match
    - `pnpm test src/components/magic-ui/__tests__/magic-card.test.ts` reports all assertions PASS (RED → GREEN transition)
    - NO files written to `src/components/ui/` (verify: `ls src/components/ui/magic-card.tsx 2>/dev/null` returns nothing)
  </acceptance_criteria>
  <done>
    Magic Card installed at `src/components/magic-ui/magic-card.tsx`, tuned with coral defaults and `#18191a` surface, hydration guard preserved. The wave-0 invariant test `magic-card.test.ts` now passes (RED → GREEN). Border Beam and Shine Border sources exist on disk but are still stock (tuned in next two tasks).
  </done>
</task>

<task type="auto">
  <name>Task 3: Tune Border Beam source to coral defaults + 8s duration</name>
  <read_first>
    - src/components/magic-ui/border-beam.tsx (the just-installed stock source — read to find exact lines to edit)
    - .planning/phases/01-foundation-route-scaffolding/01-UI-SPEC.md §"Border Beam" (tuning table, reduced-motion spec, GlassPanel compat)
    - .planning/phases/01-foundation-route-scaffolding/01-RESEARCH.md §"Common Pitfalls" — Pitfall 7 (motion/react import path)
  </read_first>
  <files>
    - src/components/magic-ui/border-beam.tsx
  </files>
  <action>
Apply the following exact prop-default diffs in the source file (use Edit tool — find each stock literal, replace with tuned value):

| Prop | Stock default | Tuned value (NEW) |
|------|---------------|-------------------|
| `colorFrom` | `"#ffaa40"` | `"rgba(255,127,80,0.9)"` |
| `colorTo` | `"#9c40ff"` | `"rgba(255,127,80,0)"` |
| `duration` | `6` (numeric, seconds) | `8` (numeric) |
| `size` | `50` (numeric) | `40` (numeric) |
| `borderWidth` | `1` (numeric) | `1` (unchanged — but verify present) |
| `delay` | `0` (numeric) | `0` (unchanged) |

Other rules for Border Beam tuning:
- Verify the import line reads `from "motion/react"` (NOT `framer-motion`) — RESEARCH §Pitfall 7. If wrong, correct it.
- Reduced-motion guard (per UI-SPEC §Border Beam): the source uses `repeat: Infinity` on motion animation. Add:

```
import { useReducedMotion } from "motion/react";

// at the top of the component body:
const reducedMotion = useReducedMotion();
if (reducedMotion) return null;
```

(Use `useReducedMotion` from motion/react — same package already imported by the source. This avoids adding another import path.)

- Prepend a header comment block (per UI-SPEC §Component Directory Contract):

```
/**
 * Magic UI — BorderBeam
 * Source: https://magicui.design/r/border-beam.json
 * Installed: 2026-05-11 via npx shadcn@latest add @magicui/border-beam
 * Tuned for Virtuna Raycast design language per 01-UI-SPEC.md
 * 'use client' — requires motion/react animation primitives
 */
```

Why these specific values: UI-SPEC tables prescribe coral-to-transparent sweep (replaces amber→violet) at Raycast pace (8s vs stock 6s — stock 6s reads as too kinetic for the Linear/Raycast idiom). Size 40 reduces eye-catching mass without losing visibility. The reduced-motion `return null` makes the beam invisible (acceptable per UI-SPEC; the underlying 1px border still renders from the parent card).
  </action>
  <verify>
    <automated>grep -q "255,127,80" src/components/magic-ui/border-beam.tsx && ! grep -qi "#ffaa40" src/components/magic-ui/border-beam.tsx && ! grep -qi "#9c40ff" src/components/magic-ui/border-beam.tsx && ! grep -qE "from\s+[\"']framer-motion[\"']" src/components/magic-ui/border-beam.tsx && pnpm test src/components/magic-ui/__tests__/border-beam.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "255,127,80" src/components/magic-ui/border-beam.tsx` ≥ 2 (colorFrom + colorTo both contain coral rgba)
    - `grep -ci "#ffaa40" src/components/magic-ui/border-beam.tsx` = 0
    - `grep -ci "#9c40ff" src/components/magic-ui/border-beam.tsx` = 0
    - `grep -c "rgba(255,127,80,0)" src/components/magic-ui/border-beam.tsx` ≥ 1 (colorTo fades to transparent)
    - `grep -E "from\s+[\"']framer-motion[\"']" src/components/magic-ui/border-beam.tsx` returns no matches
    - `grep -E "from\s+[\"']motion/react[\"']" src/components/magic-ui/border-beam.tsx` returns ≥ 1 match
    - `grep -c "useReducedMotion" src/components/magic-ui/border-beam.tsx` ≥ 1
    - `pnpm test src/components/magic-ui/__tests__/border-beam.test.ts` reports all assertions PASS (RED → GREEN)
  </acceptance_criteria>
  <done>
    Border Beam tuned with coral rgba sweep, 8s duration, reduced-motion guard, motion/react import path. Wave-0 invariant test passes.
  </done>
</task>

<task type="auto">
  <name>Task 4: Tune Shine Border source + verify (or inject) @keyframes shine in globals.css</name>
  <read_first>
    - src/components/magic-ui/shine-border.tsx (the just-installed stock source)
    - src/app/globals.css (existing keyframes/animate pattern — lines 199-202, 237, 246, 255, 267 — to mirror the pattern when adding `@keyframes shine`)
    - .planning/phases/01-foundation-route-scaffolding/01-UI-SPEC.md §"Shine Border" (tuning table, @keyframes injection note, reduced-motion via motion-safe)
    - .planning/phases/01-foundation-route-scaffolding/01-RESEARCH.md §"Common Pitfalls" — Pitfall 6 (@keyframes shine not auto-injected by shadcn CLI)
  </read_first>
  <files>
    - src/components/magic-ui/shine-border.tsx
    - src/app/globals.css (only edited IF `@keyframes shine` was not auto-injected by the CLI in Task 2)
  </files>
  <action>
Step 1 — Tune `src/components/magic-ui/shine-border.tsx` with these prop-default diffs:

| Prop | Stock default | Tuned value (NEW) |
|------|---------------|-------------------|
| `shineColor` | `"#000000"` (string) OR `"black"` (whatever the source has) | `["rgba(255,127,80,0.8)", "rgba(255,127,80,0.15)", "rgba(255,127,80,0.8)"]` (array literal — 3 stops, gradient arc) |
| `duration` | `14` (numeric seconds) | `18` (numeric) |
| `borderWidth` | `1` (numeric) | `1` (unchanged — verify present) |

Other rules:
- The component already uses `motion-safe:animate-shine` Tailwind class for reduced-motion suppression (per UI-SPEC §Shine Border reduced-motion spec) — DO NOT change that. It works as designed.
- Verify the source has `'use client'` directive — if missing (unlikely), add it.
- Prepend a header comment block:

```
/**
 * Magic UI — ShineBorder
 * Source: https://magicui.design/r/shine-border.json
 * Installed: 2026-05-11 via npx shadcn@latest add @magicui/shine-border
 * Tuned for Virtuna Raycast design language per 01-UI-SPEC.md
 * 'use client' — consumes @keyframes shine via motion-safe:animate-shine
 */
```

Step 2 — Verify `@keyframes shine` was injected into `src/app/globals.css` by the shadcn CLI in Task 2:

```
grep -c "@keyframes shine" src/app/globals.css
grep -c "animate-shine" src/app/globals.css
```

Both counts must be ≥ 1. If either returns 0, the shadcn CLI did NOT auto-inject the registry's `cssVars.theme` and `css` blocks (RESEARCH §Pitfall 6). Manual injection:

1. Open `src/app/globals.css`.
2. Inside the existing `@theme` block (where `--animate-shimmer` and `--animate-gradient-x` live around lines 199-202), append the line:

```
--animate-shine: shine var(--duration) infinite linear;
```

3. After the last existing `@keyframes` block (around line 267, after `@keyframes shimmer`), at module scope (NOT inside `@theme`), append:

```
@keyframes shine {
  0% { background-position: 0% 0%; }
  50% { background-position: 100% 100%; }
  to { background-position: 0% 0%; }
}
```

Choose insertion point that matches the existing pattern verbatim — do not change indentation, spacing, or surrounding rules.

After Task 2's CLI run, in many shadcn v4 setups both the `@theme` token line and the `@keyframes` block ARE auto-injected. The grep check determines whether manual fallback is needed. Be explicit in the SUMMARY.md whether CLI-auto or manual was used.
  </action>
  <verify>
    <automated>grep -q "@keyframes shine" src/app/globals.css && grep -q "animate-shine" src/app/globals.css && grep -q "255,127,80,0.8" src/components/magic-ui/shine-border.tsx && ! grep -qE "shineColor\s*[:=]\s*[\"']#000000[\"']" src/components/magic-ui/shine-border.tsx && pnpm test src/components/magic-ui/__tests__/shine-border.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@keyframes shine" src/app/globals.css` ≥ 1 (either CLI-injected or manually added)
    - `grep -c "animate-shine" src/app/globals.css` ≥ 1 (--animate-shine token registered)
    - `grep -c "255,127,80,0.8" src/components/magic-ui/shine-border.tsx` ≥ 1 (coral palette array first stop)
    - `grep -c "255,127,80,0.15" src/components/magic-ui/shine-border.tsx` ≥ 1 (coral palette array middle stop)
    - `grep -cE "shineColor\s*[:=]\s*[\"']#000000[\"']" src/components/magic-ui/shine-border.tsx` = 0
    - `grep -c "use client" src/components/magic-ui/shine-border.tsx` ≥ 1
    - `pnpm test src/components/magic-ui/__tests__/shine-border.test.ts` reports all assertions PASS (RED → GREEN)
  </acceptance_criteria>
  <done>
    Shine Border tuned with coral palette array + 18s duration; `@keyframes shine` confirmed present in globals.css (CLI auto or manual fallback). Wave-0 invariant test passes.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create magic-ui barrel export, restore components.json aliases.ui, run build + full Wave-0 test suite</name>
  <read_first>
    - src/components/ui/index.ts (mirror this barrel pattern — named re-exports, no defaults)
    - src/components/magic-ui/magic-card.tsx (CONFIRM the exported component name is `MagicCard`)
    - src/components/magic-ui/border-beam.tsx (CONFIRM the exported component name is `BorderBeam`)
    - src/components/magic-ui/shine-border.tsx (CONFIRM the exported component name is `ShineBorder`)
    - components.json (CONFIRM `aliases.ui` is still `@/components/magic-ui` from Task 1 — must be flipped back)
    - .planning/phases/01-foundation-route-scaffolding/01-UI-SPEC.md §"Component Directory Contract" (barrel export contract — line-by-line spec)
  </read_first>
  <files>
    - src/components/magic-ui/index.ts (NEW)
    - components.json (restored)
  </files>
  <action>
Step 1 — Create `src/components/magic-ui/index.ts` (use Write tool — new file):

```
// Magic UI — barrel export
// Three vetted primitives tuned to the Virtuna Raycast design language.
// Tuning lives in the individual source files; this barrel is a re-export layer only.
// Phase 1 of Landing Page Redesign milestone (2026-05-11).

export { MagicCard } from "./magic-card";
export { BorderBeam } from "./border-beam";
export { ShineBorder } from "./shine-border";
```

If any of the three source files exports the component under a different name (e.g. `MagicCardComponent` instead of `MagicCard`), inspect the source's `export function ...` line — the barrel must use the actual exported name. If the source uses `export default`, change the export to be named (`export function MagicCard ...`) for consistency with the rest of the codebase pattern; verify before/after with a grep.

Step 2 — Restore `components.json` `aliases.ui` to `"@/components/ui"` (use Edit tool):

```
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/app/globals.css",
    "baseColor": "slate",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui"
  },
  "registries": {
    "@magicui": "https://magicui.design/r/{name}.json"
  }
}
```

The `registries` field STAYS — it's persistent infrastructure for future Magic UI installs. Only `aliases.ui` is restored.

Step 3 — Build + run all 3 Magic UI invariant tests to confirm they pass GREEN:

```
pnpm run build
pnpm test src/components/magic-ui/__tests__/magic-card.test.ts src/components/magic-ui/__tests__/border-beam.test.ts src/components/magic-ui/__tests__/shine-border.test.ts
```

Build must exit 0. All 3 invariant tests must report PASS.

The fourth test (`marketing-shell.test.ts`) is still RED at this point — that one belongs to Plan 03 and is expected to remain RED until Plan 03 lands. DO NOT modify Plan 03's files from inside this plan.

Cache hygiene (RESEARCH §Pitfall 4): if the build complains about stale CSS or `@keyframes shine` missing despite Task 4 confirming it's there, kill the dev server, remove `.next/` and `node_modules/.cache/`, retry. Do not paper over with `--no-cache` flags — the failure mode is real and well-documented.
  </action>
  <verify>
    <automated>test -f src/components/magic-ui/index.ts && grep -q "MagicCard" src/components/magic-ui/index.ts && grep -q "BorderBeam" src/components/magic-ui/index.ts && grep -q "ShineBorder" src/components/magic-ui/index.ts && node -e "process.exit(JSON.parse(require('fs').readFileSync('components.json','utf-8')).aliases.ui === '@/components/ui' ? 0 : 1)" && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/magic-ui/index.ts` exists with named exports for `MagicCard`, `BorderBeam`, `ShineBorder`
    - `grep -c "^export {" src/components/magic-ui/index.ts` returns ≥ 3 (one re-export per primitive)
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('components.json','utf-8')).aliases.ui)"` prints `@/components/ui` (restored)
    - `node -e "console.log(JSON.parse(require('fs').readFileSync('components.json','utf-8')).registries['@magicui'])"` still prints the registry template URL (persistent)
    - `pnpm run build` exits 0 (no TypeScript errors, no missing-import errors, no missing-keyframe warnings)
    - `pnpm test src/components/magic-ui/__tests__/magic-card.test.ts src/components/magic-ui/__tests__/border-beam.test.ts src/components/magic-ui/__tests__/shine-border.test.ts` reports 3 files all PASS (GREEN)
    - `pnpm test src/components/magic-ui/__tests__/marketing-shell.test.ts` reports FAIL (still RED — Plan 03's responsibility — this is the expected state)
    - No imports from `src/components/landing/` introduced (verify: `grep -r "components/landing" src/components/magic-ui/` returns no matches)
  </acceptance_criteria>
  <done>
    Barrel export shipped; components.json `aliases.ui` restored to `@/components/ui` (with `registries` field persisting); production build clean; 3 of 4 wave-0 tests now GREEN (the fourth — marketing-shell — turns GREEN in Plan 03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time (npm registry → repo) | shadcn CLI fetches Magic UI registry JSON over HTTPS and writes editable source files into `src/components/magic-ui/`. Untrusted vendor source crosses this boundary. |
| Runtime (server) | None added — Magic UI primitives are pure client components; no new server routes, no new env vars, no new I/O. |
| Runtime (client) | Magic UI primitives execute pointer events, motion/react springs, and `useTheme()` reads. All within React's standard sandbox. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T (Tampering) | Magic UI registry payload — supply-chain risk (magicui.design serves tampered JSON) | mitigate | UI-SPEC §Registry Safety table records `view passed — no flags` for all 3 primitives as of 2026-05-11. Task 2 + Task 3 + Task 4 each include grep-based content checks that confirm tuned source contains no `fetch()`, no `process.env`, no `eval()`. Plan 03's vetting-checklist commit then locks Gate 9 (Security Scan) as a permanent invariant. |
| T-02-02 | T (Tampering) | shadcn CLI itself (downstream of npx) | accept | Mitigated by `npx shadcn@latest` being a major-vendor npm package with published checksums; same trust model as the rest of the repo's npm deps. Lockfile pins resolved versions. |
| T-02-03 | I (Information disclosure) | next-themes — could it leak system theme to a server? | accept | next-themes is pure client-side localStorage + matchMedia; no telemetry, no fetch. App is dark-mode-only so `useTheme()` always resolves dark, but no PII transit either way. |
| T-02-04 | D (Denial of service) | Border Beam / Shine Border `repeat: Infinity` animations | mitigate | Both primitives' tuning includes a reduced-motion guard (Task 3: `if (reducedMotion) return null`; Task 4: `motion-safe:animate-shine` Tailwind class). Users with `prefers-reduced-motion: reduce` never see the looping motion. |
| T-02-05 | T (Tampering) | `@keyframes shine` in globals.css could conflict with existing keyframes | mitigate | Task 4 acceptance criteria verifies `@keyframes shine` is a NEW block (no replacement of an existing `shine` keyframe — globals.css has no existing `shine` reference before this plan; verified via grep). |
</threat_model>

<verification>
- `next-themes` present in `package.json` and `pnpm-lock.yaml`
- `components.json` has `registries["@magicui"]` AND `aliases.ui = "@/components/ui"` (registries persistent, aliases restored)
- All 3 primitive source files exist under `src/components/magic-ui/` (NOT `src/components/ui/`)
- All 3 primitives have coral defaults baked in (no stock violet/amber/black literal remains)
- All 3 primitives import from `motion/react` (NOT `framer-motion`) — RESEARCH §Pitfall 7
- All 3 primitives include reduced-motion guards (per UI-SPEC reduced-motion specs)
- `@keyframes shine` present in `src/app/globals.css`
- `src/components/magic-ui/index.ts` exports all 3 named components
- `pnpm run build` exits 0
- 3 of 4 Wave-0 invariant tests now PASS (`marketing-shell.test.ts` is Plan 03's responsibility)
</verification>

<success_criteria>
- SC-2 satisfied: at least 1 Magic UI primitive (all 3, in fact) is installed, tuned to Raycast tokens, exported through `src/components/magic-ui/index.ts`. Future phases (2 hero, 3 bento, 6 pricing) can `import { MagicCard, BorderBeam, ShineBorder } from "@/components/magic-ui"`.
- SC-2.tuning satisfied: stock violet/amber/black defaults are gone; coral defaults are the new defaults at the source level, not at consumption sites.
- All 3 Magic UI Wave-0 invariant tests are GREEN.
- Build is clean.
- `components.json` left in a state safe for future shadcn-native adds (aliases.ui restored to `@/components/ui`).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-route-scaffolding/01-02-SUMMARY.md` covering:
- next-themes installed at version X.Y.Z (record the resolved version)
- components.json diffs (alias flip + registries field) and confirmation of restoration
- Per-primitive tuning diff (which stock literal → which tuned value, file:line for each)
- Whether @keyframes shine was auto-injected by the CLI or required manual fallback
- Whether reduced-motion guards were already present in the registry source or needed to be added by tuning
- Final test status: 3/4 GREEN, 1/4 RED (marketing-shell — by design, Plan 03 closes it)
- pnpm-lock.yaml diff scope
</output>
</content>
</invoke>