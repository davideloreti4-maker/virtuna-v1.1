---
phase: 01-foundation-route-scaffolding
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/magic-ui/__tests__/magic-card.test.ts
  - src/components/magic-ui/__tests__/border-beam.test.ts
  - src/components/magic-ui/__tests__/shine-border.test.ts
  - src/components/magic-ui/__tests__/marketing-shell.test.ts
autonomous: true
requirements:
  - SC-2
  - SC-2.tuning
  - SC-1

must_haves:
  truths:
    - "Wave 0 lays down 4 small invariant tests that fail today and will pass after Plan 02 + Plan 03 complete"
    - "Tests assert source-file content (string match), not React render — compatible with vitest node environment + .test.ts include glob"
    - "Tests run via `pnpm test` and finish in < 5s"
  artifacts:
    - path: "src/components/magic-ui/__tests__/magic-card.test.ts"
      provides: "Lock — tuned Magic Card source contains gradientFrom = \"#FF7F50\""
    - path: "src/components/magic-ui/__tests__/border-beam.test.ts"
      provides: "Lock — tuned Border Beam source contains coral rgba in colorFrom"
    - path: "src/components/magic-ui/__tests__/shine-border.test.ts"
      provides: "Lock — tuned Shine Border source has coral palette array in shineColor"
    - path: "src/components/magic-ui/__tests__/marketing-shell.test.ts"
      provides: "Lock — src/app/(marketing)/page.tsx contains no plagiarized AS imports/text"
  key_links:
    - from: "src/components/magic-ui/__tests__/*.test.ts"
      to: "src/components/magic-ui/*.tsx"
      via: "fs.readFileSync + string assertion"
      pattern: "readFileSync.*magic-card\\.tsx"
    - from: "src/components/magic-ui/__tests__/marketing-shell.test.ts"
      to: "src/app/(marketing)/page.tsx"
      via: "fs.readFileSync + string assertion"
      pattern: "Artificial Societies"
---

<objective>
Lay down 4 small invariant tests (Wave 0 scaffolds per VALIDATION.md) that fail today and pass after Plan 02 (Magic UI install + tune) and Plan 03 (shell + cleanup) complete. Tests use file-content assertions (not React render) because `vitest.config.ts` is set to `environment: "node"` and `include: ["src/**/*.test.ts"]` — DOM/component renders would not run under current config and would block on a Vitest reconfig that Phase 1 has no mandate to perform.

Purpose: Encode the tuning + cleanup invariants from the start so the executor cannot accidentally ship stock defaults or plagiarized AS chrome — and so regressions in later phases trip an automated test instead of silent visual drift.
Output: 4 `.test.ts` files in `src/components/magic-ui/__tests__/` that fail RED today (files don't exist yet) and turn GREEN after Plans 02 + 03 land.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/MILESTONE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-route-scaffolding/01-CONTEXT.md
@.planning/phases/01-foundation-route-scaffolding/01-VALIDATION.md
@vitest.config.ts

<interfaces>
<!-- Vitest config (verified): node environment, include glob is src/**/*.test.ts -->
<!-- DOM/JSX tests would require config changes — out of Phase 1 scope. -->
<!-- These tests assert file CONTENTS via fs.readFileSync + expect(string).toContain(...). -->
<!-- The directory src/components/magic-ui/ does NOT exist yet — tests must guard for that. -->

From vitest.config.ts:
```
test: {
  globals: true,
  environment: "node",
  include: ["src/**/*.test.ts"],
}
```

From src/components/magic-ui/ (DOES NOT EXIST YET — created by Plan 02):
- magic-card.tsx — tuned source file (created by Plan 02)
- border-beam.tsx — tuned source file (created by Plan 02)
- shine-border.tsx — tuned source file (created by Plan 02)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 3 Magic UI primitive invariant tests + 1 shell invariant test</name>
  <read_first>
    - vitest.config.ts (CONFIRM: environment: "node", include: ["src/**/*.test.ts"]) — DO NOT modify this file in Phase 1
    - .planning/phases/01-foundation-route-scaffolding/01-VALIDATION.md (§Wave 0 Requirements section)
    - .planning/phases/01-foundation-route-scaffolding/01-UI-SPEC.md (§Magic Card / Border Beam / Shine Border tuned defaults tables — these are the assertions the tests encode)
    - .planning/phases/01-foundation-route-scaffolding/01-RESEARCH.md (§Validation Architecture, §Wave 0 Gaps)
    - src/app/(marketing)/page.tsx (CONFIRM still contains plagiarized "HeroSection", "BackersSection", "FAQSection" imports from "@/components/landing" — these are what marketing-shell.test.ts asserts get DELETED)
    - One existing test file for style reference: src/lib/engine/__tests__/rules.test.ts
  </read_first>
  <files>
    - src/components/magic-ui/__tests__/magic-card.test.ts (NEW)
    - src/components/magic-ui/__tests__/border-beam.test.ts (NEW)
    - src/components/magic-ui/__tests__/shine-border.test.ts (NEW)
    - src/components/magic-ui/__tests__/marketing-shell.test.ts (NEW)
  </files>
  <action>
Create the directory `src/components/magic-ui/__tests__/` (use `mkdir -p`). Then create 4 vitest test files using node-environment-compatible string assertions on file contents. **Do NOT use React Testing Library, JSX, or @testing-library/react** — they would not run under the current vitest config.

**File 1: `src/components/magic-ui/__tests__/magic-card.test.ts`** — write verbatim:

```ts
import { describe, it, expect } from "vitest";
import { readFileSync, existsSync } from "node:fs";
import { resolve } from "node:path";

/**
 * Wave 0 invariant: Magic Card source MUST be installed at
 * src/components/magic-ui/magic-card.tsx AND tuned to coral defaults
 * per UI-SPEC §Magic Card tuning table (D-05).
 *
 * Status before Plan 02: RED (file does not exist).
 * Status after  Plan 02: GREEN (file exists, contains tuned coral defaults).
 */

const SOURCE_PATH = resolve(__dirname, "..", "magic-card.tsx");

describe("Magic Card — tuning invariants (Wave 0 lock)", () => {
  it("source file exists at src/components/magic-ui/magic-card.tsx", () => {
    expect(existsSync(SOURCE_PATH)).toBe(true);
  });

  it("ships with 'use client' directive (interactive primitive)", () => {
    const src = readFileSync(SOURCE_PATH, "utf-8");
    expect(src).toMatch(/^"use client"|^'use client'/m);
  });

  it("default gradientFrom is tuned to coral (#FF7F50) — NOT stock violet (#9E7AFF)", () => {
    const src = readFileSync(SOURCE_PATH, "utf-8");
    // Pass: coral hex literal appears as a default
    expect(src).toContain("#FF7F50");
    // Fail-fast: stock violet must NOT remain
    expect(src).not.toContain("#9E7AFF");
  });

  it("default gradientTo does NOT contain stock pink (#FE8BBB)", () => {
    const src = readFileSync(SOURCE_PATH, "utf-8");
    expect(src).not.toContain("#FE8BBB");
  });

  it("preserves next-themes mounted-state hydration guard (do not strip during tuning)", () => {
    const src = readFileSync(SOURCE_PATH, "utf-8");
    // The mounted-state guard prevents hydration mismatch per RESEARCH §Pitfall 1
    expect(src).toMatch(/useState\(false\)/);
    expect(src).toMatch(/setMounted\(true\)/);
  });
});
```

**File 2: `src/components/magic-ui/__tests__/border-beam.test.ts`** — write verbatim:

```ts
import { describe, it, expect } from "vitest";
import { readFileSync, existsSync } from "node:fs";
import { resolve } from "node:path";

/**
 * Wave 0 invariant: Border Beam source MUST be installed and tuned
 * per UI-SPEC §Border Beam tuning table.
 * Status before Plan 02: RED. Status after Plan 02: GREEN.
 */

const SOURCE_PATH = resolve(__dirname, "..", "border-beam.tsx");

describe("Border Beam — tuning invariants (Wave 0 lock)", () => {
  it("source file exists at src/components/magic-ui/border-beam.tsx", () => {
    expect(existsSync(SOURCE_PATH)).toBe(true);
  });

  it("ships with 'use client' directive", () => {
    const src = readFileSync(SOURCE_PATH, "utf-8");
    expect(src).toMatch(/^"use client"|^'use client'/m);
  });

  it("colorFrom default contains coral rgba — NOT stock amber (#ffaa40)", () => {
    const src = readFileSync(SOURCE_PATH, "utf-8");
    // Coral rgba signature — UI-SPEC prescribes rgba(255,127,80, ...)
    expect(src).toMatch(/255\s*,\s*127\s*,\s*80/);
    // Fail-fast: stock amber must NOT remain (case-insensitive)
    expect(src.toLowerCase()).not.toContain("#ffaa40");
  });

  it("colorTo default does NOT contain stock violet (#9c40ff)", () => {
    const src = readFileSync(SOURCE_PATH, "utf-8");
    expect(src.toLowerCase()).not.toContain("#9c40ff");
  });

  it("imports from motion/react (NOT framer-motion) — RESEARCH §Pitfall 7", () => {
    const src = readFileSync(SOURCE_PATH, "utf-8");
    expect(src).toMatch(/from\s+["']motion\/react["']/);
    expect(src).not.toMatch(/from\s+["']framer-motion["']/);
  });
});
```

**File 3: `src/components/magic-ui/__tests__/shine-border.test.ts`** — write verbatim:

```ts
import { describe, it, expect } from "vitest";
import { readFileSync, existsSync } from "node:fs";
import { resolve } from "node:path";

/**
 * Wave 0 invariant: Shine Border source MUST be installed and tuned
 * per UI-SPEC §Shine Border tuning table. Also asserts @keyframes shine
 * is present in globals.css after Plan 02 (registry injection fallback).
 */

const SOURCE_PATH = resolve(__dirname, "..", "shine-border.tsx");
const GLOBALS_CSS = resolve(__dirname, "..", "..", "..", "app", "globals.css");

describe("Shine Border — tuning invariants (Wave 0 lock)", () => {
  it("source file exists at src/components/magic-ui/shine-border.tsx", () => {
    expect(existsSync(SOURCE_PATH)).toBe(true);
  });

  it("ships with 'use client' directive", () => {
    const src = readFileSync(SOURCE_PATH, "utf-8");
    expect(src).toMatch(/^"use client"|^'use client'/m);
  });

  it("shineColor default contains coral rgba — NOT stock black (#000000)", () => {
    const src = readFileSync(SOURCE_PATH, "utf-8");
    expect(src).toMatch(/255\s*,\s*127\s*,\s*80/);
    // The stock default was a black string default
    expect(src).not.toMatch(/shineColor\s*[:=]\s*["']#000000["']/);
    expect(src).not.toMatch(/shineColor\s*[:=]\s*["']black["']/);
  });

  it("shineColor default is array-form (gradient arc, not single color)", () => {
    const src = readFileSync(SOURCE_PATH, "utf-8");
    // Loose match — looks for an array literal containing coral rgba
    expect(src).toMatch(/\[\s*["'`][^"'`]*255[^"'`]*127[^"'`]*80[^"'`]*["'`]/);
  });

  it("@keyframes shine is registered in src/app/globals.css (RESEARCH §Pitfall 6)", () => {
    const css = readFileSync(GLOBALS_CSS, "utf-8");
    expect(css).toMatch(/@keyframes\s+shine\s*\{/);
  });
});
```

**File 4: `src/components/magic-ui/__tests__/marketing-shell.test.ts`** — write verbatim:

```ts
import { describe, it, expect } from "vitest";
import { readFileSync, existsSync } from "node:fs";
import { resolve } from "node:path";

/**
 * Wave 0 invariant: src/app/(marketing)/page.tsx renders an empty
 * <main> with no imports from @/components/landing (AS-plagiarized).
 *
 * Status before Plan 03: RED — current page.tsx imports 7 AS sections.
 * Status after  Plan 03: GREEN — empty <main> with phase stub comments.
 *
 * Also asserts: marketing layout metadata title is "Virtuna" not
 * "Artificial Societies"; pricing page no longer imports FAQSection
 * from landing; src/components/landing/ directory is deleted.
 */

const ROOT = resolve(__dirname, "..", "..", "..", "..");
const HOME_PAGE = resolve(ROOT, "src", "app", "(marketing)", "page.tsx");
const LAYOUT = resolve(ROOT, "src", "app", "(marketing)", "layout.tsx");
const PRICING_PAGE = resolve(ROOT, "src", "app", "(marketing)", "pricing", "page.tsx");
const LANDING_DIR = resolve(ROOT, "src", "components", "landing");

describe("Marketing shell — Phase 1 cleanup invariants (Wave 0 lock)", () => {
  it("src/app/(marketing)/page.tsx has NO import from @/components/landing", () => {
    const src = readFileSync(HOME_PAGE, "utf-8");
    expect(src).not.toMatch(/from\s+["']@\/components\/landing["']/);
  });

  it("src/app/(marketing)/page.tsx renders <main> with min-h-screen + bg-background", () => {
    const src = readFileSync(HOME_PAGE, "utf-8");
    expect(src).toMatch(/<main\b/);
    expect(src).toContain("min-h-screen");
    expect(src).toContain("bg-background");
  });

  it("src/app/(marketing)/page.tsx contains no AS-plagiarized section names", () => {
    const src = readFileSync(HOME_PAGE, "utf-8");
    // These are the AS-plagiarized section component names
    expect(src).not.toContain("HeroSection");
    expect(src).not.toContain("BackersSection");
    expect(src).not.toContain("FeaturesSection");
    expect(src).not.toContain("StatsSection");
    expect(src).not.toContain("CaseStudySection");
    expect(src).not.toContain("PartnershipSection");
    expect(src).not.toContain("FAQSection");
  });

  it("marketing layout metadata title is 'Virtuna' (not 'Artificial Societies')", () => {
    const src = readFileSync(LAYOUT, "utf-8");
    expect(src).not.toMatch(/title:\s*["']Artificial Societies/);
    expect(src).toMatch(/title:\s*["']Virtuna["']/);
  });

  it("pricing page no longer imports FAQSection from @/components/landing", () => {
    const src = readFileSync(PRICING_PAGE, "utf-8");
    expect(src).not.toMatch(/from\s+["']@\/components\/landing["']/);
  });

  it("src/components/landing/ directory has been deleted", () => {
    expect(existsSync(LANDING_DIR)).toBe(false);
  });
});
```

**Notes for executor:**
- All four files use `node:fs` + `node:path` — no React, no JSX, no test renderer required.
- Vitest already detects `.test.ts` under `src/**/` per `vitest.config.ts` include glob — no config changes.
- These tests WILL fail RED when first added (sources don't exist yet, AS imports still in page.tsx). That is intended — they are invariant locks for Plans 02 + 03.
- DO NOT mark them `.skip` or `.todo`. RED is the contract.
- The four files together cover SC-1 (route shell), SC-2 (Magic UI installed), SC-2.tuning (coral defaults), and the SC-cross invariant (`/pricing` detach).
  </action>
  <verify>
    <automated>pnpm test src/components/magic-ui/__tests__/ 2>&1 | tee /tmp/wave0.log; grep -E "Tests\s+[0-9]+ failed" /tmp/wave0.log</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/magic-ui/__tests__/magic-card.test.ts` exists
    - File `src/components/magic-ui/__tests__/border-beam.test.ts` exists
    - File `src/components/magic-ui/__tests__/shine-border.test.ts` exists
    - File `src/components/magic-ui/__tests__/marketing-shell.test.ts` exists
    - `grep -c "import.*vitest" src/components/magic-ui/__tests__/*.test.ts` returns 4
    - `grep -l "import.*@testing-library/react" src/components/magic-ui/__tests__/` returns no files (would break under node env)
    - `grep -l "JSX\\|<MagicCard" src/components/magic-ui/__tests__/` returns no files (would break under node env)
    - `pnpm test src/components/magic-ui/__tests__/` runs (does NOT crash with config error)
    - Test output shows 4 test files, all RED (this is the expected Wave 0 state)
    - No file modifications outside the listed paths
  </acceptance_criteria>
  <done>
    4 vitest test files exist under `src/components/magic-ui/__tests__/`, all use `node:fs` string-based assertions (no JSX, no testing-library), `pnpm test src/components/magic-ui/` runs without config errors and reports failures (RED — expected; turns GREEN after Plans 02 + 03). The Wave 0 frontmatter checkbox in 01-VALIDATION.md can be flipped to true once these files commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test scaffolds only — read-only fs reads in test runner. No external input, no user-facing surface, no runtime exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I (Information disclosure) | Test files read `src/app/(marketing)/layout.tsx` content | accept | Layout source is non-secret; tests read public source paths via `node:fs`. No env vars, no secrets. Tests are checked in alongside source. |
| T-01-02 | T (Tampering) | An executor strips invariants by deleting tests | accept | Plan 05 final verification re-runs full `pnpm test` and would catch a deletion — Wave 0 tests in repo serve as the public contract. |
</threat_model>

<verification>
- `pnpm test src/components/magic-ui/__tests__/` runs without "config error" — vitest accepts the files
- All 4 test files visible in `git status` as new untracked files
- `node -e "console.log(require('node:fs').existsSync('src/components/magic-ui/__tests__/magic-card.test.ts'))"` prints `true`
</verification>

<success_criteria>
- 4 invariant test files exist under `src/components/magic-ui/__tests__/`
- Tests run under vitest with node environment — no JSX, no testing-library, no config changes required
- Tests currently RED — they encode contracts that Plans 02 + 03 must satisfy
- Wave 0 frontmatter (`wave_0_complete`) in `01-VALIDATION.md` set to `true` after commit
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-route-scaffolding/01-01-SUMMARY.md` summarizing:
- 4 test files created
- Invariants encoded (one bullet per test)
- Confirmation that tests run under current vitest config (no config changes)
- Confirmation that all 4 are RED today (next plans turn them GREEN)
</output>
