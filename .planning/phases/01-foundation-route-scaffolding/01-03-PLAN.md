---
phase: 01-foundation-route-scaffolding
plan: 03
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - src/app/(marketing)/pricing/page.tsx
  - src/app/(marketing)/page.tsx
  - src/app/(marketing)/layout.tsx
  - src/components/layout/header.tsx
  - src/components/landing/backers-section.tsx
  - src/components/landing/case-study-section.tsx
  - src/components/landing/comparison-chart.tsx
  - src/components/landing/cta-section.tsx
  - src/components/landing/faq-section.tsx
  - src/components/landing/feature-card.tsx
  - src/components/landing/features-section.tsx
  - src/components/landing/hero-section.tsx
  - src/components/landing/index.ts
  - src/components/landing/partnership-section.tsx
  - src/components/landing/persona-card.tsx
  - src/components/landing/social-proof-section.tsx
  - src/components/landing/stats-section.tsx
  - src/components/landing/testimonial-quote.tsx
  - BRAND-BIBLE.md
autonomous: true
requirements:
  - SC-1
  - SC-3

must_haves:
  truths:
    - "Visitor at / sees an empty <main> (no AS plagiarized chrome, no rendered placeholder text) on a dark-mode canvas — section stub comments mark where Phases 2-7 will compose"
    - "Visitor at /pricing still renders (PricingSection only) — the FAQSection that used to plagiarize AS copy is detached BEFORE landing/ is deleted, preventing a build-time 500"
    - "The plagiarized src/components/landing/ directory is deleted entirely — Phase 8 COPY-01 audit can never trip on a stale import"
    - "Header renders 'Virtuna' wordmark with 'Sign up free' CTA routing to /signup (no 'Artificial Societies' string, no Calendly link)"
    - "Marketing layout metadata reads title: 'Virtuna' (brand spine NOT pre-locked — Phase 8 finalizes the spine sentence)"
    - "BRAND-BIBLE.md gains a new '## External Library Vetting Checklist' section with the 9 gates from UI-SPEC, verbatim"
    - "Wave-0 marketing-shell invariant test turns GREEN"
  artifacts:
    - path: "src/app/(marketing)/page.tsx"
      provides: "Empty marketing route shell with 6 phase-stub comments inside <main>"
      contains: "min-h-screen"
    - path: "src/app/(marketing)/layout.tsx"
      provides: "Neutral Virtuna metadata (title + description) — brand spine deferred"
      contains: "title: \"Virtuna\""
    - path: "src/app/(marketing)/pricing/page.tsx"
      provides: "Pricing page with FAQSection import removed (cross-route landmine defused)"
    - path: "src/components/layout/header.tsx"
      provides: "Header with Virtuna wordmark and 'Sign up free' /signup CTA"
      contains: "Virtuna"
    - path: "BRAND-BIBLE.md"
      provides: "External Library Vetting Checklist — 9 gates"
      contains: "External Library Vetting Checklist"
  key_links:
    - from: "src/app/(marketing)/page.tsx"
      to: "(no imports from src/components/landing/)"
      via: "absence of @/components/landing in imports"
      pattern: "from\\s+[\"']@/components/landing[\"']"
    - from: "src/app/(marketing)/pricing/page.tsx"
      to: "(no imports from src/components/landing/)"
      via: "absence of @/components/landing in imports"
      pattern: "from\\s+[\"']@/components/landing[\"']"
    - from: "src/components/layout/header.tsx"
      to: "/signup"
      via: "Link href"
      pattern: "href=[\"']/signup[\"']"
    - from: "BRAND-BIBLE.md"
      to: "(no source — pure documentation artifact)"
      via: "markdown heading"
      pattern: "^## External Library Vetting Checklist"
---

<objective>
Replace the plagiarized Artificial Societies template at `/` with an empty Virtuna-aligned route shell (SC-1), neutralize the marketing layout metadata, stub the header to Virtuna-correct chrome, detach the cross-route FAQSection import from `/pricing` (RESEARCH §Runtime State Inventory — CRITICAL landmine), delete the entire plagiarized `src/components/landing/` directory, and commit the 9-gate vetting checklist into `BRAND-BIBLE.md` (SC-3, D-06).

Task ordering is invariant: the FAQSection detach (Task 1) MUST land BEFORE the landing/ delete (Task 4), otherwise the build breaks for `/pricing`. Every task in this plan is part of a sequenced cleanup pass that converges on a clean shell.

Purpose: SC-1 (clean intentionally-empty route) + SC-3 (vetting checklist documented). Plan 04 then verifies Plan 02's primitives feel Raycast-native in the same showcase route this plan leaves untouched.

Output: Modified `page.tsx` / `layout.tsx` / `header.tsx`, deleted `src/components/landing/` directory, modified `/pricing/page.tsx`, updated `BRAND-BIBLE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/MILESTONE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-route-scaffolding/01-CONTEXT.md
@.planning/phases/01-foundation-route-scaffolding/01-RESEARCH.md
@.planning/phases/01-foundation-route-scaffolding/01-UI-SPEC.md
@CLAUDE.md
@BRAND-BIBLE.md

<interfaces>
Current state of files this plan modifies (verified line-by-line):

`src/app/(marketing)/page.tsx` (CURRENT — to be REWRITTEN entirely):
```
import {
  HeroSection,
  BackersSection,
  FeaturesSection,
  StatsSection,
  CaseStudySection,
  PartnershipSection,
  FAQSection,
} from "@/components/landing";
import { Footer } from "@/components/layout/footer";

export default function HomePage() {
  return (
    <>
      <main>
        <HeroSection />
        <BackersSection />
        <FeaturesSection />
        <StatsSection />
        <CaseStudySection />
        <PartnershipSection />
        <FAQSection />
      </main>
      <Footer />
    </>
  );
}
```

`src/app/(marketing)/layout.tsx` (CURRENT — needs metadata replacement):
```
export const metadata: Metadata = {
  title: "Artificial Societies | Human Behavior, Simulated",
  description:
    "AI personas that replicate real-world attitudes, beliefs, and opinions. Research that was impossible is now instant.",
};
```

`src/app/(marketing)/pricing/page.tsx` (CURRENT — has the cross-route landmine on line 3):
```
import { PricingSection } from "./pricing-section";
import { Footer } from "@/components/layout/footer";
import { FAQSection } from "@/components/landing";        // ← LANDMINE: must remove BEFORE landing/ delete
import type { Metadata } from "next";
...
<main>
  <PricingSection />
  <FAQSection />     // ← also remove this usage
</main>
```

`src/components/layout/header.tsx` (CURRENT — relevant strings):
- Line 85: `<span className="font-sans text-white">Artificial Societies</span>` → must become `Virtuna`
- Line 91-93: `Sign in` link routing to `/auth/login` (unchanged — UI-SPEC says keep)
- Lines 96-103: Calendly CTA with copy "Book a Meeting" — must become `/signup` with copy "Sign up free"
- Lines 132-138: mirror Sign in link in mobile menu (unchanged)
- Lines 139-147: mirror Calendly CTA in mobile menu — same swap

`src/components/landing/` (CURRENT — 15 files including `index.ts` barrel):
- backers-section.tsx, case-study-section.tsx, comparison-chart.tsx, cta-section.tsx, faq-section.tsx, feature-card.tsx, features-section.tsx, hero-section.tsx, index.ts, partnership-section.tsx, persona-card.tsx, social-proof-section.tsx, stats-section.tsx, testimonial-quote.tsx

`BRAND-BIBLE.md` (CURRENT — insertion anchor):
- Line 289: `## Do's and Don'ts`
- Line 312: `## Accessibility`
- New section `## External Library Vetting Checklist` lands BETWEEN these two (after "Do's and Don'ts" ends, before "Accessibility" begins) — UI-SPEC §Vetting Checklist Contract is the verbatim text.

`/signup` route (CONFIRMED EXISTS): `src/app/(onboarding)/signup/` — header CTA target is valid.

The `Footer` component (`src/components/layout/footer.tsx`) is referenced by current page.tsx but is OUT OF SCOPE — Phase 7 ships the new footer; Phase 1's empty shell drops the Footer import entirely. Phase 7 will add it back.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detach FAQSection from /pricing (cross-route landmine — must precede landing/ delete)</name>
  <read_first>
    - src/app/(marketing)/pricing/page.tsx (current file — confirm line 3 imports FAQSection from @/components/landing and line 17 ish renders it inside <main>)
    - .planning/phases/01-foundation-route-scaffolding/01-RESEARCH.md §"Runtime State Inventory" (CRITICAL — explains why this must precede the landing/ delete; identifies option 3 — full removal — as the recommendation)
    - .planning/phases/01-foundation-route-scaffolding/01-RESEARCH.md §"Code Examples" — Example 5 (verbatim before/after for this file)
  </read_first>
  <files>
    - src/app/(marketing)/pricing/page.tsx
  </files>
  <action>
Edit `src/app/(marketing)/pricing/page.tsx` to remove BOTH:
1. The import line `import { FAQSection } from "@/components/landing";` (line 3 of the current file)
2. The `<FAQSection />` usage inside `<main>`

Per RESEARCH §Runtime State Inventory recommendation (option 3 — remove import + usage entirely, do not move the file or inline the JSX), this defuses the cross-route landmine without introducing a temporary stub that would itself be AS-plagiarized copy.

Final file content (the only allowed deltas vs current state are: drop import line 3, drop the `<FAQSection />` JSX, leave a single-line marker comment in its place — everything else verbatim):

```
import { PricingSection } from "./pricing-section";
import { Footer } from "@/components/layout/footer";
import type { Metadata } from "next";

export const dynamic = "force-dynamic";

export const metadata: Metadata = {
  title: "Pricing | Virtuna",
  description:
    "Choose the plan that fits your creator journey. Start with a 7-day free Pro trial.",
};

export default function PricingPage() {
  return (
    <>
      <main>
        <PricingSection />
        {/* FAQ section removed in Phase 1 (was AS-plagiarized).
            Phase 7 may add a new FAQ here if /pricing needs one. */}
      </main>
      <Footer />
    </>
  );
}
```

The marker comment is intentional — it tells future readers (and Phase 7 planner) that this is a known gap and why. The Footer import stays; it is plagiarism-adjacent but its content lives in `src/components/layout/footer.tsx`, which is outside Phase 1 scope. Phase 7 ships the new footer.

This task is sequenced BEFORE Task 4 (landing/ delete) — otherwise the build would 500 for `/pricing`.
  </action>
  <verify>
    <automated>! grep -qE 'from\s+[\"'\'']@/components/landing[\"'\'']' src/app/\(marketing\)/pricing/page.tsx && ! grep -q '<FAQSection' src/app/\(marketing\)/pricing/page.tsx && grep -q "FAQ section removed in Phase 1" src/app/\(marketing\)/pricing/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "from\s+[\"']@/components/landing[\"']" src/app/(marketing)/pricing/page.tsx` returns 0
    - `grep -c "<FAQSection" src/app/(marketing)/pricing/page.tsx` returns 0
    - `grep -c "FAQ section removed in Phase 1" src/app/(marketing)/pricing/page.tsx` returns ≥ 1 (marker comment present)
    - `grep -c "PricingSection" src/app/(marketing)/pricing/page.tsx` returns ≥ 1 (preserved; Phase 1 does not touch pricing-section.tsx)
    - `grep -c "Footer" src/app/(marketing)/pricing/page.tsx` returns ≥ 1 (still imported; Phase 7 swaps it later)
    - Metadata `title: "Pricing | Virtuna"` preserved verbatim
  </acceptance_criteria>
  <done>
    /pricing page no longer references @/components/landing. Build will be clean once landing/ is deleted in Task 4 (this task is the prerequisite).
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace src/app/(marketing)/page.tsx with empty Virtuna shell + section stub comments</name>
  <read_first>
    - src/app/(marketing)/page.tsx (current — confirm it imports 7 AS sections + Footer)
    - .planning/phases/01-foundation-route-scaffolding/01-UI-SPEC.md §"Marketing Route Shell Contract" (the EXACT replacement source code — verbatim 12 lines)
    - .planning/phases/01-foundation-route-scaffolding/01-RESEARCH.md §"Code Examples" — Example 2 (verbatim post-replacement source)
    - .planning/phases/01-foundation-route-scaffolding/01-CONTEXT.md §Claude's Discretion → "Empty shell content at /" (decision locked: minimal <main> + phase stubs, no placeholder text)
  </read_first>
  <files>
    - src/app/(marketing)/page.tsx
  </files>
  <action>
Use the Write tool to OVERWRITE `src/app/(marketing)/page.tsx` with this exact content (UI-SPEC §Marketing Route Shell Contract — verbatim, no additions, no `'use client'`, no Footer, no placeholder copy):

```
// src/app/(marketing)/page.tsx
// Phase 1 of Landing Page Redesign milestone — intentionally empty <main>.
// Each phase stub below marks where future composition lands; do NOT remove
// or reorder these comments — they are the section ledger for Phases 2-7.

export default function HomePage() {
  return (
    <main className="min-h-screen bg-background">
      {/* Phase 2: Hero & Trust Band */}
      {/* Phase 3: Three Surfaces bento */}
      {/* Phase 4: How It Works + The Science */}
      {/* Phase 5: Audience Tabs + Social Proof */}
      {/* Phase 6: Tool Consolidation Calculator + Pricing */}
      {/* Phase 7: FAQ + Final CTA + Footer */}
    </main>
  );
}
```

What this gives us:
- No `'use client'` — server component, zero client JS shipped for this route's body
- No `<Footer />` import — Footer lives in `src/components/layout/footer.tsx` and is Phase 7's domain (it carries AS branding signals currently; Phase 1 not in its blast radius)
- 6 stub comments — one per future composition phase
- `min-h-screen bg-background` — fills viewport with `#07080a` (--color-background) per UI-SPEC Visual Contract
- No imports from `@/components/landing/` (drops the 7 AS section imports the current file has)

DO NOT add: a hero placeholder, a "coming soon" message, a centered spinner, a heading. The Phase 1 contract is "blank dark canvas + header chrome only" — the header continues to render via the marketing layout. UI-SPEC §Marketing Route Shell Contract: "A reviewer who lands on `/` during Phase 1 should see: header (stubbed), then darkness. That IS the contract."
  </action>
  <verify>
    <automated>! grep -qE 'from\s+[\"'\'']@/components/landing[\"'\'']' src/app/\(marketing\)/page.tsx && ! grep -q 'use client' src/app/\(marketing\)/page.tsx && grep -q 'min-h-screen' src/app/\(marketing\)/page.tsx && grep -q 'bg-background' src/app/\(marketing\)/page.tsx && grep -q 'Phase 2: Hero' src/app/\(marketing\)/page.tsx && grep -q 'Phase 7: FAQ' src/app/\(marketing\)/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "from\s+[\"']@/components/landing[\"']" src/app/(marketing)/page.tsx` returns 0
    - `grep -c "use client" src/app/(marketing)/page.tsx` returns 0 (must be a server component)
    - `grep -c "<main" src/app/(marketing)/page.tsx` returns ≥ 1
    - `grep -c "min-h-screen" src/app/(marketing)/page.tsx` returns 1
    - `grep -c "bg-background" src/app/(marketing)/page.tsx` returns 1
    - `grep -c "Phase 2: Hero" src/app/(marketing)/page.tsx` returns 1
    - `grep -c "Phase 7: FAQ" src/app/(marketing)/page.tsx` returns 1
    - `grep -c "<HeroSection\|<BackersSection\|<FAQSection" src/app/(marketing)/page.tsx` returns 0 (all AS section JSX gone)
    - `grep -c "<Footer" src/app/(marketing)/page.tsx` returns 0 (Footer lives in Phase 7)
    - `grep -c "TODO\|coming soon\|placeholder" src/app/(marketing)/page.tsx` returns 0 (no placeholder copy per CONTEXT.md)
    - File is ≤ 20 lines (the empty shell is intentionally tiny)
  </acceptance_criteria>
  <done>
    /`/` renders a dark empty <main> with 6 phase-stub comments. Server component (no 'use client', no client JS shipped). No AS imports, no AS JSX, no placeholder copy.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update marketing layout metadata (neutral Virtuna title + description)</name>
  <read_first>
    - src/app/(marketing)/layout.tsx (current — confirm AS-plagiarized title on line 13 and description on line 14-15)
    - .planning/phases/01-foundation-route-scaffolding/01-UI-SPEC.md §"Marketing Layout Metadata Contract" (verbatim before/after — title becomes "Virtuna", description becomes "Virtuna — TikTok creator intelligence.")
    - .planning/phases/01-foundation-route-scaffolding/01-CONTEXT.md §Deferred Ideas — brand spine sentence finalization is Phase 8 COPY-02; do NOT pre-lock a sentence here, the description MUST stay neutral
  </read_first>
  <files>
    - src/app/(marketing)/layout.tsx
  </files>
  <action>
Use the Edit tool to apply two surgical changes to `src/app/(marketing)/layout.tsx`:

1. Find: `title: "Artificial Societies | Human Behavior, Simulated",`
   Replace with: `title: "Virtuna",`

2. Find the description block:
```
  description:
    "AI personas that replicate real-world attitudes, beliefs, and opinions. Research that was impossible is now instant.",
```
   Replace with:
```
  description: "Virtuna — TikTok creator intelligence.",
```

Final state of the metadata block must read EXACTLY:

```
export const metadata: Metadata = {
  title: "Virtuna",
  description: "Virtuna — TikTok creator intelligence.",
};
```

Per CONTEXT.md §Deferred Ideas and UI-SPEC §Marketing Layout Metadata Contract, the description is intentionally minimal. Phase 8 (COPY-02) finalizes the brand spine sentence and updates this. DO NOT add a sentence with "AI", "viral", "personas", "simulation", or any other v3.0 vocab.

DO NOT change: the Inter font setup, the `<html>` and `<body>` JSX, the `<Header />` placement. UI-SPEC explicitly preserves all of these.
  </action>
  <verify>
    <automated>! grep -q "Artificial Societies" src/app/\(marketing\)/layout.tsx && grep -q 'title: "Virtuna"' src/app/\(marketing\)/layout.tsx && grep -q "TikTok creator intelligence" src/app/\(marketing\)/layout.tsx && ! grep -qi "viral\|AI personas\|simulated" src/app/\(marketing\)/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Artificial Societies" src/app/(marketing)/layout.tsx` returns 0
    - `grep -c 'title: "Virtuna"' src/app/(marketing)/layout.tsx` returns 1
    - `grep -c "TikTok creator intelligence" src/app/(marketing)/layout.tsx` returns 1
    - `grep -ci "viral\|AI personas\|simulated\|human behavior" src/app/(marketing)/layout.tsx` returns 0 (per HERO-05 + COPY-02 — brand spine vocab forbidden)
    - `grep -c "import.*Inter.*next/font" src/app/(marketing)/layout.tsx` returns ≥ 1 (Inter setup preserved)
    - `grep -c "<Header" src/app/(marketing)/layout.tsx` returns ≥ 1 (Header placement preserved)
    - `grep -c "min-h-screen bg-background" src/app/(marketing)/layout.tsx` returns ≥ 1 (body class preserved)
  </acceptance_criteria>
  <done>
    Marketing layout metadata is Virtuna-correct and brand-spine-neutral. Phase 8 will finalize when the brand spine emerges.
  </done>
</task>

<task type="auto">
  <name>Task 4: Delete src/components/landing/ directory (CRITICAL ordering — runs AFTER Task 1)</name>
  <read_first>
    - src/components/landing/ directory listing (CONFIRM contents — 15 files expected per BRAND-BIBLE / RESEARCH)
    - src/app/(marketing)/pricing/page.tsx (CONFIRM Task 1 already detached the FAQSection import — if grep still finds the import here, STOP and re-run Task 1)
    - src/app/(marketing)/page.tsx (CONFIRM Task 2 already replaced the AS imports — if grep still finds them, STOP and re-run Task 2)
    - .planning/phases/01-foundation-route-scaffolding/01-UI-SPEC.md §"Plagiarized Landing Components Contract" (locks the DELETE decision and rationale)
    - .planning/phases/01-foundation-route-scaffolding/01-RESEARCH.md §"Runtime State Inventory" (confirms no DB / no env vars / no other cross-route imports)
  </read_first>
  <files>
    - src/components/landing/backers-section.tsx (DELETE)
    - src/components/landing/case-study-section.tsx (DELETE)
    - src/components/landing/comparison-chart.tsx (DELETE)
    - src/components/landing/cta-section.tsx (DELETE)
    - src/components/landing/faq-section.tsx (DELETE)
    - src/components/landing/feature-card.tsx (DELETE)
    - src/components/landing/features-section.tsx (DELETE)
    - src/components/landing/hero-section.tsx (DELETE)
    - src/components/landing/index.ts (DELETE)
    - src/components/landing/partnership-section.tsx (DELETE)
    - src/components/landing/persona-card.tsx (DELETE)
    - src/components/landing/social-proof-section.tsx (DELETE)
    - src/components/landing/stats-section.tsx (DELETE)
    - src/components/landing/testimonial-quote.tsx (DELETE)
  </files>
  <action>
Step 1 — Re-verify the prerequisite: BOTH the homepage and pricing page must NOT reference `@/components/landing` anymore. Run:

```
grep -rn "from.*[\"']@/components/landing[\"']" src/
```

Expected output: zero matches. If anything appears, STOP — Tasks 1 and 2 are not complete. Re-run them before proceeding.

Step 2 — Delete the entire directory:

```
rm -rf src/components/landing/
```

Step 3 — Re-verify the codebase has no broken imports:

```
grep -rn "components/landing" src/ tests/ extraction/ e2e/ 2>/dev/null
```

Expected output: zero matches anywhere. If any test file or e2e file references it, surface that to the executor's SUMMARY — it indicates a stale test fixture that the wave-0 marketing-shell test should catch but should be flagged regardless.

Step 4 — Confirm build remains green:

```
pnpm run build
```

Build MUST exit 0. If it fails with "Cannot find module '@/components/landing'", a hidden import was missed — re-run Step 3 with broader globs.

Per UI-SPEC §Plagiarized Landing Components Contract: "Executor must delete the entire `src/components/landing/` directory in Phase 1 after confirming page.tsx no longer imports any of its exports." v3.0 archive at `.planning/milestones/v3.0-brand-statement-landing/` preserves these files if any content is later salvageable — they are not lost, only removed from the active tree.

There is no rollback complexity: the files are dead weight the moment Tasks 1 and 2 stop importing them. RESEARCH §Runtime State Inventory confirms zero non-import dependencies (no DB rows, no env vars, no live service config tied to the directory name).
  </action>
  <verify>
    <automated>! test -d src/components/landing && ! grep -rqE "from\s+[\"'\'']@/components/landing[\"'\'']" src/ && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -d src/components/landing` exits non-zero (directory does not exist)
    - `find src/components/landing -type f 2>/dev/null | wc -l` returns 0
    - `grep -rcE "from\s+[\"']@/components/landing[\"']" src/` returns 0 across all files
    - `grep -rc "components/landing" src/ tests/ 2>/dev/null` returns 0 (or only matches in .planning/ documentation, which is OK)
    - `pnpm run build` exits 0
    - `pnpm test src/components/magic-ui/__tests__/marketing-shell.test.ts` — the "src/components/landing/ directory has been deleted" assertion now PASSES (one of the assertions in the Wave-0 file)
  </acceptance_criteria>
  <done>
    src/components/landing/ is gone. Build is clean for /, /pricing, and the rest. The plagiarized AS section components are removed from the active tree; v3.0 archive preserves history.
  </done>
</task>

<task type="auto">
  <name>Task 5: Stub the marketing header (Virtuna wordmark + /signup CTA)</name>
  <read_first>
    - src/components/layout/header.tsx (current source — note lines 85 (wordmark), 96-103 (desktop CTA), 139-147 (mobile CTA))
    - .planning/phases/01-foundation-route-scaffolding/01-UI-SPEC.md §"Header Stub Contract" (verbatim diff spec for wordmark + CTA)
    - .planning/phases/01-foundation-route-scaffolding/01-RESEARCH.md §"Code Examples" — Example 3 (verbatim before/after diff for all 3 changes)
    - src/app/(onboarding)/signup/ (CONFIRM the route exists — it does, verified)
  </read_first>
  <files>
    - src/components/layout/header.tsx
  </files>
  <action>
Apply 3 surgical Edits to `src/components/layout/header.tsx`. Preserve every other line verbatim — UI-SPEC explicitly keeps the floating-pill chrome, the V-mark SVG, the Sign in link, the mobile hamburger logic.

Change 1 — Wordmark (around line 85):
Find: `<span className="font-sans text-white">Artificial Societies</span>`
Replace: `<span className="font-sans text-white">Virtuna</span>`

Change 2 — Desktop CTA (around lines 96-103). Find this entire block:
```
              <Link
                href="https://calendly.com"
                target="_blank"
                rel="noopener noreferrer"
                className="rounded-lg bg-accent px-4 py-2 text-sm font-medium text-accent-foreground transition-colors hover:bg-accent/90"
              >
                Book a Meeting
              </Link>
```
Replace with:
```
              <Link
                href="/signup"
                className="rounded-lg bg-accent px-4 py-2 text-sm font-medium text-accent-foreground transition-colors hover:bg-accent/90"
              >
                Sign up free
              </Link>
```
(Drops `target="_blank"`, drops `rel="noopener noreferrer"` — `/signup` is internal so neither applies. Drops the Calendly URL. Drops "Book a Meeting" copy.)

Change 3 — Mobile CTA (around lines 139-147). Find this entire block:
```
              <Link
                href="https://calendly.com"
                target="_blank"
                rel="noopener noreferrer"
                className="w-full rounded-lg bg-accent px-4 py-3 text-center text-sm font-medium text-accent-foreground transition-colors hover:bg-accent/90"
                onClick={() => setMobileMenuOpen(false)}
              >
                Book a Meeting
              </Link>
```
Replace with:
```
              <Link
                href="/signup"
                className="w-full rounded-lg bg-accent px-4 py-3 text-center text-sm font-medium text-accent-foreground transition-colors hover:bg-accent/90"
                onClick={() => setMobileMenuOpen(false)}
              >
                Sign up free
              </Link>
```
(Same delta — internal route, drop external attrs and Calendly URL.)

DO NOT change: the V-mark SVG path (it's a V-shape that already works for Virtuna per UI-SPEC); the "Sign in" link (UI-SPEC keeps it pointing to `/auth/login`); the floating-pill chrome (max-w-[1204px], rounded-2xl, gradient background, inline backdropFilter, inset shadow, h-[76px]); the hamburger menu state logic; the mobile menu open/close animation; the overlay logic.

Also DO NOT replace the existing JSDoc comment at line 12-18 that mentions "societies.io" — that's a vestige but it's not user-facing chrome; updating it adds noise without conversion value. Phase 8 (COPY-01 audit) catches it later.
  </action>
  <verify>
    <automated>! grep -q "Artificial Societies" src/components/layout/header.tsx && ! grep -q "calendly" src/components/layout/header.tsx && ! grep -q "Book a Meeting" src/components/layout/header.tsx && grep -c "Virtuna" src/components/layout/header.tsx | grep -q "^1$" && grep -c '"/signup"' src/components/layout/header.tsx | grep -q "^2$" && grep -c "Sign up free" src/components/layout/header.tsx | grep -q "^2$" && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Artificial Societies" src/components/layout/header.tsx` returns 0
    - `grep -ci "calendly" src/components/layout/header.tsx` returns 0
    - `grep -c "Book a Meeting" src/components/layout/header.tsx` returns 0
    - `grep -c "Virtuna" src/components/layout/header.tsx` returns ≥ 1
    - `grep -c '"/signup"' src/components/layout/header.tsx` returns 2 (desktop + mobile)
    - `grep -c "Sign up free" src/components/layout/header.tsx` returns 2 (desktop + mobile)
    - `grep -c "Sign in" src/components/layout/header.tsx` returns ≥ 2 (preserved — desktop + mobile)
    - `grep -c "h-\[76px\]" src/components/layout/header.tsx` returns ≥ 1 (chrome preserved)
    - `grep -c "backdropFilter" src/components/layout/header.tsx` returns ≥ 1 (inline style preserved per CLAUDE.md Lightning CSS rule)
    - `grep -c 'M16 6H13L8 27H11L16 6ZM16 6L21 27H24L19 6H16Z' src/components/layout/header.tsx` returns 1 (V-mark SVG path preserved verbatim)
    - `pnpm run build` exits 0
  </acceptance_criteria>
  <done>
    Header now reads "Virtuna" wordmark with "Sign up free" CTAs routing internally to /signup. AS-plagiarized strings and external Calendly link gone. Visual chrome (gradient, blur, inset shadow) untouched.
  </done>
</task>

<task type="auto">
  <name>Task 6: Append "External Library Vetting Checklist" section to BRAND-BIBLE.md (D-06 / SC-3)</name>
  <read_first>
    - BRAND-BIBLE.md (full file — locate the existing section anchors: "## Do's and Don'ts" around line 289, "## Accessibility" around line 312)
    - .planning/phases/01-foundation-route-scaffolding/01-UI-SPEC.md §"Vetting Checklist Contract" (this contains the 9-gate verbatim text — copy this into BRAND-BIBLE.md as the body of the new section)
    - .planning/phases/01-foundation-route-scaffolding/01-CONTEXT.md §Implementation Decisions → D-06 (location is BRAND-BIBLE.md, after "Do's and Don'ts")
  </read_first>
  <files>
    - BRAND-BIBLE.md
  </files>
  <action>
Insert a new top-level section `## External Library Vetting Checklist` into `BRAND-BIBLE.md` BETWEEN the existing "## Do's and Don'ts" section and the existing "## Accessibility" section. (Use Edit tool — find the line `## Accessibility` and insert the new section block immediately BEFORE it.)

The body of the new section is the verbatim text from UI-SPEC §"Vetting Checklist Contract" — 9 gates, copy-paste-ready. Reproduced here for the executor (paste this AS-IS into BRAND-BIBLE.md, preserving markdown formatting):

```markdown
## External Library Vetting Checklist

Before committing any component from Magic UI, Aceternity UI, Origin UI,
Cult UI, or any third-party shadcn registry, ALL of the following gates
must PASS. Record pass/fail + date in the component's source file comment.

### Gate 1 — Color Audit
- [ ] No purple, violet, blue, or pink DEFAULT props bleed through to
      rendered output without being overridden
- [ ] gradientFrom / gradientTo / colorFrom / colorTo / shineColor /
      glowFrom / glowTo or equivalent color props all set to Raycast tokens
      (coral-500 for accent, gray-950/surface/surface-elevated for fills,
      transparent for neutral zones)
- [ ] No hardcoded hex values remain in tuned file that aren't Raycast tokens

### Gate 2 — Border Opacity
- [ ] Component border (if any) uses rgba(255,255,255,0.06) for resting
- [ ] Component border hover (if any) uses rgba(255,255,255,0.10)
- [ ] No 8%, 12%, or other non-standard opacities

### Gate 3 — Radius Scale
- [ ] Card-shaped containers: 12px (--radius-lg)
- [ ] Button-shaped containers: 8px (--radius-md)
- [ ] Modal-shaped containers: 12px (--radius-lg)
- [ ] Component does not introduce non-standard radii

### Gate 4 — Motion Audit
- [ ] No auto-looping high-contrast animations at duration < 6s
- [ ] No neon glow effects (colored box-shadow at high opacity on
      non-accent colors)
- [ ] No continuous scale/bounce effects that repeat without user action
- [ ] prefers-reduced-motion respected: either via motion-safe: Tailwind
      class, useReducedMotion() from motion/react, or usePrefersReducedMotion
      hook from src/hooks/usePrefersReducedMotion.ts

### Gate 5 — Font Audit
- [ ] Component source contains no embedded font-family CSS property
- [ ] No @font-face declarations
- [ ] All text inherits Inter via --font-sans (globals.css body declaration)

### Gate 6 — GlassPanel Compatibility
- [ ] Component does not declare competing backdrop-filter values in CSS
      (backdrop-filter must only be applied via React inline styles to
      bypass Lightning CSS stripping — per CLAUDE.md Known Technical Issues)
- [ ] Component composes as a child or sibling of GlassPanel without
      visual conflict (test in /showcase)

### Gate 7 — Dark-Mode-First
- [ ] Component has no `dark:` conditional logic that implies light mode
      as the default state
- [ ] If component uses useTheme(): dark theme resolves correctly on
      initial mount without flash (mounted-state guard in place)
- [ ] Component does not assume a white or light background

### Gate 8 — Bundle Size Sanity
- [ ] Component introduces no motion/animation library not already in
      package.json (motion/react is approved; framer-motion is legacy
      and not to be extended; GSAP is rejected per BRAND-BIBLE.md §4)
- [ ] Component introduces no third-party icon library beyond
      @phosphor-icons/react and lucide-react (both already installed)
- [ ] Component .tsx source is < 150 lines (if larger, split or simplify
      before commit)

### Gate 9 — Security Scan
- [ ] No fetch(), XMLHttpRequest, or navigator.sendBeacon calls
- [ ] No process.env references
- [ ] No eval(), new Function(), or dynamic imports from external URLs
- [ ] No obfuscated variable names in non-minified source
```

Insert this block immediately BEFORE the `## Accessibility` heading. Add one blank line above and one blank line below to preserve markdown spacing. Do NOT modify any other section.
  </action>
  <verify>
    <automated>grep -c "^## External Library Vetting Checklist" BRAND-BIBLE.md | grep -q "^1$" && grep -c "Gate 1 — Color Audit" BRAND-BIBLE.md | grep -q "^1$" && grep -c "Gate 9 — Security Scan" BRAND-BIBLE.md | grep -q "^1$" && awk '/^## External Library Vetting Checklist/{seen=1} /^## Accessibility/{if(seen) print "OK"}' BRAND-BIBLE.md | grep -q OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## External Library Vetting Checklist" BRAND-BIBLE.md` returns 1
    - `grep -c "Gate 1 — Color Audit" BRAND-BIBLE.md` returns 1
    - `grep -c "Gate 2 — Border Opacity" BRAND-BIBLE.md` returns 1
    - `grep -c "Gate 3 — Radius Scale" BRAND-BIBLE.md` returns 1
    - `grep -c "Gate 4 — Motion Audit" BRAND-BIBLE.md` returns 1
    - `grep -c "Gate 5 — Font Audit" BRAND-BIBLE.md` returns 1
    - `grep -c "Gate 6 — GlassPanel Compatibility" BRAND-BIBLE.md` returns 1
    - `grep -c "Gate 7 — Dark-Mode-First" BRAND-BIBLE.md` returns 1
    - `grep -c "Gate 8 — Bundle Size Sanity" BRAND-BIBLE.md` returns 1
    - `grep -c "Gate 9 — Security Scan" BRAND-BIBLE.md` returns 1
    - Section is BETWEEN "## Do's and Don'ts" and "## Accessibility" (verify with `awk` ordering — the vetting section's line number is greater than "Do's and Don'ts" and less than "Accessibility")
    - `grep -c "^## " BRAND-BIBLE.md` count increased by exactly 1 vs pre-task baseline (was 11, now 12)
    - No other section text was modified — verify by running `git diff BRAND-BIBLE.md` and confirming the only change is the inserted block
  </acceptance_criteria>
  <done>
    BRAND-BIBLE.md has the 9-gate vetting checklist as its own top-level section, located per D-06 between "Do's and Don'ts" and "Accessibility". SC-3 is satisfied: future Magic UI / Aceternity / Origin UI / Cult UI imports have a single canonical gate to pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server route → response (Next.js App Router) | Marketing route and pricing route now ship trimmed JSX; no new untrusted input crosses, but the metadata `description` is published to search engines and social-share previews. Must be brand-spine-neutral until Phase 8 finalizes. |
| Documentation artifact | BRAND-BIBLE.md is a markdown reference; no runtime exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I (Information disclosure) | Marketing layout `description` is indexed by Google + Twitter Card + OG meta | mitigate | Task 3 enforces description = "Virtuna — TikTok creator intelligence." Brand-spine-neutral. No PII, no claims that need legal review. Phase 8 finalizes the public-facing description sentence (COPY-02). |
| T-03-02 | D (Denial of service) | Header `/signup` link could route to a route that 404s if `(onboarding)/signup` was renamed | accept | Verified `src/app/(onboarding)/signup/` exists (confirmed via `find` in pre-planning). If it gets renamed in a future milestone, the header link breaks — but that's a future regression, not a Phase 1 threat. |
| T-03-03 | T (Tampering) | Deleting `src/components/landing/` could break tests in other directories | mitigate | Task 4 Step 3 runs `grep -rn "components/landing" src/ tests/ e2e/ extraction/` after the delete to confirm no stale imports survive anywhere in the repo. Build verification (`pnpm run build`) is part of the same task. |
| T-03-04 | I (Information disclosure) | Vetting checklist in BRAND-BIBLE.md mentions internal hooks (`src/hooks/usePrefersReducedMotion.ts`) | accept | Repo is private (verified GitHub remote per CLAUDE.md). The hook path is benign internal info; not a secret. BRAND-BIBLE.md is co-committed with source. |
| T-03-05 | S (Spoofing) | Header "Sign up free" CTA targets `/signup` — could an attacker create a phishing route at `/signup-virtuna` and trick visitors? | accept | Same-origin internal Next.js route; visitor URL bar shows the canonical host. No third-party redirect involved. Standard React safety. |
</threat_model>

<verification>
- No file in `src/` imports from `@/components/landing` (grep returns 0)
- `src/components/landing/` directory does not exist
- `src/app/(marketing)/page.tsx` is a server component with 6 phase-stub comments and no content
- `src/app/(marketing)/layout.tsx` metadata title = "Virtuna", description is neutral
- `src/components/layout/header.tsx` wordmark = "Virtuna", CTAs route to `/signup`, no Calendly URL
- `BRAND-BIBLE.md` has `## External Library Vetting Checklist` section between "Do's and Don'ts" and "Accessibility"
- Wave-0 invariant test `marketing-shell.test.ts` now PASSES (all 6 assertions GREEN — completing the test file that Plan 02 left at 3/6 due to landing/ still existing)
- `pnpm run build` exits 0
- `pnpm test src/components/magic-ui/__tests__/` reports 4/4 test files GREEN
</verification>

<success_criteria>
- SC-1 satisfied: visitor at `/` sees a clean dark canvas (header + empty <main>) — no plagiarized AS sections, no placeholder copy
- SC-3 satisfied: 9-gate vetting checklist exists in `BRAND-BIBLE.md` as its own section; future external-library imports have a documented gate
- All 4 Wave-0 invariant tests GREEN
- `/pricing` still renders (without the AS FAQ — explicitly per UI-SPEC)
- Build clean across all routes
- No file modifications outside the listed `files_modified` paths
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-route-scaffolding/01-03-SUMMARY.md` covering:
- The cleanup ordering (Task 1 → Task 2 → Task 4) and why it matters
- Whether `pnpm run build` was clean after Task 4 immediately, or whether cache flush was needed
- Confirmation that the marketing-shell Wave-0 test went RED → GREEN at end of plan
- A list of any files in `tests/`, `e2e/`, or `extraction/` that referenced `components/landing` (should be empty) — surfaces hidden regressions if any
- Confirmation the vetting checklist is in BRAND-BIBLE.md at the correct line range (between "Do's and Don'ts" and "Accessibility")

</output>
