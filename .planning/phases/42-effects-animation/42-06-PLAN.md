---
phase: 42-effects-animation
plan: 06
type: execute
wave: 2
depends_on: ["42-01", "42-02", "42-03", "42-04"]
files_modified:
  - src/components/motion/index.ts
  - src/components/primitives/index.ts
  - src/components/ui/index.ts
  - src/app/(marketing)/ui-showcase/page.tsx
  - src/app/(marketing)/ui-showcase/_components/phase-42-demos.tsx
autonomous: false

must_haves:
  truths:
    - "FadeInUp, StaggerReveal, HoverScale exported from @/components/motion"
    - "NoiseTexture, ChromaticAberration exported from @/components/effects"
    - "GlassBlur type exported from @/components/primitives"
    - "UI showcase page demonstrates Phase 42 effects with visual verification"
  artifacts:
    - path: "src/components/motion/index.ts"
      provides: "Updated barrel exports including new motion components"
      contains: "FadeInUp"
    - path: "src/components/effects/index.ts"
      provides: "Effects barrel already created in 42-02"
      contains: "NoiseTexture"
    - path: "src/components/primitives/index.ts"
      provides: "Updated barrel with GlassBlur type"
      contains: "GlassBlur"
  key_links:
    - from: "src/components/motion/index.ts"
      to: "src/components/motion/fade-in-up.tsx"
      via: "re-export"
      pattern: "export.*FadeInUp"
    - from: "src/components/motion/index.ts"
      to: "src/components/motion/stagger-reveal.tsx"
      via: "re-export"
      pattern: "export.*StaggerReveal"
---

<objective>
Update barrel exports and create visual verification showcase for Phase 42.

Purpose: Wire all new Phase 42 components into barrel exports so consumers can import from standard paths. Add demonstration sections to the UI showcase page for visual verification. Covers GLS-04 (performance documentation via showcase guidance).
Output: Updated barrel exports + showcase sections for all Phase 42 components.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/motion/index.ts
@src/components/primitives/index.ts
@src/components/ui/index.ts
@.planning/phases/42-effects-animation/42-01-SUMMARY.md
@.planning/phases/42-effects-animation/42-02-SUMMARY.md
@.planning/phases/42-effects-animation/42-03-SUMMARY.md
@.planning/phases/42-effects-animation/42-04-SUMMARY.md
@.planning/phases/42-effects-animation/42-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update barrel exports for motion, primitives, and effects</name>
  <files>src/components/motion/index.ts, src/components/primitives/index.ts</files>
  <action>
**motion/index.ts — Add new exports:**
```typescript
export { FadeInUp } from "./fade-in-up";
export type { FadeInUpProps } from "./fade-in-up";

export { StaggerReveal, StaggerRevealItem } from "./stagger-reveal";
export type { StaggerRevealProps } from "./stagger-reveal";

export { HoverScale } from "./hover-scale";
export type { HoverScaleProps } from "./hover-scale";
```
Keep all existing exports (FadeIn, SlideUp, FrozenRouter, PageTransition). Also add the new type exports for enhanced components:
```typescript
export type { FadeInProps } from "./fade-in";
export type { SlideUpProps } from "./slide-up";
```

**primitives/index.ts — Add GlassBlur type export:**
```typescript
export type { GlassBlur } from "./GlassPanel";
```
Keep all existing exports unchanged.

The effects/index.ts barrel was already created in Plan 02. No changes needed there.
  </action>
  <verify>Run `pnpm build` — compiles without errors. Import checks: `import { FadeInUp, StaggerReveal, HoverScale } from "@/components/motion"` resolves. `import { NoiseTexture, ChromaticAberration } from "@/components/effects"` resolves. `import { GlassBlur } from "@/components/primitives"` resolves.</verify>
  <done>All Phase 42 components importable from their barrel export paths. Type exports included.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 42 demo sections to UI showcase</name>
  <files>src/app/(marketing)/ui-showcase/page.tsx</files>
  <action>
Create a new client component `src/app/(marketing)/ui-showcase/_components/phase-42-demos.tsx` for interactive Phase 42 demonstrations. Then add it to the existing showcase page.

**phase-42-demos.tsx should include:**

1. **GlassPanel Blur Variants** — Render 6 GlassPanels side by side (xs through 2xl) with labels showing blur level and pixel value. Each panel contains sample text.

2. **FadeInUp Demo** — 3 content blocks wrapped in FadeInUp with increasing delays (0, 0.2s, 0.4s) to show the stagger-like effect.

3. **StaggerReveal Demo** — A 3-column grid of 6 cards wrapped in StaggerReveal/StaggerReveal.Item showing the automatic stagger.

4. **HoverScale Demo** — 3 cards wrapped in HoverScale that respond to hover/tap. Label instructs user to hover.

5. **NoiseTexture Demo** — A GlassPanel with NoiseTexture overlay inside it. Show with and without noise for comparison.

6. **ChromaticAberration Demo** — Large decorative text wrapped in ChromaticAberration showing the RGB split effect.

7. **Skeleton Shimmer Demo** — 3 Skeleton elements of different sizes showing the shimmer animation.

Each section should have an `<h2>` heading and brief description. Use existing showcase section styling patterns from the Phase 41 demos file.

**In page.tsx:** Import and render `<Phase42Demos />` after the existing Phase 41 demos section.

Mark the component as "use client" since it uses interactive components.
  </action>
  <verify>Run `pnpm build` — compiles without errors. Run `pnpm dev` and navigate to /ui-showcase — Phase 42 sections visible.</verify>
  <done>UI showcase displays all Phase 42 effects: GlassPanel blur variants, FadeInUp, StaggerReveal, HoverScale, NoiseTexture, ChromaticAberration, and Skeleton shimmer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All Phase 42 effects and animations: GlassPanel with 6 blur levels, NoiseTexture grain overlay, ChromaticAberration RGB split, FadeInUp scroll reveal, StaggerReveal for grids, HoverScale micro-interaction, and Skeleton shimmer.</what-built>
  <how-to-verify>
1. Run `pnpm dev` and visit http://localhost:3000/ui-showcase
2. Scroll to Phase 42 sections
3. Verify GlassPanel shows 6 distinct blur levels (xs through 2xl)
4. Verify FadeInUp sections animate on scroll (fade + slide up)
5. Verify StaggerReveal grid cards appear sequentially with ~80ms delay
6. Hover over HoverScale cards — should scale up subtly (1.02x)
7. Verify NoiseTexture shows subtle grain overlay on glass panel
8. Verify ChromaticAberration shows RGB split on decorative text
9. Verify Skeleton elements show moving shimmer highlight
10. Check mobile viewport (responsive) — blur should reduce on smaller screens
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` passes
- All barrel exports resolve correctly
- UI showcase renders Phase 42 demo sections
- Visual inspection confirms all 7 effect types working
</verification>

<success_criteria>
All Phase 42 components exported and visually verified. Complete effects system ready for use in showcase pages (Phase 43).
</success_criteria>

<output>
After completion, create `.planning/phases/42-effects-animation/42-06-SUMMARY.md`
</output>
