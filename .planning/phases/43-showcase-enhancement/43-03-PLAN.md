---
phase: 43-showcase-enhancement
plan: 03
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - src/app/(marketing)/showcase/inputs/page.tsx
  - src/app/(marketing)/showcase/_components/toggle-demo.tsx
  - src/app/(marketing)/showcase/_components/select-demo.tsx
autonomous: true

must_haves:
  truths:
    - "/showcase/inputs page shows Input in default, focus, error, and disabled states"
    - "/showcase/inputs page shows InputField with label, helper text, error message"
    - "/showcase/inputs page shows Select and SearchableSelect with grouped options"
    - "/showcase/inputs page shows Toggle in all 3 sizes with checked/unchecked/disabled states"
    - "All variants have corresponding code snippets"
  artifacts:
    - path: "src/app/(marketing)/showcase/inputs/page.tsx"
      provides: "Complete inputs showcase page"
      min_lines: 80
    - path: "src/app/(marketing)/showcase/_components/toggle-demo.tsx"
      provides: "Interactive toggle demo (client component)"
      min_lines: 15
    - path: "src/app/(marketing)/showcase/_components/select-demo.tsx"
      provides: "Interactive select demo (client component)"
      min_lines: 20
  key_links:
    - from: "inputs/page.tsx"
      to: "@/components/ui"
      via: "import Input, InputField, Select, SearchableSelect, Toggle"
      pattern: "from \"@/components/ui\""
    - from: "inputs/page.tsx"
      to: "showcase-section.tsx"
      via: "import ShowcaseSection"
      pattern: "ShowcaseSection"
---

<objective>
Build the /showcase/inputs page (SHW-02) demonstrating Input, InputField, Select, SearchableSelect, and Toggle components with all variants, sizes, and states.

Purpose: Developers can see every input component variant and state at a glance, with code snippets for each.
Output: Complete inputs showcase page with static demos for Input/InputField and client component islands for interactive Toggle/Select.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-showcase-enhancement/43-RESEARCH.md
@.planning/phases/43-showcase-enhancement/43-01-SUMMARY.md

Key references — read these for props/variants:
@src/components/ui/input.tsx — Input and InputField component props
@src/components/ui/select.tsx — Select, SearchableSelect, SelectOption, SelectGroup props
@src/components/ui/toggle.tsx — Toggle component props, sizes
@src/app/(marketing)/showcase/_components/showcase-section.tsx
@src/app/(marketing)/showcase/_components/code-block.tsx
@src/app/(marketing)/showcase/_components/component-grid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create interactive demo client components</name>
  <files>
    src/app/(marketing)/showcase/_components/toggle-demo.tsx
    src/app/(marketing)/showcase/_components/select-demo.tsx
  </files>
  <action>
    Create two client component islands:

    **toggle-demo.tsx** — "use client"
    - Import Toggle from @/components/ui
    - Show 3 Toggle instances (sm, md, lg) each with useState for checked state
    - Show a disabled toggle (checked + disabled, unchecked + disabled)
    - Show toggle with label text
    - Use ComponentGrid for layout
    - Each demo item wrapped in a labeled container (div with Text label above)

    **select-demo.tsx** — "use client"
    - Import Select, SearchableSelect from @/components/ui
    - Define sample options: `[{ value: "react", label: "React" }, { value: "vue", label: "Vue" }, { value: "svelte", label: "Svelte" }, { value: "angular", label: "Angular" }]`
    - Define grouped options: `[{ label: "Frontend", options: [...] }, { label: "Backend", options: [...] }]`
    - Show: basic Select, Select with placeholder, Select with grouped options, disabled Select
    - Show: SearchableSelect with same options, disabled SearchableSelect
    - Use useState for selected values
    - Layout with ComponentGrid or flex containers for clean presentation
  </action>
  <verify>TypeScript compiles without errors.</verify>
  <done>Interactive Toggle and Select demos ready as client component islands</done>
</task>

<task type="auto">
  <name>Task 2: Build the /showcase/inputs page</name>
  <files>src/app/(marketing)/showcase/inputs/page.tsx</files>
  <action>
    Build the complete inputs page as a server component. Read the actual component source files first to understand all available props and variants.

    Structure with ShowcaseSection blocks:

    **1. Page Header**
    - Heading level={1}: "Inputs"
    - Description: overview of input components

    **2. Input section**
    - ShowcaseSection title: "Input"
    - Show: default input, input with placeholder, input with value, input in error state (className or prop), disabled input
    - Each in a flex/grid layout with labels
    - CodeBlock for each variant showing the JSX

    **3. InputField section**
    - ShowcaseSection title: "InputField"
    - Show: InputField with label only, with label + helper text, with label + error message, disabled InputField
    - CodeBlock for each variant

    **4. Select section**
    - ShowcaseSection title: "Select"
    - Render the SelectDemo client component
    - CodeBlock showing basic Select usage, grouped options, disabled

    **5. SearchableSelect section**
    - ShowcaseSection title: "SearchableSelect"
    - Part of SelectDemo or separate section
    - CodeBlock showing SearchableSelect usage

    **6. Toggle section**
    - ShowcaseSection title: "Toggle"
    - Render the ToggleDemo client component
    - CodeBlock showing Toggle sizes, with label, disabled states

    Each section follows the pattern: description -> visual demo -> code snippet(s).
    Use ComponentGrid where appropriate for side-by-side variant display.
    Import all components from @/components/ui (barrel imports).
  </action>
  <verify>
    Run `pnpm build` to verify no compilation errors. Navigate to /showcase/inputs to see all demos.
  </verify>
  <done>
    - /showcase/inputs shows all 5 input components
    - All variants and states demonstrated (SHW-08, SHW-09)
    - Code snippets for every variant (SHW-10)
    - Interactive demos for Toggle and Select via client components
    - Consistent ShowcaseSection pattern throughout
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes
2. /showcase/inputs renders all 5 component sections
3. Toggle demo toggles interactively in all sizes
4. Select demo opens, selects, and SearchableSelect filters
5. All sections have code snippets
6. Consistent ShowcaseSection pattern used
</verification>

<success_criteria>
- SHW-02 satisfied: Input, InputField, Select, SearchableSelect, Toggle all demonstrated
- SHW-08: All variants shown (sizes, states)
- SHW-09: All states shown (default, focus, error, disabled)
- SHW-10: Consistent section pattern
</success_criteria>

<output>
After completion, create `.planning/phases/43-showcase-enhancement/43-03-SUMMARY.md`
</output>
