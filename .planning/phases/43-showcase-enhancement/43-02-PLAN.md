---
phase: 43-showcase-enhancement
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - src/app/(marketing)/showcase/page.tsx
  - src/app/(marketing)/showcase/_components/token-swatch.tsx
autonomous: true

must_haves:
  truths:
    - "/showcase page displays all color token swatches with variable names"
    - "Two-tier architecture (primitive -> semantic) mapping is visualized"
    - "Typography, spacing, shadow, radius, animation, and gradient tokens are all represented"
    - "Token values are shown via CSS variables (not hardcoded hex values)"
  artifacts:
    - path: "src/app/(marketing)/showcase/page.tsx"
      provides: "Complete token visualization page"
      min_lines: 100
    - path: "src/app/(marketing)/showcase/_components/token-swatch.tsx"
      provides: "Reusable token swatch component"
      min_lines: 15
  key_links:
    - from: "src/app/(marketing)/showcase/page.tsx"
      to: "showcase-section.tsx"
      via: "import ShowcaseSection"
      pattern: "ShowcaseSection"
    - from: "src/app/(marketing)/showcase/page.tsx"
      to: "token-swatch.tsx"
      via: "import TokenSwatch"
      pattern: "TokenSwatch"
---

<objective>
Build the /showcase main page (SHW-01) with complete token visualization covering all design system tokens: colors, typography, spacing, shadows, radius, animation, and gradients.

Purpose: The tokens page is the design system's reference card — developers see every available token at a glance with its name and visual representation. Demonstrates the two-tier architecture (primitive -> semantic).
Output: Fully populated /showcase page showing all Virtuna design tokens.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-showcase-enhancement/43-RESEARCH.md
@.planning/phases/43-showcase-enhancement/43-01-SUMMARY.md

Key references:
@src/app/globals.css — THE source of truth for all tokens. Read this file carefully to extract every token name and value.
@src/app/(marketing)/showcase/_components/showcase-section.tsx
@src/app/(marketing)/showcase/_components/code-block.tsx
@src/app/(marketing)/showcase/_components/component-grid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TokenSwatch component</name>
  <files>src/app/(marketing)/showcase/_components/token-swatch.tsx</files>
  <action>
    Create a server component for displaying color tokens:

    **TokenSwatch** — Shows a color swatch with token name and optional semantic mapping
    - Props: `{ name: string; cssVar: string; semantic?: string; className?: string }`
    - Render: A row with a 40x40 rounded swatch (use inline `style={{ backgroundColor: var(--{cssVar}) }}` via `var()` CSS function) + token name in monospace + optional semantic label below in muted text
    - The swatch must use the actual CSS variable so it stays in sync with globals.css

    Also create a **TokenRow** variant for non-color tokens (typography, spacing, etc.):
    - Props: `{ name: string; value: string; preview?: React.ReactNode }`
    - Render: A row showing token name (monospace), value, and optional preview element
  </action>
  <verify>TypeScript compiles without errors.</verify>
  <done>TokenSwatch and TokenRow components ready for use in the tokens page</done>
</task>

<task type="auto">
  <name>Task 2: Build the /showcase tokens page</name>
  <files>src/app/(marketing)/showcase/page.tsx</files>
  <action>
    Build the complete tokens page. Read globals.css thoroughly FIRST to get all token names and values. The page is a server component.

    Structure the page with these ShowcaseSection blocks:

    **1. Page Header**
    - Heading level={1}: "Design Tokens"
    - Text description: Brief explanation of the two-tier token system

    **2. Color Tokens — Coral Scale**
    - Show coral-100 through coral-900 as swatches in a grid
    - Use CSS variables: `var(--color-coral-100)` through `var(--color-coral-900)`

    **3. Color Tokens — Gray Scale**
    - Show gray-50 through gray-950 as swatches
    - Use CSS variables from globals.css

    **4. Semantic Color Tokens**
    - Show status colors: success, warning, error, info
    - Show semantic mappings: background, surface, surface-elevated, foreground, foreground-secondary, foreground-muted, accent, border, border-glass
    - For each semantic token, show which primitive it maps to (e.g., "background = gray-950")

    **5. Typography Tokens**
    - Font families: display (Funnel Display), sans (Satoshi), mono
    - Font sizes: xs through display — show each size with its actual rendered text
    - Font weights, line heights, letter spacing values

    **6. Spacing Tokens**
    - Show spacing scale with visual bars (div with width set to the spacing value)
    - List from 0 to 24 (or whatever globals.css defines)

    **7. Shadow Tokens**
    - Show each shadow (sm, md, lg, xl, glass, glow-accent, button) applied to a small card
    - Use inline style with `var(--shadow-{name})`

    **8. Border Radius Tokens**
    - Show each radius (none, sm, md, lg, xl, 2xl, full) applied to a box
    - Visual demonstration of each radius

    **9. Animation Tokens**
    - List duration values (fast, normal, slow) with their ms values
    - List easing functions
    - Show z-index scale

    **10. Gradient Tokens**
    - Show each gradient (coral, card-bg, overlay, glow-coral, navbar, feature) as a swatch panel
    - Use inline style with the gradient values from globals.css

    For each section, include a CodeBlock showing how to use the tokens in Tailwind classes (e.g., `bg-coral-500`, `text-foreground`, `shadow-glass`).

    Important: Use `var()` CSS functions for all color swatches — never hardcode hex values. This ensures the showcase stays in sync with globals.css.
  </action>
  <verify>
    Run `pnpm build` to verify no compilation errors. Navigate to /showcase to see the tokens page.
  </verify>
  <done>
    - /showcase page shows all token categories with visual representations
    - Two-tier architecture is visualized (primitive -> semantic mapping)
    - All tokens use CSS variables (not hardcoded values)
    - Consistent ShowcaseSection pattern used throughout
    - Code snippets show Tailwind usage for each token category
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes
2. /showcase page renders all token categories
3. Color swatches use CSS variables (inspect element confirms `var(--color-*)`)
4. Semantic tokens show their primitive source
5. All sections use ShowcaseSection wrapper
6. Code snippets present for each token category
</verification>

<success_criteria>
- /showcase displays complete token visualization (SHW-01)
- Two-tier architecture is clearly communicated
- All 10 token categories represented
- CSS variables used throughout (no hardcoded values)
</success_criteria>

<output>
After completion, create `.planning/phases/43-showcase-enhancement/43-02-SUMMARY.md`
</output>
