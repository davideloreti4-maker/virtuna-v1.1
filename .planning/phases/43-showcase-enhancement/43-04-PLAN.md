---
phase: 43-showcase-enhancement
plan: 04
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - src/app/(marketing)/showcase/navigation/page.tsx
  - src/app/(marketing)/showcase/feedback/page.tsx
  - src/app/(marketing)/showcase/_components/toast-demo.tsx
  - src/app/(marketing)/showcase/_components/dialog-demo.tsx
  - src/app/(marketing)/showcase/_components/spinner-demo.tsx
autonomous: true

must_haves:
  truths:
    - "/showcase/navigation shows Tabs in all sizes with TabsList, TabsTrigger, TabsContent"
    - "/showcase/navigation shows CategoryTabs with icons and counts"
    - "/showcase/navigation shows Kbd in all sizes and highlighted variant"
    - "/showcase/navigation shows ShortcutBadge with various key combos"
    - "/showcase/feedback shows Toast in all 5 variants via interactive trigger buttons"
    - "/showcase/feedback shows Dialog in all sizes with header/footer/description"
    - "/showcase/feedback shows Spinner in all sizes plus determinate progress"
    - "/showcase/feedback shows Badge in all variants and sizes"
  artifacts:
    - path: "src/app/(marketing)/showcase/navigation/page.tsx"
      provides: "Navigation components showcase"
      min_lines: 80
    - path: "src/app/(marketing)/showcase/feedback/page.tsx"
      provides: "Feedback components showcase"
      min_lines: 100
    - path: "src/app/(marketing)/showcase/_components/toast-demo.tsx"
      provides: "Interactive toast trigger demo (client)"
      min_lines: 20
    - path: "src/app/(marketing)/showcase/_components/dialog-demo.tsx"
      provides: "Interactive dialog demo (client)"
      min_lines: 20
    - path: "src/app/(marketing)/showcase/_components/spinner-demo.tsx"
      provides: "Spinner with progress slider demo (client)"
      min_lines: 15
  key_links:
    - from: "feedback/page.tsx"
      to: "toast-demo.tsx"
      via: "client island for interactive toast"
      pattern: "ToastDemo"
    - from: "feedback/page.tsx"
      to: "dialog-demo.tsx"
      via: "client island for interactive dialog"
      pattern: "DialogDemo"
    - from: "navigation/page.tsx"
      to: "@/components/ui"
      via: "import Tabs, CategoryTabs, Kbd, ShortcutBadge"
      pattern: "from \"@/components/ui\""
---

<objective>
Build the /showcase/navigation page (SHW-03) and /showcase/feedback page (SHW-04) with all component variants, states, and code snippets.

Purpose: Navigation covers Tabs/Kbd/ShortcutBadge components. Feedback covers Toast/Dialog/Spinner/Badge — the interactive notification and status components. These two pages are grouped because they are medium-sized and fit comfortably in one plan.
Output: Two complete showcase pages with interactive client component islands for Toast, Dialog, and Spinner.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-showcase-enhancement/43-RESEARCH.md
@.planning/phases/43-showcase-enhancement/43-01-SUMMARY.md

Key references — read these for props/variants:
@src/components/ui/tabs.tsx — Tabs, TabsList, TabsTrigger, TabsContent
@src/components/ui/category-tabs.tsx — CategoryTabs, CategoryTab type
@src/components/ui/kbd.tsx — Kbd sizes, highlighted variant
@src/components/ui/shortcut-badge.tsx — ShortcutBadge props
@src/components/ui/toast.tsx — ToastProvider, useToast, ToastVariant
@src/components/ui/dialog.tsx — Dialog, DialogContent sizes
@src/components/ui/spinner.tsx — Spinner sizes, progress prop
@src/components/ui/badge.tsx — Badge variants, sizes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create client demo components for feedback page</name>
  <files>
    src/app/(marketing)/showcase/_components/toast-demo.tsx
    src/app/(marketing)/showcase/_components/dialog-demo.tsx
    src/app/(marketing)/showcase/_components/spinner-demo.tsx
  </files>
  <action>
    Create three client component islands:

    **toast-demo.tsx** — "use client"
    - Import useToast from @/components/ui
    - Render 5 buttons (one per variant: default, success, warning, error, info)
    - Each button triggers `toast({ variant, title, description })` with appropriate text
    - Style buttons using Button component from @/components/ui with secondary variant
    - Layout: flex gap-3 wrap
    - Note: The ToastProvider must already be in the tree. Check if it's in the marketing layout; if not, wrap this demo in ToastProvider.

    **dialog-demo.tsx** — "use client"
    - Import Dialog, DialogTrigger, DialogContent, DialogHeader, DialogFooter, DialogTitle, DialogDescription, DialogClose from @/components/ui
    - Show multiple Dialog instances triggered by buttons:
      1. Small dialog (size="sm")
      2. Medium dialog (default)
      3. Large dialog (size="lg")
      4. Dialog with full header, description, footer with action buttons
    - Each trigger is a Button with secondary variant
    - Layout: flex gap-3 wrap

    **spinner-demo.tsx** — "use client"
    - Import Spinner from @/components/ui
    - Import useState
    - Show a slider (range input) controlling a determinate Spinner's progress (0-100)
    - Show the current progress value as text
    - Style the slider with accent color
  </action>
  <verify>TypeScript compiles without errors.</verify>
  <done>Three interactive demo client components ready for embedding in showcase pages</done>
</task>

<task type="auto">
  <name>Task 2: Build navigation and feedback showcase pages</name>
  <files>
    src/app/(marketing)/showcase/navigation/page.tsx
    src/app/(marketing)/showcase/feedback/page.tsx
  </files>
  <action>
    **Navigation page** (server component) — Read component source files first for all props:

    1. Page Header: Heading "Navigation" + description

    2. **Tabs section**
    - ShowcaseSection title: "Tabs"
    - Demo: Tabs component with 3 tab items showing content switching
    - Tabs is already a Radix client component, so it works without a wrapper
    - Show default tabs usage
    - CodeBlock with usage example

    3. **CategoryTabs section**
    - ShowcaseSection title: "CategoryTabs"
    - Demo: CategoryTabs with icon-labeled tabs and count badges
    - Define sample categories with icons (from Phosphor) and counts
    - CodeBlock with usage

    4. **Kbd section**
    - ShowcaseSection title: "Kbd"
    - Show all sizes (sm, md, lg) using ComponentGrid
    - Show highlighted variant
    - Show common key examples: Esc, Enter, Tab, Space, arrow keys, modifier keys
    - CodeBlock for each variant

    5. **ShortcutBadge section**
    - ShowcaseSection title: "ShortcutBadge"
    - Show various key combinations: Cmd+K, Cmd+Shift+P, Ctrl+C, etc.
    - Show different separator variants if available
    - CodeBlock for each

    **Feedback page** (server component):

    1. Page Header: Heading "Feedback" + description

    2. **Badge section**
    - ShowcaseSection title: "Badge"
    - Show all variants: default, success, warning, error, info in a row
    - Show all sizes: sm, md
    - CodeBlock for variants and sizes

    3. **Toast section**
    - ShowcaseSection title: "Toast"
    - Render ToastDemo client component (wrapping in ToastProvider if needed)
    - CodeBlock showing useToast usage for each variant

    4. **Dialog section**
    - ShowcaseSection title: "Dialog"
    - Render DialogDemo client component
    - CodeBlock showing Dialog usage with different sizes and content

    5. **Spinner section**
    - ShowcaseSection title: "Spinner"
    - Static demos: Spinner in sm, md, lg sizes (indeterminate)
    - Interactive demo: SpinnerDemo client component for determinate progress
    - CodeBlock for both indeterminate and determinate usage

    Both pages use ShowcaseSection for every section and CodeBlock for every code example.
  </action>
  <verify>
    Run `pnpm build` to verify no compilation errors. Navigate to /showcase/navigation and /showcase/feedback.
  </verify>
  <done>
    - /showcase/navigation shows Tabs, CategoryTabs, Kbd, ShortcutBadge (SHW-03)
    - /showcase/feedback shows Badge, Toast, Dialog, Spinner (SHW-04)
    - All variants and states demonstrated (SHW-08, SHW-09)
    - Code snippets for every variant
    - Interactive demos for Toast, Dialog, Spinner
    - Consistent ShowcaseSection pattern throughout (SHW-10)
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes
2. /showcase/navigation renders 4 component sections with demos
3. /showcase/feedback renders 4 component sections with demos
4. Toast triggers work — clicking buttons shows toast notifications
5. Dialog opens in all sizes
6. Spinner progress slider works
7. All sections have code snippets
</verification>

<success_criteria>
- SHW-03 satisfied: Tabs, CategoryTabs, Kbd, ShortcutBadge all demonstrated
- SHW-04 satisfied: Badge, Toast, Dialog, Spinner all demonstrated
- SHW-08, SHW-09: All variants and states shown
- SHW-10: Consistent section pattern
</success_criteria>

<output>
After completion, create `.planning/phases/43-showcase-enhancement/43-04-SUMMARY.md`
</output>
