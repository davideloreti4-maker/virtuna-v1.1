---
phase: 43-showcase-enhancement
plan: 06
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - src/app/(marketing)/showcase/utilities/page.tsx
  - src/app/(marketing)/showcase/_components/motion-demo.tsx
  - src/app/(marketing)/showcase/_components/traffic-lights-demo.tsx
autonomous: true

must_haves:
  truths:
    - "/showcase/utilities shows FadeIn, FadeInUp, SlideUp with configurable delay/duration/distance"
    - "/showcase/utilities shows StaggerReveal with grid stagger demo"
    - "/showcase/utilities shows HoverScale with custom scale values"
    - "/showcase/utilities shows NoiseTexture and ChromaticAberration effects"
    - "/showcase/utilities shows GradientGlow in all 6 colors and GradientMesh"
    - "/showcase/utilities shows TrafficLights in all sizes"
  artifacts:
    - path: "src/app/(marketing)/showcase/utilities/page.tsx"
      provides: "Complete utilities showcase page"
      min_lines: 120
    - path: "src/app/(marketing)/showcase/_components/motion-demo.tsx"
      provides: "Interactive motion/hover demo (client)"
      min_lines: 20
    - path: "src/app/(marketing)/showcase/_components/traffic-lights-demo.tsx"
      provides: "Interactive TrafficLights demo (client)"
      min_lines: 15
  key_links:
    - from: "utilities/page.tsx"
      to: "@/components/motion"
      via: "import FadeIn, FadeInUp, SlideUp, StaggerReveal, HoverScale"
      pattern: "from \"@/components/motion\""
    - from: "utilities/page.tsx"
      to: "@/components/effects"
      via: "import NoiseTexture, ChromaticAberration"
      pattern: "from \"@/components/effects\""
    - from: "utilities/page.tsx"
      to: "@/components/primitives"
      via: "import GradientGlow, GradientMesh, TrafficLights"
      pattern: "from \"@/components/primitives\""
---

<objective>
Build the /showcase/utilities page (SHW-07) demonstrating all motion components, visual effects, gradient primitives, and TrafficLights.

Purpose: Utilities showcases the animation, effects, and decorative primitives that enhance the design system. This is the largest showcase page, covering 3 component subsystems (motion, effects, primitives).
Output: Complete utilities page with interactive motion demos and visual effects demonstrations.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-showcase-enhancement/43-RESEARCH.md
@.planning/phases/43-showcase-enhancement/43-01-SUMMARY.md

Key references — read these for props/variants:
@src/components/motion/fade-in.tsx — FadeIn: delay, duration, distance, as
@src/components/motion/fade-in-up.tsx — FadeInUp: delay, duration, distance, as
@src/components/motion/slide-up.tsx — SlideUp: delay, duration, distance, as
@src/components/motion/stagger-reveal.tsx — StaggerReveal + StaggerReveal.Item
@src/components/motion/hover-scale.tsx — HoverScale: scale, tapScale
@src/components/effects/noise-texture.tsx — NoiseTexture: opacity, frequency, octaves
@src/components/effects/chromatic-aberration.tsx — ChromaticAberration: offset, intensity
@src/components/primitives/GradientGlow.tsx — GradientGlow: color, intensity, position, animate
@src/components/primitives/GradientMesh.tsx — GradientMesh props
@src/components/primitives/TrafficLights.tsx — TrafficLights: size, disabled, interactive
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create interactive utility demo components</name>
  <files>
    src/app/(marketing)/showcase/_components/motion-demo.tsx
    src/app/(marketing)/showcase/_components/traffic-lights-demo.tsx
  </files>
  <action>
    Create client component islands:

    **motion-demo.tsx** — "use client"
    - Import HoverScale from @/components/motion
    - Create a HoverScale demo with a card inside that responds to hover/tap
    - Show default scale (1.02) and a more dramatic scale (1.05)
    - Show custom tapScale
    - Each demo card should be a styled div with text explaining "Hover me" / "Tap me"
    - Layout: flex gap-4

    **traffic-lights-demo.tsx** — "use client"
    - Import TrafficLights from @/components/primitives
    - Import useState
    - Show TrafficLights in all sizes: sm, md, lg
    - Show interactive TrafficLights (interactive prop) — clicking buttons triggers console log or visual feedback
    - Show disabled TrafficLights
    - Layout: flex gap-8 for size comparison
  </action>
  <verify>TypeScript compiles without errors.</verify>
  <done>Interactive motion and TrafficLights demo components ready</done>
</task>

<task type="auto">
  <name>Task 2: Build the /showcase/utilities page</name>
  <files>src/app/(marketing)/showcase/utilities/page.tsx</files>
  <action>
    Build the complete utilities page. This is the largest showcase page. Read all component source files first. The page itself needs "use client" since motion components use framer-motion hooks, OR keep as server component and wrap motion demos in client islands. Prefer keeping page as server component where possible.

    **1. Page Header**: Heading "Utilities" + description covering motion, effects, and decorative primitives

    **MOTION COMPONENTS SECTION**

    **2. FadeIn section**
    - ShowcaseSection title: "FadeIn"
    - Show FadeIn with default settings — content that fades in on scroll/mount
    - Show FadeIn with custom delay (0.2, 0.4, 0.6) and custom distance (10, 20, 30)
    - Note: FadeIn uses framer-motion, which requires client context. Wrap the demo in a client component if the page is a server component, OR mark the page as "use client" if simpler.
    - CodeBlock showing FadeIn props

    **3. FadeInUp section**
    - ShowcaseSection title: "FadeInUp"
    - Show FadeInUp with default props
    - Show FadeInUp with custom distance and duration
    - Show FadeInUp with `as="span"` or `as="div"` if it has that prop
    - CodeBlock for each variant

    **4. SlideUp section**
    - ShowcaseSection title: "SlideUp"
    - Show SlideUp with default and custom distance/delay
    - CodeBlock for usage

    **5. StaggerReveal section**
    - ShowcaseSection title: "StaggerReveal"
    - Show a grid of 6-8 items that stagger in sequence using StaggerReveal + StaggerReveal.Item (or StaggerRevealItem)
    - Each item should be a small card/box with a number
    - CodeBlock showing compound component pattern

    **6. HoverScale section**
    - ShowcaseSection title: "HoverScale"
    - Render MotionDemo client component
    - CodeBlock for HoverScale with different scale/tapScale values

    **EFFECTS SECTION**

    **7. NoiseTexture section**
    - ShowcaseSection title: "NoiseTexture"
    - Show NoiseTexture overlaid on colored backgrounds with different opacity (0.03, 0.05, 0.08) and frequency settings
    - Each demo: a colored div with NoiseTexture inside (position relative + absolute)
    - CodeBlock for props

    **8. ChromaticAberration section**
    - ShowcaseSection title: "ChromaticAberration"
    - Show text with ChromaticAberration effect at different offset/intensity values
    - Use a heading or large text so the effect is visible
    - CodeBlock for props

    **PRIMITIVES SECTION**

    **9. GradientGlow section**
    - ShowcaseSection title: "GradientGlow"
    - Show all 6 colors (coral, blue, purple, green, amber, rose — check actual colorMap)
    - Show 3 intensity levels (subtle, medium, strong — check actual props)
    - Show animated variant
    - Each in a contained box so glows are visible and don't overlap
    - CodeBlock for colors, intensities, animation

    **10. GradientMesh section**
    - ShowcaseSection title: "GradientMesh"
    - Show GradientMesh with multi-color blend
    - Show animated GradientMesh
    - CodeBlock for usage

    **11. TrafficLights section**
    - ShowcaseSection title: "TrafficLights"
    - Render TrafficLightsDemo client component
    - CodeBlock for sizes, interactive, disabled

    For motion component demos: Since FadeIn/FadeInUp/SlideUp/StaggerReveal are framer-motion components, they need client context. The simplest approach is to mark the entire page as "use client" since MOST of the content on this page needs interactivity/motion. This is the one page where "use client" at the page level is justified.
  </action>
  <verify>
    Run `pnpm build` to verify no compilation errors. Navigate to /showcase/utilities.
  </verify>
  <done>
    - /showcase/utilities shows all 11 component sections
    - Motion components animate on scroll/hover
    - Effects are visually demonstrated with different parameters
    - GradientGlow shows all colors and intensities
    - TrafficLights are interactive
    - All sections have code snippets
    - SHW-07 fully satisfied
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes
2. /showcase/utilities renders all 11 sections
3. FadeIn/FadeInUp/SlideUp animate on scroll or mount
4. StaggerReveal items appear sequentially
5. HoverScale responds to hover
6. NoiseTexture grain visible on colored backgrounds
7. GradientGlow shows all 6 colors
8. TrafficLights are interactive and sized correctly
9. All sections have code snippets
</verification>

<success_criteria>
- SHW-07 satisfied: All motion, effects, and primitive utility components demonstrated
- SHW-08: All variants shown (sizes, colors, intensities, animation modes)
- SHW-09: All states shown (animated, static, disabled for TrafficLights)
- SHW-10: Consistent section pattern via ShowcaseSection
</success_criteria>

<output>
After completion, create `.planning/phases/43-showcase-enhancement/43-06-SUMMARY.md`
</output>
