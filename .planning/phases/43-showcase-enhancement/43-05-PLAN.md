---
phase: 43-showcase-enhancement
plan: 05
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - src/app/(marketing)/showcase/data-display/page.tsx
  - src/app/(marketing)/showcase/layout-components/page.tsx
autonomous: true

must_haves:
  truths:
    - "/showcase/data-display shows Avatar in all 5 sizes with fallback text"
    - "/showcase/data-display shows AvatarGroup with max truncation"
    - "/showcase/data-display shows Skeleton in various shapes"
    - "/showcase/data-display shows Card, GlassCard with header/content/footer"
    - "/showcase/data-display shows ExtensionCard with gradient themes and TestimonialCard"
    - "/showcase/layout-components shows GlassPanel with all 7 blur levels"
    - "/showcase/layout-components shows Divider horizontal, vertical, and with label"
    - "Glass component demos include colorful backgrounds to demonstrate blur effect"
  artifacts:
    - path: "src/app/(marketing)/showcase/data-display/page.tsx"
      provides: "Data display components showcase"
      min_lines: 100
    - path: "src/app/(marketing)/showcase/layout-components/page.tsx"
      provides: "Layout components showcase"
      min_lines: 80
  key_links:
    - from: "data-display/page.tsx"
      to: "@/components/ui"
      via: "import Avatar, AvatarGroup, Skeleton, Card, GlassCard, ExtensionCard, TestimonialCard"
      pattern: "from \"@/components/ui\""
    - from: "layout-components/page.tsx"
      to: "@/components/primitives"
      via: "import GlassPanel for blur demos"
      pattern: "from \"@/components/primitives\""
---

<objective>
Build the /showcase/data-display page (SHW-05) and /showcase/layout-components page (SHW-06) demonstrating Avatar, Skeleton, Card variants, ExtensionCard, TestimonialCard, GlassPanel, and Divider.

Purpose: Data display covers content presentation components; Layout covers structural/container components. Both are primarily static (no interactive demos needed), keeping them server components.
Output: Two complete showcase pages with all variants, states, and code snippets.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-showcase-enhancement/43-RESEARCH.md
@.planning/phases/43-showcase-enhancement/43-01-SUMMARY.md

Key references — read these for props/variants:
@src/components/ui/avatar.tsx — Avatar sizes, fallback, AvatarGroup max prop
@src/components/ui/skeleton.tsx — Skeleton component
@src/components/ui/card.tsx — Card, GlassCard, CardHeader, CardContent, CardFooter
@src/components/ui/extension-card.tsx — ExtensionCard, GRADIENT_THEMES
@src/components/ui/testimonial-card.tsx — TestimonialCard props
@src/components/primitives/GlassPanel.tsx — GlassPanel blur levels (none, xs, sm, md, lg, xl, 2xl), tint, borderGlow, innerGlow
@src/components/ui/divider.tsx — Divider orientation, label
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the /showcase/data-display page</name>
  <files>src/app/(marketing)/showcase/data-display/page.tsx</files>
  <action>
    Build the complete data display page as a server component. Read component source files first.

    **1. Page Header**: Heading "Data Display" + description

    **2. Avatar section**
    - ShowcaseSection title: "Avatar"
    - Show all 5 sizes: xs, sm, md, lg, xl using ComponentGrid
    - Show Avatar with image src (use placeholder URLs like `https://i.pravatar.cc/120?u=1`)
    - Show Avatar with fallback text (initials when no image)
    - Show Avatar with AvatarRoot + AvatarImage + AvatarFallback (low-level API)
    - CodeBlock for convenience API and low-level API

    **3. AvatarGroup section**
    - ShowcaseSection title: "AvatarGroup"
    - Show AvatarGroup with 5 avatars, max=3 (shows +2 overflow)
    - Show AvatarGroup with max=5 (no overflow)
    - CodeBlock showing AvatarGroup usage

    **4. Skeleton section**
    - ShowcaseSection title: "Skeleton"
    - Show rectangular skeleton (default), circular skeleton (rounded-full), text-line skeletons (multiple thin rectangles), card-like skeleton composition
    - Show the shimmer animation in action
    - CodeBlock for each shape variant

    **5. Card section**
    - ShowcaseSection title: "Card"
    - Show: basic Card, Card with CardHeader + CardContent + CardFooter
    - Show: different padding/content configurations
    - CodeBlock for each pattern

    **6. GlassCard section**
    - ShowcaseSection title: "GlassCard"
    - IMPORTANT: Include decorative colored background elements (gradient blobs, colored circles with blur) BEHIND each GlassCard demo so the glassmorphism blur effect is visible
    - Show GlassCard with content, GlassCard with glow prop
    - CodeBlock showing GlassCard usage

    **7. ExtensionCard section**
    - ShowcaseSection title: "ExtensionCard"
    - Show all 3 gradient themes: coral, purple, blue (from GRADIENT_THEMES)
    - Include sample metadata (icon, title, subtitle, installed count)
    - CodeBlock for each theme

    **8. TestimonialCard section**
    - ShowcaseSection title: "TestimonialCard"
    - Show default TestimonialCard with quote, author, role
    - Show featured variant if available
    - CodeBlock for each variant
  </action>
  <verify>
    Run `pnpm build` to verify no compilation errors. Navigate to /showcase/data-display.
  </verify>
  <done>All data display components demonstrated with variants, states, and code snippets</done>
</task>

<task type="auto">
  <name>Task 2: Build the /showcase/layout-components page</name>
  <files>src/app/(marketing)/showcase/layout-components/page.tsx</files>
  <action>
    Build the complete layout components page as a server component.

    **1. Page Header**: Heading "Layout" + description

    **2. GlassPanel section**
    - ShowcaseSection title: "GlassPanel"
    - CRITICAL: For EVERY GlassPanel demo, include colorful decorative backgrounds (gradient circles, colored blobs) BEHIND the panel. Without these, GlassPanel looks like an opaque dark box — the backdrop-filter has nothing to blur.
    - Show all 7 blur levels in a grid: none, xs, sm, md, lg, xl, 2xl — each labeled
    - Show tint color variations (if GlassPanel has tint prop): neutral, coral, blue
    - Show borderGlow and innerGlow props
    - Show different opacity levels
    - Each demo panel should be approximately the same size for consistent comparison
    - CodeBlock for blur levels, tint, glow options

    **3. Divider section**
    - ShowcaseSection title: "Divider"
    - Show horizontal divider (default)
    - Show vertical divider (inside a flex row with content on each side)
    - Show divider with label text
    - CodeBlock for each variant

    **4. Container section** (if Container component exists — check)
    - ShowcaseSection title: "Container"
    - Show default, narrow, wide sizes
    - CodeBlock for each

    Use ShowcaseSection for every section and CodeBlock for every code example. Use ComponentGrid for side-by-side blur level comparison.
  </action>
  <verify>
    Run `pnpm build` to verify no compilation errors. Navigate to /showcase/layout-components.
  </verify>
  <done>
    - /showcase/layout-components shows GlassPanel, Divider (and Container if it exists)
    - GlassPanel demos have colorful backgrounds showing blur effect
    - All 7 blur levels visible and labeled
    - All variants and states demonstrated
    - Code snippets for every variant
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes
2. /showcase/data-display renders all 8 component sections
3. /showcase/layout-components renders all sections
4. GlassCard and GlassPanel demos clearly show blur effects (not opaque dark boxes)
5. Avatar images load or show fallback text
6. Skeleton shimmer animation is visible
7. All sections have code snippets
</verification>

<success_criteria>
- SHW-05 satisfied: Avatar, AvatarGroup, Skeleton, Card, GlassCard, ExtensionCard, TestimonialCard
- SHW-06 satisfied: GlassPanel (all 7 blur levels), Divider, Container
- SHW-08, SHW-09: All variants and states shown
- SHW-10: Consistent section pattern
- Glass demos have visible blur effects
</success_criteria>

<output>
After completion, create `.planning/phases/43-showcase-enhancement/43-05-SUMMARY.md`
</output>
