---
phase: 43-showcase-enhancement
plan: 07
type: execute
wave: 3
depends_on: ["43-02", "43-03", "43-04", "43-05", "43-06"]
files_modified:
  - src/app/(marketing)/ui-showcase/page.tsx
  - src/app/(marketing)/ui-showcase/_components/phase-41-demos.tsx
  - src/app/(marketing)/ui-showcase/_components/phase-42-demos.tsx
autonomous: false

must_haves:
  truths:
    - "Old /ui-showcase page is removed — no duplicate showcase"
    - "/showcase is the single source of truth for component documentation"
    - "All 7 showcase pages render correctly with no build errors"
    - "Sidebar navigation works across all pages with active state highlighting"
  artifacts: []
  key_links: []
---

<objective>
Remove the old /ui-showcase page and visually verify the complete showcase system works end-to-end.

Purpose: The old /ui-showcase is superseded by /showcase — keeping both creates confusion about which is the source of truth. This plan cleans up the legacy page and does a final verification pass.
Output: Clean codebase with single showcase at /showcase, verified working.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-showcase-enhancement/43-01-SUMMARY.md
@.planning/phases/43-showcase-enhancement/43-02-SUMMARY.md
@.planning/phases/43-showcase-enhancement/43-03-SUMMARY.md
@.planning/phases/43-showcase-enhancement/43-04-SUMMARY.md
@.planning/phases/43-showcase-enhancement/43-05-SUMMARY.md
@.planning/phases/43-showcase-enhancement/43-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove old /ui-showcase page</name>
  <files>
    src/app/(marketing)/ui-showcase/page.tsx
    src/app/(marketing)/ui-showcase/_components/phase-41-demos.tsx
    src/app/(marketing)/ui-showcase/_components/phase-42-demos.tsx
  </files>
  <action>
    Delete the entire ui-showcase directory:
    ```bash
    rm -rf src/app/(marketing)/ui-showcase/
    ```

    Then verify no other files import from the deleted path:
    ```bash
    grep -r "ui-showcase" src/ --include="*.tsx" --include="*.ts"
    ```

    If any imports reference ui-showcase (e.g., links in the header), update them to point to /showcase instead.

    Also check for any references in the header component:
    ```bash
    grep -r "ui-showcase" src/components/layout/
    ```

    Update any navigation links from "/ui-showcase" to "/showcase".
  </action>
  <verify>
    Run `pnpm build` to confirm no broken imports. Verify /ui-showcase returns 404. Verify /showcase works.
  </verify>
  <done>Old /ui-showcase removed, no broken references, /showcase is the single source of truth</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Virtuna UI showcase system with 7 pages:
    1. /showcase — Design tokens (colors, typography, spacing, shadows, radius, animation, gradients)
    2. /showcase/inputs — Input, InputField, Select, SearchableSelect, Toggle
    3. /showcase/navigation — Tabs, CategoryTabs, Kbd, ShortcutBadge
    4. /showcase/feedback — Badge, Toast, Dialog, Spinner
    5. /showcase/data-display — Avatar, AvatarGroup, Skeleton, Card, GlassCard, ExtensionCard, TestimonialCard
    6. /showcase/layout-components — GlassPanel (7 blur levels), Divider
    7. /showcase/utilities — FadeIn, FadeInUp, SlideUp, StaggerReveal, HoverScale, NoiseTexture, ChromaticAberration, GradientGlow, GradientMesh, TrafficLights

    Old /ui-showcase page has been removed.
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` and navigate to http://localhost:3000/showcase
    2. Verify sidebar navigation shows all 7 pages with active state highlighting
    3. Click through each page and verify:
       - All components render correctly
       - Code snippets are syntax-highlighted and copy-to-clipboard works
       - Interactive demos work (Toast triggers, Dialog opens, Toggle toggles, Select selects)
       - GlassPanel/GlassCard demos show visible blur effects (not opaque dark boxes)
       - Motion components animate on scroll/hover
       - Token swatches show correct colors
    4. Verify /ui-showcase returns 404 (old page removed)
    5. Verify consistent spacing and section pattern across all pages
  </how-to-verify>
  <resume-signal>Type "approved" if the showcase looks correct, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm build` passes with no errors
2. /ui-showcase returns 404
3. /showcase renders with sidebar navigation
4. All 7 pages accessible and rendering correctly
5. No broken imports or references to ui-showcase
</verification>

<success_criteria>
- Old /ui-showcase completely removed
- /showcase is the single source of truth
- Human verification confirms all pages render correctly
- No build errors
</success_criteria>

<output>
After completion, create `.planning/phases/43-showcase-enhancement/43-07-SUMMARY.md`
</output>
