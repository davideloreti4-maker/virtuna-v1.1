---
phase: 06-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/backers-section.tsx
  - src/components/landing/case-study-section.tsx
  - src/components/landing/partnership-section.tsx
  - src/components/landing/comparison-chart.tsx
  - src/components/landing/persona-card.tsx
  - src/app/(marketing)/viz-test/page.tsx
  - src/app/(marketing)/viral-score-test/page.tsx
  - src/app/(marketing)/viral-results-showcase/page.tsx
  - src/app/(marketing)/primitives-showcase/page.tsx
  - src/components/visualization/GlassOrb.tsx
  - src/components/visualization/VisualizationCanvas.tsx
  - src/components/visualization/VisualizationContext.tsx
  - src/components/visualization/SplineOrb.tsx
  - src/components/visualization/index.ts
  - src/components/visualization/shaders/
  - src/components/effects/chromatic-aberration.tsx
  - src/components/effects/noise-texture.tsx
  - src/components/effects/index.ts
  - src/components/motion/page-transition.tsx
  - src/components/motion/frozen-router.tsx
  - src/components/motion/index.ts
  - src/components/layout/header.tsx
  - src/components/landing/testimonial-quote.tsx
  - src/components/landing/feature-card.tsx
  - src/components/hive/hive-layout.ts
  - src/components/hive/hive-renderer.ts
  - src/components/hive/hive-mock-data.ts
  - src/components/hive/hive-constants.ts
  - src/components/ui/accordion.tsx
  - src/lib/test-types.ts
autonomous: true

must_haves:
  truths:
    - "No dead landing components exist (backers-section, case-study-section, partnership-section, comparison-chart, persona-card are deleted)"
    - "No test/showcase pages exist outside /showcase (viz-test, viral-score-test, viral-results-showcase, primitives-showcase deleted)"
    - "No visualization components exist (only used by deleted viz-test page)"
    - "No societies.io references remain in code comments"
    - "Motion barrel export no longer exports PageTransition or FrozenRouter"
    - "Effects barrel export and component files are deleted (only used by showcase utilities)"
    - "The app builds cleanly with no broken imports after all deletions"
  artifacts:
    - path: "src/components/landing/index.ts"
      provides: "Clean barrel export with no references to deleted components"
    - path: "src/components/motion/index.ts"
      provides: "Barrel export without PageTransition/FrozenRouter"
  key_links:
    - from: "src/components/landing/index.ts"
      to: "Landing page components"
      via: "barrel exports"
      pattern: "export.*from"
    - from: "pnpm build"
      to: "All imports valid"
      via: "TypeScript compilation"
      pattern: "Build completed"
---

<objective>
Delete dead code inherited from the societies.io fork: unused landing components, test/showcase pages, visualization components, effects components, unused motion exports. Clean up societies.io references in comments. Verify clean build.

Purpose: Remove ~20 dead files and clean up references to the original societies.io codebase. The app should contain only code that is actually used in the MVP, with no orphaned components or misleading comments.

Output: A clean codebase with no dead imports, unused components, or societies.io fork artifacts.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish/06-RESEARCH.md (dead code inventory appendix)
@src/components/landing/index.ts
@src/components/motion/index.ts
@src/components/effects/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead files (landing components, test pages, visualization, effects)</name>
  <files>
    src/components/landing/backers-section.tsx
    src/components/landing/case-study-section.tsx
    src/components/landing/partnership-section.tsx
    src/components/landing/comparison-chart.tsx
    src/components/landing/persona-card.tsx
    src/app/(marketing)/viz-test/page.tsx
    src/app/(marketing)/viral-score-test/page.tsx
    src/app/(marketing)/viral-results-showcase/page.tsx
    src/app/(marketing)/primitives-showcase/page.tsx
    src/components/visualization/
    src/components/effects/
    src/components/motion/page-transition.tsx
    src/components/motion/frozen-router.tsx
  </files>
  <action>
    **Delete unused landing components (5 files):**
    ```bash
    rm src/components/landing/backers-section.tsx
    rm src/components/landing/case-study-section.tsx
    rm src/components/landing/partnership-section.tsx
    rm src/components/landing/comparison-chart.tsx
    rm src/components/landing/persona-card.tsx
    ```
    These are NOT exported from the barrel (`src/components/landing/index.ts`) and NOT imported anywhere. Safe to delete.

    **Delete test/showcase pages (4 directories):**
    ```bash
    rm -rf src/app/(marketing)/viz-test/
    rm -rf src/app/(marketing)/viral-score-test/
    rm -rf src/app/(marketing)/viral-results-showcase/
    rm -rf src/app/(marketing)/primitives-showcase/
    ```
    Keep `/showcase` -- it's the design system reference and remains in PUBLIC_PATHS.

    **Delete visualization components (entire directory):**
    ```bash
    rm -rf src/components/visualization/
    ```
    Only imported by the now-deleted viz-test page. Contains: GlassOrb, VisualizationCanvas, VisualizationContext, SplineOrb, shaders/, index.ts.

    **Delete effects components (entire directory):**
    ```bash
    rm -rf src/components/effects/
    ```
    Only imported by `/showcase/utilities/page.tsx`. Since the showcase utilities page imports from `@/components/effects/noise-texture` and `@/components/effects/chromatic-aberration`, we need to also remove those imports from the showcase utilities page.

    **Update showcase utilities page** to remove effects imports:
    - In `src/app/(marketing)/showcase/utilities/page.tsx`:
      - Remove the import lines for NoiseTexture and ChromaticAberration
      - Remove the entire NoiseTexture showcase section (the ShowcaseSection with id="noise-texture")
      - Remove the entire ChromaticAberration showcase section (the ShowcaseSection with id="chromatic-aberration")

    **Delete unused motion components (2 files):**
    ```bash
    rm src/components/motion/page-transition.tsx
    rm src/components/motion/frozen-router.tsx
    ```
    PageTransition is only exported from barrel, never imported in app code. FrozenRouter is only imported by PageTransition.

    **Update motion barrel export:**
    - In `src/components/motion/index.ts`, remove these two lines:
      ```
      export { FrozenRouter } from "./frozen-router";
      export { PageTransition } from "./page-transition";
      ```

    **IMPORTANT:** After all deletions, do NOT modify the landing barrel export `src/components/landing/index.ts` -- the deleted files (backers-section, case-study-section, partnership-section, comparison-chart, persona-card) are NOT exported from it. Only the following are in the barrel: FAQSection, FeatureCard, FeaturesSection, HeroSection, SocialProofSection, StatsSection, TestimonialQuote. All of these remain.
  </action>
  <verify>
    Run `npx tsc --noEmit` to check for broken imports. Then `pnpm build` to verify full build. If showcase/utilities has broken imports from effects deletion, fix them.
  </verify>
  <done>
    All dead files deleted. Barrel exports updated. No broken imports. Build passes cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean societies.io references from code comments</name>
  <files>
    src/components/layout/header.tsx
    src/components/landing/testimonial-quote.tsx
    src/components/landing/feature-card.tsx
    src/components/hive/hive-layout.ts
    src/components/hive/hive-renderer.ts
    src/components/hive/hive-mock-data.ts
    src/components/hive/hive-constants.ts
    src/components/ui/accordion.tsx
    src/lib/test-types.ts
  </files>
  <action>
    Replace all "societies.io" references in comments with "Virtuna" or remove the reference entirely. The goal is to remove fork artifacts, not change functionality.

    **Specific replacements (comment-only, no code changes):**

    1. `src/components/layout/header.tsx` (line 13):
       - "Header component matching societies.io design." -> "Header component following Raycast design language."

    2. `src/components/landing/testimonial-quote.tsx` (line 16):
       - "Styling matches societies.io reference:" -> "Styling follows Raycast design language:"

    3. `src/components/landing/feature-card.tsx` (line 17):
       - "Styling matches societies.io reference:" -> "Styling follows Raycast design language:"

    4. `src/components/hive/hive-layout.ts` (line 2):
       - "hive-layout.ts -- Deterministic scattered layout (societies.io style)" -> "hive-layout.ts -- Deterministic scattered layout"
    5. `src/components/hive/hive-layout.ts` (line 48):
       - "Compute a deterministic scattered layout (societies.io style)." -> "Compute a deterministic scattered layout."

    6. `src/components/hive/hive-mock-data.ts` (line 39):
       - "Generate a mock hive data tree (societies.io style -- 2 tiers only)." -> "Generate a mock hive data tree (2 tiers only)."

    7. `src/components/hive/hive-constants.ts` (line 21):
       - "Node sizes (societies.io style -- varied, larger nodes)" -> "Node sizes (varied, larger nodes)"
    8. `src/components/hive/hive-constants.ts` (line 31):
       - "Tier colors (societies.io style -- muted grays with per-node variation)" -> "Tier colors (muted grays with per-node variation)"
    9. `src/components/hive/hive-constants.ts` (line 48):
       - "Connection line opacity (societies.io style -- more prominent)" -> "Connection line opacity (more prominent)"

    10. `src/components/ui/accordion.tsx` (line 14):
        - "Styled to match societies.io FAQ section" -> "Styled to match Raycast design language"

    11. `src/lib/test-types.ts` (line 89):
        - "Order matches societies.io layout" -> "Order matches hive visualization layout"

    After making all replacements, do a final grep to confirm zero "societies.io" matches remain in the src/ directory.
  </action>
  <verify>
    Run `grep -r "societies.io" src/` to confirm zero matches. Run `pnpm build` to verify nothing broke (comment-only changes should be safe).
  </verify>
  <done>
    Zero "societies.io" references remain in the codebase. All comments reference either "Virtuna", "Raycast design language", or generic descriptions. Build still passes.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. `grep -r "societies.io" src/` returns zero matches
3. `ls src/components/landing/backers-section.tsx` returns "No such file" (and same for other deleted files)
4. `ls src/components/visualization/` returns "No such file or directory"
5. `ls src/components/effects/` returns "No such file or directory"
6. `ls src/components/motion/page-transition.tsx` returns "No such file"
7. Landing barrel export (`src/components/landing/index.ts`) unchanged (deleted files were never in it)
8. Motion barrel export has no PageTransition/FrozenRouter exports
9. `npx tsc --noEmit` passes with no type errors
</verification>

<success_criteria>
- All dead landing components deleted (5 files)
- All test pages deleted except /showcase (4 directories)
- Visualization directory deleted entirely
- Effects directory deleted entirely
- Unused motion components deleted (2 files) and barrel updated
- All societies.io comment references replaced
- Full build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish/06-02-SUMMARY.md`
</output>
