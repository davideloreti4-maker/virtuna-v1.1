---
phase: 03-ideas-tool
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - src/components/app/home/tool-chips.tsx
  - src/components/app/home/composer.tsx
  - src/components/app/home/platform-chip.tsx
  - src/components/thread/ideas-thread-view.tsx
  - src/hooks/queries/use-ideas-stream.ts
  - src/components/thread/idea-card-block.tsx
autonomous: false
requirements: [IDEAS-01, IDEAS-02, IDEAS-03, GROUND-03, PROFILE-01, THREAD-05]
must_haves:
  truths:
    - "The Idea chip is live; an empty send generates Auto from the profile, a typed send generates seeded around the topic — both route to the Ideas pipeline, never to the Test/analyze video flow"
    - "Generated idea cards render content-first (face + lead quote before band chip) via the fixed idea-card renderer, and rehydrate on reload"
    - "A platform chip on the composer sets the first-class platform param sent to the Ideas route"
    - "Develop this → calls the pinned /develop endpoint to carry the chosen idea into Hooks via the in-thread chain seam (anchor handoff)"
  artifacts:
    - path: "src/components/thread/ideas-thread-view.tsx"
      provides: "renders idea-card messages via MessageBlocks (content-first SSE consumer)"
      min_lines: 30
    - path: "src/hooks/queries/use-ideas-stream.ts"
      provides: "SSE consumer for the POST Ideas route via fetch + res.body.getReader() (content event then per-card score)"
      contains: "getReader"
    - path: "src/components/app/home/platform-chip.tsx"
      provides: "first-class platform selector (D-07) feeding the Ideas request"
      min_lines: 20
  key_links:
    - from: "src/components/app/home/composer.tsx"
      to: "POST /api/tools/ideas"
      via: "Idea-chip send (Auto=empty | seeded=typed) — NOT stream.start / pendingNavRef"
      pattern: "tools/ideas"
    - from: "src/components/thread/ideas-thread-view.tsx"
      to: "MessageBlocks / idea-card renderer"
      via: "renders the persisted + streamed idea-card blocks"
      pattern: "MessageBlocks"
    - from: "src/components/thread/idea-card-block.tsx"
      to: "POST /api/tools/ideas/develop"
      via: "Develop this → anchor write appends Hooks placeholder in-thread (D-15)"
      pattern: "develop"
---

<objective>
Wire the Ideas surface so a creator can actually USE it: flip the built-disabled Idea chip live (D-12), route an Idea-chip send to the new Ideas pipeline (empty → Auto, typed → seeded) WITHOUT arming the Test/analyze video navigation (RESEARCH Pitfall 5), add the first-class platform chip (D-07 — built but not wired in P1), render the streamed + persisted `idea-card` blocks content-first through the fixed renderer (Plan 02) in an Ideas thread view, and ship the "Develop this →" chain CTA that calls the pinned `/develop` endpoint (Plan 03) to hand the chosen idea to the P4 Hooks call in-thread via the anchor seam (D-15/THREAD-05/IDEAS-03).

This is the visible, UAT-checkpointed surface — it closes IDEAS-01/02/03 + GROUND-03 in product (the cards finally render). The Hooks GENERATION is P4; this plan ships the CTA + the in-thread chain handoff only.

Purpose: the creator gets idea cards on screen, content-first, grounded, chainable.
Output: live Idea chip + Auto/seeded routing, platform chip, Ideas thread view + SSE consumer, the chain CTA.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-ideas-tool/03-CONTEXT.md
@.planning/phases/03-ideas-tool/03-RESEARCH.md

# Depends on (read for the contracts these produced):
@.planning/phases/03-ideas-tool/03-02-SUMMARY.md
@.planning/phases/03-ideas-tool/03-03-SUMMARY.md

# UI source the tasks modify (read before editing):
@src/components/app/home/composer.tsx
@src/components/app/home/tool-chips.tsx
@src/components/thread/message-blocks.tsx
@src/components/thread/idea-card-block.tsx
@src/hooks/queries/use-expert-chat.ts
@src/app/(app)/home/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the Idea chip live + platform chip + Auto/seeded routing — never arm Test nav (D-12/D-07, Pitfall 5)</name>
  <files>src/components/app/home/tool-chips.tsx, src/components/app/home/composer.tsx, src/components/app/home/platform-chip.tsx</files>
  <read_first>
    - src/components/app/home/tool-chips.tsx — `CHIPS` array (`idea` has `enabled: false`); `MODEL_LABEL` (idea → "SIM-1 Flash"); the chip render + `onSelect`.
    - src/components/app/home/composer.tsx — `activeTool` state (`"test"` default), `handleSubmit` (the upload + tiktok_url paths that set `pendingNavRef.current = true` and call `stream.start`), `onSubmitForm`, `canSubmit`, `PLACEHOLDER_BY_TOOL`. CRITICAL: chip selection MUST NEVER arm `pendingNavRef` (Pitfall 5 / WR-05 — an Idea send must NOT navigate to `/analyze/[id]`).
    - 03-03-SUMMARY.md — the `POST /api/tools/ideas` request shape (ask, platform) + SSE event contract.
  </read_first>
  <action>
    In tool-chips.tsx: flip the `idea` chip to `enabled: true` (D-12). Leave `hooks`/`chat` disabled (P4/P5). Keep the model label "SIM-1 Flash" and the reserved cost slot.

    Create platform-chip.tsx: a small client selector for the first-class platform param (D-07) — `"tiktok" | "instagram" | "youtube"` (assembler's `platformSchema`), defaulting to the profile's platform when available else `"tiktok"`, changeable per message, styled per THEME-06 Raycast rules (sits next to the tool/model chips per D-07 — persistent composer chip, NOT a modal). Expose value + onChange.

    In composer.tsx: when `activeTool === "idea"`, route the send to the NEW Ideas pipeline instead of `stream.start`: empty input → Auto (POST `/api/tools/ideas` with empty/absent ask); typed input → seeded (POST with the typed ask). Pass the platform-chip value. CRITICAL (Pitfall 5): the Idea path MUST NOT set `pendingNavRef.current = true` and MUST NOT call `stream.start` — keep `pendingNavRef`/`stream.start` exclusive to the Test upload/URL paths so an Idea send never navigates to `/analyze/[id]`.

    VALIDATION TIER (WARNING-5): the Idea input is free text (a topic/angle), so relax the TikTok-URL client check for the idea tool — but this is a UX-only convenience. The REAL input validation (ask length cap + rate limit) lives server-side in the Plan-03 route (`/api/tools/ideas` returns 400 on an over-cap ask). Do NOT add client-only gating that the server does not independently enforce; the server is the trust boundary. (Optionally mirror a soft client-side max-length hint, but never rely on it.)

    Mount the platform chip in the composer chip row. Hand the SSE response to the Task 2 consumer/view.
  </action>
  <verify>
    <automated>npm run build && npx vitest run src/components/app/home 2>/dev/null || npm run build</automated>
  </verify>
  <done>
    The Idea chip is enabled; selecting it shows the idea placeholder + "SIM-1 Flash" model label + the platform chip. An empty Idea send POSTs Auto, a typed send POSTs seeded, both to `/api/tools/ideas` with the platform param. The Idea path never arms `pendingNavRef`/`stream.start` (no navigation to /analyze). The client TikTok-URL relax is UX-only; the server route independently validates the ask (WARNING-5). Test upload/URL paths unchanged. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ideas thread view + content-first SSE consumer + Develop this → chain CTA (IDEAS-02/03, THREAD-05, D-04/D-15)</name>
  <files>src/components/thread/ideas-thread-view.tsx, src/hooks/queries/use-ideas-stream.ts, src/components/thread/idea-card-block.tsx</files>
  <read_first>
    - src/hooks/queries/use-expert-chat.ts — the reference SSE reader: `fetch(url, { method, body })` → `res.body.getReader()` + `TextDecoder` → frame split on `\n\n`, `event:`/`data:` parse loop. The Ideas route is POST-with-body, so you MUST use `fetch` + `getReader()` (NOT `EventSource`, which is GET-only and cannot POST a body) — model the Ideas consumer on this exactly (RESEARCH Pattern 3, BLOCKER-1).
    - src/components/thread/message-blocks.tsx — `MessageBlocks({ body })` (re-validates each block, renders via `BLOCK_COMPONENTS` including the Plan-02 `idea-card`). This is the render surface — it is NOT yet consumed by any page, so this task is the first consumer.
    - src/components/thread/idea-card-block.tsx — the `IdeaCardRenderer` from Plan 02 (face + expand + needs-take badge + secondary band chip; the lead scroll-quote is on the FACE). This task ADDS the "Develop this →" CTA to it.
    - 03-03-SUMMARY.md — the SSE event contract (status → content[faces incl. lead quote] → per-card score) AND the pinned `/develop` endpoint path + payload shape (`POST /api/tools/ideas/develop`, `{ideaId?, anchor, platform}`).
  </read_first>
  <action>
    Create `use-ideas-stream.ts`: an SSE consumer for `POST /api/tools/ideas` modeled on `use-expert-chat.ts` — use `fetch(..., { method: "POST", body })` then `res.body.getReader()` + `TextDecoder` (NOT `EventSource` — POST needs a body, BLOCKER-1). Read the `content` event (the ≤3 gated card faces, each ALREADY carrying its lead scroll-quote per D-04/Plan 03) and render them immediately, then apply per-card `score` events to fill the band chip a beat later (content-first, D-04/IDEAS-02 — face + lead quote before band chip). Surface the legible status events ("generating…"/"scoring on your audience…") so the wait is legible (RESEARCH Pitfall 4). Track loading/error state.

    Create `ideas-thread-view.tsx`: renders the Ideas message(s) for the user's open thread via `MessageBlocks` (so persisted cards rehydrate on reload through the same double-validated path — THREAD-07 already proven) AND renders the in-flight streamed cards from `use-ideas-stream`. This is what mounts above the composer when an Idea run is active. Keep it THEME-06 flat-warm, the readable column width consistent with /home.

    Add the "Develop this →" CTA to `idea-card-block.tsx` (D-15/THREAD-05/IDEAS-03): a button on each card that calls the PINNED endpoint from Plan 03 — `POST /api/tools/ideas/develop` with `{ ideaId?, anchor: <this card's concept text>, platform }`. The endpoint writes the chosen idea as the assembler `anchor` and appends a Hooks placeholder message IN THE SAME open thread (RESEARCH Pattern 6: "Hooks coming in P4" affordance + the anchor handoff). Do NOT call a Hooks generation — that is P4. The CTA's job is the in-thread chain seam + anchor write only, via the pinned endpoint.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    `use-ideas-stream.ts` consumes the Ideas SSE via `fetch` + `res.body.getReader()` content-first (faces + lead quote first, band chip after). `ideas-thread-view.tsx` renders streamed + persisted idea-card blocks via `MessageBlocks` (rehydrates on reload). The "Develop this →" CTA POSTs the pinned `/api/tools/ideas/develop` endpoint (anchor + platform) and appends an in-thread Hooks placeholder (generation deferred to P4). Build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the Ideas surface end-to-end (UAT)</name>
  <what-built>
    The live Idea chip + platform chip, the Auto/seeded send routing to the Ideas pipeline, content-first idea-card rendering (face + lead quote → band chip), the honest grounding line, the "needs your take" badge, and the "Develop this →" in-thread chain CTA (pinned /develop endpoint). (Claude has already run `npm run build` + the unit/integration suites green; this verifies the live UX.)
  </what-built>
  <how-to-verify>
    1. Run the dev server (`npm run dev`) and sign in; open `/home`.
    2. Select the **Idea** chip. Confirm the model label reads "SIM-1 Flash" and a platform chip is visible. Confirm the placeholder switched to the idea copy.
    3. **Auto:** with the input EMPTY, hit send. Confirm: (a) it does NOT navigate to `/analyze/[id]` (stays in the Ideas surface); (b) a legible "generating…/scoring…" status shows; (c) up to 3 idea cards appear — content (title · angle · the lead scroll-quote) FIRST, then the band chip a beat later (content-first; the card face is NEVER shown without its lead quote); (d) each card shows a visually distinct grounding line ("Because: …" or the honest cold-start baseline if your profile is thin); (e) the "needs your take" badge appears where applicable; (f) tapping a card expands to mechanism · seed hook · topic×take×format.
    4. **Seeded:** type a topic/angle and send. Confirm cards are generated around that topic.
    5. Reload the page. Confirm the cards rehydrate (persistence).
    6. Click **"Develop this →"** on a card. Confirm it appends a Hooks placeholder in the SAME thread (a "Hooks coming in P4" affordance is acceptable — generation is P4) and does not error.
    7. Sanity: confirm an Idea send never triggered the Test/video analyze flow.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what's off (e.g. "band chip blocks content", "card face shows before its lead quote", "grounding line shows caveat text", "Idea send navigated to /analyze").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer → Ideas route | The composer send is the client side of the auth/validated route boundary; the Idea path must not cross into the Test/analyze navigation flow, and client-side relaxation must not substitute for server validation. |
| streamed/persisted blocks → render | Rendered idea-card props pass through `MessageBlocks` double-validation; no model-generated UI reaches the DOM unvalidated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Tampering | Idea send arming Test navigation | mitigate | The Idea path never sets `pendingNavRef`/`stream.start` (Pitfall 5); `pendingNavRef` stays exclusive to Test upload/URL. UAT step 3a + 7 verify no /analyze navigation. |
| T-03-14 | Tampering | model-generated UI on render | mitigate | `MessageBlocks` re-validates every block (incl. idea-card) on render; invalid → unsupported sentinel (D-14, Plan 02). |
| T-03-15 | Tampering / Input validation | client-side TikTok-URL relax for the idea tool | mitigate | The relax is UX-only; the Plan-03 route INDEPENDENTLY caps the ask + rate-limits server-side (400 on over-cap) — no validation lives client-only (WARNING-5). |
| T-03-16 | Info disclosure | platform/profile in client request | accept | Platform is a non-sensitive selector; the route re-loads the profile by session user_id server-side (Plan 03) — the client value does not grant data access. |

(No npm/pip/cargo installs in this plan — no Package Legitimacy / supply-chain checkpoint required, per RESEARCH §Package Legitimacy Audit.)
</threat_model>

<verification>
- `npm run build` — composer routing, platform chip, thread view, SSE consumer (fetch+getReader) type-check; both renderer maps complete.
- UAT checkpoint (Task 3) — live content-first rendering (face+lead quote then band chip), Auto/seeded, grounding line, badge, expand, rehydration, chain CTA, no Test-nav leak.
- `npm test` — full suite still green.
</verification>

<success_criteria>
- The Idea chip is live; empty=Auto, typed=seeded, routed to the Ideas pipeline, never to Test nav (IDEAS-01/D-12, Pitfall 5).
- Idea cards render content-first (face + lead quote before band chip) via the fixed renderer and rehydrate on reload (IDEAS-02/D-04/THREAD-07).
- The platform chip sets the first-class platform param on the request (D-07/PROFILE-01).
- Client-side URL relax is UX-only; server independently validates (WARNING-5).
- The legible grounding line + needs-take badge are visible on cards (GROUND-03/D-09/D-11).
- "Develop this →" calls the pinned `/develop` endpoint to carry the idea into Hooks via the in-thread anchor seam (IDEAS-03/THREAD-05/D-15).
</success_criteria>

<output>
Create `.planning/phases/03-ideas-tool/03-04-SUMMARY.md` when done. Record: the composer routing change, the platform-chip default behavior, the Ideas thread-view mount point, the `/develop` CTA wiring, and the UAT outcome (any deferred polish noted for P4).
</output>
