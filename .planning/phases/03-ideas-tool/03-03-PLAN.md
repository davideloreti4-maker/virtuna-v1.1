---
phase: 03-ideas-tool
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/lib/tools/runners/ideas-runner.ts
  - src/app/api/tools/ideas/route.ts
  - src/lib/threads/threads.ts
  - src/lib/threads/__tests__/open-thread.test.ts
  - src/app/api/tools/ideas/__tests__/route.test.ts
autonomous: true
requirements: [IDEAS-01, IDEAS-02, ENGINE-02, GROUND-03, PROFILE-01, THREAD-05]
must_haves:
  truths:
    - "Posting an Ideas request (Auto or seeded) over-generates ~5 concepts, SIMs each seed hook once on the niche-instantiated panel, drops sub-floor cards, and persists 3 survivor idea-card blocks"
    - "Card content is produced server-side before band detail; the score gate runs before presentation (content-first = card-face-before-band-chip)"
    - "Each persisted Ideas message is stamped with KC_GEN_VERSION; the chain anchor seam writes the chosen idea for the P4 Hooks call"
    - "Cold-start / thin profile degrades to an honest baseline grounding line, never blocking on onboarding"
  artifacts:
    - path: "src/app/api/tools/ideas/route.ts"
      provides: "Ideas pipeline route: auth → assemble → over-generate → niche-SIM gate → idea-card blocks → persist + stream (IDEAS-01/02, ENGINE-02)"
      exports: ["POST"]
    - path: "src/lib/tools/runners/ideas-runner.ts"
      provides: "over-generate→gate→cards orchestration (the prototype formalized; rank→gate)"
      contains: "aggregateFlash"
    - path: "src/lib/threads/threads.ts"
      provides: "createOpenThreadLazy — open thread for Ideas + the in-thread chain (D-15, RESEARCH Open Q3)"
      contains: "createOpenThreadLazy"
  key_links:
    - from: "src/app/api/tools/ideas/route.ts"
      to: "assembleBundle({mode:'idea'}) → KC_IDEAS_SYSTEM_PROMPT → Qwen"
      via: "system=cached prompt, user=volatile bundle"
      pattern: "assembleBundle"
    - from: "src/lib/tools/runners/ideas-runner.ts"
      to: "runFlashTextMode(seedHook, 'idea', { niche })"
      via: "per-candidate niche-instantiated SIM (Plan 01)"
      pattern: "runFlashTextMode"
    - from: "src/app/api/tools/ideas/route.ts"
      to: "insertMessage(openThreadId, 'assistant', ideaCardBlocks)"
      via: "validated idea-card persistence + KC_GEN_VERSION stamp"
      pattern: "insertMessage"
---

<objective>
Build the server-side Ideas pipeline: a new SSE route that authenticates, loads the creator's `creator_profiles` row, assembles the live grounding bundle (`mode:"idea"`, cold-start safe), OVER-GENERATES ~5 idea concepts against the cached `KC_IDEAS_SYSTEM_PROMPT`, extracts each concept's seed hook, runs ONE niche-instantiated Flash pass per candidate (Plan 01's panel param), GATES out sub-floor cards (drop weak, no regen loop — D-03/ENGINE-02), keeps the best 3 (D-13), builds validated `idea-card` blocks embedding the band + lead scroll-quote (Plan 02), attaches the honest grounding line (Plan 02), persists into the user's open thread stamped with KC_GEN_VERSION, and streams content-first (card faces first, band chip a beat later — IDEAS-02/D-04). It also adds the `createOpenThreadLazy` helper and the chain-anchor write seam for "Develop this →" (D-15/THREAD-05) — the Hooks GENERATION itself is P4.

Resolves the three RESEARCH Open Questions with explicit decisions (see Task notes):
- Q1 seed-hook extraction → structured `json_object` generate with an explicit `seedHook` field (RESEARCH recommendation, with a markered-prose fallback if KC prose craft degrades).
- Q2 latency → parallel SIMs (`Promise.all`) + legible "generating…/scoring…" status events; "content-first" = card-face-before-band-chip.
- Q3 thread → the user's OPEN thread (type:"open", reading_id null); the chain appends to that same thread.

Purpose: the working over-generate→gate→3-cards pipeline (the moat chain begins).
Output: `ideas-runner.ts`, `/api/tools/ideas/route.ts`, `createOpenThreadLazy`, the anchor seam.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-ideas-tool/03-CONTEXT.md
@.planning/phases/03-ideas-tool/03-RESEARCH.md

# Depends on (read the SUMMARYs for the contracts these plans produced):
@.planning/phases/03-ideas-tool/03-01-SUMMARY.md
@.planning/phases/03-ideas-tool/03-02-SUMMARY.md

# Pipeline source the route composes (read before editing):
@scripts/ideas-sim-rank.ts
@src/lib/kc/assembler.ts
@src/lib/kc/compiled.ts
@src/lib/engine/flash/run-flash-text-mode.ts
@src/lib/engine/flash/flash-aggregate.ts
@src/lib/tools/runners/flash-runner.ts
@src/lib/threads/threads.ts
@src/lib/threads/messages.ts
@src/app/api/analyze/[id]/chat/route.ts
@src/lib/engine/qwen/client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ideas runner — over-generate → niche-SIM gate → 3 idea-card blocks (D-01/D-02/D-03/D-13, ENGINE-02)</name>
  <files>src/lib/tools/runners/ideas-runner.ts, src/app/api/tools/ideas/__tests__/route.test.ts</files>
  <read_first>
    - scripts/ideas-sim-rank.ts — the working generate→SIM→rank loop: `assembleBundle({ask, platform, mode:"idea"}, profileRow)` → harness user msg → `callQwen(KC_IDEAS_SYSTEM_PROMPT, …)` → parse → `Promise.all(ideas.map(simIdea))` → rank. Lift the STAGE STRUCTURE; swap rank→GATE.
    - 03-01-SUMMARY.md — the niche panel param shape on `runFlashTextMode`, the recalibrated thresholds, and the GATE-FLOOR decision (band `!== "Weak"` vs a stop-count cutoff) that Plan 01 recorded. Use that floor here.
    - 03-02-SUMMARY.md — the final `idea-card` prop names, the `grounding-line.ts` signature, and the KC_GEN_VERSION stamp landing spot.
    - src/lib/kc/compiled.ts — `KC_IDEAS_SYSTEM_PROMPT` (the cached system prefix).
    - src/lib/engine/flash/flash-aggregate.ts — `aggregateFlash(personas) → {band, fraction}`; pick the lead scroll-quote (D-04) from the persona `scroll`/`stop` verdicts (mirror the prototype's `scrollQuotes` slice).
    - src/lib/engine/wave3/types.ts — `ContentTypeSlug` (pass `null` for Ideas v1 → `other` allocation, per RESEARCH §Niche thread-through + Plan 01).
  </read_first>
  <behavior>
    - Given a profile row with `niche_primary: "fitness"` and a seeded ask → over-generates ~5 concepts (buffer size is Claude's Discretion against cost; ~5 for N=3 per D-13), SIMs each seed hook in PARALLEL on the niche panel, drops sub-floor candidates per the Plan-01 gate floor, returns up to 3 survivor `idea-card` blocks each carrying band/fraction/lead scrollQuote + whyItFits + needsTake.
    - If fewer than 3 survive the gate → return the survivors (do NOT regenerate — D-03 NO regen loop; "All Mixed"/few survivors is a valid answer, D-04).
    - Cold-start (null profile) → still produces cards; `whyItFits` is the honest baseline line (Plan 02), niche panel falls back to generic (Plan 01 `niche:null`).
    - Each returned block passes `validateBlock` (the route persists via `insertMessage`, which re-validates).
  </behavior>
  <action>
    Create `ideas-runner.ts` exporting an orchestration function (e.g. `runIdeasPipeline({ ask, platform, profileRow }) → { blocks: IdeaCardBlock[]; warnings }`). Formalize the prototype's stages:
    1. GENERATE: `assembleBundle({ ask, platform, mode:"idea" }, profileRow)` for the user message; system = `KC_IDEAS_SYSTEM_PROMPT`. Resolve RESEARCH Open Q1 by requesting a STRUCTURED `json_object` generation of ~5 idea objects, each with an explicit `seedHook` field plus `title/angle/mechanism/topic/take/format/needsTake` — this removes brittle prose parsing and matches the tool-runner's `outputSchema` path. If structured generation degrades the KC's authored prose craft (watch for generic/flattened concepts), fall back to the prototype's `===IDEA===` machine-marker-in-prose with a parsed `seedHook` marker; document which path shipped.
    2. SIM (gate): for each candidate, `runFlashTextMode(candidate.seedHook, "idea", { niche: profileRow?.niche_primary ?? null, contentType: null })` (Plan 01) in `Promise.all` (RESEARCH Pitfall 4 — parallel so wall-time ≈ one SIM). `aggregateFlash` → `{band, fraction}`; select the lead scroll-quote (D-04).
    3. GATE + TRIM: drop candidates below the Plan-01 gate floor; keep up to 3 (D-13). Do NOT over-order (D-04 — SIM is a gate, not a ranker; soft rank only).
    4. BUILD: assemble `idea-card` blocks (Plan 02 prop names) embedding band/fraction/scrollQuote, set `whyItFits` from `buildGroundingLine(profileRow, platform)` (Plan 02), set `needsTake` from the concept.
    Use `getQwenClient()` / `QWEN_REASONING_MODEL` / `QWEN_SEED` for generation (Qwen-only). Keep generation bounded (~5, D-03) — no regen loop. Author the test fixtures here (mock Qwen generate + a fixtured/stubbed Flash) proving over-generate→gate→≤3 and cold-start behavior; the live route test lands in Task 2.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/tools/ideas/__tests__/route.test.ts -t "runner"</automated>
  </verify>
  <done>
    `runIdeasPipeline` over-generates, SIMs each seed hook on the niche panel in parallel, gates out sub-floor candidates (no regen), returns ≤3 validated `idea-card` blocks with embedded band/fraction/lead-quote + grounding line + needsTake. Cold-start produces cards with the honest baseline line. The seed-hook extraction path that shipped (structured vs markered) is documented.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ideas SSE route + open thread + KC_GEN_VERSION stamp + chain-anchor seam (IDEAS-01/02, THREAD-05, D-15)</name>
  <files>src/app/api/tools/ideas/route.ts, src/lib/threads/threads.ts, src/lib/threads/__tests__/open-thread.test.ts, src/app/api/tools/ideas/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/analyze/[id]/chat/route.ts — the SSE + security template: `sseHeaders` (`text/event-stream`, `Cache-Control: no-cache, no-transform`, `X-Accel-Buffering: no`), `supabase.auth.getUser()` auth gate (401), `MAX_MESSAGE_LENGTH=2000`, `RATE_LIMIT_WINDOW_SECS`/`RATE_LIMIT_MAX_MSGS` rolling rate limit, `new ReadableStream({ start(controller){ controller.enqueue(...) } })`.
    - src/lib/threads/threads.ts — `getOpenThread(userId)` exists but there is NO create-open helper; `createGroundedThreadLazy` is the idempotent-insert pattern to MIRROR (service client, user_id from session never body, 23505 re-select scoped by user_id — CR-01).
    - src/lib/threads/messages.ts — `insertMessage(threadId, role, blocks)` (re-validates blocks at write); the JSONB `messages.body` shape for the KC_GEN_VERSION stamp landing spot (per 03-02-SUMMARY).
    - src/lib/kc/assembler.ts — `assembleBundle` accepts `anchor` (fenced upstream idea) for the chain (D-15); the input is `{ask, platform, mode, overrides?, anchor?}`.
    - 03-02-SUMMARY.md — the `kc-stamp.ts` helper + landing spot.
  </read_first>
  <behavior>
    - `POST /api/tools/ideas` unauthenticated → 401 (auth before any DB read).
    - Authenticated Auto request (empty ask) → assembler ask defaults to "Generate ideas from my profile"; seeded request (typed ask) → the typed ask is fenced into the bundle. Both load `creator_profiles` by SESSION user_id (never from body).
    - The route streams a `status`/`content` event with the gated card FACES, then a per-card `score` (band chip) event a beat later (content-first, D-04/IDEAS-02). The 3 cards persist to the user's open thread as one assistant message stamped with KC_GEN_VERSION.
    - Ask length is capped (mirror `MAX_MESSAGE_LENGTH`); the route is rate-limited (mirror the chat route). Composer `ask`/`anchor` are fenced by `assembleBundle` (injection fence, CR-01/CR-02).
    - The "Develop this →" anchor endpoint (or a request shape) writes the chosen idea as the assembler `anchor` for a future Hooks call and appends in the SAME open thread — the Hooks GENERATION is NOT built here (P4, D-15).
  </behavior>
  <action>
    Add `createOpenThreadLazy(userId)` to threads.ts: get-or-create the user's open thread (`type:"open"`, `reading_id: null`) by mirroring `createGroundedThreadLazy`'s service-client + user_id-scoped idempotent pattern (use `getOpenThread` for the read-back; insert with `type:"open"`). Enforce ownership server-side (user_id from session, never body) — CR-01 trust boundary.

    Create `src/app/api/tools/ideas/route.ts` (`POST`): (1) `supabase.auth.getUser()` → 401 if absent; (2) validate + cap the `ask` length (mirror `MAX_MESSAGE_LENGTH`), apply the rolling rate limit (mirror the chat route's `RATE_LIMIT_*`); (3) load the `creator_profiles` row by session `user_id` (null → cold-start, never block on onboarding — D-14); (4) `createOpenThreadLazy(userId)`; (5) call `runIdeasPipeline({ ask: ask || "Generate ideas from my profile", platform, profileRow })` (Task 1); (6) emit an SSE `ReadableStream` with the chat route's `sseHeaders`: a status event ("generating…"/"scoring on your audience…"), then a `content` event with the ≤3 card faces, then per-card `score` events (RESEARCH Pattern 3 / Q2 — parallel SIMs already keep wall-time low; content-first = card-face-before-band-chip); (7) `insertMessage(openThreadId, "assistant", blocks)` and attach the KC_GEN_VERSION stamp (Plan 02 `kc-stamp.ts`) at the documented landing spot.

    Add the chain-anchor seam for D-15/THREAD-05: a request shape (or sibling action) that takes a chosen idea, fences it as the assembler `anchor`, and appends to the same open thread — leaving the Hooks generation for P4 (per RESEARCH Pattern 6: "ship the anchor write + the in-thread append seam"). Do NOT build Hooks generation.

    Qwen-only throughout (`getQwenClient`). Author `open-thread.test.ts` (idempotent get-or-create, ownership scoping) and extend `route.test.ts` with a mocked-Qwen integration: 401 unauth, over-generate→gate→3 persisted cards, KC_GEN_VERSION stamped, anchor write appends in-thread.
  </action>
  <verify>
    <automated>npx vitest run src/lib/threads/__tests__/open-thread.test.ts src/app/api/tools/ideas/__tests__/route.test.ts && npm run build</automated>
  </verify>
  <done>
    `POST /api/tools/ideas` enforces auth (401 unauth), caps + rate-limits the ask, loads the profile by session user_id (cold-start safe), creates/reuses the open thread, runs the over-generate→gate pipeline, streams content-first (card faces then per-card band chip), persists ≤3 idea-card blocks stamped with KC_GEN_VERSION, and exposes the chain-anchor write seam (Hooks generation deferred to P4). `createOpenThreadLazy` is idempotent + ownership-scoped. Build + tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Ideas API route | Untrusted composer `ask` + chain `anchor` cross into server-side generation; the route is the auth + validation + rate-limit gate. |
| service client → threads/messages | The route writes with the service client (RLS bypass); ownership must be enforced via session user_id, never request body (CR-01). |
| model output → persisted blocks | Generated idea-card props are persisted; must pass `validateBlock` at the write boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Spoofing | route auth | mitigate | `supabase.auth.getUser()` before any DB read; 401 on absent session (mirror chat route V2). |
| T-03-08 | Elevation / Info disclosure (IDOR) | open thread / messages | mitigate | `createOpenThreadLazy` + profile load scoped by SESSION user_id (never body); service-client writes ownership-scoped (CR-01). |
| T-03-09 | Tampering | prompt injection via `ask`/`anchor` | mitigate | `assembleBundle` injection fence (`<<<USER_CONTENT>>>` + sentinel-strip, overflow-safe CR-01/CR-02) wraps ask + anchor. |
| T-03-10 | Denial of Service | unbounded generation / request flood | mitigate | Bounded over-generate (~5, D-03); `MAX_MESSAGE_LENGTH` ask cap + rolling rate limit (mirror chat route). |
| T-03-11 | Tampering | model-generated UI via persisted props | mitigate | `insertMessage` re-validates each idea-card block at the write boundary (D-14); invalid → throws / sentinel. |
| T-03-12 | Compliance | non-Qwen model leak | mitigate | `getQwenClient` only; never import Claude/Gemini/DeepSeek (Qwen-only constraint). |

(No npm/pip/cargo installs in this plan — no Package Legitimacy / supply-chain checkpoint required, per RESEARCH §Package Legitimacy Audit.)
</threat_model>

<verification>
- `npx vitest run src/app/api/tools/ideas/__tests__/route.test.ts` — auth, over-generate→gate→3, KC_GEN_VERSION stamp, anchor write (mocked Qwen).
- `npx vitest run src/lib/threads/__tests__/open-thread.test.ts` — idempotent open-thread get-or-create + ownership scoping.
- `npm run build` — route + runner + threads helper type-check.
- `npm test` — full suite still green (no regression of the protected path; the route adds only Flash + generation calls).
</verification>

<success_criteria>
- The Ideas route generates Auto or seeded cards grounded on `creator_profiles`, cold-start safe, never blocking on onboarding (IDEAS-01/PROFILE-01/D-14).
- Over-generate→gate→3 with one bounded Flash pass per candidate; weak dropped, no regen loop (ENGINE-02/D-02/D-03/D-13).
- Content-first streaming: card faces render before the band chip; the gate runs server-side before presentation (IDEAS-02/D-04).
- The honest grounding line is attached per card (GROUND-03/D-09).
- The open thread + chain-anchor seam carry the chosen idea for the P4 Hooks call, in-thread (THREAD-05/IDEAS-03/D-15).
- Each persisted message is KC_GEN_VERSION-stamped.
</success_criteria>

<output>
Create `.planning/phases/03-ideas-tool/03-03-SUMMARY.md` when done. Record: the route request/response (SSE event) contract, the seed-hook extraction path that shipped (structured vs markered), the over-generate buffer size + gate floor used, and the open-thread + anchor seam shape — Plan 04 (composer UI + thread view) consumes the route contract.
</output>
