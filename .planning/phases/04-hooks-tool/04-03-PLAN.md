---
phase: 04-hooks-tool
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/components/app/home/tool-chips.tsx
  - src/hooks/queries/use-hooks-stream.ts
  - src/components/thread/hooks-thread-view.tsx
  - src/components/thread/hook-card-block.tsx
  - src/components/app/home/composer.tsx
  - src/app/api/threads/open/route.ts
  - src/components/app/home/__tests__/tool-chips.test.tsx
autonomous: false
requirements: [HOOKS-01, HOOKS-02, HOOKS-03]
must_haves:
  truths:
    - "The Hook chip is live (mirrors the P3 Idea chip): empty send → hooks from the anchored idea / profile, typed topic → seeded hooks; one composer, no new entry UI (D-09)"
    - "Hook cards render content-first in-thread (face + audience-archetype tag + rank + lead scroll-quote first, band chip a beat later); ranked #1..N (D-02/D-08)"
    - "'Test full →' deep-links the chosen hook into the existing Reading/Test upload surface as the anchored brief — never a Max run on text (D-05/D-06, HOOKS-03)"
    - "The open thread's persisted idea-card + hook-card messages rehydrate on mount (closes the P3-deferred rehydration debt — D-14 double-validation, THREAD-07)"
  artifacts:
    - path: "src/hooks/queries/use-hooks-stream.ts"
      provides: "fetch+getReader SSE consumer for POST /api/tools/hooks, content-first (ranked faces+tag+quote → band chip)"
      contains: "getReader"
    - path: "src/components/thread/hooks-thread-view.tsx"
      provides: "ranked hook-card render via MessageBlocks + streaming + the Test-handoff brief wiring"
      contains: "MessageBlocks"
    - path: "src/app/api/threads/open/route.ts"
      provides: "GET the user's open-thread persisted messages (rehydration read-back — closes the P3 debt)"
      exports: ["GET"]
  key_links:
    - from: "src/components/app/home/composer.tsx"
      to: "useHooksStream.start (activeTool === 'hooks')"
      via: "Hook send routes to hooks stream; NEVER arms pendingNavRef (no /analyze leak)"
      pattern: "useHooksStream"
    - from: "src/components/thread/hook-card-block.tsx"
      to: "the Reading/Test upload surface (deep-link brief)"
      via: "'Test full →' carries the hook as anchored context (D-05, HOOKS-03)"
      pattern: "Test full"
    - from: "src/components/app/home/composer.tsx"
      to: "GET /api/threads/open → persistedBlocks"
      via: "open-thread read-back on mount (rehydration — D-14, THREAD-07)"
      pattern: "/api/threads/open"
---

<objective>
Ship the Hooks SURFACE — flip the disabled Hook chip live (D-09, mirroring the P3 Idea-chip flip), build the content-first SSE consumer + the Hooks thread view that renders the ranked hook-cards, wire the "Test full →" handoff that deep-links a chosen hook into the existing Reading/Test upload surface as the anchored brief (D-05/D-06, HOOKS-03 — the chain endpoint), and close the P3-deferred open-thread REHYDRATION debt (the owner moved it here because Hooks shares the same open thread + persistence — D-14/THREAD-07). Then UAT the full Ideas → Hooks → Test spine in a live browser.

This REPLICATES the P3 surface (03-04): the Hook chip flip mirrors the Idea chip flip, `use-hooks-stream.ts` clones `use-ideas-stream.ts`, `hooks-thread-view.tsx` clones `ideas-thread-view.tsx`. Two things are genuinely-new here: (1) the "Test full →" handoff (HOOKS-03 — the new chain CTA, NOT a Max text run), and (2) the rehydration read-back that 03-04 explicitly deferred to this plan ("the composer hardcodes `persistedBlocks={[]}` and there is no client read-back on mount").

This plan is `autonomous: false` — it ends in a human-verify UAT checkpoint (the flagship demo must be seen working).

Purpose: the working flagship ranked-hooks demo, chaining Ideas → Hooks → Test, with reload-safe persistence.
Output: live Hook chip, `use-hooks-stream.ts`, `hooks-thread-view.tsx`, the Test handoff, the open-thread rehydration read-back, UAT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-hooks-tool/04-CONTEXT.md

# Depends on (read SUMMARYs for the consumed contracts):
@.planning/phases/04-hooks-tool/04-01-SUMMARY.md
@.planning/phases/04-hooks-tool/04-02-SUMMARY.md

# The proven surface this plan REPLICATES (read SUMMARY + source):
@.planning/phases/03-ideas-tool/03-04-SUMMARY.md
@src/components/app/home/tool-chips.tsx
@src/hooks/queries/use-ideas-stream.ts
@src/components/thread/ideas-thread-view.tsx
@src/components/app/home/composer.tsx
@src/components/thread/idea-card-block.tsx

# Persistence read-back source (rehydration — read before editing):
@src/lib/threads/threads.ts
@src/lib/threads/messages.ts
@src/components/thread/message-blocks.tsx

# Test/Reading handoff target (the deep-link surface — read to choose the seam):
@src/app/(app)/home/page.tsx
@src/components/app/home/platform-chip.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Live Hook chip + Hooks stream consumer + Hooks thread view + composer routing (D-09, HOOKS-01/02)</name>
  <files>src/components/app/home/tool-chips.tsx, src/hooks/queries/use-hooks-stream.ts, src/components/thread/hooks-thread-view.tsx, src/components/app/home/composer.tsx, src/components/app/home/__tests__/tool-chips.test.tsx</files>
  <read_first>
    - src/components/app/home/tool-chips.tsx — the `CHIPS` array; `{ id: "hooks", ..., enabled: false }` is the chip to flip to `enabled: true`. Model label already "SIM-1 Flash". Chat stays disabled (P5).
    - src/hooks/queries/use-ideas-stream.ts — the EXACT SSE consumer to clone: `fetch(POST)` + `res.body.getReader()` (NOT EventSource — BLOCKER-1), the `\n\n` frame split, the `status`/`content`/`score`/`done`/`error` event handling, the `PartialIdeaCard` content-first pattern (face renders with placeholder band until the score event patches it), and `toBlocks()`. Hooks clones this; the new fields are `audienceArchetype` + `rank` on the partial card (from 04-02's content event).
    - 04-02-SUMMARY.md — the Hooks route SSE event contract (status → content[ranked faces + audience tag + rank + lead quote] → per-card score → done) the consumer parses. Match the field names exactly.
    - src/components/thread/ideas-thread-view.tsx — the EXACT view to clone: `PlatformContext.Provider`, `MessageBlocks` render of persisted + streaming blocks, the status indicator, the 760px column. Hooks clones this; render order is the RANKED hook-cards (rank already set by the runner — render in array order, which is rank order).
    - src/components/app/home/composer.tsx — the Idea routing block (`if (activeTool === "idea") { ... await ideas.start(ask, platform); return; }`) and the CRITICAL T-03-13 rule: the Idea/Hook path NEVER sets `pendingNavRef.current` and NEVER calls `stream.start` (no /analyze leak). The `showIdeasView` gate + `IdeasThreadView` mount. Add the parallel Hooks wiring. NOTE: 04-01-SUMMARY's HookCardRenderer needs PlatformContext too (for the Test handoff) — provide it via HooksThreadView like IdeasThreadView does.
    - 04-01-SUMMARY.md — the `HookCardBlock` prop names the stream + view render.
  </read_first>
  <behavior>
    - The Hook chip is enabled and fires `onSelect("hooks")` (tool-chips test updated; only Chat stays disabled).
    - With `activeTool === "hooks"`: an empty send → `useHooksStream.start("", platform)` (auto/anchored); a typed topic → `useHooksStream.start(topic, platform)` (seeded — D-09). The Hook path NEVER arms `pendingNavRef` / calls `stream.start` (no /analyze navigation — T-03-13).
    - The HooksThreadView mounts above the composer when the Hook tool is active; renders the ranked hook-cards content-first (face + audience tag + rank + lead quote first; band chip patches in on the score event).
    - The streamed cards render in RANK order (#1 first) — the runner already ordered them (04-02).
  </behavior>
  <action>
    1. `tool-chips.tsx`: set the `hooks` chip `enabled: true`. Leave `chat` disabled (P5).
    2. `use-hooks-stream.ts`: clone `use-ideas-stream.ts` → `useHooksStream`. Point `fetch` at `POST /api/tools/hooks`. Extend the partial-card type with `audienceArchetype: string` + `rank: number` (parsed from the content event). Keep the content-first pattern: face + tag + rank + lead quote render immediately; the `score` event patches band/fraction. `toBlocks()` returns `HookCardBlock[]` (Plan 01 prop names; placeholder band "Mixed" until scored). Match by `seedHook` (same fallback as ideas-stream).
    3. `hooks-thread-view.tsx`: clone `ideas-thread-view.tsx` → `HooksThreadView` with `persistedBlocks` / `streamingBlocks` / `statusMessage` / `isStreaming` / `platform`. Provide `PlatformContext` so `HookCardRenderer`'s "Test full →" CTA can read the platform. Render the ranked hook-cards via `MessageBlocks`.
    4. `composer.tsx`: add `const hooks = useHooksStream();` + the Hook routing block mirroring the Idea block — `if (activeTool === "hooks") { const ask = trimmedUrl; setUrl(""); await hooks.start(ask, platform); return; }` placed BEFORE the Test paths, with the same T-03-13 guarantee (no pendingNavRef, no stream.start). Add `showHooksView` (active === "hooks" && streaming/blocks/error) and mount `HooksThreadView` (passing `persistedBlocks` from Task 3's read-back — wire in Task 3; pass `[]` here as the interim until Task 3 lands the read-back, then update). Show the platform chip for the Hook tool too (mirror `showPlatformChip`). Update `canSubmit` + `PLACEHOLDER_BY_TOOL.hooks` (e.g. "What topic do you want hooks for? (or leave empty for Auto)").
    5. Update `tool-chips.test.tsx`: Hook chip now fires `onSelect`; only Chat asserts disabled.
  </action>
  <verify>
    <automated>npx vitest run src/components/app/home/__tests__/tool-chips.test.tsx && npm run build</automated>
  </verify>
  <done>
    The Hook chip is live; `useHooksStream` consumes the Hooks SSE route content-first (ranked faces + audience tag + rank + lead quote → band chip); `HooksThreadView` renders the ranked cards via MessageBlocks with PlatformContext; the composer routes Hook sends to the stream and NEVER navigates to /analyze (T-03-13). Build + tool-chips tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: "Test full →" handoff — deep-link the chosen hook into the Reading/Test surface as the brief (D-05/D-06, HOOKS-03)</name>
  <files>src/components/thread/hook-card-block.tsx, src/components/app/home/composer.tsx</files>
  <read_first>
    - 04-01-SUMMARY.md — the `HookCardRenderer` "Test full →" CTA seam (the `onTest` prop name / affordance Plan 01 exposed). This task wires it.
    - src/components/app/home/composer.tsx — the Test tool path (`activeTool === "test"`): it accepts a TikTok URL or a staged video upload, then `stream.start({ input_mode, content_type, ... })` → navigates to /analyze/[id]. The handoff must land the chosen hook as a visible BRIEF above this Test upload affordance, then let the creator shoot+upload the real video (D-05 — Max scores the real upload, NEVER the text hook). Read how `activeTool`/`platform`/`url` state is set so the handoff can switch the composer to the Test tool and surface the brief.
    - 04-CONTEXT.md D-05/D-06 — "Test full →" = HANDOFF, not a Max run. P4 ships the wired handoff + hook-as-context pre-fill into the CURRENT Reading; the "Test · powered by SIM-1 Max" RENAME is P5 (do NOT rename here). The honest ladder: Flash on text = concept ceiling; Max on the shot video = realized result.
    - src/app/(app)/home/page.tsx — the composer is the Test entry surface (Test tool → /api/analyze). Confirm there is no separate upload page; the handoff targets the composer's Test tool, carrying the hook as the brief.
  </read_first>
  <behavior>
    - Clicking "Test full →" on a hook-card switches the composer to the Test tool and surfaces the chosen hook as a visible BRIEF (e.g. a "Shooting this hook:" banner above the upload affordance) — framed "shoot the video on this hook → upload → SIM-1 Max scores the real thing" (D-06). It does NOT run any model on the text and does NOT navigate to a Max result (no fabrication — D-05).
    - The brief carries the hook line (and optionally the audience-archetype tag) as the anchored context. The creator can then upload/paste the real video through the unchanged Test path → /analyze/[id] (the video path is unchanged — regression-safe).
    - No "Test · powered by SIM-1 Max" rename appears (that is P5 — D-06).
  </behavior>
  <action>
    Wire the "Test full →" handoff using the simplest reliable in-app seam (exact mechanism = Claude's Discretion per D-05; default below):

    Lift the handoff state into the composer (the component that owns `activeTool` + the Test upload affordance) and pass a callback down to `HookCardRenderer` via the same PlatformContext-style seam OR a `onTestHook` prop threaded through HooksThreadView. On "Test full →": (a) set `activeTool = "test"`; (b) store the chosen hook as a `testBrief` state (hook line + audience-archetype tag); (c) render a visible, dismissible BRIEF banner above the Test upload affordance ("Shooting this hook: '…' — upload your video and SIM-1 Max will score the real thing"); (d) keep the Test upload/URL path UNCHANGED (the creator uploads the shot video → existing /api/analyze flow → /analyze/[id]). Do NOT call any generation/Max API on the hook text (D-05 honesty spine). Do NOT rename the Test landing (P5 — D-06). Update `HookCardRenderer`'s CTA to invoke the threaded callback (replacing the Plan-01 stub seam). THEME-06 flat-warm: coral only on the CTA + brief accent.

    If a deep-link via query param is cleaner than lifted state (e.g. the hook-card lives in a persisted thread that may be viewed outside the composer), Claude may instead navigate to `/home?brief=<encoded hook>` and have the composer read it on mount (useSearchParams) — document whichever seam shipped. The default (lifted state) is preferred for the in-composer streaming case; the query-param path covers the persisted/rehydrated card case if needed.
  </action>
  <verify>
    <automated>npm run build && npx vitest run src/components/thread src/components/app/home</automated>
  </verify>
  <done>
    "Test full →" carries the chosen hook into the Test surface as a visible brief, switches the composer to the Test tool, and leaves the video upload path unchanged — NO model runs on the hook text, NO Max result is fabricated, NO P5 rename (D-05/D-06/HOOKS-03). Build + component tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Open-thread rehydration read-back — close the P3-deferred persistence debt (D-14, THREAD-07)</name>
  <files>src/app/api/threads/open/route.ts, src/components/app/home/composer.tsx</files>
  <read_first>
    - 03-04-SUMMARY.md §"Deferred to Phase 4" — the EXACT debt: "Persist now works… but the composer hardcodes `persistedBlocks={[]}` and there is no client read-back on mount. Phase 4 (Hooks) shares the same open thread + persistence, so wire the open-thread message load there and pass real `persistedBlocks` to IdeasThreadView." This task closes it for BOTH the Ideas and Hooks views.
    - src/lib/threads/threads.ts — `getOpenThread(userId)` (returns the user's open thread row or null) — the read source.
    - src/lib/threads/messages.ts — `loadMessages(threadId)` re-validates every block on rehydration (D-14) and maps invalid → `{ type: "__unsupported__" }` (never drops the message). Returns `HydratedMessage[]` with `blocks: HydratedBlock[]`. The route returns these; the composer filters to idea-card / hook-card blocks for the respective views.
    - src/components/app/home/composer.tsx — the two `persistedBlocks={[]}` interim props (the Ideas view, and the Hooks view added in Task 1). Replace both with the read-back result.
    - src/app/api/tools/ideas/route.ts — the auth pattern (`supabase.auth.getUser()` → 401) the new GET route mirrors.
  </read_first>
  <behavior>
    - `GET /api/threads/open` unauthenticated → 401.
    - Authenticated → returns the user's open-thread hydrated messages (or an empty list if no open thread yet). Each block re-validated (D-14); invalid blocks come back as the unsupported sentinel, never dropped.
    - On composer mount, the persisted idea-card + hook-card blocks load and render in their respective thread views (reload-safe — the UAT step-5 deferral from P3 now passes).
    - A persisted hook-card with a now-invalid shape rehydrates as the unsupported placeholder (no crash — D-14).
  </behavior>
  <action>
    Create `src/app/api/threads/open/route.ts` (`GET`): `supabase.auth.getUser()` → 401 if absent; `getOpenThread(user.id)` → if null, return `{ messages: [] }`; else `loadMessages(thread.id)` → return `{ threadId, messages }` (the HydratedMessage[] with re-validated blocks). Auth before any DB read; user_id from session only (CR-01).

    In `composer.tsx`: on mount (a `useEffect` with an empty dep set, client-side), `fetch('/api/threads/open')`, parse the hydrated messages, and split the blocks into `persistedIdeaBlocks` (type === "idea-card") and `persistedHookBlocks` (type === "hook-card") state. Pass `persistedIdeaBlocks` to `IdeasThreadView` (replacing the hardcoded `[]` — the 03-04 TODO) and `persistedHookBlocks` to `HooksThreadView` (replacing the Task-1 interim `[]`). Keep the unsupported sentinel handling: MessageBlocks already renders `__unsupported__` via the registry path, so pass blocks through as-is (filter only by the two card types for the two views; let MessageBlocks' own validateBlock handle the rest). Guard against the unauthenticated/empty case (render nothing extra). Do NOT block the composer render on the fetch (the views already no-op when idle).
  </action>
  <verify>
    <automated>npm run build && npx vitest run src/app/api/tools src/components/app/home</automated>
  </verify>
  <done>
    `GET /api/threads/open` returns the user's open-thread re-validated messages (401 unauth, empty list when no thread). The composer loads them on mount and passes real `persistedBlocks` to both the Ideas and Hooks views — reload now rehydrates persisted cards (closes the P3 step-5 deferral). Invalid persisted blocks degrade to the unsupported sentinel (D-14). Build + tests green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT — the flagship Ideas → Hooks → Test spine (live browser)</name>
  <what-built>
    The full Hooks surface: the live Hook chip, content-first ranked hook-cards (gate-then-rank, top 5), the audience-archetype tag, the "Develop this →" auto-fire from Ideas, the "Test full →" handoff into the Reading/Test surface, and open-thread rehydration on reload.
  </what-built>
  <how-to-verify>
    Run the dev server (`npm run dev`), log in as the E2E Test User, and on `/home`:

    1. Select the **Hook** chip → confirm it is live, the model label reads **SIM-1 Flash**, and the platform chip shows. Placeholder reads the hooks copy.
    2. **Seeded hooks:** type a topic (e.g. "protein timing myths") → send. Confirm: legible status ("Generating hooks…" / "Scoring on your audience…"), then **up to 5 ranked hook cards** appear content-first (hook line + audience-archetype tag + rank + lead scroll-quote first; the band chip fills a beat later). Confirm the ranking READS like a ranking (#1 is the strongest pull; bands non-increasing). Confirm NO numeric pull-score and NO view-count promise (qualitative band + fraction + SIM-1 Flash only — D-02/ENGINE-03).
    3. **Audience-archetype tag:** confirm each card's tag names an AUDIENCE persona it grabs ("stops the skeptic" / "grabs the gym-beginner") — NOT a craft slug (no BOLD/GAP/etc. visible — D-03/D-04). Expand a card → confirm mechanism prose + seed hook show; craft slug still NOT shown.
    4. **Develop → auto-fire (D-07):** switch to the **Idea** chip, generate ideas, click **"Develop this →"** on a card → confirm Hooks auto-generate (no extra "Generate hooks" tap) and append in the SAME thread below the idea.
    5. **Test full → handoff (D-05/D-06, HOOKS-03):** click **"Test full →"** on a hook → confirm the composer switches to the **Test** tool and surfaces the hook as a visible BRIEF above the upload affordance ("shoot this hook → upload → Max scores the real thing"). Confirm it does NOT run any model on the text and does NOT navigate to a Max result. Confirm the Test upload/URL path still works unchanged (no /analyze leak from Hooks).
    6. **Reload rehydration (closes P3 debt):** refresh the page → confirm the previously-generated idea-card AND hook-card messages re-render from the open thread (no data loss).
    7. Confirm no Test-flow leak: a Hook send never navigates to /analyze.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (rank order, tag honesty, handoff behavior, rehydration, or any /analyze leak).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client composer → Hooks/threads routes | The Hook send + the open-thread read-back cross to server routes; auth + server-side validation are the gate (the Hooks route owns ask/anchor caps — Plan 02). |
| persisted blocks → rehydrated UI | Persisted hook-card/idea-card blocks re-render; each must pass `validateBlock` on rehydration (D-14) or degrade to the unsupported sentinel. |
| hook text → Test handoff | The "Test full →" handoff must carry the hook as a BRIEF only — never invoke Max on the text (honesty-spine break / fabrication). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Spoofing | GET /api/threads/open auth | mitigate | `supabase.auth.getUser()` before any DB read; 401 on absent session; user_id from session only (CR-01). |
| T-04-11 | Tampering | model-generated UI via rehydrated blocks | mitigate | `loadMessages` + `MessageBlocks` re-validate each block on rehydration (D-14); invalid → unsupported sentinel, message never dropped. |
| T-04-12 | Spoofing/Repudiation | honesty — Test handoff implies a Max text score | mitigate | "Test full →" is a HANDOFF only: it switches to the Test tool + surfaces a brief; no model runs on the hook text; Max only ever scores a real uploaded video (D-05/D-06/ENGINE-03). |
| T-04-13 | Elevation | Hook send navigates to /analyze (Test leak) | mitigate | The Hook composer path never sets `pendingNavRef` / calls `stream.start` (T-03-13 carried forward); UAT step 7 verifies no leak. |

(No npm/pip/cargo installs in this plan — no Package Legitimacy / supply-chain checkpoint required.)
</threat_model>

<verification>
- `npx vitest run src/components/app/home/__tests__/tool-chips.test.tsx` — Hook chip live; only Chat disabled.
- `npx vitest run src/components/app/home src/components/thread src/app/api/tools src/app/api/threads` — composer routing, hooks view, rehydration route.
- `npm run build` — chip + stream + view + composer + open-thread route type-check.
- `npm test` — full suite green (no regression of the protected path; UI + a read-back route only).
- Live UAT (Task 4) — the 7-step flagship-spine verification, human-approved.
</verification>

<success_criteria>
- The Hook chip is live; empty send → anchored/auto hooks, typed → seeded (D-09); one composer, no new entry UI.
- Hook cards render content-first, ranked #1..N, with the audience-archetype tag + lead scroll-quote (D-02/D-03/D-08); no craft slug, no numeric score, no view promise (D-04/ENGINE-03).
- "Develop this →" auto-fires Hooks in the same thread (D-07); "Test full →" deep-links the hook into the Reading/Test surface as the brief — never a Max text run (D-05/D-06, HOOKS-03).
- Open-thread idea-card + hook-card messages rehydrate on reload (closes the P3 step-5 deferral — D-14/THREAD-07); invalid blocks → unsupported sentinel.
- No Hook send ever navigates to /analyze (T-03-13); the Test video path is unchanged.
- UAT approved (the flagship Ideas → Hooks → Test demo seen working live).
</success_criteria>

<output>
Create `.planning/phases/04-hooks-tool/04-03-SUMMARY.md` when done. Record: the Hook chip flip; the `useHooksStream` SSE contract consumed; the HooksThreadView render; the "Test full →" handoff seam that shipped (lifted state vs query-param) + its exact mechanism (consumed by P5's Test reframe, which renames this landing); the open-thread rehydration read-back (`GET /api/threads/open`) that closed the P3 debt; and the UAT outcome per step.
</output>
