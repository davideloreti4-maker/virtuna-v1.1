---
phase: 04-hooks-tool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tools/blocks.ts
  - src/lib/tools/block-registry.ts
  - src/components/thread/hook-card-block.tsx
  - src/components/thread/message-blocks.tsx
  - src/lib/tools/hooks/audience-archetype.ts
  - src/lib/tools/hooks/__tests__/audience-archetype.test.ts
  - src/lib/tools/__tests__/hook-card-block.test.ts
autonomous: true
requirements: [HOOKS-01]
must_haves:
  truths:
    - "A hook-card block validates at the write boundary AND on rehydration; invalid props degrade to the unsupported sentinel (D-11/D-14 — never model-generated UI)"
    - "The audience-archetype tag is derived from the Flash per-persona stop verdicts (which SIM persona stops scrolling — D-03), NOT the private craft archetype (D-04)"
    - "The hook-card renders the hook line + audience-archetype tag + lead scroll-quote on the face; mechanism/seed reasoning behind expand; band as a secondary chip; a 'Test full →' CTA"
  artifacts:
    - path: "src/lib/tools/blocks.ts"
      provides: "HookCardBlockSchema + HookCardBlock type embedding band + scroll-quote + audienceArchetype tag (D-03/D-11)"
      contains: "HookCardBlockSchema"
    - path: "src/components/thread/hook-card-block.tsx"
      provides: "HookCardRenderer — hook-line-forward face + audience-archetype tag + tap-to-expand + secondary band chip + 'Test full →' CTA (D-05/D-11)"
      min_lines: 40
    - path: "src/lib/tools/hooks/audience-archetype.ts"
      provides: "deriveAudienceArchetype — reads Flash personas → the human-facing 'stops the {persona}' tag (D-03/D-04)"
      contains: "deriveAudienceArchetype"
  key_links:
    - from: "src/lib/tools/block-registry.ts"
      to: "HookCardBlockSchema"
      via: "BLOCK_REGISTRY['hook-card']"
      pattern: "hook-card"
    - from: "src/components/thread/message-blocks.tsx"
      to: "HookCardRenderer"
      via: "BLOCK_COMPONENTS['hook-card']"
      pattern: "hook-card"
    - from: "src/lib/tools/hooks/audience-archetype.ts"
      to: "FlashResult.personas (verdict:'stop')"
      via: "read which persona archetypes stopped scrolling (D-03)"
      pattern: "stop"
---

<objective>
Build the two genuinely-new pieces this Hooks surface needs and register the single typed block the Hooks model is allowed to emit: (1) the schema-validated `hook-card` typed block (D-11) joining the fixed renderer registry with double-validation + its React renderer (hook-line-forward face mirroring P3's `idea-card`, the audience-archetype tag on the face per D-03, mechanism/seed reasoning behind expand per D-08, a secondary band chip, and the "Test full →" CTA per D-05), and (2) the `deriveAudienceArchetype` extractor (D-03/D-04) that reads the Flash per-persona output to produce the human-facing "which audience persona stops scrolling" tag — NEVER the private craft archetype (BOLD/GAP/CONTRARIAN/RESEARCH/NARRATIVE/QUESTION, D-04).

This is an explicit REPLICATION of the shipped P3 `idea-card` shape (03-02). The `hook-card` clones `idea-card`'s registry pattern (blocks.ts schema + block-registry.ts entry + message-blocks.tsx component map) and renderer anatomy (face / expand / secondary band chip), swapping the idea-specific props for hook props and adding the `audienceArchetype` tag (D-03) on the face. The "Test full →" CTA wiring (the deep-link to the Reading/Test surface) is finalized in Plan 03; here the renderer exposes the CTA affordance + an onTest callback seam.

This plan is parallel-safe (Wave 1) — disjoint files from any engine/route work. It produces the contracts Plan 02 (pipeline) and Plan 03 (UI) consume, so it defines them interface-first.

Purpose: the validated typed block + the D-03 audience-archetype tag are the new primitives the flagship ranked-hooks demo renders.
Output: `HookCardBlockSchema` + registry entry + `HookCardRenderer`; `audience-archetype.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-hooks-tool/04-CONTEXT.md
@.planning/corpus/hooks.md

# The proven shape this plan REPLICATES (read the SUMMARY for the exact contracts):
@.planning/phases/03-ideas-tool/03-02-SUMMARY.md

# Block system + renderer source (read before editing — mirror these exactly):
@src/lib/tools/blocks.ts
@src/lib/tools/block-registry.ts
@src/components/thread/message-blocks.tsx
@src/components/thread/idea-card-block.tsx
@src/components/thread/band-block.tsx

# The Flash per-persona output the audience-archetype tag reads (D-03):
@src/lib/tools/runners/flash-runner.ts
@src/lib/engine/flash/flash-aggregate.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deriveAudienceArchetype — the audience-persona tag from Flash output (D-03/D-04)</name>
  <files>src/lib/tools/hooks/audience-archetype.ts, src/lib/tools/hooks/__tests__/audience-archetype.test.ts</files>
  <read_first>
    - src/lib/tools/runners/flash-runner.ts — `mapFlashResultToBlocks` shows the FlashResult.personas shape: `Array<{ archetype: string; verdict: "stop" | "scroll"; quote: string }>`. This is the SOLE source for the audience-archetype tag (D-03 — no new SIM pass).
    - src/lib/engine/flash/flash-aggregate.ts — `aggregateFlash(personas)` and STRONG_THRESHOLD=6/MIXED_THRESHOLD=3; the personas array is the same FlashPersona shape.
    - .planning/corpus/hooks.md §Craft Archetypes — confirm BOLD/GAP/CONTRARIAN/RESEARCH/NARRATIVE/QUESTION are PRIVATE craft archetypes; the deliverable tag (D-03) is the AUDIENCE persona that stopped, NOT one of these. The function MUST NOT emit a craft-archetype slug.
    - src/lib/engine/wave3/persona-registry.ts — the persona archetype vocabulary (e.g. tough_crowd, niche_deep_buyer, lurker, loyalist) so the tag maps a raw persona archetype id to a human-facing phrase ("stops the skeptic" / "grabs the gym-beginner"). Read the archetype ids to build the human-facing label map.
  </read_first>
  <behavior>
    - Given personas where the FIRST stop-verdict persona has archetype "tough_crowd" → returns a human-facing tag string naming that audience (e.g. "Stops the skeptic" / a label mapped from tough_crowd). The tag names an AUDIENCE persona, never a craft archetype (D-03/D-04).
    - Given personas where multiple stop → the tag names the highest-signal stopper (Claude's Discretion on tie-break: e.g. the toughest crowd that still stopped reads strongest; document the choice). NEVER concatenate a craft-archetype slug.
    - Given personas where NONE stop (all scroll) → returns an honest neutral tag (e.g. "No clear audience lock" or "Weak pull") — never a fabricated "stops the X" claim (honesty spine).
    - The returned tag string NEVER contains any of the literal craft slugs "BOLD"/"GAP"/"CONTRARIAN"/"RESEARCH"/"NARRATIVE"/"QUESTION" (D-04 — asserted in test).
  </behavior>
  <action>
    Create `src/lib/tools/hooks/audience-archetype.ts` exporting `deriveAudienceArchetype(personas: Array<{ archetype: string; verdict: "stop" | "scroll"; quote: string }>): string`. It reads the Flash per-persona verdicts (D-03 — the audience archetype it GRABS = which SIM persona stops), selects the most meaningful stopper (tie-break = Claude's Discretion; default: prefer a tough/skeptical persona that stopped since that reads as the strongest pull signal, falling back to the first stopper), and maps the raw persona archetype id to a concise human-facing label (a small `Record<personaArchetypeId, string>` label map, e.g. `tough_crowd → "the skeptic"`, `niche_deep_buyer → "your core buyer"`, falling back to a humanized form of the raw id). Compose the final tag (e.g. `Stops ${label}`). On all-scroll → an honest neutral tag, never a fabricated capture claim. Keep it PURE (no I/O) so it is unit-testable and callable from the Plan 02 runner. CRITICAL (D-04): never reference, import, or emit the private craft-archetype vocabulary — this function only knows about SIM personas, not hook craft forms.

    Author `audience-archetype.test.ts` covering the four `behavior` cases, INCLUDING the explicit assertion that the output never contains any craft-archetype slug (D-04 guard).
  </action>
  <verify>
    <automated>npx vitest run src/lib/tools/hooks/__tests__/audience-archetype.test.ts</automated>
  </verify>
  <done>
    `deriveAudienceArchetype` returns an honest human-facing AUDIENCE-persona tag from the Flash per-persona stop verdicts (D-03), an honest neutral tag when none stop, and NEVER emits a craft-archetype slug (D-04). Pure, unit-tested, ready for the Plan 02 runner to call.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the hook-card typed block to the fixed registry with double-validation (D-11/D-14)</name>
  <files>src/lib/tools/blocks.ts, src/lib/tools/block-registry.ts, src/components/thread/hook-card-block.tsx, src/components/thread/message-blocks.tsx, src/lib/tools/__tests__/hook-card-block.test.ts</files>
  <read_first>
    - src/lib/tools/blocks.ts — the EXACT `IdeaCardBlockSchema` shape (lines ~65-98) to mirror: `z.object({ type: z.literal("idea-card"), props: z.object({...}) })`, and how it is added to `BlockUnionSchema`'s `z.discriminatedUnion("type", [...])`. The hook-card is a sibling.
    - src/lib/tools/block-registry.ts — `BLOCK_REGISTRY` object (the `"idea-card": { schema: IdeaCardBlockSchema as z.ZodType }` entry), `BlockType` (auto-derives), `validateBlock`, `assertBlocksInRegistry`. Adding `"hook-card"` here makes validateBlock + assertBlocksInRegistry cover it for free.
    - src/components/thread/message-blocks.tsx — the inline `BLOCK_COMPONENTS: Record<BlockType, …>` map (TypeScript forces completeness → you MUST add `"hook-card"` or build breaks) + the `validateBlock`-on-rehydration loop (D-14).
    - src/components/thread/idea-card-block.tsx — the FULL renderer to clone: face (title/angle/grounding-line/lead-quote) → expand (mechanism/seedHook/topic×take×format) → secondary band chip → "Develop this →" CTA. The hook-card mirrors this anatomy, swapping props + replacing the CTA with "Test full →".
    - src/components/thread/band-block.tsx — the `BAND_COLOR` zone tokens + `sim1-flash` MODEL_LABEL styling the secondary chip reuses.
    - 03-02-SUMMARY.md §1 — the IdeaCardBlockSchema prop list, for the parallel hook props.
  </read_first>
  <behavior>
    - `validateBlock({ type: "hook-card", props: <valid> })` returns `{ ok: true }` with the parsed block.
    - `validateBlock({ type: "hook-card", props: <missing required field> })` returns `{ ok: false }` (→ unsupported sentinel on rehydration).
    - `assertBlocksInRegistry([{type:"hook-card",...}], ["hook-card"])` does not throw; with `["band"]` it throws.
    - `BLOCK_COMPONENTS` contains `hook-card` (TypeScript `Record<BlockType,…>` completeness compiles — proven by `npm run build`).
  </behavior>
  <action>
    Replicate the verified P3 `idea-card` 3-edit pattern (03-02-SUMMARY §"D-10 idea-card block pattern"), plus the renderer:

    1. `blocks.ts`: add `HookCardBlockSchema = z.object({ type: z.literal("hook-card"), props: z.object({...}) })` and `export type HookCardBlock = z.infer<...>`. Props per D-11 (exact names Claude's Discretion within this shape, but document them for Plan 02): `hookLine` (the verbatim/executable hook — multi-modal note allowed per corpus/hooks.md), `audienceArchetype` (the D-03 tag string from Task 1), `mechanism` (named attention mechanism — PRIVATE craft reasoning surfaced behind expand, NOT a slug), `seedHook` (the line the SIM reacted to — may equal hookLine), `rank: z.number().int()` (the #1..N rank position, D-01), plus the EMBEDDED Flash signal — `band: z.enum(["Strong","Mixed","Weak"])`, `fraction: z.string()`, `scrollQuote: z.string()` (the lead per-persona scroll-quote, D-02/D-04 texture), `model: z.literal("sim1-flash")`. Optionally include `channel: z.string().nullable()` (spoken/visual/caption/edit/audio — hooks are multi-modal per corpus/hooks.md; nullable). Add `HookCardBlockSchema` to `BlockUnionSchema`'s discriminated union. Do NOT add a craft-archetype field (D-04 — craft stays private; mechanism prose is the most that surfaces, behind expand).

    2. `block-registry.ts`: add `"hook-card": { schema: HookCardBlockSchema as z.ZodType }` to `BLOCK_REGISTRY`. `BlockType` auto-derives; `validateBlock` + `assertBlocksInRegistry` cover it for free.

    3. `message-blocks.tsx`: add `"hook-card": HookCardRenderer` to the inline `BLOCK_COMPONENTS` map (TypeScript `Record<BlockType,…>` completeness forces it). (Note: P3 also touched `block-registry.tsx`, but 03-04 DELETED that dead `.tsx` — confirm it no longer exists; if absent, the single component map in `message-blocks.tsx` is the only edit needed.)

    4. `hook-card-block.tsx`: a `'use client'` `HookCardRenderer({ block }: { block: HookCardBlock })` cloning `idea-card-block.tsx`'s structure. FACE (always visible, D-11): the `hookLine` as the dominant element · the `audienceArchetype` tag rendered as a visually-distinct chip (D-03 — the headline differentiator) · the LEAD `scrollQuote` as a blockquote (D-02/D-04 texture). Optionally show the rank position (#1..N) as a small ordinal badge. EXPAND (tap/disclosure, D-08): `mechanism` (the private craft reasoning — prose, NOT a slug) + `seedHook` + the `channel` if present. The secondary band chip (band/fraction + "SIM-1 Flash" tag) reuses band-block's zone-color tokens. The "Test full →" CTA (D-05): expose a button affordance with an `onTest?: () => void` prop OR a stubbed handler — the actual deep-link wiring lands in Plan 03; here the affordance + the seam exist (mirror how idea-card-block.tsx exposed "Develop this →"). Visual treatment = Claude's Discretion under THEME-06 flat-warm SSOT (6% borders, 12px card radius, Inter, coral only on the CTA/tag accent — CLAUDE.md Raycast rules). NO model-generated markup — the model emits validated props only; the component owns layout (THREAD-04).

    Author `hook-card-block.test.ts`: cover the four `behavior` cases against `validateBlock`/`assertBlocksInRegistry`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/tools && npm run build</automated>
  </verify>
  <done>
    `hook-card` is in `BLOCK_REGISTRY` and `message-blocks.tsx`'s `BLOCK_COMPONENTS`. `validateBlock` accepts a valid hook-card and rejects a malformed one (→ unsupported). `assertBlocksInRegistry` gates it correctly. `HookCardRenderer` renders face (hookLine / audience-archetype tag / lead-quote / rank) + expand (mechanism prose / seedHook / channel) + secondary band chip + a "Test full →" CTA affordance with an onTest seam. `npm run build` passes — the BLOCK_COMPONENTS map is complete. No craft-archetype slug is emitted anywhere (D-04).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output → persisted/rendered block | The Hooks model emits `hook-card` props that are persisted and rendered; unvalidated props would be a model-generated-UI / injection vector (THREAD-04). |
| Flash personas → audience-archetype tag | Flash output flows to a user-visible tag; a fabricated "stops the X" claim when no persona stopped would breach the honesty spine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | model-generated UI via hook-card | mitigate | Fixed registry + double `validateBlock` (write + rehydration, D-14); `HookCardRenderer` owns layout, model emits validated props only (D-11/THREAD-04). Invalid → unsupported sentinel, never executed. |
| T-04-02 | Spoofing/Repudiation | honesty on the audience-archetype tag | mitigate | `deriveAudienceArchetype` returns an honest neutral tag when no persona stops; never a fabricated capture claim. Test asserts no fabricated tag + no craft-slug leak (D-03/D-04). |

(No npm/pip/cargo installs in this plan — no Package Legitimacy / supply-chain checkpoint required.)
</threat_model>

<verification>
- `npx vitest run src/lib/tools/hooks/__tests__/audience-archetype.test.ts` — audience-persona tag honesty + no craft-slug leak.
- `npx vitest run src/lib/tools` — hook-card validation + registry membership green.
- `npm run build` — BLOCK_COMPONENTS map complete (TypeScript completeness check).
</verification>

<success_criteria>
- `hook-card` is a first-class registry block validated at write AND rehydration; invalid props degrade to the unsupported sentinel (D-11/D-14, no model-generated UI).
- `HookCardRenderer` delivers the hook-line-forward face + audience-archetype tag + lead scroll-quote + rank badge + tap-to-expand (mechanism prose / seedHook / channel) + secondary band chip + a "Test full →" CTA affordance (D-05/D-08/D-11), under THEME-06.
- `deriveAudienceArchetype` emits the AUDIENCE-persona tag from Flash output (D-03), honest on all-scroll, never a craft-archetype slug (D-04).
</success_criteria>

<output>
Create `.planning/phases/04-hooks-tool/04-01-SUMMARY.md` when done. MUST record (consumed by Plan 02 + Plan 03): (1) the final `HookCardBlock` prop names; (2) the `deriveAudienceArchetype` signature; (3) the `HookCardRenderer`'s "Test full →" CTA seam (the onTest prop name / how Plan 03 wires the deep-link). Plan 02 builds blocks with these exact prop names; Plan 03 wires the CTA + renders these.
</output>
