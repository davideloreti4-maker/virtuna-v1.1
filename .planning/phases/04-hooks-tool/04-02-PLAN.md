---
phase: 04-hooks-tool
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/lib/tools/runners/hooks-runner.ts
  - src/app/api/tools/hooks/route.ts
  - src/app/api/tools/ideas/develop/route.ts
  - src/lib/tools/runners/__tests__/hooks-runner.test.ts
  - src/app/api/tools/hooks/__tests__/route.test.ts
autonomous: true
requirements: [HOOKS-01, HOOKS-02]
must_haves:
  truths:
    - "Posting a Hooks request (from an idea anchor or a seeded topic) over-generates ~8 hooks, SIMs each on the niche-instantiated Flash panel with the HOOK framing (first-2s 'do you stop?'), GATES out sub-floor hooks (band !== 'Weak'), then RANKS survivors #1..N by band tier → audience-fraction and keeps the top 5 (D-01/D-02/D-08)"
    - "Each survivor hook-card carries the audience-archetype tag (D-03 from Plan 01), the embedded band/fraction + lead scroll-quote, and a rank position; the gate keeps slop out, the rank orders the survivors (the flagship demo)"
    - "The /develop placeholder message is REPLACED with real generated hook-card blocks in the SAME open thread, content-first, KC_GEN_VERSION-stamped; the Hooks route auto-fires on 'Develop this →' (D-07/D-10)"
    - "Cold-start / thin profile or anchor-only still produces ranked hooks; the engine suite stays green and ENGINE_VERSION is NOT bumped (text-path only)"
  artifacts:
    - path: "src/lib/tools/runners/hooks-runner.ts"
      provides: "runHooksPipeline: over-generate→parallel-niche-SIM→gate(band!=='Weak')→RANK(band tier→fraction)→top-5 hook-card blocks (D-01/D-02/D-08)"
      contains: "deriveAudienceArchetype"
    - path: "src/app/api/tools/hooks/route.ts"
      provides: "Hooks SSE route: auth→cap→pipeline→content-first stream→insertMessage(KC_GEN_VERSION stamped) in the open thread (HOOKS-01/02)"
      exports: ["POST"]
    - path: "src/app/api/tools/ideas/develop/route.ts"
      provides: "/develop now invokes real Hooks generation (replaces the P3 placeholder, D-07) while preserving the PINNED response contract"
      contains: "runHooksPipeline"
  key_links:
    - from: "src/lib/tools/runners/hooks-runner.ts"
      to: "runFlashTextMode(hookLine, 'hook', { niche, contentType: null })"
      via: "per-candidate niche-instantiated SIM with the HOOK framing (HOOKS-02)"
      pattern: "runFlashTextMode"
    - from: "src/lib/tools/runners/hooks-runner.ts"
      to: "aggregateFlash + deriveAudienceArchetype"
      via: "band/fraction = rank key (D-01/D-02); personas = audience-archetype tag (D-03)"
      pattern: "aggregateFlash"
    - from: "src/app/api/tools/hooks/route.ts"
      to: "insertMessage(openThreadId, 'assistant', hookCardBlocks, kcStamp().kcGenVersion)"
      via: "validated hook-card persistence + KC_GEN_VERSION stamp in the open thread (D-10)"
      pattern: "insertMessage"
---

<objective>
Build the server-side Hooks pipeline — the flagship moat mechanism. A new runner that assembles the Hooks grounding bundle (`mode:"hooks"`, with the upstream idea as `anchor`), OVER-GENERATES ~8 hooks against the cached `KC_HOOKS_SYSTEM_PROMPT` (structured json_object, mirroring the P3 Ideas pattern), runs ONE niche-instantiated Flash pass per candidate with the **HOOK framing** ("scrolling feed, first 2s, do you stop?"), GATES out sub-floor hooks (the P3 slop-gate: `band !== "Weak"` — D-01), then RANKS the survivors #1..N by band tier → audience-fraction and keeps the **top 5** (D-08). Each survivor becomes a validated `hook-card` block (Plan 01) carrying the audience-archetype tag (D-03), the embedded band/fraction + lead scroll-quote (D-02), and its rank. A new SSE route streams them content-first into the user's open thread, KC_GEN_VERSION-stamped. Finally, the P3 `/develop` placeholder is REPLACED with real generation so "Develop this →" auto-fires Hooks (D-07) — while preserving the PINNED `/develop` response contract so Plan 03's UI continues to consume it.

This is "GATE THEN RANK" (D-01): unlike ideas (a gate, no rank), hooks are atomic, discriminable first-2s units, so ranking them is legitimate and IS the demo ("who'll actually stop scrolling"). The gate keeps slop out (honesty spine); the rank orders survivors (the flagship). Ranking is QUALITATIVE (band word + fraction + sim1-flash tag — D-02): NO fabricated numeric pull-score, NO view-count promise (ENGINE-03).

This plan REPLICATES the P3 `ideas-runner.ts` + `/api/tools/ideas/route.ts` shape (03-03), swapping `idea`→`hooks` mode, adding the RANK step after the gate, and consuming the upstream idea anchor.

Purpose: the working over-generate→gate→rank→top-5 pipeline — the moat demo's engine.
Output: `hooks-runner.ts`, `/api/tools/hooks/route.ts`, and the `/develop` placeholder replaced with real generation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-hooks-tool/04-CONTEXT.md
@.planning/corpus/hooks.md

# Depends on (read the SUMMARY for the contracts this plan consumes):
@.planning/phases/04-hooks-tool/04-01-SUMMARY.md

# The proven pipeline this plan REPLICATES (read SUMMARYs + source):
@.planning/phases/03-ideas-tool/03-03-SUMMARY.md
@.planning/phases/03-ideas-tool/03-01-SUMMARY.md
@src/lib/tools/runners/ideas-runner.ts
@src/app/api/tools/ideas/route.ts
@src/app/api/tools/ideas/develop/route.ts

# Pipeline source the runner + route compose (read before editing):
@src/lib/kc/assembler.ts
@src/lib/kc/compiled.ts
@src/lib/engine/flash/run-flash-text-mode.ts
@src/lib/engine/flash/flash-aggregate.ts
@src/lib/threads/threads.ts
@src/lib/threads/messages.ts
@src/lib/kc/kc-stamp.ts
@src/lib/engine/qwen/client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hooks runner — over-generate → niche-SIM gate → RANK → top-5 hook-card blocks (D-01/D-02/D-03/D-08, HOOKS-01/02)</name>
  <files>src/lib/tools/runners/hooks-runner.ts, src/lib/tools/runners/__tests__/hooks-runner.test.ts</files>
  <read_first>
    - src/lib/tools/runners/ideas-runner.ts — the EXACT stage structure to clone: `IDEA_BUFFER`/`MAX_SURVIVORS` constants, the `IDEAS_OUTPUT_CONTRACT` static json_object directive (needed because DashScope rejects json_object unless the literal word "json" appears — 03-04 bug #3), `generateIdeasStructured` (Qwen call shape: KC_*_SYSTEM_PROMPT + contract, temperature:0, seed:QWEN_SEED, response_format json_object), `selectLeadScrollQuote`, the `Promise.all` parallel SIM, the `band === "Weak"` gate, the block-build loop, the `IdeaCardBlockSchema.safeParse` belt-and-suspenders. Hooks clones ALL of this; the ONE structural addition is a RANK step between gate and trim.
    - 04-01-SUMMARY.md — the final `HookCardBlock` prop names + the `deriveAudienceArchetype(personas)` signature. Build blocks with these EXACT prop names; FAIL LOUD if the SUMMARY is missing/incomplete (do not invent prop names).
    - 03-01-SUMMARY.md — the GATE FLOOR (`band !== "Weak"`, MIXED_THRESHOLD=3) and the call pattern `runFlashTextMode(text, framing, { niche, contentType: null })`. The Hooks framing literal is `"hook"` (FlashFraming = "hook" | "idea" | "chat" — confirmed in flash-prompts.ts). FAIL-LOUD (WARNING-3): if MIXED_THRESHOLD is NaN/undefined, STOP — do not default silently.
    - src/lib/kc/compiled.ts — `KC_HOOKS_SYSTEM_PROMPT` (the cached Hooks system prefix — exists, `KC_BASE + KC_HOOKS_SLICE`).
    - src/lib/kc/assembler.ts — `assembleBundle({ ask, platform, mode:"hooks", anchor })`; MODE_ROLES.hooks = [niche, audience, platform, wins, flops]. The `anchor` (upstream idea) is fenced. The route passes the already-fenced bundle OR re-assembles — confirm the input shape.
    - .planning/corpus/hooks.md §Mode Job + §Craft Archetypes — the deliverable is the EXECUTABLE hook (verbatim line / named shot / caption / cut-or-sound cue), multi-modal; the craft archetypes (BOLD/GAP/…) drive intra-batch diversity (each of the N hooks uses a DISTINCT archetype) but stay PRIVATE (never an emitted slug — D-04). The output contract must request distinct-mechanism hooks WITHOUT asking the model to label the craft slug.
  </read_first>
  <behavior>
    - Given a hooks anchor (an upstream idea concept) + a profile with `niche_primary: "fitness"` → over-generates ~8 distinct-mechanism hooks (buffer = Claude's Discretion against cost; ~8 for top-5 per D-08), SIMs each hook line in PARALLEL on the niche panel with the "hook" framing, drops sub-floor hooks (`band === "Weak"`), RANKS survivors by band tier (Strong > Mixed) then audience-fraction (stop-count) descending, and returns up to 5 `hook-card` blocks each carrying `rank` (1..N), `audienceArchetype` (from deriveAudienceArchetype), band/fraction + lead scrollQuote.
    - RANK INVARIANT (D-01/D-02): survivors are ordered #1..N — block[0].props.rank === 1 and bands are non-increasing (a Strong never ranks below a Mixed). Ranking uses band tier then fraction (stop-count), QUALITATIVE only — no fabricated numeric pull-score field exists on the block.
    - D-04 LEAD-QUOTE INVARIANT: every returned hook-card already has its `scrollQuote` populated (part of the face, not deferred).
    - If fewer than 5 survive the gate → return the survivors ranked (do NOT regenerate — D-03 carried forward: no regen loop). Zero survivors is a valid honest answer.
    - Cold-start (null profile) / anchor-only → still produces ranked hooks; niche panel falls back to generic (`niche:null`); the audience-archetype tag degrades honestly (Plan 01).
    - Each returned block passes `validateBlock` (the route re-validates at insertMessage).
  </behavior>
  <action>
    Create `hooks-runner.ts` exporting `runHooksPipeline({ ask, platform, profileRow, anchor }) → { blocks: HookCardBlock[]; warnings: string[]; seedHookPath: "structured" | "markered" }`. Clone the `ideas-runner.ts` stages, adding the RANK step:

    1. GENERATE: `assembleBundle({ ask: ask || "Generate hooks for this idea", platform, mode:"hooks", anchor }, profileRow)` for the user message; system = `KC_HOOKS_SYSTEM_PROMPT` + a static `HOOKS_OUTPUT_CONTRACT` (owned by the runner, mirroring `IDEAS_OUTPUT_CONTRACT` — REQUIRED so DashScope accepts json_object, 03-04 bug #3). The contract requests a `{ hooks: [ { hookLine, mechanism, seedHook, channel?, needsTake? } ] }` array of ~8 DISTINCT-mechanism hooks (intra-batch diversity per corpus/hooks.md §4A), each `hookLine` an executable hook (verbatim line / named shot / caption / cut-or-sound cue). DO NOT request a craft-archetype slug field (D-04 — craft stays private; the `mechanism` is prose reasoning, not a slug). Use `getQwenClient()` / `QWEN_REASONING_MODEL` / `QWEN_SEED`, temperature:0, response_format json_object (Qwen-only). seedHookPath = "structured".
    2. SIM (gate): for each candidate, `runFlashTextMode(candidate.seedHook ?? candidate.hookLine, "hook", { niche: profileRow?.niche_primary ?? null, contentType: null })` in `Promise.all` (parallel — wall-time ≈ one SIM). `aggregateFlash(personas) → { band, fraction }`; select the lead scrollQuote NOW (mirror `selectLeadScrollQuote`); call `deriveAudienceArchetype(personas)` (Plan 01) for the D-03 tag.
    3. GATE: drop candidates where `band === "Weak"` (Plan-01 GATE FLOOR; fail loud if MIXED_THRESHOLD invalid — WARNING-3).
    4. RANK + TRIM (the D-01 addition): order survivors by a comparator — primary key band tier (`Strong` > `Mixed`; map to an ordinal), secondary key audience-fraction stop-count (parse the integer numerator of `fraction` e.g. "6/10 stop" → 6, descending). Tie-break beyond band→fraction = Claude's Discretion (document it; e.g. preserve generation order, or prefer the tougher-crowd stopper). Assign `rank = index + 1`. Keep the top `MAX_HOOKS` (= 5, D-08).
    5. BUILD: assemble `hook-card` blocks (Plan 01 prop names) embedding hookLine/audienceArchetype/mechanism/seedHook/channel + band/fraction/scrollQuote/rank + model:"sim1-flash". Validate each via `HookCardBlockSchema.safeParse` (D-14 belt-and-suspenders); drop + warn on failure.

    Keep generation bounded (~8, no regen loop — D-03 carried forward). Author `hooks-runner.test.ts` (mock Qwen generate + stubbed/fixtured Flash with controllable per-candidate bands) proving: over-generate→gate→rank→≤5, the RANK invariant (rank 1 first, bands non-increasing, fraction tie-break), the lead-quote-on-face invariant, the audience-archetype tag present + never a craft slug, and cold-start/anchor-only behavior. The live route test lands in Task 2.
  </action>
  <verify>
    <automated>npx vitest run src/lib/tools/runners/__tests__/hooks-runner.test.ts</automated>
  </verify>
  <done>
    `runHooksPipeline` over-generates ~8 distinct-mechanism hooks, SIMs each on the niche panel with the "hook" framing in parallel, gates out `Weak` hooks per the Plan-01 floor (fail-loud if absent), RANKS survivors band-tier→fraction, keeps the top 5 (D-08), returns ≤5 validated `hook-card` blocks each with `rank` (1..N, bands non-increasing), the audience-archetype tag (D-03, no craft slug — D-04), embedded band/fraction + LEAD scrollQuote (on the face), no regen loop (D-03). Cold-start/anchor-only produces ranked hooks honestly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hooks SSE route + replace the /develop placeholder with real generation (D-07/D-10, HOOKS-01/02)</name>
  <files>src/app/api/tools/hooks/route.ts, src/app/api/tools/ideas/develop/route.ts, src/app/api/tools/hooks/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/tools/ideas/route.ts — the EXACT SSE route template to clone: `sseHeaders()` (text/event-stream, Cache-Control no-cache no-transform, X-Accel-Buffering no), `supabase.auth.getUser()` 401 gate, the server-side `MAX_MESSAGE_LENGTH=2000` cap (WARNING-5 — independent of client), the rate-limit constants (deferred to v2, same as Ideas — keep the same posture), the `creator_profiles` load by SESSION user_id (`as unknown as ProfileRow | null` cast — 03-03 fix #3), `createOpenThreadLazy(user.id)`, the `ReadableStream({ start(controller) })` SSE loop, the `status → content → score → done` event sequence, and the `insertMessage(threadId, "assistant", blocks, kcStamp().kcGenVersion)` persist (the canonical body = blocks array; the stamp rides the wrapper — 03-04 bug #4).
    - src/app/api/tools/ideas/develop/route.ts — the PINNED contract this plan must PRESERVE: `POST { ideaId?, anchor, platform }` → `{ threadId, messageId, fencedHooksBundle, ideaId }`. Currently it writes a markdown PLACEHOLDER. This plan REPLACES the placeholder write with a real `runHooksPipeline` call + `insertMessage` of hook-card blocks (D-07 auto-fire), WITHOUT breaking the response shape Plan 03's "Develop this →" CTA already depends on (03-04 wired it). The `anchor` is the upstream idea concept; pass it through to the runner.
    - 04-01-SUMMARY.md — the `HookCardBlock` prop names the route persists.
    - src/lib/kc/kc-stamp.ts — `kcStamp().kcGenVersion` + the wrapper landing spot (insertMessage stores `{ kcGenVersion, blocks }`; loadMessages unwraps — 03-04 bug #4 contract).
    - src/lib/threads/messages.ts — `insertMessage(threadId, role, blocks, kcGenVersion?)` re-validates each block at the write boundary (D-14).
  </read_first>
  <behavior>
    - `POST /api/tools/hooks` unauthenticated → 401 (auth before any DB read).
    - Authenticated request with a typed topic → seeded hooks; empty ask + an `anchor` → hooks from the anchored idea; empty + no anchor → from profile/auto (D-09). Profile loaded by SESSION user_id (never body).
    - SERVER-SIDE VALIDATION (WARNING-5): the route INDEPENDENTLY caps `ask` (and `anchor`) length (mirror MAX_MESSAGE_LENGTH / the develop MAX_ANCHOR_LENGTH) → 400 on over-cap, proven by a test posting an over-cap body.
    - The route streams `status` → `content` (the ≤5 ranked card faces WITH lead scroll-quote + audience-archetype tag + rank — content-first) → per-card `score` (band chip a beat later) → `done`. The 5 cards persist to the user's open thread as one assistant message stamped with KC_GEN_VERSION (D-10).
    - `/develop` (the auto-fire seam, D-07): POSTing `{ ideaId?, anchor, platform }` now runs `runHooksPipeline({ ask: "", platform, profileRow, anchor })`, persists the ranked hook-card blocks to the open thread (REPLACING the placeholder), and STILL returns the pinned `{ threadId, messageId, fencedHooksBundle, ideaId }` shape (messageId = the hooks message). A test asserts the placeholder markdown is gone and real hook-card blocks are persisted.
  </behavior>
  <action>
    Create `src/app/api/tools/hooks/route.ts` (`POST`) by cloning `ideas/route.ts`: (1) `supabase.auth.getUser()` → 401; (2) INDEPENDENTLY cap `ask` (mirror MAX_MESSAGE_LENGTH → 400) and any `anchor` (mirror MAX_ANCHOR_LENGTH); keep the same v2-deferred rate-limit posture as Ideas (constants reserved, not wired — do not introduce a new table); (3) load `creator_profiles` by session user_id (`as unknown as ProfileRow | null`, cold-start safe — D-09); (4) `createOpenThreadLazy(user.id)`; (5) `runHooksPipeline({ ask, platform, profileRow, anchor })` (Task 1); (6) emit the SSE `ReadableStream` with `sseHeaders()`: status ("Generating hooks…" / "Scoring on your audience…"), then a `content` event carrying the ≤5 ranked card faces WITH lead scrollQuote + audience-archetype tag + rank (band/fraction deferred to the score event — content-first, IDEAS-02 pattern), then per-card `score` events, then `done`; (7) `insertMessage(openThread.id, "assistant", blocks, kcStamp().kcGenVersion)`.

    REPLACE THE /develop PLACEHOLDER (D-07): edit `src/app/api/tools/ideas/develop/route.ts` so that after the auth + anchor-cap + `createOpenThreadLazy`, it calls `runHooksPipeline({ ask: "", platform, profileRow: <loaded by session user_id>, anchor })`, then `insertMessage(openThread.id, "assistant", hookCardBlocks, kcStamp().kcGenVersion)` — DELETING the markdown placeholder block. PRESERVE the pinned response `{ threadId, messageId, fencedHooksBundle, ideaId }` (messageId = the hooks message row id; keep `fencedHooksBundle` for back-compat even though generation now happens server-side here). Load the profile row by session user_id inside this route too (it previously passed `null` to assembleBundle — now Hooks generation wants the profile for niche grounding; cold-start safe). Keep ALL existing security mitigations (auth before DB, anchor fence via assembleBundle inside the runner's assembler call, server-side anchor cap, block validation at insertMessage).

    Qwen-only throughout. Author `route.test.ts` (mocked runner + supabase) covering: 401 unauth, 400 over-cap ask/anchor (server-side), content-first ≤5 ranked persisted cards with rank + audience tag, KC_GEN_VERSION stamped, AND a `/develop` test proving the placeholder markdown is gone and real ranked hook-card blocks are persisted while the pinned response shape is intact.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/tools/hooks/__tests__/route.test.ts && npm run build</automated>
  </verify>
  <done>
    `POST /api/tools/hooks` enforces auth (401), INDEPENDENTLY caps (400) ask/anchor server-side, loads the profile by session user_id (cold-start safe), creates/reuses the open thread, runs the over-generate→gate→rank→top-5 pipeline, streams content-first (ranked card faces with lead quote + audience tag + rank, then per-card band chip), persists ≤5 hook-card blocks stamped with KC_GEN_VERSION. `/develop` now runs real Hooks generation (placeholder REMOVED, D-07) while preserving the pinned `{ threadId, messageId, fencedHooksBundle, ideaId }` response. Build + tests green; the engine suite is untouched (text-path only — no ENGINE_VERSION bump).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Hooks API route | Untrusted composer `ask` + chain `anchor` cross into server-side generation; the route is the auth + validation + cap gate (server-side, independent of any client check). |
| service client → threads/messages | The route writes with the service client (RLS bypass); ownership enforced via session user_id, never request body (CR-01). |
| model output → persisted blocks | Generated hook-card props are persisted; must pass `validateBlock` at the write boundary (D-14). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Spoofing | Hooks + /develop route auth | mitigate | `supabase.auth.getUser()` before any DB read; 401 on absent session (mirror Ideas route). |
| T-04-04 | Elevation / IDOR | open thread / messages | mitigate | `createOpenThreadLazy` + profile load scoped by SESSION user_id (never body); service-client writes ownership-scoped (CR-01). |
| T-04-05 | Tampering | prompt injection via `ask`/`anchor` | mitigate | `assembleBundle` injection fence (`<<<USER_CONTENT>>>` + sentinel-strip, overflow-safe) wraps ask + anchor on both routes (the runner's assembler call applies it). |
| T-04-06 | Denial of Service | unbounded generation / oversized input | mitigate | Bounded over-generate (~8, no regen — D-03); server-side ask/anchor cap (400) independent of client (WARNING-5). Rate-limit deferred to v2 (same posture as Ideas). |
| T-04-07 | Tampering | model-generated UI via persisted props | mitigate | `insertMessage` re-validates each hook-card block at the write boundary (D-14); invalid → throws. |
| T-04-08 | Compliance | non-Qwen model leak | mitigate | `getQwenClient` only; never import Claude/Gemini/DeepSeek (Qwen-only). |
| T-04-09 | Tampering | honesty spine — fabricated score / view promise | mitigate | Ranking is QUALITATIVE (band word + fraction + sim1-flash tag — D-02); no numeric pull-score, no view-count promise (ENGINE-03). |

(No npm/pip/cargo installs in this plan — no Package Legitimacy / supply-chain checkpoint required.)
</threat_model>

<verification>
- `npx vitest run src/lib/tools/runners/__tests__/hooks-runner.test.ts` — over-generate→gate→rank→≤5, rank invariant, lead-quote-on-face, audience tag, cold-start.
- `npx vitest run src/app/api/tools/hooks/__tests__/route.test.ts` — auth (401), server-side over-cap (400), content-first ranked persist, KC_GEN_VERSION stamp, /develop real-generation replacement.
- `npm run build` — runner + both routes type-check.
- `npm test` — full suite still green; engine suite untouched, same-video Max score-identity preserved (Hooks adds only Flash + generation calls — no ENGINE_VERSION bump).
</verification>

<success_criteria>
- The Hooks route generates ranked hooks from an idea anchor or a seeded topic, cold-start safe, never blocking on onboarding (HOOKS-01/D-09).
- GATE THEN RANK: over-generate→gate (`band !== "Weak"`)→rank (band tier→fraction)→top 5, qualitative only — no fabricated numeric score, no view promise (HOOKS-02/D-01/D-02/D-08/ENGINE-03).
- Each card carries the audience-archetype tag (D-03), the embedded band/fraction + lead scroll-quote (D-02), and a rank position (D-01).
- Content-first streaming: ranked card faces render before the band chip; the gate+rank run server-side before presentation (IDEAS-02 pattern).
- "Develop this →" auto-fires Hooks (D-07): the /develop placeholder is replaced with real generation in the SAME open thread while the pinned response contract holds.
- Each persisted message is KC_GEN_VERSION-stamped (D-10); the engine suite stays green (no ENGINE_VERSION bump — text-path only).
</success_criteria>

<output>
Create `.planning/phases/04-hooks-tool/04-02-SUMMARY.md` when done. MUST record (consumed by Plan 03): (1) the Hooks route SSE event contract (status → content[ranked faces + audience tag + rank + lead quote] → per-card score → done); (2) the over-generate buffer size + the MAX_HOOKS (=5) + the rank comparator (band tier → fraction tie-break used); (3) confirmation that `/develop` now returns real generated hooks (placeholder removed) with the pinned `{ threadId, messageId, fencedHooksBundle, ideaId }` shape intact — so Plan 03's existing "Develop this →" CTA needs no change beyond reading the thread back.
</output>
