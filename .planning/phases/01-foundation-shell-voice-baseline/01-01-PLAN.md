---
phase: 01-foundation-shell-voice-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - .planning/VOICE.md
autonomous: true
requirements: [DS-01, DS-02, CONTENT-01, PERF-02]
must_haves:
  truths:
    - "The root <body> carries the .numen-surface class so bridged tokens (bg-bg, text-text) resolve"
    - "No copy or metadata on the page contains hype/fake-precision language (no 'go viral', 'X% accuracy', 'predictions')"
    - ".planning/VOICE.md exists as the canonical voice baseline Phases 2-4 read before writing copy"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Single root html/body mounting .numen-surface scope + de-hyped Numen metadata"
      contains: "numen-surface"
    - path: ".planning/VOICE.md"
      provides: "Canonical calm confident-mentor voice baseline + do/don't word lists + example lines"
      min_lines: 40
  key_links:
    - from: "src/app/layout.tsx <body>"
      to: ".numen-surface token scope (globals.css @theme inline)"
      via: "className on root body"
      pattern: "className=\"numen-surface"
---

<objective>
Lay the load-bearing foundation: mount the `.numen-surface` token scope on the
single root `<body>` (Phase 1 is the FIRST consumer in the repo), de-hype the
stale snake-oil root metadata, and author the durable `.planning/VOICE.md` voice
baseline that every later phase reads before writing copy.

Purpose: Without the body class, every `bg-bg`/`text-text` utility renders
transparent (RESEARCH Pitfall 1 / globals.css `@theme inline` resolves at usage
site). The root metadata currently VIOLATES the anti-snake-oil moat ("go viral",
"AI-powered predictions") and must be replaced. VOICE.md is the canonical
register source — a net-new authoring deliverable (D-08).
Output: Modified `src/app/layout.tsx` + new `.planning/VOICE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-shell-voice-baseline/01-CONTEXT.md
@.planning/phases/01-foundation-shell-voice-baseline/01-RESEARCH.md
@.planning/phases/01-foundation-shell-voice-baseline/01-PATTERNS.md
@.planning/phases/01-foundation-shell-voice-baseline/01-UI-SPEC.md

<interfaces>
<!-- Current root layout state the executor edits. Extracted from src/app/layout.tsx -->
Root layout currently renders:
- `<html lang="en" className={`${inter.variable} ${serif.variable}`}>` — KEEP font wiring.
- `<body className="min-h-screen bg-background font-sans antialiased">` — CHANGE: add `numen-surface bg-bg text-text`.
- `export const viewport` (width/initialScale) — KEEP.
- `metadataBase: new URL("https://virtuna.ai")` — KEEP.
- `title`/`description`/`openGraph`/`twitter` strings contain "Know what will go viral before you post. AI-powered predictions…" — REPLACE (forbidden by VOICE).
- `<DevLocator />` after `{children}` — KEEP.

The `.numen-surface` token scope is declared at `src/app/globals.css:332-392`. Bridged utilities available: `bg-bg`, `bg-panel`, `bg-panel-2`, `text-text`, `text-text-muted`, `bg-accent`/`text-accent`, `border-border`. These resolve ONLY under an ancestor `.numen-surface` (`@theme inline`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount .numen-surface scope + de-hype root metadata</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (current state — see exact strings to change)
    - src/app/globals.css (lines 332-392 — the .numen-surface token scope + @theme inline bridge; confirms why the body class is load-bearing)
    - .planning/phases/01-foundation-shell-voice-baseline/01-UI-SPEC.md (§Mount contract, §SEO metadata — exact title/description strings)
    - .planning/phases/01-foundation-shell-voice-baseline/01-RESEARCH.md (§dual-html resolution Option A, Pitfall 1, Assumption A1)
  </read_first>
  <action>
    Change the root `<body>` className from `"min-h-screen bg-background font-sans antialiased"` to `"numen-surface min-h-screen bg-bg text-text font-sans antialiased"` (Option A — mount the scope on the single root body; this is the only legal place per Next 16's one-root-layout rule). Keep `bg-bg`/`text-text` so the page paints under the scope; you MAY keep `bg-background` coexisting if needed to avoid regressing untouched showcase routes (RESEARCH Assumption A1) — verify on dev server.

    Replace the stale snake-oil metadata strings (these VIOLATE the anti-snake-oil voice moat — "go viral", "AI-powered predictions"). Set the in-voice baseline from UI-SPEC §SEO:
    - `title`: `Numen — an honest verdict on your content, before you post`
    - `description`: `Numen reads your video like your sharpest audience would and gives you an honest verdict you can act on. No hype score.`
    De-hype the `openGraph.title`/`openGraph.description`/`twitter.title`/`twitter.description` strings the same way (remove "go viral", "predictions", "AI-powered"). Keep `openGraph.url`/`siteName`/`locale`/`type` and `twitter.card` as-is. OG/share IMAGE art is Phase 4 — do not build it here; only the text strings change.

    Keep unchanged: `inter`/`serif` font variable wiring on `<html>`, `viewport`, `metadataBase`, `<DevLocator />`, the `"./globals.css"` import. Do NOT add a second `<html>`/`<body>` anywhere. Do NOT apply the reserved serif to body text.
  </action>
  <verify>
    <automated>grep -q 'numen-surface' src/app/layout.tsx && ! grep -riE "go viral|predict viral|AI-powered prediction|[0-9]+% accuracy" src/app/layout.tsx && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` body `className` contains the literal substring `numen-surface`
    - `grep -riE "go viral|predict viral|AI-powered|[0-9]+% accuracy" src/app/layout.tsx` returns no matches
    - layout.tsx title equals `Numen — an honest verdict on your content, before you post`
    - Exactly one `<html>` and one `<body>` remain in the file (no second root added)
    - `npx tsc --noEmit` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>Root body mounts `.numen-surface`; all metadata strings read in-voice with zero hype/fake-precision terms; type+lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Author .planning/VOICE.md canonical voice baseline</name>
  <files>.planning/VOICE.md</files>
  <read_first>
    - .planning/phases/01-foundation-shell-voice-baseline/01-CONTEXT.md (D-08 / D-08a — what VOICE.md must capture + seed example lines)
    - .planning/phases/01-foundation-shell-voice-baseline/01-UI-SPEC.md (§Voice register, §Placeholder copy table — the register + example lines to codify)
    - .planning/LANDING-STRUCTURE.md (§2-3 — design discipline guardrails + positioning spine, the anti-snake-oil moat source)
  </read_first>
  <action>
    Create `.planning/VOICE.md` as the durable canonical voice reference Phases 2-4 read before writing ANY copy. It is a planning doc (Markdown), not code. Capture, per D-08:
    - The register: calm, plain-language, confident-mentor; second person ("your content", "you").
    - Hard rule — ZERO engine jargon: forbid "Apollo", "fold", "Omni", "model", "pipeline", "score machinery", and similar internal/engine terms.
    - Hard rule — ZERO hype / fake precision (the anti-snake-oil moat): forbid "X% accuracy", "predict virality", "go viral", "guaranteed", "95%", any naked metric promise.
    - Verdict framing rule: always a calibrated BAND + one-line WHY, NEVER a naked number (binds Phases 2-3 verdict copy).
    - Specificity over abstract claims (luma discipline).
    - A do-words / don't-words list (two explicit columns/lists).
    - 2-3 example hero/section copy lines that DEMONSTRATE the register, seeded from D-08a: hero H1 ≈ "Know if your content will land — before you post." and subhead ≈ "Numen reads your video like your sharpest audience would and gives you an honest verdict you can act on." Mark them refinable.
    Keep it scannable (headings + lists). This is the source of truth — write it so a future copy author can self-check a line against it in seconds.
  </action>
  <verify>
    <automated>test -f .planning/VOICE.md && grep -qiE "confident.?mentor" .planning/VOICE.md && grep -qiE "accuracy" .planning/VOICE.md && grep -qiE "band" .planning/VOICE.md && [ "$(grep -cv '^#' .planning/VOICE.md)" -ge 30 ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/VOICE.md` exists
    - File names the "confident-mentor" register (case-insensitive match)
    - File contains an explicit forbidden-terms section naming "accuracy" and "go viral" as banned (the doc references them to forbid them)
    - File states the verdict = band + one-line why, never a naked number rule (contains "band")
    - File contains a do/don't word list and at least 2 example copy lines
    - File body (excluding `#` heading lines) is at least 30 lines
  </acceptance_criteria>
  <done>VOICE.md exists as a scannable canonical baseline: register, jargon ban, hype/fake-precision ban, verdict-framing rule, do/don't lists, and 2-3 demonstrated example lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time source → rendered HTML | Metadata strings + body class are server-rendered; no runtime user input crosses any boundary this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | src/app/layout.tsx metadata strings | accept | Static public marketing copy only; no secrets, keys, or PII in metadata. Verified by grep gate (no credentials introduced). |
| T-01-02 | Tampering | .planning/VOICE.md | n/a | Internal planning doc, not shipped to the client bundle; no trust boundary. |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed this plan (all deps pre-exist; RESEARCH §Package Legitimacy Audit confirms N/A). |

V5 Input Validation: not applicable — no user input this phase. V2/V3/V4/V6 auth/session/access/crypto: not applicable — static markup, no auth.
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint` green after both tasks.
- `grep -riE "go viral|predict viral|AI-powered|[0-9]+% accuracy" src/app/layout.tsx` returns nothing.
- `.planning/VOICE.md` present and non-trivial (≥30 body lines).
- Manual (deferred to phase gate): `npm run dev` → load `/`, confirm warm-neutral bg + readable text render (tokens resolve under the mounted scope).
</verification>

<success_criteria>
- Single root `<body>` carries `numen-surface` (load-bearing scope mount).
- Zero hype/fake-precision language in root metadata.
- Canonical `.planning/VOICE.md` exists for downstream phases.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell-voice-baseline/01-01-SUMMARY.md` when done.
</output>
