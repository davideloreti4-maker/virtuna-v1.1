---
phase: 01-foundation-shell-voice-baseline
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/app/(marketing)/layout.tsx
  - src/app/(marketing)/page.tsx
  - src/app/(marketing)/opengraph-image.tsx
autonomous: true
requirements: [DS-01, DS-02, NAV-01, CONTENT-01, MOT-02, PERF-02]
must_haves:
  truths:
    - "The marketing layout renders a single .numen-surface wrapper <div> containing exactly one Nav, one <main>, one Footer — and no second <html>/<body>"
    - "The .numen-surface scope is on the marketing wrapper <div>; the root src/app/layout.tsx body does NOT carry .numen-surface (Option B / D-02)"
    - "The page renders the seven kero-ordered section slots (#hero..#cta) with exactly one <h1> (the hero headline) and the rest <h2>, no heading-level skips"
    - "Nav anchor links resolve to real on-page section ids"
    - "The page exposes an in-voice <title> + <meta description> with zero hype/fake-precision terms"
    - "The marketing OG image carries zero hype/fake-precision copy (de-hyped, in-voice)"
  artifacts:
    - path: "src/app/(marketing)/layout.tsx"
      provides: "No-html passthrough rendering a .numen-surface wrapper div around Nav + main + Footer; in-voice metadata override"
      contains: "numen-surface"
    - path: "src/app/(marketing)/page.tsx"
      provides: "Composed ordered SectionShell slots in kero order with in-voice headings; single hero <h1>"
      contains: "SectionShell"
    - path: "src/app/(marketing)/opengraph-image.tsx"
      provides: "OG card with de-hyped in-voice copy (text-only fix; art redesign is Phase 4)"
  key_links:
    - from: "src/app/(marketing)/layout.tsx wrapper div"
      to: ".numen-surface token scope (globals.css @theme inline)"
      via: "className on the marketing wrapper div"
      pattern: "className=\"numen-surface"
    - from: "src/app/(marketing)/page.tsx"
      to: "section-shell.tsx (SectionShell)"
      via: "import + ordered composition"
      pattern: "SectionShell"
    - from: "nav anchors"
      to: "page section ids"
      via: "matching #hero/#how-it-works/#honesty/#gallery/#proof/#cta"
      pattern: "id=\"hero\""
---

<objective>
Wire the shell together AND mount the design-system scope: convert
`(marketing)/layout.tsx` from the invalid dual-`<html>` stale state into a
no-html passthrough that renders a single `.numen-surface` wrapper `<div>` around
the new Nav + `<main>` + Footer and exports the in-voice metadata override,
rebuild `(marketing)/page.tsx` to compose the seven kero-ordered `SectionShell`
slots with in-voice placeholder copy and a single hero `<h1>`, and de-hype the
live marketing OG image copy. This is the last wave — it consumes the three
components from Plan 02 and the VOICE.md baseline from Plan 01.

Purpose: Per locked D-02 (Option B, user decision) the `.numen-surface` scope is
mounted HERE — on a wrapper `<div>` inside the marketing layout — NOT on the root
`<body>` (which stays shared/untouched by the 7 sibling routes). Without the
wrapper class, every `bg-bg`/`text-text` utility renders transparent (RESEARCH
Pitfall 1 / `@theme inline` resolves at usage site). This also removes the
dual-`<html>` landmine (RESEARCH Pitfall 3), orphans the stale societies.io
imports (D-04), demonstrates the voice baseline in applied copy (D-08), and closes
the last CONTENT-01 hype leak (the OG card still says "go viral" / "AI-powered
predictions").
Output: Modified marketing `layout.tsx`, `page.tsx`, and `opengraph-image.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-shell-voice-baseline/01-CONTEXT.md
@.planning/phases/01-foundation-shell-voice-baseline/01-RESEARCH.md
@.planning/phases/01-foundation-shell-voice-baseline/01-PATTERNS.md
@.planning/phases/01-foundation-shell-voice-baseline/01-UI-SPEC.md
@.planning/VOICE.md

<interfaces>
<!-- Produced by Plan 01 + Plan 02 (this plan's dependencies). Use directly. -->
- Plan 01 did NOT mount `.numen-surface` on the root body — per Option B / D-02 the scope mounts HERE on the marketing wrapper `<div>` (this plan's Task 1). Plan 01 only de-hyped the root metadata + authored VOICE.md.
- `.numen-surface` token scope declared at `src/app/globals.css:332-392`. Bridged utilities (`bg-bg`, `bg-panel`, `text-text`, `text-text-muted`, `bg-accent`, `border-border`) resolve ONLY under an ancestor `.numen-surface` — the wrapper `<div>` this task creates is that ancestor for the whole marketing route.
- `Nav` (default export OR named — confirm from `src/components/numen-landing/nav.tsx`) — sticky client nav.
- `Footer` from `src/components/numen-landing/footer.tsx` — static server footer.
- `SectionShell` from `src/components/numen-landing/section-shell.tsx` — `{ id, heading?, children?, className? }`; when `heading` is passed it renders an internal `<h2>`; the caller `className` overrides padding.
- `.planning/VOICE.md` (Plan 01) — read it before finalizing any heading/copy string.

Current `(marketing)/layout.tsx` BUG: renders a second `<html lang="en"><body>` + imports stale `@/components/layout/header` `Header` + stale "Artificial Societies" metadata. ALL must go.
Current `(marketing)/page.tsx`: imports stale `@/components/landing` barrel (HeroSection/BackersSection/…) + stale `@/components/layout/footer`. Rebuilding this file orphans those imports (intended, D-04).
Current `(marketing)/opengraph-image.tsx`: VIOLATES CONTENT-01 — `alt`="…AI Content Intelligence…", tagline="Know what will go viral before you post", subtitle="AI-powered predictions, trend intelligence…". Also uses the OLD coral skin (`#07080a` bg, `#FF7F50` accent) — but art redesign is Phase 4; this phase fixes the COPY only.

Locked slot order + ids + placeholder copy (UI-SPEC §Copywriting):
1. `#hero` — H1 "Know if your content will land — before you post." + subhead "Numen reads your video like your sharpest audience would and gives you an honest verdict you can act on." + placeholder CTA. Padding override `pt-28 pb-24 md:pt-40 md:pb-32`.
2. `#how-it-works` — "How the Reading works"
3. `#honesty` — "An honest verdict, not a hype score."
4. `#gallery` — "Real Readings, real creators."
5. `#proof` — neutral placeholder heading (e.g. "Creators who trust the Reading." only if real assets — else neutral)
6. `#cta` — "See what your next video is really saying."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert (marketing)/layout.tsx to no-html passthrough mounting the .numen-surface wrapper</name>
  <files>src/app/(marketing)/layout.tsx</files>
  <read_first>
    - src/app/(marketing)/layout.tsx (current dual-html bug + stale Header import + stale metadata)
    - src/components/numen-landing/nav.tsx (confirm the exact export name — default vs named)
    - src/components/numen-landing/footer.tsx (confirm export name)
    - src/app/globals.css (lines 332-392 — the .numen-surface token scope + @theme inline bridge; confirms why the wrapper class is load-bearing)
    - src/app/layout.tsx (confirm the root body does NOT carry numen-surface — Plan 01 left it untouched; this is the only scope mount)
    - .planning/phases/01-foundation-shell-voice-baseline/01-RESEARCH.md (§Open Questions (RESOLVED) Option B; §Code Examples — Option B no-html passthrough + wrapper-div shape)
    - .planning/phases/01-foundation-shell-voice-baseline/01-PATTERNS.md (§(marketing)/layout.tsx target pattern)
    - .planning/phases/01-foundation-shell-voice-baseline/01-UI-SPEC.md (§SEO metadata, §a11y landmarks — exactly one nav/main/footer)
  </read_first>
  <action>
    Rebuild `src/app/(marketing)/layout.tsx` as a no-html passthrough (RESEARCH Option B) that mounts the `.numen-surface` scope on a single wrapper `<div>` (per locked D-02 / Option B — the marketing layout is where the scope mounts, NOT the root body). Remove the second `<html>`/`<body>`, the local `Inter()` re-import, and the stale `Header` import. Import `Nav` from `@/components/numen-landing/nav` and `Footer` from `@/components/numen-landing/footer` (match the exact export shape — default or named — produced by Plan 02).

    Render a single wrapper `<div className="numen-surface min-h-screen bg-bg text-text">` as the ONLY top-level element, and inside it render — in order — `<Nav />`, then `<main>{children}</main>`, then `<Footer />`. Exactly one of each landmark (a11y). The wrapper `<div>` is the `.numen-surface` ancestor that makes every bridged token (`bg-bg`, `text-text`, …) resolve for the whole marketing route. Do NOT render any `<html>`/`<body>` (the root layout owns the only ones) and do NOT add `numen-surface` to the root layout.

    Export `const metadata: Metadata` with the in-voice override from UI-SPEC §SEO: title `Numen — an honest verdict on your content, before you post`, description `Numen reads your video like your sharpest audience would and gives you an honest verdict you can act on. No hype score.` Drop the stale "Artificial Societies" strings entirely.
  </action>
  <verify>
    <automated>! grep -q '<html' "src/app/(marketing)/layout.tsx" && ! grep -q '<body' "src/app/(marketing)/layout.tsx" && grep -q 'numen-surface' "src/app/(marketing)/layout.tsx" && ! grep -q 'numen-surface' src/app/layout.tsx && ! grep -qi 'Artificial Societies' "src/app/(marketing)/layout.tsx" && ! grep -q 'components/layout/header' "src/app/(marketing)/layout.tsx" && grep -q 'numen-landing/nav' "src/app/(marketing)/layout.tsx" && grep -q '<main>' "src/app/(marketing)/layout.tsx" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File contains NO `<html` and NO `<body` (root layout owns the only ones)
    - File renders a single top-level wrapper `<div className="numen-surface min-h-screen bg-bg text-text">` — `grep -q 'numen-surface' "src/app/(marketing)/layout.tsx"` returns a match
    - The ROOT layout does NOT carry the scope — `grep -q 'numen-surface' src/app/layout.tsx` returns NO match (asserts Option B: scope is on the marketing wrapper, not the root body)
    - Imports `Nav` from `@/components/numen-landing/nav` and `Footer` from `@/components/numen-landing/footer`; does NOT import `@/components/layout/header`
    - Renders exactly one `<main>` wrapping `{children}`, with `<Nav />` before and `<Footer />` after — all inside the `.numen-surface` wrapper div
    - `metadata.title` equals `Numen — an honest verdict on your content, before you post`; no "Artificial Societies" string remains
    - `npx tsc --noEmit` + `npm run lint` exit 0
  </acceptance_criteria>
  <done>Marketing layout is a valid no-html passthrough whose single wrapper div mounts `.numen-surface` (Option B) around one Nav/main/Footer with in-voice metadata; root body stays scope-free; dual-html bug + stale Header gone; type+lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild (marketing)/page.tsx as ordered SectionShell composition with a single hero h1</name>
  <files>src/app/(marketing)/page.tsx</files>
  <read_first>
    - src/app/(marketing)/page.tsx (current stale landing-barrel + footer composition to fully replace)
    - src/components/numen-landing/section-shell.tsx (SectionShell props: when `heading` is passed it emits an internal `<h2>`; children render below; className override behavior)
    - .planning/phases/01-foundation-shell-voice-baseline/01-PATTERNS.md (§(marketing)/page.tsx target composition)
    - .planning/phases/01-foundation-shell-voice-baseline/01-UI-SPEC.md (§Section-slot scaffold locked order, §Copywriting placeholder copy table, §Typography hero H1 treatment, §a11y heading order h1→h2)
    - .planning/VOICE.md (every heading/copy string must obey it)
  </read_first>
  <action>
    Rebuild `src/app/(marketing)/page.tsx` to compose the seven kero-ordered slots via `SectionShell` (Footer now lives in the layout, NOT the page — remove the stale `@/components/landing` barrel import and the stale `@/components/layout/footer` import entirely; this orphans them per D-04). Render in this LOCKED order with in-voice placeholder copy.

    HERO HEADING RULE (prescriptive — resolves the heading-level contract): the hero section MUST render its headline as an explicit `<h1>` that is a direct CHILD of `<SectionShell id="hero">`, and the hero SectionShell MUST NOT receive the `heading=` prop (passing `heading=` would emit an `<h2>`, producing a missing/duplicated `<h1>`). Concretely the hero is `<SectionShell id="hero" className="pt-28 pb-24 md:pt-40 md:pb-32">` with NO `heading` prop, and INSIDE it render: an explicit `<h1 className="text-text text-4xl md:text-6xl font-bold tracking-tight">Know if your content will land — before you post.</h1>`, then the subhead "Numen reads your video like your sharpest audience would and gives you an honest verdict you can act on." (`text-base md:text-lg leading-relaxed text-text-muted`), then a placeholder CTA link (`href="#cta"`, same `bg-accent text-bg ... rounded-lg` treatment + label as nav/footer).

    Every NON-hero slot uses the `heading=` prop (which renders SectionShell's internal `<h2>`) — do NOT add any extra `<h1>` anywhere. This guarantees exactly one `<h1>` (the hero) with the remaining slots as `<h2>`, no heading-level skips:
    2. `<SectionShell id="how-it-works" heading="How the Reading works" />`
    3. `<SectionShell id="honesty" heading="An honest verdict, not a hype score." />`
    4. `<SectionShell id="gallery" heading="Real Readings, real creators." />`
    5. `<SectionShell id="proof" heading="Creators who trust the Reading." />` (neutral placeholder — real proof assets are Phase 3 / D-L4)
    6. `<SectionShell id="cta" heading="See what your next video is really saying." />`
    All slots empty of real media/artifacts (Phases 2-4 fill). Every string must obey VOICE.md — zero "%", "viral", "predict", "accuracy", engine jargon. Use the same CTA label chosen in Plan 02 nav/footer.
  </action>
  <verify>
    <automated>grep -q 'SectionShell' "src/app/(marketing)/page.tsx" && ! grep -q 'components/landing' "src/app/(marketing)/page.tsx" && grep -q 'id="hero"' "src/app/(marketing)/page.tsx" && grep -q 'id="cta"' "src/app/(marketing)/page.tsx" && [ "$(grep -c '<h1' "src/app/(marketing)/page.tsx")" -eq 1 ] && ! grep -riE "go viral|predict viral|[0-9]+% accuracy|AI-powered" "src/app/(marketing)" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File imports `SectionShell` from `@/components/numen-landing/section-shell`; imports NO `@/components/landing` barrel and NO `@/components/layout/footer`
    - Renders six `SectionShell` slots with ids in exact order: `hero`, `how-it-works`, `honesty`, `gallery`, `proof`, `cta`
    - The hero `<SectionShell id="hero">` does NOT pass a `heading=` prop and instead renders an explicit `<h1>` child (the hero headline); all other slots use `heading=` (→ `<h2>`)
    - Page has exactly one `<h1>` — `grep -c '<h1' "src/app/(marketing)/page.tsx"` equals 1 — and no heading-level skips (h1 → h2 only)
    - Hero slot carries the `pt-28 pb-24 md:pt-40 md:pb-32` className override and renders the subhead + a `href="#cta"` CTA
    - `grep -riE "go viral|predict viral|[0-9]+% accuracy|AI-powered" "src/app/(marketing)"` returns no matches
    - `npx tsc --noEmit` + `npm run lint` exit 0
  </acceptance_criteria>
  <done>Page composes the seven kero-ordered slots with exactly one hero `<h1>` (no `heading=` on hero) and `<h2>` for every other slot, nav anchors resolve to real ids, stale imports orphaned, anti-snake-oil grep clean; type+lint green.</done>
</task>

<task type="auto">
  <name>Task 3: De-hype the marketing OG image copy (text-only, no art redesign)</name>
  <files>src/app/(marketing)/opengraph-image.tsx</files>
  <read_first>
    - src/app/(marketing)/opengraph-image.tsx (current file — three hype strings to replace: `alt`, tagline `<div>`, subtitle `<div>`)
    - .planning/phases/01-foundation-shell-voice-baseline/01-UI-SPEC.md (§SEO metadata — the in-voice title/description baseline)
    - .planning/VOICE.md (forbidden-terms list the new strings must obey)
  </read_first>
  <action>
    Replace ONLY the hype/fake-precision copy in `src/app/(marketing)/opengraph-image.tsx` — this is a CONTENT-01 leak (the live OG card still renders "Know what will go viral before you post" + "AI-powered predictions, trend intelligence, and audience insights"). Do NOT redesign the card art/layout/colors — the full OG/share-card art (including swapping the old coral `#07080a`/`#FF7F50` skin to the Numen-surface palette) is Phase 4 (D-11). This phase fixes the COPY so no hype ships in the meantime. Change:
    - `export const alt` → an in-voice alt string with zero hype, e.g. `Numen — an honest verdict on your content, before you post`.
    - The tagline `<div>` ("Know what will go viral before you post") → the in-voice positioning line, e.g. `An honest verdict on your content — before you post.`
    - The subtitle `<div>` ("AI-powered predictions, trend intelligence, and audience insights for TikTok creators") → an in-voice subtitle with zero "AI-powered"/"predictions"/"viral", e.g. `Numen reads your video like your sharpest audience would and gives you a verdict you can act on.`
    Keep the SVG mark, dimensions, `runtime`, `size`, `contentType`, and the existing colors as-is (art is Phase 4). Strings must obey VOICE.md.
  </action>
  <verify>
    <automated>! grep -riE "go viral|predict|AI-powered|[0-9]+% accuracy|virality" "src/app/(marketing)/opengraph-image.tsx" && grep -q 'export const alt' "src/app/(marketing)/opengraph-image.tsx" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -riE "go viral|predict|AI-powered|virality|[0-9]+% accuracy" "src/app/(marketing)/opengraph-image.tsx"` returns no matches
    - `alt`, tagline, and subtitle strings all read in-voice (no hype/fake-precision)
    - SVG mark, `size`, `runtime`, `contentType` unchanged (art redesign deferred to Phase 4)
    - `npx tsc --noEmit` + `npm run lint` exit 0
  </acceptance_criteria>
  <done>OG card copy is de-hyped + in-voice; art/colors untouched (Phase 4); no hype ships on the marketing route's social card.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time source → server-rendered HTML / edge OG image | Metadata + static slot markup + OG card; no runtime user input crosses any boundary |
| nav anchors → on-page ids | Internal `#`-fragment navigation only; no external redirect surface in layout/page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Spoofing / SEO injection | (marketing)/layout.tsx + opengraph-image.tsx metadata | mitigate | Title/description/OG strings are static literals (no interpolated user/query data) — no metadata-injection vector. Verified in-voice + hype-free by grep gate. |
| T-01-07 | Information Disclosure | (marketing)/page.tsx slot copy + hrefs | accept | All copy is static placeholder marketing text; only internal `#` anchors — no secrets, no PII, no external redirect. |
| T-01-08 | Tampering | opengraph-image.tsx (edge runtime) | accept | Pure static render, no fetch/user input/dynamic params; edge runtime executes only literal JSX. No injectable surface. |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed (all deps pre-exist; RESEARCH §Package Legitimacy Audit N/A). |

V5 Input Validation: not applicable — no user input (no forms/CTAs wired; placeholder anchors only). V2/V3/V4/V6: not applicable — static markup, no auth/session/access/crypto. External-link safety (`rel=noopener`) lives in footer (Plan 02); this plan introduces no new external links.
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint` green after each task.
- `npm run build` green (wave-merge gate — the real compile/route/type gate; confirms the dual-html bug is gone, the page + OG image render).
- Scope-mount placement: `grep -q 'numen-surface' "src/app/(marketing)/layout.tsx"` matches AND `grep -q 'numen-surface' src/app/layout.tsx` does NOT (Option B — scope on the marketing wrapper, root body untouched).
- Single-h1 gate: `grep -c '<h1' "src/app/(marketing)/page.tsx"` equals 1.
- Anti-snake-oil grep clean across the whole route: `grep -riE "go viral|predict viral|[0-9]+% accuracy|AI-powered|virality" "src/app/(marketing)"` returns nothing.
- Stale-import orphan check: `grep -r "@/components/landing\|@/components/layout/header\|@/components/layout/footer" "src/app/(marketing)"` returns nothing.
- Manual (phase gate): `npm run dev` → load `/`; confirm warm-neutral bg + readable text (scope resolves via the wrapper div), nav anchors jump to the right sections clearing the sticky bar, desktop + 375px both render nav/footer.
</verification>

<success_criteria>
- Marketing layout is a valid single-root passthrough whose wrapper div mounts `.numen-surface` (Option B); root body stays scope-free.
- Page renders seven kero-ordered slots with exactly one hero `<h1>` and `<h2>` for the rest; nav anchors resolve.
- In-voice metadata + de-hyped OG copy; zero hype/fake-precision anywhere on the route; `npm run build` green.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell-voice-baseline/01-03-SUMMARY.md` when done.
</output>
