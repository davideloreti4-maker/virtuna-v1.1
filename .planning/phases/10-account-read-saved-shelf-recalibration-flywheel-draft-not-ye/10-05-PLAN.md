---
phase: 10-account-read-saved-shelf-recalibration-flywheel
plan: 05
type: execute
wave: 3
depends_on: ["10-02", "10-04"]
files_modified:
  - src/lib/account-read/account-read.ts
  - src/lib/tools/blocks.ts
  - src/lib/tools/block-registry.ts
  - src/app/api/account-read/route.ts
  - src/components/thread/account-read-block.tsx
  - src/components/thread/message-blocks.tsx
  - src/lib/account-read/__tests__/account-read.test.ts
autonomous: true
requirements: [SELF-01, SELF-02, SELF-03]
must_haves:
  truths:
    - "Read my account scrapes the creator's own history and surfaces recurring hooks, format mix, drop-points, working-vs-fix"
    - "The Account Read renders as a thread card reusing the fixed reading/ components (no model-generated UI)"
    - "Thin/empty/unscrapeable history shows an honest 'not enough history to read yet' warning state — never fabricated patterns"
    - "An accuracy track record ('within X% on your last N') is sourced from accumulated reconciliation rows, shown only when enough exist"
    - "The Account Read card is savable to the shelf as item_type 'read'"
  artifacts:
    - path: "src/lib/account-read/account-read.ts"
      provides: "scrape + analysis/history -> pattern extraction with thin gate"
      contains: "THIN_MIN_VIDEOS"
    - path: "src/app/api/account-read/route.ts"
      provides: "SSE: own-account Read (status -> done/fallback/error)"
      exports: ["POST"]
    - path: "src/components/thread/account-read-block.tsx"
      provides: "account-read typed block composing reading/ components"
    - path: "src/lib/tools/blocks.ts"
      provides: "AccountReadBlockSchema"
      contains: "account-read"
  key_links:
    - from: "src/components/thread/account-read-block.tsx"
      to: "src/components/reading/reading-hero.tsx"
      via: "reuse ReadingHero + accordions"
      pattern: "ReadingHero|reading"
    - from: "src/app/api/account-read/route.ts"
      to: "src/lib/scraping/apify-provider.ts"
      via: "scrapeProfile + scrapeVideos (own account, apidojo multi-post)"
      pattern: "scrapeVideos"
    - from: "src/lib/account-read/account-read.ts"
      to: "src/lib/flywheel/reconciliation-repo.ts"
      via: "craft-error guidance + accuracy track record"
      pattern: "listReconciliations|reconciliation"
---

<objective>
Build the Account Read (SELF-01/02/03): "a Read on your own account" — the "know thyself" companion to Discover's "know thy competitor." Scrape the creator's own history → recurring hook/format patterns, drop-points, working-vs-fix → render as a thread card reusing the fixed `reading/` renderers. Honest thin-history fallback (carries P7 D-06). Surfaces the accuracy track record sourced from the flywheel's craft-error reconciliation rows.

Purpose: Rides existing infra LEAN per the cut line (D-01) — Apify personal-scrape (P7), analysis/history, the `reading/` components, the P7 thin gate. The "working-vs-fix" framing is where the flywheel's craft-error side surfaces to the creator (D-03b) WITHOUT ever mutating the model.
Output: `account-read.ts` (pattern extraction + thin gate), the `account-read` typed block, `POST /api/account-read` SSE route, the card composing reading/ components.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-RESEARCH.md
@.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-UI-SPEC.md

# Interface sources:
@src/lib/audience/calibration.ts
@src/lib/scraping/apify-provider.ts
@src/lib/tools/blocks.ts
@src/lib/tools/block-registry.ts
@src/components/thread/multi-audience-read-block.tsx
@src/components/thread/message-blocks.tsx
@src/app/api/audiences/calibrate/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Account Read pattern extraction + thin gate (SELF-01/02/03)</name>
  <files>src/lib/account-read/account-read.ts, src/lib/account-read/__tests__/account-read.test.ts</files>
  <read_first>
    - src/lib/audience/calibration.ts (THIN_MIN_VIDEOS=10, getFollowerTier null check, the {fallback:'thin'} vs {error:'scrape_failed'} return discrimination, deriveAudienceProfile — carry P7 D-06 fallback verbatim)
    - src/lib/scraping/apify-provider.ts (scrapeProfile + scrapeVideos(handle,30) — own account, apidojo multi-post, NOT single-URL)
    - src/lib/flywheel/reconciliation-repo.ts (listReconciliations — for craft-error guidance + accuracy track record, Plan 02)
    - 10-RESEARCH.md §6 "Account Read Generation" (deterministic + light-LLM pattern extraction: recurring hook openings, format mix, drop-points; working = top patterns, fix = craft-error dispositions; thin fallback; accuracy track record from reconciliations) + §Project Constraints (Qwen-only for any LLM step)
  </read_first>
  <behavior>
    - generateAccountRead(handle, userId, supabase): scrape own account (scrapeProfile + scrapeVideos) ∪ analysis/history → returns either an AccountReadResult (patterns: recurringHooks, formatMix, dropPoints, working[], fix[]) OR {fallback:'thin'} OR {error:'scrape_failed'}.
    - Thin gate reuses P7 exactly: getFollowerTier===null AND videos < THIN_MIN_VIDEOS → fallback 'thin' (NEVER fabricate patterns, SELF-02).
    - "fix" guidance sourced from craft-error dispositions in reconciliations (D-03b) — read-only, never touches the model.
    - Accuracy track record (SELF-03): "within X% on your last N" computed from the craft-error magnitude trend in reconciliations; only returned when N >= enough rows.
    - Any LLM pattern-naming step uses the existing Qwen path (no Gemini/DeepSeek).
  </behavior>
  <action>
    Create `account-read.ts` exporting `generateAccountRead(...)` mirroring `calibration.ts` structure (scrape → thin gate → derive). Deterministic extraction of recurringHooks/formatMix/dropPoints from `VideoData[]`; "working" = top-performing patterns, "fix" = craft-error dispositions pulled from `listReconciliations` (D-03b). Compute the accuracy track record from the reconciliation craft-error trend. Reuse `THIN_MIN_VIDEOS` + the `getFollowerTier===null && videos<THIN_MIN_VIDEOS` gate → `{fallback:'thin'}`; scrape failure → `{error}`. Tests: thin input → fallback (no fabricated patterns); rich input → patterns; track record withheld below the row threshold and present above it.
  </action>
  <verify>
    <automated>npx vitest run src/lib/account-read/__tests__/account-read.test.ts</automated>
  </verify>
  <done>generateAccountRead extracts patterns, sources fix-guidance + track record from reconciliations, and honestly falls back on thin history; tests pass.</done>
  <acceptance_criteria>
    - Thin/empty history returns {fallback:'thin'} with NO fabricated patterns (SELF-02).
    - "fix" derives from craft-error reconciliation rows; the model is never mutated.
    - Track record only present when enough reconciliation rows exist (SELF-03).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: account-read typed block (schema + registry + renderer reusing reading/)</name>
  <files>src/lib/tools/blocks.ts, src/lib/tools/block-registry.ts, src/components/thread/account-read-block.tsx, src/components/thread/message-blocks.tsx</files>
  <read_first>
    - src/lib/tools/blocks.ts (the block schema idiom: {type: z.literal(...), props: z.object({...})}; MultiAudienceReadBlockSchema is the closest precedent — a static composed card; also the BlockUnionSchema discriminatedUnion list to extend)
    - src/lib/tools/block-registry.ts (BLOCK_REGISTRY map — add "account-read"; validateBlock/assertBlocksInRegistry)
    - src/components/thread/multi-audience-read-block.tsx (the static-composed-card renderer precedent)
    - src/components/thread/message-blocks.tsx (BLOCK_COMPONENTS map — add the renderer; rehydration validateBlock path)
    - src/components/reading/index.ts + reading-hero.tsx + reading-accordion.tsx + reading-section.tsx (the fixed renderers to COMPOSE — ReadingHero for the account summary, accordions for pattern sections)
    - 10-UI-SPEC.md §"Account Read card" (hero "A Read on your own account"; section labels What's working/What to fix/Recurring hooks/Format mix/Drop-points; thin-history fallback heading+body warning-toned; accuracy track-record copy + empty copy; Save this Read → item_type 'read') + §Interaction Contracts (compose reading/ components, register like multi-audience-read, fixed typed renderer)
  </read_first>
  <action>
    Add `AccountReadBlockSchema` to `blocks.ts` (`type: z.literal("account-read")`, props carrying the AccountReadResult shape + an optional `fallback:'thin'` flag + optional `trackRecord`) and append it to `BlockUnionSchema`. Register `"account-read"` in `BLOCK_REGISTRY`. Build `account-read-block.tsx` COMPOSING the `reading/` components (ReadingHero repurposed for the account summary with "A Read on your own account"; accordions/sections for What's working · What to fix · Recurring hooks · Format mix · Drop-points). Render the thin-history fallback as a warning-toned (`--color-warning`, never error/coral) "Not enough history to read yet" state — reuse the P7 `CouldNotAnalyze`/reading.tsx D-13 honesty pattern. Render the accuracy track record (cream-primary number, never coral) or its empty copy. Mount a `<SaveAffordance item_type="read" …/>` (from Plan 04). Wire the renderer into `BLOCK_COMPONENTS` in `message-blocks.tsx`. Fixed typed renderer — NO model-generated UI.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -q "account-read" src/lib/tools/block-registry.ts && grep -q "account-read" src/components/thread/message-blocks.tsx</automated>
  </verify>
  <done>The account-read block validates, registers, composes reading/ renderers, shows the warning-toned thin fallback + track record, and is savable; build passes.</done>
  <acceptance_criteria>
    - Block registered in BLOCK_REGISTRY + BlockUnionSchema + BLOCK_COMPONENTS (rehydration-safe).
    - Renderer composes reading/ components — no new bespoke Read UI, no model-generated UI.
    - Thin fallback uses --color-warning, never error/coral; track-record number cream-primary not coral.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Account Read SSE route (SELF-01/02)</name>
  <files>src/app/api/account-read/route.ts</files>
  <read_first>
    - src/app/api/audiences/calibrate/route.ts (VERBATIM SSE pattern: maxDuration=300, getUser() auth-first, status→done/fallback/error, generic error copy never echoing input)
    - src/lib/account-read/account-read.ts (generateAccountRead — Task 1)
    - 10-RESEARCH.md §6 "Sync vs stream: Stream" (own-account scrape is the 1-3 min P7 profile; use calibrate SSE verbatim) + §Security Domain
    - 10-UI-SPEC.md §"Account Read card" loading copy ("Reading your account…") + §Interaction Contracts (stream status→done/fallback/error)
  </read_first>
  <action>
    Create `POST /api/account-read` mirroring `calibrate/route.ts`: `export const maxDuration = 300`; auth-first `getUser()`; resolve the creator's handle (from profile/audience); stream `status` "Reading your account…" → `generateAccountRead(...)` → on success `done` with the `account-read` block props (the thread persists it like other tool outputs) → on `{fallback:'thin'}` emit `fallback` (warning-toned, SELF-02) → on `{error}` emit `error` with generic copy. Never echo the raw handle into error events.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -q "maxDuration = 300" src/app/api/account-read/route.ts && grep -q "getUser" src/app/api/account-read/route.ts</automated>
  </verify>
  <done>"Read my account" streams staged status, returns a composed Account Read block or an honest thin/error state, auth-first; build passes.</done>
  <acceptance_criteria>
    - Stream emits status → done | fallback | error (calibrate parity).
    - Thin history → fallback event (warning), never a fabricated done payload.
    - getUser() before any scrape/DB read; no raw handle in error copy.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/account-read | the creator's own handle drives a scrape (own-account, ownership satisfied by P7 personal-scrape). |
| app → Apify | own-account scrape — SSRF/own-handle scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12 | Spoofing | Account Read of a handle not owned | mitigate | Resolve handle from the authenticated user's own profile/audience, not arbitrary input; getUser() auth-first (D-08 reuses P7 ownership, NOT new OAuth). |
| T-10-13 | Information disclosure | fabricated patterns misleading the creator | mitigate | Thin gate returns honest fallback; NEVER fabricate (SELF-02); craft-fix is read-only from reconciliations. |
| T-10-14 | Tampering | account-read block bypassing the registry | mitigate | validateBlock at write + rehydration; account-read added to BLOCK_REGISTRY (fixed typed renderer). |
| T-10-SC | Tampering | npm installs | mitigate | zero new packages (RESEARCH audit); no install task. |
</threat_model>

<verification>
- `npm run build` + `npx vitest run src/lib/account-read/` green.
- Block round-trips through validateBlock (registry + union + components).
- `/api/account-read` is auth-first + maxDuration=300; thin history yields a warning fallback.
</verification>

<success_criteria>
A creator can run "Read my account" and get an honest, reading/-rendered Read of their own history (or an honest thin-history state), with a flywheel-sourced accuracy track record, savable to the shelf — the "know thyself" surface built lean. Zero new packages.
</success_criteria>

<output>
Create `.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-05-SUMMARY.md` when done.
</output>
