---
phase: 10-account-read-saved-shelf-recalibration-flywheel
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/lib/tools/runners/flash-runner.ts
  - src/app/api/outcomes/signature/route.ts
  - src/components/app/simulation/outcome-signature-form.tsx
  - src/hooks/queries/use-outcome-signature.ts
  - src/lib/flywheel/__tests__/predicted-pin.test.ts
autonomous: true
requirements: [FLYWHEEL-01, FLYWHEEL-02, FLYWHEEL-03]
must_haves:
  truths:
    - "At SIM run time the predicted disposition vector + the audience_id it ran under are pinned (persisted), never recomputed later"
    - "Pasting a posted URL scrapes public metrics, optionally accepts creator-supplied private signals, and produces a realized signature with per-channel provenance"
    - "Capturing an outcome runs reconcile() and writes a reconciliation row (logged), routing calibration vs craft"
    - "A blank private field is excluded (never counted as zero)"
    - "Outcome capture streams staged SSE status (Apify latency), never blocks opaquely"
  artifacts:
    - path: "src/app/api/outcomes/signature/route.ts"
      provides: "SSE: paste URL -> scrape -> realized signature -> reconcile -> log row"
      exports: ["POST"]
    - path: "src/components/app/simulation/outcome-signature-form.tsx"
      provides: "Capture outcome form (paste URL + optional private add + provenance tags)"
    - path: "src/lib/tools/runners/flash-runner.ts"
      provides: "predicted-vector pinning at SIM run (FLYWHEEL-02)"
      contains: "predictedSignature"
  key_links:
    - from: "src/app/api/outcomes/signature/route.ts"
      to: "src/lib/scraping/apify-provider.ts"
      via: "scrapeSinglePostMetrics"
      pattern: "scrapeSinglePostMetrics"
    - from: "src/app/api/outcomes/signature/route.ts"
      to: "src/lib/flywheel/reconcile.ts"
      via: "reconcile + insertReconciliation"
      pattern: "reconcile"
    - from: "src/lib/tools/runners/flash-runner.ts"
      to: "src/lib/flywheel/outcome-repo.ts"
      via: "pin predicted_vector + audience_id"
      pattern: "insertOutcomeSignature|predicted_vector"
---

<objective>
Wire the outcome half of the flywheel: pin the PREDICTED signature when the SIM runs (FLYWHEEL-02, Pitfall 6), and build the paste-URL → public-scrape → realized-signature → reconcile → log pipeline (FLYWHEEL-01/03). This is the "measure" + "reconcile" arc of predict→post→measure→reconcile→correct.

Purpose: Reconciliation needs a pinned prediction to compare against (never recompute under a changed audience — Pitfall 6). The capture path reuses the calibrate SSE pattern + scrapeSinglePostMetrics (Plan 02) + the pure math (Plan 01), staying honest about public-vs-creator-supplied provenance (D-04).
Output: predicted-pin in flash-runner, `POST /api/outcomes/signature` SSE route, the capture form + query hook.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-RESEARCH.md
@.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-UI-SPEC.md

# Interface sources:
@src/lib/tools/runners/flash-runner.ts
@src/app/api/audiences/calibrate/route.ts
@src/app/api/outcomes/route.ts
@src/components/app/simulation/outcome-form.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the predicted signature at SIM run (FLYWHEEL-02)</name>
  <files>src/lib/tools/runners/flash-runner.ts, src/lib/flywheel/__tests__/predicted-pin.test.ts</files>
  <read_first>
    - src/lib/tools/runners/flash-runner.ts (where runFlashTextMode returns FlashPersona[]/personas; the result shape at line ~106)
    - src/lib/flywheel/signature.ts (predictedSignature — Plan 01)
    - src/lib/flywheel/outcome-repo.ts (insertOutcomeSignature — Plan 02)
    - 10-RESEARCH.md §Pitfall 6 (pin predicted vector + audience_id at SIM time, never recompute) + §"Predicted-signature persistence"
    - src/lib/engine/persona-weights.ts (the audience_id / active-audience context the SIM runs under — P7 D-04 per-thread pin)
  </read_first>
  <behavior>
    - When the flash SIM produces FlashPersona[], compute predictedSignature(personas) and persist it (predicted_vector + the audience_id the run used + analysis_id if present) so reconciliation reads a pinned prediction.
    - Pinning is non-fatal: a persistence failure must NOT block the SIM card render (mirror the existing non-fatal insertMessage pattern in the runners).
    - General/null audience runs still pin a vector (reconciliation simply may not propose for General — gate excludes it).
  </behavior>
  <action>
    In `flash-runner.ts`, after `runFlashTextMode` yields `result.personas`, compute `predictedSignature(result.personas)` and write an `outcome_signatures` row carrying `predicted_vector`, `audience_id` (the active audience the run used), and `analysis_id` (when the run is tied to an analysis; null otherwise). Wrap the write in a try/catch and log-only on failure (non-fatal — never block the card, per FLYWHEEL-02 "content-first" precedent). Do NOT recompute the predicted vector anywhere else. Add `predicted-pin.test.ts` asserting predictedSignature is called on the personas and a row is attempted with the run's audience_id (mock the repo).
  </action>
  <verify>
    <automated>npx vitest run src/lib/flywheel/__tests__/predicted-pin.test.ts && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>The predicted vector + audience_id are pinned at SIM time, non-fatally; test confirms; build passes.</done>
  <acceptance_criteria>
    - predicted_vector persisted at run time with the run's audience_id (Pitfall 6).
    - Persistence failure does not throw into the SIM card path.
    - No recomputation of the predicted vector outside this pin point.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Outcome-signature capture SSE route + reconcile-on-capture (FLYWHEEL-01/03)</name>
  <files>src/app/api/outcomes/signature/route.ts</files>
  <read_first>
    - src/app/api/audiences/calibrate/route.ts (VERBATIM SSE pattern: maxDuration=300, getUser() auth-first, ReadableStream send(event,data), status→done/fallback/error events, generic error copy never echoing input)
    - src/lib/scraping/apify-provider.ts (scrapeSinglePostMetrics — Plan 02) + resolveVideoUrl SSRF guard
    - src/lib/flywheel/{realized-signature,reconcile}.ts (Plan 01) + outcome-repo/reconciliation-repo (Plan 02)
    - 10-RESEARCH.md §Pitfall 3 (absent channel excluded, provenance per metric), §Security Domain (auth-first, SSRF, zod), §3 classifier
    - 10-UI-SPEC.md §"Outcome capture — signature" (exact copy + provenance tags + "blank = not counted")
  </read_first>
  <action>
    Create `POST /api/outcomes/signature` mirroring `calibrate/route.ts`: `export const maxDuration = 300`; auth-first `getUser()`; zod-validate body `{ analysis_id?, audience_id?, posted_url, private?: {saves?, watch_through_pct?, link_clicks?} }`. Stream: `status` "Reading the post…" → `scrapeSinglePostMetrics(posted_url)` (through the SSRF guard) → build RealizedMetrics tagging each public channel `public_scrape` and each supplied private field `creator_supplied` (BLANK field excluded entirely — never zero, Pitfall 3) → `realizedSignature(metrics)` → read the pinned `predicted_vector` for this analysis/audience via outcome-repo → `reconcile(predicted, realized)` → UPDATE the outcome_signatures row with realized_vector/provenance/raw_metrics and `insertReconciliation` with classification + `proposal_state:'logged'` → `done`. On scrape failure emit `error` with the UI-SPEC generic copy ("Couldn't read that post…") and allow the form's creator-supplied-only fallback. Never echo raw URL into error events.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -q "maxDuration = 300" src/app/api/outcomes/signature/route.ts && grep -q "getUser" src/app/api/outcomes/signature/route.ts</automated>
  </verify>
  <done>The route scrapes public metrics, builds a provenance-tagged realized signature, reconciles against the pinned prediction, logs a reconciliation row, and streams staged status — auth-first, SSRF-guarded.</done>
  <acceptance_criteria>
    - Blank private fields are excluded from the realized signature (not zero-filled).
    - reconcile() output written to reconciliations with proposal_state 'logged'.
    - Realized side compared ONLY against the pinned predicted_vector (no recompute).
    - getUser() before any DB read; URL routed through SSRF guard.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Capture-outcome form + query hook (FLYWHEEL-01 UI)</name>
  <files>src/components/app/simulation/outcome-signature-form.tsx, src/hooks/queries/use-outcome-signature.ts</files>
  <read_first>
    - src/components/app/simulation/outcome-form.tsx (existing legacy form to model the layout from — leave it intact, build a NEW signature form per RESEARCH Open Q3)
    - src/hooks/queries/use-outcomes.ts (the react-query hook idiom + SSE consumption if present)
    - 10-UI-SPEC.md §"Outcome capture — signature" (all copy: heading "Capture outcome", "Posted video URL", public helper, "Add private signals (optional)", provenance tags "from the post"/"you added", "Left blank = not counted", success "Outcome captured.", scrape-fail copy) + §Color (provenance tags neutral cream, primary CTA accent) + §Typography/§Spacing tokens
  </read_first>
  <action>
    Build `outcome-signature-form.tsx` per the UI-SPEC: paste-URL primary field + a collapsed "Add private signals (optional)" section (Saves / Watch-through % / Link clicks). Each metric shows its provenance tag (public = "from the post" neutral; supplied = "you added" neutral — NEVER coral). Show the "Left blank = not counted (we never assume zero)" honesty note. Primary CTA "Capture outcome" → flips to "Reading the post…" while the SSE runs. NO predicted-vs-actual headline delta on success — just "Outcome captured." (flywheel reconciles signatures, not scores). Use flat-warm tokens, Inter, 2 weights, 60/30/10 color, accent only on the primary CTA + focus ring. Build `use-outcome-signature.ts` consuming the SSE route (mirror the existing query/SSE hook idiom). Leave the legacy `outcome-form.tsx` / `/api/outcomes` untouched (RESEARCH Open Q3).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && npm run lint 2>&1 | grep -i "outcome-signature" | tail -5</automated>
  </verify>
  <done>The capture form posts to the signature route, discloses provenance, never zero-fills blanks, shows no headline-delta framing, and follows flat-warm SSOT; build + lint pass.</done>
  <acceptance_criteria>
    - Provenance tags use neutral cream, never coral (UI-SPEC §Color).
    - "Left blank = not counted" honesty note present.
    - Success copy is "Outcome captured." with no predicted-vs-actual delta.
    - Legacy outcome-form.tsx left intact.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/outcomes/signature | posted_url + private signals are untrusted user input. |
| app → Apify | the posted URL is scraped — SSRF surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering / Elevation | signature route input | mitigate | zod-validate body; auth-first getUser(); user_id from session; RLS on outcome_signatures/reconciliations. |
| T-10-07 | DoS / SSRF | posted_url scrape | mitigate | route through resolveVideoUrl HTTPS allowlist guard; maxDuration=300 cap. |
| T-10-08 | Repudiation / data integrity | false recalibration from a missing channel | mitigate | absent channel excluded (Pitfall 3); reconcile only over dispositions present in both (Plan 01). |
| T-10-SC | Tampering | npm installs | mitigate | zero new packages (RESEARCH audit); no install task. |
</threat_model>

<verification>
- `npm run build` + `npx vitest run src/lib/flywheel/` green.
- `POST /api/outcomes/signature` is auth-first (grep `getUser`) + `maxDuration = 300`.
- Form shows provenance + "blank = not counted"; success copy carries no headline delta.
</verification>

<success_criteria>
Predicted signatures are pinned at SIM time; pasting a posted URL produces a provenance-honest realized signature, reconciles it against the pinned prediction, and logs a classified reconciliation row — the measure→reconcile arc, streamed and SSRF-safe. Zero new packages.
</success_criteria>

<output>
Create `.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-03-SUMMARY.md` when done.
</output>
