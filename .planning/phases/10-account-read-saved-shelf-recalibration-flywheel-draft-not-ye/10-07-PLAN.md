---
phase: 10-account-read-saved-shelf-recalibration-flywheel
plan: 07
type: execute
wave: 4
depends_on: ["10-02", "10-03", "10-04", "10-05", "10-06"]
files_modified:
  - src/types/database.types.ts
  - src/lib/flywheel/outcome-repo.ts
  - src/lib/flywheel/reconciliation-repo.ts
  - src/lib/shelf/shelf-repo.ts
autonomous: false
requirements: [FLYWHEEL-01, FLYWHEEL-04, SAVE-01]
must_haves:
  truths:
    - "The three new migrations are applied to the live Supabase project"
    - "database.types.ts is regenerated and the interim (supabase as any) casts for the new tables are removed"
    - "The full engine suite is green and ENGINE_VERSION is unchanged (no scoring change this phase)"
    - "A confirmed recalibration provably never changes General audience weights"
    - "End-to-end: capture an outcome -> reconcile logs a row; >=N consistent -> propose -> confirm writes the override; save/use a shelf item; Read my account renders or honestly falls back"
  artifacts:
    - path: "src/types/database.types.ts"
      provides: "regenerated types including outcome_signatures, reconciliations, saved_items"
      contains: "outcome_signatures"
  key_links:
    - from: "src/lib/flywheel/outcome-repo.ts"
      to: "src/types/database.types.ts"
      via: "typed table access (cast removed)"
      pattern: "outcome_signatures"
---

<objective>
Land the phase: apply the three migrations to the live Supabase project, regenerate `database.types.ts`, remove the interim `(supabase as any)` casts (the step P7 deferred — STATE 07-02/07-05), then run the BLOCKING engine regression gate (suite green + ENGINE_VERSION unchanged + General-weights-unchanged) and an end-to-end UAT of the flywheel + shelf + Account Read.

Purpose: Build and type checks pass WITHOUT the push (types come from config, not the live DB) — a false-positive verification state (planning context [BLOCKING] note). The push is mandatory; the phase cannot pass verify without it. The regression gate proves the flywheel never silently changed scoring.
Output: pushed schema, regenerated types, casts removed, green regression gate, UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-RESEARCH.md

# Interface sources:
@src/lib/flywheel/outcome-repo.ts
@src/lib/flywheel/reconciliation-repo.ts
@src/lib/shelf/shelf-repo.ts
@src/lib/engine/persona-weights.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Push migrations + regenerate types + remove casts</name>
  <files>src/types/database.types.ts, src/lib/flywheel/outcome-repo.ts, src/lib/flywheel/reconciliation-repo.ts, src/lib/shelf/shelf-repo.ts</files>
  <action>
    Apply the three Plan-02 migrations to the live Supabase project, regenerate types, and remove the interim casts (the step P7 deferred): (1) `supabase db push` (non-TTY: ensure `SUPABASE_ACCESS_TOKEN` set + linked project + `--password`, as the repo already does for P7's push; run manually if interactive prompts cannot be suppressed — this task is non-autonomous for exactly that); (2) `supabase gen types typescript --linked > src/types/database.types.ts` (mirror the P7 regen command); (3) remove the `(supabase as any)` casts + the `// TODO(10-07)` comments in `outcome-repo.ts`/`reconciliation-repo.ts`/`shelf-repo.ts` now the tables are typed; (4) confirm `grep -rn "TODO(10-07)" src/lib/flywheel src/lib/shelf` is empty and `npm run build` passes; (5) confirm the three tables exist live (Supabase MCP `list_tables` or a `select` probe). Do NOT touch the contested `outcomes` table (Pitfall 1). Then pause for human sign-off.
  </action>
  <what-built>
    The three Plan-02 migrations (outcome_signatures, reconciliations, saved_items) applied to the live Supabase project, `database.types.ts` regenerated to include them, and the interim `(supabase as any)` casts removed from the three repos (Plan 02 TODO 10-07). This is the step P7 deferred.
  </what-built>
  <how-to-verify>
    1. Apply the migrations: `supabase db push` (non-TTY: ensure `SUPABASE_ACCESS_TOKEN` set + linked project + `--password`, as the repo already does for P7's push). If interactive prompts cannot be suppressed, run manually — this task is flagged non-autonomous for exactly this.
    2. Regenerate types: `supabase gen types typescript --linked > src/types/database.types.ts` (mirror the P7 regen command the repo uses).
    3. Remove the `(supabase as any)` casts + the `// TODO(10-07)` comments in `outcome-repo.ts`, `reconciliation-repo.ts`, `shelf-repo.ts` — the new tables are now typed.
    4. Confirm: `grep -rn "TODO(10-07)" src/lib/flywheel src/lib/shelf` returns nothing; `npm run build` passes against the regenerated types.
    5. Confirm the three tables exist on the live DB (e.g. via the Supabase MCP `list_tables` or a `select` probe).
  </how-to-verify>
  <read_first>
    - 10-RESEARCH.md §Runtime State Inventory "Build artifacts" (regenerate database.types.ts after push, remove the as-any casts — same step P7 deferred) + §Pitfall 1 (do NOT touch outcomes)
    - STATE.md 07-06 (the live push + types regen precedent: project qyxvxleheckijapurisj)
  </read_first>
  <verify>
    <automated>grep -rn "TODO(10-07)" src/lib/flywheel src/lib/shelf 2>/dev/null | head; npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Three tables live; database.types.ts regenerated to include them; no TODO(10-07) casts remain; build green; human signed off.</done>
  <resume-signal>Type "approved" once the three tables are live, types regenerated, and casts removed (build green).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Engine regression gate (suite green + ENGINE_VERSION unchanged + General unchanged)</name>
  <files>src/lib/engine/persona-weights.ts</files>
  <read_first>
    - src/lib/engine/persona-weights.ts (DEFAULT_PERSONA_WEIGHT_CONFIG; the AUD-03 General-default invariant)
    - 10-RESEARCH.md §Validation Architecture (mandatory regression anchor: a full flywheel run NEVER changes General weights or DEFAULT_PERSONA_WEIGHT_CONFIG) + §Common Pitfalls 5 (no ENGINE_VERSION/ARCHETYPE_DEFINITIONS/DEFAULT change)
    - STATE Hard Constraints (engine OPEN but regression-gate-PROTECTED; ENGINE_VERSION bump only on deliberate video-scoring change — there is NONE this phase)
  </read_first>
  <action>
    Run the FULL engine + flywheel suite green. Assert ENGINE_VERSION is UNCHANGED (this phase makes no video-scoring change — text-path/data/UI only). Confirm via grep that no flywheel/account-read/shelf diff touches `DEFAULT_PERSONA_WEIGHT_CONFIG`, `ARCHETYPE_DEFINITIONS`, or `ENGINE_VERSION`. Confirm the Plan-06 General-unchanged regression anchor test passes (a confirmed recalibration on a user audience leaves General weights byte-identical). If any check fails, STOP and fix before sign-off — this gate is BLOCKING.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -15 && git diff --name-only | xargs grep -l "DEFAULT_PERSONA_WEIGHT_CONFIG\|ARCHETYPE_DEFINITIONS\|ENGINE_VERSION" 2>/dev/null | grep -v test | head</automated>
  </verify>
  <done>Full suite green; ENGINE_VERSION unchanged; no scoring-source file mutated; General-unchanged anchor passes.</done>
  <acceptance_criteria>
    - Engine + flywheel test suite passes.
    - ENGINE_VERSION unchanged; DEFAULT_PERSONA_WEIGHT_CONFIG/ARCHETYPE_DEFINITIONS untouched in non-test diffs.
    - General-weights-unchanged regression anchor (Plan 06 Task 1) green.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end UAT — flywheel + shelf + Account Read</name>
  <files>.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-07-SUMMARY.md</files>
  <action>
    Drive the full Phase 10 surface live against the pushed schema and record the result. Execute the seven verification scenarios below (capture → reconcile log → propose/confirm → drift → shelf save/use → Account Read incl. thin fallback → flat-warm/cream-not-coral visual pass), fixing any defect found before sign-off. This task is human-verify because it asserts visual + interaction contracts (UI-SPEC) that cannot be fully automated. Pause for human approval.
  </action>
  <what-built>
    The full Phase 10 surface, live against the pushed schema: outcome-signature capture, reconciliation logging, confidence-gated propose→confirm recalibration, the drift-folded nudge, the flat Saved shelf (save→use), and the Account Read card (incl. honest thin fallback).
  </what-built>
  <how-to-verify>
    1. **Capture outcome:** paste a posted URL into the Capture-outcome form; confirm public metrics pull, optional private add, provenance tags ("from the post"/"you added"), blank-field-not-counted; success shows "Outcome captured." (no headline delta).
    2. **Reconcile log:** confirm a `reconciliations` row is written (classification calibration/craft) per capture.
    3. **Propose→confirm:** with >=N consistent calibration divergences, the nudge appears ("Your buyers ran warmer…"), shows the honesty footnote, Confirm writes the override (verify the active audience's persona_weights changed, General did NOT), Decline does not re-nag.
    4. **Drift:** trigger `/api/cron/audience-drift` (cron-authed) and confirm it writes a `source='drift_scrape'` row that can surface the SAME nudge (source='drift' copy).
    5. **Shelf:** Save a hook/idea/Read from a thread → appears flat on `/saved` with its type chip; "Use in thread →" launches via CHAIN_HANDOFFS; Remove confirms and does not delete the original.
    6. **Account Read:** run "Read my account" → reading/-rendered card with patterns + accuracy track record; on a thin/private handle → honest "Not enough history to read yet" (warning, never fabricated).
    7. Confirm flat-warm SSOT + cream-not-coral on chips/checks/provenance/track-record throughout.
  </how-to-verify>
  <read_first>
    - 10-UI-SPEC.md (all copy + color/interaction contracts to verify against)
    - memory uat-test-video.md (test video asset if needed)
  </read_first>
  <verify>
    <human-check>All seven UAT scenarios pass; defects fixed; visual contracts hold.</human-check>
  </verify>
  <done>End-to-end UAT signed off across capture → reconcile → propose/confirm → drift → shelf → Account Read; visual contracts verified.</done>
  <resume-signal>Type "approved" or list defects to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → live Supabase | the migration push is a privileged schema change. |
| flywheel → audiences (live) | the recalibration write verified against the live regression gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-19 | Tampering | migration corrupting the contested outcomes table | mitigate | Plan 02 added siblings only; verify push touches only the 3 new tables, never outcomes (Pitfall 1). |
| T-10-20 | Tampering | silent scoring regression | mitigate | BLOCKING engine gate: suite green + ENGINE_VERSION unchanged + General-unchanged anchor (Task 2). |
| T-10-SC | Tampering | npm installs | mitigate | zero new packages (RESEARCH audit); confirm `npm install` adds nothing. |
</threat_model>

<verification>
- Three tables live on Supabase; `database.types.ts` includes them; no `TODO(10-07)` casts remain.
- `npm test` green; ENGINE_VERSION unchanged; no scoring-source mutation.
- UAT sign-off across capture → reconcile → propose/confirm → drift → shelf → Account Read.
</verification>

<success_criteria>
The phase is live and verifiable: schema pushed + typed, the engine regression gate green (no silent scoring change), and an end-to-end UAT proving the flywheel compounds, the shelf connects, and the Account Read reads honestly. Zero new packages.
</success_criteria>

<output>
Create `.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-07-SUMMARY.md` when done.

## Artifacts this phase produces

**New tables:** `outcome_signatures`, `reconciliations`, `saved_items` (3 migrations).
**New routes:** `POST /api/outcomes/signature`, `POST /api/account-read`, `GET|POST /api/saved` (+ `DELETE`), `GET|POST /api/flywheel/proposals`, `GET /api/cron/audience-drift`.
**New cron:** `vercel.json` entry `/api/cron/audience-drift` ("0 5 * * 1").
**New pure modules:** `src/lib/flywheel/{signature,realized-signature,reconcile,confidence-gate,recalibration,propose}.ts`; `src/lib/account-read/account-read.ts`.
**New repos:** `src/lib/flywheel/{outcome-repo,reconciliation-repo}.ts`, `src/lib/shelf/shelf-repo.ts`.
**New scrape method:** `ApifyScrapingProvider.scrapeSinglePostMetrics(url)`.
**New typed block:** `account-read` (schema in `blocks.ts`, registry in `block-registry.ts`, renderer `account-read-block.tsx`, wired in `message-blocks.tsx`).
**New components:** `outcome-signature-form.tsx`, `save-affordance.tsx`, `saved-shelf.tsx`, `saved-item-card.tsx`, `recalibration-nudge.tsx`, `(app)/saved/page.tsx`.
**New hooks:** `use-outcome-signature.ts`, `use-saved-items.ts`, `use-recalibration-proposals.ts`.
**Modified:** `flash-runner.ts` (predicted-vector pin), `sidebar.tsx` (Saved nav), `database.types.ts` (regen).
</output>
