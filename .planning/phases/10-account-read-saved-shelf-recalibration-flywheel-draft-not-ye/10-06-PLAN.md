---
phase: 10-account-read-saved-shelf-recalibration-flywheel
plan: 06
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - src/app/api/flywheel/proposals/route.ts
  - src/lib/flywheel/propose.ts
  - src/components/flywheel/recalibration-nudge.tsx
  - src/hooks/queries/use-recalibration-proposals.ts
  - src/app/api/cron/audience-drift/route.ts
  - vercel.json
  - src/lib/flywheel/__tests__/propose.test.ts
autonomous: true
requirements: [FLYWHEEL-04, FLYWHEEL-05, FLYWHEEL-06]
must_haves:
  truths:
    - "A recalibration is only PROPOSED after the confidence gate passes (>=N consistent posts) — never on a single post, never silently"
    - "The creator must CONFIRM a proposal; confirming writes the analysis_override slot on the active non-general audience only"
    - "Declining logs 'declined' and does not re-nag the same proposal"
    - "Confirming never changes General/preset audiences, DEFAULT weights, archetype defs, or ENGINE_VERSION"
    - "Drift re-scrape feeds the SAME propose path (a drift_scrape outcome_signature row), not a separate mechanism"
    - "The nudge is one component with two triggers (outcome-driven + drift-driven copy)"
  artifacts:
    - path: "src/app/api/flywheel/proposals/route.ts"
      provides: "GET pending proposals; POST confirm/decline"
      exports: ["GET", "POST"]
    - path: "src/lib/flywheel/propose.ts"
      provides: "aggregate reconciliations -> evaluateGate -> proposal; confirm writes override"
      contains: "evaluateGate"
    - path: "src/components/flywheel/recalibration-nudge.tsx"
      provides: "propose->confirm nudge (one component, outcome/drift source)"
    - path: "src/app/api/cron/audience-drift/route.ts"
      provides: "scheduled own-account re-scrape -> drift outcome_signature -> same propose path"
      exports: ["GET"]
  key_links:
    - from: "src/lib/flywheel/propose.ts"
      to: "src/lib/audience/audience-repo.ts"
      via: "updateAudience persona_weights (analysis_override) on confirm"
      pattern: "updateAudience"
    - from: "src/app/api/cron/audience-drift/route.ts"
      to: "src/app/api/flywheel/proposals/route.ts"
      via: "drift_scrape outcome_signature -> same reconcile/gate path"
      pattern: "drift_scrape"
    - from: "src/components/flywheel/recalibration-nudge.tsx"
      to: "src/app/api/flywheel/proposals/route.ts"
      via: "confirm/decline POST"
      pattern: "/api/flywheel/proposals"
---

<objective>
Close the loop: the confidence-gated PROPOSE → creator-confirm → recalibration write (FLYWHEEL-04), the cross-creator reconciliation logging it produces (FLYWHEEL-05), and the drift trigger that folds INTO the same propose path (FLYWHEEL-06, D-01 "folded in, not built separately"). This is the moat moment — predict→post→measure→reconcile→**correct→compound** — with strict human-in-the-loop + regression-gate discipline.

Purpose: The recalibration write is regression-gate-critical (Pitfall 5). It writes ONLY the `analysis_override` slot on a non-general user audience; General/preset/DEFAULT/ENGINE_VERSION are never touched. Drift is the SAME mechanic (composition-shift = another disposition divergence) — one propose path, two sources.
Output: `propose.ts` (gate→proposal→confirm), `/api/flywheel/proposals` (GET/POST), the one nudge component, the drift cron + vercel.json entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-RESEARCH.md
@.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-UI-SPEC.md

# Interface sources:
@src/lib/engine/persona-weights.ts
@src/lib/audience/audience-repo.ts
@src/lib/audience/calibration.ts
@src/app/api/cron/scrape-trending/route.ts
@src/app/api/audiences/calibrate/route.ts
@vercel.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Propose + confirm/decline (gate -> override write, regression-safe)</name>
  <files>src/lib/flywheel/propose.ts, src/app/api/flywheel/proposals/route.ts, src/lib/flywheel/__tests__/propose.test.ts</files>
  <read_first>
    - src/lib/flywheel/{confidence-gate,recalibration}.ts (evaluateGate + buildOverride — Plan 01) + reconciliation-repo.ts (listReconciliations, updateProposalState — Plan 02)
    - src/lib/engine/persona-weights.ts (analysis_override precedence; is_general short-circuit; normalizeWeights)
    - src/lib/audience/audience-repo.ts (updateAudience persona_weights; GENERAL_AUDIENCE.is_general; PRESET_AUDIENCES — these are NEVER recalibrated)
    - 10-RESEARCH.md §2 (confirmed-proposal → bounded delta → updateAudience), §Pitfall 5 (only non-general user audience row; General/DEFAULT/ARCHETYPE_DEFINITIONS/ENGINE_VERSION untouched), §Security Domain
  </read_first>
  <behavior>
    - getPendingProposals(supabase, audienceId): listReconciliations → evaluateGate → if a ProposalSet, return it (with the human-readable disposition + direction); else null. NO proposal for is_general or preset audiences.
    - confirmProposal(supabase, audienceId, proposalId): buildOverride against the audience's CURRENT persona_weights → updateAudience(persona_weights) (the analysis_override slot) → updateProposalState 'confirmed'. Refuse if the audience is_general/preset (throw/skip — regression gate).
    - declineProposal: updateProposalState 'declined' (do not re-nag the same proposal).
    - Pure-ish: the gate/delta math is from Plan 01; this orchestrates I/O.
  </behavior>
  <action>
    Create `propose.ts` with `getPendingProposals`, `confirmProposal`, `declineProposal` wiring Plan 01's `evaluateGate`/`buildOverride` to the repos. HARD GUARD: confirm refuses any `is_general` or preset audience (the only legal target is a non-general user audience's `persona_weights`/analysis_override slot — Pitfall 5). Create `GET /api/flywheel/proposals?audience_id=` (auth-first, returns pending proposal or null) + `POST /api/flywheel/proposals` (`{audience_id, proposal_id, action:'confirm'|'decline'}`, auth-first, user_id from session). Tests: gate-pass → proposal returned; below-gate → null; confirm on a non-general audience writes a normalized override + marks confirmed; **confirm attempt on General is rejected and General weights are unchanged** (regression anchor, mirror P7 persona-weights.test.ts AUD-03); decline marks declined and the same proposal is not re-returned.
  </action>
  <verify>
    <automated>npx vitest run src/lib/flywheel/__tests__/propose.test.ts && grep -q "getUser" src/app/api/flywheel/proposals/route.ts</automated>
  </verify>
  <done>Proposals surface only past the gate; confirm writes a normalized override on a non-general audience and never touches General; decline does not re-nag; tests pass.</done>
  <acceptance_criteria>
    - confirm on General/preset is refused; General weights provably unchanged (regression anchor test).
    - confirm writes ONLY persona_weights (analysis_override) via updateAudience — never DEFAULT/ARCHETYPE_DEFINITIONS/ENGINE_VERSION.
    - decline logs 'declined' and the proposal is not re-surfaced.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Recalibration / Drift nudge — one component, two triggers (FLYWHEEL-04)</name>
  <files>src/components/flywheel/recalibration-nudge.tsx, src/hooks/queries/use-recalibration-proposals.ts</files>
  <read_first>
    - src/app/api/flywheel/proposals/route.ts (GET pending / POST confirm-decline — Task 1)
    - src/hooks/queries/use-bookmarks.ts (react-query hook idiom for the proposals query + confirm/decline mutations)
    - 10-UI-SPEC.md §"Recalibration PROPOSE nudge + Drift nudge" (EXACT copy: outcome headline "Your buyers ran warmer than we modeled." + body; drift headline "Your audience has shifted." + body; Confirm "Recalibrate" accent; Decline "Not now"; honesty footnote "This only adjusts this audience — it never changes your scores or the General audience."; post-confirm success; below-gate = NO nudge) + §Interaction Contracts (one component, `source` discriminates; surfaces only after gate passes; never speculative; decline = dismiss, no re-nag) + §Color (Recalibrate = accent CTA)
  </read_first>
  <action>
    Build `recalibration-nudge.tsx`: a single propose→confirm component taking `source: 'outcome' | 'drift'` that selects the headline/body copy (verbatim from UI-SPEC) and renders the SAME confirm("Recalibrate", accent) / decline("Not now") + the honesty footnote. Renders ONLY when `use-recalibration-proposals` returns a pending proposal (gate passed server-side) — never speculatively. Confirm → POST action 'confirm' → success "Recalibrated. Future Reads use the updated audience."; Decline → POST 'decline' → dismiss, never re-nag the same proposal. Build `use-recalibration-proposals.ts` (query pending + confirm/decline mutations). Flat-warm SSOT; keyboard-operable confirm/decline. NO auto-apply, NO single-post trigger (hard interaction rule D-05).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && npm run lint 2>&1 | grep -iE "recalibration-nudge|recalibration-proposals" | tail -5</automated>
  </verify>
  <done>One nudge component serves both outcome + drift triggers, renders only past the gate, is always propose→confirm with the honesty footnote, and never re-nags a decline; build + lint pass.</done>
  <acceptance_criteria>
    - `source` switches outcome vs drift copy; both share one confirm→override path.
    - Nudge never renders before the gate passes (no speculative proposal).
    - Honesty footnote present; no auto-apply; decline dismisses without re-nag.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Drift cron — scheduled re-scrape folds into the propose path (FLYWHEEL-06)</name>
  <files>src/app/api/cron/audience-drift/route.ts, vercel.json</files>
  <read_first>
    - src/app/api/cron/scrape-trending/route.ts (verifyCronAuth → createServiceClient → per-target actor call; the cron route idiom; webhook-vs-inline tradeoff)
    - src/lib/audience/calibration.ts (deriveAudienceProfile — fresh composition from a re-scrape)
    - src/lib/flywheel/{signature,reconcile}.ts + outcome-repo/reconciliation-repo (Plan 01/02)
    - 10-RESEARCH.md §7 "Drift Trigger" (weekly "0 5 * * 1" [ASSUMED] A4; for each personal audience re-scrape own account → fresh composition → composition-shift vector → write an outcome_signatures row source='drift_scrape' realized=fresh predicted=stored → run the SAME reconcile + gate path; inline .call({waitSecs}) acceptable for v1) + §Security (verifyCronAuth)
    - vercel.json (crons[] array — add one entry)
  </read_first>
  <action>
    Create `GET /api/cron/audience-drift` mirroring `cron/scrape-trending`: `verifyCronAuth(request)` first → `createServiceClient()` → for each PERSONAL audience, re-scrape the own account (scrapeProfile + scrapeVideos) → `deriveAudienceProfile` → build a fresh composition disposition vector → write an `outcome_signatures` row with `source='drift_scrape'` (realized = fresh composition, predicted = stored audience mix) → it flows through the SAME `reconcile` + reconciliation log + gate path as outcome capture (the drift half and outcome half converge — D-01). Inline `.call({waitSecs})` is fine for v1 (cron tolerates latency; respect the vercel function maxDuration). Skip is_general/preset audiences. Add a `vercel.json` crons entry `{ "path": "/api/cron/audience-drift", "schedule": "0 5 * * 1" }` (mark the cadence `[ASSUMED]` A4 in a code comment). Thin re-scrape → no drift row (honest, same gate as P7).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -q "verifyCronAuth" src/app/api/cron/audience-drift/route.ts && grep -q "audience-drift" vercel.json && grep -q "drift_scrape" src/app/api/cron/audience-drift/route.ts</automated>
  </verify>
  <done>The drift cron re-scrapes personal audiences weekly, writes drift_scrape outcome rows, and feeds the SAME reconcile/gate/propose path; cron-authed; vercel.json updated; build passes.</done>
  <acceptance_criteria>
    - Drift writes an outcome_signatures row with source='drift_scrape' that reuses the SAME reconcile+gate path (not a separate mechanism — D-01).
    - verifyCronAuth gates the route; General/preset audiences skipped.
    - vercel.json carries exactly one new crons entry for /api/cron/audience-drift.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/flywheel/proposals | confirm/decline actions on the user's own audience. |
| cron → /api/cron/audience-drift | scheduled invocation — must be cron-authed, not publicly triggerable. |
| flywheel → audiences row | the recalibration write — the regression-gate-critical boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-15 | Tampering | recalibration silently changing scoring | mitigate | confirm refuses is_general/preset; writes only analysis_override on a user audience; regression anchor test (Task 1) asserts General unchanged (Pitfall 5). |
| T-10-16 | Elevation | confirming another user's proposal | mitigate | auth-first getUser(); RLS on reconciliations/audiences; user_id from session. |
| T-10-17 | Spoofing | unauthorized drift cron trigger | mitigate | verifyCronAuth + CRON_SECRET (timing-safe) on the drift route (RESEARCH §Security). |
| T-10-18 | Tampering | single-post / silent recalibration | mitigate | gate requires >=N consistent posts (Plan 01); always PROPOSE→confirm; no auto-apply (D-05). |
| T-10-SC | Tampering | npm installs | mitigate | zero new packages (RESEARCH audit); no install task. |
</threat_model>

<verification>
- `npx vitest run src/lib/flywheel/` green INCLUDING the General-unchanged regression anchor.
- `/api/flywheel/proposals` auth-first; `/api/cron/audience-drift` cron-authed.
- Drift writes `source='drift_scrape'` and reuses the reconcile/gate path; vercel.json has the new cron.
</verification>

<success_criteria>
The full per-creator learning loop is actuated: confidence-gated propose → human-confirm → bounded override on the active audience (never General), with drift folded into the same path and every reconciliation logged for the cross-creator seed. The moat compounds, human-in-the-loop, regression-gate intact. Zero new packages.
</success_criteria>

<output>
Create `.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-06-SUMMARY.md` when done.
</output>
