---
phase: 10-account-read-saved-shelf-recalibration-flywheel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/flywheel/signature.ts
  - src/lib/flywheel/realized-signature.ts
  - src/lib/flywheel/reconcile.ts
  - src/lib/flywheel/confidence-gate.ts
  - src/lib/flywheel/recalibration.ts
  - src/lib/flywheel/__tests__/signature.test.ts
  - src/lib/flywheel/__tests__/reconcile.test.ts
  - src/lib/flywheel/__tests__/confidence-gate.test.ts
  - src/lib/flywheel/__tests__/recalibration.test.ts
autonomous: true
requirements: [FLYWHEEL-02, FLYWHEEL-03, FLYWHEEL-04]
must_haves:
  truths:
    - "Predicted disposition signature is a deterministic, pure function of FlashPersona[]"
    - "Realized signature excludes (never zero-fills) channels with no signal"
    - "Reconciliation classifies each divergence as calibration-error or craft-error"
    - "Confidence gate fires only at >=N_MIN consistent, same-direction divergences on a calibration disposition"
    - "Recalibration produces a clamped, re-normalized PersonaWeights vector that never sums off 1.0"
    - "No flywheel math function ever reads General/DEFAULT weights or mutates archetype definitions"
  artifacts:
    - path: "src/lib/flywheel/signature.ts"
      provides: "predictedSignature(personas) -> Record<Disposition, number>"
      contains: "TEMPERATURE_DISPOSITION"
    - path: "src/lib/flywheel/realized-signature.ts"
      provides: "realizedSignature(metrics) -> Partial<Record<Disposition, number>> + provenance"
    - path: "src/lib/flywheel/reconcile.ts"
      provides: "reconcile() divergence vector + calibration-vs-craft classifier"
    - path: "src/lib/flywheel/confidence-gate.ts"
      provides: "evaluateGate(rows) -> ProposalSet | null with N_MIN/DIV_THRESHOLD/AGREE_THRESHOLD"
    - path: "src/lib/flywheel/recalibration.ts"
      provides: "buildOverride(proposal, currentWeights) -> normalized PersonaWeights"
  key_links:
    - from: "src/lib/flywheel/signature.ts"
      to: "src/lib/audience/temperature-disposition.ts"
      via: "TEMPERATURE_DISPOSITION import"
      pattern: "TEMPERATURE_DISPOSITION"
    - from: "src/lib/flywheel/recalibration.ts"
      to: "src/lib/engine/persona-weights.ts"
      via: "normalizeWeights + PersonaWeights"
      pattern: "normalizeWeights"
---

<objective>
Build the flywheel's deterministic math core as a set of pure functions — the moat's "demonstrably sound, not hand-wavy" spine (CONTEXT D-02..D-05). Predicted-signature derivation, realized-signature derivation, reconciliation + calibration-vs-craft classification, the confidence gate, and the bounded recalibration delta. No model calls, no I/O, no `Date.now`/`Math.random` — every output is a pure function of its inputs, cache-stable, and regression-gate-free by construction.

Purpose: This is where the owner demanded depth. The plumbing (scrape/persist/stream/render) is all reuse; the value that cannot be borrowed is the math. Isolating it as pure functions makes it unit-testable and guarantees determinism (FLYWHEEL-02/03/04).
Output: `src/lib/flywheel/{signature,realized-signature,reconcile,confidence-gate,recalibration}.ts` + unit tests.

> **REQUIREMENTS.md reconciliation note (lead plan):** Phase 10 requirement IDs (SELF-01/02/03, SAVE-01/02, FLYWHEEL-01..06) are coined consistently across CONTEXT/RESEARCH/UI-SPEC but are NOT yet enumerated in `.planning/REQUIREMENTS.md` (same provisional gap as Phase 8 — see STATE Blockers). `mark-complete` will report `not_found` until REQUIREMENTS.md is reconciled before phase verify. This is the established project pattern, not a blocker — reconcile REQUIREMENTS.md ahead of `/gsd-verify-phase 10`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-CONTEXT.md
@.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-RESEARCH.md

# Interface sources — read these for exact contracts the math binds to:
@src/lib/audience/temperature-disposition.ts
@src/lib/audience/audience-types.ts
@src/lib/engine/flash/flash-schema.ts
@src/lib/engine/persona-weights.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Signature derivation — predicted + realized (pure)</name>
  <files>src/lib/flywheel/signature.ts, src/lib/flywheel/realized-signature.ts, src/lib/flywheel/__tests__/signature.test.ts</files>
  <read_first>
    - src/lib/audience/temperature-disposition.ts (TEMPERATURE_DISPOSITION 10-archetype lens; Disposition union = scanner|skeptic|collector|connector|converter|lurker)
    - src/lib/engine/flash/flash-schema.ts (FlashPersona = {archetype: string, verdict: "stop"|"scroll", quote})
    - 10-RESEARCH.md §"Disposition → Engagement-Proxy Mapping Table" + §"Code Examples" (predictedSignature reference impl) + §Pitfall 3
  </read_first>
  <behavior>
    - predictedSignature(personas: FlashPersona[]): Record<Disposition, number> — share of STOP verdicts attributable to each disposition via TEMPERATURE_DISPOSITION lens; normalize to shares; all-zero stays all-zero (no NaN); unknown archetype skipped (never fabricated).
    - realizedSignature(metrics): returns Partial<Record<Disposition, number>> + a parallel provenance map. Each disposition channel mapped per the §Disposition table (collector←saves, connector←shares primary + comments secondary, converter←link_clicks, scanner/lurker←watch_through, skeptic←early-drop inverse). A channel with null value is EXCLUDED (Pitfall 3 — absent ≠ zero). Normalize ACROSS PRESENT channels only.
    - Per-metric provenance preserved: "public_scrape" | "creator_supplied".
  </behavior>
  <action>
    Create `signature.ts` exporting `DISPOSITIONS` const (the 6-element ordered array) and `predictedSignature(personas)`. Mirror the §Code-Examples reference impl: iterate personas, skip non-"stop" verdicts, map `archetype` through `TEMPERATURE_DISPOSITION[archetype].disposition`, count, normalize to shares; guard totalStops===0 → return all-zero record (no division). Skip archetypes absent from the lens (honesty — never fabricate).

    Create `realized-signature.ts` exporting a `RealizedMetrics` type (views + per-channel `{value, source} | null` for saves/shares/comments/watch_through_pct/link_clicks) and `realizedSignature(metrics): { vector: Partial<Record<Disposition,number>>, provenance: Partial<Record<Disposition,"public_scrape"|"creator_supplied">> }`. Convert each PRESENT channel to a per-view rate, drop null channels entirely, then normalize across present channels only. Use the disposition→proxy mapping from RESEARCH §1. Mark the calibration-vs-craft disposition assignment with an `[ASSUMED]` code comment citing A1 (collector/connector/converter = calibration "WHO"; scanner/lurker/skeptic = craft "HOW-WELL") so the owner can confirm.

    Write `signature.test.ts` covering: fixed personas → exact predicted vector; all-scroll → all-zero no-NaN; unknown archetype skipped; realized with a missing channel excludes it (not zero); provenance preserved per channel.
  </action>
  <verify>
    <automated>npx vitest run src/lib/flywheel/__tests__/signature.test.ts</automated>
  </verify>
  <done>predictedSignature + realizedSignature are pure, deterministic, exclude absent channels, and unit tests pass.</done>
  <acceptance_criteria>
    - predictedSignature returns identical output for identical input (no Date/random).
    - realizedSignature never zero-fills a null channel; normalizes across present channels only.
    - Tests assert the all-zero no-NaN edge and absent-channel exclusion.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile + classify (calibration vs craft) — pure</name>
  <files>src/lib/flywheel/reconcile.ts, src/lib/flywheel/__tests__/reconcile.test.ts</files>
  <read_first>
    - 10-RESEARCH.md §3 "Calibration-error vs Craft-error Classifier" (the A1 split + tie-break rule) + §"Code Examples" reconcile snippet
    - 10-SPIKE.md (the OWNER-CONFIRMED A1 calibration-vs-craft split from the 10-02 spike checkpoint — use this confirmed split, keep the `[ASSUMED] A1` marker in sync with it)
    - src/lib/flywheel/signature.ts (DISPOSITIONS const + types from Task 1)
  </read_first>
  <behavior>
    - reconcile(predicted, realized): computes div[d] = realized[d] - predicted[d] ONLY over dispositions present in BOTH; classifies each diverging disposition: collector/connector/converter → "calibration"; scanner/lurker/skeptic → "craft" (per D-03, A1).
    - Returns { divergenceVector, classification: Record<Disposition,"calibration"|"craft"> }.
    - Both-diverge case: a post may carry both calibration AND craft divergences — both logged, not mutually exclusive (RESEARCH §3 tie-break).
    - A craft divergence NEVER contributes to the recalibration path (D-03 protection: content flops never corrupt the audience object).
  </behavior>
  <action>
    Create `reconcile.ts` exporting `CALIBRATION_DISPOSITIONS`/`CRAFT_DISPOSITIONS` consts (mark `[ASSUMED] A1` in a comment — single highest-value owner-confirm) and `reconcile(predicted, realized)`. Compute the divergence only over the intersection of present dispositions. Classify per the locked A1 split. Pure — no I/O. Write `reconcile.test.ts`: a calibration-only divergence fixture routes only to calibration; a craft-only fixture routes only to craft; a both-diverge fixture logs both; a disposition present in predicted but absent in realized is excluded from divergence.
  </action>
  <verify>
    <automated>npx vitest run src/lib/flywheel/__tests__/reconcile.test.ts</automated>
  </verify>
  <done>reconcile classifies calibration vs craft deterministically; craft never reaches the calibration path; tests pass.</done>
  <acceptance_criteria>
    - divergence computed only over dispositions present in both vectors.
    - classification routing matches A1; both-diverge logs both sides.
    - Test asserts craft divergence does NOT appear in the calibration set.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Confidence gate + bounded recalibration delta (pure)</name>
  <files>src/lib/flywheel/confidence-gate.ts, src/lib/flywheel/recalibration.ts, src/lib/flywheel/__tests__/confidence-gate.test.ts, src/lib/flywheel/__tests__/recalibration.test.ts</files>
  <read_first>
    - 10-RESEARCH.md §2 "Divergence + Confidence Math" (N_MIN=5, DIV_THRESHOLD=0.12, AGREE_THRESHOLD=0.70; per-disposition n/mean/agree; the PROPOSE iff rule; ASSUMED_STEP=0.05 bounded nudge + clamp + re-normalize)
    - src/lib/engine/persona-weights.ts (PersonaWeights {fyp,niche,loyalist,cross_niche}; normalizeWeights; the analysis_override slot)
    - src/lib/flywheel/reconcile.ts (CALIBRATION_DISPOSITIONS from Task 2)
  </read_first>
  <behavior>
    - evaluateGate(rows): over the last K reconciliation rows for ONE audience, per calibration disposition compute n[d] (posts with a realized signal), mean[d] (signed mean divergence), agree[d] (directional-consistency fraction). PROPOSE iff n[d] >= N_MIN AND |mean[d]| >= DIV_THRESHOLD AND agree[d] >= AGREE_THRESHOLD AND d is a calibration disposition. Returns ProposalSet | null.
    - buildOverride(proposal, currentWeights): map the diverging disposition to its PersonaWeights slot (collector→fyp-slot, converter→niche-slot, connector→fyp+loyalist — see RESEARCH §2), apply slot_new = clamp(slot_old + ASSUMED_STEP*sign(mean[d]), 0, 1), then normalizeWeights() to re-sum 1.0. Pure; deterministic.
    - Gate constants exported as named consts, tagged [ASSUMED] A2/A3 for owner tuning.
  </behavior>
  <action>
    Create `confidence-gate.ts` exporting `N_MIN`, `DIV_THRESHOLD`, `AGREE_THRESHOLD` (each `[ASSUMED]` A2 comment) and `evaluateGate(rows): ProposalSet | null`. Implement the exact n/mean/agree aggregate from RESEARCH §2; only calibration dispositions can propose. Create `recalibration.ts` exporting `ASSUMED_STEP=0.05` (`[ASSUMED]` A3) and `buildOverride(proposal, currentWeights): PersonaWeights` using the disposition→slot mapping + clamp + `normalizeWeights` from persona-weights.ts. NEVER read `DEFAULT_PERSONA_WEIGHT_CONFIG`/`ARCHETYPE_DEFINITIONS`; the only weights touched are the passed-in current audience weights. Tests: gate holds at n=N_MIN-1, fires at exactly N_MIN with consistent direction, holds when agree<threshold, never fires for a craft disposition; buildOverride output always passes the sum≈1.0 invariant and stays in [0,1].
  </action>
  <verify>
    <automated>npx vitest run src/lib/flywheel/__tests__/confidence-gate.test.ts src/lib/flywheel/__tests__/recalibration.test.ts</automated>
  </verify>
  <done>Gate fires only on >=N_MIN consistent calibration divergences; buildOverride yields a clamped, normalized weight vector; tests pass.</done>
  <acceptance_criteria>
    - evaluateGate returns null below N_MIN and below agree/div thresholds.
    - Craft dispositions can never produce a proposal.
    - buildOverride output sums to 1.0 (±0.01) and every weight in [0,1].
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Pure functions, no I/O, no untrusted input crosses a boundary here. Inputs are typed and validated upstream (Plan 03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | recalibration.ts weight math | mitigate | Output passes through normalizeWeights + clamp; the DB CHECK `audiences_weights_sum_check` is the final guard (Plan 02/05). |
| T-10-02 | Tampering | flywheel never touches General/DEFAULT | mitigate | Functions take current weights as input; General exclusion enforced at the call site (Plan 05) + regression test (Plan 07). |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH §"Package Legitimacy Audit": phase adds ZERO packages. No install task in this plan. |
</threat_model>

<verification>
- `npx vitest run src/lib/flywheel/` green.
- `npm run lint` clean on `src/lib/flywheel/`.
- No import of `DEFAULT_PERSONA_WEIGHT_CONFIG` or `ARCHETYPE_DEFINITIONS` anywhere under `src/lib/flywheel/` (grep -rL or grep -c filtered).
</verification>

<success_criteria>
All five pure modules exist, are deterministic, exclude absent channels, route craft away from calibration, gate on >=N consistent posts, and produce normalized weight deltas — proven by passing unit tests. Zero new packages.
</success_criteria>

<output>
Create `.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-01-SUMMARY.md` when done.
</output>
