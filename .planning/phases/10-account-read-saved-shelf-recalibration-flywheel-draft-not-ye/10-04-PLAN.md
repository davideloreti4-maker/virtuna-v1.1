---
phase: 10-account-read-saved-shelf-recalibration-flywheel
plan: 04
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - src/app/api/saved/route.ts
  - src/hooks/queries/use-saved-items.ts
  - src/app/(app)/saved/page.tsx
  - src/components/saved/saved-shelf.tsx
  - src/components/saved/saved-item-card.tsx
  - src/components/thread/save-affordance.tsx
  - src/components/app/sidebar.tsx
autonomous: true
requirements: [SAVE-01, SAVE-02]
must_haves:
  truths:
    - "Every thread output card exposes a Save affordance that POSTs a typed snapshot to the shelf"
    - "The Saved surface lists typed items flat (no folders, no tags) under a Saved nav item inside AppShell"
    - "A shelf item launches back into a thread via the existing CHAIN_HANDOFFS SSOT"
    - "Saved affordance flips to Saved checkmark in cream-secondary, never coral"
    - "Removing a shelf item requires confirmation and never deletes the original"
  artifacts:
    - path: "src/app/api/saved/route.ts"
      provides: "GET/POST/DELETE typed shelf items"
      exports: ["GET", "POST", "DELETE"]
    - path: "src/app/(app)/saved/page.tsx"
      provides: "flat shelf surface inside (app) route group"
    - path: "src/components/saved/saved-shelf.tsx"
      provides: "typed-item flat list/grid + type filter chips + empty state"
    - path: "src/components/thread/save-affordance.tsx"
      provides: "reusable Save button (Act->State) for thread cards"
  key_links:
    - from: "src/components/thread/save-affordance.tsx"
      to: "src/app/api/saved/route.ts"
      via: "POST {item_type, ref_id, thread_id, snapshot}"
      pattern: "/api/saved"
    - from: "src/components/saved/saved-item-card.tsx"
      to: "src/lib/tools/chain-handoff.ts"
      via: "CHAIN_HANDOFFS launch (Use in thread)"
      pattern: "CHAIN_HANDOFFS|handoffsFor"
---

<objective>
Build the lean Saved shelf (D-07): a flat, typed saved-items surface with its own nav item, every thread output savable-to-shelf, every shelf item actionable-into-thread. Implements the Acts/State IA (Acts in the thread, State on surfaces). Flat ≠ throwaway — built so the future P12 Library extends it rather than reworking it.

Purpose: Connective tissue across skills, riding existing bookmark infra LEAN per the CONTEXT cut line (D-01). The shelf surface + save/use wiring is the SAVE-01/02 surface; depth lives in the flywheel (Plans 01/03/05/06).
Output: `/api/saved` CRUD, `use-saved-items`, the `(app)/saved/` surface, the reusable Save affordance, and the sidebar nav entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-RESEARCH.md
@.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-UI-SPEC.md

# Interface sources:
@src/lib/shelf/shelf-repo.ts
@src/lib/tools/chain-handoff.ts
@src/components/app/sidebar.tsx
@src/app/api/bookmarks/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Saved-items API + query hook (SAVE-01)</name>
  <files>src/app/api/saved/route.ts, src/hooks/queries/use-saved-items.ts</files>
  <read_first>
    - src/lib/shelf/shelf-repo.ts (listSavedItems/createSavedItem/deleteSavedItem — Plan 02)
    - src/app/api/bookmarks/route.ts (the GET/POST/DELETE auth-first route idiom; getUser; createServerClient)
    - src/hooks/queries/use-bookmarks.ts (the react-query hook idiom)
    - 10-RESEARCH.md §5 (saved_items shape, item_type enum) + §Security (auth-first, RLS, user_id from session, zod)
  </read_first>
  <action>
    Create `GET /api/saved` (optional `?type=` flat client filter, NOT a folder), `POST /api/saved` (zod body `{item_type, ref_id?, thread_id?, title?, snapshot}` — `snapshot` is the block props so the shelf renders without a re-fetch), `DELETE /api/saved?id=`. Auth-first `getUser()`; user_id from session never input; delegate to `shelf-repo`. Build `use-saved-items.ts` mirroring `use-bookmarks.ts` (query + create + delete mutations with optimistic update). Keep routes < 500 lines.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -q "getUser" src/app/api/saved/route.ts</automated>
  </verify>
  <done>Typed shelf CRUD is auth-first, session-scoped, and the query hook handles list/create/delete; build passes.</done>
  <acceptance_criteria>
    - user_id never accepted from request body.
    - snapshot persisted so the shelf renders without re-fetch.
    - `?type=` is a flat filter, not a folder concept.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Save affordance on thread cards + Use-in-thread launch (SAVE-02)</name>
  <files>src/components/thread/save-affordance.tsx, src/components/saved/saved-item-card.tsx</files>
  <read_first>
    - src/lib/tools/chain-handoff.ts (CHAIN_HANDOFFS SSOT + handoffsFor; per-type CTA endpoints — saved hook→Test full, saved idea→Develop)
    - src/components/thread/hook-card-block.tsx + idea-card-block.tsx (existing card structure the Save affordance mounts into; the "Test full →"/"Develop →" CTA wiring precedent)
    - 10-UI-SPEC.md §Interaction Contracts (Save Act→State; Use State→Act; affordance flips to "Saved ✓" cream-secondary) + §Copywriting (Save / Use in thread → / per-type labels) + §Color (Save check never coral)
  </read_first>
  <action>
    Build `save-affordance.tsx`: a reusable `<SaveAffordance item_type ref_id thread_id snapshot />` button ("Save" + icon → "Saved ✓" cream-secondary check on success via use-saved-items). Mount it on the thread output card components (hook/idea/script/remix/outlier and — added in Plan 05 — account-read). Build `saved-item-card.tsx`: renders a saved item's type chip (neutral charcoal-chip, NOT coral) + title + muted timestamp + a primary accent "Use in thread →" that resolves the per-type CHAIN_HANDOFFS endpoint (hook→"Test full →", idea→"Develop →") + an overflow "Remove" (destructive, confirm dialog "Remove from shelf?" — never deletes the original). Use flat-warm tokens throughout.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && npm run lint 2>&1 | grep -iE "save-affordance|saved-item" | tail -5</automated>
  </verify>
  <done>Thread cards can save a typed snapshot; saved cards launch back via CHAIN_HANDOFFS; Save check is cream not coral; remove confirms; build + lint pass.</done>
  <acceptance_criteria>
    - "Use in thread →" resolves the per-type endpoint from CHAIN_HANDOFFS (no one-off wiring).
    - Item-type chip + Saved check use neutral/cream, never coral (UI-SPEC §Color).
    - Remove shows the "Remove from shelf?" confirmation and never deletes the source.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Saved surface + sidebar nav item (SAVE-01)</name>
  <files>src/app/(app)/saved/page.tsx, src/components/saved/saved-shelf.tsx, src/components/app/sidebar.tsx</files>
  <read_first>
    - src/components/app/sidebar.tsx (topNavItems array — add "Saved"; SidebarNavItem usage; icon import from @phosphor-icons/react)
    - 10-RESEARCH.md §5 "Surface placement" (MUST be inside (app) route group for AppShell/auth — STATE 07-05; in-shell page renders a plain content div, AppShell owns <main>, do NOT nest a second <main>)
    - 10-UI-SPEC.md §"Saved shelf" copy (heading "Saved", subtitle, empty state "Nothing saved yet" + body) + §Spacing (page wrapper max-w mx-auto px-4 py-6 sm:p-6; shelf grid gap --spacing-6) + §Interaction Contracts (flat list/grid, optional flat type-filter chip row — NOT folders) + §Color (active nav indicator accent)
  </read_first>
  <action>
    Create `(app)/saved/page.tsx` inside the route group (inherits AppShell/auth/sidebar) rendering a plain content `<div className="max-w-* mx-auto px-4 py-6 sm:p-6">` (NO nested `<main>`, STATE 07-05). Build `saved-shelf.tsx`: H1 "Saved" + subtitle, an optional flat type-filter chip row (client-side filter, NOT folders), a flat grid of `saved-item-card`s (gap --spacing-6, card radius --radius-lg), and the empty state ("Nothing saved yet" + body copy). Add a "Saved" entry to `sidebar.tsx` `topNavItems` (`href: "/saved"`, a lucide/phosphor bookmark-style icon) with the accent active-indicator. Flat-warm SSOT throughout; the flat `saved_items` shape must not bake throwaway assumptions (P12 extends, never reworks — D-07).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -q "/saved" src/components/app/sidebar.tsx</automated>
  </verify>
  <done>The Saved surface renders flat typed items inside AppShell with a sidebar nav entry + empty state; no folders/tags; build passes.</done>
  <acceptance_criteria>
    - Page lives under (app)/ route group; no nested <main>.
    - No folder/tag/collection structure — only a flat optional type-filter chip row.
    - Sidebar "Saved" nav item present with accent active indicator.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/saved | item_type/ref/snapshot originate client-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Spoofing / Elevation | saved_items ownership | mitigate | RLS own-rows-only; user_id from session; auth-first getUser() on /api/saved. |
| T-10-10 | Tampering | item_type injection | mitigate | DB CHECK constraint on item_type (Plan 02) + zod enum validation on POST. |
| T-10-11 | Information disclosure | snapshot leaking another user's data | accept | snapshot is the user's own block props, scoped by RLS; low risk, no cross-user refs. |
| T-10-SC | Tampering | npm installs | mitigate | zero new packages (RESEARCH audit); no install task. |
</threat_model>

<verification>
- `npm run build` + `npm run lint` clean.
- Sidebar renders a "Saved" item routing to `/saved`; the surface lists items flat with no folder UI.
- Save affordance POSTs a typed snapshot; "Use in thread →" resolves a CHAIN_HANDOFFS endpoint.
</verification>

<success_criteria>
A flat, typed, P12-extendable Saved shelf with full thread↔shelf wiring (save any output, launch any item) — the Acts/State connective tissue, built lean on existing infra. Zero new packages.
</success_criteria>

<output>
Create `.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-04-SUMMARY.md` when done.
</output>
