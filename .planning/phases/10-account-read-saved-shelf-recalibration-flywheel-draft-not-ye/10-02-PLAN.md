---
phase: 10-account-read-saved-shelf-recalibration-flywheel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260619100000_outcome_signatures.sql
  - supabase/migrations/20260619100100_reconciliations.sql
  - supabase/migrations/20260619100200_saved_items.sql
  - src/lib/scraping/apify-provider.ts
  - src/lib/scraping/types.ts
  - src/lib/flywheel/outcome-repo.ts
  - src/lib/flywheel/reconciliation-repo.ts
  - src/lib/shelf/shelf-repo.ts
  - .planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-SPIKE.md
autonomous: false
requirements: [FLYWHEEL-01, FLYWHEEL-05, SAVE-01]
must_haves:
  truths:
    - "Three new sibling tables (outcome_signatures, reconciliations, saved_items) exist with RLS own-rows-only"
    - "The contested outcomes table is NOT touched; the dead real_* migration is left alone"
    - "A single posted URL can be scraped for public metrics (views/likes/comments/shares + saves if present) via the clockworks single-URL path"
    - "Wave-0 spike result documents whether clockworks single-URL returns full metrics + whether saves are public"
    - "The A1 calibration-vs-craft disposition split is owner-confirmed at the spike checkpoint before Plan 01 locks it (D-03 guard)"
    - "saved_items is flat (no folder_id, no tags) and typed (read/idea/hook/script/outlier/format)"
  artifacts:
    - path: "supabase/migrations/20260619100000_outcome_signatures.sql"
      provides: "outcome_signatures table (predicted+realized vectors, provenance, raw_metrics, source)"
      contains: "outcome_signatures"
    - path: "supabase/migrations/20260619100100_reconciliations.sql"
      provides: "reconciliations cross-creator seed table (niche/goal_intent/follower_tier/divergence/classification/proposal_state)"
      contains: "reconciliations"
    - path: "supabase/migrations/20260619100200_saved_items.sql"
      provides: "saved_items flat typed shelf table"
      contains: "saved_items"
    - path: "src/lib/scraping/apify-provider.ts"
      provides: "scrapeSinglePostMetrics(url) returning VideoData public metrics"
      contains: "scrapeSinglePostMetrics"
    - path: "src/lib/shelf/shelf-repo.ts"
      provides: "typed saved_items CRUD"
  key_links:
    - from: "src/lib/scraping/apify-provider.ts"
      to: "clockworks/tiktok-scraper VIDEO_ACTOR"
      via: "postURLs single-URL path"
      pattern: "postURLs"
    - from: "supabase/migrations/20260619100000_outcome_signatures.sql"
      to: "public.audiences(id)"
      via: "audience_id FK (Pitfall 6 pin)"
      pattern: "audiences"
---

<objective>
Lay the data + scrape rails the flywheel and shelf run on: three NEW sibling Supabase tables, the Apify single-posted-URL metric path, and the typed-CRUD repos. Plus a Wave-0 spike to de-risk the two MEDIUM-confidence Apify open questions BEFORE anything depends on auto-captured private signals.

Purpose: P10 is a data-modeling phase. The landmine (RESEARCH §Stored data, Pitfall 1) is the dual `outcomes` table — this plan adds siblings and never touches the contested definition. Schema lands here so Plans 03/04/05 wire against real tables.
Output: 3 migrations, `scrapeSinglePostMetrics`, `outcome-repo.ts`/`reconciliation-repo.ts`/`shelf-repo.ts`, and `10-SPIKE.md`.

> **DB PUSH IS DEFERRED to Plan 07 (BLOCKING gate).** This plan WRITES the migration files; it does NOT run `supabase db push`. Build/type-check pass off the committed migrations + interim `(supabase as any)` casts (same pattern P7 used — STATE 07-02). Plan 07 runs the push + regen + cast removal. Do NOT push here.
> **autonomous: false** — Task 1 is a blocking-human Apify spike checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-RESEARCH.md

# Interface + landmine sources:
@src/lib/scraping/apify-provider.ts
@src/lib/scraping/types.ts
@supabase/migrations/20260619000000_audiences.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Wave-0 Apify single-URL metric spike (Open Q1/Q2)</name>
  <files>.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-SPIKE.md</files>
  <action>
    Run a throwaway one-shot probe (NOT app code): with `APIFY_TOKEN` set, call the clockworks `tiktok-scraper` actor `{ postURLs: [<a real public TikTok video URL>], resultsPerPage: 1 }` via `npx tsx` using the existing `apify-client`. Inspect the returned dataset item for `playCount`/`diggCount`/`shareCount`/`commentCount`/`collectCount` and record the findings in `10-SPIKE.md`: (a) which metrics are present + non-null on single-URL clockworks (Open Q1), (b) whether `collectCount`/saves is public (Open Q2), (c) the chosen Task-2 path — extend the `resolveVideoUrl`-style clockworks call to return `VideoData`, OR fall back to creator-supplied-only for absent channels (D-04 allows).

    SECOND, fold the A1 owner-confirmation into this SAME human gate (RESEARCH flags A1 as the HIGHEST-VALUE `[ASSUMED]`): present the calibration-vs-craft disposition split for explicit OWNER CONFIRMATION — **calibration ("WHO showed up", feeds the Audience override) = {collector, connector, converter}; craft ("HOW WELL it delivered", feeds Account Read only, NEVER the model) = {scanner, lurker, skeptic}**. Why a human gate and not just a comment: Plan 01's tests assert the split AS-WRITTEN, so a wrong A1 passes every test silently and routes craft-flops into the audience override — the exact D-03 failure. Record the confirmed (or corrected) split in `10-SPIKE.md`; Plan 01 keeps the `[ASSUMED] A1` code marker in sync with what the owner confirms here.

    Then pause for human sign-off before Task 2 implements `scrapeSinglePostMetrics` and before Plan 01 locks the A1 split.
  </action>
  <what-built>
    A throwaway one-shot probe: run the clockworks `tiktok-scraper` actor with `{ postURLs: [<a real public TikTok video URL>], resultsPerPage: 1 }` and inspect the returned item for the public metric fields (`playCount`/`diggCount`/`shareCount`/`commentCount`/`collectCount`) — answering RESEARCH Open Q1 (does single-URL return full metrics?) and Q2 (are saves/`collectCount` public?). Record findings in `10-SPIKE.md`.
  </what-built>
  <how-to-verify>
    1. With `APIFY_TOKEN` set, run the probe (a small script or `npx tsx` snippet using the existing `apify-client`; do NOT add it to the app). Use one known-public TikTok post URL.
    2. Inspect the dataset item. Confirm which of `playCount/diggCount/shareCount/commentCount/collectCount` are present and non-null.
    3. Write `10-SPIKE.md` recording: (a) metrics present on single-URL clockworks, (b) whether `collectCount`/saves is public, (c) the chosen path — extend `resolveVideoUrl`-style clockworks call to return `VideoData`, OR fall back to creator-supplied-only for absent channels (D-04 already allows this).
    4. Report findings here for sign-off before Task 2 implements `scrapeSinglePostMetrics` against the confirmed shape.
  </how-to-verify>
  <read_first>
    - 10-RESEARCH.md §Pitfall 2 (single-URL = clockworks, NOT apidojo), §Open Questions 1/2, §Assumptions A5/A7
    - src/lib/scraping/apify-provider.ts (resolveVideoUrl clockworks call shape, lines ~181-187)
  </read_first>
  <verify>
    <automated>test -f .planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-SPIKE.md</automated>
  </verify>
  <done>10-SPIKE.md records the single-URL metric availability + saves availability + the chosen Task-2 path AND the owner-confirmed A1 calibration-vs-craft split; human signed off.</done>
  <resume-signal>Type "approved" with (1) the confirmed single-URL metric shape (or the creator-supplied-only fallback) AND (2) confirmation of the A1 calibration={collector,connector,converter} / craft={scanner,lurker,skeptic} split, or the corrected split.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Three sibling migrations + scrapeSinglePostMetrics</name>
  <files>supabase/migrations/20260619100000_outcome_signatures.sql, supabase/migrations/20260619100100_reconciliations.sql, supabase/migrations/20260619100200_saved_items.sql, src/lib/scraping/apify-provider.ts, src/lib/scraping/types.ts</files>
  <read_first>
    - 10-RESEARCH.md §4 "DB Schema" (exact CREATE TABLE for outcome_signatures + reconciliations — copy the column shapes), §5 "Saved-shelf Persistence" (saved_items CREATE TABLE), §Pitfall 1 (do NOT touch outcomes; analysis_id is TEXT not uuid), §Pitfall 2/3 (clockworks single-URL; absent channel ≠ zero)
    - supabase/migrations/20260619000000_audiences.sql (RLS idiom: ENABLE ROW LEVEL SECURITY + FOR ALL TO authenticated USING(auth.uid()=user_id) WITH CHECK; the weights sum CHECK constraint)
    - src/lib/scraping/apify-provider.ts (VIDEO_ACTOR clockworks const; remapApidojoVideo for the metric field names; resolveVideoUrl call shape)
    - 10-SPIKE.md (Task 1 confirmed metric shape)
  </read_first>
  <action>
    Write the three migrations using the EXACT shapes in RESEARCH §4 and §5. `outcome_signatures`: `analysis_id text` (NOT uuid — STATE FK note), `audience_id uuid REFERENCES audiences ON DELETE SET NULL` (Pitfall 6 pin), `predicted_vector jsonb NOT NULL`, `realized_vector jsonb`, `realized_provenance jsonb`, `raw_metrics jsonb`, `source text NOT NULL DEFAULT 'paste_url'` (allow 'drift_scrape'), `user_id` FK + RLS own-rows-only + the two indexes. `reconciliations`: carry ALL cross-creator-seed fields (niche, goal_intent, follower_tier bucket, predicted/realized/divergence vectors, classification jsonb, proposal_state text DEFAULT 'logged', proposed_delta jsonb, confirmed_at) + RLS + indexes (FLYWHEEL-05 rails — do NOT build the fitting job). `saved_items`: flat + typed CHECK (item_type IN read/idea/hook/script/outlier/format), ref_id text, thread_id FK ON DELETE SET NULL, title, snapshot jsonb NOT NULL, RLS, `(user_id, item_type)` index. NO folder_id, NO tags (D-07 flat guard).

    Do NOT write any `ALTER TABLE outcomes` and do NOT reference `real_*` columns (Pitfall 1 — leave the dead 20260526 migration alone).

    In `apify-provider.ts` add `scrapeSinglePostMetrics(url: string): Promise<VideoData | null>` that runs the clockworks `VIDEO_ACTOR` single-URL call (mirror `resolveVideoUrl` but `shouldDownloadVideos:false`) and remaps the item to `VideoData` (reuse the field mapping from `remapApidojoVideo`/the clockworks item per the spike). Keep the existing SSRF/HTTPS allowlist guard `resolveVideoUrl` uses. If the spike found saves absent on single-URL, leave `saves` as `0`/null-able per VideoData and document that collector is then creator-supplied-dependent. Extend `types.ts` only if a new field is needed.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -L "ALTER TABLE outcomes\|real_save_pct\|real_views" supabase/migrations/20260619100000_outcome_signatures.sql supabase/migrations/20260619100100_reconciliations.sql supabase/migrations/20260619100200_saved_items.sql</automated>
  </verify>
  <done>Three migration files exist with RLS; none touch the outcomes table; scrapeSinglePostMetrics compiles against the confirmed clockworks shape; build passes.</done>
  <acceptance_criteria>
    - No migration contains `ALTER TABLE outcomes` or `real_*` columns.
    - All three tables enable RLS with `auth.uid()=user_id`.
    - saved_items has no folder_id/tags column; item_type CHECK present.
    - scrapeSinglePostMetrics uses VIDEO_ACTOR (clockworks), not a DISCOVER_* actor.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Typed CRUD repos (outcome, reconciliation, shelf)</name>
  <files>src/lib/flywheel/outcome-repo.ts, src/lib/flywheel/reconciliation-repo.ts, src/lib/shelf/shelf-repo.ts</files>
  <read_first>
    - src/lib/audience/audience-repo.ts (the repo idiom: SupabaseClient param, row↔domain mappers, user_id-from-session never from input, zod-validated insert shapes)
    - 10-RESEARCH.md §4/§5 (table columns the repos read/write) + §Security Domain (auth-first, RLS, user_id from session)
    - src/lib/flywheel/signature.ts + reconcile.ts (the vector/classification types these rows store — from Plan 01)
  </read_first>
  <action>
    Mirror `audience-repo.ts` exactly. `outcome-repo.ts`: `insertOutcomeSignature(supabase, row)`, `listOutcomeSignatures(supabase, audienceId)` (for the gate's K rows). `reconciliation-repo.ts`: `insertReconciliation(supabase, row)`, `listReconciliations(supabase, audienceId)` (gate input), `updateProposalState(supabase, id, state, confirmedAt?)`. `shelf-repo.ts`: `listSavedItems(supabase, type?)`, `createSavedItem(supabase, item)`, `deleteSavedItem(supabase, id)`. All take `SupabaseClient`, never set `user_id` from input (RLS + session-derived), validate with zod, keep files < 500 lines. Use `(supabase as any)` casts for the new tables until Plan 07 regenerates `database.types.ts` (STATE 07-02 interim pattern) — add a `// TODO(10-07): remove cast after types regen` comment on each.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npm run lint 2>&1 | grep -E "flywheel|shelf" | tail -10</automated>
  </verify>
  <done>Three repos compile, follow the audience-repo idiom, derive user_id from session, and carry the cast-removal TODO for Plan 07.</done>
  <acceptance_criteria>
    - No repo accepts user_id from caller input.
    - Each new-table query carries the `(supabase as any)` interim cast + the 10-07 TODO.
    - listReconciliations/listOutcomeSignatures scope by audience_id for the gate.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API/DB | user_id and saved-item refs originate client-side; RLS + session-derived user_id is the guard. |
| app → Apify | posted URL is untrusted input → SSRF/HTTPS allowlist guard on scrapeSinglePostMetrics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Spoofing | new tables row ownership | mitigate | RLS `auth.uid()=user_id` on all three tables; user_id from session in repos (mirror audience-repo CR-01). |
| T-10-04 | Information disclosure | reconciliations cross-creator seed | mitigate | follower_tier stored as bucket NOT raw count (RESEARCH §8 privacy-safe); RLS own-rows-only. |
| T-10-05 | Denial of service / SSRF | scrapeSinglePostMetrics URL | mitigate | Route the paste-URL through the existing resolveVideoUrl HTTPS allowlist guard (RESEARCH §Security). |
| T-10-SC | Tampering | npm installs | mitigate | RESEARCH Package Legitimacy Audit: zero new packages; no install task. |
</threat_model>

<verification>
- `npm run build` passes (types resolve off committed migrations + interim casts).
- Grep confirms no `ALTER TABLE outcomes` / `real_*` in any migration.
- `10-SPIKE.md` exists with the Open Q1/Q2 findings.
</verification>

<success_criteria>
Three flat/typed sibling tables written (never touching the contested outcomes table), the single-URL metric path lands on the spike-confirmed shape, and three session-safe repos compile — the rails Plans 03-05 wire against. Zero new packages.
</success_criteria>

<output>
Create `.planning/phases/10-account-read-saved-shelf-recalibration-flywheel-draft-not-ye/10-02-SUMMARY.md` when done.
</output>
