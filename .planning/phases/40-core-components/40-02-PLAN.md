---
phase: 40-core-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/card.tsx
  - src/components/ui/index.ts
autonomous: true

must_haves:
  truths:
    - "Card renders with dark surface background and border"
    - "GlassCard renders with glassmorphism blur effect"
    - "GlassCard has visible glass border glow"
    - "CardHeader, CardContent, CardFooter work as compound components"
  artifacts:
    - path: "src/components/ui/card.tsx"
      provides: "Card and GlassCard components"
      exports: ["Card", "GlassCard", "CardHeader", "CardContent", "CardFooter"]
      contains: "backdrop-filter"
  key_links:
    - from: "src/components/ui/card.tsx"
      to: "src/app/globals.css"
      via: "semantic tokens and glass utilities"
      pattern: "bg-surface|glass-blur|border-glass"
---

<objective>
Enhance Card component and add GlassCard variant with Raycast-style glassmorphism

Purpose: Create versatile card components matching Raycast styling - basic Card for standard containers, GlassCard for premium glass effects
Output: Enhanced card.tsx with Card (basic) and GlassCard (glassmorphism) variants, plus compound sub-components
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-core-components/40-CONTEXT.md
@.planning/phases/40-core-components/40-RESEARCH.md
@src/components/ui/card.tsx
@src/components/primitives/GlassCard.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Card and add GlassCard component</name>
  <files>src/components/ui/card.tsx</files>
  <action>
Rewrite card.tsx to include both Card and GlassCard components:

**Card (basic variant):**
- Keep existing pattern with forwardRef
- Styling: `bg-surface border border-border rounded-lg`
- Default padding via CardContent/CardHeader/CardFooter

**GlassCard (glassmorphism variant):**
- New component using Raycast glass values from extraction
- Glass background: `rgba(255, 255, 255, 0.05)` or use glass-base utility
- Blur: Use `glass-blur-md` (12px desktop, 8px mobile) from globals.css
- Border: `border border-border-glass` (rgba(255,255,255,0.06))
- Inner glow: `box-shadow: rgba(255, 255, 255, 0.15) 0px 1px 1px 0px inset`
- Rounded corners: `rounded-xl` (12px)

**GlassCard props:**
```typescript
interface GlassCardProps extends React.HTMLAttributes<HTMLDivElement> {
  /** Blur intensity */
  blur?: "sm" | "md" | "lg";
  /** Show inner glow effect */
  glow?: boolean;
}
```

**Compound components (keep existing):**
- CardHeader: `p-6` padding
- CardContent: `p-6 pt-0` padding
- CardFooter: `flex items-center p-6 pt-0`

**Implementation approach:**
- Card is simple div with surface background
- GlassCard uses inline styles for glassmorphism (backdrop-filter) with Safari prefix
- Both work with CardHeader/CardContent/CardFooter sub-components

**TypeScript:**
- Export CardProps, GlassCardProps interfaces
- All components use forwardRef pattern

**JSDoc:**
- Add examples for Card with CardHeader/CardContent
- Add examples for GlassCard with blur variants
  </action>
  <verify>
Run TypeScript check: `npx tsc --noEmit src/components/ui/card.tsx`
Verify GlassCard export: `grep "GlassCard" src/components/ui/card.tsx`
Verify glassmorphism: `grep -E "backdrop-filter|blur" src/components/ui/card.tsx`
  </verify>
  <done>
Card and GlassCard components exist with proper styling, GlassCard has glassmorphism with blur and inner glow, both work with compound sub-components
  </done>
</task>

<task type="auto">
  <name>Task 2: Update UI index exports for Card</name>
  <files>src/components/ui/index.ts</files>
  <action>
Update index.ts to export GlassCard and card types:

```typescript
export {
  Card,
  GlassCard,
  CardHeader,
  CardContent,
  CardFooter
} from "./card";
export type { CardProps, GlassCardProps } from "./card";
```
  </action>
  <verify>
Run: `grep "GlassCard" src/components/ui/index.ts` - should show export
  </verify>
  <done>
GlassCard and all card types properly exported from index.ts
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. GlassCard exists: `grep "export.*GlassCard" src/components/ui/card.tsx`
3. Glassmorphism applied: `grep "backdrop-filter" src/components/ui/card.tsx`
4. Safari prefix: `grep "WebkitBackdropFilter" src/components/ui/card.tsx`
5. Compound components work: All CardHeader/CardContent/CardFooter still exported
</verification>

<success_criteria>
- Card renders with dark surface background
- GlassCard renders with blur effect visible against background
- GlassCard has inner glow and glass border
- CardHeader/CardContent/CardFooter work with both Card and GlassCard
- Full TypeScript types exported
- JSDoc examples present
</success_criteria>

<output>
After completion, create `.planning/phases/40-core-components/40-02-SUMMARY.md`
</output>
