---
phase: 06-script-remix-tools
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/lib/kc/assembler.ts
  - src/lib/tools/blocks.ts
  - src/lib/tools/block-registry.ts
  - src/components/thread/remix-card-block.tsx
  - src/components/thread/message-blocks.tsx
  - src/lib/tools/__tests__/blocks-remix.test.ts
  - .planning/phases/06-script-remix-tools/06-SCOUT.md
autonomous: true
requirements: [REMIX-01]
must_haves:
  truths:
    - "assembleBundle({mode:'remix'}) returns a bundle without throwing (mode accepted)"
    - "A remix-card block validates via RemixCardBlockSchema and rehydrates (not UnsupportedBlock)"
    - "The reuse scout confirms decode.ts is read-only over the protected SIM-1 Max path (D-05a)"
  artifacts:
    - path: "src/lib/tools/blocks.ts"
      provides: "RemixCardBlockSchema + RemixCardBlock type + union member"
      contains: "RemixCardBlockSchema"
    - path: "src/lib/tools/block-registry.ts"
      provides: "'remix-card' registry entry"
      contains: "remix-card"
    - path: "src/components/thread/remix-card-block.tsx"
      provides: "RemixCardRenderer (fixed typed renderer, THREAD-04)"
      min_lines: 40
    - path: ".planning/phases/06-script-remix-tools/06-SCOUT.md"
      provides: "REMIX-01 mandated reuse scout findings (revive, don't rebuild)"
      min_lines: 20
    - path: "src/lib/kc/assembler.ts"
      provides: "remix mode in modeSchema + MODE_ROLES.remix"
      contains: "remix"
  key_links:
    - from: "src/components/thread/message-blocks.tsx"
      to: "src/components/thread/remix-card-block.tsx"
      via: "BLOCK_COMPONENTS['remix-card'] = RemixCardRenderer"
      pattern: "remix-card.*RemixCardRenderer"
    - from: "src/lib/tools/block-registry.ts"
      to: "src/lib/tools/blocks.ts"
      via: "RemixCardBlockSchema import + registry entry"
      pattern: "RemixCardBlockSchema"
---

<objective>
Lay the Remix skill's foundation AND execute the REQUIREMENTS-mandated reuse scout (REMIX-01: "revive, don't rebuild"). Produces the `remix` assembler mode, the `remix-card` typed block (schema + registry + renderer), and a written scout confirming the decode/adapt reuse seams + the D-05a verdict against live code.

Purpose: REMIX-01 explicitly mandates a reuse scout before any remix build. The scout output (06-SCOUT.md) is the gate that lets 06-04 wrap the live engine functions instead of rebuilding. Block must be registered before any persist (Pitfall 1); mode must exist before assembleBundle is called (Pitfall 2). Wave 2 — runs AFTER 06-01 lands (depends_on: ["06-01"]); the two plans share assembler.ts/blocks.ts/block-registry.ts/message-blocks.tsx, so this plan's edits are additive (append the `remix` enum member + remix-card schema/registry/component entries beside the script ones 06-01 added — never overwrite).

Output: `RemixCardBlockSchema`/`RemixCardBlock`, `"remix-card"` registry + component entries, `RemixCardRenderer`, `remix` in `modeSchema` + `MODE_ROLES.remix`, `06-SCOUT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-script-remix-tools/06-CONTEXT.md
@.planning/phases/06-script-remix-tools/06-RESEARCH.md

# Scout targets + patterns to mirror (read before editing):
@src/lib/engine/remix/decode.ts
@src/lib/engine/remix/decode-types.ts
@src/lib/engine/remix/adapt.ts
@src/lib/engine/remix/resolve-and-rehost.ts
@src/lib/tools/blocks.ts
@src/lib/tools/block-registry.ts
@src/components/thread/message-blocks.tsx
@src/components/thread/hook-card-block.tsx
@src/lib/kc/assembler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reuse scout (REMIX-01) — confirm decode/adapt seams + D-05a verdict</name>
  <files>.planning/phases/06-script-remix-tools/06-SCOUT.md</files>
  <read_first>
    - src/lib/engine/remix/resolve-and-rehost.ts (resolveAndRehost(tiktokUrl, requestId) → {signedUrl, cleanup}; cleanup MUST run in finally — T-03-02)
    - src/lib/engine/remix/decode.ts (runDecode(omni) → DecodeResult|null; omniOutputToStructuralInput(omni)→OmniStructuralInput|null; runs on analyzeVideoWithOmni, NEVER runPredictionPipeline — D-05a header proof)
    - src/lib/engine/remix/decode-types.ts (DecodeResult shape; decodeResultToAdaptInput(decode, niche)→AdaptInput; AdaptConcept shape lines 184-193; luck NEVER mapped in — D-01 guard)
    - src/lib/engine/remix/adapt.ts (generateAdaptConcepts(input)→AdaptConcept[]|null; 3 concepts; Qwen-only; returns null on graceful failure)
    - src/app/api/remix/adapt/route.ts (existing auth/ownership/CSRF/maxDuration=300 controls to preserve in the new route)
  </read_first>
  <action>
    Write `.planning/phases/06-script-remix-tools/06-SCOUT.md` recording the reuse scout (REMIX-01 mandate, D-06). The 06-RESEARCH.md already traced the call graph at HIGH confidence — the scout VERIFIES against live code and records the confirmed seams, it does not re-derive from scratch. Document:
    1. **D-05a verdict (re-confirm GREEN):** trace `resolveAndRehost → analyzeVideoWithOmni → omniOutputToStructuralInput → runDecode` and assert NONE call `runPredictionPipeline`/`aggregateScores`/`usage_tracking` and decode keeps `overall_score: null` + never touches `ENGINE_VERSION`. Cite the decode.ts header + the analyze/route.ts runDecodeStream comment. If ANY coupling to the protected path is found, FLAG LOUDLY and stop (D-05a "flag loudly if decode can't run without the protected pipeline").
    2. **Reuse surface:** exact signatures of resolveAndRehost / analyzeVideoWithOmni / omniOutputToStructuralInput / runDecode / decodeResultToAdaptInput / generateAdaptConcepts — what 06-04 imports as-is.
    3. **Revive-don't-rebuild boundary:** confirm `components/board/adapt/*` (AdaptConceptCard, RemixedFromChip) is DEAD pre-numen-rework UI — reference for concept shape only, NOT imported (D-06). New thread `remix-card` is built fresh.
    4. **Route decision (D-06 / A1):** new `/api/tools/remix/run` SSE route vs reusing `/api/remix/adapt`. Record the recommendation (new SSE route — existing route is non-SSE + persists a Reading row, wrong shape for the open thread) and the controls to copy (auth-first 401, CSRF/Content-Type guards, `maxDuration=300`, `cleanup()` in finally).
    5. **Cardinality decision (A3, Area 6 discretion):** 3 adapt concepts → 1 chosen remix-card into Hooks (studio one-card feel) vs ranked set. Record the chosen default (1 card) + rationale.
  </action>
  <verify>
    <automated>grep -q "D-05a" .planning/phases/06-script-remix-tools/06-SCOUT.md && grep -qi "runPredictionPipeline" .planning/phases/06-script-remix-tools/06-SCOUT.md</automated>
  </verify>
  <done>06-SCOUT.md exists with the D-05a GREEN re-confirmation, the reuse-surface signatures, the dead-UI boundary, the route decision, and the cardinality decision — the gate 06-04 builds against.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add remix mode to assembler + remix-card block schema/registry/renderer</name>
  <files>src/lib/kc/assembler.ts, src/lib/tools/blocks.ts, src/lib/tools/block-registry.ts, src/components/thread/remix-card-block.tsx, src/components/thread/message-blocks.tsx, src/lib/tools/__tests__/blocks-remix.test.ts</files>
  <read_first>
    - src/lib/kc/assembler.ts (modeSchema line 66 + MODE_ROLES line 102 — Pitfall 2; if 06-01 already extended modeSchema, ADD "remix" to the same enum, do not clobber "script")
    - src/lib/tools/blocks.ts (HookCardBlockSchema + BlockUnionSchema line 142)
    - src/lib/tools/block-registry.ts (BLOCK_REGISTRY line 24)
    - src/components/thread/message-blocks.tsx (BLOCK_COMPONENTS line 24)
    - src/components/thread/hook-card-block.tsx (renderer to clone)
    - src/lib/engine/remix/decode-types.ts (AdaptConcept UI mapping lines 178-193: hook→headline, format_borrowed→"Borrowed:" chip, angle/who_its_for→sub-rows — Open Q2)
  </read_first>
  <behavior>
    - Test: RemixCardBlockSchema.safeParse(validRemixCard).success === true
    - Test: validateBlock(validRemixCard).ok === true (registered → rehydrates)
    - Test: validateBlock({type:'remix-card', props:{}}).ok === false
    - Test: assembleBundle({ask:'x', platform:'tiktok', mode:'remix'}, null) does NOT throw
  </behavior>
  <action>
    Extend `modeSchema` to include `"remix"` and add `MODE_ROLES.remix` (mirror hooks role set: `["niche","audience","platform","wins","flops"]`; GROUND-02 anti-dilution). If 06-01 already added "script", keep both. (Both plans edit assembler.ts — the executor must merge additively, not overwrite.)

    Define `RemixCardBlockSchema` in blocks.ts mirroring HookCardBlockSchema. Props carry the decode→adapt anatomy + the Flash signal (Open Q2 mapping). Suggested props: `adaptedHook: z.string()` (the niche-adapted hook headline — AdaptConcept.hook), `angle: z.string()`, `whoItsFor: z.string()`, `formatBorrowed: z.string()` ("Borrowed:" chip), `sourceDecode: z.object({ hookPattern: z.string(), structure: z.string(), theTurn: z.string(), emotionalBeat: z.string() })` (the real structural decode — D-05, the moat, NOT a metadata guess), `band: z.enum(["Strong","Mixed","Weak"])`, `fraction: z.string()`, `scrollQuote: z.string()`, `model: z.literal("sim1-flash")`. Export `RemixCardBlock`; add to `BlockUnionSchema`. Honesty: band scopes the adapted hook's scroll-stop only (Pitfall 5).

    Register: import `RemixCardBlockSchema` in block-registry.ts, add `"remix-card": { schema: RemixCardBlockSchema as z.ZodType }` to BLOCK_REGISTRY.

    Build `RemixCardRenderer` in `src/components/thread/remix-card-block.tsx` (clone hook-card-block.tsx). Face: adaptedHook headline · "Borrowed:" format chip · angle/whoItsFor sub-rows · the real decoded anatomy (why it worked) on expand. Band chip scoped to the adapted hook. "Develop into hooks →" CTA affordance (remix→hooks, anchorFrom:"card" — the actual POST is wired in 06-05). THEME-06 flat-warm, coral only on CTA. Register `"remix-card": RemixCardRenderer` in BLOCK_COMPONENTS (message-blocks.tsx).

    Create `src/lib/tools/__tests__/blocks-remix.test.ts` covering the behaviors above.
  </action>
  <verify>
    <automated>npx vitest run src/lib/tools/__tests__/blocks-remix.test.ts && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>modeSchema accepts "remix"; MODE_ROLES.remix present; RemixCardBlockSchema + type + union; "remix-card" in BLOCK_REGISTRY + BLOCK_COMPONENTS; RemixCardRenderer renders adapted hook + real decode anatomy + opener-scoped band; blocks-remix.test.ts passes; tsc + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted block JSON → renderer | remix-card rehydration must re-validate (D-14) |
| decode reuse → protected SIM-1 Max path | scout confirms read-only (D-05a) — no boundary crossed into the scorer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | remix decode reuse touching protected scorer | mitigate | Scout re-confirms decode disjoint from runPredictionPipeline/ENGINE_VERSION (D-05a); regression gate asserted in 06-05 |
| T-06-04 | Spoofing | model-generated remix UI | mitigate | RemixCardRenderer owns layout; validated props only (THREAD-04) |
| T-06-05 | Information Disclosure | luck/caption leaking into adapt | mitigate | AdaptInput is a compile-time guard (no luck field — D-01); scout records that decodeResultToAdaptInput never maps luck |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No installs this plan (06-RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `npx vitest run src/lib/tools/__tests__/blocks-remix.test.ts` green
- `06-SCOUT.md` present with D-05a GREEN re-confirmation
- `npx tsc --noEmit` + `npm run lint` clean
</verification>

<success_criteria>
- REMIX-01 reuse scout complete and written (revive, don't rebuild gate satisfied)
- D-05a re-confirmed GREEN against live code (no ENGINE_VERSION bump, decode read-only over Max)
- remix-card validates + rehydrates; mode "remix" accepted by assembler
- RemixCardRenderer surfaces the REAL decode anatomy (D-05), not a metadata guess
</success_criteria>

<output>
Create `.planning/phases/06-script-remix-tools/06-02-SUMMARY.md` when done
</output>
