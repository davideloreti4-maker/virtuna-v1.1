---
phase: 06-script-remix-tools
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/lib/tools/runners/script-runner.ts
  - src/app/api/tools/script/route.ts
  - src/lib/tools/runners/__tests__/script-runner.test.ts
  - src/app/api/tools/script/__tests__/route.test.ts
autonomous: true
requirements: [SCRIPT-01]
must_haves:
  truths:
    - "runScriptPipeline generates ONE script (beats+timing+retention), Flash-gates the opening beat only, returns a validated script-card"
    - "POST /api/tools/script authenticates first, caps ask/anchor, persists the script-card to the open thread, streams content-first"
    - "The opening-beat Flash gate reuses runFlashTextMode('hook') + aggregateFlash unchanged (zero new SIM calibration)"
  artifacts:
    - path: "src/lib/tools/runners/script-runner.ts"
      provides: "runScriptPipeline + ScriptPipelineInput + ScriptPipelineResult + SCRIPT_OUTPUT_CONTRACT"
      contains: "runScriptPipeline"
    - path: "src/app/api/tools/script/route.ts"
      provides: "POST SSE route (auth, caps, persist, content-first stream, follow-up)"
      contains: "export async function POST"
  key_links:
    - from: "src/lib/tools/runners/script-runner.ts"
      to: "src/lib/engine/flash/run-flash-text-mode.ts"
      via: "runFlashTextMode(openingBeatSeed, 'hook', panel) — D-01 opener-only gate"
      pattern: "runFlashTextMode"
    - from: "src/lib/tools/runners/script-runner.ts"
      to: "src/lib/kc/compiled.ts"
      via: "KC_SCRIPT_SYSTEM_PROMPT system prompt"
      pattern: "KC_SCRIPT_SYSTEM_PROMPT"
    - from: "src/app/api/tools/script/route.ts"
      to: "src/lib/threads/messages.ts"
      via: "insertMessage(openThread, 'assistant', blocks, kcGenVersion)"
      pattern: "insertMessage"
---

<objective>
Build the Script backend: `runScriptPipeline` (generate ONE script → self-judge → opening-beat-only Flash gate → validated script-card) and the `POST /api/tools/script` SSE route that authenticates, caps input, persists to the open thread, and streams content-first with a follow-up turn.

Purpose: SCRIPT-01 core — the honest spine (D-01: Flash scores the OPENER only; the rest is the self-judge's job, never a fabricated full-watch score). Greenfield — clone hooks-runner.ts + hooks/route.ts verbatim, swapping N-cards-ranked for one-card (D-02). Reuses the Flash gate unchanged (zero new SIM calibration).

Output: `runScriptPipeline`/`ScriptPipelineInput`/`ScriptPipelineResult`, `SCRIPT_OUTPUT_CONTRACT`, `POST /api/tools/script`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-script-remix-tools/06-CONTEXT.md
@.planning/phases/06-script-remix-tools/06-RESEARCH.md
@.planning/phases/06-script-remix-tools/06-01-SUMMARY.md

# Templates to clone verbatim (read before writing):
@src/lib/tools/runners/hooks-runner.ts
@src/app/api/tools/hooks/route.ts
@src/lib/tools/runners/__tests__/hooks-runner.test.ts
@src/lib/engine/flash/run-flash-text-mode.ts
@src/lib/engine/flash/flash-aggregate.ts
@src/lib/tools/blocks.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runScriptPipeline — generate ONE script → self-judge → opener Flash gate → script-card</name>
  <files>src/lib/tools/runners/script-runner.ts, src/lib/tools/runners/__tests__/script-runner.test.ts</files>
  <read_first>
    - src/lib/tools/runners/hooks-runner.ts (FULL template: assembleBundle call line 267, generateHooksStructured line 127, json_object OUTPUT_CONTRACT pattern line 77, Flash gate line 290, HookCardBlockSchema.safeParse line 379, warnings array)
    - src/lib/tools/runners/__tests__/hooks-runner.test.ts (test structure to clone — mocks getQwenClient, runFlashTextMode, aggregateFlash)
    - src/lib/engine/flash/run-flash-text-mode.ts (runFlashTextMode(seed, framing, panel) signature; "hook" framing = first-2s scroll-stop)
    - src/lib/engine/flash/flash-aggregate.ts (aggregateFlash(personas)→{band,fraction}; MIXED_THRESHOLD)
    - src/lib/tools/blocks.ts (ScriptCardBlockSchema from 06-01 — exact props the runner builds)
    - src/lib/kc/assembler.ts (assembleBundle with mode:"script" — added in 06-01)
  </read_first>
  <behavior>
    - Test: runScriptPipeline returns exactly ONE script-card block on a successful generate (D-02 one-card, not N)
    - Test: the Flash gate is called with the script's OPENING beat seed only (openingBeatSeed), framing "hook" (D-01) — assert runFlashTextMode called once with the opener, not per-beat
    - Test: each returned beat carries label + content + timing + retentionMarker (D-02 card anatomy)
    - Test: band/fraction on the card come from aggregateFlash of the opener SIM (opener-scoped honesty — Pitfall 5)
    - Test: a card failing ScriptCardBlockSchema.safeParse is dropped with a warning (D-14 belt-and-suspenders), never returned invalid
  </behavior>
  <action>
    Create `runScriptPipeline` in `src/lib/tools/runners/script-runner.ts` cloning hooks-runner.ts structure. Export `ScriptPipelineInput { ask: string; platform; profileRow: ProfileRow|null; anchor?: string }` (anchor = the chosen hookLine carried from Hooks→Script) and `ScriptPipelineResult { blocks: ScriptCardBlock[]; warnings: string[] }`.

    Pipeline:
    1. GENERATE: `assembleBundle({ ask: ask || "Write a script for this hook", platform, mode:"script", ...(anchor?{anchor}:{}) }, profileRow)` → user message. System = `KC_SCRIPT_SYSTEM_PROMPT` + a `SCRIPT_OUTPUT_CONTRACT` (static, byte-stable, contains the literal word "json" — Pitfall 3; mirror HOOKS_OUTPUT_CONTRACT). Generate ONE structured script: `{ beats: [{ label, content, timing, retentionMarker }], openingBeatSeed }` (the opener seed = the first-2s line fed to the gate). Use getQwenClient + QWEN_REASONING_MODEL + QWEN_SEED + temperature 0 (mirror hooks).
    2. SELF-JUDGE: bounded self-judge gate (same posture as ideas/hooks — drop a sub-floor generation rather than fabricate). Keep it bounded (no regen loop — cost).
    3. GATE (D-01 — OPENER ONLY): `runFlashTextMode(script.openingBeatSeed, "hook", { niche: profileRow?.niche_primary ?? null, contentType: null })` → `aggregateFlash(personas)` → `{band, fraction}`. Select lead scrollQuote (clone selectLeadScrollQuote). This scores ONLY the opening hook beat — never the full script (honesty spine). If the SIM fails, push a warning and treat as drop (mirror hooks null handling).
    4. BUILD: assemble the script-card block (type:"script-card", props from ScriptCardBlockSchema). Validate via `ScriptCardBlockSchema.safeParse` (D-14); on failure push a warning + drop. Return `{ blocks, warnings }` (0 or 1 block).

    Create `src/lib/tools/runners/__tests__/script-runner.test.ts` covering the behaviors above (clone hooks-runner.test.ts mocking strategy — mock getQwenClient.chat.completions.create, runFlashTextMode, aggregateFlash).
  </action>
  <verify>
    <automated>npx vitest run src/lib/tools/runners/__tests__/script-runner.test.ts && npm run lint</automated>
  </verify>
  <done>runScriptPipeline returns one validated script-card; Flash gate hits the opener only (D-01); beats carry timing+retention; invalid cards dropped with warnings; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: POST /api/tools/script SSE route — auth, caps, persist, content-first stream</name>
  <files>src/app/api/tools/script/route.ts, src/app/api/tools/script/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/tools/hooks/route.ts (FULL clone target: auth gate line 79, ask/anchor caps lines 100-113, createOpenThreadLazy line 133, SSE stream + stage events lines 138-285, insertMessage + kcStamp line 218, follow-up turn lines 226-268, sseHeaders)
    - src/lib/threads/threads.ts (createOpenThreadLazy(userId) — type:"open", reading_id:null)
    - src/lib/threads/messages.ts (insertMessage signature — re-validates blocks at write)
    - src/lib/tools/runners/script-runner.ts (runScriptPipeline from Task 1)
  </read_first>
  <action>
    Create `POST /api/tools/script` in `src/app/api/tools/script/route.ts` cloning hooks/route.ts. Steps: (1) auth gate — `supabase.auth.getUser()` → 401 before any DB/LLM work (T-04-03); (2) parse body `{ ask?, platform?, anchor? }`; cap `ask` at MAX_MESSAGE_LENGTH=2000 and `anchor` at MAX_ANCHOR_LENGTH=5000 server-side (WARNING-5); normalize platform enum; (3) user_id from session ONLY, never body (CR-01); (4) load creator profile (cold-start safe); (5) `createOpenThreadLazy(user.id)`; (6) SSE ReadableStream: emit `stage` events around the single `runScriptPipeline` await (coarse transitions — Generating → Self-judge → Simulating your audience; match the 05-04 posture, no fake timers — D-02), `content` event with the script-card face (beats+timing+retention; band/fraction deferred), then `score` event(s) for the opener band chip a beat later (content-first), `insertMessage(openThread.id, "assistant", blocks, kcStamp().kcGenVersion)` when blocks.length>0, then a non-fatal model-authored `followup` turn (clone the hooks follow-up; KC_CHAT_SYSTEM_PROMPT; caught silently), then `done`. Error path → `send("error", ...)` (surfaces as SkillRunError client-side).

    Do NOT set `maxDuration` higher than needed — Script is cheap (single Flash on one beat); the default route runtime is fine, but match hooks' route posture for consistency.

    Create `src/app/api/tools/script/__tests__/route.test.ts` — clone the hooks route test: asserts 401 when unauthenticated, ask-cap rejection (400), and that runScriptPipeline output is persisted via insertMessage (mock runScriptPipeline + supabase + insertMessage).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/tools/script/__tests__/route.test.ts && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>POST /api/tools/script: 401 before any work; ask/anchor caps enforced; script-card persisted to the open thread; content-first SSE (content → score → followup → done); route test passes; tsc + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/tools/script | untrusted ask/anchor crosses here |
| ask/anchor → Qwen prompt | injection fence via assembleBundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Spoofing | unauthenticated script generation | mitigate | supabase.auth.getUser() → 401 FIRST, before DB/LLM (V2, clone hooks route) |
| T-06-07 | Tampering | prompt injection via ask/anchor | mitigate | assembleBundle <<<USER_CONTENT>>> fence + sentinel strip (V5) |
| T-06-08 | Elevation of Privilege | cross-user thread write | mitigate | user_id from session only, never body (V4 / CR-01) |
| T-06-09 | DoS | unbounded LLM cost | mitigate | single Flash on ONE beat (D-01); bounded self-judge, no regen loop; ask/anchor length caps |
| T-06-10 | Information Disclosure | honesty-spine violation (band as full-watch) | mitigate | band scoped to opener in props + renderer copy (Pitfall 5 / ENGINE-03) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No installs this plan (06-RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `npx vitest run src/lib/tools/runners/__tests__/script-runner.test.ts` green
- `npx vitest run src/app/api/tools/script/__tests__/route.test.ts` green
- `npx tsc --noEmit` + `npm run lint` clean
</verification>

<success_criteria>
- runScriptPipeline returns ONE script-card (D-02), opener-only Flash gate (D-01), opener-scoped band honesty (Pitfall 5)
- POST /api/tools/script: auth-first, capped, persists to open thread, content-first SSE with follow-up
- Zero new SIM calibration — Flash gate reused unchanged
</success_criteria>

<output>
Create `.planning/phases/06-script-remix-tools/06-03-SUMMARY.md` when done
</output>
