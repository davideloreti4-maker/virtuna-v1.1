---
phase: 06-script-remix-tools
plan: 04
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/lib/tools/runners/remix-runner.ts
  - src/app/api/tools/remix/run/route.ts
  - src/lib/tools/runners/__tests__/remix-runner.test.ts
  - src/app/api/tools/remix/run/__tests__/route.test.ts
autonomous: true
requirements: [REMIX-01]
must_haves:
  truths:
    - "runRemixPipeline runs resolve → real decode → adapt → opener Flash gate → ONE validated remix-card"
    - "cleanup() is called in a finally block unconditionally (derive-and-drop, T-03-02)"
    - "A null decode is handled gracefully (SkillRunError surface), never crashes"
    - "POST /api/tools/remix/run authenticates first, validates the URL, sets maxDuration=300, persists to the open thread"
  artifacts:
    - path: "src/lib/tools/runners/remix-runner.ts"
      provides: "runRemixPipeline + RemixPipelineInput + RemixPipelineResult"
      contains: "runRemixPipeline"
    - path: "src/app/api/tools/remix/run/route.ts"
      provides: "POST SSE route (auth, URL validate, CSRF, maxDuration=300, persist)"
      contains: "maxDuration"
  key_links:
    - from: "src/lib/tools/runners/remix-runner.ts"
      to: "src/lib/engine/remix/resolve-and-rehost.ts"
      via: "resolveAndRehost(url, requestId) + cleanup() in finally"
      pattern: "resolveAndRehost"
    - from: "src/lib/tools/runners/remix-runner.ts"
      to: "src/lib/engine/remix/decode.ts"
      via: "omniOutputToStructuralInput + runDecode (real structural decode, D-05)"
      pattern: "runDecode"
    - from: "src/lib/tools/runners/remix-runner.ts"
      to: "src/lib/engine/remix/adapt.ts"
      via: "generateAdaptConcepts(adaptInput)"
      pattern: "generateAdaptConcepts"
---

<objective>
Build the Remix backend by REVIVING the live `engine/remix` decode+adapt chain (D-05/D-06, per 06-SCOUT.md): `runRemixPipeline` (resolve URL → real structural decode → niche adapt → opener Flash gate → ONE remix-card) and the `POST /api/tools/remix/run` SSE route (auth, URL validate, CSRF guards, maxDuration=300, persist to the open thread).

Purpose: REMIX-01 — the FYP-video-I-want-to-redo flow. The moat is decoding the REAL video (D-05), not a metadata guess. Reuse is read-only over the protected SIM-1 Max path (D-05a GREEN — re-confirmed in 06-SCOUT.md). New SSE route, NOT a reuse of /api/remix/adapt (wrong shape — scout decision A1).

Output: `runRemixPipeline`/`RemixPipelineInput`/`RemixPipelineResult`, `POST /api/tools/remix/run`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-script-remix-tools/06-CONTEXT.md
@.planning/phases/06-script-remix-tools/06-RESEARCH.md
@.planning/phases/06-script-remix-tools/06-SCOUT.md
@.planning/phases/06-script-remix-tools/06-02-SUMMARY.md

# Engine functions to revive + route to clone (read before writing):
@src/lib/engine/remix/resolve-and-rehost.ts
@src/lib/engine/remix/decode.ts
@src/lib/engine/remix/decode-types.ts
@src/lib/engine/remix/adapt.ts
@src/app/api/remix/adapt/route.ts
@src/app/api/tools/hooks/route.ts
@src/lib/engine/flash/run-flash-text-mode.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runRemixPipeline — revive resolve → decode → adapt → opener Flash gate → remix-card</name>
  <files>src/lib/tools/runners/remix-runner.ts, src/lib/tools/runners/__tests__/remix-runner.test.ts</files>
  <read_first>
    - .planning/phases/06-script-remix-tools/06-SCOUT.md (the confirmed reuse seams + cardinality decision — 1 card default)
    - src/lib/engine/remix/resolve-and-rehost.ts (resolveAndRehost(url, requestId)→{signedUrl, cleanup}; cleanup MUST run in finally — T-03-02)
    - src/lib/engine/remix/decode.ts (omniOutputToStructuralInput(omni)→OmniStructuralInput|null; runDecode(structural)→DecodeResult|null — returns null on failure, Pitfall 6)
    - src/lib/engine/qwen/omni-analysis.ts (analyzeVideoWithOmni(signedUrl) — perception, NOT Max scoring; D-05a GREEN)
    - src/lib/engine/remix/decode-types.ts (decodeResultToAdaptInput(decode, niche)→AdaptInput; AdaptConcept shape; luck NEVER mapped — D-01)
    - src/lib/engine/remix/adapt.ts (generateAdaptConcepts(input)→AdaptConcept[]|null — 3 concepts; null on graceful failure)
    - src/lib/engine/flash/run-flash-text-mode.ts + flash-aggregate.ts (gate the adapted hook — D-05)
    - src/lib/tools/blocks.ts (RemixCardBlockSchema from 06-02)
    - 06-RESEARCH.md Code Examples "Remix runner core" (the exact import + finally-cleanup composition)
  </read_first>
  <behavior>
    - Test: runRemixPipeline calls resolveAndRehost → analyzeVideoWithOmni → omniOutputToStructuralInput → runDecode → decodeResultToAdaptInput → generateAdaptConcepts in order (mock each)
    - Test: cleanup() is called in finally even when decode returns null or adapt throws (T-03-02)
    - Test: a null decode returns a result that surfaces SkillRunError (no throw, Pitfall 6)
    - Test: exactly ONE remix-card returned from the 3 adapt concepts (cardinality decision — A3/scout)
    - Test: the Flash gate scores the ADAPTED hook with framing "hook" (D-05), band is opener-scoped
    - Test: runRemixPipeline NEVER imports/calls runPredictionPipeline or touches ENGINE_VERSION (D-05a — assert no such import in the module)
  </behavior>
  <action>
    Create `runRemixPipeline` in `src/lib/tools/runners/remix-runner.ts` per the 06-RESEARCH Code Example + 06-SCOUT seams. Export `RemixPipelineInput { url: string; platform; profileRow: ProfileRow|null; requestId: string }` and `RemixPipelineResult { blocks: RemixCardBlock[]; warnings: string[]; error?: "resolve_failed"|"decode_failed"|"adapt_failed" }`.

    Pipeline (revive, don't rebuild — D-06):
    1. `const { signedUrl, cleanup } = await resolveAndRehost(url, requestId)` — wrap the REST in `try { ... } finally { await cleanup() }` (UNCONDITIONAL cleanup — T-03-02).
    2. `const omni = await analyzeVideoWithOmni(signedUrl)` — perception only (NOT Max scoring — D-05a).
    3. `const structural = omniOutputToStructuralInput(omni)`; `const decode = structural ? await runDecode(structural) : null`. If `!decode` → return `{ blocks: [], warnings:[...], error:"decode_failed" }` (graceful — Pitfall 6).
    4. `const niche = profileRow?.niche_primary ?? "general"`; `const adaptInput = decodeResultToAdaptInput(decode, niche)` (luck never mapped — D-01); `const concepts = await generateAdaptConcepts(adaptInput)`. If null/empty → `error:"adapt_failed"`.
    5. Pick ONE concept (cardinality default = 1 — scout/A3; the strongest, or the first if no ranking signal).
    6. GATE: `runFlashTextMode(chosen.hook, "hook", { niche, contentType:null })` → `aggregateFlash` → `{band, fraction}` + lead scrollQuote (D-05, opener-scoped honesty — Pitfall 5).
    7. BUILD: remix-card block — adaptedHook, angle, whoItsFor, formatBorrowed, sourceDecode {hookPattern, structure, theTurn, emotionalBeat} (the REAL decode anatomy — D-05), band, fraction, scrollQuote, model "sim1-flash". Validate via `RemixCardBlockSchema.safeParse` (D-14); drop+warn on failure. Return up to 1 block.

    CRITICAL: do NOT import `runPredictionPipeline`/`aggregateScores`/`usage_tracking`; do NOT touch `ENGINE_VERSION` (D-05a — read-only over the protected path).

    Create `src/lib/tools/runners/__tests__/remix-runner.test.ts` covering the behaviors above — mock resolveAndRehost (assert cleanup called in finally on every path), analyzeVideoWithOmni, runDecode (incl. null), generateAdaptConcepts (incl. null), runFlashTextMode, aggregateFlash.
  </action>
  <verify>
    <automated>npx vitest run src/lib/tools/runners/__tests__/remix-runner.test.ts && npm run lint</automated>
  </verify>
  <done>runRemixPipeline revives resolve→decode→adapt→opener-gate→one remix-card; cleanup in finally on all paths; null decode graceful; no protected-path imports; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: POST /api/tools/remix/run SSE route — auth, URL validate, CSRF, maxDuration=300, persist</name>
  <files>src/app/api/tools/remix/run/route.ts, src/app/api/tools/remix/run/__tests__/route.test.ts</files>
  <read_first>
    - src/app/api/remix/adapt/route.ts (auth, CSRF/Content-Type guards, ownership 404 pattern, `export const maxDuration = 300; runtime="nodejs"; dynamic="force-dynamic"` lines 57-59 — copy these; SSRF handled inside resolveAndRehost)
    - src/app/api/tools/hooks/route.ts (SSE stream + stage events + insertMessage + kcStamp + follow-up + sseHeaders — clone the streaming shell)
    - src/lib/tools/runners/remix-runner.ts (runRemixPipeline from Task 1)
    - src/lib/threads/threads.ts (createOpenThreadLazy) + messages.ts (insertMessage)
  </read_first>
  <action>
    Create `POST /api/tools/remix/run` in `src/app/api/tools/remix/run/route.ts`. Set `export const maxDuration = 300; export const runtime = "nodejs"; export const dynamic = "force-dynamic";` (Pitfall 4 — resolve+decode+adapt is heavy; copy from adapt/route.ts). Steps: (1) auth gate → 401 first (T-04-03); (2) CSRF/Content-Type guards — 415 on wrong Content-Type, 403 on cross-origin (copy adapt route T-04-07); (3) parse `{ url, platform? }`; validate the URL server-side (reuse the TikTok URL regex / the resolver's SSRF validation — resolveAndRehost validates via ApifyScrapingProvider, but reject obviously-bad input early); (4) user_id from session only; (5) load profile; (6) `createOpenThreadLazy(user.id)`; (7) generate a `requestId` (nanoid) for the temp rehost path; (8) SSE ReadableStream: emit `stage` events around the runRemixPipeline await (Resolving → Decoding → Adapting → Simulating your audience — real coarse phases, no fake timers, D-02), branch on `result.error` → `send("error", ...)` (SkillRunError), else `content` (remix-card face: adaptedHook + Borrowed chip + real decode anatomy) → `score` (opener band a beat later) → `insertMessage(openThread.id, "assistant", blocks, kcStamp().kcGenVersion)` → non-fatal `followup` → `done`.

    Create `src/app/api/tools/remix/run/__tests__/route.test.ts` — 401 unauthenticated; 415/403 CSRF; bad URL rejected; remix-card persisted on success (mock runRemixPipeline + supabase + insertMessage).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/tools/remix/run/__tests__/route.test.ts && grep -q "maxDuration = 300" src/app/api/tools/remix/run/route.ts && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>POST /api/tools/remix/run: auth-first, CSRF-guarded, URL-validated, maxDuration=300, real-stage SSE, remix-card persisted, graceful error branch; route test passes; tsc + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/tools/remix/run | untrusted pasted URL crosses here |
| pasted URL → server-side fetch | SSRF surface — resolveAndRehost validates |
| Apify token → fetch | token must never reach the Omni-facing signed URL |
| decode reuse → protected SIM-1 Max path | read-only (D-05a) — no crossing into the scorer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Information Disclosure | SSRF via pasted URL | mitigate | resolveAndRehost uses ApifyScrapingProvider SSRF-validated resolver; server-side fetch only (V5/SSRF) |
| T-06-12 | Information Disclosure | Apify token leak | mitigate | token only on server-side fetch URL, never on the Omni signed URL (T-03-01, inside resolveAndRehost) |
| T-06-13 | Spoofing | unauthenticated remix run | mitigate | supabase.auth.getUser() → 401 FIRST (V2) |
| T-06-14 | Tampering | CSRF state-changing POST | mitigate | Content-Type 415 + cross-origin 403 guards (copy adapt route T-04-07) |
| T-06-15 | — | temp video orphan | mitigate | cleanup() in finally, unconditional (T-03-02) |
| T-06-16 | DoS | unbounded decode/adapt cost | mitigate | bounded retries (decode 1, adapt 1) already in engine; single Flash on adapted hook; maxDuration=300 cap |
| T-06-17 | Information Disclosure | luck/caption leak into adapt | mitigate | AdaptInput compile-time guard (no luck field — D-01); decodeResultToAdaptInput never maps luck |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No installs this plan (06-RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `npx vitest run src/lib/tools/runners/__tests__/remix-runner.test.ts` green
- `npx vitest run src/app/api/tools/remix/run/__tests__/route.test.ts` green
- `grep "maxDuration = 300"` present in the route
- `npx tsc --noEmit` + `npm run lint` clean
</verification>

<success_criteria>
- runRemixPipeline revives the live engine chain; real decode (D-05), not metadata guess
- cleanup() unconditional (T-03-02); null decode graceful (Pitfall 6)
- No protected-path coupling (D-05a — no runPredictionPipeline import, no ENGINE_VERSION touch)
- Route auth-first, CSRF-guarded, SSRF-safe, maxDuration=300, persists one remix-card
</success_criteria>

<output>
Create `.planning/phases/06-script-remix-tools/06-04-SUMMARY.md` when done
</output>
