---
phase: 06-script-remix-tools
plan: 05
type: execute
wave: 4
depends_on: ["06-03", "06-04"]
files_modified:
  - src/lib/tools/chain-handoff.ts
  - src/lib/tools/__tests__/chain-handoff.test.ts
  - src/lib/script-test-context.tsx
  - src/hooks/queries/use-script-stream.ts
  - src/hooks/queries/use-remix-stream.ts
  - src/components/thread/script-thread-view.tsx
  - src/components/thread/remix-thread-view.tsx
  - src/components/app/home/tool-chips.tsx
  - src/components/app/home/composer.tsx
autonomous: false
requirements: [SCRIPT-01, REMIX-01]
must_haves:
  truths:
    - "CHAIN_HANDOFFS hooks→script, script→test, remix→hooks placeholders have live endpoints/context wired"
    - "Script + Remix chips appear in the composer and route to their SSE streams (never navigate to /analyze)"
    - "A hook's 'Write script →' CTA carries the hookLine into Script; a script's 'Test full →' carries it into Test; a remix's 'Develop into hooks →' feeds Hooks"
    - "The engine suite stays green and ENGINE_VERSION is unchanged (3.19.0) — BLOCKING regression gate"
  artifacts:
    - path: "src/lib/tools/chain-handoff.ts"
      provides: "live endpoints for hooks→script + remix→hooks; script→test context handoff confirmed"
      contains: "/api/tools/script"
    - path: "src/lib/script-test-context.tsx"
      provides: "ScriptTestContext (script→test seam, sibling of HookTestContext)"
      contains: "ScriptTestContext"
    - path: "src/hooks/queries/use-script-stream.ts"
      provides: "useScriptStream (SSE consumer)"
      contains: "useScriptStream"
    - path: "src/hooks/queries/use-remix-stream.ts"
      provides: "useRemixStream (SSE consumer)"
      contains: "useRemixStream"
    - path: "src/components/thread/script-thread-view.tsx"
      provides: "ScriptThreadView"
      contains: "ScriptThreadView"
    - path: "src/components/thread/remix-thread-view.tsx"
      provides: "RemixThreadView"
      contains: "RemixThreadView"
  key_links:
    - from: "src/components/app/home/composer.tsx"
      to: "src/hooks/queries/use-script-stream.ts"
      via: "activeTool==='script' → script.start() (no pendingNavRef)"
      pattern: "useScriptStream|script\\.start"
    - from: "src/lib/tools/chain-handoff.ts"
      to: "src/app/api/tools/script/route.ts"
      via: "hooks→script endpoint = '/api/tools/script'"
      pattern: "/api/tools/script"
    - from: "src/components/thread/script-card-block.tsx"
      to: "src/lib/script-test-context.tsx"
      via: "ScriptCardRenderer reads ScriptTestContext for 'Test full →'"
      pattern: "ScriptTestContext"
---

<objective>
Wire both skills end-to-end into the studio and prove the protected path stayed green. Fill the three pre-staged `CHAIN_HANDOFFS` placeholders with live endpoints/context, add Script + Remix chips + composer routing (never navigating to /analyze), build the two SSE hooks + thread views + the `ScriptTestContext` seam, extend the chain-handoff test, then run UAT and the BLOCKING engine regression gate.

Purpose: D-07 — zero structural plumbing, append-only. This closes SCRIPT-01 success criteria (hook→script→test spine, "Test full →") and REMIX-01 (URL → decode → adapt → "Develop into hooks →"). Both skills register through the P5 generic chain plumbing with no one-off wiring. The regression gate (D-05a / cross-cutting constraint) is the non-negotiable phase exit.

Output: live CHAIN_HANDOFFS, ToolId += script/remix + chips, useScriptStream/useRemixStream, ScriptThreadView/RemixThreadView, ScriptTestContext, chain-handoff test extension, UAT, engine-suite-green proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-script-remix-tools/06-CONTEXT.md
@.planning/phases/06-script-remix-tools/06-RESEARCH.md
@.planning/phases/06-script-remix-tools/06-03-SUMMARY.md
@.planning/phases/06-script-remix-tools/06-04-SUMMARY.md

# Wiring templates to clone (read before editing):
@src/lib/tools/chain-handoff.ts
@src/components/app/home/composer.tsx
@src/components/app/home/tool-chips.tsx
@src/components/thread/hooks-thread-view.tsx
@src/hooks/queries/use-hooks-stream.ts
@src/lib/hook-test-context.tsx
@src/components/thread/hook-card-block.tsx
@src/lib/engine/version.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill CHAIN_HANDOFFS + ScriptTestContext + SSE hooks + thread views</name>
  <files>src/lib/tools/chain-handoff.ts, src/lib/tools/__tests__/chain-handoff.test.ts, src/lib/script-test-context.tsx, src/hooks/queries/use-script-stream.ts, src/hooks/queries/use-remix-stream.ts, src/components/thread/script-thread-view.tsx, src/components/thread/remix-thread-view.tsx</files>
  <read_first>
    - src/lib/tools/chain-handoff.ts (placeholders lines 121-153: hooks→script endpoint null→"/api/tools/script"; script→test stays endpoint null context; remix→hooks endpoint null→"/api/tools/remix/develop" OR a develop endpoint — see action; the 4-step extend procedure in the header)
    - src/lib/hook-test-context.tsx (the exact context pattern to clone for ScriptTestContext)
    - src/hooks/queries/use-hooks-stream.ts (FULL SSE consumer: start/startRefine/reset/toBlocks/stages/followupText/error/isStreaming — clone for both new hooks)
    - src/components/thread/hooks-thread-view.tsx (HooksThreadView — persisted+streaming render, ProgressChecklist, followup, SkillRunError, context provider — clone for both views)
    - src/lib/tools/__tests__/chain-handoff.test.ts does NOT exist yet — create it (06-RESEARCH test map: "extend existing" — none present, so create)
  </read_first>
  <action>
    **chain-handoff.ts (D-07):** Set the three placeholder endpoints. `hooks→script`: `endpoint: "/api/tools/script"` (anchorFrom "card" — hookLine is the anchor). `script→test`: keep `endpoint: null, anchorFrom: "context"` (context handoff via ScriptTestContext — mirrors hooks→test). `remix→hooks`: set `endpoint: "/api/tools/ideas/develop"` (anchorFrom "card" — the adapted hook is the anchor). PINNED contract: `/api/tools/ideas/develop` accepts `{ ideaId?, anchor, platform }` (PINNED endpoint, 03-03-SUMMARY) — the remix-card CTA sends `{ anchor, platform }` (ideaId omitted; the adapted hook line IS the anchor), which MATCHES that body shape, so reuse is the canonical path (least new surface). FALLBACK CONDITION: before wiring, confirm `/api/tools/ideas/develop` still accepts a body with `anchor` present + `ideaId` absent (read its route — it does as of 03-03). ONLY if that contract has drifted (e.g. ideaId became required), create a thin `POST /api/tools/remix/develop` that takes `{ anchor, platform }` and runs `runHooksPipeline({ ask:"", platform, profileRow, anchor })`, then point the endpoint there instead. Record the chosen endpoint in the SUMMARY. `SkillId` already includes "script"|"remix" (no edit). Update the D-09 header note to mark the placeholders LIVE.

    **ScriptTestContext:** Create `src/lib/script-test-context.tsx` cloning hook-test-context.tsx. `export type OnTestScriptFn = (openingBeatLine: string, brief: string) => void` (the script's opener line + a compact script brief as the Test anchor — A2/Pattern 4). Export `ScriptTestContext` + `useOnTestScript`. Update `ScriptCardRenderer` (06-01) to read `ScriptTestContext` for its "Test full →" CTA (stub when null).

    **SSE hooks:** Create `use-script-stream.ts` + `use-remix-stream.ts` cloning use-hooks-stream.ts (start/reset/toBlocks/stages/followupText/error/isStreaming). useScriptStream.start(ask, platform, anchor?) POSTs to /api/tools/script; useRemixStream.start(url, platform) POSTs to /api/tools/remix/run. Parse the same SSE events (stage/content/score/followup/done/error). toBlocks() returns ScriptCardBlock[] / RemixCardBlock[].

    **Thread views:** Create script-thread-view.tsx + remix-thread-view.tsx cloning hooks-thread-view.tsx (persisted + streaming render via MessageBlocks, ProgressChecklist while streaming, followup markdown, SkillRunError + onRetry, THEME-06). ScriptThreadView provides `ScriptTestContext.Provider` (onTestScript). RemixThreadView renders the remix-card (its "Develop into hooks →" CTA is card-anchored — fires via the handoff, no context needed).

    **chain-handoff.test.ts:** Create it — assert: handoffsFor("hooks") includes a script CTA with `endpoint === "/api/tools/script"` + `anchorFrom === "card"`; handoffsFor("script") includes a test CTA with `endpoint === null` + `anchorFrom === "context"`; handoffsFor("remix") includes a hooks CTA with `anchorFrom === "card"` AND `endpoint` pointing at the develop route that accepts the `{ anchor, platform }` payload the remix-card sends — assert the EXACT endpoint string (`"/api/tools/ideas/develop"` for the reuse path, or `"/api/tools/remix/develop"` if the fallback was taken), not merely "non-null". Add an assertion documenting the remix→hooks payload contract (a test that the chosen endpoint's accepted body includes `anchor` + `platform`) so a future endpoint drift fails this test rather than silently mis-routing. All SkillId members resolve via handoffsFor.
  </action>
  <verify>
    <automated>npx vitest run src/lib/tools/__tests__/chain-handoff.test.ts && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>Three placeholders live with pinned endpoints (hooks→script="/api/tools/script" card; script→test=null context; remix→hooks="/api/tools/ideas/develop" card, reuse path confirmed against the PINNED {ideaId?, anchor, platform} contract or fallback recorded); ScriptTestContext created + consumed by ScriptCardRenderer; useScriptStream/useRemixStream + ScriptThreadView/RemixThreadView built; chain-handoff.test.ts asserts exact endpoint strings + the remix→hooks {anchor, platform} payload contract and passes; tsc + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Composer + tool-chips wiring (script/remix chips, routing, rehydration)</name>
  <files>src/components/app/home/tool-chips.tsx, src/components/app/home/composer.tsx</files>
  <read_first>
    - src/components/app/home/tool-chips.tsx (ToolId union line 21; CHIPS array line 41; MODEL_LABEL line 24 — add script/remix → "SIM-1 Flash")
    - src/components/app/home/composer.tsx (activeTool routing: useHooksStream wiring lines 130-134, handleSubmit hooks branch lines 288-294, showHooksView gate, threadContent HooksThreadView lines 484-497, handleTestHook seam line 235, persisted rehydration split lines 213-220, PLACEHOLDER_BY_TOOL line 71, showPlatformChip line 452)
  </read_first>
  <action>
    **tool-chips.tsx:** Add `"script"` and `"remix"` to the `ToolId` union. Add CHIPS entries `{ id:"script", label:"Script", enabled:true, costSlot:"credit" }` and `{ id:"remix", label:"Remix", enabled:true, costSlot:"credit" }`. Add `MODEL_LABEL.script = "SIM-1 Flash"` and `MODEL_LABEL.remix = "SIM-1 Flash"`.

    **composer.tsx:** Wire both new tools mirroring the hooks wiring EXACTLY:
    - Instantiate `const script = useScriptStream()` + `const remix = useRemixStream()`; compute `showScriptView`/`showRemixView` (active tool + streaming/blocks/error/persisted).
    - Add to `hasThread` the new streams' isStreaming + blocks + persisted arrays.
    - `handleSubmit`: add a `script` branch (ask = trimmedUrl, carry `testBrief`-style anchor if a hook was carried in — see below; call `script.start(ask, platform, anchor)`; CRITICAL: NEVER set pendingNavRef / call stream.start — T-03-13) and a `remix` branch (url = trimmedUrl; `remix.start(url, platform)`; URL is required so canSubmit gates on trimmedUrl for remix). Neither navigates to /analyze.
    - PLACEHOLDER_BY_TOOL: add `script: "Carry a hook in, or type a topic to script…"` and `remix: "Paste a trending or competitor TikTok URL to remix…"`.
    - showPlatformChip: include "script" (platform-relevant); remix uses the URL's platform implicitly — include or exclude per the hooks posture (include for consistency).
    - canSubmit: script = !submitting && !script.isStreaming (empty ask allowed when an anchor is carried); remix = !submitting && !remix.isStreaming && trimmedUrl.length>0 (URL required).
    - Render `<ScriptThreadView .../>` and `<RemixThreadView .../>` in threadContent (clone the HooksThreadView block; wire stages/followupText/error/onRetry).
    - **Chain-in seams:**
      - hooks→script (PINNED card-POST model — anchorFrom:"card"): the hook-card's "Write script →" CTA POSTs the hook line as the `anchor` body param directly to `/api/tools/script` (card-level fetch, exactly mirroring the proven idea→hooks card-POST to /api/tools/ideas/develop). The `/api/tools/script` route (06-03) already accepts an `anchor` body param, so this is the canonical path. On a successful POST, switch `setActiveTool("script")` so ScriptThreadView renders the resulting card; the SSE response streams into useScriptStream (or the route returns the persisted card and the view rehydrates — match whichever shape 06-03's route emits). Do NOT use a context-handoff for hooks→script (that pattern is reserved for the null-endpoint script→test seam below); do NOT fire `script.start` with a separately-threaded anchor as an alternative — the card-POST is the single wiring.
      - script→test: provide `onTestScript` to ScriptThreadView (sets activeTool "test" + a script test brief, mirroring handleTestHook — D-07 / honesty spine: no model call on the script text).
      - remix→hooks: the remix-card "Develop into hooks →" CTA POSTs the adapted hook to the develop endpoint (from Task 1) → switches to hooks view to render the result (mirror the chat onSuggestChain → setActiveTool("hooks") pattern).
    - Open-thread rehydration (loadPersistedBlocks): add `script-card` and `remix-card` to the block-type split (alongside idea-card/hook-card/markdown) so reload rehydrates them.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
  </verify>
  <done>Script + Remix chips live; composer routes script/remix to their streams (no /analyze nav); hooks→script CTA POSTs the hook line as the `anchor` body param to /api/tools/script (card-POST model, no context-handoff, no script.start alternative); script→test via ScriptTestContext; remix→hooks via the develop endpoint; persisted script-card/remix-card rehydrate; tsc + lint + build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Script + Remix skills wired end-to-end into the studio: chips in the composer, both SSE streams, both thread cards, all three chain CTAs (hooks→script, script→test, remix→hooks). Engine regression gate verified automatically below.
  </what-built>
  <how-to-verify>
    1. `npm run dev`, open the home composer logged in.
    2. SCRIPT spine: select Hooks chip → generate hooks (Auto) → on a hook card tap "Write script →". Verify it switches to Script and produces ONE script card with beats + per-beat timing + per-beat retention markers, and a band chip scoped to the OPENER ("opener stops the scroll" — NOT a full-watch claim, D-01/Pitfall 5). Tap "Test full →" → verify it switches to Test with the script brief above the upload (no model call on the text).
    3. REMIX entry: select Remix chip → paste a trending/competitor TikTok URL → run. Verify real stages stream (Resolving → Decoding → Adapting → Simulating), then ONE remix card showing the adapted niche hook + "Borrowed:" format chip + the REAL decoded anatomy (why it worked) + an opener band chip. Tap "Develop into hooks →" → verify it feeds the Hooks chain.
    4. Reload the page → verify the script card and remix card rehydrate (do not vanish/placeholder — Pitfall 1).
    5. Confirm neither Script nor Remix ever navigated to /analyze (open thread stayed put — T-03-13).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. "remix card shows metadata not real decode", "script band claims full watch")</resume-signal>
</task>

<task type="auto">
  <name>Task 4: BLOCKING engine regression gate + full suite + UAT record</name>
  <files>.planning/phases/06-script-remix-tools/06-UAT.md</files>
  <read_first>
    - src/lib/engine/version.ts (ENGINE_VERSION must still be "3.19.0" — D-05a, cross-cutting constraint: no bump)
  </read_first>
  <action>
    Run the BLOCKING regression gate (D-05a + cross-cutting "engine OPEN but protected"). Both skills touch the engine-adjacent text/Flash path and Remix revives engine/remix code, so the protected SIM-1 Max path must be proven unchanged:
    1. `npm test -- src/lib/engine` — the engine suite (version.test.ts, decode.test.ts, adapt.test.ts, omni-to-structural.test.ts, remix-core-grounding.test.ts) MUST stay green.
    2. Assert `ENGINE_VERSION === "3.19.0"` (grep version.ts) — NO bump (decode reuse is read-only over Max; Script is text-path Flash).
    3. `npm test` — full suite green (incl. the new blocks-script/blocks-remix/script-runner/remix-runner/route/chain-handoff tests).
    4. `npm run build` — production build clean.

    Record the results in `.planning/phases/06-script-remix-tools/06-UAT.md`: engine-suite status, ENGINE_VERSION value, full-suite pass count, build status, and the human-verify checkpoint outcome from Task 3. If the engine suite fails or ENGINE_VERSION changed, STOP — this is a blocking gate, surface the regression (do not proceed to phase completion).
  </action>
  <verify>
    <automated>npm test -- src/lib/engine && grep -q 'ENGINE_VERSION = "3.19.0"' src/lib/engine/version.ts && npm test && npm run build</automated>
  </verify>
  <done>Engine suite green; ENGINE_VERSION unchanged at 3.19.0; full suite green; build clean; 06-UAT.md records the gate + checkpoint outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chain CTA → downstream skill | anchor carried client-side; downstream route re-caps + re-fences |
| revived engine/remix code → protected SIM-1 Max path | regression gate proves no crossing (D-05a) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-18 | Tampering | silent regression of SIM-1 Max scoring | mitigate | BLOCKING regression gate (Task 4): engine suite green + ENGINE_VERSION==3.19.0 + same-video Max score-identity preserved |
| T-06-19 | Elevation of Privilege | chain CTA bypassing server caps | mitigate | downstream routes (06-03/06-04) re-enforce auth + ask/anchor caps + fence (anchor is client metadata only) |
| T-06-20 | Spoofing | script/remix send navigating to /analyze | mitigate | script/remix branches NEVER set pendingNavRef / call stream.start (T-03-13) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No installs this plan (06-RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `npx vitest run src/lib/tools/__tests__/chain-handoff.test.ts` green
- `npm test -- src/lib/engine` green (BLOCKING regression gate)
- `ENGINE_VERSION === "3.19.0"` (unchanged)
- `npm test` full suite green; `npm run build` clean
- Human-verify checkpoint approved
</verification>

<success_criteria>
- SCRIPT-01: hook → script card (beats+timing+retention) → "Test full →" spine works; opener-only honest band
- REMIX-01: URL → real decode → niche adapt → remix card → "Develop into hooks →" feeds the chain
- Both register via the P5 generic chain plumbing (append-only, no one-off wiring — D-07)
- Cards rehydrate on reload (Pitfall 1 closed)
- BLOCKING: engine suite green + ENGINE_VERSION unchanged (D-05a / cross-cutting protected path)
</success_criteria>

<output>
Create `.planning/phases/06-script-remix-tools/06-05-SUMMARY.md` when done
</output>
