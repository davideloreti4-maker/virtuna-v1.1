---
phase: 02-remix-mode-one-board-two-config
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/board/board-types.ts
  - src/components/board/board-constants.ts
  - src/components/board/decode/DecodeShellNode.tsx
  - src/components/board/adapt/AdaptShellNode.tsx
  - src/components/board/decode/__tests__/DecodeShellNode.test.tsx
  - src/components/board/adapt/__tests__/AdaptShellNode.test.tsx
  - src/components/board/GroupFrameOverlay.tsx
  - src/components/board/Board.tsx
  - src/components/board/BoardMobile.tsx
  - src/components/board/__tests__/board-constants.test.ts
  - src/components/board/__tests__/BoardMobile.test.tsx
autonomous: true
requirements: [REMIX-02]
user_setup: []

must_haves:
  truths:
    - "resolveBoardLayout(measured,'remix') returns frames with ids decode+adapt and no verdict/actions"
    - "resolveBoardLayout(measured,'remix') keeps decode bounds == verdict bounds and adapt bounds == actions bounds verbatim (1:1 swap)"
    - "resolveBoardLayout(measured) and resolveBoardLayout(measured,'score') are byte-identical to the pre-change return (56-assertion suite stays green)"
    - "DecodeShellNode and AdaptShellNode are DOM components (return a <div>), not Konva nodes"
    - "Desktop overlay dispatch renders DecodeShellNode for id 'decode' and AdaptShellNode for id 'adapt'"
    - "Mobile remix order is [input, decode, audience, adapt, content-analysis, engine]; score order unchanged"
    - "computePresetTargets does not crash in remix mode (byId.verdict ?? byId.decode fallback)"
  artifacts:
    - path: "src/components/board/decode/DecodeShellNode.tsx"
      provides: "static DOM decode shell — descriptor only (title from overlay layout.label)"
      contains: "decode-shell"
    - path: "src/components/board/adapt/AdaptShellNode.tsx"
      provides: "static DOM adapt shell"
      contains: "adapt-shell"
    - path: "src/components/board/board-constants.ts"
      provides: "mode-aware resolveBoardLayout(measured, mode='score') + decode/adapt in AUTO_HEIGHT_FRAMES + computePresetTargets fallback"
      contains: "byId.verdict ?? byId.decode"
    - path: "src/components/board/board-types.ts"
      provides: "GroupId extended with 'decode' | 'adapt'"
      contains: "| 'decode'"
  key_links:
    - from: "src/components/board/Board.tsx"
      to: "DecodeShellNode / AdaptShellNode"
      via: "overlay per-id dispatch switch"
      pattern: "layout.id === 'decode'"
    - from: "src/components/board/board-constants.ts"
      to: "verdict/actions bounds"
      via: "rewrite {id,label} on resolved verdict/actions entries (no base['decode'] lookup)"
      pattern: "id: 'decode'"
---

<objective>
Make the board mode-aware: extend `GroupId`, teach `resolveBoardLayout` to return a remix frame set (decode↔verdict, adapt↔actions, bounds 1:1 per D-07), build the two static DOM shell components, and dispatch them in both the desktop Konva overlay and the mobile card-stack. The score-mode return must stay byte-identical so the existing 56-assertion gap-regression suite never breaks (D-03 zero regression).

Purpose: REMIX-02 board half — one-board-two-config swap on desktop + mobile, score board unchanged. Empty shells only (Decode/Adapt content is Phase 3/4).
Output: GroupId union, mode-aware resolver + camera fallback, two ~12-line DOM shells, overlay + mobile dispatch, extended unit/component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-remix-mode-one-board-two-config/02-RESEARCH.md
@.planning/phases/02-remix-mode-one-board-two-config/02-PATTERNS.md
@.planning/phases/02-remix-mode-one-board-two-config/02-UI-SPEC.md

<interfaces>
<!-- Extracted this session. CRITICAL: frame bodies are DOM React, NOT Konva. -->

board-types.ts:15-28 — GroupId union (input|engine|audience|verdict|actions|content-analysis); CameraPresetKey union (keep 'verdict' key name).

board-constants.ts:
  GROUP_FRAMES verdict entry: { id:'verdict', label:'Score', bounds:{ x:864, y:0, width:360, height:280 } }
  GROUP_FRAMES actions entry: { id:'actions', label:'Actions', bounds:{ x:864, y:312, width:360, height:760 } }
  resolveBoardLayout(measured: Partial<Record<GroupId,number>>): GroupFrameLayout[]  — builds `base` from GROUP_FRAMES by id; base['decode'] would be undefined (Pitfall 2).
  AUTO_HEIGHT_FRAMES: Set<GroupId> (verdict, actions, ... auto-height).
  computePresetTargets(...) line ~166: `const ver = byId.verdict;` then reads ver.x (Pitfall 3 — undefined in remix).

VerdictNode.tsx:84-89 — node root shape to clone: `return (<div className="relative flex w-full flex-col gap-4" data-testid="verdict-node">...`  (DOM, returns JSX).

GroupFrameOverlay.tsx:
  :157 renders title from layout.label → shells render DESCRIPTOR ONLY (no repeated label).
  :13 EMPTY_STATE_COPY: Record<string,{title;sub}> — add decode/adapt keys (or leave title '' so empty block skips).
  :42 ARIA_LABEL: Record<string,string> — add 'decode'/'adapt' to avoid undefined → falls back to layout.label anyway, but UI-SPEC §Accessibility wants specific labels.

Board.tsx:
  :98 `resolveBoardLayout(measuredH)` — make mode-aware (Plan 03 supplies the value; this plan adds the optional param + dispatch).
  :481-482 overlay dispatch: `{layout.id === 'verdict' && <VerdictNode .../>}` / `{layout.id === 'actions' && <ActionsNode .../>}` — add decode/adapt cases here.
  :90-92 `expanded` initialized from GROUP_FRAMES (score ids only) → guard `expanded[layout.id] ?? true` for new ids.
  NAMING COLLISION: Board.tsx:418/:495 `mode` is the RESPONSIVE view mode ('cards'|'desktop' via ViewModeToggle). DO NOT reuse `mode` for intent — use `boardMode` for score/remix in this file.

BoardMobile.tsx: MOBILE_ORDER (24-31), renderBody switch (81-107), LAYOUT_BY_ID from GROUP_FRAMES (48-50; .get('decode') undefined → guard). Card label map (121-129) reads LAYOUT_BY_ID.get(id)!.label.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mode-aware resolveBoardLayout + GroupId + camera fallback (Wave-0 tests first)</name>
  <files>src/components/board/board-types.ts, src/components/board/board-constants.ts, src/components/board/__tests__/board-constants.test.ts</files>
  <read_first>
    - src/components/board/board-types.ts:15-28 (GroupId, CameraPresetKey) — PATTERNS.md §board-types.ts
    - src/components/board/board-constants.ts (resolveBoardLayout ~107-147, AUTO_HEIGHT_FRAMES ~82-89, computePresetTargets ~157-184) — PATTERNS.md §board-constants.ts
    - src/components/board/__tests__/board-constants.test.ts (the 56-assertion suite — extend, never weaken)
    - .planning/phases/02-remix-mode-one-board-two-config/02-RESEARCH.md Pitfalls 2,3 + Pattern 2 option (a)
  </read_first>
  <behavior>
    - resolveBoardLayout(measured) (no 2nd arg) === resolveBoardLayout(measured, 'score') — both byte-identical to current output (the existing 56 assertions still pass unchanged)
    - resolveBoardLayout(measured, 'remix'): the set contains an id 'decode' with label 'Decode' and an id 'adapt' with label 'Adapt'; contains NO 'verdict' or 'actions' id
    - decode entry bounds deep-equal the score-mode verdict entry bounds; adapt entry bounds deep-equal the score-mode actions entry bounds (1:1 swap, D-07)
    - all non-swapped frames (input, engine, audience, content-analysis) are positionally identical between score and remix
    - computePresetTargets over a remix-resolved frame set does not throw (verdict-key preset resolves via decode fallback)
  </behavior>
  <action>
    Tests first (RED): extend board-constants.test.ts with a `describe('remix mode')` block asserting the behaviors above, plus a regression assertion that the default-arg call equals the explicit-'score' call. Then implement (GREEN):
    1. board-types.ts: add `| 'decode' | 'adapt'` to GroupId. Keep CameraPresetKey's 'verdict' key name (internal).
    2. board-constants.ts: change signature to `resolveBoardLayout(measured, mode: 'score' | 'remix' = 'score')`. Default param keeps every existing caller byte-identical. Use Pattern 2 option (a): after computing the score frame array, if mode==='remix' map it — rewrite the verdict entry to `{ ...entry, id: 'decode', label: 'Decode' }` and the actions entry to `{ ...entry, id: 'adapt', label: 'Adapt' }`, bounds verbatim. Do NOT look up base['decode'] (undefined — Pitfall 2). Add 'decode' and 'adapt' to AUTO_HEIGHT_FRAMES. In computePresetTargets change `const ver = byId.verdict;` to `const ver = byId.verdict ?? byId.decode;` (Pitfall 3); apply the same `?? byId.adapt` guard to any byId.actions read if present.
  </action>
  <verify>
    <automated>npx vitest run src/components/board/__tests__/board-constants.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - board-constants.test.ts has a remix describe block; ALL pre-existing assertions still pass (suite count ≥ prior 56)
    - `grep -c "byId.verdict ?? byId.decode" src/components/board/board-constants.ts` == 1
    - `grep -c "id: 'decode'" src/components/board/board-constants.ts` ≥ 1 and resolver does NOT reference `base\['decode'\]` (grep -c returns 0)
    - GroupId in board-types.ts contains `'decode'` and `'adapt'`
    - AUTO_HEIGHT_FRAMES contains both new ids
  </acceptance_criteria>
  <done>resolveBoardLayout is mode-parameterized with a byte-identical score path, decode/adapt reuse verdict/actions bounds 1:1, camera preset no longer crashes in remix; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DecodeShellNode + AdaptShellNode (static DOM shells)</name>
  <files>src/components/board/decode/DecodeShellNode.tsx, src/components/board/adapt/AdaptShellNode.tsx, src/components/board/decode/__tests__/DecodeShellNode.test.tsx, src/components/board/adapt/__tests__/AdaptShellNode.test.tsx</files>
  <read_first>
    - src/components/board/verdict/VerdictNode.tsx:84-89 (DOM node root shape to clone) — PATTERNS.md §DecodeShellNode
    - src/components/board/GroupFrameOverlay.tsx:157 (title bar renders layout.label → shell is descriptor only) + :196 (existing muted-descriptor idiom)
    - src/components/board/_kit/FrameHero.tsx:65 (label typography idiom, reference only)
    - .planning/phases/02-remix-mode-one-board-two-config/02-UI-SPEC.md §3, §4, §Copywriting (locked descriptor strings)
    - .planning/phases/02-remix-mode-one-board-two-config/02-RESEARCH.md Pitfall 1 (Konva trap — shells MUST be DOM)
  </read_first>
  <behavior>
    - DecodeShellNode renders a <div data-testid="decode-shell"> containing a <p> with exactly "Structural breakdown of why this video worked."
    - AdaptShellNode renders a <div data-testid="adapt-shell"> containing a <p> with exactly "Niche-adapted concepts drawn from the source format."
    - Neither renders a skeleton/shimmer/spinner element nor any "coming soon"/date text (D-10/D-11)
    - Neither imports from react-konva (they are DOM)
  </behavior>
  <action>
    Create src/components/board/decode/DecodeShellNode.tsx and src/components/board/adapt/AdaptShellNode.tsx as ~12-line static DOM components shaped like VerdictNode (a `<div>` returning JSX — Pitfall 1: NOT Konva `<Text>`/`<Rect>`, which render nothing inside the GroupFrameOverlay DOM layer). Body is the descriptor `<p>` ONLY — the frame title comes from GroupFrameOverlay layout.label (overlay:157), do not repeat the label. Descriptor className `max-w-[44ch] text-xs leading-[1.4] text-white/35` (UI-SPEC §Typography: 12px medium muted), root `relative flex w-full flex-col gap-2`. Shells take NO props (they self-render static copy; VerdictNode takes camera/layout only because it reads the stream). Copy strings are the UI-SPEC §Copywriting locked defaults verbatim. Create matching __tests__ files asserting the testid + exact descriptor text + absence of skeleton (mirror the descriptor-render behavior above).
  </action>
  <verify>
    <automated>npx vitest run src/components/board/decode/__tests__/DecodeShellNode.test.tsx src/components/board/adapt/__tests__/AdaptShellNode.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both shell files exist; `grep -L "react-konva" src/components/board/decode/DecodeShellNode.tsx src/components/board/adapt/AdaptShellNode.tsx` lists both (neither imports react-konva)
    - DecodeShellNode contains the exact string "Structural breakdown of why this video worked."
    - AdaptShellNode contains the exact string "Niche-adapted concepts drawn from the source format."
    - Both shell tests pass; neither shell renders a skeleton/shimmer (test asserts no `animate-pulse`/skeleton testid)
  </acceptance_criteria>
  <done>Two static DOM shells render their locked descriptor, no Konva, no skeleton; component tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire shells into Board.tsx overlay + BoardMobile order/dispatch</name>
  <files>src/components/board/Board.tsx, src/components/board/BoardMobile.tsx, src/components/board/GroupFrameOverlay.tsx, src/components/board/__tests__/BoardMobile.test.tsx</files>
  <read_first>
    - src/components/board/Board.tsx:90-98 (expanded init + resolveBoardLayout call), :481-484 (overlay dispatch), :418/:495 (the RESPONSIVE `mode` collision — use `boardMode`) — PATTERNS.md §Board.tsx
    - src/components/board/BoardMobile.tsx (MOBILE_ORDER 24-31, renderBody 81-107, LAYOUT_BY_ID 48-50, card label map 121-129) — PATTERNS.md §BoardMobile.tsx
    - src/components/board/GroupFrameOverlay.tsx:13 (EMPTY_STATE_COPY), :42 (ARIA_LABEL)
    - src/components/board/__tests__/BoardMobile.test.tsx (extend)
    - .planning/phases/02-remix-mode-one-board-two-config/02-UI-SPEC.md §6 (mobile order), §Accessibility (aria-labels)
  </read_first>
  <behavior>
    - In remix-resolved layout, Board overlay renders DecodeShellNode where id==='decode' and AdaptShellNode where id==='adapt'
    - MOBILE_ORDER_REMIX === ['input','decode','audience','adapt','content-analysis','engine']; MOBILE_ORDER_SCORE unchanged === ['input','verdict','audience','actions','content-analysis','engine']
    - BoardMobile renderBody returns DecodeShellNode for 'decode' and AdaptShellNode for 'adapt'
    - Mobile decode/adapt cards get a label without crashing on LAYOUT_BY_ID.get('decode') (which is undefined)
    - expanded[layout.id] never undefined-crashes for new ids
  </behavior>
  <action>
    Board.tsx: (1) add overlay dispatch cases at :481-482 — `{layout.id === 'decode' && <DecodeShellNode />}` and `{layout.id === 'adapt' && <AdaptShellNode />}` (shells take no props). (2) Guard `expanded[layout.id] ?? true` where expanded is read (it's keyed off GROUP_FRAMES score ids). (3) Accept an optional intent value into resolveBoardLayout — add a local `const boardMode = ...` (Plan 03 will source it from stream/permalink; for now default `'score'` and pass `resolveBoardLayout(measuredH, boardMode)`). MUST name it `boardMode`, NOT `mode` — `mode` already = the responsive 'cards'|'desktop' view mode at :418/:495.
    BoardMobile.tsx: split MOBILE_ORDER into MOBILE_ORDER_SCORE + MOBILE_ORDER_REMIX (UI-SPEC §6), select by an intent param (default score; Plan 03 threads the live value). Add renderBody cases `case 'decode': return <DecodeShellNode />; case 'adapt': return <AdaptShellNode />;`. For the card label, supply a fallback for the GROUP_FRAMES-absent ids — `LAYOUT_BY_ID.get(id)?.label ?? (id==='decode' ? 'Decode' : id==='adapt' ? 'Adapt' : id)` (or a small MOBILE_LABELS record). MobileFrameCard chrome unchanged.
    GroupFrameOverlay.tsx: add 'decode'/'adapt' keys to ARIA_LABEL ("Decode frame — structural breakdown" / "Adapt frame — niche concepts", UI-SPEC §Accessibility) and to EMPTY_STATE_COPY with empty titles (so the empty-state block skips — the shell owns the descriptor).
    Extend BoardMobile.test.tsx asserting remix order + renderBody cases + score order unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/components/board/__tests__/BoardMobile.test.tsx && npx tsc --noEmit 2>&1 | grep -v node_modules | grep -i error | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "layout.id === 'decode'" src/components/board/Board.tsx` == 1 and `'adapt'` == 1
    - Board.tsx uses `boardMode` for intent (NOT `mode`): `grep -c "boardMode" src/components/board/Board.tsx` ≥ 1; the responsive `mode === 'cards'` at :418 is untouched
    - BoardMobile.tsx contains MOBILE_ORDER_REMIX === `['input','decode','audience','adapt','content-analysis','engine']`
    - GroupFrameOverlay ARIA_LABEL has 'decode' and 'adapt' keys
    - BoardMobile.test.tsx asserts both score (unchanged) and remix order; tests green; `npx tsc --noEmit` no new errors
  </acceptance_criteria>
  <done>Desktop overlay + mobile stack render the decode/adapt shells in the correct positions in remix layout, score layout untouched, no undefined-id crashes; tests green and typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted row → board render | `mode` (read in Plan 03) selects the frame set; a confused/missing value would mis-render the board config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering / Confusion | resolveBoardLayout mode param | mitigate | Default param `'score'` + closed `'score'|'remix'` type; any non-remix value yields the unchanged score frame set (fail-safe to the no-regression path) |
| T-02-07 | Denial of Service (crash) | computePresetTargets / LAYOUT_BY_ID / expanded lookups on new ids | mitigate | `?? byId.decode`, `?? true`, `?? label` guards so a remix layout never reads undefined.x (Pitfalls 2,3) — board cannot crash on a remix permalink |
| T-02-08 | Tampering | Decode/Adapt shell content | accept | Shells are static DOM with hardcoded copy and no inputs/data flow this phase — no injection surface |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx vitest run src/components/board/__tests__/board-constants.test.ts src/components/board/__tests__/BoardMobile.test.tsx src/components/board/decode/__tests__/DecodeShellNode.test.tsx src/components/board/adapt/__tests__/AdaptShellNode.test.tsx` — green
- `npx tsc --noEmit` — no new errors
- Manual (deferred to phase gate / Plan 03): Konva canvas visual reflow + mobile <768px order screenshot (02-VALIDATION Manual-Only)
</verification>

<success_criteria>
- resolveBoardLayout returns a remix frame set (decode/adapt at verdict/actions bounds, 1:1) while the score-mode return is byte-identical (existing suite green)
- DecodeShellNode/AdaptShellNode are DOM components rendering the locked descriptors, dispatched in Board overlay + BoardMobile renderBody
- Mobile remix order matches UI-SPEC §6; score order unchanged
- No undefined-id crash in camera presets, expanded, or LAYOUT_BY_ID
</success_criteria>

<output>
Create `.planning/phases/02-remix-mode-one-board-two-config/02-02-SUMMARY.md` when done
</output>
