---
phase: 02-remix-mode-one-board-two-config
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/components/app/content-form.tsx
  - src/components/app/__tests__/content-form.test.tsx
  - src/hooks/queries/use-analysis-stream.ts
  - src/components/board/Board.tsx
  - src/components/board/BoardMobile.tsx
autonomous: true
requirements: [REMIX-01, REMIX-02]
user_setup: []

must_haves:
  truths:
    - "The input shows an explicit two-segment Score/Remix selector above the input tabs; default is Score"
    - "Selecting Remix hides the Text tab from the DOM and suppresses the caption field; Video+Link only, Video default"
    - "Flipping to Remix while the Text tab is active resets activeTab to video_upload and syncs input_mode"
    - "A remix submission carries mode='remix' through to the POST body (form → AnalysisStreamInput → /api/analyze)"
    - "The live board renders the remix frame set immediately on a remix submission (boardMode sourced from the result/submitted mode)"
    - "Mode survives permalink reload: /analyze/[id] renders the persisted row's mode frame set (decode+adapt for remix)"
    - "Selecting Score keeps all three tabs + caption; the score board renders verdict+actions unchanged"
  artifacts:
    - path: "src/components/app/content-form.tsx"
      provides: "intent segmented control + remix tab coupling + mode field on ContentFormData"
      contains: "Remix a viral video"
    - path: "src/hooks/queries/use-analysis-stream.ts"
      provides: "AnalysisStreamInput carries mode forwarded into the POST body"
      contains: "mode?:"
    - path: "src/components/board/Board.tsx"
      provides: "boardMode derived from stream.result.mode / permalink row.mode, fed to resolveBoardLayout + BoardMobile"
      contains: "boardMode"
  key_links:
    - from: "src/components/app/content-form.tsx"
      to: "AnalysisStreamInput"
      via: "ContentFormData.mode → handleContentSubmit stream.start({... mode: data.mode})"
      pattern: "mode: data\\.mode"
    - from: "src/components/board/Board.tsx"
      to: "resolveBoardLayout"
      via: "boardMode from stream.result.mode ?? permalink row.mode ?? 'score'"
      pattern: "resolveBoardLayout\\(measuredH, boardMode\\)"
---

<objective>
Build the intent selector and close the mode loop: a two-segment Score/Remix control above the input tabs, remix tab-coupling (Video+Link only, no caption), thread `mode` through the form → stream payload → API, and derive `boardMode` on the board from the live submitted value and the permalink row so the live board and a reloaded `/analyze/[id]` agree on which config to render.

Purpose: REMIX-01 (explicit selector → remix path + persisted mode) and the REMIX-02 wiring half (live + permalink board agree on mode). Depends on Plan 01's persisted/validated `mode` and Plan 02's mode-aware resolver/mobile order.
Output: intent segmented control + coupling, AnalysisStreamInput field, explicit submit threading, boardMode derivation in Board + BoardMobile, content-form component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-remix-mode-one-board-two-config/02-RESEARCH.md
@.planning/phases/02-remix-mode-one-board-two-config/02-PATTERNS.md
@.planning/phases/02-remix-mode-one-board-two-config/02-UI-SPEC.md
@.planning/phases/02-remix-mode-one-board-two-config/02-01-SUMMARY.md
@.planning/phases/02-remix-mode-one-board-two-config/02-02-SUMMARY.md

<interfaces>
<!-- From Plans 01/02 (read their SUMMARYs) + this session's reads. -->

content-form.tsx:
  ContentFormData (112-123): input_mode + caption/niche/.../tiktok_url/video_file/video_caption/... — ADD `mode: "score" | "remix"`.
  MODE_CONFIG (132-136): [{video_upload,Video},{text,Text},{tiktok_url,URL}] — filter out text when remix (D-04).
  PLACEHOLDERS (138-142): tiktok_url placeholder — override to "Paste a TikTok URL to decode..." in remix (UI-SPEC §Copywriting).
  activeTab useState (155-157) default video_upload (or text on result route). formData useState (167-178) — add `mode: "score"` (D-02).
  handleTabChange (220-224): sets activeTab + formData.input_mode together — mirror for the coupling reset.
  Video caption textarea is conditional on formData.video_file — suppress entirely when remix (D-05).
  Selected-tab class idiom (401-405): `bg-white/[0.08] text-foreground px-2.5` selected vs `text-foreground-muted ... px-1.5`.

use-analysis-stream.ts:
  AnalysisStreamInput (59-68) — closed interface (NOT a spread). ADD `mode?: "score" | "remix"`.
  mutationFn body: `JSON.stringify(input)` (:323) — forwards mode verbatim, no other change.

Board.tsx (from Plan 02 — boardMode currently hardcoded 'score'):
  handleContentSubmit (282-312) builds stream.start({...}) with an EXPLICIT field allowlist (does NOT spread `data`) — ADD `mode: data.mode` (Pitfall 7).
  stream.result is cast inline (397-402) — extend the cast to include `mode?: 'score'|'remix'`.
  permalinkQuery.data (132-142) is the /api/analysis/[id] row (select('*') → carries mode from Plan 01).
  NAMING: `mode` at :418/:495 = responsive view mode ('cards'|'desktop'). Intent value MUST be `boardMode`.

BoardMobile.tsx (from Plan 02): MOBILE_ORDER selection takes an intent param — Board passes boardMode down.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Intent selector + remix coupling + mode on ContentFormData (Wave-0 test first)</name>
  <files>src/components/app/content-form.tsx, src/components/app/__tests__/content-form.test.tsx</files>
  <read_first>
    - src/components/app/content-form.tsx (ContentFormData 112-123, MODE_CONFIG 132-136, PLACEHOLDERS 138-142, activeTab 155-157, formData 167-178, handleTabChange 220-224, tab render 390-420, video caption textarea conditional) — PATTERNS.md §content-form.tsx
    - .planning/phases/02-remix-mode-one-board-two-config/02-UI-SPEC.md §1 (segmented control spec), §2 (coupling), §Copywriting, §Accessibility
    - .planning/phases/02-remix-mode-one-board-two-config/02-RESEARCH.md Pitfall 8 (tab-coupling desync)
    - CLAUDE.md §Raycast Design Language Rules (selected = bg-white/[0.08] NOT coral; 8px radius; 6% border)
  </read_first>
  <behavior>
    - Renders a container with role="tablist" holding two role="tab" segments labelled "Score my content" and "Remix a viral video"; Score has aria-selected=true on mount (D-02)
    - Clicking Remix: the Text tab is absent from the DOM; only Video + Link tabs render (Video first); no caption textarea present
    - Clicking Remix while activeTab==='text': activeTab becomes 'video_upload' and formData.input_mode==='video_upload' (no empty input area — Pitfall 8)
    - Clicking back to Score: all three tabs present again; caption textarea returns for video_upload
    - In remix, the URL tab placeholder reads "Paste a TikTok URL to decode..."
    - onSubmit payload includes mode: 'remix' after selecting Remix; mode: 'score' by default
    - Selected segment fill is white-transparency (bg-white/[0.08]), not coral
  </behavior>
  <action>
    Tests first (RED): create src/components/app/__tests__/content-form.test.tsx (RTL) covering the behaviors above. Then implement (GREEN):
    1. ContentFormData: add `mode: "score" | "remix"`. formData useState default `mode: "score"` (D-02).
    2. Intent selector: render above the existing tab bar — a two-segment `flex w-full` pill, container `bg-white/[0.03] border border-white/[0.06] rounded-md` 36px tall, each segment `flex-1` 0px gap `rounded-md px-4 py-2`, selected `bg-white/[0.08] text-foreground`, unselected `text-foreground-muted hover:bg-white/[0.04]`, focus-visible coral outline. role="tablist" + role="tab" + aria-selected + arrow/Enter/Space keyboard (UI-SPEC §1/§Accessibility). Hold intent in local `useState<'score'|'remix'>` default 'score'; on change set formData.mode. Name the state `intent` (NOT `mode` — avoid the InputMode param name collision in handleTabChange:220).
    3. Coupling (D-04): when intent==='remix', filter MODE_CONFIG to `value !== 'text'`. On the flip to remix, if activeTab==='text' reset activeTab→'video_upload' and formData.input_mode→'video_upload' (mirror handleTabChange body — Pitfall 8).
    4. Caption suppression (D-05): short-circuit the video caption textarea render when intent==='remix'.
    5. URL placeholder: in remix, override the tiktok_url placeholder to "Paste a TikTok URL to decode..." (UI-SPEC §Copywriting).
    Score mode (intent==='score') is entirely unchanged (D-03).
  </action>
  <verify>
    <automated>npx vitest run src/components/app/__tests__/content-form.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - content-form.test.tsx exists; asserts default Score, remix hides Text + caption, coupling reset, mode in onSubmit payload — all green
    - `grep -c "Remix a viral video" src/components/app/content-form.tsx` ≥ 1 and `Score my content` ≥ 1
    - ContentFormData contains `mode: "score" | "remix"`; formData default has `mode: "score"`
    - `grep -c "role=\"tablist\"" src/components/app/content-form.tsx` ≥ 1
    - selected segment uses `bg-white/[0.08]` (grep present), no coral/accent class on the selected fill
    - URL placeholder override string "Paste a TikTok URL to decode..." present
  </acceptance_criteria>
  <done>Intent selector renders with Raycast styling + a11y, remix coupling hides Text/caption and resets the tab safely, mode threads onto ContentFormData; score path unchanged; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread mode form → stream payload → API (AnalysisStreamInput + handleContentSubmit)</name>
  <files>src/hooks/queries/use-analysis-stream.ts, src/components/board/Board.tsx</files>
  <read_first>
    - src/hooks/queries/use-analysis-stream.ts:59-68 (AnalysisStreamInput closed interface), :300/:323 (mutationFn body JSON.stringify) — PATTERNS.md §use-analysis-stream.ts
    - src/components/board/Board.tsx:282-312 (handleContentSubmit explicit allowlist — does NOT spread data) — PATTERNS.md §Board.tsx + RESEARCH Pitfall 7
    - .planning/phases/02-remix-mode-one-board-two-config/02-01-SUMMARY.md (confirm route.ts persists validated.mode + schema accepts mode)
  </read_first>
  <behavior>
    - AnalysisStreamInput type accepts an optional mode field
    - handleContentSubmit's stream.start({...}) object includes mode: data.mode (explicit, not via spread)
    - A remix submission results in the POST body carrying mode:'remix' (the mutationFn JSON.stringify forwards it unchanged)
  </behavior>
  <action>
    use-analysis-stream.ts: add `mode?: "score" | "remix";` to the AnalysisStreamInput interface (59-68). No other change — `body: JSON.stringify(input)` (:323) forwards it verbatim (Plan 01 already validates+persists it server-side).
    Board.tsx handleContentSubmit (282-312): add `mode: data.mode,` as an explicit key in the stream.start({...}) object (RESEARCH Pitfall 7 — the object is a field allowlist, NOT `...data`; missing this line silently persists the 'score' default).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v node_modules | grep -i error | head; grep -c "mode: data.mode" src/components/board/Board.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mode?:" src/hooks/queries/use-analysis-stream.ts` ≥ 1 (AnalysisStreamInput field)
    - `grep -c "mode: data.mode" src/components/board/Board.tsx` == 1 (explicit submit threading)
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>mode is carried form → stream input type → POST body; the three Pitfall-7 touch points (ContentFormData from Task 1, AnalysisStreamInput, explicit submit key) are all present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Derive boardMode (live + permalink) and feed the resolver + mobile order</name>
  <files>src/components/board/Board.tsx, src/components/board/BoardMobile.tsx</files>
  <read_first>
    - src/components/board/Board.tsx:98 (resolveBoardLayout call), :132-147 (permalinkQuery + stream initialData), :397-409 (stream.result inline cast), :418-425 (BoardMobile branch), :435/:481-484 (frame map) — PATTERNS.md §Board.tsx
    - src/components/board/BoardMobile.tsx (MOBILE_ORDER selection from Plan 02 — takes an intent param)
    - .planning/phases/02-remix-mode-one-board-two-config/02-02-SUMMARY.md (resolveBoardLayout signature + MOBILE_ORDER_* names)
    - .planning/phases/02-remix-mode-one-board-two-config/02-RESEARCH.md Pitfall 4 + Open Q1 (where the live board reads mode)
  </read_first>
  <behavior>
    - On a live remix submission, Board renders the remix frame set (decode+adapt) without reload — boardMode resolves from the submitted/result mode
    - On `/analyze/[id]` direct nav (permalink), Board renders the frame set matching the persisted row's mode (remix row → decode+adapt; score row → verdict+actions)
    - A score-mode analysis (live or permalink) renders verdict+actions (no regression)
    - BoardMobile receives boardMode and uses MOBILE_ORDER_REMIX for remix, MOBILE_ORDER_SCORE for score
  </behavior>
  <action>
    Board.tsx: derive `const boardMode: 'score' | 'remix' = (stream.result as { mode?: 'score'|'remix' } | null)?.mode ?? (permalinkQuery.data as { mode?: 'score'|'remix' } | null)?.mode ?? 'score';`. Because handleContentSubmit now sends mode (Task 2), the server persists it and the SSE/permalink result echoes it back (Plan 01 added mode to PredictionResult + select('*') carries the row field) — so both the live and reload paths agree (REMIX-02 crit 5; Pitfall 4 / Open Q1). Replace the Plan-02 placeholder: `resolveBoardLayout(measuredH, boardMode)` at :98 (add boardMode to the useMemo deps). Pass boardMode into the BoardMobile branch (:419) so it selects MOBILE_ORDER_REMIX/SCORE. Extend the inline stream.result cast (:397-402) to include `mode?: 'score'|'remix'`. Keep using `boardMode` — never reuse the responsive `mode` (:418/:495).
    Note: if the SSE complete payload does not echo mode (A4 risk), the submitted value still reaches the row via Task 2's POST and returns on the permalink read; the live pre-reload board falls back to 'score' only if neither result nor row carries it — acceptable since the submit→row→result chain carries it. Confirm against 02-01-SUMMARY whether PredictionResult echoes mode; if it does not, additionally hold the submitted intent in local Board state set in handleContentSubmit and prefer it for the live path (Open Q1 recommendation).
  </action>
  <verify>
    <automated>npx vitest run src/components/board/__tests__/board-constants.test.ts src/components/board/__tests__/BoardMobile.test.tsx && npx tsc --noEmit 2>&1 | grep -v node_modules | grep -i error | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "resolveBoardLayout(measuredH, boardMode)" src/components/board/Board.tsx` == 1
    - boardMode derivation reads both `stream.result` mode and `permalinkQuery.data` mode with a `?? 'score'` fallback
    - BoardMobile receives boardMode (grep the prop passed in the BoardMobile branch)
    - The responsive `mode === 'cards'` at :418 and ViewModeToggle at :495 are untouched (still `mode`, not boardMode)
    - `npx tsc --noEmit` no new errors; board-constants + BoardMobile suites green
  </acceptance_criteria>
  <done>boardMode is derived from the live result and the permalink row and drives both the desktop resolver and the mobile order; live and reload agree; score path unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → submit payload | User-selected intent crosses into the stream POST body (validated server-side in Plan 01) |
| persisted row → board config | `mode` read back selects the frame set on live + permalink render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering (logic) | remix+text invalid state via UI | mitigate | UI makes it unrepresentable (Text removed from DOM in remix + activeTab reset, Pitfall 8); the server `.refine` from Plan 01 (T-02-02) is the real boundary |
| T-02-10 | Confusion / Spoofing | mode mismatch live vs permalink | mitigate | Single derivation `stream.result.mode ?? row.mode ?? 'score'`; the persisted row is the source of truth on reload (D-15), guaranteeing live + reload render the same config (REMIX-02 crit 5) |
| T-02-11 | Tampering | client sends arbitrary mode in POST body | mitigate | Re-validated by AnalysisInputSchema enum + DB CHECK (Plan 01 T-02-01) — the client value is never trusted server-side |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx vitest run src/components/app/__tests__/content-form.test.tsx src/components/board/__tests__/board-constants.test.ts src/components/board/__tests__/BoardMobile.test.tsx` — green
- `npx tsc --noEmit` — no new errors
- Phase-gate full suite + build: `npx vitest run && npm run build`
- Manual (02-VALIDATION Manual-Only): submit a Remix URL → screenshot desktop Konva board shows decode+adapt reflow; resize <768px → decode+adapt cards in [input,decode,audience,adapt,content-analysis,engine] order; reload the /analyze/[id] permalink → same remix config
</verification>

<success_criteria>
- Explicit Score/Remix selector above the tabs, default Score, no auto-detect; Remix shows Video+Link only, no caption, and routes a row with mode='remix' (REMIX-01)
- mode threads form → AnalysisStreamInput → /api/analyze with no dropped touch point
- Live remix board and a reloaded /analyze/[id] both render decode+adapt (desktop + mobile); score path renders verdict+actions unchanged (REMIX-02 crit 1-5)
</success_criteria>

<output>
Create `.planning/phases/02-remix-mode-one-board-two-config/02-03-SUMMARY.md` when done
</output>
