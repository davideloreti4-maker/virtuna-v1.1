---
phase: 02-remix-mode-one-board-two-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260601000000_add_mode_to_analysis_results.sql
  - src/types/database.types.ts
  - src/lib/engine/types.ts
  - src/lib/engine/cache/prediction-cache.ts
  - src/lib/engine/cache/__tests__/prediction-cache.test.ts
  - src/lib/engine/__tests__/analysis-input-schema.test.ts
  - src/app/api/analyze/route.ts
autonomous: false
requirements: [REMIX-01, REMIX-02]
user_setup: []

must_haves:
  truths:
    - "A remix submission persists a row with mode='remix'"
    - "A score submission persists a row with mode='score' (default, no regression)"
    - "AnalysisInputSchema rejects {mode:'remix', input_mode:'text'}"
    - "computeContentHash of the same URL differs between score and remix"
    - "computeContentHash for every score-mode input is byte-identical to pre-change output"
    - "analysis_results.mode flows through the /api/analysis/[id] select('*') read with zero route change"
  artifacts:
    - path: "supabase/migrations/20260601000000_add_mode_to_analysis_results.sql"
      provides: "additive mode column TEXT NOT NULL DEFAULT 'score' CHECK (mode IN ('score','remix')) + historical backfill"
      contains: "ADD COLUMN IF NOT EXISTS mode"
    - path: "src/lib/engine/types.ts"
      provides: "AnalysisInputSchema mode enum + remix-not-text refine; PredictionResult optional mode"
      contains: "z.enum([\"score\", \"remix\"])"
    - path: "src/lib/engine/cache/prediction-cache.ts"
      provides: "score-path-safe mode fold in computeContentHash (segment only when mode==='remix')"
      contains: "::mode=remix"
    - path: "src/app/api/analyze/route.ts"
      provides: "mode persisted at both INSERT sites (buildInsertRow + placeholder)"
      contains: "mode: validated.mode"
    - path: "src/types/database.types.ts"
      provides: "analysis_results Row/Insert/Update carry mode"
      contains: "mode: string"
  key_links:
    - from: "src/app/api/analyze/route.ts"
      to: "analysis_results.mode column"
      via: "buildInsertRow + placeholder INSERT object"
      pattern: "mode: validated\\.mode"
    - from: "src/lib/engine/cache/prediction-cache.ts"
      to: "content_hash cache identity"
      via: "createHash update before each digest, remix-only"
      pattern: "input\\.mode === \"remix\""
---

<objective>
Build the data spine for remix mode: add the `mode` column to `analysis_results`, push the migration to the live DB, thread `mode` through the Zod boundary schema, fold it into the content hash (score-path-safe), and persist it at both API INSERT sites. This is the foundation Plans 02/03 read from — without the persisted `mode` column and the schema field, the board cannot rehydrate intent on permalink reload and the cache would collapse score/remix entries.

Purpose: Make `mode` a first-class persisted, validated, cache-distinct field (D-12/D-13/D-14, REMIX-02 success criterion 5; REMIX-01 persistence half).
Output: One migration (applied to DB), updated generated types, Zod schema + refine, hash fold, two INSERT sites, Wave-0 schema + hash tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-remix-mode-one-board-two-config/02-RESEARCH.md
@.planning/phases/02-remix-mode-one-board-two-config/02-PATTERNS.md

<interfaces>
<!-- Extracted this session. Executor uses these directly — no exploration needed. -->

prediction-cache.ts:31-47 — computeContentHash has THREE early returns (one per input_mode branch):
  line 40: `return h.digest("hex");`  (video_upload branch)
  line 44: `return h.digest("hex");`  (tiktok_url branch)
  line 47: `return h.digest("hex");`  (text/fallback branch)
  Signature: `computeContentHash(input: AnalysisInput, videoBuffer?: Buffer): string`

route.ts:402 buildInsertRow — sets `input_mode: finalResult.input_mode` at line 432.
route.ts:585 placeholder `.insert({ ... input_mode: validated.input_mode ... })`.
route.ts:337 `validated = AnalysisInputSchema.parse(body);` — `validated.mode` available after schema gains it.
route.ts:530 JSON-branch insert spreads `...buildInsertRow(...)` — inherits mode from buildInsertRow.

analysis/[id]/route.ts:28 uses `.select("*")` → new mode column flows through with NO edit to that route.

types.ts:135-167 — AnalysisInputSchema is `z.object({...}).refine(...)`; `AnalysisInput = z.infer<typeof AnalysisInputSchema>`. PredictionResult type defined in same file.

Analog migration: supabase/migrations/20260528113615_add_heatmap_to_analysis_results.sql (additive ALTER + COMMENT, public. prefix).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 tests — schema mode enum/refine + hash mode fold</name>
  <files>src/lib/engine/__tests__/analysis-input-schema.test.ts, src/lib/engine/cache/__tests__/prediction-cache.test.ts</files>
  <read_first>
    - src/lib/engine/types.ts:135-167 (AnalysisInputSchema, refine, PredictionResult) — the schema being tested
    - src/lib/engine/cache/prediction-cache.ts:31-47 (computeContentHash, three early returns) — PATTERNS.md §prediction-cache
    - src/lib/engine/cache/__tests__/prediction-cache.test.ts (existing suite — extend, do not rewrite)
    - .planning/phases/02-remix-mode-one-board-two-config/02-RESEARCH.md Pitfall 5 (score-path-safe) + Pitfall 6 (remix+text refine)
  </read_first>
  <behavior>
    - AnalysisInputSchema.parse({input_mode:'tiktok_url', tiktok_url:'https://...', mode:'remix'}) → mode==='remix'
    - AnalysisInputSchema.parse(body without mode) → mode==='score' (default)
    - AnalysisInputSchema.parse({mode:'remix', input_mode:'text', content_text:'x'}) → throws (refine rejects remix+text)
    - AnalysisInputSchema.parse({mode:'bogus', ...}) → throws (enum)
    - computeContentHash(scoreInput) === computeContentHash(scoreInput) and is byte-identical to the value produced WITHOUT a mode field (capture a known fixture hash string and assert equality)
    - computeContentHash({...urlInput, mode:'remix'}) !== computeContentHash({...urlInput, mode:'score'})
  </behavior>
  <action>
    Create src/lib/engine/__tests__/analysis-input-schema.test.ts with the enum/default/refine cases above (import AnalysisInputSchema from @/lib/engine/types). Extend the existing prediction-cache.test.ts with two new describe blocks: (a) score-path-stability — assert a fixed score-mode tiktok_url input hashes to a hardcoded expected hex (compute it from the CURRENT code first and paste the literal, so any future mode-leak into the score path fails the test); (b) mode-distinctness — same url, mode 'score' vs 'remix' produce different hashes. These tests RED now (schema has no mode field, hash ignores mode). Do NOT modify production code in this task.
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/analysis-input-schema.test.ts src/lib/engine/cache/__tests__/prediction-cache.test.ts 2>&1 | grep -E "fail|FAIL" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/engine/__tests__/analysis-input-schema.test.ts exists with ≥4 assertions
    - prediction-cache.test.ts contains a hardcoded score-mode expected-hash literal (grep `digest`/hex literal present)
    - `npx vitest run` on both files shows failing assertions for the new cases (RED state confirmed before Task 2)
  </acceptance_criteria>
  <done>New schema test + extended cache test exist and fail against current (un-modified) production code, proving the gates are real.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration + schema + hash + types + API persistence (GREEN)</name>
  <read_first>
    - supabase/migrations/20260528113615_add_heatmap_to_analysis_results.sql (analog ADD COLUMN) — PATTERNS.md §migration
    - src/lib/engine/types.ts:135-167 (schema + refine + PredictionResult) — PATTERNS.md §types.ts
    - src/lib/engine/cache/prediction-cache.ts:31-47 — PATTERNS.md §prediction-cache (note the THREE early returns)
    - src/app/api/analyze/route.ts:402-485 (buildInsertRow), :585-601 (placeholder INSERT) — PATTERNS.md §route.ts
    - src/types/database.types.ts analysis_results Row(~181-232)/Insert/Update — PATTERNS.md §database.types.ts
    - .planning/phases/02-remix-mode-one-board-two-config/02-RESEARCH.md Pitfalls 5,6,7
  </read_first>
  <action>
    1. Migration: create supabase/migrations/20260601000000_add_mode_to_analysis_results.sql cloning the heatmap analog shape — `ALTER TABLE public.analysis_results ADD COLUMN IF NOT EXISTS mode TEXT NOT NULL DEFAULT 'score' CHECK (mode IN ('score','remix'));` plus a COMMENT ON COLUMN describing intent vs input_mode (D-12). DEFAULT 'score' backfills historical rows in the same ADD COLUMN (D-13) — NO separate UPDATE. Pick the timestamp 20260601000000 (after latest 20260531000003).
    2. types.ts: add `mode: z.enum(["score", "remix"]).default("score")` to the AnalysisInputSchema object; add a SECOND `.refine((d) => !(d.mode === "remix" && d.input_mode === "text"), { message: "Remix mode requires a video or link source, not text" })` chained after the existing refine (Pitfall 6, D-04 server-side). Add optional `mode?: "score" | "remix"` to the PredictionResult type (Pitfall 4 / A2 — lets the value carry row→board without casts).
    3. prediction-cache.ts: in computeContentHash, add `if (input.mode === "remix") h.update("::mode=remix");` BEFORE each of the three `return h.digest("hex")` calls (lines 40,44,47) — strategy 5a, segment ONLY in remix so every score-mode hash stays byte-identical (Pitfall 5). cacheKey and lookupPredictionCache unchanged.
    4. route.ts: add `mode: validated.mode` to buildInsertRow's returned object (alongside `input_mode: finalResult.input_mode` at :432 — use `validated.mode` not finalResult) AND to the placeholder `.insert({...})` at :585. The JSON-branch insert at :530 inherits via the spread. Do NOT add a second rate limiter.
    5. database.types.ts: add `mode: string` to analysis_results Row (after input_mode), `mode?: string` to Insert and Update. (Hand-edit per RESEARCH Open Q3; Task 3 verifies against live DB.)
    Tests from Task 1 must now pass (GREEN). Do NOT touch GROUP_FRAMES, board files, or content-form (other plans).
  </action>
  <verify>
    <automated>npx vitest run src/lib/engine/__tests__/analysis-input-schema.test.ts src/lib/engine/cache/__tests__/prediction-cache.test.ts && npx tsc --noEmit 2>&1 | grep -v "node_modules" | grep -i "error" | head</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260601000000_add_mode_to_analysis_results.sql contains `mode TEXT NOT NULL DEFAULT 'score'` and `CHECK (mode IN ('score','remix'))`
    - `grep -c "::mode=remix" src/lib/engine/cache/prediction-cache.ts` ≥ 1; the line is guarded by `input.mode === "remix"`
    - `grep -c "mode: validated.mode" src/app/api/analyze/route.ts` == 2 (buildInsertRow + placeholder)
    - types.ts contains `z.enum(["score", "remix"])` and a refine message `Remix mode requires a video or link source, not text`
    - Both Task-1 test files pass (GREEN); `npx tsc --noEmit` reports no new errors
    - score-mode hardcoded-hash assertion still green (proves score path byte-identical)
  </acceptance_criteria>
  <done>mode validated at the boundary, folded into the hash remix-only, persisted at both insert sites; migration file written; all Task-1 tests green; typecheck clean.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Push migration to live DB + verify column</name>
  <files>supabase/migrations/20260601000000_add_mode_to_analysis_results.sql, src/types/database.types.ts</files>
  <action>Apply the Task-2 migration to the linked Supabase project (Supabase MCP apply_migration with the migration SQL, name add_mode_to_analysis_results; or supabase db push with SUPABASE_ACCESS_TOKEN). Then confirm the column per the verification steps below. This task is human-gated because the push may require interactive auth that cannot be suppressed in this terminal.</action>
  <what-built>Migration file 20260601000000_add_mode_to_analysis_results.sql written in Task 2. Build/typecheck pass without the push because types come from database.types.ts (a static artifact), NOT the live DB — so verification would be a false-positive until the column actually exists on the server.</what-built>
  <how-to-verify>
    Apply the migration to the linked Supabase project, then confirm the column. Preferred path (non-TTY safe): use the Supabase MCP `apply_migration` tool with the SQL from the new migration file (name `add_mode_to_analysis_results`). Fallback CLI: `supabase db push` with `SUPABASE_ACCESS_TOKEN` exported (interactive prompts cannot be suppressed in this terminal — hence this task is autonomous:false / blocking-human).
    After applying, verify:
      1. Column exists: query `select column_name, data_type, column_default, is_nullable from information_schema.columns where table_name='analysis_results' and column_name='mode';` → one row, default `'score'::text`, NOT NULL.
      2. Backfill: `select count(*) from analysis_results where mode is null;` → 0.
      3. CHECK works: an INSERT/UPDATE attempting `mode='bogus'` is rejected.
      4. Optional: regenerate types via Supabase MCP `generate_typescript_types` and diff against the hand-edited database.types.ts; reconcile if the generated shape differs (Task 2 hand-edit is the fallback per RESEARCH Open Q3).
  </how-to-verify>
  <acceptance_criteria>
    - information_schema shows analysis_results.mode TEXT NOT NULL DEFAULT 'score'
    - count of NULL mode rows == 0 (historical backfill applied)
    - mode='bogus' write is rejected by the CHECK constraint
    - database.types.ts agrees with the live column shape (regen-diff clean or hand-edit confirmed)
  </acceptance_criteria>
  <resume-signal>Type "applied" once the column is confirmed in the live DB, or paste the error if the push failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/analyze | Untrusted JSON body (incl. client-supplied `mode`) crosses into server validation/persistence |
| app → analysis_results | Row write — `mode` becomes durable, cache-identity-bearing state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `mode` field in /api/analyze body | mitigate | `z.enum(["score","remix"])` in AnalysisInputSchema rejects any other value; DB `CHECK (mode IN ('score','remix'))` is defense-in-depth (ASVS V5) |
| T-02-02 | Tampering (logic) | remix+text dead-end payload | mitigate | Second `.refine` rejects `mode==='remix' && input_mode==='text'` server-side (Pitfall 6) — UI hiding Text is not the security boundary |
| T-02-03 | Spoofing / Confusion | content-hash cache key | mitigate | Mode folded into hash remix-only (strategy 5a) so a remix-decode and a score of the same URL never collapse into one cache entry (REMIX-02 crit 5); score hashes unchanged → no orphaning |
| T-02-04 | Information Disclosure | cross-tenant cache read | accept | Existing `cacheKey`/`lookupPredictionCache` filter `.eq('user_id', userId)`; `mode` adds no new access surface (ASVS V4) — unchanged |
| T-02-05 | Denial of Service | remix paste-spam cost exhaustion | accept | Existing mode-agnostic daily limiter (route.ts:296-314, STATE Plan 03) covers the remix path; adding a second limiter is an explicit anti-pattern |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: not applicable; REQUIREMENTS Out-of-Scope rejects new deps) |
</threat_model>

<verification>
- `npx vitest run src/lib/engine/__tests__/analysis-input-schema.test.ts src/lib/engine/cache/__tests__/prediction-cache.test.ts` — green
- `npx tsc --noEmit` — no new errors
- Live DB column confirmed (Task 3 checkpoint)
- `grep -c "mode: validated.mode" src/app/api/analyze/route.ts` == 2
</verification>

<success_criteria>
- analysis_results has a `mode` column, TEXT NOT NULL DEFAULT 'score', CHECK constrained, applied to the live DB with historical rows backfilled to 'score'
- AnalysisInputSchema accepts `mode`, defaults to 'score', rejects remix+text and invalid enum values
- computeContentHash distinguishes score vs remix for the same URL while keeping every score-mode hash byte-identical
- mode persisted at both INSERT sites; `/api/analysis/[id]` select('*') carries it through unchanged
</success_criteria>

<output>
Create `.planning/phases/02-remix-mode-one-board-two-config/02-01-SUMMARY.md` when done
</output>
