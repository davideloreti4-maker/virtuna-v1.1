---
phase: 01
plan: 08
type: execute
wave: 5
depends_on: ["01-02", "01-03", "01-07"]
files_modified:
  - src/app/(app)/analyze/page.tsx
  - src/app/(app)/analyze/analyze-client.tsx
  - src/app/(app)/analyze/[id]/page.tsx
  - src/app/(app)/analyze/[id]/result-card.tsx
  - src/app/(app)/analyze/[id]/result-card-skeleton.tsx
  - src/app/(app)/analyze/__tests__/result-card.test.tsx
  - e2e/result-surface-stream.spec.ts
autonomous: true
requirements:
  - R2.1
threat_refs:
  - T-01-RC-XSS
  - T-01-RC-IDOR
tags:
  - route-scaffold
  - result-card
  - skeleton
  - e2e
  - r2-1

must_haves:
  truths:
    - "GET /analyze renders the form page (reuses existing ContentForm). Auth-gated via (app)/layout.tsx middleware."
    - "Submitting on /analyze starts analysis via useAnalysisStream.start(input), captures the started-event id, navigates to /analyze/[id]"
    - "GET /analyze/[id] server-side fetches the analysis_results row scoped by user_id + deleted_at IS NULL"
    - "Server page renders ResultCard with initialData=row as a client component"
    - "ResultCard with completed initialData (overall_score != null) skips stream and renders all panels in ready state (Pitfall #3)"
    - "ResultCard with null initialData (fresh) OR overall_score=null opens stream via useAnalysisStream"
    - "Each placeholder panel toggles between PanelSkeleton and content based on panelReady[id]"
    - "E2E test: submit on /analyze, navigate to /analyze/[id], assert panels transition idle to loading to ready in DOM"
  artifacts:
    - path: "src/app/(app)/analyze/page.tsx"
      provides: "Server shell for the form page; reuses ContentForm in a Suspense boundary"
      min_lines: 25
    - path: "src/app/(app)/analyze/[id]/page.tsx"
      provides: "Server shell for the result page; fetches analysis_results row by id + user_id, renders ResultCard with initialData"
      min_lines: 50
    - path: "src/app/(app)/analyze/[id]/result-card.tsx"
      provides: "Client wrapper using useAnalysisStream; renders 10 placeholder GlassPanel slots gated by panelReady"
      min_lines: 80
    - path: "src/app/(app)/analyze/[id]/result-card-skeleton.tsx"
      provides: "Per-panel skeleton primitive that flips on panelReady prop"
      min_lines: 20
  key_links:
    - from: "src/app/(app)/analyze/[id]/page.tsx"
      to: "src/lib/supabase/server.ts"
      via: "createClient() + .from('analysis_results').eq('user_id', user.id) for SSR fetch"
      pattern: "createClient.*supabase/server"
    - from: "src/app/(app)/analyze/[id]/result-card.tsx"
      to: "src/hooks/queries/use-analysis-stream.ts"
      via: "useAnalysisStream({ initialData })"
      pattern: "useAnalysisStream"
    - from: "src/app/(app)/analyze/[id]/result-card.tsx"
      to: "src/lib/engine/panel-mapping.ts"
      via: "import { PANEL_IDS } for slot iteration"
      pattern: "PANEL_IDS"
    - from: "src/app/(app)/analyze/page.tsx"
      to: "src/components/app/content-form.tsx"
      via: "Reuses existing ContentForm component (D-10)"
      pattern: "ContentForm"
---

<objective>
Ship the route scaffold + ResultCard skeleton (D-09, D-10, D-11) so the foundation is end-to-end testable. Form page at /analyze submits and navigates to /analyze/[id]. Result page server-renders the row, hands off to a client ResultCard that wires useAnalysisStream and renders 10 placeholder GlassPanel slots, each gated by panelReady.

P3-P5 will replace each placeholder body with the real panel implementation. This plan ships the contract: panels are addressable by PANEL_IDS, gated by panelReady, and SSR-friendly via initialData.

Purpose: This is the demo-able foundation. After this plan ships, a developer can hit /analyze in the browser, submit, watch the skeleton transition wave-by-wave, and verify the entire SSE consumer + stage-to-panel mapping + reconnect ladder all work together end-to-end.

Output: 4 new component/page files + 1 client wrapper + filled assertions in result-card.test.tsx + Playwright e2e spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-sse-consumer-engine-signal-extensions/01-CONTEXT.md
@.planning/phases/01-foundation-sse-consumer-engine-signal-extensions/01-RESEARCH.md
@.planning/phases/01-foundation-sse-consumer-engine-signal-extensions/01-PATTERNS.md
@.planning/phases/01-foundation-sse-consumer-engine-signal-extensions/01-02-SUMMARY.md
@.planning/phases/01-foundation-sse-consumer-engine-signal-extensions/01-03-SUMMARY.md

# Analog files + interfaces to reuse
@src/app/(app)/dashboard/page.tsx
@src/app/(app)/dashboard/dashboard-client.tsx
@src/app/(app)/competitors/[handle]/page.tsx
@src/components/app/content-form.tsx
@src/components/ui/skeleton.tsx
@src/lib/supabase/server.ts
@src/hooks/queries/use-analysis-stream.ts
@src/lib/engine/panel-mapping.ts

<interfaces>
Server page pattern (verbatim from competitors/[handle]/page.tsx):
- params: Promise of an object with id
- generateMetadata reads id from params
- Default export: async server function that calls createClient() then .from analysis_results filtered by id + user_id + deleted_at IS NULL
- Row may be null for fresh navigations (placeholder INSERT race per Pitfall 6) — pass null to ResultCard which opens the stream

Client wrapper sketch:
- "use client" directive
- Receives analysisId + initialData props
- Calls useAnalysisStream with the initialData (Pitfall 3 — completed initialData skips stream)
- Iterates PANEL_IDS, renders a GlassPanel per id with data-panel-id + data-panel-ready attributes
- When panelReady[id] === "ready" renders content placeholder; otherwise PanelSkeleton
- Exposes a Retry button when phase === "error"

Skeleton primitive:
- Pure presentational component
- Three visual states: idle (faint), loading (animate-pulse), error (red border)
- Wraps the existing Skeleton from @/components/ui/skeleton
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 01-08-T1: Author /analyze form page + AnalyzeClient wrapper</name>
  <read_first>
    - src/app/(app)/dashboard/page.tsx (verbatim — server shell pattern)
    - src/app/(app)/dashboard/dashboard-client.tsx (verbatim — client wrapper analog)
    - src/components/app/content-form.tsx (existing ContentForm — verify exact prop name for submit handler)
    - src/app/(app)/layout.tsx (auth-gating layout — new route inherits this)
    - .planning/phases/01-foundation-sse-consumer-engine-signal-extensions/01-PATTERNS.md (analyze/page.tsx + dashboard-client analog sections)
  </read_first>
  <files>
    src/app/(app)/analyze/page.tsx,
    src/app/(app)/analyze/analyze-client.tsx
  </files>
  <action>
    Create the server shell at src/app/(app)/analyze/page.tsx — exports metadata + an AnalyzePage default export that renders AnalyzeClient inside a Suspense boundary.

    Required content:
    - import type Metadata from "next"
    - import Suspense from "react"
    - import AnalyzeClient from "./analyze-client"
    - export const metadata: Metadata with title "Analyze | Virtuna" + description "Analyze your TikTok content with Virtuna's prediction engine."
    - export default function AnalyzePage that returns Suspense fallback={null} wrapping AnalyzeClient

    Create the client wrapper at src/app/(app)/analyze/analyze-client.tsx:
    - "use client" directive at top
    - Imports: useEffect from "react", useRouter from "next/navigation", ContentForm from "@/components/app", useAnalysisStream from "@/hooks/queries/use-analysis-stream"
    - export function AnalyzeClient():
      - const router = useRouter()
      - const stream = useAnalysisStream()
      - useEffect that watches stream.analysisId + stream.phase: when analysisId is truthy AND phase === "analyzing", call router.push(`/analyze/${stream.analysisId}`)
      - returns a div with a heading "Analyze your content" + ContentForm wired to stream.start + error fallback paragraph (role="alert") shown when stream.phase === "error"

    Executor notes:
    - Read src/components/app/content-form.tsx to confirm the actual prop name for the submit handler (likely onAnalyze, onSubmit, or it uses useAnalyze internally). Replicate dashboard's wiring substituting useAnalyze for useAnalysisStream.
    - If ContentForm wires useAnalyze internally with no overridable submit prop, you must either (a) add an optional prop to ContentForm for custom submit handling, OR (b) create a thin AnalyzeForm variant in src/app/(app)/analyze/analyze-form.tsx that mirrors ContentForm's UI but calls useAnalysisStream.start. Prefer option (a); if it's invasive to ContentForm, fall back to option (b) and document in SUMMARY.
    - router.push triggers SSR fetch on the destination — the placeholder row inserted by Plan 02's POST modification should be queryable by the time the destination's server fetch runs (Pitfall 6 mitigation).
  </action>
  <verify>
    <automated>ls "src/app/(app)/analyze/page.tsx" "src/app/(app)/analyze/analyze-client.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - ls "src/app/(app)/analyze/page.tsx" exits 0
    - ls "src/app/(app)/analyze/analyze-client.tsx" exits 0
    - grep -c 'export const metadata' "src/app/(app)/analyze/page.tsx" returns 1
    - grep -c 'export default function AnalyzePage' "src/app/(app)/analyze/page.tsx" returns 1
    - grep -c 'use client' "src/app/(app)/analyze/analyze-client.tsx" returns 1
    - grep -c "useAnalysisStream" "src/app/(app)/analyze/analyze-client.tsx" returns 1 or more
    - grep -c "router.push" "src/app/(app)/analyze/analyze-client.tsx" returns 1
    - grep -c "ContentForm" "src/app/(app)/analyze/analyze-client.tsx" returns 1 or more
    - npx tsc --noEmit reports zero NEW errors
  </acceptance_criteria>
  <done>Form page + client wrapper exist; reuses ContentForm (or documented variant); submits then navigates to /analyze/[id] once analysisId captured.</done>
</task>

<task type="auto">
  <name>Task 01-08-T2: Author /analyze/[id] server page + ResultCard client + Skeleton primitive</name>
  <read_first>
    - src/app/(app)/competitors/[handle]/page.tsx (verbatim — dynamic segment server fetch pattern)
    - src/components/ui/skeleton.tsx (existing Skeleton — reuse, do not author a new one)
    - src/components/primitives/glass-panel.tsx OR src/components/ui/glass-panel.tsx (find correct path; PROJECT.md says zero-config, 4 props only)
    - src/lib/engine/panel-mapping.ts (PANEL_IDS + PanelId + PanelReadyState from Plan 02)
    - src/hooks/queries/use-analysis-stream.ts (Plan 02 output)
    - .planning/phases/01-foundation-sse-consumer-engine-signal-extensions/01-PATTERNS.md (analyze/[id]/page.tsx + result-card analog sections)
    - CLAUDE.md (Raycast Design Language Rules — borders, glass, skeleton placeholder treatment)
  </read_first>
  <files>
    src/app/(app)/analyze/[id]/page.tsx,
    src/app/(app)/analyze/[id]/result-card.tsx,
    src/app/(app)/analyze/[id]/result-card-skeleton.tsx
  </files>
  <action>
    File 1 — src/app/(app)/analyze/[id]/page.tsx (server component):
    - imports: type Metadata from "next", createClient from "@/lib/supabase/server", ResultCard from "./result-card"
    - interface PageProps with params: Promise of an object with id
    - export async function generateMetadata reads id, returns title "Analysis {id} | Virtuna" + description
    - export default async function AnalyzeResultPage:
      - await params, destructure id
      - createClient(), getUser; if no user return null (defensive — layout already redirects)
      - .from("analysis_results").select("*").eq("id", id).eq("user_id", user.id).is("deleted_at", null).single()
      - Renders main element wrapping h1 "Analysis" + ResultCard with analysisId={id} initialData={row ?? null}
      - Row may be null for fresh navigation — pass null so ResultCard opens stream

    File 2 — src/app/(app)/analyze/[id]/result-card-skeleton.tsx ("use client"):
    - Imports: Skeleton from "@/components/ui/skeleton", type PanelReadyState from "@/lib/engine/panel-mapping"
    - interface PanelSkeletonProps with ready: PanelReadyState
    - export function PanelSkeleton({ ready }):
      - Compute animClass: "animate-pulse" when loading, "border-red-500/40" when error, "opacity-50" otherwise
      - Returns a div with data-skeleton-state={ready} className={`space-y-2 ${animClass}`} containing 3 stacked Skeleton elements (h-4 widths 1/2, 3/4, 2/3)

    File 3 — src/app/(app)/analyze/[id]/result-card.tsx ("use client"):
    - Imports: useAnalysisStream from "@/hooks/queries/use-analysis-stream", PANEL_IDS from "@/lib/engine/panel-mapping", GlassPanel (verify exact import path — likely @/components/primitives/glass-panel), PanelSkeleton from "./result-card-skeleton"
    - type AnalysisResultRow = Record<string, unknown> & { overall_score?: number | null }
    - interface ResultCardProps with analysisId: string + initialData: AnalysisResultRow | null
    - const PANEL_LABEL: Record<string, string> mapping each PanelId to a human label (verdict → "Verdict", retention → "Retention curve", persona_breakdown → "Personas", hook_decomp → "Hook decomposition", similar_videos → "Similar videos", reasoning → "Reasoning", emotion_arc → "Emotion arc", comparative_baseline → "Comparative baseline", optimal_post → "When to post", anti_virality → "Anti-virality")
    - export function ResultCard({ analysisId, initialData }):
      - const { panelReady, phase, result, error, reconnect } = useAnalysisStream({ initialData: initialData ? { id: analysisId, ...initialData } : null })
      - Returns wrapping div containing:
        - A status paragraph with data-stream-phase={phase} showing "Phase: {phase}" + score percentage when result?.overall_score is non-null
        - A grid (md:grid-cols-2) iterating PANEL_IDS — each renders GlassPanel with key={id}, className="p-4 min-h-[120px]", data-panel-id={id}, data-panel-ready={panelReady[id]}, containing an h3 with PANEL_LABEL[id] + conditional body: when panelReady[id] === "ready" render content placeholder ("Panel content placeholder — implemented in Phase 3-5"); otherwise render PanelSkeleton with ready={panelReady[id]}
        - When phase === "error" render an error region (role="alert") with the error message + a Retry button bound to reconnect

    Executor notes:
    - Verify the GlassPanel import path before writing. Per CLAUDE.md it's zero-config (children, className, style, as). If GlassPanel does not forward data-* attributes through className-only, wrap the GlassPanel in an outer div that carries the data attributes.
    - Confirm Skeleton from @/components/ui/skeleton is the correct export name (named or default).
    - Per CLAUDE.md backdrop-filter rule: if any skeleton needs backdrop blur, apply via React inline style, not CSS class.
    - PANEL_LABEL stays local so P3-P5 panel implementations can override per-panel labels independently.
  </action>
  <verify>
    <automated>ls "src/app/(app)/analyze/[id]/page.tsx" "src/app/(app)/analyze/[id]/result-card.tsx" "src/app/(app)/analyze/[id]/result-card-skeleton.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - ls "src/app/(app)/analyze/[id]/page.tsx" exits 0
    - ls "src/app/(app)/analyze/[id]/result-card.tsx" exits 0
    - ls "src/app/(app)/analyze/[id]/result-card-skeleton.tsx" exits 0
    - grep -c "createClient" "src/app/(app)/analyze/[id]/page.tsx" returns 1
    - grep -c "user_id" "src/app/(app)/analyze/[id]/page.tsx" returns 1
    - grep -c "useAnalysisStream" "src/app/(app)/analyze/[id]/result-card.tsx" returns 1
    - grep -c "PANEL_IDS" "src/app/(app)/analyze/[id]/result-card.tsx" returns 1
    - grep -c "data-panel-id" "src/app/(app)/analyze/[id]/result-card.tsx" returns 1
    - grep -c "data-panel-ready" "src/app/(app)/analyze/[id]/result-card.tsx" returns 1
    - grep -c "data-stream-phase" "src/app/(app)/analyze/[id]/result-card.tsx" returns 1
    - grep -c "reconnect" "src/app/(app)/analyze/[id]/result-card.tsx" returns 1 or more
    - grep -c "Skeleton" "src/app/(app)/analyze/[id]/result-card-skeleton.tsx" returns 1 or more
    - grep -c "PanelReadyState" "src/app/(app)/analyze/[id]/result-card-skeleton.tsx" returns 1
    - npx tsc --noEmit reports zero NEW errors
  </acceptance_criteria>
  <done>Result page + ResultCard + Skeleton primitive exist; SSR fetch scoped by user_id; client iterates PANEL_IDS with panelReady gating + data-* attributes for E2E hooks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-08-T3: Fill result-card.test.tsx + e2e spec assertions</name>
  <read_first>
    - src/app/(app)/analyze/__tests__/result-card.test.tsx (Plan 01 stubs)
    - src/app/(app)/analyze/[id]/result-card.tsx (Task T2 output — under test)
    - e2e/result-surface-stream.spec.ts (Plan 01 stubs)
    - e2e/playwright.config.ts (config + baseURL)
    - src/test/fixtures/completed-prediction.ts (COMPLETED_PREDICTION + IN_FLIGHT_ROW)
  </read_first>
  <behavior>
    Unit tests (happy-dom, React Testing Library):
    - Renders 10 GlassPanel slots (one per PANEL_IDS)
    - With initialData=null all panels render PanelSkeleton with data-panel-ready="idle"
    - With initialData containing overall_score=non-null all panels render content placeholder and data-panel-ready="ready" (Pitfall 3 — no stream opened)
    - With initialData containing overall_score=null hook opens stream (mocked fetch); panels stay in skeleton state
    - When phase="error" the alert region renders with Retry button

    E2E tests (Playwright) — fixme entries from Plan 01 converted to real tests. May still be skip() if mock-pipeline backend isn't set up:
    - Test 1: submit on /analyze → URL transitions to /analyze/[id] → at least one panel has data-panel-ready that's not "idle" within 5s (proves stream connected)
    - Test 2: revisit a completed /analyze/[id] URL → all panels have data-panel-ready="ready" without re-streaming (Pitfall 3)
    - Test 3 (optional): simulate connection drop → assert phase transitions to "reconnecting" within 3s
  </behavior>
  <files>
    src/app/(app)/analyze/__tests__/result-card.test.tsx,
    e2e/result-surface-stream.spec.ts
  </files>
  <action>
    Replace it.todo entries in src/app/(app)/analyze/__tests__/result-card.test.tsx with concrete assertions. Use happy-dom env pragma, React Testing Library render, QueryClientProvider wrapper, mock fetch with vi.fn for the streaming case, and the COMPLETED_PREDICTION / IN_FLIGHT_ROW fixtures from Plan 01.

    Test structure:
    - Pragma line: vitest-environment happy-dom
    - Imports: describe/it/expect/vi/beforeEach from "vitest"; render + screen from "@testing-library/react"; QueryClient + QueryClientProvider from "@tanstack/react-query"; ResultCard from "@/app/(app)/analyze/[id]/result-card"; COMPLETED_PREDICTION + IN_FLIGHT_ROW from "@/test/fixtures/completed-prediction"; PANEL_IDS from "@/lib/engine/panel-mapping"; React from "react"
    - Helper wrapper that wraps children in a fresh QueryClientProvider with retry=false
    - beforeEach restoring vi mocks

    Six assertions:
    1. "renders one slot per PANEL_IDS" — render with initialData=null; assert document.querySelectorAll('[data-panel-id]').length === PANEL_IDS.length
    2. "every panel starts data-panel-ready='idle' when initialData=null and no stream events received yet" — render with initialData=null + a fetch mock that never resolves; assert each [data-panel-id] has data-panel-ready === "idle" or "loading"
    3. "Pitfall 3 — initialData with overall_score non-null renders all panels ready, no fetch called" — set global.fetch to a vi.fn; render with initialData=COMPLETED_PREDICTION; assert every [data-panel-id] has data-panel-ready === "ready" AND fetch was NOT called
    4. "initialData with overall_score=null DOES NOT short-circuit (stream opens)" — render with initialData={ id: 'x', overall_score: null }; assert phase indicator (data-stream-phase attribute) is not "complete"
    5. "phase=error shows alert + Retry button" — mock fetch to return a 500; render; await an alert with role="alert"; assert a button with text matching /retry/i exists
    6. "data-stream-phase attribute reflects useAnalysisStream phase" — render with initialData=null; assert [data-stream-phase] is present in the DOM

    For the e2e spec at e2e/result-surface-stream.spec.ts:
    - import { test, expect } from "@playwright/test"
    - First test (use test.fixme if backend isn't mock-pipeline-ready — current Phase 1 doesn't yet have a deterministic e2e backend; flag the test for fix in P2): navigate to /analyze (assuming auth fixture exists from existing e2e setup) → fill form → click submit → expect URL to match /analyze/[A-Za-z0-9_-]+/ within 5s → expect at least one [data-panel-id][data-panel-ready] to NOT equal "idle" within 30s
    - Second test (fixme if no completed-analysis fixture exists): navigate directly to /analyze/[known-completed-id] → expect all [data-panel-id] to have data-panel-ready="ready" without a network flush
    - Third test (fixme): can be left as test.fixme because Phase 1 doesn't have a way to deterministically simulate stream drops in e2e — defer to Phase 2 with the live persona viz

    Notes for executor:
    - Prefer test.fixme over test.skip so the tests remain in the file as planned-for-completion-in-future-work; the README convention treats fixme as known-broken-but-tracked.
    - If existing e2e auth fixture pattern is unclear, inspect e2e/* for a setup like "use authenticated state" or sign-in helper. If none exists, mark all three as test.fixme with a comment "e2e auth fixture pending"; Phase 7 (mobile + integration) is the milestone for the e2e auth fixture.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(app)/analyze/__tests__/result-card.test.tsx" --reporter=basic 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^[[:space:]]*//' "src/app/(app)/analyze/__tests__/result-card.test.tsx" | grep -c "it.todo" returns 0
    - grep -c "it(" "src/app/(app)/analyze/__tests__/result-card.test.tsx" returns 6 or more
    - grep -c "vitest-environment happy-dom" "src/app/(app)/analyze/__tests__/result-card.test.tsx" returns 1
    - npx vitest run "src/app/(app)/analyze/__tests__/result-card.test.tsx" exits 0
    - grep -c "test(" e2e/result-surface-stream.spec.ts returns 1 or more (real tests OR fixme placeholders both count; original Plan 01 stubs were test.fixme which counts as test.fixme not test(; if all three stay fixme that's acceptable per the docs above)
    - At least one e2e test attempted to be real (not fixme) — verify via grep -c "test(" exceeds 0 OR confirm via SUMMARY that backend e2e fixture work is deferred
  </acceptance_criteria>
  <done>All 6 result-card.test.tsx assertions pass; e2e spec has real or documented-deferred tests.</done>
</task>

<task type="auto">
  <name>Task 01-08-T4: Phase 1 final verification gate</name>
  <read_first>
    - .planning/phases/01-foundation-sse-consumer-engine-signal-extensions/01-VALIDATION.md (the full validation contract)
  </read_first>
  <files></files>
  <action>
    Run the full Phase 1 test suite and capture results. This is the final gate before /gsd-verify-work runs.

    Commands to execute:
    1. npx vitest run — full unit suite, expect zero failures, all P1 tests green
    2. npx tsc --noEmit — zero NEW errors vs baseline
    3. npx eslint src/lib/engine src/hooks/queries "src/app/api/analyze" "src/app/(app)/analyze" — zero errors
    4. npx playwright test --config e2e/playwright.config.ts e2e/result-surface-stream.spec.ts — should pass (or all fixme/skipped if backend not ready); zero failures
    5. Manual smoke check (developer): start dev server, navigate to /analyze in browser, submit a tiktok URL, observe URL transitions to /analyze/[id] + skeleton panels progress visually

    Record results in SUMMARY: which commands passed, which were skipped, any unexpected failures requiring revision in upstream plans.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run exits 0
    - Total failed count from vitest output is 0
    - npx tsc --noEmit reports zero NEW errors
    - npx eslint over the 4 directories reports zero errors (warnings acceptable)
    - npx playwright test reports zero failed (fixme/skipped acceptable)
    - Manual smoke check attempted; result documented in SUMMARY
  </acceptance_criteria>
  <done>All gates green; Phase 1 ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /analyze + /analyze/[id] | Authenticated routes inside (app) group. Auth gating happens at layout.tsx + middleware level. |
| Server page → analysis_results table | createClient cookie auth + explicit .eq("user_id", user.id) filter for defense-in-depth IDOR (same pattern as Plan 03 GET stream). |
| ResultCard panel content | Currently placeholder text only. When P3-P5 wire real content, panel components MUST escape user-generated reasoning narratives (DeepSeek output) via React's default JSX escaping; never use dangerouslySetInnerHTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-RC-XSS | T (Tampering via XSS) | Result content rendered in panels | mitigate | All P1 placeholder content is static strings. P3-P5 panel implementations MUST use React's default JSX escaping for any dynamic field (factors[].rationale, reasoning narrative, persona reasoning). Document this rule in 01-08-SUMMARY for P3-P5 plans to inherit. |
| T-01-RC-IDOR | E (Elevation of Privilege via IDOR) | Server page row lookup | mitigate | .eq("user_id", user.id) filter on the analysis_results query (mirrors Plan 03 GET stream pattern). Wrong-owner ids return null → ResultCard renders with initialData=null and stream open fails 404 → user sees error state, never sees the other tenant's data. |
| T-01-RC-meta-leak | I (Information Disclosure via metadata) | generateMetadata exposes analysis id in title | accept | Page title contains the analysis id only (already known to the URL). No PII or content. |
| T-01-RC-deeplink-replay | R (Repudiation) | Direct navigation to /analyze/[id] without prior submit | mitigate | Server-side row lookup forces ownership check. If row missing or wrong-owner, initialData=null and the stream open attempt against /api/analyze/[id]/stream returns 404 (per Plan 03 IDOR mitigation). User sees error state. |
</threat_model>

<verification>
- All four task verify commands pass
- Full Phase 1 unit + lint + type-check green
- E2E spec either passes OR all tests are fixme'd with documented deferral
- Manual smoke check completed
</verification>

<success_criteria>
- /analyze form page exists, server-rendered, reuses ContentForm (or documented variant), submits via useAnalysisStream + navigates to /analyze/[id]
- /analyze/[id] server page exists, server-fetches row with user_id scoping, renders ResultCard with initialData
- ResultCard exists, iterates PANEL_IDS, gates each panel by panelReady, exposes data-* attributes for E2E hooks
- PanelSkeleton primitive exists, animates per ready state, reuses existing Skeleton component
- Pitfall 3 behavior verified: completed initialData renders all panels ready without opening stream
- All 6 result-card.test.tsx assertions pass; no it.todo remaining
- E2E spec has real tests OR documented fixme deferrals
- Full Phase 1 test suite green
- TypeScript + ESLint clean on all modified files
- Manual smoke check (browser navigation) completed
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-sse-consumer-engine-signal-extensions/01-08-SUMMARY.md documenting: (1) ContentForm wiring approach used (prop-based vs variant fork), (2) GlassPanel + Skeleton actual import paths used, (3) E2E deferral notes for P7, (4) XSS escaping convention for P3-P5 panel implementations, (5) full Phase 1 verification results — which commands passed/skipped/failed.
</output>
