---
phase: 02-design-system
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/layout/container.tsx
  - src/components/layout/header.tsx
  - src/components/layout/footer.tsx
  - src/components/layout/index.ts
autonomous: true

must_haves:
  truths:
    - "Container constrains content width correctly"
    - "Header renders with logo and navigation"
    - "Footer renders with links and copyright"
  artifacts:
    - path: "src/components/layout/container.tsx"
      provides: "Container component for content width"
      exports: ["Container"]
    - path: "src/components/layout/header.tsx"
      provides: "Header component with navigation"
      exports: ["Header"]
    - path: "src/components/layout/footer.tsx"
      provides: "Footer component with links"
      exports: ["Footer"]
    - path: "src/components/layout/index.ts"
      provides: "Barrel exports"
      exports: ["Container", "Header", "Footer"]
  key_links:
    - from: "src/components/layout/header.tsx"
      to: "src/lib/utils.ts"
      via: "cn import"
      pattern: "import.*cn.*from"
    - from: "src/components/layout/header.tsx"
      to: "src/components/ui/button.tsx"
      via: "Button import"
      pattern: "import.*Button.*from"
---

<objective>
Create layout components for page structure.

Purpose: Container, Header, and Footer provide consistent page structure across landing and app pages. These are structural components used in layouts.
Output: Three layout components that match societies.io structure and styling.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-design-system/02-RESEARCH.md
@.planning/phases/02-design-system/02-01-SUMMARY.md
@.reference/design-tokens.md
@.reference/landing/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Container component</name>
  <files>src/components/layout/container.tsx</files>
  <action>
Create Container component for content width constraints:

1. Import cn from @/lib/utils

2. Define ContainerProps:
   - children: React.ReactNode
   - className?: string
   - size?: "default" | "narrow" | "wide" (optional variant)
   - as?: React.ElementType (default: "div")

3. Container styles based on societies.io:
   - Base: mx-auto w-full px-4 sm:px-6 lg:px-8
   - Default: max-w-7xl (~1280px - main content)
   - Narrow: max-w-4xl (~896px - for focused content)
   - Wide: max-w-screen-2xl (~1536px - for full-width sections)

4. Use cn() for className merging
5. Support polymorphic "as" prop for semantic HTML

Example usage:
```tsx
<Container>Content</Container>
<Container size="narrow" as="section">Narrow content</Container>
```
  </action>
  <verify>
    - Component renders with correct max-width
    - as prop changes element type
    - className prop merges correctly
  </verify>
  <done>
    - Container component constrains width
    - Supports 3 size variants
    - Polymorphic with "as" prop
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Header and Footer components</name>
  <files>
    src/components/layout/header.tsx
    src/components/layout/footer.tsx
    src/components/layout/index.ts
  </files>
  <action>
1. Create Header component (src/components/layout/header.tsx):
   "use client" - needs interactivity for mobile menu

   Structure matching societies.io:
   - Fixed/sticky header with bg-background/80 backdrop-blur-sm
   - Container for max-width
   - Logo on left (text "Virtuna" with accent color, or placeholder for logo)
   - Navigation links in center (hidden on mobile)
   - CTA button on right
   - Mobile menu button (hamburger icon from Phosphor)

   Props:
   - variant?: "landing" | "app" (different nav items)
   - className?: string

   Landing nav items (placeholder for now):
   - Features, Pricing, About, Contact

   Styles:
   - Header: py-4 border-b border-border/50
   - Nav links: text-foreground-muted hover:text-foreground transition-colors
   - Active link: text-foreground

   Note: Mobile menu implementation can be basic toggle for now - full implementation in landing phase

2. Create Footer component (src/components/layout/footer.tsx):
   Structure matching societies.io:
   - Full-width bg-background-elevated
   - Container for content
   - Grid layout:
     - Logo + tagline column
     - Links columns (Product, Company, Legal)
     - Social icons (placeholder)
   - Bottom bar with copyright

   Props:
   - className?: string
   - minimal?: boolean (for app footer - just copyright)

   Styles:
   - Footer: py-12 lg:py-16
   - Section titles: text-sm font-medium text-foreground mb-4
   - Links: text-sm text-foreground-muted hover:text-foreground transition-colors
   - Copyright: text-xs text-foreground-muted

3. Create barrel export (src/components/layout/index.ts):
   - export { Container } from "./container"
   - export { Header } from "./header"
   - export { Footer } from "./footer"
  </action>
  <verify>
    - npm run build passes
    - Header renders with navigation structure
    - Footer renders with link grid
    - Both use design tokens correctly
  </verify>
  <done>
    - Header with logo placeholder, nav links, CTA
    - Footer with links grid and copyright
    - Both components match societies.io structure
    - Barrel exports all layout components
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. Import works: `import { Container, Header, Footer } from "@/components/layout"`
3. Container constrains width to max-w-7xl by default
4. Header is sticky with backdrop blur
5. Footer has proper grid layout with link columns
</verification>

<success_criteria>
- 3 layout components created (Container, Header, Footer)
- All use design tokens (bg-background, text-foreground-muted, etc.)
- Header supports landing/app variants
- Footer supports minimal variant
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system/02-03-SUMMARY.md`
</output>
