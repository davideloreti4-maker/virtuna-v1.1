---
phase: 02-design-system
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - src/app/showcase/page.tsx
autonomous: false

must_haves:
  truths:
    - "Showcase page displays all UI components"
    - "All components render correctly in browser"
    - "Design tokens apply correct colors and fonts"
    - "Animations trigger on scroll"
  artifacts:
    - path: "src/app/showcase/page.tsx"
      provides: "Component showcase page"
      min_lines: 100
  key_links:
    - from: "src/app/showcase/page.tsx"
      to: "src/components/ui"
      via: "component imports"
      pattern: "from \"@/components/ui\""
    - from: "src/app/showcase/page.tsx"
      to: "src/components/layout"
      via: "component imports"
      pattern: "from \"@/components/layout\""
    - from: "src/app/showcase/page.tsx"
      to: "src/components/motion"
      via: "component imports"
      pattern: "from \"@/components/motion\""
---

<objective>
Create component showcase page and verify visually.

Purpose: Validates all design system components work together. Provides a single page to visually verify styling matches societies.io before building actual pages.
Output: Showcase page at /showcase displaying all components, followed by visual verification checkpoint.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-design-system/02-01-SUMMARY.md
@.planning/phases/02-design-system/02-02-SUMMARY.md
@.planning/phases/02-design-system/02-03-SUMMARY.md
@.planning/phases/02-design-system/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create component showcase page</name>
  <files>src/app/showcase/page.tsx</files>
  <action>
Create a showcase page that displays all design system components:

1. Import all components:
   - { Button } from "@/components/ui"
   - { Input } from "@/components/ui"
   - { Card, CardHeader, CardContent, CardFooter } from "@/components/ui"
   - { Skeleton } from "@/components/ui"
   - { Container, Header, Footer } from "@/components/layout"
   - { FadeIn, SlideUp } from "@/components/motion"

2. Page structure:
   - Header at top (with variant="landing")
   - Container wrapping main content
   - Sections for each component type
   - Footer at bottom

3. Sections to include:

   **Typography Section:**
   - H1 with font-display class: "Design System Showcase"
   - H2 with font-display: "Section Heading"
   - Body text with font-sans demonstrating different weights
   - Show color tokens (text-foreground, text-foreground-muted, text-accent)

   **Button Section:**
   - All variants: primary, secondary, ghost, link
   - All sizes: sm, md, lg, icon
   - Disabled state example
   - Group of buttons in a row

   **Input Section:**
   - Default input with placeholder
   - Focused input (can't show focus state statically - note this)
   - Input with error state (error={true})
   - Disabled input

   **Card Section:**
   - Card with CardHeader, CardContent, CardFooter
   - Show title and description in card
   - Include a button in CardFooter

   **Skeleton Section:**
   - Skeleton for text (height h-4, width variations)
   - Skeleton for avatar (rounded-full, h-12 w-12)
   - Skeleton for card (full card placeholder)

   **Animation Section:**
   - FadeIn wrapper around some content
   - SlideUp wrapper around other content
   - Note: Scroll down to trigger animations

4. Wrap sections in FadeIn for demo effect
5. Use design tokens throughout (bg-background, bg-background-elevated, etc.)

The page should be visually impressive and demonstrate the design system works correctly.
  </action>
  <verify>
    - npm run build passes
    - npm run dev - visit localhost:3000/showcase
    - Page renders without errors
    - All components visible and styled correctly
  </verify>
  <done>
    - Showcase page exists at /showcase
    - All UI components (Button, Input, Card, Skeleton) displayed
    - All layout components (Container, Header, Footer) used
    - Animation components wrap sections
    - Page uses design tokens correctly
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete design system with:
- Design tokens (colors, fonts, spacing)
- UI components (Button, Input, Card, Skeleton)
- Layout components (Container, Header, Footer)
- Animation components (FadeIn, SlideUp, PageTransition)
- Showcase page displaying all components
  </what-built>
  <how-to-verify>
1. Run `npm run dev` if not already running
2. Visit http://localhost:3000/showcase
3. Verify the following:

**Fonts:**
- [ ] Headings use Funnel Display (display font)
- [ ] Body text uses Satoshi (no Inter visible)
- [ ] No flash of unstyled text on page load

**Colors:**
- [ ] Background is dark (#0D0D0D)
- [ ] Cards have elevated background (#1A1A1A)
- [ ] Text is white (#FFFFFF)
- [ ] Accent color is orange (#E57850)
- [ ] Muted text is gray (#CCCCCC)

**Buttons:**
- [ ] Primary button has orange background
- [ ] Secondary button has dark background with border
- [ ] Ghost button is transparent with hover effect
- [ ] Link button shows underline on hover
- [ ] All sizes (sm, md, lg, icon) render correctly

**Inputs:**
- [ ] Default input has dark background and border
- [ ] Error input has red border
- [ ] Disabled input is grayed out

**Cards:**
- [ ] Card has rounded corners and elevated background
- [ ] Card sections (header, content, footer) are spaced correctly

**Skeletons:**
- [ ] Skeleton shows pulse animation
- [ ] Animation stops if you have prefers-reduced-motion enabled

**Animations:**
- [ ] Sections fade in as you scroll down
- [ ] SlideUp sections animate with vertical movement
- [ ] Animations feel smooth (ease-out timing)

**Layout:**
- [ ] Header is at top with navigation structure
- [ ] Footer is at bottom with link columns
- [ ] Container constrains content width

4. Compare with societies.io:
- [ ] Overall feel matches the dark, modern aesthetic
- [ ] Typography weights and sizes feel similar
- [ ] Spacing is comfortable (not too cramped or sparse)
  </how-to-verify>
  <resume-signal>
Type "approved" if everything looks correct.
Or describe any issues that need fixing (e.g., "fonts not loading", "colors don't match", "animations too fast").
  </resume-signal>
</task>

</tasks>

<verification>
1. Showcase page accessible at /showcase
2. All components render without console errors
3. Design tokens applied correctly (visually verified)
4. Fonts load without flash
5. Human verification checkpoint passed
</verification>

<success_criteria>
- Showcase page displays all design system components
- Visual verification confirms components match societies.io aesthetic
- No console errors or build failures
- Human approves the design system
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system/02-05-SUMMARY.md`
</output>
