---
phase: 13-ambient-numen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/engine/flash/build-reaction-panel.ts
  - src/lib/tools/runners/ideas-runner.ts
  - src/lib/tools/runners/hooks-runner.ts
  - src/app/api/tools/react/route.ts
  - src/app/api/tools/react/__tests__/route.test.ts
  - src/lib/engine/flash/__tests__/build-reaction-panel.test.ts
autonomous: true
requirements: [AMBIENT-01]
must_haves:
  truths:
    - "Posting a thought to /api/tools/react returns a real Flash reaction { fraction, scrollQuote } scoped to the active audience"
    - "The reaction route resolves its niche panel via resolveNicheKey + the audience repaint identically to ideas-runner/hooks-runner (not a hand-built generic panel)"
    - "A typed thought in a real niche returns a discriminating band (not 'all Mixed' niche-blind)"
    - "Both ideas-runner and hooks-runner still build their Flash panel + repaint byte-identically (via the shared helper) — no behavior change, regression suite green, ENGINE_VERSION stays 3.19.0"
  artifacts:
    - path: "src/lib/engine/flash/build-reaction-panel.ts"
      provides: "Shared buildReactionPanel(profileRow, audience) → { panel, audienceRepaint } (the niche-resolution + repaint extraction lifted from the runners)"
      exports: ["buildReactionPanel"]
    - path: "src/app/api/tools/react/route.ts"
      provides: "POST type-to-room reaction route (the only new server code in the phase)"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/tools/react/route.ts"
      to: "src/lib/engine/flash/run-flash-text-mode.ts"
      via: "runFlashTextMode(text, framing, panel, audienceRepaint)"
      pattern: "runFlashTextMode"
    - from: "src/app/api/tools/react/route.ts"
      to: "src/lib/engine/flash/build-reaction-panel.ts"
      via: "buildReactionPanel(profileRow, audience)"
      pattern: "buildReactionPanel"
    - from: "src/lib/tools/runners/ideas-runner.ts"
      to: "src/lib/engine/flash/build-reaction-panel.ts"
      via: "shared helper replacing inline niche/repaint construction"
      pattern: "buildReactionPanel"
---

<objective>
Build the ONLY new server code in Phase 13: a thin type-to-room reaction route (`POST /api/tools/react`) that fires ONE Flash text-mode reaction for an ad-hoc thought and returns the real `{ fraction, scrollQuote }` — the data the client turns into a spotlight + Lens (D-04). To guarantee the typed reaction discriminates by niche exactly like a card reaction (RESEARCH Open Q1 + Pitfall 2 — the "all Mixed" niche-blind failure is a moat-credibility miss), first EXTRACT the niche-panel + audience-repaint construction that `ideas-runner.ts` and `hooks-runner.ts` currently duplicate inline into a shared `buildReactionPanel` helper, refactor both runners onto it, then have the new route call the same helper.

Purpose: Type-to-room is "test a thought against my people" without running a full skill — the ambient cheatcode. It MUST reuse the shipped Flash reaction primitive (not the markdown chat route) and the shipped niche-resolution path (not a generic panel), or it returns prose / niche-blind Mixed and the moat reads as fake.
Output: One shared helper + one new POST route + tests; both runners refactored with zero behavior change.
</objective>

<artifacts_this_phase_produces>
Every symbol Phase 13 (Ambient Numen) creates, across all four plans. This section lives on the wave-1 anchor plan so executors see the full phase surface up front (D-01..D-06, AMBIENT-01):

**New server code (Plan 13-01):**
- `buildReactionPanel(profileRow, audience) → { panel, audienceRepaint }` — `src/lib/engine/flash/build-reaction-panel.ts` (the niche-resolution + repaint extraction lifted from ideas-runner/hooks-runner; resolves RESEARCH Open Q1).
- `POST /api/tools/react` — `src/app/api/tools/react/route.ts` (the type-to-room reaction route; the ONLY new model-calling code in the phase; returns `{ fraction, scrollQuote }`).

**New client components / types / hooks:**
- `CardReactionAtRest({ fraction })` — `src/components/audience-lens/card-reaction-at-rest.tsx` (Plan 13-02; the resting per-card reaction readout rendered inside the shipped LensTrigger on all 4 cards).
- `AmbientPresence({ audience, focus, reducedMotion, onFocusChange })` — `src/components/audience-lens/ambient-presence.tsx` (Plan 13-03; the persistent dot-cloud strip + spotlight subject + type-to-room input; Surfaces 1/2/4).
- `AmbientFocus`, `AmbientPresenceProps` — `src/components/audience-lens/ambient-presence-types.ts` (Plan 13-03; the focus contract the composer drives).
- `useAmbientFocus(...)` — `src/components/app/home/use-ambient-focus.ts` (Plan 13-04; default-latest + tap-priority + scroll-spy focus resolution).

**Edited (no new public symbols; behavior extended):**
- `src/lib/tools/runners/{ideas,hooks}-runner.ts` — refactored onto `buildReactionPanel` (byte-identical output, Plan 13-01).
- `src/components/thread/{idea,hook,script,remix}-card-block.tsx` — render `CardReactionAtRest` inside the existing LensTrigger (Plan 13-02).
- `src/components/app/home/composer.tsx` — mount `<AmbientPresence>` sticky in `composer-thread-region` + wire scroll-spy/tap/type-to-room focus (Plan 13-04).

**Net-new routes:** `POST /api/tools/react` (one).
**Reused verbatim (NOT recreated — the "extend, don't duplicate" mandate):** `AudienceLens`, `LensTrigger`, `cardScrollQuoteReactions`, `runFlashTextMode`, `aggregateFlash`, `PersonaCloud`/`PersonaGraph` (`mulberry32` + `<animate>` + sr-only mirror), `getAudience`/`GENERAL_AUDIENCE`. ENGINE_VERSION stays 3.19.0.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-proactive-numen-ambient-initiated-expansion-the-ambient-audi/13-CONTEXT.md
@.planning/phases/13-proactive-numen-ambient-initiated-expansion-the-ambient-audi/13-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract buildReactionPanel shared helper from the two runners (zero behavior change)</name>
  <read_first>
    - src/lib/tools/runners/ideas-runner.ts (L280-300: `resolveNicheKey(profileRow?.niche_primary ?? null)` → `panel = { niche, contentType: null }`; `audienceRepaint` from `audience.personas.map((p) => [p.archetype, p.repaint])`, undefined for General/no-audience; L236-244 `selectLeadScrollQuote`; L97-108 `IdeasPipelineInput`)
    - src/lib/tools/runners/hooks-runner.ts (L313-323: byte-identical niche + repaint construction)
    - src/lib/engine/wave3/niche-resolver.ts (L62 `resolveNicheKey(nichePrimary: string | null): string | null`)
    - src/lib/engine/flash/run-flash-text-mode.ts (L33/L37 `NichePanel` type re-export; L89-93 signature `runFlashTextMode(content_text, framing: FlashFraming, panel?: NichePanel, audienceRepaint?: Record<string,string>)`)
    - src/lib/audience/audience-types.ts (the `Audience` type — `is_general`, `personas[]` with `archetype` + `repaint`)
  </read_first>
  <action>
    Create `src/lib/engine/flash/build-reaction-panel.ts` exporting `buildReactionPanel(profileRow, audience)` that returns `{ panel, audienceRepaint }`, lifting the EXACT logic currently inline in both runners: `panel = { niche: resolveNicheKey(profileRow?.niche_primary ?? null), contentType: null }` and `audienceRepaint = (audience && !audience.is_general && audience.personas?.length) ? Object.fromEntries(audience.personas.map((p) => [p.archetype, p.repaint])) : undefined`. Accept `profileRow: ProfileRow | null` and `audience: Audience | null`. Type the return `panel` as `NichePanel` (imported from run-flash-text-mode). Keep `resolveAudienceWeights` OUT of this helper (it is `void resolvedWeights` dead-wired in the runners for the future Max path — not part of the Flash reaction path). Then refactor BOTH `ideas-runner.ts` and `hooks-runner.ts` to call `buildReactionPanel(profileRow, audience)` in place of the inline construction — `panel` and `audienceRepaint` must remain byte-identical so `runFlashTextMode(seed, framing, panel, audienceRepaint)` is unchanged. Do NOT touch the `void resolvedWeights` line in the runners (leave the weights wiring as-is). No `ENGINE_VERSION` change — this is a pure refactor of the text path.
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/engine/flash/__tests__/build-reaction-panel.test.ts src/lib/tools/runners 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/engine/flash/build-reaction-panel.ts` exists and exports `buildReactionPanel`.
    - For a calibrated audience (is_general=false, personas with archetype+repaint), `buildReactionPanel` returns `audienceRepaint` as a `Record<string,string>` mapping each persona's `archetype` → `repaint`.
    - For `GENERAL_AUDIENCE` (is_general=true, personas:[]) OR `audience=null`, `audienceRepaint` is `undefined` (the byte-identical no-op path, regression-gate-safe).
    - `panel.niche === resolveNicheKey(profileRow?.niche_primary ?? null)` and `panel.contentType === null`.
    - `grep -n "Object.fromEntries(audience.personas.map" src/lib/tools/runners/ideas-runner.ts` returns NOTHING (inline construction removed) AND `grep -c "buildReactionPanel" src/lib/tools/runners/ideas-runner.ts` ≥ 1 AND same for hooks-runner.ts.
    - The existing ideas-runner + hooks-runner test suites pass unchanged (no behavior drift).
    - `grep -rn "3.19.0\|3\.20\|3\.19\.1" src/lib/engine/version.ts` (or wherever ENGINE_VERSION lives) shows the version UNCHANGED at 3.19.0.
  </acceptance_criteria>
  <behavior>
    - buildReactionPanel(profileRow={niche_primary:"fitness"}, calibratedAudience) → panel.niche = resolveNicheKey("fitness"), audienceRepaint has one entry per persona archetype.
    - buildReactionPanel(profileRow=null, GENERAL_AUDIENCE) → panel.niche = null, audienceRepaint = undefined.
    - buildReactionPanel(anyProfile, null) → audienceRepaint = undefined.
  </behavior>
  <done>The niche/repaint construction lives in one shared helper; both runners consume it with byte-identical output; the new route (Task 2) can reuse it so type-to-room discriminates by niche exactly like a card reaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/tools/react — the thin type-to-room reaction route</name>
  <read_first>
    - src/app/api/tools/ideas/route.ts (L46/L53 imports `createClient`, `getAudience`, `GENERAL_AUDIENCE`; L75-83 auth-first `supabase.auth.getUser()` → 401; L99-101 Zod 400 boundary; L127-131 load `creator_profiles` → `profileRow`; L137-147 read `thread.active_audience_id` off the open thread → `getAudience(supabase, activeAudienceId)`)
    - src/lib/engine/flash/run-flash-text-mode.ts (L57 `FlashRunResult`; L89-93 signature; returns `{ result: { personas } }`)
    - src/lib/engine/flash/flash-aggregate.ts (L4 `aggregateFlash(personas) → { band, fraction }`; honesty: no numeric score)
    - src/lib/tools/runners/ideas-runner.ts (L236-244 `selectLeadScrollQuote(personas)` — private; copy an identical local function into the route per RESEARCH A4, OR import if Task 1 also exported it — A4 says inline-copy matches the 4-runner precedent)
    - src/lib/engine/flash/build-reaction-panel.ts (NEW from Task 1 — `buildReactionPanel`)
    - src/app/api/tools/chat/route.ts (CONFIRM it streams markdown `text/event-stream`, NOT a reaction — the anti-pattern this route must NOT reuse; RESEARCH Pitfall 1)
    - src/app/api/tools/ideas/__tests__/route.test.ts (the route-test precedent: mocking auth + supabase + the Flash primitive)
  </read_first>
  <action>
    Create `src/app/api/tools/react/route.ts` exporting an async `POST(request)`. Order: (1) auth-first via `createClient()` + `supabase.auth.getUser()`, return `Response.json({ error: "Unauthorized" }, { status: 401 })` when absent (CR-01 pattern). (2) Zod-validate the body to `{ text: string (min 1 non-whitespace), framing?: "hook" | "idea" }` (CLAUDE.md boundary rule) → 400 on failure. (3) Load `creator_profiles` for the user → `profileRow` (same select the ideas route uses). (4) Resolve the active audience the SAME way the ideas route does: read `active_audience_id` off the user's open thread, then `getAudience(supabase, activeAudienceId)` (NEVER trust an audience id from the body — server-resolved under the session). (5) `const { panel, audienceRepaint } = buildReactionPanel(profileRow, audience)`. (6) `const { result } = await runFlashTextMode(text, framing ?? "hook", panel, audienceRepaint)` — default framing `"hook"` (first-2s "do you stop?", matching every card-level reaction; RESEARCH A1). (7) `const { fraction } = aggregateFlash(result.personas)` + `const scrollQuote = selectLeadScrollQuote(result.personas)`. (8) Return `Response.json({ fraction, scrollQuote })`. NO streaming (one whole ~8-17s call — the client shows "Reading the room…"). NO persistence (type-to-room is ephemeral — RESEARCH Open Q3 default). NO `ENGINE_VERSION` bump (text path, Qwen-only, reuses the shipped primitive). Do NOT import or call `/api/tools/chat`. Wrap the Flash call in try/catch and return `Response.json({ error: "reaction_failed" }, { status: 502 })` on failure (the client renders the honest retry copy, never error-red).
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/app/api/tools/react/__tests__/route.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/tools/react/route.ts` exists and exports `POST`.
    - Unauthenticated request → 401 `{ error: "Unauthorized" }` (test mocks `getUser` returning no user).
    - Empty / whitespace-only `text` → 400 (Zod boundary), no Flash call fired.
    - Happy path (mocked `runFlashTextMode` returning 10 personas with stop verdicts + quotes): response is `{ fraction, scrollQuote }` where `fraction` matches `aggregateFlash` output (e.g. "6/10 stop") and `scrollQuote` is the first stop-verdict persona's quote.
    - The route calls `buildReactionPanel(profileRow, audience)` and passes its `panel` + `audienceRepaint` into `runFlashTextMode` (assert via spy/mock that `runFlashTextMode` was called with the resolved panel — proving niche discrimination is wired, not a generic panel; RESEARCH Pitfall 2). `grep -c "buildReactionPanel" src/app/api/tools/react/route.ts` ≥ 1.
    - The audience is resolved server-side via `getAudience(supabase, activeAudienceId)` from the open thread — `grep -n "body.audienceId\|body.audience_id" src/app/api/tools/react/route.ts` returns NOTHING (no body-trusted audience id).
    - `grep -n "api/tools/chat\|text/event-stream\|markdown" src/app/api/tools/react/route.ts` returns NOTHING (not the markdown chat route; RESEARCH Pitfall 1).
    - Flash-call failure → 502 `{ error: "reaction_failed" }` (test forces the primitive to throw).
  </acceptance_criteria>
  <behavior>
    - POST { text: "what if I open with a question?" } authed, calibrated fitness audience → { fraction: "N/10 stop", scrollQuote: "<real lead quote>" }, runFlashTextMode called with a non-null panel.niche.
    - POST { text: "" } → 400, no model call.
    - POST unauthenticated → 401.
    - runFlashTextMode throws → 502 { error: "reaction_failed" }.
  </behavior>
  <done>Type-to-room has a real, auth-guarded, niche-discriminating reaction endpoint returning the exact `{ fraction, scrollQuote }` shape the client feeds to `cardScrollQuoteReactions` → spotlight + Lens. No markdown route, no client-side Qwen, no ENGINE_VERSION change.</done>
</task>

</tasks>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/lib/engine/flash src/lib/tools/runners src/app/api/tools/react` — all green.
- `npm run build` succeeds.
- Regression anchor: `ENGINE_VERSION` unchanged at 3.19.0 (this plan touches only the text/Flash path + a route; zero video-scoring bytes).
- `grep -rn "#FF7F50\|255,127,80" src/app/api/tools/react/route.ts src/lib/engine/flash/build-reaction-panel.ts` returns NOTHING (no legacy coral in new server code — N/A for server, but the gate guards copy-paste drift).
</verification>

<success_criteria>
- A `POST /api/tools/react` route exists that returns a real `{ fraction, scrollQuote }` from one Flash text-mode call, niche-discriminating via the shared `buildReactionPanel` helper.
- Both runners consume `buildReactionPanel` with byte-identical output; engine + KC regression suites green; ENGINE_VERSION 3.19.0.
- RESEARCH Open Q1 resolved (shared helper, not inline duplication) and Pitfall 2 closed (niche panel resolved, not generic).
</success_criteria>

<output>
Create `.planning/phases/13-proactive-numen-ambient-initiated-expansion-the-ambient-audi/13-01-SUMMARY.md` when done.
</output>
