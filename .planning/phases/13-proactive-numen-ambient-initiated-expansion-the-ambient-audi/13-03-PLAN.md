---
phase: 13-ambient-numen
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/components/audience-lens/ambient-presence.tsx
  - src/components/audience-lens/ambient-presence-types.ts
  - src/components/audience-lens/__tests__/ambient-presence.test.tsx
autonomous: true
requirements: [AMBIENT-01]
must_haves:
  truths:
    - "The presence renders a persistent thin persona-cloud strip — one cream-alpha dot per calibrated persona — that is always visible (the audience felt even between cards) (D-01)"
    - "When a concept is in focus the strip shows a 'reacting to: {concept}' subject and tones the dots to that ONE concept's real reaction; it NEVER aggregates across concepts (D-02)"
    - "At idle (no concept in focus) the strip shows the roster dots at calm uniform cream with the idle copy, NO reaction — it never invents a reaction to nothing (D-01 honesty spine)"
    - "Typing a thought + submitting fires the /api/tools/react route once (explicit submit only, no keystroke calls), shows 'Reading the room…', and on return sets the subject to the typed thought + tones the dots (D-04)"
    - "All motion gates on reducedMotion (hard-stop under reduce) and an sr-only roster + subject mirror is always present (accessibility)"
    - "Opening the presence (cue tap or type-to-room result) opens the SAME shipped AudienceLens scoped to the in-focus concept — no fork, no restyle (D-05)"
  artifacts:
    - path: "src/components/audience-lens/ambient-presence.tsx"
      provides: "Surfaces 1/2/4 — the persistent dot-cloud strip + spotlight subject + type-to-room input; opens the shipped AudienceLens"
      exports: ["AmbientPresence"]
    - path: "src/components/audience-lens/ambient-presence-types.ts"
      provides: "The AmbientFocus contract the composer (Plan 04) drives the spotlight with"
      exports: ["AmbientFocus", "AmbientPresenceProps"]
  key_links:
    - from: "src/components/audience-lens/ambient-presence.tsx"
      to: "src/app/api/tools/react/route.ts"
      via: "fetch POST on explicit type-to-room submit"
      pattern: "api/tools/react"
    - from: "src/components/audience-lens/ambient-presence.tsx"
      to: "src/components/audience-lens/AudienceLens.tsx"
      via: "renders <AudienceLens open onOpenChange flatPersonas conceptText> from the in-focus concept"
      pattern: "AudienceLens"
    - from: "src/components/audience-lens/ambient-presence.tsx"
      to: "src/components/audience-lens/flat-card-reactions.ts"
      via: "cardScrollQuoteReactions(fraction, scrollQuote) → flatPersonas for the Lens + dot toning"
      pattern: "cardScrollQuoteReactions"
---

<objective>
Build the persistent living-audience presence component (Surfaces 1/2/4 — D-01/D-02/D-04): a thin persona-cloud strip that is always docked, shows the calibrated roster as cream-alpha dots, spotlights ONE in-focus concept with a `reacting to: {concept}` subject (never aggregating), idles honestly when nothing is in focus, and carries a type-to-room input that fires the Wave-1 `/api/tools/react` route on explicit submit. Every open door routes to the SAME shipped `<AudienceLens>` scoped to the in-focus concept (D-05). This plan builds the component in isolation against a driven `focus` prop + `audience` prop — the composer wires it (scroll-spy + tap + mount) in Plan 04.

Purpose: This is the "always present and reacting, the audience feels real, you can interact" north star made concrete — reusing the dot-cloud family, the honest data path, the Flash reaction route, and the one Lens. Nothing here is a new visual language or a new engine.
Output: `AmbientPresence` + its focus-contract types + a happy-dom test, all reusing shipped primitives.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-proactive-numen-ambient-initiated-expansion-the-ambient-audi/13-CONTEXT.md
@.planning/phases/13-proactive-numen-ambient-initiated-expansion-the-ambient-audi/13-UI-SPEC.md
@.planning/phases/13-proactive-numen-ambient-initiated-expansion-the-ambient-audi/13-RESEARCH.md
@src/components/audience-lens/ambient-presence-types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AmbientPresence shell — dot-cloud strip + spotlight subject + idle state + Lens door (no type-to-room yet)</name>
  <read_first>
    - src/components/reading/persona-cloud.tsx (L42-67 dot sizing `rad: 4 + clamp01(weight)*9`; L101-117 the `<svg role="img">` + cream-alpha fill `rgba(236, 231, 222, ${(0.2 + clamp01(watchThrough)*0.5)})` + worst-cluster accent on `tone:'accent'`; L114-117 the `<ul className="sr-only">` mirror — reuse ALL of this at strip scale)
    - src/components/board/_kit/PersonaGraph.tsx (L59-67 `mulberry32` seed; L231-238 the `reducedMotion`-gated `<animate>` opacity pulse `values="0.85;1;0.85"` staggered by index — reuse for the calm "alive" pulse; L19-30 `PersonaNode` / `tone`)
    - src/lib/audience/audience-types.ts (L63-70 `CalibratedPersona` — `archetype`, `repaint`, `temperature`, `disposition`, `share`, optional `label`; the `Audience` interface — `name`, `is_general`, `personas[]`)
    - src/components/audience-lens/AudienceLens.tsx (L80-110 `AudienceLensProps` — `open`, `onOpenChange`, `flatPersonas?`, `conceptText?`, `reducedMotion?`; construct it directly OR wrap a cue in LensTrigger — both byte-identical depth, RESEARCH Pattern 1)
    - src/components/audience-lens/flat-card-reactions.ts (L57-63 `cardScrollQuoteReactions(fraction, scrollQuote)` → flatPersonas; `[]` on bad input)
    - .planning/phases/13-proactive-numen-ambient-initiated-expansion-the-ambient-audi/13-UI-SPEC.md (§Surface 1 form/dock/states; §Surface 2 subject rules; §Copywriting idle copy `Your people are here. Make something — or type a thought to test it.`, title `Your audience`, subtitle `{N} calibrated people` / `General audience · default panel`; §Color cream dots / worst-cluster coral / NO coral on container-border-title-subject; §Typography 13px/500 subject with muted prefix + foreground concept, `truncate`)
    - src/app/globals.css (L72 `--spacing-12`=48px collapsed height; L88/L92/L93 surface-elevated/foreground-secondary/foreground-muted; L184 `--duration-normal`; L188 `--ease-out-cubic`; L197 `--z-sticky`=200)
  </read_first>
  <action>
    First create `src/components/audience-lens/ambient-presence-types.ts` exporting: `AmbientFocus` = `{ conceptText: string; fraction: string; scrollQuote: string } | null` (null = idle), and `AmbientPresenceProps` = `{ audience: Audience | null; focus: AmbientFocus; reducedMotion?: boolean; onFocusChange?: (f: AmbientFocus) => void }` (the composer in Plan 04 owns scroll-spy/tap and passes `focus` down + receives type-to-room focus via a callback — keep the contract minimal and documented).
    Then create `src/components/audience-lens/ambient-presence.tsx` (`'use client'`) exporting `AmbientPresence`. Render a collapsed strip at 48px (`--spacing-12`) tall: the title `Your audience`, a thin horizontal dot-cloud (one `<circle>` per `audience.personas` — for General/empty personas render a small calm default roster or the subtitle `General audience · default panel`), and — WHEN `focus` is non-null — the `reacting to: {focus.conceptText}` subject (muted prefix + `--color-foreground` concept, `truncate` one line, 13px/500). Derive dot nodes from the personas using the SAME cream-alpha fill + sizing as PersonaCloud; when `focus` is non-null, tone the dots from `cardScrollQuoteReactions(focus.fraction, focus.scrollQuote)` (the worst slot reads `tone:'accent'` coral exactly as PersonaCloud paints it); when `focus` is null (IDLE), render all dots at calm uniform cream with NO reaction and show the idle copy. Gate the `<animate>` pulse on `reducedMotion` (hard-stop under reduce). Always render an sr-only `<ul>`/`role="status"` mirror of the roster + (when focused) the subject + its stop/total. Provide a ≥44px `role="button"` open cue (reuse the `LensTrigger` minHeight:44 + Enter/Space idiom) that opens `<AudienceLens open onOpenChange flatPersonas={cardScrollQuoteReactions(focus.fraction, focus.scrollQuote)} conceptText={focus.conceptText} reducedMotion={reducedMotion} />` — the open cue is disabled/no-op at idle (nothing to open). Add an expand toggle (`aria-expanded`) that grows the panel to a self-sizing `max-h-[40vh]` region on `--color-surface` with a hairline top border (the type-to-room input lands here in Task 2 — leave a labeled placeholder slot). Reuse the composer-controls outside-click/Escape effect (composer-controls.tsx L389-403) to collapse the expanded panel. Coral is FORBIDDEN on the container/border/title/subject/liveness — liveness reads via motion + cream opacity only (UI-SPEC §Color). NO `#FF7F50`. NO `Math.random`/`Date.now` in render (mulberry32 only).
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/audience-lens/__tests__/ambient-presence.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/audience-lens/ambient-presence-types.ts` exists exporting `AmbientFocus` and `AmbientPresenceProps`.
    - `src/components/audience-lens/ambient-presence.tsx` exists exporting `AmbientPresence`.
    - IDLE: `render(<AmbientPresence audience={calibrated10} focus={null} />)` shows the idle copy `Your people are here. Make something — or type a thought to test it.`, renders the roster dots, and shows NO `reacting to:` subject (assert the subject text is ABSENT).
    - FOCUSED: `render(<AmbientPresence audience={calibrated10} focus={{conceptText:"what if I open with a question?", fraction:"6/10 stop", scrollQuote:"..."}} />)` shows `reacting to: what if I open with a question?` (assert present + truncate class) and renders exactly ONE subject (never two — no aggregation).
    - The dot-cloud renders one `<circle>` per persona for the calibrated audience (10 personas → 10 dots); the sr-only `<ul>` mirror is present in BOTH idle and focused states.
    - General audience (`is_general:true, personas:[]`): renders the subtitle `General audience · default panel` and does NOT crash on empty personas.
    - Tapping the open cue when focused opens `<AudienceLens>` (assert the Lens mounts with `conceptText` = the focus concept); the cue is a no-op at idle.
    - `grep -n "#FF7F50\|255,127,80" src/components/audience-lens/ambient-presence.tsx` returns NOTHING.
    - `grep -n "Math.random\|Date.now" src/components/audience-lens/ambient-presence.tsx` returns NOTHING (deterministic; mulberry32 only — grep should find `mulberry32` referenced or imported).
    - `grep -n "reducedMotion" src/components/audience-lens/ambient-presence.tsx` ≥ 1 (motion gated).
  </acceptance_criteria>
  <done>The presence shell renders the roster always, spotlights exactly one labeled concept when focused, idles honestly when not, gates motion + ships the sr-only mirror, and opens the one shipped Lens scoped to the focus — driven entirely by a `focus` prop the composer will supply.</done>
</task>

<task type="auto">
  <name>Task 2: Type-to-room input inside the expanded presence — explicit-submit Flash reaction (D-04)</name>
  <read_first>
    - src/components/audience-lens/PersonaChatDrawer.tsx (L99-110 the `send` callback shape; L177 `PERSONA_CHAT_ERROR` copy to align the error string with; L231-233 the disabled send-gate `disabled={isStreaming || ask.trim().length === 0}`; L247-251 the `onKeyDown` Enter-submits / Shift+Enter-newline idiom — reuse verbatim)
    - src/app/api/tools/react/route.ts (NEW from Plan 13-01 — `POST` accepts `{ text, framing? }`, returns `{ fraction, scrollQuote }` or `{ error }` with 401/400/502; the contract the input fetches)
    - src/components/audience-lens/flat-card-reactions.ts (L57-63 `cardScrollQuoteReactions(fraction, scrollQuote)` — turn the route result into flatPersonas for the Lens + dot toning)
    - .planning/phases/13-proactive-numen-ambient-initiated-expansion-the-ambient-audi/13-UI-SPEC.md (§Surface 4 states: resting/active/loading/result/error; placeholder `Type a thought — see how the room reacts`; loading `Reading the room…` with `aria-live="polite"`; result honesty caption `A quick SIM read on your {audienceName} — not a full Test.`; CTA `Test this →`; error `Couldn't reach the audience right now. Your thought is saved — try again in a moment.` → quiet `Retry →`, NEVER error-red; send disabled until ≥1 non-whitespace char; SIM fires on EXPLICIT submit only)
  </read_first>
  <action>
    Add the type-to-room input into the expanded-panel slot left in Task 1. Render a single-line `<textarea>`/`<input>` (42px tall, `--color-surface`, border `--color-border`, focus → `--color-border-hover` + `--color-active` lift) with the placeholder `Type a thought — see how the room reacts` and a send affordance labeled `Test this →` (the accessible name) that is `disabled` until the trimmed value has ≥1 non-whitespace char. Reuse the PersonaChatDrawer keydown idiom: Enter submits, Shift+Enter newline. On EXPLICIT submit only (button click or Enter — NEVER on keystroke/debounce; the live-composer-as-you-type stretch is DEFERRED per CONTEXT): set a local `loading` state, render `Reading the room…` in an `aria-live="polite"` region, and `fetch("/api/tools/react", { method:"POST", body: JSON.stringify({ text }) })`. On success (`{ fraction, scrollQuote }`): build `flatPersonas = cardScrollQuoteReactions(fraction, scrollQuote)`, set the spotlight subject to the typed thought (call `onFocusChange?.({ conceptText: text, fraction, scrollQuote })` so the composer's focus state updates AND tone the local dots), show the honesty caption `A quick SIM read on your {audience.name} — not a full Test.`, and auto-open `<AudienceLens open conceptText={text} flatPersonas={flatPersonas} …>` scoped to the thought. On error (non-200 or throw): show the verbatim error copy + a quiet `Retry →` in `--color-foreground-muted`/`--color-cream-secondary` (NEVER error-red) that re-submits the preserved text (mirror the PersonaChatDrawer `lastQuestion` retry pattern). The reaction is EPHEMERAL — do not persist it to the thread (RESEARCH Open Q3). NO coral on the input/placeholder/send/loading (UI-SPEC §Color). Keep `runFlashTextMode`/Qwen OFF the client — the input only `fetch`es the server route (RESEARCH anti-pattern: no client Qwen).
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/audience-lens/__tests__/ambient-presence.test.tsx 2>&1 | tail -20 && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The expanded presence renders the input with placeholder `Type a thought — see how the room reacts` and a `Test this →` send affordance.
    - Send is `disabled` for empty / whitespace-only input and ENABLED once ≥1 non-whitespace char is typed (assert the disabled attribute toggles).
    - Submitting (mock `fetch` resolving `{ fraction:"7/10 stop", scrollQuote:"..." }`) fires exactly ONE `fetch` to `/api/tools/react` with the typed `text` in the body; assert NO fetch fires on keystroke (type without submitting → fetch not called).
    - During the await, `Reading the room…` is shown inside an `aria-live="polite"` region.
    - On success: the subject becomes the typed thought (`onFocusChange` called with `{ conceptText: text, fraction, scrollQuote }`), the honesty caption `A quick SIM read on your {name} — not a full Test.` renders, and `<AudienceLens>` opens with `conceptText` = the typed thought.
    - On error (mock `fetch` rejecting or returning 502): the verbatim error copy renders with a `Retry →` affordance; `grep -n "color-error\|text-red\|#ef" ` against the error branch finds NOTHING (never error-red).
    - `grep -n "runFlashTextMode\|getQwenClient" src/components/audience-lens/ambient-presence.tsx` returns NOTHING (no client-side Qwen).
    - `grep -n "#FF7F50\|255,127,80" src/components/audience-lens/ambient-presence.tsx` returns NOTHING.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Type-to-room is live: an explicit submit fires one server reaction, shows the honest loading + caption, sets the spotlight to the typed thought, and opens the one Lens scoped to it — with an honest non-red retry on failure and zero client-side model calls.</done>
</task>

</tasks>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/components/audience-lens` — all green (new ambient-presence suite + existing lens suites unaffected).
- `npm run build` succeeds.
- `grep -rn "#FF7F50\|255,127,80\|Math.random\|Date.now\|getQwenClient\|runFlashTextMode" src/components/audience-lens/ambient-presence.tsx src/components/audience-lens/ambient-presence-types.ts` returns NOTHING.
- Determinism + engine posture: the presence makes ZERO model calls on render/re-focus (reads driven `focus` data); the ONLY model call is one explicit type-to-room submit via the server route. ENGINE_VERSION unchanged at 3.19.0.
</verification>

<success_criteria>
- `AmbientPresence` exists and reuses the shipped dot-cloud family, the honest `cardScrollQuoteReactions` path, the type-to-room server route, and the one `<AudienceLens>` — no fork, no new visual language, no new engine.
- Idle/focused/type-to-room states all honor the honesty spine (one labeled concept, idle roster, never fabricate, degrade to quiet).
- Motion gated on `reducedMotion`; sr-only mirror always present.
</success_criteria>

<output>
Create `.planning/phases/13-proactive-numen-ambient-initiated-expansion-the-ambient-audi/13-03-SUMMARY.md` when done.
</output>
