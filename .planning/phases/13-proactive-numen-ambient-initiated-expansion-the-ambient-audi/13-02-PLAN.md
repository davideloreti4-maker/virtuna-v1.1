---
phase: 13-ambient-numen
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/audience-lens/card-reaction-at-rest.tsx
  - src/components/audience-lens/__tests__/card-reaction-at-rest.test.tsx
  - src/components/thread/hook-card-block.tsx
  - src/components/thread/idea-card-block.tsx
  - src/components/thread/script-card-block.tsx
  - src/components/thread/remix-card-block.tsx
autonomous: true
requirements: [AMBIENT-01]
must_haves:
  truths:
    - "Every generated skill card (idea, hook, script, remix) shows its room reacting AT REST — the real stop fraction + a thin sentiment ribbon — without the creator tapping (D-01/D-03)"
    - "The resting reaction reuses each card's already-emitted fraction + scrollQuote — zero new model calls (determinism-gate-safe, ENGINE_VERSION 3.19.0)"
    - "When the card's fraction is unparseable / no signal, the cue collapses to its plain child — no '0/0', no placeholder, silence is the honest degrade (D-02 honesty spine)"
    - "Tapping the promoted cue still opens the SAME shipped AudienceLens scoped to that card (D-05 — the per-card tap is unchanged)"
    - "A positive reaction renders in cream, never coral; coral on the ribbon is reserved for the worst-cluster signal only; new code uses var(--color-accent), never the legacy #FF7F50"
  artifacts:
    - path: "src/components/audience-lens/card-reaction-at-rest.tsx"
      provides: "The resting-state reaction readout (stop fraction + thin ribbon) rendered INSIDE the existing LensTrigger on each card — one shared sub-component, no 4-way duplication"
      exports: ["CardReactionAtRest"]
  key_links:
    - from: "src/components/thread/hook-card-block.tsx"
      to: "src/components/audience-lens/card-reaction-at-rest.tsx"
      via: "CardReactionAtRest rendered inside <LensTrigger> above the blockquote"
      pattern: "CardReactionAtRest"
    - from: "src/components/audience-lens/card-reaction-at-rest.tsx"
      to: "the card's real fraction"
      via: "parse N/T from fraction string; no model call"
      pattern: "stop|total"
---

<objective>
Promote the per-card reaction from a quiet "tap to see the room" cue into a resting-state reaction readout on all four generated skill cards (Surface 3, D-01/D-03). Today each of `idea/hook/script/remix-card-block.tsx` already wraps its lead `scrollQuote` blockquote in `<LensTrigger flatPersonas={cardScrollQuoteReactions(fraction, scrollQuote)} …>`. This plan adds, INSIDE that same wrapper (no sibling element), the real `{stop}/{total} stop` fraction + a thin `h-[3px]` cream-vs-muted sentiment ribbon — keeping the existing italic verbatim quote and the existing band chip exactly where they are.

Purpose: "Always felt, never summoned" begins at the card — the room's reaction is visible at rest, not hidden behind a tap. This is pure presentation of data the card already emits: zero new model calls, determinism-gate-safe, no ENGINE_VERSION bump.
Output: One shared `CardReactionAtRest` sub-component + its test + the 4 card blocks edited to render it inside their LensTrigger.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-proactive-numen-ambient-initiated-expansion-the-ambient-audi/13-CONTEXT.md
@.planning/phases/13-proactive-numen-ambient-initiated-expansion-the-ambient-audi/13-UI-SPEC.md
@.planning/phases/13-proactive-numen-ambient-initiated-expansion-the-ambient-audi/13-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the shared CardReactionAtRest sub-component (honest, token-correct, degrades to nothing)</name>
  <read_first>
    - src/components/audience-lens/flat-card-reactions.ts (L35-45 `parseFraction(fraction)` matches `/(\d+)\s*\/\s*(\d+)/` → `{ stop, total }` or null; L57-63 `cardScrollQuoteReactions` returns `[]` when the fraction can't be parsed — the honest-degrade contract)
    - src/components/thread/hook-card-block.tsx (L133-151 the LensTrigger + blockquote structure to render INSIDE; L168 existing band-chip fraction rendering for tone reference)
    - .planning/phases/13-proactive-numen-ambient-initiated-expansion-the-ambient-audi/13-UI-SPEC.md (§Surface 3 anatomy: fraction at 13px/500 `--color-foreground` `tabular-nums`; ribbon `h-[3px] rounded-full` cream(stop) vs muted(scroll) track; quote stays 14px/400 italic; §Color "positive reaction = cream, never coral"; "Known inconsistency" — use `var(--color-accent)` never `#FF7F50`)
    - src/components/board/_kit/PersonaGraph.tsx (L30 `tone?: 'accent' | 'default'` — the worst-cluster coral convention the ribbon follows)
  </read_first>
  <action>
    Create `src/components/audience-lens/card-reaction-at-rest.tsx` (`'use client'`) exporting `CardReactionAtRest` taking props `{ fraction: string }`. Parse the fraction with the SAME regex contract as `parseFraction` in flat-card-reactions.ts (`/(\d+)\s*\/\s*(\d+)/` → `{ stop, total }`); when it does NOT parse, `return null` (render nothing — honesty degrade, mirroring `cardScrollQuoteReactions([])`). When it parses, render a small two-row block: (row 1) `{stop}/{total} stop` text at 13px / font-medium / `text-foreground` with `tabular-nums`; (row 2) a thin sentiment ribbon — a `h-[3px] rounded-full` container with `--color-foreground-muted` (or a muted track) as the scroll track and a cream (`--color-foreground`) fill whose width = `stop/total * 100%`. Positive/neutral fill is CREAM, never coral. Do NOT use `#FF7F50` or `rgba(255,127,80,…)` anywhere — if a coral accent is ever needed it is `var(--color-accent)` and reserved for a worst-cluster signal only (out of scope for v1 — keep the ribbon cream). The component renders NO quote (the blockquote stays in the card) and NO band chip (it stays in the card's secondary row). Keep it under ~60 lines. Source the OS motion preference is NOT needed here (static readout — the dot pulse lives on the presence, not the ribbon).
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/audience-lens/__tests__/card-reaction-at-rest.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/audience-lens/card-reaction-at-rest.tsx` exists and exports `CardReactionAtRest`.
    - `render(<CardReactionAtRest fraction="6/10 stop" />)` shows the text `6/10 stop` and renders a ribbon element with a fill width derived from 6/10 (60%).
    - `render(<CardReactionAtRest fraction="not-a-fraction" />)` renders NOTHING (`container.firstChild === null`) — the honest degrade.
    - `render(<CardReactionAtRest fraction="" />)` renders NOTHING.
    - `grep -n "#FF7F50\|255,127,80" src/components/audience-lens/card-reaction-at-rest.tsx` returns NOTHING (token-correct; UI-SPEC Pitfall 3).
    - `grep -n "blockquote\|scrollQuote\|band\b" src/components/audience-lens/card-reaction-at-rest.tsx` returns NOTHING (no quote, no band chip — those stay in the card).
    - The fraction text uses `tabular-nums` (grep confirms).
  </acceptance_criteria>
  <behavior>
    - fraction "6/10 stop" → renders "6/10 stop" + a ribbon at 60% cream fill.
    - fraction "10/10 stop" → ribbon at 100%.
    - fraction "garbage" / "" → renders null (nothing).
  </behavior>
  <done>A single honest, token-correct resting-reaction readout exists that degrades to nothing on bad input — ready to drop inside all four cards' LensTrigger with no duplication.</done>
</task>

<task type="auto">
  <name>Task 2: Render CardReactionAtRest inside the LensTrigger on all four card blocks</name>
  <read_first>
    - src/components/thread/hook-card-block.tsx (L31-33 imports; L133-151 the `<LensTrigger>` wrapping the blockquote — render `<CardReactionAtRest fraction={fraction} />` as the FIRST child inside LensTrigger, above the blockquote; L110-112 the existing `#FF7F50` archetype-chip — migrate to `var(--color-accent)` ONLY if the executor is already editing that line, do not expand scope to untouched CTAs)
    - src/components/thread/idea-card-block.tsx (L33-34 imports; L159-178 the LensTrigger + child; L129-131 `#FF7F50` chip)
    - src/components/thread/script-card-block.tsx (L27-28 imports; L104-122 the LensTrigger + child)
    - src/components/thread/remix-card-block.tsx (L36-37 imports; L153-174 the LensTrigger + child; L108-110 `#FF7F50` chip)
    - src/components/audience-lens/card-reaction-at-rest.tsx (NEW from Task 1)
  </read_first>
  <action>
    In each of the four card blocks, import `CardReactionAtRest` from `@/components/audience-lens/card-reaction-at-rest` and render `<CardReactionAtRest fraction={fraction} />` as the FIRST child INSIDE the existing `<LensTrigger>…</LensTrigger>`, immediately above the existing `<blockquote>`. Do NOT add a sibling element outside the LensTrigger, do NOT change the LensTrigger's `flatPersonas`/`conceptText`/`rewrite`/`label` props, do NOT touch the secondary band-chip row, do NOT change the blockquote. The honest collapse is automatic: when `cardScrollQuoteReactions(fraction, scrollQuote)` returns `[]`, `LensTrigger` already returns its plain children (the blockquote + the now-nested CardReactionAtRest, which itself returns null on an unparseable fraction) — so a no-signal card shows neither ribbon nor a broken cue. While editing each card's imports/JSX region, if you touch a line carrying the legacy `#FF7F50` archetype-chip color, migrate THAT line to `var(--color-accent)` (UI-SPEC: correct, don't propagate) — but do not refactor untouched CTA buttons elsewhere in the file (scope discipline). Each card's `fraction` is already in scope (e.g. hook-card-block L62/L168) — no new prop threading.
  </action>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/components/thread src/components/audience-lens 2>&1 | tail -25 && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CardReactionAtRest" src/components/thread/hook-card-block.tsx` ≥ 2 (import + usage) AND same for idea-/script-/remix-card-block.tsx (all four cards promoted).
    - In each card, `CardReactionAtRest` appears textually BETWEEN the `<LensTrigger` opening tag and the `<blockquote` tag (rendered inside the wrapper, above the quote) — verify by reading the edited region.
    - No card added a reaction element OUTSIDE its `<LensTrigger>` (the cue must collapse with the Lens degrade; assert no new JSX sibling after `</LensTrigger>`).
    - The four `<LensTrigger>` `flatPersonas={cardScrollQuoteReactions(fraction, scrollQuote)}` props are UNCHANGED (grep still finds them verbatim on all four).
    - `npm run build` succeeds.
    - The existing thread + audience-lens test suites pass (no regression to card rendering, Lens chat gate, or rewrite wiring).
    - No NEW `#FF7F50` introduced: `git diff` adds zero `#FF7F50` lines (any change to that token is a removal/migration to `var(--color-accent)`, never an addition).
  </acceptance_criteria>
  <done>All four generated cards show the room reacting at rest inside the same shipped LensTrigger; tapping still opens the one AudienceLens scoped to that card; no-signal cards degrade to silence; no new model calls; ENGINE_VERSION 3.19.0.</done>
</task>

</tasks>

<verification>
- `node ./node_modules/vitest/vitest.mjs run src/components/thread src/components/audience-lens` — all green (card rendering, lens-chat-gate, card-rewrite, population-swarm suites unaffected).
- `npm run build` succeeds.
- `git diff --unified=0 src/components/thread | grep '^+' | grep -c '#FF7F50'` returns 0 (no new legacy coral added).
- Determinism: zero new model calls added — the resting reaction reads `fraction`/`scrollQuote` already on the card. ENGINE_VERSION unchanged at 3.19.0.
</verification>

<success_criteria>
- Surface 3 shipped on all four card blocks via one shared `CardReactionAtRest` (no 4-way duplication), reusing the shipped LensTrigger + honest degrade.
- Positive reactions read cream; new code is token-correct (`var(--color-accent)`, no `#FF7F50`).
- The per-card tap still opens the same single AudienceLens (D-05 preserved).
</success_criteria>

<output>
Create `.planning/phases/13-proactive-numen-ambient-initiated-expansion-the-ambient-audi/13-02-SUMMARY.md` when done.
</output>
