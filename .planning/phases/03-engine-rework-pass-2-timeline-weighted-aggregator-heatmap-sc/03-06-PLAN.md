---
phase: 03
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 04]
files_modified:
  - src/lib/engine/wave3/persona-prompts-pass2.ts
  - src/lib/engine/wave3/pass2.ts
  - src/lib/engine/__tests__/pass2.test.ts
autonomous: true
requirements: [R2, NF4]
tags: [phase-3, pass2, thinking-mode, orchestrator]

must_haves:
  truths:
    - "runWave3Pass2 fires 10 parallel qwen3.6-plus calls with enable_thinking=true, thinking_budget=8000 (D-02, D-03)"
    - "Each call receives Wave 0 segments + keyframeUris (nullable) + Pass 1 verdict + persona archetype (D-03)"
    - "Pass 2 prompt enriched with demographic context + time-of-day scrolling state (D-04)"
    - "Pass2ResponseSchema validates D-05 output shape (attention in [0,1])"
    - "Quality guards drop personas on Zod failure, segment_count mismatch, or out-of-range attention (D-06)"
    - "Returns pass2_aggregate_built=true only when at least 7 of 10 succeed (D-06)"
    - "D-23 telemetry: every persona logs pass2_latency_ms and pass2_cost_cents; wave-level p50/p95"
    - "D-24 cost ceiling: flag if any single analysis Pass 2 exceeds 0.50 dollars via structured logger error"
  artifacts:
    - path: src/lib/engine/wave3/persona-prompts-pass2.ts
      provides: "STABLE_PASS2_SYSTEM_PROMPT + buildPass2UserContent + Pass2ResponseSchema"
      contains: "Pass2ResponseSchema"
      min_lines: 80
    - path: src/lib/engine/wave3/pass2.ts
      provides: "runWave3Pass2 orchestrator + Wave3Pass2Outcome type"
      contains: "export async function runWave3Pass2"
      min_lines: 150
  key_links:
    - from: src/lib/engine/wave3/pass2.ts
      to: src/lib/engine/wave3/persona-prompts-pass2.ts
      via: "import STABLE_PASS2_SYSTEM_PROMPT, buildPass2UserContent, Pass2ResponseSchema"
      pattern: "persona-prompts-pass2"
    - from: src/lib/engine/wave3/pass2.ts
      to: src/lib/engine/wave3/weighted-aggregator.ts
      via: "import type Pass2PersonaResult (single source of truth)"
      pattern: "Pass2PersonaResult"
    - from: src/lib/engine/wave3/pass2.ts
      to: src/lib/engine/qwen/client.ts
      via: "QWEN_REASONING_MODEL + getQwenClient"
      pattern: "QWEN_REASONING_MODEL"
---

<objective>
Implement the Pass 2 dedicated per-persona timeline call (D-01..D-06, D-23, D-24). Centerpiece engine extension: 10 parallel qwen3.6-plus thinking-mode calls per analysis, each receiving Wave 0 segments + keyframe images + Pass 1 verdict + persona archetype, producing per-segment attention timeline with reasoning at inflection points.

Two coordinated files:

1. persona-prompts-pass2.ts — STABLE_PASS2_SYSTEM_PROMPT (cache-friendly, no per-request interpolation), buildPass2UserContent (OpenAI content array with image_url items + text), Pass2ResponseSchema (Zod validates D-05 output shape).
2. pass2.ts — orchestrator mirroring wave3.ts 10-parallel / Promise.allSettled / SUCCESS_THRESHOLD=7 pattern. Adds enable_thinking + thinking_budget=8000 + 60s per-call timeout (thinking is slower than flash). Emits per-persona stage events. Logs D-23 telemetry. Flags D-24 cost ceiling.

Output: 2 new source files + Plan 01 pass2.test.ts flipped from 12 it.skip stubs to 12 live tests, all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-CONTEXT.md
@.planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-RESEARCH.md
@.planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-PATTERNS.md
@src/lib/engine/wave3.ts
@src/lib/engine/wave3/persona-prompts.ts
@src/lib/engine/wave3/persona-registry.ts

<interfaces>
Pass2PersonaResult re-export source of truth is in Plan 04 weighted-aggregator.ts. pass2.ts imports the type and re-exports for downstream consumers.

D-05 Pass 2 output schema (Zod):
- persona_id: string
- segment_reactions: array of { t_start, t_end, attention 0-1, reason? max 400, swipe_predicted bool }
- pass2_latency_ms: number
- pass2_cost_cents: number

D-06 quality guards:
- attention in [0,1] (Zod handles)
- segment_reactions.length === segments.length (manual check after parse; persona dropped on mismatch)
- Below 7/10 successes → pass2_aggregate_built=false (Plan 08 sets heatmap=null upstream)

Thinking-mode call pattern (verbatim from 03-PATTERNS.md lines 67-82):
- model: QWEN_REASONING_MODEL
- response_format: json_object
- enable_thinking: true (with @ts-expect-error)
- thinking_budget: 8000 (with @ts-expect-error)

wave3.ts patterns to mirror exactly (lines 33-38, 75-271):
- PER_CALL_TIMEOUT_MS = 60_000 (vs 45_000 in Pass 1 since thinking is slower)
- SUCCESS_THRESHOLD = 7
- emitStageStart(onEvent, "wave_3_pass2", 3) wave-level
- per-persona stage name: wave_3_pass2_persona_{archetype}_{slot_type}
- Promise.allSettled + survivor counting + warnings array
- Retry-once on Zod validation failure ONLY (never on AbortError)
- Cost rollup via calculateCost(QWEN_REASONING_MODEL, response.usage)

D-04 prompt enrichment:
- Creator demographic context: age_bucket, geo, follower_tier
- Time-of-day scrolling state grounding
- Both injected as STATIC blocks in STABLE_PASS2_SYSTEM_PROMPT so DashScope cache prefix covers the full tuple
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement persona-prompts-pass2.ts</name>
  <files>src/lib/engine/wave3/persona-prompts-pass2.ts</files>
  <read_first>
    - src/lib/engine/wave3/persona-prompts.ts (FULL file — STABLE_SYSTEM_PROMPT pattern, volatile user builder, Zod response schema)
    - src/lib/engine/wave3/persona-registry.ts (FULL file — PersonaSlot type, archetype values, slot_type values)
    - src/lib/engine/types.ts (SegmentGrid, PersonaSimulationResult — Pass 1 result type to consume)
    - .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-PATTERNS.md lines 138-205 (persona-prompts-pass2.ts structure)
    - .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-CONTEXT.md lines 49-65 (D-03, D-04, D-05 verbatim)
    - .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-RESEARCH.md lines 543-568 (DashScope thinking-mode + image_url content array)
  </read_first>
  <behavior>
    - STABLE_PASS2_SYSTEM_PROMPT is byte-identical across calls (cache key prefix preservation)
    - buildPass2UserContent returns OpenAI content array; image_url items appear only for non-null keyframeUris; text block always last
    - Pass2ResponseSchema rejects attention values outside [0,1] (Zod min/max constraints)
    - Pass2ResponseSchema accepts optional reason of up to 400 chars
    - Module exports DemographicContext type with age_bucket, geo, follower_tier, time_of_day, scrolling_state slots
  </behavior>
  <action>
    Create src/lib/engine/wave3/persona-prompts-pass2.ts with:

    Imports: z from zod; PersonaSlot from ./persona-registry; SegmentGrid + PersonaSimulationResult from ../types.

    Export DemographicContext interface (D-04). Optional fields: age_bucket ('13-17'|'18-24'|'25-34'|'35-44'|'45+'), geo string, follower_tier ('new'|'growing'|'established'|'creator'), time_of_day ('morning'|'midday'|'afternoon'|'evening'|'late_night'), scrolling_state ('commute'|'work_break'|'leisure'|'pre_sleep').

    Export STABLE_PASS2_SYSTEM_PROMPT constant — a single multi-line backtick string covering: (a) task description (simulate single TikTok viewer, receive segments + keyframes + Pass 1 verdict + archetype + demo + scrolling state), (b) per-segment output rules with reason populated ONLY at inflection points (first segment, drops of at least 0.20, swipe moment, final segment), (c) D-06 hard constraints (attention in [0,1], EXACTLY N segment_reactions matching segments.length, t_start/t_end match input, swipe_predicted monotonic after swipe moment, reason at most 200 chars, strict JSON output, no markdown), (d) D-05 output JSON schema example with placeholder 0 for latency/cost.

    Export buildPass2UserContent(slot, pass1, segments, keyframeUris, demo?) returning array of items each shaped `{ type: 'image_url'; image_url: { url: string } } | { type: 'text'; text: string }`. Push one image_url item per non-null entry in keyframeUris; finally push one text item produced by buildPass2TextBlock.

    Internal buildPass2TextBlock(slot, pass1, segments, demo?) returns a multi-line string with: persona archetype + slot_type + id, demographic+scrolling line (each field falls back to 'unknown' when undefined), Pass 1 verdict fields (watch_through_pct, share_intent, save_intent, comment_intent — fallback 'n/a'), then JSON.stringify of segments mapped to { idx, t_start, t_end, visual_event, audio_event, is_hook_zone }.

    Export Pass2ResponseSchema = z.object({ persona_id: z.string(), segment_reactions: z.array(z.object({ t_start: z.number().min(0), t_end: z.number().min(0), attention: z.number().min(0).max(1), reason: z.string().max(400).optional(), swipe_predicted: z.boolean() })), pass2_latency_ms: z.number(), pass2_cost_cents: z.number() }). Export type Pass2Response = z.infer<typeof Pass2ResponseSchema>.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-result-surface &amp;&amp; pnpm tsc --noEmit 2&gt;&amp;1 | grep "persona-prompts-pass2" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/engine/wave3/persona-prompts-pass2.ts exists
    - grep -c "export const STABLE_PASS2_SYSTEM_PROMPT" src/lib/engine/wave3/persona-prompts-pass2.ts returns 1
    - grep -c "export function buildPass2UserContent" src/lib/engine/wave3/persona-prompts-pass2.ts returns 1
    - grep -c "export const Pass2ResponseSchema" src/lib/engine/wave3/persona-prompts-pass2.ts returns 1
    - grep -c "export interface DemographicContext" src/lib/engine/wave3/persona-prompts-pass2.ts returns 1
    - grep -c "image_url" src/lib/engine/wave3/persona-prompts-pass2.ts returns at least 2
    - grep -c "z.number().min(0).max(1)" src/lib/engine/wave3/persona-prompts-pass2.ts returns 1
    - File line count is at least 80
    - pnpm tsc --noEmit produces no new errors in this file
  </acceptance_criteria>
  <done>persona-prompts-pass2.ts implemented; exports verified; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pass2.ts orchestrator</name>
  <files>src/lib/engine/wave3/pass2.ts, src/lib/engine/__tests__/pass2.test.ts</files>
  <read_first>
    - src/lib/engine/wave3.ts (FULL file — orchestration pattern to MIRROR end-to-end: constants 33-38, wave-level emitStageStart/End 75-109, per-persona callPersona shape 127-243 including retry-once loop, Promise.allSettled survivor count 246-268)
    - src/lib/engine/wave3/persona-prompts-pass2.ts (Task 1 output)
    - src/lib/engine/wave3/persona-registry.ts (selectPersonaSlots returns 10 slots with archetype/slot_type/id)
    - src/lib/engine/wave3/weighted-aggregator.ts (Plan 04 — Pass2PersonaResult source of truth)
    - src/lib/engine/qwen/client.ts (QWEN_REASONING_MODEL constant + getQwenClient)
    - src/lib/engine/qwen/cost.ts (calculateCost helper — already priced for qwen3.6-plus)
    - src/lib/engine/events.ts (Plan 02 added pass2_persona_start, pass2_persona_end variants)
    - src/lib/engine/__tests__/wave3.test.ts (FULL file — copy mock setup verbatim into pass2.test.ts: logger mock, Sentry mock, openai hoisted mock, DASHSCOPE_API_KEY env)
    - src/lib/engine/__tests__/pass2.test.ts (Plan 01 stub — flip 12 it.skip to live it())
    - .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-PATTERNS.md lines 40-135 (pass2.ts structure)
    - .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-PATTERNS.md lines 597-609 (12-test surface)
    - .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-RESEARCH.md lines 320-355 (Pattern 1 — Pass 2 Orchestrator)
    - .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-CONTEXT.md lines 173-176 (D-23 + D-24 telemetry)
  </read_first>
  <behavior>
    The 12 tests from Plan 01 stub (now flipped to live):
    1. Fires exactly 10 parallel calls with enable_thinking: true (assert mockCreate.mock.calls.length === 10 and each call.args[0].enable_thinking === true)
    2. All-succeed → 10 Pass2PersonaResult entries with full shape
    3. 7/10 succeed → pass2_aggregate_built=true + 3 failure warnings
    4. 5/10 succeed → pass2_aggregate_built=false (Plan 08 uses this to null heatmap)
    5. Promise.allSettled isolation — 10 attempted even with rejections
    6. Cost telemetry: wave_3_pass2 stage_end carries cost_cents at least 0
    7. Events: 22 stage events when all 10 succeed (10 per-persona pairs + 1 wave pair)
    8. Zod validation failure → retry-once → 11 total API calls
    9. segment count mismatch validation → persona dropped from aggregate (D-06)
    10. AbortError → no retry → slot fails after first attempt
    11. thinking_budget: 8000 present in every API call argument
    12. D-23 telemetry: pass2_latency_ms and pass2_cost_cents logged per persona (assert log.info called with object containing both keys)
  </behavior>
  <action>
    Create src/lib/engine/wave3/pass2.ts. Mirror src/lib/engine/wave3.ts structure end-to-end with these changes:

    Module constants:
    - PER_CALL_TIMEOUT_MS = 60_000 (vs 45_000 in Pass 1)
    - SUCCESS_THRESHOLD = 7
    - COST_ALERT_THRESHOLD_CENTS = 50 (D-24: 0.50 dollars)

    Imports: Sentry, createLogger from @/lib/logger, StageEventCallback + emitStageStart + emitStageEnd from ../events, selectPersonaSlots + PersonaSlot from ./persona-registry, STABLE_PASS2_SYSTEM_PROMPT + buildPass2UserContent + Pass2ResponseSchema + DemographicContext from ./persona-prompts-pass2, getQwenClient + QWEN_REASONING_MODEL from ../qwen/client, calculateCost from ../qwen/cost, PersonaSimulationResult + SegmentGrid from ../types, Pass2PersonaResult from ./weighted-aggregator (re-export this type from pass2.ts as well — single source of truth: type Pass2PersonaResult re-exported via `export type { Pass2PersonaResult } from "./weighted-aggregator"`).

    Export Wave3Pass2Outcome interface: { pass2Results: Pass2PersonaResult[]; warnings: string[]; cost_cents: number; pass2_success_count: number; pass2_aggregate_built: boolean }.

    Export async function runWave3Pass2(segments: SegmentGrid[], keyframeUris: (string | null)[], pass1Results: PersonaSimulationResult[], demo?: DemographicContext, onEvent?: StageEventCallback): Promise<Wave3Pass2Outcome>.

    Function body mirrors wave3.ts orchestration:
    1. Read slots via selectPersonaSlots() (deterministic 10 slots — see persona-registry.ts).
    2. Emit wave-level start: const stageStart = emitStageStart(onEvent, "wave_3_pass2", 3).
    3. Inner async callPersona(slot, pass1, idx):
       a. Per-persona stage name: `wave_3_pass2_persona_${slot.archetype}_${slot.slot_type}`.
       b. Emit pass2_persona_start event (new in Plan 02): onEvent?.({ type: "pass2_persona_start", persona_id: slot.id ?? slot.archetype, archetype: slot.archetype }).
       c. emitStageStart for per-persona.
       d. AbortController with PER_CALL_TIMEOUT_MS setTimeout.
       e. Retry-once loop (attempt from 0 to 1): try ai.chat.completions.create with model=QWEN_REASONING_MODEL, messages=[{role:'system', content: STABLE_PASS2_SYSTEM_PROMPT}, {role:'user', content: buildPass2UserContent(slot, pass1, segments, keyframeUris, demo)}], response_format json_object, enable_thinking: true (with @ts-expect-error), thinking_budget: 8000 (with @ts-expect-error), `as never`, signal controller.signal.
       f. Parse choices[0].message.content via Pass2ResponseSchema.safeParse; on failure mark isValidation=true and continue retry loop (max attempt=1); never retry AbortError.
       g. Quality guard (D-06): if parsed.segment_reactions.length !== segments.length, throw `validation failed: segment_reactions.length mismatch (${parsed.segment_reactions.length} vs ${segments.length})`.
       h. callCostCents = calculateCost(QWEN_REASONING_MODEL, response.usage ?? undefined); accumulate across retries.
       i. Build Pass2PersonaResult { persona_id, archetype: slot.archetype, slot_type: slot.slot_type, segment_reactions: parsed.segment_reactions, pass2_latency_ms: Math.round(performance.now()-callStart), pass2_cost_cents: +callCostAccum.toFixed(6) }.
       j. D-23 telemetry: log.info("pass2 persona complete", { archetype, persona_id, pass2_latency_ms, pass2_cost_cents }).
       k. Emit pass2_persona_end event with latency_ms + cost_cents + ok=true.
       l. emitStageEnd for per-persona with cost_cents + ok=true.
       m. Return result.
       Errors: emit pass2_persona_end with ok=false; emitStageEnd with warning; Sentry capture with tags { stage: "wave_3_pass2", archetype, slot_type }; return rejected promise (or sentinel that Promise.allSettled captures).
    4. Outer: const settled = await Promise.allSettled(slots.map((slot, i) => callPersona(slot, pass1Results[i]!, i))).
    5. Walk settled: collect survivors into pass2Results[]; collect rejection.reason.message into warnings[]; tally total cost_cents.
    6. pass2_success_count = pass2Results.length; pass2_aggregate_built = pass2_success_count >= SUCCESS_THRESHOLD.
    7. D-24 cost ceiling: if total cost_cents > COST_ALERT_THRESHOLD_CENTS (50), log.error("pass2 cost ceiling exceeded", { total_cost_cents, threshold_cents: 50 }) and Sentry.captureException(new Error("pass2_cost_ceiling")).
    8. emitStageEnd(onEvent, "wave_3_pass2", 3, stageStart, { cost_cents: +totalCostCents.toFixed(4), ok: pass2_aggregate_built, warning: pass2_aggregate_built ? undefined : "wave_3_pass2_below_threshold" }).
    9. Return Wave3Pass2Outcome.

    Update src/lib/engine/__tests__/pass2.test.ts:
    Copy mock infrastructure from wave3.test.ts lines 27-62 verbatim. Add fixture factories: makeSegments(count) per Plan 01 stub; mockPass2Response(segmentCount) returning { choices: [{ message: { content: JSON.stringify({ persona_id, segment_reactions: array of length segmentCount with valid attention, pass2_latency_ms: 1200, pass2_cost_cents: 0.3 }) } }], usage: { prompt_tokens: 5000, completion_tokens: 400 } }; makePass1Result(idx) returning a valid PersonaSimulationResult mock.

    Flip all 12 it.skip cases to live it() with concrete assertions per behavior block. Remove @ts-expect-error pending comment.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-result-surface &amp;&amp; pnpm vitest run src/lib/engine/__tests__/pass2.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/engine/wave3/pass2.ts exists, line count at least 150
    - grep -c "export async function runWave3Pass2" src/lib/engine/wave3/pass2.ts returns 1
    - grep -c "export interface Wave3Pass2Outcome" src/lib/engine/wave3/pass2.ts returns 1
    - grep -c "QWEN_REASONING_MODEL" src/lib/engine/wave3/pass2.ts returns at least 2 (import + call)
    - grep -c "enable_thinking: true" src/lib/engine/wave3/pass2.ts returns 1
    - grep -c "thinking_budget: 8000" src/lib/engine/wave3/pass2.ts returns 1
    - grep -c "SUCCESS_THRESHOLD = 7" src/lib/engine/wave3/pass2.ts returns 1
    - grep -c "PER_CALL_TIMEOUT_MS = 60_000" src/lib/engine/wave3/pass2.ts returns 1
    - grep -cE "COST_ALERT_THRESHOLD_CENTS|cost_ceiling" src/lib/engine/wave3/pass2.ts returns at least 1 (D-24)
    - grep -c "Promise.allSettled" src/lib/engine/wave3/pass2.ts returns 1
    - grep -c "calculateCost" src/lib/engine/wave3/pass2.ts returns at least 1
    - grep -c "pass2_persona_start" src/lib/engine/wave3/pass2.ts returns at least 1
    - grep -c "pass2_persona_end" src/lib/engine/wave3/pass2.ts returns at least 2 (success path + error path)
    - grep -c "@ts-expect-error" src/lib/engine/wave3/pass2.ts returns at least 2 (DashScope extensions)
    - Test file src/lib/engine/__tests__/pass2.test.ts has 0 `it.skip(` (grep -v '^//' | grep -c "it.skip(" returns 0)
    - Test file contains 12 live `it(` cases
    - pnpm vitest run src/lib/engine/__tests__/pass2.test.ts exits 0 with all 12 tests passing
    - pnpm tsc --noEmit produces no new errors in pass2.ts or pass2.test.ts
  </acceptance_criteria>
  <done>pass2.ts orchestrator implemented mirroring wave3.ts; all 12 tests live and green; D-23 telemetry and D-24 cost ceiling logged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pass 2 LLM output → server-side Zod validator | qwen3.6-plus can hallucinate fields; Pass2ResponseSchema gates entry |
| keyframe signed URL → image_url payload to DashScope | We send Supabase-signed URLs as image input; recipient is DashScope only |
| Pass 1 verdict content → Pass 2 prompt | Pass 1 output passes through buildPass2TextBlock; treated as engine-internal trusted state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering (out-of-range attention) | Pass2ResponseSchema | mitigate | z.number().min(0).max(1) rejects invalid; persona dropped from aggregate via D-06 guard |
| T-03-06-02 | Tampering (segment count mismatch) | quality guard in callPersona | mitigate | explicit length check after Zod parse; throws validation error; retry-once consumes the slot |
| T-03-06-03 | Information Disclosure (keyframe signed URL leakage to DashScope) | image_url content array | accept | URLs are 30-day signed (Plan 03 bucket); recipient is DashScope only; Sentry breadcrumbs do not log image URLs by default — verify in code |
| T-03-06-04 | Denial of Service (cost amplification) | thinking_budget config | mitigate | thinking_budget: 8000 caps per-call tokens; D-24 alert at 50 cents/analysis fires log.error + Sentry capture |
| T-03-06-05 | Tampering (prompt injection via Pass 1 verdict / segments) | buildPass2TextBlock | mitigate | Pass 1 verdict comes from internal engine state (untrusted upstream content is the video itself, processed by Wave 0). visual_event/audio_event capped at 200 chars by SegmentSchema (Plan 02). Treat as data, never re-run as system role |
| T-03-06-06 | Denial of Service (10 parallel thinking calls hitting DashScope rate limits) | runWave3Pass2 concurrency | accept | DashScope handles per-API-key concurrency; Promise.allSettled isolates failures; circuit-breaker pattern from wave3.ts also applies if added |
</threat_model>

<verification>
- pnpm vitest run src/lib/engine/__tests__/pass2.test.ts exits 0 with 12 tests green
- pnpm tsc --noEmit green
- All grep counts above pass
- No regression on src/lib/engine/__tests__/wave3.test.ts (Pass 1 orchestrator untouched)
</verification>

<success_criteria>
runWave3Pass2 ready to be wired in pipeline.ts (Plan 08). Two new modules implement the dedicated Pass 2 call with D-04 prompt enrichment, D-05 output validation, D-06 quality guards, D-23 telemetry, D-24 cost ceiling. Plan 04 weighted-aggregator can now ingest live Pass2PersonaResult[] returned by this orchestrator.
</success_criteria>

<output>
After completion, create .planning/phases/03-engine-rework-pass-2-timeline-weighted-aggregator-heatmap-sc/03-06-SUMMARY.md recording the runWave3Pass2 signature verbatim, the 12 test names with pass/fail status, sample of D-23 log output shape, and confirmation of D-24 cost ceiling code path.
</output>
