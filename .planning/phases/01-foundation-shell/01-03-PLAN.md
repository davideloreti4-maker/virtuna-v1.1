---
phase: 01-foundation-shell
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/app/globals.css
  - src/components/marketing/motion-config.tsx
  - src/components/marketing/index.ts
  - src/app/(marketing)/page.tsx
autonomous: true
requirements: [FOUND-04]
must_haves:
  truths:
    - "The marketing tree is wrapped in <MotionConfig reducedMotion=\"user\"> so every motion element auto-respects the OS reduced-motion setting"
    - "A CSS @media (prefers-reduced-motion: reduce) block sets animation:none for the non-Framer animations (skeleton-breathe, shimmer, marquee, marquee-vertical)"
    - "A grep confirms all src/components/motion/* wrappers import motion/react (zero framer-motion) — D-16 is verified, no migration performed"
    - "No animation is load-bearing for content or first paint"
  artifacts:
    - path: "src/components/marketing/motion-config.tsx"
      provides: "Client MotionConfig shell wrapping children with reducedMotion=\"user\""
      contains: "MotionConfig"
    - path: "src/app/globals.css"
      provides: "@media (prefers-reduced-motion: reduce) block gating non-Framer animations"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "src/app/(marketing)/page.tsx"
      to: "src/components/marketing/motion-config.tsx"
      via: "wraps the skeleton subtree"
      pattern: "MotionConfig|MotionProvider"
    - from: "src/components/marketing/motion-config.tsx"
      to: "motion/react"
      via: "import MotionConfig"
      pattern: "from ['\"]motion/react['\"]"
---

<objective>
Lay the motion foundation: a global `<MotionConfig reducedMotion="user">` boundary plus a CSS reduced-motion block, so every animation on the landing has a reduced-motion fallback from the start. Also verify (do NOT migrate) that the existing motion wrappers are already on `motion/react`.

Purpose: Satisfy FOUND-04 — motion wired via `motion/react` behind a global reduced-motion fallback, with no animation load-bearing for content or first paint. (FOUND-04)
Output: A client `<MotionConfig>` shell in `src/components/marketing/`, wired into the `/` skeleton; a `@media (prefers-reduced-motion: reduce)` block appended to `globals.css`; and a verified D-16 grep recorded in the summary (no migration work — the marketing surface is already framer-motion-free).

> Wave/dependency note: this plan runs in Wave 3 (NOT Wave 2) and depends on 01-02 because it shares the `src/components/marketing/index.ts` barrel with 01-02 — 01-02 creates the barrel (with the `Placeholder` export), this plan appends `MotionConfigShell`. Serializing avoids a concurrent-write race on the barrel. It does NOT share files with 01-04 (header) or 01-05 (footer), so all three Wave-3 plans run in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-shell/01-CONTEXT.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md
@.planning/phases/01-foundation-shell/01-UI-SPEC.md

<interfaces>
<!-- VERIFIED facts (RESEARCH §3) — these resolve D-16 to a no-op for the marketing surface. -->
All 6 src/components/motion/* wrappers ALREADY import from "motion/react":
  hover-scale, stagger-reveal, fade-in-up, fade-in, page-transition, slide-up
The ONLY 4 framer-motion importers are OUT-OF-SCOPE PRODUCT files (do NOT touch):
  src/components/app/simulation/{analysis-loading,loading-phases}.tsx
  src/components/viral-results/{FactorCard,ViralScoreRing}.tsx
→ D-16 conclusion: leave-product-files-alone + defer the `framer-motion` dep removal to Phase 5.

From motion/react (motion@^12.29.2, VERIFIED supported — Context7 motion.dev):
  export const MotionConfig   // prop reducedMotion: "user" | "always" | "never"
  // reducedMotion="user" disables transform/layout animations under OS reduce,
  // PRESERVES opacity/backgroundColor. Complements the wrappers' own useReducedMotion() self-gating.

The marketing barrel created by 01-02 (append to it, do NOT clobber):
  src/components/marketing/index.ts  →  already exports { Placeholder, placeholderVariants } + type PlaceholderProps

The non-Framer animation utilities the CSS block must gate (present in globals.css after 01-01):
  .animate-skeleton-breathe (keyframe skeleton-breathe), .animate-shimmer (shimmer),
  .animate-marquee + .animate-marquee-vertical (@theme inline marquee)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the client &lt;MotionConfig&gt; shell and wire it into the / skeleton</name>
  <files>src/components/marketing/motion-config.tsx, src/components/marketing/index.ts, src/app/(marketing)/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-shell/01-UI-SPEC.md (Motion Contract — two-layer reduced-motion; MotionConfig at the marketing tree root)
    - /Users/davideloreti/virtuna-landing-v2/src/app/(marketing)/page.tsx (the skeleton from 01-01 — a server component; this plan adds the client MotionConfig wrapper around its motion subtree WITHOUT making the whole page client)
    - /Users/davideloreti/virtuna-landing-v2/src/components/marketing/index.ts (the barrel 01-02 created — APPEND to it, preserve the Placeholder export)
    - /Users/davideloreti/virtuna-landing-v2/src/components/motion/fade-in.tsx (example of an existing `"use client"` motion wrapper — for the client-boundary convention)
  </read_first>
  <action>
    Create `src/components/marketing/motion-config.tsx` as a `"use client"` component that imports `MotionConfig` from `motion/react` and returns `<MotionConfig reducedMotion="user">{children}</MotionConfig>` (props: `{ children: React.ReactNode }`). Name the export `MotionConfigShell` (to avoid confusion with the library's `MotionConfig`). This is the global boundary that makes any raw `motion.*` element auto-respect the OS reduced-motion setting (D-17 layer 1); it complements — does not replace — the wrappers' own `useReducedMotion()` self-gating.

    Wire it into `(marketing)/page.tsx`: the RSC page stays a server component; wrap the skeleton's animated subtree (the `<main>` of stub sections — and `<Header/>`/`<Footer/>` if you choose the widest wrap) inside `<MotionConfigShell>...</MotionConfigShell>`. Choose the tightest wrap that covers all animated content; do not break the Header's own client boundary (it owns `"use client"` via 01-04).

    APPEND to `src/components/marketing/index.ts` (created by 01-02): add `export { MotionConfigShell } from "./motion-config";`. Do NOT remove or alter the existing `Placeholder`/`placeholderVariants`/`PlaceholderProps` exports.
  </action>
  <verify>
    <automated>grep -q '"use client"' src/components/marketing/motion-config.tsx && grep -Eq 'from ["'"'"']motion/react["'"'"']' src/components/marketing/motion-config.tsx && grep -q 'reducedMotion="user"' src/components/marketing/motion-config.tsx && grep -q "MotionConfigShell" "src/app/(marketing)/page.tsx" && grep -q "MotionConfigShell" src/components/marketing/index.ts && grep -q "Placeholder" src/components/marketing/index.ts && echo "wired OK (barrel preserves Placeholder)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/marketing/motion-config.tsx` is a `"use client"` file importing `MotionConfig` from `motion/react` and rendering `reducedMotion="user"`; exports `MotionConfigShell`.
    - `(marketing)/page.tsx` references `MotionConfigShell` wrapping the animated subtree.
    - `marketing/index.ts` exports BOTH `MotionConfigShell` and `Placeholder` (01-02's export preserved — not clobbered).
  </acceptance_criteria>
  <done>The global MotionConfig boundary exists, wraps the landing's motion subtree, and the barrel keeps both exports.</done>
</task>

<task type="auto">
  <name>Task 2: Append the CSS reduced-motion block to globals.css + record the D-16 verify</name>
  <files>src/app/globals.css</files>
  <read_first>
    - /Users/davideloreti/virtuna-landing-v2/src/app/globals.css (post-01-01 — confirm `.animate-skeleton-breathe` (~line 293), `.animate-shimmer`, and the `@theme inline` marquee vars exist; confirm there is NO existing `prefers-reduced-motion` block — there is not)
    - .planning/phases/01-foundation-shell/01-RESEARCH.md (§3 — the verified D-16 grep result to record; §1e — which animations the block must gate)
  </read_first>
  <action>
    (a) Append a `@media (prefers-reduced-motion: reduce)` block to `globals.css` (D-17 layer 2 — for the NON-Framer animations that `MotionConfig` does not catch). It must set `animation: none !important;` (or a near-zero duration) for `.animate-skeleton-breathe`, `.animate-shimmer`, `.animate-marquee`, and `.animate-marquee-vertical`. This pairs with the `motion-reduce:animate-none` Tailwind utility already used on `ui/skeleton.tsx` and on the opt-in `<Placeholder>` breathe (01-02). Place the block after the `@theme inline` marquee section (a clean spot). This block is NEW — it does NOT arrive with 01-01's wholesale globals.css copy (numen-rework's file has no such block either — VERIFIED).

    (b) D-16 verification (NO migration — RESEARCH §3 CRISP RECOMMENDATION): run a grep proving all `src/components/motion/*` wrappers import `motion/react` and zero import `framer-motion`. Record the result in the plan summary. Do NOT touch the 4 out-of-scope product `framer-motion` files; do NOT uninstall the `framer-motion` dep (4 product files still import it — removal deferred to Phase 5, per CONTEXT `<deferred>`). Phase-1's D-16 obligation is satisfied by verify + the `MotionConfig` wiring (Task 1), not by migration.
  </action>
  <verify>
    <automated>grep -q "prefers-reduced-motion" src/app/globals.css && grep -A12 "prefers-reduced-motion" src/app/globals.css | grep -q "animate-skeleton-breathe" && grep -A12 "prefers-reduced-motion" src/app/globals.css | grep -q "animate-marquee" && (grep -rl "from ['\"]framer-motion['\"]" src/components/motion/ >/dev/null 2>&1 && echo "!! FRAMER IN WRAPPERS — INVESTIGATE" && false || echo "D-16 verified: 0 framer-motion in motion/*") && pnpm build > /tmp/p0103_build.log 2>&1 && echo "BUILD OK" || (echo "FAIL"; tail -25 /tmp/p0103_build.log; false)</automated>
  </verify>
  <acceptance_criteria>
    - `globals.css` contains a `@media (prefers-reduced-motion: reduce)` block referencing `.animate-skeleton-breathe`, `.animate-shimmer`, `.animate-marquee`, and `.animate-marquee-vertical`, setting `animation: none`.
    - `grep -rl "from 'framer-motion'" src/components/motion/` returns NOTHING (D-16 verified — wrappers on `motion/react`).
    - The 4 product files (`app/simulation/*`, `viral-results/*`) are untouched and `framer-motion` remains in `package.json` (dep removal deferred).
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>The CSS reduced-motion fallback is in place, the D-16 verify passes, and the build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | Client motion configuration + CSS only. No user input, no network, no datastore. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of service (accessibility) | unbounded animation ignoring OS reduce | mitigate | `<MotionConfig reducedMotion="user">` + the `@media (prefers-reduced-motion: reduce)` CSS block guarantee every animation halts under the user's OS setting — no motion is load-bearing for content or first paint (FOUND-04/FOUND-07). This is the mitigation, not a residual risk. |

No untrusted input or auth in scope; this plan only wires a global reduced-motion fallback.
</threat_model>

<verification>
- `pnpm build` exits 0.
- `grep -rl "from 'framer-motion'" src/components/motion/` → empty (D-16 verified).
- (At phase gate) DevTools → Rendering → "Emulate prefers-reduced-motion: reduce" → skeleton-breathe / shimmer / marquee stop.
</verification>

<success_criteria>
- FOUND-04: motion is wired via `motion/react`, wrapped in a global `<MotionConfig reducedMotion="user">`, with a CSS `@media (prefers-reduced-motion: reduce)` fallback for non-Framer animations; D-16 verified with zero migration; no animation load-bearing for content/first paint.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-03-SUMMARY.md` when done. Record the D-16 verify grep result in it.
</output>
