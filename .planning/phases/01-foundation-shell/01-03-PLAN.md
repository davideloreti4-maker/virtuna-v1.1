---
phase: 01-foundation-shell
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/app/(app)/home/page.tsx
  - src/components/app/home/home-greeting.tsx
  - src/components/app/home/composer.tsx
  - src/components/app/home/__tests__/home.test.tsx
  - src/components/app/home/__tests__/composer.test.tsx
  - src/components/app/home/__tests__/composer-layout.test.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-04, SHELL-06, THEME-04, THEME-05]
must_haves:
  truths:
    - "A first-time user lands on a clean home: serif greeting + centered composer + NumenMark stele glyph, no chips, no Simulation list"
    - "Pasting a TikTok URL enables submit; a non-TikTok URL is rejected with the D-21 copy"
    - "The + control uploads a video (MP4/MOV) reusing VideoUpload's validation"
    - "Submitting creates a Simulation and navigates to its /analyze/[id] permalink, where the composer drops to bottom-pinned"
    - "On the permalink route (id present) the composer renders bottom-pinned; on the empty home it renders centered"
    - "D-09: all user-facing copy on the home uses the product noun 'Simulation' (greeting 'simulate your audience', creation flow) — never 'Reading'/'analysis'"
    - "D-25: the P1 home shows NO demo affordance — the first-run demo is deferred to Phase 5 (DEMO-01); its absence here is a deliberate no-op"
  artifacts:
    - path: "src/app/(app)/home/page.tsx"
      provides: "Authed home route (server page) rendering the greeting + centered composer"
      min_lines: 10
    - path: "src/components/app/home/home-greeting.tsx"
      provides: "Serif voice-moment greeting with name from useProfile, isLoading skeleton"
      contains: "font-serif"
    - path: "src/components/app/home/composer.tsx"
      provides: "Slim TikTok-URL + upload composer, two-layout, submit->navigate loop"
      contains: "tiktok"
  key_links:
    - from: "src/components/app/home/composer.tsx"
      to: "/api/analyze (via stream.start + navigate-on-id effect)"
      via: "lift the submit->create->navigate loop from Board.tsx:300-345"
      pattern: "router.push|/analyze/"
    - from: "src/components/app/home/composer.tsx"
      to: "src/components/app/video-upload.tsx"
      via: "reuse VideoUpload (bare variant) verbatim for the + upload"
      pattern: "VideoUpload"
    - from: "src/components/app/home/home-greeting.tsx"
      to: "src/hooks/queries/use-profile.ts"
      via: "useProfile() name for the greeting"
      pattern: "useProfile"
---

<objective>
Build the clean authed **home** (SHELL-01) — a serif greeting + a centered universal composer + the NumenMark stele glyph, no chips (D-18), no Simulation list under it — and the **two-layout composer** (SHELL-04 / D-24): centered when empty, bottom-pinned when a Simulation exists.

The composer is the #1 net-new risk (RESEARCH Pitfall 5): do NOT reuse `ContentForm` wholesale (it drags in Score/Remix intent, the Apollo model-tier picker, a 3-mode tab set, and Instagram URL acceptance — none wanted). Build a SLIM composer that reuses the validated sub-parts: `VideoUpload` (bare) for upload, a TikTok-only URL check (D-21), and the proven submit→create→navigate loop (lifted from `Board.tsx:300-345`, since the home is NOT the Konva board).

Ingestion (SHELL-02/03) and permalink restore (SHELL-06) reuse mechanisms that ALREADY work — `POST /api/analyze` → SSE `started{id}` → `router.push('/analyze/${id}')`, and `usePermalinkAnalysis` + the IDOR-defended `/api/analysis/[id]`. **P1 builds the shell + composer position; what renders ABOVE the bottom-pinned composer is Phase 2.** Keep `/analyze/[id]` as the permalink — do NOT rename (RESEARCH A2 / anti-pattern).

Depends on plan 01-01 for `--font-serif` (the greeting consumes `font-serif`) and the flat-warm tokens.
Purpose: the home + composer is the surface the human reviews (empty + active) at the THEME-06 gate.
Output: `/home` route, greeting + composer components, Wave-0 home/composer/layout tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-shell/01-CONTEXT.md
@.planning/phases/01-foundation-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md
@.planning/phases/01-foundation-shell/01-VALIDATION.md

<interfaces>
<!-- Verified against live files 2026-06-13. Read each source in full before lifting from it. -->

The submit->create->navigate loop to LIFT (Board.tsx — do NOT import Board; replicate in the composer):
- L309 handleContentSubmit(data) — builds payload, calls stream.start()
- L300-307 navigate-on-id effect:
    const prev = useRef(stream.analysisId);
    useEffect(() => { const id = stream.analysisId; if (id && prev.current === null) router.push(`/analyze/${id}`); prev.current = id; }, [stream.analysisId, router]);
- The id originates server-side: POST /api/analyze does nanoid(12) (route L842) + emits SSE started{id} (L951); useAnalysisStream surfaces it as stream.analysisId.

VideoUpload (src/components/app/video-upload.tsx) — reuse VERBATIM:
- export VideoUpload, props { file, onFileSelect, uploadProgress?, className?, bare? }
- MAX_FILE_SIZE 200MB (L8); accept="video/*" (L210); type allowlist MP4/MOV/WebM (L246 "MP4, MOV, WebM · Max 200MB"); rejects oversize (L128-129) + wrong type
- use the `bare` variant (drops the bordered card chrome) so the + control reads inline

TikTok URL validation (D-21 — TikTok ONLY; ContentForm's pattern ALSO allows Instagram — do NOT copy it):
- ContentForm SOCIAL_URL_PATTERN (content-form.tsx L105-106) allows tiktok.com|vm.tiktok.com|@handle/video|instagram.com — TOO BROAD
- Use a TikTok-only client pattern, e.g. /^https?:\/\/(www\.|vm\.)?tiktok\.com\//i (mirrors the SERVER trust-boundary regex at /api/analyze L465). Client check = fast UX reject; server stays the boundary (RESEARCH Architectural Responsibility Map).

useProfile (src/hooks/queries/use-profile.ts) — greeting name:
- useProfile() -> { name, email, company, role, avatar, notifications } + isLoading (TanStack)
- greeting: profile?.name (fallback: name-less greeting while isLoading; do NOT flash "[Name]" — RESEARCH Open Q3)

NumenMark (src/components/brand/numen-logo.tsx):
- NumenMark({ size?, variant?, className }) — stele path, fill="currentColor" (coral via parent text-accent), aria-hidden

Routing facts (RESEARCH R1 / C16):
- New home MUST be a new authed route in the (app) group: src/app/(app)/home/page.tsx — NOT `/` (that is the public marketing page).
- (app)/layout.tsx already auth-gates (getUser() -> redirect /login) + wraps AppShell. /home inherits the gate.
- The permalink is the EXISTING /analyze/[id] — keep it.

UI-SPEC bindings:
- § Home empty state: greeting + composer ONLY, NO chips (D-18), NO demo affordance (D-25), NO list under composer; centered in ~760px column; NumenMark (not asterisk).
- § Composer two layouts: centered-when-empty -> bottom-pinned-when-active; whisper-shadow when centered (the --shadow-float token from 01-01); same component both states.
- § Copywriting: greeting "[UAT] Ready to simulate your audience, [Name]?"; empty placeholder "Paste a TikTok link or drop a video…"; active placeholder "Ask about this simulation…"; non-TikTok error "Numen reads TikTok videos for now. Paste a TikTok link or upload the file."; unsupported-upload error "That file type isn't supported — upload an MP4 or MOV."
- § Typography: greeting serif ~38px (mobile ~28-30px), italic on the name; ALL else Inter. § Motion: calm reflow centered->pinned, respect usePrefersReducedMotion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 tests — home composition, TikTok/upload validation, two-layout position</name>
  <files>src/components/app/home/__tests__/home.test.tsx, src/components/app/home/__tests__/composer.test.tsx, src/components/app/home/__tests__/composer-layout.test.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-shell/01-VALIDATION.md § Wave 0 Requirements + Per-Task Verification Map (SHELL-01/02/03/04, THEME-04 rows)
    - src/components/app/__tests__/video-upload.test.tsx (existing — the upload validation is already covered here; the composer test asserts wiring, not re-testing VideoUpload internals)
    - src/components/sidebar/__tests__/Sidebar.recent.test.tsx (mirror the happy-dom render + TanStack/query mock pattern)
    - src/test/setup.ts (jest-dom + matchMedia/ResizeObserver shims)
  </read_first>
  <behavior>
    - home.test.tsx: renders the greeting element with a serif (font-serif) class + the NumenMark glyph + the composer; asserts NO starter-chip nodes (no "Paste link"/"Upload"/"Try a demo" chips), NO Simulation list
    - composer.test.tsx: a TikTok URL enables submit; a non-TikTok URL (e.g. youtube/instagram) shows the D-21 error copy and keeps submit disabled; the + control renders VideoUpload
    - composer-layout.test.tsx: with no id (mock useParams -> {}) the composer is centered; with an id (useParams -> { id }) it is bottom-pinned (assert the layout class/data-attr that distinguishes the two)
  </behavior>
  <action>
    Author London-style (mock useProfile, useParams/usePathname, useAnalysisStream/stream.start, useIsMobile, usePrefersReducedMotion). EXPECTED to fail until Tasks 2-3 land — write first.
    Create the 3 test files under src/components/app/home/__tests__/. Drive the two-layout test off a mocked route param (the composer reads whether an id is present — mirror ContentForm's isOnResultRoute = !!params.id, content-form.tsx L158). Assert a stable selector (e.g. data-layout="centered" | "pinned") so the test is not brittle to styling.
    Assert the non-TikTok rejection uses the exact D-21 copy substring ("Numen reads TikTok videos for now").
    Use vitest run (never watch).
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-rework && test -f src/components/app/home/__tests__/home.test.tsx && test -f src/components/app/home/__tests__/composer.test.tsx && test -f src/components/app/home/__tests__/composer-layout.test.tsx && echo WAVE0_HOME_TESTS_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - home.test.tsx asserts a font-serif greeting, the NumenMark glyph, and the absence of chip nodes
    - composer.test.tsx asserts TikTok-enable + non-TikTok reject (with the "Numen reads TikTok videos for now" copy)
    - composer-layout.test.tsx asserts centered (no id) vs bottom-pinned (id present) via a stable selector
  </acceptance_criteria>
  <done>The 3 Wave-0 home/composer scaffolds exist and define the target; they fail until the components land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the slim composer (TikTok URL + upload + submit->navigate, two-layout)</name>
  <files>src/components/app/home/composer.tsx</files>
  <read_first>
    - src/components/board/Board.tsx L300-345 (the navigate-on-id effect + handleContentSubmit to LIFT — do NOT import Board)
    - src/components/app/content-form.tsx (REFERENCE ONLY for the stream.start() payload shape + isOnResultRoute L158 — do NOT reuse its intent/tier/3-tab/IG complexity, Pitfall 5)
    - src/components/app/video-upload.tsx (reuse VideoUpload bare verbatim; props + 200MB/type validation)
    - src/hooks/queries/use-analyze.ts (useAnalysisStream — stream.start, stream.analysisId surface)
    - .planning/phases/01-foundation-shell/01-UI-SPEC.md § Composer + § Copywriting + § Motion
    - .planning/phases/01-foundation-shell/01-RESEARCH.md Pitfall 5 (composer complexity bleed) + § Don't Hand-Roll (TikTok regex / VideoUpload / submit-navigate reuse) + Pattern 1/3
  </read_first>
  <behavior>
    - A TikTok URL (tiktok.com / vm.tiktok.com) enables submit; a non-TikTok URL shows the D-21 copy + keeps submit disabled
    - The + control mounts VideoUpload (bare); selecting an MP4/MOV stages it; oversize/wrong-type is rejected by VideoUpload's existing validation
    - On submit, stream.start() fires; when stream.analysisId arrives, router.push(`/analyze/${id}`) navigates (loop lifted from Board)
    - data-layout = "centered" when no route id, "pinned" when an id is present (drives the two-layout container in Task 3)
  </behavior>
  <action>
    Create `src/components/app/home/composer.tsx` ("use client"). Build a SLIM composer: a single text input (URL paste / future follow-up), a `+` upload control mounting `VideoUpload` bare, and a submit button. NO intent selector, NO model-tier picker, NO 3-mode tabs, NO Instagram acceptance (Pitfall 5 / D-18 / D-21).
    URL validation (D-21): client-check TikTok-only via /^https?:\/\/(www\.|vm\.)?tiktok\.com\//i (fast UX reject; the server regex at /api/analyze L465 stays the trust boundary). On a non-TikTok URL show the exact copy "Numen reads TikTok videos for now. Paste a TikTok link or upload the file." and keep submit disabled.
    Submit->navigate: lift `handleContentSubmit` (build the /api/analyze payload — TikTok url OR uploaded video_storage_path) + the navigate-on-id effect from Board.tsx L300-345 into this component (the home is not the Konva board, so replicate, do not import). Use `useAnalysisStream` for stream.start()/stream.analysisId. On id arrival, router.push(`/analyze/${id}`).
    Expose the layout signal: read the route id (useParams) — set a `data-layout` attribute ("centered" if no id, "pinned" if id present) so Task 3's container can position it. Placeholder: empty -> "Paste a TikTok link or drop a video…"; active (id present) -> "Ask about this simulation…" (the active follow-up BEHAVIOR is Phase 5 — here it is just the input + placeholder).
    Errors: surface the unsupported-upload + invalid-URL copy from the UI-SPEC Copywriting table. Coral only on focus ring + submit affordance.
    Motion (THEME-05): any reflow/transition is calm + gated on usePrefersReducedMotion. Keep the file under 500 lines.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-rework && grep -qi tiktok src/components/app/home/composer.tsx && grep -q VideoUpload src/components/app/home/composer.tsx && grep -q 'router.push' src/components/app/home/composer.tsx && npx vitest run src/components/app/home/__tests__/composer.test.tsx src/components/app/home/__tests__/composer-layout.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - composer.tsx references a TikTok-only URL check, mounts VideoUpload, and calls router.push(`/analyze/${id}`) on stream.analysisId
    - composer.tsx does NOT import ContentForm and contains no intent/tier/Instagram logic
    - vitest run of composer.test.tsx + composer-layout.test.tsx exits 0
  </acceptance_criteria>
  <done>The slim composer validates TikTok-only, uploads via VideoUpload, submits through the lifted navigate loop, and exposes the centered/pinned layout signal; composer tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the home route + serif greeting + center/pin the composer</name>
  <files>src/app/(app)/home/page.tsx, src/components/app/home/home-greeting.tsx</files>
  <read_first>
    - src/app/(app)/layout.tsx (the auth-gated AppShell wrapper /home renders inside)
    - src/app/(app)/analyze/[id]/page.tsx (the existing permalink page — confirm where the bottom-pinned composer will sit relative to it; P2 renders the thread above)
    - src/hooks/queries/use-profile.ts (useProfile name + isLoading for the greeting skeleton)
    - src/components/brand/numen-logo.tsx (NumenMark API)
    - .planning/phases/01-foundation-shell/01-UI-SPEC.md § Home empty state + § Typography (serif ~38px, italic name) + § Spacing (760px column) + § Copywriting (greeting)
    - .planning/phases/01-foundation-shell/01-RESEARCH.md Open Q3 (greeting loading state — do not flash "[Name]")
  </read_first>
  <behavior>
    - /home renders inside the authed AppShell: a serif greeting (font-serif, name from useProfile, italic name accent) + the NumenMark glyph + the centered composer; NO chips, NO Simulation list
    - While useProfile isLoading, the greeting shows a name-less form or a serif skeleton (no "[Name]" flash)
    - Empty home: composer centered in the ~760px column with the whisper float shadow; on /analyze/[id] the same composer is bottom-pinned (data-layout from Task 2 drives the container)
  </behavior>
  <action>
    Create `src/app/(app)/home/page.tsx` — a server page that renders the client greeting + composer centered in the ~760px column (D-17). It lives in the (app) group so it inherits the auth gate (getUser -> /login) and the AppShell sidebar.
    Create `src/components/app/home/home-greeting.tsx` ("use client") — the serif voice moment: `font-serif`, ~38px desktop / ~28-30px mobile, with the name in italic (`<em>`), name from `useProfile()`. While `isLoading`, render a name-less greeting ("Ready to simulate your audience?") or a serif skeleton — never flash "[Name]" (Open Q3). Copy is `[UAT]` ("Ready to simulate your audience, [Name]?") — locks at THEME-06. Render the NumenMark stele glyph (D-20), coral via a parent `text-accent`.
    Two-layout container: position the composer based on its `data-layout` signal (Task 2). On the empty home (centered) the composer floats (use the `--shadow-float` token from 01-01); on the permalink route it is bottom-pinned to the column with the thread scroll area above (the thread itself is Phase 2 — here the area above the pinned composer can be empty/placeholder). Keep the reflow calm + reduced-motion-aware (THEME-05).
    Do NOT add starter chips (D-18), a demo affordance (D-25), or a Simulation list (it lives in the sidebar). Files under 500 lines.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-rework && test -f "src/app/(app)/home/page.tsx" && grep -q 'font-serif' src/components/app/home/home-greeting.tsx && grep -q useProfile src/components/app/home/home-greeting.tsx && grep -q NumenMark src/components/app/home/home-greeting.tsx && npx vitest run src/components/app/home/__tests__/home.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/home/page.tsx exists and renders the greeting + composer (no chips, no list)
    - home-greeting.tsx uses font-serif, useProfile, and NumenMark; has an isLoading path that does not render "[Name]"
    - vitest run of home.test.tsx exits 0
  </acceptance_criteria>
  <done>The clean authed home renders the serif greeting + NumenMark + centered composer; the composer pins to the bottom on the permalink route; home test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client composer -> POST /api/analyze | Untrusted URL / uploaded file crosses here. The server is the trust boundary; the client check is UX. |
| browser -> /analyze/[id] (permalink restore) | Reopening a Simulation by id — ownership enforced server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering / (cost) | composer URL input -> /api/analyze | mitigate (client) + accept (server validated) | Client rejects non-TikTok via the TikTok-only regex (D-21, fast UX reject). The SERVER regex at /api/analyze L465 remains the trust boundary and is NOT weakened — the slim composer posts through the same existing endpoint. No new ingestion path, no SSRF surface beyond the existing engine path. |
| T-03-02 | Tampering | composer upload -> Supabase storage | accept (mitigated by reusing existing validated code) | Reuses VideoUpload's existing MP4/MOV + 200MB validation verbatim (RESEARCH) — the control is not weakened. Server still validates content-length (route L416). |
| T-03-03 | Information Disclosure | /analyze/[id] permalink restore | accept (mitigated by reusing existing validated code) | Restore goes through the existing IDOR-defended /api/analysis/[id] (`.eq user_id`). The composer/home add no client-only fetch that skips the user scope (RESEARCH Security Domain V4). |
| T-03-04 | Spoofing | new /home authed surface | mitigate | /home is created INSIDE the (app) group, inheriting the layout's server getUser() gate (redirect /login if unauthenticated). Plan 01-04 additionally repoints the landing + adds defense-in-depth; this plan ensures the route is born auth-gated, never public. |
</threat_model>

<verification>
- home.test.tsx green: serif greeting + NumenMark + composer, no chips, no list (SHELL-01, THEME-04).
- composer.test.tsx green: TikTok-enable + non-TikTok reject with D-21 copy; VideoUpload mounted (SHELL-02/03, D-21).
- composer-layout.test.tsx green: centered (no id) vs bottom-pinned (id present) (SHELL-04 / D-24).
- composer references router.push(`/analyze/${id}`) (SHELL-06 permalink seam; what renders above = Phase 2).
- Exact greeting copy / serif size perception is `[UAT]` — verified at the THEME-06 gate (plan 01-05).
</verification>

<success_criteria>
- Clean authed `/home`: serif greeting + NumenMark + centered composer, no chips, no list.
- Slim composer: TikTok-only URL (D-21) + VideoUpload upload + submit->navigate loop; two-layout centered/pinned.
- Permalink seam preserved (`/analyze/[id]`, not renamed).
- Home/composer/layout Wave-0 tests green.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-03-SUMMARY.md` when done.
</output>
