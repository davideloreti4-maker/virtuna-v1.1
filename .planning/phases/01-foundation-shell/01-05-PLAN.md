---
phase: 01-foundation-shell
plan: 05
type: execute
wave: 3
depends_on: [01-01]
files_modified:
  - src/components/layout/footer.tsx
  - src/components/layout/__tests__/footer.test.tsx
autonomous: true
requirements: [NAV-02]
must_haves:
  truths:
    - "The footer shows the Numen brand (NumenLogo + one-line tagline)"
    - "The footer provides in-page section links that mirror the header nav anchors"
    - "The footer shows legal/social placeholders — Privacy, Terms, X, TikTok"
    - "The footer is flat-warm (no glass, no gradient) with a hairline top border; all old 'Artificial Societies' content is gone"
  artifacts:
    - path: "src/components/layout/footer.tsx"
      provides: "Flat-warm compact 2–3 column Footer: brand, product anchors, legal/social placeholders"
      min_lines: 40
      contains: "NumenLogo"
    - path: "src/components/layout/__tests__/footer.test.tsx"
      provides: "NAV-02 coverage — brand, anchor mirror, legal/social stubs"
  key_links:
    - from: "src/components/layout/footer.tsx"
      to: "src/components/brand/numen-logo.tsx"
      via: "import { NumenLogo }"
      pattern: "NumenLogo"
    - from: "src/components/layout/footer.tsx"
      to: "#how-it-works / #the-simulation / #pricing / #faq"
      via: "in-page anchor links mirroring the nav"
      pattern: "#the-simulation"
---

<objective>
Rebuild the footer from scratch on flat-warm: a compact 2–3 column footer with the Numen brand + tagline, in-page anchor links mirroring the header nav, and legal/social placeholders (Privacy, Terms, X, TikTok). Strips all old "Artificial Societies" content. The `/` skeleton already mounts `<Footer/>` (same path/export).

Purpose: Satisfy NAV-02 — footer with brand, in-page section links, and legal/social placeholders. (NAV-02)
Output: A rewritten `src/components/layout/footer.tsx` and a happy-dom `footer.test.tsx` covering the brand, the anchor mirror, and the legal/social stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-shell/01-CONTEXT.md
@.planning/phases/01-foundation-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-shell/01-VALIDATION.md

<interfaces>
<!-- Reused as-is. No exploration needed. -->
From src/components/brand/numen-logo.tsx:
  export function NumenLogo(...)   // stele mark + "Numen" wordmark; color inherits via currentColor (use cream)

Nav anchor ids to mirror (the same set the 01-04 Header links to): how-it-works, the-simulation, pricing, faq

Flat-warm token classes (available after 01-01's theme port):
  surface     = bg-[--color-background-elevated] (#1a1a18) OR page bg (#262624) — pick one
  top edge    = border-t border-[--color-border] (rgba(255,255,255,0.06))
  links       = text-[--color-foreground-secondary] (#c2bdb4) hover text-[--color-foreground]
  fine print  = text-[--color-foreground-muted] (#8a857c)  — note: ≥18px or non-essential only (4.13:1)
  column head = --text-sm (14px) / --font-semibold (600)

Footer is a STATIC server component (no interactivity) — do NOT add "use client".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Wave-0 failing test for the Footer (NAV-02)</name>
  <files>src/components/layout/__tests__/footer.test.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-shell/01-VALIDATION.md (Wave 0 — footer.test.tsx behaviors)
    - .planning/phases/01-foundation-shell/01-UI-SPEC.md (Component Inventory item 3 + Copywriting Contract — tagline + legal/social labels)
  </read_first>
  <action>
    Create `src/components/layout/__tests__/footer.test.tsx` with the happy-dom pragma `/** @vitest-environment happy-dom */` on the first line. Import `{ render, screen }` from `@testing-library/react`. Import `{ Footer }` from `@/components/layout/footer`. Write assertions against the NEW NAV-02 contract so they FAIL until the rewrite lands (the current footer is full "Artificial Societies" content):
    - renders the Numen brand — assert `NumenLogo`/"Numen" present AND that "Artificial Societies" is NOT in the document (the old footer contains it → FAILS pre-rewrite).
    - in-page anchor mirror — assert links exist whose `href` targets the nav anchors, at minimum `#the-simulation` and `#faq` (and `#how-it-works`, `#pricing`). The old footer has calendly/societies links, not these → FAILS pre-rewrite.
    - legal/social placeholders — assert the labels `Privacy`, `Terms`, `X`, `TikTok` are present (old footer has "Subprocessors"/LinkedIn/founders@societies.io → FAILS pre-rewrite).
    This is the RED step.
  </action>
  <verify>
    <automated>test -f src/components/layout/__tests__/footer.test.tsx && grep -q "vitest-environment happy-dom" src/components/layout/__tests__/footer.test.tsx && (pnpm vitest run src/components/layout/__tests__/footer.test.tsx > /dev/null 2>&1 && echo "unexpectedly GREEN — tighten assertions to the NEW contract" && false || echo "RED confirmed")</automated>
  </verify>
  <acceptance_criteria>
    - File exists, first line is the happy-dom pragma, imports `Footer` from `@/components/layout/footer`.
    - Asserts Numen brand present + "Artificial Societies" absent; anchor links to `#the-simulation`/`#faq` (+ `#how-it-works`/`#pricing`); legal/social labels `Privacy`/`Terms`/`X`/`TikTok`.
    - Fails (RED) against the current societies footer.
  </acceptance_criteria>
  <done>The failing footer test exists, encoding the NAV-02 contract, and is RED.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild footer.tsx flat-warm (brand + anchor mirror + legal/social) — make the test GREEN</name>
  <files>src/components/layout/footer.tsx</files>
  <read_first>
    - /Users/davideloreti/virtuna-landing-v2/src/components/layout/footer.tsx (CURRENT — full societies content to strip: "Ready to understand your audience?", calendly "Book a meeting", founders@societies.io, "Artificial Societies" brand, LinkedIn societies.io, "Subprocessors", phosphor SSR icons)
    - /Users/davideloreti/virtuna-landing-v2/src/components/brand/numen-logo.tsx (NumenLogo API + currentColor)
    - .planning/phases/01-foundation-shell/01-UI-SPEC.md (Component Inventory item 3 — the authoritative compact 2–3 column spec + Copywriting Contract tagline/labels)
    - src/components/layout/__tests__/footer.test.tsx (the contract to satisfy)
  </read_first>
  <action>
    Replace `footer.tsx` wholesale (D-22). Keep the export `export function Footer` (the `layout/index.ts` barrel + the `(marketing)/page.tsx` skeleton import + `pricing/page.tsx` all depend on it). Footer is a STATIC server component — do NOT add `"use client"`; replace the phosphor SSR icons with lucide if any icons are kept (UI-SPEC primary), or use plain text labels for the social links.

    Structure (UI-SPEC item 3): a `<footer>` landmark, flat-warm surface (secondary tone #1a1a18 or page bg #262624) with a hairline TOP border (`--color-border`), no glass, no gradient. Compact 2–3 columns:
    1) Brand — `<NumenLogo/>` (cream via currentColor) + a one-line tagline (≤ ~8 words, hybrid voice, sentence case; suggested "Know if it'll pop before you post." — planner/executor may refine, keep one line).
    2) Product anchors — mirror the header nav set as in-page links: `How it works` (#how-it-works) · `The Simulation` (#the-simulation) · `Pricing` (#pricing) · `FAQ` (#faq). Cream-secondary, hover cream-primary. Use a `--text-sm` semibold column heading (e.g. "Product").
    3) Legal/social placeholders — `Privacy` · `Terms` and social `X` · `TikTok`, as labelled swappable stub links (`href="#"` or stub routes — they are placeholders the human fills later). Cream-secondary links; cream-muted for any fine print (keep fine print ≥ the muted-contrast guidance — non-essential or ≥18px).

    Strip ALL old societies content (no calendly, no founders@societies.io, no LinkedIn societies.io, no "Subprocessors", no "Artificial Societies"). Iterate until the Task-1 test is GREEN.
  </action>
  <verify>
    <automated>grep -q "NumenLogo" src/components/layout/footer.tsx && grep -q "#the-simulation" src/components/layout/footer.tsx && grep -Eq "Privacy" src/components/layout/footer.tsx && grep -Eq "TikTok" src/components/layout/footer.tsx && ! grep -Eq "Artificial Societies|societies.io|calendly|Subprocessors|use client" src/components/layout/footer.tsx && pnpm vitest run src/components/layout/__tests__/footer.test.tsx > /dev/null 2>&1 && echo "GREEN" || (echo "FAIL"; pnpm vitest run src/components/layout/__tests__/footer.test.tsx 2>&1 | tail -15; false)</automated>
  </verify>
  <acceptance_criteria>
    - `footer.tsx` imports + renders `NumenLogo`, contains anchor link `#the-simulation` (+ the other three), and the labels `Privacy`/`Terms`/`X`/`TikTok`.
    - `footer.tsx` contains NONE of: `Artificial Societies`, `societies.io`, `calendly`, `Subprocessors`, `use client` (static server component, fully de-societies'd).
    - `pnpm vitest run src/components/layout/__tests__/footer.test.tsx` passes (GREEN).
  </acceptance_criteria>
  <done>The footer is a flat-warm compact 2–3 column meeting NAV-02, and its test is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | Static server-rendered footer. No user input, no network, no datastore. In-page anchors + placeholder social/legal stub links only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | footer placeholder/social links | accept | Links are developer-authored in-page anchors (`#...`) or `href="#"` stubs — not user-derived. When real external social links replace the stubs later, add `rel="noopener noreferrer"` for any `target="_blank"`; not applicable this phase (no live external targets yet). |
| T-01-05b | Information disclosure | footer bundle | accept | No secrets/env/PII in the footer; it is static chrome (old `founders@societies.io` is removed, not replaced with a real address). |

No untrusted input or auth in scope; the footer is static chrome.
</threat_model>

<verification>
- `pnpm vitest run src/components/layout/__tests__/footer.test.tsx` GREEN.
- `pnpm build` stays green (run at the wave merge per VALIDATION sampling).
- (At phase gate) `/` footer shows the Numen brand + anchor mirror + Privacy/Terms/X/TikTok, flat-warm, no societies content.
</verification>

<success_criteria>
- NAV-02: footer provides the Numen brand, in-page section links mirroring the nav, and legal/social placeholders (Privacy/Terms/X/TikTok); flat-warm, no glass/gradient, all societies content stripped.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-05-SUMMARY.md` when done.
</output>
