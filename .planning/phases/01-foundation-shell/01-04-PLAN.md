---
phase: 01-foundation-shell
plan: 04
type: execute
wave: 3
depends_on: [01-01]
files_modified:
  - src/components/layout/header.tsx
  - src/components/layout/__tests__/header.test.tsx
autonomous: true
requirements: [NAV-01, NAV-03]
must_haves:
  truths:
    - "The header shows the Stele NumenLogo + 'Numen' wordmark and a 'Try it free' primary CTA"
    - "The 'Try it free' CTA links to /signup (via the shared SIGNUP_URL constant); 'Sign in' links to /login"
    - "On small screens the header collapses to a mobile-appropriate nav (icon-button toggle) that opens a flat panel and closes on link tap"
    - "The header is a flat opaque sticky bar with a hairline bottom border — no glass, no backdrop-blur, no shine, no drop shadow"
  artifacts:
    - path: "src/components/layout/header.tsx"
      provides: "Flat-matte sticky Header: NumenLogo + anchor links + 'Try it free' CTA + 'Sign in' + mobile collapse"
      min_lines: 50
      contains: "NumenLogo"
    - path: "src/components/layout/__tests__/header.test.tsx"
      provides: "NAV-01 + NAV-03 coverage — logo, CTA href, mobile toggle, close-on-tap"
  key_links:
    - from: "src/components/layout/header.tsx"
      to: "@/lib/routes"
      via: "SIGNUP_URL / LOGIN_URL constants"
      pattern: "SIGNUP_URL"
    - from: "src/components/layout/header.tsx"
      to: "src/components/brand/numen-logo.tsx"
      via: "import { NumenLogo }"
      pattern: "NumenLogo"
---

<objective>
Rewrite the header from scratch on flat-warm: a flat opaque sticky bar with the Stele logo + "Numen" wordmark, 3–4 in-page anchor links, a terracotta "Try it free" CTA → `/signup`, a subtle "Sign in" → `/login`, and a simple mobile collapse. Replaces the old glass-pill societies header in place.

Purpose: Satisfy NAV-01 (logo + wordmark + CTA) and NAV-03 (mobile-appropriate collapse). (NAV-01, NAV-03)
Output: A rewritten `src/components/layout/header.tsx` (same path/export — already mounted by the 01-01 skeleton) and a happy-dom `header.test.tsx` covering the logo, the CTA href, the mobile toggle, and close-on-tap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-shell/01-CONTEXT.md
@.planning/phases/01-foundation-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-shell/01-VALIDATION.md

<interfaces>
<!-- Reused as-is + the new constants from 01-01. No exploration needed. -->
From src/components/brand/numen-logo.tsx:
  export function NumenLogo(...)   // stele mark + "Numen" wordmark; color inherits via currentColor (use cream)

From src/lib/routes.ts (created by 01-01):
  export const SIGNUP_URL = "/signup";   // "Try it free" target (D-20)
  export const LOGIN_URL  = "/login";    // "Sign in" target

From src/hooks/useIsMobile.ts (present, 768px breakpoint, SSR-safe default):
  // confirm default vs named export on read

Lucide (installed): Menu, X  (mobile trigger icons)

Flat-warm token classes (available after 01-01's theme port):
  bar surface = bg-[--color-background-elevated] (#1a1a18) OR bg-[--color-background] (#262624) — pick one, must be OPAQUE
  bottom edge = border-b border-[--color-border] (rgba(255,255,255,0.06))
  CTA fill    = bg-[--color-accent] (#d97757), label text-[--color-accent-foreground] (#1a0f0a), rounded-[--radius-md] (8px), shadow-[--shadow-button] (flat-matte)
  nav links   = text-[--color-foreground-secondary] (#c2bdb4) hover text-[--color-foreground]
  sticky z    = --z-sticky (200)
  float panel = --shadow-float (the ONE allowed shadow — the mobile dropdown floats)

Section anchor ids (assigned by 01-01's skeleton — the links target these): how-it-works, the-simulation, pricing, faq
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Wave-0 failing test for the Header (NAV-01 + NAV-03)</name>
  <files>src/components/layout/__tests__/header.test.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-shell/01-VALIDATION.md (Wave 0 — header.test.tsx behaviors)
    - .planning/phases/01-foundation-shell/01-UI-SPEC.md (Component Inventory item 2 + Copywriting Contract — exact CTA copy + a11y labels)
    - /Users/davideloreti/virtuna-landing-v2/src/components/ui/button.tsx (for asChild/Link rendering if the Header uses the Button primitive for the CTA)
  </read_first>
  <action>
    Create `src/components/layout/__tests__/header.test.tsx` with the happy-dom pragma `/** @vitest-environment happy-dom */` on the first line. Import `{ render, screen }` from `@testing-library/react` and `userEvent` from `@testing-library/user-event` (both present after 01-01 install). Import `{ Header }` from `@/components/layout/header`. Write assertions against the NEW contract so they FAIL until the rewrite lands (the old glass header has different copy/hrefs):
    - renders `NumenLogo` — assert the brand is present via the "Numen" wordmark text (or a test-id/accessible name).
    - "Try it free" link is present and its `href` is exactly `/signup` (NAV-01). The old header has "Book a Meeting"→calendly + "Sign in"→`/auth/login`, so this FAILS pre-rewrite.
    - "Sign in" link present with `href` `/login` (old header points at `/auth/login` → FAILS pre-rewrite).
    - mobile collapse (NAV-03): a menu toggle button with `aria-label="Open menu"` is present; clicking it (via `userEvent`) reveals the nav panel; clicking a nav link inside closes the panel. Assert the open→closed state transition on the panel (e.g. a panel element/role appears then disappears).
    This is the RED step — the new copy/hrefs/toggle won't all pass against the old societies header.
  </action>
  <verify>
    <automated>test -f src/components/layout/__tests__/header.test.tsx && grep -q "vitest-environment happy-dom" src/components/layout/__tests__/header.test.tsx && (pnpm vitest run src/components/layout/__tests__/header.test.tsx > /dev/null 2>&1 && echo "unexpectedly GREEN — tighten assertions to the NEW contract" && false || echo "RED confirmed")</automated>
  </verify>
  <acceptance_criteria>
    - File exists, first line is the happy-dom pragma, imports `userEvent`, imports `Header` from `@/components/layout/header`.
    - Asserts CTA href `/signup`, "Sign in" href `/login`, NumenLogo/"Numen" presence, and a mobile toggle that opens a panel and closes it on link tap.
    - Fails (RED) against the current old glass header.
  </acceptance_criteria>
  <done>The failing header test exists, encoding the NAV-01 + NAV-03 contract, and is RED.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite header.tsx flat-matte (logo + anchors + CTA + Sign in + mobile collapse) — make the test GREEN</name>
  <files>src/components/layout/header.tsx</files>
  <read_first>
    - /Users/davideloreti/virtuna-landing-v2/src/components/layout/header.tsx (CURRENT — the OLD GLASS PILL to fully replace: var(--gradient-navbar) + backdropFilter blur(5px) + inset shine, rounded-2xl pill, "Sign in"→/auth/login, "Book a Meeting"→calendly, phosphor List/X)
    - /Users/davideloreti/virtuna-landing-v2/src/components/brand/numen-logo.tsx (NumenLogo API + currentColor)
    - /Users/davideloreti/virtuna-landing-v2/src/lib/routes.ts (SIGNUP_URL / LOGIN_URL)
    - /Users/davideloreti/virtuna-landing-v2/src/hooks/useIsMobile.ts (confirm export shape + breakpoint)
    - .planning/phases/01-foundation-shell/01-UI-SPEC.md (Component Inventory item 2 — the authoritative flat sticky bar spec)
    - src/components/layout/__tests__/header.test.tsx (the contract to satisfy)
  </read_first>
  <action>
    Replace `header.tsx` wholesale with a flat-matte sticky bar (D-19/D-20/D-21). Keep the export `export function Header` (the `layout/index.ts` barrel + the 01-01 skeleton import depend on it). It is a `"use client"` component (the mobile menu needs `useState`).

    Structure (UI-SPEC item 2): `<header>` landmark, sticky at top, z `--z-sticky` (200), height 56–64px (`--spacing-16` max), inline padding 24px desktop / 16px mobile. OPAQUE secondary-tone bg (`--color-background-elevated` #1a1a18 or app #262624 — pick one, must NOT use `backdrop-blur`), hairline bottom border (`--color-border`), NO shine, NO drop shadow.
    Contents: left = `<NumenLogo/>` (cream via currentColor), optionally wrapped in a `<Link href="#top">` to the hero anchor. Center/right = 3–4 in-page anchor links to the skeleton section ids — UI-SPEC set `How it works` (#how-it-works) · `The Simulation` (#the-simulation) · `Pricing` (#pricing) · `FAQ` (#faq), cream-secondary, hover cream-primary. Right = a "Try it free" primary CTA (accent fill #d97757, label #1a0f0a, 8px radius, flat-matte `--shadow-button`) as a `<Link href={SIGNUP_URL}>` (reuse the `Button` primitive `asChild` if convenient) + a subtle "Sign in" ghost link `<Link href={LOGIN_URL}>` (cream-secondary, NO accent). Use `SIGNUP_URL`/`LOGIN_URL` from `@/lib/routes` — never hardcode the paths.

    Mobile collapse (NAV-03, D-21): a SIMPLE flat collapse — NOT a heavyweight Radix `Sheet` (it exists at `ui/sheet.tsx` but D-21 says don't use it for 3–4 items). Use a `useState` disclosure: an icon-button trigger (lucide `Menu`/`X`, `aria-label="Open menu"`/`"Close menu"` per the copy table, tap target ≥44px) toggling a flat opaque dropdown/disclosure panel (charcoal, hairline border, `--shadow-float` permitted — it floats) containing the anchor links + a reachable "Try it free" CTA. The panel closes on tap of any link (set state false in the link onClick). Gate desktop-vs-mobile with `useIsMobile` and/or Tailwind responsive classes; ensure the toggle's open/close transition is testable (the Task-1 test drives it).

    Use lucide icons (not phosphor — UI-SPEC primary). No glass/blur/shine anywhere (flat-matte, D-06/D-19). Iterate until the Task-1 test is GREEN.
  </action>
  <verify>
    <automated>grep -q "NumenLogo" src/components/layout/header.tsx && grep -q "SIGNUP_URL" src/components/layout/header.tsx && grep -q "LOGIN_URL" src/components/layout/header.tsx && ! grep -Eq "backdropFilter|backdrop-blur|gradient-navbar|calendly" src/components/layout/header.tsx && pnpm vitest run src/components/layout/__tests__/header.test.tsx > /dev/null 2>&1 && echo "GREEN" || (echo "FAIL"; pnpm vitest run src/components/layout/__tests__/header.test.tsx 2>&1 | tail -15; false)</automated>
  </verify>
  <acceptance_criteria>
    - `header.tsx` imports + renders `NumenLogo`, and references `SIGNUP_URL` and `LOGIN_URL` from `@/lib/routes` (CTA href resolves to `/signup`, "Sign in" to `/login`).
    - `header.tsx` contains NO `backdropFilter`, `backdrop-blur`, `gradient-navbar`, or `calendly` (flat-matte, fresh-rewrite).
    - The mobile toggle renders with `aria-label="Open menu"`/`"Close menu"` and opens/closes a panel that closes on link tap.
    - `pnpm vitest run src/components/layout/__tests__/header.test.tsx` passes (GREEN).
  </acceptance_criteria>
  <done>The header is a flat-matte sticky bar meeting NAV-01 + NAV-03, and its test is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → off-surface navigation | The header's "Try it free"/"Sign in" links navigate to `/signup` and `/login` (in-app routes). No auth handshake, form, or API call happens in the header itself — it is plain `<Link>` navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering / open redirect | header CTA hrefs | accept | Hrefs are hardcoded internal app paths via the `SIGNUP_URL`/`LOGIN_URL` constants (`/signup`, `/login`) — not user-derived, not external, so no open-redirect vector. Internal navigation only; no `target=_blank`/external link in the header this phase. |
| T-01-04b | Information disclosure | header bundle | accept | No secrets, tokens, or env vars in the header; it is static chrome. |

No untrusted input or auth logic in scope; the header is static navigation chrome.
</threat_model>

<verification>
- `pnpm vitest run src/components/layout/__tests__/header.test.tsx` GREEN.
- `pnpm build` stays green (run at the wave merge per VALIDATION sampling).
- (At phase gate) `/` shows one flat opaque sticky header (no double-mount, no glass), CTA navigates to `/signup`, mobile menu toggles + closes on tap.
</verification>

<success_criteria>
- NAV-01: header shows the Stele NumenLogo + "Numen" wordmark + a "Try it free" CTA → `/signup`.
- NAV-03: on small screens the header collapses to an icon-button nav that opens a flat panel and closes on link tap.
- Flat-matte throughout — no glass/blur/shine/drop-shadow; CTA paths sourced from the shared route constants.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-04-SUMMARY.md` when done.
</output>
