---
phase: 01-foundation-shell
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - src/lib/supabase/middleware.ts
  - src/app/auth/callback/route.ts
  - src/lib/supabase/__tests__/middleware.landing.test.ts
autonomous: true
requirements: [SHELL-06, THEME-05]
must_haves:
  truths:
    - "An authenticated user landing post-login (or hitting an auth page while signed in) is sent to /home, not /analyze"
    - "The OAuth callback default destination is /home (not the sunset /dashboard)"
    - "/home is auth-gated — an unauthenticated user hitting /home is redirected to /login"
    - "/analyze stays reachable (not deleted) for direct navigation and as the /analyze/[id] permalink host"
  artifacts:
    - path: "src/lib/supabase/middleware.ts"
      provides: "Authed landing repointed to /home; /home in PROTECTED_PREFIXES (defense-in-depth)"
      contains: "/home"
    - path: "src/app/auth/callback/route.ts"
      provides: "OAuth callback default next -> /home"
      contains: "/home"
  key_links:
    - from: "src/lib/supabase/middleware.ts"
      to: "/home"
      via: "authed-off-auth-page redirect + PROTECTED_PREFIXES entry, same-origin clone"
      pattern: "/home"
    - from: "src/app/auth/callback/route.ts"
      to: "/home"
      via: "default landing when no explicit next param"
      pattern: "/home"
---

<objective>
Make the new Numen `/home` the default authed landing (D-23), repointing the two places that decide where a signed-in user lands, and keep `/analyze` dormant-but-reachable (per the brief; do NOT delete it).

RESEARCH resolved the open A4 question by reading the live files: the landing decision lives in **two** places, BOTH must be repointed:
1. `src/lib/supabase/middleware.ts` — authed users hitting `/login`/`/signup` are sent to `/analyze` (L130-131); `/dashboard`→`/analyze` 308 (L55-58).
2. `src/app/auth/callback/route.ts` — the OAuth callback defaults `next = searchParams.get("next") ?? "/dashboard"` (L18), which then falls through middleware's `/dashboard`→`/analyze`. This default destination must point at `/home`.

Security (RESEARCH Security Domain): the repoint MUST stay same-origin (no open redirect — V5), `/home` MUST be auth-gated (it is born inside `(app)` in plan 01-03; add it to `PROTECTED_PREFIXES` for defense-in-depth — Spoofing mitigation), and the IDOR-defended permalink (`/analyze/[id]`) is untouched.

Depends on plan 01-03: `/home` must exist before we route users to it (avoids a redirect-to-404 window).
Purpose: close the SHELL-06 loop — a signed-in user lands on the new home, and reopening a Simulation still restores via the kept permalink.
Output: repointed middleware + auth callback, a landing-redirect test, `/analyze` left dormant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-shell/01-CONTEXT.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md
@.planning/phases/01-foundation-shell/01-VALIDATION.md

<interfaces>
<!-- Verified against live files 2026-06-13 (RESEARCH C15 + A4 grep, re-verified). Read both files in full before editing. -->

src/lib/supabase/middleware.ts (153 lines) — VERIFIED:
- L12-13  PROTECTED_PREFIXES = [ "/analyze", … ]   // ADD "/home" (defense-in-depth, Spoofing)
- L25-29  PUBLIC_PATHS = [ "/", "/login", … ]        // do NOT add /home here
- L55-58  if (pathname === "/dashboard" || startsWith("/dashboard/")) -> 308 redirect to "/analyze" (same-origin clone)   // KEEP /dashboard sunset, but its TARGET may move to /home (see action)
- L120-127 unauthenticated on protected -> redirect /login?returnTo=…   // /home inherits this once it is in PROTECTED_PREFIXES
- L130-131 if (user && (pathname === "/login" || pathname === "/signup")) -> redirect new URL("/analyze", request.url)   // REPOINT -> "/home"
- Redirects use request.nextUrl.clone() / new URL(path, request.url) — same-origin (no reflected user input). PRESERVE this.

src/app/auth/callback/route.ts — VERIFIED:
- L18  const next = searchParams.get("next") ?? "/dashboard";   // REPOINT default -> "/home"
- L56-66 first-time users -> redirectTo = "/welcome" (onboarding)   // KEEP (onboarding takes precedence over the landing)
- L71  NextResponse.redirect(new URL(redirectTo, origin))   // same-origin; PRESERVE
- NOTE: `next` can be a user-supplied param. Keep using `new URL(next, origin)` so it resolves same-origin; do NOT redirect to an absolute external URL derived from `next` (open-redirect guard, V5).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test — authed landing + /home auth-gate redirect behavior</name>
  <files>src/lib/supabase/__tests__/middleware.landing.test.ts</files>
  <read_first>
    - src/lib/supabase/middleware.ts (the redirect branches to assert; the helper export surface)
    - .planning/phases/01-foundation-shell/01-VALIDATION.md § Per-Task Verification Map (SHELL-06 row) + § Sampling Rate
    - any existing test under src/lib/supabase/__tests__/ (mirror the mock pattern for the Supabase SSR client + NextRequest if one exists)
  </read_first>
  <behavior>
    - An authed user hitting /login or /signup is redirected to /home (not /analyze)
    - An unauthenticated user hitting /home is redirected to /login (it is a protected prefix)
    - Redirect Location is same-origin (path-only / same host) — no external/absolute redirect
  </behavior>
  <action>
    Author a focused test for the middleware landing logic. Mock the Supabase SSR client's getUser() (authed vs unauthenticated) and construct a NextRequest for /login, /signup, and /home. Assert the redirect target path is /home for the authed-on-auth-page case and /login for the unauthenticated-on-/home case; assert the redirect URL host equals the request host (same-origin).
    If the middleware is structured as one exported function that is awkward to unit-test, assert at the smallest testable seam available (e.g. extract or call the path-classification helper) — but prefer a behavior test over the redirect. EXPECTED to fail until Task 2 lands. Use vitest run (never watch).
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-rework && test -f src/lib/supabase/__tests__/middleware.landing.test.ts && grep -q '/home' src/lib/supabase/__tests__/middleware.landing.test.ts && echo WAVE0_LANDING_TEST_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/supabase/__tests__/middleware.landing.test.ts exists and asserts a /home landing for an authed user + a /login redirect for an unauthenticated /home hit
    - the test asserts the redirect is same-origin
  </acceptance_criteria>
  <done>The landing-redirect Wave-0 test exists and defines the target; it fails against the current /analyze landing until Task 2 lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repoint authed landing -> /home (middleware + auth callback), keep /analyze dormant</name>
  <files>src/lib/supabase/middleware.ts, src/app/auth/callback/route.ts</files>
  <read_first>
    - src/lib/supabase/middleware.ts (read in full — the PROTECTED_PREFIXES/PUBLIC_PATHS lists + the two redirect branches L55-58 and L130-131)
    - src/app/auth/callback/route.ts (read in full — the L18 default next + the L56-66 first-time/welcome branch + the L71 same-origin redirect)
    - .planning/phases/01-foundation-shell/01-RESEARCH.md § Security Domain (Known Threat Patterns: open redirect / auth bypass) + Open Q1/Q2 + C15
    - .planning/phases/01-foundation-shell/01-CONTEXT.md D-23 (new home = default authed landing; /analyze dormant-but-reachable)
  </read_first>
  <behavior>
    - middleware.landing.test.ts passes: authed-on-auth-page -> /home; unauthenticated-on-/home -> /login; same-origin
    - The OAuth callback default destination is /home (no explicit next), with /welcome still taking precedence for first-time users
    - /analyze and /analyze/[id] still resolve (not deleted; still protected)
  </behavior>
  <action>
    middleware.ts: add "/home" to PROTECTED_PREFIXES (L12-13) so unauthenticated hits redirect to /login (defense-in-depth on top of the (app) layout gate — Spoofing mitigation). Repoint the authed-off-auth-page redirect (L130-131) from `new URL("/analyze", request.url)` to `new URL("/home", request.url)`. For the `/dashboard` sunset 308 (L55-58): keep redirecting `/dashboard` away, but point its target at `/home` (the new landing) instead of `/analyze` — `/dashboard` stays sunset, just lands on the new home. Do NOT add /home to PUBLIC_PATHS. PRESERVE the same-origin `request.nextUrl.clone()` / `new URL(path, request.url)` pattern — never reflect a user-supplied value into Location unvalidated (open-redirect guard, V5).
    auth/callback/route.ts: change the default at L18 from `?? "/dashboard"` to `?? "/home"`. Keep the L56-66 first-time-user `/welcome` branch (onboarding precedence) and the L71 `new URL(redirectTo, origin)` same-origin redirect intact. If `next` is user-supplied, keep resolving it via `new URL(next, origin)` (same-origin) — do not follow an absolute external URL.
    Do NOT delete or modify /analyze or /analyze/[id] (D-23: dormant-but-reachable). Do NOT rename the permalink.
    The greeting/composer reflow + any landing transition stays calm (THEME-05) — though this plan is mostly routing, ensure no jarring redirect loop.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-rework && grep -q '"/home"' src/lib/supabase/middleware.ts && grep -q '/home' src/app/auth/callback/route.ts && ! grep -q '?? "/dashboard"' src/app/auth/callback/route.ts && npx vitest run src/lib/supabase 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - middleware.ts contains "/home" in PROTECTED_PREFIXES and the authed-off-auth-page redirect targets /home
    - auth/callback/route.ts default next is /home (no `?? "/dashboard"` remains)
    - middleware.ts still redirects via same-origin `new URL(..., request.url)` / `.clone()` (no reflected external redirect)
    - `npx vitest run src/lib/supabase` exits 0 (landing test green)
  </acceptance_criteria>
  <done>Authed users land on /home (middleware + callback both repointed, same-origin, /home auth-gated); /analyze stays dormant-but-reachable; landing test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated -> authed surface | The auth gate decides who reaches /home and where a signed-in user lands. This plan edits that decision — the highest-care surface of the phase. |
| user-supplied `next` param -> redirect Location | The OAuth callback's `next` is attacker-influenceable; it must resolve same-origin only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering (open redirect) | middleware + auth/callback redirect Location | mitigate | All redirects use `request.nextUrl.clone()` / `new URL(path, request.url|origin)` — same-origin by construction. The repoint preserves this; no user-supplied value is reflected into Location unvalidated (RESEARCH Security Domain V5). Verify command asserts the same-origin pattern remains. |
| T-04-02 | Spoofing (auth bypass) | new /home route | mitigate | /home is auth-gated twice: the (app)/layout.tsx server getUser() gate (plan 01-03) AND a PROTECTED_PREFIXES entry here (defense-in-depth). An unauthenticated /home hit redirects to /login — asserted by middleware.landing.test.ts. /home is NOT added to PUBLIC_PATHS. |
| T-04-03 | Information Disclosure | /analyze/[id] permalink (unchanged) | accept (mitigated by reusing existing validated code) | The permalink + its IDOR defense (`.eq user_id`) are untouched by the landing repoint. SHELL-06 restore continues through the existing route. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH Package Legitimacy Audit: zero external installs in Phase 1). No supply-chain surface; no legitimacy checkpoint required. |
</threat_model>

<verification>
- middleware.landing.test.ts green: authed -> /home, unauthenticated /home -> /login, same-origin (SHELL-06 / D-23).
- middleware.ts has "/home" in PROTECTED_PREFIXES + authed-landing repointed; auth/callback default is /home.
- Same-origin redirect pattern preserved (open-redirect guard).
- /analyze + /analyze/[id] untouched (dormant-but-reachable; permalink IDOR intact).
</verification>

<success_criteria>
- Authed users land on `/home`; both the middleware redirect and the OAuth callback default are repointed, same-origin.
- `/home` is auth-gated (PROTECTED_PREFIXES + the (app) layout gate); unauthenticated hits go to `/login`.
- `/analyze` stays dormant-but-reachable; the `/analyze/[id]` permalink + IDOR defense are untouched.
- Landing-redirect test green.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-04-SUMMARY.md` when done.
</output>
