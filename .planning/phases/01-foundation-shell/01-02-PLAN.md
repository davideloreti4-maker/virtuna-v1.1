---
phase: 01-foundation-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sidebar/Sidebar.tsx
  - src/components/app/app-shell.tsx
  - src/components/sidebar/__tests__/Sidebar.collapse.test.tsx
  - src/components/sidebar/__tests__/Sidebar.recent.test.tsx
  - src/components/sidebar/__tests__/Sidebar.glass-strip.test.tsx
autonomous: true
requirements: [SHELL-05, SHELL-07, THEME-02, THEME-05]
must_haves:
  truths:
    - "On desktop the sidebar is persistent and collapses to an icon rail via Cmd-backslash; the choice persists across reload"
    - "On mobile (<768px) the sidebar is a slide-in drawer from the left with a backdrop"
    - "The sidebar lists past Simulations from useAnalysisHistory under a 'Simulations' label with score chips"
    - "No Pinned / Projects / Boards / Running dead affordances remain in the sidebar"
    - "No 137deg Raycast gradient or backdrop-filter blur remains in Sidebar.tsx (glass strip Layer B)"
    - "Main content shifts right by the real sidebar width when expanded and resets to 0 on mobile"
  artifacts:
    - path: "src/components/sidebar/Sidebar.tsx"
      provides: "Lean reskinned sidebar — revived collapse, flat-warm (no inline glass), Simulations list, @handle selector kept"
      contains: "isCollapsed"
    - path: "src/components/app/app-shell.tsx"
      provides: "Main content offset wired to real sidebar width (desktop) / 0 (mobile)"
      contains: "sidebar"
    - path: "src/components/sidebar/__tests__/Sidebar.glass-strip.test.tsx"
      provides: "THEME-02 Layer B source-grep regression guard"
      contains: "137deg"
  key_links:
    - from: "src/components/sidebar/Sidebar.tsx"
      to: "src/stores/sidebar-store.ts"
      via: "useSidebarStore() — consume isCollapsed/toggleCollapsed instead of hardcoded false"
      pattern: "isCollapsed"
    - from: "src/components/app/app-shell.tsx"
      to: "src/stores/sidebar-store.ts"
      via: "read isCollapsed to compute the main-content offset"
      pattern: "marginLeft|sidebar-offset"
    - from: "src/components/sidebar/Sidebar.tsx"
      to: "useAnalysisHistory"
      via: "Simulations list rows route to /analyze/[id] permalink"
      pattern: "/analyze/"
---

<objective>
Reskin and re-wire the existing `Sidebar.tsx` (756 lines, fully wired to `useAnalysisHistory` + a complete-but-dead collapse implementation) into the lean flat-warm sidebar of past **Simulations**, and wire `app-shell.tsx` so main content shifts for the persistent desktop sidebar.

Four moves, all on existing code (RESEARCH: ~80% rewire, not a rebuild):
1. **Strip glass Layer B** (THEME-02 / D-06) — the 3 hardcoded inline-glass spots (nav L414-420, account popover ~683-686, hamburger ~747-751). Token swap alone will NOT fix these (Pitfall 4).
2. **Revive collapse** (SHELL-05/07 / D-14/D-15/D-16) — replace the hardcoded `effectiveCollapsed = false` (L358) with the persisted `sidebar-store` (`isCollapsed`/`toggleCollapsed` already exist, C5), add the Cmd-backslash keybind, expanded-by-default, mobile = drawer.
3. **Cut dead affordances** (D-11) — remove Pinned stub, Projects "Soon", "Boards" nav, the Running stub.
4. **Relabel + reskin** (D-13) — "Recent" → "Simulations"; keep score chips + remix tag + `SidebarAccountSelector` (@handle, D-12); rows route to `/analyze/[id]` (the permalink, kept).

Plan 01-01 owns Layer A (token glass strip); this plan owns Layer B (inline). Both must land before the THEME-06 gate.
Purpose: the sidebar is half the shell the human reviews at the THEME-06 gate; it must be lean, flat-warm, and collapsible.
Output: reskinned `Sidebar.tsx`, offset-wired `app-shell.tsx`, Wave-0 collapse/glass tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-shell/01-CONTEXT.md
@.planning/phases/01-foundation-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md
@.planning/phases/01-foundation-shell/01-VALIDATION.md

<interfaces>
<!-- Verified against live files 2026-06-13. Read the full Sidebar.tsx + app-shell.tsx + sidebar-store.ts before editing. -->

src/stores/sidebar-store.ts (COMPLETE — consume, do not author):
  isOpen (default true) | isCollapsed (default false)
  toggle() | open() | close() | toggleCollapsed() | setCollapsed(v)
  persisted via persist name 'virtuna-sidebar'   // KEEP this key (existing users' pref — RESEARCH Runtime State Inventory)

src/components/sidebar/Sidebar.tsx (756 lines) — VERIFIED spans:
- L358  const effectiveCollapsed = false;            // REPLACE with store-driven value
- L410  "w-[220px]"                                    // expanded width; icon-rail target ~56-64px (UI-SPEC Spacing)
- L414-417  nav inline glass: backgroundImage 137deg gradient + backdropFilter blur(5px) + inset shine   // STRIP to flat
- ~L683-686 account popover inline glass: backgroundColor rgba(22,23,25,0.98) + backdropFilter blur(12px)   // STRIP blur -> opaque charcoal
- ~L747-751 hamburger inline glass: 137deg gradient + backdropFilter blur(8px)   // STRIP to flat
- L8-12 header comment documents the nodes to cut: "Boards / Trending" nav, "Running" stub, "Pinned" stub, "Projects" placeholder
- L470 SectionLabel "Navigate" + L474 "Boards" nav   // CUT Boards
- ~L490-497 Running stub   // CUT
- ~L498-521 Pinned section (+ collapsed-rail tooltip)   // CUT
- ~L522-617 Recent section   // RELABEL "Recent" -> "Simulations"; rows already route by id
- ~L618-630 Projects "Coming soon" placeholder   // CUT
- L59 relativeTime() | L75 scoreTone() | SidebarAccountSelector (@handle) — KEEP (reskin flat)

src/components/app/app-shell.tsx (25 lines):
- L16-21 main has style --sidebar-offset 0px   // wire offset to real width; the var is declared-but-unread today

Existing hooks (reuse — RESEARCH Don't Hand-Roll):
- useIsMobile() — 768px breakpoint, SSR-safe (mobile=true default)
- usePrefersReducedMotion() — calm/reduced-motion (THEME-05)

Existing tests to honor:
- src/components/sidebar/__tests__/Sidebar.recent.test.tsx   // EXTEND (Simulations label, no dead nodes)
- src/components/sidebar/__tests__/Sidebar.a11y.test.tsx      // must stay green
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 tests — collapse behavior, dead-node removal, glass-strip regression guard</name>
  <files>src/components/sidebar/__tests__/Sidebar.collapse.test.tsx, src/components/sidebar/__tests__/Sidebar.glass-strip.test.tsx, src/components/sidebar/__tests__/Sidebar.recent.test.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-shell/01-VALIDATION.md § Wave 0 Requirements + Per-Task Verification Map (SHELL-05/07, THEME-02 rows)
    - src/components/sidebar/__tests__/Sidebar.recent.test.tsx (existing pattern to mirror + extend — happy-dom pragma, render harness, query-client mock)
    - src/components/sidebar/__tests__/Sidebar.a11y.test.tsx (render/mock pattern)
    - src/test/setup.ts (matchMedia + ResizeObserver shims available for the viewport-mocked tests)
    - src/stores/sidebar-store.ts (the store the collapse test drives)
  </read_first>
  <behavior>
    - Sidebar.collapse.test.tsx: Cmd-backslash keydown toggles isCollapsed; collapsed desktop renders the icon rail (narrower); mobile (useIsMobile true) renders the drawer (never the collapsed rail); collapse choice reads from the persisted store
    - Sidebar.recent.test.tsx (extended): section label reads "Simulations" (not "Recent"); no node with text "Pinned", "Projects", or "Boards" renders
    - Sidebar.glass-strip.test.tsx: source-asserts Sidebar.tsx file text contains neither the 137deg gradient nor backdropFilter (THEME-02 Layer B regression guard — cheap, fast)
  </behavior>
  <action>
    Author these as London-style tests (mock the store + hooks) per CLAUDE.md "TDD London for new code". They are EXPECTED to fail initially (Tasks 2-3 make them pass) — write them first so the implementation has a target.
    Create src/components/sidebar/__tests__/Sidebar.collapse.test.tsx (happy-dom pragma): mock useSidebarStore + useIsMobile; assert a Cmd-backslash (metaKey + backslash) keydown calls toggleCollapsed; assert the collapsed-rail vs expanded vs mobile-drawer rendering branches.
    Create src/components/sidebar/__tests__/Sidebar.glass-strip.test.tsx (node env — it reads the source file): readFileSync the Sidebar.tsx path and assert the source does NOT match the 137deg-gradient regex and does NOT match backdropFilter.
    Extend src/components/sidebar/__tests__/Sidebar.recent.test.tsx: add assertions that the label is "Simulations" and that "Pinned"/"Projects"/"Boards" do not appear.
    Use vitest run (never watch).
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-rework && test -f src/components/sidebar/__tests__/Sidebar.collapse.test.tsx && test -f src/components/sidebar/__tests__/Sidebar.glass-strip.test.tsx && grep -q Simulations src/components/sidebar/__tests__/Sidebar.recent.test.tsx && echo WAVE0_SIDEBAR_TESTS_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.collapse.test.tsx exists and references toggleCollapsed + a Cmd-backslash keydown
    - Sidebar.glass-strip.test.tsx exists and asserts the absence of the 137deg gradient and backdropFilter
    - Sidebar.recent.test.tsx contains an assertion on the string Simulations
  </acceptance_criteria>
  <done>The Wave-0 scaffolds exist and define the target behavior; they fail against the current sidebar (red) until Tasks 2-3 land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strip inline glass (Layer B) + cut dead affordances + relabel Simulations</name>
  <files>src/components/sidebar/Sidebar.tsx</files>
  <read_first>
    - src/components/sidebar/Sidebar.tsx (read ENTIRE 756-line file — know the section structure before cutting)
    - .planning/phases/01-foundation-shell/01-UI-SPEC.md § Glass Strip Map (Layer B table) + § Sidebar (D-10..D-16) + § Copywriting Contract (Simulations labels + empty copy)
    - .planning/phases/01-foundation-shell/01-RESEARCH.md Pitfall 4 (inline glass survives token reskin) + § Don't Hand-Roll (relativeTime/scoreTone reuse)
  </read_first>
  <behavior>
    - Sidebar.tsx source contains no 137deg gradient and no backdropFilter (the 3 inline spots flattened) -> Sidebar.glass-strip.test.tsx passes
    - No "Pinned", "Projects", "Boards", or "Running" nodes render -> extended Sidebar.recent.test.tsx passes
    - The history section label reads "Simulations"; empty state reads "No simulations yet."
    - SidebarAccountSelector (@handle) + score chips + remix tag + relativeTime/scoreTone are retained
  </behavior>
  <action>
    Replace the 3 inline-glass styles with flat-warm surfaces consuming the plan-01-01 tokens: nav (L414-420) -> solid charcoal-sidebar / surface token + hairline border + NO blur + NO inset shine; account popover (~683-686) -> opaque charcoal surface + NO blur; hamburger (~747-751) -> flat charcoal + NO blur. Matte target = no blur at all (D-05/D-06). If any blur legitimately survived it would have to go via inline style (Lightning CSS strips the class — Pitfall 2), but here DELETE it.
    CUT (D-11): the "Boards" nav item (L474, and the "Navigate" SectionLabel if no items survive it), the Running stub (~L490-497), the entire Pinned section incl. its collapsed-rail tooltip (~L498-521), and the Projects "Coming soon" placeholder (~L618-630). Remove now-unused icon/import lines. Update the header doc comment (L8-12) to the lean composition.
    RELABEL (D-13): the "Recent" SectionLabel -> "Simulations"; empty-state copy -> "No simulations yet." (muted, per UI-SPEC Copywriting). KEEP the history rows routing by id to /analyze/[id] (D-13 = rows route to the Simulation permalink, which IS /analyze/[id], retained per RESEARCH A2 — do NOT rename to /s/[id]).
    KEEP + reskin flat: SidebarAccountSelector (@handle, D-12); score chips (scoreTone logic stays — reskin to the score-zone data colors); remix tag; relativeTime. The New Simulation CTA stays at top (coral) — point its href at /home (the new authed home created in plan 01-03; a Link href="/home" is a string and is safe before the route exists).
    Watch the 500-line budget (CLAUDE.md): the cuts shrink the file; if it remains over 500 lines after, extract SidebarAccountSelector into its own file under src/components/sidebar/.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-rework && ! grep -q 'linear-gradient(137deg' src/components/sidebar/Sidebar.tsx && ! grep -q backdropFilter src/components/sidebar/Sidebar.tsx && grep -q Simulations src/components/sidebar/Sidebar.tsx && npx vitest run src/components/sidebar/__tests__/Sidebar.glass-strip.test.tsx src/components/sidebar/__tests__/Sidebar.recent.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.tsx contains zero `linear-gradient(137deg` and zero backdropFilter occurrences
    - Sidebar.tsx contains the string Simulations; contains no Pinned/"Coming soon"/Boards rendered labels
    - vitest run of Sidebar.glass-strip.test.tsx + Sidebar.recent.test.tsx exits 0
  </acceptance_criteria>
  <done>The sidebar is flat-warm (no inline glass), lean (no dead affordances), and labelled "Simulations"; glass-strip + recent tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Revive collapse (Cmd-backslash, persisted) + mobile drawer + wire app-shell offset</name>
  <files>src/components/sidebar/Sidebar.tsx, src/components/app/app-shell.tsx</files>
  <read_first>
    - src/components/sidebar/Sidebar.tsx (the post-Task-2 file; the L358 effectiveCollapsed site + the w-[220px] container + the mobile drawer/translate branch)
    - src/components/app/app-shell.tsx (25 lines — the --sidebar-offset declaration on main)
    - src/stores/sidebar-store.ts (isCollapsed/toggleCollapsed/isOpen — already persisted)
    - .planning/phases/01-foundation-shell/01-RESEARCH.md § "Reviving the sidebar collapse" + § "wire the offset" code examples; § Anti-Patterns
    - .planning/phases/01-foundation-shell/01-UI-SPEC.md § Sidebar (D-14 desktop persistent+collapsible / D-15 mobile drawer / D-16 expanded default) + § Motion (calm, respect reduced-motion)
  </read_first>
  <behavior>
    - Cmd-backslash (metaKey/ctrlKey + backslash) toggles the sidebar collapse and preventDefaults
    - Desktop: effectiveCollapsed = !isMobile && isCollapsed (icon rail when collapsed, ~56-64px; full 220px when expanded)
    - Mobile (useIsMobile true): never collapsed-rail; the sidebar is the fixed translate-in drawer driven by isOpen, with a backdrop
    - app-shell main offset = 0 on mobile; on desktop = rail width when collapsed, 220px when expanded (content shifts, no overlap)
    - Collapse/expand persists across reload (sidebar-store persist key 'virtuna-sidebar' unchanged)
  </behavior>
  <action>
    Replace Sidebar.tsx L358 `const effectiveCollapsed = false;` with store-driven state: read `{ isOpen, close, isCollapsed, toggleCollapsed }` from useSidebarStore() and `const isMobile = useIsMobile();` then `const effectiveCollapsed = !isMobile && isCollapsed;` (mobile is a drawer, never a collapsed rail).
    Add a Cmd-backslash keybind in a useEffect: on `(e.metaKey || e.ctrlKey) && e.key === backslash` call `e.preventDefault()` + `toggleCollapsed()`; clean up the listener on unmount.
    Mobile drawer (D-15): keep the existing fixed translate-in behavior driven by `isOpen` with a backdrop that calls `close()` on tap. Desktop (D-14): the sidebar is persistent (still `fixed`, but main content is offset to avoid overlap — see app-shell). Expanded by default (D-16) is already the store default (isCollapsed false / isOpen true) — do not change the defaults.
    Wire app-shell.tsx: read `{ isCollapsed, isOpen }` + `useIsMobile()`; compute `offset = isMobile ? "0px" : (isCollapsed ? railWidth : "220px")` and apply it as `marginLeft` (or `paddingLeft`) on the `<main>` element (replace the static `--sidebar-offset: 0px`). Use the same rail/expanded widths the sidebar uses (define them once, e.g. as CSS vars `--sidebar-w` / `--sidebar-rail`, or shared constants).
    Motion (THEME-05): the collapse/drawer transition is calm (use existing `--duration-*` / `--ease-out-cubic`); gate any motion on `usePrefersReducedMotion()`. No flashy reveal.
    app-shell is a thin wrapper today — if it must read the store/hook it becomes `"use client"`; add the directive if needed (it only wraps `<main>` + children).
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-numen-rework && grep -q useSidebarStore src/components/sidebar/Sidebar.tsx && grep -q toggleCollapsed src/components/sidebar/Sidebar.tsx && ! grep -q 'effectiveCollapsed = false' src/components/sidebar/Sidebar.tsx && npx vitest run src/components/sidebar 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.tsx no longer contains `effectiveCollapsed = false`; it reads isCollapsed/toggleCollapsed from useSidebarStore and a Cmd-backslash keydown handler
    - app-shell.tsx reads the collapse state and applies a non-static offset to `<main>` (no longer a hardcoded 0px)
    - `npx vitest run src/components/sidebar` exits 0 (collapse + recent + a11y all green)
  </acceptance_criteria>
  <done>Desktop sidebar collapses via Cmd-backslash and persists; mobile is a drawer; main content shifts for the persistent desktop sidebar; full sidebar test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentation reskin of an already-authed sidebar. The history data already flows through the IDOR-defended `/api/analysis/history`; this plan does not change data fetching. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Sidebar Simulation list (useAnalysisHistory) | accept (mitigated by reusing existing validated code) | The list already consumes `/api/analysis/history`, which enforces `.eq("user_id", user.id)` (RESEARCH-verified IDOR defense). This plan only reskins/relabels the rows — it does not add a new fetch or bypass the user scope. |
| T-02-02 | Tampering | Sidebar row navigation target (`/analyze/[id]`) | accept (mitigated by reusing existing validated code) | Rows route to the existing permalink `/analyze/[id]`, whose route enforces ownership server-side. No client-only fetch is introduced. Keeping `/analyze/[id]` (not renaming) preserves the verified auth check. |
| T-02-03 | Tampering | persisted `sidebar-store` (localStorage) | accept | Stores only a UI boolean (collapse/open). No secret, no auth token. Keeping the `virtuna-sidebar` key avoids resetting user prefs; a tampered value only changes local sidebar width. |
</threat_model>

<verification>
- Source-grep proves no 137deg gradient / no backdropFilter in `Sidebar.tsx` (THEME-02 Layer B) — both the inline guard test and the verify command.
- `effectiveCollapsed = false` is gone; collapse reads the persisted store; Cmd-backslash handler present (SHELL-05/07).
- Extended recent test proves "Simulations" label + no Pinned/Projects/Boards nodes (D-11/D-13).
- `npx vitest run src/components/sidebar` green (collapse + recent + a11y).
- Visual flat-warm correctness is `[UAT]` — verified at the THEME-06 gate (plan 01-05).
</verification>

<success_criteria>
- Lean flat-warm sidebar: no inline glass, no dead affordances, "Simulations" list with score chips, @handle kept.
- Desktop collapse via Cmd-backslash persists; mobile drawer; main content offset wired.
- Sidebar test suite (collapse, recent, glass-strip, a11y) green.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-02-SUMMARY.md` when done.
</output>
