---
phase: 01-foundation-shell
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/marketing/placeholder.tsx
  - src/components/marketing/index.ts
  - src/components/marketing/__tests__/placeholder.test.tsx
autonomous: true
requirements: [FOUND-03]
must_haves:
  truths:
    - "A <Placeholder> renders a labelled, aspect-ratio-correct stand-in in image / video / avatar / logo variants"
    - "When src is passed it renders the real <img>/<video>; when absent it renders the labelled stand-in — the one-prop swap"
    - "Each slot reserves its box via CSS aspect-ratio before any asset loads (no layout shift)"
    - "The optional skeleton-breathe is reduced-motion gated (motion-reduce:animate-none), and is OFF by default"
  artifacts:
    - path: "src/components/marketing/placeholder.tsx"
      provides: "The reusable CVA placeholder-slot component (image/video/avatar/logo variants, src swap, aspect lock)"
      min_lines: 40
      contains: "cva"
    - path: "src/components/marketing/index.ts"
      provides: "Barrel export for marketing components"
      exports: ["Placeholder"]
    - path: "src/components/marketing/__tests__/placeholder.test.tsx"
      provides: "FOUND-03 unit coverage — variant→icon, src-vs-stand-in, aspect lock, reduced-motion gate"
  key_links:
    - from: "src/components/marketing/placeholder.tsx"
      to: "@/lib/utils"
      via: "cn() className composition"
      pattern: "cn\\("
    - from: "src/components/marketing/index.ts"
      to: "./placeholder"
      via: "barrel re-export"
      pattern: "export.*Placeholder.*placeholder"
---

<objective>
Build the reusable `<Placeholder>` slot — the single component every later section uses to stand in for product visuals (screenshots, demos, avatars, logos) until the human swaps in real assets via one `src` prop.

Purpose: Give Phases 2–4 a labelled, aspect-locked, flat-warm stand-in that reserves its layout box so the page never shifts when assets arrive. (FOUND-03)
Output: `src/components/marketing/placeholder.tsx` (CVA, matching `ui/` conventions), its barrel, and a happy-dom unit test covering variant resolution, the `src` swap, the aspect-ratio lock, and the reduced-motion breathe gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-shell/01-CONTEXT.md
@.planning/phases/01-foundation-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-shell/01-VALIDATION.md

<interfaces>
<!-- The CVA + cn() convention this component MUST mirror. Extracted so no exploration is needed. -->
From src/components/ui/button.tsx (the pattern analog):
  import { cva, type VariantProps } from "class-variance-authority";
  import { cn } from "@/lib/utils";
  const buttonVariants = cva([base], { variants: {...}, defaultVariants: {...} });
  // forwardRef, displayName, named export + variants export

From src/components/ui/skeleton.tsx (the reduced-motion breathe-gating pattern):
  // className includes "motion-reduce:animate-none" which sets animation:none and
  // overrides the inline animation style. Mirror this for the optional breathe.

From src/lib/utils.ts:
  export function cn(...inputs): string   // twMerge(clsx(inputs))

Lucide glyphs (already installed, lucide-react ^0.563.0) per UI-SPEC:
  image→Image, video→Video, avatar→UserRound, logo→Building2

The flat-warm token classes (available after 01-01's theme port):
  surface  = bg-[--color-surface-elevated]  (#2f2e2b chip)
  border   = border-[--color-border]        (rgba(255,255,255,0.06))
  caption  = text-[--color-foreground-muted](#8a857c cream-muted)
  radius   = rounded-[--radius-lg]           (12px); avatar → rounded-full
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the Wave-0 failing test for &lt;Placeholder&gt; (FOUND-03)</name>
  <files>src/components/marketing/__tests__/placeholder.test.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-shell/01-VALIDATION.md (Wave 0 Requirements — the exact behaviors this file must cover)
    - .planning/phases/01-foundation-shell/01-UI-SPEC.md (§"Component Inventory" item 1 — the `<Placeholder>` API + look + variant overrides)
    - /Users/davideloreti/virtuna-landing-v2/src/components/ui/skeleton.tsx (the motion-reduce gating reference)
  </read_first>
  <behavior>
    - Test: renders variant="image" with no `src` → shows the labelled stand-in (the `label` text is in the document) and does NOT render an `<img>` with that src.
    - Test: renders variant="image" `src="/real.png"` → renders a real `<img>` whose `src` ends with `/real.png` (the one-prop swap).
    - Test: renders variant="video" `src="/clip.mp4"` → renders a `<video>` element.
    - Test: variant→icon resolution — each of image/video/avatar/logo renders its lucide glyph (assert via an icon test-id or an accessible name / svg presence per variant). Use a stable hook (e.g. `data-variant={variant}` on the root, or `aria-hidden` svg count) so the assertion is deterministic.
    - Test: aspect lock — the root element carries an inline `style` with `aspect-ratio` reflecting the `aspect` prop (e.g. `aspect="16/9"` → `aspectRatio: "16/9"`); `avatar` defaults to `1/1`.
    - Test: reduced-motion gate — when breathe is opted in, the animated element carries the `motion-reduce:animate-none` class (string assertion on className); by default no breathe animation class is present.
  </behavior>
  <action>
    Create the happy-dom test at `src/components/marketing/__tests__/placeholder.test.tsx`. Add the file pragma `/** @vitest-environment happy-dom */` at the very top (the suite default env is `node`). Import from `@testing-library/react` (`render`, `screen`) — confirmed present after 01-01's install. Import `{ Placeholder }` from `../placeholder` (it does NOT exist yet — this test MUST fail at import/red). Write the assertions per `<behavior>`. This is the RED step: run it and confirm it fails because `Placeholder` is undefined.
  </action>
  <verify>
    <automated>pnpm vitest run src/components/marketing/__tests__/placeholder.test.tsx 2>&1 | grep -Eq "Cannot find module|is not defined|No test|fail" && echo "RED confirmed (Placeholder missing)" || echo "expected RED"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/marketing/__tests__/placeholder.test.tsx` exists, begins with the happy-dom pragma, and imports `{ Placeholder }` from `../placeholder`.
    - Running it fails (RED) because `../placeholder` / `Placeholder` does not yet exist.
  </acceptance_criteria>
  <done>The failing test exists and fails at red for the right reason (component not implemented).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement &lt;Placeholder&gt; (CVA, flat-warm) + barrel — make the test GREEN</name>
  <files>src/components/marketing/placeholder.tsx, src/components/marketing/index.ts</files>
  <read_first>
    - /Users/davideloreti/virtuna-landing-v2/src/components/ui/button.tsx (CVA + forwardRef + variants-export convention to mirror)
    - /Users/davideloreti/virtuna-landing-v2/src/components/ui/skeleton.tsx (motion-reduce:animate-none breathe gate)
    - /Users/davideloreti/virtuna-landing-v2/src/lib/utils.ts (cn())
    - .planning/phases/01-foundation-shell/01-UI-SPEC.md (§"Component Inventory" item 1 — authoritative look/behavior/variant overrides)
    - src/components/marketing/__tests__/placeholder.test.tsx (the contract this must satisfy)
  </read_first>
  <action>
    Create `src/components/marketing/placeholder.tsx` (D-12/D-13/D-14/D-15). Props: `variant: "image" | "video" | "avatar" | "logo"` (CVA enum, `defaultVariants: { variant: "image" }`), `aspect?: string` (free CSS aspect-ratio string → inline `style={{ aspectRatio: aspect }}`, NOT a CVA key; default `16/9`, but `avatar` defaults `1/1`), `label?: string` (cream-muted caption), `src?: string` (the one-prop swap), plus `className`. Use `cva` + `cn()` exactly like `ui/button.tsx`; `forwardRef`, `displayName = "Placeholder"`, named export + a `placeholderVariants` export.

    Behavior: when `src` is present, render the real media — `<img src={src} .../>` for image/avatar/logo, `<video src={src} .../>` for video (muted/no controls is fine — it's a stand-in). When `src` is absent, render the labelled stand-in: charcoal **chip** surface (`--color-surface-elevated` #2f2e2b), hairline **6% border** (`--color-border`), **12px radius** (`--radius-lg`); a centered low-opacity (~0.25–0.35) lucide media-type icon colored `--color-foreground-muted` (image→`Image`, video→`Video`, avatar→`UserRound`, logo→`Building2`); the `label` caption beneath in cream-muted; a faint **mono** dimension hint (`--font-mono`, `--text-xs`, cream-muted, lower opacity) echoing the resolved aspect (e.g. `16:9`). NO dashed-wireframe border, NO glass, NO shine, NO glow (D-13). Variant overrides: `avatar` → `rounded-full` + `1/1`; `logo` → icon-only acceptable (caption optional), muted; `video` → may overlay a static muted play-triangle affordance.

    No-layout-shift (D-14): the inline `aspect-ratio` reserves the box before any asset loads — this is the FOUND-06 setup. Optional `skeleton-breathe`: expose it behind an opt-in prop (default OFF), and when on, apply the `animate-skeleton-breathe` utility together with `motion-reduce:animate-none` (mirror `ui/skeleton.tsx`) so it auto-halts under reduced motion. Default OFF means a plain non-animated slot unless a caller opts in. (The `skeleton-breathe` keyframe + the CSS reduced-motion gate arrive via 01-01's globals.css + 01-03's `@media` block.)

    Create `src/components/marketing/index.ts` barrel: `export { Placeholder, placeholderVariants } from "./placeholder"; export type { PlaceholderProps } from "./placeholder";` (D-15 — marketing surface kept out of the shared `ui/` design system).

    Iterate until the Task-1 test is fully GREEN.
  </action>
  <verify>
    <automated>pnpm vitest run src/components/marketing/__tests__/placeholder.test.tsx 2>&1 | tail -5; pnpm vitest run src/components/marketing/__tests__/placeholder.test.tsx > /dev/null 2>&1 && echo "GREEN" || (echo "still RED"; false)</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/components/marketing/__tests__/placeholder.test.tsx` passes (all behaviors GREEN).
    - `placeholder.tsx` uses `cva` and `cn(`, exports `Placeholder` + `placeholderVariants`, and renders the root with an inline `aspect-ratio` style.
    - `index.ts` re-exports `Placeholder` from `./placeholder`.
    - No `backdrop`, `blur`, or `--gradient-glass` strings appear in `placeholder.tsx` (flat-matte, D-13).
  </acceptance_criteria>
  <done>The `<Placeholder>` component exists, satisfies its test, and follows the flat-warm + CVA contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | Pure presentational component. No user input, no network, no datastore. `src` is a developer-supplied static path, not user-controlled at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering / XSS | `<Placeholder src>` rendering `<img>/<video>` | accept | `src` is a build-time developer prop (e.g. `/real.png`), never end-user input — there is no user-controlled data path on this static marketing surface. Standard React attribute escaping applies; no `dangerouslySetInnerHTML`. |

No untrusted input or auth in scope; the component is static and presentational.
</threat_model>

<verification>
- `pnpm vitest run src/components/marketing/__tests__/placeholder.test.tsx` GREEN.
- (At phase gate) DevTools "emulate prefers-reduced-motion: reduce" → an opted-in breathing slot halts.
</verification>

<success_criteria>
- FOUND-03: `<Placeholder variant aspect label src?>` renders labelled aspect-locked stand-ins in all 4 variants and swaps to real media via the single `src` prop, with no layout shift and a reduced-motion-gated optional breathe.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-02-SUMMARY.md` when done.
</output>
