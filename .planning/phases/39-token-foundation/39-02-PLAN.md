---
phase: 39-token-foundation
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - .planning/phases/39-token-foundation/39-EXTRACTION-DATA.md
autonomous: false

must_haves:
  truths:
    - "Store page fully extracted (hero, extension cards, badges, search, grid)"
    - "Pro page fully extracted (features, pricing highlights, testimonials)"
    - "AI page fully extracted (chat interface, code blocks, AI-specific styling)"
    - "iOS page fully extracted (app showcase, mobile-specific patterns)"
    - "Windows page fully extracted (platform-specific UI patterns)"
    - "Teams page fully extracted (enterprise features, comparison tables)"
    - "Pricing page fully extracted (pricing cards, toggles, feature lists)"
    - "Each page has screenshot proof of real Playwright extraction"
  artifacts:
    - path: ".planning/phases/39-token-foundation/39-EXTRACTION-DATA.md"
      provides: "Complete extraction data from ALL navbar pages"
      contains: "## Store Page"
  key_links:
    - from: "Playwright MCP"
      to: "raycast.com/store, /pro, /ai, /ios, /windows, /teams, /pricing"
      via: "browser_navigate sequence with screenshots"
      pattern: "MANDATORY real browser extraction for every page"
---

<objective>
Extract design tokens from ALL remaining Raycast pages using Playwright MCP.

Pages to extract (in navbar order):
1. Store (raycast.com/store)
2. Pro (raycast.com/pro)
3. AI (raycast.com/ai)
4. iOS (raycast.com/ios)
5. Windows (raycast.com/windows)
6. Teams (raycast.com/teams)
7. Pricing (raycast.com/pricing)

Each page: Navigate → Screenshot → Extract top-to-bottom → Document.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/39-token-foundation/39-RESEARCH.md
@.planning/phases/39-token-foundation/39-01-SUMMARY.md
@.planning/phases/39-token-foundation/39-EXTRACTION-DATA.md
</context>

<critical_constraint>
**MANDATORY PLAYWRIGHT USAGE**

You MUST use these Playwright MCP tools for EVERY page - NO fabricated data:
- `mcp__playwright__browser_navigate` - to visit each URL
- `mcp__playwright__browser_take_screenshot` - capture EVERY page
- `mcp__playwright__browser_evaluate` - extract computed styles
- `mcp__playwright__browser_snapshot` - understand page structure
- `mcp__playwright__browser_hover` - trigger hover states before extraction

**EXTRACTION PATTERN FOR EACH PAGE:**
1. Navigate to page
2. Take full-page screenshot
3. Extract hero/above-fold styles
4. **Trigger hover on interactive elements, capture hover styles**
5. Scroll down, screenshot each section
6. Extract unique components on that page
7. Document ALL values with source reference

If Playwright fails, STOP and report. Do NOT fabricate values.
</critical_constraint>

<extraction_priorities>
**CONTINUE DETAILED EXTRACTION FROM PLAN 01:**

1. **HOVER STATES** - For every interactive element on each page:
   - Use `browser_hover` → then `browser_evaluate`
   - Capture: background change, color change, transform, shadow change

2. **RAYCAST BRAND COLOR** - Note every occurrence of their red/pink:
   - Buttons, links, badges, gradients, accents
   - We replace ALL with coral #FF7F50

3. **GLASSMORPHISM** - Any glass/blur effects on subpages:
   - Pricing cards often have glass
   - Modal/overlay effects
   - Document: backdrop-filter, background alpha, border

4. **PAGE-SPECIFIC FONTS** - If any page uses different typography:
   - Pricing numbers might use display font
   - Code blocks use mono font
   - Note any variations from homepage
</extraction_priorities>

<tasks>

<task type="auto">
  <name>Task 1: Extract Store Page (raycast.com/store)</name>
  <files>.planning/phases/39-token-foundation/39-EXTRACTION-DATA.md</files>
  <action>
    **PAGE: https://raycast.com/store**

    **STEP 1: Navigate and initial screenshot**
    1. `mcp__playwright__browser_navigate` to https://raycast.com/store
    2. `mcp__playwright__browser_take_screenshot` - save as "03-store-hero.png"
    3. `mcp__playwright__browser_snapshot` to understand structure

    **STEP 2: Extract Store hero/search area**
    ```javascript
    (() => {
      const getStyles = (el, props) => {
        if (!el) return null;
        const styles = window.getComputedStyle(el);
        const result = {};
        props.forEach(p => result[p] = styles.getPropertyValue(p));
        return result;
      };

      const searchInput = document.querySelector('input[type="search"], input[type="text"], [class*="search"]');
      const pageTitle = document.querySelector('h1');

      return {
        pageTitle: {
          text: pageTitle?.textContent?.substring(0, 50),
          styles: getStyles(pageTitle, ['font-size', 'font-weight', 'color', 'margin-bottom'])
        },
        searchInput: {
          element: searchInput?.tagName + '.' + searchInput?.className?.split(' ')[0],
          styles: getStyles(searchInput, [
            'background-color', 'border', 'border-radius', 'color',
            'font-size', 'padding', 'height', 'box-shadow', 'outline'
          ]),
          placeholder: searchInput?.placeholder
        }
      };
    })();
    ```

    **STEP 3: Extract extension cards**
    ```javascript
    (() => {
      const getStyles = (el, props) => {
        if (!el) return null;
        const styles = window.getComputedStyle(el);
        const result = {};
        props.forEach(p => result[p] = styles.getPropertyValue(p));
        return result;
      };

      // Find extension cards
      const cards = document.querySelectorAll('[class*="extension"], [class*="card"], article');
      const firstCard = cards[0];
      const cardTitle = firstCard?.querySelector('h2, h3, [class*="title"]');
      const cardDesc = firstCard?.querySelector('p, [class*="description"]');
      const cardIcon = firstCard?.querySelector('img, svg');

      // Find category badges/tags
      const badges = document.querySelectorAll('[class*="badge"], [class*="tag"], [class*="category"]');
      const firstBadge = badges[0];

      // Find grid container
      const grid = firstCard?.parentElement;

      return {
        extensionCard: {
          count: cards.length,
          styles: getStyles(firstCard, [
            'background-color', 'background', 'border', 'border-radius',
            'box-shadow', 'padding', 'width', 'min-height', 'backdrop-filter'
          ])
        },
        cardTitle: {
          text: cardTitle?.textContent?.substring(0, 30),
          styles: getStyles(cardTitle, ['font-size', 'font-weight', 'color', 'margin-bottom'])
        },
        cardDescription: {
          styles: getStyles(cardDesc, ['font-size', 'color', 'line-height'])
        },
        cardIcon: {
          dimensions: cardIcon ? { w: cardIcon.offsetWidth, h: cardIcon.offsetHeight } : null,
          borderRadius: getStyles(cardIcon, ['border-radius'])
        },
        badge: {
          count: badges.length,
          text: firstBadge?.textContent?.trim(),
          styles: getStyles(firstBadge, [
            'background-color', 'color', 'font-size', 'font-weight',
            'padding', 'border-radius', 'border'
          ])
        },
        gridLayout: {
          styles: getStyles(grid, [
            'display', 'grid-template-columns', 'gap', 'row-gap', 'column-gap'
          ])
        }
      };
    })();
    ```

    **STEP 4: Scroll and capture more**
    - Scroll down to see more cards
    - Screenshot: "03-store-grid.png"
    - Check for hover states if possible

    **STEP 5: Append to 39-EXTRACTION-DATA.md**
    ```markdown
    ## Store Page

    URL: https://raycast.com/store
    Screenshots: 03-store-hero.png, 03-store-grid.png

    ### Search Input
    - **Background:** [value]
    - **Border:** [value]
    - **Border-radius:** [value]
    - **Font-size:** [value]
    - **Height:** [value]
    - **Placeholder text:** [value]

    ### Extension Cards
    - **Background:** [value - note if glassmorphism]
    - **Border:** [value]
    - **Border-radius:** [value]
    - **Box-shadow:** [value]
    - **Padding:** [value]

    ### Card Title
    - **Font-size:** [value]
    - **Font-weight:** [value]
    - **Color:** [value]

    ### Card Description
    - **Font-size:** [value]
    - **Color:** [value]

    ### Card Icon
    - **Dimensions:** [w x h]
    - **Border-radius:** [value]

    ### Category Badges
    - **Background:** [value]
    - **Color:** [value]
    - **Font-size:** [value]
    - **Padding:** [value]
    - **Border-radius:** [value]

    ### Grid Layout
    - **Columns:** [value]
    - **Gap:** [value]

    ---
    ```
  </action>
  <verify>
    - Screenshots 03-store-*.png exist
    - Store Page section added to extraction data
    - Extension card styling fully documented
    - Search input styling documented
    - Badge styling documented
    - Grid layout documented
  </verify>
  <done>Store page fully extracted with Playwright</done>
</task>

<task type="auto">
  <name>Task 2: Extract Pro and AI Pages</name>
  <files>.planning/phases/39-token-foundation/39-EXTRACTION-DATA.md</files>
  <action>
    **PAGE 1: https://raycast.com/pro**

    1. `mcp__playwright__browser_navigate` to https://raycast.com/pro
    2. `mcp__playwright__browser_take_screenshot` - "04-pro-hero.png"
    3. Extract unique Pro page elements:

    ```javascript
    (() => {
      const getStyles = (el, props) => {
        if (!el) return null;
        const styles = window.getComputedStyle(el);
        const result = {};
        props.forEach(p => result[p] = styles.getPropertyValue(p));
        return result;
      };

      const hero = document.querySelector('[class*="hero"], section:first-of-type');
      const proBadge = document.querySelector('[class*="pro"], [class*="badge"]');
      const featureCards = document.querySelectorAll('[class*="feature"], [class*="card"]');
      const testimonials = document.querySelectorAll('[class*="testimonial"], [class*="quote"], blockquote');

      return {
        heroSection: {
          styles: getStyles(hero, ['background', 'background-image', 'padding'])
        },
        proBadge: {
          text: proBadge?.textContent?.trim(),
          styles: getStyles(proBadge, [
            'background', 'background-color', 'color', 'font-size',
            'padding', 'border-radius', 'font-weight'
          ])
        },
        featureCard: {
          count: featureCards.length,
          styles: getStyles(featureCards[0], [
            'background-color', 'border', 'border-radius', 'padding', 'box-shadow'
          ])
        },
        testimonial: {
          count: testimonials.length,
          styles: getStyles(testimonials[0], [
            'background-color', 'border-left', 'padding', 'font-style', 'color'
          ])
        }
      };
    })();
    ```

    4. Scroll through page, screenshot sections: "04-pro-features.png"
    5. Document Pro page patterns

    ---

    **PAGE 2: https://raycast.com/ai**

    1. `mcp__playwright__browser_navigate` to https://raycast.com/ai
    2. `mcp__playwright__browser_take_screenshot` - "05-ai-hero.png"
    3. Extract AI-specific elements:

    ```javascript
    (() => {
      const getStyles = (el, props) => {
        if (!el) return null;
        const styles = window.getComputedStyle(el);
        const result = {};
        props.forEach(p => result[p] = styles.getPropertyValue(p));
        return result;
      };

      // Look for chat/conversation UI
      const chatContainer = document.querySelector('[class*="chat"], [class*="conversation"], [class*="message"]');
      const messageBubble = document.querySelector('[class*="bubble"], [class*="message"]');
      const codeBlock = document.querySelector('pre, code, [class*="code"]');
      const aiGradient = document.querySelector('[class*="gradient"], [class*="glow"]');

      return {
        chatInterface: {
          found: !!chatContainer,
          styles: getStyles(chatContainer, [
            'background-color', 'border-radius', 'padding', 'box-shadow'
          ])
        },
        messageBubble: {
          styles: getStyles(messageBubble, [
            'background-color', 'border-radius', 'padding', 'color', 'font-size'
          ])
        },
        codeBlock: {
          styles: getStyles(codeBlock, [
            'background-color', 'border', 'border-radius', 'padding',
            'font-family', 'font-size', 'color'
          ])
        },
        aiGradient: {
          styles: getStyles(aiGradient, ['background', 'background-image'])
        }
      };
    })();
    ```

    4. Scroll through AI page, screenshot: "05-ai-features.png"
    5. Document AI page patterns

    **Append both to 39-EXTRACTION-DATA.md**
  </action>
  <verify>
    - Screenshots 04-pro-*.png exist
    - Screenshots 05-ai-*.png exist
    - Pro Page section added with feature cards, badges
    - AI Page section added with chat interface, code blocks
  </verify>
  <done>Pro and AI pages extracted with Playwright</done>
</task>

<task type="auto">
  <name>Task 3: Extract iOS and Windows Pages</name>
  <files>.planning/phases/39-token-foundation/39-EXTRACTION-DATA.md</files>
  <action>
    **PAGE 1: https://raycast.com/ios**

    1. `mcp__playwright__browser_navigate` to https://raycast.com/ios
    2. `mcp__playwright__browser_take_screenshot` - "06-ios-hero.png"
    3. Extract iOS-specific elements:

    ```javascript
    (() => {
      const getStyles = (el, props) => {
        if (!el) return null;
        const styles = window.getComputedStyle(el);
        const result = {};
        props.forEach(p => result[p] = styles.getPropertyValue(p));
        return result;
      };

      const hero = document.querySelector('[class*="hero"], section:first-of-type');
      const appPreview = document.querySelector('[class*="preview"], [class*="phone"], [class*="device"], img[class*="app"]');
      const downloadBtn = document.querySelector('a[href*="app-store"], a[href*="apple"], [class*="download"]');
      const featureList = document.querySelectorAll('[class*="feature"] li, ul li');

      return {
        pageHero: {
          styles: getStyles(hero, ['background', 'background-color', 'padding'])
        },
        appPreview: {
          found: !!appPreview,
          dimensions: appPreview ? { w: appPreview.offsetWidth, h: appPreview.offsetHeight } : null,
          styles: getStyles(appPreview, ['border-radius', 'box-shadow'])
        },
        downloadButton: {
          text: downloadBtn?.textContent?.trim(),
          styles: getStyles(downloadBtn, [
            'background-color', 'color', 'padding', 'border-radius', 'font-size'
          ])
        },
        features: {
          count: featureList.length,
          sampleStyles: getStyles(featureList[0], ['font-size', 'color', 'padding'])
        }
      };
    })();
    ```

    4. Scroll and screenshot: "06-ios-features.png"

    ---

    **PAGE 2: https://raycast.com/windows**

    1. `mcp__playwright__browser_navigate` to https://raycast.com/windows
    2. `mcp__playwright__browser_take_screenshot` - "07-windows-hero.png"
    3. Extract Windows-specific elements:

    ```javascript
    (() => {
      const getStyles = (el, props) => {
        if (!el) return null;
        const styles = window.getComputedStyle(el);
        const result = {};
        props.forEach(p => result[p] = styles.getPropertyValue(p));
        return result;
      };

      const hero = document.querySelector('[class*="hero"], section:first-of-type');
      const waitlistForm = document.querySelector('form, [class*="waitlist"], [class*="signup"]');
      const emailInput = document.querySelector('input[type="email"], input[type="text"]');
      const submitBtn = document.querySelector('button[type="submit"], [class*="submit"]');

      return {
        pageHero: {
          styles: getStyles(hero, ['background', 'background-color', 'padding'])
        },
        waitlistForm: {
          found: !!waitlistForm,
          styles: getStyles(waitlistForm, ['background-color', 'border', 'border-radius', 'padding'])
        },
        emailInput: {
          styles: getStyles(emailInput, [
            'background-color', 'border', 'border-radius', 'padding', 'font-size', 'height'
          ])
        },
        submitButton: {
          text: submitBtn?.textContent?.trim(),
          styles: getStyles(submitBtn, [
            'background-color', 'color', 'padding', 'border-radius', 'font-size', 'font-weight'
          ])
        }
      };
    })();
    ```

    4. Scroll and screenshot: "07-windows-features.png"
    5. Document both pages in extraction data
  </action>
  <verify>
    - Screenshots 06-ios-*.png exist
    - Screenshots 07-windows-*.png exist
    - iOS Page section added with app preview, download button
    - Windows Page section added with waitlist form, input styles
  </verify>
  <done>iOS and Windows pages extracted with Playwright</done>
</task>

<task type="auto">
  <name>Task 4: Extract Teams and Pricing Pages</name>
  <files>.planning/phases/39-token-foundation/39-EXTRACTION-DATA.md</files>
  <action>
    **PAGE 1: https://raycast.com/teams**

    1. `mcp__playwright__browser_navigate` to https://raycast.com/teams
    2. `mcp__playwright__browser_take_screenshot` - "08-teams-hero.png"
    3. Extract Teams/enterprise elements:

    ```javascript
    (() => {
      const getStyles = (el, props) => {
        if (!el) return null;
        const styles = window.getComputedStyle(el);
        const result = {};
        props.forEach(p => result[p] = styles.getPropertyValue(p));
        return result;
      };

      const hero = document.querySelector('[class*="hero"], section:first-of-type');
      const comparisonTable = document.querySelector('table, [class*="comparison"], [class*="table"]');
      const tableHeader = comparisonTable?.querySelector('th, thead');
      const tableCell = comparisonTable?.querySelector('td, tbody td');
      const enterpriseCard = document.querySelector('[class*="enterprise"], [class*="team"]');

      return {
        pageHero: {
          styles: getStyles(hero, ['background', 'background-color', 'padding'])
        },
        comparisonTable: {
          found: !!comparisonTable,
          styles: getStyles(comparisonTable, [
            'background-color', 'border', 'border-radius', 'border-collapse'
          ])
        },
        tableHeader: {
          styles: getStyles(tableHeader, [
            'background-color', 'color', 'font-weight', 'padding', 'border-bottom'
          ])
        },
        tableCell: {
          styles: getStyles(tableCell, [
            'padding', 'border-bottom', 'color', 'font-size'
          ])
        },
        enterpriseCard: {
          styles: getStyles(enterpriseCard, [
            'background-color', 'border', 'border-radius', 'padding', 'box-shadow'
          ])
        }
      };
    })();
    ```

    4. Scroll and screenshot: "08-teams-features.png"

    ---

    **PAGE 2: https://raycast.com/pricing**

    1. `mcp__playwright__browser_navigate` to https://raycast.com/pricing
    2. `mcp__playwright__browser_take_screenshot` - "09-pricing-hero.png"
    3. Extract pricing page elements:

    ```javascript
    (() => {
      const getStyles = (el, props) => {
        if (!el) return null;
        const styles = window.getComputedStyle(el);
        const result = {};
        props.forEach(p => result[p] = styles.getPropertyValue(p));
        return result;
      };

      const pricingCards = document.querySelectorAll('[class*="pricing"], [class*="plan"], [class*="tier"]');
      const firstCard = pricingCards[0];
      const highlightedCard = document.querySelector('[class*="popular"], [class*="recommended"], [class*="highlight"]');
      const priceValue = document.querySelector('[class*="price"], [class*="amount"]');
      const toggle = document.querySelector('[class*="toggle"], [class*="switch"], [role="switch"]');
      const featureList = document.querySelector('[class*="feature"] ul, ul[class*="feature"]');
      const checkIcon = document.querySelector('[class*="check"], svg');

      return {
        pricingCard: {
          count: pricingCards.length,
          styles: getStyles(firstCard, [
            'background-color', 'border', 'border-radius', 'padding', 'box-shadow'
          ])
        },
        highlightedCard: {
          found: !!highlightedCard,
          styles: getStyles(highlightedCard, [
            'background-color', 'border', 'border-color', 'box-shadow'
          ])
        },
        priceValue: {
          text: priceValue?.textContent?.trim(),
          styles: getStyles(priceValue, [
            'font-size', 'font-weight', 'color', 'line-height'
          ])
        },
        billingToggle: {
          found: !!toggle,
          styles: getStyles(toggle, [
            'background-color', 'border-radius', 'width', 'height', 'padding'
          ])
        },
        featureList: {
          styles: getStyles(featureList, ['padding', 'margin', 'list-style'])
        },
        featureItem: {
          styles: getStyles(featureList?.querySelector('li'), [
            'padding', 'margin-bottom', 'font-size', 'color'
          ])
        },
        checkIcon: {
          styles: getStyles(checkIcon, ['width', 'height', 'color', 'fill'])
        }
      };
    })();
    ```

    4. Scroll and screenshot: "09-pricing-plans.png"
    5. Document both pages fully
  </action>
  <verify>
    - Screenshots 08-teams-*.png exist
    - Screenshots 09-pricing-*.png exist
    - Teams Page section with comparison table, enterprise cards
    - Pricing Page section with pricing cards, toggle, feature lists
    - Highlighted/recommended card styling captured
  </verify>
  <done>Teams and Pricing pages extracted with Playwright</done>
</task>

<task type="auto">
  <name>Task 5: Create Consolidated Token Summary</name>
  <files>.planning/phases/39-token-foundation/39-EXTRACTION-DATA.md</files>
  <action>
    After all pages extracted, create a consolidated summary section.

    **STEP 1: Test responsive breakpoints**
    ```javascript
    // Use mcp__playwright__browser_resize to test:
    // 640px (mobile), 768px (tablet), 1024px (desktop), 1280px (wide)
    ```

    Take screenshots at each breakpoint on homepage.

    **STEP 2: Append consolidated summary to extraction data**

    ```markdown
    ---

    ## Consolidated Token Summary

    Deduplicated values from all pages for implementation.

    ### Colors (Unique values found)

    #### Backgrounds
    | Token | Value | Source |
    |-------|-------|--------|
    | bg-base | [value] | Homepage body |
    | bg-elevated | [value] | Cards, navbar |
    | bg-surface | [value] | Card interiors |

    #### Text Colors
    | Token | Value | Source |
    |-------|-------|--------|
    | text-primary | [value] | Body text |
    | text-secondary | [value] | Subtitles, descriptions |
    | text-muted | [value] | Placeholders |

    #### Accent Colors
    | Token | Value | Source |
    |-------|-------|--------|
    | accent-primary | [value] | Raycast red (we replace with coral) |

    #### Semantic Colors
    | Token | Value | Source |
    |-------|-------|--------|
    | success | [value] | If found |
    | error | [value] | If found |

    ### Typography (Unique values found)

    #### Font Families
    | Token | Value | Source |
    |-------|-------|--------|
    | font-display | [value] | Hero titles |
    | font-sans | [value] | Body text |
    | font-mono | [value] | Code blocks |

    #### Font Sizes
    | Token | Value | Source |
    |-------|-------|--------|
    | text-hero | [value] | Homepage h1 |
    | text-4xl | [value] | Section headings |
    | text-2xl | [value] | Card titles |
    | text-base | [value] | Body |
    | text-sm | [value] | Captions |

    #### Font Weights
    | Token | Value | Source |
    |-------|-------|--------|
    | font-bold | [value] | Headlines |
    | font-semibold | [value] | Card titles |
    | font-medium | [value] | Buttons |
    | font-regular | [value] | Body |

    ### Spacing (Unique values found)

    | Token | Value | Source |
    |-------|-------|--------|
    | spacing-1 | 4px | |
    | spacing-2 | 8px | |
    | spacing-4 | 16px | |
    | spacing-6 | 24px | Card padding |
    | spacing-8 | 32px | Section gaps |

    ### Shadows (Unique values found)

    | Token | Value | Source |
    |-------|-------|--------|
    | shadow-sm | [value] | |
    | shadow-card | [value] | Extension cards |
    | shadow-elevated | [value] | Navbar, modals |
    | shadow-glass | [value] | Glassmorphism |

    ### Border Radius (Unique values found)

    | Token | Value | Source |
    |-------|-------|--------|
    | radius-sm | [value] | Badges |
    | radius-md | [value] | Buttons, inputs |
    | radius-lg | [value] | Cards |
    | radius-xl | [value] | Large cards |

    ### Breakpoints

    | Token | Value | Behavior |
    |-------|-------|----------|
    | sm | 640px | [observed changes] |
    | md | 768px | [observed changes] |
    | lg | 1024px | [observed changes] |
    | xl | 1280px | [observed changes] |

    ---

    ## Extraction Complete

    - **Total pages extracted:** 8 (Homepage + 7 subpages)
    - **Total screenshots:** [count]
    - **Extraction method:** Playwright MCP (real browser)
    - **Date:** [date]
    ```
  </action>
  <verify>
    - Breakpoint screenshots exist
    - Consolidated Token Summary section added
    - Colors deduplicated with source references
    - Typography deduplicated with source references
    - Spacing values documented
    - Shadows documented
    - Border radius values documented
    - Breakpoint behavior documented
  </verify>
  <done>Extraction consolidated into implementation-ready summary</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete extraction of ALL Raycast pages using Playwright MCP:

    1. Store - extension cards, badges, search, grid
    2. Pro - feature cards, pro badge, testimonials
    3. AI - chat interface, code blocks, AI styling
    4. iOS - app preview, download buttons
    5. Windows - waitlist form, input styles
    6. Teams - comparison tables, enterprise cards
    7. Pricing - pricing cards, toggle, feature lists

    Plus consolidated summary with deduplicated tokens.
  </what-built>
  <how-to-verify>
    1. Verify ALL screenshots exist (03-* through 09-*)
    2. Open 39-EXTRACTION-DATA.md and check:
       - Each page has its own section
       - Consolidated summary has deduplicated values
       - Source references included
    3. Spot-check 3-5 values by visiting raycast.com pages in your browser
    4. Verify the extraction covers unique elements from each page
    5. Check breakpoint observations make sense
  </how-to-verify>
  <resume-signal>
    Type "approved" if extraction is complete and accurate
    OR list specific pages/values that need re-extraction
  </resume-signal>
</task>

</tasks>

<verification>
- [ ] Store page: cards, badges, search, grid ✓
- [ ] Pro page: features, pro badge ✓
- [ ] AI page: chat interface, code blocks ✓
- [ ] iOS page: app preview, download button ✓
- [ ] Windows page: waitlist form, inputs ✓
- [ ] Teams page: comparison table, enterprise cards ✓
- [ ] Pricing page: pricing cards, toggle, features ✓
- [ ] Breakpoints tested and documented ✓
- [ ] Consolidated summary created ✓
- [ ] All screenshots captured as proof ✓
- [ ] User has verified extraction completeness
</verification>

<success_criteria>
ALL pages extracted with Playwright:
- 8 total pages (Homepage from Plan 01 + 7 here)
- Every page has screenshot proof
- Consolidated summary ready for implementation
- User approval received
</success_criteria>

<output>
After completion, create `.planning/phases/39-token-foundation/39-02-SUMMARY.md`
</output>
