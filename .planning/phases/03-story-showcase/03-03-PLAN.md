---
phase: 03-story-showcase
plan: 03
type: execute
wave: 3
depends_on: ["03-00", "03-01", "03-02"]
files_modified:
  - src/components/marketing/story/feature-block.tsx
  - src/components/marketing/story/feature-blocks.tsx
  - src/components/marketing/index.ts
  - src/app/(marketing)/page.tsx
  - src/components/layout/header.tsx
  - src/components/layout/footer.tsx
autonomous: true
requirements: [STORY-03]

must_haves:
  truths:
    - "Visiting / shows a new 'Features' section (between The Simulation and Pricing) with 3-4 feature deep-dive blocks, each pairing a benefit with a labelled <Placeholder>"
    - "The blocks alternate column order (image left/right flips each row) on desktop and stack on mobile"
    - "A 'Features' anchor (#features) appears in BOTH the header nav and the footer product links — 5 nav links total"
    - "/ stays statically prerendered (○) — no 'use client' leaked to any section root"
  artifacts:
    - path: "src/components/marketing/story/feature-blocks.tsx"
      provides: "STORY-03 — the 3-4 alternating feature deep-dive section (RSC)"
      min_lines: 25
      contains: "export function FeatureBlocks"
    - path: "src/components/marketing/story/feature-block.tsx"
      provides: "the single alternating benefit + Placeholder row (RSC leaf)"
      contains: "export function FeatureBlock"
    - path: "src/app/(marketing)/page.tsx"
      provides: "new <section id='features'> inserted between #the-simulation and #pricing"
      contains: "id=\"features\""
    - path: "src/components/layout/header.tsx"
      provides: "'Features' → #features added to NAV_LINKS"
      contains: "#features"
    - path: "src/components/layout/footer.tsx"
      provides: "'Features' → #features added to PRODUCT_LINKS"
      contains: "#features"
    - path: "src/components/marketing/index.ts"
      provides: "barrel export of FeatureBlocks"
      contains: "FeatureBlocks"
  key_links:
    - from: "src/components/marketing/story/feature-blocks.tsx"
      to: "./feature-block (FeatureBlock leaf) + @/components/marketing/placeholder"
      via: "maps benefits through <FeatureBlock flip={i % 2 === 1} />"
      pattern: "FeatureBlock"
    - from: "src/app/(marketing)/page.tsx"
      to: "FeatureBlocks"
      via: "named import rendered inside the new <section id='features'>"
      pattern: "<FeatureBlocks"
    - from: "src/components/layout/header.tsx + footer.tsx"
      to: "#features section in page.tsx"
      via: "anchor link in NAV_LINKS / PRODUCT_LINKS"
      pattern: "#features"
---

<objective>
Build STORY-03 — the 3-4 feature deep-dive blocks — AND the skeleton edit they need (D-B): INSERT a new `<section id="features">` between `#the-simulation` and `#pricing`, and add a "Features" anchor to BOTH the header nav and the footer product links (5 nav links total). Each block is a two-column row pairing a benefit headline + one-line body with a labelled `<Placeholder>` visual, the image/text sides flipping each row, stacking on mobile. Finally, a BLOCKING build gate asserts `/` stays statically prerendered (no `"use client"` leaked to any section root).

Purpose: STORY-03 — close the story body with concrete benefits, each shown via a swappable product frame (OpusClip/Linear feature-deep-dive pattern). This plan also owns the phase-level static-prerender guarantee (Success Criterion / Pitfall 5).

Output: `feature-block.tsx` + `feature-blocks.tsx` (RSC), barrel export, the new `#features` section in `page.tsx`, the "Features" nav anchor in header + footer, and a verified `○ /` build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-story-showcase/03-PATTERNS.md
@.planning/phases/03-story-showcase/03-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — use directly, no exploration. -->

The alternating flip leaf (RESEARCH § Code Examples — no in-repo alternating-row analog; this is the idiom):
```
// feature-block.tsx — RSC leaf
import { cn } from "@/lib/utils";
import { Placeholder } from "@/components/marketing/placeholder";
interface FeatureBlockProps { title: string; body: string; flip?: boolean; }
export function FeatureBlock({ title, body, flip }: FeatureBlockProps) {
  return (
    <div className="grid grid-cols-1 items-center gap-10 md:grid-cols-2">
      <div className={cn(flip && "md:order-2")}>
        <h3 className="text-2xl font-semibold text-foreground md:text-3xl">{title}</h3>
        <p className="mt-4 text-base text-foreground-secondary md:text-lg">{body}</p>
      </div>
      <Placeholder variant="image" aspect="16/10" label={title} className={cn(flip && "md:order-1")} />
    </div>
  );
}
```

NAV_LINKS array in header.tsx (lines 17-22) — ADD the Features entry (mapped twice: desktop + mobile panel, so ONE array edit covers both):
```
const NAV_LINKS = [
  { label: "How it works", href: "#how-it-works" },
  { label: "The Simulation", href: "#the-simulation" },
  { label: "Features", href: "#features" },   // ← ADD between The Simulation and Pricing
  { label: "Pricing", href: "#pricing" },
  { label: "FAQ", href: "#faq" },
] as const;
```

PRODUCT_LINKS array in footer.tsx (lines 15-20) — by contract "mirror the header nav set exactly". Add the SAME `{ label: "Features", href: "#features" }` in the same position.

New section to INSERT in page.tsx between #the-simulation and #pricing (MATCH the LOCKED rhythm exactly):
```
<section id="features" className="border-t border-border px-6 py-20">
  <div className="mx-auto max-w-5xl">
    <FeatureBlocks />
  </div>
</section>
```

Build/static gate: `npm run build` → the route table must show `○ /` (Static), NOT `ƒ /` (Dynamic). `○` confirms no `"use client"` leaked to a section root (Pitfall 5).

Suggested 3-4 benefits (RESEARCH Pattern 5 — refine copy, avoid barred words "viral"/"AI"): (1) Know before you post (the prediction), (2) See exactly where viewers drop (retention), (3) Understand your audience (the simulation / persona cloud), (4) Fix the weakest lever (Hook · Retention · Shareability).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build FeatureBlock leaf + FeatureBlocks section + barrel export</name>
  <behavior>
    - Test 1 (STORY-03): renders 3-4 blocks, each pairing a benefit headline (h3) with exactly one `<Placeholder>` (Placeholder count === block count, 3 ≤ n ≤ 4).
    - Test 2 (alternating flip): the `md:order-*` flip utility is applied to alternating rows.
    - Test 3 (Success Crit 4): each Placeholder is labelled + aspect-locked.
    (These tests already exist RED from 03-00 — this task turns them GREEN.)
  </behavior>
  <read_first>
    - src/components/marketing/hero/hero.tsx (section discipline — pure RSC, token usage, NO "use client")
    - src/components/marketing/placeholder.tsx (the slot API + className override)
    - src/lib/utils.ts (cn — the order-swap idiom)
    - src/components/marketing/index.ts (barrel convention)
    - src/components/marketing/story/__tests__/feature-blocks.test.tsx (the RED gate this task satisfies)
    - .planning/phases/03-story-showcase/03-RESEARCH.md § Pattern 5 + § Code Examples "Alternating feature block (the flip)" (suggested benefits, the flip layout, mobile stack, barred words)
    - .planning/phases/03-story-showcase/03-PATTERNS.md § feature-blocks.tsx + feature-block.tsx (the flip pattern + read_first)
  </read_first>
  <files>src/components/marketing/story/feature-block.tsx, src/components/marketing/story/feature-blocks.tsx, src/components/marketing/index.ts</files>
  <action>
    Create `src/components/marketing/story/feature-block.tsx` — a PURE RSC leaf (NO `"use client"`). Export `function FeatureBlock({ title, body, flip }: { title: string; body: string; flip?: boolean })` following the flip idiom in the interfaces block: a `grid grid-cols-1 items-center gap-10 md:grid-cols-2` row; the copy column gets `md:order-2` when `flip`, the `<Placeholder variant="image" aspect="16/10" label={title}>` gets `md:order-1` when `flip` (use `cn(flip && "...")`). Title is `<h3 className="text-2xl font-semibold text-foreground md:text-3xl">`; body is `<p className="mt-4 text-base text-foreground-secondary md:text-lg">`. Stacks to single column below `md` (responsive by construction, Pitfall 6).

    Create `src/components/marketing/story/feature-blocks.tsx` — a PURE RSC section (NO `"use client"`). Export `function FeatureBlocks({ className }: { className?: string })`. Imports: `FeatureBlock` from `./feature-block`; optionally `StaggerReveal`/`FadeInUp` from `@/components/motion` for a calm entrance; `cn` if needed. Define a module-level `FEATURES` const of 3 or 4 entries (recommend the four from the interfaces block: predict / where-they-drop / audience / weakest-lever) — each `{ title, body }`, body one tight Inter line naming the real benefit. AVOID the barred words "viral" and "AI" in headlines (D-09 carried). Render a section `<h2>` (SANS `font-semibold text-foreground` — D-C) e.g. "Everything you need to know before you post" or "Features", then map `FEATURES` through `<FeatureBlock key={i} {...f} flip={i % 2 === 1} />` inside a vertical-gap container (e.g. `flex flex-col gap-16 md:gap-24` or wrapped in `<StaggerReveal>`). Use the canonical noun "Simulation" where a product noun is needed; never "reading".

    Then add to `src/components/marketing/index.ts`: `export { FeatureBlocks } from "./story/feature-blocks";`. (FeatureBlock leaf need not be barrel-exported — it is consumed internally by feature-blocks.)

    Do NOT import from `board/*`, `reading/*`, `viral-results/*`, or any engine/data hook (anti-pattern guard).
  </action>
  <verify>
    <automated>npx vitest run src/components/marketing/story/__tests__/feature-blocks.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/marketing/story/__tests__/feature-blocks.test.tsx` exits 0 (all STORY-03 cases GREEN).
    - FeatureBlocks renders 3-4 blocks, each with exactly one labelled `<Placeholder>` (the test asserts 3 ≤ `[data-variant]` count ≤ 4 and matching h3 count).
    - The flip is applied to alternating rows: `grep -q 'md:order-' src/components/marketing/story/feature-block.tsx` matches and the test confirms alternating application.
    - `grep -L '"use client"' src/components/marketing/story/feature-blocks.tsx` AND `grep -L '"use client"' src/components/marketing/story/feature-block.tsx` both return their file (NO client directive).
    - `grep -q "FeatureBlocks" src/components/marketing/index.ts` (barrel export added).
    - No banned imports: `grep -Eq 'board/|reading/|viral-results' src/components/marketing/story/feature-block*.tsx` returns nothing.
    - No banned words in headlines: `grep -Eiq '\bviral\b|\bAI\b' src/components/marketing/story/feature-blocks.tsx` returns nothing (copy hygiene).
  </acceptance_criteria>
  <done>FeatureBlock + FeatureBlocks render 3-4 alternating benefit+Placeholder rows, sans heading, no banned words/imports; the 03-00 feature-blocks test is GREEN; barrel exports FeatureBlocks; no "use client".</done>
</task>

<task type="auto">
  <name>Task 2: Insert the #features section in page.tsx + add the "Features" nav anchor to header and footer</name>
  <read_first>
    - src/app/(marketing)/page.tsx (current skeleton — the `#the-simulation` section ends, `#pricing` begins; the `@/components/marketing` named import line 3, now carrying Hero/MotionConfigShell/HowItWorks/SimulationShowcase)
    - src/components/layout/header.tsx (NAV_LINKS array lines 17-22 — mapped twice at lines 80-88 desktop + 130-139 mobile; one array edit covers both)
    - src/components/layout/footer.tsx (PRODUCT_LINKS array lines 15-20 — "mirror the header nav set exactly")
    - src/components/layout/__tests__/footer.test.tsx (the 03-00 extension already gates `#features`)
    - .planning/phases/03-story-showcase/03-PATTERNS.md § page.tsx "Pattern 2 — INSERT new #features section" + § header.tsx + § footer.tsx (the exact array edits)
    - .planning/phases/03-story-showcase/03-RESEARCH.md § Pitfall 2 (skeleton edit, not a fill) + § Pitfall 3 (LOCKED rhythm)
  </read_first>
  <files>src/app/(marketing)/page.tsx, src/components/layout/header.tsx, src/components/layout/footer.tsx</files>
  <action>
    In `page.tsx`: add `FeatureBlocks` to the existing `@/components/marketing` named import. INSERT a NEW section between the `#the-simulation` section (it now renders `<SimulationShowcase/>`, from 03-02) and the `#pricing` section, matching the LOCKED rhythm EXACTLY: `<section id="features" className="border-t border-border px-6 py-20">` → `<div className="mx-auto max-w-5xl">` → `<FeatureBlocks />`. Do NOT alter any existing section's id/border/padding/measure (D-E / Pitfall 3); this is an ADD, not a fill.

    In `header.tsx`: add `{ label: "Features", href: "#features" }` to the `NAV_LINKS` array, positioned between `{ label: "The Simulation", ... }` and `{ label: "Pricing", ... }` (the LOCKED nav order). This single array edit surfaces "Features" in BOTH the desktop bar and the mobile disclosure panel (both map NAV_LINKS). Change nothing else; the mobile `useState` disclosure handles 5 items fine (verify it still reads cleanly — it's a simple list).

    In `footer.tsx`: add the SAME `{ label: "Features", href: "#features" }` to `PRODUCT_LINKS` in the same position (the footer mirrors the header nav set exactly).
  </action>
  <verify>
    <automated>grep -q 'id="features" className="border-t border-border px-6 py-20"' "src/app/(marketing)/page.tsx" && grep -q '<FeatureBlocks' "src/app/(marketing)/page.tsx" && grep -q '"#features"' src/components/layout/header.tsx && grep -q '"#features"' src/components/layout/footer.tsx && npx vitest run src/components/layout/__tests__/footer.test.tsx src/components/layout/__tests__/header.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` has a new `<section id="features" className="border-t border-border px-6 py-20">` rendering `<FeatureBlocks />`, positioned BETWEEN `#the-simulation` and `#pricing` (verify order: `#the-simulation` line precedes `#features` line precedes `#pricing` line).
    - All EXISTING section wrappers (`#hero`, `#how-it-works`, `#the-simulation`, `#pricing`, `#faq`) are unchanged.
    - `grep -c '"#features"' src/components/layout/header.tsx` returns 1 and it sits between `#the-simulation` and `#pricing` in NAV_LINKS → 5 links total.
    - `grep -c '"#features"' src/components/layout/footer.tsx` returns 1 in PRODUCT_LINKS, same position.
    - `npx vitest run src/components/layout/__tests__/footer.test.tsx` exits 0 (the 03-00 `#features` anchor row is now GREEN).
    - The existing header/footer tests still pass (no regression to CTA/auth/mobile behavior).
  </acceptance_criteria>
  <done>A new `#features` section is mounted between The Simulation and Pricing; "Features" → #features appears in header NAV_LINKS and footer PRODUCT_LINKS (5 links); the footer anchor gate is GREEN; existing sections untouched.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Phase static-prerender + full-suite gate</name>
  <read_first>
    - src/app/(marketing)/page.tsx (the assembled page — confirm no section root carries "use client")
    - src/components/marketing/story/how-it-works.tsx, simulation-showcase.tsx, feature-blocks.tsx, feature-block.tsx (all must be pure RSC — Pitfall 5)
    - .planning/phases/03-story-showcase/03-VALIDATION.md § "Cross: / stays statically prerendered" (the build assertion) + § "after every plan wave" (full suite + build)
    - .planning/phases/02-hero-signature-moment/02-02-SUMMARY.md (the precedent: `/` verified `○` static after the hero pivot — match it)
  </read_first>
  <files>(verification only — no source changes; if a section root leaked "use client", fix it here by lifting motion to a leaf)</files>
  <action>
    Run the phase-gate checks and BLOCK on any failure:
    1. Confirm NO story section root has a `"use client"` directive (the motion must live only in the `StaggerReveal`/`FadeInUp` leaves, which already carry their own directive). If any of `how-it-works.tsx` / `simulation-showcase.tsx` / `feature-blocks.tsx` / `feature-block.tsx` starts with `"use client"`, REMOVE it and lift the offending motion to a leaf wrapper (Pitfall 5).
    2. Run the full test suite — all green (Phase-1/2 ~2041 baseline + the 3 new story suites GREEN + the extended footer anchor row GREEN).
    3. Run `npm run build` — exit 0 AND the route table shows `○ /` (Static). `ƒ /` (Dynamic) is a FAILURE — it means a `"use client"` leaked to a section root; fix per step 1 and rebuild.
    This is the phase-level guarantee that Success Criterion 4 (no layout shift, swappable placeholders) and Pitfall 5 (static prerender) hold across the assembled page.
  </action>
  <verify>
    <automated>grep -rL '"use client"' src/components/marketing/story/how-it-works.tsx src/components/marketing/story/simulation-showcase.tsx src/components/marketing/story/feature-blocks.tsx src/components/marketing/story/feature-block.tsx | wc -l | grep -q '^ *4$' && npm test 2>&1 | tail -5 && npm run build 2>&1 | grep -E '○|ƒ' | grep -E '/\s' | grep -q '○' && echo "STATIC-OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 story section/leaf files have NO `"use client"` directive (the grep returns all 4 as not-matching).
    - `npm test` exits 0 — full suite green (no regressions; the 3 story suites + footer `#features` row GREEN).
    - `npm run build` exits 0 AND the route table shows `○ /` (Static), NOT `ƒ /`.
    - If the build showed `ƒ /`, the offending `"use client"` was removed and the build re-run to `○ /` before this task is marked done.
  </acceptance_criteria>
  <done>No story section root carries "use client"; full suite green; `npm run build` produces `○ /` (static). The phase static-prerender + no-CLS guarantee holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→in-page anchor | Anchor links (`#features`) are in-page scroll targets — no external navigation, no open-redirect surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | feature copy | accept | Static literals; never `dangerouslySetInnerHTML`; no user input rendered. |
| T-03-03-02 | Tampering | nav anchors (#features) | accept | In-page `#anchor` only; no external target; the one external CTA (SIGNUP_URL) is a centralized constant (src/lib/routes.ts) and is NOT touched here. No open-redirect surface. |
| T-03-03-03 | Tampering | imports | mitigate | No import from board/reading/viral-results or engine/data hooks. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed (RESEARCH § Package Legitimacy Audit). No install task. |
</threat_model>

<verification>
- `npx vitest run src/components/marketing/story/__tests__/feature-blocks.test.tsx` → exits 0 (STORY-03 GREEN).
- `npx vitest run src/components/layout/__tests__/footer.test.tsx` → exits 0 (the `#features` anchor row GREEN).
- `npm test` → full suite green (all 3 story suites + extended footer + Phase-1/2 baseline).
- `npm run build` → exit 0, route table `○ /` (Static). This is the BLOCKING phase gate.
</verification>

<success_criteria>
- FeatureBlocks renders 3-4 alternating benefit + `<Placeholder>` deep-dive blocks, stacking on mobile.
- A new `<section id="features">` is inserted between The Simulation and Pricing, matching the LOCKED rhythm.
- "Features" → #features appears in BOTH header NAV_LINKS and footer PRODUCT_LINKS (5 links total).
- All story sections are pure RSC (no "use client"); the assembled `/` builds `○` static.
- Full suite green; `npm run build` exit 0 with `○ /`.
- The 03-00 feature-blocks gate + the footer `#features` gate are GREEN.
- No banned imports, no banned headline words, no glass/glow, coral precious.
</success_criteria>

<output>
Create `.planning/phases/03-story-showcase/03-03-SUMMARY.md` when done.
</output>
