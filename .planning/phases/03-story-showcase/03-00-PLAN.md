---
phase: 03-story-showcase
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/marketing/story/__tests__/how-it-works.test.tsx
  - src/components/marketing/story/__tests__/simulation-showcase.test.tsx
  - src/components/marketing/story/__tests__/feature-blocks.test.tsx
  - src/components/layout/__tests__/footer.test.tsx
autonomous: true
requirements: [STORY-01, STORY-02, STORY-03]

must_haves:
  truths:
    - "Three story-section test files exist and FAIL with module-not-found (their section components do not exist yet)"
    - "The footer anchor test gates a '#features' link (extends the existing it.each anchor array)"
    - "Each story test asserts placeholder presence via data-variant + inline aspect-ratio (Success Criterion 4)"
  artifacts:
    - path: "src/components/marketing/story/__tests__/how-it-works.test.tsx"
      provides: "STORY-01 gate — 3 steps in order, each a Placeholder; noun discipline (Simulation/simulates, never 'reading')"
      contains: "@vitest-environment happy-dom"
    - path: "src/components/marketing/story/__tests__/simulation-showcase.test.tsx"
      provides: "STORY-02 gate — heading 'The Simulation'; names all 5 outputs; product visual is a Placeholder"
      contains: "@vitest-environment happy-dom"
    - path: "src/components/marketing/story/__tests__/feature-blocks.test.tsx"
      provides: "STORY-03 gate — 3-4 blocks, each benefit + Placeholder; alternating flip"
      contains: "@vitest-environment happy-dom"
    - path: "src/components/layout/__tests__/footer.test.tsx"
      provides: "extended anchor gate including '#features'"
      contains: "#features"
  key_links:
    - from: "story/__tests__/how-it-works.test.tsx"
      to: "../how-it-works (component, not yet built)"
      via: "import { HowItWorks } from '../how-it-works'"
      pattern: "from \"\\.\\./how-it-works\""
    - from: "story/__tests__/simulation-showcase.test.tsx"
      to: "../simulation-showcase (component, not yet built)"
      via: "import { SimulationShowcase } from '../simulation-showcase'"
      pattern: "from \"\\.\\./simulation-showcase\""
    - from: "story/__tests__/feature-blocks.test.tsx"
      to: "../feature-blocks (component, not yet built)"
      via: "import { FeatureBlocks } from '../feature-blocks'"
      pattern: "from \"\\.\\./feature-blocks\""
---

<objective>
Wave 0 Nyquist scaffold. Create the three story-section test files (RED by design — the components they import do not exist yet) plus extend the existing footer anchor test to gate the new `#features` link. These tests encode the STORY-01/02/03 acceptance contract so the section plans (03-01/02/03) turn them GREEN.

Purpose: Per `workflow.nyquist_validation: true`, every requirement must have an automated check that exists BEFORE the implementation. RED-by-design (module-not-found) is the success signal here — the tests fail because the section components are not built, exactly as intended.

Output: 3 new test files under `src/components/marketing/story/__tests__/` + 1 extended footer test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-story-showcase/03-VALIDATION.md
@.planning/phases/03-story-showcase/03-PATTERNS.md

<interfaces>
<!-- Test idioms the executor must mirror. Extracted from the in-repo analogs. -->
<!-- Use these directly — no codebase exploration needed. -->

Mandatory pragma (line 1 of EVERY test file — from placeholder.test.tsx / hero.test.tsx line 1):
```
/** @vitest-environment happy-dom */
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
```

Noun-discipline / resilience idiom (hero.test.tsx lines 40-49) — assert STABLE TOKENS, not full sentences:
```
const noun = screen.getByText(/simulat(?:es|ion|e)/i);
```

Placeholder-presence idiom (placeholder.test.tsx lines 54-63, 74-79):
```
const root = container.querySelector('[data-variant="image"]');     // every Placeholder root carries data-variant
container.querySelectorAll('[data-variant]')                          // count all product visuals (Success Crit 4)
(root as HTMLElement).style.aspectRatio.replace(/\s+/g, "")           // CSSOM normalizes "16/10" → "16 / 10"; compare whitespace-insensitive
```

Footer it.each anchor idiom (footer.test.tsx lines 38-49) — the array to EXTEND:
```
it.each([["#how-it-works"],["#the-simulation"],["#pricing"],["#faq"]])("links to the nav anchor %s", (anchor) => {
  const links = screen.getAllByRole("link").filter((el) => el.getAttribute("href") === anchor);
  expect(links.length).toBeGreaterThanOrEqual(1);
});
```

Placeholder root API: `data-variant` attribute present; `style.aspectRatio` set inline; label rendered as text caption (image/video/avatar variants only — NOT logo).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the three RED story-section test files</name>
  <read_first>
    - src/components/marketing/hero/__tests__/hero.test.tsx (render + token-assert + resilience-rule idiom, happy-dom pragma)
    - src/components/marketing/__tests__/placeholder.test.tsx (it.each + data-variant counting + style.aspectRatio read)
    - src/components/marketing/placeholder.tsx (the Placeholder root carries data-variant line 125 + inline aspectRatio line 120 — the test hooks)
    - .planning/phases/03-story-showcase/03-VALIDATION.md (the requirement → test map these files must satisfy)
    - .planning/phases/03-story-showcase/03-PATTERNS.md § "Test files (Wave 0)" (per-test mapping + resilience-rule + Placeholder-presence idioms)
  </read_first>
  <files>src/components/marketing/story/__tests__/how-it-works.test.tsx, src/components/marketing/story/__tests__/simulation-showcase.test.tsx, src/components/marketing/story/__tests__/feature-blocks.test.tsx</files>
  <action>
    Create the `src/components/marketing/story/__tests__/` directory and the three test files. Every file starts with the `/** @vitest-environment happy-dom */` pragma (line 1) and imports `describe, it, expect` from vitest + `render, screen` from @testing-library/react (mirror hero.test.tsx lines 1-3).

    `how-it-works.test.tsx` — `import { HowItWorks } from "../how-it-works"`. Assert (STORY-01): renders exactly 3 step blocks IN ORDER (e.g. via 3 level-3 headings or 3 numbered markers — assert the ordered step titles/numerals appear and there are 3); each step renders a `<Placeholder>` (query `container.querySelectorAll('[data-variant]')` length === 3); noun discipline — the rendered text contains the product noun via `/simulat(?:es|ion|e)/i` AND a NEGATIVE assertion that "reading" never appears as a user-facing product noun (`screen.queryByText(/\breading\b/i)` is null). Match step copy by stable tokens, not verbatim sentences (resilience rule).

    `simulation-showcase.test.tsx` — `import { SimulationShowcase } from "../simulation-showcase"`. Assert (STORY-02): the section heading reads "The Simulation" VERBATIM (LOCKED — matches the `#the-simulation` anchor; use `getByRole("heading", { level: 2 })` and `.textContent` toBe "The Simulation", OR `getByText("The Simulation")`); the section names ALL FIVE required outputs by stable token — audience simulation (`/audience/i` + `/simulat/i`), watch-through % (`/watch[- ]?through|watch %|% watch/i`), Hook (`/hook/i`), Retention (`/retention/i`) AND where-they-drop (`/drop/i`), Shareability (`/shareab/i`); the product visual is a `<Placeholder>` (`container.querySelector('[data-variant]')` not null) with an inline aspect-ratio set (`style.aspectRatio` non-empty → no-CLS, Success Crit 4).

    `feature-blocks.test.tsx` — `import { FeatureBlocks } from "../feature-blocks"`. Assert (STORY-03): renders 3 or 4 blocks (`container.querySelectorAll('[data-variant]')` length toBeGreaterThanOrEqual 3 AND toBeLessThanOrEqual 4 — every block pairs a benefit with exactly one Placeholder, so the Placeholder count === the block count); each block pairs a benefit headline (a level-3 heading) with its Placeholder (assert `getAllByRole("heading", { level: 3 })` length matches the Placeholder count); alternating flip — assert that the `md:order-2`/`md:order-1` flip utility is applied to alternating rows (query for elements whose className matches `/md:order-/` and assert at least one block carries it — the even/odd flip). Avoid the barred words "viral"/"AI" assertion is NOT needed (that is a copy-review concern, not a gate).

    Do NOT create the section components — they are built in 03-01/02/03. These tests MUST fail at import (module-not-found) right now; that is the RED signal.
  </action>
  <verify>
    <automated>npx vitest run src/components/marketing/story/ 2>&1 | grep -Eiq "Failed to resolve import|Cannot find module|how-it-works|simulation-showcase|feature-blocks" && echo "RED-as-expected" || (echo "UNEXPECTED — tests should fail at import" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - The three files exist under `src/components/marketing/story/__tests__/`.
    - Each file's line 1 is exactly `/** @vitest-environment happy-dom */`.
    - `grep -l 'from "../how-it-works"' src/components/marketing/story/__tests__/how-it-works.test.tsx` matches (and the analogous import in the other two).
    - `npx vitest run src/components/marketing/story/` exits NON-zero with module-not-found errors for the three section components (RED by design — the components do not exist yet).
    - Each test asserts placeholder presence via `[data-variant]` and at least one asserts `style.aspectRatio` is set.
  </acceptance_criteria>
  <done>Three RED story test files exist; scoped vitest run fails with module-not-found for HowItWorks/SimulationShowcase/FeatureBlocks (the intended RED state).</done>
</task>

<task type="auto">
  <name>Task 2: Extend the footer anchor test to gate '#features'</name>
  <read_first>
    - src/components/layout/__tests__/footer.test.tsx (the existing it.each anchor array lines 38-49 — the exact block to extend)
    - src/components/layout/footer.tsx (PRODUCT_LINKS array lines 15-20 — what 03-03 will add "#features" to)
    - .planning/phases/03-story-showcase/03-PATTERNS.md § "header.test.tsx + footer.test.tsx (MODIFY)" (confirms both tests exist; extend, do not create a page test)
  </read_first>
  <files>src/components/layout/__tests__/footer.test.tsx</files>
  <action>
    In `footer.test.tsx`, ADD `["#features"]` to the existing `it.each([...])` anchor array (currently `[["#how-it-works"],["#the-simulation"],["#pricing"],["#faq"]]`) so the parametrized "links to the nav anchor %s" test now also gates the Features mirror. Place `["#features"]` between `["#the-simulation"]` and `["#pricing"]` to match the locked nav order. Change NOTHING else in the file. This row will be RED until 03-03 adds "Features" to footer.tsx PRODUCT_LINKS.
  </action>
  <verify>
    <automated>grep -q '"#features"' src/components/layout/__tests__/footer.test.tsx && echo "anchor-row-added" || { echo "MISSING #features row"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"#features"' src/components/layout/__tests__/footer.test.tsx` returns 1.
    - The `#features` entry sits between `#the-simulation` and `#pricing` in the it.each array.
    - Running `npx vitest run src/components/layout/__tests__/footer.test.tsx` shows the `#features` case present (RED until 03-03 wires footer.tsx — acceptable at Wave 0).
    - No other line in footer.test.tsx changed.
  </acceptance_criteria>
  <done>footer.test.tsx gates a `#features` anchor; the new row is RED until 03-03 adds the footer link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only plan. No runtime surface, no user input, no network, no rendered untrusted content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-00-01 | Tampering | test files | accept | Tests are static developer assets; no user input, no `dangerouslySetInnerHTML`, no external links. Security surface nil (RESEARCH § Security Domain). |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 3 installs ZERO packages (RESEARCH § Package Legitimacy Audit). No install task in this plan; if one were ever proposed, run the Package Legitimacy Gate first. |
</threat_model>

<verification>
- `npx vitest run src/components/marketing/story/` → fails with module-not-found for the three section components (RED by design).
- `grep -c '"#features"' src/components/layout/__tests__/footer.test.tsx` → 1.
- The full suite (`npm test`) will show these new files RED; that is expected at Wave 0 and turns GREEN across 03-01/02/03. Do NOT run a green-suite gate here.
</verification>

<success_criteria>
- 3 story test files created with the happy-dom pragma, each importing its (not-yet-built) section component.
- Each story test asserts: ordered content, Placeholder presence (`[data-variant]`), and no-CLS aspect-ratio (Success Crit 4).
- how-it-works test gates noun discipline (Simulation/simulates present; "reading" absent as product noun).
- simulation-showcase test gates the verbatim "The Simulation" heading + all 5 named outputs.
- feature-blocks test gates 3-4 blocks + alternating flip.
- footer.test.tsx extended with the `#features` anchor row.
- Scoped story vitest run is RED with module-not-found (the intended Wave-0 state).
</success_criteria>

<output>
Create `.planning/phases/03-story-showcase/03-00-SUMMARY.md` when done.
</output>
