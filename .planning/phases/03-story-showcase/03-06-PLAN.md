---
phase: 03-story-showcase
plan: 06
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/app/(marketing)/page.tsx
autonomous: true
requirements: [STORY-01, STORY-02, STORY-03]
must_haves:
  truths:
    - "Section vertical padding is tightened so the story body reads higher-density on desktop, not low-density with excess whitespace (GAP-3 page-level)"
    - "Every scroll-target section anchor carries scroll-margin-top equal to the sticky header height (~65px) so headings clear the sticky header by a comfortable, reliable offset (GAP-5)"
    - "/ stays statically prerendered (page.tsx remains a pure RSC, no \"use client\")"
  artifacts:
    - path: "src/app/(marketing)/page.tsx"
      provides: "Tighter section rhythm + scroll-margin-top anchors + refreshed docblock"
      contains: "scroll-mt-"
  key_links:
    - from: "src/app/(marketing)/page.tsx"
      to: "the sticky header (h-16 / 64px bar)"
      via: "scroll-margin-top on section anchors matches sticky header height"
      pattern: "scroll-mt-"
---

<objective>
Tighten the page-level vertical rhythm so the assembled story body reads
higher-density on desktop (GAP-3 page-level), and add a reliable
`scroll-margin-top` to every in-page section anchor so headings clear the sticky
header by a comfortable offset instead of by accidental padding luck (GAP-5).
Also refresh the stale Phase-1 docblock (IN-02).

This plan owns ONLY `src/app/(marketing)/page.tsx`. It does NOT touch any story
component (03-05 owns component-level density) or the header/footer (03-07). It
is file-disjoint from every other Wave-1 plan.

Purpose: Close the page-level half of GAP-3 (whitespace/density) and GAP-5
(anchor offset) with surgical section-shell edits — no content changes, stub lock
untouched, `/` stays static.
Output: A page.tsx with denser section padding, scroll-margin-top anchors, and an
accurate docblock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-story-showcase/03-VERIFICATION.md

<interfaces>
<!-- Current page.tsx section shells (all the anchor targets that need scroll-margin-top): -->
<!--   #hero            px-6 py-16 md:py-20 -->
<!--   #how-it-works    border-t border-border px-6 py-20 -->
<!--   #the-simulation  border-t border-border px-6 py-20 -->
<!--   #features        border-t border-border px-6 py-20 -->
<!--   #pricing         border-t border-border px-6 py-20 -->
<!--   #faq             border-t border-border px-6 py-20 -->
<!-- Sticky header height: <nav class="... h-16 ..."> => 64px bar (header.tsx). scroll-margin-top ~= 65-72px clears it. -->
<!-- page.tsx is a PURE RSC (no "use client") wrapping <MotionConfigShell><Header/><main/><Footer/>. Keep it pure. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten section padding + add scroll-margin-top anchors + refresh the docblock</name>
  <read_first>
    - src/app/(marketing)/page.tsx (the section shells + the stale Phase-1 docblock at lines 17-30)
    - src/components/layout/header.tsx (confirm the sticky bar height — the nav is `h-16` = 64px — to size scroll-margin-top)
  </read_first>
  <action>
    1. GAP-5 — add a `scroll-mt-` utility to EVERY in-page scroll-target section
       so its heading clears the 64px sticky header by a comfortable offset. Add
       `scroll-mt-20` (80px, ≥ the 64px bar + breathing room) to each of:
       `#hero`, `#how-it-works`, `#the-simulation`, `#features`, `#pricing`,
       `#faq`. (Tailwind `scroll-mt-20` → `scroll-margin-top: 5rem`; this is the
       reliable, intentional offset replacing the accidental ~16px clearance.)
    2. GAP-3 (page-level) — reduce the section vertical padding so the body reads
       higher-density on desktop. Change the story/teaser sections from `py-20`
       to a tighter responsive rhythm, e.g. `py-16 md:py-20` (mobile denser, a
       calm desktop step) consistently across `#how-it-works`,
       `#the-simulation`, `#features`, `#pricing`, `#faq`; leave the hero rhythm
       sensible (`py-12 md:py-16` or keep its current `py-16 md:py-20`). Keep the
       hairline `border-t border-border` section separators and the
       `mx-auto max-w-5xl` inner measure — only the vertical padding changes.
    3. IN-02 — rewrite the component docblock to reflect reality: the hero,
       how-it-works, the-simulation, and features sections now render real
       content (the STORY sections are populated); only `#pricing` and `#faq`
       remain placeholder stubs (Phase 4). Remove the stale "empty-but-anchored …
       muted placeholder heading / Real content lands in Phases 2–4" wording.
    4. Keep page.tsx a PURE RSC — do NOT add "use client". Do not change any
       section `id`, the component imports, or the content (stub lock: #pricing /
       #faq stay as-is).
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && grep -c 'scroll-mt-' src/app/\(marketing\)/page.tsx | grep -qE '^[6-9]$|^1[0-9]$' && ! grep -q '"use client"' src/app/\(marketing\)/page.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - every section in page.tsx (#hero, #how-it-works, #the-simulation, #features, #pricing, #faq) carries a `scroll-mt-` utility (>= 6 occurrences) sized to clear the 64px sticky header (scroll-mt-20)
    - the story/teaser sections no longer use a bare `py-20`; padding is tightened to a denser responsive rhythm (e.g. `py-16 md:py-20`)
    - the page.tsx docblock no longer says sections are "empty-but-anchored" / "Real content … land in Phases 2–4"; it states STORY sections are populated and only #pricing / #faq remain stubs (IN-02)
    - `grep -n '"use client"' src/app/(marketing)/page.tsx` returns NOTHING
    - no section `id` changed; #pricing / #faq content unchanged (stub lock)
  </acceptance_criteria>
  <done>Sections clear the sticky header via scroll-margin-top (GAP-5), the desktop body reads denser (GAP-3 page-level), the docblock is accurate (IN-02), and / stays a pure RSC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | No new boundary — CSS utility edits + a docblock in a pure-RSC page |

## STRIDE Threat Register

No new attack surface: Tailwind utility + docblock edits in the marketing page
shell. No user input, no data flow, no network, no secrets. ASVS L1 — nothing to
mitigate.
</threat_model>

<verification>
- `grep -c 'scroll-mt-' src/app/(marketing)/page.tsx` >= 6 (every anchor offset)
- `npm run build` exits 0 and the route table shows `○ /` (Static) — page.tsx stays pure RSC
- No section id changed; #pricing / #faq still stubs
</verification>

<success_criteria>
- scroll-margin-top on all section anchors (GAP-5)
- Denser section padding on desktop (GAP-3 page-level)
- Accurate docblock (IN-02); / stays static
</success_criteria>

<output>
Create `.planning/phases/03-story-showcase/03-06-SUMMARY.md` when done
</output>
