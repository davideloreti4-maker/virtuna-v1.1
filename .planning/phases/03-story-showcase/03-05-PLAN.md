---
phase: 03-story-showcase
plan: 05
type: execute
wave: 2
gap_closure: true
depends_on: [03-04]
files_modified:
  - src/components/marketing/story/how-it-works.tsx
  - src/components/marketing/story/simulation-showcase.tsx
  - src/components/marketing/story/feature-blocks.tsx
  - src/components/marketing/story/feature-block.tsx
  - src/components/marketing/story/__tests__/how-it-works.test.tsx
  - src/components/marketing/story/__tests__/simulation-showcase.test.tsx
  - src/components/marketing/story/__tests__/feature-blocks.test.tsx
autonomous: true
requirements: [STORY-01, STORY-02, STORY-03]
must_haves:
  truths:
    - "The Simulation device frame is no longer a tall empty void — it is height-capped and filled with the score-gauge → audience-cloud → driver-rows skeleton so it reads as the SHAPE of a Numen Simulation (GAP-2)"
    - "Each how-it-works step shows a section-appropriate product skeleton instead of a flat dark box (GAP-1)"
    - "Feature-block visuals use a wider-shorter ratio, top-align with their copy, and read paired-not-stranded (GAP-3 component-level)"
    - "Story sections stay pure RSC; / stays statically prerendered (no \"use client\" leak)"
  artifacts:
    - path: "src/components/marketing/story/simulation-showcase.tsx"
      provides: "Device frame filled with the gauge/cloud/driver-rows skeleton, height-capped"
      contains: "ScoreGaugeSkeleton"
    - path: "src/components/marketing/story/how-it-works.tsx"
      provides: "Three step skeletons hinting paste / audience / prediction"
    - path: "src/components/marketing/story/feature-block.tsx"
      provides: "Wider-shorter top-aligned feature row"
  key_links:
    - from: "src/components/marketing/story/simulation-showcase.tsx"
      to: "src/components/marketing/story/skeletons"
      via: "import { ScoreGaugeSkeleton, AudienceCloudSkeleton, DriverRowsSkeleton }"
      pattern: "from \"@/components/marketing/story/skeletons\"|from \"./skeletons\""
    - from: "src/components/marketing/story/how-it-works.tsx"
      to: "src/components/marketing/story/skeletons"
      via: "import skeleton primitives"
      pattern: "skeletons"
---

<objective>
Apply the 03-04 product-skeleton primitives and craft polish to the three story
sections so the placeholders read as intentional product skeletons rather than
unfinished boxes (GAP-1), the Simulation device frame stops being a ~640px empty
void (GAP-2), and the feature blocks read paired-not-stranded (GAP-3
component-level). Also fold in the SimulationShowcase docblock fix (IN-01) and
the brittle-query test fix (WR-04), and extend the story tests to gate the new
skeleton structure.

This plan owns ALL story-section component + story-test files and depends on
03-04 (the skeleton primitives + the ratio-label-free Placeholder). It does NOT
touch page.tsx (03-06 owns page-level rhythm/whitespace — GAP-3 page-level).

Purpose: Close the two High gaps (GAP-1 craft, GAP-2 void) and the
component-level half of GAP-3 by giving each section the right product skeleton
and tighter copy↔visual pairing — while honoring the STUB LOCK (no real data, no
real screenshots, no engine/data wiring) and the noun lock ("Simulation").
Output: Three re-crafted story sections + a re-crafted feature-block leaf + the
three story tests extended for the new skeleton structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-story-showcase/03-VERIFICATION.md
@.planning/phases/03-story-showcase/03-04-PLAN.md

<interfaces>
<!-- 03-04 skeleton primitives this plan consumes (barrel: @/components/marketing/story/skeletons): -->
<!--   ScoreGaugeSkeleton   — static 270deg arc + score 87 + "Strong"        (role=img) -->
<!--   AudienceCloudSkeleton — static dot-cloud (>=16) + "68% watch-through"  (role=img) -->
<!--   DriverRowsSkeleton    — Hook / Retention (drops at 0:07) / Shareability (role=img) -->
<!--   BrowserChrome         — slim bar + 3 dots + numen.app pill, wraps children -->
<!--   PhoneChrome           — rounded phone bezel, wraps children -->

<!-- Existing copy↔test token discipline that MUST be preserved (03-00 single-match getByText queries): -->
<!--   simulation-showcase: <h2> LOCKED VERBATIM "The Simulation" is the SOLE /simulat/i node — -->
<!--     skeleton labels/captions MUST NOT contain "simulat*". Outputs <dl> labels: "Audience reaction", -->
<!--     "Watch-through %", "Hook · Retention · Shareability"; details mention "drop", "Retention", "Hook", "Shareab". -->
<!--   how-it-works: step 3 title "Get your Simulation" is the /simulat/i node; "reading" is FORBIDDEN. -->
<!--   skeleton captions contain "%" / "watch-through" / "drop" — beware NEW /watch[- ]?through/ or /drop/ -->
<!--     duplicate matches breaking the existing strict getByText. If a skeleton caption duplicates a token the -->
<!--     test queries, migrate THAT test assertion to getAllByText(...).length>=1 (WR-04 pattern). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill the Simulation device frame with the gauge/cloud/driver-rows skeleton + cap its height (GAP-2)</name>
  <read_first>
    - src/components/marketing/story/simulation-showcase.tsx (the current device-framed empty <Placeholder> at lines 81-124 — the ~640px void)
    - src/components/marketing/story/skeletons/index.ts (the primitives to import + BrowserChrome)
    - src/components/marketing/story/__tests__/simulation-showcase.test.tsx (KEEP GREEN — heading + 5-output token + Placeholder-aspect assertions)
    - .planning/phases/03-story-showcase/03-RESEARCH.md (Pitfall 4 — the reading IA order: thumbnail → gauge → cloud+watch% → driver rows)
  </read_first>
  <action>
    Rework the showcase device frame so it depicts the SHAPE of a Numen
    Simulation instead of rendering an empty Placeholder void:

    1. Replace the nested empty `<Placeholder ... label="Your prediction" />`
       inside the browser window with the assembled skeleton: top-to-bottom
       `ScoreGaugeSkeleton` (the hero number), then a row pairing
       `AudienceCloudSkeleton` (with its "68% watch-through" caption) beside or
       above `DriverRowsSkeleton` (Hook · Retention · Shareability). Lay them out
       in a calm flat-warm internal grid (e.g. `grid gap-6 p-6 md:p-8`,
       gauge full-width on top, cloud + rows two-up on `md`). Keep the existing
       `BrowserChrome` look — EITHER reuse the 03-04 `BrowserChrome` wrapper, OR
       keep the inline window chrome already present; do not duplicate both.
    2. CAP THE HEIGHT (GAP-2): the filled frame must not be a ~640px void. Drop
       the `aspect="16/10"` void box; let the content define height, and cap the
       window body with a sensible `max-h` (e.g. `max-h-[460px]` on the body) so
       the frame reads as a compact product window, not a tall empty rectangle.
       Preserve no-CLS by keeping the internal skeleton SVGs aspect-stable (they
       are static-size).
    3. NOUN/TOKEN DISCIPLINE: the `<h2>` "The Simulation" stays the SOLE
       /simulat/i node — skeleton labels/captions must NOT contain "simulat*".
       Keep the three `NAMED_OUTPUTS` `<dl>` chips below the frame exactly as-is
       (their tokens satisfy the 03-00 watch-through/Hook/Retention/drop/Shareab
       assertions). If a NEW skeleton caption duplicates a token the test queries
       strictly (e.g. "watch-through" or "drop"), migrate THAT one test assertion
       in Task 4 — do not weaken the copy.
    4. IN-01: align the file's docblock to the THREE rendered output chips (the
       third = combined "Hook · Retention · Shareability"), not five.
    5. Keep the section a PURE RSC (no "use client"); the only client leaf stays
       the existing `FadeInUp` import. Flat-warm tokens only; coral kept precious
       (at most the single weakest hint, inherited from the skeletons).
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && grep -q 'ScoreGaugeSkeleton' src/components/marketing/story/simulation-showcase.tsx && grep -q 'DriverRowsSkeleton' src/components/marketing/story/simulation-showcase.tsx && ! grep -q '"use client"' src/components/marketing/story/simulation-showcase.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - simulation-showcase.tsx imports and renders ScoreGaugeSkeleton, AudienceCloudSkeleton, and DriverRowsSkeleton from the skeletons barrel
    - simulation-showcase.tsx no longer renders a single empty `aspect="16/10"` Placeholder as the frame body (the void is gone); the window body carries a `max-h-` cap
    - `grep -c '/simulat/i' ...` intent holds: only the `<h2>` text "The Simulation" matches /simulat/i (skeleton text contains no "simulat*")
    - the rendered frame `<svg>` set contains an arc/path element AND >= 6 circle elements (cloud) AND the three driver labels Hook/Retention/Shareability
    - `grep -n '"use client"' src/components/marketing/story/simulation-showcase.tsx` returns NOTHING
    - the file docblock lists THREE output chips (IN-01), not five
  </acceptance_criteria>
  <done>The Simulation frame reads as the shape of a Simulation (gauge → cloud+watch% → driver rows), is height-capped, stays pure RSC, and preserves the locked noun/token discipline (GAP-2, IN-01).</done>
</task>

<task type="auto">
  <name>Task 2: Give how-it-works the three step skeletons (GAP-1)</name>
  <read_first>
    - src/components/marketing/story/how-it-works.tsx (the 3 STEPS with empty image Placeholders at lines 44-66, 85)
    - src/components/marketing/story/skeletons/index.ts (the primitives to import)
    - src/components/marketing/story/__tests__/how-it-works.test.tsx (KEEP GREEN — 3 data-variant slots OR equivalent count, 3 level-3 headings, ordinals, /simulat/i, no "reading")
  </read_first>
  <action>
    Replace each step's empty `<Placeholder variant="image" aspect="16/10" />`
    with a section-appropriate static skeleton that hints the step's shape, while
    keeping the no-CLS aspect lock and the test contract:

    - Step 01 "Paste a TikTok link": a small `PhoneChrome` (or a simple
      URL-input-row skeleton) hinting the pasted-link input.
    - Step 02 "The audience reacts": `AudienceCloudSkeleton`.
    - Step 03 "Get your Simulation": `ScoreGaugeSkeleton` (the prediction shape).

    CONSTRAINTS:
    - The how-it-works test currently counts `container.querySelectorAll(
      "[data-variant]")` === 3 (one Placeholder per step). The 03-04 skeleton
      primitives do NOT carry `data-variant`. To keep STORY-01's "3 product
      visuals" gate meaningful with skeletons, in Task 4 update that test to
      count the 3 step visual slots by a stable hook (e.g. wrap each step visual
      in an element with `data-step-visual` and count those, or count the three
      `role="img"` skeletons) — keep it at exactly 3. Add the chosen stable hook
      (`data-step-visual`) to each step visual wrapper here.
    - Keep the no-CLS guarantee: each step visual wrapper keeps an aspect-stable
      box (e.g. an `aspect-[16/10]` wrapper around the skeleton, or the skeleton's
      own fixed dimensions) so mount introduces no layout shift.
    - Keep step 3 title "Get your Simulation" as the /simulat/i node; "reading"
      stays forbidden. Pure RSC; only `StaggerReveal` stays the client leaf.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && grep -qE 'ScoreGaugeSkeleton|AudienceCloudSkeleton|PhoneChrome' src/components/marketing/story/how-it-works.tsx && ! grep -q '"use client"' src/components/marketing/story/how-it-works.tsx && ! grep -qiE '\breading\b' src/components/marketing/story/how-it-works.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - how-it-works.tsx imports >= 2 skeleton primitives and renders one per step (3 step visuals total)
    - each step visual wrapper carries the stable `data-step-visual` hook (count === 3) OR exactly 3 `role="img"` skeletons render
    - no empty default `<Placeholder>` boxes remain as step visuals
    - `grep -n '"use client"' src/components/marketing/story/how-it-works.tsx` returns NOTHING
    - `grep -niE '\breading\b' src/components/marketing/story/how-it-works.tsx` returns NOTHING (noun lock)
    - step 3 title "Get your Simulation" still present (the /simulat/i node)
  </acceptance_criteria>
  <done>Each how-it-works step shows an intentional product skeleton, no empty boxes, no-CLS preserved, noun lock held (GAP-1).</done>
</task>

<task type="auto">
  <name>Task 3: Feature-block craft — wider-shorter ratio, top-align, paired density (GAP-3 component-level)</name>
  <read_first>
    - src/components/marketing/story/feature-block.tsx (the row: items-center grid + tall aspect="16/10" Placeholder at lines 34-56)
    - src/components/marketing/story/feature-blocks.tsx (the FEATURES list + gap-16 md:gap-24 row spacing at line 72)
    - src/components/marketing/story/skeletons/index.ts (optional: a small skeleton hint per block instead of an empty box)
    - src/components/marketing/story/__tests__/feature-blocks.test.tsx (KEEP GREEN — 3-4 visuals, level-3 heading per block, no-CLS aspect, md:order flip)
  </read_first>
  <action>
    Rebalance the feature rows so copy and visual read paired, not stranded
    (GAP-3 at the component level — page-level whitespace is 03-06):

    1. In `feature-block.tsx`: change the grid from `items-center` to
       `items-start` (top-align copy with the visual) and replace the tall
       `aspect="16/10"` visual with a wider-shorter ratio — use `aspect="16/9"`
       or `aspect="3/2"` and cap with a `max-h` (e.g. `max-h-[300px]`) so the
       image stops being a tall rectangle a single line floats against.
    2. Give the visual an intentional skeleton instead of an empty default box:
       wrap a small relevant 03-04 skeleton inside a `BrowserChrome` (or render a
       minimal flat-warm panel skeleton) per block so it reads as product
       set-dressing, not a flat box. Keep ONE visual per block and keep a stable
       count hook so the test still counts 3-4 visuals (preserve `data-variant`
       on a wrapping Placeholder OR add `data-feature-visual` and update the test
       in Task 4 to count that). Keep the `md:order-*` flip on the visual column.
    3. In `feature-blocks.tsx`: tighten inter-row spacing from
       `gap-16 md:gap-24` to a denser rhythm (e.g. `gap-12 md:gap-16`) so rows
       feel connected. (Section-level outer padding is 03-06's job — do not edit
       page.tsx.)
    4. Keep both files PURE RSC (only `StaggerReveal` stays the client leaf);
       flat-warm tokens only; coral kept precious; no-CLS preserved
       (aspect-stable visual wrapper).
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && grep -q 'items-start' src/components/marketing/story/feature-block.tsx && ! grep -q 'aspect="16/10"' src/components/marketing/story/feature-block.tsx && ! grep -q '"use client"' src/components/marketing/story/feature-block.tsx && ! grep -q '"use client"' src/components/marketing/story/feature-blocks.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - feature-block.tsx grid uses `items-start` (top-aligned), not `items-center`
    - feature-block.tsx visual no longer uses `aspect="16/10"`; it uses a wider-shorter ratio (16/9 or 3/2) with a `max-h-` cap
    - each feature block renders exactly one visual with an intentional skeleton (not an empty default Placeholder box); count hook keeps the test at 3-4 visuals
    - feature-block.tsx still applies the `md:order-*` flip on the visual column
    - feature-blocks.tsx inter-row gap is denser than `gap-24` (e.g. `md:gap-16`)
    - `grep -n '"use client"'` returns NOTHING in either feature file
  </acceptance_criteria>
  <done>Feature rows top-align with a wider-shorter intentional-skeleton visual at a denser row rhythm so copy↔visual read paired (GAP-3 component-level); pure RSC, no-CLS held.</done>
</task>

<task type="auto">
  <name>Task 4: Extend the three story tests for the new skeleton structure + fix brittle queries (WR-04)</name>
  <read_first>
    - src/components/marketing/story/__tests__/simulation-showcase.test.tsx (the strict getByText queries — WR-04)
    - src/components/marketing/story/__tests__/how-it-works.test.tsx (the data-variant === 3 count)
    - src/components/marketing/story/__tests__/feature-blocks.test.tsx (the data-variant 3-4 count)
    - the three section components as edited in Tasks 1-3 (the new stable hooks: data-step-visual / data-feature-visual / role=img skeletons)
  </read_first>
  <action>
    Bring the three story tests in line with the new skeleton structure and
    de-brittle them (WR-04):

    1. simulation-showcase.test.tsx (WR-04): replace any plausibly-multi-match
       strict `getByText(/.../i)` (audience/simulat/hook/retention/drop/shareab)
       with `getAllByText(/.../i).length >= 1`, OR scope the query to the outputs
       `<dl>` (`within(dl).getByText(...)`). Add a structural assertion: the
       showcase renders >= 1 `<svg>` skeleton with an arc/path AND >= 6 circles
       AND the three driver labels (Hook/Retention/Shareability) — proving the
       void is filled (GAP-2). Keep the LOCKED `<h2>` "The Simulation" verbatim
       assertion (level-2 heading textContent === "The Simulation").
    2. how-it-works.test.tsx: update the "3 product visuals" count to match the
       new step-visual hook (count `[data-step-visual]` === 3, OR count `role=img`
       skeletons === 3). Keep the 3 level-3 headings, the ordinal markers, the
       /simulat/i node, and the negative `\breading\b` assertion GREEN.
    3. feature-blocks.test.tsx: update the visual count to the new feature-visual
       hook (3-4), keep the level-3-heading-per-block pairing, the no-CLS
       aspect-stable assertion (assert the visual wrapper carries a style with
       aspectRatio OR an aspect-* class), and the `md:order-*` flip assertion.

    Do not weaken intent — assert STABLE TOKENS / structure, not full sentences.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && npx vitest run src/components/marketing/story/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/marketing/story/` is GREEN (all story suites pass against the re-crafted sections)
    - simulation-showcase.test.tsx no longer uses a strict `getByText` on a plausibly-multi-match token (uses getAllByText/within(dl)) — WR-04 closed
    - simulation-showcase.test.tsx asserts the filled-frame skeleton structure (>= 1 svg with arc + >= 6 circles + Hook/Retention/Shareability) — GAP-2 gated
    - how-it-works.test.tsx counts exactly 3 step visuals via the new stable hook
    - feature-blocks.test.tsx counts 3-4 feature visuals and still asserts the md:order flip + no-CLS aspect
  </acceptance_criteria>
  <done>The three story tests gate the new skeleton structure, the brittle simulation-showcase queries are de-brittled (WR-04), and the full story suite is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | No new boundary — static SVG skeletons + presentational layout, no user input |

## STRIDE Threat Register

No new attack surface: static SVG skeletons + CSS layout in existing pure-RSC
sections. No user input, no data flow, no network, no secrets. ASVS L1 — nothing
to mitigate.
</threat_model>

<verification>
- `npx vitest run src/components/marketing/story/` is GREEN (all 4 story suites)
- `grep -rl '"use client"' src/components/marketing/story/*.tsx` is EMPTY (all sections pure RSC)
- The Simulation frame renders the gauge/cloud/driver-rows skeleton, no empty void
- `npm run build` exits 0 and `/` is `○` static (re-confirmed; with 03-06)
</verification>

<success_criteria>
- The Simulation device frame is filled + height-capped (GAP-2)
- how-it-works steps + feature blocks show intentional skeletons, no empty boxes (GAP-1)
- Feature rows top-aligned, wider-shorter visual, denser rhythm (GAP-3 component-level)
- IN-01 docblock + WR-04 queries fixed; story suite GREEN; sections stay pure RSC
</success_criteria>

<output>
Create `.planning/phases/03-story-showcase/03-05-SUMMARY.md` when done
</output>
