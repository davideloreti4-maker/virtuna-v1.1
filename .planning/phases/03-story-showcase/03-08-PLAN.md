---
phase: 03-story-showcase
plan: 08
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/components/marketing/hero/hero.tsx
  - src/components/marketing/hero/__tests__/hero.test.tsx
autonomous: true
requirements: [STORY-01, STORY-02, STORY-03]
must_haves:
  truths:
    - "The hero on / no longer ships \"Numen reading\" — the desktop showcase slot is labelled \"Numen Simulation\", honoring the page-wide locked product noun above the fold (WR-01)"
    - "The hero test pins the locked noun (/numen simulation/i), not the retired noun (/numen reading/i) (WR-01)"
  artifacts:
    - path: "src/components/marketing/hero/hero.tsx"
      provides: "Hero desktop slot labelled with the locked noun"
      contains: "Numen Simulation"
    - path: "src/components/marketing/hero/__tests__/hero.test.tsx"
      provides: "Hero test asserting the locked noun"
      contains: "numen simulation"
  key_links:
    - from: "src/components/marketing/hero/__tests__/hero.test.tsx"
      to: "src/components/marketing/hero/hero.tsx"
      via: "getByText(/numen simulation/i) matches the slot label"
      pattern: "numen simulation"
---

<objective>
Lock the product noun above the fold (WR-01): the hero's desktop showcase slot
still renders `label="Numen reading"` (hero.tsx:132) and its test pins
`getByText(/numen reading/i)` — the one place on `/` that still ships the retired
noun, directly contradicting the page-wide "Simulation, never reading" lock the
STORY sections enforce. Rename the label to "Numen Simulation" and update the
test assertion.

This plan owns ONLY `hero.tsx` + `hero.test.tsx`. It is file-disjoint from every
other Wave-1 plan. Tiny, surgical, two-line intent.

Purpose: A visitor sees "Simulation" (not "reading") above the fold, matching the
locked noun the rest of the page already honors.
Output: A hero slot labelled "Numen Simulation" and a hero test that pins it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-story-showcase/03-VERIFICATION.md
@.planning/phases/03-story-showcase/03-REVIEW.md

<interfaces>
<!-- hero.tsx:129-134 — the desktop reading slot to rename: -->
<!--   <Placeholder variant="image" aspect="16/10" label="Numen reading" className="rounded-none border-0 bg-surface" /> -->
<!-- hero.test.tsx:71-77 — the assertion to flip: -->
<!--   it("renders the desktop reading slot (the OUTPUT)", () => { ... expect(screen.getByText(/numen reading/i)).toBeTruthy(); }) -->
<!-- The phone slot label "Your TikTok" + the "numen.app" pill + the H1/subcopy/CTA tests stay UNCHANGED. -->
<!-- The hero subcopy already uses "simulates" (the /simulat/i node) — do NOT touch it. Hero stays a PURE RSC. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the hero desktop slot to "Numen Simulation" + flip the hero test assertion (WR-01)</name>
  <read_first>
    - src/components/marketing/hero/hero.tsx (the desktop showcase Placeholder label at line 132; keep everything else — phone slot, chrome, H1, subcopy, CTA — unchanged)
    - src/components/marketing/hero/__tests__/hero.test.tsx (the HERO-03/04 "desktop reading slot" assertion at lines 71-77)
  </read_first>
  <action>
    1. In hero.tsx: change the desktop showcase Placeholder label from
       "Numen reading" to "Numen Simulation". This is the ONLY content change —
       do not touch the phone slot ("Your TikTok"), the browser chrome
       ("numen.app"), the H1, the subcopy, or the CTA. You MAY update the
       adjacent comment/docblock wording from "the Numen reading (output)" to
       "the Numen Simulation (output)" for consistency. Hero stays a PURE RSC
       (no "use client").
    2. In hero.test.tsx: change the "desktop reading slot" assertion from
       getByText(/numen reading/i) to getByText(/numen simulation/i). Optionally
       rename the it(...) title to "renders the desktop Simulation slot (the
       OUTPUT)". Leave the phone-slot test (/your tiktok/i), the browser-window
       test (/numen\.app/i), and all HERO-01/02 tests untouched.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && grep -q 'Numen Simulation' src/components/marketing/hero/hero.tsx && ! grep -qi 'numen reading' src/components/marketing/hero/hero.tsx && grep -qi 'numen simulation' src/components/marketing/hero/__tests__/hero.test.tsx && ! grep -qi 'numen reading' src/components/marketing/hero/__tests__/hero.test.tsx && npx vitest run src/components/marketing/hero/ 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i 'numen reading' src/components/marketing/hero/hero.tsx` returns NOTHING (retired noun gone from the hero)
    - hero.tsx renders the desktop slot with `label="Numen Simulation"`
    - `grep -i 'numen reading' src/components/marketing/hero/__tests__/hero.test.tsx` returns NOTHING; the test asserts `/numen simulation/i`
    - `npx vitest run src/components/marketing/hero/` is GREEN (the flipped assertion passes; all other HERO tests untouched and passing)
    - the hero subcopy /simulat/i node and the phone "Your TikTok" / "numen.app" labels are unchanged
  </acceptance_criteria>
  <done>The hero ships "Numen Simulation" (not "reading") above the fold and the hero test pins the locked noun — page-wide noun lock now holds (WR-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | No new boundary — a static label string + test assertion change in a pure-RSC hero |

## STRIDE Threat Register

No new attack surface: a one-string label rename + test assertion update in an
existing pure-RSC component. No user input, no data flow, no network, no secrets.
ASVS L1 — nothing to mitigate.
</threat_model>

<verification>
- `grep -ri 'numen reading' src/components/marketing/hero/` returns NOTHING
- `npx vitest run src/components/marketing/hero/` is GREEN
- `npm run build` exits 0 and `/` stays `○` static (hero remains a pure RSC)
</verification>

<success_criteria>
- Hero desktop slot labelled "Numen Simulation"; retired noun gone above the fold (WR-01)
- Hero test pins /numen simulation/i; hero suite GREEN
</success_criteria>

<output>
Create `.planning/phases/03-story-showcase/03-08-SUMMARY.md` when done
</output>
