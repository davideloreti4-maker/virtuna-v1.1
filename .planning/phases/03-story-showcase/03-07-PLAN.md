---
phase: 03-story-showcase
plan: 07
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/lib/nav.ts
  - src/components/layout/header.tsx
  - src/components/layout/footer.tsx
  - src/components/layout/__tests__/header.test.tsx
  - src/components/layout/__tests__/footer.test.tsx
autonomous: true
requirements: [STORY-01, STORY-02, STORY-03]
must_haves:
  truths:
    - "Pressing Escape while the mobile nav panel is open closes it (GAP-4 / WR-03)"
    - "While the mobile nav panel is open, focus is trapped inside the panel and on close focus returns to the trigger button (GAP-4 / WR-03)"
    - "The body scroll-lock saves the previous overflow value and restores it on cleanup instead of clobbering it to \"\" (WR-02)"
    - "Header and footer import ONE shared nav anchor constant — the two arrays are no longer hand-duplicated (IN-03)"
  artifacts:
    - path: "src/lib/nav.ts"
      provides: "Single source of truth for the in-page nav anchor set"
      contains: "#features"
    - path: "src/components/layout/header.tsx"
      provides: "Escape-close + focus trap + focus restore + save/restore overflow + shared nav import"
      contains: "Escape"
    - path: "src/components/layout/footer.tsx"
      provides: "Footer product links imported from the shared constant"
  key_links:
    - from: "src/components/layout/header.tsx"
      to: "src/lib/nav.ts"
      via: "import { NAV_LINKS } from \"@/lib/nav\""
      pattern: "from \"@/lib/nav\""
    - from: "src/components/layout/footer.tsx"
      to: "src/lib/nav.ts"
      via: "import the shared nav anchor set"
      pattern: "from \"@/lib/nav\""
---

<objective>
Close the mobile-nav accessibility gap (GAP-4 / WR-03): Escape-to-close, a focus
trap while the panel is open, and focus restore to the trigger on close. Fold in
the scroll-lock save/restore fix (WR-02) and lift the byte-identical header/
footer nav arrays into one shared constant (IN-03).

This plan owns `src/lib/nav.ts` (new), `header.tsx`, `footer.tsx`, and their two
test files. It is file-disjoint from every other Wave-1 plan.

Purpose: Keyboard users must be able to dismiss the mobile menu and never get
trapped behind the trigger with scroll locked — the confirmed-live a11y defect.
Output: A shared nav constant + an accessible mobile disclosure + updated layout
tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-story-showcase/03-VERIFICATION.md
@.planning/phases/03-story-showcase/03-REVIEW.md

<interfaces>
<!-- src/lib/routes.ts — the existing shared-constant analog to mirror for nav.ts (SIGNUP_URL / LOGIN_URL). -->
<!-- src/lib/a11y.ts — EXISTING helpers (useArrowKeyFocusGrid, useRovingTabIndex, announce). -->
<!--   NOTE: a11y.ts has NO focus-trap / focus-restore helper. Implement the trap + restore INLINE in header.tsx -->
<!--   (header is the only consumer); do NOT add a new exported hook to a11y.ts for this one-off. -->

<!-- Current header NAV_LINKS / footer PRODUCT_LINKS (byte-identical — the IN-03 duplication): -->
<!--   { "How it works": "#how-it-works" }, { "The Simulation": "#the-simulation" }, -->
<!--   { "Features": "#features" }, { "Pricing": "#pricing" }, { "FAQ": "#faq" } -->

<!-- Current header scroll-lock (WR-02 clobber) lives in the useEffect at header.tsx:45-54: -->
<!--   it always restores document.body.style.overflow = "" — must snapshot+restore the prior value. -->
<!-- Mobile trigger button + panel: button has aria-expanded/aria-controls="mobile-nav-panel"; panel is -->
<!--   data-testid="mobile-nav-panel", conditionally rendered when mobileMenuOpen. Header is already "use client". -->

<!-- header.test.tsx already covers: open menu, close-on-link-tap, aria-label "Open menu"/"Close menu". -->
<!-- footer.test.tsx it.each pins all 5 anchors incl. #features — KEEP GREEN after the shared-constant lift. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared nav constant (src/lib/nav.ts) imported by header + footer (IN-03)</name>
  <read_first>
    - src/lib/routes.ts (the shared-constant pattern + JSDoc style to mirror)
    - src/components/layout/header.tsx (the NAV_LINKS array to replace with the import, lines 16-23)
    - src/components/layout/footer.tsx (the PRODUCT_LINKS array to replace with the import, lines 10-21)
    - src/components/layout/__tests__/footer.test.tsx (the it.each pinning all 5 anchors — must stay GREEN)
  </read_first>
  <action>
    1. Create `src/lib/nav.ts` exporting a single readonly `NAV_LINKS` array of
       `{ label, href }` for the in-page anchor set, in this exact order:
       "How it works"→#how-it-works, "The Simulation"→#the-simulation,
       "Features"→#features, "Pricing"→#pricing, "FAQ"→#faq. Mirror the
       `src/lib/routes.ts` JSDoc/`as const` style. This is the single source of
       truth IN-03 calls for.
    2. In `header.tsx`: delete the local `NAV_LINKS` const and
       `import { NAV_LINKS } from "@/lib/nav"`. Use it for both the desktop nav
       map and the mobile panel map (unchanged rendering).
    3. In `footer.tsx`: delete the local `PRODUCT_LINKS` const and instead
       import `NAV_LINKS` from `@/lib/nav`; map it in the Product column
       (rename the local usage or alias — the rendered output is identical).
       Remove the "keep them exactly in sync by hand" comment.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && grep -q 'from "@/lib/nav"' src/components/layout/header.tsx && grep -q 'from "@/lib/nav"' src/components/layout/footer.tsx && grep -q '#features' src/lib/nav.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/nav.ts` exists and exports a readonly `NAV_LINKS` array containing all 5 anchors incl. `#features` in the locked order
    - header.tsx imports NAV_LINKS from `@/lib/nav` and no longer declares a local NAV_LINKS array
    - footer.tsx imports the shared constant from `@/lib/nav` and no longer declares a local PRODUCT_LINKS array
    - `grep -c 'href: "#' src/components/layout/header.tsx` for the nav set === 0 (the literal array is gone from header)
    - `npx vitest run src/components/layout/__tests__/footer.test.tsx` is GREEN (all 5 anchors incl. #features still render)
  </acceptance_criteria>
  <done>Header and footer share one nav anchor source of truth; the hand-duplicated arrays are gone; footer test GREEN (IN-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mobile-nav a11y — Escape-close + focus trap + focus restore + save/restore overflow (GAP-4 / WR-02 / WR-03)</name>
  <read_first>
    - src/components/layout/header.tsx (the mobileMenuOpen state, the scroll-lock useEffect lines 45-54, the trigger button lines 106-119, the panel lines 124-157)
    - src/components/layout/__tests__/header.test.tsx (existing NAV-03 coverage to extend; keep all current tests GREEN)
    - src/lib/a11y.ts (confirm there is NO focus-trap helper — implement inline)
  </read_first>
  <behavior>
    - Test: opening the menu then pressing Escape closes the panel (queryByTestId("mobile-nav-panel") is null after Escape) and the trigger flips back to "Open menu"
    - Test: after the panel closes (via Escape or link tap), document.activeElement is the trigger button (focus restored)
    - Test: while open, Tab from the last focusable in the panel wraps to the first (focus trap) — assert focus stays within the panel container
    - Test (regression): the scroll-lock saves and restores the prior body overflow — set document.body.style.overflow="scroll" before opening; after close, it is "scroll" again, not ""
    - Test (kept GREEN): existing open / close-on-link-tap / aria-label tests still pass
  </behavior>
  <action>
    Add accessible disclosure behavior to the header mobile panel. Implement the
    trap/restore INLINE (a11y.ts has no such helper and the header is the only
    consumer):

    1. Add a `useRef<HTMLButtonElement>` on the trigger button and a
       `useRef<HTMLDivElement>` on the panel container.
    2. WR-02 — fix the scroll-lock useEffect to snapshot and restore the prior
       value: capture `const prev = document.body.style.overflow` at effect
       start, set `"hidden"` only while open, and restore `prev` in the cleanup
       (not `""`).
    3. WR-03 / GAP-4 Escape — while `mobileMenuOpen`, add a `keydown` listener
       that calls `setMobileMenuOpen(false)` on `e.key === "Escape"`; remove the
       listener in cleanup.
    4. WR-03 / GAP-4 focus trap — while open, move focus into the panel (focus
       the first focusable link on open), and on `Tab` / `Shift+Tab` keep focus
       cycling within the panel's focusable elements (query the panel ref for
       `a[href], button` and wrap at the ends). Handle this in the same keydown
       listener or a dedicated one scoped to the panel.
    5. WR-03 / GAP-4 focus restore — when the panel closes (Escape OR link tap OR
       toggle), return focus to the trigger button via the trigger ref
       (`triggerRef.current?.focus()`), e.g. in a `useEffect` that runs on the
       open→closed transition. Do not steal focus on initial mount (only restore
       after an open).
    6. Keep `aria-expanded` / `aria-controls` / the panel `id` /
       `data-testid="mobile-nav-panel"` exactly as they are. Header stays
       `"use client"` (already is). Flat-warm tokens only; no visual change.
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && grep -q 'Escape' src/components/layout/header.tsx && grep -qE 'const prev = document.body.style.overflow|prev = document\.body\.style\.overflow' src/components/layout/header.tsx && npx vitest run src/components/layout/__tests__/header.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - header.tsx contains an Escape keydown handler that calls `setMobileMenuOpen(false)` (grep "Escape" + setMobileMenuOpen)
    - header.tsx scroll-lock useEffect snapshots `document.body.style.overflow` into a `prev` var and restores `prev` in cleanup (WR-02 — no bare `= ""` clobber on cleanup)
    - header.tsx restores focus to the trigger button on close (a `triggerRef.current?.focus()` call gated on the open→closed transition)
    - header.tsx implements a focus trap while open (queries panel focusables and wraps Tab at the ends)
    - `npx vitest run src/components/layout/__tests__/header.test.tsx` is GREEN, including NEW tests for Escape-close, focus-restore, and the overflow save/restore regression, with the existing open / close-on-link-tap / aria-label tests still passing
  </acceptance_criteria>
  <done>Keyboard users can Escape-close the mobile menu, focus is trapped while open and restored to the trigger on close, and the body overflow is saved/restored — header test GREEN (GAP-4, WR-02, WR-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client keyboard events → React state | Escape/Tab keydown on the client toggles disclosure state; no data leaves the client |

## STRIDE Threat Register

No new attack surface: client-side a11y keydown handlers + a static TS constant
in existing chrome. No user input persisted, no data flow, no network, no
secrets. The keydown listeners are scoped to the open panel and removed on
cleanup (no leaked global listeners). ASVS L1 — nothing to mitigate.
</threat_model>

<verification>
- `npx vitest run src/components/layout/` is GREEN (header + footer suites)
- header.tsx: Escape-close + focus trap + focus restore + save/restore overflow present
- `src/lib/nav.ts` is the single nav source; header + footer import it
- `npm run build` exits 0; `/` stays `○` static (header was already a client island; no new section-root client leak)
</verification>

<success_criteria>
- Mobile nav: Escape-close, focus trap, focus restore (GAP-4 / WR-03)
- Scroll-lock saves/restores prior overflow (WR-02)
- One shared nav constant imported by header + footer (IN-03); layout suites GREEN
</success_criteria>

<output>
Create `.planning/phases/03-story-showcase/03-07-SUMMARY.md` when done
</output>
