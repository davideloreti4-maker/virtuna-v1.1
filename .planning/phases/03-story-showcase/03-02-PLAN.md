---
phase: 03-story-showcase
plan: 02
type: execute
wave: 2
depends_on: ["03-00", "03-01"]
files_modified:
  - src/components/marketing/story/simulation-showcase.tsx
  - src/components/marketing/index.ts
  - src/app/(marketing)/page.tsx
autonomous: true
requirements: [STORY-02]

must_haves:
  truths:
    - "Visiting / shows a 'The Simulation' section presenting the output in a flat-warm device-framed placeholder"
    - "The section names all required outputs: audience simulation, watch-through %, Hook · Retention (where viewers drop) · Shareability"
    - "The product visual is a labelled, aspect-locked <Placeholder> — no real product component imported, no engine/data wiring"
  artifacts:
    - path: "src/components/marketing/story/simulation-showcase.tsx"
      provides: "STORY-02 — 'The Simulation' output showcase RSC section"
      min_lines: 30
      contains: "export function SimulationShowcase"
    - path: "src/components/marketing/index.ts"
      provides: "barrel export of SimulationShowcase"
      contains: "SimulationShowcase"
    - path: "src/app/(marketing)/page.tsx"
      provides: "#the-simulation stub filled in place with <SimulationShowcase/>"
      contains: "<SimulationShowcase"
  key_links:
    - from: "src/components/marketing/story/simulation-showcase.tsx"
      to: "@/components/marketing/placeholder"
      via: "import { Placeholder }"
      pattern: "Placeholder"
    - from: "src/app/(marketing)/page.tsx"
      to: "SimulationShowcase"
      via: "named import from @/components/marketing rendered inside <section id='the-simulation'>"
      pattern: "<SimulationShowcase"
---

<objective>
Build STORY-02 — the "The Simulation" output showcase — and fill the existing `#the-simulation` stub in place. One prominent flat-warm device-framed `<Placeholder>` depicts the SHAPE of a Numen Simulation (reading IA: thumbnail → score gauge → audience cloud + watch% → Hook · Retention · Shareability driver rows), with the three named outputs surfaced as labelled text chips beneath. Everything stays a labelled placeholder — nothing imported from board/reading/viral-results, no engine/data wiring (D-D Option A).

Purpose: STORY-02 — show creators what they GET, framed as the real product, swappable for a real screenshot later. Echoes the hero's device-framed product-shot craft bar (the Phase-2 pivot lesson: show the product's shape, not abstract effects).

Output: `src/components/marketing/story/simulation-showcase.tsx` (RSC), barrel export, and the `#the-simulation` stub filled in `page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-story-showcase/03-PATTERNS.md
@.planning/phases/03-story-showcase/03-RESEARCH.md
@.planning/phases/02-hero-signature-moment/02-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — use directly, no exploration. -->

Placeholder (src/components/marketing/placeholder.tsx) — the product-visual slot:
```
<Placeholder variant="image" aspect="16/10" label="Your Simulation" className="rounded-none border-0 bg-surface" />
// when nested in a device frame, override the chip's own radius/border so the frame owns the chrome (hero does this, hero.tsx line 133)
```

Device-frame chrome to REUSE (extracted from hero.tsx lines 113-136 — the flat-warm browser window; copy this treatment, do NOT import the hero):
```
<div className="overflow-hidden rounded-xl border border-border bg-surface-elevated shadow-[0_40px_80px_-24px_rgba(0,0,0,0.72),0_14px_30px_-12px_rgba(0,0,0,0.5)]">
  <div className="flex items-center border-b border-border px-4 py-2.5">
    <span className="flex gap-2" aria-hidden="true"> {/* 3 × <span className="h-2.5 w-2.5 rounded-full bg-foreground-muted/30" /> */} </span>
    <span className="mx-auto rounded-md bg-background px-4 py-1 font-mono text-[11px] tracking-wide text-foreground-muted">numen.app</span>
    <span className="w-[42px]" aria-hidden="true" />
  </div>
  <Placeholder variant="image" aspect="16/10" label="Your Simulation" className="rounded-none border-0 bg-surface" />
</div>
```
The inline box-shadow is a LAYERED DARK DROP shadow (flat-warm-legal depth), NOT a glow. A faint warm "seat" radial (hero.tsx lines 99-108, `rgba(236,231,222,0.07)`, commented "NOT a glow") may be copied verbatim if a seat is wanted.

Section wrapper to PRESERVE in page.tsx (LOCKED rhythm — id/border/padding/measure unchanged):
```
<section id="the-simulation" className="border-t border-border px-6 py-20">
  <div className="mx-auto max-w-5xl">
    <SimulationShowcase />   // replaces the muted stub <h2>The Simulation</h2>
  </div>
</section>
```

Canonical reading IA (SHAPE REFERENCE ONLY — read for believable set-dressing, NEVER import): thumbnail → score gauge (270° arc + number + band word; ≥70 = Strong) → audience persona cloud + "{n}% watch-through" caption → 3 driver rows Hook · Retention (drop timestamp) · Shareability. Honesty floor: if a score is shown as set-dressing, keep it ≥70 (BAND_THRESHOLDS.STRONG = 70).

Flat-warm tokens (globals.css SSOT): text-foreground/secondary/muted, text-accent (coral, sparingly), bg-surface/surface-elevated/background, border-border. FORBIDDEN: glass/glow/backdrop-blur, #FF7F50, hardcoded hex (except the hero's explicit dark-shadow + warm-seat rgba, both legal/commented).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the SimulationShowcase RSC section + export it from the barrel</name>
  <behavior>
    - Test 1 (STORY-02): the section heading reads "The Simulation" VERBATIM (matches the `#the-simulation` anchor).
    - Test 2 (named outputs): the section names all five — audience simulation, watch-through %, Hook, Retention (where viewers drop), Shareability — by stable token.
    - Test 3 (Success Crit 4): the product visual is a `<Placeholder>` (`[data-variant]`) with an inline aspect-ratio (no-CLS).
    (These tests already exist RED from 03-00 — this task turns them GREEN.)
  </behavior>
  <read_first>
    - src/components/marketing/hero/hero.tsx lines 92-151 (the EXACT flat-warm browser-window device chrome + warm seat to reuse — copy the treatment, do NOT import)
    - src/components/marketing/placeholder.tsx (the slot API; the className override pattern for nesting in a frame)
    - src/components/marketing/index.ts (barrel convention)
    - src/components/marketing/story/__tests__/simulation-showcase.test.tsx (the RED gate this task satisfies)
    - .planning/phases/03-story-showcase/03-RESEARCH.md § Pattern 4 (Layout A — one big frame + named outputs in copy) + § Pitfall 4 (believable reading IA + the ≥70 honesty floor) + § Pitfall 1 (noun = "The Simulation")
    - .planning/phases/03-story-showcase/03-PATTERNS.md § simulation-showcase.tsx (browser-chrome lines + heading + named-outputs requirement)
  </read_first>
  <files>src/components/marketing/story/simulation-showcase.tsx, src/components/marketing/index.ts</files>
  <action>
    Create `src/components/marketing/story/simulation-showcase.tsx` as a PURE RSC — NO `"use client"` directive (Pitfall 5). Export `function SimulationShowcase({ className }: { className?: string })`.

    Imports: `Placeholder` from `@/components/marketing/placeholder`; optionally `FadeInUp` from `@/components/motion` for a calm single-element entrance; `cn` from `@/lib/utils` if needed.

    Render (D-D Layout A):
    1. An `<h2>` reading EXACTLY "The Simulation" (LOCKED — matches the `#the-simulation` anchor; the test asserts verbatim) in SANS `font-semibold text-foreground` (D-C — serif stays precious to the hero). Optionally a one-line `text-foreground-secondary` subhead naming what the Simulation is.
    2. ONE prominent flat-warm device-framed `<Placeholder variant="image" aspect="16/10" label="Your Simulation">` nested inside the browser-window chrome from the hero (overflow-hidden rounded-xl border-border bg-surface-elevated + the slim browser bar with 3 dots + a `numen.app` address pill + the layered dark drop shadow). Give the nested Placeholder `className="rounded-none border-0 bg-surface"` so the frame owns the chrome. Optionally copy the hero's faint warm "seat" radial (commented "NOT a glow") to float the frame.
    3. Beneath the frame, surface the THREE named outputs as a row of labelled text chips / a small caption list: "Audience simulation", "Watch-through %", and "Hook · Retention (where viewers drop) · Shareability". The test asserts the stable tokens audience+simulat, watch-through %, Hook, Retention + drop, Shareability — ensure ALL appear in rendered text. (Where viewers drop is the Retention sub-label.) Use `text-foreground-secondary`/`text-foreground-muted`; coral at most ONCE as a single subtle accent if at all (A6).

    Honesty floor (Pitfall 4): if any score number is shown as set-dressing (in copy or a label), keep it ≥70 so the "will pop" framing is truthful (BAND_THRESHOLDS.STRONG = 70). Prefer not showing a number at all — the placeholder frame is the swappable real screenshot.

    Mobile-sane (Pitfall 6): the chip row stacks/wraps below 320px; no fixed pixel widths that overflow. This is NOT the Phase-5 hardening pass — just responsive by construction.

    Then add to `src/components/marketing/index.ts`: `export { SimulationShowcase } from "./story/simulation-showcase";`.

    Do NOT import from `board/*`, `reading/*`, `viral-results/*`, or any engine/data hook — the reading IA is a SHAPE reference read during research, never an import (anti-pattern guard / D-D).
  </action>
  <verify>
    <automated>npx vitest run src/components/marketing/story/__tests__/simulation-showcase.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/marketing/story/__tests__/simulation-showcase.test.tsx` exits 0 (all STORY-02 cases GREEN).
    - The rendered `<h2>` text is exactly "The Simulation" (asserted by the test).
    - All five named outputs appear in rendered text, rendered to satisfy the 03-00 test's exact regex branches (cross-plan copy↔test coupling — do NOT tighten the copy past these): "audience simulation" (matches `/audience/i`+`/simulat/i`), "Watch-through %" (matches `/watch[- ]?through/i`), "Hook", "Retention" + "where viewers drop" (matches `/retention/i`+`/drop/i`), "Shareability".
    - `grep -L '"use client"' src/components/marketing/story/simulation-showcase.tsx` returns the file (NO client directive).
    - The product visual is a `<Placeholder>` with an inline aspect-ratio (the test reads `style.aspectRatio` non-empty).
    - `grep -q "SimulationShowcase" src/components/marketing/index.ts` (barrel export added).
    - `grep -Eq 'board/|reading/|viral-results' src/components/marketing/story/simulation-showcase.tsx` returns NOTHING (no banned imports).
    - No glass/glow: `grep -Eiq 'backdrop-blur|shadow-glow|FF7F50' src/components/marketing/story/simulation-showcase.tsx` returns NOTHING.
  </acceptance_criteria>
  <done>SimulationShowcase renders the verbatim "The Simulation" heading, a device-framed Placeholder, and all five named outputs; the 03-00 showcase test is GREEN; barrel exports it; no "use client", no banned imports, no glass/glow.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the #the-simulation stub in page.tsx with <SimulationShowcase/></name>
  <read_first>
    - src/app/(marketing)/page.tsx (current skeleton — `#the-simulation` section lines 50-60 + the `@/components/marketing` named import line 3, which now also imports HowItWorks from 03-01)
    - src/components/marketing/index.ts (now exports SimulationShowcase)
    - .planning/phases/03-story-showcase/03-PATTERNS.md § page.tsx "Pattern 1 — fill stub IN PLACE"
    - .planning/phases/03-story-showcase/03-RESEARCH.md § Pitfall 3 (LOCKED section rhythm)
  </read_first>
  <files>src/app/(marketing)/page.tsx</files>
  <action>
    In `page.tsx`: add `SimulationShowcase` to the existing named import from `@/components/marketing` (the line that, after 03-01, reads `import { Hero, MotionConfigShell, HowItWorks } from "@/components/marketing";`). Inside the EXISTING `<section id="the-simulation" className="border-t border-border px-6 py-20">` → `<div className="mx-auto max-w-5xl">`, REPLACE the muted stub `<h2 className="text-3xl font-semibold text-foreground-muted">The Simulation</h2>` with `<SimulationShowcase />`.

    Do NOT change the `<section>` element, its `id`, its `border-t border-border px-6 py-20`, or the inner `mx-auto max-w-5xl` (LOCKED rhythm, D-E / Pitfall 3). Touch ONLY the `#the-simulation` section; leave the now-filled `#how-it-works` (03-01), `#pricing`, `#faq`, and the hero untouched.
  </action>
  <verify>
    <automated>grep -q "<SimulationShowcase" "src/app/(marketing)/page.tsx" && grep -q 'id="the-simulation" className="border-t border-border px-6 py-20"' "src/app/(marketing)/page.tsx" && npx vitest run src/components/marketing/story/__tests__/simulation-showcase.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` imports `SimulationShowcase` from `@/components/marketing` and renders `<SimulationShowcase />` inside `<section id="the-simulation">`.
    - The `#the-simulation` wrapper is unchanged: `grep -q 'id="the-simulation" className="border-t border-border px-6 py-20"' "src/app/(marketing)/page.tsx"` matches.
    - The muted stub `<h2 ...text-foreground-muted>The Simulation</h2>` is gone from that section.
    - The `#how-it-works` (filled), `#pricing`, `#faq`, and `#hero` sections are unchanged.
    - `npx vitest run src/components/marketing/story/__tests__/simulation-showcase.test.tsx` still exits 0.
  </acceptance_criteria>
  <done>The `#the-simulation` stub is filled in place with `<SimulationShowcase/>`; the LOCKED rhythm and all other sections are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Static presentational section. No user input, no network, no data, no live product. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | simulation-showcase.tsx copy/set-dressing | accept | All copy is static literals; never `dangerouslySetInnerHTML`; no user input rendered. |
| T-03-02-02 | Information disclosure | depicted score / outputs | accept | The frame is a Placeholder with FAKE set-dressing, not a live read — no real engine output, no PII. Honesty floor (≥70) keeps the "will pop" framing truthful, not a data risk. |
| T-03-02-03 | Tampering | imports | mitigate | No import from board/reading/viral-results or engine/data hooks; reading IA is shape-reference-only. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed (RESEARCH § Package Legitimacy Audit). No install task. |
</threat_model>

<verification>
- `npx vitest run src/components/marketing/story/__tests__/simulation-showcase.test.tsx` → exits 0 (STORY-02 GREEN).
- `npx vitest run src/components/marketing/story/` → how-it-works + simulation-showcase GREEN (feature-blocks still RED until 03-03).
- Per-wave merge: `npm test` full suite green + `npm run build` exit 0 (`○ /`). The phase-level BLOCKING static gate lives in 03-03.
</verification>

<success_criteria>
- SimulationShowcase RSC renders the verbatim "The Simulation" heading.
- Names all five outputs: audience simulation, watch-through %, Hook · Retention (where viewers drop) · Shareability.
- Product visual is a flat-warm device-framed, labelled, aspect-locked `<Placeholder>` (swappable, no-CLS).
- Any depicted score honors the ≥70 honesty floor; no glass/glow; coral precious.
- `#the-simulation` stub filled in place; LOCKED rhythm + other sections untouched.
- Barrel exports SimulationShowcase; no "use client"; no banned imports.
- The 03-00 showcase gate is GREEN.
</success_criteria>

<output>
Create `.planning/phases/03-story-showcase/03-02-SUMMARY.md` when done.
</output>
