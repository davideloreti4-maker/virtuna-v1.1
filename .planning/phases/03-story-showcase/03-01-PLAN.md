---
phase: 03-story-showcase
plan: 01
type: execute
wave: 1
depends_on: ["03-00"]
files_modified:
  - src/components/marketing/story/how-it-works.tsx
  - src/components/marketing/index.ts
  - src/app/(marketing)/page.tsx
autonomous: true
requirements: [STORY-01]

must_haves:
  truths:
    - "Visiting / shows a 'How it works' section with exactly three ordered steps — paste a TikTok link → the audience simulates → get your Simulation"
    - "Each step renders a labelled, aspect-locked <Placeholder> product visual (swappable later via src, no layout shift)"
    - "All step copy uses the canonical noun 'Simulation'/'simulates' — never 'reading' as a user-facing product noun"
  artifacts:
    - path: "src/components/marketing/story/how-it-works.tsx"
      provides: "STORY-01 — the 3-step how-it-works RSC section"
      min_lines: 30
      contains: "export function HowItWorks"
    - path: "src/components/marketing/index.ts"
      provides: "barrel export of HowItWorks"
      contains: "HowItWorks"
    - path: "src/app/(marketing)/page.tsx"
      provides: "#how-it-works stub filled in place with <HowItWorks/>"
      contains: "<HowItWorks"
  key_links:
    - from: "src/components/marketing/story/how-it-works.tsx"
      to: "@/components/marketing/placeholder"
      via: "import { Placeholder }"
      pattern: "Placeholder"
    - from: "src/app/(marketing)/page.tsx"
      to: "HowItWorks"
      via: "named import from @/components/marketing rendered inside <section id='how-it-works'>"
      pattern: "<HowItWorks"
---

<objective>
Build STORY-01 — the three-step "How it works" section — and fill the existing `#how-it-works` stub in place. Three numbered steps read left→right on desktop, stacking on mobile: 1) paste a TikTok link, 2) the audience simulates, 3) get your Simulation. Each step pairs a short title + one line of copy with a labelled `<Placeholder>` product visual.

Purpose: STORY-01 — the calm opener of the scroll body that explains the loop in three beats, mirroring the hero subcopy. Linear/OpusClip typographic restraint; this is the calm phase (no motion spectacle beyond a `StaggerReveal` entrance).

Output: `src/components/marketing/story/how-it-works.tsx` (RSC), barrel export, and the `#how-it-works` stub filled in `page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-story-showcase/03-PATTERNS.md
@.planning/phases/03-story-showcase/03-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — use directly, no exploration. -->

Placeholder (src/components/marketing/placeholder.tsx) — the slot for every product visual:
```
<Placeholder variant="image" aspect="16/10" label="Paste a link" />   // labelled stand-in, no-CLS via inline aspect-ratio
// props: variant ("image"|"video"|"avatar"|"logo", default image) · aspect (free CSS string) · label · src (one-prop swap) · breathe (default off) · className
// root carries data-variant; label is rendered as a caption for image/video/avatar (NOT logo)
```

StaggerReveal (src/components/motion/stagger-reveal.tsx) — the only client leaf; self-gates reduced-motion (returns plain <div> under reduce):
```
<StaggerReveal className="...grid...">
  <StaggerReveal.Item key={...} className="...">{step}</StaggerReveal.Item>
</StaggerReveal>
// StaggerReveal.Item is a static prop (no separate import). Import from "@/components/motion".
```

Section wrapper to PRESERVE in page.tsx (the LOCKED rhythm — do NOT change id/border/padding/measure):
```
<section id="how-it-works" className="border-t border-border px-6 py-20">
  <div className="mx-auto max-w-5xl">
    <HowItWorks />     // replaces the muted stub <h2>
  </div>
</section>
```

Flat-warm semantic tokens (globals.css SSOT): text-foreground (cream) · text-foreground-secondary · text-foreground-muted · text-accent (coral, PRECIOUS — sparingly) · bg-surface · bg-surface-elevated · border-border (6%). FORBIDDEN: glass/glow/backdrop-blur, #FF7F50, hardcoded hex.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the HowItWorks RSC section + export it from the barrel</name>
  <behavior>
    - Test 1 (STORY-01): renders exactly 3 steps in order (paste link → audience simulates → get your Simulation), each with a `<Placeholder>` (3 `[data-variant]` roots).
    - Test 2 (noun discipline): copy contains `/simulat(?:es|ion|e)/i`; "reading" never appears as a user-facing product noun.
    - Test 3 (Success Crit 4): each Placeholder has an inline aspect-ratio (no-CLS) and a label.
    (These tests already exist RED from 03-00 — this task turns them GREEN.)
  </behavior>
  <read_first>
    - src/components/marketing/hero/hero.tsx (the canonical pure-RSC marketing-section + Placeholder + token pattern — NO "use client" directive; coral only on the CTA)
    - src/components/marketing/placeholder.tsx (the slot API: variant/aspect/label/src; data-variant hook; inline aspect-ratio = no-CLS)
    - src/components/motion/stagger-reveal.tsx (the client leaf; self-gates reduced-motion; StaggerReveal.Item is a static prop)
    - src/components/marketing/index.ts (barrel — line 7 `export { Hero } from "./hero"` is the convention to mirror)
    - src/components/marketing/story/__tests__/how-it-works.test.tsx (the RED gate this task satisfies)
    - .planning/phases/03-story-showcase/03-RESEARCH.md § Pattern 3 + § Code Examples "Section component shape" (the 3-step stagger pattern, copy guidance, sans-heading recommendation A3)
    - .planning/phases/03-story-showcase/03-PATTERNS.md § how-it-works.tsx (read_first list + serif-vs-sans resolution + StaggerReveal.Item idiom)
  </read_first>
  <files>src/components/marketing/story/how-it-works.tsx, src/components/marketing/index.ts</files>
  <action>
    Create `src/components/marketing/story/how-it-works.tsx` as a PURE RSC — NO `"use client"` directive anywhere in this file (Pitfall 5; keeps `/` static). Export `function HowItWorks({ className }: { className?: string })`.

    Imports: `StaggerReveal` from `@/components/motion`; `Placeholder` from `@/components/marketing/placeholder`; `cn` from `@/lib/utils` only if a className merge is needed.

    Define a module-level `STEPS` const of exactly 3 entries, in this order:
      1. title "Paste a TikTok link", one Inter line (e.g. drop any TikTok URL — no upload, no waiting), slot `{ variant: "image", aspect: "16/10", label: "Paste a link" }`.
      2. title "The audience simulates", one Inter line (a synthetic audience reacts to your video, frame by frame), slot `{ variant: "image", aspect: "16/10", label: "Audience simulating" }`.
      3. title "Get your Simulation", one Inter line naming the real outputs (a score, watch-through %, and where viewers drop — before you post), slot `{ variant: "image", aspect: "16/10", label: "Your Simulation" }`.
    Per D-A noun discipline: use "Simulation"/"simulates" as the product noun/verb; NEVER "reading" as a user-facing product noun. "your score"/"your prediction" are also safe synonyms.

    Render: a section heading `<h2>` reading "How it works" using SANS `font-semibold text-foreground` (per D-C / A3 — keep the Newsreader serif precious to the hero; do NOT use font-serif here). Below it, a `<StaggerReveal className="mt-12 grid grid-cols-1 gap-8 md:grid-cols-3">` mapping STEPS to `<StaggerReveal.Item key={s.n} className="flex flex-col gap-4">` — each containing a small cream-muted numeral (`text-foreground-muted`, e.g. `0{n}` mono), the `<Placeholder {...s.slot} />`, a `<h3 className="text-lg font-semibold text-foreground">` title, and a `<p className="text-base text-foreground-secondary">` body. Responsive by construction: `grid-cols-1 md:grid-cols-3`, no fixed pixel widths (Pitfall 6 — mobile-sane, NOT a Phase-5 hardening pass). Coral is NOT used in this section (keep it precious — A6).

    Then add to `src/components/marketing/index.ts`: `export { HowItWorks } from "./story/how-it-works";` (mirror the `export { Hero }` convention, line 7).

    Do NOT import anything from `board/*`, `reading/*`, `viral-results/*`, or any engine/data hook (anti-pattern guard).
  </action>
  <verify>
    <automated>npx vitest run src/components/marketing/story/__tests__/how-it-works.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/marketing/story/__tests__/how-it-works.test.tsx` exits 0 (all STORY-01 cases GREEN).
    - `grep -L '"use client"' src/components/marketing/story/how-it-works.tsx` returns the file (NO client directive present).
    - `grep -c 'Placeholder' src/components/marketing/story/how-it-works.tsx` ≥ 1 and the component renders exactly 3 Placeholder slots (asserted by the test's `[data-variant]` count === 3).
    - `grep -iq '\breading\b' src/components/marketing/story/how-it-works.tsx` returns NOTHING (no user-facing "reading" product noun).
    - `grep -q "HowItWorks" src/components/marketing/index.ts` (barrel export added).
    - No import from `@/components/board`, `@/components/reading`, or `viral-results` (`grep -Eq 'board/|reading/|viral-results' src/components/marketing/story/how-it-works.tsx` returns nothing).
  </acceptance_criteria>
  <done>HowItWorks renders 3 ordered steps with Placeholder slots, sans heading, noun-disciplined copy; the 03-00 how-it-works test is GREEN; barrel exports it; no "use client", no banned imports.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the #how-it-works stub in page.tsx with <HowItWorks/></name>
  <read_first>
    - src/app/(marketing)/page.tsx (the current skeleton — the `#how-it-works` section lines 39-48 + the `@/components/marketing` named import line 3)
    - src/components/marketing/index.ts (now exports HowItWorks — the import source)
    - .planning/phases/03-story-showcase/03-PATTERNS.md § page.tsx "Pattern 1 — fill stub IN PLACE" (the exact before/after; preserve id/border/padding/measure)
    - .planning/phases/03-story-showcase/03-RESEARCH.md § Pitfall 3 (LOCKED section rhythm: border-t border-border px-6 py-20 + inner mx-auto max-w-5xl)
  </read_first>
  <files>src/app/(marketing)/page.tsx</files>
  <action>
    In `page.tsx`: add `HowItWorks` to the existing named import from `@/components/marketing` (line 3, currently `import { Hero, MotionConfigShell } from "@/components/marketing";`). Inside the EXISTING `<section id="how-it-works" className="border-t border-border px-6 py-20">` → `<div className="mx-auto max-w-5xl">`, REPLACE the muted stub `<h2 className="text-3xl font-semibold text-foreground-muted">How it works</h2>` with `<HowItWorks />`. The section component renders its OWN real cream heading + body.

    Do NOT change the `<section>` element, its `id`, its `border-t border-border px-6 py-20` classes, or the inner `mx-auto max-w-5xl` (LOCKED rhythm, D-E / Pitfall 3 — preserves anchor-nav, the border rhythm, and no-CLS). Touch ONLY the `#how-it-works` section; leave `#the-simulation`, `#pricing`, `#faq`, and the hero untouched (those are 03-02/03 or out of scope).
  </action>
  <verify>
    <automated>grep -q "<HowItWorks" "src/app/(marketing)/page.tsx" && grep -q 'id="how-it-works" className="border-t border-border px-6 py-20"' "src/app/(marketing)/page.tsx" && grep -q "HowItWorks" "src/app/(marketing)/page.tsx" && npx vitest run src/components/marketing/story/__tests__/how-it-works.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` imports `HowItWorks` from `@/components/marketing` and renders `<HowItWorks />` inside `<section id="how-it-works">`.
    - The `#how-it-works` section wrapper is unchanged: `grep -q 'id="how-it-works" className="border-t border-border px-6 py-20"' "src/app/(marketing)/page.tsx"` matches.
    - The muted stub `<h2 ...text-foreground-muted>How it works</h2>` is gone from that section.
    - `#the-simulation`, `#pricing`, `#faq`, and `#hero` sections are byte-for-byte unchanged.
    - `npx vitest run src/components/marketing/story/__tests__/how-it-works.test.tsx` still exits 0.
  </acceptance_criteria>
  <done>The `#how-it-works` stub is filled in place with `<HowItWorks/>`; the LOCKED section rhythm and all other sections are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Static presentational section. No user input rendered, no network, no data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | how-it-works.tsx copy | accept | All copy is static literals; never `dangerouslySetInnerHTML`; no user input rendered (RESEARCH § Security Domain — surface nil). |
| T-03-01-02 | Tampering | imports | mitigate | No import from board/reading/viral-results or engine/data hooks (anti-pattern guard); avoids dragging store coupling. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed (RESEARCH § Package Legitimacy Audit). No install task. |
</threat_model>

<verification>
- `npx vitest run src/components/marketing/story/__tests__/how-it-works.test.tsx` → exits 0 (STORY-01 GREEN).
- Per-wave merge (run after this plan): `npm test` full suite green + `npm run build` exit 0 with route table `○ /` (the build/static gate is owned by 03-03's BLOCKING gate, but a quick `npm run build` here is wise since this is the first page.tsx edit of the phase).
</verification>

<success_criteria>
- HowItWorks RSC renders 3 ordered steps, each with a labelled aspect-locked `<Placeholder>`.
- Copy is noun-disciplined ("Simulation"/"simulates"; no "reading" product noun).
- Section heading is sans `font-semibold` (serif reserved to hero).
- `#how-it-works` stub filled in place; LOCKED rhythm + other sections untouched.
- Barrel exports HowItWorks; no "use client"; no banned imports.
- The 03-00 how-it-works gate is GREEN.
</success_criteria>

<output>
Create `.planning/phases/03-story-showcase/03-01-SUMMARY.md` when done.
</output>
