---
phase: 03-story-showcase
plan: 04
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/components/marketing/story/skeletons/score-gauge-skeleton.tsx
  - src/components/marketing/story/skeletons/audience-cloud-skeleton.tsx
  - src/components/marketing/story/skeletons/driver-rows-skeleton.tsx
  - src/components/marketing/story/skeletons/device-chrome.tsx
  - src/components/marketing/story/skeletons/index.ts
  - src/components/marketing/story/__tests__/skeletons.test.tsx
  - src/components/marketing/placeholder.tsx
autonomous: true
requirements: [STORY-01, STORY-02, STORY-03]
must_haves:
  truths:
    - "Reusable static-SVG product-skeleton primitives exist (score-gauge arc, audience dot-cloud, Hook·Retention·Shareability driver rows, device chrome) that hint the real Numen Simulation shape (GAP-1, GAP-2)"
    - "Each skeleton primitive is a PURE Server Component (no \"use client\") with zero animation and flat-warm tokens only (GAP-1)"
    - "No marketing <Placeholder> renders the literal \"16:10\" (or any \\d+:\\d+) developer ratio label (GAP-1)"
  artifacts:
    - path: "src/components/marketing/story/skeletons/score-gauge-skeleton.tsx"
      provides: "Static 270deg arc + score number (>=70) + band word skeleton"
      contains: "<svg"
    - path: "src/components/marketing/story/skeletons/audience-cloud-skeleton.tsx"
      provides: "Static dot-cloud (>=6 dots) + watch-through caption skeleton"
      contains: "<svg"
    - path: "src/components/marketing/story/skeletons/driver-rows-skeleton.tsx"
      provides: "Three driver rows (Hook/Retention/Shareability) with neutral bars + drop timestamp"
      contains: "Retention"
    - path: "src/components/marketing/story/skeletons/device-chrome.tsx"
      provides: "Browser-window + phone chrome wrappers reused across sections"
    - path: "src/components/marketing/story/skeletons/index.ts"
      provides: "Barrel for the four skeleton primitives"
    - path: "src/components/marketing/placeholder.tsx"
      provides: "Marketing stand-in with the ratio label removed"
  key_links:
    - from: "src/components/marketing/story/skeletons/index.ts"
      to: "./score-gauge-skeleton + ./audience-cloud-skeleton + ./driver-rows-skeleton + ./device-chrome"
      via: "barrel re-export"
      pattern: "export.*Skeleton"
---

<objective>
Build the reusable, static-SVG **product-skeleton primitives** that turn the
Phase-3 placeholders from "flat dark boxes" into intentional product skeletons
hinting the real Numen Simulation shape (GAP-1, GAP-2), and remove the
developer-artifact "16:10" ratio label from the marketing `<Placeholder>`
(GAP-1).

This plan is purely additive (new files) plus one surgical edit to
`placeholder.tsx`. It owns NO story-section files — 03-05 consumes these
primitives in Wave 2. The skeleton shapes are derived from the canonical
numen-rework reading IA (03-RESEARCH Pitfall 4): score gauge, audience dot-cloud
+ watch-through caption, three driver rows (Hook · Retention · Shareability).

Purpose: Give the stubs an intentional skeleton vocabulary so they read as
"this is the real product, screenshot pending" — not "unfinished" — while
honoring the STUB LOCK (no real data, no real screenshots, no engine/data
wiring).
Output: 4 skeleton primitive components + a barrel + a Nyquist test file, and a
ratio-label-free marketing Placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-story-showcase/03-VERIFICATION.md

<!-- Flat-warm token contract (globals.css) the skeletons MUST use, NO hardcoded hex except commented depth rgba: -->
<!-- bg-surface (#1e1d1b) · bg-surface-elevated (#2f2e2b) · bg-background (#262624) -->
<!-- border-border (white/6%) · text-foreground / -secondary / -muted (cream #ece7de/#c2bdb4/#8a857c) -->
<!-- text-accent / fill-accent → coral (--color-accent ≈ #d97757), used ONLY for the single worst/weakest hint, kept precious -->
<!-- radius: rounded-[--radius-lg] = 12px · NO glass, NO glow, NO backdrop-blur, NO #FF7F50, NO animation -->

<interfaces>
<!-- Canonical reading IA the skeletons depict (03-RESEARCH Pitfall 4, VERIFIED against ~/virtuna-numen-rework). DEPICT the shape inside SVG; do NOT import any of it. -->
<!-- 1. score gauge: 270deg stroked arc + number (KEEP >=70, honesty floor BAND_THRESHOLDS.STRONG=70) + band word "Strong" -->
<!-- 2. audience persona cloud: dot-cloud, one dot per persona; a "{n}% watch-through" caption beside it -->
<!-- 3. three driver rows: Hook · Retention · Shareability (fixed order, neutral bars; Retention shows a drop timestamp e.g. "0:07") -->

<!-- existing Placeholder render (placeholder.tsx) — the ratio label to REMOVE lives at the formatDimensionHint() span: -->
<!--   <span className="font-mono text-xs ...">{formatDimensionHint(resolvedAspect)}</span>  // renders "16:10" -->
<!-- formatDimensionHint() + its usage are the ONLY thing this plan removes from placeholder.tsx. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Static-SVG product-skeleton primitives (score gauge, audience cloud, driver rows, device chrome)</name>
  <read_first>
    - src/components/marketing/story/simulation-showcase.tsx (the device-frame chrome to factor into device-chrome.tsx — browser bar + 3 dots + numen.app pill)
    - src/components/marketing/hero/hero.tsx (the phone chrome pattern + the layered dark shadow / warm-seat idiom, lines 98-150)
    - src/app/globals.css (flat-warm tokens — confirm bg-surface-elevated, border-border, text-foreground-muted, --color-accent, --radius-lg)
    - .planning/phases/03-story-showcase/03-RESEARCH.md (Pitfall 4 — the canonical reading IA the skeletons depict)
  </read_first>
  <behavior>
    - score-gauge-skeleton renders ONE <svg> containing a <path> (or <circle> with stroke-dasharray) describing a ~270deg arc, a score number text node that is >= 70, and a band word "Strong"
    - audience-cloud-skeleton renders ONE <svg> containing >= 6 <circle> dots, with at most ONE dot tinted with the coral accent (worst-cluster hint), plus a visible watch-through caption containing the "%" character
    - driver-rows-skeleton renders exactly 3 labelled rows reading "Hook", "Retention", "Shareability" (in that order), each with a bar element (<rect> or a div bar), and the Retention row carries a drop timestamp matching /\d:\d{2}/ (e.g. "0:07")
    - device-chrome exports a BrowserChrome wrapper (slim bar + 3 dots + "numen.app" pill, children = window body) and a PhoneChrome wrapper (rounded phone bezel, children = screen) — both pure presentational wrappers
    - every primitive is a pure RSC: NO "use client" directive anywhere in src/components/marketing/story/skeletons/
  </behavior>
  <action>
    Create `src/components/marketing/story/skeletons/` with four pure-RSC files
    and a barrel:

    1. `score-gauge-skeleton.tsx` — export `ScoreGaugeSkeleton`. Draw a static
       270deg arc via an SVG `<path>` (or a `<circle>` with `stroke-dasharray`)
       using `stroke` from a flat-warm token (track = `border-border`/cream-muted,
       filled portion = `text-foreground-secondary` cream; the coral accent stays
       precious — use it only if depicting a single emphasis tick, otherwise
       cream only). Center a score NUMBER text node — pick a fixed value **87**
       (>= 70 honesty floor per 03-RESEARCH Pitfall 4) — and a band word
       "Strong". `role="img"` + `aria-label="Virality score (sample)"`. No
       animation.
    2. `audience-cloud-skeleton.tsx` — export `AudienceCloudSkeleton`. Draw an
       SVG dot-cloud of **>= 16** `<circle>` elements at deterministic
       hand-authored coordinates (NO Math.random — avoid any hydration concern;
       hardcode a small coordinate array). Fill dots with cream-muted at low
       opacity; tint EXACTLY ONE dot with `fill-accent` (the worst-cluster hint,
       coral kept precious). Beside/under the cloud render a caption
       "68% watch-through" (contains "%"). `role="img"` +
       `aria-label="Audience reaction (sample)"`.
    3. `driver-rows-skeleton.tsx` — export `DriverRowsSkeleton`. Render exactly
       three rows in fixed order: "Hook", "Retention", "Shareability". Each row =
       a label + a neutral horizontal bar (a div with a flat-warm fill width %,
       or an SVG `<rect>`). On the "Retention" row add a small drop-timestamp
       caption "drops at 0:07" (matches /\d:\d{2}/). Bars are neutral
       (cream-muted track + cream-secondary fill); the single weakest bar MAY use
       the coral accent — keep it to at most one. No numbers above 100, no
       invented metrics. `role="img"` + `aria-label="Hook, Retention and
       Shareability (sample)"`.
    4. `device-chrome.tsx` — export `BrowserChrome` and `PhoneChrome` pure
       presentational wrappers. `BrowserChrome` = the slim browser bar (3
       `bg-foreground-muted/30` dots + a "numen.app" mono pill, optically
       centered) over an `overflow-hidden rounded-xl border border-border
       bg-surface-elevated` window that wraps `children`; carry the existing
       layered DARK drop shadow idiom from simulation-showcase.tsx line 97 (a
       commented flat-warm-legal `shadow-[...]`, NOT a glow). `PhoneChrome` = the
       rounded phone bezel from hero.tsx lines 141-149 (border-[5px]
       border-background-elevated, ring-1 ring-border, deep dark shadow) wrapping
       `children`. These let 03-05 reuse the chrome without re-importing the hero.
    5. `index.ts` — barrel re-exporting `ScoreGaugeSkeleton`,
       `AudienceCloudSkeleton`, `DriverRowsSkeleton`, `BrowserChrome`,
       `PhoneChrome`.

    STUB LOCK: these are static SVG/markup shape hints only — no real data, no
    `src`, no engine/data hook, no import from the retired product UI
    (board/reading/viral-results). Flat-warm tokens only; no glass/glow/blur/
    #FF7F50; ZERO animation (no animate-*). Product noun in any visible text =
    "Simulation"; never "reading".
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && grep -rL '"use client"' src/components/marketing/story/skeletons/*.tsx | wc -l | grep -q 4 && grep -RhoE 'animate-[a-z-]+' src/components/marketing/story/skeletons/ | grep -v '^$' | wc -l | grep -q '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/components/marketing/story/skeletons/` lists score-gauge-skeleton.tsx, audience-cloud-skeleton.tsx, driver-rows-skeleton.tsx, device-chrome.tsx, index.ts
    - `grep -rl '"use client"' src/components/marketing/story/skeletons/` returns NOTHING (all pure RSC)
    - `grep -RhoE 'animate-[a-z-]+' src/components/marketing/story/skeletons/` returns NOTHING (zero animation)
    - `grep -RnE 'backdrop-blur|#FF7F50|\bglass\b|drop-shadow|blur\(' src/components/marketing/story/skeletons/` returns NOTHING (excluding the commented flat-warm-legal shadow)
    - `grep -rn '\breading\b' src/components/marketing/story/skeletons/` returns NOTHING in user-facing text
    - driver-rows-skeleton.tsx contains the strings "Hook", "Retention", "Shareability" and a timestamp matching /\d:\d{2}/
    - score-gauge-skeleton.tsx renders a number >= 70 (grep for "87") and the word "Strong"
  </acceptance_criteria>
  <done>The four skeleton primitives + barrel exist, are pure RSC, zero-animation, flat-warm, and depict the canonical reading IA shapes (GAP-1, GAP-2 primitives ready for 03-05).</done>
</task>

<task type="auto">
  <name>Task 2: Remove the "16:10" dev ratio label from the marketing Placeholder + add skeleton Nyquist tests</name>
  <read_first>
    - src/components/marketing/placeholder.tsx (the formatDimensionHint span at lines 172-174 to remove; keep aspectRatio inline style intact — it is the no-CLS lock)
    - src/components/marketing/__tests__/placeholder.test.tsx (KEEP GREEN — confirms the change does not break the stand-in/swap/aspect/breathe tests)
    - src/components/marketing/story/skeletons/index.ts (the primitives Task 1 created — import for the test)
  </read_first>
  <action>
    1. In `placeholder.tsx`: DELETE the dimension-hint `<span>` that renders
       `{formatDimensionHint(resolvedAspect)}` (the `font-mono text-xs ...`
       caption that shows "16:10"), and remove the now-unused
       `formatDimensionHint` helper function. KEEP everything else: the
       `resolvedAspect` computation and the inline `aspectRatio` style on the
       root (that is the no-CLS lock — do NOT touch it), the variant icon, the
       label caption, and the `src` swap. OPTIONAL (IN-04, only because the file
       is open): simplify `const resolvedVariant = variant ?? "image"` to
       `const resolvedVariant = variant` since the destructure default already
       guarantees a value — leave the rest of the API unchanged.
    2. Create `src/components/marketing/story/__tests__/skeletons.test.tsx`
       (`/** @vitest-environment happy-dom */`). Render each primitive and assert
       the structural skeleton contract: ScoreGaugeSkeleton has an `<svg>` with a
       `path` or `circle` and a number text >= 70; AudienceCloudSkeleton has an
       `<svg>` with >= 6 `circle` elements and a caption containing "%";
       DriverRowsSkeleton renders "Hook"/"Retention"/"Shareability" and a
       /\d:\d{2}/ timestamp; BrowserChrome renders the "numen.app" pill;
       PhoneChrome renders its children. Use `container.querySelectorAll` for
       counts and `getAllByText(...).length >= 1` where multiplicity is plausible
       (avoid strict single-match getByText — the WR-04 lesson).
  </action>
  <verify>
    <automated>cd /Users/davideloreti/virtuna-landing-v2 && npx vitest run src/components/marketing/__tests__/placeholder.test.tsx src/components/marketing/story/__tests__/skeletons.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -RnE '\b\d+:\d+\b' src/components/marketing/placeholder.tsx` returns NOTHING (the "16:10"-style label is gone)
    - `grep -n 'formatDimensionHint' src/components/marketing/placeholder.tsx` returns NOTHING (helper removed)
    - `grep -n 'aspectRatio' src/components/marketing/placeholder.tsx` STILL returns the inline-style line (no-CLS lock intact)
    - `npx vitest run src/components/marketing/__tests__/placeholder.test.tsx` is GREEN (existing 11 placeholder tests still pass — the stand-in/swap/aspect/breathe contract is unbroken)
    - `npx vitest run src/components/marketing/story/__tests__/skeletons.test.tsx` is GREEN (new skeleton structure gates pass)
  </acceptance_criteria>
  <done>Marketing placeholders no longer show the "16:10" dev label (GAP-1), the no-CLS aspect lock + existing placeholder tests stay GREEN, and the skeleton primitives are covered by a Nyquist gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | No new boundary — static SVG + presentational wrappers, no user input |

## STRIDE Threat Register

No new attack surface: these are static SVG skeleton primitives + a label
removal in an existing presentational component. No user input, no data flow, no
network, no secrets. ASVS L1 — nothing to mitigate beyond existing project
conventions.
</threat_model>

<verification>
- `npx vitest run src/components/marketing/` is GREEN (placeholder + skeleton suites)
- `grep -rl '"use client"' src/components/marketing/story/skeletons/` is empty (pure RSC)
- No `\d+:\d+` ratio label rendered by placeholder.tsx
- `npm run build` exits 0 and the `/` route is `○` static (no client leak introduced) — re-confirmed downstream in 03-05/03-06 after wiring
</verification>

<success_criteria>
- 4 skeleton primitives + barrel exist, pure RSC, zero-animation, flat-warm, depicting the canonical reading IA (GAP-1, GAP-2 primitives ready)
- placeholder.tsx no longer renders the "16:10" dev label; no-CLS aspect lock + existing tests intact (GAP-1)
- New skeletons Nyquist gate GREEN; placeholder gate still GREEN
</success_criteria>

<output>
Create `.planning/phases/03-story-showcase/03-04-SUMMARY.md` when done
</output>
