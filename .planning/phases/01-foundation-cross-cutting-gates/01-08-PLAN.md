---
phase: 01-foundation-cross-cutting-gates
plan: "08"
type: execute
wave: 2
depends_on:
  - "00"
files_modified:
  - .planning/SECTION-BRIEF-TEMPLATE.md
autonomous: true
requirements:
  - FOUND-14
tags:
  - foundation
  - section-brief
  - anti-slop
must_haves:
  truths:
    - "`.planning/SECTION-BRIEF-TEMPLATE.md` exists with the 7 mandatory subsections from D-04: Purpose, Audience, Content, Interaction goals, Success criteria, Anti-slop list for this section, Reference anchors."
    - "Each H2 header is parseable by the gsd-ui-checker (numbered prefix `## N. ...`) so a brief can be machine-validated for completeness."
    - "The template instructs that briefs are written per-phase, lazy — Phase 2-7 each write their own brief at `${phase_dir}/${padded_phase}-SECTION-BRIEF.md` before any markup (D-03)."
  artifacts:
    - path: ".planning/SECTION-BRIEF-TEMPLATE.md"
      provides: "7-subsection brief template for per-phase section authoring"
      contains: "## 1. Purpose"
  key_links:
    - from: "SECTION-BRIEF-TEMPLATE.md"
      to: "${phase_dir}/${padded_phase}-SECTION-BRIEF.md (Phase 2-7)"
      via: "template copy + fill per phase"
      pattern: "## [1-7]\\."
---

<objective>
Create `.planning/SECTION-BRIEF-TEMPLATE.md` — the 7-subsection brief template that EVERY Phase 2-7 section must complete before any markup is written. This is the primary derivative-feel prevention mechanism per D-03 and FOUND-14.

Purpose: A section without a written brief CANNOT ship. The gsd-ui-researcher rejects any phase that begins execution without a filled brief at `${phase_dir}/${padded_phase}-SECTION-BRIEF.md`. The template encodes the 7 sections (D-04): Purpose, Audience, Content, Interaction goals, Success criteria, Anti-slop list, Reference anchors.

Output: One Markdown template file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-cross-cutting-gates/01-CONTEXT.md
@.planning/phases/01-foundation-cross-cutting-gates/01-RESEARCH.md
@.planning/phases/01-foundation-cross-cutting-gates/01-PATTERNS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- D-04 (CONTEXT.md): section brief template must include 7 subsections -->
1. Purpose in Virtuna's narrative
2. Audience served (creator / investor / partner)
3. Content — original Virtuna copy
4. Interaction goals
5. Success criteria
6. Anti-slop list for this section
7. Reference anchors

<!-- D-03 (CONTEXT.md): per-phase lazy authoring -->
Phase 1: writes TEMPLATE only (this plan)
Phase 2-7: each writes filled brief at ${phase_dir}/${padded_phase}-SECTION-BRIEF.md

<!-- Machine-parseable structure (RESEARCH §12 line 1267-1271): -->
The gsd-ui-checker verifies:
- All 7 sections present (regex: `^## \d\. `)
- Section 3 has ≥ 1 line of copy
- Section 6 cites ≥ 1 AS-XX ID
- Section 7 cites both linear.app AND raycast.com
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write .planning/SECTION-BRIEF-TEMPLATE.md with 7 numbered subsections</name>
  <files>.planning/SECTION-BRIEF-TEMPLATE.md</files>
  <read_first>
    - RESEARCH.md §12 lines 1195-1281 (full template skeleton — copy verbatim)
    - PATTERNS.md "SECTION-BRIEF-TEMPLATE.md" entry (lines 467-512) — analog (CONTEXT.md parseable markdown structure) + full target skeleton
    - CONTEXT.md D-03 + D-04 (per-phase lazy authoring + 7-subsection requirement)
    - .planning/REQUIREMENTS.md — for audience definitions (Creator / Investor / Partner)
    - .planning/PROJECT.md — Virtuna narrative source for inline guidance text
  </read_first>
  <action>
Create the file `.planning/SECTION-BRIEF-TEMPLATE.md` with the EXACT content below (verbatim from RESEARCH §12 lines 1200-1262 / PATTERNS lines 473-509). Use the Write tool.

```markdown
# Section Brief — [Section Name]

**Phase:** [N — phase name]
**Owner:** Davide (narrative) + Claude (execution)
**Created:** [YYYY-MM-DD]
**Status:** Draft / Approved / Locked

---

## 1. Purpose in Virtuna's narrative

[ONE paragraph — Why does this section exist in Virtuna's product story? Must be derivable from Virtuna's narrative alone (prediction / competitor intel / brand deals / behavioral science / pricing) without reference to linear.app. If the answer is "because Linear has a section here", REWRITE.]

## 2. Audience served

[Pick one or more — Creator (primary conversion), Investor (brand impression), Partner (brand-deal eligibility signal)]

[Then 1-2 sentences on what THIS audience needs to see/feel/understand to take the next step.]

## 3. Content — original Virtuna copy

- **Eyebrow / pre-headline:** [exact text]
- **H1 / section headline:** [exact text]
- **Sub-headline / supporting copy:** [exact text]
- **CTAs:** [exact button text + destination]
- **Body content:** [bullet list, table rows, card content — written fresh]

Copy rule: every word original to Virtuna. No language lifted from linear.app, raycast.com, or prior abandoned milestones.

## 4. Interaction goals

- **Default state:** [what the user sees]
- **Hover / focus state:** [what changes]
- **Active / click state:** [what triggers]
- **Scroll behavior:** [reveal animation? sticky? once-only?]
- **Reduced-motion fallback:** [what the user sees with `prefers-reduced-motion: reduce`]

## 5. Success criteria

- [ ] Visible at 375 / 768 / 1280 (no horizontal overflow)
- [ ] Lighthouse mobile section LCP / CLS within phase budget
- [ ] Section copy delivered from this brief — no agent ad-libbing
- [ ] [section-specific gate]
- [ ] [section-specific gate]

## 6. Anti-slop list for THIS section

Which of AS-01..AS-15 are highest risk here? List them by ID and document the specific guard.

Example:
- **AS-02** (3-column equal grid) — this is a bento section, must be mixed-size 2×2+1×1+1×1. NOT 3 equal cards.
- **AS-08** (gradient orb) — card visuals are real Virtuna product UI fragments, NEVER abstract shapes.
- **AS-15** (section-level motion wrapper) — staggers are leaf-level on cards, NOT a `<m.section>` wrapping everything.

## 7. Reference anchors

- **linear.app:** [URL path + specific section] — anchored craft observation (NOT layout to copy)
  e.g. "linear.app/features feature-section — observe: 32px gap between card and image, 12px card radius, no decorative borders"
- **raycast.com:** [URL path + specific section] — anchored craft observation
  e.g. "raycast.com hero — observe: monochrome chrome, single accent CTA, generous 96px+ vertical rhythm"
- **DESIGN.md citation:** [path + line range from .planning/reference/design-md/{linear|raycast}.md]
  e.g. ".planning/reference/design-md/linear.md lines 45-67 (colors block)"

Reference anchors are for CRAFT calibration only — never for structural reproduction.

---

*Brief written before any markup. Approved by Davide before Phase N execution begins.*

---

## Authoring rules

- **Per-phase, lazy** (D-03). Phase 1 writes only this template. Phases 2-7 each write their own brief at `.planning/phases/${padded_phase}-${slug}/${padded_phase}-SECTION-BRIEF.md` BEFORE any markup is written.
- **Machine-parseable.** The gsd-ui-checker verifies:
  - All 7 numbered sections present (`^## \d\. `)
  - Section 3 has ≥ 1 line of copy (NOT empty)
  - Section 6 cites ≥ 1 `AS-XX` ID
  - Section 7 cites both `linear.app` AND `raycast.com`
- **Owner.** Davide writes section 1, 2, 3, 7 (narrative + audience + original copy + reference anchors). Claude writes section 4, 5, 6 (interaction + success criteria + section-specific anti-slop guards), drafts proposals for section 1-3 only when Davide explicitly delegates.
- **Approval gate.** Status must transition Draft → Approved (signed off by Davide) before Phase N execution begins. Status "Locked" means no further edits without CONTEXT.md amendment.
```

Key constraints (per RESEARCH §12 + PATTERNS):
- Section H2 headers use the EXACT form `## N. Title` (numeric prefix is parseable; deviating breaks the gsd-ui-checker regex).
- Section 6 example MUST cite at least one `AS-XX` ID by number (the example uses AS-02, AS-08, AS-15).
- Section 7 example MUST mention both `linear.app` and `raycast.com` explicitly.
- The "Authoring rules" trailer is informational; it gives Phase 2-7 authors a clear contract for how to use the template.
  </action>
  <verify>
    <automated>test -f .planning/SECTION-BRIEF-TEMPLATE.md && grep -q '^# Section Brief — \[Section Name\]$' .planning/SECTION-BRIEF-TEMPLATE.md && [ "$(grep -cE '^## [1-7]\. ' .planning/SECTION-BRIEF-TEMPLATE.md)" -eq 7 ] && grep -q '^## 1\. Purpose in Virtuna' .planning/SECTION-BRIEF-TEMPLATE.md && grep -q '^## 2\. Audience served$' .planning/SECTION-BRIEF-TEMPLATE.md && grep -q '^## 3\. Content — original Virtuna copy$' .planning/SECTION-BRIEF-TEMPLATE.md && grep -q '^## 4\. Interaction goals$' .planning/SECTION-BRIEF-TEMPLATE.md && grep -q '^## 5\. Success criteria$' .planning/SECTION-BRIEF-TEMPLATE.md && grep -q '^## 6\. Anti-slop list for THIS section$' .planning/SECTION-BRIEF-TEMPLATE.md && grep -q '^## 7\. Reference anchors$' .planning/SECTION-BRIEF-TEMPLATE.md && grep -q 'AS-' .planning/SECTION-BRIEF-TEMPLATE.md && grep -q 'linear.app' .planning/SECTION-BRIEF-TEMPLATE.md && grep -q 'raycast.com' .planning/SECTION-BRIEF-TEMPLATE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/SECTION-BRIEF-TEMPLATE.md`
    - Top heading is `# Section Brief — [Section Name]`
    - EXACTLY 7 H2 headers match the regex `^## [1-7]\. ` (one each numbered 1 through 7)
    - The 7 sections have the exact titles: `1. Purpose in Virtuna's narrative`, `2. Audience served`, `3. Content — original Virtuna copy`, `4. Interaction goals`, `5. Success criteria`, `6. Anti-slop list for THIS section`, `7. Reference anchors`
    - File mentions at least one `AS-` ID (section 6 example)
    - File mentions both `linear.app` and `raycast.com` (section 7 example)
    - File mentions `.planning/reference/design-md/` path (DESIGN.md citation row)
  </acceptance_criteria>
  <done>
    Section brief template is the canonical authoring contract for every Phase 2-7 section. The gsd-ui-checker can parse a filled brief deterministically. Phase 2-7 starts MUST produce a brief at `${phase_dir}/${padded_phase}-SECTION-BRIEF.md` before any markup.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template document ↔ Phase 2-7 agents | Every Phase 2-7 brief is forked from this template. A malformed template (missing section, wrong numbering) cascades into every phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08-01 | Tampering | template header format drift | mitigate | Acceptance criteria require exact `^## [1-7]\. ` regex match — any deviation fails the verify step |
</threat_model>

<verification>
- 7 numbered H2 sections present
- AS-XX, linear.app, raycast.com all referenced in the appropriate sections
- Template is machine-parseable by the regex specified in RESEARCH §12 line 1267
</verification>

<success_criteria>
- Phase 2 (Nav + Hero) can fork this template into `.planning/phases/02-nav-hero/02-SECTION-BRIEF.md` and fill it with Davide's narrative.
- gsd-ui-checker accepts the brief if-and-only-if all 7 sections are present + section 3 non-empty + section 6 cites AS-XX + section 7 cites linear.app and raycast.com.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cross-cutting-gates/01-08-SUMMARY.md` capturing:
- File path + line count
- Confirmation: 7 numbered subsections present
- Confirmation: gsd-ui-checker validation regex passes
</output>
