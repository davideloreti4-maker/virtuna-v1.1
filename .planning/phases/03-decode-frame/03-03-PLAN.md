---
phase: 03-decode-frame
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/components/board/decode/DecodeShellNode.tsx
  - src/components/board/decode/__tests__/DecodeShellNode.test.tsx
autonomous: false
requirements: [DECODE-01, DECODE-02]
must_haves:
  truths:
    - "DecodeShellNode renders all 4 beats (Hook pattern / Structure & pacing / The turn / Emotional beat) in fixed order from variants.remix.decode, on both live-stream and permalink reads"
    - "A beat with verdict:'absent'|'weak' renders muted (text-white/35) but still shows its honest body line — never hidden, never a placeholder"
    - "Both lanes render: 'What you can repeat' (bulleted) and 'What was luck / timing' (category tag + note), the luck lane always non-empty"
    - "Rendered beats + lanes contain NO advice verbs or second-person (fix/improve/should/try/consider/you) except the sanctioned 'What you can repeat' header"
    - "While decoding (phase analyzing/reconnecting/polling and no decode yet) the frame shows the honest 'Decoding structure…' state — no fake beat/bullet skeletons"
    - "On a permalink reload of a remix row (overall_score null) the frame reads permalinkData.variants.remix.decode directly (m3) — does not stay stuck loading"
  artifacts:
    - path: "src/components/board/decode/DecodeShellNode.tsx"
      provides: "Decode frame body: 4 beat blocks + 2 lanes + in-flight + error state, dual-read (stream + permalink)"
      min_lines: 80
    - path: "src/components/board/decode/__tests__/DecodeShellNode.test.tsx"
      provides: "render-from-fixture, no-advice-verb assertion, absent-beat muted render, in-flight state, permalink (m3) read"
  key_links:
    - from: "src/components/board/decode/DecodeShellNode.tsx"
      to: "src/hooks/queries/use-analysis-stream.ts + use-permalink-analysis.ts"
      via: "useAnalysisStream({ initialData: permalinkData }) + usePermalinkAnalysis"
      pattern: "useAnalysisStream"
    - from: "src/components/board/decode/DecodeShellNode.tsx"
      to: "variants.remix.decode"
      via: "row?.variants?.remix?.decode ?? permalinkData?.variants?.remix?.decode"
      pattern: "variants.*remix.*decode"
---

<objective>
Replace the `DecodeShellNode` body (currently one muted descriptor line) with the full decode UI: 4 stacked labeled beat blocks, 2 stacked lanes (repeatable + luck), an honest in-flight state, and an error state — reading `variants.remix.decode` from both the live SSE stream and a permalink reload (with the m3 fix for overall_score-null rows).

Purpose: Implements DECODE-01 (the rendered structural teardown) and DECODE-02 (the repeatable-vs-luck split + honest no-advice voice) at the UI layer, per the locked 03-UI-SPEC.md contract. This is the user-visible payoff of remix mode.
Output: modified `DecodeShellNode.tsx` + Wave-0 component test. Ends with a human-verify checkpoint for the live decode render.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-decode-frame/03-UI-SPEC.md
@.planning/phases/03-decode-frame/03-PATTERNS.md
@.planning/phases/03-decode-frame/03-RESEARCH.md
@.planning/phases/03-02-SUMMARY.md

<interfaces>
<!-- From 03-PATTERNS.md §DecodeShellNode + codebase reads. -->

From src/lib/engine/remix/decode-types.ts (Plan 01):
- `DecodeResult = { beats: DecodeBeat[]; repeatable: string[]; luck: { category: LuckCategory; note: string }[] }`
- `DecodeBeat = { id: BeatId; body: string; verdict: 'present'|'weak'|'absent' }`
- `BeatId = 'hook_pattern'|'structure_pacing'|'the_turn'|'emotional_beat'`; `BEAT_IDS` fixed-order array; `LuckCategory` = the 4 fixed values.

From src/hooks/queries/use-analysis-stream.ts: `useAnalysisStream({ initialData })` → `{ result, phase }`. NOTE m3: the short-circuit at line 127 keys on `overall_score != null` — it will NOT fire for a decode row.
From src/hooks/queries/use-permalink-analysis.ts: `usePermalinkAnalysis()` → `{ data }` (the `/api/analysis/[id]` `select('*')` row, includes `variants`).

Dual-read pattern (mirror src/components/board/content-analysis/ContentAnalysisFrame.tsx lines 64-99):
- `const { data: permalinkData } = usePermalinkAnalysis();`
- `const stream = useAnalysisStream({ initialData: permalinkData ?? null });`
- `const row = stream.result as unknown as { variants?: { remix?: { decode?: DecodeResult } } } | null;`
- `const decode = row?.variants?.remix?.decode ?? (permalinkData as {...})?.variants?.remix?.decode ?? null;` (m3 direct permalink read)
- `const isDecoding = stream.phase === 'analyzing' || stream.phase === 'reconnecting' || stream.phase === 'polling';`

Exact Raycast tokens (03-UI-SPEC.md §Typography/Color + 03-PATTERNS.md §Raycast typography):
- Beat/lane labels: `text-[11px] uppercase tracking-[0.08em] text-white/45` (inline — do NOT import VerdictNode SectionHead)
- Beat body present: `text-[13px] leading-[1.4] text-white/60`; absent|weak: `text-white/35`
- Beat separator: `border-b border-white/[0.06]` between blocks, `pb-4 last:pb-0 last:border-0`
- Lanes divider: `mt-6 border-t border-white/[0.06] pt-4`
- Luck category tag: `text-[11px] font-medium text-white/40`; luck note: `text-[13px] leading-[1.5] text-white/55`
- Repeatable bullet: `text-[13px] text-white/60 pl-3` with em-dash `—` prefix at `text-white/25`
- In-flight label: `text-[10px] uppercase tracking-[0.1em] text-white/45 motion-safe:animate-skeleton-breathe`; glyph `text-[44px] font-semibold leading-none tabular-nums text-white/25 motion-safe:animate-skeleton-breathe` "—"
- Root: `relative flex w-full flex-col gap-2` + `aria-busy={isDecoding}` + `data-testid="decode-shell"` (keep existing wrapper).

Exact copy strings (03-UI-SPEC.md §Copywriting Contract):
- Beat labels: `Hook pattern`, `Structure & pacing`, `The turn`, `Emotional beat`
- Lane headers: `What you can repeat`, `What was luck / timing`
- Luck category display map: timing_trend_moment → `timing / trend-moment`; existing_audience_reach → `existing audience reach`; algorithmic_outlier → `algorithmic outlier`; topic_zeitgeist → `topic / zeitgeist`
- In-flight: `Decoding structure…`
- Error heading: `Decode unavailable`; error body: `Structural analysis couldn't complete. The breakdown will appear when it's ready.` at `text-[13px] leading-[1.4] text-white/45`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — DecodeShellNode component test (red)</name>
  <files>src/components/board/decode/__tests__/DecodeShellNode.test.tsx</files>
  <read_first>
    - src/components/board/content-analysis/ContentAnalysisFrame.tsx (the exact dual-read render analog + how its tests mock useAnalysisStream/usePermalinkAnalysis — check for a sibling test).
    - src/components/board/decode/DecodeShellNode.tsx (current shell to replace).
    - src/lib/engine/remix/__tests__/fixtures/omni-structural.ts (Plan 01 — for reference; prefer a small inline DecodeResult fixture for component tests).
    - .planning/phases/03-decode-frame/03-UI-SPEC.md §Beat Block / §Lane / §In-Flight / §Copywriting Contract (prohibited tokens list).
    - .planning/phases/03-decode-frame/03-PATTERNS.md §DecodeShellNode (full component structure + dual-read).
  </read_first>
  <action>
    Create `DecodeShellNode.test.tsx`. Mock `useAnalysisStream` and `usePermalinkAnalysis` (vi.mock). Build an inline `DecodeResult` fixture with 4 beats (one with verdict:'absent' and a canonical absent body like "No distinct turn — it rides one continuous bit."), a repeatable list (>=2 items), and a luck list (>=1 item, category 'timing_trend_moment'). Cases: (1) renders all 4 beat labels in fixed order from a completed stream `result.variants.remix.decode`; (2) the absent beat renders its honest body and carries the muted class `text-white/35`; (3) both lane headers render and the luck category renders as `timing / trend-moment` with its note; (4) NO prohibited token appears in rendered text — assert the rendered beat bodies + luck notes + repeatable bullets text does NOT match `/\b(fix|improve|should|try|consider)\b/i` and contains no standalone "you" OUTSIDE the "What you can repeat" header; (5) in-flight: with `phase:'analyzing'` and no decode, renders `Decoding structure…` and the `—` glyph and NO beat labels; (6) permalink m3: with `useAnalysisStream` returning `{ result: { overall_score: null }, phase: 'idle' }` but `usePermalinkAnalysis` returning `{ data: { overall_score:null, variants:{ remix:{ decode } } } }`, the 4 beats still render (direct permalink read). Tests RED until Task 2.
  </action>
  <verify>
    <automated>npx vitest run src/components/board/decode/__tests__/DecodeShellNode.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists with the 6 cases above as discrete `it(...)`.
    - The no-advice-verb assertion is scoped to rendered beat bodies + luck notes + repeatable bullets (not the sanctioned lane header).
    - Tests run red (pending Task 2 implementation).
  </acceptance_criteria>
  <done>Component test encodes DECODE-01 render, DECODE-02 lanes + voice, in-flight, and m3 permalink read; red.</done>
</task>

<task type="auto">
  <name>Task 2: Implement DecodeShellNode body (beats + lanes + in-flight + error + m3)</name>
  <files>src/components/board/decode/DecodeShellNode.tsx</files>
  <read_first>
    - src/components/board/decode/DecodeShellNode.tsx (current 24-line shell — keep the `'use client'` + root wrapper, replace the `<p>` child).
    - src/components/board/content-analysis/ContentAnalysisFrame.tsx lines 64-99 (dual-read setup to copy).
    - src/components/board/verdict/VerdictNode.tsx (SectionHead classes ~line 27, VerdictSkeleton ~lines 248-261, aria-busy ~line 86 — copy the breathe markup).
    - src/components/board/_kit/FrameHero.tsx (insight `<p>` typography ~line 108).
    - .planning/phases/03-decode-frame/03-UI-SPEC.md (FULL — every typography/color/spacing/copy value is locked here).
    - src/lib/engine/remix/decode-types.ts (Plan 01 types + BEAT_IDS).
  </read_first>
  <action>
    Replace the body of `DecodeShellNode`. Add the dual-read hook setup (usePermalinkAnalysis + useAnalysisStream({ initialData: permalinkData }) → derive `decode` and `isDecoding`) per the interfaces block, with the m3 fallback that direct-reads `permalinkData.variants.remix.decode` when the stream result lacks it. Render branches: (a) `decode == null && isDecoding` → `DecodingState` (inline: `flex flex-col gap-2`, label `Decoding structure…` at the in-flight label classes + `—` glyph at the skeleton glyph classes, both `motion-safe:animate-skeleton-breathe`); (b) `decode == null && !isDecoding` after a failed/complete-null stream (phase 'error' or completed with null decode) → error state (`Decode unavailable` heading + the error body string at `text-white/45`); (c) `decode != null` → `DecodedBody`: a `BeatList` (`flex flex-col`) mapping the 4 beats IN FIXED ORDER (index by BEAT_IDS so order is deterministic regardless of array order) into `BeatBlock`s (`pb-4 last:pb-0` + `border-b border-white/[0.06] last:border-0`), each with an inline label `<span>` (11px uppercase tracking-[0.08em] text-white/45 mb-2 block) using the beat-label map and a body `<p>` at `text-[13px] leading-[1.4]` with `text-white/60` when `verdict==='present'` else `text-white/35`, `style={{ textWrap:'balance' }}`; then a `LanesDivider` (`mt-6 border-t border-white/[0.06] pt-4`) and `LanesSection` (`flex flex-col gap-4`): lane 1 `What you can repeat` header + bullet list (`flex flex-col gap-2`, each item a div `pl-3 text-[13px] text-white/60` with an em-dash prefix span `text-white/25`); lane 2 `What was luck / timing` header + `flex flex-col gap-3` of `LuckItem`s (`flex flex-col gap-1`: category tag `<span text-[11px] font-medium text-white/40>` via the LuckCategory display map, note `<p text-[13px] leading-[1.5] text-white/55>`). Keep root `relative flex w-full flex-col gap-2` + `aria-busy={isDecoding}` + `data-testid="decode-shell"`. Silently ignore any `improvement_tip` if present in data (never render advice). All copy strings EXACT per the interfaces block. No coral/accent, no hover, no colored luck styling (neutral palette per UI-SPEC §Color).
  </action>
  <verify>
    <automated>npx vitest run src/components/board/decode/__tests__/DecodeShellNode.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DecodeShellNode.test.tsx fully green; existing content-analysis component tests still green (no shared-util regression).
    - `grep -c "Hook pattern" src/components/board/decode/DecodeShellNode.tsx` and the other 3 beat labels each return >= 1; both lane headers present.
    - `grep -Ei '\b(fix|improve|should|try|consider)\b' src/components/board/decode/DecodeShellNode.tsx` returns no match inside rendered string literals (advice verbs absent from copy; allowed only in comments if any).
    - `grep -c "variants?.remix?.decode\|variants.*remix.*decode" src/components/board/decode/DecodeShellNode.tsx` returns >= 1 (m3 dual-read).
    - `grep -c "animate-skeleton-breathe" src/components/board/decode/DecodeShellNode.tsx` returns >= 1 (honest in-flight, reused treatment).
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>The Decode frame renders 4 beats + 2 lanes from variants.remix.decode (live + permalink), with honest in-flight + error states and no advice voice.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — live remix decode render</name>
  <what-built>The Decode frame body now renders 4 beat blocks + repeatable/luck lanes from a live lightweight Omni→Qwen decode path (auto-runs on remix submit, ~70–100s), plus the honest "Decoding structure…" in-flight state and permalink hydration for remix rows.</what-built>
  <how-to-verify>
    1. Run the dev server (kill + clear `.next/` first if CSS is stale, per CLAUDE.md).
    2. On the input, select "Remix a viral video" and paste a known viral non-owned TikTok URL.
    3. Confirm the Decode frame shows "Decoding structure…" with subtle motion while computing (no fake beat/bullet skeletons), and the decode completes in well under the full ~332s pipeline (expect ~70–100s).
    4. Confirm all 4 beat blocks render (Hook pattern / Structure & pacing / The turn / Emotional beat) — any genuinely-absent beat reads as an honest absence (e.g. "No distinct turn — it rides one continuous bit"), not fabricated.
    5. Confirm "What you can repeat" lists structural moves and "What was luck / timing" is NON-EMPTY with at least one fixed-taxonomy category.
    6. Confirm copy is honest teardown — no "fix/improve/should/try" and no "you" outside the repeatable lane header.
    7. Reload `/analyze/[id]` for that remix row — confirm the decode re-renders (does not stay stuck on "Decoding structure…").
    8. Check on mobile width (<768px) — same 4 beats + 2 lanes, no truncation.
    9. Confirm Raycast styling: 6% borders, neutral (no coral, no warning color on the luck lane).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (missing beat, empty luck, advice voice, stuck loading, latency, styling).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Decode payload → DOM render | `variants.remix.decode` strings (model-generated) render into the frame; React escapes by default. |
| Permalink row → frame hydration | An overall_score-null remix row must hydrate via `variants.remix != null`, not overall_score (m3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering / XSS | DecodeShellNode (render decode strings) | mitigate | Render via JSX text children only (React auto-escapes); no `dangerouslySetInnerHTML`; decode fields are Zod-validated strings (Plan 01). |
| T-03-08 | Repudiation / UX-integrity | DecodeShellNode (m3 hydration) | accept | Permalink direct-read of `variants.remix.decode` is read-only; failure mode is a graceful error/loading state, not data corruption. Covered by the m3 component test. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (milestone-locked); all UI is first-party. No install task. |
</threat_model>

<verification>
- `npx vitest run src/components/board/decode/__tests__/DecodeShellNode.test.tsx` green (4 beats fixed order, absent-beat muted honest render, both lanes, no-advice-verb, in-flight, m3 permalink).
- `npm test` full suite green (no regression on score-path board/route — SC#5).
- Human-verify checkpoint approved (live remix decode renders non-empty beats + non-empty luck; honest voice; permalink reload; mobile parity; Raycast styling).
</verification>

<success_criteria>
- For a remix-mode video, the Decode frame renders the structural teardown (DECODE-01) and the explicit repeatable-vs-luck split with a non-empty luck lane (DECODE-02), in honest "why it worked" voice (SC#4), on both live and permalink reads.
- In-flight state is honest (no fabricated content); no advice/second-person voice; grade-mode board unchanged (SC#5).
</success_criteria>

<output>
Create `.planning/phases/03-decode-frame/03-03-SUMMARY.md` when done.
</output>
