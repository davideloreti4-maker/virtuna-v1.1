---
phase: 03-decode-frame
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/api/analyze/route.ts
  - src/app/api/analyze/__tests__/decode-route.test.ts
  - src/app/api/analyze/__tests__/derive-and-drop.test.ts
autonomous: true
requirements: [DECODE-01, DECODE-02]
must_haves:
  decisions: [D-08, D-10]
  truths:
    - "D-08: Decode auto-runs on remix submit — a POST /api/analyze with mode:'remix' branches BEFORE runPredictionPipeline and BEFORE the usage_tracking upsert (no separate trigger), and the decode path never calls runPredictionPipeline and never increments usage_tracking"
    - "The decode branch runs resolveAndRehost → analyzeVideoWithOmni → runDecode, then persists variants.remix.decode via read-merge-write that preserves sibling variants.craft / filmstrip_segments"
    - "D-10: The decode row keeps overall_score:null and mode:'remix', persists to variants.remix.decode; the completion marker is variants.remix != null, never overall_score"
    - "The decode branch deletes the re-hosted temp mp4 (cleanup in finally) and never sets video_storage_path (derive-and-drop)"
    - "The DAILY_LIMITS read guard (cost-exhaustion reject) still runs on the remix branch; only the usage increment is skipped"
    - "The score path (non-remix) is byte-for-byte unchanged — runPredictionPipeline + usage_tracking + persistCraftToVariants still fire"
  artifacts:
    - path: "src/app/api/analyze/route.ts"
      provides: "remix decode branch (runDecodeStream helper) + persistDecodeToVariants read-merge-write"
      contains: "persistDecodeToVariants"
    - path: "src/app/api/analyze/__tests__/decode-route.test.ts"
      provides: "C2 (no pipeline/usage), variants.remix.decode persistence, overall_score null, mode remix"
  key_links:
    - from: "src/app/api/analyze/route.ts"
      to: "src/lib/engine/remix/decode.ts"
      via: "runDecode(omni)"
      pattern: "runDecode"
    - from: "src/app/api/analyze/route.ts"
      to: "src/lib/engine/remix/resolve-and-rehost.ts"
      via: "resolveAndRehost(tiktok_url)"
      pattern: "resolveAndRehost"
    - from: "src/app/api/analyze/route.ts (complete event)"
      to: "DecodeShellNode (Plan 03)"
      via: "SSE complete payload { id, mode:'remix', overall_score:null, variants:{remix:{decode}} }"
      pattern: "variants.*remix.*decode"
---

<objective>
Add the lightweight decode branch to `/api/analyze`: branch on `validated.mode === 'remix'` before `runPredictionPipeline` and before the `usage_tracking` upsert, run `resolveAndRehost → analyzeVideoWithOmni → runDecode`, persist to `variants.remix.decode`, and stream the `complete` event carrying the decode payload. Keep the score path untouched.

Purpose: Implements DECODE-01's lightweight-path requirement (NOT the 332s scorer, NOT usage_tracking — pitfall C2) and the persistence/streaming spine the frame reads. Guards the IP boundary (derive-and-drop, pitfall C4) and the m3 completion marker (overall_score stays null).
Output: modified `route.ts` (decode branch + persistDecodeToVariants) + Wave-0 route test + extended derive-and-drop test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-decode-frame/03-RESEARCH.md
@.planning/phases/03-decode-frame/03-PATTERNS.md
@.planning/phases/03-01-SUMMARY.md

<interfaces>
<!-- Route anchors extracted from src/app/api/analyze/route.ts (read these exact lines). -->

- Line 286: `const today = new Date().toISOString().split("T")[0]!` and line 294 `const currentCount = usage?.analysis_count ?? 0` — the DAILY_LIMITS read.
- Lines 301-310: DAILY_LIMITS / 429 cost-exhaustion guard (mode-agnostic, KEEP on remix branch).
- Line 335-337: `validated = AnalysisInputSchema.parse(body)` — `validated.mode` is available here ('remix' | 'score'/default); `validated.tiktok_url`, `validated.input_mode`.
- Line 356: `const contentHash = computeContentHash(validated)` (mode already folded, D-14).
- Lines 102-145: `persistCraftToVariants(service, id, finalResult, log)` — the read-merge-write template to copy as `persistDecodeToVariants`.
- Lines 583-608: placeholder INSERT (overall_score:null, engine_version:"pending", input_mode, mode:validated.mode, content_hash). The decode placeholder reuses this; keep video_storage_path unset.
- Lines 613-620: SSE `encoder` + `ReadableStream` + `send(event,data)` helper.
- Line 625: `send("started", { id: analysisId })`.
- Lines 629-633: `send("phase", { phase:"analyzing", message:"..." })`.
- Line 634: `runPredictionPipeline(validated, {...})` — the score-path call the remix branch MUST skip.
- Lines 709-717: `usage_tracking` upsert — the increment the remix branch MUST skip.

From src/lib/engine/remix/decode.ts (Plan 01): `runDecode(omni): Promise<DecodeResult | null>`.
From src/lib/engine/remix/resolve-and-rehost.ts (Plan 01): `resolveAndRehost(tiktokUrl): Promise<{ signedUrl, cleanup }>`.
From src/lib/engine/qwen/omni-analysis.ts: `analyzeVideoWithOmni(signedUrl): Promise<OmniAnalysisOutput>`.
From src/lib/engine/remix/decode-types.ts: `DecodeResult`, `OmniStructuralInput`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — decode-route test + extend derive-and-drop test (red)</name>
  <files>src/app/api/analyze/__tests__/decode-route.test.ts, src/app/api/analyze/__tests__/derive-and-drop.test.ts</files>
  <read_first>
    - src/app/api/analyze/__tests__/route.test.ts (route test setup: how POST handler, supabase auth, and service client are mocked).
    - src/app/api/analyze/__tests__/stream-route.test.ts (how the SSE ReadableStream is consumed + events parsed in tests).
    - src/app/api/analyze/__tests__/derive-and-drop.test.ts (existing derive-and-drop assertions — extend, don't rewrite).
    - src/lib/engine/remix/__tests__/fixtures/omni-structural.ts (Plan 01 fixture to reuse).
    - .planning/phases/03-decode-frame/03-RESEARCH.md §Lightweight Path Architecture + Pitfall C2 + Pitfall m3 + Pitfall C4.
  </read_first>
  <action>
    Create `decode-route.test.ts`. Mock `runPredictionPipeline`, `analyzeVideoWithOmni`, `runDecode`, `resolveAndRehost`, and the supabase service client. POST a remix body (`{ input_mode:'tiktok_url', tiktok_url:'https://www.tiktok.com/@x/video/1', content_type:..., mode:'remix' }`) and assert: (1) `runPredictionPipeline` mock is NEVER called; (2) the `usage_tracking` upsert is NEVER called (spy on `service.from('usage_tracking')`); (3) the persisted `variants` update merges `remix.decode` while a pre-seeded `variants.craft` survives (read-merge-write — seed the select mock with `{ craft:{...}, filmstrip_segments:[...] }` and assert the update payload still contains both); (4) the placeholder/row write has `overall_score: null` and `mode: 'remix'`; (5) the SSE `complete` event payload contains `variants.remix.decode` and `overall_score: null`; (6) the DAILY_LIMITS read still rejects with 429 when `currentCount >= limit` on a remix body. Extend `derive-and-drop.test.ts` with a case asserting the decode branch calls `resolveAndRehost`'s `cleanup()` (mock resolveAndRehost to return a `cleanup` spy; assert it ran even when `runDecode` returns null) and never sets `video_storage_path` on the remix row. Tests are RED until Task 2.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/analyze/__tests__/decode-route.test.ts src/app/api/analyze/__tests__/derive-and-drop.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `decode-route.test.ts` exists with discrete cases for: no-pipeline, no-usage-increment, read-merge-write-preserves-craft, overall_score-null + mode-remix, complete-event-carries-variants.remix.decode, DAILY_LIMITS-429-on-remix.
    - `derive-and-drop.test.ts` has a new case asserting `cleanup()` is invoked on the decode path (including when runDecode returns null) and `video_storage_path` is never written.
    - Both test files run (red, pending Task 2).
  </acceptance_criteria>
  <done>Route + derive-and-drop tests express all C2/m3/C4 behaviors; red until the branch lands.</done>
</task>

<task type="auto">
  <name>Task 2: Add remix decode branch + persistDecodeToVariants to route.ts</name>
  <files>src/app/api/analyze/route.ts</files>
  <read_first>
    - src/app/api/analyze/route.ts lines 100-145 (persistCraftToVariants), 280-360 (DAILY_LIMITS read + validate + contentHash), 574-620 (placeholder insert + SSE setup), 625-717 (started/phase/pipeline/usage flow).
    - .planning/phases/03-decode-frame/03-PATTERNS.md §route.ts (persistDecodeToVariants pattern, placeholder-for-remix pattern, SSE send helpers, branch insertion point) + §Critical Implementation Notes (C2, m3, C4 anchors).
    - src/lib/engine/remix/{decode,resolve-and-rehost,decode-types}.ts (Plan 01).
    - src/lib/engine/qwen/omni-analysis.ts (analyzeVideoWithOmni).
  </read_first>
  <action>
    Add `async function persistDecodeToVariants(service, id, decode, log)` next to `persistCraftToVariants` (~line 145), copying the read-merge-write exactly per 03-PATTERNS.md §route.ts: select `variants`, three-level merge `{ ...current, remix: { ...remix, decode } }`, update by id, warn-on-error (never throw). Add a `runDecodeStream` helper (module-scope, near the other helpers) that takes `(controller, send, { service, user, validated, analysisId, contentHash, log })` and runs the decode path inside the SSE `start`: `send("started",{id:analysisId})`; `send("phase",{phase:"analyzing", message:"Decoding structure…"})`; `const { signedUrl, cleanup } = await resolveAndRehost(validated.tiktok_url)`; wrap the rest in `try { const omni = await analyzeVideoWithOmni(signedUrl); const decode = await runDecode(omni as OmniStructuralInput); if (decode) await persistDecodeToVariants(service, analysisId, decode, log); send("complete",{ id:analysisId, mode:'remix', overall_score:null, variants:{ remix:{ decode } } }); } finally { await cleanup(); controller.close(); }`. Do NOT call `runPredictionPipeline`, `aggregateScores`, `persistCraftToVariants`, `populatePredictionCache`'s score path, or the `usage_tracking` upsert on this branch. Reuse the existing placeholder INSERT (lines 583-608) for the remix row (overall_score:null, mode:validated.mode='remix', input_mode, content_hash; do NOT set video_storage_path). Insert the branch BEFORE the existing score-path SSE `ReadableStream`: build the stream's `start` to call `runDecodeStream(...)` when `validated.mode === 'remix'` and otherwise run the existing pipeline body unchanged (branch on mode inside `start`, OR build a separate ReadableStream returned early when remix — choose the early-return-stream form to keep the score `start` body byte-for-byte identical; per RESEARCH Open Q1 recommendation, isolate into runDecodeStream). KEEP the DAILY_LIMITS read guard (lines 301-310) running for remix (it is mode-agnostic, read-to-reject — STATE Plan 03 decision); only skip the usage increment.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/analyze/__tests__/decode-route.test.ts src/app/api/analyze/__tests__/derive-and-drop.test.ts && npx vitest run src/app/api/analyze/__tests__/route.test.ts src/app/api/analyze/__tests__/stream-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - decode-route.test.ts + derive-and-drop.test.ts fully green; existing route.test.ts + stream-route.test.ts (score path) still green (no regression).
    - `grep -c 'persistDecodeToVariants' src/app/api/analyze/route.ts` returns >= 2 (definition + call).
    - In the remix branch code path there is NO call to `runPredictionPipeline` or `usage_tracking` upsert (verified by the test spies + a grep that the remix branch / runDecodeStream body contains neither `runPredictionPipeline` nor `usage_tracking`).
    - `grep -c 'overall_score: null' src/app/api/analyze/route.ts` still present for the remix row; `grep -c 'video_storage_path' ` shows it is not set on the remix branch.
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>Remix submit runs the lightweight decode path, persists variants.remix.decode, streams complete with overall_score null; score path unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/analyze (remix) | Input = tiktok permalink + mode flag; validated by AnalysisInputSchema + TikTok URL regex (route.ts:238). |
| Decode branch → DashScope | Must not leak the signed video URL/token (derive-and-drop, Phase 1); Omni gets a Supabase signed URL only. |
| Read-merge-write on variants JSONB | Concurrent craft/filmstrip writers must not be overwritten. |
| Decode branch → usage_tracking | Must NOT increment (non-scored row, pitfall C2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | persistDecodeToVariants (JSONB overwrite) | mitigate | Read-merge-write three-level spread preserves sibling `craft`/`filmstrip_segments`; test asserts craft survives the decode write. |
| T-03-05 | Denial of Service | remix decode branch (paste-spam) | mitigate | DAILY_LIMITS read guard (route.ts:301-310) stays on the remix branch (read-to-reject 429); only the usage increment is skipped. |
| T-03-06 | Elevation / billing integrity | usage_tracking on decode | mitigate | No usage_tracking upsert on the remix branch (C2); test spy asserts zero calls. Decode row overall_score stays null (m3). |
| T-03-02 | Compliance / IP boundary | runDecodeStream cleanup | mitigate | `cleanup()` in `finally` deletes temp mp4 regardless of Omni/decode outcome; video_storage_path never set; derive-and-drop test asserts. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (milestone-locked); no install task. |
</threat_model>

<verification>
- `npx vitest run src/app/api/analyze/__tests__/decode-route.test.ts` green (C2: no pipeline, no usage; variants.remix.decode read-merge-write; overall_score null; mode remix; complete payload).
- `npx vitest run src/app/api/analyze/__tests__/derive-and-drop.test.ts` green (decode-path cleanup + no video_storage_path).
- `npx vitest run src/app/api/analyze/__tests__/route.test.ts src/app/api/analyze/__tests__/stream-route.test.ts` green (score path no regression — SC#5).
- `npm test` full suite green at wave merge.
</verification>

<success_criteria>
- A remix submission completes on the lightweight Omni→Qwen path with overall_score null and variants.remix.decode populated (DECODE-01 lightweight path, pitfall C2).
- Derive-and-drop preserved on the decode path; no usage_tracking increment; score path unchanged (SC#5).
- The SSE complete payload carries the decode result for Plan 03's frame to render.
</success_criteria>

<output>
Create `.planning/phases/03-decode-frame/03-02-SUMMARY.md` when done.
</output>
