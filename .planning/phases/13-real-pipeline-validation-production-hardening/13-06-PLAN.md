---
phase: 13
plan: 06
type: execute
wave: 5
depends_on: ['05']
files_modified:
  - .planning/phases/13-real-pipeline-validation-production-hardening/validations/video-02.md
  - .planning/phases/13-real-pipeline-validation-production-hardening/validations/video-03.md
  - .planning/phases/13-real-pipeline-validation-production-hardening/validations/video-04.md
  - .planning/phases/13-real-pipeline-validation-production-hardening/validations/video-05.md
autonomous: false
requirements: [SC#4, D-25, D-26, D-31]

must_haves:
  truths:
    - "Videos 2-5 processed through the UI flow per D-25 cadence"
    - "Each video has a validations/video-NN.md report (PATTERNS.md template)"
    - "Score-band stratification per D-26: across the 5-video set there is at least 1 expected low scorer + 1 expected high scorer"
    - "≥ 1 video in input_mode=tiktok_url across Plans 05+06 cumulative per D-31"
    - "Engine code paths UNMODIFIED across Plans 05+06 — if mid-cadence fix is required, restart count and document"
    - "Signal-completeness gate: each video passes audio-fingerprint + ≥7-personas + numeric platform_fit + non-null Stage 11"
  artifacts:
    - path: ".planning/phases/13-real-pipeline-validation-production-hardening/validations/video-02.md"
      provides: "Video 2 prediction-vs-actuals diff"
      contains: "## Verdict"
    - path: ".planning/phases/13-real-pipeline-validation-production-hardening/validations/video-03.md"
      provides: "Video 3 diff"
      contains: "## Verdict"
    - path: ".planning/phases/13-real-pipeline-validation-production-hardening/validations/video-04.md"
      provides: "Video 4 diff"
      contains: "## Verdict"
    - path: ".planning/phases/13-real-pipeline-validation-production-hardening/validations/video-05.md"
      provides: "Video 5 diff"
      contains: "## Verdict"
  key_links:
    - from: "validations/video-0{2,3,4,5}.md"
      to: "scripts/urls-5.txt"
      via: "URL pasted by user pre-run"
      pattern: "tiktok\\.com"
---

<objective>
Process videos 2-5 through the UI flow using the smoke runner built in Plan 05. Per CONTEXT D-25, the user uploads each video manually and pastes the source URL back so Claude can WebFetch actuals. Each video gets a `validations/video-NN.md` report. Anti-pattern: do NOT modify engine code paths mid-cadence — if a fix is required (e.g. a different signal degradation surfaces on real content), restart the count or scope the fix to a follow-up plan.

Purpose: 5-video cadence increases the surface for catching bugs without burning the full 10-video budget. Stratification per D-26 (low / high / mid mix) exercises Stage 11 adaptive bands across all three.

Output: 4 validation reports (video-02 through video-05). Cumulative 5/5 pass count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-real-pipeline-validation-production-hardening/13-CONTEXT.md
@.planning/phases/13-real-pipeline-validation-production-hardening/13-05-SUMMARY.md
@.planning/phases/13-real-pipeline-validation-production-hardening/validations/video-01.md
@scripts/smoke-tiktok-pipeline.ts
@scripts/urls-5.txt
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6.1: Pre-cadence — confirm urls-5.txt populated + binary tag stable</name>
  <what-built>
    - Plan 05 video-01 passed
    - smoke runner ready
    - 5-URL seed file `scripts/urls-5.txt` (created in Plan 01 as placeholder)
  </what-built>
  <how-to-verify>
    1. Confirm `scripts/urls-5.txt` has 5 non-comment lines with valid TikTok URLs. Per D-26 stratification, the user should pick:
       - 1-2 expected low scorers (videos with obvious flaws — bad hook, no payoff, etc.)
       - 1-2 expected high scorers (high-performing videos that "worked")
       - 1-2 expected mid-tier
       Across ≥ 3 niches (per CONTEXT D-26: "Across at least 3 niches").
    2. Confirm at least 1 of these URLs will be processed in `tiktok_url` mode (D-31 — if video-01 was tiktok_url, this is already covered cumulatively; otherwise pick one for video-02).
    3. Confirm `git rev-parse HEAD` matches the binary tag in `validations/video-01.md`. If not (code drift since Plan 05), decide:
       - Re-run video-01 to align binary tag, OR
       - Document the drift in `13-06-SUMMARY.md` and proceed
    4. Confirm `pnpm tsx scripts/engine-self-test.ts` still exits 0 (model lineup hasn't drifted in the days since Plan 05).
  </how-to-verify>
  <resume-signal>Type "approved" with a one-line listing the 5 URLs' expected band stratification (e.g. "2 low, 2 high, 1 mid, all ≥3 niches"). Note the binary tag (git SHA).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 6.2: Run videos 2-5 + write validation reports</name>
  <read_first>
    - scripts/smoke-tiktok-pipeline.ts (Plan 05 — runner)
    - .planning/phases/13-real-pipeline-validation-production-hardening/validations/video-01.md (Plan 05 report — template to follow + baseline signal patterns)
    - scripts/urls-5.txt (5 user-picked URLs)
    - .planning/phases/13-real-pipeline-validation-production-hardening/13-PATTERNS.md (lines 1015-1047 — per-video report template)
  </read_first>
  <action>
For each video 2 through 5 (4 videos in this plan; video-01 was Plan 05), repeat the D-25 cadence:

1. **User uploads** the local mp4 via the dev-server UI (or runs `--direct` mode if testing tiktok_url path).
2. **Engine produces** the prediction; user pastes source TikTok URL back.
3. **Claude WebFetches** the URL and extracts post-publish actuals (views, likes, shares, comments, completion %). If WebFetch is blocked, request a screenshot.
4. **Claude writes** `validations/video-0N.md` using the exact template from PATTERNS.md lines 1015-1047. All 4 sections + Verdict required.
5. **Signal-completeness gate** per video — verify:
   - audio fingerprint non-null (or audio chip shows ⚠ with documented reason)
   - Wave 3 personas: ≥ 7 of 10 returned verdicts (per RESEARCH ≥7-of-10 dissent threshold proxy)
   - Wave 4: numeric platform_fit for at least TikTok
   - Stage 11: non-null `CounterfactualResult` with `band` matching the score (low if <50, mid 50-69, high ≥70) AND suggestion count matching D-05 (3 / 3 / 3-4)
   - Per-analysis cost ≤ $0.40 (D-20 budget)
6. **Diff analysis** per video — "was the prediction directionally right? did Stage 11 suggestions match obvious issues?". One-line user thumbs-up/down is the gate.

**Mid-cadence fix policy (Anti-pattern enforcement):**

If a video fails the signal-completeness gate AND the failure indicates an engine bug (not just a directionally-wrong prediction — that's a calibration concern for M2, not a phase 13 blocker):

- **Tier 1 — non-engine fix (continue cadence):** documentation update, validation template fix, URL re-paste, etc. → just write the report and continue.
- **Tier 2 — engine fix required:** STOP. Document the bug in `13-06-SUMMARY.md`. Decide:
  - (a) **Hot-patch in this plan:** if the fix is < 5 lines and clearly correct, apply it, commit, update binary tag, and re-run all videos done so far in this plan from scratch (videos 2 through current).
  - (b) **Defer to a Plan 06.5 patch:** if fix is larger, freeze cadence, spawn a patch plan, run it, then restart videos 2-5 from scratch.
  - (c) **Accept the degradation:** if the failure is acceptable for milestone-merge (e.g. transient Gemini API blip, not a deterministic engine bug), document in the validation report and continue. User judgment call.

After all 4 reports written, run an aggregate check:
- Each report has a `## Verdict` line that says either `PASS` or `FAIL`.
- Across 4 reports + Plan 05's video-01: cumulative count of PASS / FAIL.
- Cumulative cost (sum of per-video cost_cents).
- Per-video runtime distribution (capture min/median/max wall clock).

Write `13-06-SUMMARY.md` (deferred to the `<output>` block below).
  </action>
  <verify>
    <automated>ls .planning/phases/13-real-pipeline-validation-production-hardening/validations/video-0{2,3,4,5}.md 2>&amp;1 | wc -l &amp;&amp; for nn in 02 03 04 05; do node -e "const r=require('./.planning/phases/13-real-pipeline-validation-production-hardening/validations/video-${nn}-raw.json'); if (!r.audio?.embedding) throw new Error('v${nn}: audio degraded'); if ((r.personas?.length ?? 0) &lt; 7) throw new Error('v${nn}: persona count &lt;7'); if (typeof r.platform_fit !== 'number') throw new Error('v${nn}: platform_fit not numeric'); if (!r.counterfactuals) throw new Error('v${nn}: Stage 11 missing'); if (r.cost_cents &gt; 40) throw new Error('v${nn}: cost over budget');" || exit 1; done</automated>
    <notes>Probe loops over `validations/video-{02..05}-raw.json` (emitted by Plan 05's `--json-out` contract). Asserts signal-completeness + D-20 cost budget structurally for each video. Plan 05's smoke runner MUST be invoked with `--json-out` for each of videos 02-05 during the cadence so these files exist.</notes>
  </verify>
  <acceptance_criteria>
    - Files `validations/video-02.md`, `video-03.md`, `video-04.md`, `video-05.md` all exist.
    - Each report contains all 4 sections (`## Section 1 — Prediction`, `## Section 2 — Actuals`, `## Section 3 — Diff Analysis`, `## Section 4 — Signal-by-signal Calibration`) + `## Verdict`.
    - Each report records the binary tag (git SHA) used for that video.
    - At least 1 of videos 1-5 cumulative used `input_mode = tiktok_url` (D-31).
    - Per-video cost_cents ≤ $0.40 in each report (or explicit documentation of overage).
    - Stage 11 band matches score range in each report.
    - 5/5 cumulative PASS verdict OR explicit documentation of why a FAIL is acceptable for Plan 07 to proceed.
  </acceptance_criteria>
  <done>Cumulative 5/5 videos processed. Engine ready for the final 10-video gate (Plan 07).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6.3: Mid-cadence review checkpoint</name>
  <what-built>
    - Cumulative 5 validation reports
    - 13-06-SUMMARY.md draft with cumulative metrics
  </what-built>
  <how-to-verify>
    1. Skim each of `video-02.md` through `video-05.md`. For each FAIL or DEGRADED report, decide: continue cadence to Plan 07 (10 videos) OR pause for a hot-fix?
    2. Verify D-26 stratification was actually exercised — across the 5 cumulative reports, is there genuinely 1+ low + 1+ high + 1+ mid? If all 5 scored 70+ by accident, the cadence didn't exercise the low-band Stage 11 path. Decide: pick a sixth video with an obvious flaw for Plan 07, OR accept the band gap.
    3. Verify cumulative cost across 5 videos. Estimate Plan 07 cost (likely 2x). If approaching budget concerns, decide whether to truncate prompt or proceed.
    4. Spot-check: do the Stage 11 "fix" suggestions in any low-band report match the obvious issues a human would identify in the source video? This is the user-thumbs-up gate.
  </how-to-verify>
  <resume-signal>Type "approved" with a one-line: cumulative pass count, whether D-26 stratification was exercised, total cost across 5 videos. If FAIL count > 1, list which videos failed and why.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real user-picked TikTok URLs → engine | Larger surface than Plan 05 (5 vs 1); higher chance of edge-case content |
| WebFetch → TikTok pages | Cumulative 4 WebFetches; rate-limiting / IP-blocking risk |
| Engine code paths | Anti-pattern: must remain unmodified across plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-29 | Denial of Service | TikTok rate-limiting WebFetch | accept | Fallback to user-paste screenshot per Plan 05 pattern. WebFetch volume is small (~5 calls across plans 05+06). |
| T-13-30 | Tampering | Mid-cadence engine code drift | mitigate | Binary tag captured per video; cumulative diff visible. Task 6.3 explicitly asks whether to restart count if drift occurred. |
| T-13-31 | Information Disclosure | Per-video TikTok URL committed to git | accept | URLs are user-supplied test fixtures; public TikTok content. User can redact if private. |
| T-13-32 | Denial of Service | Cumulative cost across plans 05-07 | mitigate | D-20 per-analysis cap $0.40 × 16 cumulative videos = ~$6.40 worst-case. Acceptable for milestone gate. |
</threat_model>

<verification>
- 4 validation reports exist with all sections + verdict
- 13-06-SUMMARY.md captures cumulative metrics
- Checkpoint 6.3 approved with stratification + cost answers
</verification>

<success_criteria>
- SC#4 (partial): cumulative 5 real videos processed end-to-end
- D-25: cadence pattern followed
- D-26: stratification exercised (or documented gap)
- D-31: cumulative ≥ 1 tiktok_url mode video
</success_criteria>

<output>
After completion, create `.planning/phases/13-real-pipeline-validation-production-hardening/13-06-SUMMARY.md` recording:
- Cumulative 5-video pass count (X/5)
- Cumulative cost across the 5 videos ($N.NN)
- Per-video runtime distribution (min / median / max wall clock seconds)
- D-26 stratification: actual scores observed vs. user's pre-cadence expectation
- D-31 mode coverage: count of video_upload vs tiktok_url
- Mid-cadence fixes (if any): files modified + reason + binary-tag impact
- Any signal degradation patterns recurring across multiple videos (informs Plan 07 attention focus)
- Stage 11 user-thumbs-up rate (X/5 — proxy for the "suggestions match obvious issues" qualitative gate)
</output>
