---
phase: 13
plan: 04
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - .planning/phases/13-real-pipeline-validation-production-hardening/13-CODE-REVIEW-PHASES-9-12.md
autonomous: true
requirements: [SC#5]

must_haves:
  truths:
    - "Phase 9 wave wiring, signal fallback paths, and error swallowing reviewed file-by-file"
    - "Phase 10 calibration math + ML disable decision reviewed"
    - "Phase 11 UI integration + storage retention cron reviewed"
    - "Phase 12 superseded artifacts noted (--max-rows kept, smoke-v3.json flagged for Plan 08 deletion)"
    - "Every finding tagged severity (BLOCKER / WARNING / INFO) and assigned an action (fix-now / defer-to-followup-plan / accept-as-is)"
    - "Bug triage table lists Plan-13 follow-up tasks created from BLOCKER findings"
    - "BLOCKER-4 nyquist gate: each phase section contains ≥ 1 finding tagged ❌ or ⚠️ via the severity-symbol grep (cumulative ≥ 1 across all phases), OR an explicit per-phase 'LGTM — no issues found' verdict line when truly clean"
  artifacts:
    - path: ".planning/phases/13-real-pipeline-validation-production-hardening/13-CODE-REVIEW-PHASES-9-12.md"
      provides: "Cross-phase code-logic review artifact"
      contains: "## Phase 9\n## Phase 10\n## Phase 11\n## Phase 12"
  key_links:
    - from: "13-CODE-REVIEW-PHASES-9-12.md"
      to: "src/lib/engine/wave3.ts, wave4/platform-fit.ts, stage10-critique.ts, ml.ts, calibration.ts, retention/* "
      via: "file:line citations per finding"
      pattern: "src/lib/engine/.+:[0-9]+"
---

<objective>
Read-only cross-phase code-logic review of Phases 9 (platform fit + critique + counterfactuals), 10 (ML audit + Platt + aggregator extension), 11 (UI integration + privacy policy), 12 (superseded). Produces one artifact: `13-CODE-REVIEW-PHASES-9-12.md`. No code changes in this plan — findings either become BLOCKER tasks in a follow-up plan or DEFER notes for STATE.md verification debt.

Purpose: Phases 9-12 shipped through gsd-executor agents without full human code review. Phase 13 is the milestone gate — bugs in those phases will surface during E2E and either (a) be caught here proactively (cheap fix) or (b) silently degrade the 10-video cadence (expensive false-confidence). This plan is the "look once carefully before you ship" step.

Output: One read-only doc artifact. Findings with severity classification. Action items per finding routed to: (a) Plan 04 follow-up tasks within this plan for BLOCKERS, (b) STATE.md verification debt for WARNINGS, (c) accepted as-is for INFOs.

BLOCKER-4 (revision iter 1): each task's acceptance_criteria now adds a content-aware severity-symbol grep — `grep -cE '❌|⚠️|BLOCKER|WARNING'` over the review doc must return ≥ 1 cumulative across all four phases reviewed, OR each clean phase must carry an explicit "LGTM — no issues found" verdict line. The grep proves the executor actually performed a critical review rather than producing a boilerplate "all green" doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-real-pipeline-validation-production-hardening/13-CONTEXT.md
@.planning/phases/13-real-pipeline-validation-production-hardening/13-RESEARCH.md
@.planning/phases/13-real-pipeline-validation-production-hardening/13-PATTERNS.md
@.planning/phases/13-real-pipeline-validation-production-hardening/13-01-SUMMARY.md
@.planning/phases/13-real-pipeline-validation-production-hardening/13-AUDIT-CAPTION-LESS.md
@.planning/phases/12-accuracy-benchmark-acceptance-gate/12-HANDOFF.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 4.1: Produce 13-CODE-REVIEW-PHASES-9-12.md — phase 9 review</name>
  <read_first>
    - .planning/phases/09-platform-algo-fit-self-critique-counterfactuals/09-CONTEXT.md
    - .planning/phases/09-platform-algo-fit-self-critique-counterfactuals/09-RESEARCH.md (if exists)
    - .planning/phases/09-platform-algo-fit-self-critique-counterfactuals/09-VALIDATION.md (analog for review-doc shape)
    - All `.planning/phases/09-*/09-*-SUMMARY.md` files (capture decisions)
    - src/lib/engine/wave4/platform-fit.ts
    - src/lib/engine/wave4/platform-fit-prompts.ts
    - src/lib/engine/stage10-critique.ts
    - src/lib/engine/stage10-critique-prompts.ts
    - src/lib/engine/aggregator.ts (Stage 10 + platform_fit signal integration)
    - src/lib/engine/pipeline.ts (Wave 4 invocation site)
    - src/lib/engine/__tests__/aggregator.test.ts (signal coverage for platform_fit + critique)
  </read_first>
  <action>
Create `.planning/phases/13-real-pipeline-validation-production-hardening/13-CODE-REVIEW-PHASES-9-12.md`. Use the shape from PATTERNS.md lines 970-1005 + analog `.planning/phases/12-.../12-HANDOFF.md` for per-section ✅/⚠️/❌ status framing.

Top of file:
```markdown
# Phase 13 — Cross-Phase Code Review (Phases 9-12)

**Reviewed:** <YYYY-MM-DD>
**Scope:** Wave wiring correctness, signal fallback paths, silent degradations across phases 9, 10, 11, 12.
**Note:** Read-only artifact. Findings tagged severity → action.

## Legend
- ✅ Passes review — no concerns
- ⚠️ Concern with workaround — fix recommended in follow-up
- ❌ Blocker — must fix before Plan 05 (1-video E2E)
- 📦 Archival note — for superseded artifacts (Phase 12)

## Severity → Action Mapping
- BLOCKER (❌) → Create a Task 4.5 follow-up in this plan
- WARNING (⚠️) → Add to STATE.md "Phase 13 follow-ups"
- INFO (ℹ️) → Acknowledge inline; no action
```

Add a `## Phase 9 — Platform-Algo-Fit + Self-Critique + Counterfactuals` section.

Review focus per CONTEXT (RESEARCH §Plan 04):
1. **Wave wiring** — does Wave 4 (`platform-fit`) actually consume the output of Wave 3 (personas) per its spec? Read `pipeline.ts` Wave 3 → Wave 4 transition; confirm `personaResults` or equivalent flows through. Cite file:line.
2. **Signal fallback paths** — when `platform_fit` returns null (DeepSeek error), does the aggregator gracefully redistribute weight or does the signal silently drop with weight=0? Read `aggregator.ts` `selectWeights` redistribution; trace what happens when `platform_fit` is null.
3. **Stage 10 critique → confidence adjustment** — `applyCritiqueAdjustment` modifies confidence. Does it ever mutate `result.overall_score`? (Should NOT — critique is confidence-only.) Cite file:line.
4. **Watermark detection** — Phase 9 D-X says watermark flag fires from Gemini hook segment. Is the flag actually consumed anywhere downstream? Search for `watermark_detected` usage. If unused, flag as ⚠️.
5. **Counterfactuals legacy** — Phase 9 produced the original Stage 11 (DeepSeek V4 Flash, skip≥70). Plan 02 of this phase rebuilt it. Confirm no stale Phase 9 references to the old contract remain in tests / docs.
6. **Caption reads in Phase 9 stages** — cross-reference with `13-AUDIT-CAPTION-LESS.md` Wave 4 row. `wave4/platform-fit-prompts.ts:133` reads `payload.content_text || "(no caption)"` — does this gracefully handle caption-less video-mode? Verify behavior with empty caption.

For each finding, format:
```markdown
### ✅/⚠️/❌ <area name>
**Finding:** <one-sentence description>
**File:line:** `src/lib/engine/.../foo.ts:123`
**Severity:** BLOCKER / WARNING / INFO
**Action:** <fix-now / defer-to-STATE.md / accept>
**Notes:** <2-3 sentences of evidence>
```

Minimum 5 findings for Phase 9 (one per review focus above).

**BLOCKER-4 enforcement:** if all 5 findings come up ✅ (genuinely no concerns), the section MUST include the explicit line:
```
**Phase 9 verdict: LGTM — no issues found.** (No ❌ or ⚠️ tags; all 5 review-focus items pass cleanly with file:line evidence.)
```
This proves the executor critically reviewed each focus area rather than producing boilerplate.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-real-pipeline-validation-production-hardening/13-CODE-REVIEW-PHASES-9-12.md &amp;&amp; grep -c "## Phase 9" .planning/phases/13-real-pipeline-validation-production-hardening/13-CODE-REVIEW-PHASES-9-12.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/13-real-pipeline-validation-production-hardening/13-CODE-REVIEW-PHASES-9-12.md` exists.
    - `grep -c "## Phase 9" 13-CODE-REVIEW-PHASES-9-12.md` returns ≥ 1.
    - Phase 9 section contains ≥ 5 findings.
    - Each finding has a `**File:line:**` line citing a specific src/ path with `:NN` line number.
    - At least one finding has severity ❌ OR ⚠️ OR ✅ explicitly tagged.
    - Wave wiring (Wave 3 → Wave 4) explicitly addressed with file:line citation.
    - Signal fallback path for platform_fit null case explicitly addressed.
    - Caption-less behavior at `wave4/platform-fit-prompts.ts:133` explicitly addressed.
    - **BLOCKER-4 nyquist check:** `grep -cE '❌|⚠️|BLOCKER|WARNING|LGTM — no issues found' .planning/phases/13-real-pipeline-validation-production-hardening/13-CODE-REVIEW-PHASES-9-12.md` returns ≥ 1 (Phase 9 section produced either a real non-✅ finding OR an explicit LGTM verdict — proves critical review happened, not boilerplate).
  </acceptance_criteria>
  <done>Phase 9 section complete with severity-classified findings.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4.2: Add phase 10 + phase 11 review sections</name>
  <read_first>
    - .planning/phases/10-ml-audit-calibration-aggregator-extension/10-CONTEXT.md
    - All `.planning/phases/10-*/10-*-SUMMARY.md` (if any plans completed)
    - .planning/phases/11-existing-ui-integration-privacy-policy/11-CONTEXT.md
    - All `.planning/phases/11-*/11-*-SUMMARY.md`
    - src/lib/engine/ml.ts (Phase 10 ML disable)
    - src/lib/engine/calibration.ts (Platt parameters)
    - src/lib/engine/aggregator.ts (extended SignalAvailability + selectWeights redistribution)
    - src/app/api/cron/retention/route.ts (or wherever retention cron lives — search for "storage_retention_opted_in")
    - src/app/api/analyze/route.ts (Phase 11 modifications — analysis_count RPC, retention gate, video_storage_path)
    - src/components/app/simulation/results-panel.tsx (PredictionResult render — pre-Plan-02 wiring + post-Plan-02 changes)
    - src/components/onboarding/* (9-card profile integration)
  </read_first>
  <action>
Append `## Phase 10 — ML Audit + Calibration + Aggregator Extension` section.

Review focus:
1. **ML disable correctness** — Phase 10 D-05 disabled ML signal. Confirm `SCORE_WEIGHTS.ml === 0` (post-Plan-02 it's 0). Confirm `predictWithML` is still wired (so re-enabling is a 1-line weight flip). Cite file:line.
2. **Platt calibration scope** — `getPlattParameters()` reads from DB. Phase 12 trained Platt on text-mode predictions; D-29 flags it as "text-mode-trained, video-mode re-train pending". Verify the active parameters in DB still get applied at predict-time. If `applyPlattScaling` is called regardless of training distribution, flag as ⚠️ (engine applies a miscalibrated curve).
3. **SignalAvailability dynamic redistribution** — `selectWeights` redistributes weight when sources are unavailable. After Plan 02's hard-coded `retrieval=0, rules=0`, does the redistribution still meaningfully redistribute, or does it now act on a 5-signal subset? Walk through a scenario (e.g. audio=unavailable, what happens?). Cite file:line.
4. **ENGINE_VERSION bump trigger** — Phase 10 D-06 says "ENGINE_VERSION bumped to 3.0.0". Confirm current state is still `3.0.0-dev` (per Plan 08 D-27 — flip happens only after 10/10 pass).
5. **Aggregator-tests coverage** — read `aggregator.test.ts`; confirm new signals (personas, audio, retrieval, hook, algo-fit) have at least one assertion each.

Append `## Phase 11 — Existing UI Integration + Privacy Policy` section.

Review focus:
1. **`/api/analyze` engine-v3 wiring** — does the route call `runPredictionPipeline` from the current engine module (not a stale `engine-v2.1`)? Verify imports.
2. **analysis_count RPC integration** — Phase 11 D-X adds an analysis_count column. Trace where it's incremented; confirm idempotent on retry.
3. **Storage retention cron** — `storage_retention_opted_in` flag controls 30-day auto-delete. Confirm the cron correctly filters by opt-in. Read cron route file. Cite file:line.
4. **9-card profile modal interception** — does `video-upload.tsx` check for profile existence and gate first upload? Walk the auth state check.
5. **Dashboard render of new fields** — `ResultsPanel` renders `SignalAvailabilityChips`, `GoalRecheckBanner` (Phase 11 D-X). Confirm these are present in the post-Plan-02 codebase OR flag them as missing if Plan 02 removed them.
6. **SC#5 deferred items** — Phase 11 Plan 05 was a smoke test checkpoint. Confirm whether it ran or is deferred.

Minimum 4 findings per phase.

If any finding is severity ❌ (BLOCKER), add a Task 4.5 placeholder at the bottom of the review doc with the action plan.

**BLOCKER-4 enforcement (same pattern as Task 4.1):** if either phase section comes up entirely ✅, include the explicit "**Phase 10 verdict: LGTM — no issues found.**" / "**Phase 11 verdict: LGTM — no issues found.**" line per clean phase.
  </action>
  <verify>
    <automated>grep -c "## Phase 10\|## Phase 11" .planning/phases/13-real-pipeline-validation-production-hardening/13-CODE-REVIEW-PHASES-9-12.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Phase 10" 13-CODE-REVIEW-PHASES-9-12.md` returns 1.
    - `grep -c "## Phase 11" 13-CODE-REVIEW-PHASES-9-12.md` returns 1.
    - Each section has ≥ 4 findings.
    - Each finding cites a specific `src/` file:line.
    - Phase 10: ML disable + Platt staleness + selectWeights redistribution all addressed.
    - Phase 11: /api/analyze v3 wiring + retention cron + 9-card profile gate all addressed.
    - **BLOCKER-4 nyquist check:** `grep -cE '❌|⚠️|BLOCKER|WARNING|LGTM — no issues found' .planning/phases/13-real-pipeline-validation-production-hardening/13-CODE-REVIEW-PHASES-9-12.md` returns ≥ 2 after this task (Phase 9 contributed ≥1 from Task 4.1; Phases 10+11 each contribute at least one severity-flag or LGTM marker).
  </acceptance_criteria>
  <done>Phases 10 + 11 review sections complete.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4.3: Add phase 12 archival + bug triage table</name>
  <read_first>
    - .planning/phases/12-accuracy-benchmark-acceptance-gate/12-CONTEXT.md
    - .planning/phases/12-accuracy-benchmark-acceptance-gate/12-HANDOFF.md
    - All `.planning/phases/12-*/12-*-SUMMARY.md`
    - scripts/eval.ts (--max-rows flag — kept per D-29)
    - .planning/research/smoke-v3.json (if exists — flagged for deletion in Plan 08 per D-29)
    - .planning/research/v2.1-baseline.md
    - src/lib/engine/calibration.ts (platt_parameters DB row — kept per D-29)
    - .planning/phases/13-real-pipeline-validation-production-hardening/13-PHASE12-CLEANUP.md (from Plan 01)
    - .planning/phases/13-real-pipeline-validation-production-hardening/13-CODE-REVIEW-PHASES-9-12.md (current contents)
  </read_first>
  <action>
Append `## Phase 12 — Accuracy Benchmark (Superseded)` section per D-29.

Required content:
1. **📦 Archival note** — repeat the D-29 disposition: Phase 12 status = "Superseded by Phase 13". Reference `13-PHASE12-CLEANUP.md` for the obsolete env inventory.
2. **Kept artifacts (do NOT delete in this phase):**
   - `--max-rows` flag in `scripts/eval.ts` (utility — kept per D-29)
   - `platt_parameters` DB row (flagged "text-mode-trained, video-mode re-train pending")
   - All `12-*.md` planning artifacts (historical record)
3. **Discard list (Plan 08 owns the deletion):**
   - `.planning/research/smoke-v3.json` (invalid — force-disabled DeepSeek + text-mode)
4. **Update list (Plan 08 owns the edit):**
   - `12-HANDOFF.md` — add closing note pointing forward to Phase 13 outcomes
5. **Remnant audit** — search the repo for any active import / call site that depends on Phase 12 logic that won't exist after Plan 08 deletion:
   ```bash
   grep -rn "smoke-v3" .planning/ src/
   ```
   Cite each match with severity. Most should be in docs only.

Append `## Bug Triage` section at the bottom — a single table consolidating all findings across Phases 9-12:

```markdown
## Bug Triage

| Finding | Source phase | File:line | Severity | Action | Owner Plan |
|---------|-------------|-----------|----------|--------|------------|
| <one-line> | 9 | src/lib/engine/...:NN | ❌ | fix-now | 13-04 Task 4.5 (placeholder) OR 13-05 (E2E) |
| <one-line> | 10 | ... | ⚠️ | defer-to-STATE.md | post-13 follow-up |
| <one-line> | 11 | ... | ✅ | accept | — |
| <one-line> | 12 | ... | 📦 | archival | 13-08 cleanup |
```

If ANY BLOCKER (❌) appears in the triage table, append a `## Task 4.5 — BLOCKER follow-up tasks` section with the actionable fix plan per BLOCKER. Format each task block as:
```markdown
### BLOCKER-N: <one-line summary>
**File:line:** `src/...:NN`
**Fix plan:** <2-3 sentences with concrete change>
**Verify:** <command or grep that proves the fix worked>
```

If NO BLOCKERS, write the explicit line `**No BLOCKERS found — Plan 05 unblocked.**`

Finally, append `## Cross-cutting Observations` — 2-3 paragraphs synthesizing patterns across the four phases (e.g. "Most error-swallowing happens at LLM-boundary catch blocks; pattern is consistent and probably fine but should be audited if E2E reveals silent-degradation").

**BLOCKER-4 final attestation:** the Bug Triage table MUST contain ≥ 1 row tagged ❌ or ⚠️ OR Phase 12 archival row tagged 📦, AND/OR the explicit "**No BLOCKERS found — Plan 05 unblocked.**" line. Together with Tasks 4.1+4.2's per-phase markers, the cumulative grep `grep -cE '❌|⚠️|BLOCKER|WARNING|LGTM — no issues found|No BLOCKERS found' 13-CODE-REVIEW-PHASES-9-12.md` returns ≥ 1.
  </action>
  <verify>
    <automated>grep -c "## Phase 12\|## Bug Triage" .planning/phases/13-real-pipeline-validation-production-hardening/13-CODE-REVIEW-PHASES-9-12.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Phase 12" 13-CODE-REVIEW-PHASES-9-12.md` returns 1.
    - `grep -c "## Bug Triage" 13-CODE-REVIEW-PHASES-9-12.md` returns 1.
    - Bug triage table has ≥ 1 row per phase (at least 4 rows total — one per phase).
    - If any row has severity ❌, a `## Task 4.5` section follows with concrete fix plans.
    - If no BLOCKER rows, the line `**No BLOCKERS found — Plan 05 unblocked.**` appears.
    - `## Cross-cutting Observations` section appears at the end with ≥ 2 paragraphs.
    - Phase 12 archival section references `13-PHASE12-CLEANUP.md`.
    - Remnant audit `grep -rn "smoke-v3" .planning/ src/` results captured in the doc.
    - **BLOCKER-4 final nyquist check:** `grep -cE '❌|⚠️|BLOCKER|WARNING|LGTM — no issues found|No BLOCKERS found' .planning/phases/13-real-pipeline-validation-production-hardening/13-CODE-REVIEW-PHASES-9-12.md` returns ≥ 1 — proves the four phases reviewed produced at least one severity-classified finding or an explicit clean verdict per phase (rules out content-agnostic boilerplate reviews).
  </acceptance_criteria>
  <done>Cross-phase review doc complete. Plan 05 either unblocked (no BLOCKERS) or has explicit follow-up tasks (Task 4.5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review doc → public repo | Findings cite file:line; must not leak secrets or sample user data |
| reviewer → src/ | Read-only access; no code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-20 | Information Disclosure | Cited file:line snippets in review doc | mitigate | Cite line numbers and short identifiers only — no secret material or PII embedded. Phase 9-12 code is all server-side engine — no client-credential paths in review scope. |
| T-13-21 | Tampering | Bug triage might mislabel BLOCKER as WARNING | mitigate | Severity classification justified inline per finding (action column + notes). Plan 05 (1-video E2E) will surface any miscategorized BLOCKER as a runtime failure — restart cadence per Anti-Pattern note in RESEARCH. BLOCKER-4 nyquist grep guards against content-agnostic boilerplate. |
| T-13-22 | Information Disclosure | review doc references DB column names | accept | Public engineering artifact. Schema names are not secrets. |
</threat_model>

<verification>
- `test -f .planning/phases/13-real-pipeline-validation-production-hardening/13-CODE-REVIEW-PHASES-9-12.md` returns 0
- All 4 phase sections + Bug Triage + Cross-cutting Observations present
- ≥ 4 findings per phase, each with file:line citation
- Either "No BLOCKERS" line OR Task 4.5 follow-up plan present
- `grep -cE '❌|⚠️|BLOCKER|WARNING|LGTM — no issues found|No BLOCKERS found' 13-CODE-REVIEW-PHASES-9-12.md` returns ≥ 1 (BLOCKER-4)
</verification>

<success_criteria>
- SC#5 (Phases 9-12 code-logic review): wave wiring + signal fallback + silent-degradation review committed
- D-29: Phase 12 archival disposition documented; Plan 08 handed the deletion list
- BLOCKER findings (if any) routed to actionable follow-up tasks; Plan 05 explicitly unblocked or blocked
- BLOCKER-4: severity-symbol nyquist grep replaces the prior content-agnostic generic-grep gate; review doc now structurally proves critical review happened
</success_criteria>

<output>
After completion, create `.planning/phases/13-real-pipeline-validation-production-hardening/13-04-SUMMARY.md` recording:
- Total findings count by severity (❌ N, ⚠️ N, ✅ N, 📦 N)
- Whether Plan 05 is unblocked (no BLOCKERS) or has a Task 4.5 prerequisite list
- Top 3 highest-priority WARNINGs added to STATE.md verification debt
- Any cross-cutting pattern that should inform Plan 05/06/07 E2E observation focus
- BLOCKER-4 attestation: severity-symbol grep count + per-phase verdict summary
</output>
