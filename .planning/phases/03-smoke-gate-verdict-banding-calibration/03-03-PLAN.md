---
phase: 03-smoke-gate-verdict-banding-calibration
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - .planning/phases/03-smoke-gate-verdict-banding-calibration/03-GATE-DECISION.md
autonomous: false
requirements: [GATE-03]
must_haves:
  truths:
    - "A dated go/no-go decision exists recording GO or NO-GO for Phase 4"
    - "The decision records the three conjuncts: all automated GREEN, human honest/sane, buffer provably > variance (D-08)"
    - "An F42 authenticated permalink to a real persisted analysis is recorded"
    - "A full measure-pipeline pass (local cross-check) is recorded"
    - "A NO-GO on any failing conjunct blocks Phase 4"
  artifacts:
    - path: ".planning/phases/03-smoke-gate-verdict-banding-calibration/03-GATE-DECISION.md"
      provides: "The dated, recorded verdict-banding go/no-go that literally gates Phase 4"
      contains: "go/no-go"
  key_links:
    - from: "03-GATE-DECISION.md"
      to: "03-01-SUMMARY.md (variance) + 03-02-SUMMARY.md (buffer)"
      via: "buffer-half-width > variance-half-width assertion"
      pattern: "buffer"
    - from: "03-GATE-DECISION.md"
      to: "deployed Vercel persisted analysis"
      via: "F42 authenticated permalink"
      pattern: "https"
---

<objective>
Record the dated verdict-banding go/no-go (GATE-03, D-08): GO only if (all automated
assertions GREEN) AND (human signed off honest/sane) AND (buffer provably > variance).
Include the F42 authenticated permalink and a full local measure-pipeline pass. A NO-GO
on any conjunct blocks Phase 4.

Purpose: This is the literal hard gate. Phase 4 (Mobile Reading Thread) cannot start
against real output until this dated decision reads GO. It composes the GATE-01 evidence
(Plan 01) and the GATE-02 calibration (Plan 02) into one auditable verdict.

Output: `03-GATE-DECISION.md` — the dated decision + the three-conjunct evidence + F42
permalink + measure-pipeline pass. NO code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-smoke-gate-verdict-banding-calibration/03-CONTEXT.md

# The two evidence sources this decision composes
@.planning/phases/03-smoke-gate-verdict-banding-calibration/03-01-SUMMARY.md
@.planning/phases/03-smoke-gate-verdict-banding-calibration/03-02-SUMMARY.md

# Local measure-pipeline (GATE-03 structural pass — runs the engine LOCALLY, NOT the production latency)
@scripts/measure-pipeline.ts

<interfaces>
<!-- measure-pipeline.ts prints a greppable line: OVERALL_SCORE=... CONFIDENCE=... LABEL=... + per-stage durations.
     IT RUNS THE ENGINE LOCALLY (imports runPredictionPipeline directly) — use it for the
     GATE-03 structural "measure-pipeline pass" + sanity cross-check ONLY, NOT the D-09
     production latency (that came from Plan 01's deployed-Vercel runs). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the local measure-pipeline pass + assemble the go/no-go evidence</name>
  <files>.planning/phases/03-smoke-gate-verdict-banding-calibration/03-GATE-DECISION.md</files>
  <read_first>
    - scripts/measure-pipeline.ts (the local probe + its env guards + greppable output line)
    - .planning/phases/03-smoke-gate-verdict-banding-calibration/03-01-SUMMARY.md (production latency, variance figure, GATE-01 GREEN, honesty sign-off)
    - .planning/phases/03-smoke-gate-verdict-banding-calibration/03-02-SUMMARY.md (chosen buffer half-width)
  </read_first>
  <action>
    Run `pnpm tsx scripts/measure-pipeline.ts` (local) for the GATE-03 structural
    measure-pipeline pass — capture its greppable `OVERALL_SCORE=... CONFIDENCE=... LABEL=...`
    line + per-stage durations as a sanity cross-check (NOT the production latency — that is
    sourced from Plan 01's deployed runs per D-09 / Pitfall 1). Then assemble the three D-08
    conjuncts from the two prior SUMMARYs: (1) all automated GREEN — gate-assert.ts GATE_ASSERT=GREEN
    (Plan 01) + verdict.test.ts + full suite GREEN (Plan 02); (2) human honest/sane — the D-07
    sign-off recorded in Plan 01's 03-GATE-RUNS.md; (3) buffer provably > variance — read the
    buffer half-width from Plan 02 and the variance half-width from Plan 01 and assert the
    numeric inequality (buffer >= variance, rounded up => strictly >; record both numbers).
    Capture the F42 authenticated permalink to one persisted analysis from the Plan 01 batch.
  </action>
  <verify>
    <automated>pnpm tsx scripts/measure-pipeline.ts 2>&1 | grep -E "OVERALL_SCORE="</automated>
  </verify>
  <acceptance_criteria>
    - measure-pipeline.ts ran and a greppable `OVERALL_SCORE=` line was captured
    - The numeric buffer half-width (Plan 02) and variance half-width (Plan 01) are both recorded with the inequality buffer > variance evaluated explicitly
    - An F42 authenticated permalink (https URL to a persisted analysis) is captured
    - No code files changed by this task (git diff shows only .planning/ additions)
  </acceptance_criteria>
  <done>Local measure-pipeline pass recorded; the three D-08 conjuncts assembled with the buffer-vs-variance numbers explicit and the F42 permalink captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Record the dated go/no-go decision (D-08) — the hard gate on Phase 4</name>
  <read_first>
    - .planning/phases/03-smoke-gate-verdict-banding-calibration/03-CONTEXT.md (D-08 definition: go/no-go = all-GREEN AND honest/sane AND buffer>variance)
    - .planning/phases/03-smoke-gate-verdict-banding-calibration/03-01-SUMMARY.md + 03-02-SUMMARY.md (the assembled evidence)
  </read_first>
  <what-built>
    The full evidence chain is assembled: GATE-01 GREEN assertions + production latency +
    variance figure + human honesty sign-off (Plan 01), the calibrated dead-band buffer
    (Plan 02), and the local measure-pipeline pass + buffer-vs-variance inequality + F42
    permalink (Task 1 above).
  </what-built>
  <how-to-verify>
    Write `03-GATE-DECISION.md` with a DATED decision (2026-06-12 format). State GO or NO-GO.
    GO requires ALL THREE conjuncts true (D-08): (1) all automated GREEN, (2) human signed off
    the verdict is honest + the expert insight is sane, (3) buffer provably > variance (record
    both half-widths and the inequality). Embed: the run table summary + production ENG-03
    latency (from Plan 01), the DashScope-429 documentation, the F42 authenticated permalink,
    the local measure-pipeline result line, and the chosen DEAD_BAND_FLOOR / buffer half-width
    vs the measured variance half-width. If ANY conjunct fails, record NO-GO and state which
    conjunct failed — a NO-GO BLOCKS Phase 4 (Mobile Reading Thread must not start). Confirm
    with Davide before recording GO: is the verdict honest, is the insight sane, is the gate
    truly cleared?
  </how-to-verify>
  <acceptance_criteria>
    - `03-GATE-DECISION.md` exists with a dated GO or NO-GO line
    - All three D-08 conjuncts are explicitly evaluated (GREEN/honest/buffer>variance) with the buffer and variance numbers shown
    - The F42 authenticated permalink and the production ENG-03 latency are embedded
    - The DashScope-429 behavior is documented
    - If GO: the decision states Phase 4 may proceed. If NO-GO: it names the failing conjunct and states Phase 4 is blocked
  </acceptance_criteria>
  <resume-signal>Type "approved" once 03-GATE-DECISION.md records the dated go/no-go with all three conjuncts evaluated — or "no-go: <reason>" to record a blocking decision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local engine run -> DashScope | measure-pipeline.ts runs the real engine locally; outbound model calls authenticated by DASHSCOPE_API_KEY |
| persisted analysis -> recorded permalink | An authenticated permalink URL is written into a planning doc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | F42 permalink in 03-GATE-DECISION.md | mitigate | Record the analysis permalink (path/id) only — it is access-gated by Supabase auth; do NOT embed a session cookie or signed/tokenized URL. PII-review before commit. |
| T-03-03b | Information Disclosure | DASHSCOPE_API_KEY (measure-pipeline run) | accept | Key stays in .env.local (gitignored); measure-pipeline reads it via the existing env-guard pattern. No new exposure. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH: Package Legitimacy Audit = N/A). No install task. |

Surface is a recorded decision document + a local engine probe that reuses the existing
env-guarded path. No new attack surface; permalink-handling (T-03-03) is the only control.
</threat_model>

<verification>
- `03-GATE-DECISION.md` exists with a dated GO/NO-GO and all three D-08 conjuncts evaluated
- `pnpm tsx scripts/measure-pipeline.ts` produced a greppable OVERALL_SCORE= line (structural measure-pipeline pass)
- buffer half-width > variance half-width is shown numerically
- No code files changed; ENGINE_VERSION unchanged
</verification>

<success_criteria>
- GATE-03 satisfied: a dated, recorded go/no-go exists composing all-GREEN + human honest/sane + buffer>variance, with F42 permalink + measure-pipeline pass
- The decision is the literal gate on Phase 4 — a NO-GO blocks the mobile Reading thread
</success_criteria>

<output>
Create `.planning/phases/03-smoke-gate-verdict-banding-calibration/03-03-SUMMARY.md` when done.
State the final GO/NO-GO so the orchestrator and Phase 4 planning can read the gate outcome.
</output>
