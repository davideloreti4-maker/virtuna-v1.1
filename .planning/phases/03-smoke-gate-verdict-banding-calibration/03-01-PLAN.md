---
phase: 03-smoke-gate-verdict-banding-calibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/gate-assert.ts
  - .planning/phases/03-smoke-gate-verdict-banding-calibration/03-GATE-RUNS.md
autonomous: false
requirements: [GATE-01]
must_haves:
  truths:
    - "One real-video E2E on deployed Vercel reaches the `complete` SSE event and yields a non-null overall_score"
    - "The live output is not truncated (hero + apollo_reasoning both present top-level)"
    - "confidence_label is one of HIGH | MEDIUM | LOW"
    - "Apollo §N.N citations are present and use the KNOWLEDGE-CORE §2.x taxonomy"
    - "A real ENG-03 production latency (wall-clock per run) is captured from the deployed run, not local"
    - "A documented same-video score-noise figure (max-min spread + stdev) exists across >=3 sequential runs"
    - "A human has signed off that the produced Reading is honest (not a confident lie) and the expert insight is sane"
  artifacts:
    - path: "scripts/gate-assert.ts"
      provides: "GREEN/RED automated assertions over a captured live-<id>.json (D-06)"
      contains: "confidence_label"
    - path: ".planning/phases/03-smoke-gate-verdict-banding-calibration/03-GATE-RUNS.md"
      provides: "Recorded 3+ run table (score, latency, 429 events) + variance figure + human honesty sign-off"
      contains: "variance"
  key_links:
    - from: "deployed Vercel /api/analyze (authenticated browser fetch)"
      to: "window.__smokeCapture -> live-<id>.json"
      via: "in-browser authenticated fetch accumulating SSE until complete"
      pattern: "__smokeCapture"
    - from: "scripts/gate-assert.ts"
      to: "live-<id>.json fixture"
      via: "readFileSync + field assertions"
      pattern: "apollo_reasoning"
---

<objective>
Clear GATE-01 and produce the GATE-02 variance input in ONE batch: run one near-boundary
video through the DEPLOYED Vercel pipeline >=3 times sequentially via the authenticated
browser-fetch path, capture each live `complete` payload, assert the output is honest
(D-06), and record the per-run production latency + the same-video score-noise figure.

Purpose: The downstream band calibration (Plan 02) and the recorded go/no-go (Plan 03)
both depend on the measured variance figure and on proof the live contract is real. This
batch is folded per D-01 — the same runs serve the smoke-gate assertions, the variance
figure, AND the ENG-03 latency samples.

Output: `scripts/gate-assert.ts` (automated GREEN/RED assertions), captured `live-<id>.json`
fixtures, and `03-GATE-RUNS.md` recording the run table + variance figure + human honesty
sign-off. NO production code changes. NO engine edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-smoke-gate-verdict-banding-calibration/03-CONTEXT.md
@.planning/phases/03-smoke-gate-verdict-banding-calibration/03-RESEARCH.md
@.planning/phases/03-smoke-gate-verdict-banding-calibration/03-PATTERNS.md

# Auth-capture recipe (THE working path — non-obvious, critical)
@$HOME/.claude/projects/-Users-davideloreti-virtuna-v1-1/memory/numen-fixture-capture-auth.md

# Rig tooling — REUSE as-is, do NOT modify
@scripts/capture-reading-fixture.ts
@e2e/create-test-user.ts

<interfaces>
<!-- Field shapes the assertion script checks. From the existing captured fixture
     live-WEkihfOzJphv.json (score 71, confidence_label HIGH, §2.1-2.5 cites). -->
<!-- A live `complete` payload carries these TOP-LEVEL (canonicalFromLive reads them top-level):
     - overall_score: number (e.g. 71) — must be non-null
     - confidence_label: 'HIGH' | 'MEDIUM' | 'LOW' (note: engine uses MEDIUM, not MED)
     - hero: object (present => not truncated, F46/F47)
     - apollo_reasoning: object (present => not truncated) with §N.N citation strings (F23)
  Capture interface (reuse verbatim):
     pnpm tsx scripts/capture-reading-fixture.ts <live-payload.json> <user-id>
     -> writes live-<id>.json + persisted-<id>.json to src/lib/reading/__tests__/fixtures/
  Route auth gate: /api/analyze returns 401 with no Supabase session cookie
     => `--direct` mode CANNOT be the gate driver; only an authenticated browser fetch works. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the GATE-01 automated assertion script (D-06)</name>
  <files>scripts/gate-assert.ts</files>
  <read_first>
    - scripts/capture-reading-fixture.ts (bootstrap + CLI-arg + FIXTURES_DIR idioms to copy)
    - scripts/measure-pipeline.ts (tsconfig-paths register + env guards + greppable output line)
    - src/lib/reading/__tests__/fixtures/live-WEkihfOzJphv.json (the real shapes to assert against)
  </read_first>
  <action>
    Create `scripts/gate-assert.ts` — a thin CLI that takes a captured live payload path
    (`pnpm tsx scripts/gate-assert.ts <live-<id>.json>`) and emits deterministic GREEN/RED
    per D-06. Copy the dotenv + path bootstrap from capture-reading-fixture.ts (no Supabase
    client needed — this reads a local JSON file only). Assertions, each printed as a
    PASS/FAIL line: (1) not truncated — `hero` AND `apollo_reasoning` both present at top
    level (F46/F47); (2) `confidence_label` strictly in the set HIGH, MEDIUM, LOW (F22) —
    accept MED as an alias of MEDIUM but the canonical set is the three engine labels;
    (3) §-cites present — at least one `§N.N` string in apollo_reasoning AND every cite
    matches the KNOWLEDGE-CORE §2.x taxonomy regex `^§2\.\d+$` style (F23); (4) `overall_score`
    non-null and a finite number (F45). Print a final greppable summary line mirroring
    measure-pipeline's style: `GATE_ASSERT=GREEN OVERALL_SCORE=<n> CONFIDENCE=<label> CITES=<count>`
    (or `GATE_ASSERT=RED REASON=<which assertion failed>`), and exit non-zero on any RED.
    Latency is NOT asserted here (it is wall-clock recorded manually in Task 2). Do NOT
    modify capture-reading-fixture.ts, measure-pipeline.ts, or smoke-tiktok-pipeline.ts.
  </action>
  <verify>
    <automated>pnpm tsx scripts/gate-assert.ts src/lib/reading/__tests__/fixtures/live-WEkihfOzJphv.json</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/gate-assert.ts` exists and imports nothing from `src/lib/engine/`
    - Running it against `live-WEkihfOzJphv.json` exits 0 and prints `GATE_ASSERT=GREEN`
    - The greppable line contains `OVERALL_SCORE=71` and `CONFIDENCE=HIGH` for that fixture
    - Feeding a payload with `apollo_reasoning` removed exits non-zero with `GATE_ASSERT=RED`
    - No edits to capture-reading-fixture.ts / measure-pipeline.ts / smoke-tiktok-pipeline.ts (git diff shows them unchanged)
  </acceptance_criteria>
  <done>Assertion script is GREEN on the real near-boundary fixture and RED on a truncated payload; engine + rig scripts untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Run the live variance+latency batch on deployed Vercel (D-01/D-09/D-10) + human honesty sign-off (D-07)</name>
  <read_first>
    - $HOME/.claude/projects/-Users-davideloreti-virtuna-v1-1/memory/numen-fixture-capture-auth.md (the only working auth-capture path)
    - scripts/capture-reading-fixture.ts (the capture/settle step — reuse verbatim)
    - e2e/create-test-user.ts (provisions e2e-test@virtuna.local — run before capturing)
    - .planning/phases/03-smoke-gate-verdict-banding-calibration/03-RESEARCH.md ("The gate run shape" data flow + DashScope-429 behavior)
  </read_first>
  <what-built>
    The `scripts/gate-assert.ts` GREEN/RED harness (Task 1) plus the reused capture path
    (`scripts/capture-reading-fixture.ts`, unchanged) are ready to validate live runs.
  </what-built>
  <how-to-verify>
    Run the same near-boundary video through the DEPLOYED Vercel app 3 times SEQUENTIALLY
    (never parallel — D-10 429 safety). Use video WEkihfOzJphv's source (the already-captured
    near-boundary subject at score 71, sits on the 70 boundary) or `scripts/urls-1.txt`
    (@areyoukiddingtv, ~112s) if a fresh URL is needed.

    For EACH of the 3 runs:
    1. Provision the test user once: `npx tsx e2e/create-test-user.ts`
    2. Playwright-login as e2e-test@virtuna.local against the DEPLOYED Vercel URL (NOT
       localhost — D-09 requires the production rig; the ENG-03 latency must be production-real).
    3. In-browser authenticated `fetch('/api/analyze', ...)` accumulating SSE into
       `window.__smokeCapture` until the `complete` event. Record the wall-clock from
       fetch-start to `complete` = this run's ENG-03 latency sample.
    4. Dump `window.__smokeCapture.complete` to a per-run JSON, then pair it:
       `pnpm tsx scripts/capture-reading-fixture.ts <run-N-live.json> <user-id>`.
    5. Assert it: `pnpm tsx scripts/gate-assert.ts <live-<id>.json>` — must print GATE_ASSERT=GREEN.
    6. Record overall_score, latency, and any DashScope-429 events (frequency + whether
       Apollo degraded — D-10: a 429 is an infra signal, NOT an honesty failure; engine
       backoff handles it; only total inability to ever get one clean E2E blocks).

    D-01b escalation safeguard: if the 3 scores come back SUSPICIOUSLY IDENTICAL, that is
    EXPECTED — the determinism gate (temp:0 + seed:7 + maxRetries:0) IS live in 3.19.0
    (RESEARCH Unknown 1), so variance ~= 0 is by-design; verify each run actually reached
    `complete` (not cached/failed-identical), then lock with the floor (Plan 02 handles the
    floor). If WILDLY SPREAD (>20pt), add runs up to ~5-7 before locking the figure.

    Then write `03-GATE-RUNS.md` recording: the run table (run #, overall_score, latency,
    429 events), the variance figure as BOTH max-min spread AND stdev (lead with max-min —
    directly comparable to the buffer half-width), the production ENG-03 latency (median or
    range of the samples), the deployed URL + permalink to one persisted analysis, and the
    human honesty sign-off (D-07): is the verdict honest (not a confident lie)? is the Apollo
    expert insight sane? PII-review the captured fixtures before they land (no tokens,
    emails, session cookies).
  </how-to-verify>
  <acceptance_criteria>
    - `03-GATE-RUNS.md` contains a run table with >=3 rows, each with a non-null overall_score and a latency sample
    - At least one run printed `GATE_ASSERT=GREEN`
    - The variance figure is recorded as max-min spread AND stdev
    - A production ENG-03 latency number sourced from the DEPLOYED run (not measure-pipeline local) is recorded
    - DashScope-429 frequency + behavior is documented (or "no 429s observed")
    - A dated human honesty sign-off line (verdict honest? insight sane?) is present and affirmative
    - Captured fixtures contain no secrets/tokens/emails (PII-review note recorded)
  </acceptance_criteria>
  <resume-signal>Type "approved" once the run table, variance figure, production latency, and honesty sign-off are recorded in 03-GATE-RUNS.md — or describe what failed (e.g. could never reach a clean `complete`, which blocks per D-10).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev -> deployed Vercel /api/analyze | Authenticated POST crosses into production; carries a Supabase session cookie |
| engine -> DashScope API | Outbound model calls authenticated by DASHSCOPE_API_KEY |
| captured payload -> committed fixture | Live response data persisted into the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | test credentials (e2e-test@virtuna.local) | mitigate | Credentials live ONLY in e2e/create-test-user.ts (existing non-secret dev fixture per RESEARCH Security Domain); gate-assert.ts and 03-GATE-RUNS.md MUST NOT hardcode or echo them. Read from the existing e2e helper / env only. |
| T-03-02 | Information Disclosure | authenticated session cookie | mitigate | The session cookie stays in the browser context; never dumped into committed fixtures or 03-GATE-RUNS.md. PII-review captured payloads before commit (no cookies/tokens/emails). |
| T-03-03 | Information Disclosure | DASHSCOPE_API_KEY | accept | Key stays in .env.local (gitignored, existing pattern); gate-assert.ts reads no engine env. No new exposure surface. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this phase (RESEARCH: Package Legitimacy Audit = N/A). No install task exists; nothing to verify. |

Surface is operational + an assertion script with no new attack surface beyond REUSING the
existing authenticated capture path. Credential-handling mitigation (T-03-01/02) is the only
load-bearing control.
</threat_model>

<verification>
- `pnpm tsx scripts/gate-assert.ts src/lib/reading/__tests__/fixtures/live-WEkihfOzJphv.json` exits 0 / GATE_ASSERT=GREEN
- `03-GATE-RUNS.md` exists with the run table, variance figure, production latency, 429 doc, and human sign-off
- `git diff scripts/capture-reading-fixture.ts scripts/measure-pipeline.ts scripts/smoke-tiktok-pipeline.ts` is empty (reused, not modified)
- No file under `src/lib/engine/` changed; ENGINE_VERSION unchanged
</verification>

<success_criteria>
- GATE-01 satisfied: a real-video E2E on live Vercel returned honest output (F46/F47/F22/F23/score) and a production ENG-03 latency was captured
- The GATE-02 variance input exists: a documented same-video score-noise figure (max-min + stdev) over >=3 sequential runs
- Human honesty sign-off (D-07) recorded and affirmative
</success_criteria>

<output>
Create `.planning/phases/03-smoke-gate-verdict-banding-calibration/03-01-SUMMARY.md` when done.
Record the measured variance half-width explicitly — Plan 02 reads it to set the buffer.
</output>
