---
phase: 02
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/components/app/cards/platform-picker.tsx
  - src/components/app/cards/niche-picker.tsx
  - src/components/app/cards/audience-picker.tsx
  - src/components/app/cards/goal-stage-picker.tsx
  - src/components/app/cards/content-style-picker.tsx
  - src/components/app/cards/reference-creators-input.tsx
  - src/components/app/cards/wins-flops-input.tsx
  - src/components/app/cards/cadence-picker.tsx
  - src/components/app/cards/pain-points-input.tsx
autonomous: true
requirements:
  - PROFILE-03
  - PROFILE-04
  - PROFILE-05
  - PROFILE-06
  - PROFILE-07
  - PROFILE-08
  - PROFILE-09
  - PROFILE-10
  - PROFILE-11
user_setup: []

must_haves:
  truths:
    - "All 9 card-picker components exist as controlled inputs (value/onChange props), reusing primitives from src/components/ui"
    - "Selected tile state uses bg-white/[0.08] + border-white/[0.12] — NOT the accent fill that goal-step.tsx uses"
    - "NichePicker drills down from primary → sub by reading NICHE_TREE from src/lib/niches/taxonomy"
    - "Reference creator and wins/flops inputs cap entries (3 / 2+2) and expose aria-label='Remove creator|win|flop {index+1}'"
    - "PainPointsInput hard-caps at 500 characters and shows the live count"
  artifacts:
    - path: "src/components/app/cards/platform-picker.tsx"
      provides: "Card 0 multi-select platform tile group (TikTok / IG Reels / YT Shorts)"
    - path: "src/components/app/cards/niche-picker.tsx"
      provides: "Card 1 hierarchical drill-down picker reading NICHE_TREE"
    - path: "src/components/app/cards/audience-picker.tsx"
      provides: "Card 2 audience composition (age Select + gender toggle + geo Input + language Input)"
    - path: "src/components/app/cards/goal-stage-picker.tsx"
      provides: "Card 3 goal 2x2 tiles + stage 3-tile row"
    - path: "src/components/app/cards/content-style-picker.tsx"
      provides: "Card 4 style 2x3 tile grid + cuts/sec 3-option toggle"
    - path: "src/components/app/cards/reference-creators-input.tsx"
      provides: "Card 5 dynamic list (max 3) of TikTok handle/URL inputs"
    - path: "src/components/app/cards/wins-flops-input.tsx"
      provides: "Card 6 dual-column dynamic list (max 2 each) for past wins + flops URLs"
    - path: "src/components/app/cards/cadence-picker.tsx"
      provides: "Card 7 posting frequency Select + time-of-day awareness toggle"
    - path: "src/components/app/cards/pain-points-input.tsx"
      provides: "Card 8 Textarea with 500-char live counter"
  key_links:
    - from: "src/components/app/cards/niche-picker.tsx"
      to: "src/lib/niches/taxonomy.ts"
      via: "import { NICHE_TREE, getNicheBranches } from '@/lib/niches/taxonomy'"
      pattern: "@/lib/niches/taxonomy"
    - from: "src/components/app/cards/*.tsx"
      to: "src/components/ui (Button, Input, Textarea, Select, Toggle)"
      via: "import"
      pattern: "from \"@/components/ui"
---

<objective>
Build the 9 leaf card-picker components consumed by the interview modal (Plan 02-04) and
the settings form (Plan 02-05). These are pure controlled inputs — every picker takes
`value` and `onChange` props and contains zero state machine logic. Card-advance + persistence
live one level up in the Zustand store.

Purpose: Decouple the visual pickers from the modal so the settings page can render the
same 9 components as form sections (without the wizard wrapper). This also keeps each
picker's surface tiny enough to land in one task without context blowup.

Output: 9 client components in `src/components/app/cards/` covering PROFILE-03 through
PROFILE-11. Selected-state styling on tile pickers MUST diverge from `goal-step.tsx`'s
accent fill — the UI-SPEC override (no coral on tiles) is the load-bearing visual decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-creator-profile-9-card-interview/02-UI-SPEC.md
@.planning/phases/02-creator-profile-9-card-interview/02-PATTERNS.md
@.planning/phases/02-creator-profile-9-card-interview/02-RESEARCH.md
@.planning/phases/02-creator-profile-9-card-interview/02-CONTEXT.md
@src/components/onboarding/goal-step.tsx
@src/components/ui/button.tsx
@src/components/ui/input.tsx
@src/components/ui/textarea.tsx
@src/components/ui/select.tsx
@src/components/ui/toggle.tsx
@src/lib/niches/taxonomy.ts

<interfaces>
<!-- Every picker exposes a `value` + `onChange` pair. The Plan 02-04 modal store wires
     these into draft state; the Plan 02-05 settings form wires them into a TanStack
     mutation. Keep the shapes EXACTLY as declared here. -->

// src/components/app/cards/platform-picker.tsx
export type PlatformId = "tiktok" | "instagram" | "youtube";
export interface PlatformPickerProps {
  value: PlatformId[];
  onChange: (next: PlatformId[]) => void;
}
export function PlatformPicker(props: PlatformPickerProps): JSX.Element;

// src/components/app/cards/niche-picker.tsx
export interface NichePickerProps {
  primary: string | null;
  sub: string | null;
  onChange: (next: { primary: string | null; sub: string | null }) => void;
}
export function NichePicker(props: NichePickerProps): JSX.Element;

// src/components/app/cards/audience-picker.tsx
export type AgeRange = "13-17" | "18-24" | "25-34" | "35-44" | "45+";
export type GenderSkew = "female" | "balanced" | "male";
export interface TargetAudience {
  age_range: AgeRange | null;
  gender_skew: GenderSkew | null;
  geo: string | null;
  language: string | null;
}
export interface AudiencePickerProps {
  value: TargetAudience;
  onChange: (next: TargetAudience) => void;
}
export function AudiencePicker(props: AudiencePickerProps): JSX.Element;

// src/components/app/cards/goal-stage-picker.tsx
export type CreatorGoal = "growth" | "engagement" | "brand_deals" | "conversion";
export type CreatorStage = "new" | "growing" | "established";
export interface GoalStagePickerProps {
  goal: CreatorGoal | null;
  stage: CreatorStage | null;
  onChange: (next: { goal: CreatorGoal | null; stage: CreatorStage | null }) => void;
}
export function GoalStagePicker(props: GoalStagePickerProps): JSX.Element;

// src/components/app/cards/content-style-picker.tsx
export type ContentStyle = "talking_head" | "b_roll" | "educational" | "comedy" | "tutorial" | "vlog";
export type CutsPerSecond = "slow" | "medium" | "fast"; // <1/s, 1-3/s, 3+/s
export interface ContentStylePickerProps {
  style: ContentStyle | null;
  cuts: CutsPerSecond | null;
  onChange: (next: { style: ContentStyle | null; cuts: CutsPerSecond | null }) => void;
}
export function ContentStylePicker(props: ContentStylePickerProps): JSX.Element;

// src/components/app/cards/reference-creators-input.tsx
export interface ReferenceCreatorEntry {
  handle_or_url: string;
}
export interface ReferenceCreatorsInputProps {
  value: ReferenceCreatorEntry[]; // max 3 entries enforced by component
  onChange: (next: ReferenceCreatorEntry[]) => void;
}
export function ReferenceCreatorsInput(props: ReferenceCreatorsInputProps): JSX.Element;

// src/components/app/cards/wins-flops-input.tsx
export interface UrlEntry { url: string; }
export interface WinsFlopsInputProps {
  wins: UrlEntry[]; // max 2
  flops: UrlEntry[]; // max 2
  onChange: (next: { wins: UrlEntry[]; flops: UrlEntry[] }) => void;
}
export function WinsFlopsInput(props: WinsFlopsInputProps): JSX.Element;

// src/components/app/cards/cadence-picker.tsx
export type PostingFrequency = "daily" | "3-4_per_week" | "1-2_per_week" | "few_per_month" | "rarely";
export interface CadencePickerProps {
  frequency: PostingFrequency | null;
  todAware: boolean;
  onChange: (next: { frequency: PostingFrequency | null; todAware: boolean }) => void;
}
export function CadencePicker(props: CadencePickerProps): JSX.Element;

// src/components/app/cards/pain-points-input.tsx
export interface PainPointsInputProps {
  value: string;
  onChange: (next: string) => void;
}
export function PainPointsInput(props: PainPointsInputProps): JSX.Element;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build tile-picker cards — PlatformPicker (Card 0), GoalStagePicker (Card 3), ContentStylePicker (Card 4)</name>
  <files>
    src/components/app/cards/platform-picker.tsx,
    src/components/app/cards/goal-stage-picker.tsx,
    src/components/app/cards/content-style-picker.tsx
  </files>
  <read_first>
    - .planning/phases/02-creator-profile-9-card-interview/02-UI-SPEC.md §"Card 0" (lines 173-179), §"Card 3" (lines 197-202), §"Card 4" (lines 204-208), §"Color" (lines 82-107) — selected state contract
    - .planning/phases/02-creator-profile-9-card-interview/02-PATTERNS.md §14 (lines 688-732) — tile-button analog from goal-step.tsx + the explicit UI-SPEC override "selected state uses bg-white/[0.08], NOT coral fill"
    - src/components/onboarding/goal-step.tsx lines 60-94 — tile structure (icon + label/description) to copy
    - src/components/ui/button.tsx — Button variants ("primary", "secondary", "ghost") if used as inner elements
    - .planning/phases/02-creator-profile-9-card-interview/02-UI-SPEC.md §"Copywriting Contract" (lines 256-292) — exact labels per card
  </read_first>
  <action>
Create three "use client" components in `src/components/app/cards/`. All three share a
common tile shape — extract no shared helper file, inline the styling (3 files is below
the de-duplication threshold and keeps each card self-contained).

UI-SPEC §Color override (critical — divergent from `goal-step.tsx` analog):
  - Selected tile classes: `bg-white/[0.08] border-white/[0.12]`
  - Unselected: `bg-transparent border-white/[0.06] hover:bg-white/[0.02]`
  - Hover (selected too): hover layer is `hover:bg-white/[0.02]` only on unselected
  - Focus ring: `focus-visible:ring-2 focus-visible:ring-accent/50`
  - Icon container (unselected): `bg-white/[0.05] text-foreground-secondary`
  - Icon container (selected): `bg-white/[0.12] text-foreground` — NOT coral, NOT `bg-accent/20`

`PlatformPicker` (Card 0) — multi-select:
  - 3 tiles in a row: TikTok (icon: `Music2` from lucide), Instagram Reels (icon: `Instagram`), YouTube Shorts (icon: `Youtube`)
  - PlatformId values: "tiktok", "instagram", "youtube"
  - Labels: "TikTok", "Instagram Reels", "YouTube Shorts"
  - Tile renders as `<button type="button" aria-pressed={value.includes(id)}>`
  - Click toggles ID in/out of the `value` array; onChange always receives a new array
  - Layout: `grid grid-cols-3 gap-2`
  - Tile height 80px, icon 24px above label 14px medium, centered
  - At-least-one-selected validation is enforced at the modal level (Plan 02-04) — this component does not display the validation error itself

`GoalStagePicker` (Card 3) — two single-selects:
  - Two labeled sections stacked, label uses `text-sm font-medium`, gap-2 below
  - Section 1 — "Goal" — 2×2 grid: Growth (icon `TrendingUp`), Engagement (icon `Heart`), Brand Deals (icon `Briefcase`), Conversion (icon `DollarSign`)
  - Section 2 — "Stage" — 3-tile row: New creator, Growing, Established (no icons, just label tiles)
  - CreatorGoal values: "growth" | "engagement" | "brand_deals" | "conversion"
  - CreatorStage values: "new" | "growing" | "established"
  - Each tile is `<button type="button" aria-pressed={selected}>`; clicking calls `onChange({ goal, stage })` with the updated half
  - When `goal` or `stage` is null, the matching tiles are all unselected

`ContentStylePicker` (Card 4) — single-select + toggle group:
  - 2×3 tile grid of styles: Talking Head, B-Roll, Educational, Comedy, Tutorial, Vlog (icons optional — use `Mic`, `Video`, `BookOpen`, `Smile`, `GraduationCap`, `Camera`)
  - ContentStyle values: "talking_head" | "b_roll" | "educational" | "comedy" | "tutorial" | "vlog"
  - Below tiles: a small section labeled "Cuts per second preference" with a 3-button toggle row "Slow (<1/s) | Medium (1-3/s) | Fast (3+/s)"
  - CutsPerSecond values: "slow" | "medium" | "fast"
  - Each click updates the relevant half via `onChange({ style, cuts })`

Add `data-testid="card-{cardIndex}-tile-{slug}"` on every tile button to make e2e selectors
stable. Card indices: 0 (Platform), 3 (Goal/Stage — use "card-3-goal-{id}" and "card-3-stage-{id}"),
4 (ContentStyle — use "card-4-style-{id}" and "card-4-cuts-{id}").

All three components are `"use client"`, accept exactly the props in the `<interfaces>` block,
and contain no Supabase calls / no store hooks. They are pure controlled inputs.
  </action>
  <verify>
    <automated>test -f src/components/app/cards/platform-picker.tsx &amp;&amp; test -f src/components/app/cards/goal-stage-picker.tsx &amp;&amp; test -f src/components/app/cards/content-style-picker.tsx &amp;&amp; grep -q 'bg-white/\[0.08\]' src/components/app/cards/platform-picker.tsx &amp;&amp; grep -q 'aria-pressed' src/components/app/cards/platform-picker.tsx &amp;&amp; ! grep -q 'border-accent' src/components/app/cards/platform-picker.tsx &amp;&amp; ! grep -q 'bg-accent/\[0.08\]' src/components/app/cards/goal-stage-picker.tsx &amp;&amp; ! grep -q 'border-accent' src/components/app/cards/content-style-picker.tsx &amp;&amp; pnpm exec tsc --noEmit 2&gt;&amp;1 | (! grep -q 'src/components/app/cards/')</automated>
  </verify>
  <acceptance_criteria>
    - 3 files exist: `platform-picker.tsx`, `goal-stage-picker.tsx`, `content-style-picker.tsx`
    - Each exports the exact named component + props interface declared in the `<interfaces>` block
    - Every tile button has `aria-pressed={...}` (grep `aria-pressed` returns >=1 hit per file)
    - Selected state uses `bg-white/[0.08]` — verified by `grep 'bg-white/\[0.08\]'`
    - NO accent classes on tiles — `grep -E 'border-accent|bg-accent/\[0' src/components/app/cards/{platform-picker,goal-stage-picker,content-style-picker}.tsx` returns empty
    - `data-testid="card-0-tile-{platform}"` exists on PlatformPicker tiles, `card-3-goal-{id}` and `card-3-stage-{id}` on GoalStagePicker, `card-4-style-{id}` and `card-4-cuts-{id}` on ContentStylePicker
    - `pnpm exec tsc --noEmit` does NOT emit errors involving any of the three files
    - Each component is `"use client"` and contains no `from "@/lib/supabase/"` or `from "@/stores/"` imports
  </acceptance_criteria>
  <done>
Three tile-pickers exist as pure controlled components. PlatformPicker enforces no internal
state, GoalStagePicker splits goal/stage cleanly, ContentStylePicker pairs style + cuts. UI-SPEC
color override (no accent on tiles) is grep-enforced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build input-heavy cards — NichePicker (Card 1), AudiencePicker (Card 2), CadencePicker (Card 7)</name>
  <files>
    src/components/app/cards/niche-picker.tsx,
    src/components/app/cards/audience-picker.tsx,
    src/components/app/cards/cadence-picker.tsx
  </files>
  <read_first>
    - .planning/phases/02-creator-profile-9-card-interview/02-UI-SPEC.md §"Card 1" (lines 181-188), §"Card 2" (lines 190-195), §"Card 7" (lines 231-235)
    - .planning/phases/02-creator-profile-9-card-interview/02-PATTERNS.md §17 (lines 792-797) — Select + Toggle composition
    - .planning/phases/02-creator-profile-9-card-interview/02-CONTEXT.md D-09 — 2-level depth (NOT 3-level — the UI-SPEC's optional level-3 micro-niche IS DROPPED per user decision)
    - src/lib/niches/taxonomy.ts (created by Plan 02-02) — `NICHE_TREE`, `getNicheBranches`, `getPrimaryLabel`, `getSubLabel`
    - src/components/ui/select.tsx — Select API (see existing usage)
    - src/components/ui/input.tsx lines 269-313 — InputField pattern for labeled text inputs
    - src/components/ui/toggle.tsx — Switch/Toggle component for time-of-day-aware toggle
    - .planning/phases/02-creator-profile-9-card-interview/02-UI-SPEC.md §"Color" (lines 82-107) — same tile color rules apply to NichePicker tiles
  </read_first>
  <action>
Three "use client" components, more form-heavy than Task 1's pickers.

`NichePicker` (Card 1) — hierarchical drill-down, 2 levels:
  - Imports `NICHE_TREE`, `getNicheBranches`, `getPrimaryLabel`, `getSubLabel` from `@/lib/niches/taxonomy`
  - Renders a grid of primary tiles. Tile style matches Task 1 — `bg-white/[0.08] border-white/[0.12]` when selected. NO coral.
  - Grid: `grid grid-cols-2 sm:grid-cols-3 gap-2`
  - Tile is `<button aria-pressed={primary === slug}>`, label = `getPrimaryLabel(slug)`
  - When `primary !== null`, a second section appears below with breadcrumb "{primaryLabel}" → and a sub-niche grid built from `getNicheBranches(primary)`
  - Sub-niche reveal animation: wrap the sub section in a `<div>` with classes `transition-all duration-200 ease-out` and toggle `max-h-0 opacity-0` ↔ `max-h-[600px] opacity-100` (height-only animation; CSS-only, no JS animation lib needed)
  - Clicking a primary clears `sub` automatically: `onChange({ primary: newPrimary, sub: null })`
  - Clicking a sub: `onChange({ primary, sub: subSlug })`
  - Skippable — no validation required (per UI-SPEC §Card 1)
  - PER D-09: drop the level-3 micro-niche picker entirely. The UI-SPEC §Card 1 mentions level-3 as optional; user explicitly removed it.
  - Add `data-testid="card-1-primary-{slug}"` and `data-testid="card-1-sub-{slug}"`

`AudiencePicker` (Card 2) — composite form, all fields optional:
  - Age range — uses `<Select>` from `@/components/ui/select.tsx` with placeholder "Select age range"
    - Options: { value: "13-17", label: "13-17" }, "18-24", "25-34", "35-44", "45+"
  - Gender skew — a 3-button toggle row using `<button aria-pressed={...}>` tile pattern (same color rules as Task 1)
    - Options: Female-skewed ("female"), Balanced ("balanced"), Male-skewed ("male")
  - Geo — `<Input>` text field, placeholder "Country or region (e.g. United States)"
  - Language — `<Input>` text field, placeholder "Primary language (e.g. English)"
  - Each field's onChange spreads the existing `value` and overwrites that one slot
  - Layout: `space-y-4`, each section has a `<label className="text-sm font-medium">` heading above its control

`CadencePicker` (Card 7) — Select + Switch composition:
  - Frequency `<Select>` placeholder "How often do you post?"
    - Options: { value: "daily", label: "Daily" }, "3-4_per_week" → "3-4×/week", "1-2_per_week" → "1-2×/week", "few_per_month" → "A few times/month", "rarely" → "Rarely"
  - Below: `<Toggle>` (or the existing Switch primitive in `src/components/ui/toggle.tsx`) with label "I pay attention to optimal posting times"
  - Toggle bool maps to `todAware`
  - Skippable per UI-SPEC

All three components are `"use client"`, accept exactly the props in `<interfaces>`, and
contain no store/Supabase calls. They re-render cleanly on prop change.

If the existing `src/components/ui/select.tsx` API differs from what's needed (e.g. it
exposes a different `onChange` signature), READ THE FILE before writing the picker and
adapt the call shape — do not modify `select.tsx` itself.
  </action>
  <verify>
    <automated>test -f src/components/app/cards/niche-picker.tsx &amp;&amp; test -f src/components/app/cards/audience-picker.tsx &amp;&amp; test -f src/components/app/cards/cadence-picker.tsx &amp;&amp; grep -q 'from "@/lib/niches/taxonomy"' src/components/app/cards/niche-picker.tsx &amp;&amp; grep -q "getNicheBranches" src/components/app/cards/niche-picker.tsx &amp;&amp; ! grep -qE "micro_niche|microNiche|micro-niche" src/components/app/cards/niche-picker.tsx &amp;&amp; grep -q 'bg-white/\[0.08\]' src/components/app/cards/niche-picker.tsx &amp;&amp; ! grep -q "border-accent" src/components/app/cards/niche-picker.tsx &amp;&amp; pnpm exec tsc --noEmit 2&gt;&amp;1 | (! grep -q 'src/components/app/cards/')</automated>
  </verify>
  <acceptance_criteria>
    - `niche-picker.tsx` imports from `@/lib/niches/taxonomy` (`grep 'from "@/lib/niches/taxonomy"' ...` returns hit)
    - `niche-picker.tsx` does NOT reference micro-niche, level-3, or third-level concepts (per D-09 — `grep -E 'micro_niche|microNiche|level.?3|micro-niche'` returns empty)
    - All three files use `bg-white/[0.08]` for selected tile state; none reference `border-accent` or `bg-accent/`
    - `audience-picker.tsx` renders all four sub-fields (age, gender, geo, language) — verifiable by `grep -E 'age_range|gender_skew|geo|language' src/components/app/cards/audience-picker.tsx` returning >= 4 hits
    - `cadence-picker.tsx` imports from `@/components/ui/select` AND `@/components/ui/toggle` (or `@/components/ui` index)
    - All three are `"use client"` with no `supabase` or `stores` imports
    - `pnpm exec tsc --noEmit` does NOT emit errors involving any of the three files
  </acceptance_criteria>
  <done>
NichePicker drills down 2 levels (no level-3), AudiencePicker exposes the four sub-fields as a
composite, CadencePicker pairs Select + Switch cleanly. All three are stateless controlled
components ready for the modal store (Plan 02-04) and settings form (Plan 02-05).
  </done>
</task>

<task type="auto">
  <name>Task 3: Build URL-list cards + free-text card — ReferenceCreatorsInput (Card 5), WinsFlopsInput (Card 6), PainPointsInput (Card 8)</name>
  <files>
    src/components/app/cards/reference-creators-input.tsx,
    src/components/app/cards/wins-flops-input.tsx,
    src/components/app/cards/pain-points-input.tsx
  </files>
  <read_first>
    - .planning/phases/02-creator-profile-9-card-interview/02-UI-SPEC.md §"Card 5" (lines 210-217), §"Card 6" (lines 219-229), §"Card 8" (lines 237-243)
    - .planning/phases/02-creator-profile-9-card-interview/02-PATTERNS.md §15 (lines 736-770) — dynamic URL list pattern
    - src/components/ui/input.tsx lines 269-313 — `InputField` (the labeled-input wrapper)
    - src/components/ui/textarea.tsx — Textarea API
    - .planning/phases/02-creator-profile-9-card-interview/02-UI-SPEC.md §"Accessibility" lines 339-352 — exact aria-label strings for remove buttons
    - .planning/phases/02-creator-profile-9-card-interview/02-CONTEXT.md D-06 — Card 5 accepts handle or URL, no validation, raw persisted string
  </read_first>
  <action>
Three "use client" components — dynamic-list cards plus the free-text card.

`ReferenceCreatorsInput` (Card 5):
  - Renders up to 3 URL input rows. Each row: an `<Input>` + a ghost-button "X" icon (lucide `X`) on the right.
  - Empty initial state: render ONE empty row (placeholder visible) so the user has a clear target.
  - Placeholder: `"@handle or TikTok profile URL"`
  - Below rows: a ghost button `"+ Add creator"` — visible only when `value.length < 3`
  - Add button: appends `{ handle_or_url: "" }` to the array via onChange
  - Remove button: filters out that index via onChange. aria-label MUST be exactly `"Remove creator {index + 1}"` (1-indexed; e.g. "Remove creator 1", "Remove creator 2", "Remove creator 3")
  - NO URL validation — accept any raw string (per D-06). Handle parsing happens at save-time in Plan 02-06.
  - Each row's input onChange: `(e) => onChange(value.map((entry, idx) => idx === rowIndex ? { handle_or_url: e.target.value } : entry))`
  - data-testid="card-5-input-{index}" on each input, data-testid="card-5-remove-{index}" on each remove button, data-testid="card-5-add" on the add button

`WinsFlopsInput` (Card 6):
  - Two-column layout (`grid grid-cols-1 sm:grid-cols-2 gap-4`):
    - Column 1 — heading "Past wins" — up to 2 URL inputs + "+ Add win" ghost button when `wins.length < 2`
    - Column 2 — heading "Past flops" — up to 2 URL inputs + "+ Add flop" ghost button when `flops.length < 2`
  - Placeholder: `"TikTok video URL"`
  - aria-label on remove buttons EXACTLY: `"Remove win {index + 1}"` for the wins column, `"Remove flop {index + 1}"` for the flops column (1-indexed)
  - onChange always emits the full `{ wins, flops }` object; each column update preserves the other column
  - data-testid="card-6-win-{index}", "card-6-flop-{index}", "card-6-win-remove-{index}", "card-6-flop-remove-{index}", "card-6-win-add", "card-6-flop-add"
  - NOTE: The TruthfulnessCallout appears ABOVE this picker — but it lives inside the Card 6 wrapper (rendered by Plan 02-04's modal). Do NOT render the callout from inside this picker.

`PainPointsInput` (Card 8):
  - Uses `<Textarea size="md" rows={4}>` from `@/components/ui/textarea`
  - Placeholder: `"What's your biggest challenge as a creator right now?"`
  - Hard cap at 500 characters — onChange filters `value.slice(0, 500)` before forwarding via `onChange`
  - Below the textarea (right-aligned): `<p className="text-xs text-foreground-muted text-right">{value.length} / 500</p>`
  - The Textarea's `maxLength={500}` attribute also enforces the cap on the input element
  - data-testid="card-8-textarea" on the textarea, "card-8-counter" on the counter

All three components are pure controlled. No store imports, no Supabase imports, no async work.
  </action>
  <verify>
    <automated>test -f src/components/app/cards/reference-creators-input.tsx &amp;&amp; test -f src/components/app/cards/wins-flops-input.tsx &amp;&amp; test -f src/components/app/cards/pain-points-input.tsx &amp;&amp; grep -q 'Remove creator' src/components/app/cards/reference-creators-input.tsx &amp;&amp; grep -q 'Remove win' src/components/app/cards/wins-flops-input.tsx &amp;&amp; grep -q 'Remove flop' src/components/app/cards/wins-flops-input.tsx &amp;&amp; grep -q 'maxLength={500}' src/components/app/cards/pain-points-input.tsx &amp;&amp; pnpm exec tsc --noEmit 2&gt;&amp;1 | (! grep -q 'src/components/app/cards/')</automated>
  </verify>
  <acceptance_criteria>
    - 3 files exist: `reference-creators-input.tsx`, `wins-flops-input.tsx`, `pain-points-input.tsx`
    - `reference-creators-input.tsx` contains the exact aria-label pattern matching `aria-label={`Remove creator ${index + 1}`}` (or equivalent template literal evaluating to "Remove creator 1", "Remove creator 2", "Remove creator 3")
    - `wins-flops-input.tsx` contains aria-label patterns matching both "Remove win {index + 1}" and "Remove flop {index + 1}"
    - `pain-points-input.tsx` has `maxLength={500}` on the textarea AND a live counter element showing `{value.length} / 500`
    - No URL validation regex in `reference-creators-input.tsx` (per D-06)
    - `pnpm exec tsc --noEmit` emits no errors involving any of the three files
    - Each file is `"use client"` and contains no `supabase` or `stores` imports
  </acceptance_criteria>
  <done>
Dynamic URL lists honor the 3 / 2+2 caps, expose exact aria-labels for accessibility, and
the pain points textarea enforces the 500-char cap at both the JS layer (slice) and the
HTML attribute. All 9 card pickers are now complete and ready for the modal (Plan 02-04)
and settings form (Plan 02-05) to consume.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (typed text) -> React state | All three URL inputs accept raw strings without validation per D-06; sanitation/parsing happens at save-time in Plan 02-06 (handle normalization) and Plan 02-05 (zod schema on the API boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01c | T (Tampering) — prompt injection precursor | reference-creators-input + wins-flops-input + pain-points-input | mitigate | Pain points hard-capped at 500 chars in the component. Reference and win/flop URL inputs cap entry count (3 / 2+2). Length/count caps reduce blast radius if the user pastes adversarial content destined for Gemini prompts via CreatorContext. Full sanitation (control-character strip + zod max length) lands in Plan 02-05 at the API boundary. |
| T-02-03a | I (Information Disclosure) — Apify cost runaway | reference-creators-input | mitigate | 3-entry cap on Card 5 inputs prevents a single profile save from triggering unbounded Apify scrape jobs. Deduplication against existing competitors happens in Plan 02-06 (addCompetitor idempotency on `23505` unique constraint). |
</threat_model>

<verification>
- `find src/components/app/cards -name '*.tsx' -not -path '*/__tests__/*' | wc -l` returns `9`
- `pnpm exec tsc --noEmit` emits no errors related to any picker file
- `grep -RE "border-accent|bg-accent/\[0" src/components/app/cards/` returns empty (UI-SPEC color override is enforced everywhere)
- All exported component names match the `<interfaces>` block exactly so Plan 02-04 imports compile without modification
</verification>

<success_criteria>
- 9 picker components in `src/components/app/cards/` covering PROFILE-03..11
- All controlled — no internal state machines, no Supabase calls, no async work
- UI-SPEC color override (no accent on tile selected state) enforced by grep gates
- Exact aria-labels for remove buttons (Card 5: "Remove creator N"; Card 6: "Remove win N" / "Remove flop N")
- Card 8 hard-caps at 500 characters with a live counter
</success_criteria>

<output>
After completion, create `.planning/phases/02-creator-profile-9-card-interview/02-03-SUMMARY.md`
listing the 9 component files with their exported names, the exact prop shapes, and the
data-testid prefixes per card (so Plan 02-06 e2e tests can target stable selectors).
</output>
