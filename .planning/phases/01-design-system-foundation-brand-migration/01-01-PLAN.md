---
phase: 01-design-system-foundation-brand-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/globals.css
  - src/app/layout.tsx
  - scripts/check-apca.ts
  - tests/numen/tokens.test.ts
  - tests/numen/type.test.ts
  - tests/numen/glass.test.ts
  - tests/numen/primitives.test.ts
  - tests/numen/stage-reveal.test.ts
autonomous: false
requirements: [DS-01, DS-02, DS-03, DS-04]
user_setup: []

must_haves:
  truths:
    - "Utilities bg-bg / text-text / bg-accent / text-verdict-good resolve to warm-neutral hex values when rendered inside .numen-surface, and are unaffected outside it (D-01 parallel namespaced layer; D-02 scope-class + semantic token names)"
    - "No pure black (#000 / #000000) exists in the .numen-surface token block; base is #1a1714 (D-11 calibrated palette base)"
    - "Every dark token in the .numen-surface block is authored as exact hex, never oklch() (D-03)"
    - "scripts/check-apca.ts runs and asserts each verdict + accent + text pairing meets its APCA Lc target on #1a1714 (D-12 APCA-aware contrast)"
    - "--font-serif is wired via next/font/google (Source Serif 4) and bridged into @theme inline alongside the existing --font-inter (D-13 voice serif)"
  artifacts:
    - path: "src/app/globals.css"
      provides: ".numen-surface scope-class token block + new @theme inline bridge"
      contains: ".numen-surface"
    - path: "src/app/layout.tsx"
      provides: "Source Serif 4 voice font wired as --font-serif"
      contains: "Source_Serif_4"
    - path: "scripts/check-apca.ts"
      provides: "Dev-only APCA contrast gate"
      contains: "APCAcontrast"
    - path: "tests/numen/tokens.test.ts"
      provides: "Resolved-CSS-var assertions for the scope-class token layer"
  key_links:
    - from: "src/app/globals.css @theme inline"
      to: ".numen-surface custom properties"
      via: "var(--numen-*) reference resolved at usage site"
      pattern: "var\\(--numen-"
    - from: "src/app/layout.tsx"
      to: "@theme inline --font-serif"
      via: "next/font variable injected on <html> className"
      pattern: "serif\\.variable"
---

<objective>
Lay the foundation of the Numen warm-neutral kit: install the two new deps (gated), scaffold the Wave-0 failing tests, write the APCA contrast gate, author the parallel `.numen-surface` token layer in `globals.css` (DS-01/DS-02/DS-03), calibrate the palette, and wire the Source Serif 4 voice font (DS-04).

Purpose: Every later primitive, motion component, and Reading block resolves its colors and serif voice against this layer. It must coexist with the live coral/Raycast app untouched (D-01).
Output: Token scope block + @theme inline bridge, calibrated palette validated by APCA, serif font wiring, dev APCA script, and 5 failing Wave-0 test files the downstream plans turn green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation-brand-migration/01-CONTEXT.md
@.planning/phases/01-design-system-foundation-brand-migration/01-RESEARCH.md
@.planning/phases/01-design-system-foundation-brand-migration/01-PATTERNS.md
@.planning/phases/01-design-system-foundation-brand-migration/01-UI-SPEC.md

<interfaces>
<!-- Existing precedents the executor copies STRUCTURE from. Do NOT touch the existing @theme coral block. -->

From src/app/globals.css (existing, KEEP untouched):
- line 4: `@custom-variant dark (&:is(.dark *));` — the dark variant the new scope stacks under
- lines 6+: `@theme { --color-coral-*, --color-gray-* … }` — OLD coral/Raycast layer, DO NOT MODIFY
- lines 28-38: hex-for-dark precedent already in repo (`--color-gray-950: #07080a`) — confirms D-03 hex practice
- lines 311-330: existing `@theme inline { --animate-marquee … }` — THE precedent proving the inline-bridge mechanism
- line 170: `--ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);` — the FORBIDDEN bounce token (do not reuse; relevant to Plan 03)

From src/app/layout.tsx (existing, EXTEND):
- `import { Inter } from "next/font/google";`
- `const inter = Inter({ subsets: ["latin"], display: "swap", variable: "--font-inter" });`
- `<html lang="en" className={`${inter.variable}`}>`
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Install gate — tailwind-variants + apca-w3 (supply-chain checkpoint)</name>
  <read_first>
    - .planning/phases/01-design-system-foundation-brand-migration/01-RESEARCH.md (§ Package Legitimacy Audit + § Standard Stack: both deps are [VERIFIED: npm registry] but tailwind-variants name originated in CONTEXT not an authoritative source → gated per protocol)
  </read_first>
  <what-built>Two new dependencies are about to be installed: `tailwind-variants@^3.2.2` (prod — the slot/variant API for multi-part primitives, D-08) and `apca-w3@^0.1.9` (dev — the W3C reference APCA contrast algorithm, D-12). Both are [ASSUMED]-tier per the package-legitimacy gate (slopcheck unavailable in this sandbox).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/tailwind-variants — confirm: maintained by the HeroUI org (github.com/heroui-inc/tailwind-variants), latest ~3.2.2, high download count, official docs at tailwind-variants.org.
    2. Open https://www.npmjs.com/package/apca-w3 — confirm: by Myndex (W3C APCA reference impl, github.com/Myndex/apca-w3), last publish ~2022 (expected — frozen reference algorithm, not abandonment).
    3. Confirm neither has a concerning postinstall script.
  </how-to-verify>
  <action>This checkpoint is NEVER auto-approvable (legitimacy gate). After the human approves, the executor runs `pnpm add tailwind-variants` and `pnpm add -D apca-w3`. Do NOT run `pnpm remove framer-motion` — that touches 4 live files and is out of Phase-1 scope per D-04 (deferred to a later surface-rebuild phase). Do NOT install Phosphor, Base UI, or any Magic UI / Aceternity package.</action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies['tailwind-variants'])throw new Error('tailwind-variants missing'); if(!p.devDependencies['apca-w3'])throw new Error('apca-w3 missing'); console.log('deps ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies` contains `tailwind-variants` (^3.2.2 or compatible)
    - `package.json` `devDependencies` contains `apca-w3`
    - `framer-motion` is STILL present in `dependencies` (NOT removed — D-04 deferral)
    - No Phosphor / Base UI / Magic UI / Aceternity package added
  </acceptance_criteria>
  <resume-signal>Type "approved" after verifying both packages on npmjs.com, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wave-0 — failing test scaffolds + APCA contrast gate</name>
  <read_first>
    - src/components/sidebar/__tests__/Sidebar.a11y.test.tsx (THE harness analog: `@vitest-environment happy-dom` pragma, `render` + `axe`, `vi.mock`)
    - src/lib/utils.ts (the `cn()` convention)
    - .planning/phases/01-design-system-foundation-brand-migration/01-RESEARCH.md (§ Code Examples: the exact `apca-w3` call `APCAcontrast(sRGBtoY(colorParsley(text)), sRGBtoY(colorParsley(bg)))`; § Validation Architecture: the per-req test map)
    - .planning/phases/01-design-system-foundation-brand-migration/01-VALIDATION.md (Wave 0 Requirements list + Per-Task Verification Map)
  </read_first>
  <action>Create the 5 Wave-0 test files under `tests/numen/` so downstream plans have automated gates, plus the dev APCA script. Each test file uses the `/** @vitest-environment happy-dom */` pragma (copy from the Sidebar a11y analog) and runs via `pnpm vitest run <file>`. These are scaffolds that import not-yet-existing modules — they are EXPECTED to fail RED now; Plans 02/03/04 turn them GREEN.

Files and required assertions:
- `tests/numen/tokens.test.ts` (DS-01): render a `<div className="numen-surface dark">` with a child carrying `bg-bg` / `text-text` / `bg-accent` / `text-verdict-good`, then assert `getComputedStyle` (happy-dom) resolves them to the warm-neutral hexes (NOT empty, NOT transparent). Also assert a string scan of `src/app/globals.css` `.numen-surface` block contains no `#000000`/`#000`/`oklch(` and contains `#1a1714`.
- `tests/numen/type.test.ts` (DS-04): assert an element with `font-serif` resolves to the `--font-serif` family var and that body/`font-sans` resolves to the Inter var; assert serif is NOT applied to a plain body element.
- `tests/numen/glass.test.ts` (DS-05): assert `<Glass>` from `@/components/numen/glass` renders with an inline `style.backdropFilter` matching `/blur\(\d+px\)/` and an inline `style.WebkitBackdropFilter`, and that it carries NO `backdrop-blur` utility class.
- `tests/numen/primitives.test.ts` (DS-05): assert the `tailwind-variants` primitives (`pill-chip`, `icon-button`, `verdict-swatch`, `surface`) export slot/variant functions and that the pill-chip `agentic` intent produces a different class string than `instant`; icon-button enforces a `min-h-[44px]` / `min-w-[44px]` class.
- `tests/numen/stage-reveal.test.ts` (DS-07): mock `motion/react` `useReducedMotion` to return `true` and assert `<StageBlock show>` renders children with NO translate/`y` (static), and that the easing used is never the forbidden `cubic-bezier(0.34, 1.56, 0.64, 1)`.

Create `scripts/check-apca.ts`: import `{ APCAcontrast, sRGBtoY, colorParsley }` from `apca-w3`; define an `lc(textHex, bgHex)` helper returning `Math.abs(APCAcontrast(sRGBtoY(colorParsley(textHex)), sRGBtoY(colorParsley(bgHex))))`; assert against the base `#1a1714`: body `#f0ebe3` ≥ Lc 75 (floor; 90 preferred), muted `#a39c91` (non-body UI) ≥ Lc 60, accent `#d98a5e` text-on-base ≥ Lc 45 (large/heavy), and each verdict swatch (`#7faf7a` / `#d6a85a` / `#c97a64`) ≥ Lc 45 as a large band label. Exit non-zero on any failure so it is a real gate. Run via `pnpm tsx scripts/check-apca.ts`. NOTE: these hexes are the D-11 proposal — if Task 4 adjusts them after user sign-off, the script's asserted hexes update to match.</action>
  <verify>
    <automated>pnpm vitest run tests/numen/tokens.test.ts tests/numen/glass.test.ts tests/numen/primitives.test.ts tests/numen/stage-reveal.test.ts tests/numen/type.test.ts 2>&1 | tail -20 ; echo "EXPECT: tests RED (modules not yet built) — scaffolds exist and execute"</automated>
  </verify>
  <acceptance_criteria>
    - 5 files exist under `tests/numen/` each starting with `/** @vitest-environment happy-dom */`
    - `scripts/check-apca.ts` exists, imports `APCAcontrast`/`sRGBtoY`/`colorParsley` from `apca-w3`, and exits non-zero on a failing pairing
    - `grep -c 'APCAcontrast' scripts/check-apca.ts` returns ≥ 1
    - Running the vitest command executes (does not error on syntax); tests fail RED on missing `@/components/numen/*` imports — this is the expected pre-implementation state
  </acceptance_criteria>
  <done>5 Wave-0 test scaffolds + APCA script exist and run; tests are RED pending Plans 02/03/04; APCA script is executable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the .numen-surface token layer + @theme inline bridge</name>
  <read_first>
    - src/app/globals.css (CURRENT state — see the existing `@theme` coral block lines 6+ which MUST stay untouched, the `@custom-variant dark` line 4, and the existing `@theme inline { --animate-marquee }` block lines 311-330 = the bridge precedent)
    - .planning/phases/01-design-system-foundation-brand-migration/01-RESEARCH.md (Pattern 1 = THE core pattern with the exact CSS; Pitfall 1 = `@theme` vs `@theme inline` resolution landmine; Pitfall 3 = oklch L<0.15 bug)
    - .planning/phases/01-design-system-foundation-brand-migration/01-PATTERNS.md (§ globals.css APPEND scope block — copy STRUCTURE not values; § "DO NOT carry into the Numen kit" anti-patterns)
    - .planning/phases/01-design-system-foundation-brand-migration/01-UI-SPEC.md (§ Color table = the proposed hexes)
  </read_first>
  <behavior>
    - Test 1 (tokens.test.ts): `bg-bg` inside `.numen-surface` resolves to `#1a1714`, not empty/transparent
    - Test 2: `text-text` resolves to `#f0ebe3`; `bg-accent` to the clay hex; `text-verdict-good` to the muted-green hex
    - Test 3: the `.numen-surface` block source contains `#1a1714` and contains NO `oklch(` and NO `#000`/`#000000`
    - Test 4: utilities outside `.numen-surface` are unaffected (the old coral `@theme` still resolves)
  </behavior>
  <action>APPEND (never edit the existing coral `@theme`) a new block to `src/app/globals.css`. Two parts per Research Pattern 1:

(1) Plain custom properties scoped under the selector, authored as EXACT HEX (D-03 — never `oklch()` for these L<0.15 darks):
`.numen-surface { --numen-bg:#1a1714; --numen-panel:#211e1a; --numen-panel-2:#2a2622; --numen-text:#f0ebe3; --numen-text-muted:#a39c91; --numen-accent:#d98a5e; --numen-verdict-good:#7faf7a; --numen-verdict-mixed:#d6a85a; --numen-verdict-bad:#c97a64; --numen-border:rgba(240,235,227,0.06); }`
Stack the dark variant: also emit `.dark .numen-surface { … }` if any value must differ under `.dark` (for Phase 1 they are identical — dark-only kit — so a single `.numen-surface` rule suffices; document that choice in a CSS comment).

(2) A NEW `@theme inline` block (the `inline` keyword is LOAD-BEARING per Pitfall 1 — a plain `@theme` makes `var(--numen-bg)` resolve at `:root` where it is undefined → empty utilities). Map: `--color-bg:var(--numen-bg); --color-panel:var(--numen-panel); --color-panel-2:var(--numen-panel-2); --color-text:var(--numen-text); --color-text-muted:var(--numen-text-muted); --color-accent:var(--numen-accent); --color-verdict-good:var(--numen-verdict-good); --color-verdict-mixed:var(--numen-verdict-mixed); --color-verdict-bad:var(--numen-verdict-bad); --color-border:var(--numen-border); --font-serif:var(--font-serif);` (the `--font-serif` map pairs with Task 5).

Hard rules: do NOT copy the Raycast `--animate-shimmer`/`gradient-x` keyframes or any coral/gradient token into this block (D-07 anti-pattern). Do NOT touch the existing `@theme` coral block. Add a comment header marking the block as the Numen parallel layer (D-01).</action>
  <verify>
    <automated>pnpm vitest run tests/numen/tokens.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\.numen-surface' src/app/globals.css` ≥ 1
    - `grep -A12 '\.numen-surface' src/app/globals.css | grep -v '^#' | grep -c 'oklch('` returns `0` (no oklch in the scope block)
    - `grep -A12 '\.numen-surface' src/app/globals.css | grep -c '#1a1714'` ≥ 1
    - `grep -A12 '\.numen-surface' src/app/globals.css | grep -ic '#000000\|#000\b'` returns `0`
    - A NEW `@theme inline` block exists mapping `--color-bg: var(--numen-bg)` (literal `inline` keyword present, not plain `@theme`)
    - The existing coral `@theme {` block and `--color-coral-500` are byte-unchanged
    - `tests/numen/tokens.test.ts` passes GREEN
  </acceptance_criteria>
  <done>The parallel warm-neutral token layer resolves under `.numen-surface` via `@theme inline`; old pages unaffected; tokens.test.ts green.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Palette calibration sign-off (D-11 §9 fork)</name>
  <read_first>
    - .planning/phases/01-design-system-foundation-brand-migration/01-UI-SPEC.md (§ Color — the proposal table + accent-reserved rules)
    - .planning/phases/01-design-system-foundation-brand-migration/01-CONTEXT.md (D-11, D-12)
  </read_first>
  <decision>Approve or adjust the warm-neutral palette hexes. The APCA script (`pnpm tsx scripts/check-apca.ts`) is the objective pass/fail gate; the user owns the final aesthetic call on hue (D-11 retains user final say).</decision>
  <context>Direction is locked (warm-neutral / muted verdict scale / matured-clay accent). Only the exact hues are calibration. Run `pnpm tsx scripts/check-apca.ts` first and report the Lc result per pairing so the user decides against real contrast numbers, not vibes.</context>
  <options>
    <option id="approve-proposal">
      <name>Approve the D-11 proposal as-is</name>
      <pros>Already APCA-validated by the script; unblocks immediately</pros>
      <cons>User hasn't seen the hues rendered yet (full visual sign-off happens on the deployed showcase in Plan 04)</cons>
    </option>
    <option id="adjust">
      <name>Adjust specific hexes</name>
      <pros>Tunes accent warmth / verdict mutedness to taste</pros>
      <cons>Each adjusted hex must re-pass `check-apca.ts` before proceeding</cons>
    </option>
  </options>
  <action>If the user adjusts any hex: update BOTH the `.numen-surface` block in `globals.css` AND the asserted hexes in `scripts/check-apca.ts`, then re-run the APCA script and confirm all pairings still pass their Lc target. If approved as-is, no change. Either way the final hexes are locked here for the rest of the phase. Visual sign-off of the rendered swatches is deferred to Plan 04's deployed showcase (it is NOT re-opened here).</action>
  <verify>
    <automated>pnpm tsx scripts/check-apca.ts && echo "APCA gate passed for locked palette"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm tsx scripts/check-apca.ts` exits 0 (all pairings meet their Lc target) for whatever hexes are locked
    - If any hex changed, both `globals.css` and `check-apca.ts` reflect the identical new value
  </acceptance_criteria>
  <resume-signal>Type "approved" to lock the proposed palette, or specify hex adjustments.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Wire Source Serif 4 voice font (DS-04)</name>
  <read_first>
    - src/app/layout.tsx (CURRENT — the Inter wiring at the top + the `<html className={`${inter.variable}`}>` line to extend)
    - .planning/phases/01-design-system-foundation-brand-migration/01-RESEARCH.md (Pattern 4 = the next/font serif wiring; D-13 = Source Serif 4 lead)
    - .planning/phases/01-design-system-foundation-brand-migration/01-PATTERNS.md (§ layout.tsx ADD serif voice font)
    - .planning/phases/01-design-system-foundation-brand-migration/01-UI-SPEC.md (§ Typography — serif reserved for voice ONLY)
  </read_first>
  <action>Extend `src/app/layout.tsx` mirroring the existing Inter pattern. Import `Source_Serif_4` from `next/font/google`; declare `const serif = Source_Serif_4({ subsets: ["latin"], display: "swap", variable: "--font-serif" });` (variable font → no `weight` array; opsz axis auto). Append `${serif.variable}` to the existing `<html>` className so it reads `className={`${inter.variable} ${serif.variable}`}`. The `--font-serif` → utility bridge was already added to `@theme inline` in Task 3; this task supplies the actual variable value. Do NOT apply `font-serif` to `<body>` or any functional element — serif is reserved for voice slots (greeting/hero + verdict line), surfaced only where a component opts in via `className="font-serif"` (the showcase in Plan 04 demonstrates it). Newsreader is the alt to specimen in Plan 04 — for now wire only Source Serif 4 as the lead.</action>
  <verify>
    <automated>pnpm vitest run tests/numen/type.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Source_Serif_4' src/app/layout.tsx` ≥ 1
    - `grep -c 'serif.variable' src/app/layout.tsx` ≥ 1
    - The `<html>` className includes both `${inter.variable}` and `${serif.variable}`
    - `<body>` does NOT carry a `font-serif` class (serif reserved for voice only)
    - `tests/numen/type.test.ts` passes GREEN
  </acceptance_criteria>
  <done>Source Serif 4 self-hosted as `--font-serif`, bridged to the `font-serif` utility, reserved for voice; type.test.ts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | New dependency code crosses into the build (supply-chain) |
| Google Fonts → build | `next/font/google` fetches font files at build time, self-hosts (no runtime CDN) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `tailwind-variants` + `apca-w3` installs | mitigate | Blocking human checkpoint (Task 1, never auto-approvable) + npmjs.com verification; both [VERIFIED] on registry in RESEARCH |
| T-01-FNT | Tampering | `next/font/google` Source Serif 4 | accept | next/font self-hosts at build — no runtime CDN fetch; Google Fonts is a trusted source; low risk |
| T-01-02 | Information disclosure | token hexes / palette | accept | Public design tokens, no secrets; nothing sensitive in CSS vars |
</threat_model>

<verification>
- `pnpm vitest run tests/numen/tokens.test.ts tests/numen/type.test.ts` both GREEN
- `pnpm tsx scripts/check-apca.ts` exits 0
- `pnpm build` completes without error (token layer compiles under Lightning CSS)
- Existing coral `@theme` block in globals.css byte-unchanged (old pages unaffected)
</verification>

<success_criteria>
- `.numen-surface` token layer resolves warm-neutral hexes via `@theme inline` (DS-01)
- Verdict scale + accent locked and APCA-validated (DS-02/DS-03)
- Source Serif 4 wired as `--font-serif`, reserved for voice (DS-04)
- No pure black, no oklch in dark tokens, existing app untouched
- 5 Wave-0 tests scaffolded (tokens + type GREEN; glass/primitives/stage-reveal RED pending downstream plans)
</success_criteria>

<output>
Create `.planning/phases/01-design-system-foundation-brand-migration/01-01-SUMMARY.md` when done
</output>
