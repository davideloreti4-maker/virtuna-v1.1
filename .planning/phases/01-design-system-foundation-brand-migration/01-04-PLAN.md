---
phase: 01-design-system-foundation-brand-migration
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - src/app/(kit)/numen-kit/page.tsx
  - tests/numen/showcase.a11y.test.tsx
autonomous: false
requirements: [DS-04, DS-05, DS-07, DS-08]
user_setup: []

must_haves:
  truths:
    - "A deployed-buildable showcase route renders every Numen primitive inside .numen-surface against live warm-neutral tokens"
    - "The showcase specimens the serif voice (Source Serif 4 vs Newsreader) on the greeting/hero + verdict line at real sizes on #1a1714 so the user can pick"
    - "The glass primitive's blur is visible on a DEPLOYED build (Lightning CSS did not strip it)"
    - "A sample keyframe still sits beside near-neutral chrome so the chrome visibly recedes and the image carries the chroma (DS-08)"
    - "The showcase passes axe with no violations"
  artifacts:
    - path: "src/app/(kit)/numen-kit/page.tsx"
      provides: "Deployed kit showcase under .numen-surface — primitives, serif specimen, stage-reveal demo, keyframe-chroma panel"
      contains: "numen-surface"
    - path: "tests/numen/showcase.a11y.test.tsx"
      provides: "axe a11y gate on the showcase render"
  key_links:
    - from: "src/app/(kit)/numen-kit/page.tsx"
      to: "src/components/numen/*"
      via: "imports every primitive + StageBlock"
      pattern: "@/components/numen/"
    - from: "showcase root"
      to: ".numen-surface tokens"
      via: "wrapping <div className=\"numen-surface dark\">"
      pattern: "numen-surface dark"
---

<objective>
Build the kit showcase route (D-06) that mounts every Numen primitive inside `.numen-surface` against live tokens, doubles as the serif-specimen surface (D-13) and the keyframe-as-chroma demonstration (DS-08), and serves as the deployed-build verification surface that proves the glass blur survives Lightning CSS (D-05) and the tokens render with no pure black (DS-01). Closes with the human-verify deployed-build checkpoint.

Purpose: This route is how the success criteria "verified on a deployed build" is satisfied (criteria 1 & 3). It is also where the user signs off the serif pick and confirms the warm-neutral chrome recedes.
Output: `src/app/(kit)/numen-kit/page.tsx` + an axe test, plus a human-verify checkpoint on the deployed build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation-brand-migration/01-RESEARCH.md
@.planning/phases/01-design-system-foundation-brand-migration/01-PATTERNS.md
@.planning/phases/01-design-system-foundation-brand-migration/01-UI-SPEC.md

<interfaces>
<!-- The showcase imports the Plan 02/03 primitives and mounts them under the Plan 01 scope class. -->

From src/app/(marketing)/primitives-showcase/page.tsx (the showcase analog):
- `export const metadata = { title, description };`
- `export default function …Page() { return (<div className="min-h-screen … p-8 md:p-16"><h1>…</h1><section className="mb-16">{/* one section per primitive */}</section>…</div>); }`

Primitives to import (from Plans 02/03):
- `@/components/numen/glass` → `Glass` (inline backdrop)
- `@/components/numen/surface` → `Surface`
- `@/components/numen/pill-chip` → tool chip (instant + agentic intents)
- `@/components/numen/icon-button` → circular icon button
- `@/components/numen/verdict-swatch` → good/mixed/bad swatches
- `@/components/numen/stage-reveal` → `StageBlock`

Serif: `font-serif` utility (Plan 01 `--font-serif` = Source Serif 4). Newsreader specimened via a second next/font instance OR a documented comparison — see action.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the kit showcase route + a11y test</name>
  <read_first>
    - src/app/(marketing)/primitives-showcase/page.tsx (the metadata export + section-per-primitive layout to copy STRUCTURE)
    - src/components/sidebar/__tests__/Sidebar.a11y.test.tsx (the `@vitest-environment happy-dom` + `render` + `axe` harness for the a11y test)
    - .planning/phases/01-design-system-foundation-brand-migration/01-PATTERNS.md (§ numen-kit/page.tsx — mount inside `.numen-surface dark`, keep SEPARATE from the old showcase, (kit) route group)
    - .planning/phases/01-design-system-foundation-brand-migration/01-RESEARCH.md (Q9 = showcase route under a `(kit)` route group; doubles as serif specimen + deployed glass check)
    - .planning/phases/01-design-system-foundation-brand-migration/01-UI-SPEC.md (§ Typography serif roles; § Color keyframes carry chroma; § Copywriting Contract showcase copy: "Nothing here yet" / "This is a kit showcase — components render below under the Numen surface tokens.")
    - All six primitive files from Plans 02/03 (their exact prop signatures)
  </read_first>
  <action>Create `src/app/(kit)/numen-kit/page.tsx` under a NEW `(kit)` route group (keep it separate from the old `(marketing)/primitives-showcase` — parallel kit, D-01). Export `metadata = { title: "Numen Kit", description: "…" }`. The page's root wrapper is `<div className="numen-surface dark min-h-screen bg-bg text-text p-8 md:p-16">` so every primitive resolves against LIVE warm tokens (Q9). One `<section className="mb-16">` per primitive group, copying the analog's section-per-primitive layout. Sections:

1. Tokens swatch grid: render `bg-bg / bg-panel / bg-panel-2 / text-text / text-text-muted / bg-accent` chips with their hex labels — proves no pure black + warm-neutral.
2. Verdict scale: the three `VerdictSwatch` (good/mixed/bad) with a text label on each (proves Lc ≥ 60 legibility) — amber "mixed" shown as first-class, not error.
3. Surface + Glass: a `Surface` card and a `Glass` panel (the glass is the DEPLOYED-build blur check — place a busy/keyframe background behind it so the blur is visibly testable in prod).
4. Tool chips: `PillChip` in BOTH `instant` and `agentic` intents side-by-side (must read visually distinct) with Lucide icons.
5. Icon buttons: circular `IconButton`s (Lucide icons) — confirm 44px hit area.
6. Serif specimen (D-13): render the greeting/hero line AND a sample verdict one-liner in `font-serif` (Source Serif 4) at 28px/600 on `#1a1714`. ALSO render the same two lines in Newsreader for the side-by-side specimen so the user can pick — wire Newsreader as a second `next/font/google` instance scoped to this page (e.g. `--font-serif-alt`) OR clearly label a documented comparison. The functional UI around it stays Inter (`font-sans`) — proves the sans-led + serif-voice split (DS-04).
7. Stage-reveal demo (DS-07): a `StageBlock` toggled by a button + a "Toggle reduced motion" note, demonstrating the calm reveal and the reduced-motion static fallback.
8. Keyframe-as-chroma panel (DS-08): a sample video keyframe still placed beside the near-neutral chrome so the chrome visibly recedes and the (often cool) image carries the color — the verifiable DS-08 demonstration. Use a placeholder/sample image asset (no real user data).

Copy from the UI-SPEC Copywriting Contract: empty-state heading "Nothing here yet"; body "This is a kit showcase — components render below under the Numen surface tokens." Voice = calm, plain-language, no engine jargon. NO neon/gradient/beam/glow/shimmer anywhere (D-07).

Create `tests/numen/showcase.a11y.test.tsx` (`@vitest-environment happy-dom`): render the page and assert `axe(container)` returns no violations (copy the Sidebar a11y harness). Mock `next/font` if needed for the test environment.</action>
  <verify>
    <automated>pnpm vitest run tests/numen/showcase.a11y.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(kit)/numen-kit/page.tsx` exists; root wrapper class contains `numen-surface dark`
    - `grep -c '@/components/numen/' "src/app/(kit)/numen-kit/page.tsx"` ≥ 5 (imports the primitives)
    - The page renders both `instant` and `agentic` pill-chip intents and both `font-serif` (Source Serif 4) and the Newsreader specimen lines
    - A keyframe/sample-image element sits adjacent to near-neutral chrome (DS-08 section present)
    - `tests/numen/showcase.a11y.test.tsx` passes GREEN (no axe violations)
    - No `backdrop-blur` class, no neon/gradient/shimmer in the page source
  </acceptance_criteria>
  <done>Showcase route renders all primitives + serif specimen + stage-reveal + keyframe-chroma under live tokens; axe green.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite + APCA + production build gate</name>
  <read_first>
    - .planning/phases/01-design-system-foundation-brand-migration/01-VALIDATION.md (§ Sampling Rate: per-wave = full suite + APCA; phase gate = build clean + deployed showcase)
  </read_first>
  <action>Run the phase-gate validation chain locally before the human-verify checkpoint: `pnpm test` (full Vitest suite — all `tests/numen/*` GREEN), `pnpm tsx scripts/check-apca.ts` (palette gate exits 0), and `pnpm build` (production Lightning CSS build compiles clean — this is the build that would strip a class-form backdrop-filter; the inline Glass must survive). Fix any red before proceeding. Do NOT start `pnpm start` here — the live deployed check is the human checkpoint (Task 3).</action>
  <verify>
    <automated>pnpm test 2>&1 | tail -15 && pnpm tsx scripts/check-apca.ts && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 (all `tests/numen/*` GREEN: tokens, type, glass, primitives, stage-reveal, showcase.a11y)
    - `pnpm tsx scripts/check-apca.ts` exits 0
    - `pnpm build` completes with no error
  </acceptance_criteria>
  <done>Full unit suite + APCA gate + production build all green locally; ready for deployed verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deployed-build visual verification (criteria 1, 3, 4 + DS-08 + serif pick)</name>
  <what-built>The Numen kit showcase route renders every primitive under live `.numen-surface` warm-neutral tokens, deployed (Vercel preview or `pnpm build && pnpm start`). This is the deployed-build proof the success criteria require — the glass blur, the token rendering, and the keyframe-chroma discipline can only be confirmed on a real production build (Lightning CSS strips class-form backdrop-filter in prod but not the inline form).</what-built>
  <how-to-verify>
    1. Open the deployed showcase route (`/numen-kit` on the Vercel preview, or `pnpm build && pnpm start` → http://localhost:3000/numen-kit).
    2. GLASS (DS-05, criterion 3): confirm the Glass panel shows a REAL backdrop blur over the busy background — NOT a flat panel. (A flat panel = Lightning CSS stripped it = a bug.)
    3. TOKENS (DS-01, criterion 1): confirm the base is warm near-black `#1a1714`, NO pure black, chrome reads warm-neutral.
    4. VERDICT SCALE (DS-02, criterion 2): confirm green/amber/clay-red are muted (calm, not alarming); amber "mixed" reads first-class, not like an error; text on each swatch is legible.
    5. SERIF PICK (DS-04, D-13): compare Source Serif 4 vs Newsreader on the greeting + verdict line; pick the final voice serif.
    6. KEYFRAME-CHROMA (DS-08): confirm the sample keyframe carries the color/energy while the chrome recedes beside it.
    7. STAGE-REVEAL (DS-07): toggle it; confirm the reveal is calm (no bounce/snap); enable OS reduced-motion and confirm it degrades to a static appear.
  </how-to-verify>
  <action>Record the user's serif choice. If Source Serif 4 is NOT chosen, the executor updates `src/app/layout.tsx` (swap the `Source_Serif_4` import/instance for the picked font) and re-runs `pnpm vitest run tests/numen/type.test.ts` + `pnpm build`. If the glass blur is flat on the deployed build, that is a DS-05 regression — fix the Glass primitive (confirm inline `style` not class) before sign-off. Capture any palette/chrome adjustments the user requests (re-run `check-apca.ts` if hexes change).</action>
  <resume-signal>Type "approved" + the chosen serif (Source Serif 4 or Newsreader), or describe issues (flat glass, pure black, alarming verdict colors, bouncy motion).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → deployed asset | Production Lightning CSS pass (strips class-form backdrop-filter — the inline Glass must survive) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering (build-time CSS strip) | Glass backdrop-filter on deployed build | mitigate | Inline `style` (Plan 02) survives Lightning CSS; deployed human-verify (Task 3) is the gate that catches a regression |
| T-04-02 | (none) | static showcase, no input | accept | No user input, no data, no auth on the showcase route — static render |
</threat_model>

<verification>
- `pnpm test` full suite GREEN (all tests/numen/*)
- `pnpm tsx scripts/check-apca.ts` exits 0
- `pnpm build` clean
- Deployed showcase: glass blur visible, no pure black, verdict muted, serif picked, reduced-motion static (human-verify)
</verification>

<success_criteria>
- Deployed showcase proves criteria 1 (warm-neutral, no pure black, verified on deployed build) & 3 (component vocabulary + glass blur survives) — DS-01/DS-05/DS-08
- Serif voice pick locked (DS-04)
- Stage-reveal reads calm + reduced-motion static (DS-07)
- Keyframe carries the chroma, chrome recedes (DS-08)
- Full suite + APCA + production build all green
</success_criteria>

<output>
Create `.planning/phases/01-design-system-foundation-brand-migration/01-04-SUMMARY.md` when done
</output>
