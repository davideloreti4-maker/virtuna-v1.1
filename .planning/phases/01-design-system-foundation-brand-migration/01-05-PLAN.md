---
phase: 01-design-system-foundation-brand-migration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/numen-migration-boundary.md
autonomous: true
requirements: [DS-06]
user_setup: []

must_haves:
  truths:
    - "A grep inventory of every costume target (#07080a / #FF7F50 / GlassPanel / coral- / framer-motion / TrafficLights / chat dock) exists with measured per-target file counts"
    - "A written boundary doc records, per target, its v5.0 disposition (replace vs defer) and the owning phase"
    - "Nothing live is deleted — Phase 1 documents the boundary only (D-04)"
    - "The chat-dock grep is widened before concluding it is absent (Research Q8 caveat)"
  artifacts:
    - path: "docs/numen-migration-boundary.md"
      provides: "DS-06 migration boundary doc — inventory + replace/defer/owning-phase table"
      contains: "Disposition"
  key_links: []
---

<objective>
Produce the DS-06 migration boundary: a grep inventory of the old Raycast/coral/glass-everywhere costume targets with measured counts, plus a written boundary doc stating what v5.0 replaces vs defers and which phase owns each removal. Delete nothing live (D-04).

Purpose: A documented, bounded retirement so the parallel Numen kit can coexist with the live app and each later feature phase knows exactly what to retire when it rebuilds its surface.
Output: `docs/numen-migration-boundary.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation-brand-migration/01-RESEARCH.md
@.planning/phases/01-design-system-foundation-brand-migration/01-PATTERNS.md
@.planning/phases/01-design-system-foundation-brand-migration/01-UI-SPEC.md

<interfaces>
<!-- Pre-measured counts from RESEARCH Q8 (this repo, 2026-06-11) — re-run to confirm, do not trust blindly. -->
- `#07080a` → 5 files (the cold base → replace with --numen-bg)
- `FF7F50` (case-insensitive) → 17 files (hardcoded coral → replace with --numen-accent)
- `GlassPanel` → 10 files (→ replaced by the rare Glass primitive)
- `coral-` / `--color-coral` → 7 files (→ map to --numen-accent)
- `framer-motion` → 4 files (→ standardize NEW code on motion; defer removal per D-04)
- `TrafficLights` → 5 refs (fake macOS chrome → retire in Phase 5 shell rebuild)
- chat dock → 0 hits for `chat-dock`/`ChatDock` — WIDEN the grep before concluding absent (Q8 caveat)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the grep inventory + write the boundary doc</name>
  <read_first>
    - .planning/phases/01-design-system-foundation-brand-migration/01-RESEARCH.md (Q8 = the exact grep commands, pre-measured counts, the boundary-doc column format, and the chat-dock widen-grep caveat)
    - .planning/phases/01-design-system-foundation-brand-migration/01-UI-SPEC.md (§ Migration Boundary table = the disposition + owning-phase decisions already made)
    - .planning/phases/01-design-system-foundation-brand-migration/01-CONTEXT.md (D-04 = delete nothing; D-06 audit-only scope; known targets TrafficLights.tsx + Sidebar.tsx)
  </read_first>
  <action>Run the grep inventory against `src/` (this repo's `rg` is shimmed — use `grep -rl` / `grep -rni` per Research Q8) and capture the CURRENT counts (re-run, do not just copy the pre-measured numbers — they may have drifted):
- `grep -rni "07080a" src` (cold base)
- `grep -rni "FF7F50\|#ff7f50" src` (hardcoded coral)
- `grep -rl "GlassPanel" src`
- `grep -rni "coral-\|--color-coral" src`
- `grep -rl "framer-motion" src`
- `grep -rl "TrafficLights" src`
- chat dock — WIDEN per Q8: `grep -rni "chat.*dock\|ChatDock\|chat-dock\|chat.*drawer\|chat.*panel" src` (the dock likely lives under a different name; do the widened search before recording "0 / absent").

Write `docs/numen-migration-boundary.md` (per CLAUDE.md: docs go in `/docs`). Structure:
1. A header stating the D-04 rule explicitly: Phase 1 deletes NOTHING live — this is an inventory + boundary doc; each removal happens inside the phase that rebuilds that surface.
2. The inventory table, one row per costume target, columns: `Target | Files/Count (measured <date>) | Surface(s) | v5.0 Disposition (replace/defer) | Owning Phase`. Use the dispositions already decided in UI-SPEC § Migration Boundary:
   - `#07080a` cold base → Replace with `--numen-bg`, per-surface → each surface's rebuild
   - `#FF7F50` hardcoded coral → Replace with `--numen-accent`, per-surface → each surface's rebuild
   - `GlassPanel` everywhere → Replace with the rare Glass primitive (ephemeral only), per-surface → each surface's rebuild
   - `coral-` / `--color-coral` tokens → Map to `--numen-accent`, per-surface → each surface's rebuild
   - `framer-motion` → Standardize NEW code on `motion`; DEFER the `pnpm remove framer-motion` + 4-file migration (D-04: don't touch live) → later surface-rebuild phase
   - `TrafficLights` (fake macOS chrome) → Retire (no window chrome in the mobile thread) → Phase 5 (shell rebuild)
   - chat dock → Absorbed into the thread → Phase 4/5 (record the widened-grep finding: the real component name if found, or "no match — flag for Phase 4/5 to locate at rebuild")
3. A short "What v5.0 replaces vs defers" prose summary distinguishing the parallel Numen kit (built now) from the per-surface retirements (deferred).
4. Note the also-forbidden carry-overs the kit must never adopt: the OLD `--animate-shimmer` / `gradient-x` keyframes and the `--ease-spring` 1.56 bounce token (D-07 anti-patterns).

Delete nothing. Modify no `src/` file. This plan produces ONLY the doc.</action>
  <verify>
    <automated>test -f docs/numen-migration-boundary.md && grep -qi "Disposition" docs/numen-migration-boundary.md && grep -qi "delete nothing\|deletes nothing\|delete no" docs/numen-migration-boundary.md && grep -qi "TrafficLights" docs/numen-migration-boundary.md && grep -qi "GlassPanel" docs/numen-migration-boundary.md && echo "boundary doc ok"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/numen-migration-boundary.md` exists
    - The doc contains a table with a `Disposition` (replace/defer) column and an owning-phase column
    - All 7 costume targets are present: `07080a`, `FF7F50`, `GlassPanel`, `coral-`, `framer-motion`, `TrafficLights`, chat dock
    - Measured counts are captured (re-run, dated), not just copied
    - The chat-dock row reflects a WIDENED grep result (component name found, or an explicit "flag for Phase 4/5" note)
    - The doc explicitly states Phase 1 deletes nothing live (D-04)
    - `git status --porcelain src/` shows NO modified `src/` files attributable to this plan (audit-only)
  </acceptance_criteria>
  <done>The DS-06 boundary doc exists with measured inventory + per-target replace/defer disposition + owning phase; nothing live deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure static analysis (grep) producing a markdown doc; no runtime, no input, no data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | (none) | grep audit + doc | accept | No runtime, no network, no input, no secrets — read-only static analysis |
</threat_model>

<verification>
- `docs/numen-migration-boundary.md` exists with all 7 targets + disposition + owning-phase columns
- Counts re-measured and dated
- No `src/` files modified (delete-nothing, D-04)
</verification>

<success_criteria>
- DS-06 satisfied: documented migration boundary with grep inventory + replace/defer decision per target
- Chat-dock grep widened before concluding absent
- Zero live deletions (D-04)
</success_criteria>

<output>
Create `.planning/phases/01-design-system-foundation-brand-migration/01-05-SUMMARY.md` when done
</output>
