---
phase: 01-design-system-foundation-brand-migration
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/numen/glass.tsx
  - src/components/numen/surface.tsx
  - src/components/numen/pill-chip.tsx
  - src/components/numen/icon-button.tsx
  - src/components/numen/verdict-swatch.tsx
autonomous: true
requirements: [DS-05, DS-02]
user_setup: []

must_haves:
  truths:
    - "The Glass primitive applies backdrop-filter via React inline style only — never a backdrop-blur utility class — so it survives Lightning CSS in a production build"
    - "Tool chip (full-pill), icon button (circular, 44px hit), surface (hairline border + soft elevation), and verdict swatch all render under .numen-surface against warm-neutral tokens"
    - "Tool chip intent variants (instant vs agentic) are visually distinct (different class strings) for later TOOL-04"
    - "Verdict swatch surfaces the three muted verdict tokens; any text on a swatch meets APCA Lc >= 60"
    - "Every primitive is built with tailwind-variants slots composed through cn() — none uses class-variance-authority"
  artifacts:
    - path: "src/components/numen/glass.tsx"
      provides: "Rare glass primitive, inline backdropFilter + WebkitBackdropFilter"
      contains: "backdropFilter"
    - path: "src/components/numen/pill-chip.tsx"
      provides: "Full-pill tool chip with instant/agentic intent variants via tv({slots})"
      contains: "tv("
    - path: "src/components/numen/icon-button.tsx"
      provides: "Circular 44px-min icon button"
    - path: "src/components/numen/surface.tsx"
      provides: "Hairline-border soft-elevation container"
    - path: "src/components/numen/verdict-swatch.tsx"
      provides: "Three muted verdict swatches"
  key_links:
    - from: "src/components/numen/glass.tsx"
      to: "rendered DOM inline style"
      via: "style={{ backdropFilter, WebkitBackdropFilter }}"
      pattern: "WebkitBackdropFilter"
    - from: "src/components/numen/*.tsx"
      to: ".numen-surface tokens"
      via: "tailwind utilities (bg-panel, border-border, text-verdict-good) from Plan 01"
      pattern: "border-border|bg-panel|verdict-"
---

<objective>
Build the load-bearing component vocabulary (DS-05) every later Reading component depends on: the rare Glass primitive (inline backdrop-filter), the soft-elevation Surface, the full-pill Tool chip, the circular Icon button, and the Verdict swatch — all on `tailwind-variants` slots, composed through `cn()`, rendering against the Plan-01 `.numen-surface` tokens.

Purpose: These five primitives are the kit's working parts. The Glass inline-style mechanism is the load-bearing proof that backdrop-filter survives Lightning CSS in production (D-05).
Output: 5 primitive components under `src/components/numen/` that turn `tests/numen/glass.test.ts` and `tests/numen/primitives.test.ts` GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation-brand-migration/01-RESEARCH.md
@.planning/phases/01-design-system-foundation-brand-migration/01-PATTERNS.md
@.planning/phases/01-design-system-foundation-brand-migration/01-UI-SPEC.md

<interfaces>
<!-- Analog signatures to copy STRUCTURE from (invert values to warm-neutral). cn() from Plan 01's existing convention. -->

From src/lib/utils.ts:
- `export function cn(...inputs: ClassValue[]): string` — clsx + tailwind-merge

From src/components/primitives/GlassPanel.tsx (the inline-backdrop analog):
- 4-prop API `{ children, className, style, as }`, `"use client"`
- `style={{ backdropFilter: "blur(5px)", WebkitBackdropFilter: "blur(5px)", ...style }}` ← copy MECHANISM, drop the Raycast gradient + inset glow

From src/components/ui/button.tsx (the variant-API analog — SWAP cva → tv):
- `size: { md: "h-11 min-h-[44px] min-w-[44px] …" }` ← copy the 44px floor
- `focus-visible:ring-2 focus-visible:ring-accent` ← copy focus pattern
- `export interface ButtonProps extends …, VariantProps<typeof buttonVariants>` ← mirror with `VariantProps<typeof toolChip>` from tailwind-variants

From src/components/primitives/GlassPill.tsx:
- `const Component = onClick ? "button" : "span";` polymorphism + `focus-visible:ring` for the pill

Tokens available from Plan 01 (.numen-surface): `bg-bg bg-panel bg-panel-2 text-text text-text-muted bg-accent text-accent border-border bg-verdict-good bg-verdict-mixed bg-verdict-bad text-verdict-good text-verdict-mixed text-verdict-bad`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Glass + Surface primitives</name>
  <read_first>
    - src/components/primitives/GlassPanel.tsx (FULL — the inline `style={{ backdropFilter, WebkitBackdropFilter, ...style }}` mechanism + `as` polymorphism + `cn(base, className)`; "use client")
    - src/lib/utils.ts (cn)
    - .planning/phases/01-design-system-foundation-brand-migration/01-RESEARCH.md (Pattern 5 = inline glass; Pitfall 2 = Lightning CSS strips the class form)
    - .planning/phases/01-design-system-foundation-brand-migration/01-PATTERNS.md (§ glass.tsx + § surface.tsx — copy STRUCTURE, INVERT values)
    - tests/numen/glass.test.ts (the assertions to satisfy — inline backdropFilter present, no backdrop-blur class)
  </read_first>
  <behavior>
    - Test 1 (glass.test.ts): `<Glass>` renders with inline `style.backdropFilter` matching `/blur\(\d+px\)/`
    - Test 2: `<Glass>` renders inline `style.WebkitBackdropFilter` (Safari/iOS PWA)
    - Test 3: `<Glass>` carries NO `backdrop-blur` / `backdrop-filter` utility class
    - Test 4: a configurable `blur` prop (default 12) changes the rendered px value
  </behavior>
  <action>Create `src/components/numen/glass.tsx`: `"use client"`, props `{ blur = 12, className, children, style, as = "div", ...p }`. Render `<Component className={cn("rounded-xl border border-border bg-panel/70", className)} style={{ backdropFilter: \`blur(${blur}px)\`, WebkitBackdropFilter: \`blur(${blur}px)\`, ...style }} {...p}>`. NEVER emit a `backdrop-blur-*` class (Lightning CSS strips it in prod — D-05). Drop the Raycast 137deg gradient and the `rgba(255,255,255,0.15) inset` glow from the analog — use the warm `bg-panel/70` + `border-border` tokens. radius = `lg` (12px → `rounded-xl` is 12px in this scale; confirm against repo radius scale, use `rounded-[12px]` if the utility differs). Document in a header comment: glass is RARE — composer + tool-sheet only (§6); verify blur on a DEPLOYED build (Plan 04).

Create `src/components/numen/surface.tsx`: a plain `<div>` (NO glass, NO backdropFilter) container with the GlassPanel 4-prop API `{ children, className, style, as = "div" }`. Render `cn("rounded-[12px] border border-border bg-panel", className)` with a SOFT elevation shadow (a subtle `shadow-*` or inline low-opacity box-shadow — NOT the Raycast `rgba(255,255,255,0.15) inset` glow). This is the default hairline-border surface for cards/panels.</action>
  <verify>
    <automated>pnpm vitest run tests/numen/glass.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'backdropFilter' src/components/numen/glass.tsx` ≥ 1 AND `grep -c 'WebkitBackdropFilter' src/components/numen/glass.tsx` ≥ 1
    - `grep -ic 'backdrop-blur\|backdrop-filter' src/components/numen/glass.tsx` returns `0` (no class form)
    - `glass.tsx` uses `border-border` and `bg-panel` warm tokens, NOT the Raycast `linear-gradient(137deg` string
    - `surface.tsx` exists, renders a `<div>`, has NO `backdropFilter`, uses `border-border`
    - `tests/numen/glass.test.ts` passes GREEN
  </acceptance_criteria>
  <done>Glass (inline backdrop, rare) + Surface (hairline + soft elevation) exist on warm tokens; glass.test.ts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pill-chip + Icon-button + Verdict-swatch (tailwind-variants slots)</name>
  <read_first>
    - src/components/ui/button.tsx (the cva STRUCTURE to mirror with `tv` — the `size` 44px floor, `focus-visible:ring-accent`, `disabled:opacity-50`, `VariantProps` typing)
    - src/components/primitives/GlassPill.tsx (the `onClick ? "button" : "span"` polymorphism + `rounded-full inline-flex items-center` + focus-visible ring)
    - src/components/ui/badge.tsx (named-color token pattern for the verdict swatch)
    - .planning/phases/01-design-system-foundation-brand-migration/01-RESEARCH.md (Pattern 2 = the EXACT `tv({ slots: { root, icon, label }, variants: { intent: { instant, agentic } }, compoundVariants })` example; Pitfall 5 = class strings must be literal, no `bg-${x}`)
    - .planning/phases/01-design-system-foundation-brand-migration/01-PATTERNS.md (§ pill-chip + § icon-button + § verdict-swatch — copy STRUCTURE, swap cva→tv, INVERT values)
    - .planning/phases/01-design-system-foundation-brand-migration/01-UI-SPEC.md (§ Component Vocabulary table; § Spacing radius defaults; § Color verdict-mixed is first-class not error)
    - tests/numen/primitives.test.ts (the slot/variant assertions to satisfy)
  </read_first>
  <behavior>
    - Test 1 (primitives.test.ts): `pill-chip` exports a `tv()` result; `intent: "agentic"` produces a DIFFERENT root class string than `intent: "instant"`
    - Test 2: `icon-button` enforces `min-h-[44px]` and `min-w-[44px]` and `rounded-full`
    - Test 3: `verdict-swatch` `verdict: "mixed"` uses `bg-verdict-mixed` (literal), never an error/red token
    - Test 4: all three import `tv` from `tailwind-variants`, none imports from `class-variance-authority`
  </behavior>
  <action>Build all three with `tailwind-variants` `tv()` (D-08), composing the result through the existing `cn()` for caller `className` override (Research Pattern 2). Use LITERAL class strings only — never interpolate token names (Pitfall 5).

`src/components/numen/pill-chip.tsx` (Tool chip): `tv({ slots: { root: "inline-flex items-center gap-2 rounded-full border border-border px-3 py-1.5 transition-colors text-sm font-medium", icon: "size-4 shrink-0 text-text-muted", label: "text-text" }, variants: { intent: { instant: { root: "bg-panel hover:bg-panel-2" }, agentic: { root: "bg-panel-2 ring-1 ring-accent/30" } } }, compoundVariants: [{ intent: "agentic", class: { icon: "text-accent" } }], defaultVariants: { intent: "instant" } })`. Export `type PillChipProps = VariantProps<typeof toolChip> & { className?: string; … }`. Polymorphic `button` when interactive (copy GlassPill `onClick ? "button" : "span"`), `focus-visible:ring-2 focus-visible:ring-accent`. instant vs agentic MUST be visually distinct (later TOOL-04). Icons = Lucide. radius = `full`.

`src/components/numen/icon-button.tsx` (Circular): `tv({ base/slots })` with `rounded-full inline-flex items-center justify-center min-h-[44px] min-w-[44px]` (44px hit-area floor even if the icon is smaller — copy the button.tsx floor, INVERT `rounded-md`→`rounded-full`), icon resting at `text-text-muted`, `focus-visible:ring-2 focus-visible:ring-accent`, `disabled:opacity-50`. Lucide icon as child/prop. Warm-neutral fill (`bg-transparent hover:bg-panel` or similar near-neutral). Do NOT use the Phosphor `ui/icon.tsx` wrapper (D-09 — Lucide only).

`src/components/numen/verdict-swatch.tsx`: `tv({ variants: { verdict: { good: "bg-verdict-good", mixed: "bg-verdict-mixed", bad: "bg-verdict-bad" } } })` — literal class strings. Three muted swatches; amber "mixed" is FIRST-CLASS, never styled as error/degraded (UI-SPEC Color). Any text rendered on a swatch must meet APCA Lc ≥ 60 (validated by `scripts/check-apca.ts` from Plan 01). Muted, not saturated.

All three: warm-neutral fills, hairline `border-border`, NO glass, NO neon/glow/gradient (D-07).</action>
  <verify>
    <automated>pnpm vitest run tests/numen/primitives.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l 'from "tailwind-variants"' src/components/numen/pill-chip.tsx src/components/numen/icon-button.tsx src/components/numen/verdict-swatch.tsx` lists all three
    - `grep -rc 'class-variance-authority' src/components/numen/` returns `0` across the numen dir
    - `grep -c 'min-h-\[44px\]' src/components/numen/icon-button.tsx` ≥ 1 AND `grep -c 'rounded-full' src/components/numen/icon-button.tsx` ≥ 1
    - `pill-chip.tsx` defines both `instant` and `agentic` intent variants with different root classes
    - `verdict-swatch.tsx` references literal `bg-verdict-mixed` and contains no `error`/`destructive`/red token
    - `tests/numen/primitives.test.ts` passes GREEN
  </acceptance_criteria>
  <done>Pill-chip, icon-button, verdict-swatch built on tailwind-variants slots, warm tokens, 44px hit area, distinct intents; primitives.test.ts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component props → rendered style | `blur` prop interpolated into an inline style string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / injection | Glass `blur` prop interpolated into `backdropFilter: blur(${blur}px)` | mitigate | Type `blur` as `number` (TS); the template emits `blur(<number>px)` — no string passthrough that could inject CSS; no user-controlled string reaches the style |
| T-02-02 | Information disclosure | presentational primitives | accept | No data, no network, no secrets — pure render |
</threat_model>

<verification>
- `pnpm vitest run tests/numen/glass.test.ts tests/numen/primitives.test.ts` GREEN
- `pnpm build` compiles all 5 primitives clean
- `grep -rc 'class-variance-authority' src/components/numen/` returns 0
- No `backdrop-blur-*` class anywhere in `src/components/numen/`
</verification>

<success_criteria>
- 5 primitives exist under `src/components/numen/` on warm-neutral tokens (DS-05)
- Glass uses inline backdrop-filter only (survives Lightning CSS — verified deployed in Plan 04)
- Verdict swatch surfaces the 3 muted load-bearing colors (DS-02), Lc ≥ 60 for text
- All built on tailwind-variants slots, none on cva
- glass.test.ts + primitives.test.ts GREEN
</success_criteria>

<output>
Create `.planning/phases/01-design-system-foundation-brand-migration/01-02-SUMMARY.md` when done
</output>
