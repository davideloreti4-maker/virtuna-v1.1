---
phase: 01-design-system-foundation-brand-migration
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/numen/stage-reveal.tsx
  - src/app/globals.css
autonomous: true
requirements: [DS-07]
user_setup: []

must_haves:
  truths:
    - "StageBlock reveals each child via opacity tween + a high-damping (ratio >= 1, no overshoot) spring on translate — never the forbidden cubic-bezier(0.34,1.56,0.64,1) bounce; built on the `motion` lib, not framer-motion (D-10)"
    - "When useReducedMotion() is true, StageBlock renders a static opacity appear with NO translate/slide (accessibility, D-14)"
    - "A new calm easing token exists in globals.css; the old --ease-spring 1.56 bounce token is NOT reused by the Numen kit"
    - "The stage-reveal is the ONE key motion moment — no presence theater elsewhere"
  artifacts:
    - path: "src/components/numen/stage-reveal.tsx"
      provides: "AnimatePresence stage-reveal honoring prefers-reduced-motion"
      contains: "useReducedMotion"
    - path: "src/app/globals.css"
      provides: "A new calm easing token (e.g. --numen-ease-calm) appended to the Numen layer"
      contains: "ease"
  key_links:
    - from: "src/components/numen/stage-reveal.tsx"
      to: "motion/react"
      via: "import { AnimatePresence, motion, useReducedMotion }"
      pattern: "useReducedMotion"
---

<objective>
Build the calm motion moment of the kit (DS-07): a `StageBlock` stage-reveal that materializes each completed engine stage's block via opacity tween + a non-overshooting spring on translate, degrading to a static appear under `prefers-reduced-motion`. Define a NEW calm easing token; NEVER reuse the existing bouncy `--ease-spring`.

Purpose: The stage-reveal is the single load-bearing motion of the whole product (Phase 4's Reading reveal builds on this). It must read calm, never bouncy/snappy (§6), and must honor reduced-motion as an accessibility requirement (not optional).
Output: `src/components/numen/stage-reveal.tsx` + a calm easing token, turning `tests/numen/stage-reveal.test.ts` GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation-brand-migration/01-RESEARCH.md
@.planning/phases/01-design-system-foundation-brand-migration/01-PATTERNS.md
@.planning/phases/01-design-system-foundation-brand-migration/01-UI-SPEC.md

<interfaces>
<!-- Analog: stagger-reveal already uses motion + useReducedMotion. Copy the gate, INVERT the easing. -->

From src/components/motion/stagger-reveal.tsx (the gate analog):
- `"use client"; import { motion, useReducedMotion } from "motion/react"; import type { Variants } from "motion/react";`
- `const prefersReducedMotion = useReducedMotion(); if (prefersReducedMotion) { return <div className={className}>{children}</div>; }` ← copy this static early-return
- analog easing `[0.25, 0.1, 0.25, 1.0]` is calm (fine to reuse this style); the FORBIDDEN one is the spring token below

From src/app/globals.css:
- line 170: `--ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);` ← FORBIDDEN (1.56 > 1 overshoots = bounce §6 forbids). Define a NEW token, do not reference this one.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StageBlock stage-reveal + calm easing token</name>
  <read_first>
    - src/components/motion/stagger-reveal.tsx (FULL — the `"use client"` + `motion/react` import, the `useReducedMotion()` early-return-to-static gate, `Variants` typing)
    - src/components/motion/fade-in-up.tsx (the secondary reduced-motion fallback analog)
    - src/app/globals.css line 170 (the FORBIDDEN `--ease-spring` token — see what NOT to reference; and the Numen layer appended by Plan 01)
    - .planning/phases/01-design-system-foundation-brand-migration/01-RESEARCH.md (Pattern 3 = the EXACT AnimatePresence + tween-opacity + high-damping-spring + reduced-motion code; the no-bounce rule = damping ratio ≥ 1)
    - .planning/phases/01-design-system-foundation-brand-migration/01-PATTERNS.md (§ stage-reveal.tsx — copy the gate, INVERT the easing/transition; the three hard inversions header)
    - .planning/phases/01-design-system-foundation-brand-migration/01-UI-SPEC.md (§ Motion — duration tiers, forbidden token, reduced-motion mandatory)
    - tests/numen/stage-reveal.test.ts (assertions: reduced-motion strips translate; forbidden easing never used)
  </read_first>
  <behavior>
    - Test 1 (stage-reveal.test.ts): with `useReducedMotion` mocked → `true`, `<StageBlock show>` renders children with NO `y`/translate (static opacity appear only)
    - Test 2: with reduced-motion `false`, the reveal uses opacity tween + a spring; the spring damping is high relative to stiffness (ratio ≥ 1, no overshoot)
    - Test 3: the component source NEVER contains the string `0.34, 1.56` (the forbidden bounce cubic-bezier)
    - Test 4: `AnimatePresence` wraps the conditional block so exits are handled
  </behavior>
  <action>Create `src/components/numen/stage-reveal.tsx` (`"use client"`). Import `{ AnimatePresence, motion, useReducedMotion } from "motion/react"`. Export `StageBlock({ show, children }: { show: boolean; children: React.ReactNode })` per Research Pattern 3:
- `const reduce = useReducedMotion();`
- Wrap in `<AnimatePresence>`; when `show`, render `<motion.div initial={{ opacity: 0, y: reduce ? 0 : 12 }} animate={{ opacity: 1, y: 0, transition: { opacity: { duration: 0.4, ease: [0.215, 0.61, 0.355, 1] }, y: reduce ? { duration: 0 } : { type: "spring", stiffness: 220, damping: 30 } } }} exit={{ opacity: 0, transition: { duration: 0.2 } }}>`.
- No-bounce rule: keep spring `damping` high relative to `stiffness` (ratio ≥ 1 → no overshoot). NEVER use or reference the existing `--ease-spring: cubic-bezier(0.34,1.56,0.64,1)` token — the 1.56 overshoots, which §6 forbids.
- Reduced motion zeroes the translate (`y: 0` initial + `duration: 0`) → static opacity appear; this is the D-14 accessibility hard requirement.
- Duration values map to the UI-SPEC tiers (opacity ~base tier 300-400ms; exit ~fast 200ms).

ALSO append a NEW calm easing token to the Numen layer in `src/app/globals.css` (do NOT edit the existing coral `@theme` or the old `--ease-spring`): e.g. `--numen-ease-calm: cubic-bezier(0.215, 0.61, 0.355, 1);` in/near the `.numen-surface` block (or an existing `@theme` extension scoped to the kit — keep it clearly Numen-namespaced). This documents the calm easing as a token even though the motion component inlines the array (the token is the design source of truth for future kit motion).

This stage-reveal is the ONE key motion moment — do NOT add presence/entrance theater to the other primitives.</action>
  <verify>
    <automated>pnpm vitest run tests/numen/stage-reveal.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'useReducedMotion' src/components/numen/stage-reveal.tsx` ≥ 1 AND `grep -c 'AnimatePresence' src/components/numen/stage-reveal.tsx` ≥ 1
    - `grep -c '0.34, 1.56\|0.34,1.56' src/components/numen/stage-reveal.tsx` returns `0` (forbidden bounce never present)
    - `grep -c 'spring' src/components/numen/stage-reveal.tsx` ≥ 1 with `damping` ≥ `stiffness/ratio` such that no overshoot (damping 30 / stiffness 220 per pattern)
    - `grep -c 'numen-ease' src/app/globals.css` ≥ 1 (new calm token added)
    - The existing `--ease-spring` line in globals.css is byte-unchanged (not reused, not deleted)
    - `tests/numen/stage-reveal.test.ts` passes GREEN
  </acceptance_criteria>
  <done>StageBlock reveals via opacity tween + non-overshoot spring, static under reduced-motion, calm easing token added, forbidden bounce never used; stage-reveal.test.ts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none material) | Pure client-side presentational motion; no input, data, or network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of service (UX) | reduced-motion users get vestibular-triggering motion | mitigate | `useReducedMotion()` gate strips translate → static appear (D-14 accessibility); asserted by stage-reveal.test.ts |
| T-03-02 | (none) | presentational motion | accept | No data/network/secret surface |
</threat_model>

<verification>
- `pnpm vitest run tests/numen/stage-reveal.test.ts` GREEN
- `pnpm build` compiles the client component clean
- `--ease-spring` (old bounce) byte-unchanged and unreferenced by the Numen kit
</verification>

<success_criteria>
- StageBlock is the calm stage-reveal moment (DS-07): opacity tween + non-overshoot spring
- Reduced-motion degrades to static opacity appear (D-14)
- A new calm easing token replaces the forbidden bounce token for the kit
- stage-reveal.test.ts GREEN
</success_criteria>

<output>
Create `.planning/phases/01-design-system-foundation-brand-migration/01-03-SUMMARY.md` when done
</output>
