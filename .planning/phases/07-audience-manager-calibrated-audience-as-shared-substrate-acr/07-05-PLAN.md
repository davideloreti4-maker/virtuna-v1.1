---
phase: 07-audience-manager-calibrated-audience-as-shared-substrate-acr
plan: 05
type: execute
wave: 4
depends_on: ["07-02", "07-03", "07-04"]
files_modified:
  - src/app/audience/page.tsx
  - src/app/audience/new/page.tsx
  - src/app/audience/[id]/page.tsx
  - src/components/audience/audience-manager.tsx
  - src/components/audience/audience-form.tsx
  - src/components/audience/audience-profile-view.tsx
  - src/components/audience/calibration-flow.tsx
  - src/components/app/home/audience-chip.tsx
  - src/components/sidebar/Sidebar.tsx
  - src/components/app/home/composer.tsx
autonomous: false
requirements: [AUD-07]
must_haves:
  truths:
    - "A creator can open the Audience Manager from the sidebar, see General + 2 presets + their audiences, and create/calibrate/delete a custom audience"
    - "After calibration the creator sees a read-only Audience Profile + the 10 calibrated personas (the 'see your audience' moat surface), with no edit affordances (D-03)"
    - "A thin/failed calibration shows the honest warning-toned 'Couldn't read enough — using General' notice, never a fabricated audience"
    - "The composer shows a single audience pill ('for {platform} · {name}'); tapping it picks an audience (dropdown on desktop, bottom sheet on mobile); selection pins per-thread (D-04); General shows neutral, calibrated shows coral"
  artifacts:
    - path: "src/app/audience/page.tsx"
      provides: "Audience Manager list page (/audience)"
      contains: "Your audiences"
    - path: "src/components/audience/calibration-flow.tsx"
      provides: "Personal-handle + target-describe calibration with SSE staged status + honest fallback notice"
      contains: "Couldn't read enough"
    - path: "src/components/app/home/audience-chip.tsx"
      provides: "Composer audience pill → dropdown/bottom-sheet picker, per-thread pin"
      contains: "for"
  key_links:
    - from: "src/components/app/home/audience-chip.tsx"
      to: "src/app/api/audiences/route.ts + thread active_audience_id"
      via: "GET /api/audiences to populate picker; PATCH thread pin on select"
      pattern: "/api/audiences"
    - from: "src/components/audience/calibration-flow.tsx"
      to: "src/app/api/audiences/calibrate/route.ts"
      via: "SSE consume status/fallback/error/done events"
      pattern: "/api/audiences/calibrate"
    - from: "src/components/sidebar/Sidebar.tsx"
      to: "src/app/audience/page.tsx"
      via: "NavItem href=/audience"
      pattern: "/audience"
---

<objective>
Ship the Audience Manager UI, the calibration flow, the read-only Audience Profile + persona display (the "see your audience" moat surface), and the composer audience chip with per-thread pin (D-04) — all on the THEME-06 flat-warm system, reusing the shipped component inventory (NO re-rolling). Honesty spine throughout: the calibration fallback is calm and never implies a fabricated audience.

Purpose: make "your audience" visible and selectable — the hero-object pitch — and route the active selection into the per-thread pin that 07-04 already consumes.
Output: /audience pages + Manager/form/profile/calibration components + sidebar entry + composer audience chip + per-thread pin.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-CONTEXT.md
@.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-UI-SPEC.md
@.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-02-SUMMARY.md
@.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-03-SUMMARY.md
@.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-04-SUMMARY.md

# Reuse-first surfaces (read before building — do NOT re-roll):
@src/components/sidebar/Sidebar.tsx
@src/components/app/home/platform-chip.tsx
@src/components/app/home/composer.tsx
@src/components/board/_kit/PersonaGraph.tsx
@.planning/sketches/MANIFEST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audience Manager pages + form + read-only Profile/persona display</name>
  <files>src/app/audience/page.tsx, src/app/audience/new/page.tsx, src/app/audience/[id]/page.tsx, src/components/audience/audience-manager.tsx, src/components/audience/audience-form.tsx, src/components/audience/audience-profile-view.tsx, src/components/sidebar/Sidebar.tsx</files>
  <read_first>
    - 07-UI-SPEC.md §1 (Manager), §3 (Profile + persona display READ-ONLY), Copywriting Contract (exact copy strings), Color (flat-warm tokens, coral reserve list), Typography/Spacing tables
    - src/components/sidebar/Sidebar.tsx (NavItem L129 + SectionLabel L103 — add the "Audience" entry; collapsed icon+tooltip behavior)
    - src/components/board/_kit/PersonaGraph.tsx (node-cloud props: weight ← share %, label ← persona name; reducedMotion; sr-only mirror; tone)
    - 07-01-SUMMARY.md (Temperature×Disposition labels for the persona table) + 07-02-SUMMARY.md (General/preset sentinel ids, listAudiences shape)
  </read_first>
  <action>
    Build the Manager on flat-warm semantic tokens ONLY (`bg-background`, `bg-surface`, `bg-surface-elevated`, `text-foreground`, `text-foreground-secondary`, `border-white/[0.06]`) — do NOT copy settings-page `zinc-*` classes (UI-SPEC note).

    `src/app/audience/page.tsx` (Manager list): `mx-auto max-w-4xl p-6`; title 24px/600 "Your audiences", subtitle "Who Numen writes and tests for." Render `audience-manager.tsx` which fetches GET /api/audiences and lists audience cards (`bg-surface`, `rounded-xl`, hover `bg-white/[0.02]` only). Each card: name (16px) · `platform · type` sub-label · trailing General badge (coral, with the locked-default tooltip copy) OR a `⋯` overflow menu (edit/delete, `aria-label="Audience options"`). General has NO delete affordance. Empty-state copy per UI-SPEC. Primary "Create audience" CTA (coral, the only primary button).

    `src/app/audience/new/page.tsx` + `src/app/audience/[id]/page.tsx`: full-page (mobile-first, not a modal) rendering `audience-form.tsx`: name `Input` (required), type segmented `personal|target`, platform `Select`, goal free-text `Input` + goal-intent surfacing (a `Select`/dropdown of grow/sell/authority/nurture per RESEARCH Open Q1 dropdown recommendation — deterministic). NO persona-edit affordances (D-03). The form's "calibrate" action mounts the calibration flow (Task 2).

    `src/components/audience/audience-profile-view.tsx` (READ-ONLY, D-03): `PersonaGraph` hero (10 personas, weight←share, label←name, reducedMotion threaded, tone:'default' / no coral cluster in v1); `StatTileRow` for platform / goal / temperature mix / top dispositions (labels wrap, never truncate); `DataTable` persona list (Name · Temperature `Badge` dot+word using the semantic palette · Disposition neutral `Badge` · Share %) built so an edit column can be added later without restructure; the muted read-only caption copy. Delete uses a solid-opaque `Dialog` with the exact destructive copy.

    Add the sidebar `NavItem` ("Audience", Phosphor `UsersThree`/`Users`) in the Settings group region above "Simulations", routing to `/audience`, with collapsed icon+tooltip.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm run lint --max-warnings=0 2>/dev/null || pnpm run lint</automated>
  </verify>
  <done>/audience list + new + [id] pages render flat-warm (no zinc-*); General badge non-deletable; read-only Profile shows PersonaGraph + stat tiles + persona DataTable with no edit affordances + the read-only caption; sidebar entry routes to /audience; tsc + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Calibration flow (SSE) + composer audience chip + per-thread pin</name>
  <files>src/components/audience/calibration-flow.tsx, src/components/app/home/audience-chip.tsx, src/components/app/home/composer.tsx</files>
  <read_first>
    - 07-UI-SPEC.md §2 (Calibration flow — personal/target paths, MANDATORY fallback state), §4 (Composer audience chip — pill → desktop dropdown / mobile bottom sheet, sketch-001 reconciliation, active state, per-thread pin), Copywriting Contract (progress/fallback/error/chip-prefix/picker-footer copy)
    - src/components/app/home/platform-chip.tsx (the chip pattern to extend — "on" prefix, coral active, 44px touch via pointer-coarse)
    - src/components/app/home/composer.tsx (L719 chip row flex-wrap; showPlatformChip gating set; how platform state + per-send params flow; where to add the audience pill)
    - 07-03-SUMMARY.md (calibrate SSE event names: status/fallback/error/done; route paths + request contracts)
  </read_first>
  <action>
    `src/components/audience/calibration-flow.tsx`: Personal path = single "Your @handle" field + "Calibrate audience" primary CTA; Target path = describe textarea + "Calibrate audience". On submit, POST to /api/audiences/calibrate and consume the SSE stream: render `Spinner` + the honest staged status copy ("Reading your followers…" → "Building your audience profile…", no fabricated counts). On the `fallback` event → render the MANDATORY warning-toned (`--color-warning`, never error/coral) inline notice banner with the exact "Couldn't read enough yet" copy + "Continue with General" affordance (NEVER fabricate). On `error` → the distinct "Calibration failed" copy + Retry. On `done` → transition to the read-only Audience Profile view (Task 1 component).

    `src/components/app/home/audience-chip.tsx`: a SINGLE collapsed pill showing `for {platform} · {name}` (mirrors PlatformChip's prefix-word + coral-active grammar; reuse its token classes). Tapping opens the picker: desktop = compact inline dropdown (`bg-surface-elevated`, 12px radius, `shadow-float`) listing General + presets + user audiences with the active one coral-checked + a "Manage audiences →" footer link to /audience; mobile = native bottom sheet with full-width 44px rows (pointer-coarse). This matches sketch-001's pill→dropdown / pill→bottom-sheet grammar (do NOT introduce a conflicting pattern; command-palette out of scope). Active state: General → neutral; calibrated → coral border/tint (signals the moat is engaged). Populate from GET /api/audiences.

    Per-thread pin (D-04): on selection, PATCH the current thread's `active_audience_id` (the column 07-02 added; NULL = General). New threads default to General; switching mid-thread re-grounds future turns only (existing cards keep their audience — they are immutable persisted blocks). The pill reflects the current thread's pinned audience.

    Edit `src/components/app/home/composer.tsx`: mount `audience-chip.tsx` in the chip row under the SAME `showPlatformChip` gating set (present whenever a generation/test tool is active), defaulting to "General". Ensure the row still wraps cleanly on mobile (single collapsed pill, not a 9th expanded chip-group — UI-SPEC §4 constraint). Wire the pinned audience id into the thread state the ideas route already reads (07-04 reads thread.active_audience_id server-side; the chip just persists the pin — the runner call need not change client-side).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm run lint --max-warnings=0 2>/dev/null || pnpm run lint</automated>
  </verify>
  <done>Calibration flow streams honest stages, renders the mandatory warning fallback (never fabricates) + distinct error/retry, transitions to the read-only profile on done; the composer audience pill picks via desktop-dropdown/mobile-sheet, marks General neutral vs calibrated coral, and pins per-thread via PATCH active_audience_id; chip row wraps cleanly on mobile; tsc + lint clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Audience Manager (/audience), calibration flow with honest fallback, read-only Audience Profile + 10-persona display, and the composer audience chip with per-thread pin — all flat-warm, mobile-first.</what-built>
  <how-to-verify>
    1. `pnpm dev` → open `/audience`. Confirm: "Your audiences" title, General card (coral badge, no delete), 2 preset cards, "Create audience" CTA. On a phone-width viewport the layout is mobile-first (no cramped modal).
    2. Create a Personal audience with your own @handle → confirm honest staged status ("Reading your followers…"), then either a read-only Profile (PersonaGraph + stat tiles + persona table, NO edit buttons, read-only caption) OR — if your account is thin — the warning-toned "Couldn't read enough yet" notice with "Continue with General" (NOT an error, NOT coral, NO fabricated personas).
    3. Open the composer. Confirm the audience pill reads "for TikTok · General" (neutral). Tap it → desktop dropdown / mobile bottom sheet lists audiences with a "Manage audiences →" footer. Select a calibrated audience → pill turns coral. Reload the thread → pin persists. Start a NEW thread → it defaults back to General.
    4. Confirm the General badge tooltip + delete-confirmation copy match the UI-SPEC Copywriting Contract verbatim.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. fallback copy wrong, pill not pinning, persona table has edit affordances).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` — 0 errors.
- `pnpm run lint` — clean.
- Grep guard: `grep -rn "zinc-" src/components/audience src/components/app/home/audience-chip.tsx` returns nothing (flat-warm tokens only).
- Human checkpoint passed (UI-SPEC §1–4 + Copywriting Contract honored; honesty-spine fallback verified).
</verification>

<success_criteria>
"Your audience" is visible (read-only personas + profile), selectable (composer chip → per-thread pin), and honest (calm General fallback, never fabricated). All surfaces are flat-warm and mobile-first, reusing the shipped component inventory.
</success_criteria>

<output>
Create `.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-05-SUMMARY.md`. Record: the page routes, the chip→pin flow (PATCH endpoint used), and the human-verify outcome.
</output>
