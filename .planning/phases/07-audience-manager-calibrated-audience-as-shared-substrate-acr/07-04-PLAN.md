---
phase: 07-audience-manager-calibrated-audience-as-shared-substrate-acr
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - src/lib/engine/flash/flash-prompts.ts
  - src/lib/engine/flash/run-flash-text-mode.ts
  - src/lib/tools/runners/ideas-runner.ts
  - src/app/api/tools/ideas/route.ts
  - src/lib/audience/audience-grounding.ts
  - src/lib/audience/__tests__/audience-grounding.test.ts
autonomous: true
requirements: [AUD-04, AUD-05, AUD-08]
must_haves:
  truths:
    - "When a calibrated audience is active, the ideas SIM/react path resolves the audience's stored weights via resolveAudienceWeights and folds the stored persona repaint into the Flash prompt (deterministic, cache-stable)"
    - "When General (or no audience) is active, the Flash path is byte-identical to today — the repaint layer is a no-op for General (regression gate preserved)"
    - "ideas-runner grounds its 'why it fits' line on the active Audience (steer proof, AUD-05) — making GROUND-03 literally true for ideas — falling back to profile/cold-start grounding when no audience is active"
    - "Creator profile read-path in ideas is slimmed: ideas grounds on the active Audience; other runners still read creator_profiles (blast radius confined to ideas-runner, AUD-08)"
  artifacts:
    - path: "src/lib/audience/audience-grounding.ts"
      provides: "buildAudienceGroundingLine(audience, platform) — audience-facing 'why it fits' (steer), falls back to buildGroundingLine when no audience"
      exports: ["buildAudienceGroundingLine"]
    - path: "src/lib/engine/flash/flash-prompts.ts"
      provides: "Optional audience-repaint fold over byte-stable archetype blocks (no-op for General)"
      contains: "audience"
  key_links:
    - from: "src/lib/tools/runners/ideas-runner.ts"
      to: "src/lib/audience/resolve-audience-weights.ts + audience-grounding.ts"
      via: "resolve active audience weights + audience grounding line (steer)"
      pattern: "resolveAudienceWeights|buildAudienceGroundingLine"
    - from: "src/app/api/tools/ideas/route.ts"
      to: "src/lib/audience/audience-repo.ts"
      via: "load active audience by thread.active_audience_id, pass to runner"
      pattern: "getAudience|active_audience"
    - from: "src/lib/engine/flash/run-flash-text-mode.ts"
      to: "src/lib/engine/flash/flash-prompts.ts"
      via: "pass optional repaint into buildNicheAwareSystemPrompt"
      pattern: "buildNicheAwareSystemPrompt"
---

<objective>
Wire the audience into the two engine positions for P7's scoped proof: **② react** (the audience's stored weights + persona repaint feed the Flash SIM path) and **① steer** (ideas-runner grounds on the active Audience — AUD-05, the ONE steer proof per D-01). Both are additive and **no-op for General** so the regression gate holds. The audience flows: thread.active_audience_id → ideas route loads the Audience → passes weights + repaint + audience-grounding into ideas-runner → Flash prompt builder folds the stored repaint deterministically.

Purpose: make "your audience" literally drive generation+scoring for ideas (GROUND-03 becomes true) without touching the protected Max path or breaking cache discipline.
Output: audience-grounding lib + Flash repaint fold + ideas-runner/route wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-CONTEXT.md
@.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-RESEARCH.md
@.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-01-SUMMARY.md
@.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-02-SUMMARY.md
@.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-03-SUMMARY.md

# Seams (read before implementing):
@src/lib/tools/runners/ideas-runner.ts
@src/lib/kc/grounding-line.ts
@src/lib/engine/flash/flash-prompts.ts
@src/lib/engine/flash/run-flash-text-mode.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: audience-grounding (steer) + Flash audience-repaint fold (react), both no-op for General</name>
  <files>src/lib/audience/audience-grounding.ts, src/lib/audience/__tests__/audience-grounding.test.ts, src/lib/engine/flash/flash-prompts.ts, src/lib/engine/flash/run-flash-text-mode.ts</files>
  <read_first>
    - src/lib/kc/grounding-line.ts (buildGroundingLine — the profile fallback to delegate to when no audience; same {line, coldStart} shape)
    - src/lib/engine/flash/flash-prompts.ts (buildNicheAwareSystemPrompt — L162+; how slot.niche_instantiation folds into the per-archetype block; the byte-stable skeleton stays)
    - src/lib/engine/flash/run-flash-text-mode.ts (L85-105 — how the panel resolves to buildNicheAwareSystemPrompt; the no-panel/no-niche path is byte-identical to the stable prompt)
    - 07-RESEARCH.md Pattern 2 (repaint over byte-stable defaults) + Pitfall 1 (do NOT route General through an override) + Pitfall 2 (deterministic repaint, stored not generated)
  </read_first>
  <behavior>
    - buildAudienceGroundingLine(audience, platform): for a calibrated audience returns an audience-facing "Because: your {platform} audience — {temperature mix / top dispositions}" line (steer, GROUND-03); for General/null audience DELEGATES to buildGroundingLine(profileRow, platform) (existing behavior preserved)
    - Flash repaint fold: buildNicheAwareSystemPrompt with NO audience repaint param produces a BYTE-IDENTICAL string to today (snapshot/equality test against the current output) — General is a true no-op
    - Flash repaint fold: with an audience repaint (the stored 10 descriptions) substitutes the per-archetype description text while keeping the skeleton (task framing, Output Schema, TYPE RULES) byte-stable; same audience → same prompt string (determinism)
  </behavior>
  <action>
    Create `src/lib/audience/audience-grounding.ts`: export `buildAudienceGroundingLine(audience: Audience | null, platform: string, profileRow: ProfileRow | null): { line: string; coldStart: boolean }`. If `audience` is null or `is_general`, return `buildGroundingLine(profileRow, platform)` (delegate — zero behavior change for General). Otherwise compose an audience-facing line from the stored AudienceProfile (temperature mix + top dispositions + platform), honesty-spine clean (no fabricated counts). Pure function.

    Edit `src/lib/engine/flash/flash-prompts.ts`: extend `buildNicheAwareSystemPrompt` to accept an OPTIONAL audience-repaint argument (e.g. `audienceRepaint?: Record<Archetype, string>`). When absent → the existing code path runs unchanged (byte-identical output — this is the regression-critical no-op). When present → for each slot, substitute the stored audience description in place of (or appended deterministically to) `slot.niche_instantiation`, keeping the skeleton byte-stable. Do NOT mutate ARCHETYPE_DEFINITIONS. Add a comment citing Pitfall 2 (repaint must be the stored, deterministic text).

    Edit `src/lib/engine/flash/run-flash-text-mode.ts`: thread an optional `audienceRepaint` through the panel/options so it reaches `buildNicheAwareSystemPrompt`. The no-audience path stays byte-identical (the existing `panel.niche === null` branch is untouched).

    Write `audience-grounding.test.ts`: assert General/null delegates to buildGroundingLine (same output); calibrated audience produces an audience-facing line. Add a Flash-prompt equality test: `buildNicheAwareSystemPrompt(panel)` (no repaint) === current output for a fixed panel (the byte-stability/regression guard).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/audience/__tests__/audience-grounding.test.ts src/lib/engine/flash && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>audience-grounding delegates to buildGroundingLine for General; Flash repaint fold is a proven byte-identical no-op when no repaint is passed; calibrated repaint substitutes deterministically; engine constants unmutated; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: ideas-runner steer + react wiring + ideas route loads active audience</name>
  <files>src/lib/tools/runners/ideas-runner.ts, src/app/api/tools/ideas/route.ts</files>
  <read_first>
    - src/lib/tools/runners/ideas-runner.ts (L231-327 — the runIdeasPipeline body: buildGroundingLine call at L274, the panel `{ niche, contentType: null }` at L260, the runFlashTextMode loop at L263)
    - src/app/api/tools/ideas/route.ts (L121-160 — how profileRow is loaded + runIdeasPipeline is called; where to load the active audience)
    - src/lib/audience/resolve-audience-weights.ts + audience-grounding.ts + audience-repo.ts (07-01/07-03 outputs)
    - 07-CONTEXT.md D-01 (steer = ideas-runner ONLY) + D-04 (per-thread active audience) + RESEARCH §"Creator profile slim-down blast radius" (confined to ideas-runner this phase)
  </read_first>
  <action>
    Edit `src/lib/tools/runners/ideas-runner.ts`: extend `IdeasPipelineInput` with an optional `audience: Audience | null`. In `runIdeasPipeline`: (1) STEER — replace the `buildGroundingLine(profileRow, platform)` call with `buildAudienceGroundingLine(audience, platform, profileRow)` (which delegates to the old behavior when audience is null/General — AUD-05/AUD-08, blast radius confined HERE). (2) REACT — resolve the audience's weights via `resolveAudienceWeights(audience ? [audience] : [])` and pass the stored persona repaint (from `audience.personas`) into the `runFlashTextMode` call (the new optional `audienceRepaint` arg from Task 1). When audience is null/General, the repaint arg is omitted → byte-identical Flash behavior. Keep the gate floor + survivor logic unchanged. Preserve isolation discipline (document the new imports in the header comment).

    Edit `src/app/api/tools/ideas/route.ts`: after loading the thread + profileRow, load the active audience: read `thread.active_audience_id` (NULL → General) and resolve via `getAudience` (virtual constant for General/presets, DB row otherwise). Pass `audience` into `runIdeasPipeline`. Keep the existing creator_profiles read (other runners still use it; AUD-08 slims only the ideas grounding read-path, not the column). Auth + SSE flow unchanged.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/tools src/app/api/tools/ideas && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>ideas-runner steers on the active audience (delegates to profile/cold-start for General) and reacts with audience weights+repaint; ideas route loads thread.active_audience_id → audience → runner; General path byte-identical; existing ideas tests still green; tsc clean.</done>
</task>

</tasks>

<verification>
- `pnpm exec vitest run src/lib/audience src/lib/engine/flash src/lib/tools src/app/api/tools/ideas` — all green.
- `pnpm exec tsc --noEmit` — 0 errors.
- Grep guard: `grep -rn "ARCHETYPE_DEFINITIONS\s*=" src/lib/engine/flash/flash-prompts.ts` returns nothing (no in-place mutation).
- Byte-stability: the Flash-prompt equality test from Task 1 stays green (General no-op).
- The full engine regression assertion runs in the BLOCKING 07-05 gate (not here).
</verification>

<success_criteria>
For ideas, "your audience" literally drives generation grounding (steer) and SIM scoring (react) when a calibrated audience is active; General behaves byte-for-byte as today (regression gate preserved). The blast radius of the profile slim-down is confined to ideas-runner this phase (D-01/AUD-08).
</success_criteria>

<output>
Create `.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-04-SUMMARY.md`. Record: the new IdeasPipelineInput.audience contract, the audienceRepaint Flash arg signature, and confirmation that General is a proven byte-identical no-op (so 07-05's gate knows what to assert). Note for the post-P7 refinement run: steer-everywhere extends this same pattern to the other 5 runners + chat-runner.
</output>
