---
phase: 07-audience-manager-calibrated-audience-as-shared-substrate-acr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/audience/audience-types.ts
  - src/lib/audience/goal-intent.ts
  - src/lib/audience/temperature-disposition.ts
  - src/lib/audience/resolve-audience-weights.ts
  - src/lib/audience/__tests__/goal-intent.test.ts
  - src/lib/audience/__tests__/temperature-disposition.test.ts
  - src/lib/engine/__tests__/persona-weights.test.ts
autonomous: true
requirements: [AUD-04, AUD-06]
must_haves:
  truths:
    - "A goal-intent (grow/sell/authority/nurture) maps to a deterministic PersonaWeights bias — same intent always yields the same weights"
    - "Each of the 10 engine archetypes has a stable Temperature×Disposition presentation label (no archetype unmapped)"
    - "A calibrated audience's weights resolve through the existing analysis_override precedence; a General/null audience resolves to DEFAULT_PERSONA_WEIGHT_CONFIG unchanged"
  artifacts:
    - path: "src/lib/audience/audience-types.ts"
      provides: "Audience domain type, AudienceProfile, CalibratedPersona, GoalIntent union, multi-select-ready resolver shape"
      contains: "export interface Audience"
    - path: "src/lib/audience/goal-intent.ts"
      provides: "Deterministic GOAL_INTENT_BIAS table (D-05) seeded from WEIGHT_PRESETS"
      exports: ["GOAL_INTENT_BIAS", "biasForGoalIntent"]
    - path: "src/lib/audience/temperature-disposition.ts"
      provides: "10-archetype → Temperature×Disposition label lens (D-02 presentation only)"
      exports: ["TEMPERATURE_DISPOSITION", "labelForArchetype"]
    - path: "src/lib/audience/resolve-audience-weights.ts"
      provides: "resolveAudienceWeights(audiences[]) — array-shaped (multi-select-ready), General→DEFAULT, calibrated→analysis_override"
      exports: ["resolveAudienceWeights"]
  key_links:
    - from: "src/lib/audience/resolve-audience-weights.ts"
      to: "src/lib/engine/persona-weights.ts"
      via: "resolveWeights(DEFAULT_PERSONA_WEIGHT_CONFIG, { analysis_override })"
      pattern: "resolveWeights\\(DEFAULT_PERSONA_WEIGHT_CONFIG"
    - from: "src/lib/audience/goal-intent.ts"
      to: "src/components/board/audience/audience-constants.ts"
      via: "import { WEIGHT_PRESETS }"
      pattern: "WEIGHT_PRESETS"
---

<objective>
Build the pure, deterministic audience domain core — no DB, no I/O, no UI. Three lego bricks every other Phase-7 plan consumes: the `Audience` domain type (multi-select-ready), the goal-intent → deterministic weight-bias table (D-05), the Temperature×Disposition presentation lens over the existing 10 archetypes (D-02), and an array-shaped `resolveAudienceWeights` that routes a calibrated audience through the EXISTING `analysis_override` precedence while leaving General on `DEFAULT_PERSONA_WEIGHT_CONFIG` (so the regression gate stays free by construction).

Purpose: lock the contracts the persistence, calibration, and wiring plans build against — interface-first, so no downstream plan has to reverse-engineer shapes.
Output: `src/lib/audience/{audience-types,goal-intent,temperature-disposition,resolve-audience-weights}.ts` + unit tests + an extended `persona-weights.test.ts` General-identity case.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-CONTEXT.md
@.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-RESEARCH.md

# Load-bearing seams (read before implementing):
@src/lib/engine/persona-weights.ts
@src/lib/engine/wave3/persona-registry.ts
@src/components/board/audience/audience-constants.ts
@src/components/board/audience/audience-types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audience domain type + goal-intent bias table + Temperature×Disposition lens</name>
  <files>src/lib/audience/audience-types.ts, src/lib/audience/goal-intent.ts, src/lib/audience/temperature-disposition.ts, src/lib/audience/__tests__/goal-intent.test.ts, src/lib/audience/__tests__/temperature-disposition.test.ts</files>
  <read_first>
    - src/lib/engine/persona-weights.ts (PersonaWeights interface — the exact 4 keys fyp/niche/loyalist/cross_niche)
    - src/lib/engine/wave3/persona-registry.ts (ARCHETYPES — the canonical 10 slugs + SlotType union; do NOT redefine, import)
    - src/components/board/audience/audience-constants.ts (WEIGHT_PRESETS — seed values; do NOT invent new mixes)
    - 07-RESEARCH.md §"Temperature × Disposition Label Lens" table + §"Goal-intent → deterministic bias" — the [ASSUMED] structure tables to lock
  </read_first>
  <behavior>
    - goal-intent: biasForGoalIntent('grow') === GOAL_INTENT_BIAS.grow; all 4 intents (grow/sell/authority/nurture) present; each value is a PersonaWeights summing to 1.0 ±0.01 (assert via normalizeWeights round-trip)
    - goal-intent: GOAL_INTENT_BIAS.grow === WEIGHT_PRESETS.new_creator, .sell === niche_heavy, .authority === niche_heavy, .nurture === established (the locked D-05 [ASSUMED] mapping — structure now, values tuned later)
    - temperature-disposition: labelForArchetype returns a defined {temperature, disposition} for every one of the 10 ARCHETYPES (iterate ARCHETYPES, assert no undefined) — covers the "no archetype unmapped" gate
    - temperature-disposition: temperature ∈ {'cold','warm','hot'}; disposition ∈ {'scanner','skeptic','collector','connector','converter','lurker'}
  </behavior>
  <action>
    Create `src/lib/audience/audience-types.ts` as the domain SSOT. Define: `GoalIntent = 'grow'|'sell'|'authority'|'nurture'`; `Temperature = 'cold'|'warm'|'hot'`; `Disposition = 'scanner'|'skeptic'|'collector'|'connector'|'converter'|'lurker'`; `AudienceType = 'personal'|'target'`; `AudiencePlatform = 'tiktok'|'instagram'|'youtube'|'custom'`. Define `CalibratedPersona = { archetype: Archetype; repaint: string; temperature: Temperature; disposition: Disposition; share: number }`. Define `AudienceProfile = { temperature_mix: Record<Temperature, number>; top_dispositions: Disposition[]; follower_tier: string | null }`. Define the `Audience` domain interface mirroring the AUD-01 schema columns (id, user_id, name, type, platform, goal_label, goal_intent, is_general, is_preset, persona_weights: PersonaWeights, personas: CalibratedPersona[], profile, calibration). Import `PersonaWeights` from engine, `Archetype`/`ARCHETYPES` from `wave3/persona-registry.ts` — never redefine the 10 slugs.

    Create `src/lib/audience/goal-intent.ts`: import `WEIGHT_PRESETS` from `@/components/board/audience/audience-constants`. Define `GOAL_INTENT_BIAS: Record<GoalIntent, PersonaWeights>` per the locked D-05 [ASSUMED] mapping in the behavior block. Export `biasForGoalIntent(intent: GoalIntent): PersonaWeights`. Add a top comment marking these as [ASSUMED] structure-not-values (D-05) — refinement run tunes them; cite WEIGHT_PRESETS as source.

    Create `src/lib/audience/temperature-disposition.ts`: define `TEMPERATURE_DISPOSITION: Record<Archetype, { temperature: Temperature; disposition: Disposition }>` per the locked RESEARCH lens table (tough_crowd→cold/skeptic, lurker→cold/lurker, high_engager→warm/connector, saver→warm/collector, sharer→warm/connector, purposeful_viewer→warm/scanner, niche_deep_buyer→hot/converter, niche_deep_scout→hot/skeptic, loyalist→hot/connector, cross_niche_curiosity→cold/scanner). Export `labelForArchetype(a: Archetype)`. Add a comment: presentation lens ONLY (D-02) — engine vocabulary stays fyp/niche/loyalist/cross_niche + the 10 slugs; this never feeds the engine.

    Write the two test files asserting the behaviors above. Use a loop over `ARCHETYPES` to prove no archetype is unmapped (regression guard if someone adds an 11th).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/audience/__tests__/goal-intent.test.ts src/lib/audience/__tests__/temperature-disposition.test.ts</automated>
  </verify>
  <done>Both test files green; goal-intent covers all 4 intents with WEIGHT_PRESETS-sourced values; temperature-disposition maps all 10 archetypes; no engine file imported into the lens beyond the Archetype type.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resolveAudienceWeights (array-shaped, multi-select-ready) + persona-weights General-identity test</name>
  <files>src/lib/audience/resolve-audience-weights.ts, src/lib/engine/__tests__/persona-weights.test.ts</files>
  <read_first>
    - src/lib/engine/persona-weights.ts (resolveWeights signature + precedence chain — analysis_override is highest)
    - src/lib/audience/audience-types.ts (the Audience type from Task 1)
    - 07-RESEARCH.md §"Resolving the active audience's weights in the react path" code example + Pitfall 5 (audience_ids[]-ready)
  </read_first>
  <behavior>
    - resolveAudienceWeights([]) returns DEFAULT mix via resolveWeights(DEFAULT_PERSONA_WEIGHT_CONFIG, {}) → source 'default' (General = no override = gate-protected)
    - resolveAudienceWeights([generalAudience]) where is_general=true → DEFAULT mix, source 'default' (never injects an override for General)
    - resolveAudienceWeights([calibratedAudience]) → audience.persona_weights via analysis_override slot, source 'analysis_override'
    - resolveAudienceWeights([a, b]) (multi-select future) → resolves the FIRST audience in v1 (array-shaped signature, single-resolution semantics) — proves the signature is audience_ids[]-ready without a later refactor (Pitfall 5)
    - persona-weights.test.ts gains: resolveWeights(DEFAULT_PERSONA_WEIGHT_CONFIG, {}) returns exactly { fyp:0.65, niche:0.20, loyalist:0.10, cross_niche:0.05 } (General-identity — the regression-gate anchor; D-02/AUD-03)
  </behavior>
  <action>
    Create `src/lib/audience/resolve-audience-weights.ts`. Export `resolveAudienceWeights(audiences: Audience[]): { weights: PersonaWeights; source: WeightsSource }`. Implementation per D-04/Pitfall 5: accept an ARRAY (multi-select-ready), resolve the first element in v1. If the array is empty OR the first audience `is_general === true`, return `resolveWeights(DEFAULT_PERSONA_WEIGHT_CONFIG, {})` (no override → gate-protected DEFAULT). Otherwise return `resolveWeights(DEFAULT_PERSONA_WEIGHT_CONFIG, { analysis_override: first.persona_weights })`. Add a comment: the goal-intent bias is PRE-BAKED into `persona_weights` at calibration time (07-03) — this resolver does NOT re-apply bias per call (Pitfall 2/anti-pattern: no per-run nondeterminism). Do NOT mutate DEFAULT_PERSONA_WEIGHT_CONFIG.

    Extend `src/lib/engine/__tests__/persona-weights.test.ts` (do not rewrite existing cases) with a `describe("General-audience identity (AUD-03 regression-gate anchor)")` block asserting the empty-context DEFAULT resolution returns the exact byte-stable mix. This is the anchor the BLOCKING gate (07-05) re-runs.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/audience/__tests__ src/lib/engine/__tests__/persona-weights.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>resolveAudienceWeights array-shaped with General→DEFAULT and calibrated→analysis_override proven; persona-weights.test.ts General-identity case green; tsc 0 errors; DEFAULT_PERSONA_WEIGHT_CONFIG never mutated.</done>
</task>

</tasks>

<verification>
- `pnpm exec vitest run src/lib/audience src/lib/engine/__tests__/persona-weights.test.ts` — all green.
- `pnpm exec tsc --noEmit` — 0 errors.
- Grep guard: `grep -rn "ARCHETYPE_DEFINITIONS\|DEFAULT_PERSONA_WEIGHT_CONFIG =" src/lib/audience/` returns nothing (no engine constant mutated in this lib).
</verification>

<success_criteria>
The four audience-core modules exist, are pure (no I/O), and every downstream plan can import `Audience`, `biasForGoalIntent`, `labelForArchetype`, and `resolveAudienceWeights` without redefining engine vocabulary. General resolves to the byte-stable DEFAULT mix (regression gate free by construction).
</success_criteria>

<output>
Create `.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-01-SUMMARY.md` when done. Record: the locked GOAL_INTENT_BIAS mapping (so 07-03 bakes the same values), the Temperature×Disposition table (so 07-04 UI labels match), and the resolveAudienceWeights signature (so 07-04 wiring calls it correctly).
</output>
