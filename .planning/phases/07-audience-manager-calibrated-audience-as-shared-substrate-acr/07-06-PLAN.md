---
phase: 07-audience-manager-calibrated-audience-as-shared-substrate-acr
plan: 06
type: execute
wave: 5
depends_on: ["07-02", "07-03", "07-04", "07-05"]
files_modified:
  - src/lib/engine/__tests__/audience-regression-gate.test.ts
autonomous: false
requirements: [AUD-03]
must_haves:
  truths:
    - "The audiences migration is applied to the live DB (supabase db push) — verification is no longer a false-positive"
    - "ENGINE_VERSION is still exactly '3.19.0' — no deliberate video-scoring change shipped this phase"
    - "The General audience reproduces the byte-stable DEFAULT_PERSONA_WEIGHT_CONFIG mix (regression gate holds by construction)"
    - "The full engine + route suite is green and the Max video path's same-video score-identity is preserved"
  artifacts:
    - path: "src/lib/engine/__tests__/audience-regression-gate.test.ts"
      provides: "BLOCKING gate: ENGINE_VERSION unchanged + General→DEFAULT identity + Max path untouched assertions"
      contains: "3.19.0"
  key_links:
    - from: "src/lib/engine/__tests__/audience-regression-gate.test.ts"
      to: "src/lib/engine/version.ts + persona-weights.ts"
      via: "assert ENGINE_VERSION === '3.19.0' + General resolves DEFAULT"
      pattern: "ENGINE_VERSION|DEFAULT_PERSONA_WEIGHT_CONFIG"
---

<objective>
Close the phase with the mandatory schema push and the BLOCKING engine regression gate. Apply the `audiences` migration to the live DB (without it, build/type checks pass but the running app is broken — a false-positive verification per the schema-push requirement), then prove the protected Max path is untouched: `ENGINE_VERSION` unchanged, General reproduces the DEFAULT mix, full suite green. Mirrors the P6-05 blocking gate.

Purpose: guarantee the additive audience layer did not regress the protected video-scoring path, and that the DB matches the migrations.
Output: applied migration + a consolidated regression-gate test + a UAT pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-RESEARCH.md
@.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-02-SUMMARY.md
@.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-04-SUMMARY.md
@.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-05-SUMMARY.md

# Gate anchors:
@src/lib/engine/version.ts
@src/lib/engine/persona-weights.ts
@src/lib/engine/__tests__/version.test.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The audiences table + threads.active_audience_id migration (07-02), ready to apply to the live Supabase project.</what-built>
  <how-to-verify>
    Apply the migration: run `supabase db push` (set `SUPABASE_ACCESS_TOKEN` in the environment for non-TTY; the project is linked per supabase/config.toml). If `supabase db push` requires an interactive confirmation that cannot be suppressed, run it manually and paste the result. Confirm the new migration `20260619000000_audiences.sql` is listed as applied and the push reports success. Then regenerate `src/types/database.types.ts` against the now-migrated DB (the project's gen:types script) IF 07-02 deferred it.
  </how-to-verify>
  <resume-signal>Type "pushed" once `supabase db push` succeeded (paste the applied-migration line), or describe the push error.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: [BLOCKING] consolidated audience regression gate</name>
  <files>src/lib/engine/__tests__/audience-regression-gate.test.ts</files>
  <read_first>
    - src/lib/engine/version.ts (ENGINE_VERSION = "3.19.0")
    - src/lib/engine/__tests__/version.test.ts (the existing version assertion to mirror)
    - src/lib/engine/persona-weights.ts (DEFAULT_PERSONA_WEIGHT_CONFIG + resolveWeights)
    - src/lib/audience/resolve-audience-weights.ts (07-01 — General→DEFAULT)
    - 07-RESEARCH.md §"Sampling Rate" Phase gate (BLOCKING) + Pitfall 1 (aggregator.ts default path untouched)
  </read_first>
  <behavior>
    - ENGINE_VERSION === "3.19.0" (no bump — text/generation path only, additive weight channel)
    - resolveWeights(DEFAULT_PERSONA_WEIGHT_CONFIG, {}) === { fyp:0.65, niche:0.20, loyalist:0.10, cross_niche:0.05 } (General identity)
    - resolveAudienceWeights([]) and resolveAudienceWeights([generalAudience]) both resolve source 'default' with the DEFAULT mix (General injects no override → Max path unaffected)
    - the Max-path default weight resolution (aggregator) is documented as untouched — assert no audience override reaches the {niche}-only resolution used by the video path (test the resolver contract; the aggregator file is not modified this phase)
  </behavior>
  <action>
    Create `src/lib/engine/__tests__/audience-regression-gate.test.ts` consolidating the phase gate. Assert: ENGINE_VERSION literal === "3.19.0"; the General-identity DEFAULT resolution (re-asserting the 07-01 anchor here so the gate is self-contained); `resolveAudienceWeights([])` and `[generalAudience]` both yield source 'default' + DEFAULT mix. Add a comment-documented assertion that the Max video path resolves weights with `{ niche }` only and the audience layer never injects an override for General (Pitfall 1) — reference aggregator.ts L1095-1122 in the comment. This test is the BLOCKING anchor; it must be green before the phase passes.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/engine/__tests__/audience-regression-gate.test.ts src/lib/engine/__tests__/version.test.ts src/lib/engine/__tests__/persona-weights.test.ts</automated>
  </verify>
  <done>The gate test is green; ENGINE_VERSION proven 3.19.0; General reproduces DEFAULT via both resolveWeights and resolveAudienceWeights.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] full suite + tsc green</name>
  <files>src/lib/engine/__tests__/audience-regression-gate.test.ts</files>
  <read_first>
    - 07-RESEARCH.md §Validation Architecture (Full suite command `pnpm test`; phase gate = full suite green + ENGINE_VERSION + tsc 0 errors)
  </read_first>
  <action>
    Run the full engine + route suite (`pnpm test`) and `pnpm exec tsc --noEmit`. This is the BLOCKING phase gate (mirrors P6-05): the phase CANNOT pass with any red engine test or any tsc error. If a pre-existing flake is encountered, document it in the SUMMARY and confirm it is unrelated to the audience layer (e.g. a known-flaky network test); audience-touching tests must be green. Confirm the Max video path tests (same-video score-identity) remain green — that is the protected-path proof.
  </action>
  <verify>
    <automated>pnpm test && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Full suite green (or any red is documented-unrelated + audience tests green); tsc 0 errors; Max same-video score-identity tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete Phase-7 audience substrate, live: create/calibrate an audience, pin it to a thread, and generate ideas grounded + scored on that audience.</what-built>
  <how-to-verify>
    1. With the migration pushed and `pnpm dev` running: create a Personal (or preset) calibrated audience and pin it on a thread via the composer chip.
    2. Run the Ideas tool in that thread. Confirm the idea cards' "why it fits" grounding line reflects the AUDIENCE (not the old creator-profile niche) — AUD-05 steer proof — and the SIM band/quote reflects the audience (react).
    3. Switch the thread to General → run Ideas again → confirm behavior matches today's General output (no regression).
    4. Confirm the deferred boundaries hold: hook/script/test/remix/chat still ground on the creator profile (only ideas steers on the audience this phase — D-01); personas are read-only; no multi-select.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. ideas grounding still shows profile niche, General regressed, calibration broke).</resume-signal>
</task>

</tasks>

<verification>
- `supabase db push` applied `20260619000000_audiences.sql` to the live DB (human-action checkpoint).
- `pnpm test` — full engine + route suite green.
- `pnpm exec tsc --noEmit` — 0 errors.
- `audience-regression-gate.test.ts` green: ENGINE_VERSION "3.19.0" + General→DEFAULT.
- Human UAT: ideas steers+reacts on the active audience; General unchanged; deferred boundaries hold.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live DB | Schema change applied to production Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | regression gate bypass (silent ENGINE_VERSION bump) | mitigate | BLOCKING test asserts ENGINE_VERSION === "3.19.0"; suite must be green to pass |
| T-07-07 | Denial of service | migration breaks live DB | mitigate | Additive-only migration (CREATE TABLE IF NOT EXISTS + ADD COLUMN); no destructive DDL; analysis_results untouched |
</threat_model>

<success_criteria>
The DB matches the migrations (no false-positive verification), the protected Max video path is provably untouched (ENGINE_VERSION 3.19.0, General reproduces DEFAULT, suite green), and the end-to-end audience loop works for ideas while the deferred boundaries hold.
</success_criteria>

<output>
Create `.planning/phases/07-audience-manager-calibrated-audience-as-shared-substrate-acr/07-06-SUMMARY.md`. Record: the applied-migration confirmation, the gate results (ENGINE_VERSION, General-identity, suite status), any documented-unrelated flake, and the UAT outcome. This closes Phase 7.
</output>
