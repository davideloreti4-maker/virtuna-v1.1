---
phase: 10-final-qa
plan: 05
type: execute
wave: 2
depends_on: ["10-01", "10-02", "10-03", "10-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Mobile navigation drawer works correctly at 375px"
    - "Dashboard is usable on mobile (forms, results, history)"
    - "Landing page adapts properly to mobile viewport"
    - "Touch targets are at least 44px"
    - "No horizontal scrolling on any page"
  artifacts:
    - path: ".planning/phases/10-final-qa/discrepancies/responsive.md"
      provides: "Documented mobile responsiveness issues and fixes"
  key_links:
    - from: "src/components/app/mobile-nav.tsx"
      to: "mobile usability"
      via: "responsive testing"
---

<objective>
Perform comprehensive responsive testing at 375px mobile viewport for both app and landing pages.

Purpose: Ensure Virtuna is fully functional and visually polished on mobile devices. Since societies.io doesn't have a mobile view, this is Virtuna's own responsive adaptation that must maintain design language consistency.

Output: Verified mobile experience or documented issues with fixes applied.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-final-qa/10-CONTEXT.md
@.planning/phases/10-final-qa/10-RESEARCH.md
@src/components/app/mobile-nav.tsx
@src/components/app/app-shell.tsx
@src/components/app/sidebar.tsx
@src/components/layout/header.tsx
@src/components/landing/hero-section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test Mobile App Experience</name>
  <files>.planning/phases/10-final-qa/discrepancies/responsive.md</files>
  <action>
Test app dashboard at 375x812 mobile viewport:

1. **Open DevTools Device Mode:**
   - Set viewport: 375x812 (iPhone X dimensions)
   - Enable touch simulation
   - DPR: 2

2. **Test mobile navigation:**
   - Hamburger menu tap target (>= 44px)
   - Drawer opens smoothly
   - All nav items accessible
   - Society selector opens from drawer
   - View selector accessible
   - Close behavior (tap outside, swipe, X button)

3. **Test dashboard main area:**
   - Network visualization adapts to width
   - Context bar stacks or hides appropriately
   - Forms (content form, survey form) fit viewport
   - No horizontal scroll

4. **Test complete flow on mobile:**
   - Open test type selector
   - Select test type (modal fits)
   - Fill form (keyboard doesn't obscure inputs)
   - Submit and see loading states
   - View results (scrollable, readable)
   - Check history list (in drawer)
   - Delete test (modal fits)

5. **Test settings on mobile:**
   - Tab navigation works (tabs may stack)
   - Forms usable
   - All sections accessible

6. **Document issues:**
   ```markdown
   # Mobile Responsiveness Issues

   ## App Dashboard
   | Issue | Component | Expected | Actual | Severity |

   ## Touch Targets
   | Element | Size | Meets 44px? |
   ```
  </action>
  <verify>Mobile app flow documented with any issues identified</verify>
  <done>App mobile experience tested and issues documented</done>
</task>

<task type="auto">
  <name>Task 2: Test Mobile Landing and Fix Issues</name>
  <files>
    src/components/app/mobile-nav.tsx
    src/components/app/app-shell.tsx
    src/components/layout/header.tsx
    src/components/layout/footer.tsx
    src/components/landing/hero-section.tsx
    src/components/landing/features-section.tsx
    src/components/landing/stats-section.tsx
    src/components/landing/faq-section.tsx
    src/app/globals.css
  </files>
  <action>
Test landing page and fix all mobile issues:

1. **Test landing page at 375x812:**
   - Header: hamburger menu (if exists), logo sizing
   - Hero: headline readable, persona cards stack or scroll
   - Features: cards stack vertically
   - Stats: chart readable, may need horizontal scroll indicator
   - FAQ: accordion works with touch
   - Footer: links stack, social icons accessible

2. **Fix common mobile issues:**
   - Add/adjust responsive breakpoints (md:, lg: prefixes)
   - Ensure no overflow-x on containers
   - Stack horizontal layouts on mobile
   - Increase touch target sizes where needed
   - Fix text sizing for readability

3. **Verify no horizontal scroll:**
   ```css
   /* Add to globals.css if needed */
   html, body {
     overflow-x: hidden;
   }
   ```

4. **Touch targets verification:**
   - All buttons >= 44x44px on mobile
   - Links have adequate tap area
   - Form inputs have adequate height

5. **Update discrepancy doc with fixes**
  </action>
  <verify>npm run build passes, no horizontal scroll on any page at 375px</verify>
  <done>All mobile responsiveness issues fixed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human Verification of Mobile Experience</name>
  <what-built>Mobile-responsive app and landing page</what-built>
  <how-to-verify>
1. Use Chrome DevTools Device Mode at 375x812 (touch simulation enabled)
2. **Landing Page:**
   - Navigate landing page top to bottom
   - Verify no horizontal scroll
   - Test all interactive elements (nav, accordion, CTAs)
3. **App Dashboard:**
   - Open mobile nav drawer
   - Navigate through all sections
   - Complete full test flow (select type -> form -> simulate -> results)
   - Check history in drawer
   - Test settings tabs
4. **Touch Targets:**
   - Can easily tap all buttons without mis-taps
   - Dropdown menus work with touch
  </how-to-verify>
  <resume-signal>Type "approved" if mobile works, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- App fully functional at 375px viewport
- Landing page adapts properly to mobile
- No horizontal scrolling
- Touch targets adequate size
- Build passes without errors
</verification>

<success_criteria>
- Mobile navigation drawer works
- Full test flow completable on mobile
- Landing page sections stack/adapt properly
- No overflow issues
- Touch targets >= 44px
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/10-final-qa/10-05-SUMMARY.md`
</output>
