---
phase: 02-design-system-components
plan: 05
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - src/app/page.tsx
  - src/app/layout.tsx
autonomous: false

must_haves:
  truths:
    - "All components render correctly in browser"
    - "Button hover/tap animations work"
    - "Design tokens apply correct colors"
    - "Layout components display proper structure"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Component showcase page"
      min_lines: 50
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/ui/*"
      via: "component imports"
      pattern: 'import.*from.*"@/components/ui'
    - from: "src/app/page.tsx"
      to: "src/components/animations"
      via: "animation imports"
      pattern: 'import.*from.*"@/components/animations"'
---

<objective>
Create a component showcase page that displays all components, then verify visual correctness. This plan includes a human verification checkpoint.

Purpose: Visual verification ensures components look correct before building on top of them.
Output: Showcase page demonstrating all components, verified by user.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-components/02-RESEARCH.md
@.planning/phases/02-design-system-components/02-02-SUMMARY.md
@.planning/phases/02-design-system-components/02-03-SUMMARY.md
@.planning/phases/02-design-system-components/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update layout with Header and Footer</name>
  <files>src/app/layout.tsx</files>
  <action>
    Update the root layout to include Header and Footer:

    **Requirements:**
    - Import Header from "@/components/layout/header"
    - Import Footer from "@/components/layout/footer"
    - Wrap children in main element
    - Keep existing metadata and font setup

    **Structure:**
    ```tsx
    <body>
      <Header />
      <main className="min-h-screen">{children}</main>
      <Footer />
    </body>
    ```

    Keep the existing Geist font configuration.
    Keep className on body for font variables.
  </action>
  <verify>
    Run `npm run dev` and verify header and footer appear on page
  </verify>
  <done>Layout shows Header, main content area, and Footer</done>
</task>

<task type="auto">
  <name>Task 2: Create component showcase page</name>
  <files>src/app/page.tsx</files>
  <action>
    Replace the default Next.js page with a component showcase:

    **Requirements:**
    - Add "use client" directive (needed for Button animations)
    - Import Container from "@/components/layout/container"
    - Import Button from "@/components/ui/button"
    - Import Input from "@/components/ui/input"
    - Import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card"
    - Import Skeleton from "@/components/ui/skeleton"
    - Import { FadeIn, SlideUp } from "@/components/animations"

    **Sections to display:**

    1. **Buttons section:**
       - All variants: primary, secondary, outline, ghost, danger
       - All sizes: sm, md, lg
       - Disabled state

    2. **Inputs section:**
       - Default input
       - Input with placeholder
       - Disabled input
       - Error state input

    3. **Cards section:**
       - Card with header and content
       - Card with only content

    4. **Skeletons section:**
       - Text skeleton lines
       - Card skeleton

    5. **Animations section:**
       - FadeIn wrapped content
       - SlideUp wrapped content

    Wrap each section in FadeIn with staggered delays (0, 0.1, 0.2...).

    Use Container for layout.
    Add section headings (h2) with proper typography.
  </action>
  <verify>
    Run `npm run dev` and open http://localhost:3000
    Verify all components render without console errors
  </verify>
  <done>Showcase page displays all components organized by category</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete design system foundation with:
    - Design tokens (colors, typography, spacing)
    - UI components (Button, Input, Card, Skeleton)
    - Layout components (Header, Footer, Container)
    - Animation wrappers (FadeIn, SlideUp, PageTransition)
    - Component showcase page displaying all components
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` if not already running
    2. Open http://localhost:3000 in browser
    3. Verify the following:

    **Header:**
    - [ ] "Virtuna" logo text visible
    - [ ] Border at bottom

    **Buttons:**
    - [ ] Primary button: purple/primary color background, white text
    - [ ] Secondary button: gray background
    - [ ] Outline button: transparent with border
    - [ ] Ghost button: transparent, hover shows gray
    - [ ] Hover effect: slight scale up
    - [ ] Click effect: slight scale down
    - [ ] All three sizes render correctly

    **Inputs:**
    - [ ] Default input has gray border
    - [ ] Focus shows primary ring color
    - [ ] Error input has red border
    - [ ] Placeholder text is gray

    **Cards:**
    - [ ] Rounded corners, subtle shadow
    - [ ] Header and content spaced properly

    **Skeletons:**
    - [ ] Gray boxes with pulse animation

    **Animations:**
    - [ ] Content fades in on page load
    - [ ] Staggered animation visible

    **Footer:**
    - [ ] 4-column grid layout on desktop
    - [ ] Copyright at bottom

    **General:**
    - [ ] No console errors
    - [ ] Typography looks clean
    - [ ] Spacing is consistent
  </how-to-verify>
  <resume-signal>
    Type "approved" if everything looks correct.
    If issues found, describe them (e.g., "Button hover animation not working", "Primary color is wrong shade").
  </resume-signal>
</task>

</tasks>

<verification>
1. All components render without errors
2. Button animations play on hover/tap
3. Input focus states work
4. Animations stagger correctly
5. User approves visual appearance
</verification>

<success_criteria>
- Showcase page loads without errors
- All components display correctly
- User verifies visual appearance
- Ready to proceed to Phase 3 (Landing Pages)
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-components/02-05-SUMMARY.md`
</output>
