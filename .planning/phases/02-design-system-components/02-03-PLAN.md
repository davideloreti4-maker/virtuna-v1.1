---
phase: 02-design-system-components
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/layout/header.tsx
  - src/components/layout/footer.tsx
  - src/components/layout/container.tsx
autonomous: true

must_haves:
  truths:
    - "Header component renders navigation structure"
    - "Footer component renders site links"
    - "Container component provides consistent max-width and padding"
  artifacts:
    - path: "src/components/layout/header.tsx"
      provides: "Site header with navigation slots"
      exports: ["Header"]
      min_lines: 20
    - path: "src/components/layout/footer.tsx"
      provides: "Site footer with link sections"
      exports: ["Footer"]
      min_lines: 20
    - path: "src/components/layout/container.tsx"
      provides: "Max-width container wrapper"
      exports: ["Container"]
      min_lines: 10
  key_links:
    - from: "src/components/layout/header.tsx"
      to: "src/lib/utils.ts"
      via: "cn() import"
      pattern: 'import.*cn.*from.*"@/lib/utils"'
---

<objective>
Build the layout components: Header, Footer, and Container. These define the page structure used across landing and app pages.

Purpose: Every page needs consistent layout. These components are Server Components by default (no "use client") for optimal performance.
Output: Three layout components that compose into full page layouts.
</objective>

<execution_context>
@/Users/davideloreti/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davideloreti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-components/02-RESEARCH.md
@.planning/phases/02-design-system-components/02-01-SUMMARY.md
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Container component</name>
  <files>src/components/layout/container.tsx</files>
  <action>
    Create a Container component for consistent page width and padding:

    **Requirements:**
    - Use `cn()` from @/lib/utils
    - Accept children and className props
    - This is a Server Component (no "use client" directive)

    **Props interface:**
    ```typescript
    interface ContainerProps {
      children: React.ReactNode
      className?: string
    }
    ```

    **Styles:**
    - mx-auto (center horizontally)
    - max-w-7xl (1280px max width)
    - px-4 sm:px-6 lg:px-8 (responsive horizontal padding)
    - w-full

    Export as default function component (not forwardRef needed for layout).
  </action>
  <verify>
    Run `npx tsc --noEmit` - no TypeScript errors
  </verify>
  <done>Container component provides consistent max-width layout</done>
</task>

<task type="auto">
  <name>Task 2: Create Header component</name>
  <files>src/components/layout/header.tsx</files>
  <action>
    Create the site Header component:

    **Requirements:**
    - Use `cn()` from @/lib/utils
    - Import Container from "./container"
    - This is a Server Component (no "use client")
    - Placeholder structure - actual navigation links added in Phase 3

    **Structure:**
    ```tsx
    <header className="border-b border-gray-200 bg-white">
      <Container>
        <div className="flex h-16 items-center justify-between">
          {/* Logo placeholder */}
          <div className="flex items-center">
            <span className="text-xl font-bold">Virtuna</span>
          </div>

          {/* Navigation placeholder - will be populated in Phase 3 */}
          <nav className="hidden md:flex items-center gap-6">
            {/* Links go here */}
          </nav>

          {/* Actions placeholder */}
          <div className="flex items-center gap-4">
            {/* Auth buttons go here */}
          </div>
        </div>
      </Container>
    </header>
    ```

    **Styles:**
    - border-b, border-gray-200
    - bg-white
    - Sticky positioning not added yet (will be added when animations come)

    Export as default function.
  </action>
  <verify>
    Run `npx tsc --noEmit` - no TypeScript errors
  </verify>
  <done>Header component renders with Container, has slots for nav and actions</done>
</task>

<task type="auto">
  <name>Task 3: Create Footer component</name>
  <files>src/components/layout/footer.tsx</files>
  <action>
    Create the site Footer component:

    **Requirements:**
    - Use `cn()` from @/lib/utils
    - Import Container from "./container"
    - This is a Server Component (no "use client")
    - Placeholder structure - actual links added in Phase 3

    **Structure:**
    ```tsx
    <footer className="border-t border-gray-200 bg-gray-50">
      <Container>
        <div className="py-12">
          {/* Footer content grid - 4 columns on desktop */}
          <div className="grid grid-cols-2 md:grid-cols-4 gap-8">
            {/* Column 1: Brand */}
            <div className="col-span-2 md:col-span-1">
              <span className="text-xl font-bold">Virtuna</span>
              <p className="mt-2 text-sm text-gray-500">
                Building the future of communities.
              </p>
            </div>

            {/* Column 2: Product links placeholder */}
            <div>
              <h3 className="font-semibold text-sm text-gray-900">Product</h3>
              <ul className="mt-4 space-y-2">
                {/* Links go here in Phase 3 */}
              </ul>
            </div>

            {/* Column 3: Company links placeholder */}
            <div>
              <h3 className="font-semibold text-sm text-gray-900">Company</h3>
              <ul className="mt-4 space-y-2">
                {/* Links go here in Phase 3 */}
              </ul>
            </div>

            {/* Column 4: Legal links placeholder */}
            <div>
              <h3 className="font-semibold text-sm text-gray-900">Legal</h3>
              <ul className="mt-4 space-y-2">
                {/* Links go here in Phase 3 */}
              </ul>
            </div>
          </div>

          {/* Copyright */}
          <div className="mt-12 pt-8 border-t border-gray-200">
            <p className="text-sm text-gray-500">
              &copy; {new Date().getFullYear()} Virtuna. All rights reserved.
            </p>
          </div>
        </div>
      </Container>
    </footer>
    ```

    **Styles:**
    - border-t, border-gray-200
    - bg-gray-50
    - Grid layout for columns

    Export as default function.
  </action>
  <verify>
    Run `npx tsc --noEmit` - no TypeScript errors
  </verify>
  <done>Footer component renders with Container, has grid layout for link sections</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes without errors
2. All three components import correctly
3. Header and Footer both use Container internally
4. `npm run build` completes successfully
5. Components render correctly when added to a test page
</verification>

<success_criteria>
- Container provides max-w-7xl with responsive padding
- Header shows logo placeholder and nav/action slots
- Footer shows brand, 4-column grid, and copyright
- All components are Server Components (no "use client")
- All components accept className for customization
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-components/02-03-SUMMARY.md`
</output>
