/**
 * Vitest alias stub for @pmndrs/detect-gpu.
 * Package is not installed — this stub prevents Vite import-analysis from erroring
 * when perf-tier.ts is transitively imported in tests.
 */
export const getGPUTier = async () => ({ tier: 3, type: 'BENCHMARK' });
