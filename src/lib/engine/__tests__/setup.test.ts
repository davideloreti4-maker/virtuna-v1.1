/**
 * Vitest infrastructure smoke test.
 * Validates that the test runner, globals, and alias resolution work.
 * This file can be removed once real tests exist.
 */
describe("vitest setup", () => {
  it("runs", () => {
    expect(true).toBe(true);
  });
});
