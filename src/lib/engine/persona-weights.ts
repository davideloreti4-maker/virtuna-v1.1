/**
 * Persona weight precedence resolver — stub for Wave 0 test scaffolding.
 * Implementation ships in Plan 04.
 * @see src/lib/engine/__tests__/persona-weights.test.ts for test surface.
 */

// Stub exports — replaced by Plan 04 implementation.
export const DEFAULT_PERSONA_WEIGHT_CONFIG: Record<string, number> = {};

export function resolveWeights(..._args: unknown[]): never {
  throw new Error("resolveWeights: not yet implemented — see Plan 04");
}

export function normalizeWeights(..._args: unknown[]): never {
  throw new Error("normalizeWeights: not yet implemented — see Plan 04");
}
