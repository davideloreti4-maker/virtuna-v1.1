/**
 * Weighted curve aggregator + HeatmapPayload assembly — stub for Wave 0 test scaffolding.
 * Implementation ships in Plan 04.
 * @see src/lib/engine/__tests__/weighted-aggregator.test.ts for test surface.
 */

// Stub exports — replaced by Plan 04 implementation.
export const DEFAULT_WEIGHTS: Record<string, number> = {};

export function buildWeightedCurve(..._args: unknown[]): never {
  throw new Error("buildWeightedCurve: not yet implemented — see Plan 04");
}

export function assembleHeatmapPayload(..._args: unknown[]): never {
  throw new Error("assembleHeatmapPayload: not yet implemented — see Plan 04");
}
