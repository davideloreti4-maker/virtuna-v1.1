import { HeroSection } from "@/components/landing";

export default function HomePage() {
  return (
    <main>
      <HeroSection />
      {/* More sections will be added in subsequent plans */}
    </main>
  );
}
