# E2E platform audit — core actions + in-thread UX (2026-07-26)

**Worktree:** `~/virtuna-e2e-audit` · **Branch:** `audit/e2e-walkthrough` off `origin/main` (`f1cd20d4`)
**Mode:** audit-only. NO fixes land on this branch. Findings collect here; fixing happens in later
sessions, each cutting its own branch off main.

## ▶ RESUME HERE (handoff, rewritten 2026-07-26 end of session 2)

**State (s3):** `/audience` + all 8 settings tabs (s1) · the reachability map · Act B · **all 7
skills** in Act C — C5 ran 2026-07-26 s3, live-billed, upload path. **20 findings, TWO blockers
(F-017 + F-019: the Test card seals into a thread the UI can never open).** Still audit-only.

**Next task: Act D** (in-thread UX: waiting, card legibility, CTAs, reload, skill-switch, sidebar
history, degrade honesty — note C5 already banked one: 169 s of static *"Uploading your video…"*
with no progress). Then **Act E** (mobile 390×844 — the mobile account menu did not open under five
different trigger selectors in s2; unverified, could easily be my selector).

**s3 rig notes:** the Test skill's menu label is now **"A real video"** (renamed on main;
`skill.mjs`'s ROW map still keys the old "Video test" label but matches by DESCRIPTION, so it still
works — `c5-test.mjs` asserts the new label). The Chromium login did NOT survive to s3 — reset via
`npx tsx e2e/create-test-user.ts`, then the password form (click *"Sign in with a password
instead"*, and scope fills to the form CONTAINING the password input — both forms stay mounted and
a bare `input[type=email]` fill hits the code-flow form). Chromium profile lives at
`/private/tmp/claude-501/-Users-davideloreti-virtuna-v1-1/17f2063e-…/scratchpad/audit-profile`.

**⚠️ Port moved: `:3030` → `:3040`.** The onboarding worktree's dev server owns `:3030` and belongs
to another live session — do not kill it. Launcher: `scratchpad/launch-audit-dev.py` (already
repointed).

**Startup checklist**
1. `cd ~/virtuna-e2e-audit && git fetch origin main && git rebase origin/main` — main moves fast
   (5 commits during session 2, 10 during session 1).
2. `pnpm install` only if the rebase touched deps. **Repo is pnpm — `npm install` stalls forever.**
3. `rm -rf .next`, relaunch detached on **`:3040`**, then **verify its cwd**:
   `lsof -a -p $(lsof -tiTCP:3040 -sTCP:LISTEN) -d cwd` must print this worktree.
   *Session 2 caught `:3030` serving `~/virtuna-onboarding` — the check earns its keep.*
4. The shared Chromium on CDP `:9222` has a persistent profile; the login survived both sessions.
   Cookies are per-host, not per-port, so the `:3040` move cost nothing.
5. Test user: `e2e-test@virtuna.local` / `e2e-test-password-2026`
   (`npx tsx e2e/create-test-user.ts` resets it).

**⚠️ The dev server died mid-session** right after a video-bearing remix run (clean log end — most
likely the 2 GB heap cap). A page read taken seconds later returns Chrome's `ERR_CONNECTION_REFUSED`
shell, which reads exactly like "the UI rendered nothing". **Confirm the server is alive before
interpreting any empty surface** — this nearly became a false finding.

**Instrument lessons that cost real time in s2 — read before writing a driver:**
- The Start-view **artifact tile** and the **skill-menu row** render nearly the same string. Match
  the *description* (`Funnel-top idea cards`), never the label (`Ideas`) — a sidebar thread title
  beginning "ideas…" outranks both. The two doors behave **differently** (F-017), so conflating
  them hides bugs.
- Find the send button by walking a **fixed 6 levels up from the skill pill**, then taking the
  rightmost non-`⚙` button. Walking "up until it contains the textarea" short-circuits when the
  textarea is absent and silently returns a card CTA; a y-band around the model pill catches a
  card's `Simulate` button at the same height.
- A **1,200 ms settle is too short** to observe a redirect chain — it manufactured a false orphan.
- Don't grep rendered copy for `error`/`failed`: generated hook copy legitimately contains
  *"Prediction Error"*. Match failure **phrases**, and remember the real copy is
  *"Couldn't **read** that video"*.
- `/api/threads/open` does **not** return `active_audience_id`; read the thread pin from the DB.
  Tables are `threads` / `messages`, and the blocks live in **`messages.body.blocks`**.
- Skill runs are not all under `/api/tools/*` — **account teardown posts to `/api/account-read`**.

⚠️ **Apify budget re-measured at the start of s2: $1.31 of $5 → `$3.69` headroom** (unchanged from
s1). Session 2 spent one remix scrape ×2 plus omni passes (~0.23¢ each) — negligible, but C5 is the
first genuinely expensive station.

⚠️ **Test-user state mutated** (s1: default audience → 2nd card, `emailUpdates` true→false;
s2: default audience is now **Fitness Creators**, and several throwaway threads were created).
None of it is a bug.

⚠️ **Apify budget ~$3.69.** `.env.local` here holds the good key (`Fj…pQpf`, `arcuate_azurite`).
The trunk key (`FG…lZIV`) is exhausted — if a scrape-backed run degrades, check credit before
blaming the product.

⚠️ **Test-user state was mutated** by session 1: default audience switched to the 2nd card, and
`emailUpdates` toggled `true → false`. Neither is a bug.

---

## Scope

**IN — the core loop, and whether it fits the ICP.**
Landing logged-in → configure the run → run every action in-thread → read the card → what happens
next. Both halves: *is the backend actually working*, and *is this the UX we want*.

**OUT — owned by other live sessions, do not touch:**
- the simulation rail
- onboarding / the funnel
- the landing page

Where an out-of-scope surface sits inside the core loop (the ambient rail is mounted next to the
composer), it is audited **only** for how it affects the in-thread action flow. Anything internal to
it gets noted and handed off, not fixed here.

> ⚠️ `/start` is not a surface. `src/app/(app)/start/page.tsx` is a 14-line `redirect("/home")`
> (hidden 2026-07-15, lane/launch-prep). The flow that reads as "start" is **`/home`** — the composer
> Start view. All references below are to `/home`.

## Run configuration

| Knob | Value | Why |
|---|---|---|
| `NEXT_PUBLIC_AMBIENT_V2` | `true` (in `.env.local`) | The ship-state is *flip the flag*, not cut over. Auditing flag-OFF audits a product we aren't shipping. |
| `BILLING_ENFORCE_QUOTA` | unset (OFF) | Matches prod. |
| `GROUNDING_*` | ON (inherited) | Matches the intended shipping config. |
| `APIFY_TOKEN` | **swapped** — see N-1 | The inherited key was out of credit. |
| Dev server | port `3030`, this worktree only | Confirm cwd before trusting any visual check. |
| Live runs | **real, billed** | Rendering can be audited against fixtures; "is the backend working" cannot. |

**Verified at runtime, not assumed:** `--color-accent` = `#ff6363`, body bg = `rgb(31,31,30)`
(`#1f1f1e`), no horizontal overflow at 1440. Matches the design SSOT in `globals.css`.

## Rig

| Piece | Where |
|---|---|
| Dev server | `localhost:3030`, detached, log at `scratchpad/dev-3030.log` |
| Shared browser | headed Chromium, persistent profile, CDP on `localhost:9222` |
| Station driver | `scratchpad/station.mjs` → freezes animations, screenshots, dumps measured facts |
| Captures | `scratchpad/shots/<slug>.png` + `<slug>.json` |

One browser window, one session. The owner logs in once; every station script drives that same
window, so what gets measured is exactly what the owner is looking at.

**Before trusting any visual check:**
```
lsof -tiTCP:3030 -sTCP:LISTEN            # -> pid
lsof -a -p <pid> -d cwd                  # -> must be /Users/davideloreti/virtuna-e2e-audit
```

## How a station runs

1. Owner names the surface to dissect.
2. I drive it, capture the shot + measured facts, and read the code/DB/network behind it.
3. Owner rules on the UX; I rule on whether the backend did what the UI claims.
4. Every gap becomes a numbered finding. We do **not** fix it. We move on.

A station is DONE when every element on it has been either ticked or turned into a finding.

## Finding format

```
### F-NNN · <one-line claim>
- **Station:** · **Kind:** BACKEND | UI | UX | ICP-FIT | DATA-HONESTY | PERF | COPY
- **Severity:** blocker | major | minor | polish
- **Saw:** what actually happened
- **Expected:** what should happen, and on whose authority (spec / owner call / design system)
- **Evidence:** file:line, network call, DB row, shot path
- **Repro:** exact steps
```

*blocker* = cannot ship; path broken or lies to the user. *major* = ships but visibly wrong,
dishonest, or a dead end. *minor* = noticeable, not damaging. *polish* = craft debt.

---

## Stations

Owner picks the order. This is the coverage list, not a script.

### A — Landing logged in (`/home`)
| # | Station | What we're auditing |
|---|---|---|
| A1 | First paint | greeting, serif voice-moment, layout, no flash of wrong state, time-to-usable |
| A2 | Artifact grid | Content (ideas · hooks · script · remix · test · **ad**=soon) / Intel (explore · account · **compare**=soon); lens lines render; "soon" tiles inert |
| A3 | ICP fit | is this the first thing our ICP should see? does the grid read as "what do I get" or as a tool menu? |
| A4 | Empty / first-run | no orphaned footer, no dead receipts, no empty-loop dead end |

### B — Configuring the run
| # | Station | What we're auditing |
|---|---|---|
| B1 | Audience chip | what it offers, what it defaults to, whether it **binds to the run** |
| B2 | Scene (TikTok / IG / No feed) | same — does the choice reach the engine or decorate the UI? |
| B3 | Fidelity (Flash / Max) | does it change the run, the cost, and the wait? is the trade legible? |
| B4 | Config → run contract | prove config reaches the API payload, not just React state |

### C — Running every action in-thread
Per skill: the run capsule (label → spine → ✓receipt → cards), the card's fields, the follow-up
CTAs, and whether the backend produced grounded output or **degraded silently**.
| # | Skill | Known context |
|---|---|---|
| C1 | ideas | grounding facets known-blocked on niche vocab |
| C2 | hooks | structural retrieval; per-persona assignment |
| C3 | script | |
| C4 | remix | |
| C5 | **test (video)** | paid, ~83s. Upload → pipeline → **seal in thread**, no navigate-out. Director's fixes derive from Apollo dims. |
| C6 | explore | |
| C7 | account teardown | |
| C8 | `ad` · `compare` | `status:"soon"` — confirm inert, and that "soon" is honest |

### D — In-thread UX (the refinement target)
| # | Station | What we're auditing |
|---|---|---|
| D1 | Waiting | what the user sees for 83s. Is the spine honest about stage and remaining time? |
| D2 | Card legibility | does the card answer "what do I do now" above the fold? |
| D3 | Follow-up CTAs | every CTA goes somewhere real. No dead ends, no interim links presented as final. |
| D4 | Reload mid-thread | unified stream renders every skill's history, ordered |
| D5 | Skill switch | swap-on-switch; no vanishing text |
| D6 | Sidebar history | list, open, delete |
| D7 | Degrade honesty | force a failure (Apify out of credit / bad URL / >30s video) — does the UI admit it or ship a confident empty card? |

### E — Mobile pass (390×844)
Re-walk A–D at mobile width. Top nav, composer dock, card legibility, gutters.

---

## Notes (environment, not product)

### N-1 · The inherited Apify key was out of credit
The `.env.local` carried from trunk held `apify_api_FG…lZIV` (account `rousing_saxophone`) — **$5.037
spent of a $5/mo FREE allowance**, i.e. exhausted. Every scrape-backed run would have failed or
degraded, and I'd have logged product findings for an environment fault. Swapped to
`apify_api_Fj…pQpf` (account `arcuate_azurite`, $1.31 of $5 → ~$3.69 headroom), the key the
ambient-v2 and onboarding worktrees have been using since 2026-07-25.

Two things follow: **budget is ~$3.69**, so scrape-backed runs are finite — and *what the product
does when Apify is out of credit* is a real audit question (station D7), not a hypothetical.

---

## Findings

### F-001 · The E2E auth setup targets a route that no longer exists
- **Station:** rig · **Kind:** BACKEND (test infra) · **Severity:** minor
- **Saw:** `e2e/auth.setup.ts:33-36` waits for a URL containing `dashboard` or `welcome` after login.
  There is no `/dashboard` route in `src/app/`; login lands on `/home`.
- **Expected:** the auth fixture reflects the shipped post-login destination.
- **Evidence:** `e2e/auth.setup.ts:33`; route list under `src/app/(app)/`.
- **Repro:** `pnpm e2e:auth` with valid creds — the `waitForURL` times out at 30s.
- **Correction (measured):** `/dashboard` is not a 404 — it 308-redirects to `/home`. The fixture
  still fails, because it matches on `pathname.includes('dashboard')` and the pathname *after* the
  redirect is `/home`. So the fix is the assertion, not a missing route.
- **Note:** worked around for this audit (shared persistent browser + manual login, which also
  survives OTP). Logged because the whole Playwright E2E project depends on this fixture, so the
  suite is presumably not runnable as-is.

### F-002 · `/api/team` 500s on an infinite RLS recursion — the Team tab renders a blank page
- **Station:** S-team · **Kind:** BACKEND · **Severity:** blocker (for the feature)
- **Saw:** `/settings?tab=team` renders **nothing at all** in the panel. Three `GET /api/team`
  requests return `500 {"error":"Failed to create team"}`. Server log:
  `code: '42P17', message: 'infinite recursion detected in policy for relation "team_members"'`.
- **Expected:** either the team loads, or the UI says it failed. It currently shows an empty
  panel — indistinguishable from "you have no team", which is not what happened.
- **Evidence:** `src/app/api/team/route.ts:29-40` (auto-create path); dev log 42P17; probe
  `scratchpad/net.mjs '/settings?tab=team'`. `TeamSection` is rendered unconditionally
  (`settings-page.tsx:115`) and has **no error or empty branch**, hence the void.
- **Repro:** log in → `/settings?tab=team`.
- **Root cause is in the database, not the code:** the RLS policy on `team_members` queries
  `team_members`. Fixing needs a migration, not a component change.
- **Ask the owner first:** see F-009 — this may be a feature to delete, not repair.

### F-003 · The "$1" plan button fails silently — 503, and the user is told nothing
- **Station:** S-billing · **Kind:** UX + BACKEND · **Severity:** major
- **Saw:** clicking `Start for $1` fires `POST /api/whop/checkout` → `503 {"error":"Checkout is
  unavailable for this plan"}` (twice). **No toast, no inline error, no state change** — the button
  looks like it did nothing at all. Zero elements matching `[role=alert]`/`[role=status]`.
- **Expected:** a failed checkout says so. The known cause (no Whop keys configured) is an ops
  gap — the *silence* is the product defect, and it will survive Whop being configured.
- **Evidence:** probe `scratchpad/click.mjs '/settings?tab=billing' 'button:has-text("Start for $1")'`.
- **Repro:** `/settings?tab=billing` → click any of the three `Start for $1` buttons.

### F-004 · "No plan" is badged **Active**, in green
- **Station:** S-billing · **Kind:** COPY + UI · **Severity:** minor
- **Saw:** the Current plan card reads `No plan` with a green `Active` pill beside it.
- **Expected:** "No plan" and "Active" are contradictory. Separately, **green is off-system** —
  the design system is cream + coral-red `#FF6363` at near-zero dosage; there is no green state
  colour in `globals.css`.
- **Evidence:** `shots/set-billing.png`; `docs/DESIGN-SYSTEM.md` dosage rule.

### F-005 · The subscription payload reports `used: 100` against `limit: 0`
- **Station:** S-billing · **Kind:** DATA-HONESTY · **Severity:** minor (not user-visible *here*)
- **Saw:** `GET /api/subscription` → `usage: {used: 100, limit: 0, remaining: 0, enforced: false}`.
  100 used out of a limit of 0.
- **Expected:** a coherent meter. `limit: 0` should mean "no allowance", which cannot coexist with
  100 consumed.
- **Note — do not overstate:** `billing-section.tsx:245` guards with `!usage.limit ? 0`, so no
  meter currently renders a lie on this tab. But `composer.tsx` also reads `usage`, so the same
  payload feeds the in-thread credits display — **verify there before ruling this cosmetic.**

### F-006 · Account settings ends with a Sketchfab 3D-model attribution block
- **Station:** S-account · **Kind:** UI · **Severity:** minor
- **Saw:** below "Danger zone", a `Credits` heading links to a Sketchfab
  ["Brain"](https://sketchfab.com/3d-models/brain-cadd2bde67404c43b2359a6a3281d84a) model by
  `dgallichan` under CC-BY-4.0.
- **Checked before calling it dead:** the asset **is still used** — `public/brain/cortex.glb`. So
  the CC-BY credit is a real legal obligation, not leftovers. It is simply in the wrong place:
  the last thing in a user's *Account* settings, under the delete-my-account panel.
- **Expected:** attribution belongs on an about/legal/credits surface, not in account security.
- **Evidence:** `src/components/app/settings/settings-page.tsx` (only `sketchfab` reference in `src`).

### F-007 · "Delete account" is disabled and points at a support channel that doesn't exist
- **Station:** S-account · **Kind:** UX · **Severity:** major
- **Saw:** the Danger zone renders a **disabled** `Delete account` button plus "Contact support to
  delete your account." No support address, link, or widget appears anywhere on the surface.
- **Expected:** either account deletion works, or the copy names a reachable contact. As shipped
  there is no path to delete an account at all.
- **Evidence:** `shots/set-account.png`; DOM shows `[disabled] Delete account`.

### F-008 · The Profile tab asks a creator for their "Company" and "Role"
- **Station:** S-profile · **Kind:** ICP-FIT · **Severity:** minor
- **Saw:** Profile = Full name · Email (disabled) · **Company** · **Role** · Save changes.
  `GET /api/profile` confirms both persist and are empty (`"company":"", "role":""`).
- **Expected:** for a solo-creator ICP these are B2B-SaaS boilerplate. The fields a creator would
  expect — handle, platform, niche — live in a *different* tab (Creator Profile), so the first
  settings screen asks the least relevant questions.

### F-009 · The product ships a full Team feature — invites, roles, owner/admin/member
- **Station:** S-team · **Kind:** ICP-FIT · **Severity:** major (scope decision, owner's call)
- **Saw:** `team-section.tsx` is a complete multi-tenant team UI (invite by email, role badges
  Owner/Admin/Member, remove member), with `/api/team`, `/api/team/invite`,
  `/api/team/members/[id]` behind it.
- **Question for the owner:** for a solo-creator ICP, is this feature in the product at all? If
  not, deleting it also deletes F-002 — and the `teams`/`team_members` tables and their broken
  RLS policies go with it.

### F-010 · The audience composition bar is unlabeled and `aria-hidden` — real data rendered as ornament
- **Station:** A-audience · **Kind:** UI + UX · **Severity:** minor
- **Saw:** each audience card carries a 7px segmented strip. It encodes genuine information —
  one segment per persona, width = that persona's share, brightness = their temperature
  (cold/warm/hot). On screen it reads as a decorative grey ruler: no legend, no label, no tooltip,
  no hover. Both branches set `aria-hidden="true"`, so it is invisible to assistive tech too.
- **Expected:** the most compact factual summary of an audience should be decodable. Right now the
  only way to learn what it means is to read the source.
- **Evidence:** `src/components/audience/audience-composition-bar.tsx` (`TEMP_FILL`, `aria-hidden`).
- **Note:** the component's own doc comment argues it replaced a placeholder that "carried no
  information on any row". It now carries information nobody can read.

### F-011 · Creator Profile is a ~2,790px questionnaire — and it *is* wired (verify the ask, not the wiring)
- **Station:** S-creator-profile · **Kind:** ICP-FIT · **Severity:** major
- **Saw:** one scroll of ~2,790px covering niche + sub-genre, audience age/gender/geo/language,
  goal, creator stage, content style, cutting pace, reference creators, past wins, past flops,
  posting cadence, pain points, and video-retention consent.
- **Checked before calling it dead:** **all 19 schema fields have non-test consumers** in
  `src/lib/engine`, `src/lib/tools`, `src/lib/kc`, or `src/app/api` — `niche_primary` (23 files),
  `language` (17), `past_wins`/`past_flops`/`target_audience` (10 each), down to a floor of 3
  (`creator.ts` + `pipeline.ts` + a route) for `cuts_per_second`, `pain_points`,
  `posting_frequency`, `reference_creators`, `time_of_day_aware`.
- **So the finding is not "it's dead".** It is: (a) nothing on this screen tells the user any of it
  matters or what it changes, so the completion incentive is zero; and (b) the floor-of-3 fields
  reach the engine only as prompt plumbing — **whether they change any output is unproven and worth
  a targeted test** before we defend the length of this form.

---

## Deletion review — Team and Creator Profile (owner asked 2026-07-26)

### Team — **safe to delete.** Verified isolated.
- `team_id` appears in `src/types/database.types.ts` **only inside `team_members` itself** (the
  column, its FK, and the Insert/Update shapes). No other table is scoped by team; no query filters
  by it. One migration owns the whole feature: `supabase/migrations/20260217200000_teams.sql`.
- Surfaces to remove: the `team` tab + `team-section.tsx`, `use-team` hooks, `/api/team`,
  `/api/team/invite`, `/api/team/members/[id]`, and the `teams` / `team_members` tables.
- **Deleting it resolves F-002** — the recursive RLS policy goes with the table, so nobody has to
  debug a 42P17 for a feature we don't ship.

### Creator Profile — **the interview is dead; the data is live. Do not delete the data.**

The instinct is right about the *system* and wrong about the *dependency*. Both halves matter:

**1. The old 9-card interview is genuinely unreachable.** The mount chain is broken:
`ProfileInterviewModal` ← mounted only by `content-form.tsx:558` ← imported only by
`command-bar/CommandBar.tsx:7` ← **imported by nothing** (the only other mention is a comment in
`reading-chat.tsx:33`). So the modal that `profile_interview_seen_at` gates can never render.
That is dead code, and it is the system being replaced.

**2. But the profile row it wrote is read on every shipped skill, by two different paths.**

| Path | Entry | Reads |
|---|---|---|
| KC role-map | `/api/tools/{ideas,hooks,script,remix/run,react,refine,chat}` → `profileRow` → `PROFILE_ROLE_MAP` | **10 columns**: `niche_primary`, `niche_sub`, `target_audience{age_range,gender_skew,geo,language}`, `primary_goal`, `creator_stage`, `past_wins`, `past_flops`, `target_platforms`, `writing_voice_sample` |
| Video pipeline | `pipeline.ts:440` → `fetchCreatorContext(supabase, creator_handle, niche)` | the **full** set, incl. the 6 below — but keyed on `tiktok_handle`, not `user_id` |

**3. Six fields the form asks for are invisible to every generative skill.**
`content_style`, `cuts_per_second`, `reference_creators`, `posting_frequency`, `time_of_day_aware`,
`pain_points` are absent from `ProfileRow` — the type whose docstring is *"only the columns the
role-map reads"* (`profile-role-map.ts:41-54`). Only the video pipeline sees them.

**4. The single strongest field cannot be set from this form at all.**
`writing_voice_sample` drives the role-map's `voice` role, but it is **not** in the settings tab's
`GET_COLUMNS` (`api/profile/creator-profile/route.ts:16-32`). The only writer is
`profile-interview-store.ts:185` — which belongs to the dead modal. So the voice role is
unfillable by hand today.

**5. The rework already replaces that field — from real data, not a form.**
`applyCreatorPersona` backfills `writing_voice_sample` from the audience's auto-derived
`creator_persona`, "frozen at calibration from the real scrape + transcripts + omni-flash
watchNotes". It is deliberately **fallback-shaped** (manual voice wins), and gated to *calibrated,
non-General* audiences.

**So the real finding is a direction, not a delete.** The calibrated audience is already the
replacement mechanism, and it has replaced exactly one field so far. The questionnaire is the
cold-start fallback for everyone who has no calibrated audience yet.

**If we delete the tab today**, `niche_primary` (23 consumer files), `language` (17),
`target_audience`, `past_wins`/`past_flops`, `primary_goal`, `creator_stage` and `target_platforms`
lose their only manual source — and they lose it precisely for cold-start users, who are the ones
with no calibrated audience to derive from. That starves the engine at the worst moment.

**Recommended sequencing (owner's call):**
1. Delete Team now. Isolated, unambiguous, removes a live 500.
2. Delete the dead interview chain now — `ProfileInterviewModal`, `content-form.tsx`,
   `CommandBar.tsx`, `profile-interview-store.ts`, the `profile_interview_seen_at` gate. Unreachable
   code, zero risk. **This is the "old system" instinct, and it's correct.**
3. **Keep the profile data.** Cut the *form* down to the 10 fields the role-map actually reads, and
   drop the 6 that only the video pipeline consumes (or move them where they're earned).
4. Then decide the endgame: extend the `applyCreatorPersona` pattern so calibration derives niche /
   audience / goals the way it already derives voice — and retire the questionnaire when the
   derivation covers it. **Until the new onboarding writes these columns, something must.**

**Open question for the new onboarding session:** does it populate `creator_profiles`? If it does
not, this is not dead code to remove — it is a live dependency about to be starved, which is a
worse failure and an invisible one.

### What the engine actually demands (measured, not assumed)

`MODE_ROLES` (`assembler.ts:121-127`) declares exactly what each skill pulls, **in priority order —
later roles are dropped first when the bundle exceeds `BUNDLE_CHAR_CAP`**:

| mode | roles pulled (highest priority → dropped first) |
|---|---|
| idea | niche · audience · goals · voice · wins · flops · **platform** |
| hooks | niche · audience · voice · wins · flops · **platform** |
| script | niche · audience · voice · wins · flops · **platform** |
| remix | niche · audience · voice · wins · flops · **platform** |
| chat | niche · audience · **platform** |

Two things fall straight out:
- **`goals` is used by `idea` alone.** Hooks, script, remix and chat exclude it by design.
- **`platform`, `flops`, `wins` are the tail** — the first grounding silently discarded under cap
  pressure. The engine's own comment calls them "lower-signal grounding". `platform` is weaker
  still: `PROFILE_ROLE_MAP`'s docstring says the per-request platform param **wins over**
  `target_platforms[0]` (D-07), so the stored column is a fallback for a value the composer already
  sets on every run.

### The overlap: 6 of the 7 roles already have an audience-side source

| role | profile column(s) | what a calibrated audience already carries | verdict |
|---|---|---|---|
| niche | `niche_primary`, `niche_sub` | `AudienceProfile.interest_tags`, `topic_vocab`, `CreatorPersona.content_description` | derivable |
| **audience** | `target_audience{age_range, gender_skew, geo, language}` | **nothing** — personas carry archetype / disposition / temperature / share, never demographics | **genuine gap** |
| goals | `primary_goal`, `creator_stage` | `Audience.goal_label` + `goal_intent` (already read live: `goalIntentToLens` in the hooks route) and `AudienceProfile.maturity` (`new`/`growing`/`established`) + `follower_tier` | already duplicated |
| voice | `writing_voice_sample` | `CreatorPersona.writing_style_sample` | **already wired** via `applyCreatorPersona` |
| wins | `past_wins` (self-reported URLs) | `AudienceProfile.what_resonates` (derived from the real scrape) | derived version is better evidence |
| flops | `past_flops` (self-reported URLs) | `AudienceProfile.what_falls_flat` (same) | same |
| platform | `target_platforms` | `Audience.platform`, **and** the per-request composer chip | duplicated twice |

So the questionnaire's most-consumed field (`niche_primary`, 23 files) is also its most replaceable,
and the two fields the user is asked to research by hand (`past_wins`/`past_flops` — go find URLs of
your old videos) are the ones the engine drops first *and* already measures better from the scrape.

**Exactly one role has no derivation path: the demographics.** Nothing in calibration produces
age / gender / geo / language, because the persona model is behavioural, not demographic.

### Recommendation — the profile is a cold-start shim, not a standing input

The derivation only fires for **calibrated, non-General** audiences. Cold-start users have no
calibrated audience, so something must supply these values for their first runs. That is the
profile's remaining job — and its only one.

1. **Delete the interview chain** (`ProfileInterviewModal`, `content-form.tsx`, `CommandBar.tsx`,
   `profile-interview-store.ts`, the `profile_interview_seen_at` gate). Unreachable today.
2. **Keep the columns.** The engine reads them; nothing else fills them yet.
3. **Cut the form from ~15 sections to 2**: *niche* and *who you're making it for* (the
   demographics). Everything else is already on the audience, set per-run by the composer, or
   measured better from the scrape.
4. **Make the audience win where it can.** Extend the `applyCreatorPersona` fallback pattern from
   voice to niche / goals / wins / flops — typed value wins if present, derived fills the gap.
   That is the same shape already shipped for voice, so it needs no new concept.
5. **Keep it out of the first-run path.** The 6 fields only the video pipeline reads
   (`content_style`, `cuts_per_second`, `reference_creators`, `posting_frequency`,
   `time_of_day_aware`, `pain_points`) should be asked where they're earned — next to a video run —
   not on a settings wall before the user has seen a single output.

**The end state to aim at:** a creator types their niche once, runs something, and calibration
takes over. The questionnaire shrinks as derivation grows, and the demographics question is the
last thing left standing — unless we decide to derive that too, which would need a new signal
(the persona model has no demographic axis today).

### ✅ Blocking question answered: the new onboarding writes NOTHING

Checked `~/virtuna-onboarding` on `milestone/onboarding`. Every `creator_profiles` touch is either
a `select` or the legacy bootstrap:
- `auth/callback/route.ts:96` — **reads** `onboarding_completed_at` only.
- `(onboarding)/welcome/page.tsx:67` — inserts `{ user_id, onboarding_step: "connect" }`.
- `(onboarding)/login/actions.ts:42` — read.

**Not one content column is populated.** So today every new user runs with all seven roles null
until they either stumble onto the settings form or calibrate an audience. The assembler's
cold-start flag means this degrades *honestly* rather than fabricating (`assembler.ts:19`) — but
"honest" here means "the thinnest possible grounding, for everyone, by default".

This does not change the recommendation; it raises its priority. The shim isn't just small — it is
**empty for every new user**, which is why cutting it to two well-placed questions is worth more
than defending fifteen nobody answers.

### DECISION (owner, 2026-07-26) — approved, queued for a fix session

Audit-only branch: **nothing below is implemented here.** Ordered so each step is independently
shippable, cheapest and safest first.

| # | Change | Risk | Notes |
|---|---|---|---|
| 1 | Delete the Team feature — tab, `team-section.tsx`, `use-team`, `/api/team*`, `teams` + `team_members` tables | none | verified isolated; also closes F-002 |
| 2 | Delete the dead interview chain — `ProfileInterviewModal`, `content-form.tsx`, `CommandBar.tsx`, `profile-interview-store.ts`, `use-pending-profile-gate`, the `profile_interview_seen_at` gate | none | unreachable today |
| 3 | Cut the Creator Profile form to **niche** + **demographics** | low | columns stay; only the form shrinks |
| 4 | Move the 6 video-pipeline-only fields next to a video run | medium | `content_style`, `cuts_per_second`, `reference_creators`, `posting_frequency`, `time_of_day_aware`, `pain_points` |
| 5 | Extend `applyCreatorPersona` from voice → niche · goals · wins · flops | medium | same fallback shape already shipped; typed wins, derived fills |
| 6 | Seed niche from the new onboarding | low | needs the onboarding session; removes the last cold-start blank |

**Guard for step 5:** `applyCreatorPersona` is currently a pure, deterministic no-op for General /
uncalibrated audiences, which is what preserves the generation regression gate. Any extension must
keep that property — otherwise every existing golden output shifts.

**Do NOT delete the `creator_profiles` content columns in any step.** Steps 3–5 change who fills
them, never whether the engine can read them.

### F-012 · `View analytics →` and `Open →` are different verbs for the same destination shape
- **Station:** A-audience · **Kind:** COPY + UX · **Severity:** minor
- **Saw:** both CTAs navigate to an audience detail page — `Open →` → `/audience/6b1114e6…`,
  `View analytics →` → `/audience/4c9923d8…`. The link is **not dead** (my earlier suspicion was
  wrong): `/audience/[id]` canonicalizes an account id to its audience URL, exactly as the page
  header documents.
- **Expected:** one verb, or two verbs that lead somewhere genuinely different. As shipped, an
  analytics-only account promises "analytics" and lands on the same surface the neighbouring card
  calls "Open".
- **Not verified:** whether the destination's SOURCE zone actually renders the account analytics the
  label promises. Worth one check before wording the fix.
- **Evidence:** `scratchpad/finish-audience.mjs`; `src/app/(app)/audience/page.tsx` header comment.

### ✅ Verified working (no finding)
- **`Open →`** — navigates to the audience detail page.
- **`Set default`** — `POST /api/settings/last-audience` → 200, and it **persists**: after a reload
  only 1 of 2 cards still offers "Set default".
- **Notification toggles** — `POST /api/settings/notifications` → 200, `/api/profile` reflects the
  new value, and it **survives a reload**. No save button needed; auto-save is real.

> ⚠️ **Test-user state was mutated by these checks**: the default audience was switched to the 2nd
> card, and `emailUpdates` was toggled `true → false`. Harmless, but don't read either as a bug
> later.

### F-013 · ~~Nine app routes have no entry point~~ → RESOLVED: they are deliberate redirect stubs. The dead weight is behind them.
- **Station:** reachability · **Kind:** UX + scope · **Severity:** downgraded major → **minor** for
  the routes; the code mass behind them is a separate owner call (F-014).
- **Status:** ✅ **closed 2026-07-26 (session 2), and it overturns the original claim.**

**What the original finding got wrong.** It inferred "no nav entry ⇒ orphaned page". Followed to
ground, 14 of the 15 candidate routes are **12–36-line `redirect()` tombstones** — deliberate
launch-cut stubs that keep old URLs and bookmarks landing somewhere sane. Not one is an orphaned
page. Measured live, every one of them lands:

| Route | → lands on | Route | → lands on |
|---|---|---|---|
| `/discover` | `/feed` → `/home` | `/competitors/[handle]` | `/home` |
| `/feed` | `/home` | `/competitors/compare` | `/home` |
| `/feed/hooks` | `/home` | `/analytics` | `/audience?tab=account` |
| `/feed/channels` | `/home` | `/grow` | `/audience?tab=account` |
| `/library` | `/home` | `/grow?tab=referrals` | `/settings?tab=referrals` |
| `/saved` | `/library` → `/home` | `/referrals` | `/settings?tab=referrals` |
| `/calendar` | `/home` | `/analyze` | `/home` |
| `/competitors` | `/home` | `/start` | `/home` |

All 200, no 404s, no error shells. `?tab=account` is genuinely honoured
(`audience/page.tsx:59` forwards it to the account's audience detail page), so the two
`/analytics`-class redirects do not dump the user on a dead param.

**The shipped nav, confirmed from rendered buttons** (not hrefs — the doc's warning was right, an
`a[href]` crawl returns only the `/` logo): **New Thread · Audience · the threads list ·
account → {Settings, Log out}**. Exactly four things, as suspected. `/dev/cards` and `/ambient-v2`
are the only real pages with no nav entry, and both are dev surfaces — correctly unnavigable.

**Two measurement traps worth recording:**
- A 1,200 ms settle window reported `/discover` as *not* redirecting. It does — at 6 s it lands on
  `/home`. The redirect chain simply outlived the probe. **A short wait manufactures false orphans.**
- Body-text length varies 555–18,087 chars for the *same* destination purely by hydration timing.
  Char count measures when you looked, not what shipped. Neither is a product signal.

**So there is nothing to "reconnect" at the route layer, and nothing to fix.** The three-way split
the session opened with resolves almost entirely to *ships*; the real decision moved one layer down,
into the code these stubs no longer render — see **F-014**.

### F-014 · ~8,300 lines of feature code sit behind the tombstones, rendered by nothing
- **Station:** reachability · **Kind:** scope · **Severity:** major (scope decision, owner's call)
- **Saw:** the redirect stubs work, but the components they used to mount are all still in the tree.
  Verified orphaned — **zero live importers**, checked against `@/`-alias *and* relative import
  styles, excluding tests and the stub routes themselves:

| Orphaned | Lines |
|---|---|
| `src/components/competitors/` (31 files) | 3,115 |
| `src/components/feed/` | 1,097 |
| `src/components/calendar/` | 927 |
| `src/components/saved/` | 855 |
| `src/components/discover/` — **3 of 5 files only** (`discover-entry`, `discover-hub`, `discover-tab-bar`) | 274 |
| client files under the stub route dirs (`discover`, `feed`, `competitors`, `library`, `analyze`) | 2,011 |
| **Total** | **~8,279** |

  Plus three API routes with no caller: `/api/feed`, `/api/discover`, `/api/saved`.

- ⚠️ **Do NOT blanket-delete `src/components/discover/`.** Two of its five files are **live**:
  `discover-grid.tsx` and `outlier-tile.tsx` are imported by `thread/outlier-grid-block.tsx` and
  `thread/explore-thread-view.tsx` — i.e. they render the **explore** skill (station C6).
  `components/saved/saved-item-card.tsx` also imports `outlier-tile`, but that edge points
  dead → live, so removing the dead side is safe. A dir-level `rm -rf` here breaks a shipped skill.
- **Not a cost finding — checked:** `refresh-competitors` and the other 9 crons are already gone
  from `vercel.json` (`crons` key absent as of `716b5312`). Nothing scheduled feeds these surfaces.
- **The owner call is per feature, not per file** — and it is the same shape as Team: is
  *competitors* (3,115 lines, the largest block) a feature we are coming back to, or one we cut?
  Delete and it goes with its tombstone; keep and it needs a door. **Recorded, not decided.**

### F-015 · The SIM-1 Flash/Max selector changes nothing. It is a live control wired to no one.
- **Station:** B3 · **Kind:** DATA-HONESTY + UX · **Severity:** **major**
- **Saw:** the composer's model selector offers `SIM-1 Flash — "Text-only runs — hooks, ideas, chat"`
  and `SIM-1 Max — "With-video runs — real video Test"`. Both options are **fully enabled**
  (`disabled:false`, `aria-disabled:null`, `pointer-events:auto`, `opacity:1`), and selecting Max
  visibly updates the pill: `"SIM-1 Flash" → "SIM-1 Max"`. The run then ignores it completely.
- **Six independent confirmations** — this is not inferred from one probe:
  1. **On the wire.** With the pill reading **SIM-1 Max**, the request body was
     `{"ask":"…","platform":"tiktok","intent":"grow"}` — **no model/fidelity key at all.**
  2. **In the persisted record.** The five `hook-card` blocks that run stored at `15:52:21` all
     carry `model=sim1-flash` — the run recorded the *opposite* of what the UI showed armed.
  3. **The control plane.** `selectedModel` is `useState<SkillModel>("Flash")`
     (`composer.tsx:350`), passed to the selector at `:2859`, and read by **no** fetch body.
  4. **No consumer.** No route under `src/app/api/tools/` parses `body.model`/`fidelity`/`tier`.
  5. **The route hardcodes it.** `hooks/route.ts` imports `QWEN_REASONING_MODEL` (Qwen-only, T-04-08);
     the tier is a property of the endpoint, not the request.
  6. **The code says so.** `composer-controls.tsx:19-21`: *"the creator can override until they
     switch skills — **UI-only for now; routing still skill-driven**."*
- **The tier itself is real — it just isn't the user's to pick.** `video-test-card` rows persist
  `model=sim1-max` while every `hook-card` persists `sim1-flash`. The skill picks the tier; the
  selector is a second, fake steering wheel next to the real one.
- **Why major, not polish:** it is the only control in the composer that names a **cost/quality
  trade** ("text-only" vs "with-video"). A user who picks Max to get a better result gets an
  identical Flash run and no indication otherwise. It reads as a lie precisely because it works —
  the pill updates, so the feedback confirms a choice that was discarded.
- **Evidence:** `shots/actb-run2.json` (payload + option state), DB `messages.body.blocks[].model`,
  `composer.tsx:350,2859`, `composer-controls.tsx:19-21`, `api/tools/hooks/route.ts:54,125-131`.
- **Repro:** `/home` → New Thread → skill pill → Hooks → model pill → SIM-1 Max → send. Inspect the
  `POST /api/tools/hooks` body.

### F-016 · There is no scene control — platform is a property of the audience, not of the run
- **Station:** B2 · **Kind:** ICP-FIT (scope correction) · **Severity:** minor
- **Correction to this document:** station B2 assumed a "Scene (TikTok / IG / No feed)" picker.
  **No such control is rendered.** The composer's control row is exactly
  `[+ attach] · [skill pill] · [model pill] · [send]` (plus a dev-only ⚙ mock panel).
- **What actually happens:** `composer.tsx:466` —
  `const platform = audienceToPlatform(selectedAudience?.platform)`. Platform reaches the payload
  (`platform:"tiktok"` observed), but it is **derived**, never chosen per-run.
- **The ICP consequence:** to ask the same question for Instagram, a creator must switch *audience* —
  a heavier, more permanent object than a per-run toggle. Every seeded audience on the test user is
  `platform=tiktok`, so today there is no path at all to a per-run IG framing.
- **Not a defect on its own** — audience-bound platform is defensible. Logged because the audit
  scope named a control that does not exist, and because the workaround is non-obvious.

### ✅ B1/B4 verified working — the audience chip genuinely binds (no finding)
Established with a **discriminating** test, after a first attempt proved nothing:

- **The trap:** the first run's payload was `platform:"tiktok", intent:"grow"` — which looks like
  audience binding but is not. `goalIntentToLens(null)` returns **`"grow"`**
  (`intent-lens.ts:33-35`) and the route defaults `platform` to `"tiktok"`
  (`hooks/route.ts:126`). The thread was pinned to **General** (`active_audience_id` NULL). So both
  values were plain defaults, and would have appeared identically had the chip been inert.
- **The real test:** switch the chip to an audience with `goal_intent = "sell"`, re-run.
  Payload flipped to **`intent:"sell"`**. The chip binds.
- **Server side agrees:** `hooks/route.ts:173` resolves the audience independently via
  `resolveThreadAudience(supabase, openThread, user.id)` off `thread.active_audience_id` — the
  binding does not rely on the client body at all.
- **Method note:** `/api/threads/open` does **not** return `active_audience_id`; reading it from
  that response yields `undefined` and reads as "General". The thread pin has to come from the DB.

> So of the three Act-B knobs: **audience binds · platform derives · fidelity is inert.**

### F-017 · 🔴 The **Ideas** tile on the Start view arms a skill that cannot run — and the only way to enable its button is to paste a video URL
- **Station:** A2 / C1 · **Kind:** BACKEND (contract) + UX · **Severity:** **blocker**
- **Saw:** clicking **Ideas — "Concepts worth making"**, the first tile of the Start view's Content
  row, sets the skill pill to `Skill: Ideas` — and leaves the send button reading **`Simulate`,
  disabled**. Typing an idea prompt does **not** enable it. Pasting a **TikTok URL does**.
- **Root cause — a one-character id mismatch across two SSOTs:**
  - the artifact grid emits `{ id: "ideas", label: "Ideas" }` (`ambient-v2-adapters.ts:272`)
  - the composer's `ToolId` union is **`"idea"`**, singular (`composer-controls.tsx:87`)

  `"ideas"` therefore matches no branch of either the `aria-label` chain (`composer.tsx:2874`) or
  `canSubmit` (`:757-791`), so both fall through to the **final else** — which is the *video Test*
  branch: `(isValidTikTok || file !== null) && !submitting`. That is exactly the observed behaviour,
  and it is why a TikTok URL is what unlocks the button.
- **Why blocker, not major:** the tile grid is the Start view's primary call to action, and Ideas is
  its first tile. The failure is silent and self-consistent — the pill *agrees* it is Ideas — so a
  user has no signal that the composer is actually armed for something else. Worse than a dead end:
  a user who pastes a URL to "make the button work" gets a **SIM-1 Max video Test** (the paid ~83 s
  path) from a control labelled Ideas.
- **Isolated to this one tile — every sibling is fine** (measured, same method):

  | Tile | pill | send | |
  |---|---|---|---|
  | **Ideas** | Skill: Ideas | **`Simulate` [disabled]** | ❌ |
  | Hooks | Skill: Hooks | `Generate hooks` | ✅ |
  | Script | Skill: Script | `Generate script` | ✅ |
  | Remix | Skill: Remix | `Remix video` [disabled] | ✅ correct — remix requires a URL (`:768`) |
  | Explore | Skill: Explore | `Run Explore` | ✅ |
  | Account teardown | Skill: Your account | `Read my account` | ✅ |

  Every other tile id (`hooks`, `script`, `remix`, `test`, `explore`, `account`, `ad`) matches the
  composer SSOT exactly. **`ideas` vs `idea` is the only divergence.**
- **The same skill reached the other way works perfectly.** Via the skill-pill menu row
  (`IdeasFunnel-top idea cards`), the send button reads **`Generate ideas`** and is enabled. So the
  skill is healthy; only the tile's door is wired to the wrong id.
- **Evidence:** `ambient-v2-adapters.ts:272` vs `composer-controls.tsx:87`; `composer.tsx:757-791`
  (canSubmit fallback), `:2874` (aria-label fallback); measured arming table above.
- **Repro:** `/home` → New Thread → click the **Ideas** tile → type anything → send stays disabled →
  paste a TikTok URL → the button labelled `Simulate` becomes enabled.
- ⚠️ **Instrument note (cost me four probes):** the Start view's artifact tile and the skill-menu row
  render *nearly* the same string. The tile is `IdeasConcepts worth making`; the menu row is
  `IdeasFunnel-top idea cards/ideas`. Matching on `startsWith("Ideas")` hits whichever is higher in
  the DOM — and a sidebar *thread title* beginning "ideas…" outranks both. Any future driver must
  match the **description**, not the label. The two doors behave differently, so conflating them
  hides exactly this class of bug.

### F-018 · A failed run's honest error does not survive a reload — the thread becomes a silent dead end
- **Station:** C4 / D4 / D7 · **Kind:** UX + DATA-HONESTY · **Severity:** minor
- **Saw:** a remix run that failed at decode renders a genuinely good in-session error —
  *"Couldn't finish that run. The generation or SIM-1 pass dropped out. Tap to retry — nothing was
  charged. Retry →"*. **After a reload it is gone.** The thread then shows the user's pasted URL
  followed by generic starters (*Give me ideas · Write hooks · Draft a script*) — indistinguishable
  from a message the product simply never answered.
- **Why:** the failure path returns `{blocks: [], error: "decode_failed"}`
  (`remix-runner.ts:212-213`), and only *blocks* persist. The error is an SSE event rendered into
  transient client state, so the user turn is stored with no assistant turn beside it — verified in
  the DB: the run persisted **only** the `markdown` user block, while the neighbouring script and
  explore runs persisted `script-card` and `outlier-grid`.
- **Expected:** a failed run should leave a durable trace. The retry affordance is the most valuable
  thing on screen and it is the first thing lost.
- **Evidence:** measured before/after reload; `messages` rows for `16:24:38` (user block only);
  `remix-runner.ts:212`; dev log `WARN remix run returned error … decode_failed`.

### F-019 · 🔴 The video Test's paid Max card lands in a thread the UI can never open — a ~4.5-min run completes into a blank screen
- **Station:** C5 · **Kind:** BACKEND + UX · **Severity:** blocker
- **Saw:** the upload path ran end-to-end clean — 23 MB staged to storage (169 s), `/api/analyze`
  200, ~112 s pipeline with an honest run spine (*"Uploading your video…"* → *"Watching it frame by
  frame"*), seal `POST /api/tools/test/card` → 200, URL stayed `/home` throughout (the D-05
  no-navigate-out contract held). **Then: nothing.** The thread area rendered empty (657 chars on
  the whole page), no card, no CTAs. After a full reload, still nothing — the card's thread is
  untitled and the sidebar never lists it. The card is unreachable from the UI, full stop; the Read
  renders only by hand-typing `/analyze/RpTSjjmS40eJ`.
- **Why (three cooperating parts):**
  1. `composer.tsx:1567-1576` — Test is **excluded from `ensureThreadForSend`**, justified by the
     stale comment *"(Test/video navigates to /analyze and owns no chat thread — excluded.)"* —
     written before the D-05 rework made Test seal in-thread. The active-thread cookie stays
     `NEW_THREAD_SENTINEL` for the whole run.
  2. `threads.ts:98` — `getOpenThread` returns null on the sentinel **by design** ("row is created
     on first send"). With the sentinel never flipped, every server-side `createOpenThreadLazy`
     call inserts a FRESH thread. This one run created **three**: two empty at 20:11:52.83/.11 (an
     analyze-time race) and `e1b47ade` at the 20:13:44 seal, where the card landed.
  3. `reloadChatThread()` then reloads the client's open thread — the sentinel — and renders nothing.
- **Both Test input modes are affected** — the `tiktok_url` branch skips `ensureThreadForSend` too
  (`composer.tsx:1850+`). Precondition: Test as the FIRST send of a New Thread. From an existing
  open thread the pointer resolves and the seal targets the right thread — presumably how D-05's
  own verification missed this.
- **Expected:** the composer materialises the thread on send like every other in-thread skill, and
  the sealed card surfaces in it.
- **Evidence:** `.scratch/audit/shots/c5-video-test.json` (timeline: storage POST +2 s → 200 at
  +169 s, `/api/analyze` 200 +174 s, `test/card` 200 +285 s) · `c5-video-test.png` (composer alone
  over a dead void) · DB: message 20:13:44 thread `e1b47ade` `[video-test-card]` `model=sim1-max`;
  threads `0e09b057`/`1b018309` empty, title null, absent from the sidebar after reload ·
  `c5-analyze-fallback.png` (the Read exists and renders at `/analyze/RpTSjjmS40eJ`).
- **Adjacent facts:** (a) the card CONTENT contract held — tier, craftScore, drivers,
  working/notWorking, filmstrip, and a real director's fix (`why`/`move`/`atMs`) — this is purely a
  surfacing break; (b) `test` is also excluded from `USER_TURN_TOOLS`, so no user turn persists —
  even with routing fixed, a reload shows a card with no question above it (F-018's cousin);
  (c) two empty DB threads are minted per run — litter; (d) the `/go` funnel runs Test as the first
  send of a fresh anon session — out of this lane's scope, but if it leans on the same lazy path
  the same strand may apply there. Hand off, don't fix here.
- **Repro:** New Thread → arm *A real video* → upload any MP4 → send → wait out the run.
- **⚠️ Layer 2 (verified in s3): fixing the thread pointer is NOT enough.** I set
  `maven_active_thread=e1b47ade` by hand — the exact state a future sidebar click would produce —
  and `/api/threads/open` returned the card, yet `/home` still rendered the **Start grid**. Why:
  `hasConversationContent` (`composer.tsx:708-730`) counts every per-skill bucket but **never
  `persistedChatTurns`**, and a Test thread persists **no user turn** (`test` is excluded from
  `USER_TURN_TOOLS`, `composer.tsx:1518-1520`) so `lastUserTurn` is null too — and
  `composer.tsx:3090` renders `AmbientStartHome` whenever `!hasConversationContent && !startEngaged`.
  `video-test-card` is the ONE block type outside every bucket (profile family has its bucket,
  account its own term), so a Test-only thread is invisible even when correctly pointed at.
  The restore map `TYPE_TO_TOOL` (`composer.tsx:933-939`) omits it too. Two independent fixes
  needed: materialise the thread on Test send AND count the unified stream (or the card type) in
  `hasConversationContent`. Shot: `d2-video-test-card.png` (Start grid rendered while the API
  returned the card).
- **Layer 2 re-proved on the REAL path (no cookie hack):** the card's thread later surfaced in the
  sidebar as *"New chat · 12m ago"* (only after a load had touched it — it was absent in both
  post-run sweeps). Clicking that row sets the pointer to `e1b47ade` and `/home` still renders the
  **Start grid** — `hasCard: false, startGrid: true`, shot `d2-newchat-click.png`. A user who
  finds the thread and opens it STILL cannot see what they paid for.

### F-020 · The Test skill has two names — the Start tile says "Video test", the composer menu says "A real video"
- **Station:** C5 / A2 · **Kind:** COPY · **Severity:** minor
- **Saw:** the Start-view tile renders *"Video test — Frame by frame, one fix"* while the skill menu
  row for the same `id: "test"` renders *"A real video — Watch-through + full Read"*. Same skill,
  two doors, two names (and two descriptions).
- **Why:** the menu label was renamed on main (`composer-controls.tsx:92`) but the Start-view tile
  fixture kept the old name (`start-fixture.ts:33`).
- **Expected:** one name per skill across doors — the F-017 lesson is that the two doors already
  diverge in BEHAVIOR; diverging in NAME too makes that harder to see, not easier.
- **Evidence:** rendered dump 2026-07-26 (sidebar/tile sweep) · `composer-controls.tsx:92` ·
  `start-fixture.ts:33`.

### F-021 · "Delete thread" can never succeed — the live DB rejects `type: 'archived'`, and the UI hides the failure
- **Station:** D6 · **Kind:** BACKEND + DATA-HONESTY · **Severity:** major
- **Saw:** the sidebar delete (a clean two-step trash→✓ confirm) always ends with the row back in
  the list. `DELETE /api/threads/<id>` → **500, every time**. The optimistic mutation removes the
  row instantly, the 500 rolls it back ~2 s later, and **no error is ever shown** — the thread
  "flickers deleted" and returns, which reads as a UI glitch rather than a server failure.
- **Why:** `archiveThread` (`threads.ts:248`) archives via `update({ type: "archived" })`, but the
  live schema's **`threads_type_check` CHECK constraint does not allow `'archived'`** — Postgres
  `23514`, reproduced directly with the service client. Migration drift: the code's archive concept
  never landed in the DB. `archiveThread` then throws, and the route has no try/catch → 500. On the
  client, `useArchiveThread.onError` (`use-threads.ts:93-97`) only rolls back the cache — no toast,
  no message.
- **Expected:** delete archives the thread; on failure the user is told. On whose authority: the
  route's own contract (it returns 404 "Thread not found" for the only failure it anticipates).
- **Evidence:** `DELETE /api/threads/23e51a4b…` → 500 (dev log line 274) · direct service-client
  repro: `23514 new row for relation "threads" violates check constraint "threads_type_check"` ·
  `threads.ts:243-257` · `use-threads.ts:73-101`.
- **Repro:** hover any sidebar thread → trash → ✓. Watch the row return.

### F-022 · The paid upload path shows a static "Uploading your video…" for minutes — no progress, no size feedback
- **Station:** D1 / C5 · **Kind:** UX + PERF · **Severity:** minor
- **Saw:** the C5 run spent **169 s of its 288 s** in a single unchanging spinner line
  (*"Uploading your video…"*) for a 23 MB file. No percent, no MB counter, no "large files take a
  couple of minutes" expectation-setting. The 3-step run spine (`ProgressChecklist`) only takes
  over after the upload completes (`composer.tsx:2431-2443` — the checklist renders only once
  `testAnalyzing`/`carding`).
- **Expected:** the longest single stage of the most expensive skill should show progress
  (Supabase storage upload exposes progress events) or at least set expectations.
- **Evidence:** `c5-video-test.json` timeline (ticks 16 s→167 s all identical) · `composer.tsx:2431`.

### Act D — verified working (checked before logging — NOT findings)
- **D2 legibility** — the script card answers "what do I do now" above the fold: beats with
  timecodes + camera directions + a HOW-TO-FILM block + a scored opener (*Strong 8/10 would stop*)
  with a scroll-quote; explore renders 26 outliers scored vs niche with per-row `Remix → / Save`;
  hooks cards carry bands + scroll-quotes (s2). The one illegible card is the video-test-card —
  it cannot render at all (F-019).
- **D3 CTA destinations** — `Test this script →` is a genuinely good handoff: swaps the skill to
  *A real video*, mounts the upload affordance, and carries a visible brief — *"Shoot this hook →
  upload → SIM-1 Max scores the real thing"* + the quoted opener labeled *script opener* — while
  the ambient rail queues the script (*NOT SIMULATED YET · 1 QUEUED · SIMULATE →*). Shot
  `d3-script-test-cta.png`. `Copy script` / `Save` present; explore `Remix →` not clicked (paid).
- **D4 reload fidelity** — explore thread re-renders byte-identical (13,332 chars both sides);
  script thread re-renders the full card + CTAs. The known exception is a FAILED run (F-018).
- **D5 skill switch** — a typed draft survives arming a different skill (verbatim), and the
  swap-on-switch view change loses no thread content.
- **D6 list/open** — sidebar list, open, active-row highlight all correct (delete is F-021).
- **D7 degrade honesty (client-side)** — a non-TikTok URL under Test gets an honest inline
  *"Maven reads TikTok videos for now. Paste a TikTok link or upload the file."* with send
  disabled; the in-session failed-run copy was already verified honest in s2 (F-018 covers only
  its reload amnesia).

### ✅ Verified working in Act C (checked before logging — these are NOT findings)
- **The failure copy is TRUE.** "nothing was charged" survives scrutiny: `billUsage` is gated on
  `result.blocks.length > 0` (`remix/run/route.ts:277-278`), so a `decode_failed` run bills the user
  nothing. I twice came close to logging this as dishonest — first as a silent failure, then as a
  false billing claim. **Both were wrong.** The business absorbs a real Apify scrape plus an omni
  pass (`cost_cents: 0.229` logged) — a cost worth knowing, but not a lie to the user.
- **C1 ideas** — `POST /api/tools/ideas` 200; scored idea cards (9/10, 7/10) + `Write hooks for
  this →` / `Simulate →` / `Save`. Healthy **via the skill menu** (the tile door is F-017).
- **C2 hooks** — 200 in ~25 s, ×2 runs, five ranked cards persisted with bands and scroll-quotes.
- **C3 script** — `POST /api/tools/script` 200; `script-card` with beats/timings + a follow-up
  markdown turn; CTAs `Copy script` · `Test this script →` · `Save`.
- **C5 video Test, the mechanics** — storage upload 200 · `/api/analyze` 200 · ~112 s pipeline ·
  seal `POST /api/tools/test/card` 200 · **no navigate-out** · card block persisted with a real
  derived director's fix (`model=sim1-max`) · degrade path untriggered. Every stage of D-05 held
  EXCEPT the last inch — the card surfaces nowhere (F-019). Run: `analysisId RpTSjjmS40eJ`,
  ~288 s wall, of which 169 s was the 23 MB upload with only a static *"Uploading your video…"*
  line (no progress %, an Act-D note).
- **C6 explore** — `POST /api/tools/explore` 200 (12.9 s); `outlier-grid` persisted; `Remix →` CTAs.
- **C7 account teardown** — runs on **`POST /api/account-read`** (200 in 25.4 s), *not* under
  `/api/tools/*`; CTA `Write to my strengths →`. My tool-route filter missed it — scope of the
  instrument, not a defect.
- **C8 `ad` · `compare`** — both genuinely inert: `disabled`, `opacity 0.45`, `cursor:default`, and
  clicking changes nothing (skill pill stays put). "Soon" is honest.
- **The remix failure was not caused by a bad URL or exhausted credit** — the Apify scrape succeeded
  (`1/1 detail pages`) and the omni analysis completed (`emotion_arc_points: 7`,
  `verbatim_present: true`). Only the decode step returned null.

> ⚠️ **Rig note:** the dev server **died mid-session** right after the first remix run (log ends
> clean, no crash trace — most likely the 2 GB `--max-old-space-size` cap on a video-bearing run).
> A page read taken seconds later returned Chrome's `ERR_CONNECTION_REFUSED` shell, which I briefly
> mistook for "the UI rendered nothing". **Re-check the server is alive before interpreting an empty
> surface.** Restarted; cwd re-verified.

---

## Station log

| Station | Date | Verdict | Findings |
|---|---|---|---|
| Rig · login | 2026-07-26 | done | F-001 |
| A-audience (`/audience`) | 2026-07-26 | **done** — CTAs exercised | F-010, F-012 |
| S-profile | 2026-07-26 | done | F-008 |
| S-account | 2026-07-26 | done | F-006, F-007 |
| S-connected | 2026-07-26 | rendered; actions not exercised | — |
| S-notifications | 2026-07-26 | **done** — persistence verified | — |
| S-billing | 2026-07-26 | done | F-003, F-004, F-005 |
| S-referrals | 2026-07-26 | rendered (Pro-gated, sparse) | — |
| S-team | 2026-07-26 | done | F-002, F-009 |
| Reachability map | 2026-07-26 | **done** — 15 routes driven live; F-013 overturned | F-013 (closed), F-014 |
| B1 · Audience chip | 2026-07-26 | **done** — binds (discriminating test) | — |
| B2 · Scene | 2026-07-26 | **done** — no such control exists | F-016 |
| B3 · Fidelity | 2026-07-26 | **done** — inert, 6 confirmations | F-015 |
| B4 · Config → run contract | 2026-07-26 | **done** — proven on the wire | (F-015) |
| A2 · Artifact grid (arming) | 2026-07-26 | **done** — all 6 tiles measured | **F-017 (blocker)** |
| C1 · ideas | 2026-07-26 | **done** — healthy via skill menu | (F-017 = tile door) |
| C2 · hooks | 2026-07-26 | **done** — ×2 live, 200 in ~25s, cards persisted | — |
| C3 · script | 2026-07-26 | **done** — script-card + follow-up | — |
| C4 · remix | 2026-07-26 | **done** — decode_failed ×2; error honest, not durable | F-018 |
| C6 · explore | 2026-07-26 | **done** — outlier-grid persisted | — |
| C7 · account | 2026-07-26 | **done** — `/api/account-read` 200 in 25.4s | — |
| C8 · ad / compare | 2026-07-26 | **done** — genuinely inert | — |
| C5 · test (video) | — | **NOT RUN** — next up, paid ~83s | — |
| S-creator-profile | 2026-07-26 | done | F-011 |
