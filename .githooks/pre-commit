#!/bin/sh
# Pre-commit hook -- no-op after v3.0 milestone abandonment.
#
# Previously ran scripts/lint-vocab.mjs (Phase 01 of v3.0 Brand Statement
# Landing milestone), which has been archived to
# .planning/milestones/v3.0-brand-statement-landing/scripts/lint-vocab.mjs
# along with its companion vocab spec at
# .planning/milestones/v3.0-brand-statement-landing/reference/BRAND-SPINE.md.
#
# The Landing Page Redesign milestone can decide whether to re-enable a
# vocab guardrail (with its own rules) or replace this hook with a
# different gate (eg. type-check / lint / test).
exit 0
